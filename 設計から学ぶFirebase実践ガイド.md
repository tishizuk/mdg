### **本書について** 

本書は、 Firebase を用いた設計・開発に関するノウハウを体系的に解説するものです。

Firebase は 2011 年に登場し、その後 2014 年に Google 社によって買収された BaaS ( Backend as a Service )です。 Firebase はさまざまなサービスから構成される開発プラットフォームです。 NoSQL データベースの Cloud Firestore 、認証基盤の Firebase Authentication 、サーバーレスなコンピューティングを提供する Cloud Functions for Firebase 、近年は機械学習を手軽にアプリに組み込める Firebase ML など、非常に多くの製品群から成っています。 Firebase は、 2023 年現在でもいまだに根強い人気を持つプラットフォームです。

Firebase はとても手軽に始めることができ、高い開発生産性を提供してくれる魅力的なサービスです。また、公式ドキュメントや書籍、 Web 上の日本語の記事なども豊富です。ライブラリや API の基本的な使い方を学び、シンプルな Web サービスを作って公開することは、それほど難しくないでしょう。一方、実際の業務などで中規模以上の Web サービスを作ろうとすると、どうすればよいか迷うケースが増えてきます。従来の Web サービス開発とは異なる勘所を要することが多いとも言われています。使い方が簡単であるがゆえにさまざまな実現方法が考えられるため、結果的に非効率なデータモデリングや、 Firebase のメリットを活かせないような実装が生まれてしまいがちです。筆者らも Firebase を基盤として複数の Web サービスをリリースしてきており、その過程でいくつもの失敗をしてきました。そうした中で、ある程度こうやればうまく Firebase を活用できるというプラクティスや、 Firebase では避けたほうがよいアンチパターンといった知見も増えてきました。

Firebase に入門するための情報は豊富ですが、現場でうまく扱うための実践的なノウハウはまだまだ限定的だと感じています。本書は、筆者らの経験を通じて得たノウハウをできる限り詰め込み、 Firebase を利用する開発者に向けて、 Web サービス開発を成功に導くための指針となるような情報を提供することを目的としています。単なる Firebase の API の使い方や、実装手順を示すのみだけではなく、まず設計段階で検討すべきポイントを示すところに重点をおいて説明を割いています。そのあとに、実際にサービスを開発する流れを紹介するという構成としています。そのため、本書内で扱うサンプルとなる Web サービスは、シンプルな登録・参照・更新・削除 (いわゆる CRUD 機能)だけで済むような ToDo リストや、チャットアプリではなく、「安否情報共有サービス」という中規模の Web サービスを題材としています。すでに Firebase を開発に使っている方も、これから使うという方にも、本書を通じて Firebase がより身近なものになると幸いです。

2023 年 3 月押野泰平、⻑谷川健史、岡田菜摘

### **本書の読み方** 

本書は、以下のような方を想定読者としています。

- ・これから Firebase を利用して Web サービスを構築しようとしている。 

- ・実務で Firebase の利用を検討している、あるいは、すでに利用している。 

- ・サーバーレスのアーキテクチャに興味がある。 

- ・ Web サービスの開発経験がある。本書は、「 **anpi!** 」という名前の「安否情報共有サービス」の設計・開発を通じて、 Firebase を用いたプロジェクトの流れを理解して頂くことを目的としています。本書は、手順を 1 ステップずつ細かくなぞれば動くサービスが誰でも作れるようなハンズオンではありません。 Firebase に関する汎用的な理解と、実践的な設計・開発のノウハウを、疑似的な開発プロジェクトに参画しているような気分で習得できることを目指しています。そのため、本書を読むにあたり、 Firebase の前提知識は必要ありませんが、なんらかの Web サービスの開発経験がある方を想定しています。 

anpi! は TypeScript と Vue.js で実装しています。本書内のサンプルコードは、 JavaScript やなんらかの言語の経験があれば、十分読み進められるものとなっています。また、本書内ではリレーショナルデータベース(本書では「 RDB 」と呼びます)との比較という観点で解説している箇所もあるため、 RDB の知見もあればさらに理解が深まります。サンプルコードは GitHub に公開していますので、もしお手元にダウンロードしてローカルで起動したり、細かくコードリーディングしたりされる場合は、 Vue.js の知識があればより理解しやすいです。章構成は次の通りです。いずれの章・節もそれ以前に書かれた内容を前提としていますので、順番に読み進めることを推奨します。

- ・ **1章はじめに** : Firebase のサービス概要や、基本的な情報をまとめた一般的な前提知識を確認します。 

- ・ **2章設計編** : anpi! の設計を通じて Firebase の考え方や、実装に入る前の段階で検討すべきポイントを紹介します。 

- ・ **3章開発編** : 2 章の設計編を受けて、実際に anpi! を開発します。 Firebase ライブラリの使い方など、ソースコードの書き方を紹介します。 

### **サンプルコード** 

anpi! のサンプルコードは GitHub で公開しています <u>1</u> ~~。~~ anpi! で利用する主要なライブラリのバージョンは次の通りです。

Firebase ライブラリ

- ・ Firebase JavaScript SDK : 9.10.0 

- ・ Firebase Admin Node.js SDK : 10.0.17 

- ・ Firebase SDK for Cloud Functions : 4.0.2 

- ・ Firebase CLI : 11.16.0 

#### フロントエンド構成ライブラリ 

- ・ Vue.js : 3.0.0 ・ Vite : 1.0.0 

- ・ Vue Router : 4.0.0 

・ Pinia : 2.0.11

- ・ Quasar : 2.6.0 

### **注意事項** 

本書を読み進めるにあたり、次の点にご留意ください。

- ・ Firebase コンソールを利用した手順などは、参考として画面キャプチャを添付しています。各種 UI は、上記ライブラリのバージョンとは関係がなく、変更される可能性があります。あくまで本書執筆時点の参考情報としてご参照ください。 

- ・各種サンプルコードを提示していますが、説明の分かりやすさを優先し、一部記載を省略したり修正したりしているものがあります。実際に動作確認済みのコードは GitHub で公開していますので、そちらをご参照ください。 

- ・ GitHub で公開しているサンプルコードは、本書執筆時点で筆者らのテストを行い一通りの機能が動作することは確認しています。しかし、その後 Firebase や各種ライブラリのバージョン変更、またはテスト漏れによる不具合などが含まれる可能性があります。 

- ・ GitHub で公開しているサンプルコードは、 Firebase の使い方を分かりやすく紹介することに焦点をあてています。そのため、一部コーディングのスタイルや規約は、 Vue.js やフロントエンドのベストプラクティスに当てはまらない箇所もあります。 

### **免責事項** 

本書に記載された内容は、情報の提供のみを目的としています。これらの情報に基づいて発生するいかなる結果についても、著者や所属する組織は責任を追いかねます。本書を用いた開発や運用などは、必ずご自身の責任と判断でご利用ください。

### **名称等の表記について** 

本書に記載されている会社名、製品名などは、一般に各社の登録商標または商標、商品名です。会社名、製品名については、本文中では © 、 ® 、 TM マークなどは表示していません。

- ※ 本書に収録されている概念図の制作にあたっては、「 diagrams.net 」( https://www.diagrams.net )を使用しました。 

> 1. <u>https://github.com/firebase-anpi/anpi</u> 

### **電子書籍閲覧に関するご注意** 

- 本書では、プログラムリストに専用の等幅フォントを使用してい 

- ます。ビューアによって以下の作業が必要になります。・ Kindle Paperwhite の場合:フォント設定画面で「出版者のフォント」を選択 

- ・ kobo Android アプリの場合:フォント画面で「オリジナル」を選択 

### **目次** 

<u>本書について</u>

<u>本書の読み方サンプルコード注意事項免責事項名称等の表記について電子書籍閲覧に関するご注意</u>

<u>1 章はじめに 1-1 Firebase の製品概要とその強み 1-2 サンプルアプリ:安否情報共有サービス「 anpi! 」</u>

<u>2 章設計編</u>

<u>2-1 なぜ設計から始めるか 2-2 Firebase のサービス構成 2-3 Firebase の概要設計を行う 2-4 Firestore のデータモデルを設計する 2-5 認証を設計する 2-6 認可を設計する 2-7 コレクションのデータモデリングを完成させる</u>

<u>3 章開発編 3-1 開発環境を準備する 3-2 Firebase Authentication で認証機能を実装する 3-3 Firestore でデータを永続化する 3-4 Storage でオブジェクトのアップロード・ダウンロードを実装する</u>

<u>あとがき著者紹介</u>

# **1 章はじめに** 

**まずは Firebase がどのような製品であるかを説明します。そして、 2 章以降の設計編や開発編の題材である Web サービスについて要件や機能概要を示し、本編を読み進めるために必要な前提知識を確認します。**

## **1-1 Firebaseの製品概要とその強み** 

### **1-1-1 Webサービスのアーキテクチャ設計** 

Firebase の製品概要や強みを紹介する前に、まずはその登場の背景を説明します。

Web サービスなどの何らかのシステムを新規で開発する際は、システムの要件がある程度固まってきた段階でアーキテクチャを設計します。アーキテクチャ設計では、適切な技術スタックを選定し、求められる要件を実現できるようにさまざまな判断をしていきます。システム全体の最終的な完成度を左右する、もっとも重要な工程の 1 つです。近年の Web サービスのアーキテクチャは、 **SPA (Single Page Application)** と呼ばれる構成が有力な候補として挙がるようになっています。 SPA とは、 1 つの Web ページ( HTML 、 CSS 、 JavaScript ) 内で、変更のあった箇所だけを動的かつ部分的に再描画することでコンテンツを切り替える Web アプリケーションです。必要なデータのみを通信で取得し、ページ全体の再読み込みをしなくても画面情報を更新することができます。

SPA を用いた Web サービス構成は次のようなものが一般的です。

- ・フロントエンドを Web ブラウザー上で動作する SPA とし、 Web サーバーやホスティングサーバーから静的なリソースとして配信します。 

- ・バックエンドを REST API で実装した API サーバーとし、 Web ブラウザーからリクエストを受けます。 

- ・ API サーバーは、 DB サーバー( RDB の使用が一般的)に対してデータの登録、取得、更新、削除などの操作( CRUD 操作)を行います。 

- ・ API サーバーは、 DB サーバーに対する操作完了後、リクエスト元のフロントエンドに対して JSON を返却します。 

- ・フロントエンドは、受け取った JSON の情報をもとに Web ブラウザー上で画面描画を更新します。図 1.1 に示すようなこの構成を、本書では「 SPA 構成」、もしくは単に「 SPA 」と呼びます。 

##### 図1.1:基本的なSPA構成の概念図この SPA 構成の特⻑として、以下のようなものが挙げられます。 

- ・フロントエンドとバックエンドを疎結合にすることで、責任範囲を明確にできます。また影響範囲が限定されることから、改修や互換性・再利用性といった面で優位性があります。 

- ・画面の初期ロードに時間はかかりますが、以後の画面遷移はページ内で完結し高速です。 

- ・ PWA ( Progressive Web Apps )と組み合わせることで、モバイルとの親和性が高くなります。これらの特⻑が好感され、 2010 年代後半から多くの Web サービスに SPA 構成が取り入れられるようになりました。 

SPA 構成が出始めた当初は、画面のルーティングや状態管理といった SPA 固有の課題が多く、開発の手間やコスト面で従来のアプリケーションより不利でした。しかし、 React や Vue.js といったフロントエンド開発用フレームワークの成熟とともに、現在では SPA 構成の実現は大幅に容易になっていると言えるでしょう。

### **1-1-2 BaaSの活用** 

SPA 構成をとるとしても、実際に Web サービスを完成させるには多くのやるべきことがあります。たとえば、意図したユーザーにだけサービスを使ってもらうためには、バックエンドに認証(ログイン)機能が必要になります。セキュリティを考慮した認証機能の実装はとても大変ですが、それだけでは入口ができただけで、本来実現したいことにはあまり近づいていません。また、 Web サービスを稼働させるサーバーが必要になるので、その運用もしなくてはなりません。こういった悩みはどのシステムでも一般的に起こりうるものですが、ここは本来労力をかけたいところではありません。リーンスタートアップの中で言及された MVP ( Minimum Viable Product )のように、その Web サービス固有の価値のある機能に開発資源を集中させるべきです。現在では、さまざまなクラウドベンダーがこれらのバックエンドを担うサービス群を、 BaaS ( Backend as a Service )として提供しています。適切に BaaS を使えば、 MVP に集中して高い生産性で開発ができるでしょう。

### **1-1-3 BaaSとしてのFirebase** 

本書で取り上げる Firebase は、 Google 社が提供する BaaS です。ホスティング、認証、データベース、コンピューティングといった、バックエンドとして求められるものが一通りそろっています。さらにモバイル向けの機能として、プッシュ通知やクラッシュレポートなどの機能も取りそろえています。

Firebase 以外にも複数の BaaS が存在しますが、現時点での BaaS としての完成度や知名度は Firebase が群を抜いていると筆者らは考えています。大手クラウドベンダーの提供する BaaS は、各社のクラウドサービスに関する知識が一定程度求められるのに対し、 Firebase は Google Cloud の知識を前提としません。開発メンバーの中にインフラ担当者が不在、または、インフラの知識に乏しい場合でも、スムーズに SPA 構成の実装ができるはずです。近年は Firebase に影響を受けた後発のオープンソースライブラリが開発される動きもあり、そういった意味でももっとも注目される BaaS の 1 つと言えます。一方で、 BaaS はメリットばかりではありません。 Firebase を使う上でのデメリットとしては、以下のようなものが挙げられます。

- ・ Firebase に依存する実装となるため、 Google にベンダーロックインされるリスクがあります。 

- ・データベースが NoSQL であるため、複雑なデータ構造を取り扱う際にはデータモデルに注意が必要になります。 

- ・大規模な開発にはあまり向きません(機能数が多い、多数の Web サービスが連携する、ロジックが複雑、等)。上記のデメリットを踏まえると、 **スモールスタートし改善を繰り返す** ような Web サービスが、 Firebase の使いどころとしてもっとも適しています。不確定要素が多い中でもデータモデルを柔軟に変更することで対応でき、インフラ基盤としてのスケーリングもするので、初期投資を抑えつつ利用状況に応じて規模を拡大していくこともできます。また、後述する Firebase のデータベース・サービスの 1 つである Cloud Firestore は、 WebSocket によってリアルタイムにデータを取得できる、ユニークな機能を持っています。最新のデータを常に見たい、などのリアルタイム性の要件がある場合にも Firebase は適しています。 

Firebase に限らず、技術選定にはさまざまなトレードオフが発生します。さきほど挙げたデメリットと対比して、得られるメリットは次のようなものとなります。

- ・ Firebase が提供するさまざまなマネージドサービスを利用して、速やかに開発に着手できます。 

- ・データベース( Cloud Firestore )が持つリアルタイム機能やスケーラビリティを手軽に享受できます。 

- ・スモールスタートな開発に向いています(機能数が絞られている、要件が未確定のため作りながら明確化する必要がある、等)。これらの用途のみでしか使えないというわけではありませんが、もし上記のメリットがさほど重要でない場合は、次の点にも考慮することをお勧めします。 

- ・ Firebase 単体ではなく、 Firebase 以外の製品と組み合わせて使う。 

- ・ Firebase 以外の製品を検討する。 

Web サービスが成⻑していくと、 Firebase のデメリットに足を引っ張られるようになります。その場合も、同様にアーキテクチャの見直しが必要になるでしょう。

## **1-2 サンプルアプリ:安否情報共有サービス「anpi!」** 

### **1-2-1 システム要求** 

本書は、 Firebase を用いて Web サービスを作る際の参考となることを目指して執筆しています。前述の通り、網羅的に Firebase の機能を紹介した Web サービスのサンプルとして、本書では「 anpi! 」という名前の「安否情報共有サービス」を実装することとします。このサービスは、大規模な災害等が発生した際に、互いの安否状況を共有し、メッセージのやりとりなどができます。類似のサービスは多数あるので読者の方にとっても比較的イメージしやすいであろう題材として選びました。今回は主に次のようなシステム要求を設定しています。

- 1安否状況の回答依頼を受けたユーザーは、自分の安否状況を報告できること。 

- 2ユーザー同士がリアルタイム性の高い形で、最新情報をやりとりできること。 

- 3常に掲示しておくべき情報(緊急時の連絡先など)や、ファイル (災害時の避難場所への経路が記載された PDF など)など、全体へのお知らせ情報を登録・管理できること。 

4登録されたユーザーのみが情報にアクセスできること。

- 5安否確認の回答依頼および全体へのお知らせ情報の登録は、限られた業務上の管理者ユーザーである「アプリ管理者」のみが実行できること。 

6ユーザーの登録などの重要な機能は、システム運用上の管理者ユーザーである「テナント管理者」のみが実行できること( ※ 「テナント」の意味については後述します)。

7マルチテナントを実現できること。最後のマルチテナントの要求は、このサービスを SaaS 型で提供できるようにすることを意図しています。 SaaS 型はそうでないシステムに比べて複雑な構成になりがちですが、 Firebase を使って正しく設計すれば、そのような複雑な要件も満たせることを示すためにあえてサンプルアプリに盛り込んでいます。実際に筆者らが過去リリースした Firebase を基盤とする SaaS も、本書でこれから解説するような考え方に従って開発してきました。

SaaS やマルチテナントという概念や複雑なポイントについても、先に説明しておきたいと思います。

## ■SaaSとマルチテナント近年、 SaaS 型でアプリケーションを提供する機会が増え、顧客 (ここでは企業などの組織のことを指し、組織に所属する複数のユーザーが利用する)はシステムをサービスとして利用する形が一般的となっています。 SaaS というビジネスモデルは、複数顧客に対して同一サービスを速やかに、安く提供し、スケールメリットを活かすことで利益を得るモデルです。従来型のシステム構築は、顧客ごとに要件を聞き、専用システムを、時間をかけて構築し導入するシステムインテグレーション型が中心でした。 SaaS は、クラウド基盤などの技術的な進歩により、システムを複数顧客で共有する構成がとりやすくなったことに合わせて急速に発展しました。顧客側からも、速やかにシステム利用を開始できる、運用やインフラ面の管理を気にしなくてよい、従量課金というコストモデルが分かりやすいといった点が好まれ、結果幅広いユーザー層に受け入れられています。 

SaaS を開発・運用するサービスプロバイダーとしては、運用負荷を下げたりマシンリソースを有効に活用してコストを下げたりすることが、より安く顧客にサービスを提供することにつながります。そこで、このような SaaS と相性のよいマルチテナントと呼ばれるアーキテクチャを採用するケースが多くなっています。マルチテナントとは、複数の顧客で 1 つのアプリケーションやデータベースのインスタンスを共有して使う方式です。インフラを複数顧客で共有するためマシンリソースの効率がよくなりますが、顧客間のデータを論理的に分離して安全に管理することは、サービスプロバイダーの責務となります。この顧客単位に与えられるデータ格納場所などの論理的な領域を **テナント** と呼びます。テナントの分離方法に問題があった場合、顧客間でのデータ漏洩などセキュリティ上のリスクにつながります。マルチテナントに対して、シングルテナントは顧客ごとに専用のサーバーやデータベースを割り当てて環境を構築します。他テナントとは物理的に別のサーバーに配置されるため、安全性は非常に高くなります。しかし、先に述べたようなスケールメリットを活かしづらくなるため、セキュリティがとくに重要な場合や特定顧客に限った高品質なサービスとして提供する際に例外的に採用されることが多いです。

### **1-2-2 サービス実現に必要なシステム構成要素** 

このシステム要求を実現する上で、最低限必要と思われるシステム構成要素としては以下のようなものが挙げられます。

1データを永続化するためのデータベース

2ポーリングや WebSocket を用いたリアルタイム通信機能(および、それをホストするアプリケーションサーバー)

3ファイルの保管先となるストレージサーバー

4ユーザーの本人確認が可能な認証機能、または、シングルサインオンなどを提供する外部認証基盤

5ユーザーにロールや権限を設定しアクセス制御を行える認可機能 6マルチテナントを実現し、テナントを論理的に分離する機能(認可機能の一種として扱います)

これらは先に挙げた BaaS である Firebase を用いることで、ほぼすべてがワンストップで提供されます(認可機能は、サービスごとの仕様に合わせて一部個別の実装が必要です)。 2 章以降では、 Firebase の主要製品の詳細を紹介しつつ、具体的な設計手順を説明していきます。

### **1-2-3 画面イメージ** 

本書は Firebase と関連する設計や開発を中心に説明します。 UI の検討なども通常は設計工程の中で行いますが、本書ではすでに作りたい画面のイメージや要件は決まっているという前提のもと、その中で Firebase をどう組み込んでいくか、という観点で解説します。そのため、先に anpi! の完成形の画面キャプチャをお見せし、ゴールのイメージを持って頂きます。その上で、この先 2 章で詳細な機能要件やデータモデルなどの検討をしていきます(要件の整理は、このあと「 2-3-1 要件を整理する」で行います)。

anpi! はある組織内において、管理者による安否確認の依頼とその回答を集めることができます。まず、ユーザーはシステムにログインします(図 1.2 )。図1.2:anpi!のログイン画面ログインすると、ユーザーに回答依頼されている安否確認の一覧を確認できます(図 1.3 )。図1.3:anpi!の安否確認の依頼安否確認を 1 つ選ぶと、安否状況の回答ができます。自分の安否状況や連絡事項を記入し、回答を送信します(図 1.4 )。

##### 図1.4:anpi!の安否確認の回答アプリ管理者向けに、これまでに依頼した安否確認の回答を一覧表示したり、期日を設定・変更する画面があります。また、新しい安否確認の依頼を登録することができます(図 1.5 )。これらを安否確認管理と呼びます。図1.5:anpi!の安否確認の管理安否確認の管理画面から 1 つ選ぶと、回答状況の一覧を確認できます(図 1.6 )。図1.6:anpi!の安否確認の回答一覧以上が anpi! のコア機能である、安否確認の依頼と回答のための画面になります。続いて、お知らせやメッセージなど、安否確認業務をサポートするための画面をお見せします。アプリ管理者から全ユーザーに対するお知らせを載せる画面があります(図 1.7 )。図1.7:anpi!のお知らせ一覧お知らせを 1 つ選ぶと、内容の詳細を確認できます。ファイルが添付されていることもあります。管理者であれば、お知らせ内容の編集なども可能です(図 1.8 )。 

##### 図1.8:anpi!のお知らせの詳細さらに、ユーザー同士がリアルタイムのチャットコミュニケーションを行うためのメッセージ画面があります(図 1.9 )。 

##### 図1.9:anpi!のメッセージテナント管理者向けに、ユーザーを管理する画面があります。ユーザーアカウントの新規登録や無効化ができます(図 1.10 )。図1.10:anpi!のユーザー一覧ユーザー一覧からユーザーを 1 人選ぶと、その詳細を確認し、更新できます(図 1.11 )。 

##### 図1.11:anpi!のユーザー情報の更新すべてのユーザーは自分自身のプロフィールを編集できます(図 1.12 )。メニューバー上部からプロフィールページに遷移します。図1.12:anpi!のユーザープロフィール以上が anpi! の基本的な画面構成になります。 

# **2 章設計編** 

**本章では Firebase を用いた Web サービスを設計します。 anpi! の設計を通じて、 Firebase を中心としたクラウドネイティブなアーキテクチャの特性を理解することが本章のゴールです。**

## **2-1 なぜ設計から始めるか** 

Firebase を BaaS として利用することにより、バックエンド側で検討すべき、ユーザー認証・認可・データアクセスといった基本的な機能は簡単に実現できます。さらにバックエンドサーバーの運用保守・管理からも解放され、フロントエンド側の開発に集中できます。データベースはアプリケーション開発の中でもっとも重要なミドルウェアの 1 つです。 Firebase の代表的なデータベース製品である Cloud Firestore (本書では「 Firestore 」と呼びます)は、スキーマ設計が不要な NoSQL データベースであり、主要な言語に対応した公式の SDK が提供されています。そのため、いきなり開発を始めてもある程度の規模のアプリであれば簡単に動くものが作れるでしょう。その一方で、 Firebase を用いた開発は、 RDB を用いた Web 開発の考え方とは異なる使い方が求められることもあります。 RDB になじみのある人ほど、 NoSQL である Firestore の思想とのギャップに苦しんでいるようなケースも、筆者らの経験上多く見受けられました。 RDB では一般的とされるものの、 Firestore では避けたほうがいいプラクティスを気づかずに実践してしまったりすることもありました。また、その特性上、あらゆるアプリ開発に適しているわけでもなく、 Firestore ではなく RDB を採用すべきケースも依然としてたくさんあります。

Firestore は、正しく扱えばメンテナンスしやすいソースコードが手に入り、スケールしやすく安全なデータベースとして活用できます。しかし正しく設計しないとかえって複雑な処理をフロントエンド側で実装しなければならなくなったり、セキュリティ上の脆弱性を生んでしまうリスクがあります。そのためには Firestore の特性を理解した上での設計が重要です。さらに、 Firestore は他の Firebase サービス群と組み合わせることでさまざまな便利な使い方があります。そこで、本章ではまず Firebase の主要なサービス群の基本的な特性について説明します。次に、もっとも重要なデータベース周りの設計、つまり Firestore の扱い方やデータモデリングの考え方などについて重点的に詳しく解説します。 RDB を知らなくても理解できるよう努めていますが、知っている人にとってはよりその違いが分かるように、 RDB との比較によって Firestore の特性を説明する箇所も盛り込んでいます。そして、 anpi! というアプリケーションの要件を満たすための設計を通じて、 Firebase の活用法を整理しながら、設計工程の成果物として Firestore のデータモデルを完成させていきます。

## **2-2 Firebaseのサービス構成** 

Firebase にはバックエンド基盤を構成するさまざまなサービスが提供されています。本節では、 Firebase の各種サービスが提供する機能と注意点を一般論として紹介します。このあと設計を進めるにあたっての基礎知識として、まずは Firebase がどのような製品なのかを見ていきましょう。

### **2-2-1 Cloud Firestore** 

Cloud Firestore (本書では「 Firestore 」と呼びます)は Firebase の代表的なサービスの 1 つであり、ドキュメント指向の NoSQL データベースです。複数クライアントからの同時アクセス、高負荷時の自動スケーリング、データの冗⻑化など、データベースとして本番運用する上での重要なポイントは Firebase のサービスとして担保されています。そのため、開発者はデータをどのように永続化するかというアプリケーション開発の本質的な部分に集中できます。本書で扱うテーマとしても Firestore の設計はかなりの割合を占めているため、 Firestore についてはサブ機能レベルで紹介します。また、次節以降で設計やデータモデリング方法を解説する際にも、改めて詳しく特性などを説明します。

## ■ドキュメントとコレクション 

Firestore は、 **ドキュメントとコレクション** という構造体を使ってデータを管理します。ドキュメントは 1 つのデータオブジェクトを表し、複数の key-value のペアをフィールドに格納します。たとえば、田山剛さんというユーザー情報をドキュメントで扱う場合について考えます。名前やメールアドレスを持たせるには、nameフィールドには " 田山剛 " 、 emailフィールドに "tsuyoshi.tayama@example.com" 、という文字列を持つ構造として定義できます。データ型はほかにもブール値、日時 (タイムスタンプ)、配列、マップ( JSON のような階層構造のオブジェクト)、参照型などが用意されており、さまざまな値をフィールドに保持できます。つまり、ドキュメントは JSON のような構造体のデータオブジェクトとほぼ等価になります。

●リスト2.1:ユーザー情報を扱うドキュメントの例

{ name: “田山剛”, email: “tsuyoshi.tayama@example.com”, address: { prefecture: “東京都”, city: “中央区”, addressLine: “xxx” }, phoneNumbers: [“080-1234-xxxx”, “03-1234-xxxx”], isActive: true }

コレクションには複数のドキュメントをまとめて格納できます。コレクションに対してクエリを発行することで、条件にマッチしたドキュメントを取得したり、ソートしたりできます。さらに Firestore の重要な特徴の 1 つとして、ドキュメント自体もコレクションを持つことができ、入れ子構造にできる点が挙げられます。このようなドキュメント配下に持つコレクションのことを **サブコレクション** と呼びます。コレクションがサブコレクションを持ったり、ドキュメントがドキュメントを持つことはできません。必ず、コレクション、ドキュメント、サブコレクション、ドキュメント、と交互に持つ構造になり、サブコレクションの最大深度は 100 となります。ドキュメントやコレクションのデータのありかは /collection/documentId/subCollection/subDocumentId のようなパス表現で一意に識別可能です。 collection や subCollectionの部分は実際にはコレクションの物理名として分かりやすい名前が付与され(たとえば、users)、これを **コレクション ID** や **コレクション名** とも呼んだりします。 documentId や subDocumentIdはドキュメントごとに一意に付与される ID で、 **ドキュメントID** と呼びます。ドキュメント ID は Firestore 側で自動生成が可能で、その場合はGoPdXIyV5SPcGak92MGmのようなアルファベット大文字小文字と数字からなる 20 桁の文字列です。これはそのコレクション内で一意であることが保証されています。なお、ドキュメント ID は自動生成せずに明示的に指定することもできます。

RDB と比較すれば、コレクションはテーブル(表)、ドキュメントはレコード(行)のようなものと言えます。一方、ファイルシステムと比較すれば、コレクションがフォルダ、ドキュメントがファイルのようなものと捉えることもできます。図 2.1 にコレクションとサブコレクションおよび、ドキュメント構造の具体例を示します。会社のようなグループを表すtenantsというコレクションに各社を表すテナントドキュメントが格納されています。また、テナントドキュメントにはusersというサブコレクションが含まれており、この中には実際のユーザー情報を表すユーザードキュメントが含まれています。 1 つのドキュメントは複数のサブコレクションを持つこともできます。たとえば、テナントコレクションはほかにもmessagesというサブコレクションを持っており、まったく異なる種類のドキュメントも保持しています。

##### 図2.1:コレクションとドキュメント 

Firebase 公式の SDK を用いてドキュメントを取得する際も、このようなパス表現を用いてアクセスします。リスト 2.2 は SDK を使って

Firestore の/tenants/t1/users/u1というパスのドキュメントを読み込むサンプルコードです。ライブラリの詳細な使い方は 3 章で紹介しますので、ここではこのような SDK を使えば Firestore のデータに簡単にアクセスできるということをご理解いただければと思います。

●リスト2.2:Firestoreからドキュメントを取得するサンプルコード

import { getFirestore, getDoc, doc } from 'firebase/firestore'; const db = getFirestore(); const userRef = doc(db, 'tenants', 't1', 'users', 'u1'); const userSnapshot = await getDoc(userRef); const user = userSnapshot.data(); console.log(user.name); // => "山田"

Firestore では、このようなドキュメントとコレクションの階層構造を利用してデータモデリングをします。

## ■スキーマレスコレクションは RDB のテーブルとは違い、スキーマ情報は一切持ちません。 RDB のテーブルは作成時に列の名前や型をすべて列挙し、スキーマ構造を明確に定義します。それ以外の構造を持つレコードを挿入することは許されません。一方、 Firestore はスキーマレスのため、どのようなフィールド構造を持つドキュメントであってもコレクションに格納できます。たとえば、図 2.1 のusersコレクションに対して、nameやemailフィールドを持たないドキュメントや、addressというフィールドを持つドキュメントを個別に登録したりもできます。柔軟性が高く、データ構造の変更に強いため、変化の多いシステムやとにかく市場に早くリリースしてユーザーに使ってもらい、フィードバックを得てから改修を繰り返すようなシステムには向いています。しかし、開発者が好き勝手にデータ構造を定義してしまうと、フィールドの有無によって条件分岐をアプリケーション側で制御する必要が生じるなど、結果的に複雑な実装を生んでしまう危険性も含んでいます。そのため、実運用上は各コレクションにどのようなフィールド構造のドキュメントを保存するのかをきちんと設計し、 1 つのコレクション内には極力同じ構造のドキュメントを保存するようにしましょう。アプリケーション側のバリデーション等で未然に防ぐか、後述するセキュリティルールによってある程度バリデーションをかけることができます。 

## ■クエリ機能 

Firestore におけるクエリは、コレクション内から 1 件あるいは複数件のドキュメントを取り出す操作のことです。 RDB における SQL の SELECT 文のように、条件を指定してドキュメントのフィールド値に基づいてフィルタリングしたり、特定のフィールドでソートをかけたり、取得件数を絞ったりできます。リスト 2.3 は、messagesコレクションに対して検索クエリを発行し、条件にマッチするドキュメントを検索するコードの例です。

●リスト2.3:コレクションに対してクエリを発行するサンプルコード

import { getFirestore, query, collection, where, orderBy, getDocs } from 'firebase/firestore';

const db = getFirestore(); const q = query( collection(db, 'tenants', 't1', 'messages'), where('created_by', '==', 'u1'), orderBy('created_by', 'asc') ); const messagesSnapshot = await getDocs(q); messagesSnapshot.docs.forEach((doc) => console.log(doc.data().body)); // => "こんにちは" "元気ですか?"

## ■リアルタイムリスナー 

Firestore の便利な機能として、データベース側の更新をほぼリアルタイムにクライアントへ自動連携してくれるものがあります。リスナーをコレクションやドキュメントやクエリに対して設定でき、変更を監視できます。データベースの値に更新があった場合、リスナーがその通知を受け取り、通知後にフックしてフロントエンド側の画面表示を最新化するコードを簡単に書くことができます。この機能を活用するとクライアント側からバックエンドへのデータ取得リクエストが不要となります。たとえば、チャット機能などリアルタイムにデータ反映が必要とされる機能もシンプルに実装できます。

## ■セキュリティルール 

Firestore において、ユーザーの認証や認可などのアクセス制御やデータのバリデーションのために、 **セキュリティルール** と呼ばれる機能が利用できます。これは、ルールを一定の記法に従って設定ファイルに記載することで、アクセス時にそれらのルールを満たしているかを判定し、満たしていない場合はアクセスを拒否できます。 Firebase を利用する SPA 構成は、フロントエンド側のブラウザーから直接 Firestore にアクセスさせることで、バックエンド側に API サーバーを用意しなくて済むという点が最大のメリットです。一方で、通常バックエンド側の API サーバーはブラウザーからのアクセスに対して、認証や認可などのアクセス制御のチェック処理を実装し、不正な要求は拒否する役目を担っています。 Firebase を用いて API サーバーを用意しない場合、このようなチェックは別の場所で実装する必要があります。しかし、フロントエンド上で認証や認可を実装してしまった場合、悪意あるユーザーによってブラウザー上でそのチェックを無効化されてしまうことも技術的には不可能ではありません。そこで、そのようなリスクに対処できるように、 Firestore ではデータベース側でセキュリティルールを設定し、リクエスト内容をチェックすることでデータのセキュリティを担保します。図2.2:Firestoreにおけるセキュリティルールの役割セキュリティルールによって、たとえば次のようなことが実現できます。

- ・usersコレクションのドキュメントは認証済みのユーザーのみ読み取りできる。 

- ・messagesコレクションのドキュメントは管理者権限を持つユーザーしか読み書きできない。 

- ・messagesコレクションのドキュメントのbodyフィールドは最大 200 文字までしか書き込みできない。このようなルールは、次のリスト 2.4 のように書くことができます。match文でルールを適用するコレクションを指定し、allow文で許可するアクションと条件を指定します。 

●リスト2.4:Firestoreのセキュリティルール設定例

rules_version = '2'; service cloud.firestore { match /databases/{database}/documents { // usersコレクションのドキュメントは認証済みのユーザーのみ読み取りできる // ※request.auth != nullの意味については「2-5 認証を設計する」で説明 match /users/{userId} { allow read : if request.auth != null; } // messagesコレクションのドキュメントは管理者権限を持つユーザーしか書き込みできない // ※isAdministrator()の具体的な実装方法は「2-6-4 ロールベースのアクセス制御」で説明 // bodyフィールドは最大200文字までしか書き込みできない match /messages/{messageId} { allow read: if isAdministrator(); allow write: if isAdministrator() && request.resource.data.body.size() <= 200; } } }

セキュリティルールは許可リスト方式で、明示的に書かれた権限のみ許可されます。つまり、usersコレクションのこのルールの書き方は、readは認証済みのユーザーであれば許可されますが、 writeは何も書かれてないので一切許可されないということになります。セキュリティルールの書き方は次節以降の説明でも紹介しますが、実際に書く際は一度公式マニュアル <u>1</u> に目を通しておいたほうがよいでしょう。このようなルールは、ユーザーが Firestore へアクセスするたびに評価されます。セキュリティルールを適切に設定しておかないと、データ漏洩などにつながるリスクがあるため、正しく理解して十分に注意して扱うことが大切です。セキュリティルールは、ブラウザーで Firebase 管理コンソールから GUI で直接更新する方法と、ローカルでfirestore.rulesというファイルに記載してアップロードする方法があります。運用する上では、後者の方法で管理し、firestore.rulesを Git リポジトリに置きつつ、メンテナンスすることを推奨します。

## ■データ整合性 

Firestore は RDB と同じように複数のドキュメントに対する一貫性のある読み書きをサポートしています。アトミックな複数ドキュメントの読み書きの機能としては、 **トランザクション** と **バッチ書き込み** という 2 つの方法があります。トランザクションは読み込みと書き込みが複数回行われる際に、整合性のとれた状態で一連の操作ができるよう保証します。バッチ書き込みは複数回の書き込みのみを行いたい場合に、一連の更新処理を一貫性を保ったまま実行できます。これらの機能を活用すれば、使い勝手は少々異なりますが、 Firestore でも RDB で使われるトランザクション機能と同じように一貫性のあるアトミックな操作が可能です。

## ■課金体系 

Firebase は従量課金制のサービスですが、無料で使える量も多いため、小規模なアプリや、個人開発で使う程度であればそれほど気にしなくても大丈夫です。とはいえ、何に対して課金されるのかを理解すれば、むだな出費をなるべく抑えられるようなアーキテクチャを検討しやすくなります。

Firestore の場合は、基本的にドキュメント単位の読み取りおよび書き込みに対して課金されます。そのため、不必要なドキュメントまで検索するようなクエリは極力避け、必要最低限のリソースのみリクエストするようにしましょう。ときおり、無限ループ等のプログラムバグが原因で Firestore ドキュメントへの読み書きを意図せず大量にリクエストしてしまい、思わぬ高額請求を招いてしまったという事故がニュースになることもあります。 Firebase に限らずあらゆるクラウドサービスを使う際に言えることですが、このようなミスには十分に注意しましょう。その他、ストレージの容量やネットワーク帯域の使用量などに対しても課金は発生します。他のサービスの課金体系もあわせて、最新の情報を公式ドキュメント <u>2</u> ~~で~~ ご確認ください。

### **2-2-2 Cloud Storage for Firebase** 

Cloud Storage for Firebase (本書では「 Storage 」と呼びます)はファイルのアップロードや保管、ダウンロードなどの機能を提供するオブジェクト・ストレージ・サービスです。画像や動画、 PDF などユーザーがアップロードしたファイルを保存したり、他のユーザーが安全にダウンロードするためのエンドポイントを公開し、署名付きの URL を発行できたりします。

Storage も Firestore と同様に、セキュリティルールを用いてアクセス制御が可能です。後述の Firebase Authentication にて認証済みであるかどうかのチェックのほか、ファイル名やサイズ、コンテンツタイプなど、ファイル特有の条件をルールに用いることも可能です。また、 Storage のセキュリティルールから Firestore のデータを参照したルールの設定もできます。 Firestore と同様に、 Storage のセキュリティルールについても十分に考慮し、正しい理解に基づいた設計が必要となります。

### **2-2-3 Firebase Authentication** 

Firebase Authentication (本書では「 Authentication 」と呼びます) は、ユーザー認証のためのセキュリティ・サービスです。一般に、認証とは、アクセスしようとしているユーザーが本当に本人であるかを確認することです。よくあるケースとしてはログイン ID とパスワードをユーザーに入力してもらうパスワード認証が一般的です。これは、正しいパスワードを知っているのは本人しかいないという前提のもとで本人確認しています。パスワード認証においてセキュリティを担保するためにはユーザーが入力するフォームとサーバー間を安全に通信したり、パスワードを暗号化してデータベースに安全に管理したりとさまざまな考慮が必要です。

Authentication はこのようなユーザー認証にまつわる複雑な仕組みを開発者が意識することなく、安全に実現できるサービスです。一般的なメールアドレスとパスワードの組み合わせによる認証のほか、パスワードを使わず実際にメールを送信することで本人確認できるメールリンク認証といった仕組みがあります。ほかにも、 Google 認証、 Facebook 認証、 GitHub 認証といった外部のサービスに認証を移譲したり、 OAuth 2.0 や OpenID Connect もサポートしています。ユーザーログインの部分は基本的にどのようなサービスでも必要な機能であり、セキュリティを十分に考慮する必要があります。 Authentication に認証処理を移譲することでパスワードなどの秘匿情報の管理、ユーザーとの ID や認証トークンのやりとりなどの複雑な処理の実装から解放されるため、サービスのコア機能の開発に集中できます。

Firestore や Storage とも統合でき、セキュリティルールと組み合わせることでログイン済みのユーザーにのみアクセスを許すなど、安全なデータやファイルアクセス手段を提供することも簡単にできます。なお、認証に対して認可もセキュリティを担保するための重要な概念です。認可は、アクセスしようとしているユーザーが必要な権限を持っているかどうかを確認することです。 Authentication に認可の機能はないため、権限に応じたアクセス制御は前述の通りセキュリティルールを用いて Firestore や Storage にて実現する必要があります。

### **2-2-4 Cloud Functions for Firebase** 

Cloud Functions for Firebase (本書では「 Functions 」と呼びます) はサーバーレスのコンピューティング・サービスです。もともと

Google Cloud に Cloud Functions というサービスが提供されていますが、 "for Firebase" の名の通り、 Firebase から扱いやすいように統合されたものです。実体としては Google Cloud にある Cloud Functions そのものです。

Functions を使うと、インフラやサーバーを気にすることなくバックエンド側にビジネスロジックを実装し、簡単にクラウド上にデプロイできます。 SPA 構成の場合、フロントエンド側から直接 Firestore にアクセスし、ビジネスロジックもフロントエンド側に JavaScript や TypeScript などで実装することになります。その場合、ブラウザー上でコードが実行されるため、知識のあるユーザーはロジックを改ざんしたりデータの流れを読み取ることも仕組み上は可能です。また、ファイルの加工処理やユーザー認証に関するデータのやりとりなど、ブラウザー上で実行するには不向きなものもあります。そのような場合に、 Functions は手軽に使うことができるバックエンド基盤として非常に有用です。

Functions は Firestore や Storage と連携することで、トリガー実行させる使い方が便利です。たとえば、ある Firestore コレクションのドキュメントを登録した際に Function を呼び出すように設定します。呼び出された Function 側で、そのドキュメントを読み取りデータ加工した結果をまたドキュメントに書き込む実装をすると、簡単に非同期のバックエンド処理を実現できます。ファイル加工も Storage にファイルがアップロードされたことをトリガーに実行することで、たとえば画像データの圧縮やサムネイル作成といった処理もバックエンド側に容易に組み込むことができます。ほかにも HTTP 経由で起動も可能で、外部に API を公開するような使い方もできます。注意点としては、 Functions で実装された処理はすべてバックエンド側で実行されるため、ユーザーの権限に基づいた制御ができません。つまり、セキュリティルールは無視して実行されるため、自前のチェックロジックを実装する必要があります。こちらについては「 2-6-7 Functions の認証・認可」で別途説明します。このように、 Functions はさまざまな使い方ができる便利なものです。注意点はあるものの、どうしてもバックエンド側で処理が必要な場合は活用を検討してみてください。

### **2-2-5 Firebase Hosting** 

Firebase Hosting (本書では「 Hosting 」と呼びます)はその名の通りコンテンツを配信するためのホスティング・サービスです。簡単なコマンドだけで、 Web アプリケーションをデプロイし、 URL を与えてインターネットに公開できます。コンテンツを圧縮したり、 CDN エッジでキャッシュしたりと、 Web アプリケーションを高速に配信するためのさまざまな処理を自動的に行ってくれるため、ここでも開発者がインフラ面で気にする必要はありません。カスタムドメインを割り当てて任意の URL でサービスを公開することや、開発用・本番用に環境を分けることも容易です。簡単なコンテンツのリライトといった Web サーバーとしての機能もある程度有しているため、本番サービスとしても問題なく利用できます。開発時に便利な機能として、 **プレビューチャンネル** と呼ばれる一時的な URL を発行してリソース配信する機能があります。筆者らのプロジェクトでは、次のような使い方をしています。まず、 Git リポジトリの機能ブランチからメインブランチへプルリクエストを発行する際に、 CI/CD サーバーでそのコミットのソースコードでアプリをビルドします。ビルド結果を Hosting のプレビューチャンネルで公開し、一時的に有効な URL を付与します。これにより、プルリクエスト単位で成果物をチェックできるようになります。チーム内でのレビューでは機能ブランチ単位の断面、内部の品質保証チーム向けにはリリース用の断面と、目的ごとの環境を公開し、便利に活用できました。注意点としては、あくまで公開先の URL と配信リソースを複数持てるというだけですので、 Firestore や Storage などは同じものを共有する形になります。

### **2-2-6 Realtime Database** 

Realtime Database は Firebase の初期から提供されているデータベース・サービスです。もともとはその名の通り、リアルタイム同期を可能とするデータベースで、登場時はほかに例を見ない画期的なものでした。現在では後継の Firestore のほうが Realtime Database に比べて強化されたサービスという位置づけになっています。リアルタイム同期機能も、 Firestore におけるリアルタイムリスナーとして受け継がれています。そのため、新規開発の場合は、とくに理由がない限り Firestore を採用するほうが無難です。本書でも Firestore を採用し、 Realtime Database については扱いません。しかしながら、 Realtime Database があらゆる点で Firestore に劣っているわけではありません。たとえば、プレゼンス機能というモバイルクライアントの場合にそれがオンラインかオフラインかの接続状態を知ることができる機能があります。これは現在 Realtime Database では標準でサポートされている一方、 Firestore にはそのような機能はありません。 Firestore で同等のものを実現するには、 Functions などを組み合わせて自前で実装する必要があります。

#### Realtime Database と Firestore の比較については、公式ドキュメント <u>3</u> をご覧ください。 

1. <u>https://cloud.google.com/firestore/docs/security/get-started</u> 

2. <u>https://firebase.google.com/pricing</u> 

3. <u>https://firebase.google.com/docs/firestore/rtdb-vs-firestore</u> 

## **2-3 Firebaseの概要設計を行う** 

本書では、地震や台風などの災害時、あるいは定期的な訓練などにおいて、さまざまな会社などの組織内で安否確認や情報共有を円滑に進められるような anpi! という名前の Web サービスを題材とします。 anpi! の設計・開発を進めていく中で、 Firebase を使った Web サービス開発のノウハウを紹介していきます。まずはサービスに求められる機能や要件について確認しましょう。 Firebase を使った設計においては、とくに Firestore のデータモデリングが重要になります。認証や権限管理なども Firestore のデータ構造とセキュリティ機能を用いて実現することになるため、どこまでの機能レベルが要求されているかを抑えることも大切です。なお、すでに 1 章の最後で anpi! の完成系の画面キャプチャを紹介しています。もし作りたい機能そのもののイメージがわかない場合は、そちらも適宜ご参照ください。

### **2-3-1 要件を整理する** 

anpi! は、次の表のような機能要求を持つ安否情報共有サービスとします。

|機能|要求|
|---|---|
|マルチテナント|SaaSのように、1つのサービスを複数の企業などのグループで共有して使える。|
|安否確認|グループ内のユーザーに向けて、安否状況の報告を依頼できる。また、その回答を参照できる。|
|安否確認回答|ユーザーは自分自身の安否確認状況を回答できる。|
|お知らせ|グループ内の全ユーザー向けに情報を発信できる。|
|メッセージ|グループ内のユーザー同士がチャット形式でメッセージのやりとりができる。|
|ユーザー管理|グループの管理者がグループ内へのユーザー追加や無効化ができる。|

|ユーザープロフィール|ユーザーは自分自身のプロフィール情報を更新できる。|
|---|---|
|認証|ユーザーはメールアドレスを用いてログインできる。なりすまし等ができないよう、セキュアな仕組みとする。|
|認可|安否確認の報告依頼や回答の参照は業務上の管理ユーザーであるアプリ管理者、ユーザー管理機能はシステム運用上の管<br>理ユーザーであるテナント管理者しか利用できないよう制御可能とする。|

これらの要求に基づき、各機能が持つべき要件は次の通りとします。

- ・マルチテナント構成 

- ・テナントとは、 1 つの会社や家族といった災害情報を共有したいユーザーのグループのことを指します。 

- ・ 1 つのテナントには複数のユーザーが所属し、 1 人のユーザーは複数のテナントに所属可能です(たとえば、会社テナントと家族テナント)。 

- ・テナント内の情報はテナントに所属しているユーザーのみアクセスできます。テナント間のデータは論理的に分離しています。 

- ・安否確認と回答 

- ・安否確認 

- ・安否確認者となるテナント内のユーザーは、同テナントに所属する各ユーザーへ自身の安否状況を報告するように依頼をします。 

- ・この依頼のことを **安否確認** と呼び、安否確認を依頼することをシステム上では **安否確認を登録する** と表現します。 

- ・安否確認は災害発生時や毎月 1 回の定期訓練など、イベント単位で登録でき、好きなタイミングで登録できます。 

- ・安否確認には回答の期日があります。 

- ・安否確認回答 

- ・安否確認が 1 件以上登録されている場合、ユーザーは安否確認を選択して回答が可能になります。 

- ・このような安否確認に対してユーザーが自身の安否状況を報告することを **安否確認回答** と呼びます。 

- ・安否確認回答は、自分自身の現在の安否状況を回答(安全、軽傷、重傷の中から選択する)したり、自由記述のメモ欄から安否に関する簡単な連絡事項を登録したりできます。 

- ・お知らせ 

- ・テナント内の全ユーザーに向けた重要な共有事項を掲載できます。 

- ・お知らせは複数件登録でき、 1 つのお知らせの中には、タイトルや本文の情報を持ちます。 

- ・お知らせには **公開状態** として、 **即時公開** または **下書き(非公開)** のいずれかを選択できます。即時公開の場合のみ、全ユーザーが閲覧できます。 

- ・ 1 つのお知らせには連絡先一覧などのファイルをアップロードできます。また、各ユーザーはそれらのファイルのダウンロードや参照が可能です。 

- ・メッセージ 

- ・テナント内のユーザー同士がチャット形式のメッセージで連絡をとることができます。 

- ・メッセージはテナント内の全ユーザーが登録でき、すべてのメッセージを一覧形式で参照できます。 

- ・メッセージは誰が、いつ発信したかが分かるようなチャット形式で表示されます。 

- ・一度投稿したメッセージはあとから変更や削除ができません。 

- ・特定のユーザー間や複数のグループメンバーしか閲覧できないようなメッセージという概念はありません。 

- ・ユーザー管理 

- ・テナントに所属するユーザーを新たに登録したり、属性を変更したりできます。ユーザー管理機能は限られた管理者権限のあるユーザーのみ利用可能です( **認可** にて後述します)。 

- ・ユーザーの有効状態を切り替えることができます。無効なユーザーはテナントにログインできず、一切の操作ができなくなります。 

- ・ユーザープロフィール 

- ・ユーザーは自分自身の氏名、所在地を変更できます。 

- ・ユーザーは別のテナントに切り替えることができます。 

- ・認証 

- ・ユーザーはサービスにログインする際、メールアドレスをキーとして Google 認証またはメールリンク認証によって本人確認します。本人確認がとれたユーザーのみサービスを利用できます。 

- ・ Google 認証は、メールアドレスが Google アカウントだった場合に、 Google に認証を移譲する方式です。 Google アカウントにログインできれば、本人確認がとれたと見なします。 

- ・メールリンク認証は、ログインの際にそのメールアドレスに対してメールを送信し、その本文中に記載された URL をクリックされたら、本人確認がとれたと見なします。 

- ・認可 

- ・ユーザーには 1 つのロールが付与され、ロールごとに決まった権限で機能を利用したりデータにアクセスしたりできます。 

- ・ユーザーのロール設定はユーザー管理機能で行います。 

- ・ロールは **アプリ利用者** 、 **アプリ管理者** 、 **テナント管理者** の 3 つです。 

- ・アプリ利用者は、安否確認回答ができ、メッセージの投稿ができます。また、お知らせの閲覧ができます。 

- ・アプリ管理者は、アプリ利用者ができることに加え、安否確認を登録したり、安否確認回答の確認ができます。また、お知らせを登録できます。 

- ・テナント管理者は、アプリ管理者ができることに加え、ユーザーの登録や変更ができます。 

### **2-3-2 概念データモデルを作成する** 

概念データモデルはシステムで必要となる実体(エンティティ) を抽出し、エンティティ間の関係(リレーション)を整理することで、システム全体を俯瞰できる有益なモデルです。 RDB のテーブル設計をする際は ER 図を書くことが一般的です。このようなモデリングは、データベースがリレーショナルであるか key-value であるかに関わらず有効です。関係を実現する方法は、 RDB であれば中間テーブルを用いる、 Firestore であれば後述する通りサブコレクションを使う、など実装のパターンは異なります。しかし、本質的にシステムが満たしたいビジネスの構造は同じです。そのため、システム全体のデータモデルを最初に整理すると今後の設計や開発を進めるにあたり見通しがよくなります。

anpi! の概念データモデルの整理を始めていきます。なお、本節の内容は Firebase に限らず、データモデリングを行うときの一般的な手法によるものです。ここで扱う概念データモデルのエンティティはまだ Firestore のコレクションに対応づけられる、という意味は含んでいないことにご注意ください。概念データモデルをコレクション構造にモデリングするのは次のステップとします。もしこのような概念レベルのモデリングに精通している読者の方であれば、最終的な概念データモデル(図 2.7 )にだけ目を通し、あとは次節まで読み飛ばして頂いても構いません。最初に、前節で洗い出した機能から、エンティティとなりそうなものを列挙します。

- ・テナント 

- ・ユーザー 

- ・安否確認 

- ・安否確認回答 

- ・お知らせ 

- ・添付ファイル 

- ・メッセージ 

- ・ロール 

- ・権限次に、これらのエンティティ同士の関係性について検討していきます。なお、以降の本節内の本文では、エンティティであることを強調する場合に「エンティティ名」のようにカギ括弧付きで表現することにします。まずは「テナント」と「ユーザー」という基本的なエンティティから始めていきましょう。機能要件は次のように書かれています。 

1 つのテナントには複数のユーザーが所属し、 1 人のユーザーは複数のテナントに所属可能ですこれを概念データモデルのエンティティとリレーションの図で表現すると、図 2.3 のようになります。このように、概念データモデルはエンティティ間のリレーションを結合するコネクターの足の形によって表現します。本書は ER 図や UML の専門書ではないため、ここでは簡易的な表現とし、今回のシステムで登場するパターンと本書内の表現方法について一通りの注釈をつけるにとどめておきます。図2.3:テナントとユーザーの関係続いて、「安否確認」および「安否確認回答」という anpi! 特有の機能を中心としたエンティティについて考えます。想定利用シーンとして、地震などの災害などが発生した際にテナント内の管理者 (たとえば、会社の総務部門のリーダー)がテナントの所属するメンバー(たとえば、会社の全社員)の安否状況を確認したいとします。その際、管理者は安否確認の依頼をメンバーに対して投げかけます。メンバーは安否確認を受けると、それに対して、自分自身の現在の安否状況やメモなどを登録することで、管理者に安否状況を回答します。管理者は、 1 度の安否確認から複数のメンバーの安否確認回答を受け付け、メンバーの回答を一覧できます。つまり、「安否確認」と「安否確認回答」は一対多となります。また、「安否確認」は別の災害が発生したときや毎月 1 回の定期確認など、その都度依頼するので、「テナント」と「安否確認」の関係も一対多となります。「安否確認」に限らず、あるエンティティの 1 件のデータ登録は、 1 人の「ユーザー」の操作によって行われます。 Firestore を使う場合は 1 件のデータは 1 つのドキュメントに対応します。言い換えると、ある 1 つのドキュメントを登録した人物は 1 人の「ユーザー」と紐づけられるので、多くのエンティティは「ユーザー」と一対一の関係にあります。ただし、これはシステム上で各ドキュメントの登録時に誰が登録したかを記録しておく、という仕様があれば十分です。このあとに出てくるエンティティも同様のため、以降は「ユーザー」とのこのような関連性については割愛します。これらの関係は図 2.4 のように表現できます(「ユーザー」は割愛しています)。図2.4:安否確認と安否確認回答の関係「お知らせ」と「添付ファイル」、および「メッセージ」の関係性もここまでの議論とほぼ同じようになります。お知らせはテナントに複数持つことができるもので、 1 つのお知らせの中にはタイトルや本文といったテキストの情報に加え、複数の添付ファイルをアップロードできます。よって、「テナント」と「お知らせ」は一対多、「お知らせ」と「添付ファイル」も一対多となります。メッセージという機能では、ユーザー間で複数のメッセージのやりとりが行われるため、テナントとメッセージは一対多となります。メッセージには会話をグループでまとめたり話題ごとに切り替えたりするような機能はありません。これらの関係は図 2.5 のように表現できます。図2.5:お知らせと添付ファイルとメッセージの関係最後は「ロール」と「権限」です。ここは認可に関する部分です。「ユーザー」には、 **アプリ利用者** 、 **アプリ管理者** 、 **テナント管理者** の 3 つのロールのうちいずれか 1 つが割り当てられます。そのため、「ユーザー」に着目すると「ロール」は常に 1 つですが、対応関係は固定ではなく変更可能です。「権限」とは、 **安否確認を依頼できる権限** や **メッセージを送信できる権限** などがあります。 1 つの「ロール」には複数の「権限」が与えられ、また、 1 つの「権限」は複数の「ロール」に対し割り当てることができます。よって、これらの関係は多対多となり、図 2.6 のように表現できます。ただし、今回の anpi! においてはロールとそれに紐づく権限はあらかじめ決まっています。たとえば、「安否確認を依頼できる権限」はアプリ管理者「ロール」に与えられるとあらかじめ決まっています。システムの種類によっては、権限とロールの対応関係をエンドユーザーが動的にマッピングできる場合もありますが、今回の anpi! においてはそうではなく、静的な対応関係です。そのため、「権限」は「ロール」の一部と捉えることもできるでしょう。いったん、ロールと権限についてはこれ以上踏み込まず、のちほど認可を設計する段階で改めて検討します。図2.6:ユーザーとロールと権限の関係これですべてのエンティティ間の関係を検討できました。最後に各エンティティが持つべき属性を追記してまとめると図 2.7 のようになります。各ドキュメントを登録した「ユーザー」については、その登録時のユーザー名を記録するだけでよいという前提にして、各エンティティ内の属性として表現しています。図2.7:概念データモデル実際の業務では、開発中に変更が入ったり、構成を修正したりすることはよくあることですので、まずはベースとなるモデル図をまとめて随時ブラッシュアップしていくとよいでしょう。次節では、この概念データモデルを踏まえて、実際に Firestore で物理設計する方法を見ていきます。

## **2-4 Firestoreのデータモデルを設計する** 

Firestore のデータモデリングを行っていきます。概念データモデルの各エンティティとその間の関係を Firestore のコレクションとドキュメントの構造で表現するための考え方を紹介します。

### **2-4-1 RDBのデータモデリングとの違い** 

具体的なモデリングに入る前に、 RDB と Firestore を比較しながらデータモデリングの特性の違いを見ていきます。

RDB は一般的に利用されるデータベース・モデルで、複数のテーブルを用いてデータを保存します。 1 つのテーブルにデータを保存すると、冗⻑なデータが紛れ込む場合もあります。このときに、データの冗⻑性がなくなるよう適切にテーブルを分解することをテーブルの **正規化** と言います。正規化により、 RDB のデータは一貫性のある状態を保持しやすくなります。複数のテーブルにあるデータを横断してまとめたくなったときは、テーブルを **結合(ジョイン:Join)** します。結合とは、複数のテーブル間のデータに存在する共通の値を結合のキーとして、行を 1 つにまとめることです。

RDB はこのようにテーブルを分解しながらも、厳密な整合性を保証することを重視した思想となっています。そのため、 RDB ではスキーマ構造を事前に決定し、外部キーや一意制約など整合性を維持するための機能を備えています。トランザクション管理も得意で、 SQL を使って複数テーブルから一貫性を保ちながらデータを結合して取得したり、更新したりできます。 RDB ではこれらの強力な整合性担保の機能を最大限に活用するためにも、テーブルを適切に正規化することが求められます。一方、 RDB よりあとの時代に登場した NoSQL は、 RDB が重視した厳密な整合性担保よりも、スケーラビリティを優先した思想になっています。厳密な一貫性を守ることは、それだけ高いリソースが必要となります。たとえば、複数クライアントから同時に 1 つのデータを更新しようとする場合や、データの読み込み中に他の書き込みがあった場合でも一貫した結果を保証できる仕組みがあります。このような厳密な一貫性は、性能やスケーラビリティとのトレードオフによって成り立ちます。そこで、このような厳密性を一定程度緩めてでも、高い性能やスケーラビリティを重視したいという要望に応えるため、新しいデータベースの選択肢として登場しました。

NoSQL におけるデータの管理構造は、スキーマをあらかじめ定義したテーブル構造ではなく、 key-value ストアやドキュメント形式といったスキーマレスなものが採用されます。検索においても、複数のデータソースを結合することや、集計などのクエリ機能が十分でないという弱点もあります。近年は RDB と近いレベルのトランザクション機能を提供する NoSQL も増えてきていますが、一貫性を保証する機能自体は RDB ほど厳密でないことや、実装が複雑になってしまうことがあります。そのため、必ずしも正規化による分解が最適とは限らず、性能を重視する場合や厳密な整合性が不要な場合などに意図的に非正規化することがあります。 Firestore も NoSQL データベースの 1 つとして、このような性質を持っています。

RDB ではテーブルの正規化というある程度確立した理論に基づく設計手法があります。一方、 Firestore をはじめとする NoSQL は製品ごとの特性の違いも大きく、どのようにデータモデリングすべきかという決まった方法論はありません。本節でこれから説明する内容は、筆者らの Firebase 活用の経験をもとにして Firestore のデータモデリングの考え方を整理したものになります。最初に基本的なモデリングのパターンを紹介し、それらを関係の多重度(一対一、一対多、多対多)ごとにどのように適用させるか、という流れで説明していきます。

### **2-4-2 正規化と結合** 

データベースの種類に限らず、データ冗⻑性の排除、つまりデータ重複をなくすことは重要です。同じデータが複数あった場合、それを更新するときはすべてを同時に更新しなければ矛盾や不整合を引き起こしかねません。

Firestore のデータモデリングでは、各エンティティをどのようにコレクションに分離・配置するかを考える必要があります。理想的には、データが冗⻑でなく、シンプルな構造にすることが望ましいと言えます。しかし、前節「 2-4-1 RDB のデータモデリングとの違い」で述べたように Firestore は性能やスケーラビリティを優先するような思想に基づいているため、厳密な整合性が重要視されないユースケースでは、意図的に非正規化を選択することもあります。そこで、非正規化がどのような場面で役立つかのパターンを紹介します。その前に、非正規化の利点を理解する上で重要な **クライアントサイドジョイン(Client Side Join)** という結合方法について解説します。なお、本書では、以降 Firestore における **正規化** という言葉は、単に「 1 つのデータを 1 箇所にしか保存しないこと」という意味で使います。 **非正規化** は、「 1 つのデータを異なる複数の場所にコピーして保存すること」とします。結合が必要な機能の典型的な例として、チャットアプリのデータ構造を考えます。これは、複数のユーザー間でメッセージをやりとりする機能を持っており、今回のサンプルアプリである anpi! においても「メッセージ機能」として同等のものがあります。図2.8:チャットアプリの画面イメージこの機能に登場するのは、ユーザーエンティティとメッセージエンティティの 2 つです。ユーザーエンティティはユーザーのメールアドレスや氏名といった属性を持ち、メッセージエンティティはメッセージの本文や投稿日時、そして投稿者名といった属性があります。チャットアプリの画面上には、メッセージの本文と日時、さらに投稿者名を表示します。投稿者名はユーザーエンティティで管理しているユーザーの氏名と一致している必要があります。この例を RDB で実装した場合と、 Firestore で実装した場合のそれぞれで比較してみましょう。まずは RDB で実装した場合です。 RDB であれば次のような設計にするのがよさそうです。まずユーザーテーブルとメッセージテーブルを正規化し、それぞれ ID (id)列を主キーとします。ユーザーテーブルは氏名(name)列を持ち、メッセージテーブルはメッセージの本文(body)や投稿日時(created_at)を持ちます。

RDB において正規化されたテーブルを結合する場合、通常は外部キーを用います。外部キーとは 2 つのテーブルを結合するための共通のキーとなる列のことです。今回の場合は、メッセージテーブルに投稿者ユーザー ID (author_id)列を持たせ、外部キーとして「ユーザーテーブル」の「 ID 」列を参照する制約を与えます。こうすると、リスト 2.5 のような SQL でユーザーテーブルとメッセージテーブルを結合でき、必要となるメッセージの本文・投稿日時・投稿者名のすべての列を 1 回のクエリで取得できます。

●リスト2.5:テーブル2つを結合するSQL

SELECT messages.body       -- メッセージの本文 , messages.created_at -- メッセージの投稿日時 , users.name          -- ユーザー名(投稿者名) FROM messages INNER JOIN users ON messages.author_id = users.id -- メッセージとユーザーをキー値で結合クエリの処理はデータベース上で完結するため、フロントエンドは必要なデータをバックエンドに要求し、バックエンドはデータベースに問い合わせた SQL の実行結果を整形してフロントエンドに返却するだけです。フロントエンドは受け取った結果をそのまま画面表示に使えます。この処理の概要は図 2.9 のようになります。図2.9:RDBにおける結合(テーブルジョイン)

次に、同じチャットアプリを Firestore で実装した場合について考えます。 RDB の設計思想のままではうまくいかないということを理解してもらうために、まずは RDB のテーブルをそのままコレクションに置き換える発想で設計してみます。各エンティティはユーザーコレクションとメッセージコレクションの 2 つで扱います。 Firestore のドキュメントはドキュメント ID によって一意に識別されます。各ドキュメントが持つフィールド構成は RDB のテーブルの構成とほぼ同じです。ユーザードキュメントのフィールドはユーザーの氏名を持ちます。メッセージドキュメントのフィールドにはメッセージの本文、投稿日時、そして投稿者に対応するユーザードキュメントのドキュメント ID を持つ設計とします。

Firestore のクエリ機能では、 SQL のようなデータベース側でコレクションを結合することはサポートされていません。そのため、もしデータの結合が必要な場合は、フロントエンド側から各コレクションへの問い合わせをそれぞれ実行し、フロントエンド側で結合する必要があります。メッセージドキュメントとユーザードキュメントの集合をそれぞれ取得し、各ドキュメントに含まれるキーとなるフィールド値(今回の例では投稿者のドキュメント ID )を比較して一致するものだけを抽出する処理を自前で実装します。具体的な実装方法の一例としては、以下のような流れになるでしょう。

1メッセージコレクションからすべてのドキュメントを取得する。 2メッセージドキュメントごとにループし、次の処理をする。

1メッセージドキュメントから投稿者 ID を取り出す。 2ユーザーコレクションから、ドキュメント ID が取り出した投稿者 ID であるユーザードキュメントを 1 件取得する。

3メッセージドキュメントとユーザードキュメントを結合し、 1 件のメッセージを表現するデータに結合する。フロントエンド側、つまりクライアント端末上で結合することから、この方法を **クライアントサイドジョイン(Client Side Join)** と呼びます。 RDB であれば、このような実装はネステッドループ結合としてデータベース側で処理されるため、開発者は普段あまり意識しなくて済むようになっています。クライアントサイドジョインの処理概要は図 2.10 のようになります。図2.10:Firestoreにおけるクライアントサイドジョインクライアントサイドジョインを行う際、複数のコレクションのドキュメントを横断的に読み取る必要があるため、次のような問題が生じます。まず、ループしながらドキュメントをリクエストしているため、リクエスト回数がデータ件数に比例して膨らんでしまいます。いわゆる N+1 問題のような動きとなり、ネットワーク帯域の使用量やオーバーヘッドが大きくなります。データ件数が増えるにつれて計算量も大きくなっていくため、これをフロントエンド側、つまりエンドユーザーのブラウザー上で実行すると、ユーザーマシンの CPU やメモリリソースを逼迫し、画面描画の遅延など UX の低下につながるおそれがあります。これが、もし 1 件ずつの結合で済む場合は、クライアントサイドジョインも大きな問題にはなりません。たとえば、ある一覧を表示するためにコレクション内のドキュメントを表示し、ユーザーのそのうちの 1 件を選択させて詳細を表示するような場合です。ユーザーが選択してはじめて、その 1 件のみを取得し結合するので上記のような問題は起きません。このように、結合先を遅延して読み込むような場合であれば、クライアントサイドジョインも有効な選択肢となります。

## ■非正規化とデータ整合性 

RDB では、データベースが自動的にデータを結合してくれるため、正規化するメリットがあります。しかし、 Firestore では正規化することがデータを結合しやすくすることにはつながりません。そのため、クライアントサイドジョインを避けるために、意図的に非正規化することが有効になります。チャットアプリの例に戻り、これを非正規化させてみましょう。具体的には、メッセージコレクション側に投稿者名というフィールドに、ユーザーエンティティが持つ氏名フィールドのデータをコピーして保存します。図 2.11 は、メッセージコレクションにauthorというフィールドに、投稿者ユーザーに対応するユーザードキュメントをマップ型で保存した場合の処理概要です。今回はマップ型を使っていますが、他の実装案として、author_idとauthor_nameのようにフィールドを展開して持たせることも考えられます。

##### 図2.11:Firestoreにおける非正規化複雑なクライアントサイドジョインがなくなり、非常にシンプルな流れになりました。チャット画面に必要なデータは、すべてメッセージドキュメントにそろっているため、メッセージコレクションの中だけを見れば手に入ります。正規化していたときと比べると、ドキュメントの読み取り回数自体も減り、むだなドキュメントの読み取りも発生しません。もちろん非正規化を選択したことにより、データの重複部分についての整合性の問題が生じます。チャットアプリの例ではそれほど頻繁に発生することはなさそうですが、ユーザーの氏名データを重複して保持しているため、もし氏名が変更になった場合、データ不整合となるおそれがあります。ユーザードキュメントの氏名は更新されているのに、過去のチャットメッセージの投稿者名が変更前の氏名のまま、というようなことが起こり得ます。非正規化する場合は、この整合性の問題との向き合い方を決めておく必要があります。整合性を担保するにはユーザードキュメントの更新時に、該当するメッセージドキュメントの投稿者フィールドにもその更新を反映させる必要があります。この場合、 2 つの方法があります。 

1 つはユーザードキュメント更新時に、 Firestore のトランザクション機能を用いて、アトミックに複数コレクションを更新する方法です。この方法は、もしメッセージドキュメントへの更新に失敗した場合はユーザードキュメントの更新もロールバックされるため、常に整合性が保証されます。 NoSQL はトランザクション機能が弱いと最初に述べましたが、実は Firestore は強い整合性を担保した読み書きの機能を有しています。複数ドキュメントを同時に更新する際は、このトランザクション機能を用いて整合性を保証することを推奨します。もう 1 つの方法は、ユーザードキュメントの更新時トリガー (onUpdateトリガー)を利用して Functions を実行し、バックエンドで各メッセージドキュメントとデータ同期をするというものです。この方法のメリットは、もしユーザードキュメントがさまざまな機能から更新されるような場合に、メッセージドキュメントへのデータ同期処理を個々の開発者が意識しなくてもよくなるため、実装漏れをなくせるという点です。先のトランザクションを利用する場合、各機能がユーザードキュメントを更新する際にメッセージも同時に更新するという開発上のルールを強いることになります。このようなルールは暗黙知になりやすく、テストケースからも漏れてしまいがちです。更新時トリガーであれば、ユーザードキュメントが更新された際にメッセージドキュメントへのデータ同期処理が少なくとも 1 回実行されることが Functions の機能によって保証されます。これは、結果整合性の保証になります。一方、この Functions を使ったバックエンドのデータ同期のデメリットとしては、トリガーを多用すると処理のつながりが見えにくくなるという点があります。また、当然トランザクションが分かれてしまうので、 Functions のデータ同期処理が終わるまでの時間はエンドユーザーから見ると更新前のデータが見えてしまい、一貫性がない状態になります。データ同期処理は状況にもよりますが、 Functions がコールドスタートしてしまうと数秒程度かかる場合もあります。さらに、次のようなデータ不整合が発生するリスクもあります。

- ・プログラムバグなど予期せぬ不具合によって Functions のデータ同期処理が失敗した場合に、メッセージドキュメントが古いデータのままになってしまう。 

- ・ほぼ同時に同じユーザードキュメントが複数の操作によって更新された場合に、トリガー実行順序が保証されないため、古い方のデータで同期されてしまう(いわゆる「追い抜き」や「あと勝ち」が発生する)。このように、非正規化を選ぶと読み取り時のシンプルさを手に入れた一方で、更新時のデータ整合性を保つための処理が複雑になります。読み取り頻度に比べて、書き込みの頻度が圧倒的に少ないようなユースケースではメリットを強く活かせるでしょう。もし、データ同期が不要な場合には非正規化がとくに有効な選択肢となります。たとえば、「メッセージの投稿者名欄は、そのユーザーの投稿時点の名前を表示する」というのが正しい仕様であったとします。このときはユーザードキュメント側の更新をメッセージ側に反映させるべきではなく、メッセージドキュメント側で別に投稿者名を持たせる設計が正解になります。つまり、メッセージドキュメントのauthorフィールドは、その時点のユーザードキュメントのスナップショット(断面)データという扱いになります。スナップショットデータとして保持するようなユースケースでは、積極的に非正規化すべきです。最後にもう 1 点、非正規化を選ばざるを得ないユースケースを紹介します。それは、メッセージドキュメントとユーザードキュメントのアクセス制御の要件が異なる場合です。たとえば、ユーザードキュメントに本人しか参照できないような重要な個人情報(住所や年収など)を扱っていたとします。一方で、チャット画面は誰でもアクセスできるとします。このとき、正規化とクライアントサイドジョインは採用できません。なぜなら、セキュリティルールによりユーザードキュメントは本人しかアクセスできないように制御されているためです。 Firestore のドキュメントは特定のフィールドのみ隠す、といった制御はできません。この場合はメッセージドキュメント側に「投稿者名」フィールドを持たせるべきでしょう(もしくは、ユーザードキュメントを分割し、ユーザー公開情報とユーザー個人情報にコレクションを分ける方法もあります)。 

### **2-4-3 Firestoreで関係を表現するための機能** 

Firestore ではキー項目を使った結合は直接サポートされていませんが、関係を実現するのに役立つデータモデリング機能を 3 つ紹介します。

1参照型 2サブコレクション 3コレクショングループクエリ

## ■参照型 

Firestore では、ドキュメント内のフィールドに「参照」という型を持たせることができます。これにより、 RDB における外部キーと似た役割を果たすことができます。参照型は、あるドキュメントへのパス構造を表すデータ型です。これをチャットアプリに使うとすると、「メッセージドキュメント」に投稿者ユーザーの「ユーザードキュメント」への参照を持たせることができます。具体的には authorというフィールドに/users/u1といったドキュメントパスを保存します。これにより、「メッセージドキュメント」を読み取れば、その投稿者ユーザーのドキュメントパスも同時に取得できます。そのため、必要になったタイミングで簡単にクエリを発行し、「ユーザードキュメント」を読み取ることができます。

##### 図2.12:ドキュメントの参照型を用いた関連付けもちろん参照型を使わずに、ドキュメント ID の値を文字列型フィールドに保存しても問題ありません。author_idというフィールドにu1という値を保存する方法です。この場合と比較すると、参照型の方が SDK やセキュリティルールからアクセスする際に、 message.authorというコード表現で参照先のドキュメントデータにアクセスできるため、シンプルに表せます。一方、セキュリティルールなどでドキュメント ID の値そのものを使いたい場合は、参照型だとパス表現の末尾から ID 値を抜き出す手間がかかります。どちらでも大きな差はありませんが、ドキュメント ID 値を直接保存したくなった場合は、参照型で代替できないかも検討してみるとよいでしょう。このように参照型は RDB の外部キーと似ていますが、注意点があります。参照型を用いても Firestore のデータベース側で結合できるようになるわけではなく、依然としてクライアントサイドジョインが必要です。ドキュメントの読み取りも、メッセージドキュメントとユーザードキュメントはそれぞれで 1 回ずつとカウントされるので、データ件数が変わらなければ読み取り回数自体は同じです。また、 RDB の外部キーはデータの整合性を保証します。参照先に存在しない値を外部キーとして登録できません。一方、 Firestore の参照型は、指定したパスが存在しないドキュメントであったり、ユーザーコレクション以外のドキュメントを参照させたとしてもエラーになったりはしません。あくまで、別のドキュメントの参照をシンプルな形で持っているだけということです。参照関係の不備といったデータ不整合が発生しないようにするためには、セキュリティルールでバリデーションしたり、別途アプリケーションプログラム側で制御したりするなど、整合性を保証する必要があります。これらの注意点を理解し、適切に扱うことで、参照型をより効果的に活用できます。参照型がうまく利用できるかどうかを見分ける 1 つの基準として、エンティティ間の依存関係の有無に着目する方法があります。依存関係とは、親子関係となっているようなエンティティの関係において、親エンティティが存在しなければ子エンティティが存在しないようなものです。たとえば、請求書などおける、「ヘッダー」と「明細」パターンが代表的な親子関係です。親である「請求書」エンティティが存在しなければ、その子である「明細」エンティティは存在しません。「請求書」が削除されると、同時にその「明細」もなくなってしまうと考えるのが自然です。非依存関係は、逆にこの 2 つのエンティティ間にそのような依存が存在しないことです。先ほどの例の「メッセージ」と「ユーザー」の関係は非依存で、それぞれが独立して存在しています。「ユーザー」が削除されたとしても、過去にその「ユーザー」が投稿した「メッセージ」は実体として残り続けるという考え方です。もちろん、逆も同様で、「メッセージ」を削除しても「ユーザー」には影響がなく、よってこれらは非依存関係です。参照型は、非依存関係の表現に役立つ場合が多いです。これは、クライアントサイドジョインが有効な例としても説明した次のようなケースと相性がよいです。つまり、あるエンティティのドキュメントを一覧で表示する UI があり、そこから 1 つのドキュメントをユーザーが選択した時点で参照先のドキュメントを取得し詳細を表示するような場合です。クライアントサイドジョインも 1 件ずつ、必要最小限にしか発生しないのでむだがありません。一対多で非依存な関係の場合は、積極的にこの UI パターンを狙って仕様を固めていくことでシステムと Firestore の相性がよくなります。 

## ■サブコレクション 

Firestore の基本構造は、コレクションとドキュメントが入れ子構造であり、コレクション配下のドキュメントがさらにサブコレクションを持てるというものでした。この基本構造は、依存関係がある場合のエンティティで、とくに一対多を表現する場合に有効です。たとえば、「請求書」と「明細」のような親子関係の場合、「請求書」コレクションのドキュメントにヘッダー情報(「請求先」や「請求日」など)を保存しつつ、「明細」サブコレクションを紐づけます。「明細」ドキュメントに具体的な請求項目(「品名」や「金額」などの 1 つの明細)を保存すると、このような親子関係をうまく扱えます。サブコレクションを使わずに、「請求書」ドキュメントに明細フィールドを配列で保持するのはどうでしょう。ドキュメントの読み取り数はサブコレクションを使うよりは大きく減らせます。しかし、基本的にはエンティティを分けてサブコレクション化しておくことを推奨します。 1 つのドキュメント内に配列など複雑な構造のフィールドを持つと次のデメリットが生じます。

- ・明細の追加や削除のたびに配列操作が必要となり、実装が複雑になる。 

- ・セキュリティルールで配列内の値に対するバリデーションを書くことが困難。 

- ・ドキュメントサイズが大きくなり、 1 度のドキュメント取得の際のオーバーヘッドが大きくなる。 

- ・ドキュメントサイズが大きくなり、 Firestore のドキュメントサイズ上限の制約に触れてしまうリスクが発生する。このようにドキュメントフィールド上の配列型や Map 型は、その中の要素を頻繁に更新したり、セキュリティルールでチェックしようとすると扱いづらくなりがちです。 

Firestore では、エンティティ同士を結合させるという発想から離れ、さまざまなエンティティ間の関係をどのように階層構造に落とし込むか、という視点を持つことが重要です。なるべく結合(クライアントサイドジョイン)を選択せずに、サブコレクションをうまく利用する形にできると Firestore の特性を活かしやすくなります。

## ■コレクショングループクエリ同名のサブコレクションを横断してドキュメント検索できる、 **コレクショングループクエリ** と呼ばれる機能があります。うまく活用できると、結合を回避できる便利なものであるため、ここで紹介します。例として、チャットアプリを次のように少し拡張したユースケースを考えます。話題ごとに「チャンネル」があり、その中で「ユーザー」が「メッセージ」をやりとりできるとします。チャンネルはたとえば、ニュースチャンネルや趣味チャンネルのように、話題ごとに用意されているイメージです。もしニュースチャンネル内にあるすべてのメッセージを取得したい場合は、そのチャンネルドキュメントのサブコレクションパスを検索します。これは、今まで紹介した通常のクエリで実現できるため、ある1つのチャンネル内のメッセージの一覧を取得する場合はとくに問題ありません。しかし、チャンネルを横断したクエリを発行したい場合には問題が発生します。たとえば、田中さんという名前のユーザーが投稿したすべてのチャンネルのメッセージを一覧化する、という場合です。通常のクエリで実装すると、次のようなものとなります。まずはチャンネルコレクションからすべてのチャンネルドキュメントを取得します。次に、それらの各チャンネルドキュメントのメッセージサブコレクションの中を調べ、authorフィールドが田中さんであるドキュメントを絞り込みます。最後に、これらの複数のドキュメントをまとめてフロントエンドに返します。これはチャンネルとメッセージの間でクライアントサイドジョインが発生してしまい、非効率な処理となっています。このようなときは、コレクショングループクエリが有効です。コレクショングループクエリは、 Firestore のコレクション名(今回の例だとmessages)が同じサブコレクションに対して、横断的にドキュメント検索できる機能です。これを使うとチャンネルドキュメントにクエリを発行せずに、直接「田中さんが投稿したメッセージ」を検索できます。図2.13:コレクショングループクエリコレクショングループクエリの注意点としては、サブコレクションの名前が一致していれば検索対象となってしまうことです。チャンネルドキュメント以下に制限できないため、サブコレクションの名前をつける際には注意が必要です。 

●リスト2.6:コレクショングループクエリですべてのmessagesサブコレクションから検索するソースコード例

import { collectionGroup, getFirestore, query, where, getDocs } from 'firebase/firestore'; const db = getFirestore(); const messagesGroup = collectionGroup(db, 'messages'); const q = query( messagesGroup, where('author', '==', '田中'), ); const snapshot = await getDocs(q);

### **2-4-4 データモデリングパターン** 

参照やサブコレクションをうまく活用すると Firestore においても正規化のメリットを活用しやすくなります。まずは、 Firestore の機能をうまく活用して正規化を目指しつつ、結合の要件やデータアクセスの粒度で問題があれば、非正規化も視野に入れて検討するとよいでしょう。ここからはデータモデリングのパターンを、関係の多重度に着目してどう使い分けるのがよいかを整理します。

## ■一対一一対一の関係を表現するために使えるデータモデリングパターンを図 2.14 にまとめます。エンティティ A とエンティティ B があり、それらは一対一に紐づく関係であるとします。エンティティ A に対応するデータを A コレクション、エンティティ B に対応するデータを B 

#### コレクションのドキュメントでそれぞれ保存するとした場合、図中の 3 種類の選択肢があります。 

##### 図2.14:一対一の関係を表現するFirestoreのデータモデリングパターン基本的には一対一の場合は正規化するかどうかを決めるだけです。正規化する場合は参照型を使うとシンプルになり、非正規化する場合は一方のドキュメントの Map 型等でもう一方のドキュメントデータのコピーを保存します。ただ、一対一の場合に限り、図 2.14 の左で示すような共通 ID という方式も役立つためここで紹介します。これは正規化の一種ですが、具体的には次のような手法になります。 

A コレクションと B コレクションが一対一である場合、 A コレクション内のあるドキュメント a1 (ドキュメント ID がa1とします)に対し、 B コレクション内のただ 1 つのドキュメントが紐づくことになります。この B コレクション内のドキュメント ID もa1にそろえてしまうという方法になります。

Firestore のドキュメント ID は、ドキュメント登録時に自動採番せずに明示的に指定することもできます。また、ドキュメント ID の一意性はそのコレクション内のみで保証されるため、異なるコレクションであれば同じ ID を別のドキュメント ID として使うことができます。これにより、同じドキュメント ID を持った異なるドキュメント同士を一対一の対応関係にあると見なせるようになります。これが共通 ID 方式です。ある一方のドキュメント ID が分かれば、もう一方のドキュメントパスも容易に取得できるため、実質的にこれで結合できることになります。

RDB において主キーが同一の一対一のエンティティ同士は、 1 つのテーブルにまとめるのが普通であり、このように分割することはあまり一般的ではありません。一方、 Firestore のコレクションにおいては共通 ID 方式をとるメリットは、次の 2 点です。・一対一という関係を保証できる。・それぞれのコレクションに異なるアクセス制御を定義できる。

1 つ目のメリットについてですが、これは Firestore のコレクション内にその ID を持つドキュメントは 1 つしか存在できないという一意性制約を利用することを意味します。フィールドに参照型として持ってしまうと、プログラムバグなどで 2 つのドキュメントが単一のドキュメントを参照するようなケースも理論上は発生してしまいますが、共通 ID 方式であればその心配はいりません。ただし、ドキュメント ID はあとから変更できないため、一対一の関係性が固定である場合にのみ採用できます。

2 つ目のメリットについてですが、実はこれはメリットというよりも、 Firestore のセキュリティルール上の制約とも言えます。 Firestore のセキュリティルールは、 1 つのコレクションに対して異なるアクセス制御の条件を適用できません。たとえば、ユーザードキュメントに氏名やメールアドレスなどを保存していたときに、あとから住所や生年月日といった個人情報も保存したくなったとしましょう。氏名やメールアドレスは公開情報ですが、個人情報は本人や管理者しかアクセスできないという制御を施す必要があります。実は、セキュリティルールではこのような要件を満たす条件を書くことができません。 1 つのドキュメントは、セキュリティルールの条件を満たしていれば「読める」、そうでないなら「読めない」のどちらかです。一部のフィールドだけを非公開にするような制御はできません。そこで、ユーザー公開情報コレクションと、ユーザー個人情報コレクションに分けつつ、それぞれを別ドキュメントで扱うようにします。こうすると、ユーザー公開情報コレクションには誰でも読めるような緩い条件にしつつ、ユーザー個人情報コレクションのほうだけ厳しい条件で守ることができます。このとき、これらの各ドキュメントのドキュメント ID を共通のユーザー ID としておくことで、別コレクションに分割しながらも容易に結合できるようになります。このように、共通 ID 方式は、ある 1 つのエンティティを表現したものの、扱うデータのセキュリティ要件が異なりアクセス制御を分けなければならない場合にとくに有効です。

## ■一対多一対多の関係を表現するために使えるデータモデリングパターンを図 2.15 にまとめます。 1 つのエンティティ A に対し、複数のエンティティ B が紐づく関係です。図2.15:一対多の関係を表現するFirestoreのデータモデリングパターン 

Firestore のもっとも基本的な構造であるサブコレクションを利用する代表的なパターンです。この方法で何か問題があった場合は、参照型や非正規化の選択肢も検討しましょう。

## ■多対多多対多の関係を表現するために使えるデータモデリングパターンを図 2.16 にまとめます。 1 つのエンティティ A から見ると複数のエンティティ B が紐づき、また 1 つのエンティティ B から見ても複数のエンティティ A が紐づく関係です。図2.16:多対多の関係を表現するFirestoreのデータモデリングパターン 

RDB であれば中間テーブルを切り出して、両テーブルの主キーのマッピングを保存することが一般的です。 Firestore の場合でも同様に、マッピングを保存する結合コレクションという方法が使えます。図の一番左のように、ドキュメント内にそれぞれのマッピングを保存するという方法です。 A→B コレクションという形で、 A コレクションから見た B ドキュメントの対応関係を参照型の配列として保存しています。例として、ドキュメント a1 のb-refsフィールドに/b/b1と/b/b2という B コレクション内の 2 つのドキュメント b1 、 b2 への参照を保存しています。結合の保存方法もこれまで紹介したパターンを組み合わせる形でいろいろ考えられます。主となるエンティティが決まっていれば、共通 ID 方式を使うとシンプルになります。主となるコレクションのドキュメント ID と、結合コレクションのドキュメント ID を同じ値にしておき、主となるドキュメントから見て紐づいているエンティティのドキュメントの参照を配列で保存します。多対多を表現すると、どうしても正規化・非正規化いずれの場合でもドキュメント内に配列で情報を持つ必要が出てきます。この場合、そのエンティティに紐づく相手エンティティの数が増えると、ドキュメントサイズも比例して大きくなるため注意が必要です。ドキュメントが大きくなると、それだけ不要なデータも一緒に取得することになり、オーバーヘッドが発生します。そのような場合は、図の一番右のように、サブコレクションを使ってそれぞれ持つ方法も考えられます。こうすることで、ドキュメントサイズは一定で済みます。ただし同一ドキュメントのコピーがたくさん作られてしまうので、データ整合性についての検討を忘れないように気を付けましょう。以上がデータモデリングについての説明になります。ここで紹介したパターンがすべてというわけではありませんが、ベースとなる考え方や基本的な実現方法としてまずは押さえておくとよいポイントは網羅したつもりです。あとは実際の要件によってはこれらの考え方をそのまま使ったり、場合によっては組み合わせることもありえます。さまざまな観点でトレードオフが発生するので、うまく管理して落としどころを見つけることが大切です。

## **2-5 認証を設計する** 

ここからは、実際に anpi! の要件に基づいて Firestore のデータモデリングを考えていきます。本節では、各要件に従いながらそれを実現するセキュリティルールを順に検討し拡張していくような流れで説明していきます。説明の際は注目しているポイントに絞ったルールのみを記載していきます。そのため、必要に応じて、本節の話はサンプルコードの firestore.rulesの完成形も適宜参照しながらお読みください。最初に認証や認可といったセキュリティ要件を満たす観点から検討を始めます。なぜサービスのコアとなる機能や画面からではなく、ユーザーに見えない部分であるセキュリティから考えるのかというと、それは Firestore のセキュリティ機能はコレクションやドキュメントのデータ構造を利用して実装することになるためです。また、業務機能であればユーザービリティや機能配置を多少工夫することである程度柔軟に要件を組み替えることもできますが、セキュリティ要件は原則抜け漏れが許されない領域です。画面や機能を実装したあとにセキュリティ要件を組み込む方法をとってしまうと、あとで大きな手戻りとなる可能性が高くなります。そこで、早い段階からセキュリティ設計と、機能に関する設計を同時に満たせるように考慮していくことにします。本節でまず認証について扱い、次節以降で認可について扱います。認証は、ユーザーが本当に本人であるかどうかを確認し、本人であればシステムへのログインとサービスの利用を許可することです。認証の仕組み自体は Authentication によって提供されるため、パスワードを安全に保存したりセキュアに通信したりすることなどは、開発者が意識する必要は基本的にありません。一方で、認証されたユーザーのみデータや機能にアクセスできることは開発者の責任で実現する必要があります。バックエンド側に API サーバーを配置する構成の場合は、リクエストがあった時点でそのユーザーが認証済みかどうかを判定し、認証済みでない場合はエラーを返すような処理を API サーバーで行います。そのため、データベースは API サーバーとの通信だけ許可すればデータは守られることになります。一方、 Firebase の場合はバックエンド側のデータベースである Firestore にクライアントから直接アクセスする経路となります。そのため、認証されていないユーザーはデータにアクセスできないよう Firestore のセキュリティルールで守る必要があります。

Firestore のセキュリティルールは標準で Authentication と連携が可能です。アクセスしているユーザーのリクエストから Auth オブジェクトを参照して認証に関する情報を取得できます。セキュリティルール上ではrequest.authというオブジェクトにリクエストしたユーザーの認証情報を保持したトークン(request.auth.token)が保存されています。この認証情報のトークンには次のような情報が含まれています。たとえば、認証に成功したメールアドレス(auth.token.email) や、アカウントに紐づけられている電話番号が存在していればその番号(auth.token.phone_number)などです。ここで、もし認証されていないユーザーだった場合、この Auth オブジェクトは存在せず nullとなります。つまり、認証済みユーザーかどうかのチェックは Auth オブジェクトが存在している(request.auth != nullである) ことを確認すれば問題ありません。メールアドレスを用いて本人確認の仕組みを構築する場合、そのメールアドレスが本当に本人のものであるかどうかを確認することが重要です。 Authentication では、新規ユーザーを登録する際に、登録したメールアドレスに対してメールを送信し、本人確認をします。送信されたメールに URL が記載されているので、そのリンクをクリックすることでメールアドレスの疎通確認がとれたと見なされます。この疎通確認を済ませると、auth.token.email_verifiedというフィールドの値がtrueになります。つまり、セキュリティルール上でも常にこの値のチェックを一緒にすることが大切です。以上をまとめると、認証をチェックするためのセキュリティルールはisAuthenticated関数によって、次のように実現できます。

●リスト2.7:認証済み、かつメールアドレスが検証済みのユーザーしか読み書きができないセキュリティルール

function isAuthenticated() { return request.auth != null && request.auth.token.email_verified; } match /<some_path> { allow read, write: if isAuthenticated(); }

なお、セキュリティルールではこのように関数として処理を切り出すことができます。認証や認可のチェックは、何度も登場することになるため、なるべく再利用可能な部品単位で開発していくと見通しがよくなります。このように Authencation とセキュリティルールを組み合わせて、認証済みのユーザーに対してのみアクセスを許可する処理の概要を図 2.17 に示します。図2.17:認証の実装方式

Authentication にユーザーアカウントを登録すると、一意で不変のユーザー ID (以下、本書では単に **UID** と記載します)が与えられます。この UID は Auth オブジェクトからauth.uidとして取得できるので、ユーザー単位のセキュリティルールを構築する際には便利です。本書でも、このあとに認可制御を設計するときに UID は重要な役目を果たしますので覚えておきましょう。ユーザーアカウントは Firebase の管理コンソールから手動で登録することもできますが、通常はシステム側でユーザー登録画面のような機能を提供することが多いと思います。 Firebase Authentication SDK を用いると、ユーザーアカウントの登録だけでなく、認証処理の移譲や、認証状態の確認など便利な機能が使えます。フロントエンド側のフレームワーク(たとえば、 React や Vue.js )に組み込むことで、バックエンド側の処理は Authentication に任せつつ、画面の見た目だけを作ればよいことになります。認証チェックも簡単に行えるので、ログインしていないユーザーには機能のページを見せずログイン画面にリダイレクトさせるような処理も実装しやすくなります。このように、 Authentication を活用することで認証機能は簡単に実現できます。 Authentication は非常に軽量ですので、 Firestore など他の Firebase サービスを使わず単独でも使えます。そのため、バックエンド側に自前の API サーバーを配置する構成であっても、認証だけ Authentication を採用するような場合もあります。手軽に認証を組み込みたいときはぜひ活用を検討してみてください。

## **2-6 認可を設計する** 

### **2-6-1 アクセス制御のパターン** 

認可は、ユーザーがその機能に対する権限を持っているかどうかを確認し、権限がある人にのみ機能やデータへのアクセスを許可することです。システムは一般のユーザーや管理者、運用担当者などさまざまな役割の人が使うので、役割によって使える機能やアクセスできるデータを限定したいというケースは多いです。その判断基準も複数あることが多く、そのぶんだけアクセス制御を実現する処理のパターンを設計しなければなりません。そのため、まずは設計対象システムのアクセス制御のパターンを洗い出し、パターンごとに制御方式を検討するとスムーズに設計できます。今回の anpi! においても同様の流れで設計します。認可は、認証における Authentication のようにそれに特化した単体の Firebase 標準サービスはありません。そのかわり、 Firestore や Storage などの各製品サービス側にセキュリティを担保するための機能が用意されています。 SPA 構成の場合は、ブラウザーから直接 Firestore へアクセスされる中でどのようにデータを守るかを検討することになります。なお、 Storage についても同様にセキュリティルールで守る必要がありますが、 2 章では Firestore のセキュリティルールに限定して説明します。 Storage 側のルールもほぼ同等となりますので、 3 章の開発編の中の「 3-4-2 Storage 用のセキュリティルールの実装」にて説明します。「 2-3-1 要件を整理する」で整理した機能要件の中から、アクセス制御に関するものだけを抜き出すと、次のようになります。

- ・マルチテナント構成 

- ・テナント内の情報はテナントに所属しているユーザーのみアクセスできます。 

- ・ 1 つのテナントには複数のユーザーが所属し、 1 人のユーザーは複数のテナントに所属可能です(たとえば、会社テナントと家族テナント)。 

- ・メッセージ 

- ・メッセージはテナント内の全ユーザーが登録でき、すべてのメッセージを一覧形式で参照できます。 

- ・ユーザープロフィール 

- ・ユーザーは自分自身の氏名、所在地を変更できます。 

- ・ユーザーは別のテナントに切り替えることができます。 

- ・認可 

- ・ユーザーには 1 つのロールが付与され、ロールごとに決まった権限で機能を利用したりデータにアクセスしたりできます。 

- ・ロールは **アプリ利用者** 、 **アプリ管理者** 、 **テナント管理者** の 3 つです。 

- ・アプリ利用者は、安否確認回答ができ、メッセージの投稿ができます。また、お知らせの閲覧ができます。 

- ・アプリ管理者は、アプリ利用者ができることに加え、安否確認を登録したり、安否確認回答を確認したりできます。また、お知らせを登録できます。 

- ・テナント管理者は、アプリ管理者ができることに加え、ユーザーの登録や変更ができます。この機能要件から、 3 通りのアクセス制御パターンを実現しなければならないことが分かります。 

1 つ目は、テナントベースのアクセス制御です。マルチテナント構成となるため、ユーザーは常に自分が所属するテナントに対してサービスを利用します。テナントとユーザーが多対多の関係であり、操作対象のテナントを切り替えるということにも注意が必要です。 2 つ目は、ロールベースのアクセス制御です。アプリ利用者、アプリ管理者、テナント管理者の 3 つのロール単位でアクセスできる機能が異なります。

3 つ目は、個人ベースのアクセス制御です。ユーザーは「自分自身」の情報を変更できるということは、ユーザーひとりひとりに紐づく情報があり、それらは本人のみアクセスできるという制御が必要ということです。これらは独立したルールです。すべてのアクセス制御チェックをクリアした場合のみ機能やデータへのアクセスを許可し、いずれか 1 つでも違反した場合はアクセスを拒否します。

### **2-6-2 認可設計の検討ポイント** 

認可を実装するための具体的な方法を見ていきましょう。認可の流れは次のようになります。

- 1ユーザーがブラウザー経由で Firestore のデータに対してアクセスを要求します。アクセスの種類には、参照、書き込み、削除などいくつかの操作の種類があります。 

2システムはユーザーが持つ権限情報を参照します。

- 3ユーザーが持つ権限でデータへのアクセス可否(そのデータに対して要求した操作が許されるか)を判定し、許されない場合は拒否します。このとき、アクセス制御を実現する上での重要な検討ポイントは次の 2 点です。・アクセス可否の判断はどこで実装するか。 

- ・ユーザーが持つ権限情報(たとえば、所属テナントや保持しているロールなど)はどこに保持するか。 

1 つ目の「アクセス可否の判断はどこで実装するか」を考えます。 Firestore の権限制御はセキュリティルールによって実現するのがベストプラクティスです。もしセキュリティルールを使わずにアクセス制御のロジックを自前で実装しようとした場合、処理はバックエンド側で実行する必要があります。そこで、 Functions で HTTP 呼び出し可能な API を公開し、その中で制御を実装するという方法が考えられます。この方法のメリットは、 Functions 内のビジネスロジックで任意のチェックを実装できることです。 Firestore のセキュリティルールは決められた構文でルールを記述する方式のため、複雑なルールは実現困難だったり、場合によっては不可能だったりします。自前で実装する場合は、そのような問題は起きません。一方、デメリットは、ロジックの重複や抜け漏れなどが紛れ込む危険性が生じる点です。結果として、セキュリティリスクの発生やメンテナンス性の低下につながりかねません。クライアントから直接アクセスできる BaaS であることの利点も損なわれるため、この方法だけに推奨しません。一方、ビジネス上の要件から負荷の高い処理や機密情報を扱う処理をフロントエンドではなくバックエンドで行うべき場合もあります。そうしたときに Functions を使う場合、必ず認可の制御を

Functions の処理の中に含め、個別に対応するようにしましょう。セキュリティルールはあくまでフロントエンドから直接ユーザーがアクセスする場合に可能な制御であり、 Functions などバックエンドで実行する場合、セキュリティルールは無効となります。クライアントからの直接アクセスという大多数のアクセスパターンをセキュリティルールでカバーし、カバーできないケースは Functions に個別のアクセス制御を実装するのがよいでしょう。

2 つ目の「ユーザーが持つ権限情報はどこに保持するか」という点を考えます。ユーザーのアクセス権限の制御をいかに正しく安全に実装できたとしても、肝心のユーザー権限の変更を簡単に許してしまうとセキュリティリスクとなります。たとえば、悪意あるユーザーが自分自身に管理者ロールを付与することが簡単にできてしまうようでは意味がありません。権限情報それ自体も安全な方法で管理しなければなりません。このような場合、 Authentication のカスタムクレームと呼ばれる機能が便利です。カスタムクレームとは、 Authentication のユーザーアカウントに独自のカスタム属性を保持できる機能で、データ構造は単純な key-value 形式のオブジェクトとなっています。カスタムクレームは、まさにこのようなユーザー単位でのアクセス制御を実現するための機能として設計されており、権限情報を保持させるのにうってつけです。カスタムクレームは Firestore のドキュメントデータと違ってクライアント側からは直接変更できません。 Functions などから Firebase Admin SDK を使ってバックエンド側でしか更新できないため、安全に管理できます。また、セキュリティルールからカスタムクレームを参照することも簡単にできるため、カスタムクレームに基づいてアクセス制御を判定することでルールがシンプルに書けるようになります。注意点としては、カスタムクレームは 1,000 バイトというサイズ上限があるため、要件によっては不十分な可能性があります。たとえば、ユーザーが複数の権限情報を持つことができる要件だった場合、それらをカスタムクレーム上に配列で持たせる方法が考えられますが、その場合は持てる権限数の制約が発生します。カスタムクレームを使う場合はそのデータサイズが一定量でとどまるような仕様にしておくと安全です。そのため基本的にはカスタムクレームでうまく扱う方法を検討し、扱いきれないような場合は別途 Firestore のコレクションに管理するなどの代替手段を使うのがよいでしょう。以上のような方針に基づき、基本的には Firestore のセキュリティルールとカスタムクレームをうまく使って、 3 つのアクセス制御パターンを実現する方法を見ていきます。・テナントベースのアクセス制御・ロールベースのアクセス制御・個人ベースのアクセス制御なお、以降の節では Firestore とセキュリティルールを活用して上記の 3 種の認可を実現する方法を紹介します。

### **2-6-3 テナントベースのアクセス制御** 

## ■マルチテナント・アーキテクチャ「 1-2-1 システム要求」で SaaS とマルチテナントの相性のよさについて説明しました。ここからは、そのようなマルチテナントのアーキテクチャを Firebase で実現する方法を紹介します。 Firebase ではプロジェクトという単位でアプリケーションをデプロイしたり、 Firestore のデータベースを作ったりします。今回は 1 つの Firebase プロジェクトで複数顧客に対応したマルチテナント型の SaaS を作ります。 1 つの Firestore 内で複数顧客のデータを分離する方法や、テナント単位のアクセス制御を実現する方法をこれから見ていきます。このようなテナントベースのアクセス制御を実現するために、本節以降では、以下の順に検討していきます。まず、「テナントの分離方法」について考えます。マルチテナントを実現するための重要な検討ポイントは次の 3 点です。 

- ・テナントに依存するデータとテナント横断となるデータをどのように区別するか。また、テナント間のデータ独立性をどのように実現するか。・テナントとユーザーの紐づけをどのように保持するか。 

- ・「あるテナント依存データにアクセスできるのは、そのテナントに所属するユーザーのみ」とするセキュリティルールはどう書くか。続いて、「複数テナントに所属するユーザー」に対してどのようにサービスを提供するかを考えます。 SaaS はさまざまな顧客に対して提供されるので、 1 人のユーザーが複数のテナントに所属するケースは少なくありません。 anpi! もこの要件を満たす必要があります。具体的には以下のようなことを考える必要があります。 

- ・あるユーザーが「現在ログインしているテナント」という概念 (どのテナントに対する操作か)はどのように実現するか。 

- ・「現在ログインしているテナント」の切り替えはどのように実現するか。 

- ・あるユーザーがログインする際に、ログイン先のテナントにユーザー登録されていること(これをユーザーの「テナント所属チェック」と呼ぶことにします)はどのように実現するか。 

## ■テナントデータのコレクション構造テナントを分離するにあたり、まずは Firestore コレクションの大まかな全体構成を検討します。テナントに依存するデータについて考えるため、図 2.7 の概念データモデルを思い出しましょう。安否確認やお知らせといったすべての業務機能に関するデータは、ある 1 つのテナントに所属するものです。テナントエンティティと各業務データを扱うエンティティは親子関係にあると言えます。そこで、 1 つのテナントは、テナントコレクションに 1 ドキュメントとして表現し、そのテナントドキュメントに各業務データのサブコレクションをそれぞれ持たせる構成とします。あとは、テナントに所属していることをチェックするセキュリティルールがうまく書ければ、テナントの独立性は実現できそうです。テナントを横断するようなデータはどうでしょう。ここまでの機能要件を見る限り、テナントを横断して扱うエンティティはなさそうです。しかし、運用を考慮すると、そのようなユースケースが発生することはよくあります。たとえば全テナントに対し、システムの運営側からシステムメンテナンスや新機能のアップデートなどの連絡を表示するようなアナウンス機能が考えられます。この場合、アナウンスコレクションに運用担当者が登録したメッセージを保存し、各テナントのトップページにはアナウンスコレクションの各ドキュメントを表示する作りとすればよさそうです。このような特定のテナントに依存しない横断的なデータは、トップレベルコレクションとして配置すると扱いやすいです。テナントベースのアクセス制御対象とそれ以外の領域でコレクションの管理方法が明確に区別され、セキュリティルールも設定しやすくなります。テナント依存データとテナント横断データの格納領域を区別し、それぞれの階層に異なるアクセス制御のルールを適用できるようにするため、図 2.18 のようなコレクション構造とします。図2.18:テナント依存データとテナント横断データのコレクション構造次にテナントとユーザーの紐づけ方法について考えます。ユーザー情報はテナントに依存するデータなので、テナントドキュメントにusersというユーザーサブコレクションを持たせることにします。そのテナントに所属するユーザーの氏名、メールアドレスや所在地といったプロフィール情報をフィールドに持つドキュメントを格納します。こうすることで、今回の要件にはありませんが、そのユーザーのテナント内の役割など、ユーザーとそのテナントのみに関する情報があった場合にもここで管理できます。ここまでのコレクション構造を整理すると、図 2.19 のようになります。図2.19:テナントデータとユーザーデータこれで、テナントデータ分離のためのデータ構造が決まりました。ただし、まだセキュリティルールについては議論していないので、このままでは全ユーザーが全テナントのデータにアクセスできてしまいます。そこで、次はテナントのアクセス制御方法を見ていきます。 

## ■カスタムクレームを利用したテナント所属チェックユーザーがあるテナントに所属している場合にのみ、そのテナントデータへのアクセスを許可するようなセキュリティルールを書く方法について説明していきます。まずは、ユーザーとテナントの関係性について、改めて要件を振り返ります。まず、 1 人のユーザーは 1 つのユーザーアカウントで複数のテナントに所属できるという仕様です。あるユーザーがテナント A とテナント B の 2 つに所属しているとします。この場合、テナント所属チェックは A と B のそれぞれに対して行わなければなりません。また、テナント間のデータ独立性の観点から、同時に両テナントのデータにアクセスできてしまうようなことは避けたいです。テナント A 内で作業をしているときはテナント A に対しての変更のみを意識すればよく、たとえユーザーの操作ミスやプログラムのバグが混入したとしても、テナント B に誤った変更が波及しない仕組みになっていると安心です。そこで、 1 人のユーザーが同時にログインできるテナントは最大 1 つまでとする制約を設け、複数のテナントには同時にアクセスできないものとします。ユーザーごとに「現在ログインしているテナント」という情報を管理しつつ、テナントデータへのアクセスの際に「現在ログインしているテナント」とアクセス先テナントが一致していることを検証する制御とします。このようなユーザーアカウントに紐づく認可に関するデータを安全に管理するには、カスタムクレームに保存するのがベストプラクティスです。そこで、カスタムクレームのcurrentTenantIdというキーに「現在ログインしているテナント」のドキュメント ID を保存します。たとえば、現在ログインしているテナントのドキュメント ID がt1の場合のカスタムクレームは、リスト 2.8 のような状態となります。 

●リスト2.8:現在ログインしているテナントのドキュメントIDが"t1"である場合のカスタムクレームの例

{ currentTenantId: "t1" }

カスタムクレームのcurrentTenantIdに保存されているテナント ID が、アクセス先テナントの ID と同じであるかどうかをチェックするisCurrentTenant関数は簡単に実装できます。これを利用すると、「アクセス先のテナント」と「現在ログインしているテナント」が一致している場合のみアクセスを許可するセキュリティルールは、リスト 2.9 のように書けます。カスタムクレームは、認証情報のトークン(auth.token)に挿入されますので、さきほど追加したカスタムクレームはauth.token.currentTenantIdから取得できます。テナントコレクション以下のサブコレクションにアクセスする際にそのパス情報からテナント ID の値を見て、currentTenantIdと一致していればアクセスを許可するというルールになっています。

●リスト2.9:「アクセス先のテナント」と「現在ログインしているテナント」が一致していればusersコレクションを読み書きできるセキュリティルール

function isCurrentTenant(tenantId) { return request.auth.token.currentTenantId == tenantId; }

match /tenants/{tenantId} { match /users/{uid} { allow read, write: if isCurrentTenant(tenantId); } }

カスタムクレームを作成するには、 Firebase Admin SDK を使用します。具体的な実装サンプルはのちほどユーザーアカウントの登録方法を議論する際に改めて提示します。これでテナントベースのアクセス制御のためのセキュリティルールをうまく書くことができました。

## ■テナント所属ユーザーの管理「現在ログインしているテナント」という情報を扱うようになったことで、ユーザーはログイン先テナントを切り替えるという操作をすることになります。また、認証後、最初にログインするのはどのテナントか、という点も考えなければなりません。そこで、画面機能としては次のような仕様とします。 

- 1ユーザーは、ログイン時にカスタムクレームのcurrentTenantId で保持しているテナントに入ります。 

- 2ユーザーは、ログイン後にテナントを自由に切り替えることができます。自分がアクセスできるテナントの一覧を表示し、そこから切り替えたいテナントを選択します。 

- 3テナントを選択すると、カスタムクレームのcurrentTenantIdの値が、選択したテナントに更新されます。 

- 4ユーザーがログアウトしても、最後にログインしていたテナントの情報がカスタムクレームのcurrentTenantIdに保持し続けられます。これにより、次回ログイン時はまた 1 に戻り、同じ手順でログイン可能です。 

anpi! にユーザーアカウントが登録されて初めてログインする場合のことも考えます。あるテナント内にユーザー登録された場合、同時にそのユーザーのカスタムクレームのcurrentTenantIdにそのテナント ID を登録すればよさそうです。この動きであれば、ユーザーは常に 1 つの「現在ログインしているテナント」情報を持っていることになります。実装上は、もし持っていない場合には所属可能なテナントのいずれかに強制ログインするような動きとなっていれば十分でしょう。あとはログインのタイミングで、「現在ログインしているテナント」のテナントデータ内に自身のユーザーアカウントが正しく登録されていることを確認すればさらに安全です。ログイン時に各テナントのusersコレクションから自分自身のドキュメントを検索し、見つからなければ認証を失敗させるというのはどうでしょうか。残念ながら、すでにこのコレクションは認証済みでないとアクセスできない制御をかけているため(リスト 2.9 参照)、認証前には使うことができません。そのため、あるユーザーが所属しているテナント一覧という情報を別に切り出す必要が出てきます。つまり「テナント横断データ」として扱うということになります(図 2.18 の下側の点線の領域です)。さらにユーザーとテナントの所属関係は多対多です。ユーザーとテナントのマッピングの一覧を保存するために、「テナント所属ユーザー(tenantUsers)」という結合コレクションをトップレベルのコレクションとして新しく用意します。 tenantUsersコレクションは、「いずれかのテナントに所属しているユーザー」を表すエンティティです。このtenantUsersドキュメントはtenantsサブコレクションを持ち、所属しているテナントごとにドキュメントを持ちます。このコレクション構造を表すと図 2.20 のようになります。テナント横断データを扱う領域に、テナント所属ユーザーコレクション (tenantUsers )とその配下の所属テナントサブコレクション ( / tenantUsers/[UID]/tenants)を用意しました。ユーザー単位で所属しているテナント情報を切り出したものになっていることが確認できます。また、各ドキュメント ID (テナントドキュメントの t1、t2、ユーザードキュメントのu1、u2など)がそれぞれのエンティティを一意に表す ID です。これをテナント所属ユーザーコレクション配下の各ドキュメントでも同一の値をドキュメント ID として使っています。図 2.20 の例を用いて具体的に説明します。テナント所属ユーザーコレクションのドキュメント ID がu1のものに着目します。これは、ユーザーサブコレクションのドキュメント ID がu1である田中さんの所属テナントを表しています。このドキュメントの所属テナントサブコレクションを見ると、ドキュメント ID がt1のものと、t2のものがあります。テナントコレクションを見ると、ドキュメント ID がt1 のものはnameが A というテナントであり、ドキュメント ID がt2のものはnameが B というテナントであることが分かります。以上より、田中さんはテナント A とテナント B に所属していることを表現しています。同様に、鈴木さん(u2)はテナント B (t2)のみに所属しています。図2.20:テナント所属ユーザーのコレクション構造これで、テナントとユーザーのマッピング情報を既存のセキュリティルールに妨げられることなく、参照できるようになりました。最後に、ユーザーがテナントに所属しているどうかのチェックをログイン時に行う方法について説明します。認証は Authentication によりログイン時に入力したメールアドレスが本当に本人のものかどうかを判定します。一方、テナントの概念は Authentication とは独立したものです。そこで、ログイン時に Authentication による認証が成功したあとに、テナントベースのアクセス制御を個別にチェックする処理を実装します。図 2.20 の例で説明すると、鈴木さん(u2)はテナント B (t2)のみに所属しているため、メールアドレスで本人確認をしたあと、さらにテナント B のユーザーコレクションに鈴木さん(u2)というユーザードキュメントが正しく存在することをチェックするということです。

Firebase Authentication SDK を用いるとonAuthStateChangedという関数で認証状態が変わったというイベントに対して処理をフックできます。そこで、このときにログインしようとしているテナントがテナント所属ユーザーコレクション配下に正しく存在することを見て、もし存在しなければ認証を取り消すようにします。リスト 2.10 は、この考え方に基づいてログイン時に所属ユーザーをチェックするサンプルコードです。 SDK を使っているため、カスタムクレームの取得方法などがセキュリティルールのときの書き方とは少し異なっている点にご注意ください。

●リスト2.10:認証時のテナントベースのアクセス制御チェックをする

const auth = getAuth(); auth.onAuthStateChanged(user => { if (!user) return

// カスタムクレームからテナントID取得 const res = await user.getIdTokenResult(); const currentTenantId = res.claims.currentTenantId;

if (currentTenantId) { const snapshot = await getDoc(

doc(db, 'tenantUsers', user.uid, 'tenants', currentTenantId) );

if (!snapshot.data()) {

// ユーザーデータが取得できなければログアウトさせる。

// ここにログアウト処理を実装する。

} // 以降、正常にログインできた場合の後続処理を実装する。 } })

これで、認証時にもテナントベースのアクセス制御がかかるようになりました。以上で、テナントベースのアクセス制御については完了になります。

### **2-6-4 ロールベースのアクセス制御** 

システムによってユーザーの権限管理方法はさまざまです。その中でも「ロールベースアクセス制御」( Role Based Access Control 、 RBAC と略されます)は一般的な管理方法の 1 つです。もっともシンプルなものは、単に一般ユーザーと管理者ユーザーの 2 種類のロールのみで制御する構成です。複雑な基幹業務システムやクラウド管理基盤ともなると、数十種類のロールに非常に多くの権限を組み合わせて細かく制御できるものもあります。ロールベースアクセス制御はユーザーに対して複数のロールを付与できるようにする場合もあります。機能ごとにロールを用意することでユーザーひとりひとりがどの機能を使えるかを細かく制御できる一方で、アプリ側では付与されている全ロールの権限をチェックするなど処理の複雑さは増します。ロール同士が持つ権限の関係性が完全に包含関係の場合、つまり上位ロールは下位ロールの権限をすべて持っている、という権限構成の場合はユーザーに対してはただ 1 つのロールを付与すればよいので、処理はシンプルになります。

anpi! の場合、ロールは「アプリ利用者」「アプリ管理者」「テナント管理者」の 3 つです。「アプリ利用者」は自分の安否状況の報告やメッセージのやりとりなど、1人のユーザーとして自分自身に関わる機能しか使えません。「アプリ管理者」は「アプリ利用者」ができることはすべて実行可能で、さらに安否状況の全体の確認や全体へのお知らせの掲示など組織全体に関する業務上の操作が実行可能です。しかし、ユーザーを新規に登録したり無効化するといった運用管理上の機能は実行できません。「テナント管理者」はユーザー登録などの運用管理上の機能も含め、これらのすべての操作が可能な特権的なロールです。これらの 3 つは完全に上位・下位の構成となっているので、今回はユーザーにはただ 1 つのロールのみを付与する構成とします。ユーザーのロール情報はどこに保存すべきしょうか。ユーザーのカスタムクレームに保存することも考えられますが、そもそもロールはテナント単位で付与されるため、カスタムクレームで管理する場合はやや構造が複雑になってしまいます。そこで、テナント所属ユーザードキュメントのサブコレクション内の所属テナントドキュメントのroleフィールドに、そのテナントのロール情報を保存することにします。図 2.21 のu1ユーザーの場合、t1テナントでは「アプリ利用者」ロールが、t2テナントでは「テナント管理者」ロールが付与されていることになります。このように、あるユーザーに付与されるロールは所属テナントごとに管理します。図2.21:テナント所属ユーザー配下でロール情報を管理上記の図中では分かりやすさのために日本語でロール値を記載していますが、実際には以下のようにroleフィールドは文字列型の値として保存しています。

- ・「アプリ利用者」ロール: applicationUser 

- ・「アプリ管理者」ロール: applicationManager 

- ・「テナント管理者」ロール: tenantManager 

あとは、この値をチェックするセキュリティルールを書けば、ロールベースアクセス制御を実現できます。セキュリティルールを書くにあたり、各ロールの値を評価する関数を作っておくと便利です。各ロールの上位下位の関係性も表現できるようにします。つまり、「アプリ利用者」権限のチェックは、ロールが「アプリ利用者」または「アプリ管理者」または「テナント管理者」のいずれかであればアクセスを許可するようにします。同様に、「アプリ管理者」権限を持つことのチェックは、ロールが「アプリ管理者」または「テナント管理者」であればアクセスを許可するようにします。いずれのロールも付与されていなければ、どの機能にもアクセスできません。これをセキュリティルールで実装するとリスト 2.11 のようになります。

●リスト2.11:各ロールが持つ権限でアクセス可否を判断する関数

function isApplicationUser(tenantId) {

return get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationUser', 'applicationManager', 'tenantManager']; } function isApplicationManager(tenantId) {

return get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationManager', 'tenantManager']; } function isTenantManager(tenantId) {

return get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role == 'tenantManager'; }

この関数を利用し、各コレクションへのアクセス制御をそのロールに基づいて判定するようにします。たとえば、usersコレクションでは、「テナント管理者」しか読み書きできない制御を次のようなルールとして書けます。

- ●リスト2.12:テナント管理者しかアクセスできないようにするセキュリティルール 

match /tenants/{tenantId} { match /users/{uid} { allow read, write: if isTenantManager(tenantId);

} }

ここまでの方式をまとめると、ユーザーがブラウザーを使って Firestore にデータアクセスを要求し、アクセス制御を行う流れは図 2.22 のような構成となります。図2.22:ロールベースのアクセス制御の実装方式これで、ロールベースのアクセス制御も実現できました。

### **2-6-5 個人ベースのアクセス制御** 

ここまではロールやテナントなど、ユーザーを一定ルールのグループにまとめてグループ単位で権限を制御する方法を見てきました。最後の認可要件は、自分自身に関連するデータは本人しかアクセスできない、という個人ベースのものです。「自分自身に関連するデータ」と言うとユーザープロフィールや個人情報、そのユーザーが作成したコンテンツなど、さまざまなユースケースがあります。一方、ここまで見てきたように Authentication を用いると、セキュリティルール上でアクセスユーザーの UID は簡単に取得して条件に使えます。そのため、考えるべきはユーザー個人に紐づくデータ、つまり Firestore ドキュメントをどのように 1 人のユーザーと紐づけるのかという点のみです。もしユーザーと一対一の関係にあるエンティティの場合、もっともお勧めなのはドキュメント ID に UID を利用することです。分かりやすい例としては、ユーザーそのものを表すユーザードキュメントでしょう。ユーザードキュメントのドキュメント ID は Authentication で発行された UID を使っています。そこにアプリケーション固有の情報として、メールアドレスや氏名などをそのドキュメント上に保持しています。これを読み書きできるのは本人のみとする場合のセキュリティルールは、リスト 2.13 の通りです。isOwnData関数により、ユーザー自分自身のデータであるかどうかを判定します。

●リスト2.13:ユーザー自分自身のデータであれば読み書きできるセキュリティルール

function isOwnData(docId) { return request.auth.uid == docId; } match /tenants/{tenantId}/users/{uid} { allow read, write: isOwnData(uid); }

今回の要件上、個人ベースのアクセス制御を要求される機能のエンティティはすべてユーザーエンティティと一対一の関係にあるものばかりです。そのため、リスト 2.13 のようなisOwnData関数だけで要件を満たすことができます。では、もし一対多の関係にあるエンティティがあった場合はどうしたらよいでしょうか。今回の要件にはありませんが、たとえばファイル共有サービスにおいて、 1 つのファイルを決められた複数人で共有したいといった場合を考えてみます。 1 つの設計案として、そのドキュメントにアクセスが可能なユーザーの UID を配列として保存しておくという方法が考えられます。ファイル情報を表すドキュメントをリスト 2.14 のような構造で保持しているとします。viewersは閲覧者、つまり読み取り権限のみを持つすべてのユーザーの UID のリストです。

●リスト2.14:ファイル情報を表すドキュメント

{ fileName: “メンバーリスト.pdf”, viewers: [“user-id-a”, “user-id-b”, “user-id-c”] }

このとき、閲覧者だけが読み取りできるセキュリティルールはリスト 2.15 のようになります。

- ●リスト2.15:ユーザー単位で読み書きを制御するセキュリティルール 

match /files/{fileId} { allow read: request.auth.uid in resource.data.viewers; }

これをリスト 2.13 のisOwnData関数の代わりに用いることで、一対多の関係にあるエンティティであってもユーザーベースのアクセス制御が実現できました。このようにセキュリティルールはresource.dataオブジェクトを参照することでアクセス先のドキュメントデータをもとに権限判定ができます。ドキュメントごとに権限定義を区別したい場合などにこの方法を覚えておくと便利です。これで、個人ベースのアクセス制御も実現できました。以上で、 3 通りのアクセス制御パターンをすべて Firestore のセキュリティルールで実現する方法についての説明は完了です。

### **2-6-6 ユーザーアカウントの登録時の処理** 

ユーザーアカウントの登録時の処理について見ていきます。今回、カスタムクレームやテナント所属ユーザーコレクションを導入したため、ユーザー登録時にあわせてそれらのデータも作る処理が必要となります。ユーザーアカウントはテナント管理者により、ユーザー管理画面からユーザーの新規登録により発行できます。その際に、氏名やメールアドレス、ロール等の情報を入力しボタン操作等により実行します。 Authentication と Firestore のそれぞれにデータを初期登録する必要があります。このタイミングで登録が必要なデータは以下となります。・ Authentication :ユーザーアカウントを作成する

- ・ Authentication :カスタムクレームのcurrentTenantIdにテナント ID 値を登録する 

- ・ Firestore :tenants/{tenantId}/usersコレクションにユーザードキュメントを作成する 

- ・ Firestore :tenantUsers/{uid}/tenantsコレクションにテナントドキュメントを作成する 

Authentication のユーザー作成は Firebase Admin SDK を用いてバックエンドで実行する必要があります。そのために、ユーザーアカウント登録用に Functions を 1 つ用意します。これを HTTP 関数としてデプロイしておけば、フロントエンドからは Firebase SDK の機能を用いて呼び出せるようになります。また、今回は Authentication でユーザーアカウントを登録した際に払いだされる UID を Firestore 側の各種ユーザー関連のドキュメント ID にも利用する方針でした。これらをまとめると、ユーザーアカウント登録時の処理は以下の順となり、図 2.23 はその概要図となります。 2 〜 5 の処理は Functions 側の処理のため、バックエンドで実行されていることに注意してください。

- 1 **ユーザー登録Functionsの呼び出し** : テナント管理者が画面から新規ユーザーを登録する。その際に入力したロールや氏名などの情報とテナント ID を Functions のパラメータに渡して、 Functions を実行する。 

- 2 **ユーザーアカウント登録APIを実行** :ユーザー登録 Functions は、 Authentication に対してユーザーアカウント作成 API を実行する(カスタムクレームも付与)。 

- 3 **ユーザーアカウント情報の返却** : Authentication ユーザーアカウントが登録され、 API の応答として作成したアカウント情報( UID など)を返す。 

- 4 **ユーザードキュメントを作成** :対象テナントの配下のユーザーサブコレクションにユーザードキュメントを登録する(ドキュメント ID を UID とする)。 

- 5 **所属テナントドキュメントを作成** :テナント所属ユーザードキュメント(ドキュメント ID が UID であるもの)の配下に所属テナントドキュメントを登録する。ドキュメント内のroleフィールドに、渡されたロール情報に基づく値を書き込む。 

##### 図2.23:ユーザーアカウント登録時の処理なお、テナント所属ユーザードキュメントはドキュメント ID をユーザーの UID と一致するように保持する以外、他に必要な情報はありません。そのため、ドキュメントフィールドが不要となります。この場合、 Firestore 上ではドキュメントの物理的な実体は存在しないという扱いになります。つまり、テナント所属ユーザードキュメントという実体は存在せず、あくまでパス表現があるのみです。その場合でも、所属テナントサブコレクションという実体のあるドキュメントを持つことは許されます。管理コンソールで見ると、祖先ドキュメントが存在しない旨の警告が表示されますが、これは Firestore の使い方としてとくに問題があるというわけではありません。ユーザーアカウントを登録する Functions の実装イメージはリスト 2.16 のようなソースコードになります。関数の冒頭で SDK の機能を利用して、 Authentication にユーザーアカウントを登録し、カスタムクレームのセットを行っています。その後、 Firestore 側の初期登録処理に進んでいます。より詳細な実装に興味のある方はサンプルのソースコードの/functions/src/module/tenants.tsのcreateUser 関数もあわせてご覧ください。 

●リスト2.16:Authenticationにユーザーアカウントを登録しカスタムクレームをセットするFunctionsのサンプルコード

import { getAuth } from 'firebase-admin/auth'; export const createUser = functions .region('asia-northeast1') .https.onCall(async (data, context) => { const auth = getAuth(); const tenantId = context.auth.token.currentTenantId; const user = { ...context.data.user } // Authenticationのユーザーアカウントを登録する const userRecord = await auth.createUser({ email: user.email, displayName: user.name, emailVerified: false,

}); // カスタムクレームをセット await auth.setCustomUserClaims(userRecord.uid, { currentTenantId: tenantId, }); const uid = userRecord.uid; // Authenticationで発行されたUID // 以降、Firestoreの登録処理が続く // 略 }

テナント所属ユーザーコレクションに対して書き込みをするのはこの Functions からのみのため、クライアント側からは書き込みできないよう制御しておくと安全です。もしクライアントから書き込めてしまうと、自身に強力な権限を持つロールを自分に付与するなどの不正ができてしまい、脆弱性のリスクとなります。読み取りは、ユーザー本人がログイン状態で自分自身に関するドキュメントを参照するケースと、テナント管理者がユーザー管理する画面で各メンバーのロールを参照するケースがあります。また、ユーザーは、ログイン後にテナントを切り替えたい場合に、自分がどのテナントに所属しているかの一覧を見る必要もあるでしょう。よって、これを満たすセキュリティルールは次の通りです。

●リスト2.17:テナント所属ユーザーコレクションは参照のみ許容するセキュリティルール

match /tenantUsers/{uid} { match /tenants/{tenantId} { allow get: if isAuthenticated() && (request.auth.uid == uid || request.auth.token.currentTenantId == tenantId) allow list: if isAuthenticated() && request.auth.uid == uid } }

なお、ここまでのセキュリティルールのサンプルでは読み取り時はreadという操作を許可してきましたが、今回はドキュメント単体の取得操作と一覧の取得操作に分けたルールを適用するため、get とlistという操作に対してルールを適用しています。実はreadは getとlistに分解できるのですが、このような記法については「 2-68 セキュリティルールのまとめ」で改めて解説します。これにより、ログイン後の状態でユーザーはtenantUsersの自分自身のドキュメントにアクセスし、自分が所属しているテナント一覧を取得できるようになりました。そのほかに、テナント切り替えの機能では、ユーザーがテナント切り替えを画面から要求した際にcurrentTenantIdを更新する処理も必要です。リスト 2.17 のセキュリティルールでクライアントサイドからの更新をガードしているため、これも Functions 経由での更新になります。実装方法の詳細についてはここでは割愛します。ここまでの議論の延⻑線上にあり、それほど難しい処理ではありません。こちらも興味があれば、サンプルコードの実装をご覧ください。

### **2-6-7 Functionsの認証・認可** 

ここまでの議論で、 Firestore のセキュリティルールでフロントエンドからのアクセス制御は要件通りに実現できることを見てきました。一方、ユーザーアカウント登録用の Functions や、テナント切り替え用の Functions が必要でした。これらは HTTP 関数としてデプロイしているため、外部から呼び出し可能な形で公開されるエンドポイントとなります。また、 Functions 内から Firestore へアクセスする際は Firebase Admin SDK を用いた特権的な管理者アクセスとなります。フロントエンド側で実行する SDK と異なりセキュリティルールによる制御は効きません。そのため、 HTTP 関数の場合は Functions 内で個別に認証・認可の制御を実装する必要があります。なお、 Firestore へのドキュメント登録や更新によってトリガーされる Functions の場合は、トリガーの起点となるコレクションに対して登録や更新処理自体をセキュリティルールで保護できます。これはトリガー実行判定に認証や認可のアクセス制御をかけられることと同義のため、 HTTP 関数よりは安全で扱いやすいです。もし、 Firestore トリガー関数で置き換え可能であれば、そちらを利用するのがよいでしょう。

HTTP 関数における認証・認可をチェックする実装を見ていきます。認証については Firebase Admin SDK でも認証コンテキストから Auth オブジェクトを取得できるため、認証済みかどうかは簡単に判定できます。認可の制御は Firestore のドキュメントを直接見て判断するロジックを自前で実装する必要があります。少々複雑ですが、リスト 2.18 に HTTP 関数で認証・認可を自前でチェックするサンプルコードのイメージを記載します。この Functions はテナントの切り替え処理を行っていますが、細かな内容はさておき、これまで Firestore のセキュリティルールとほぼ同等のチェックを自前で検証している、というのがポイントです。

●リスト2.18:Functions内で認証・認可をチェックするサンプルコード

export const switchTenant = functions .region('asia-northeast1') .https.onCall(async (data, context) => { // 認証チェック if ( // 認証(Email属性がセット)されていなければ拒否 !context?.auth?.token?.email ||

// Emailが確認済みでなければ拒否 !context?.auth?.token?.email_verified || // 切替先のテナントIDがセットされていなければ拒否 !data?.switchTargetTenantId || // 切替先のテナントIDに対するValidation typeof data.switchTargetTenantId !== 'string' ) { // 認証エラー throw new functions.https.HttpsError('unauthenticated', '認証に失敗しました'); } // 認可: テナント所属チェック db .collection('tenantUsers') .doc(uid) .collection('tenants') .doc(switchTargetTenantId) .then(doc => { // テナントに所属していた場合の処理 // 略 }) .catch(error => { // テナントに所属していなかった場合はエラー throw new functions.https.HttpsError('unauthenticated', '認証に失敗しました'); });

ユーザーアカウントの登録や、テナント切り替えなど、サーバーサイドで処理すべき処理がある場合、認証や認可は個別に制御が必要ということを説明しました。

⻑くなりましたが、これで、認可に関する設計は以上になります。

### **2-6-8 セキュリティルールのまとめ** 

認証・認可のためのセキュリティルールを一通り設計してきました。最後にここまでのルールをとりまとめ、firestore.rulesの全体を俯瞰しながら振り返ります。これまで見てきたように、セキュリティルールは判定条件ごとにチェック用の関数を切り出すと、ルールの再利用性が高まり見通しもよくなります。ここまでで認証や認可の条件ごとに関数を用意してきました。改めて、 anpi! で必要となるルールは次の 4 つになります。

- 1認証済みであり、かつ、メールアドレスが検証済みであること (認証)。 

- 2条件1を満たし、かつ、「現在ログインしているテナント」であること(テナントベースのアクセス制御)。 

- 3条件2を満たし、かつ、当該テナントに対して有効なロールを保持していること(ロールベースのアクセス制御)。 

- 4条件2を満たし、かつ、自分自身のデータであること(個人ベースのアクセス制御)。テナントに依存しないトップレベルコレクションである tenantUsersコレクション配下は「 2-6-6 ユーザーアカウントの登録時の処理」で議論しました(リスト 2.17 参照)。テナントに依存するtenantsコレクション配下のサブコレクションに対しては機能の要件に応じて、条件3のロールベースのアクセス制御および条件4の個人ベースのアクセス制御を適用します。今回はサンプルとして、ユーザーサブコレクションに対してルールを適用した場合の設定例を記載しており、usersサブコレクションの要件は次の通りとしています。 

- ・get:本人またはテナント管理者のみ可能 

- ・list:アプリ管理者(またはテナント管理者)のみ可能 

- ・update: 本人のみ可能なお、前節までセキュリティルールのサンプルは、説明の分かりやすさを優先してほぼすべてreadまたはwrite操作に対してルールを記載してきました。実際には、read(読み取り)は、get(単一ドキュメントの取得)とlist(コレクションからの複数取得)に分けられます。また、 write (書き込み)は、 create (登録)と update(更新)とdelete(削除)に分けられます。権限管理上は必要最低限の権限のみ付与すべきですので、これらの分解された最小粒度の操作に対して適切にルールを設定するようにしましょう。条件をコレクションに適用する際はmatch文を使いますが、このときにmatch文を入れ子構造にすることで、同一階層の各コレクションに対して同一ルールを一括で適用できます。ここまでの内容を実際のセキュリティルールの設定内容として記載すると次のようになります。 

●リスト2.19:セキュリティルールのまとめ

rules_version = '2'; service cloud.firestore { match /databases/{database}/documents {

// 条件1: 認証済みであり、かつ、メールアドレスが検証済みであること function isAuthenticated() { return request.auth != null && request.auth.token.email_verified; }

// 条件2: 条件1を満たし、かつ、「現在ログインしているテナント」であること(テナントベースのアクセス制御) function isCurrentTenant(tenantId) { return isAuthenticated() && request.auth.token.currentTenantId == tenantId; }

// 条件3: 条件2を満たし、かつ、当該テナントに対して有効なロールを保持していること(ロールベースのアクセス制御) // - アプリ利用者ロールであること function isApplicationUser(tenantId) {

return isCurrentTenant(tenantId) && get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationUser',

'applicationManager', 'tenantManager'];

}

// - アプリ管理者ロールであること

function isApplicationManager(tenantId) {

return isCurrentTenant(tenantId) && get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationManager', 'tenantManager']; }

// - テナント管理者ロールであること

function isTenantManager(tenantId) {

return isCurrentTenant(tenantId) && get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role == 'tenantManager'; } // 条件4: 条件2を満たし、かつ、自分自身のデータであること(個人ベースのアクセス制御)。

function isOwnData(tenantId, docId) {

return  isCurrentTenant(tenantId) && request.auth.uid == docId; } // トップレベルのテナントに依存しないコレクション match /tenantUsers/{uid} {

match /tenants/{tenantId} {

allow get: if isAuthenticated() && request.auth.uid == uid || request.auth.token.currentTenantId == tenantId allow list: if isAuthenticated() && request.auth.uid == uid }

} // tenants配下のテナントに依存するコレクション match /tenants/{tenantId} { match /users/{userId} {

allow get: if isOwnData(tenantId, userId) || isTenantManager(tenantId) allow list: if isApplicationManager(tenantId) allow update: if isOwnData(tenantId, userId) } // 略 } } }

#### **【コラム】Firebase Authentication with Identity Platform** 

「2-6-3 テナントベースのアクセス制御」ではテナントベースの認可をFirestoreとセキュリティルールを使って実現する方法を紹介しました。実は、テナントベースの認可は Google Cloud のIdentity Platformの機能を活用しAuthenticationと組み合わせるという別の手法もあります。これにより、一部の要件はFirestoreとセキュリティルールだけを使うより簡単に実現できるケースがあるため、こちらのコラムで簡単に紹介します。

Firebase Authenticationをアップデートし、Google Cloudが提供するIdentity Platformという認証サービスと組み合わせることが可能です。これにより、いくつかの便利な機能を使えるようになります。なお、アップデートと言っても、管理コンソール上で機能を有効化すれば、今までのAutheticationの実装や環境はそのまま継続して利用できます。互換性があるため、プロジェクトの途中から導入することも問題ありません。

Identity Platformにはさまざまな機能がありますが、ここまでで説明した認証や認可の要件を満たす上で、よりシンプルに実装できるような機能を2つ紹介します。

1つ目は、 **マルチテナンシー機能** です。今回AutheticationとFirestoreのみでマルチテナント機能を設計しました。そのため、Authentication内部のユーザーアカウント自体はテナントを区別せずに管理し、Firestoreのデータ構造でマルチテナントを実現していました。実は、Identity Platformにはマルチテナンシーの機能があります。これを用いると、 Authentication内部でテナントの概念を持つことができるようになり、ユーザーアカウントをテナント単位で持つことができます。Authenticationの認証時にテナントID(Identity Platformのテナントに付与するID)を与えるようにすれば、あるユーザーの認証時にそのテナントに所属しているかどうかのチェックもAuthenticationの機能のみで検証できるようになります。Authenticationのテナント内にそのユーザーアカウントが登録されていない場合は、Authenticationの認証処理自体が失敗するようになります。

2つ目は、 **ブロッキング関数(Blocking functions)** と呼ばれる機能です。これは、 Authenticationの認証時に処理をフックし、固有の認証チェックを追加で検証できるようになる機能です。今回、認証後にロールのチェックなど、自前の認可処理を直接実装していました。厳密には、これらの処理はAuthenticationの認証自体が成功したあとでチェックし、失敗すれば強制ログアウトさせるという動きとなっています。そこで、ロールをチェックするロジックを切り出し、ブロッキング関数の beforeSignIn内で実行するようにします。そうすると、Authenticationの認証処理の中でロールチェックも行われるようになります。もし、このロジック内でエラーを返した場合は、Authenticationの認証自体が失敗するようになります。そのため、認証と認可をログイン時のチェックの中でまとめてかけられるようになり、より直感的で安全なセキュリティ機能として動作するようになります。

Identity PlatformとFirebase Authenticationの違いやサービスの位置づけについては、ドキュメント1 ~~に~~ 詳しく記載されています。まだまだ出たばかりの機能であり、事例の情報なども少ないため、筆者らの会社でも Identity Platformは社内検証レベルにとどまっています。しかし、うまく活用すれば今回提示した認証・認可の仕組みはより簡単な実装に置き換えられそうな感触もあります。興味のある方は、ぜひ公式ドキュメントをご覧頂き、まずは試してみてはいかがでしょうか。

1. <u>https://cloud.google.com/identity-platform/docs/product-comparison</u> 

## **2-7 コレクションのデータモデリングを完成させる** 

概念データモデルと、セキュリティ要件を踏まえたモデリングの検討が終わったので、最後に anpi! に必要なエンティティをコレクション・データモデルに落とし込み、本章の締めくくりとします。改めて必要なエンティティを列挙すると次の通りです。インデントは親子関係(依存関係)を表現しています。「 2-3 Firebase の概要設計を行う」で最初に検討した概念データモデルのエンティティとは異なり、これらは明確に Firestore のコレクションで表現されるべきものとして識別されました。・テナント:tenants

・ユーザー:users

・安否確認:safetyConfirmations

・安否確認回答:answers

・お知らせ:informations

・メッセージ:messages

・テナント所属ユーザー:tenantUsers

・所属テナント:tenants

これらのエンティティを Firestore のコレクションとドキュメントの構造に落とし込んでいきます。

### **2-7-1 コレクション、ドキュメント一覧** 

anpi! で利用する各コレクションと、そのドキュメントのフィールド構造を一覧化します。

## ■テナント:/tenants/[TENANT_ID] 

テナントを扱います。テナントに依存するデータはすべてこのテナントドキュメントのサブコレクションに格納します。

|フィールド名|論理名||型|値(例)|
|---|---|---|---|---|
|name|テナント名|文字列||"株式会社A"|

## ■ユーザー:/tenants/[TENANT_ID]/users/[UID] 

ユーザーのメールアドレス、氏名、所在地など、その人物に関わる情報を扱います。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|email|メールアドレス|文字列|"suzuki.taro@example.com"|
|name|氏名|文字列|"鈴木太郎"|
|location|所在地|文字列|"東京都"|
|isActive|有効状態|ブール値|trueならば有効、falseならば無効|

安否確

■ 安否確認: /tenants/[TENANT_ID]/safetyConfirmations/ [ID]

安否確認の依頼を扱います。 1 つの安否確認に対して、複数のユーザーが安否確認を回答する形となるため、安否確認ドキュメントは安否確認回答サブコレクションを持ちます。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|title|タイトル|文字列|"Y年M月のXX地震における安否確認"|
|body|本文|文字列|"緊急で社員の安否を確認します"|
|hazardType|災害種別|文字列|"地震"、"風水害"、"その他"のいずれか|
|dueDate|回答期日|文字列|"2022/12/31"|

回答期日は、この安否確認に対していつまで入力が可能かを表すために用います。日時型で持たせることも可能ですが、 Firestore の日時型と JavaScript のDate型との変換のしやすさなどの実装のシンプルさを優先して文字列で保持しています。災害種別のような項目は値を文字列で保存します。実装上は、"hazard_quake"( ” 地震 ” を表す)などコード値として保持し、画面側でラベルに変換する方法も考えられます。 Firestore ではこのような文字列の値の種類があらかじめ制限しておくような列挙型というデータ型はありません。そこで、 Firestore のセキュリティルールを用いて、フィールドに予期せぬ値を登録することを防止するバリデーションを実装します。リスト 2.20 は、登録しようとしている災害種別の値をrequest.resource.data.hazardTypeから取得し、所定の値でなければ登録させないようにするサンプルです。セキュリティルールは認証や認可のルールと併用しながら、バリデーションとしても活用できます。スキーマレスとはいえ、予期せぬデータの混入は、バグの発見を遅らせ、より致命的な障害に発展するおそれもあります。可能な限り、バリデーションも組み込みましょう。

●リスト2.20:セキュリティルールで列挙値のバリデーションを行うサンプルコード

function isValidHazardType() { return request.resource.data.hazardType in ['hazard_quake', 'hazard_water', 'hazard_other']; } match /tenants/{tenantId}/safetyConfirmations/{safetyConfirmationId} { allow create: if isValidHazardType(); }

## ■ 安否確認回答: /tenants/[TENANT_ID]/safetyConfirmations/ [ID]/answers/[UID] 

安否確認の依頼に対するユーザーひとりひとりの回答を扱います。 1 つの安否確認に対して各ユーザーの回答は最大 1 つという制約があるため、ドキュメント ID にはユーザードキュメントの ID (つまり Authentication で発行される UID )を用います。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|safetyStatus|安否状況|文字列|"安全"、"軽傷"、"重傷"のいずれか|
|memo|メモ|文字列|"無事です。"|
|nameSnapshot|報告者氏名(回答時)|文字列|"鈴木太郎"|
|locationSnapshot|報告者所在地(回答時)|文字列|"千葉県"|

今回は報告者の安否状況を文字列として扱っていますが、ソートキーなどに利用したい場合は数値として持つことも考えられます。安否確認回答ドキュメントには報告者の氏名や所在地を保存しますが、ユーザードキュメントと結合させて最新の値をとるのではなく、回答時の情報をコピーし持たせる仕様としています。つまり、非正規化します。これは、安否確認と回答という業務の性質上、最新の状況よりはその時点ではどうだったか、という情報を扱うべきという考え方によるものです。その意図が明確になるように、フィールド名にもsnapshotという単語を採用して断面データであることを強調しています。

## ■ お知せ:/tenants/[TENANT_ID]/informations/[ID] 

## らテナント内の全ユーザーに対してのお知らせを扱います。 

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|title|タイトル|文字列|"緊急連絡先について"|
|body|本文|文字列|"有用な公共機関の連絡先リストを共有します"|
|publisherName|投稿者名|文字列|"鈴木太郎"|
|isPublished|公開状態|文字列|trueならば公開、falseならば非公開|
|attachedFiles|添付ファイル|配列|[ "/tenants/[TENANT_ID]/informations/[ID]/連絡先.pdf", "..." ]}|

お知らせの投稿者名も安否確認回答と同様に、お知らせ投稿時点のユーザー名の断面情報としています。こちらはユーザードキュメント側で氏名フィールドが更新された場合に、その反映を取り込んで整合性をとる設計にすることも考えられます。しかし、今回は書籍用のサンプルとして仕様をシンプルに保つため、データ整合性のための同期はしない方針としています。お知らせは複数ファイルの添付ができます。添付ファイルは Storage にアップロードして保管しますが、多くの場合は画面にもファイル名を表示したりダウンロードリンクを提供したりする機能が求められます。そのため、 Firestore のドキュメントデータでも Storage のファイルパスを保持しておくことで、このような画面要件を満たせるようになります。今回、attachedFilesフィールドは

Storage パス文字列の配列にすることで、複数の添付ファイルの情報を保持しています。

## セ 

## メッ 

## ■ メッセジ:/tenants/[TENANT_ID]/messages/[ID] 

ユーザー間のチャットコミュニケーション機能におけるメッセージを扱います。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|body|本文|文字列|"電車が止まっていますね"|
|publisherName|投稿者名|文字列|"田中一郎"|

こちらの投稿者名もお知らせ同様、ユーザー名のメッセージ投稿時点の断面情報とし、同期はしません。

## ■テナント所属ユーザー:/tenantUsers/[UID] 

テナントに所属するユーザーを扱います。ドキュメント ID は UID です。前節で述べた通り、テナント所属ユーザードキュメントはドキュメント ID 以外のフィールドが存在しないため、ドキュメントの実体は存在しません。

## ■ 

## 所 

## 属テナン 

## ト:/tenantUsers/[UID]/tenants/[TENANT_ID] 

テナント所属ユーザーが所属しているテナントを扱います。テナントと一対一のため、ドキュメント ID はテナントドキュメントの ID と一致させます。ロールベースのアクセス制御のところで検討した通り、このドキュメントにはそのテナント内でのロール設定を保存します。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|role|ロール|文字列|"tenantManager"、"applicationManager"、<br>"applicationUser"のいずれか|

## ■全ドキュメント共通のシステムフィールド各ドキュメントのフィールド構造を定義しました。それらに加え、システム上運用しやすくなるよう次の管理フィールドも全ドキュメントに持たせる方針とします。これらのフィールドは他の業務機能を実現するためのフィールドとは区別し、 **バックエンドで責任をもってデータを管理する** という意味を込めて、_の接頭辞をフィールド物理名に付与することとします。 

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|_createdAt|作成日時|日時|2022年12月1日15:30:10 UTC+9|
|_createdBy|作成者|文字列|"2Y6p4PX9sQcjBH55AylqUZxxxxxx"|
|_updatedAt|最終更新日時|日時|2022年12月1日15:30:10 UTC+9|
|_updatedBy|最終更新者|文字列|"Q425s7Ay9BXSoucWsacxP9xxxxxx"|

バックエンドで責任をもってデータを管理する、という言葉の意味と、具体的な方法について説明します。作成日時と更新日時は、それぞれドキュメントの初回登録時と最終更新時のそれぞれを表すタイムスタンプ値を記録するためのフィールドです。これらのタイムスタンプ値はクライアントから受け取るのではなく、必ず Firestore 側のサーバー生成タイムスタンプ (serverTimestamp)を使います。これは、クライアント側から値を受け取ってしまうと不正な時刻を登録されてしまうおそれがあるためです。必ずサーバーサイドで信頼あるタイムスタンプ値として登録し、クライアントからの値は受け取らないようセキュリティルールでガードする必要があります。作成者と更新者は、それぞれ初回登録操作と最終更新操作を行ったユーザーが誰かを記録するためのフィールドです。今回は文字列で UID を保存する方式とします。登録・更新時のときは、サーバー生成タイムスタンプを取得する機能が SDK に用意されていましたが、 UID をバックエンドで簡単に取得する機能はないので、クライアントから UID を受け取ることになります。この際、別のユーザーになりすましができないようにセキュリティルールでしっかりと偽りがないことをチェックします。さらに、初回登録時の操作を記録するフィールドである作成日時と作成者は、登録時のみ書き込みを許可し、それ以降は更新できないように制御します。登録(create)だけでなく、更新(update)も同様に、不正な変更が起きないようにバリデーションルールを適用します。これらのバリデーションを関数化したものは次の通りです。これを全コレクションに適用します。

●リスト2.21:管理フィールドのバリデーションを行うサンプルコード

function isValidCreated() { return // _updatedAt属性にはサーバー生成タイムスタンプがセットされていること request.resource.data._updatedAt == request.time && // _updatedBy属性には操作者のUIDがセットされていること request.resource.data._updatedBy == request.auth.uid && // _createdAt属性にはサーバー生成タイムスタンプがセットされていること request.resource.data._createdAt == request.time && // _createdBy属性には操作者のUIDがセットされていること request.resource.data._createdBy == request.auth.uid; } function isValidUpdated() { return // _updatedAt属性にはサーバー生成タイムスタンプがセットされていること request.resource.data._updatedAt == request.time && // _updatedBy属性には操作者のUIDがセットされていること request.resource.data._updatedBy == request.auth.uid && // _createdAt/_createdBy属性は変更されていないこと request.resource.data._createdAt == resource.data._createdAt && request.resource.data._createdBy == resource.data._createdBy; }

以上で Firestore のデータモデリングは完成です。いよいよ、次の章からは実際の開発に取り掛かります。

# **3 章開発編** 

**2 章では、 Firebase を用いた Web サービスの設計について見てきました。本章ではサンプルアプリ「 anpi! 」の実装を例に、 Firebase を用いた具体的な開発方法を紹介します。**

## **3-1 開発環境を準備する** 

まずは Firebase プロジェクトの作成や Firebase CLI の導入、 Firebase Local Emulator Suite を利用したローカル開発環境の構築など、開発環境の準備をしましょう。

### **3-1-1 Firebaseプロジェクトの初期セットアップ** 

Firebase を利用するには、まずリソースの管理単位である Firebase プロジェクトを作成する必要があります。 Firebase プロジェクトとは、 Firebase のサービスを使用する Google Cloud のプロジェクトです。 Firebase プロジェクトと、 Firestore や Storage などの各リソースは、一対一で紐づきます。 1 つの Firebase プロジェクトに対して、複数の Firestore を作成するなどといったことはできません。また、 Firebase サービスをモバイルアプリや Web サービスで利用できるようにするためには、 Firebase プロジェクトに **アプリ** を登録する必要があります。アプリを登録すると、 Firebase 構成オブジェクトが提供され、 Firebase プロジェクトとアプリを接続できるようになります。アプリは、 1 つの Firebase プロジェクトに最大 30 個まで登録可能です。ただし、前述した通り 1 つのプロジェクトには 1 つのリソースが紐づくため、登録されたアプリはすべて同じリソースへアクセスする点には注意してください。

## ■Firebaseプロジェクトを作成するそれでは Firebase プロジェクトを作成してみましょう。 Firebase プロジェクトの作成には Google アカウントが必要です。 Google アカウントを持っていない場合は取得してください。既存の Google Cloud プロジェクトがある場合はあとから Firebase の追加もできますが、本書では新規に Firebase プロジェクトを作成する方法を紹介します。 

Web ブラウザーを開いて Firebase コンソールの URL1 へアクセスし、案内に従ってプロジェクトを作成します。プロジェクトの作成には、次の 2 つを指定します。

- ・プロジェクト名 

- ・プロジェクト ID 

プロジェクト名には英数字、スペース、および-!'"の記号を利用した任意の名称を指定でき、プロジェクト作成後も変更が可能です。それに対して、プロジェクト ID はグローバルに一意である必要があり、プロジェクト作成後に変更ができません。プロジェクト ID は、 Hosting のサブドメインや、 Storage のデフォルトのバケット名などに利用されます。プロジェクト名をそのまま利用することが多いですが、すでに同じ ID のプロジェクトが存在する場合は変更が必要です。プロジェクト名とプロジェクト ID を指定したら、画面の案内に従って進めます。図3.1:Firebaseプロジェクトの作成

Firebase プロジェクトの作成時には、 Google アナリティクスの設定が可能です。 Google アナリティクスを有効にすることで、 Firebase で実装したアプリの使用状況などの分析が可能になります。本設定は作成後にも有効化が可能です。作成が完了したら、 Firebase プロジェクトのコンソール画面に遷移します。 Firebase プロジェクトのコンソール画面では、プロジェクト自体の設定や各サービスの設定を管理できます。

## ■アプリを登録する作成した Firebase プロジェクトにアプリを登録しましょう。 Android や iOS といったモバイル用や、 Web 用などプラットフォーム別にアプリの登録が可能です。本書では、 Web 用のアプリの登録を紹介します。 Firebase プロジェクトのコンソール画面中央にある、 Web アイコンをクリックすることでアプリの登録が可能です。 

##### 図3.2:アプリの登録アプリの登録には、ニックネームを設定します。ニックネームは Firebase プロジェクトのコンソール画面上でアプリを表す名称として利用され、そのほかに影響はありません。任意の名称を指定したら、案内に従ってアプリの登録を進めてください。 Firebase SDK の追加などの手順が表示されますが次項以降で説明するためここではスキップしてください。以上で、アプリの登録は完了です。 Firebase プロジェクトのコンソール画面で、左上の⻭車マークから「プロジェクトの設定」に遷移し、少し下にスクロールすると「マイアプリ」で追加したアプリの構成情報を確認できます。この構成情報を利用することで、アプリケーションと Firebase プロジェクトを接続できます。 

## ■サービスを設定する 

Firebase では 2 章で紹介したようにさまざまなサービスが利用できますが、サービスの中には利用前に有効化や事前に設定を必要とするものがあります。本書では、 Firestore 、 Storage 、 Authentication のサービスの設定を紹介します。まずは、 Firestore の有効化です。 Firebase プロジェクトのコンソール画面を開き、左側のパネルの「構築」から「 Firestore Database 」を選択します。 Firestore のサービスページへ遷移するので「データベースの作成」をクリックしてください。データベースを作成するには、セキュリティ保護ルールをテストモードにするか本番モードにするかを選択します。本番モードとテストモードの違いは、 Firestore のセキュリティルールの初期設定の違いです。テストモードでは 30 日間すべてのユーザーにアクセスが許可され、本番モードではすべてのアクセスが制限されます。ただし、これはあくまで初期設定であり、どちらを選んでもあとからセキュリティルールの修正が可能です。図3.3:セキュリティ保護ルールの選択次に、ロケーションを選択します。ロケーションが設定できるのは、 Firestore を有効化するこのタイミングのみです。 1 度 Firestore を有効化してしまうと変更ができないため注意してください。また、ここで指定したロケーションが、デフォルトの Google Cloud Platform リソースロケーションとなります。本書では東京リージョンの asia-northeast1を選択し、画面の案内に従って Firestore の有効化を行います。以上で Firestore の有効化が完了です。次に、 Storage です。 Storage は Firestore と同様に有効化が必要です。 Firebase プロジェクトのコンソール画面を開き、左側のパネルの「構築」から「 Storage 」を選択し、 Storage のサービスページで「始める」をクリックしてください。 Firestore と同様に、セキュリティ保護ルールの選択とロケーションの設定を行うことで Storage が有効になります。ただし、ロケーションは「デフォルトの Google Cloud Platform リソースロケーション」つまり、ここでは Firestore の有効化で設定したロケーションが適用されるので注意してください。最後に、 Authentication の設定です。 Authentication ではさまざまな認証方法を提供していますが、これらの認証機能を利用するには有効化が必要です。 Firebase プロジェクトのコンソール画面を開き、左側のパネルの「構築」から「 Authentication 」を選択し、「始める」をクリックします。「 Sign-in method 」タブに利用可能な認証プロバイダーの一覧が表示され、有効化を行うことで利用が可能となります。ここでは、次の 2 つの有効化の方法を紹介します。・ Google 認証・メールリンク認証図3.4:ログインプロバイダの一覧「 Google 」を選択し「有効にする」トグルをクリックし保存します。これで Google 認証が有効化されました。続いて、メールリンク認証も有効化します。「新しいプロバイダを追加」から、「メール / パスワード」をクリックします。「メールリンク(パスワードなしでログイン)」の「有効にする」トグルをクリックし保存します。図 3.5 の通り、 Google とメール / パスワードのステータスが「有効」となっていれば完了です。図3.5:ログインプロバイダの有効化

### **3-1-2 Firebase CLIを利用したFirebaseプロジェクトの管理** 

Firebase CLI を導入して、 CUI で Firebase プロジェクトの管理を可能にしましょう。

## ■なぜFirebase CLIを導入するのか 

Firebase の各サービスの設定は以下 2 通りの方法で行うことができます。

- 1 GUI で設定: Firebase プロジェクトのコンソール画面から設定します。 

- 2 CUI で設定: Firebase CLI が提供するfirebaseコマンドと設定ファイルを使用して設定します。基本的には、2の CUI での設定が便利です。1の GUI での設定の場合、誰がいつ設定値を変更したのかという履歴が残りません(セキュリティルールなど、一部履歴が確認可能なものもあります)。また、開発環境用と本番環境用など複数プロジェクトを利用している場合、手動で各プロジェクトへ設定値を反映させる必要があります。それに対して2の CUI での管理は、設定値を設定ファイルで管理しコマンドを利用して Firebase プロジェクトへ反映させます。設定ファイルで値を管理できるので、 Git でのバージョン管理が可能ですし、複数環境に同じ設定を簡単に反映させることができます。 CI/CD ツールとの連携も可能です。 

Firebase CLI をインストールすると、firebaseというコマンドで Firebase の各種サービスを操作できるようになります。また、 firebaseコマンドで初期設定ファイルの生成も可能です。

## ■Firebase CLIのインストールおよびFirebaseプロジェクトへのログイン 

Firebase CLI をインストールしましょう。次のコマンドを実行します。

●リスト3.1:Firebase CLIのインストール

$ yarn global add firebase-tools

インストールが完了したら、次のコマンドを実行して Firebase へログインします。

●リスト3.2:Firebaseプロジェクトへのログイン

$ firebase login

実行すると最初に使用状況とエラーレポート情報の収集を許可するか聞かれるので、「 Y 」または「 n 」で回答してください。回答すると、自動的に Google アカウントのログインページがブラウザーで立ち上がります。立ち上がらない場合は、コンソールに表示される URL からログインページへのアクセスが可能です。

Firebase プロジェクトを作成したアカウントでログインし、リクエストの内容を確認の上「許可」を選択してください。「 Firebase CLI Login Successful 」という画面が表示されればログイン成功です。図3.6:Firebase CLI Login Successful

試しに、ログインした Google アカウントに紐づく Firebase プロジェクトの一覧を表示してみましょう。次のコマンドでプロジェクト一覧を表示します。

●リスト3.3:Firebaseプロジェクトの一覧表示

$ firebase projects:list

ログインが成功していれば、アカウントに紐づくプロジェクトの一覧が表示されます。同一の Google アカウントで複数の Firebase プロジェクトを作成している場合は、複数件表示されます。

## ■firebase initコマンドで設定ファイルを生成する続いて、設定ファイルを生成しましょう。設定ファイルの生成には、firebase initコマンド(本書では「 init コマンド」と呼びま 

#### す)を実行します。 

init コマンドを実行し対話形式で質問に回答していくと、その回答に沿った設定ファイルを自動生成してくれます。質問の回答次第で生成されるファイルや内容は変わりますが、 Firestore 、 Storage 、 Functions を利用した構成の場合、次のファイルが生成されます。

- ・firebase.json:各サービスの設定値を持つ構成ファイルです。 Firestore であればセキュリティルールを記述するファイル名を、 Hosting であればデプロイするアセットファイルのディレクトリがどれかなどを設定します。 

- ・.firebaserc:エイリアスを管理するための設定ファイルです。たとえば、本番環境と開発環境の 2 つの環境を用意したい場合、 2 つの Firebase プロジェクトを作成し.firebasercファイルに設定することで、切り替えて開発できます。 

- ・firestore.rules: Firestore のセキュリティルールを記述するファイルです。 

- ・firestore.indexes.json: Firestore のインデックスを管理するファイルです。 

- ・storage.rules: Storage のセキュリティルールを記述するファイルです。 

- ・functionsディレクトリ: Functions を管理するディレクトリです。ディレクトリ内に以下ファイルが生成されます。 

- ・package.json 

- ・.eslintrc.js 

- ・tsconfig.json 

- ・tsconfig.dev.json 

- ・src/index.ts 

- ・.gitignore 

最初の 2 つfirebase.jsonと.firebasercは必ず生成され、 Firebase プロジェクトへのデプロイに必須のファイルです。それ以降のファイルは、サービスごとに必要なファイルであり、利用するサービスに応じて生成されます。これらのファイルは init コマンドを実行せずに手動でファイルを作成し設定値を記述することでも設定可能です。しかし、 init コマンドを実行することでより簡単にこれらの設定ファイルを作成できます。それでは init コマンドを実行してみましょう。 Firebase を組み込みたいプロジェクトディレクトリのルートで次のコマンドを実行します。

●リスト3.4:initコマンドの実行

$ firebase init

コマンドを実行すると、コンソール上で質問に答えながら設定をしていくことになります。順番に見ていましょう。本項の最後に質問の一覧も掲載していますので必要に応じて参照してください(リスト 3.13 )。

#### **利用するFirebaseサービスの選択** 

まずは、利用する Firebase サービスを選択します。選択したサービスによって後続の質問内容が変わりますが、本書では以下のサービスを選択した場合を紹介します。

- ・ Firestore 

- ・ Functions 

- ・ Hosting 

- ・ Storage 

- ・ Firebase Emulator 

#### **Firebaseプロジェクトの設定** 

続いて Firebase プロジェクトの設定です。プロジェクトの設定では次の質問に回答します。

- ・ Firebase プロジェクトを新規作成するか、既存のプロジェクトを対象とするか・対象の Firebase プロジェクトはどれか 

1 点目は、すでにある Firebase プロジェクトを利用するか、新規で Firebase プロジェクトを作成するかを選択します。先ほど GUI ベースで作成した Firebase プロジェクトを利用する場合は、「 Use an existing project 」を選択します。その他のオプションを選択することで、 CUI ベースでの新規プロジェクトの作成や、既存の Google Cloud プロジェクトへ Firebase を追加することも可能です。

2 点目は、設定する対象の Firebase プロジェクトを選択します。ここで選択したプロジェクトが、エイリアスを管理するための設定ファイルである.firebasercでデフォルトプロジェクトとして設定されます。たとえばanpi-prodという Firebase プロジェクトを選択した場合は、リスト 3.5 の通り .firebaserc ファイルが生成されます。.firebasercを利用したエイリアス管理についてはコラム「複数環境を管理する」で紹介していますのでそちらを参照ください。

●リスト3.5:.firebaserc

{ "projects": { "default": "anpi-prod" } }

#### **Firestoreの設定** 

以降は、最初に選択したサービスごとの設定をしていきます。 Firestore の設定では次の質問に回答します。・セキュリティルールを記述するファイルはどれか・インデックスの設定ファイルはどれかファイル名は、セキュリティルールを記述するファイルは firestore.rules 、インデックスを設定するファイルは firestore.indexes.jsonがデフォルトで指定されますが、任意の名称を指定できます。回答すると、指定したファイルが自動生成され、firebase.jsonに次の通り設定値が追加されます。

##### ●リスト3.6:firebase.jsonのFirestore設定 

{ "firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" } }

#### **Functionsの設定** 

次に、 Functions の設定です。次の質問に回答することで、 functionsディレクトリが作成され Functions の構築に必要なファイルが生成されます。

- ・ Functions で利用する言語はどれか 

- ・ ESLint を有効にするか 

- ・必要なライブラリをすぐにインストールするか 

1 つ目の Functions で利用する言語は、 TypeScript か JavaScript が選択可能です。 2 つ目の ESLint を有効にするかどうかですが、 ESLint とは JavaScript/TypeScript 向けの Linter ツールで、ルールに反した書き方をした場合に指摘をしてくれる静的解析ツールです。とくに理由がなければ有効化をお勧めします。ここまでの質問に回答すると、functionsディレクトリが作成され、配下に必要なファイルが生成されます。言語を TypeScript 、 ESLint を有効と設定した場合は次のファイルが生成されます。

- ・functions/package.json 

- ・functions/.eslintrc.js 

- ・functions/tsconfig.json 

- ・functions/tsconfig.dev.json 

- ・functions/src/index.ts 

- ・functions/.gitignore 

また、firebase.jsonにも質問の回答が反映され、 Functions のデプロイ前に実行されるコマンドが設定されます。 ESLint を有効化すると、デプロイ前に ESLint のチェックが行われ、ルールに反する場合はデプロイされません。

●リスト3.7:firebase.jsonのFunctions設定

{ "functions": { "predeploy": [ "npm --prefix \"$RESOURCE_DIR\" run lint", "npm --prefix \"$RESOURCE_DIR\" run build" ] } }

最後に、必要なライブラリをすぐにインストールするかを問われるので、「 Y 」または「 n 」で回答してください。

#### **Hostingの設定** 

続いて Hosting の設定です。次の質問に回答するとfirebase.json に設定値が追加されます。

- ・ Hosting を利用してデプロイするアセットファイルはどれか 

- ・ SPA 構成とするか 

- ・ GitHub を利用した自動ビルドとデプロイ設定をするか 

1 つ目は、 Web サービスのビルド後のファイルの格納場所を指定します。たとえば、 anpi! の構成ではビルド後のファイルはdistディレクトリに生成されるため、distと指定します。 2 つ目は SPA 構成とするか否かを回答します。これらの質問の回答は、firebase.jsonに反映されます。

●リスト3.8:firebase.jsonのHosting設定

{ "hosting": { "public": "dist", "ignore": [ "firebase.json", "**/.*", "**/node_modules/**" ], "rewrites": [ { "source": "**", "destination": "/index.html" } ] }, }

#### 少し詳しく見てみましょう。 

- ・public:デプロイするアセットファイルの格納先ディレクトリです。 1 つ目の質問の回答が反映されます。 

- ・ignore:デプロイ時に無視するファイルです。質問の回答に関わらず自動的に指定されます。 

- ・rewrites:sourceへ指定した条件に合致した URL へリクエストすると、代わりにdestinationへ指定したコンテンツが表示されます。これによって、複数の URL に対して同じコンテンツを表示させることが可能です。 2 つ目の質問で SPA 構成を有効にした場合のみリスト 3.8 の通り設定されます。最後に GitHub を利用した自動ビルドとデプロイ設定するかの選択を求められます。本書では扱いませんが、 GitHub を利用している方は設定が可能です。 

#### **Storageの設定** 

次に Storage の設定です。次の質問に回答すると、セキュリティルールを記述するファイルが自動生成され、firebase.jsonへ設定値が追加されます。・セキュリティルールを記述するファイルはどれかデフォルトではstorage.rulesが指定されますが、任意のファイル名を指定できます。また、質問の回答は次の通りfirebase.json に反映されます。

●リスト3.9:firebase.jsonのStorage設定

{ "storage": {

"rules": "storage.rules" } }

#### **Firebase Emulatorの設定** 

最後に、 Firebase Emulator の設定です。具体的な設定内容の前に、 Firebase が提供しているエミュレータについて説明します。前述した通り、 Firebase プロジェクトと Firestore や Storage などの各リソースは一対一で紐づきます。チーム開発をする場合、このリソースを複数メンバーで共有してしまうと、動作確認がとれていないものがデプロイされてしまったり、テストデータが混在してしまったりとさまざまな問題が生じます。そこで登場するのが、 Firebase Local Emulator Suite (本書では「 Firebase Emulator 」と呼びます)です。 Firebase Emulator を利用することで、 Firebase サービスの動作をローカル環境で再現できます。たとえば、 Firestore のエミュレータを利用することで、クラウド環境上の Firestore データに影響を与えることなく、開発者ごとに独立したテストが可能となります。また、 Firebase Emulator のそれぞれのサービスは独立しているため、 Functions はクラウド環境上の Firebase プロジェクトへ接続し Firestore は Firebase Emulator へ接続するといったことも可能です。本書執筆時点では以下の 8 サービスをサポートしています。

- ・ Firestore 

- ・ Realtime Database 

- ・ Storage 

- ・ Authentication 

- ・ Functions 

- ・ Pub/Sub 

- ・ Hosting 

- ・ Extensions 

Firebase Emulator で利用したサービスに対しては課金が行われません。クラウドコストを負担せずに開発ができるため、開発費用も抑えることができます。メリットばかりに見えますが、 Firebase Emulator はあくまで Firebase サービスを再現しているものであり、まったく同じ動作をするものではないという点には注意してください。たとえば、 Firestore のエミュレータでは複合インデックスが追跡されません。ほかにも、クラウド環境上では適用される制限がエミュレータでは許容される場合があるなど、いくつかの差異があります。加えて、 Firebase Emulator を利用したローカル開発ではインターネットを介した Firebase とのやりとりがなくなるぶん、ネットワークの遅延がなくなる点も考慮してください。それでは Firebase Emulator の設定内容に戻りましょう。次の質問に回答することで、firebase.jsonに設定値が追加されます。

- ・ Firebase Emulator を利用する Firebase サービスはどれか 

- ・それぞれのサービスで利用するポート番号はどれか 

- ・ Emulator UI を利用するか 

- ・エミュレータライブラリをすぐにインストールするか最初に、 Firebase Emulator を利用するサービスの選択を求められます。本書では、以下のサービスを対象とします。 

- ・ Authentication Emulator 

- ・ Functions Emulator 

- ・ Firestore Emulator 

- ・ Hosting Emulator 

#### ・ Storage Emulator 

2 つ目の、それぞれのサービスで利用するポート番号は、次の通りデフォルト値が指定されます。すでに利用しているポート番号があれば任意のポート番号を指定可能です。

- ・ Authentication Emulator : 9099 

- ・ Functions Emulator : 5001 

- ・ Firestore Emulator : 8080 

- ・ Hosting Emulator : 5000 

- ・ Storage Emulator : 9199 

3 つ目の質問では Emulator UI の利用有無を指定します。有効にすると GUI のエミュレータ管理画面を利用でき、 Firestore のデータへのアクセスやログの確認が GUI で可能になります。便利な機能なので有効化をお勧めします。ここまでの質問の回答が、次の通りfirebase.jsonに反映されます。

●リスト3.10:firebase.jsonのFirebase Emulator設定

{ "emulators": { "auth": { "port": 9099 }, "functions": { "port": 5001 }, "firestore": { "port": 8080 }, "storage": { "port": 9199 }, "ui": {

"enabled": true } } }

それぞれのサービスで利用するポート番号と、 Emulator UI の有効 / 無効の設定が反映されています。最後に、エミュレータライブラリのインストールを今行うか問われるので、「 Y 」または「 n 」で回答してください。

⻑くなりましたが、これで init コマンドの実行が完了です。コンソールに「 Firebase initialization complete! 」と表示され、 firebase.jsonと.firebasercが作成されれば完了です。これまでの質問の回答が反映された、firebase.jsonと.firebasercをリスト 3.11 と 3.12 に示します。

●リスト3.11:firebase.json

{ "firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" }, "functions": { "predeploy": [ "npm --prefix \"$RESOURCE_DIR\" run lint", "npm --prefix \"$RESOURCE_DIR\" run build" ] }, "hosting": { "public": "dist", "ignore": [ "firebase.json", "**/.*", "**/node_modules/**" ], "rewrites": [ {

"source": "**", "destination": "/index.html" } ] }, "storage": { "rules": "storage.rules" }, "emulators": { "auth": { "port": 9099 }, "functions": { "port": 5001 }, "firestore": { "port": 8080 }, "hosting": { "port": 5000 }, "storage": { "port": 9199 }, "ui": { "enabled": true } } }

●リスト3.12:.firebaserc

{ "projects": { "default": "anpi-prod" } }

設定を変更したい場合はfirebase.jsonや.firebasercを直接変更し、 Firebase プロジェクトへデプロイします。デプロイの方法は後述します。最後に、質問の一覧を掲載します。分かりやすさを優先し一部記載を省略しています。

●リスト3.13:initコマンドの質問一覧

? Which Firebase features do you want to set up for this directory? ( ) Realtime Database

- (*) Firestore 

(*) Functions >(*) Hosting (*) Storage (*) Emulators === Project Setup ? Please select an option: > Use an existing project Create a new project Add Firebase to an existing Google Cloud Platform project Don't set up a default project ? Select a default Firebase project for this directory: anpi-prod (anpi-prod) === Firestore Setup ? What file should be used for Firestore Rules? firestore.rules ? What file should be used for Firestore indexes? firestore.indexes.json === Functions Setup ? What language would you like to use to write Cloud Functions? JavaScript > TypeScript ? Do you want to use ESLint to catch probable bugs and enforce style? Yes +  Wrote functions/package.json +  Wrote functions/.eslintrc.js +  Wrote functions/tsconfig.json +  Wrote functions/tsconfig.dev.json +  Wrote functions/src/index.ts +  Wrote functions/.gitignore Do you want to install dependencies with npm now? Yes === Hosting Setup

- ? What do you want to use as your public directory? public 

- ? Configure as a single-page app? Yes 

- ? Set up automatic builds and deploys with GitHub? No +  Wrote public/index.html === Storage Setup 

- ? What file should be used for Storage Rules? storage.rules +  Wrote storage.rules 

=== Emulators Setup ? Which Firebase emulators do you want to set up? (*) Storage Emulator (*) Authentication Emulator >(*) Functions Emulator (*) Firestore Emulator (*) Hosting Emulator ? Which port do you want to use for the auth emulator? 9099 ? Which port do you want to use for the functions emulator? 5001 ? Which port do you want to use for the firestore emulator? 8080 ? Which port do you want to use for the hosting emulator? 5000 ? Which port do you want to use for the storage emulator? 9199 ? Would you like to enable the Emulator UI? Yes ? Which port do you want to use for the Emulator UI (leave empty to use any available port)?

? Would you like to download the emulators now? Yes i  Writing configuration info to firebase.json... i  Writing project information to .firebaserc...

+  Firebase initialization complete! 

## ■Firebaseプロジェクトへデプロイする 

init コマンドで生成した設定ファイルを Firebase プロジェクトへ反映するためにデプロイを実行しましょう。まずはデプロイ先の Firebase プロジェクトを確認します。次のコマンドを実行してください。

●リスト3.14:デプロイ先プロジェクトの確認

$ firebase use

実行すると、.firebasercで指定しているプロジェクトの一覧が表示され、今利用しているプロジェクトに*がついています。この* がついているプロジェクトがデプロイ先となります。変更したい場合は、$ firebase use [エイリアス名]コマンドを実行してプロジェクトを切り替えてください。デプロイ先のプロジェクトが確認できたら、デプロイするアセットファイルを生成するために必要に応じてビルドしてください。以上の準備が完了したら、次のコマンドを実行し Firebase プロジェクトへデプロイします。

●リスト3.15:デプロイ

$ firebase deploy

firebase deployコマンドは、 Hosting やセキュリティルール、 Functions などすべてのサービスを一括でデプロイします。--onlyオプションを利用することで特定のサービスを指定してデプロイも可能です。以下サービスをサポートしており、コンマ区切りで複数のサービスを指定することも可能です。

- ・hosting: Hosting コンテンツ 

- ・database: Firebase Realtime Database のルール(本書では Firebase Realtime Database は扱いません) 

- ・storage: Storage のセキュリティルール 

- ・firestore: Firestore のセキュリティルールとインデックス 

- ・firestore:rules: Firestore のセキュリティルールのみ 

- ・firestore:indexes: Firestore のインデックスのみ 

- ・functions: Functions 関数たとえば、 Hosting と Firestore のセキュリティルールのみをデプロイしたい場合、以下のコマンドを実行します。 

●リスト3.16:デプロイコマンド onlyオプションの利用

$ firebase deploy --only hosting,firestore:rules

### **3-1-3 FirebaseプロジェクトとWebサービスの紐づけ** 

前項では、 CUI で Firebase プロジェクトの管理やデプロイをできるようにしました。続いて、 Web サービス内で Firebase を利用できるようにしていきましょう。ここからは、サンプルアプリ anpi! を例に Firebase プロジェクトと Web サービスの紐づけ方法を説明します。 anpi! のソースコードは GitHub 上に公開していますので、必要に応じて手元にダウンロードしてください。

Web サービスで Firestore や Authentication などの Firebase サービスを利用するには、 Firebase で提供されている SDK を利用します。 Firebase で提供されている SDK には、大きく分けて次の 2 種類があります。

1クライアント向け

2サーバーサイド向け

1のクライアント向けの SDK は、クライアントから Firestore や Storage などのリソースに直接アクセスするため、セキュリティルールでアクセス権限のチェックが実施されます。一方、2のサーバーサイド向けの SDK はセキュリティルールを無視して実行され、クライアント向けの SDK ではできない処理も実行が可能です。 2 章で紹介した Functions は、このサーバーサイド向けの SDK である Firebase Admin SDK を利用しています。ここでは、1クライアント向けの SDK である Firebase JavaScript SDK (本書では「 Firebase SDK 」と呼びます)を利用します。早速 Firebase SDK をインストールしましょう。 anpi! ではpackage.jsonに追加済みのため、yarn installコマンドの実行でインストール可能です。プロジェクトのルートディレクトリで次のコマンドを実行し、ライブラリをインストールしてください。

●リスト3.17:ライブラリのインストール

$ yarn install

続いて Web サービスのソースコード内で Firebase SDK を利用するには、最初にinitializeApp関数で初期化処理を行う必要があります。 anpi! では、src/firebase.tsでこの処理を行っています。実際のコードを見てみましょう。

●リスト3.18:initializeAppの実行(src/firebase.ts)

import { FirebaseApp, initializeApp } from 'firebase/app'; import { Auth, getAuth } from 'firebase/auth'; import { Firestore, getFirestore, } from 'firebase/firestore'; import { Functions, getFunctions, } from 'firebase/functions'; import { FirebaseStorage, getStorage, } from 'firebase/storage'; const firebaseConfig = { // [Note] 自分のFirebaseプロジェクトの値を以下に設定してください apiKey: 'xxx',

authDomain: 'xxx', projectId: 'xxx', storageBucket: 'xxx', messagingSenderId: 'xxx', appId: 'xxx', measurementId: 'xxx', }; const firebase: FirebaseApp = initializeApp(firebaseConfig); const region = 'asia-northeast1'; const auth: Auth = getAuth(); const db: Firestore = getFirestore(); const storage: FirebaseStorage = getStorage(); const functions: Functions = getFunctions(firebase, region); // 略 export { firebase, auth, db, storage, functions };

リスト 3.18 のfirebaseConfigに Firebase プロジェクトに登録したアプリの構成情報を埋め込むことで、 Web サービスと Firebase プロジェクトの紐づけが可能です。「 3-1-1 Firebase プロジェクトの初期セットアップ」で Firebase プロジェクトに登録したアプリを紐づけてみましょう。アプリの構成情報は、 Firebase プロジェクトのコンソール画面から確認が可能です。「プロジェクトの設定」画面の「全般」タブを表示します。「マイアプリ」>「 SDK の設定と構成」で、「 Config 」ラジオボタンを選択して表示されるfirebaseConfigが構成情報です。コピーし、src/firebase.tsのfirebaseConfigを書き換えます。図3.7:firebaseConfigの確認

firebaseConfigには、 API キーが含まれるため秘匿すべき情報のように思うかもしれませんが、公式ドキュメントでも記載のある通り安全にコードに含めることができます。あくまでfirebaseConfig の値はクライアント向けの SDK を利用するための情報であり、クライアント向けの SDK からのアクセスはセキュリティルールで保護されるためです。そのため、 2 章でも言及した通り、セキュリティルールは十分考慮し適切に設定する必要があります。

initializeApp 関数を実行したあとは、 getAuth 関数や getFirestore関数を用いて各サービスのインスタンスを生成することで各サービスが利用可能になります。以上で、 Firebase プロジェクトと anpi! の紐づけが完了です。

#### **【コラム】複数環境を管理する** 

実際の開発では、開発環境、ステージング環境、本番環境など、複数の環境を切り替えて開発することが多いでしょう。Firebaseを利用したWebサービスの開発では、複数環境を管理するには2つの方法が考えられます。

1同一Firebaseプロジェクトを利用して、複数のHostingサイトを扱う。 2Firebaseプロジェクトを複数作成する。

1の方法は、環境ごとにHostingで複数環境ぶんのリソースをそれぞれ公開しつつ、 FirestoreやStorageを共有する方法です。Firebaseプロジェクトには複数のHostingサイトを設定できるため、この方法が可能です。ただし、Storageはbucketを複数作成することで分離が可能ですが、Firestoreは論理的に分離させるしかありません。環境を分離させることを考慮したFirestoreのデータモデル設計が必要になり、不用意に複雑になってしまうおそれがあります。一方2の方法は、1環境につき1Firebaseプロジェクトを作成する方法です。管理する Firebaseプロジェクトは増えますが、明確にFirebaseプロジェクトが分かれるため、環境の分離を考慮した設計等は不要になります。開発/本番などの環境を用意するにはこちらが適しているでしょう。

.firebasercを利用してエイリアスを管理することで2の方法が実現可能です。initコマンド実行の際にも触れましたが、複数のFirebaseプロジェクトを同じプロジェクトディレクトリに関連付けることができ、必要に応じて切り替えながら開発が可能です。initコマンド実行後の.firebasercを確認してみましょう。

●リスト3.19:.firebasercのデフォルト設定

{ "projects": { "default": "anpi-prod", } }

この時点では、defaultのみが指定されています。defaultというエイリアス名に対して、Firebaseプロジェクトanpi-prodが紐づいている状態です。

●リスト3.20:.firebasercの複数エイリアス管理設定

{ "projects": { "dev": "anpi-dev", "prod": "anpi-prod" } }

たとえば、projectsをリスト3.20のように指定すると、次の通りにプロジェクトとエイリアス名を紐づけることができます。

||**エイリアス名**||**Firebaseプロジェクト**|
|---|---|---|---|
|dev||anpi-dev||
|prod||anpi-prod||

この設定は、.firebasercファイルを直接変更する方法のほかに、firebase use --addコマンドを実行することでも追加が可能です。 .firebasercで指定したエイリアスは、firebase useコマンドを利用してプロジェクトの切り替えが可能です。firebase useコマンドを実行すると、エイリアスのリストを表示し、現在利用しているプロジェクトを確認できます。

●リスト3.21:利用中のエイリアスの確認

$ firebase use Active Project: dev (anpi-dev) * dev (anpi-dev) prod (anpi-prod)

firebase use [エイリアス名]とエイリアス名を指定すると、指定したエイリアスに紐づくFirebaseプロジェクトへ切り替えることができます。

●リスト3.22:エイリアスの切り替え

$ firebase use prod Now using alias prod (anpi-prod)

これはあくまで、デプロイ先のFirebaseプロジェクトです。WebサービスとFirebaseプロジェクトの紐づけは、initializeAppに指定するfirebaseConfigの値で決まります。firebaseConfigの値を、envファイルに切り出し、dotenvやcrossenvなどのライブラリを利用して環境ごとに切り替えることで、Webサービス内で接続するFirebaseプロジェクトを切り替えることが可能です。

### **3-1-4 Firebase Emulatorを利用したローカル開発環境の構築** 

Firebase Emulator を利用したローカル開発環境を構築しましょう。 Firebase Emulator を利用するには以下環境が必要です。 Java JDK は、公式ドキュメントで案内のある通り OpenJDK の利用を推奨します。

- ・ Node.js バージョン 8.0 以降 

- ・ Java JDK バージョン 11 以降前項で anpi! と Firebase プロジェクトの紐づけを行いましたが、今の状態では、ローカルサーバーで anpi! を立ち上げた場合もクラウド環境上の Firebase プロジェクトへアクセスしてしまいます。そこで、ローカルサーバーで立ち上げた場合は Firebase Emulator に、 Hosting サービスでデプロイされた環境からはクラウド環境上の Firebase プロジェクトへ接続されるようにしましょう。 anpi! のサンプルコード src/firebase.tsで実装しています。 

●リスト3.23:Firebase Emulator設定(src/firebase.ts)

import { FirebaseApp, initializeApp } from 'firebase/app'; import { Auth, getAuth, connectAuthEmulator } from 'firebase/auth'; import { Firestore, getFirestore, connectFirestoreEmulator, } from 'firebase/firestore'; import { Functions, getFunctions, connectFunctionsEmulator, } from 'firebase/functions'; import { FirebaseStorage,

getStorage, connectStorageEmulator, } from 'firebase/storage'; const firebaseConfig = { // 略 }; const firebase: FirebaseApp = initializeApp(firebaseConfig); const region = 'asia-northeast1'; const auth: Auth = getAuth(); const db: Firestore = getFirestore(); const storage: FirebaseStorage = getStorage(); // Emulator上のFunctionsのリージョンは、明示しないとus-central1になる const functions: Functions = getFunctions(firebase, region); const useEmulator = window.location.hostname === 'localhost'; if (useEmulator) { connectAuthEmulator(auth, 'http://localhost:9099', { disableWarnings: true }); connectFirestoreEmulator(db, 'localhost', 8080); connectStorageEmulator(storage, 'localhost', 9199); connectFunctionsEmulator(functions, 'localhost', 5001); } // 略

localhost から接続された場合のみ、 Firebase Emulator に接続するよう設定しています。 Firebase Emulator へ接続するには connectAuthEmulatorなど、サービスごとに用意されている関数を利用し、ポート番号はfirebase.jsonに設定しているものを指定します。準備はこれだけです。実際に Firebase Emulator を起動してみましょう。今回、 Functions は TypeScript を利用しているため、事前にトランスパイルが必要です。次のコマンドを実行してfunctionsディレクトリをビルドします。

●リスト3.24:Functionsのビルド

$ yarn --cwd functions install & yarn --cwd functions run build

続いて、 Firebase Emulator を立ち上げます。次のコマンドを実行してください。

●リスト3.25:Firebase Emulatorの起動

$ firebase emulators:start --import=./export

--importオプションの引数にディレクトリを指定することで、そのディレクトリに置かれたデータセットファイルを取り込んで Firebase Emulator を立ち上げます。 anpi! では、exportディレクトリに Firestore と Authentication のサンプルデータを提供しています。 Firebase Emulator は停止するとデータが消えてしまいますが、起動時に--export-on-exitオプションを指定することで、停止時のデータをエクスポートさせることも可能です。また、クラウド環境上の Firebase プロジェクトからデータをエクスポートし、 Firebase Emulator に取り込むことも可能です。コマンド実行後「All emulators ready!」と表示されれば、エミュレータが起動されています。早速、 Emulator UI を見てみましょう。View Emulator UI at に続く URL へアクセスしてください。デフォルトではhttp://localhost:4000が利用されます。図3.8:Firebase Emulator UI

Overview タブでは、各サービスのステータスを確認できます。 init コマンド実行時に設定した 5 つのサービスの Status が「 On 」になっていることが分かります。また、 Emulator UI では Firebase Emulator のサービスの管理を GUI ベースで行うことができます。試しに、 Firestore タブを開いてみると、 anpi! のサンプルコードで提供しているサンプルデータを確認することができます。

Firebase Emulator が正常に立ち上がることを確認したら、続いて anpi! を立ち上げてみましょう。 Firebase Emulator を立ち上げている状態でリスト 3.26 のコマンドを実行してください。「 3-1-3 Firebase プロジェクトと Web サービスの紐づけ」で紹介したyarn installコマンドを実行していない場合は、先に実行してください。

●リスト3.26:anpi!の起動

$ yarn quasar dev

http://localhost:9000で anpi! が立ち上がります。これで Firebase Emulator を利用したローカル開発環境の構築が完了です。

#### **【コラム】Firebase SDK v9のリリース** 

2021年8月25日にFirebase JavaScript SDKのv9がリリースされました。本書執筆時点ではv8とv9の2つが提供されています。バージョンを指定せずにFirebase SDKをインストールすると自動的にv9がインストールされます。このv9ですが、前バージョンのv8と互換性がない点に注意してください。v8で書いたコードをv9にアップデートするには大きな改修が必要になります。

v9ではモジュール形式が採用されており、必要な機能だけを必要な場所で読み込むことができるようになりました。モジュール形式となったことでツリーシェイキングが容易になり、v8に比べてバンドルサイズが大幅に削減できます。筆者らも、v9に切り替えたことでビルドが速くなるなどパフォーマンスの向上を実感できました。しかし、このアップデートによりソースコードの書き方が大きくが変わりました。 Firestoreへドキュメントを追加するサンプルコードで比較してみましょう。

●リスト3.27:Firebase SDK v8のサンプル

import firebase from 'firebase/app'; import 'firebase/firestore'; const db = firebase.firestore(); // citiesコレクションにドキュメントを追加する await db.collection('cities').doc('LA').set({ name: 'Los Angeles', state: 'CA', country: 'USA' });

●リスト3.28:Firebase SDK v9のサンプル

import { doc, setDoc } from 'firebase/firestore'; // citiesコレクションにドキュメントを追加する await setDoc(doc(db, 'cities', 'LA'), { name: 'Los Angeles', state: 'CA', country: 'USA' });

v8ではfirebase/firestoreを一括で読み込んでいるのに対し、v9では必要なモジュール単位で読み込んでいます。このように、v9では使用する機能のみを読み込むことができるため、アプリサイズを小さくできるのです。その結果、v8では数珠つなぎにsetメソッドを呼び出しているのに対し、v9ではsetDoc関数を直接呼び出すなど書き方が大きく変わりました。

v8は将来的にサポート終了が確定しているため、新しく作成するアプリではv9を利用することをお勧めします。既存のアプリもv9へのアップデートが必要になりますが、規模が大きいアプリでは一括でアップデートを行うのは困難です。アップデートを段階的に実施可能とするために Compatという互換性ライブラリも提供されています。Compatライブラリを利用することでv8とv9のコードを共存させることができます。ただし、こちらも将来的にサポート終了が確定しており、Compatライブラリでは前述したv9でのメリットが得られません。あくまで段階的にアップデートするために利用するべきです。

Web上の紹介記事ではv8の利用方法を記載するものも多いため、ご自身で調査する際は Firebase SDKのバージョンには注意してください。

1. <u>https://console.firebase.google.com</u> 

## **3-2 Firebase Authenticationで認証機能を実装する** 

Firebase Authentication を利用した認証機能の実装を見ていきましょう。 Authentication を利用することで認証機能を簡単に実装できます。 Authentication では、基本的なメールアドレスとパスワードの認証や、 Google や Apple などの主要なプロバイダーと統合した認証などさまざまな認証方法が提供されています。本書ではその中でも Google 認証とメールリンク認証を紹介します。

### **3-2-1 Google認証の実装** 

まずは Google 認証です。 Google 認証の有効化を行っていない場合は、「 3-1-1 Firebase プロジェクトの初期セットアップ」で紹介している手順で有効化してください。

anpi! では、ログイン画面の「 GOOGLE アカウントでログイン」ボタンが Google 認証を呼び出しています。クリックするとポップアップウィンドウが開き、 Google アカウントでログインすると、トップ画面へ遷移しログインが完了するという動きになっています。それでは、実装を見ていきましょう。 src/stores/auth.tsの loginWithGoogleメソッドで実装しています。

●リスト3.29:src/stores/auth.ts

import { signInWithPopup, GoogleAuthProvider

} from 'firebase/auth'; import { auth } from 'src/firebase'; // 略

const provider = new GoogleAuthProvider(); async loginWithGoogle() { // 略 await signInWithPopup(auth, provider); // 略 }

まず、 Firebase SDK から必要なモジュールを import しています。実際のサンプルコードではほかにもいくつかモジュールを import していますが、ここでは説明の便宜上、 Google 認証の実装に利用しているもののみに絞っています。また、src/firebase.tsで export した authインスタンスも import しています。メインの処理はsignInWithPopup関数です。引数には、authインスタンスとGoogleAuthProviderクラスのインスタンスを渡します。 signInWithPopup関数を実行すると、 Google アカウントでログインを求めるポップアップウィンドウを開き、ログインが成功するとユーザーとプロバイダーの資格情報を返します。つまり、 signInWithPopup関数を呼び出すだけで Google 認証の実装が完了するのです。認証したユーザーの一覧は、 Emulator UI で確認できます。「 3-1-4 Firebase Emulator を利用したローカル開発環境の構築」で紹介した通り Firebase Emulator を立ち上げ、 Emulator UI の「 Authentication 」タブを開いてください。認証したユーザーのメールアドレスや、認証プロバイダーの種類、 UID などを確認できます。もちろん、クラウド環境上の Firebase プロジェクトのコンソール画面でも同様に確認が可能です。図3.9:認証ユーザー一覧

### **3-2-2 メールリンク認証の実装** 

続いて、メールリンク認証を実装しましょう。メールリンク認証とは、ログインの際に指定したメールアドレスに対してメールを送信し、本文中に記載された URL がクリックされたイベントを受信することで、本人確認がとれたと見なす認証方法です。早速コードを見ていきましょう。メールリンク認証では 2 つのメソッドを実装します。 anpi! では src/stores/auth.tsの以下メソッドで実装しています。

- 1sendMailLink():指定したメールアドレス宛に、認証リンク付きのメールを送信します。 

- 2verifyMailLink():認証リンクをクリックしアクセスした場合に呼び出され、本人確認をし、認証します。 

1 つずつ見ていきましょう。まずは1の認証リンク付きのメールを送信するメソッドです。

- ●リスト3.30:src/stores/auth.ts 

import { sendSignInLinkToEmail } from 'firebase/auth'; import { auth } from 'src/firebase'; // 略 async sendMailLink(email: string) { const host = window.location.hostname === 'localhost' ? 'http://localhost:9000' : 'https://' + window.location.hostname; const actionCodeSettings = { url: host + '/login', handleCodeInApp: true, }; // 略 await sendSignInLinkToEmail(auth, email, actionCodeSettings); // 同一の環境・ブラウザーでメールリンクを開いた場合、メールアドレス入力を省略できるようにLocalStorageに保持 localStorage.setItem('emailForSignIn', email); // 略 }

Firebase SDK で提供されているsendSignInLinkToEmail関数を利用することで、認証リンク付きのメールを送信できます。デフォルトの状態では Firebase が用意したドメインから送信されます。迷惑メールとしてブロックされることがあるため、必要に応じて SMTP サーバーを自前で用意してください。 Firebase プロジェクトのコンソール画面の、 Authentication サービス画面「 Templates 」タブにて SMTP 設定が可能です。また、 Firebase Emulator を利用したローカル開発環境では、 sendSignInLinkToEmail関数を実行してもメールは送信されません。コンソール画面に認証用のリンクだけが生成され、疑似的なメールリンク認証が可能です。こちらは後ほど実際に動かしながら確認してみましょう。

sendSignInLinkToEmail関数は、 Auth インスタンス、メールリンク認証に利用するメールアドレス、設定値オブジェクトを引数に渡すことで利用できます。リスト 3.30 を見てみましょう。設定値オブジェクトのactionCodeSettingsには、urlとhandleCodeInAppを指定します。urlは認証リンクをクリックした際のリダイレクト先を指定します。 anpi! ではログインページを指定しており、ローカル開発環境とデプロイされた環境からアクセスした場合とで host を切り替えています。handleCodeInAppにはメールリンクがアプリで開かれるかどうかを指定します。メールリンク認証では、常にアプリで開かれる必要があるためtrueを指定します。

sendSignInLinkToEmailが成功した場合、続いて LocalStorage にメールアドレスを保存しています。これは、もう 1 つの認証リンクのクリック後に実行されるverifyMailLink関数で利用するためです。それではverifyMailLink関数の実装も見ていきましょう。

●リスト3.31:src/stores/auth.ts

import { isSignInWithEmailLink, signInWithEmailLink } from 'firebase/auth'; import { auth } from 'src/firebase'; // 略 async verifyMailLink(url: string) { if (isSignInWithEmailLink(auth, url)) { let email: string | null = localStorage.getItem('emailForSignIn'); if (!email) { // メールリンクを要求した時点とは異なる環境でメールリンクを開いた場合に該当する // セッション固定化攻撃を回避するために、ユーザー自身に要求したメールアドレスを入力させる email = window.prompt('メールアドレスを再度入力してください'); } if (!!email) { return signInWithEmailLink(auth, email, url) .then(() => { localStorage.removeItem('emailForSignIn'); }) .catch((error) => { // 略 }); }

} }

まず、このverifyMailLink関数はユーザーがログイン画面にアクセスするたびに実行されます。 anpi! では認証リンクをクリックした際のリダイレクト先をログインページに指定しているため、都度メールリンク認証によってアクセスされたかどうかの判断が必要になります。メールリンクを使用したアクセスであるかどうかは Firebase SDK で提供されているisSignInWithEmailLink関数を利用して確認が可能です。 Firebase が自動的に発行する認証リンクには、いくつかのクエリパラメータが付与されており、その値をもとに判断し結果を返します。メールリンク認証によってアクセスされたと判断した場合、続いて LocalStorage のemailForSignInの値を確認しています。これは、 1の認証リンク付きのメール送信が成功したあとに、 LocalStorage に保存していたものです。なぜ LocalStorage を利用するのでしょうか。答えは、メールリンク認証を要求したものと同じデバイスでアクセスされていることを確認するためです。万が一認証リンクが漏れてしまった場合、第三者が認証リンクを利用してログインできてしまっては困ります。意図しないユーザーが、意図しないデバイスでログインできないように、メールリンク認証を要求するのと同じデバイスで認証リンクを開いているかを確認しているのです。異なるデバイスから開かれた場合は、ユーザー自身にメールアドレスを再度入力させています。これらの確認が済んだら、 signInWithEmailLink関数を利用して認証します。それでは実際に動かしてみましょう。「 3-1-4 Firebase Emulator を利用したローカル開発環境の構築」で紹介した通り Firebase Emulator を立ち上げたのち、 anpi! を立ち上げてください。

http://localhost:9000にアクセスすると anpi! のログイン画面が表示されます。

anpi! では事前に登録されたユーザーのみログインが可能です。ここでは、サンプルデータのユーザーでログインしてみましょう。 Emulator UI の「 Authentication 」タブに登録されているユーザーであればログインが可能です。該当のユーザー行の三点リーダー (「 ... 」)から「 Edit User 」を選択することで、メールアドレスなどのユーザー情報の確認が可能です。ここでは、メールリンク認証で必要なメールアドレスを控えてください。図3.10:認証ユーザー情報

anpi! のログイン画面で、先ほど確認したメールアドレスを入力し「メールリンクでログイン」ボタンをクリックします。「メールリンク送信完了」と表示されれば、 1 つ目に実装したsendMailLinkメソッドが正常に実行されています。クラウド環境上の Firebase プロジェクトに接続されている環境の場合は、ここでメールが送信されますが、前述した通りローカル開発環境ではメールは送信されません。 Firebase Emulator を立ち上げたコンソール画面を確認してみましょう。図3.11:コンソールに表示される認証リンクこのように、本来はメールに記載される認証リンクがコンソール上に表示されます。このリンクにアクセスすると、 2 つ目に実装した verifyMailLinkメソッドが呼ばれ、自動的にログイン処理が完了し anpi! のトップ画面に遷移します。クラウド環境上の Firebase プロジェクトに接続されている環境では、図 3.12 のようなメールが送信されます。メールの文面は変更できませんが、言語は Firebase プロジェクトのコンソール画面で設定が可能です。デフォルトでは英語となっています。

##### 図3.12:認証リンク付きのメールサンプルここまでログイン処理を見てきましたが、ログアウト処理も実装しましょう。 Firebase SDK ではsignOut関数が提供されており、どの認証方法を利用してログインしていても、このsignOut関数を実行するだけでログアウトできます。ここでは実際のソースコードは割愛しますので、興味があればサンプルコードを参照ください。 

## **3-3 Firestoreでデータを永続化する** 

Firebase の代表的なサービスである Firestore を利用して、データを永続化しましょう。本節では、まず Firestore の基本的な CRUD 操作を紹介します。その後、 Firestore のコレクションとドキュメントという構造体を利用して親子関係のデータを扱う実装例や、 Firestore 特有の機能であるリアルタイムリスナーの実装例を紹介します。

### **3-3-1 基本のCRUD操作** 

まずは、 Firestore の基本の CRUD 操作を、 anpi! のアプリ管理者向け機能である「安否確認管理」を例に見ていきましょう。

## ■ドキュメントの作成まずは安否確認の登録処理を例にドキュメントの作成を見ていきましょう。「安否確認管理」メニューから新規追加が可能です。 2 章で整理した通り、安否確認はsafetyConfirmationsコレクションに、以下フィールドを持たせたドキュメントを登録することで管理します。 

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|title|タイトル|文字列|"Y年M月のXX地震における安否確認"|
|body|本文|文字列|"緊急で社員の安否を確認します"|
|hazardType|災害種別|文字列|"地震"、"風水害"、"その他"のいずれか|
|dueDate|回答期日|文字列|"2022/12/31"|

##### 図3.13:安否確認の新規登録それでは早速コードを見てみましょう。 src/pages/ApplicationManager/SafetyConfirmationNew.vue の addSafetyConfirmationで実装しています。 

- ●リスト3.32:src/pages/ApplicationManager/SafetyConfirmationNew.vue 

import { db } from 'src/firebase' import { collection, addDoc } from 'firebase/firestore'; // 略 const addSafetyConfirmation = async () => { const safetyConfirmation: SafetyConfirmation = { title: title.value, body: body.value || '', hazardType: hazardType.value, dueDate: dueDate.value, }; // 略 const ref = collection( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations' ); await addDoc(ref, safetyConfirmation); };

まず、 Firebase SDK より必要なモジュールをインポートします。ここでは、collectionとaddDoc関数をインポートしています。どのように利用するのか 1 つずつ見ていきましょう。

1 つ目のcollecion関数は、CollectionReferenceを取得します。 Firestore には「リファレンス」という考え方あり、 Firestore のすべてのコレクションとドキュメントは、データベース内の場所によって一意に識別されます。コード内でそのコレクションもしくはドキュメントを参照するのに必要なのが「リファレンス」です。このリファレンスは、データベース内の場所を示すだけであり、実データは持ちません。また、データが存在するかどうかに関わらず作成でき、リファレンスの作成には実際に Firestore へのアクセスは行われません。

collecion関数は、第 1 引数には Firestore インスタンスを、第 2 引数以降にコレクションへのパスを指定することで、 CollectionReferenceが取得できます。リスト 3.32 では、安否確認ドキュメントの格納先である、 tenants/[TENANT_ID]/safetyConfirmationsコレクションへの参照を取得するため、第 2 引数以降、順番にパスを指定しています。テナント ID はユーザーによって動的なため、ログイン時にストアへ保存した値を利用しています。第 2 引数以降はスラッシュ区切りでコレクションへのパスを指定することも可能です。次のコードの ref1 と ref2 はまったく同じ CollectionReferenceを取得します。

●リスト3.33:コレクションへのパスをまとめて指定する

const ref1 = collection( db, 'tenants', currentTenantId, 'safetyConfirmations' ); const ref2 = collection( db, `tenants/${currentTenantId}/safetyConfirmations` );

リスト 3.32 の続きを見ていきましょう。 2 つ目のaddDoc関数は、 Firestore のドキュメントを作成します。使い方は、第 1 引数に先ほど説明したCollectionReferenceを、第 2 引数に作成したいドキュメントのデータをオブジェクトで渡すだけです。 addDoc 関数は CollectionReference のドキュメント版である、 DocumentReferenceを返します。DocumentReferenceはドキュメントのデータ自体は持ちませんが、idやpathといったプロパティからリファレンス情報の確認が可能です。また、addDoc関数は、作成するドキュメントのドキュメント ID は指定できません。自動的に、アルファベット大文字小文字と数字からなる 20 桁の文字列が割り当てられます。ドキュメント ID はコレクション内で重複が許されず、連番を振ろうとするとトランザクション処理が必要になるなど、処理が複雑になるためこの自動採番機能は便利です。ドキュメント ID を指定して作成するには、後ほど紹介するsetDoc関数を利用します。いかがでしょうか。 Firestore ではスキーマ構造を一切定義せずに、たったこれだけのコードでドキュメントの作成が可能です。また、ドキュメントを作成するためにコレクションを作成する必要もありません。コレクション内に最初のドキュメントが作成されると、コレクションが暗黙的に作成されます。同様に、コレクション内のドキュメントをすべて削除するとコレクションも削除されます。コレクションはあくまでドキュメントをまとめる概念であり、コレクション自体はフィールドを持ちません。

## ■単一ドキュメントの取得続いて、単一ドキュメントの取得方法を見ていきましょう。安否確認の編集機能では、編集対象の安否確認ドキュメントを取得する処理があります。その実装を例に見てみましょう。 src/pages/ApplicationManager/SafetyConfirmationEdit.vue の onMounted内で実装しています。onMountedは Vue のライフサイクルフックで、コンポーネントが作成されるタイミングで実行されます。 

●リスト3.34:src/pages/ApplicationManager/SafetyConfirmationEdit.vue

import { db } from 'src/firebase'; import { doc, getDoc } from 'firebase/firestore'; // 略 onMounted(async () => { // 略 const snapshot = await getDoc(

doc( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations', safetyConfirmationId ) ); if (snapshot.exists()) { const safetyConfirmation = snapshot.data(); title.value = safetyConfirmation.title; body.value = safetyConfirmation.body || ''; hazardType.value = safetyConfirmation.hazardType; dueDate.value = safetyConfirmation.dueDate; state.safetyConfirmation = safetyConfirmation; } else { notificationStore.show( 'データ取得に失敗', '対象のデータが存在しません。' ); } });

先ほどのaddDoc関数を利用した登録処理と比較して、今度はdoc とgetDoc関数をインポートしています。 1 つずつ見ていきましょう。

1 つ目のdoc関数は、collection関数のドキュメント版と考えてください。collection関数がCollectionReferenceを取得するのに対して、 doc 関数はドキュメントのリファレンスである DocumentReference を取得します。引数も同様で、第 1 引数に Firestore インスタンスを、第 2 引数以降にドキュメントへのパスを指定します。ドキュメントへのパスをスラッシュ区切りで第 2 引数にまとめて指定することも可能です。

2 つ目のgetDoc関数は、単一のドキュメントを取得します。引数にドキュメントへのリファレンスであるDocumentReferenceを渡すことで、指定したドキュメントのデータを持つDocumentSnapshotを返します。DocumentSnapshotには、便利なメソッドとプロパティが用意されています。よく利用するものを紹介しておきましょう。

- ・data():取得したドキュメントのデータをオブジェクトで返します。ドキュメント ID は含まれないため、必要な場合は id プロパティから取得する必要がある点に注意してください。指定したドキュメントが存在しない場合はundefinedを返します。 

- ・exists():指定したドキュメントの存在有無を真偽値で返します。 

- ・id:ドキュメント ID を取得します。 

- ・ref:DocumentReferenceを取得します。リスト 3.34 では、existsメソッドを利用してドキュメントの有無を確認し、存在する場合はdataメソッドを用いてデータを取得、存在しない場合はエラーを表示しています。 

## ■ドキュメントの更新続いて、更新処理を見ていきましょう。更新処理にはupdateDoc 関数もしくはsetDoc関数を利用します。 

- ・updateDoc():指定したドキュメントの、指定したフィールドのみを更新します。指定したドキュメントが存在しない場合はエラーとなります。 

- ・setDoc():指定したドキュメントが存在する場合、ドキュメントごと上書きます。指定したドキュメントが存在しない場合は新規でドキュメントを作成します。 

setDoc関数は、ドキュメントの更新と作成が可能です。ただし、更新の場合はupdateDoc関数とは異なり、ドキュメントごと上書きする点に注意してください。たとえば、以下のようなドキュメントがあったとします。

●リスト3.35:サンプルドキュメント構造

{ nameSnapshot: '安否太郎', locationSnapshot: '東京', safetyStatus: '軽傷' }

まず、リスト 3.36 のように updateDoc 関数の引数に、 safetyStatusプロパティしか持たないオブジェクトを与えた場合を見てみましょう。

●リスト3.36:updateDoc関数のサンプル

import { updateDoc } from 'firebase/firestore'; await updateDoc(ref, { safetyStatus: '安全' });

この場合、リスト 3.37 の通りsafetyStatusフィールドのみが「軽傷」から「安全」に更新され、そのほかのフィールドはそのままです。

●リスト3.37:updateDoc関数実行後のサンプルドキュメント

{ nameSnapshot: '安否太郎', locationSnapshot: '東京',

safetyStatus: '安全' }

それに対して、setDoc関数の引数に、safetyStatusプロパティしか持たないオブジェクトを与えた場合はどうなるでしょうか。

●リスト3.38:setDoc関数のサンプル

import { setDoc } from 'firebase/firestore'; await setDoc(ref, { safetyStatus: '安全' });

この場合、リスト 3.39 の通り引数に指定したオブジェクトがそのままドキュメントへ反映され、指定していないsafetyStatus以外のフィールドは削除されます。

●リスト3.39:setDoc関数実行後のサンプルドキュメント

{ safetyStatus: '安全' }

setDoc関数はオプションを設定することで、updateDoc関数と同様に、更新の処理を行うよう変更も可能です。また、安否確認ドキュメントの登録に利用していたaddDoc関数は、ドキュメント ID の指定ができませんでしたが、setDoc関数はドキュメント ID まで指定したドキュメントの作成が可能です。前置きが⻑くなりましたが、 anpi! の安否確認ドキュメントの更新処理を見てみましょう。 setDoc 関数を利用し、

src/pages/ApplicationManager/SafetyConfirmationEdit.vue の updateSafetyConfirmationで実装しています。

●リスト3.40:src/pages/ApplicationManager/SafetyConfirmationEdit.vue

import { db } from 'src/firebase' import { doc, setDoc } from 'firebase/firestore'; // 略 const updateSafetyConfirmation = async () => { // 略 const safetyConfirmation = { ...state.safetyConfirmation }; safetyConfirmation.title = title.value; safetyConfirmation.body = body.value || ''; safetyConfirmation.hazardType = hazardType.value; safetyConfirmation.dueDate = dueDate.value; const ref = doc( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations', safetyConfirmationId ); await setDoc(ref, safetyConfirmation, { merge: true }); };

setDoc関数は、第 1 引数にDocumentReferenceを、第 2 引数に作成したいドキュメントのデータをオブジェクトで渡します。addDoc関数と異なるのは、 CollectionReference ではなく DocumentReferenceを渡している点です。DocumentReferenceを指定することで、ドキュメント ID まで指定したドキュメントの作成・更新が可能です。また、リスト 3.40 では、第 3 引数に{ merge: true }を指定しています。第 3 引数は任意のオプションです。merge: trueを指定することで、ドキュメントごと上書きするのではなく、もともとあったフ

#### ィールドはそのままに、指定したフィールドに対してだけ更新をかけることが可能です。 

#### **【コラム】FirestoreDataConverter** 

anpi!ではFirestoreDataConverterを利用しています。説明の便宜上、本書に掲載しているコードからは省いていますが、便利な機能なので紹介しておきます。 FirestoreDataConverterを使用すると、Firestoreにオブジェクトを保存したり取得したりする際に、独自の型を指定できます。Firestoreのフィールドには型がついていますが、フロントエンドで取得する際には型がついてきません。anpi!ではTypeScriptを利用しているため、型を付与したい場合は、型アサーションを利用する必要があります。ただし、型アサーションは型情報を強制的に修正するため型安全とは言えません。そこで利用するのが、 FirestoreDataConverterです。まずは、FirestoreDataConverterを利用してConverterを定義します。

●リスト3.41:src/converters/safetyConfirmation.ts

import { DocumentData, FirestoreDataConverter, QueryDocumentSnapshot, serverTimestamp, SnapshotOptions, } from 'firebase/firestore'; import { SafetyConfirmation } from 'src/@types/type'; // 略 export const safetyConfirmationConverter: FirestoreDataConverter<SafetyConfirmation> = { toFirestore(data: SafetyConfirmation): DocumentData { return { title: data.title, hazardType: data.hazardType, body: data.body, dueDate: data.dueDate, _createdAt: data._createdAt || serverTimestamp(), _createdBy: data._createdBy || authStore.currentUserId, _updatedAt: serverTimestamp(), _updatedBy: authStore.currentUserId, }; }, fromFirestore( snapshot: QueryDocumentSnapshot,

options: SnapshotOptions ): SafetyConfirmation { const data = snapshot.data(options); const safetyConfirmation: SafetyConfirmation = { title: data.title, hazardType: data.hazardType, body: data.body, dueDate: data.dueDate, _id: snapshot.id, // 利便性を考慮し、_idフィールドを含めて返却 _createdAt: data._createdAt, _createdBy: data._createdBy, _updatedAt: data._updatedAt, _updatedBy: data._updatedBy, }; return safetyConfirmation; }, };

toFirestoreとfromFirestoreでデータの保存時と取得時に型を指定しています。それだけではなく、データの保存前・取得後に共通処理を差し込むことも可能です。 anpi!では保存前の処理で全ドキュメントに共通で持たせる作成者や最終更新日時フィールドなどを付与したり、取得後にidフィールドを含めて返却させていたりします。とても便利ですね。あとは、このConverterをリファレンス型に用意されているwithConverterメソッドに渡すだけです。

●リスト3.42:src/pages/ApplicationManager/SafetyConfirmationEdit.vue

import { safetyConfirmationConverter } from 'src/converters/safetyConfirmation'; // 略 const snapshot = await getDoc( doc( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations', safetyConfirmationId ).withConverter(safetyConfirmationConverter) ); // 略 const ref = doc( db, 'tenants',

authStore.currentTenantId, 'safetyConfirmations', safetyConfirmationId ).withConverter(safetyConfirmationConverter); await setDoc(ref, safetyConfirmation, { merge: true });

こうすることで、getDoc関数やsetDoc関数を呼び出した際に、Converterで指定した型への変換や共通処理を実行してくれます。ただし、updateDoc関数はサポートされていないので注意してください。anpi!ではsetDoc関数のmerge: trueオプションを利用することで代用しています。

## ■複数ドキュメントの取得続いて、複数のドキュメントを 1 度に取得する方法を見ていきましょう。安否確認を一覧表示する機能を例に見ていきます。安否確認管理メニューを開くと、登録された安否確認の一覧が表示されます。回答期限が過ぎているものはデフォルトでは非表示となっており、画面上部の「期限切れも表示」トグルを切り替えることによって表示が可能です。また、表示順もソートされており、回答期限の古い順に表示されます。図3.14:安否確認一覧先ほどの更新処理では、特定の単一ドキュメントを取得していました。今回は、コレクション内のドキュメントに対して、 1 度のリクエストで複数のドキュメントを取得したいです。そこで、 2 章でも紹介したクエリ機能を利用します。それでは、コードを見てみましょう。src/pages/ApplicationUser/SafetyConfirmationList.vueの getSafetyConfirmationsで実装しています。 

●リスト3.43:src/pages/ApplicationUser/SafetyConfirmationList.vue

import { collection, getDocs, orderBy, query, where } from 'firebase/firestore'; // 略 const getSafetyConfirmations = async () => {

// 略 const q = query( collection( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations' ), where( 'dueDate', '>=', date.formatDate(new Date(), 'YYYY/MM/DD') ), orderBy('dueDate', 'asc') ); const snapshot = await getDocs(q); snapshot.docs.forEach((doc) => { const data = doc.data(); state.safetyConfirmations.push(data); }); // 略 };

まずは、query関数を利用してクエリを生成します。第 1 引数に、コレクションへのリファレンスであるCollectionReferenceを、第 2 引数以降にクエリの条件を指定します。条件がない場合、つまりコレクション配下のドキュメントをソートせずにすべて取得する場合は、第 2 引数以降は省略可能です。安否確認管理一覧では、デフォルトでは回答期限が過ぎていない安否確認ドキュメントを取得します。これは、where関数を用いて dueDateフィールドが今日以降のものという条件を指定することでフィルターできます。次に、回答期限を昇順に並べ替えるため、 orderBy関数を用いて、dueDateの昇順(asc)を指定しています。このように、whereやorderByなど条件を指定するための関数が用意されており、これらを組み合わせてクエリを生成します。クエリ条件に関しては後ほど詳しく紹介します。クエリが生成できたら、クエリの実行にはgetDocs関数を利用します。引数に先ほど作成したクエリを渡すだけです。単一ドキュメントの取得で利用したgetDoc関数とは異なるため注意してください。 getDocs 関数は戻り値に QuerySnapshot を返します。 QuerySnapshotは、クエリの実行結果を持ち、docsプロパティでクエリの実行結果を取得できます。ほかにもいくつかのメソッドとプロパティが用意されており、よく利用するものを紹介しておきます。

- ・docs:クエリの実行結果を配列で取得します。配列の要素には、 DocumentSnapshotを持ちます。 

- ・size:クエリ結果のドキュメント数を取得します。 

- ・empty:クエリの条件に合うドキュメントがなかった場合にtrue を返します。 

- ・forEach():QuerySnapshot を配列ライクに扱うことが可能です。コールバック関数の引数にはDocumentSnapshotを持ちます。安否確認管理一覧の要件に沿ったクエリを見てきましたが、 Firestore ではほかにもさまざまなクエリ条件をサポートしています。本書ではよく利用する以下を詳しく見てみましょう。 

- ・orderBy():特定のフィールドを指定して並べ替えます。 

- ・limit():取得ドキュメント数を制限します。 

- ・where():特定のフィールドに対して条件を指定します。 

#### **orderBy** 

orderBy関数は、第 1 引数に並べ替え対象のフィールド名を、第 2 引数に昇順( asc )または、降順( desc )の並べ替えの順序を指定できます。第 2 引数は省略可能で、デフォルトでは昇順に並べ替えます。また、orderBy関数は、指定したフィールドの有無によるフィルターも行います。指定したフィールドがないドキュメントは取得結果には含まれない点に注意してください。

#### **limit** 

limit関数は、第 1 引数に取得するドキュメントの最大値を指定することで、取得ドキュメント数を制限できます。リスト 3.44 のように、引数に「 10 」と指定した場合、クエリ条件に合致するドキュメントが 10 件以上ある場合でも、 10 件のドキュメントのみを返します。このとき、orderBy関数で並べ替えを指定している場合は、並べ替えられた順に 10 件のドキュメントを返します。また、クエリ条件に合致するドキュメントが 10 件未満の場合は、全ドキュメントを返します。

●リスト3.44:limitを利用したサンプルコード

const q = query(collectionRef, limit(10)); const querySnapshot = getDocs(q); // ドキュメント数は10以下となる console.assert(querySnapshot.size <= 10);

#### **where** 

特定のフィールドに対して条件を設定するにはwhere関数を利用します。引数には、対象のフィールド、比較演算子、値の 3 つを渡します。比較演算子は文字列で渡します。比較演算子には Firestore 特有のものや、制限があるため詳しく見ていきましょう。まずはスタンダードな等価・不等価・範囲比較の演算子です。・== :等価・!= :不等価・<= :以下・> :上回る・>= :以上不等価演算子!=を利用する場合は注意が必要です。不等価演算子は、指定したフィールドが存在し、その値が比較対象の値と一致しないドキュメントを返します。つまり、指定したフィールドが存在しないドキュメントは、取得結果に含まれないため注意してください。続いて、inとnot-inです。

- ・in:フィルター対象のフィールドが、値に指定した配列のうちいずれかに一致するドキュメントを返します。値には最大 10 個まで指定が可能です。 

- ・not-in:フィルター対象のフィールドが、値に指定した配列のうちいずれにも一致しないドキュメントを返します。値には最大 10 個まで指定が可能です。フィルター対象のフィールドが存在しない場合は除外される点に注意してください。 

●リスト3.45:inとnot-inを利用したサンプルコード

// countryフィールドの値が、'USA'もしくは'Japan'であるドキュメントを返すクエリ const inQuery = query(citiesRef, where('country', 'in', ['USA', 'Japan'])); // countryフィールドの値が、'USA'でも'Japan'でもないドキュメントを返すクエリ const notInQuery = query(citiesRef, where('country', 'not-in',  ['USA', 'Japan']));

#### 続いて、配列型に対する演算子です。 

- ・array-contains:フィルター対象のフィールドの配列値内に、 where関数の第 3 引数に指定した値を含むドキュメントを返します。 1 つのクエリで 1 度だけ使用できます。 

- ・array-contains-any:array-contains句を OR 条件で複数指定したい場合に利用します。最大 10 個まで値を指定できます。 

●リスト3.46:array-containsとarray-contains-anyを利用したサンプルコード

// regionsフィールドの配列値に、'west_coast'が含まれるドキュメントを返すクエリ const arrayContainsQuery = query(citiesRef, where('regions', 'arraycontains', 'west_coast'));

// regionsフィールドの配列値に、'west_coast'または'east_coast'が含まれるドキュメントを返すクエリ const arrayContainsAnyQuery = query(citiesRef,

where('regions', 'array-contains-any', ['west_coast', 'east_coast']));

このように Firestore 特有の演算子を利用することで、さまざまな条件を指定できます。ただし、制限事項もあるためよくつまずく制限を紹介しておきます。

- ・複合クエリは、 AND 条件となります。 OR 条件は in と arraycontains-anyでのみサポートしており、それ以外の場合は、 OR 条件ごとにクエリを実行し、クライアントサイドで結合する必要があります。 

- ・範囲(<、<=、>、>=)の条件を含める場合、最初の並べ替えは同じフィールドで行う必要があります。 

- ・範囲(<、<=、>、>=)と不等価(!=、not-in)の比較は、すべて同一のフィールドに対してのみ指定が可能です。 

- ・等価(==)またはin句に含まれるフィールドで、クエリを並べ替えることはできません。 

- ・複合クエリで、not-inと!=を組み合わせることはできません。また、in、not-in、array-contains-anyも組み合わせることはできず、いずれかの句を 1 度だけ使用できます。 

- ・クエリ内のフィルター、並べ替え順指定、親ドキュメントパス (サブコレクションの場合は 1 、ルートコレクションの場合は 0 と換算される)の数の合計値は、 100 を超えることはできません。これらクエリの制限はデータモデルの設計にも大きく関わります。データモデル設計前に 1 度は公式ドキュメントでクエリ制限を確認することをお勧めします。 

#### **【コラム】複合インデックス** 

Firestoreでは、すべてのクエリにインデックスを使用することでクエリのパフォーマンスを維持しています。基本的なクエリ実行に必要なインデックスは自動的に生成されますが、既存のインデックスにマップされていない範囲句を使用して複合クエリを実行する場合は、複合インデックスの設定が必要です。インデックスが不足しているかどうかは、クエリを実行するとエラーが発生するため簡単に分かります。エラーメッセージには、必要なインデックスの作成を行えるリンクが付いており、このリンクから不足しているインデックスの追加が可能です。

anpi!のお知らせ一覧を例に見てみましょう。まず、複合インデックスを追加せずにお知らせ一覧を表示すると、図3.15のようなエラーがコンソールに表示されます。図3.15:複合インデックスエラーエラーメッセージに含まれるリンクをクリックすると、複合インデックスの作成画面に遷移します。あとは案内に従って作成するだけです。図3.16:複合インデックスの作成このようにインデックスが不足している場合は、エラーメッセージから必要なインデックスの作成が可能です。ただし、Firebase Emulatorでは複合インデックスエラーが発生しないため注意してください。「3-1 開発環境を準備する」でも触れましたが、Firebase EmulatorはあくまでFirebaseサービスを再現しているものであり、まったく同じ動作をするものではありません。テストをする際は注意してください。

## ■ドキュメントの削除ドキュメントの作成・取得・更新を見てきましたが、最後にドキュメントの削除処理を見てみましょう。 anpi! では物理削除は行わないため実装例はありませんが、サンプルコードをリスト 3.47 に示します。 

●リスト3.47:ドキュメントの削除

import { doc, deleteDoc } from 'firebase/firestore'; const ref = doc( db, 'tenants', tenantId, 'safetyConfirmations', docId

);

await deleteDoc(ref);

削除にはdeleteDoc関数を利用し、第 1 引数にDocumentReference を指定します。 1 度に複数ドキュメントの削除はできず、削除したドキュメントのサブコレクションは削除されない点に注意してください。サブコレクションを含めて削除したい場合は、サブコレクション内のドキュメントをすべて 1 件ずつ削除したのち、親ドキュメントを削除する必要があります。

### **3-3-2 親子関係のデータ構造** 

Firestore の基本的な CRUD 操作を、安否確認管理の実装を通して見てきましたが、より複雑な要件を満たすために親子関係のデータ構造を実装しましょう。各ユーザーは、安否確認管理メニューから登録された安否確認の依頼に対して、それぞれ 1 件の回答を登録します。 2 章で整理したデータ構造を振り返ってみましょう。・安否確認:tenants/[TENANT_ID]/safetyConfirmations/[ID] ・安否確認回答 : tenants/[TENANT_ID]/safetyConfirmations/[ID]/answers/[UI D]

安否確認回答は、依頼であるsafetyConfirmationsコレクションのanswersサブコレクションに登録します。ドキュメント ID は UID とし、 1 ユーザーに対して 1 件の回答が登録可能です。また、安否確認回答には、報告時点のユーザーの氏名と所在地を登録します。

|フィールド名|論理名|型|値(例)|
|---|---|---|---|
|safetyStatus|安否状況|文字列|"安全"、"軽傷"、"重傷"のいずれか|
|memo|メモ|文字列|"無事です。"|
|nameSnapshot|報告者氏名(回答時)|文字列|"鈴木太郎"|
|locationSnapshot|報告者所在地(回答時)|文字列|"千葉県"|

そのため、回答時にはusersドキュメントを取得する必要があります。それでは実装を見てみましょう。 src/pages/ApplicationUser/SafetyConfirmationAnswer.vue の addSafetyConfirmationAnswerで実装しています。

●リスト3.48:src/pages/ApplicationUser/SafetyConfirmationAnswer.vue

import { db } from 'src/firebase'; import { doc, setDoc, getDoc } from 'firebase/firestore'; // 略 const addSafetyConfirmationAnswer = async () => { // 略 // ユーザー情報の取得 const snapshot = await getDoc( doc( db, 'tenants', authStore.currentTenantId, 'users', authStore.currentUserId ) ); // 略 const user = snapshot.data(); // 登録対象のobjectを生成 const answer = { ...state.safetyConfirmationAnswer }; answer.safetyStatus = safetyStatus.value; answer.memo = memo.value; answer.nameSnapshot = user.name; answer.locationSnapshot = user.location; const ref = doc( db, 'tenants', authStore.currentTenantId, 'safetyConfirmations',

safetyConfirmationId, 'answers', authStore.currentUserId ); await setDoc(ref, answer, { merge: true }); };

いかがでしょう。安否確認回答のドキュメント登録・更新処理は、 UID と親ドキュメントの ID が取得できれば今まで紹介した機能で実装が可能です。まず、報告時点の氏名と所在地を取得するため、回答するユーザーのusersドキュメントを取得しています。これは UID が分かっていれば、getDoc関数を利用して取得が可能です。 UID はログイン時にストアへ値を保持しているため取得が可能です。これで、登録するドキュメント内のデータはそろったので、あとはanswersコレクションへドキュメントを追加するだけです。こちらも、回答対象の安否確認ドキュメントの ID と UID で DocumentReferenceが作成可能です。最後にsetDoc関数を利用すれば、既存の回答があれば更新を、未回答の場合はドキュメントを新規作成してくれます。このように、サブコレクションだからと言って特別な対応は不要です。ただし、親のドキュメント ID が必ず必要になるため、アプリ内で ID を取得するための仕組みは必要になります。

### **3-3-3 リアルタイムリスナー** 

Firestore の特徴的な機能にリアルタイムリスナーというものがあります。 Firestore の更新をほぼリアルタイムにクライアントへ自動連携できる便利な機能です。 anpi! では、メッセージ機能でリアルタイムリスナーを利用しています。ーまずはコドを見てみましょう。 src/pages/ApplicationUser/MessageList.vue の onMounted と onUnmounted内で実装しています。onMountedとonUnmountedは Vue のライフサイクルフックであり、コンポーネント作成されるタイミングと破棄されるタイミングでそれぞれ実行されるものです。

●リスト3.49:src/pages/ApplicationUser/MessageList.vue

import { collection, onSnapshot, orderBy, query, Timestamp, Unsubscribe where, } from 'firebase/firestore'; // 略 let unsubscribe: Unsubscribe; // リアルタイムリスナーの解除 onMounted(async () => { // 略 // 現時点より新しいメッセージに対してリアルタイムリスナーを設定 const q = query( collection( db, 'tenants', authStore.currentTenantId, 'messages' ), where('_createdAt', '>', Timestamp.now()), orderBy('_createdAt', 'asc') ); // [Note] onSnapshotの戻りはリスナー解除用のFunctionになっている unsubscribe = onSnapshot(q, (snapshot) => { snapshot.docChanges().forEach((change) => { if (change.type === 'added') { // 先頭(もっとも新しい)に最新メッセージを追加 state.messages.unshift(change.doc.data()); // 略 }

}); }); // 略 }); // 略 onUnmounted(() => {

// [Note] 不要になったタイミング(コンポーネント破棄時など)で、必ずリスナーも解除する。解除しないと残り続ける

if (unsubscribe !== undefined) { unsubscribe();

} });

メッセージは、いわゆるチャットの機能です。新たにメッセージの投稿があった場合、再読み込みを行わずに自動的に連携されるようリアルタイムリスナーを利用しています。onSnapshot関数を利用することでリスナーが設定でき、以下 2 つを指定します。・リスナー対象のクエリ・コールバック関数まずクエリですが、これはgetDocs関数を用いてドキュメントを取得する際に利用するクエリと同じです。ここでは、messagesコレクションの現時点より作成日が新しいドキュメントを指定しています。

2 つ目のコールバック関数は、クエリ条件に合致するドキュメントに対して作成・更新・削除が行われると呼ばれます。コールバック関数の引数にはQuerySnapshotを持ちますが、これはgetDocs関数でクエリを実行したときの戻り値と同じものです。ここでは、新たに追加されたメッセージを取得し、画面表示用の配列にデータを追加しています。新たに追加されたメッセージか否かは、 QuerySnapshotのdocChangesメソッドを利用して判断が可能です。 docChangesメソッドを利用することで、リッスンした各ドキュメントが作成( added )されたのか、更新( modified )されたのか、削除 ( removed )されたのかを確認が可能です。続いて、リスナーの解除です。リスナーを解除しないと、 onSnapshot関数で指定したコールバック関数が、いつまでも呼び出されてしまいます。onSnapshot関数の戻り値がリスナー解除用の関数を返すので、必要がなくなったタイミングでその関数を実行すればリスナーが解除できます。 anpi! では Vue のライフサイクルフックであるonUnmountedのタイミングでリスナーを解除しています。

## **3-4 Storageでオブジェクトのアップロード・ダウンロードを実装する** 

ここまでは、 Firestore を使ってデータをドキュメントとして永続化する方法を紹介してきました。この節では、テキスト・画像などのファイルオブジェクト(本書では「オブジェクト」と呼びます) を管理する方法を紹介します。

2 章で軽く触れたように、 Firebase におけるオブジェクトの保存には Cloud Storage for Firebase が利用できます。名前の示す通り、 Google Cloud の Cloud Storage を Firebase のプロジェクトに統合したものです。 AWS S3 などの類似サービスと同様に低料金かつ高い可用性をもったクラウドストレージが、 Firebase プロジェクトを作成すると利用できるようになります。

### **3-4-1 Storageでの認可の実現** 

Firestore における認可の設計が重要だったように、 Storage でも認可をどう実現するかは重要なテーマです。大きく分けて、以下 2 つの方式が考えられます。

|方式|概要|メリット|
|---|---|---|
|セキュリティルールによる実<br>装|Storage用のセキュリティルール<br>を利用する|・クライアントが直接Storageにアクセスするためオーバーヘッドが小さい<br>・認可に関連する情報をセキュリティルールに集中させて管理ができる|
|Functionsによる実装|FunctionsのAPIとしてラップす<br>る|・Storageへのアクセスをユーザーから隠ぺいできる<br>・複雑なロジックや外部サービスとの連携を組み込むこともできる|

Firebase の BaaS としての恩恵を享受するためにも、多くの場合でセキュリティルールによる実装がベターです。 Functions の利用が避けられない場合は、 Firebase の製品選定が間違っている可能性も含め再考することをお勧めします。以降では、 Storage 向けセキュリティルールによる認可の実装上の注意点を説明していきます。

### **3-4-2 Storage用のセキュリティルールの実装** 

## ■セキュリティルールを検討する上でのStorage とFirestoreの差異 

Storage 向けのセキュリティルールのシンプルな例を以下に記載します。一見して分かりますが、 Firestore 向けのセキュリティルールとほぼ同じです。

- ●リスト3.50:Storage向けセキュリティルールのシンプルな例 

service firebase.storage { match /b/{bucket}/o { match /images/{imageId} {

// 認証されているユーザーのみが images 配下にオブジェクトを作ることができる

allow write: if request.auth != null; } } }

差異として認識すべきは以下の点です。

- ・ Firestore :コレクションとドキュメントの入れ子で階層になる。 

- ・ Storage :パスとして指定する(各階層は/(スラッシュ)で区切る)。 

Storage のパスの指定は単純な文字列ですが、/の文字があることで、あたかもフォルダが存在するかのように扱うことが可能です。 path/to/file/filename.txt というパスは以下のように階層化されます。

path └── to └── file └── filename.txt

ただし、フォルダのような操作ができるというだけでフォルダの実体があるわけではないので、フォルダの作成・削除といった操作はできません。明示的にフォルダを作成せずとも、そのフォルダを含むパスでオブジェクトを生成すればよいだけです(削除も同様)。

## ■認可を想定したStorageのオブジェクト階層構造 

Storage へのオブジェクトの保管にはいろいろな用途が考えられますが、多くのユースケースでは各オブジェクトは Firestore のデータと紐づきます。たとえば、問い合わせフォームに画像を添付する要件であれば、「問い合わせ情報を保持する Firestore のドキュメント」と「 Storage 上の添付画像オブジェクト」は一対一または一対多の関係になるでしょう。このように、オブジェクト保管の仕方はデータベースの構造と関係性が強くなります。また、オブジェクトへのアクセス権限は、データへのアクセス権限と一致するケースが多いです。先ほどの問い合わせフォームの例で言えば、問い合わせを新規作成できる権限があれば、オブジェクトをアップロード(新規作成)する権限もあるでしょう。上記を踏まえて、 Firestore と Storage の両サービスではデータの階層構造を極力一致させることをお勧めします。

##### 図3.17:階層構造の一致これにより以下のメリットがあります。 

- ・セキュリティルール内の関数の構造をある程度両サービスで共通化できる(ただし、各サービス固有の書式があるため完全一致にはできない)。 

- ・ Firestore のドキュメントへのパスから、暗黙的に Storage のオブジェクトへのパスが推定できる( Firestore に Storage のパスを保持しないという方法もとれる)。とくに 1 点目は重要で、 Firestore のセキュリティルールの一部を流用することで、 Storage 用のセキュリティルールを作り上げることができます。 

## ■セキュリティルールの実装ここからは、サンプルアプリ anpi! を例に Storage 用のセキュリティルールを説明します。 anpi! には「お知らせ」の機能があり、周知したい情報として文字とは別にファイルを添付できる必要があります。添付ファイルを Storage に安全に保管できるよう、 Storage 用のセキュリティルールを記述していきます。 

Firestore 用のセキュリティルールでは、お知らせのデータに対して以下のような記述をしています。前述した通り、 Firestore 用のセキュリティルールとの共通化を意識しつつ、 Storage へ移植していきます。

●リスト3.51:Firestoreセキュリティルール(firestore.rules)

rules_version = '2'; service cloud.firestore { match /databases/{database}/documents { // === 【認証】 === //

// Condition1: 認証済みであり、かつ、メールアドレスが検証済みであるか function isAuthenticated() {

return request.auth != null && request.auth.token.email_verified; }

// === 【認可】テナントベースのアクセス制御 === //

// Condition2: Condition1を満たし、かつ、「現在ログインしているテナント」かどうか function isCurrentTenant(tenantId) { return isAuthenticated() && request.auth.token.currentTenantId == tenantId; } // === 【認可】テナントかつロールベースのアクセス制御 === // // Condition3-1: Condition2を満たし、かつ、当該テナントに対して有効なロールを保持しているか(ロール別) function isApplicationUser(tenantId) { // 今回のロールは上位が下位を包含するので、この判定はアプリ利用者 or アプリ管理者 or テナント管理者を判定する

return isCurrentTenant(tenantId) && get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationUser', 'applicationManager', 'tenantManager']; } function isApplicationManager(tenantId) { // 同上

return isCurrentTenant(tenantId) && get(/databases/$(database)/documents/tenantUsers/$(request.auth.uid)/te nants/$(tenantId)).data.role in ['applicationManager', 'tenantManager']; } // 略 match /tenants/{tenantId} { match /informations/{informationId} { allow get, list: if isApplicationManager(tenantId) || // applicationUserは公開済みのデータのみ表示可能 (isApplicationUser(tenantId) && resource.data.isPublished) allow create: if isValidCreated() && isValidSetInformation(tenantId) && isApplicationManager(tenantId) allow update: if isValidUpdated() && isValidSetInformation(tenantId) && isApplicationManager(tenantId) } } } }

ここから、オブジェクト保存に関係する部分を抜粋し、 Storage 用に移植したものが以下になります。

●リスト3.52:Storageセキュリティルール(storage.rules)

rules_version = '2'; service firebase.storage { match /b/{bucket}/o { // Condition1: 認証済みであり、かつ、メールアドレスが検証済みであるか function isAuthenticated() { return request.auth != null && request.auth.token.email_verified; } // === 【認可】テナントベースのアクセス制御 === // // Condition2: Condition1を満たし、かつ、「現在ログインしているテナント」かどうか function isCurrentTenant(tenantId) { return isAuthenticated() && request.auth.token.currentTenantId == tenantId; } // === 【認可】テナントかつロールベースのアクセス制御 === // // Condition3: Condition2を満たし、かつ、当該テナントに対して有効なロールを保持しているか(ロール別) // [Note] Storage 側から Firestore 上のデータを参照( cross-service Security Rule)し、認可情報を取得している // ref: https://firebase.blog/posts/2022/09/announcing-crossservice-security-rules function isApplicationUser(tenantId) { return isCurrentTenant(tenantId) && firestore.get(/databases/(default)/documents/tenantUsers/$(requ est.auth.uid)/tenants/$(tenantId)).data.role in ['applicationUser', 'applicationManager', 'tenantManager']; } function isApplicationManager(tenantId) { return isCurrentTenant(tenantId) && firestore.get(/databases/(default)/documents/tenantUsers/$(requ est.auth.uid)/tenants/$(tenantId)).data.role in ['applicationManager', 'tenantManager']; } match /tenants/{tenantId}/informations/{informationId}/{fileId} { allow get: if isApplicationManager(tenantId) || ( isApplicationUser(tenantId) &&

// applicationUserは公開済みのデータのみ取得可能 firestore.get(/databases/(default)/documents/tenants/$(tenant Id)/informations/$(informationId)).data.isPublished

);

allow create, delete: if isApplicationManager(tenantId); } }

Firestore 同様に、 Storage も Authentication と連携しています。 request.auth変数の中には認証情報が含まれているので、「認証されているユーザーか」や「アクセス権限のあるテナントか」といった基本的な認可は、 Firestore とまったく同じ形で判定できます。今回の要件ではオブジェクトを 1 件ずつ操作するため、get権限を設定しています。また、オブジェクトを作成・削除するため、create / delete権限を設定しています。なお、オブジェクトの上書き更新にはupdate権限を設定する必要はありません。同名オブジェクトのアップロードになるためcreate権限で可能となり、update権限はオブジェクトのメタ情報を更新する場合に必要です。

anpi! では各ユーザーの認可情報は Firestore に保存されています。この情報を Storage のセキュリティルール上で参照するには、 Storage から Firestore のドキュメントにアクセスする必要があります。 Storage からの Firestore のデータ参照にはfirestore.get()が利用でき、使い方は Firestore 用のセキュリティルールにおけるget()と同じです。

firestore.get()によるサービスを横断したデータ取得は、 2022 年 9 月に発表された cross-service Security Rules という名称の機能になります。この新機能が導入される以前はオブジェクトのメタデータを使う方式が一般的でしたが、現在は柔軟性の高さからこの方式をお勧めします。

### **3-4-3 オブジェクトのアップロード** 

セキュリティルールの設定ができたので、次は実際にオブジェクトのアップロードを行っていきましょう。お知らせの新規作成画面を例に説明します。この画面では、 Input フォームに指定された複数の添付ファイルを Storage にアップロードします。

●リスト3.53:src/pages/ApplicationUser/InformationNew.vue

import { storage } from 'src/firebase'; import { ref as storageRef, uploadBytes } from 'firebase/storage'; const uploadFiles = (): Promise<boolean> => { const promiseArray = []; for (let index = 0; index < attachmentFileObjects.value.length; index++) { const file: File = attachmentFileObjects.value[index]; // Vueのref()と名称がかぶるため、別名としてstorageRefを付けてimportしています const ref = storageRef( storage, `tenants/${authStore.currentTenantId}/informations/${informationD ocumentId}/${file.name}` ); promiseArray.push(uploadBytes(ref, file)); } return Promise.all(promiseArray).then(() => { // 略 }) };

オブジェクトのアップロードにはuploadBytes関数を利用します。後述するダウンロード用のgetBlob関数なども含め、 SDK に用意された関数を利用することで、セキュリティルールを介して Storage 上のオブジェクトに安全にアクセスできます。

Promise.all()は、すべてのオブジェクトのアップロード完了を待つために利用しています。 1 件ごとのアップロード自体は、引数としてパスを指定するだけで簡単に利用できることが分かります。

### **3-4-4 オブジェクトのダウンロード** 

次にオブジェクトのダウンロードを試してみましょう。お知らせの詳細画面を例に説明します。この画面では、お知らせに添付されたファイル名が表示されており、クリックすることで添付ファイルをダウンロードできます。

●リスト3.54:src/pages/ApplicationUser/InformationDetail.vue

import { storage } from 'src/firebase'; import { getBlob, ref } from 'firebase/storage'; const downloadFile = async (filepath: string) => { try { //StorageからBlobとして取得 const blob = await getBlob(ref(storage, filepath)); // ブラウザーにダウンロードとして認識させるための小細工 const url = (window.URL || window.webkitURL).createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = filepath.split('/').pop() || 'ファイル名不詳'; document.body.appendChild(a); a.click(); document.body.removeChild(a); } catch (error) { // 略 } };

オブジェクトのダウンロードにはgetBlob関数を利用します。ダウンロードしたデータは BLOB としてブラウザーのメモリ内に保持されるため、ブラウザーでダウンロードできるようにオブジェクト URL に変換しています。 a タグの download 属性を使ったダウンロードのよくある実装なので、詳しい説明は割愛します。

### **3-4-5 ダウンロードのためのCORS設定** 

Firebase Emulator では以上で問題なくダウンロードできるのですが、クラウド環境上の Firebase プロジェクトにデプロイした際には CORS ( Cross-Origin Resource Sharing )の設定が必要になります。これを行わないと、ダウンロード時に CORS エラーによりダウンロードが失敗します。

CORS の設定には、gsutil( Google が公式で提供している、 Cloud Storage 用の CLI )を利用します。インストールの手順は公式ドキュメント <u>1</u> に記載されています。gsutilが正常にインストールできたら、 CORS 設定に必要なファイルを作成していきます。 anpi! のルート直下にcors.jsonという設定ファイルをひな形として置いてあります。

●リスト3.55:cors.json

[ { "origin": ["*"], "method": ["GET"], "maxAgeSeconds": 3600 } ]

#### 上記の記述内容の場合、以下の CORS 設定がされます。 

- ・ダウンロード元のoriginを制限しない。 

- ・ HTTP メソッドはGETのみを許可する。 

- ・プリフライトのリクエストキャッシュは 1 時間( 3600 秒)保持する。本番環境ではoriginは制限するのが一般的なので、各プロジェクトで適切な値に書き換えてください。 Firebase の用意したデフォルトのドメインを使っているのであれば、 https://<projectId>.web.appとなるはずです。修正したcors.jsonを引数にして、リスト 3.56 のようにgsutil cors setコマンドを実行することで Storage に対して CORS の設定ができます。gs://からはじまる部分は、「 3-1 開発環境を準備する」で設定したfirebaseConfig内のstorageBucketの値に読み替えてください。 

●リスト3.56:gsutilコマンドの実行

$ gsutil cors set cors.json gs://<projectId>.appspot.com

gsutilはここで行った CORS 設定以外にも、 Storage に関連したさまざまな操作が可能です。テストや運用の過程で、オブジェクトの一覧取得や一括ダウンロードなどの操作が求められることも多いため、インストールすることをお勧めします。

### **3-4-6 URLを介したオブジェクトの取得** 

ここまではオブジェクトを直接取得する例を紹介しましたが、 URL を介した方法も SDK には用意されています。クライアント向けの SDK ではgetDownloadURL関数、サーバーサイド向けの SDK ではgetSignedUrl関数を使うことで、オブジェクト取得用の URL が生成できます。本書執筆時点の Web 上の紹介記事では、こちらの方法を使ったものが多いように見受けられます。なお、両関数ともに URL を生成できることは同じなのですが、以下のように若干の仕様差異があります。

|関数名|実行環境|URLの有効期限|セキュリティルールの設定|
|---|---|---|---|
|getDownloadUrl()|ブラウザー|明示的に剥奪するまで有効<br>手動で権限剥奪可能|URL発行時のみ制限を受ける<br>発行されたURLを介したアクセスは制限を受け<br>ない|
|getSignedUrl()|サーバー|ユーザー指定(最大7日)|制限を受けない|

## ■URLを介したオブジェクト取得の注意点今回、オブジェクトを直接操作するgetBlob関数などを先に紹介したのは、上記の URL 生成の方法は、実運用を考えるといくつか難しい点があるためです。まず、生成された URL は認証情報を token として含んでいるため、 URL を生成したユーザー以外が利用した場合も操作ができます。不用意に生成した URL が第三者に広まってしまうと、予期せぬダウンロードが起こるリスクになります。また、getSignedUrl関数を使った場合、セキュリティルールを迂回して URL が生成されます。この関数は Functions とセットで使われることが多く、 Functions 内に認可のルールを記述することになります。セキュリティルール以外で権限を記述することになるので、認可を管理・判断する箇所が分散します。意図せぬ操作を許してしまう脆弱性の作りこみにならないよう、実装時には注意が必要です。上記の理由を踏まえ、あえてこれらを使う場合があるとすれば以下のようなものが考えられます。各システムの要件を踏まえて、適切な方法を選択ください。 

- ・システム外ユーザー( Authentication に登録されていないユーザー)に一時的にダウンロードを許可したい 

- ・対象のオブジェクトがグローバルに公開しても問題ないものであり、 URL のほうが取り回しやすい( image タグに埋め込む画像が Storage に保存されていて、そのリンクを動的に生成している場合など) 

1. <u>https://cloud.google.com/storage/docs/gsutil_install</u> 

### **あとがき** 

私たちがはじめて Firebase に触れたのは 2019 年頃で、当時ビジネス用の SaaS を作ろうとしていました。どのような機能をどのような UI で使えればユーザーにとって便利であるかを考え、手探りで開発しなければなりませんでした。そのため、小さな Web サービスを細かくリリースし、ユーザーからフィードバックを受けながら開発するスタイルを採っていました。そのような中、アーキテクチャのコア部品として Firebase を採用しました。不確定な状況の中ですばやく動くものを作る必要があったので、 Firebase (とくに Firestore )の高い柔軟性は相性がよいと考えました。 UI はとくに試行錯誤が必要だったので、スキーマレスのメリットを活かしながらフロントエンドの開発に集中できる BaaS という点も魅力的でした。インフラ面もほとんど手間がかからないので、プロジェクト初期のエンジニアが少ない頃でも問題なく運用できました。しかし、サービスや開発体制が大きくなるにつれ Firebase 特有の課題も出てきました。最初は機能がシンプルだったので、 1 つのコレクションからデータを出し入れしていれば十分でした。しかし、機能間の連携が増え、データエンティティ同士の関連が複雑になるにつれ、つらいと思うようなことも次第に増えていきます。データ整合性の保証や、セキュリティリスクなどにも気を配ることが増えます。また、後から高い要求レベルの認証や認可の要件が追加されると、データモデル全体の見直しが余儀なくされることもあります。 Firestore はデータモデルとセキュリティルールが表裏一体のため、セキュリティ要件の変更が難しく、とくに苦労したケースもあります。そのため、それ以降、新規に Web サービス開発で Firestore を使うときは早めに認証や認可の要件を固めてデータモデルを整理するように心がけました。このノウハウは本書の中でも紹介した通りです。このように、 Firebase を使った開発を進める過程で多くの課題に立ち向かった結果、さまざまなナレッジがたまっていきました。そこで、社内の開発者向け資料や一部のメンバーの暗黙知となっていたものを棚卸し、 1 冊の本としてまとめることにしました。とくに、これから Firebase を扱う人に向けて、私たちがすでに経験した失敗を避け、なるべく迷わずに開発できる知識をまとめたつもりです。 Firebase はまだまだ進歩している BaaS で、本書で紹介できなかった便利な機能もたくさんあります。しかし、本書で紹介した内容の多くの考え方や注意点などの根本は大きく変わらないと考えています。本書を読み、 Firebase を使った Web サービス開発を成功に導いてもらえると幸いです。最後に、本書執筆にあたり株式会社プログレスの室伏勇二氏、中山宗典氏、野村寛氏、宮路孝一氏、堂本了史氏、⻑谷部桐子氏から有益なコメントを多数いただきました。ここに記して感謝します。

2023 年 3 月押野泰平、⻑谷川健史、岡田菜摘

##### 著者紹介 

##### **押野泰平** (おしのたいへい) 

京都大学大学院情報学研究科を修了後、アクセンチュア株式会社に入社。テクノロジー領域のコンサルティングやアーキテクチャ設計開発やマネージャー業務を経験する。その後、国内コンサルティング会社にて AI 関連の業務や、スタートアップ企業にて CTO として BtoB SaaS 開発のリードなどの業務に従事する。チームマネジメントやお客様との対話、アーキテクチャ設計だけでなく、フロントエンド・バックエンド全般のコーディング、クラウドベースのインフラ基盤構築など自らも手を動かすことを重視し、得意とする。

2023 年現在、株式会社プログレスに所属。シニアテックリードとして、新規サービス事業を企画し、プロダクト開発を推進中。

##### **⻑谷川健史** (はせがわたけし) 

東京大学大学院情報理工学系研究科を修了後、 SIer やコンサル企業を渡り歩く。小売やメーカーなど様々な業界のお客様を対象に、システムの設計・構築支援を実施。数年前からはデータ分析・ AI 関連の業務にも携わっており、データの収集・前処理から分析・可視化までの一連の流れを担当。データに基づく意思決定のアーキテクチャ設計を担う。

2023 年現在は、株式会社プログレスに所属。シニアテックリードとして、要件定義から基本設計のフェーズにおいて複数のプロジェクトに関わる。

##### **岡田菜摘** (おかだなつみ) 

お茶の水女子大学理学部物理学科を卒業後、 SIer にて上流工程や PMO 業務に従事。その後、エンジニアに転身。スタートアップ企業にて、モダンなアーキテクチャを採用した Web アプリケーションの設計・開発に従事する。 DevOps やドメイン駆動設計などの開発手法にも興味があり積極的に取り入れている。マーケティングや採用活動の経験もあり、技術的な観点だけでなくビジネスの観点からもアプローチできるエンジニアを目指している。 2023 年現在は、株式会社プログレスに所属。シニアシステムエンジニアとして、新規サービスの開発などに携わっている。

◎本書スタッフアートディレクター / 装丁:岡田章志+ GY 編集:向井領治ディレクター:栗原翔

**●お断り** 掲載したURLは2023年3月1日現在のものです。サイトの都合で変更されることがあります。また、電子版ではURLにハイパーリンクを設定していますが、端末やビューアー、リンク先のファイルタイプによっては表示されないことがあります。あらかじめご了承ください。 **●本書の内容についてのお問い合わせ先** 株式会社インプレスR&D メール窓口 <u>np-info@impress.co.jp</u> 件名に「『本書名』問い合わせ係」と明記してお送りください。電話やFAX、郵便でのご質問にはお答えできません。返信までには、しばらくお時間をいただく場合があります。なお、本書の範囲を超えるご質問にはお答えしかねますので、あらかじめご了承ください。また、本書の内容についてはNextPublishingオフィシャルWebサイトにて情報を公開しております。 <u>https://nextpublishing.jp/</u>

## OnDeck Books 設計から学ぶ Firebase 実践ガイド 

2023 年 3 月 31 日初版発行 Ver.1.0 (リフロー版)

著者押野泰平 , ⻑谷川健史 , 岡田菜摘編集人桜井徹発行人高橋隆志発行インプレス NextPublishing 〒 101-0051 東京都千代田区神田神保町一丁目 105 番地 https://nextpublishing.jp/ 販売株式会社インプレス〒 101-0051 東京都千代田区神田神保町一丁目 105 番地

◉本書は著作権法上の保護を受けています。本書の一部あるいは全部について株式会社インプレスから文書による許諾を得ずに、いかなる方法においても無断で複写、複製することは禁じられています。 ©2023 Progress Inc. All rights reserved. ISBN978-4-295-60188-3
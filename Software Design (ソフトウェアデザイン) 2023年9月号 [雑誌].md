##### **ご利用の前に必ずお読みください** 

本書は紙書籍『Software Design 2023年9月号』(ISBN0582709)を元に製作した電子書籍です。紙書籍とはデザインやレイアウトが異なり、ご覧になる端末により表示が異なる場合があります。表示設定は端末の標準設定を推奨します。配信後に補足訂正等でデータの再配布を行う場合があります。更新方法は購入先の電子書店のヘルプ等をご確認ください。本書は情報提供のみを目的としており、掲載内容の運用結果について技術評論社および著者は一切の責任を負いません。掲載内容は特に断りのない限り執筆時点より以前の情報のため、変更される場合があります。特に、ソフトウェアはバージョンアップされる場合があり、本書での説明とは機能内容や画面図などが異なってしまうこともありえます。以上をあらかじめご承諾の上、ご利用をお願いします。本文中に記載されている製品の名称は、すべて関係各組織、各社の商標または登録商標です。

**Software Design 2023/September 9**

##### **Contents** 

**<u>Special Feature 1 TypeScriptプログラマーも必見 JavaScript非同期処理の疑問を解き明かす複雑な概念をひとつずつ腹落ちさせていこう</u>**

<u>第1章</u> **<u>JavaScriptの非同期処理の変遷</u>** <u>suin</u> **<u>async/await登場までの模索と進化</u>**

<u>第2章</u> **<u>なぜ非同期処理は難しいのか? 初学者がハマらないための前提知識と用語の整理</u>** <u>PADAone</u>

<u>第3章</u> **<u>Promiseとは? コールバック関数の問題点とPromiseの使い方</u>** <u>小田島太郎</u>

<u>第4章</u> **<u>Promiseを深掘りする「オレオレPromise」を実装して構造を理解しよう</u>** <u>小田島太郎</u>

<u>第5章</u> **<u>async/awaitを深掘りする Promiseと比べてわかる挙動の違い</u>** <u>小田島太郎</u>

<u>第6章</u> **<u>AbortSignalとAsyncLocalStorage 本格的な非同期処理で求められる機能</u>** <u>うひょ(鈴木僚太)</u>

**<u>Special Feature 2 複雑性から逃げない</u>**

#### **<u>カオスエンジニアリングの始め方システムを安定運用するための技術</u>** 

<u>第1章原則を正しく理解しよう</u> **<u>カオスエンジニアリング超入門</u>** <u>小杉山拓弥</u>

<u>第2章不確実性に立ち向かうための段取り</u> **<u>カオスエンジニアリングの進め方</u>** <u>立見祐介</u>

<u>第3章「継続」こそ成功の鍵</u> **<u>カオスエンジニアリングの効果と学び</u>** <u>立見祐介</u>

###### **<u>Extra Feature</u>** 

<u>[特別企画]潤沢なCPU/メモリを使い倒すため一から開発</u> **<u>国産リレーショナルデータベースTsurugi</u>** <u>神林飛志</u>

<u>[特別企画]マルウェア対策とエンドポイントセキュリティ[後編]</u> **<u>エンドポイントセキュリティの現実</u>** <u>佐々木康介</u>

###### **<u>Á La Carte</u>** 

<u>ITエンジニア必須の最新用語解説[177]</u> **<u>Wasmer 4.0</u>** <u>杉山貴章</u>

**<u>読者プレゼントのお知らせ</u>**

**<u>SD BOOK REVIEW</u>**

**<u>バックナンバーのお知らせ</u>**

###### **<u>SD NEWS & PRODUCTS</u>** 

###### **<u>Reader's Link</u>** 

###### **<u>Column</u>** 

<u>平林万能IT技術研究所 2ndシーズン[16]</u> **<u>マリー・アントワネットの「暗号」を解く──秘密のコードで記された「フェルセン伯爵への手紙」</u>** <u>平林純</u>

<u>ハピネスチームビルディング[18]</u> **<u>自分の考えたオリジナルの手法で楽しい振り返りに</u>** <u>小島優介</u>

<u>エンジニアのためのやる気UPエクササイズ[13]</u> **<u>疲れ目で生産性が最大28%低下!? 疲れ目を予防する3つの方法</u>** <u>え</u>

<u>くろプロテイン</u>

<u>ひみつのLinux通信[115]</u> **<u>簡単詐欺</u>** <u>くつなりょうすけ</u>

<u>あなたのスキルは社会に役立つ〜エンジニアだからできる社会貢献〜[141]</u> **<u>AI技術と国会議事録が融合した「国会議事録検索(The Diet Search)プラグイン」</u>** <u>小泉勝志郎</u>

**<u>Development</u>**

<u>Stable Diffusionで学ぶ画像生成AIのしくみ[4]</u> **<u>画像生成器のしくみ</u>** <u>國田圭佑</u>

<u>オンラインホワイトボード「Miro」徹底活用術[最終回]</u> **<u>おすすめプラクティスとMiroの新機能</u>** <u>橋本憲洋、山川宏賢、今村恭子、三浦茜、室木梨沙</u>

<u>MLOpsのすすめ[2]</u> **<u>MLOpsのためのチームとスキルセット</u>** <u>澁井雄介</u>

<u>位置情報エンジニアリングのすすめ[2]</u> **<u>位置情報に触れる・加工する(前編)</u>** <u>鈴木祐亮</u>

<u>新時代の分散型SNS Nostr[3]</u> **<u>Nostrの関連サービスを見てみよう</u>** <u>鎌倉光臣</u>

<u>Google Cloudを軸に実践するSREプラクティス[6]</u> **<u>Renovateによる依存関係の更新</u>** <u>山田圭一、(監修)小森裕介</u>

<u>なるほど納得Go言語[8]</u> **<u>インターフェース(後編)</u>** <u>崎原晴香(H.Saki)</u>

<u>AWS活用ジャーニー[12]</u> **<u>AWS CloudFormation</u>** <u>武田隆志</u>

**<u>OS/Network/Security</u>** <u>現場から学ぶAWSのクラウドセキュリティ[新連載]</u> **<u>クラウドセキュリティと責任共有モデル</u>** <u>花塚亮祐、(監修)株式会社サイバーエージェントシステムセキュリティ推進グループ</u>

<u>魅惑の自作シェルの世界[10]</u> **<u>パイプライン処理の実装(その2)</u>** <u>上田隆一</u>

<u>[Ansible]現場を支えるPlaybook[10]</u> **<u>正常性確認用Playbookの実装テクニック</u>** <u>岡田淳</u>

ロゴデザインデザイン集合ゼブラ+坂井哲也表紙デザイン藤井耕志(Re:D Co.) 表紙写真

takadahirohito / Adobe Stock 本文デザイン

- ●安達恵美子 

- ●石田昌治(マップス) 

- ●近藤しのぶ 

- ●SeaGrape 

- ●轟木亜紀子、阿保裕美、佐藤みどり、相馬喜代子(トップスタジオデザイン室) 

- ●伊勢歩、横山慎昌(BUCH+) 

- ●堂上未来(Re:D Co.) 

- ●森井一三 

# 第 177 回 

### **ITエンジニア必須の最新用語解説** 

**テーマ募集**

本連載では、最近気になる用語など、今後取り上げてほしいテーマを募集しています。 <u>sd@gihyo.co.jp宛にお送りください。</u>

**TEXT:(有)オングス杉山貴章 SUGIYAMA Takaaki** **<u>takaaki@ongs.co.jp</u>**

#### **Wasmer 4.0** 

##### **WebAssemblyの可能性を拡大する「Wasmer」** 

「 Wasmer 」は、 Wasmer 社によって開発された、 WebAssembly (以下、 WASM )を Web ブラウザ以外の環境で動作できるようにするオープンソースのランタイムです。

WASM はもともと、 Web アプリケーションの実行速度を大幅に向上させることを目指して開発されました。 WASM 自体は、高水準のアセンブリ言語と、それを実行する仮想命令セットアーキテクチャです。 WASM を利用する Web アプリケーションでは、 C や Rust などで記述されたコードを WASM バイナリにコンパイルし、それを Web ブラウザ上で動作する専用の仮想マシン( WASM ランタイム)で実行します。

JavaScript と違ってあらかじめコンパイル済みのバイナリフォーマットを使う WASM は、ネイティブコードに近い速度で実行できることが最大の特徴です。それに加えて、 WASM バイナリの出力に対応したさまざまなプログラミング言語で開発できることや、特定のプロセッサに依存しないなどの強みもあります。そのコンパクトで汎用性が高いしくみに注目して、 WASM を Web アプリケーション以外の開発にも拡張しようという試みで「 WASI ( WebAssembly System Interface )」が作られました。 WASI は、 WASM から OS のシステムリソースに安全にアクセスするためのインターフェース仕様です。 Wasmer はこの WASI に準拠したランタイムであり、 Web ブラウザ以外の環境でも安全に WASM バイナリを実行できるプラットフォームを提供します。

Wasmer では、 WASI に加えて Emscripten もサポートしています。 Emscripten は LLVM をベースとしたオープンソースのコンパイルツールチェーンであり、 C/C++ のコードを WASM バイナリに直接コンパイルすることができます。 WASM では Emscripten でコンパイルされたアプリケーションをそのまま実行することが可能です。そのほかに、 WASM で CGI アプリケーションを使用するための WCGI もサポートします。なお、 Wasmer 本体は Rust で実装されており、ソースコードは GitHub リポジトリにおいて MIT ライセンスの下で公開されています。

##### **Wasmer 4.0の新機能** 

Wasmer 社は、 2023 年 6 月に Wasmer の最新版となる「 Wasmer 4.0 」をリリースしました。 Wasmer 4.0 におけるおもなアップデートのひとつが、 WASIX の実装が安定版になったことです。 WASIX は、 WASI 仕様をベースとして Wasmer 社が作成した拡張仕様です。 WASIX のツールチェーンを利用することで、効率的なマルチスレッド処理やソケット、プロセスのフォーク、 DNS 解決などといった、

POSIX が定義する多くの機能が利用可能になります。 Wasmer 4.0 は、 WASIX をサポートする初の安定リリースです。もうひとつの重要な変更点が、 Wasmer ランタイムにおいてアプリケーションの実行を担うランナーのアーキテクチャが新しくなったことです。 Wasmer では現在、 WASIX/WASI 、 Emscripten 、 WCGI の 3 種類のランナーを公式にサポートしています。 Wasmer 4.0 では、プラグイン可能なランナーアーキテクチャによって、ユーザーが自由にカスタムのランナーを作成して Wasmer に接続できるようになりました。その他の変更点では、 WASM モジュールのパッケージマネージャー「 WAPM 」が統合されたことが挙げられます。 Wasmer 4.0 では、コマンドラインインターフェース( Wasmer CLI )において WAPM でパッケージの公開や保守を行うためのコマンドが利用できます。 Wasmer 社では現在、 Wasmer を利用したサーバレス型のアプリケーションプラットフォーム「 Wasmer Edge 」の提供を始めています。 Wasmer 4.0 では Wasmer Edge との統合も強化されており、 Wasmer CLI から Wasmer Edge 上で新しいアプリケーションの作成やデプロイが行えるようになりました。

**Wasmer**

<u>https://wasmer.io/</u>

### **_平林万能_** 

### **_IT技術研究所_** 

**<mark>2ndシーズン</mark>**

平林純 (ひらばやしじゅん) <u>@hirax</u>

**第** 16 **回マリー・アントワネットの「暗号」を解く──秘密のコードで記された「フェルセン伯爵への手紙」**

##### **<mark>王妃アントワネットがフェルセン伯爵と交わした暗号</mark>** 

マリー・アントワネットは悲劇の王妃などとも呼ばれます。オーストリア王家に生まれ、ルイ 16 世と結婚して 1774 年にフランス王妃となり、フランス革命後の 1793 年 1 月に処刑されました。池田理代子氏の描く『ベルサイユのばら』(ベルばら)の主人公としても有名です。そして、『ベルばら』にも登場する重要人物、フェルセン伯爵はアントワネットの恋人です( **図** 1)。アントワネットとフェルセンは「( “ あぶり出し ” のような)見えないインク」や「暗号」を使った手紙をやりとりしていました( **図** 2)。今回は、アントワネットがフェルセンに送った「暗号で書いた手紙」を解読してみることにします。

**暗号鍵を使い、文字の交換相手を文字ごとに変える「多表換字法」**

アントワネットが使った暗号は、「各文字を違う文字と交換する」「交換方法を文字ごとに切り替える」多表換字法と呼ばれる暗号です。アントワネットとフェルセンが使った暗号、アントワネット・コード注1 ~~の~~ しくみを図示したのが **図** 3です。まず「暗号鍵を選ぶための本」と「暗号表」を共有しておきます。そして、手紙を書く場合には、

- ❶ 手紙の先頭に本のページ番号を書く 

- ❷ 該当ページの先頭単語を暗号鍵とする 

- ❸ 各文字に対して(もしくは1文字飛ばしで)、暗号鍵文字を反復的に割り当てる 

- ❹ 各文字に対して、暗号表と暗号鍵文字を使い「文字の交換相手」を見つける 

- ❺ 各文字を(あるいは1文字おきに)交換するという手順で暗号化を行います。文字同士を「互いに交換」するというだけの手順ですので、暗号化とは逆方向の復号(暗号文を読む)手順も「暗号化と完全に同じ手順」になります。また、アントワネットとフェルセンは、しばしば手抜きをしていました。それは、暗号化や復号作業を楽にするために「すべての文字を暗号化」するのではなく「 1 文字おきに暗号化/残りはそのまま」というものです。ちなみに、アントワネット・コードでは、 

- ・ “W”:フランス語では低頻度なので未使用 

- ・ “I”、“J”:同文字とみなし“I”で代表 

- ・ “U”、“V”:同文字とみなし“U”で代表といった扱いになっていました。そのため、 **図** 3の暗号表を眺めれば、 W や J や V といった文字は登場していないことがわかります。そして、アントワネットとフェルセンの暗号は「単語間にスペースは付けない」というものです。それは、単語の間にスペースを付けてしまうと、暗号が第三者に解読されやすくなってしまうからです。そのため、 

- ・暗号化時には、スペースを取り除く 

- ・復号時には、想定で単語分割するという作業を行うことになります。 

**アントワネット・コードを解くPythonコードを書いてみよう!**

アントワネット・コードの暗号化と復号を行う Python 関数を書いてみます( **図** 4)。多表換字用の「辞書」を作り、「暗号鍵」「テキスト」「 1 文字おきの手抜きをするか」を引数として渡せば、「暗号化や復号がされたテキストを返す」というものです。

Python コードで、文字列 “Hello World” ならぬ “hellouuorld” を、アントワネットが使った暗号鍵の 1 つ “courage” (勇気)を使って暗号化 → 復号をさせてみると、正しく処理ができていることが確認できます。

##### **<mark>「ヴァレンヌ事件」後にマリーが手紙の最後に書いた言葉</mark>** 

フランス革命下の 1791 年 6 月 20 日夜、フェルセンの助けを借りて、フランス国王ルイ 16 世とアントワネットたちはパリの宮殿を抜け出し、オーストリアへの逃亡を試みます。しかし、現在のフランスとベルギーの国境に近い街ヴァレンヌで、ルイ 16 世やアントワネットたちは捕まってしまい、パリへと戻されることになります。それが、国⺠から見放されていく原因ともなったヴァレンヌ事件です。事件直後の 1791 年 7 月 9 日、アントワネットはフェルセンに手紙を書きます。暗号で書かれたその手紙を、最後に解読してみることにしましょう( **図** 5)。手書きの暗号文を全文解読するのは手間ですので、手紙の最後だけを解読してみることにします。手紙の先頭に書かれた暗号鍵を示すページ番号は 36 。暗号鍵を選ぶために使われた書籍は 1734 年にモンテスキューが書いた『ローマ人盛衰原因論』でした。その 36 ページの冒頭に書かれている単語 “depuis” (日本語の「以来」)を暗号鍵にして、手紙の最後の部分を解読してみると、こんな文字が出力されます。

“ ADIEU ”

##### **<mark>「さよなら。神のもとでいつか」</mark>** 

フランス語の「さよなら」、アデュー( adieu )の語源は「神」 = “Dieu” に「場所や方向」を表す前置詞 “a” が置かれた「いつか神のもとで会いましょう=永遠のさよなら」です。

1791 年 7 月 9 日の手紙後も、アントワネットとフェルセンは暗号で書く手紙交換を、もう少し続けることになります。そのうえで、この手紙の最後は、その後に起きる、アントワネットとフェルセンの「永遠の別れ」を予感させます。 ...... あるいはもしかして、ふたりは「神のもとで再会」しているのでしょうか。

<mark>本記事コード</mark> <u><mark>https://github.com/hirax/SD202309</mark></u>

注1) 「コード」には「暗号」という意味もあります。たとえば、「ダ・ヴィンチ・コード」は、「ダ・ヴィンチの暗号」です。

(本文に戻る)

# **Stable Diffusionで学ぶ画像生成 AIのしくみ** 

**第 4 回画像生成器のしくみ**

Author 國田圭佑(くにたけいすけ) 株式会社soda URL <u>https://www.nico-soda.jp/</u>

前回(本誌 2023 年 8 月号)は、 Stable Diffusion のテキストエンコーダ部分について説明しました。今回は、画像生成部分のしくみを紹介します。ベースとなっているのは、 LDM ( Latent Diffusion Model :潜在拡散モデル)というテキストから画像への変換を行うモデルの 1 種です。

##### **Stable Diffusionの画像生成器の構造** 

Stable Diffusion の画像生成器( LDM )の構造を振り返ってみましょう。第 2 回(本誌 2023 年 7 月号)でも簡単に解説していますので、そちらも併せてご確認ください。

LDM は、 Scheduler 、 U-Net 、 VAE から構成されています( **図** 1)。実際の画像生成のプロセスにおいては、まずランダムノイズと呼ばれるノイズ化された潜在変数を用意します注1 ~~。~~ ランダムノイズは Scheduler を通ったあと、テキストプロンプトのベクトルと一緒にノイズ除去モデル( U-Net )に渡されます。 U-Net を一度通過して除去できるノイズ量は少量ですので、数十回(〜 50 回程度、 Scheduler によって異なる)にわたって繰り返し通過させ、完全に除去する必要があります。その後、ノイズ除去が完了すると、潜在変数は VAE (デコーダ)に渡されます。 VAE は潜在変数を美麗な画像へと変換(デコード)する役割を担っています。以上のように、 Stable Diffusion における画像生成器側の処理内容は比較的シンプルなものです。 Stable Diffusion の画像生成時における VAE は、単純に潜在変数を元の画像に戻す復号器(デコーダ)のみの利用ですが、本来の VAE は画像を潜在変数に圧縮する圧縮器 (エンコーダ)とセットで使われます。実際に圧縮の前後で画像の損失はほとんどなく、かなり精密に元の画像に近いものを再現することができます。 **図** 2に圧縮前の画像と復号後の画像の比較を示しました。圧縮前と復号後でほぼ同じ画像になっていることがよくわかるかと思います。

VAE はノイズ除去を行う U-Net と比べ、地味な機能に見えるかもしれません。しかし、 512 ピクセル四方の画像全体をそのままノイズ除去するのではなく、いったん潜在変数へと圧縮することで、計算量の大幅な短縮に貢献しています。 VAE のような圧縮器を用いないモデルでは 1 枚の画像を生成するのに数分〜数十分という時間が必要でしたが、数十秒スケールでの画像生成を実現しており、現在の画像生成 AI の広がりにも重要な役割を果たしたと言っても過言ではないでしょう。

##### **LDMによるノイズ除去** 

それでは、 LDM の中身について詳しく見ていきましょう。画像生成のメインとも言えるノイズ除去モデル( U-Net )部分の動きをソースコード上で確認してみます。 **リスト** 1は Stable Diffusion における画像生成のプロセスの詳細を記述したコードです注2 ~~。~~

**▼リスト1 U-Net部分の実装**

**from PIL import Image import torch from diffusers import StableDiffusionPipeline from tqdm import tqdm**

**device = "cuda" model_id = "stabilityai/stable-diffusion-2-1-base" pipe = StableDiffusionPipeline.from_pretrained(model_id, torch_dtype=torch.float16, revision="fp16") # 1コンポーネントの読み込み vae = pipe.components["vae"].to(device) tokenizer = pipe.components["tokenizer"] text_encoder = pipe.components["text_encoder"].to(device) unet = pipe.components["unet"].to(device) scheduler = pipe.components["scheduler"] # 2テキストプロンプトおよび空白テキスト("")のベクトル変換 prompt = “Latte art of gorilla wearing glasses” # プロンプトのベクトルを準備 text_input = tokenizer([prompt], padding="max_length", max_length=tokenizer.model_max_length, truncation=True, return_tensors="pt") with torch.autocast(): with torch.no_grad(): text_embeddings = text_encoder(text_input.input_ids.to(device))[0] max_length = text_input.input_ids.shape[-1] uncond_input = tokenizer([""], padding="max_length", max_length=max_length, return_tensors="pt") # 空白テキストのベクトルを準備 uncond_embeddings = text_encoder(uncond_input.input_ids.to(device))[0] text_embeddings = torch.cat([uncond_embeddings, text_embeddings]) # 3ノイズ除去の準備 seed = 51 CFG_scale = 7.5 latents = (torch.randn((1, 4, 64, 64), generator=torch.manual_seed(seed), ).to(torch.float16)*scheduler.init_noise_sigma).to(device) scheduler.set_timesteps(50)**

**# 4ノイズ除去の実行 with torch.autocast(device):**

**for t in tqdm(scheduler.timesteps): latent_model_input = torch.cat([latents]*2) latent_model_input = scheduler.scale_model_input(latent_model_input, timestep=t)**

**with torch.no_grad(): noise_pred = unet(latent_model_input, t, encoder_hidden_states=text_embeddings).sample**

**noise_pred_uncond, noise_pred_text = noise_pred.chunk(2) res = noise_pred_text - noise_pred_uncond noise_pred = noise_pred_uncond + CFG_scale * res latents = scheduler.step(noise_pred, t, latents).prev_sample**

**# 5ノイズを除去した潜在変数を画像化 with torch.no_grad(): image = vae.decode(latents/0.18215).sample image = (image/2+0.5).clamp(0, 1) image = image.detach().cpu().permute(0, 2, 3, 1).numpy() images = (image*255).round().astype("uint8") pil_images = [Image.fromarray(image) for image in images] pil_images[0]**

# **1コンポーネントの読み込み** 

今回は U-Net 部分の動きを確認するため、事前に

StableDiffusionPipeline.from_pretrained() で呼び出した Stable Diffusion のモデルを、コンポーネントごとに分割します。

**2テキストプロンプトおよび空白テキスト("")のベクトル変換**

続いてテキストプロンプトをベクトルに変換します。この際、単純に入力テキストを変換したベクトルだけではなく、 uncond_embeddings という空白テキスト( **""** )注3 ~~の~~ ベクトルも用意します(なぜこのベクトルが必要かについては後述します)。テキストプロンプトと空白テキストは、 cat メソッドを使って結合させます。このようにすることで、本来テキスト・空白テキストを別個でノイズ除去しなくてはならないところをバッチ処理できるようになります。

# **3ノイズ除去の準備** 

プロンプトがどれほど生成される絵に影響を及ぼすかを決めるために、 CFG_scale という変数を定義します。基本的には、この値を大きくするほどプロンプトの影響が強くなり、逆に小さくするほどプロンプトと関係ない絵が生成されやすくなります。

Scheduler 、 U-Net に通すランダムノイズも生成します。 Scheduler の種類によって想定されているノイズの値域が微妙に異なるため、ここではスケーリングするための係数である scheduler.init_noise_sigma を乗じています(ただし、本例の Scheduler は init_noise_sigma の値が 1 ですので、実際、乗じなくても結果は変わりません)。さらに、 Scheduler のタイムステップを設定することで、具体的に何回のノイズ除去プロセスを行うのかを数値で定義しています。 **4ノイズ除去の実行**

U-Net におけるノイズ除去プロセスのループを先ほど定義したステップの数だけ回して、繰り返しノイズ除去を行います。ですが、肝心のループの中身を見ても何のためにこのようなことをしているのか、いまいちピンときませんよね? この処理は **図** 3のように表せます。まず、潜在変数を 2 倍します。これは2で準備したテキストプロンプトと空白テキストの両方を同時に処理するためです。 2 倍にしたノイズの前半部分にテキストプロンプト、後半部分に空白テキストを与え、ノイズ除去を行います。当然、前者のほうが明確にテキストに近い画像が生成されますが、ここで重要なのは、「テキストありと空白テキスト(=テキストなし)のベクトルを比較し、差分を考慮することで、より効率的に画像とテキストを近づけることができる」という点です。テキストあり/なしの差分ベクトルは、テキストなしノイズを始点とし、テキストありノイズを終点としたベクトルです。つまり、差分ベクトルの方向は、テキストプロンプトの内容をそのまま示す方向にあたります。したがって、この方向に向かってノイズ除去を施していくだけで、テキストどおりの画像に近づいていきます。その際、あらかじめ CFG_scale で定義した量を掛けてあげると、より効率的にテキストの内容に画像を近づけていくことができます。

# **5ノイズを除去した潜在変数を画像化** 

ノイズ除去が完了した潜在変数は VAE を通じて画像に戻されます。このとき、得られた潜在変数を定数( 0.18215 )注4 ~~で~~ 割ってから渡しています。 U-Net から出力された潜在変数があらかじめ平均 0 、標準偏差 1 に標準化されているためです。 VAE のデコーダへの入力は標準化されていない状態を前提としていますので、得られた潜在変数を定数で割ることで本来の VAE の入力とスケールを合わせています。

##### **まとめ** 

今回は、 Stable Diffusion における画像生成器側の処理を確認しました。紹介したコードは diffusers ライブラリ版 Stable Diffusion のソースコード注5 ~~を~~ わかりやすく説明したものです。画像生成 AI の原理についてより深い理解を得ていただければうれしい限りです。 Stable Diffusion をはじめとした画像生成 AI は、さまざまな場所で広がりを見せています。アプリケーションや画像編集ソフトへの組み込みなど、今ではさまざまな現場で利用されるようになりました。ここまで普及した理由は、単に、オープンソースであったことや、美麗な画像を生成できるというだけではありません。今回紹介した LDM という技術により高速で処理できるようになったことが非常に大きな理由として挙げられます。

LDM 登場以前は、 1 枚の画像を生成するのに数分〜数十分かかっており、現実的な利用は難しいとされていました。しかし、アーキテクチャに VAE を用いることで、数秒〜十数秒のスケールで任意の画像生成が可能になり、実際のビジネスやサービスに応用できるようになったのです。将来、さらなるアーキテクチャの改良により、画像生成 AI は現在よりも格段に高速化されることも期待されます。アート作品の生成やデザインの補助、バーチャルリアリティの世界の構築のために、数秒で数百枚の画像を生成するというのもけっして非現実的な話ではないでしょう。単にアーキテクチャの工夫と言われるとささいなことのようですが、その貢献は非常に大きいです。今後も、より高度な画像生成 AI の研究と開発、そして計算機の進化も相まって、私たちの生活やビジネスに革新的な変化をもたらす可能性を秘めていると言えます。注1) イメージしやすくするため図1では画像になっていますが、実際は画像ではなく 64×64×4サイズのベクトル(テンソル)です。 (本文に戻る)

> 注2) サンプルコードは次のサポートページからダウンロードできます。 <u>https://gihyo.jp/magazine/SD/archive/2023/202309</u> (本文に戻る) 

- 注3) この空白テキスト部分を具体的なプロンプトに変更したものが、前号で解説したネガティブプロンプトになります。 

   - (本文に戻る) 

- 注4) 定数(0.18215)の算出方法につきましては次のURLをご参照ください。 <u>https://github.com/huggingface/diffusers/issues/437</u> 

   - (本文に戻る) 

### **<u>リモートワークでも楽しいチーム開発をめざす</u> ハピネスチームビルディング** 

## **第 18 回** 

**自分の考えたオリジナルの手法で楽しい振り返りに**

小島優介(こじまゆうすけ) <u>@kojimadev kojimadev@gmail.com</u> 株式会社デンソークリエイト

**振り返りのマンネリ化**

筆者のチームでは 2 〜 4 週間ごとに、みんなで良かった点や改善点を挙げてチームを少しずつ改善していく「振り返り」をしています。過去の筆者のチームの振り返りは、経験の多いメンバーが多く発言し、若いメンバーがあまり発言しないということがありました。その対策を、本連載の第 3 回(本誌 2022 年 6 月号)で紹介しました注 <u>1</u> ~~。~~ 具体的には、全員に順番にファシリテーター(会議を円滑に進めるための進行役)を任せたうえで、やってみたい振り返り手法 ( KPT 、 Fun/Done/Learn 、ハピネスレーダーなど)を選択して実施してもらいました。その結果、各自が振り返りで主体的に発言してくれるようになりました。ただ、全員が積極的に発言できるようになっても、 **だんだん振り返りがマンネリ化してしまう** ことはあると思います。今回は、そのマンネリ化を防止する施策を紹介します。

**変化し続ける振り返りに**

**振り返りをマンネリ化させないために必要なのは変化し続けること** です。そういう点では、先に挙げた毎回ファシリテーターや振り返り手法を変える方法も有効です。そのほかに、ユニークでおもしろかった施策が今回紹介する「 **ファシリテーターがオリジナルの振り返り手法を考えて実施する** 」です。これを行うには、チームでさまざまな振り返り手法を実施したことがあり、各メンバーにファシリテーターの経験があるほうが良いです。その条件を満たしていれば、オリジナルの振り返り手法を考えて実施するのはそこまで難しくありません。具体的には、 **ファシリテーターが、それまでの経験をもとに、自分がやってみたい振り返り手法を考えます** 。ゼロからまったく新しい手法を考えても良いですが、既存の手法をアレンジするほうがやりやすくてお勧めです。以降で筆者のチームメンバーが考えてくれた例を紹介します。

**例1 Good/Bad/Thanks**

KPT ( Keep/Problem/Try )を参考にアレンジしています。次の観点で振り返り、 Good と Bad で出た内容について議論し、アクションを決めます。

- ・ Good:うまくいったこと、できたこと、うれしかったこと 

- ・ Bad:うまくいかなかったこと、できなかったこと、モヤっとしたこと 

- ・ Thanks:お礼したいことこれを考案したメンバーが当時、チームに説明した内容を紹介します。 

“Good と Bad で良かった点と悪かった点を挙げれば、私たちのチームなら、いつもどおり議論して改善アクションを決められそうなので Try の観点は省略しました。 Keep/Problem ではなく Good/Bad と表現した理由は、挙げる意見に幅を持たせることで意見を出しやすくする狙いがあります。 Bad に関しては、うまくいかなかった/できなかったわけじゃないけど、何かモヤっとしたことも書いてもらえば、それも改善できるかもしれないという狙いがあります。

Thanks **に関しては、いつも個人的にお礼を言うことは多くあっても、それをみんなで共有することは少ないので、良いところや日ごろの感謝をあらためて伝える場にもなると良いな** と思いました ” 。

**例2 Fun/Fail/Learn**

Fun/Done/Learn のアレンジで、 Done を Fail に変更した手法です。次の観点で振り返ります。

- ・ Fun:楽しかったこと 

- ・ Fail:失敗してしまったこと 

- ・ Learn:学んだこと考案者の説明は次のとおりです。 

“ 今のチームはペアプロ/モブプロが中心なので、 Done に書く内容がみんなで重複し個性が出にくそうだと考えました。そこで、 Done の代わりに Fail にしました。 **このチームなら、ポジティブ思考で失敗さえも学びや楽しさにつなげられそう** だと思ったからです ” 。

**例3 ハピネスレーダーNEO**

ハピネスレーダーを参考にアレンジしています。次の 2 つの観点を、開発・テスト・管理の 3 つに分けて振り返ります( **図** 1)。

- ・良かったこと、うまくいったこと 

- ・悪かったこと、うまくいかなったこと考案者の説明は次のとおり。 

- “ 挙げる観点は個人的なものでも、チーム全体のものでも OK です。今回のスプリントは、 **全員が開発、テスト、プロジェクト管理 (タスクのやり方や優先度を見直す提案など)のすべてに関わっているのでそれぞれの内容を書ける** と思います ” 。 

**例4 DYA**

KPT をアレンジしています。次の 3 つの観点で振り返ります( **図** 2)。

- ・ドヤりたい!こと:超がんばったぜ!ほめてくれ!と思うこと 

- ・やれやれ......なこと:超大変だったぜ!疲れたぜ!と思うこと (うまくいった、いかなかったは問わず) 

- ・明日の自分に期待!なこと:次にやりたいぜ!と思うこと。もしくは心残りになったこと考案者の説明は次のとおり。 

- “ 前回の振り返りでは工程ごとに意見を出したので、今回はその逆の方向でアバウトに思ったことを挙げる手法にしました。今回のスプリントは慌ただしかったし大変だった部分も多いと思うので(個人の感想)、ポジティブに締めたいです。そのため、 **肩の力を抜いて書きやすくするために、観点の文言は「ドヤりたい」などのコミカルな感じに** しました ” 。 

**やってみた結果**

従来のやり方よりも、意見が出て盛り上がる傾向がありました。おそらく 3 つの理由が考えられます。

1 つめは、 **参加者がより協力的な姿勢になった** ことです。振り返りのような正解のない活動では、参加する個人の意欲が重要になります。その点で、ファシリテーターがチームのことを考えて考案した振り返り手法の場合、参加者はそれを成功させたいという気持ちが出やすく、協力的になりやすかったと思います。

2 つめは、 **単純に従来のやり方よりもおもしろい** ことです。たとえば、ある技術に関する記事を読む場合、公式サイトのヘルプページを読むよりも、自分が注目する技術者の個人ブログを読むほうがおもしろく感じることがあると思います。それと似た感覚で、世の中で広まっている KPT の観点よりも、チームメンバーが考案した「ドヤりたいこと」などの観点で意見を出すほうが楽しく感じました。 3 つめは、 **今のチームに適したやり方になりやすい** ことです。ファシリテーターはチームの特性や直近の振り返り手法の傾向から、自分なりにチームに適している手法を考えてくれたため、実際にやりやすかったです。

◆ ◆ ◆

このように「各自がオリジナルの振り返り手法を考えて実施」はマンネリ化を防止しつつ良い効果がありました。ぜひみなさんも試してみてください。

### **<u>オンラインホワイトボード「Miro」徹底活用術</u>** 

###### **無限に広がる共創ツールでチームを活性化しよう** 

# **最終回おすすめプラクティスとMiroの新機能** 

Agile Studio 橋本憲洋(はしもとのりひろ) 山川宏賢(やまかわひろさと) 今村恭子(いまむらきょうこ) 三浦茜(みうらあかね) 室木梨沙(むろきりさ)

<u>https://www.agile-studio.jp/</u>

本連載も、今回で最終回となりました。本誌 2023 年 7 月号より筆者らのチームで実際に使っている様子を紹介してきましたが、今回はより発展的な内容として、筆者らオリジナルのプラクティスや次世代の技術を使用した機能、また本連載でこれまで紹介してきたものを実際に Miro 上でご覧いただける QR コードを掲載します。

##### **Sprint Varve** 

Sprint Varveは、筆者のチームで実践しているオリジナルのプラクティスです。第 3 回(本誌 2022 年 12 月号)でも少し紹介していますが、あらためて由来や目的、作り方、作る際のコツなどをお伝えします。

Sprint Varve を一言で紹介すれば「チームの歴史絵巻」です( **図** 1)。福井県の三方五湖には年縞博物館注1 ~~と~~ いう施設があります。ここには湖底に積み重なった 7 万年ぶんもの地層のストライプが展示されています。その壮大な光景にインスパイアされ、「チームの活動」も三方五湖の地層のように層を重ねることで、楽しいものができあがるのではないかと思い、 Sprint Varve を始めました。

Sprint Varve の目的は、自分たちの活動を可視化して残していくことです。チームの動向、楽しい瞬間、新たな挑戦、変化の瞬間など、決まったルールはとくにありませんので、残したいと思ったあらゆる要素を盛り込みましょう。四半期や半年といった⻑いスパンでのふりかえりを実施しているチームは多いと思います。しかし、過去の出来事を思い出すのは一苦労ではないでしょうか? 単に記憶を引き出すだけでも時間がかかりますし、多くの場合、苦い経験ばかりが思い起こされてしまいますよね。そこで Sprint Varve の登場です。無限に広がる Miro のボード上にチームの活動の層をどんどん重ねていきましょう!

作り方は驚くほど簡単です。四角の図形に文字を記入し貼り付けたり、写真やスクリーンショットを切り貼りしたりして左から右へと次々に追加していくだけです。ただし、各層がどの期間を表しているのかは明確に示す必要があります。筆者のチームではスプリント番号、日付、スプリントゴールを記載することでわかりやすくしています( **図** 2)。さらに、画像やイラストだけを掲示すると、後日その内容が何だったのかわからなくなる恐れがあるため、同時にテキストも掲示することを推奨しています。チームの活動の歴史をふりかえることで自分たちがどのように成 ⻑し、どんな困難や喜びを経て今に至ったのか、そしてどこに向かおうとしているのかを見つめ直すきっかけにもなります。ぜひみなさんのチームにも “Sprint Varve” を取り入れて、自分たちだけの歴史絵巻を作ってみてください!

##### **おすすめプラクティスの紹介ボード** 

これまで 12 回にわたって紹介してきた内容から、 Miro を使う際におすすめのプラクティスを集めた Miro ボードを作成しました。紹介ボードに含まれるプラクティスは次のとおりです([]内は紹介した記事)。

- ・ DailyScrumエリア[本誌2023年8月号] 

- ・ Sprint Varve[本記事] 

- ・偏愛マップ[本誌2023年7月号] 

- ・ KPTA(ふりかえり)[本誌2022年12月号] 

- ・ Happy、Learn、Puzzle(大ふりかえり) 

- ・ 1日ひとつだけ強くなる[本誌2023年3月号] 

- ・ Respect 

- ・ハピネス指標[本誌2023年8月号] ほかこれから Miro を使ってみようという方にも、すでに使っている方にも参考にしていただけるものばかりです。ぜひ次の URL にアクセスして、お好きなプラクティスをご自身のボードへコピー&ペーストして使ってみてください。 

<u>https://miro.com/app/board/uXjVM8oL9Z8=/</u>

情報を Miro でオープンにし、オンラインでも同じ場所で集まって同じものを見て会話をすることで、開発や作業の効率が上がることを実感できると思います!

##### **進化するMiro** 

1 年間の連載のあいだにも、 Miro には多くの新機能が追加されました。その中から少しだけピックアップしてご紹介します。

**Spinny Picker Wheel**

チームとして活動しているとき、「誰にやってもらおうかな」「どれにしようかな、どれでもいいな」など、決めきれない場面ってよくありますよね。そんなときに活躍するのがSpinny Picker Wheel(スピニーピッカーホイール)です。選択肢を入力してルーレットを回すだけで、決めきれないことを Miro がズバッと選択してくれます( **図** 3)。なんとも言えない空気の中でものごとを決めることはやめて、ルーレットに身を任せてみましょう! ゲーム感覚でできるので、盛り上がること間違いなしです。

###### **Miro AI** 

ChatGPT などの AI 技術がトレンドですが、 Miro も例外ではなく、 Miro AIの機能が搭載されました( **図** 4)。たとえば、 1 つの付箋を作って Miro AI を実行すると、関連するようなことが書かれた付箋をたくさん作ってくれます。付箋以外のさまざまな機能でも Miro AI を使えますし、「 iPhone で動作する家計簿アプリのユーザーストーリーマッピング」といったプロンプトを実行することもできます。ただし、まだベータ版のようで、必ずしも日本語で出力してくれるわけでもなく、まだ安定はしていないようです。付箋から画像生成する機能もありますが、今はまだ全然違うものが生成されますね ( **図** 5)。今後のアップデートに期待しましょう。

##### **Miroの魅力をおさらい** 

ここまで多くの利用シーンをお伝えしてきましたが、 Miro の魅力をふりかえってみましょう。

**いつでもどこでも、どんな端末からでも**

利用者全員がリアルタイムに共同作業ができるので、リモートや離れて業務をしているメンバーとも気軽に利用できます。 PC だけでなく iPad でも活用してみてください。

**ブレインストーミングでの意見出しに**

付箋やマインドマップを使ってグループワークでアイデアの発おはこ散・収束をしていくのは Miro の十八番です。単に付箋などを書き出すだけではありません。タイマー、投票、クラスター化など、アイデア出しのためのワークを補強してくれるツールがそろっているのも魅力的です。

**プロジェクト管理に**

プロジェクトの進捗や話し合ったことなど、あらゆることを Miro に集約すると、すべてが可視化されます。メンバー全員で認識合わせできる場となります。無限に広がる Miro なので場所の心配はいりません。

**ダイアグラムを活用して**

フローチャートや図を組み合わせてダイアグラムを簡単に作成できるのも魅力の 1 つです。自分たちのチームに必要なものを自分たちで作ってみましょう。 AWS や Google のダイアグラムも用意されているので、 Miro 上でネットワーク図やアーキテクチャ図を簡単に書くこともできます。

**アジャイルチームの活動に**

多様なテンプレートがそろっているため、アジャイルプロセスをさらにパワーアップすることができます。フレームやテンプレートを活用して、アジャイルを加速させましょう。

##### **連載のまとめ** 

ちょうど 1 年間、 12 回にわたってお届けしてきた本連載も、今回が最終回となりました。連載当初は 2 ページでしたが、途中から 2 倍の 4 ページをいただけることになり、当初考えていた構成を大きく変えることにもなりました。 1 年も続けることになるとは思っておらず、そのうえページが増えるというのは予想外で、うれしい悲鳴でもありました。筆者らのチームは Miro とともに成⻑してきたと言っても過言ではありません。 Miro は日々進化をしていて、以前の連載で紹介した機能が今では大きく変わっているといったことも起きています。「いつの間にかこんな機能ができてる!」「あの機能が使いやすくなってる!」といった発見をするたび、この連載で紹介するのが楽しみでもありました。この連載に掲載してきた Miro ボードの画像は、ほとんどが筆者らの実際のボードから切り取ったものです。日ごろ使っている機能を紹介するためにあらためて調べていると、今までは気づいていなかった機能などに出会うこともありました。連載を通じて Miro の便利さを今一度実感し、実際の開発にフィードバックすることができました。

Miro は「オンラインホワイトボードツール」と一言で説明できないほどの機能があります。ぜひこれからも Miro を使い続けて、筆者らが思いつかなかった使い方や、みなさんオリジナルの工夫を見つけてみてください。活用できるかどうかはあなたの使い方しだいです。

**フィジカル&メンタルに効くエンジニアのためのやる気UPエクササイズ第13回疲れ目で生産性が最大 28%低下!? 疲れ目を予防する 3つの方法**

えくろプロテイン株式会社Gozal/CTO YouTuber/えくろプロテインちゃんねる @ekuro0513

##### **パソコン作業による疲れ目の問題とその影響について** 

こんにちは、健康オタクエンジニアのえくろです! みなさん、目の疲れに悩んでいませんか? 実はパソコン作業者の 66 %が「コンピュータビジョン症候群(以下、 CVS )」を患っていると言われています注1 ~~。~~ CVS とはパソコンなどのディスプレイに ⻑時間途切れることなく目の焦点を合わせることで起こる目の疾患で、症状として頭痛や、目の疲労、ドライアイ、首の痛みなどを引き起こします。単なる目の疲れと放置されがちですが、アラバマ大学の研究によると、 CVS によって最大 28.7 %生産性が落ちることがわかっています注2 ~~。~~

##### **コンピュータビジョン症候群の原因** 

CVS の原因は多岐にわたりますが、 2022 年のメタアナリシスによっておもな原因が報告されています。その報告によると、ディスプレイとの距離が近いと遠い場合に比べて 4.24 倍、休憩をとる習慣がないとある場合に比べて 2.24 倍、⻑時間ディスプレイを見る人はそうでない人に比べて 2.02 倍、姿勢が悪い人はいい人に比べて 2.65 倍、仕事以外でも電子機器を使う人はそうでない人に比べて 1.66 倍、 CVS になりやすいことがわかっています注3 ~~。~~

さらに、同研究では、 CVS に関する知識がないだけで正しい知識を持つ人に比べて CVS のリスクが 4.04 倍になるということも明らかになっています。ということで、まずは本記事で正しい CVS の予防法を学んでいきましょう。

##### **CVSを予防する3つのポイント** 

CVS の症状を予防または軽減するポイントは次の 3 つです。 ❶ **ディスプレイの位置**

CVS を予防するために目とディスプレイの距離は非常に大切です。この距離は 40 〜 75cm くらい空けるのが好ましいとされています ( **図** 11)。さらに、ディスプレイの高さも大切で高過ぎると目が見開いてドライアイの原因となります。ディスプレイの上部に目線をふかくあわせて 15 〜 20° の俯角になるようにしましょう( **図** 12)。

<u>http://aoa.org/healthy-eyes/eye-and-vision-conditions/computer-vision-syndrome</u>

###### ❷ **座り方** 

また、間違った座り方も頭痛や肩こり、腰痛の原因となります。ひざもも裏を圧迫しないように膝と股関節の位置が床と平行になるようにし、足を床につけるようにしてください( **図** 13)。足が床につかない場合はフットマットなどを使って高さを調整するといいでしょひじう。肘の角度も 90° くらいになるように机の高さを調整することで手首や肩首への負荷を減らせます( **図** 14)。

❸ **休憩の習慣**

CVS 予防のためには休憩の習慣を持つことが大切です。⻑時間パソコンを使うときは定期的に目を休めるようにしましょう。これには米国眼科学会も推奨している「 20-20-20 ルール」がお勧めです。やり方は簡単で、 20 分間作業したら、 20 フィート(約 6 メートル)先を 20 秒見るようにするというものです( **図** 2)。人は近くを見続けるとピントを合わせる筋肉が緊張し疲労を感じるようになります。そのため、定期的に遠くを見ることでその緊張を和らげるのです。そのほかにも厚生労働省がガイドラインで推奨している方法が、 1 時間パソコン作業をしたら 10 〜 15 分休憩するというものです注4 ~~。~~ いずれにしろ連続して何時間もディスプレイを見続けることがないよう休憩の習慣を作るようにしましょう。また、パソコンの操作中はまばたきの回数が減ることがわかっています注5 ~~。~~ 研究によるとリラックスしている状態に比べてパソコン操作中はまばたきの数が 3 分の 1 ほどになるそうです。さらに、まばたきの数はフォントサイズが小さかったり、タスクが複雑になったりすることでも減少します。とくに、エンジニアはそのような状況に陥りがちなので、頻繁にまばたきをするように心がけましょう

**終わりに**

- ⻑時間にわたって複雑な作業を余儀なくされるエンジニアにとっ 

- て CVS は職業病とも言えるかもしれません。しかし、ちょっとした対策で予防することは可能です。 

- 我々エンジニアは目の前の課題を解くことに熱中するあまり、自 

- 身の健康をないがしろにしがちです。 

- 本記事で紹介した予防策を実践することで、 1 人でも多くのエンジ 

- ニアがご自身の体と向き合うキッカケになれば幸いです。注3) 注1と同じURLにて報告されています。 

- (本文に戻る) 

# **第 1 章** 

### **JavaScriptの非同期処理の変遷 async/await登場までの模索と進化** 

**コールバック関数、Promise、async/awaitと、JavaScriptには非同期処理を実現するためのさまざまな方法があります。これらはどのような経緯で登場したものなのでしょうか。本章では、非同期処理がどのように動作するのか、その概要をおさらいしつつ、 JavaScriptにおける非同期処理の変遷を解説していきます。**

suin @suin

##### **序文** 

近年の Web アプリケーションは高い性能が求められています。優れた見た目のデザインだけではなく、同時に複数のタスクを処理し、ユーザーのアクションにすばやく応える反応性も求められています。複数のタスクを効率的に処理するには、 CPU の複数コアの活用が求められます。しかし、 JavaScript の複数コア活用は簡単ではありません。 JavaScript が採用しているスレッドモデルはシングルスレッドだからです。マルチコアの活用を念頭に置いたマルチスレッド言語と比べると、 JavaScript のシングルスレッドは見劣りする特徴で ~~す~~ <u>注1</u> ~~。~~ それでも、高いパフォーマンスを実現するために、 Web アプリケーションの舞台裏では非同期処理が活躍しています。非同期処理を用いれば、シングルスレッドながらも並行してタスクを効率的に処理できるのです。非同期処理と一言で言っても、 JavaScript の誕生から近年に至るまで、コールバック、 Promise 、そして最新の async/await など、非同期処理の実現にはさまざまな模索と進化がありました。本特集では、これらの非同期処理の核心に迫り、ハイパフォーマンスで良いユーザー体験を提供する Web アプリケーションを構築するための知識を提供します。まず、 JavaScript の非同期処理の発展と変遷の歴史をかえりみて、非同期処理の理解の入り口を提供します(第 1 章)。次いで、初学者が把握しておくべき前提知識と専門用語を整理し、非同期処理の複雑さを解きほぐします(第 2 章)。さらに、 Promise の意義や利点を理解すべく、それを深掘りします(第 3 章)。そして、 Promise を自作してみて、そのしくみに迫ります(第 4 章)。さらに、現代の非同期処理に不可欠な async/await について詳しく解説します(第 5 章)。最後に、 AbortSignal や AsyncLocalStorage など、より進んだ非同期処理関連の API を紹介し、非同期処理の高度なテクニックを提示します(第 6 章)。この先には、興味深く、実用的で、奥深い非同期処理の世界が広がっています。それではさっそく、この非同期処理の深淵に飛び込んでみましょう!

##### **非同期処理の基本とその重要性** 

90 年代の Web ページは静的でした。ブラウザの仕事は HTML を表示するだけで、ユーザーとの相互作用はサーバサイドが担っていました。ところが、 1995 年の JavaScript の登場ですべてが変わりました。 JavaScript はブラウザ向けに開発されたプログラミング言語で、ユーザーが Web ページと動的に相互作用できる強力な特徴を持っていました。

JavaScript を用いた Web ページは、入力のバリデーションを行うような単純なものから始まり、 Gmail や Google Maps に代表される複雑な Web アプリケーションというジャンルに発展しました。そして、最近は Web API からのデータ取得、ユーザー入力の処理、画面描画などありとあらゆる処理をクライアントサイドで行うことを前提としたアプリケーションも一般的になっています。クライアントサイドアプリケーションの複雑化につれ、効率的なタスク処理が重要になりました。とくに Web API からのデータ取得といった時間のかかるタスクも当たり前になってきています。こうした処理を **同期処理** で行ってしまうと、ユーザーインターフェースのタスクが停止することがあり、結果的にユーザー体験に悪影響を与えます。処理を効率的に行い、良いユーザー体験を作るには **非同期処理** が重要です。

###### **同期処理と非同期処理** 

JavaScript の非同期処理の機能を知る前に、同期処理と非同期処理の違いを見てみましょう。同期処理と非同期処理は、タスクがどのように実行されるかを指す言葉です。同期処理は、タスクを **直列** ( sequential )に実行します ( **図** 1)。より簡単に言うと、タスクをひとつひとつ順番に実行する処理方法です。同期処理では、前のタスクが終わって初めて次のタスクが実行されます。そのため、前のタスクに時間がかかる場合、その間、次のタスクは待ち状態になります。この待ち状態のことを **ブロッキング** ( blocking )と言います。また、待ち状態にさせることを「ブロックする」と言います。非同期処理は、タスクを **並行** ( concurrent )に実行します( **図** 2)。複数のタスクを同時に実行するため、前のタスクの完了を待たずに、次のタスクが実行されます。したがって、前のタスクに時間がかかっていても、次のタスクは待ち状態になりません。言い換えると、非同期処理ではブロッキングが発生しないということです。ブロッキングしないことを **ノンブロッキング** ( non-blocking )と言います。同期処理の問題点は、待ち時間を有効活用できないところです。待ちの間は、コンピューターの計算資源が有効活用できないため、パフォーマンスの問題につながります。とくに JavaScript はシングルスレッドを採用しているため、ブロッキングが発生している間はほかの処理が行われません。ほかの処理の中にはユーザーと相互作用する UI の処理も含まれるため、結果的にユーザー体験の悪化を招きます。一方、非同期処理はノンブロッキングであるため、待ち時間に計算資源を有効活用でき、 UI の処理も並行して実行できるため、ユーザー体験を損なわずに済みます。

##### **Column 現実世界でのたとえ話** 

同期処理と非同期処理を理解するために、1つの比喩を出します。家でディナーを作る状況を想像してみてください。作るものはサラダ、スープ、メインディッシュです。同期処理的なアプローチでは、1つの料理に専念し、それが完全に終了してから次の料理に移ります。たとえば、まずサラダを作り、それが完了してからスープを作り、スープが終わってからメインディッシュに取り掛かるとします。この場合、効率的に次の料理に着手できず、余計な待ち時間が発生し、ディナーができあがるまでの時間が⻑くなります。非同期処理の場合は、複数の料理を同時に作ります。たとえば、先にスープやメインディッシュに着手し、スープを煮込んでいる間やメインディッシュをオーブンで焼いている間に、サラダを作ります。この方法では、複数の料理が同時に進行するため、待ち時間が短く効率的になり、ディナーができあがるまでの時間が短くなります。

###### **演習1:ブロッキングを体験する** 

同期処理が起こすブロッキング現象の問題点を、具体的な実験を通じて理解してみましょう。 XMLHttpRequest の open メソッドは HTTP リクエストを初期化します。このメソッドは第 3 引数に false を指定すると、通信処理が同期処理になります注2 ~~。~~ 通信が行われている間、どのようなことが起こるのか実験して確かめてみましょう。まず、 blocking.html という名前のファイルを用意します( **リスト** 1)。この HTML ファイルは、現在の UNIX タイムスタンプをリアルタイムに次々と更新され再描画されていく機能と、同期処理を開始するためのボタンを含みます( **図** 3)。

###### **▼リスト1 blocking.htmlの内容** 

**<mark><!--現在時刻のタイムスタンプを表示する部分--></mark>**

**<div id="time"></div>**

**<mark><!--同期処理タスクを実行するボタン--></mark>**

**<button onclick="sendRequest()">処理開始</button>**

**<script>** **<mark>// タイムスタンプを更新するタスク</mark> setInterval(() => time.innerText = Date.now());** **<mark>// 少なくとも1秒かかる同期処理タスク</mark> const sendRequest = () => { const request = new XMLHttpRequest(); const url = "https://httpbin.org/delay/1"; request.open("GET", url, false); request.send(null); console.log("通信完了");**

**}**

**</script>**

<u>注3</u>

ボタンを押すと、 httpbin.org に HTTP リクエストを送信します。 httpbin.org はシンプルな HTTP リクエストを試験できるサービスで、 /delay/1 のエンドポイントにリクエストすると、サーバサイドで 1 秒間の待ち時間が発生します。次に、ブラウザ注4 ~~で~~ この HTML ファイルを開き、「処理開始」ボタンを押してみて、現在時刻の描画がどのように変化するか観察してみます。ボタンを押すと通信が行われ、その間は現在時刻の再描画処理が停止します( **図** 4)。これは、通信中に setInterval で指定した処理が停止するためです。その結果、現在時刻の描画が一時的に停止します。これがブロッキングです。今回は画面の描画更新がブロックされることを確認しましたが、ブロッキングの影響は JavaScript で処理しているすべての処理に影響しますので、 Web アプリケーションケーションの作りによってはアプリ全体が停止してしまうこともあります。上の実験を通じて、同期処理のブロッキング問題がどのようなものか確認しました。ブロッキングは、ほかの JavaScript の処理を停止させるため、とくにクライアントサイドにおいては、ユーザー体験を損ねます。ブロッキングを避けるためにも、非同期処理が重要となってくるわけです。

##### **コールバック関数による非同期処理** 

JavaScript では非同期処理を実現するために、コールバック関数が用いられ始めました。その後、 Promise へと発展していきますが、当初の概念の理解から始めてみましょう。

###### **タスク、キュー、イベント** 

非同期処理は、タスク、キュー、イベントなどの概念に支えられています。これらの概念の詳細なしくみや動作は第 2 章で解説していますので、ここでは非同期処理のイメージを持ってもらうために必要な要点に絞って紹介することにします。タスクとは、プログラムの実行単位であり、通信や画面描画など、特定の目的を達成するために行う操作を指します。また、 JavaScript の非同期処理が行われる際、その基盤となるのがタスクキューです。キューは英語で行列や待ち行列を意味しますが、ここではとくにタスクの待機列という意味で使われます。タスクキューに格納されたタスクは順番に従って実行されていきます。タスクキューにタスクが格納されるタイミングに関係しているのが「イベント」です。イベントはプログラムが特定の状態になったときに発生します。たとえば、ユーザーがボタンをクリックしたとき、通信処理が終了したときなどがこれに該当します。そして、イベントに対応して非同期的にタスクを実行するために利用されるのがコールバック関数です。

###### **コールバック関数** 

コールバック関数とは、ほかの関数に引数として渡される関数で、特定のイベントで実行されます。特定のイベントで実行したいタスクを関数に書いておき、その関数をコールバック関数として渡すことで、非同期処理が実現されます。コールバック関数を渡される関数は、何らかのタイミングで非同期的にタスクを実行するためのトリガーになります。これにより、一度コールバック関数が設定されれば、その完了を待つことなくほかの処理を進めることができ、特定のタイミングにコールバック関数が実行されるという流れとなります。以上の流れにより、非同期処理は一貫性を持ちつつも、待ち時間を有効利用することで全体的なプログラムのパフォーマンス向上に寄与します。次のサンプルコードは、 setTimeout 関数に callback 関数をコールバック関数として渡した例です。

**const callback = () => console.log("3秒経ちました");** **<mark>// ↓3秒後callbackを実行する</mark> setTimeout(callback, 3000);**

setTimeout 関数は、第 2 引数で指定したミリ秒後の経過後のタイミングで第 1 引数で指定した関数を実行します。この例では、 3,000 ミリ秒後(つまり 3 秒後)のタイミングで、 callback 関数が実行されます。このように、コールバック関数を使用することで特定の時間が経過した後や、通信処理で取得したデータが到着した時点で、特定の処理を非同期的に実行することが可能となります。ちなみに、「コールバック」は英語の “call back” からきており、「電話を折り返しかけ直す」という意味もあります。電話で依頼を受けた際に、あとで折り返すとき連絡先の電話番号を聞くことがありますが、コールバック関数は折り返しの連絡先と理解するとわかりやすいかもしれません。

**書いた順≠実行順**

コールバック関数は、特定の条件が満たされたときに実行されます。そのため、コードとして書かれた順番で処理が実行されていくとは限りません。たとえば、次のサンプルコードは 3 つの関数(処理)を行う例です。

**a(); setTimeout(b, 3000); c();**

コード上では a 、 b 、 c の順で書いてありますが、実行順は b が最後になる a 、 c 、 b の順で関数が呼び出されます( **図** 5)。これは、 setTimeout により b が 3 秒後に呼び出されるためです。

###### **演習2:ノンブロッキングを体験する** 

コールバック関数を用いた非同期処理が、ブロッキングを発生させないことを確かめてみましょう。演習 1 では XMLHttpRequest を用いて同期処理の実験を行いましたが、ここではその非同期処理版を作ります。まず、 nonblocking.html というファイルを用意します( **リスト** 2)。このプログラムは、演習 1 同様に現在時刻を表示する部分と、非同期処理を開始するボタンがあります。

**▼リスト2 nonblocking.htmlの内容** **<mark><!--現在時刻のタイムスタンプを表示する部分--></mark>**

**<div id="time"></div>** **<mark><!--非同期処理タスクを実行するボタン--></mark> <button onclick="sendRequest()">処理開始</button> <script>** **<mark>// タイムスタンプを更新するタスク</mark> setInterval(() => time.innerText = Date.now());** **<mark>// コールバック関数</mark> const onLoad = () => {** **<mark>// リクエストが完了したときに実行される</mark> console.log("通信完了"); }**

**<mark>// 非同期タスク</mark> const sendRequest = () => { const request = new XMLHttpRequest(); const url = "https://httpbin.org/delay/1"; request.open("GET", url);** **<mark>// コールバック関数をrequestオブジェクトに渡す</mark> request.onload = onLoad; request.send(); } </script>**

XMLHttpRequest の open メソッドは第 3 引数を省略すると、非同期通信になります。 request.onload が、データ取得完了時に実行されるコールバック関数です。 load イベントが発生すると、その時点で onload に割り当てられたコールバック関数が実行されます。ブラウザでこのファイルを開いて、「処理開始」ボタンをクリックしてみてください。すると、演習 1 の同期処理とは異なり、現在時刻の描画が一時停止しないことに気がつくはずです。これは、非同期処理がほかの処理をブロッキングしていないことの証しです。

###### **コールバック地獄** 

コールバック関数により、非同期処理が実現できるようになりましたが、新たな問題も発生します。多くの非同期処理を連続して行う場合、コールバック関数が入れ子状になることがあります。これにより、可読性が低下する問題を **コールバック地獄** ( callback hell ) と呼びます。

**リスト** 3は、特定の投稿( post )、その投稿者( user )、その投稿者が属する組織( organization )を順に取得する非同期処理のイメージです。このコードは非同期処理の連鎖を示しています。まず getPost 関数が非同期で投稿を取得します。その投稿は post としてコールバック関数に渡ります。さらに、そのコールバック関数内で、 post から投稿者 ID ( userId )を取り出し、 getUser に渡します。 getUser 関数も非同期的に投稿者を取得し、それが次のコールバック関数に渡ります。同様に、 getOrganization も非同期に組織を取得します。

###### **▼リスト3 コールバック地獄のイメージ** 

**getPost(postId, (post) => { getUser(post.userId, (user) => { getOrganization(user.orgId, (org) => {** **<mark>// ... コードがネストされて読みにくくなる</mark> }); }); });**

このように、非同期関数の処理結果を連鎖して扱う必要が出てくるとネストが深くなり、コールバック地獄が生じます。この例は、説明のためコールバック地獄を単純化した作例であるため、まだシンプルなほうです。現実は、これに加えてさまざまなロジックや条件分岐が入ってくるため、可読性がますます悪くなります。

**不統一なコールバックAPI**

コールバック API はそれがどのような API であれ、一貫したパターンのほうが、開発者が各 API を個別に理解する必要がないため、利便性が高くなります。しかし、当初はコールバック API には標準的なパターンがなく、各 API は独自のパターンを持っていました。ある API では、成功とエラーの両方に対して単一のコールバック関数を使用し、第 1 引数としてエラーを渡す形式が採用されることがありました。たとえば、 readFile 関数です( **リスト** 4)。これは Node.js の API で、ファイルを読む関数です。この関数の第 3 引数にコールバック関数を渡すと、ファイル読み込み結果がコールバック関数に渡ってきます。読み込みに成功したときは、コールバック関数の第 2 引数の data にファイルの中身が、失敗時には第 1 引数にエラーの内容が渡ってきます。

**▼リスト4 readFileの例 import { readFile } from "fs"; readFile("file.txt", "utf8", (error, data) => { if (error) {** **<mark>// 失敗時の処理</mark> } else {** **<mark>// 成功時の処理</mark> } });**

またある API では、コールバック関数の渡し方が異なったり、エラーと成功の両方に対して別々のコールバック関数を提供する形式が採用されたりしていました。 XMLHttpRequest はその典型的な例です( **リスト** 5)。この API は、 HTTP リクエストを送信する API です。コールバック関数の渡し方は、 XMLHttpRequest オブジェクトのプロパティにセットするパターンを採用しており、 readFile と比べると様子がかなり異なります。また、処理成功時のコールバックは onload

プロパティにセットし、処理失敗時のコールバックは onerror プロパティにセットする方式を採用しています。

###### **▼リスト5 XMLHttpRequestの例** 

**onst xhr = new XMLHttpRequest(); xhr.open("GET", "https://api.example.com"); xhr.onload = () => {** **<mark>// 成功時の処理</mark> }; xhr.onerror = () => {** **<mark>// 失敗時の処理</mark> }; xhr.send();**

コールバック API が不統一だったため、開発者は各 API ごとにコールバックパターンを正しく理解する必要があり、実装の障壁となったり、コールバック地獄をさらに複雑化させたりする要因ともなりました。コールバック地獄や不統一なコールバック API などの問題を解決する方法の 1 つとして、 Promise や async/await が登場します。これらを利用することで、非同期処理のコードがより読みやすく、理解しやすくなっていきます。

##### **Promiseを用いた非同期処理の改善** 

非同期処理では処理結果の値をすぐに使えないという性質があるため、同期処理のように結果を関数の戻り値にできません。たとえば次のように、投稿取得処理 getPost が処理結果となる投稿 post を返すことはできません。

**<mark>// 非同期処理では結果をすぐに返せない</mark> const post = getPost(postId);**

結果を次の処理に受け渡すために、どうしてもコールバック関数の受け渡しが必要でした。この制約がコールバック地獄のような問題を招いたわけです。こうした問題を解決するために、 ECMAScript 2015 ( ES2015 ~~)~~ <u>注5</u> ~~の~~ ように、投稿取得処理 getPost は将来得られるであろう投稿 post を Promise オブジェクト postPromise として返すことができます。

**<mark>// 結果の代わりにPromiseを返す</mark> const postPromise = getPost(postId);**

Promise が返された時点では結果が確定していない場合もあるので、 Promise から Promise.result のように結果を参照することはできません。その代わりに、 Promise の then メソッドにコールバック関数を渡すことで、結果が確定したときにその値を参照できます。たとえば、 HTTP 通信を行う XMLHttpRequest の代替手段としては fetch 関数が近年もっぱら利用されますが、この fetch 関数は Promise オブジェクトを返します。 **リスト** 6の例では、 fetch 関数の戻り値を responsePromise に代入しています。この値も Promise オブジェクトであり、将来的に HTTP レスポンスがこの Promise を通じて得られるようになります。 Promise オブジェクトの then メソッドにコールバック関数を渡すことで、レスポンスが得られたときに行う処理を予約しています。非同期的に通信が行われ、レスポンスが得られた際には、予約したコールバック関数の第 1 引数にレスポンスが渡ってきます。そして、コールバック関数の中では、そのレスポンスの情報を参照できます。ここでは、例としてレスポンスが持つ HTTP ステータスコードを出力しています。

###### **▼リスト6 fetchの戻り値はPromise** 

**const url = "https://httpbin.org/delay/1"; const responsePromise = fetch(url);** **<mark>// ↓HTTPレスポンスのステータスコードが参照できる</mark>**

**responsePromise.then((response) => { console.log(response.status); });**

###### **コールバック地獄の解消** 

Promise の特徴は、処理結果の代理となるオブジェクトを想定することで、同期処理の関数呼び出しと戻り値の関係と同じ形で書けるようになるところです。そのため、コールバック地獄の例で見たような処理の入れ子が発生しなくなります。

**リスト** 7は Promise を用いて、コールバック地獄の例( **リスト** 3) を解消した例です。ここで呼び出される各関数 getPost 、 getUser 、 getOrganization は Promise を返すように実装される必要があります。これらの関数はそれぞれ非同期処理を行い、その結果を Promise として返すものとします。見てのとおり、コールバック地獄が発生せず、フラットなコードになっていることがわかります。

###### **▼リスト7 非同期処理がフラットになった様子** 

**const promise1 = getPost(postId); const promise2 = promise1.then((post) => getUser(post.userId)); const promise3 = promise2.then((user) => getOrganization(user.orgId)); promise3.then((org) => {** **<mark>// ここで必要な処理を行う</mark> });**

**リスト** 7のように Promise を変数に代入する書き方は少し冗⻑です。 Promise を連鎖させて一連の非同期処理を続けて行う場合は、 **リスト** 8のように then メソッドをつなげて書くと、よりスッキリした見た目になります。

###### **▼リスト8 thenをつなげたコード** 

**getPost(postId) .then((post) => getUser(post.userId))**

**.then((user) => getOrganization(user.orgId)) .then((org) => {** **<mark>// ここで必要な処理を行う</mark>**

**});**

###### **不統一なコールバックAPIの解消** 

Promise が導入されたことで、前述した不統一なコールバック API の問題も解消されてきました。たとえば、 Node.js ではコールバック API を用いていた readFile の Promise 版として、 fs/promises モジュールで readFile 関数を提供しています。これは従来の readFile とほぼ同じ動作をする関数を提供していますが、コールバック API ではなく Promise を返すようになった点が大きく異なります。そのため、 **リスト** 4と同じことをする場合、 **リスト** 9のように呼び出せます。

###### **▼リスト9 Promise化されたreadFileの使用例** 

**import { readFile } from "fs/promises"; readFile("file.txt", "utf8") .then((data) => {** **<mark>// 成功時の処理</mark> }) .catch((error) => {** **<mark>// 失敗時の処理</mark> });**

さらに先ほどの XMLHttpRequest の例( **リスト** 5)を、 Promise を返す fetch を用いて書き換えると **リスト** 10のようになります。

###### **▼リスト10 Promiseを返すfetchの使用例** 

**fetch("https://api.example.com") .then((response) => {** **<mark>// 成功時の処理</mark> }) .catch((error) => {**

**<mark>// 失敗時の処理</mark> });**

このように、非同期処理に Promise が用いられると、まったく異なる目的と機能を持つ API でも、同じ形式で書くことができるようになりました。こうした非同期処理の統一性が、 JavaScript の Promise がもたらした最も重要な改善の一つです。

##### **async/awaitを使った非同期処理の簡略化** 

ここまで、私たちは JavaScript の非同期処理がどう変遷してきたかを見てきました。先ほど取り上げた Promise の then も非同期処理には有効な手立てです。しかし、 then を用いた非同期処理はコールバック関数を必要とするため、コードがネストしてしまい、次のような同期処理と比べると可読性が劣ります。

**const post = getPost(postId); const user = getUser(post.userId); const org = getOrganization(user.orgId);**

このコードはあくまで同期処理にすぎませんが、今まで見てきた非同期処理と比べて、操作が順に書かれており、入力と出力の関係が明瞭で直感的なコードになっています。同期的な書き味と非同期処理を両立した書き方が望まれるようになりました。そこで、 Promise を用いたコードをより直感的に扱えるようにする構文として、 ECMAScript 2017 ( ES2017 )で導入されたのが、 async (エイシンク)と await (アウェイト)です。

**await**

await とは、非同期処理の完了を待つためのキーワードです。 Promise の値が参照できるようになるまで、 await より下に書かれた処理に進まないようにする役割を持つキーワードです。 await は、基本的には Promise を返す関数の前に配置されます。 **リスト** 11のような構文になります。

###### **▼リスト11 awaitの文法 const** **<u>値 = await Promiseを返す関数();</u>** 

関数呼び出しに await をキーワードを付けた場合、戻り値に代入される値は Promise ではなく、 Promise が約束した値になります。したがって、たとえば **リスト** 12のような投稿の Promise を返す関数 getPost に await を付けて呼び出した場合、投稿( post )そのものが代入されます。

###### **▼リスト12 getPostにawaitを用いた例 const post = await getPost(postId);** **<mark>// post.userId のように直接投稿を扱える</mark>** 

await キーワードは Promise が解決するまで次の行に進みません。これは一見、同期処理のように見えるかもしれませんが、プログラム全体の進行がブロックされるわけではありません。先述したタイムスタンプの描画がブロッキングによって一時停止した実験を思い出してください。その実験では、通信タスクがプログラム全体をブロックしたため、関係のない時刻描画タスクにも影響が及んでいました。 await 式が影響を及ぼすのは、それ以降に書かれたタスクだけです。別のところに書かれたタスク(たとえば、ほかの通信処理や描画処理など)には影響しません。これにより、 UI がフリーズするといった問題は発生せず、ユーザー体験も損なわれません。

###### **async** 

async は非同期関数を宣言するためのキーワードです。構文は **リスト** 13のとおりです。

###### **▼リスト13 asyncの文法** 

**const** **<u>関数名 = async () => {</u>** **<mark>// 処理</mark> };**

このキーワードが付いた関数は必ず Promise を返すようになります。たとえば、 return 文などで文字列を返す関数の場合、戻り値は単に文字列ではなく、文字列の Promise になります( **リスト** 14)。

###### **▼リスト14 文字列を返す非同期関数の実装例** 

**const getString = async() => { return "Hello world"; }; console.log(getString());** **<mark>// => Promise { "Hello world" }</mark>**

async を付けた非同期関数では、その関数内で await 文が使えるようになります( **リスト** 15)。逆に言うと、 await 文を使えるのは非同期関数の内部だけである点に注意が必要です注6 ~~。~~

###### **▼リスト15 asyncとawaitを組み合わせた例** 

**const sendRequest = async () => { const url = "https://httpbin.org/delay/1"; const response = await fetch(url);** **<mark>// awaitが使える</mark> return response.status; };**

async/await を使うことで非同期処理を行っているにもかかわらず、直感的かつ同期処理のような形で書くことができるようになります。このような書き方は理解も容易で、可読性が高いため、非同期処理が頻繁に行われる現代の JavaScript プログラミングにおいては極めて有用な構文です。

##### **Column  TypeScriptと非同期処理** 

最近は、JavaScriptだけでなくTypeScriptを用いた開発も増えているので、TypeScript での非同期処理についても少し触れておきます。

JavaScriptのスーパーセットであるTypeScriptは、静的型付けを採用しており、より安全で保守性の高いコードを記述することが可能な言語です。TypeScriptで非同期処理を書く場合、初学者によくある疑問として、非同期関数にどのように型注釈をすればいいかわからないというものがあります。通常、非同期関数の戻り値はPromise<T>型を指定します。ここでTはPromiseが解決されたときの値の型を表します( **リスト** A)。

###### **▼リストA 非同期関数の戻り値の型注釈** 

**<mark>// 誤った型注釈</mark>**

**const getString = async (): string => { return "hello world";**

**};**

**<mark>// 正しい型注釈</mark>**

**const getString = async (): Promise<string> => { return "hello world"; };**

この関数は文字列をreturnしているため、一見すると型注釈はstringが正しそうですが、asyncには戻り値の値をPromise化する効果があるため、型注釈には Promise<string>を用いるのが正しいです。また、awaitを使用して非同期関数の戻り値を取得する場合、その戻り値はPromiseが解決された結果の型、つまりTとなります( **リスト** B)。

###### **▼リストB awaitと型注釈** 

**<mark>// 誤った型注釈</mark>**

**const str: Promise<string> = await getString();** **<mark>// 正しい型注釈</mark> const str: string = await getString();**

await文にはPromiseから値を取り出す効果があるため、Promise<string>ではなく stringを型注釈するのが正解です。これらの基本的なルールを理解しておくと、 TypeScriptでの非同期処理の実装に取り組みやすくなるでしょう。

###### **Promiseからasync/awaitへの書き換え** 

**リスト** 8で示した Promise を用いた処理を、 async/await を用いて記述すると **リスト** 16のようになります。

**▼リスト16 Promiseをasync/awaitに書き換えた例**

**const myFunc = async (postId) => { const post = await getPost(postId); const user = await getUser(post.userId); const org = await getOrganization(user.orgId);** **<mark>// ここで必要な処理を行う</mark> };**

**リスト** 16のコード例では、最初に **getPost(postId)** を実行し、その結果が得られしだい次の **getUser(post.userId)** を実行します。すべての操作が非同期で行われますが、 await によってその処理がコード上は同期的に見えるように記述されています。なお、 Promise の then を使った場合と同様、ここで扱うそれぞれの関数 getPost 、 getUser 、 getOrganization も Promise を返す形になっている必要があります。これまで JavaScript の非同期処理の変遷を見てきました。 JavaScript の非同期処理は、コールバックから Promise 、さらに async/await へと進化してきました。その進化は、開発者が非同期処理をより簡易に、そして直感的に扱えるようにするものでした。さらに深く掘り下げるために、このあとの章では非同期処理の前提知識と用語の整理、 Promise と async/await の詳細を見ていきます。非同期処理への理解をいっそう深めるために、次章へ進みましょう。

- 注1) JavaScriptでもマルチスレッドプログラミングができないわけではありません。ブラウザではweb worker、サーバサイドではNode.jsのworker threadsを用いることでマルチコアを活かしたプログラムが作れます。 (本文に戻る) 

- 注2) 実験のためにopenメソッドの第3引数にfalse(すなわち同期処理)を指定しますが、実際の使用では非推奨です。ただし、Workerの中での同期処理は非推奨ではありません。 

   - (本文に戻る) 

- 注3) 静止画ですのでわかりにくいですが、タイムスタンプはものすごい勢いで更新されていきます。 (本文に戻る) 

- 注4) 執筆時点ではFirefox 114.0.2(64ビット)で動作確認しました。 (本文に戻る) 

- 注5) ECMAScript(ES)は、JavaScriptの仕様です。ESは毎年アップデートされ、各ブラウザベンダーはその仕様に基づいてJavaScriptを実装します。ES2015の2015 のような数字の部分は、仕様が策定された⻄暦を表します。 

   - (本文に戻る) 

- 注6) awaitが使える非同期関数以外の場所として、トップレベルawaitもあります。 (本文に戻る) 

# **第 2 章** 

### **なぜ非同期処理は難しいのか? 初学者がハマらないための前提知識と用語の整理** 

**非同期処理の構文を学習しても、どこか納得できないところは残っていませんか? 本章では非同期処理にまつわる数々の疑問に迫ります。本章の説明には第3章以降で説明する内容が若干含まれます。難しく感じたらほかの章を読んでからで良いので本章を通読してみてください。新たな視点で非同期処理をとらえられるでしょう。**

PADAone

@pd1xx yo-goto

##### **JavaScriptにおける非同期処理** 

非同期処理は正確に定義することが難しい概念ですが、なるべく具体的な目的や背景情報を補って「 JavaScript における非同期処理」とは何かを定義してみると次のようになります。

JavaScriptは言語レベルでシングルスレッドであり、通信やファイルアクセスといった時間のかかる処理があると、実行環境の JavaScriptを実行するスレッド(ブラウザ環境でUI処理を担うUI さばスレッドや、非常に多くのリクエストを捌くサーバサイドランタイム環境のメインスレッド)がブロッキングによって⻑時間専有される恐れがあります。非同期処理はそのような問題を発生させないためのものです。より具体的に言えば、非同期処理とは、実行環境から提供される非同期APIの処理と、その後続の処理によって起こる「複数の関連する事象が前の事象の完了を待たずに起きる」非同期の性質と、非同期APIを利用する一連の関連処理を記述・制御するために必要なシンタックスと技法、そしてそれらを包括的に言及するテーマであると言えます。多くの用語を使い定義しましたが、非同期処理というテーマのコアは「実行環境から提供される非同期 API とそれを利用する一連の関連作業をいかに記述して制御するか」という部分です。 Promise や async/await といったシンタックスの話題はその記述と制御の部分に相当していますが、なぜ非同期処理を行うのかという目的の部分と実際に非同期処理を実現するための機構についての答えは与えてくれないため、それらを補うための前提知識が必要となります。

##### **非同期処理のテーマとトラップの事前検知** 

非同期処理は背景的に多くの概念や話題が絡み合っており、丁寧つじつまに整理しないと説明の辻褄が合わなくなるような状況が起きるため、シンプルに理解することが困難です。まずは、非同期処理を「記述や制御が難しいが効率的に処理を行えるプログラミング」についての **テーマ** であると、とらえることで多くの知識を矛盾なく受け入れられます。あるいは、非同期処理という言葉自体を使わずに非同期プログラミング( Asynchronous programming )という言葉で、プログラミングにおける **技法** であると、とらえておくのも良いでしょう。非同期プログラミングでは、そうでない普通のプログラミングに比べて効率的に複数の処理を進められます。その一方で、記述や制御が複雑で難しく、多くの知識が必要となります。初学者にとっては非同期処理を行うために必要な Promise などのシンタックスを理解するだけでも難しいのですが、そのほかにも、 **シンタックスを学んだだけでは必要な前提知識が欠如しており** 、行いたい一連の作業の実行と完了の順番を担保した記述や制御を正確に行えないという不可視のトラップも存在しています。

▼ トラップその 1

非同期処理はシンタックスを学んだだけでは何が起きているかわからず、自信を持ってコードを読み書きするには実行環境の API 機能やランタイムモデルなどの前提知識が必要です。この章では初学者がトラップにハマらないように、それらの重要な前提知識とトラップを整理して、非同期処理を理解できるようになるための道筋を示します。

###### **使用環境とサンプルコードの実行** 

サンプルコードは Deno 環境( v1.35.1 )を使い、 **deno run -A sample.js** のようにコマンドラインからのスクリプト実行で非同期処理の動作を検証します。コードは次のように表記し、 console.log() 内に実行順序の番号を記載しておくので、どのような実行順序になるかはその数字を追ってください。

**console.log("[1]"); console.log("[2]"); console.log("[3]");**

誌面節約の都合上、コマンド実行の表記は省略し、 URL やファイルパスなども具体的な値を指定せずに変数で url や path として表現しています。

##### **非同期処理とは──用途と概念の整理** 

非同期処理とはいわばテーマであって、さまざまなものが内訳として含まれています。たとえば、 setTimeout という関数( **リスト** 1) はんちゅうは非同期処理の範疇に入ります。

###### **▼リスト1 setTimeout関数** 

**console.log("[1]"); setTimeout(() => { console.log("[3]: 1000ms後に実行されます"); }, 1000);** **<mark>// 上記の処理の完了を待たずに次の処理を実行します</mark> console.log("[2]");**

同様に Promise チェーン( **リスト** 2)や async/await などのシンタックスによって書かれた処理も非同期処理の範疇となります。

###### **▼リスト2 Promiseチェーン** 

**console.log("[1]"); Promise.resolve() .then(() => console.log("[3]"));** **<mark>// 上記の処理の完了を待たずに次の処理を実行します</mark> console.log("[2]");**

どちらのコードにも前の処理の完了を待たずに次の処理を実行している箇所があり、書いた順番に console.log が実行されていないことがわかります。ただし、両者はそれぞれ異なる役割やメカニズムを持っているため、初学者が非同期処理という 1 つの用語で同じ処理として理解しようとすると、非同期処理そのものが結局なんなのかわからなくなるというトラップがあります。それゆえに、非同期処理というテーマの中にある具体的なものそれぞれを異なるものとして認識していく必要があります。

▼ トラップその 2

非同期処理のテーマは広範囲にわたり、異なる役割とメカニズムを持つさまざまな処理が一様に非同期処理として扱われることで、非同期の性質や実行順序が理解しづらくなっています。

###### **実行環境から提供されるAPI** 

#### **APIはJavaScriptの機能ではない** 

setTimeout と Promise チェーンは両者ともに非同期処理の範疇に入ると説明しましたが、実は setTimeout は JavaScript の機能ではなく、実行環境( Runtime environment )から提供される API 機能に分類されます注1 ~~。~~

実行環境とは具体的に言えば、 Chrome や Edge といった各種のブラウザ環境やサーバサイドで利用される Node.js や Deno といった環境のことです。 JavaScript を実行するにはそのプログラムを実行するための実行環境が必要であり、それらの環境が提供する API によって多数の便利機能を JavaScript のコードから呼び出せるようになっています。よく利用される console.log も API であり、 JavaScript は環境の API 機能がなければ大したことはできません。ただし、 API は環境によって提供されるので、それぞれの環境によって使える機能が異なるという問題があります。そもそも Chrome と Node.js を比べてみればクライアントサイドとサーバサイドというまったく用途が異なる環境で同じ JavaScript という言語が利用できるのは、どちらの環境も「JavaScript **エンジン** 」と呼ばれる JavaScript を実行するためのコンポーネントを搭載しているためです。変数や関数の書き方などのシンタックスは ECMAScript 仕様によって定義されています。基本的に JavaScript は ECMAScript を実装する JavaScript エンジンにある機能しか利用できませんが、実行環境は

JavaScript エンジン以外にもさまざまなコンポーネントを搭載しているため、 API として呼び出せる便利な機能を JavaScript へと提供できます。当該の setTimeout ですが、 JavaScript エンジンはこの機能を実装しておらず注2 ~~、~~ 各実行環境が独自にその機能を実装しています。つまり、 setTimeout というのは実行環境によって異なる挙動があったりします。実際、 Node.js の setTimeout は Chrome などのそれとは異なる挙動をとります。複数の環境で setTimeout という同じ名前で同じ機能を利用できるのは、 WHATWG という仕様策定団体が定める HTML 仕様に定義された setTimeout を、各種ブラウザベンダーやサーバサイドランタイムの開発者がそれぞれ実装しているからです。逆に、そういった標準仕様に定義されていないものとして、 Node.js や Deno では実行環境独自の API があることにも注意してください。基本的にそれらの API はほかの環境では利用できません。繰り返しますが、 setTimeout は JavaScript の機能ではなく環境から提供される機能です。一方で、 Promise チェーンや async 関数などのシンタックスは ECMAScript 仕様で定義されているため、 JavaScript の機能です。非同期処理の解説ではこの 2 つを明確に区別せずに同じ非同期処理として扱うことがあるので両者を混同しないように相当に注意する必要があります。

async 関数( async function )は async キーワードが付いた関数のことですが、似た用語である非同期関数( asynchronous function )は、処理結果が非同期的に得られる関数や次の処理の実行を妨げない関数のことを指します。この 2 つの用語は抽象度や用途が異なりますが、 async 関数と setTimeout などの非同期 API は両者ともに非同期関数と呼ばれることがあるため、 async 関数と非同期 API を同じものだと混同させるトラップがあります。 async 関数と非同期 API はそのしくみも役割もまったく異なるので明確に呼び分けることを推奨します。そもそも非同期関数という用語は、常に非同期の事象を起こすような処理がそれ単体で存在しているかのような錯覚を起こさせるので、区別できるようになるまでは使用を控えたほうが良いです。

▼ トラップその 3

非同期処理のテーマでは訳語やキーワード自体が学習者を惑わすトラップとなり得ます。丁寧に違いを区別して相互の関係を理解することを心がけてください。

#### **APIの種類** 

非同期処理のテーマでは実行環境から提供される API 機能が非常に重要な位置を占めます。実行環境は Chrome や Firefox といったブラウザ環境や、 Node.js や Deno といったサーバサイドなどで利用される非ブラウザ環境などに分類できます。

API 自体にもいろいろな種類と分け方があり、ブラウザによって提供される setTimeout や fetch などの API は MDN のドキュメントでは Web API と呼ばれています。ただし、この呼び方は API に詳しくない学習者を誤解させる可能性が高いため、実態に即した Browser API という呼び方のほうが適切でしょう。本章で Web API と言ったらブラウザの提供する機能であると認識してください。

setTimeout や fetch のような標準仕様が存在する Web API は非ブラウザ環境も提供していることが多いです。しかし、ブラウザ環境にないような API についてはそれぞれの非ブラウザ環境が独自に定義しているために基本的には互換性はありません。ここではそういった非ブラウザ環境独自の API のことを便宜的に Runtime API と呼ぶことにします。さらに、 API は同期 API と非同期 API に分類できます。同期 API についてはあとで解説しますが、基本的に非同期 API を使ったほうが効率的な処理を行えることを覚えておいてください。

#### **非同期APIの種類** 

当該の setTimeout は非同期 API に分類されますが、非同期 API 自体にもさまざまな種類があります。非同期 API はそれらが使用する機構から、たとえば次のように分類できます。

###### **(** 1 **)タスクベースの非同期** API 

コールバック関数(の実行コンテキスト)をタスクとして発行する例: setTimeoutやsetIntervalなど

- **(** 2 **)マイクロタスクベースの非同期** API 

   - **(** A **)コールバック関数(の実行コンテキスト)をマイクロタスクとして発行する非同期** API 

例:queueMicrotaskやMutationObserverなど

- **(** B **)** Promise-based API 

Promiseオブジェクトを返し、その中にAPIの処理結果(取得データなど)を入れておく例:fetchやBlob.textなど非同期 API はタスク( Task )を利用するかマイクロタスク ( Microtask )を利用するかで分類でき、マイクロタスクベースの非同期 API はマイクロタスクを直接利用するか、 Promise を介してマイクロタスクを間接的に利用するかという観点でさらに 2 つのタイプに分類できます。タスクは古い機構でマイクロタスクは Promise 処理のために追加された新しい機構です。

setTimeout は昔から存在する API で古い機構であるタスクを利用します。一方、 fetch といったモダンな非同期 API は基本的に Promise を返すような API 、つまり、Promise-based APIとして設計されており、間接的にマイクロタスクを使って後続の関連処理を実行できます。ここで Promise はモダンな非同期 API の処理結果を統一的に扱えるインターフェースとなってることに注意してください。このように各種の非同期 API は利用する裏のしくみが異なるため、 **それぞれ実行タイミングや考え方が異なります** 。これを認識して処理の順番を予測できるように必要なのが、イベントループやタスクキュー、マイクロタスクキューの概念です。

▼ トラップその 4

非同期 API は利用する機構から複数の分類に分けられるため、それらを同列に扱うと実行タイミングについて混乱します。違いを認識することが Promise 化などの手法を理解するうえでも重要です。

#### **同期APIとブロッキング** 

非同期 API の効率性と利用の難しさは同期 API について知ることで理解できます。簡単に言えば、同期 API とはその処理を環境が実行している間は JavaScript のスレッドをブロッキング(⻑時間専有)して、待ち時間を含めて処理が完全に完了してから次の処理を行うようにする API です。ユーザーインタラクションが重要なブラウザ環境ではほぼ見かけませんが、コマンドラインからの書き捨てのスクリプトなどを書ける非ブラウザ環境ではさまざまな同期 API を利用できます。わかりやすい例として、 Node.js や Deno 環境では I/O 処理などのために同期 API と非同期 API の両方が提供されています。同期 API は非同期 API の名前の後ろに Sync が付いています。たとえば、ファイルへの書き込み処理を行う API は各環境で次の名前のものとして提供されています。

- **・非同期** API **(ノンブロッキング** API **)** 

   - Node: 

   - ・fs.writeFile(Callback-based API注3 ~~)~~ 

   - ・fsPromises.writeFile(Promise-based API) 

   - Deno:Deno.writeFile(Promise-based API) 

- **・同期** API **(ブロッキング** API **)** 

Node:fs.writeFileSync

Deno:Deno.writeFileSync

非同期 API と同期 API はそれぞれノンブロッキング API やブロッキング API とも呼ばれます。非同期や同期はブロッキングの概念を介することで理解がしやすくなります(異なる概念であることに注意)。たとえば、 Deno 環境でテキストファイルの読み取りを行うための非同期 API ( Deno.readTextFile )と同期 API ( Deno.readTextFileSync ) を比較してみましょう。 **リスト** 3の処理は同期 API を利用したコードで、上から下に書いた順番に処理が実行、完了していくため非常にわかりやすいです。同期 API が呼び出されるとき、 JavaScript を実行するためのスレッドは API 処理が完了するまで専有されて、次の処理を実行できなくなります。これがブロッキングです。

###### **▼リスト3 ブロッキングを行う同期APIの例** 

|**console.log("[1]");**|
|---|
|**const data = Deno.readTextFileSync(path);**|
|**//上の同期APIの完了を待ってから次の処理を実行**|
|**console.log(`[2] ${data}`);**|
|**console.log("[3]");**|

一方で、非同期 API を利用した **リスト** 4のコードでは書いた順番に処理は実行されず、非同期 API とそれを起点にした一連の関連作業 (この場合はコンソールへの出力)が完了する前に次の処理が実行されていることがわかります。 JavaScript を実行するためのスレッドは API 呼び出しのあとでもブロッキングされないため、次のコードを API 処理が完了するかどうかとは関係なく実行できます。この場合はノンブロッキングだと言えます。

**▼リスト4 ノンブロッキングな非同期APIの例**

**console.log("[1]"); Deno.readTextFile(path) .then((data) => console.log(`[3] ${data}`));** **<mark>// 上の非同期APIと後続の関連処理の完了を待たずに次の処理を実行</mark> console.log("[2]");**

まとめると、同期 API を使ったコードは処理の流れが非常にわかりやすい一方で、同期 API の処理が完了するまで次の処理を行えないというデメリットが存在しています。 API は環境の提供する機能であり、上記のような API 処理は環境を介して OS へシステムコールを行うことで実現しています。そして、こういったファイルの読み書きやネットワークを使用する処理は待ち時間が⻑いので、その待ち時間中にも別の処理( JavaScript や API 呼び出し)を行えたほうが効率的ですね。これを実現するために利用するのが非同期 API です。非同期 API を使ったコードは処理の流れのわかりやすさを犠牲に、 JavaScript のスレッドをブロッキングすることなく効率的な処理を行えます。

###### **並行プログラミングの手法** 

非同期処理が起きているときに実際はどのようなことが起こっているかを理解するには、並行や並列などの関連した別の概念の理解が必要となるというトラップが存在します。並行・並列も理解するのが非常に難しい概念ですが、非同期プログラミングはある種の **並行プログラミングを記述するための手法** としてとらえると理解しやすくなります。

▼ トラップその 5

非同期処理を理解するには、並行・並列といった関連する別の概念を区別したうえで理解する必要があります。英語では並行の概念は Concurrency で、並列の概念は Parallelism という用語で表現されます。どちらも複数の処理を **同時** に実行することで効率的に処理を行うことを考えるための概念です。ただし、 **図** 1に示したように、同時という言葉にはとらえ方が複数あることに注意してください。ある一定の幅のある時間で複数の計算処理が行われている場合には並行( concurrent )であると、とらえられます。この場合、待機時間があるかや、複数処理をどのように行うかは、気にしません。一方で、ある特定のタイミングで複数の計算処理が実際に実行されている場合には並列( parallel )であると、とらえられます。並行だが並列ではない処理や、並行かつ並列な処理があり得ます。並行のある時点を切り出せば並列という状況があり得るので、並行プログラミングを考えることで両者をとらえられます。 JavaScript の非同期処理を書いているとき、実際にはシングルスレッドで処理を順番に行うような並行処理を記述しています( **図** 2)。これはあとで解説するイベントループを使って実現されますが、環境がバックグラウンドで行う API 処理も考慮したスケールでは **さらに複雑な並行** となっています。 API は実質的にスレッドとして機能するので、その内部について知らなければマルチスレッドでの並列な状態が発生しているように見えます。実際には環境の API はさまざまなメカニズムの処理を抽象化しているため、並行になるとは言えても並列になるとは言い切れないのが実情です。たとえば、 setTimeoutAPI は定期的に環境が管理する時計と開始時間の差分計算を行ってタイマーを実現していたりするので、複数のタイマーを同時起動しても並列は起きないわけです( **リスト** 5)。ほかにも fetchAPI の処理は待ち時間がほとんどなので、複数のデータフェッチをそれぞれ完了を待たずに連続で行ったときに正確に計算処理が並列化しているかどうかはどのようなスケールでとらえるかに左右されます( **リスト** 6)。内部メカニズムを無視してデータフェッチ自体を作業単位としてみなすと継続的に複数作業を行っているので並列的であると、とらえることも可能です(一般には並行です)。

###### **▼リスト5 並列が発生し得ない並行処理(タイマーは一括管理されている)** 

**console.log("[1]");** **<mark>// 2つのタイマーが同時に進んでいるが実現のされ方は並列ではない</mark> setTimeout(() => console.log("[4]"), 300);** **<mark>// 上の処理の完了を待たずに次の処理を実行</mark> setTimeout(() => console.log("[3]"), 200);** **<mark>// 上の処理の完了を待たずに次の処理を実行</mark> console.log("[2]");**

###### **▼リスト6 複数のfetchの完了を待たずに順番に呼び出す** 

**fetch(url1);** **<mark>// 完了を待たずに次の処理を実行</mark> fetch(url2);** **<mark>// 完了を待たずに次の処理を実行</mark> fetch(url3);** **<mark>// 完了を待たずに次の処理を実行</mark> console.log("完了を待たず実行");**

**リスト** 6のコードとは対照的に **リスト** 7のコードでは 1 つずつ完了を待って処理を行うため時間がかかってしまいます。

###### **▼リスト7 1つずつfetchの完了を待って次の処理を呼び出す** 

**(async () => { await fetch(url1);** **<mark>// 完了してから次の処理を実行</mark> await fetch(url2);** **<mark>// 完了してから次の処理を実行</mark> await fetch(url3);** **<mark>// 完了してから次の処理を実行</mark> console.log("すべて完了してから実行"); })();**

この 2 つのコードを比較すると **リスト** 6のほうが圧倒的に効率的です。関連して Promise.all などの複数の Promise 処理をまとめあげる静的メソッドの話題でよく聞く「並列化」というワードはその実態が何なのか語られることなく使われているケースが多いです。これは結局のところ非同期 API はブロッキングしないので、複数の非同期

API をそれぞれの完了を待つことなく連続で呼び出せるという話になります。現実的にある特定のタイミングでこれらの複数の計算処理(内部的には OS の機能を利用する通信処理)が並列な状態になっているかは JavaScript からは知るよしもないですが、あるタイミングでは並列になっているかもしれません(大部分は待ち時間が重ね合わさった状態です)。

Promise.all 自体は処理を並列化する能力などは持っていません。 **リスト** 8のようなコードではブロッキングしない複数個の非同期 API を連続で呼び出すことでそれぞれの待ち時間を重ね合わせ、 1 つずつ完了を待つよりも大幅に時間を短縮できるというのが並列化という言葉の実態です。

- **▼リスト8 Promise.allとは関係なく複数個の非同期APIが連続で呼び出される** 

**Promise.all([** **<mark>// 複数のfetchを完了を待たずに順番に呼び出す</mark> fetch(url1),** **<mark>// 完了を待たずに次の処理を実行</mark> fetch(url2),** **<mark>// 完了を待たずに次の処理を実行</mark> fetch(url3), ]);**

より現実的には非同期 API 単体で呼び出すのではなく、後続の関連処理と合わせて async/await を使って **リスト** 9のようにまとめあげます。この場合はさらに複雑で、 async 関数内部の非同期 API を呼び出す際に関数内の処理を一時中断して、次の async 関数を起動するということを繰り返すことで、複数個の非同期 API を短時間に連続で呼び出して待ち時間を重ね合わせる状況を作り出しています。そして、 API 処理が完了したタイミングで、中断していた async 関数内の後続処理をイベントループによって処理します。さらに、別の非同期 API を利用していればそのサイクルを繰り返すことになります。 async 関数単位での複数作業は、非同期 API とイベントループを使うことでシングルスレッド上で同時に進められるので、ある時間幅で **並行プログラミング** を行っていることになります。

###### **▼リスト9 より現実的な非同期APIを使った並行処理の記述** 

**async function fetcher(url) { const res = await fetch(url); const text = await res.text(); return text; }**

**Promise.all([ fetcher(url1), fetcher(url2), fetcher(url3), ]).then((data) => console.table(data));**

###### ▼ トラップその 6 

並列化という言葉はその複雑な実態が語られることなく気軽に利用されている(あるいは誤解されている)ので注意する必要があります。

###### **非同期処理の目的** 

JavaScript はシングルスレッドで動き、ブラウザ環境では JavaScript が実行される UI スレッドをブロックしないようにするために、大部分の API が非同期型にされています。そのため、プログラマーは非同期 API を使用した一連の処理を記述・制御するためのシンタックスで書くことを求められます。非ブラウザ環境でも同様にメインスレッドをブロックしないようにすることが求められるので、非同期 API を使うことになります。処理効率が求められない書き捨てのプログラムなどでは、非同期 API を使わなくても済むのなら制御の流れや記述が簡単な同期処理で書けばいいのですが、非同期 API を使うならその制御に非同期のシンタックスを書かざるを得ません。非同期 API を直接的に使用せずとも、ライブラリなどから提供される async 関数を使う場合には非同期プログラミングを行う必要が出てきます。そのような関数でも内部的には想定される実行環境の非同期 API を利用しています。結局、 JavaScript における非同期処理というテーマの目的は「JavaScript **のスレッドをブロッキングしない効率的な非同期** API **を使いたい** 」ということです。この目的を果たすために、記述と制御に利用する Promise や async/await といったシンタックスを学ぶことが非同期プログラミングを習得するという話になります。

###### **結局のところ非同期とは** 

非同期というテーマは「 **メインのプログラムの流れとは独立に発生するイベントとそれらへの対応をどのようにするかという方法** 」に関する主張や議論です。通常のプログラムは書かれた順番に実行されていくのでそのような特殊な方法を考える必要はありません。一方で、 JavaScript のプログラムとは独立に発生する事象(環境の API 処理の結果)と完了後に発生させたい一連の事象(後続の関連処理)をコード上で記述しようとすると、コールバック関数や async/await などの特殊な方法が必要になります。これが非同期プログラミングという **技法** です。非同期処理という言葉を使った場合には「非同期になる処理がある」という認識になりますが、コードとその実行結果から非同期という性質を観測しようとすると、非同期は「 **複数の関連する事象が前の事象の完了を待たずに起きる** 」という性質としてとらえられます。非同期の性質は基本的には実行環境の機能である非同期 API の処理と後続の処理との関係において観測できます( **リスト** 10)。

###### **▼リスト10 非同期の性質を観測できるコードと実行結果** 

**console.log("[1]"); fetch(url)** **<mark>// 非同期API(環境がバックグラウンドで実行)</mark>**

**.then((res) => res.text()) .then((data) => console.log("[3]", data));** **<mark>// 上の処理の完了を待たずに次の処理を実行できる</mark> console.log("[2]");**

ただし、 **リスト** 11のように非同期 API の後続に処理がなければ非同期の性質を観測できないことに注意してください。

###### **▼リスト11 非同期の性質を観測できないコードと実行結果** 

**console.log("[1]"); fetch(url) .then((res) => res.text()) .then((data) => console.log("[2]", data));**

**<mark>// 後続の処理がないので非同期を観測できない</mark>**

つまり、「非同期処理」単体では非同期的に処理されるかどうかは明らかではありません。非同期は後続のコードとの関係があって初めて成り立つ性質で、状況しだいで観測できたりできなかったりするようなものなのです。

JavaScript の非同期処理というテーマの目的は効率的な非同期 API の利用にあり、 Promise や async/await といったシンタックスはその目的をかなえるコードを記述するために必要なものでした。非同期 API の話題を抜いてこれらのシンタックスだけについて考えるときでも非同期を観測できますが、正しく使わなければ同期になります。

**リスト** 12のように単に Promise オブジェクトを生成しただけでは非同期的には処理されません。 **リスト** 13のように Promise チェーンによって後続の関連処理をコールバック関数として実行するときになって非同期を観測できます。同様に async 関数も内部に await 式がなければ同期的に処理されます。

###### **▼リスト12 非同期の性質を観測できない例** 

**console.log("[1]"); new Promise((resolve) => { console.log("[2]"); resolve(); }); console.log("[3]");**

###### **▼リスト13 非同期の性質を観測できる例** 

**console.log("[1]"); new Promise((resolve) => { console.log("[2]"); resolve(); }).then(() => console.log("[4]"));** **<mark>// 上の処理の完了を待たずに次の処理を実行できる</mark> console.log("[3]");**

状況しだいで観測できる性質を「非同期処理」とか「非同期関数」といったワードで認識しようとすると、常に非同期になる処理がそれ単体で存在するかのような錯覚にとらわれます。非同期処理をテーマや技法として扱うことを推奨してきたのはそれを防ぐためです。非同期はコード上では複数の処理の関係性から観測できるものであることに注意してください。

##### **ランタイムモデルと実行環境──JavaScriptの振る舞い** 

###### **ランタイムモデルとは** 

JavaScript には実行時にプログラムがどのように処理されるかを表現するランタイムモデル( Runtime model )が存在しています。このモデルを知ることで、 Promise チェーンにおける then メソッドのコールバック関数や async 関数内の await 式で分割された後続処理の実行タイミングを理解できます。基本的なランタイムモデルは ECMAScript 仕様で定義されています。ブラウザ環境ではさらに別の仕様である HTML 仕様からランタイムモデルに定義が追加されます。とくに重要なのがイベントループであり、この機構がランタイムモデルの中枢となりますが、イベントループの詳細は HTML 仕様から追加定義されます。一方、非ブラウザ環境は JavaScript の互換性を保つために、ブラウザ環境のイベントループ仕様に近づこうとしています。そのため、 Node.js や Deno のランタイムモデルを理解するうえでも、ブラウザ環境のランタイムモデルを知っておく必要があります。ただし、 Node.js のイベントループはブラウザ環境に近づこうとする一方で、ブラウザ環境には存在しない Phase や NextTick などの機構も同時に持っているため、注意してください。

###### ▼ トラップその 7 

実行環境における JavaScript の振る舞いは ECMAScript 仕様だけを見ても正確にわからないため、追加の外部仕様や実装そのものを見る必要があります。各環境固有にイベントループの振る舞いがあるため、最終的には利用する環境そのものについて調査する必要があります。イベントループは非同期処理を理解するうえで応用的な話題として位置づけられることも多いです。しかし実際には、これこそがシングルスレッド上で非同期処理を可能にさせている機構であり、さらに JavaScript のプログラムの振る舞いを決定づけるモデルであるため、確実に理解しなければなりません。誌面の都合上、各環境におけるイベントループの詳細は解説できませんが、非同期処理を理解するうえで非常に重要な共通動作について解説しておきます。

###### **イベントループの共通動作** 

JavaScript のコードはタスクかマイクロタスクのいずれかとして処理されます。イベントループは、各種キューにタスクとマイクロタスクを配置したうえでルールに沿って順番に処理を行うループの機構です。マイクロタスクは Promise 処理のために追加された新しい機構だと述べました。 fetch といったモダンな非同期 API は Promise を返し、そのオブジェクトにチェーンした then メソッドのコールバック関数はマイクロタスクとして処理されます。タスクは古くからある機構で、クリックといった UI イベントのコールバック関数や setTimeout といった非同期 API のコールバック関数はタスクとして処理されます。タスクやマイクロタスクがイベントループ上で連鎖的に処理されることで非同期 API を起点にした一連の関連作業が逐次的に処理されます。これが JavaScript のシングルスレッドで非同期処理を成立させるメカニズムであり、実行順序を予測するためのメンタルモデルにもなります。タスクとマイクロタスクはそれぞれタスクキュー( Task queue )とマイクロタスクキュー( Microtask queue )と呼ばれるキューに挿入されて管理されます。マイクロタスクのほうがタスクよりも優先的に処理されるルールがあり、複数の実行環境においてもイベントループの共通動作として次の事柄が期待されます。単一のタスクが処理されたら次にすべてのマイクロタスクが処理されるタスクキューの先頭にある 1 つのタスクが処理されたら、次にマイクロタスクキュー内にあるすべてのマイクロタスクが処理されます。このモデルはたとえば、 **リスト** 14のような擬似コードで表現できます。基本的には無限ループですが、この章ではコマンドラインからのスクリプト実行を考えているのでタスクかマイクロタスクがある限り回り続けるループとしています。

###### **▼リスト14 イベントループの共通動作を擬似コードで表現** 

**while (tasksAreWaiting()) {**

**<mark>// 少なくとも1つ以上のタスクキューから(環境定義のルールで)1つ</mark>**

**<mark>// のタスクキューを選ぶ</mark> taskQueue = getTaskQueue();** **<mark>// 1つのタスクを処理する</mark>**

**task = taskQueue.pop(); execute(task);**

**<mark>// 1つのマイクロタスクキューにあるすべてのマイクロタスクを処理する</mark> while (microtaskQueue.hasTasks()) { microtask = microtaskQueue.pop(); execute(microtask); } }**

マイクロタスクキューは基本的に 1 つですが、タスクキューは複数あり得ることに注意してください。タスクの種類( ≒API の種類)によってどのタスクキューにどのタスクが入れられるかが決まっており、イベントループでは複数のタスクキューからまず 1 つのタスクキューが選択されたうえでそのキューの先頭にあるタスクが処理されます。このときどのタスクキューを選ぶかは環境側が独自に決定します。たとえば、 Chrome 環境では Blink というレンダリングエンジンがタスクキューに対して優先度を振っており、どのタスクキューを選択するかを決定しています。基本的にプログラマー側からはこの決定を操作できないと考えてください。単一タスクが処理されたあとにマイクロタスクキューがチェックされて、そのキュー内にあるすべてのマイクロタスクが先頭から順番に処理されていきます。マイクロタスクキューが空になったら次のタスクが処理されることになりますが、ブラウザ環境ならこの段階にレンダリング更新作業が定期的に挟まれます。つまり、⻑い時間のかかるタスクがあると、このレンダリング更新作業ができずにフレームが落ちることになります。各種の実行環境において、この重大ルールが適用されていることが期待され、それぞれのイベントループは **リスト** 14の擬似コードで表現されたものに環境独自のルールが付与されて拡張されたものであると考えて良いです。実際のコードの実行順序で考えてみるとわかりやすいです。 **リスト** 15のような **スクリプトを実行するとき、スクリプト全体の評価はタスクとして処理されます。** 頭から最終行にかけて setTimeoutAPI の呼び出しや Promise の初期化などが順番に行われて、最後の console.logAPI の呼び出しまでが最初のタスクとしてみなされます。

###### **▼リスト15 イベントループにおけるコードの実行順序を考える** 

**<mark>// このスクリプト全体は最初のタスクとして処理される</mark>**

**console.log("[1]");**

**setTimeout(() => {** **<mark>// タスクとして処理されるコールバック関数</mark> console.log("[5]");**

**});**

**Promise.resolve() .then(() => {** **<mark>// マイクロタスクとして処理されるコールバック関数</mark> console.log("[3]"); }) .then(() => {**

**<mark>// マイクロタスクとして処理されるコールバック関数</mark> console.log("[4]"); });**

###### **console.log("[2]");** 

このタスクの中をより詳細に見ていくと、まず最初にコンソール出力の API 呼び出しが行われ、次に setTimeoutAPI の呼び出しで実行環境のタイマーが起動します。このとき、第 2 引数の遅延時間は指定していないのですぐにコールバック関数がタスクとして発行されます注4 ~~。~~ 次の Promise オブジェクトの作成では fulfilled (履行/成功) 状態として初期化しているので、チェーンした then メソッドのコールバック関数は直ちにマイクロタスクとして発行されます。そして、最終行のコンソール出力が終わった時点で最初のタスクが処理されたことになります。このとき、タスクキューとマイクロタスクキューにそれぞれ積まれているタスクとマイクロタスクですが、スクリプト評価という単一タスクがすでに処理されているため、次に処理されるのはマイクロタスクとなります。つまり、 then メソッドのコールバック関数が処理されることになります。しかし、関数内では何も返していないので then から返る新しい Promise オブジェクトは暗黙的に undefined で履行( fulfilled )されて、次にチェーンされている then メソッドのコールバック関数が連鎖的にマイクロタスクとして発行されてマイクロタスクキューに挿入されます。このとき、マイクロタスクキュー内のマイクロタスクがすべて処理されるまでは次のタスクを処理できません。そのため、新しいマイクロタスクが処理されてようやく setTimeout によって発行されたタスクが処理できることになります。

###### **「JavaScriptはシングルスレッドで実行される」の意味** 

最後のトラップとして取り上げるのは非同期処理の解説でよく聞く次の文言についてです。

JavaScriptはシングルスレッドで実行されるほかにも類型として「シングルスレッドなのに非同期処理ができる」や「シングルスレッドなのに同時実行できる」などがあります。これらの文言は非同期処理を理解するうえで役立つ一方で、うかつ迂闊に使用すると学習者に大きな混乱をもたらします。この文言を十分に解釈するには、これまで解説してきた実行環境が提供する非同期 API とイベントループといった背景知識の共有が必要となります。不足している情報を補って説明すると、この文言では「 JavaScript は確かにシングルスレッドで実行されるが、 JavaScript の範疇ではない実行環境の提供する非同期 API 処理はスレッドをブロッキングすることなく別のどこかで実行されているので、結果的に同時に複数の作業ができる」ということを言っています。 JavaScript ではない実行環境の提供する API 機能の呼び出しは、 JavaScript の外側へ処理を委任してバックグラウンドで処理され、完了後にはその処理結果と後続の関連処理を呼び出し元のスレッドに通知することで、シングルスレッド上で並行処理および非同期処理を実現しています。並行処理はマルチスレッドを使いスレッドを切り替えて処理を行うという方法があります。この方法は直感的にも理解しやすいのですが、シングルスレッド言語である JavaScript で効率的な並行処理を実現しようとするとイベントループの通知のしくみと実行環境の非同期 API 機能を併用する必要があります。たとえば、ブラウザの WebAudio といった複雑な API 機能は、環境が C++ などのシステムレベルの言語を使った処理として別のところで行っているため、 UI 処理を止めることなく利用できます。そもそも Chrome などのモダンブラウザ環境はマルチプロセスアーキテクチャであり、さまざまなプロセスの中に複数のスレッドを持ち、 API 機能は JavaScript とは別のところでバックグラウンドで処理されているため、同時に作業が進んでいるような状況が起きます。ただし、 JavaScript エンジンから見れば外側で何が起きているかは知るよしもないので、主語を JavaScript にすれば確かにイベントループを使ってシングルスレッドで実行されているわけです。だからこそJavaScript **の範疇であるかどうかが重要** で、このようなレトリックになっているのです。

▼ トラップその 8

setTimeout や then メソッドのコールバック関数として登録した関数はイベントループを使ってシングルスレッド上で処理されます。 API そのものは JavaScript の範疇ではなく、環境が提供する機能なので JavaScript がシングルスレッドであることに制限されません。

##### **まとめ** 

ここまで非同期処理の学習において初学者がハマりやすいトラップや用語について解説してきました。このテーマにはいくつもの厄介なトラップが存在しています。紹介したものはその一部にすぎませんが、初学者はこれらのトラップを認識できないため、さまざまな誤解や混乱の原因となっています。とくに Promise などのシンタックスの話題はそれ自体が難しいうえに目立つので、非同期処理の目的やメカニズムにまつわる重要な前提知識から目を逸らさせるような効果があります。これは学習対象となる領域のスケール感を見誤らせることにつながり、わからない原因がわからないという状況を発生させます。非同期処理を読み書きしていてわからないことがあれば、それはシンタックス以外の箇所での理解不足が原因になっている可能性があります。その場合には本章で解説したような前提知識を確認してみてください。

#### **■参考資料** 

- (1)“HTML Living Standard”(https://html.spec.whatwg.org/#event-loops) 

- (2)Philip Roberts,“What the heck is the event loop 

   - anyway?”(https://youtu.be/8aGhZQkoFbQ) 

- (3)高野祐輝著、『並行プログラミング入門 ─Rust、C、アセンブリによる実装からのアプローチ』、オライリー・ジャパン、2021年 

- 注1) 一般にはブラウザの提供するAPIを含めてJavaScriptとして扱うこともありますが、ここでは言語機能とそれを実装するエンジンに絞ります。 (本文に戻る) 

- 注2) たとえば、Node.jsやDenoに搭載されているV8エンジンは開発に役立つようにタイマー機能のないsetTimeoutを提供していますが、我々が実際に実行環境で利用するようなまともなsetTimeoutは提供していません。 

      - (本文に戻る) 

- 注3) Node.jsに存在している古いタイプのタスクベースのAPIです。これを使い、逐次的に処理を行うにはネスト形式のコールバックを作ることになります。 (本文に戻る) 

- 注4) 0秒遅延の議論はここでは重要ではないので無視し、直ちにタスクが発行されるものとします。 

      - (本文に戻る) 

# **第 3 章** 

### **Promiseとは?** 

##### **コールバック関数の問題点とPromiseの使い方** 

**従来、非同期処理にはコールバック関数が用意されていました。しかしそれには問題があります。本章ではこの問題点を明らかにし、Promiseがどのように解決するのかを解説します。Promiseの意義や使い方を押さえ、非同期処理の全貌に迫りましょう。** 小田島太郎(おだしまたろう) さくらインターネット株式会社 @shimataro999

この章では、従来のコールバック関数による非同期処理の問題点と、その問題を Promise がどのように解決するかを説明します。また、次章では実際に Promise を自作して内部構造を詳しく追っていきます。なお、本記事は筆者が「さくらのナレッジ」に寄稿した記事注1 ~~を~~ 元に作成しています。

##### **コールバック関数の問題点** 

コールバック関数の「ネストが深くなる」「インターフェースが統一されていない」という問題点は第 1 章で触れましたが、それ以外にも次のような問題があります。

- ・複数の非同期処理の待ち合わせが大変 

- ・エラー処理が面倒 

###### **複数の非同期処理の待ち合わせが大変** 

「ファイルの内容を取得する」と「特定の URL に HTTP GET リクエストを送る」のように、互いに依存していない非同期処理は並列実行してすべての処理が完了したら次に移る ...... ということができれば、アプリケーションの応答性が良くなります。しかし、これらの処理をコールバック関数で実現しようとすると、面倒な処理が必要になります。興味があれば試しに実装してみてください。制御用の変数を用意するなど、かなり複雑になってしまうと思います。

###### **エラー処理が面倒** 

非同期処理の初心者がよくやりがちなミスに、 **コールバック関数内から送出した例外を外でキャッチしようとする** というものがあります( **リスト** 1)。

###### **▼リスト1 初心者がやりがちな例外処理の使い方** 

**const fs = require("fs"); try { fs.readFile("./file.txt", (err, data) => { if (err !== null) { throw err;** **<mark>// ここで送出した例外を......</mark> } console.log(data); }); } catch (err) {** **<mark>// ここでキャッチできる?</mark> console.error(err); }**

しかし、一般的には **コールバック関数が呼ばれた時点で** try **節の処理は終わっている** ため、この方法ではキャッチできません。そのため、 JavaScript でのエラー処理は try-catch が一般的にもかかわらず、コールバック関数内では地道に if で判定していく必要があります。

##### **Promiseとは** 

Promiseとは ECMAScript 2015 ( ES2015 )で標準化された組み込みのクラスで、Future **パターン** というデザインパターンを実装したものです。この Promise を使うことで、コールバック方式の次のような問題が解決されます。

- ・ネストが深くなる問題 

- ・インターフェースの統一の問題 

- ・複数の非同期処理の待ち合わせの問題 

- ・エラー処理の問題 

Node.js v10 以降では、ファイル操作 API の Promise バージョンが使えます。 **リスト** 1で書いたファイルの読み取りを Promise バージョンで書き直すと **リスト** 2のようになります。

###### **▼リスト2 Promiseを使ったファイル読み取り** 

**const fsPromises = require("fs/promises");** **<mark>// ↑Promiseバージョンを使うには"fs"ではなく"fs/promises"</mark> fsPromises.readFile("./file.txt")** **<mark>// ↑Promiseバージョンの関数は「Promiseオブジェクト」を返す</mark> .then((data) => {** **<mark>// 正常終了したらこちら</mark> console.log(data); }) .catch((err) => {** **<mark>// エラーが発生したらこちら</mark> console.error(err); });**

くわしい動作はのちほど説明しますが、まず一見してわかるとおり、 readFile() 関数の引数にコールバック関数がありません。また、関数はPromise **オブジェクト** を返します。これは「今は値を返せないけどあとでちゃんと返すよ」と約束するオブジェクトのことです。

Promise オブジェクトは、正常終了時には then() に指定したコールバック関数が、エラー時には catch() に指定したコールバック関数が呼ばれます。これは Promise 共通のインターフェースで、 Promise オブジェクトを返す関数であればすべて同じように使えます。このように、 Promise を使うことでインターフェースが統一されるのが第一の特徴です。

##### **Promiseオブジェクトの3つの状態** 

Promise オブジェクトは、次の 3 つの内部状態を持ちます。

- ・ pending:まだ非同期処理は終わっていない(成功も失敗もしていない) 

- ・ fulfilled:非同期処理が成功した 

- ・ rejected:非同期処理が失敗した初期状態は pending で、一度 fulfilled または rejected になったら **それ以降は状態は変わらず、非同期処理の終了時に返す値もそれ以降は変わりません** 。これはとても重要ですので、しっかり理解してください。 

##### **Promise版非同期処理関数の作り方** 

Promise オブジェクトを返す非同期処理関数は **リスト** 3のように作ります。例として、コールバック版の readFile() から Promise 版を作ってみます。

- **▼リスト3 コールバック方式からPromise方式へ変換** 

**const fs = require("fs"); function readFilePromise(path) { return new Promise((resolve, reject) => {** **<mark>// Promiseのコンストラクタに関数を指定</mark> fs.readFile(path, (err, data) => {** **<mark>// 関数内で非同期処理関数を実行</mark> if (err === null) { resolve(data);** **<mark>// 成功したら結果をresolve()に渡す</mark> } else { reject(err);** **<mark>// 失敗したらエラーをreject()に渡す</mark> } }) }); }**

これで fsPromises.readFile() と同じように、ファイルの中身を Promise オブジェクトで返す関数ができました。ポイントは次の 3 点です。

- ・ Promiseのコンストラクタに引数として関数を渡す 

- ・その関数はresolve、rejectという2つの **関数** を引数に取る 

- ・非同期処理が成功したら結果をresolve()に、失敗したらエラーを reject()に渡すここでの注意事項は、非同期処理が成功したら必ず resolve() に渡さなければいけないということです。コンストラクタに渡した関数から値を返しても、その値を then() で取り出すことはできません( **リスト** 4)。 

- **▼リスト4 Promiseコンストラクタの誤った使い方** 

**function wrongPromise() { return new Promise((resolve, reject) => { return 123;** **<mark>// 間違い! resolve(123)とすること</mark> }); }**

なお、コンストラクタに渡した関数内から例外が送出されても catch() で受け取れます( **リスト** 5)。このような仕様にしている理由は、先ほどの readFilePromise() を例に取ると、コンストラクタ内で呼んでいる fs.readFile() 自体が例外を送出した場合(第 1 引数にファイル名ではなく null を指定した場合など)でもエラーを受け取れるようにしているためです。

###### **▼リスト5 例外はcatch()で受け取れる** 

**function throwingPromise() { return new Promise((resolve, reject) => { throw new Error("error");** **<mark>// ↑rejectを使わずエラーを送出</mark> }); }**

**throwingPromise() .catch((err) => {** **<mark>// ここで受け取れる</mark> console.error(err); });**

##### **非同期処理の直列実行** 

続いて、複数の非同期処理を直列に実行する場合を考えましょう。たとえば 2 つのファイルの内容を読み取って表示する場合は、 **リスト** 6のように書きます。

- **▼リスト6 Promiseを使った非同期処理の直列実行** 

**const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt")** **<mark>// 1つめのファイルを読み取る(Promiseオブジェクトを返す)</mark>**

**.then((data1) => {** **<mark>// 1つめのファイル読み取りが正常終了したらこちら</mark> console.log(data1);**

**return fsPromises.readFile("./file2.txt");** **<mark>//</mark>** <mark>★</mark> **<mark>2つめのファイルを読み取る</mark>**

**<mark>(Promiseオブジェクトを返す)</mark>**

**}) .then((data2) => {** **<mark>// 2つめのファイル読み取りが正常終了したらこちら</mark> console.log(data2); }) .catch((err) => {** **<mark>// どこかでエラーが発生したらこちら</mark> console.error(err); });**

ポイントは「★」のところで、 then() のコールバック関数が Promise オブジェクトを返しています。その中身は次の then() で取り出せるのですが、このとき **ネストが深くなっていない** のが見て取れます。「ファイル 1 を読み取ったあとでファイル 2 を読み取り、ファイル 2 に書かれている URL に HTTP リクエストを送り、その戻り値をもとに別の非同期処理を ...... 」のようにどれだけ非同期処理があっても、非同期処理の数だけ then() をつなげられるのでネストがまったく深くなりません。なお、このような then() の連鎖をPromise **チェーン** と呼びます。

##### **then()のしくみ** 

じつは、 then() は引数を 2 つ受け取れます。 1 つめの引数は先ほど説明したとおり非同期処理が成功した場合に呼び出される関数で、 2 つめの引数は非同期処理が失敗した場合に呼び出される関数です。 1 つめの引数が関数でなければidentity function(入力値をそのまま返す関数)が、 2 つめの引数が関数でなければthrower function(入力値を例外として送出する関数)が代わりに使われます。そして catch() は1 **番めの引数に** identity function **を指定した** then() **と同じ** です。さらに、then() **は新たな** Promise **オブジェクトを返します** 。具体的には次のような値です。

- ・コールバック関数がPromiseオブジェクトを返したら、そのオブジェクト 

- ・ Promiseオブジェクト以外を返したら、その値を成功値として返す(then()の1番めの関数を呼び出す)Promiseオブジェクト 

- ・コールバック関数が例外を送出したら、その値を失敗値として返す(then()の2番めの関数を呼び出す)Promiseオブジェクト 

**リスト** 6のような Promise チェーンができるのは、このように then() が新たな Promise を返すおかげです( **リスト** 7)。

- **▼リスト7 then()のくわしい仕様 const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt")** **<mark>// then()は引数を2つ受け取れる</mark> .then((data) => {console.log(data);}, (err) => {console.error(err);});** 

**fsPromises.readFile("./file1.txt")** **<mark>// これは......</mark> .then((data) => {console.log(data);}) .catch((err) => {console.log(err);});**

**fsPromises.readFile("./file1.txt")** **<mark>// ......thenの2番めの引数や1番目の引数を省略しているのと同じ</mark>**

**.then((data) => {console.log(data);}, undefined) .then(undefined, (err) => {console.log(err);});**

**fsPromises.readFile("./file1.txt")** **<mark>// 引数が関数でなければ、</mark>**

**<mark>// 内部的にはこのようにidentity/thrower functionが使われる</mark> .then((data) => {console.log(data);}, (err) => {throw err; /* thrower */}) .then((data) => {return data; /* identity */}, (err) => {console.log(err);});**

identity/thrower function は難しそうな名前ですが、実際は **リスト** 7 のとおりとても単純な関数です。

##### **エラー処理** 

**リスト** 7のコードの実行時にエラーが発生した場合、具体的には「最初の readFile() でエラーが発生した場合」「最初の then() のコールバック内で例外が発生した場合」「 2 番めの readFile() でエラーが発生した場合」「 2 番めの then() のコールバック内で例外が発生した場合」のいずれにおいても、残りの then() がスキップされて catch() のコールバックが呼ばれます( **リスト** 8)。

**▼リスト8 try-catch構文と同様に、例外が発生したらcatch()内に処理が移る const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt") .then((data1) => { console.log(data1); throw new Error("error");** **<mark>// 試しにここで例外を発生させてみる</mark> return fsPromises.readFile("./file2.txt"); }) .then((data2) => {** **<mark>// ここは実行されずに......</mark> console.log(data2); }) .catch((err) => {** **<mark>// ......ここに処理が移る</mark> console.error(err); });**

このような挙動はどこかで見たことがありませんか? そう、 trycatch です。

try-catch では、 try 節のどこで例外が発生してもそれ以降の処理を飛ばして catch 節に移ります。同じように、 Promise チェーンのどこで例外が発生してもそれ以降の処理を飛ばして catch() のコールバックに移ります( **図** 1)。なお、エラーが発生したにもかかわらず catch() で処理されていない場合は unhandledrejection イベントが発生します。意味的には送出された例外をキャッチしないのと同じですので、必ず catch() は付けてください。

##### **finally()とPromise** 

前節で、 Promise が try-catch のように使えるということを説明しました。ところで try-catch にはもう 1 つ、 finally がありますね。 Promise にも同様の finally() メソッドがあります( **リスト** 9)。

**▼リスト9 try-catch構文と同様に使えるfinally() const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt") .then((data1) => { console.log(data1); }) .catch((err) => { console.error(err); }) .finally(() => {** **<mark>// 最後に必ずここが実行される</mark> });**

finally ブロックと同様に、 finally() メソッドに渡したコールバック関数は Promise の成功・失敗にかかわらず呼ばれます。 finally() メソッドも then() や catch() 同様に Promise オブジェクトを返すので Promise チェーンができますが、コールバック関数からの戻り値にかかわらず、直前の Promise オブジェクトの成功・失敗情報を返します。そのため、 Promise チェーンが返す値は finally() があってもなくても変わりません( **リスト** 10)。

- **▼リスト10 finally()のコールバック関数が返す値はPromise全体の値には影響しない** 

- **const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt")** 

**.finally(() => {** **<mark>// ファイル読み取りの成功・失敗にかかわらずここを通る</mark> return 123; })**

**.then((data1) => {** **<mark>// finally()が返した値に関係なく、読み取り結果を取得できる</mark> console.log(data1); })**

**.catch((err) => {** **<mark>// finally()が返した値に関係なく、読み取り時にエラーが発生したらここを通る</mark> console.error(err); });**

また、コールバック関数が返す Promise オブジェクトが失敗した場合やコールバック関数が例外を送出した場合は、続く catch() メソッドで補足できます( **リスト** 11)。このあたりも try-catch-finally と同じ挙動ですね。

- **▼リスト11 finally()のコールバック関数が例外を送出したら、catch()内に処理が移る const fsPromises = require("fs/promises"); fsPromises.readFile("./file1.txt")** 

**.finally(() => {** **<mark>// ファイル読み取りの成功・失敗にかかわらずここを通る</mark> throw new Error("error!"); })**

**.then((data1) => {** **<mark>// finally()が例外を送出したのでここには来ない</mark> console.log(data1);**

**}) .catch((err) => {** **<mark>// finally()が送出した例外はここで捕捉できる</mark> console.error(err); });**

##### **複数の非同期処理の待ち合わせ** 

さて、 **リスト** 6の例では、そもそも file1.txt と file2.txt の読み取りは互いに依存関係がないので並列実行できそうですね。このような場合も、 Promise を使えば簡単に実現できます( **リスト** 12)。

**▼リスト12 複数のPromiseの並列実行と待ち合わせ const fsPromises = require("fs/promises"); Promise.all([fsPromises.readFile("./file1.txt"), fsPromises.readFile("./file2.txt")])** **<mark>// ↑2つのPromiseオブジェクトを並列実行</mark> .then(([data1, data2]) => {** **<mark>// thenのコールバックに2つの結果が渡される</mark> console.log(data1); console.log(data2); }) .catch((err) => { console.error(err); });**

Promise.all() という関数に複数の Promise オブジェクトを配列で渡すと、 **すべての結果** が取得できた時点で then() のコールバック関数が呼ばれます( **図** 2)。この Promise.all() 自体も Promise オブジェクトを返す関数です。ここで注意してほしいのは、複数の Promise オブジェクトのどれか 1 つでもエラーが発生したら catch() に処理が移ってしまうということです。 100 個の Promise オブジェクトを並列実行したとして、仮に 99 個が正常に終了しても、最後の 1 個でエラーが発生したら Promise.all() は catch() 内のコールバックが実行されます。また、どの Promise オブジェクトでエラーが発生したのかを知るすべもありません( **図** 3)。

##### **コールバックとの使い分け** 

ここまで、 Promise の意義や使い方を説明しました。 Promise だけに(?)いくつか “ 約束事 ” があるので、勘所を押さえるまではなかなかとっつきにくいかもしれませんが、一度理解してしまえば非同期処理の強力な味方となってくれることがご理解いただけるでしょうか。それでは、 Promise さえあればコールバック関数は完全に不要になるのでしょうか?

答えは No です。基本的には Promise で問題ありませんが、イベントハンドラのように複数回実行する可能性のある( 1 回も実行しない可能性もある)処理は Promise では対応できないため、これまでとおりコールバック処理が必要です。

##### **おわりに** 

本章のまとめは次のとおりです。

- **・コールバック関数の問題点** 

- ・インターフェースが統一されていない 

- ・非同期処理が複数あるとネストが深くなる 

- **・** Promise **の利点** 

- ・統一されたインターフェース 

- ・非同期処理が複数あってもネストが2段以上にならない 

- **・それでもまだコールバック関数の出番はある** 

次章では、実際に Promise を自作しながら内部の動作をくわしく追っていきます。

# **第 4 章** 

### **Promiseを深掘りする** 

##### **「オレオレPromise」を実装して構造を理解しよう** 

**本章ではPromiseの構造を理解するために、Promiseを自作してみます。自作Promise の全体像を押さえたあとに、コンストラクタ、then()、Promiseチェーン、複数の非同期処理の待ち合わせ、finally()と実装を進めます。一連の実装を体感することで、よりしくみを理解することができるでしょう。**

小田島太郎(おだしまたろう) さくらインターネット株式会社 @shimataro999

いよいよ Promise の自作に入ります。ただし、目的はあくまで Promise の内部構造の理解なので、 ES2015 に規定されている Promise の完全な実装は目指しません。実際に作るのは雰囲気がつかめる程度の「なんちゃって Promise 」ですので、未実装のメソッドや規格に厳密に従っていないメソッド、冗⻑な記述、非効率な記述などがあるのはご了承ください。クラス名は Yakusoku とします。なお、本記事は筆者が「さくらのナレッジ」に寄稿した記事注1 ~~を~~ 元に作成しています。

##### **自作するPromiseの全体像** 

まずは全体像です( **リスト** 1)。コンストラクタと then() はこれから作っていきますが、 then() を使って catch() を実装できます。

**▼リスト1 コンストラクタ、then()、catch()**

**class Yakusoku { constructor(executor) {**

**this.state = "pending";** **<mark>// ↑内部状態; pending / fulfilled / rejected</mark> this.resolvedValue = null;** **<mark>// 成功時の値を保持</mark> this.rejectedValue = null;** **<mark>// 失敗時の値を保持</mark>**

**<mark>// TODO: 実装</mark> }**

**then(onFulfilled, onRejected) {** **<mark>// TODO: 実装</mark> }**

**catch(onRejected) { return this.then(undefined, onRejected);** **<mark>// ↑1番めの引数をidentity functionにする</mark> } }**

2 つのプロパティ resolvedValue と rejectedValue は、あとで値を取り出すときに使います。内部状態は一度確定したらそれ以降変化しないため、実際にはこれらのプロパティはどちらか 1 つしか使われません。そのため 1 つのプロパティでカバーできるのですが、 1 つのプロパティに複数の意味があるとややこしくなるので、わかりやすさを優先して 2 つに分けています。第 3 章で「 catch() は1 **番めの引数に** identity function **を指定した** then() **と同じ** 」と書いたとおり、 catch() の実装は難しくありません。また、 then() があれば finally() も実装できますが、これは catch() ほど単純ではないのでのちほど実装します。

##### **コンストラクタ、resolveとrejectの役割** 

次にコンストラクタを実装していきます。第 3 章で説明したとおり、コンストラクタは関数を引数として受け取ります( **リスト** 2、コンストラクタ部分のみ記載)。

###### **▼リスト2 コンストラクタの実装(コンストラクタ部分のみ記載)** 

**constructor(executor) { this.state = "pending";** **<mark>// ↑内部状態; pending / fulfilled / rejected</mark> this.resolvedValue = null;** **<mark>// 成功時の値を保持</mark> this.rejectedValue = null;** **<mark>// 失敗時の値を保持 // 成功時に呼ばれる関数</mark> const resolve = (resolvedValue) => { if (this.state !== "pending") { return;** **<mark>// 内部状態の変更は一度だけ</mark> } this.state = "fulfilled"; this.resolvedValue = resolvedValue; };** **<mark>// 失敗時に呼ばれる関数</mark> const reject = (rejectedValue) => { if (this.state !== "pending") { return;** **<mark>// 内部状態の変更は一度だけ</mark> } this.state = "rejected"; this.rejectedValue = rejectedValue; }; try { executor(resolve, reject); } catch (err) {** **<mark>// 例外が発生したらrejectedにする</mark> reject(err); } }**

resolve および reject は、それぞれ成功時・失敗時にコールバック関数から呼ばれる関数です。呼ばれたら状態を変更して値を確定させています。ただし、前章で「一度 fulfilled または rejected になったらそれ以降は状態は変わらない」と説明したとおり、すでに pending 状態でなければ何もせずに終了しています。

##### **then()の実装** 

いよいよ非同期処理の結果を取り出す段階です。 Promise オブジェクトのキモである then() を実装します。前章で説明したとおり then() は 2 つの関数を引数に取り、 Promise の状態が fulfilled になったら 1 番めの関数が、 rejected になったら 2 番めの関数が実行されます。実装は **リスト** 3のような感じでしょうか( then() 部分のみ掲載します)。

**▼リスト3 then()の実装(不完全版)(then()部分のみ記載) then(onFulfilled, onRejected) { if (typeof onFulfilled !== "function") { onFulfilled = (value) => {** **<mark>// identity function</mark> return value;** **<mark>// 入力値をそのまま返すだけ</mark> }; } if (typeof onRejected !== "function") { onRejected = (value) => {** **<mark>// thrower function</mark> throw value;** **<mark>// 入力値を例外として送出</mark> }; } if (this.state === "fulfilled") { onFulfilled(this.resolvedValue); } if (this.state === "rejected") { onRejected(this.rejectedValue);**

**} }**

なお、 onFulfilled() と onRejected() の呼び出し周りのインデントが不自然ですが、以降の修正で diff を取るときに見づらくしないためのもので、誤植や調整ミスではありません。ここまでに実装した Yakusoku クラスを使って、 readFileYakusoku() を作ってみましょう( **リスト** 4)。これを実行すると ...... 、あれ? **何も表示されずに終了してしまいます** 。どこがおかしいのでしょうか。

**▼リスト4 この段階のYakusokuクラスで非同期処理を実装 const fs = require("fs"); function readFileYakusoku(path) { return new Yakusoku((resolve, reject) => {** **<mark>// Yakusokuのコンストラクタに関数を</mark>** **<u><mark>指定</mark></u> fs.readFile(path, (err, data) => {** **<mark>// 関数内で非同期処理関数を実行</mark> if (err === null) { resolve(data);** **<mark>// 成功したら結果をresolve()に渡す</mark> } else { reject(err);** **<mark>// 失敗したらエラーをreject()に渡す</mark> } }) }); } readFileYakusoku("./file.txt")** **<mark>// 実行してみる</mark> .then((data) => {** **<mark>// 正常終了したらこちら</mark> console.log(data); }, (err) => {** **<mark>// エラーが発生したらこちら</mark> console.error(err); });**

**then()を呼び出した時点でまだpending状態だった場合**

**リスト** 4の実装の then() メソッドをよく見るとわかりますが、この実装では then() を **呼び出した時点** で状態が fulfilled か rejected でないと渡した関数が一切実行されません。つまり、 then() を呼び出した時点で pending だった場合、 **状態が変わった時点で** 関数を実行するように書き換える必要があります。渡された関数を記録しておいて、状態が変わったときに実行しましょう。 **リスト** 4のコードからの差分( unified diff 形式)は **リスト** 5のとおりです。

###### **▼リスト5 状態が変わったときにコールバック関数が実行されるように変更** 

**@@ -3,6 +3,7 @@ this.state = "pending";** **<mark>// ↑内部状態; pending / fulfilled / rejected</mark> this.resolvedValue = null;** **<mark>// 成功時の値を保持</mark> this.rejectedValue = null;** **<mark>// 失敗時の値を保持</mark> +    this.thenFunctions = [];** **<mark>//</mark>** <mark>1</mark> **<mark>then()に渡された関数を保持</mark>**

**<mark>// 成功時に呼ばれる関数</mark> const resolve = (resolvedValue) => { @@ -11,6 +12,11 @@ } this.state = "fulfilled"; this.resolvedValue = resolvedValue; + +** **<mark>//</mark>** <mark>2</mark> **<mark>then()に渡された関数をすべて実行</mark> +      for (const thenFunction of this.thenFunctions) { +        thenFunction.onFulfilled(resolvedValue); +      } };** **<mark>// 失敗時に呼ばれる関数</mark> const reject = (rejectedValue) => { @@ -19,6 +25,11 @@ } this.state = "rejected"; this.rejectedValue = rejectedValue;**

<!-- Start of picture text --> +<br>+       //  3 then()に渡された関数をすべて実行<br>+      for (const thenFunction of this.thenFunctions) {<br>+        thenFunction.onRejected(rejectedValue);<br>+      }<br>      };<br>      try {<br> @@ -42,6 +53,10 @@<br>        };<br>      }<br>+     //  4 pendingならあとで呼び出すので関数を記録しておく<br>+    if (this.state === "pending") {<br>+      this.thenFunctions.push({ onFulfilled, onRejected });<br>+    }<br>        if (this.state === "fulfilled") {<br>          onFulfilled(this.resolvedValue);<br>        }<br><!-- End of picture text -->

変更が 4 箇所(1〜4)あります。1〜3はコンストラクタ、4は then() の処理です。

1では、コンストラクタ内で then() に渡されたコールバック関数を保持する配列を定義しています。4では、その then() 内で Yakusoku オブジェクトの状態が "pending" ならコールバック関数をあとで呼び出すために1の配列に保存しています。2と3では、 resolve() 、 reject() 内で非同期処理が成功/失敗したときに、4で保存したコールバック関数を実行しています。これで、時間がかかる処理でも状態が変更された時点で関数が実行されるようになりました。そして、存在しないファイルや読み取り権限のないファイルを読み出そうとすると、 then() の 2 番めの関数が実行されるのがわかると思います。この実装を見てわかるとおり、 then() メソッドを複数回呼ぶと、状態が fulfilled になったときに渡した関数がすべて実行されます。たとえば次のようなコードを書くと、ファイルの内容が 2 回コンソールに出力されます。

**const p = readFileYakusoku("./file.txt"); p.then((data) => console.log(data)); p.then((data) => console.log(data));**

これで Promise の大部分を実装しました。しかし、まだ完成ではありません。次節で最も重要な部分を実装します。

##### **Promiseチェーンの実装** 

Promise で最も重要な部分は、前章で説明したPromise **チェーン** です。 Promise チェーンにより、非同期処理を直列に実行してもネストが深くならないことは説明したとおりです。

Promise チェーンを実現するには、 then() や catch() が新たな Promise オブジェクトを返す必要があります。具体的には次のような挙動です。

- ・引数に渡した関数の戻り値がPromiseオブジェクトの場合は、そのオブジェクトをそのまま返す 

- ・そうでなければ、戻り値をPromiseで包んで返すこれを実装するために、 **リスト** 6のようなヘルパー関数を作ります。 

**▼リスト6 2つのヘルパー関数 function isThenable(value) { if (value === null || value === undefined) { return false;**

**} return typeof value.then === "function"; }**

**function wrapWithYakusoku(value) { if (isThenable(value)) { return value; } return new Yakusoku((resolve) => { resolve(value); }); }**

thenableという概念が出てきました。難しそうに見えますが、単に「 then できる」、つまり「 then() メソッドを持っている」という意味で、「 Promise オブジェクトである」とほぼ同義です。 wrapWithYakusoku() では、与えられた引数が thenable ( ≒Promise オブジェクト)なら引数をそのまま返し、そうでなければ Yakusoku オブジェクトで包んで返します。これらのヘルパー関数を使って Yakusoku クラスを **リスト** 7のように書き直します( isThenable() と wrapWithYakusoku() の中身は先ほどと同じなので、誌面の都合上ここでは省略します)。

###### **▼リスト7 Promiseチェーンに対応** 

**@@ -15,7 +15,22 @@** **<mark>//</mark>** <mark>2</mark> **<mark>then()に渡された関数をすべて実行</mark> for (const thenFunction of this.thenFunctions) {** **<mark>-        thenFunction.onFulfilled(resolvedValue);</mark> +** **<mark>// 5実行結果に対してresolve()、reject()を呼び出す</mark> +        try { +          const ret = thenFunction.onFulfilled(resolvedValue); +          if (isThenable(ret)) { +** **<mark>// 戻り値がthenableならthen()に渡す</mark> +            ret.then(thenFunction.resolve, thenFunction.reject); +          }**

<!-- Start of picture text --> +          else {<br>+             // Yakusokuオブジェクトでなければfulfilled<br>+            thenFunction.resolve(ret);<br>+          }<br>+        }<br>+        catch (err) {<br>+           // 例外が発生したらrejected<br>+          thenFunction.reject(err);<br>+        }<br>        }<br>      };<br>// 失敗時に呼ばれる関数<br> @@ -28,7 +43,22 @@<br>//  3 then()に渡された関数をすべて実行<br>        for (const thenFunction of this.thenFunctions) {<br>-        thenFunction.onRejected(rejectedValue);<br>+         // 6実行結果に対してresolve()、reject()を呼び出す<br>+        try {<br>+          const ret = thenFunction.onRejected(rejectedValue);<br>+          if (isThenable(ret)) {<br>+             // 戻り値がYakusokuオブジェクトならthen()に渡す<br>+            ret.then(thenFunction.resolve, thenFunction.reject);<br>+          }<br>+          else {<br>+             // Yakusokuオブジェクトでなければfulfilled<br>+            thenFunction.resolve(ret);<br>+          }<br>+        }<br>+        catch (err) {<br>+           // 例外が発生したらrejected<br>+          thenFunction.reject(err);<br>+        }<br>        }<br>      };<br> @@ -55,17 +85,33 @@<br>//  4 pendingなら後で呼び出すので関数を記録しておく<br>      if (this.state === "pending") {<br><!-- End of picture text -->

<!-- Start of picture text --> -      this.thenFunctions.push({ onFulfilled, onRejected });<br>+       //  7 then()からYakusokuオブジェクトを返すため、<br>+       // 新しいYakusokuオブジェクトを作ってresolve、rejectもあとで呼び出すため<br>に記録<br>+      return new Yakusoku((resolve, reject) => {<br>+        this.thenFunctions.push({ onFulfilled, onRejected, resolve, reject });<br>+      });<br>      }<br>+<br>+    try {<br>        if (this.state === "fulfilled") {<br>-        onFulfilled(this.resolvedValue);<br>+         //  9 then()からYakusokuオブジェクトを返すため、<br>+         // onFulfilled()の結果をYakusokuオブジェクトでラップ<br>+        return wrapWithYakusoku(onFulfilled(this.resolvedValue));<br>        }<br>        if (this.state === "rejected") {<br>-        onRejected(this.rejectedValue);<br>+         //  10 then()からYakusokuオブジェクトを返すため、<br>+         // onRejected()の結果をYakusokuオブジェクトでラップ<br>+        return wrapWithYakusoku(onRejected(this.rejectedValue));<br>        }<br>+    }<br>+    catch (err) {<br>+       //  8 onFulfilled()、onRejected()内で送出された例外を捕捉し、<br>+       // 「失敗するYakusokuオブジェクト」として返す<br>+      return new Yakusoku((resolve, reject) => {<br>+        reject(err);<br>+      })<br>+    }<br>    }<br>    catch(onRejected) {<br>      return this.then(undefined, onRejected);  // 1番目の引数をidentity functionにす<br>る<br>    }<br>  }<br><!-- End of picture text -->

**+**

**+** **<mark>// 以下にisThenable関数とwrapWithYakusoku関数を追加(掲載は省略)</mark>**

少し複雑になりましたが、今回は変更が 6 箇所(5〜10)あります。

7910は then() の中の処理で、いずれも Promise チェーンを実現するために Yakusoku オブジェクトを返すように変更しています。7では新たな Yakusoku オブジェクトを作り、コンストラクタのコールバック関数に渡された resolve 、 reject も後で呼び出すために記録しています。8では onFulfilled() 、 onRejected() 内で発生した例外を try-catch で捕捉し、 Yakusoku オブジェクトとして返しています。56では、もともとは単に保存した onFulfilled() 、 onRejected() を呼び出すだけでしたが、今回は呼び出し結果に応じて7で保存した resolve 、 reject を呼び出しています。これで、 Promise チェーンならぬYakusoku **チェーン** もできるようになりました。

##### **複数の非同期処理の待ち合わせ** 

続いて、複数の非同期処理の待ち合わせを実装してみます。待ち合わせのメソッドは Promise.all() 、 Promise.race() などいくつかありますが、ここではすでに前章で説明しており使う機会も多い Promise.all() のみ実装します。

**リスト** 8の実装は複雑に見えますが、「すべて成功する or 1 つでも失敗するまで待つ」「 then() に渡される値の順番は Yakusoku オブジェクトを渡した順番と同じ」の 2 点に注目しながら流れを追っていけばそこまで難しくはないと思います。

**▼リスト8 待ち合わせ関数Promise.all()を実装**

**class Yakusoku { static all(yakusokuObjects) { return new Yakusoku((resolve, reject) => { const resolvedValues = [];** **<mark>// 結果を格納する配列</mark> let fulfilledCounter = 0;** **<mark>// fulfilledになった数のカウンター</mark> for (const [i, y] of yakusokuObjects.entries()) { y.then((value) => { resolvedValues[i] = value; fulfilledCounter++; if (fulfilledCounter >= yakusokuObjects.length) {** **<mark>// すべて解決</mark> resolve(resolvedValues); } }, reject);** **<mark>// 1つでもrejectedになったら全体をrejectedにする</mark> } }); } }**

##### **finally()の実装** 

最後の仕上げは、冒頭で「あとで実装する」と述べた finally() です。このメソッドの実装を最後に持ってきたのは、「 finally() だけに最後」と言いたかっただけ ...... ではなく、先ほどの

wrapWithYakusoku() ヘルパー関数が必要だったからです。 **リスト** 9の実装も少々複雑ですが、前章で説明した次の内容を頭に入れておけば理解できるのではないでしょうか。

- ・コールバック関数からの戻り値にかかわらず、直前のYakusoku オブジェクトの成功・失敗情報を返す 

- ・コールバック関数が返すYakusokuオブジェクトが失敗した場合やコールバック関数が例外を送出した場合は、続くcatch()メソッ 

###### ドで捕捉できるようにする 

**▼リスト9 finally()を実装 finally(onFinally) { return this.then( (value) => { return wrapWithYakusoku(onFinally()) .then(() => { return value;** **<mark>// onFinally()を呼び出す前に渡された値を返す</mark> }); }, (err) => { return wrapWithYakusoku(onFinally()) .then(() => { throw err;** **<mark>// onFinally()を呼び出す前に発生したエラーを例外として再送出す</mark> る }); } ); }**

##### **おわりに** 

駆け足で Promise を実際に実装しながら内部の動きを見てきました。普段 Promise を使っている方も、内部の動きを追っていくと新たな発見があるのではないでしょうか。説明が足りない部分などもあるかもしれませんが、消化不良の部分はぜひ筆者までご連絡ください。次章では async/await について説明します。 async/await を使うと、非同期処理をさらに簡潔に記述できるようになります。お楽しみに。

**第 5 章**

### **async/awaitを深掘りする** 

##### **Promiseと比べてわかる挙動の違い** 

**本章では、いよいよ非同期処理の大本命、async/awaitについて説明します。 async/awaitの使い方と、それによって非同期処理がどのように簡潔に書けるようになるかを見ていきましょう。また、これまで見てきた3種類の非同期処理の記述方法をどのように使い分ければ良いいのかについても紹介します。**

小田島太郎(おだしまたろう) さくらインターネット株式会社 @shimataro999

##### **async/awaitについて** 

async/await を使うと、 Promise オブジェクトからの値の取り出しや例外処理をとても簡単に行えます。指定の URL からリソースを取得できるfetch APIを使って URL にアクセスする例を、 Promise 版( **リスト** 1)と async/await 版( **リスト** 2)で見比べてみましょう。

###### **▼リスト1 fetch APIの使用例(Promise版)** 

**<mark>// fetch()はPromiseを返す</mark> fetch("https://example.com") .then((res) => { console.log(res);** **<mark>// 取得結果が表示される</mark> }) .catch((err) => { console.error(err);** **<mark>// 取得に失敗したらエラーが出力される</mark> });**

###### **▼リスト2 fetch APIの使用例(async/await版)** 

**<mark>// async/awaitに置き換えてみると......</mark> (async() => {** **<mark>// おまじない</mark> try {**

**const res = await fetch("https://example.com"); console.log(res);** **<mark>// 取得結果が表示される</mark>**

**}**

**catch (err) {**

**console.error(err);** **<mark>// 取得に失敗したらエラーが出力される</mark>**

**} })();**

ポイントは「 async キーワードを付けた関数の中で」「 Promise オブジェクトに await をつける」という 2 点です。それだけで Promise オブジェクトの中身を取り出せて、さらに try-catch でエラー処理できるようになりました。同期処理と同じような感覚で書けるので、とても簡単ですね! 即時実行関数式の「おまじない」についてはのちほど await の項で説明します。

###### **async** 

まずは async の説明です。 async キーワードを付けて定義した関数には **必ず** Promise **オブジェクトを返す** という特徴があります( **リスト** 3)。

- **▼リスト3 asyncキーワードを付けた関数は必ずPromiseオブジェクトを返す1 async function asyncFunction() {** **<mark>// asyncキーワードをつけると......</mark> return 0;** **<mark>// 通常の値を返しても......</mark> }** 

**asyncFunction() .then((data) => {** **<mark>// ......必ずPromiseオブジェクトに変換される</mark> console.log(data);**

**});**

一方、 Promise オブジェクトを返した場合は、元の Promise オブジェクトがそのまま返ります注1 ~~(~~ **リスト** 4)。なお、 TypeScript で記述する場合は戻り値の型を Promise で囲ってください( **リスト** 5)。これだけでは便利さがイマイチわからないかもしれませんが、次項の await と組み合わせることで真価を発揮します。

- **▼リスト4 asyncキーワードを付けた関数は必ずPromiseオブジェクトを返す2 async function asyncFunction() { return fetch("https://example.com");** **<mark>// Promiseオブジェクトを返すと......</mark> }** 

###### **asyncFunction()** 

**.then((data) => {** **<mark>// ......そのままPromiseオブジェクトとして扱える</mark>**

**console.log(data);**

**});**

###### **▼リスト5 TypeScriptによるasync関数の型定義例** 

- **async function asyncFunction(): Promise<number> { return 0;** 

**}**

###### **await** 

async キーワード付きで定義した関数内では await というキーワードを使えるようになります。逆に、 async キーワードを使わずに定義した関数内では await を使えません。先ほどの「おまじない」の理由はこれです。通常は async キーワードで定義した関数内でしか await を使えないことに注意してください。

await の後に Promise オブジェクトを指定すると、その Promise オブジェクトが成功または失敗するまで処理を待ちます。そして Promise オブジェクトが成功したら await 元に処理が移り、 Promise オブジェクトの結果を返します。ただし、「待つ」と言ってもプログラム全体の実行が停止する(ブロックされる)わけではなく、ほかに処理すべき関数がタスクキューにあればそれが実行されます。たとえば **リスト** 6の例を見てみましょう。このプログラムを実行すると、ほとんどの場合注2 ~~は~~ 2→1→3 の順で表示されます。つまり、 fetch() の結果を await で待っている間に、 setTimeout() により 5 ミリ秒後にコールバック関数がキューに登録・実行されています。

###### **▼リスト6 awaitで待機中もプログラム全体がブロックされているわけではないことを示す例** 

**(async() => { setTimeout(() => {** **<mark>// 5ミリ秒後にコールバック関数をタスクキューに追加</mark> console.log(1); }, 5); console.log(2); await fetch("https://example.com"); console.log(3); })();**

この挙動から、プログラム全体が停止しているわけではないということがご理解いただけたでしょうか。もしプログラム全体が停止しているなら、コールバック関数も実行されないはずです。また、 Promise オブジェクトに await をつけると、Promise **オブジェクトが失敗した際に例外が送出される** ようになります。そのため Promise オブジェクトの失敗を try-catch で捕捉できます。もちろん Promise オブジェクト以外が送出した通常の例外も、これまでどおり try-catch で捕捉できます。そのため、本章の最初に書いた **リスト** 1のような簡潔なコードで Promise を扱えます。この await を使えば、 Promise チェーンを使わなくても非同期処理を **リスト** 7のように簡単に直列化できますね。

**▼リスト7 Promiseチェーンをasync/awaitで実現**

**<mark>// Promise版</mark> fetch("https://example.com")** **<mark>// URLにアクセスして......</mark> .then((res) => { return res.text();** **<mark>// UTF-8の文字列に変換して......</mark>**

**}) .then((text) => { console.log(text);** **<mark>// ......出力</mark> }) .catch((err) => { console.error(err);** **<mark>// 取得に失敗したらエラーが出力される</mark> });** **<mark>// async/await版</mark> (async() => { try { const res = await fetch("https://example.com"); const text = await res.text();** **<mark>// UTF-8の文字列に変換して......</mark> console.log(text);** **<mark>// ......出力</mark> } catch (err) { console.error(err);** **<mark>// 取得に失敗したらエラーが出力される</mark> } })();**

なお、 await の後に Promise オブジェクトでない値を指定した場合は値がそのまま返されます( **リスト** 8)。一見何の意味もないように見えますが、内部的にはマイクロタスクキューの調整が行われるので完全に無意味というわけではありません。本章ではこれ以上深掘りしませんが、興味のある方は調べてみてください。

###### **▼リスト8 Promiseオブジェクト以外にawaitを適用しても結果は変わらない** 

**(async() => { const data = await 0; console.log(data);** **<mark>// 0が出力される</mark> })();**

##### **Column 「async/await」の読み方** 

“async”とは“asynchronous”(非同期)の略、“await”は「待つ」という意味の英単語です。asynchronousは「 **えい** しんくろなす」と読むので、“async”も正確には「えいしんく」なのでしょうが、「あしんく」と読むネイティブも多いようです。また、「待つ」という意味では“wait”という単語を思い浮かべる方が多いと思いますが、「〜を待つ」という他動詞的な用法では“await”を使うことが多いようです。結果として、語呂のいい「あしんく/あうぇいと」という用語ができあがりました。

##### **async/awaitを使う際の注意** 

このように便利な await ですが、使用の際にはいくつか注意事項があります。

###### **awaitは単なるPromiseの糖衣構文ではない** 

Promise 版と async/await 版のソースコードを比較すると、 await は単に Promise オブジェクトの then() メソッドを使わなくて済むようにするだけの構文で、中でやっていることは同じように見えるかもしれません。しかし、同じように見えても内部のしくみはまったく異なります。たとえば **リスト** 9のようなコードを見てみましょう。どちらも https://example.com/1.html 〜 https://example.com/10.html を読み取って出力するコードですが、 Promise 版は並列に、 async/await は直列に実行されます。

###### **▼リスト9 Promiseとasync/awaitの挙動の違い** 

**<mark>// Promise版</mark> for(let i = 1; i <= 10; i++) { fetch(`https://example.com/${i}.html`) .then((res) => { console.log(res); }); }**

**<mark>// async/await版</mark> (async() => {**

**for(let i = 1; i <= 10; i++) { const res = await fetch(`https://example.com/${i}.html`); console.log(res); } })();**

言い換えると、 Promise 版は “1.html” の **読み取り結果を待たずに** “2.html” を読み取り始め、先に読み取ったものからコールバック関数が実行されます。それに対して async/await 版は、 “1.html” を読み取り終わるまで待ち、読み取ったら内容を画面に出力して “2.html” の読み取りを始めます。これは、次のような違いとして表れます。

- ・ Promise版は結果の出力順序が保証されないが、async/await版では必ず“1.html”、“2.html”、......の順で結果が表示される。たとえば“1.html”の読み取りに10秒、“2.html”の読み取りに0.1秒のように読み取り時間に大きな違いがある場合は、Promise版では“2.html”を先に読み取り終わってこちらの結果が先に出力される可能性が高くなる 

- ・ Promise版は読み取りが並列に実行されるため、(ネットワーク帯域幅などによっては)すべての処理が終わるまでの時間が async/awaitより短くなる可能性がある 

- ・ Promise版は読み取りが並列に実行されるため、常に1つのリソースしか読み取らないasync/awaitに比べてネットワーク帯域幅を消費する。また、今回の例のように複数の読み取り先が同一サービスの場合は読み取り先により負荷がかかる 

###### **thenableなオブジェクトであれば値を取り出せる** 

await が値を取り出せるのは Promise オブジェクトだけではありません。前章で説明した “thenable” 、つまり then() メソッドが実装されているオブジェクトであれば何でも取り出せます。つまり、 Yakusoku オブジェクトからも値を取り出せます(第 4 章参照)!

ただし注意点として、「 then という名前だが、非同期処理とはまったく関係ないメソッド」を持つオブジェクトに await を適用した場合は、 then() が予期せぬ形で呼ばれて誤動作したり、 await が値を取り出せずにフリーズしたように見えたりすることが挙げられます。そのため、 JavaScript でコーディングする際には自作のクラスには then という名前のメソッドは作らないほうがよいでしょう。

###### **async関数外でawaitを使う方法** 

await の項では「通常は async キーワードで定義した関数内でしか await を使えないことに注意してください。」と説明しましたが、「通常は」という表現が気になった方もいるかもしれません。通常があるなら通常でない場合もあるはずです。ここでの「通常でない場合」とは関数の外、つまりソースコードのトップレベルで await を使う場合です。実は、 **リスト** 10のような書き方もできるので、本章で今まで使ってきた「おまじない」は必ずしも必要ありません。

- **▼リスト10 関数の外なら例外的にawaitを使える(トップレベルawait)** 

**try {** **<mark>// async関数を使わず、トップレベルでawaitを使う</mark> const res = await fetch("https://example.com"); console.log(res);** **<mark>// 取得結果が表示される</mark> } catch (err) { console.error(err);** **<mark>// 取得に失敗したらエラーが出力される</mark> }**

ただし Node.js では、 CommonJS モードではなくES Modules **モードでないとトップレベル** await **を使えない** ことに注意してください。言い換えると、トップレベル await を使うにはモジュールのロードに require() ではなく import 構文を使う必要があります。本章では今まで、どちらのモードでも動くようにトップレベル await ではなく「おまじない」を使ってきました。

##### **3種類の非同期処理の使い分け** 

async/await を使えば非同期処理をとても簡潔に記述できることを説明しました。それでは、もはや Promise やコールバック関数を覚える必要はないのでしょうか?

答えは No です。基本的には async/await を使えば問題ないのですが、そもそもの話としてこの async/await は Promise オブジェクトを使いやすくする構文ですので、 Promise についての理解は必須です。とくに、 Promise を返す非同期処理関数を自分で実装する場合は、 Promise を理解していないと話になりません。加えて、複数の非同期処理を並列実行する場合は async/await だけでは対応できず、 Promise.all() のような待ち合わせ関数が必要です。また、前章の Promise での説明と同様に、複数回呼ばれる可能性のある処理にはコールバック関数が必要です。今回説明を省きましたが、 **非同期ジェネレーター** と **for await of** を使えば理論的にはコールバック関数を使わずに記述できます。しかし、キー入力イベントやシグナルの扱い( SIGTERM や SIGINT など)のような「あくまでサブ的な処理であり、他にメインの処理がある」「頻繁には呼ばれず、せいぜい終了時に 1 回呼ばれる程度」「イベントが発生するタイミングが局所的ではなく、プログラム実行中のいろいろなタイミングで発生する可能性がある」という場合は従来どおりコールバック関数を使ったほうが扱いやすいことが多いです。

##### **おわりに** 

###### 本章のまとめは次のとおりです。 

- ・ async/awaitの利点 

   - ・非同期処理を同期処理とほとんど変わらない文法で簡潔に記述できる 

- ・ async/awaitの注意点 

   - ・単なるPromiseの糖衣構文ではない 

   - ・ Node.jsでソースコードのトップレベルでawaitを使う場合は ES Modulesモードでの動作が必要 

- ・コールバックやPromiseとの使い分け 

   - ・基本的には非同期処理はasync/awaitでOK 

   - ・ただし、複数の非同期処理の並列処理ではPromise.all()のような待ち合わせが必要 

   - ・イベントのように複数回呼ばれる可能性のある処理ではコールバック関数が有効な場合も多い 

- 注1) 厳密には元のオブジェクトの参照を返すわけではないのですが、そのあたりの細かい話はここでは解説しません。 

      - (本文に戻る) 

- 注2) 例外はfetch()が5ミリ秒未満で終わるという超特殊な状況です。 (本文に戻る) 

# **第 6 章** 

### **AbortSignalとAsyncLocalStorage 本格的な非同期処理で求められる機能** 

**前章までに解説したコールバック関数、Promise、async/awaitは非同期処理の基本です。実際の開発ではこれ以外にも多くの機能が必要になります。その中から2つ、非同期処理の中止を実現するAbortSignalと、一連の非同期処理の中でデータの共有を実現するAsyncLocalStorageを紹介します。**

うひょ(鈴木僚太(すずきりょうた)) @uhyo_ http://uhy.ooo/ uhyo@uhy.ooo uhyo

##### **JavaScriptの非同期処理を支える2つの追加機能** 

この特集では、これまで Promise や async/await といった、 JavaScript の非同期処理を支える機能について解説してきました。これらは実務でも毎日使うような実用的な機能であり、しっかりと理解しておくことはとても重要です。しかし、 Promise および async/await だけでは、普段の開発に必要な道具がすべてそろったとは言えません。そこで、この章では非同期処理にまつわる実装でよく使われる機能を 2 つ追加で紹介します。

##### **非同期処理の中断をつかさどるAbortSignal** 

JavaScript の Promise に欠けているものの 1 つが、「中断」の概念です。非同期処理というのは一瞬で終わるのではなく、終了するまで時間がかかるのが普通です。そのため、結果が出るまでの間に気が変わって処理をやめたくなることもあるでしょう。しかし、非同期処理の結果を表す Promise オブジェクトに対してできることは、結果を取得することだけです。その Promise が担当している処理を中断するといったインターフェースは用意されていません。そこで JavaScript では、非同期処理の中断を制御するために AbortSignalというオブジェクトを用います。 AbortSignal は Promise とは異なり、 JavaScript 本体の言語仕様には含まれていません。代わりに、 DOM 仕様の一部として定義されています。 AbortSignal はもともとブラウザ上の JavaScript で使うために作られていましたが、非同期処理の中断というのは DOM 以外においても有用な概念です。そのため、ブラウザ以外の Node.js や Deno 、 Cloudflare Workers といった実行環境でも AbortSignal が使えるようになっています。ちなみに、このようにもともと Web 用に定義された API をブラウザ以外でも広く利用していこうという動きをとる WinterCG というグループがあり、 AbortSignal 以外にも多くの Web API が WinterCG からサポートされています。

###### **AbortSignalの基本的な使い方** 

AbortSignal は、その名のとおり、非同期処理の中止を知らせる役割を持つオブジェクトです。中止する可能性がある非同期処理を発生させる際にオプションとして AbortSignal を渡すことで、その AbortSignal を通じて中止を知らせることができます。

#### **AbortSignalの作成と使用** 

AbortSignal の基本的な作り方は、AbortControllerを介して作成することです。作成された AbortController オブジェクトの signal プロパティとして AbortSignal オブジェクトを得られます。

**リスト** 1の例から察せられるように、 AbortController は AbortSignal とセットで作成されます。 AbortController の側で中止の操作

( abort() )をすることで、対応する AbortSignal にそれが伝わるというしくみになっています。

###### **▼リスト1 AbortSignalの作り方と使い方** 

**<mark>// AbortControllerを作成</mark>**

**const controller = new AbortController();** **<mark>// 対応するAbortSignalを取得</mark> const signal = controller.signal;**

**<mark>// 非同期処理を発生させる際にオプションとしてsignalを渡す</mark> const promise = fetch('https://example.com', { signal });** **<mark>// 何らかのタイミングで中止する</mark> controller.abort();**

**リスト** 1の例では、 fetch() の処理を中止するために AbortController の abort() を呼び出しています。 fetch() に渡された AbortSignal に AbortController を通じて中止の操作が伝わると、 fetch() によって行われていた非同期処理が中止されます。 fetch() は HTTP 通信を行う関数ですから、通信の最中に中止が発生すると通信が実際に中断されます。

#### **addEventListenerと一緒に使う例** 

非同期処理はさまざまな場面で行われますから、それに対応するように多くの Web API が AbortSignal に対応しています。 fetch() 以外の例としては、 DOM などにおいてイベントを登録するのに使われる addEventListener() も AbortSignal に対応しています。渡された AbortSignal が中止されたときにイベントリスナが削除されるという動作になります。

**リスト** 2の例では、イベントリスナを登録する際に AbortSignal を渡しています。この AbortSignal が中止されると、 removeEventListener を呼び出したときのように、イベントリスナが削除されます。これは非同期処理の中止とは少し異なりますが、イベント待機の中断とみなせばしっくり来ますね。

###### **▼リスト2 AbortSignalの使い方(addEventListenerでの使用例)** 

**const controller = new AbortController(); window.addEventListener('resize', () => { console.log('resize'); }, { signal: controller.signal });**

**<mark>// 何らかのタイミングで中止する</mark> controller.abort();**

###### **中止による失敗の取り扱い方** 

AbortSignal によって非同期処理が中止された場合、当該非同期処理の結果はどのように扱われるのでしょうか。ご存じのように、 JavaScript では非同期処理の結果は Promise として表現され、 Promise の結果は成功( fulfilled )と失敗( rejected )があります。そして、 AbortSignal によって非同期処理が中止された場合は、結果は失敗として扱われるのが普通です。その場合の挙動もおおよそ統一されています。

AbortSignal を介した中断によって非同期処理の結果が失敗となった場合、 DOMException というエラーオブジェクトが失敗の結果として現れます。さらに、この DOMException は name が AbortError となっています。これを利用して、失敗が中止によるものかどうか判定できます( **リスト** 3)。

###### **▼リスト3 中止による失敗を判定する方法** 

**fetch('/', { signal: controller.signal }) .then((res) => { console.log('成功', res); }) .catch((err) => {**

**if (err instanceof DOMException && err.name === 'AbortError') { console.log('中止');**

**} else { console.log('その他の失敗', err); } });**

###### **AbortSignalを受け取る側の実装** 

これまでは、 AbortSignal を発行する側の実装を見てきました。では、 AbortSignal を受け取る側の実装はどのようになるのでしょうか。つまり、自分が非同期処理を実装する側で、 AbortSignal を受け取って中止する機能を付けたい場合です。この場合には、 AbortSignal から情報を得るための 2 つの機能が役に立ちます。

#### **一番簡単な例** 

たとえば、内部で 3 回の fetch() を順番に行う非同期処理を実装するとしましょう。引数として AbortSignal を受け取って、それが中止されたら一連の処理がそこで止まるようにします。そのためには、実は受け取った AbortSignal を中の fetch() に渡すだけで OK です。

**リスト** 4の例は最低限のものでエラー処理なども省略していますが、そこは本筋とは関係ないということで大目に見てください。

**▼リスト4 中止機能の実装(内部でAbortSignal対応の処理を呼ぶ場合) async function transferMoney(signal: AbortSignal) { await fetch('/api/withdraw', { method: 'POST', body: JSON.stringify({ user: "Alice", amount: 1000 }), signal, }); await fetch('/api/deposit', { method: 'POST', body: JSON.stringify({ user: "Bob", amount: 1000 }), signal, });**

**await fetch('/api/notification', { method: 'POST', body: JSON.stringify({ text: "Alice sent 1000 yen to Bob", }), signal, }); }**

※ リスト4は現実の開発のコードを意識して一部TypeScriptのコードにしています (transferMoneyの引数signalに「: AbortSignal」という型注釈を付けています)。これ以降のサンプルコードも必要に応じてTypeScriptのコードで示していきます。このように各 fetch() に signal をそのまま渡すことで、中止が発生した時点で実行されていた fetch() が中止されます。その await でエラーが発生するため、以降の処理は行われません。途中で中止されたときにそれまでの処理をもとに戻したいといったことを考えなければ、これだけで中止が実装できます。このように、基本的には渡された AbortSignal を内部処理にそのまま渡すだけで中止機能を実装できます。

#### **手動で中止処理を行う必要がある場合の実装** 

しかし、ここまで簡単にいかない場合もあります。たとえば、内部で行っている処理が AbortSignal に対応していない場合です。具体例として、処理を Web Worker で行うようにしている場合を考えます。 Web Worker は、基本的にシングルスレッドで実行される JavaScript において、別のスレッドで処理を行うためのしくみです。うまく使えば、メインスレッドとの並列処理により高速化が望めます。

Worker とは、 postMessage() というメソッドを用いてデータのやりとりをすることしかできません。そのため、非同期処理開始時に

Worker に処理開始命令を送り、処理が終わったら Worker からデータが送られてくるという処理を手で実装します。中止機能を付けたい場合は、渡された AbortSignal が中止された場合を手動で検知して、中止命令を Worker に送る必要があります。それを組み込んだ Worker の実装が **リスト** 5です。

**▼リスト5 中止機能の実装(内部の処理がAbortSignalに対応していない場合) async function doSomeWork(signal: AbortSignal) { if (signal.aborted) { throw signal.reason; } const worker = new Worker('worker.js');** **<mark>// Workerに処理開始命令を送る</mark> worker.postMessage({ command: 'doSomeWork' });** **<mark>// AbortSignalが中止されたらWorkerに中止命令を送る</mark> signal.addEventListener('abort', () => { worker.postMessage({ command: 'abort' }); });** **<mark>// Workerの処理が終わるまで待つ</mark> const result = await new Promise((resolve, reject) => { worker.addEventListener('message', (event) => { resolve(event.data); }); worker.addEventListener('error', (event) => { if (event.error === "aborted") { reject(signal.reason); } else { reject(event.error); } }); }); return result; }**

関数の真ん中あたりで、 signal.addEventListener を実行しているところがポイントです。渡された AbortSignal が中止されたら abort イベントとして検知することができるので、これを利用しましょう。また、関数の先頭では signal.aborted をチェックしています。これは、 AbortSignal が中止済みであれば true になるプロパティです。 abort イベントを登録する前にすでに中止済みだった場合は abort イベントが発生しないので、中止済みの場合も漏れずにチェックする必要があります。中止の場合に signal.reason が使用されているのが目につきますが、これは中止の理由を表す値です。中止の理由は中止を行う側 ( AbortController 側)で指定することができ、それが AbortSignal を通して伝わってくるという形です。基本的には、これをそのまま非同期処理の失敗の理由を表す値として使えます。中止の理由は、

AbortController の abort() メソッドの引数として指定することができます。 **リスト** 1の例では引数なしで呼び出していましたが、この場合はデフォルトの理由( AbortError という名前の DOMException )となります。ちなみに、「 AbortSignal が中止済みだった場合は signal.reason を throw する」というのは頻出処理のため、簡単に書けるメソッドが提供されています。 **リスト** 5の 2 〜 4 行目は次のように置き換えられます。

###### **signal.throwIfAborted();** 

###### **AbortSignalを作るほかの方法** 

ここまで、 AbortSignal は AbortController の signal プロパティから取得するという方法を紹介してきました。しかし、 AbortController を使わなくても AbortSignal を作れます。その方法を紹介します。

#### **中止済みのAbortSignalを作る** 

まず、中止済みの AbortSignal を作る方法です。これは、 AbortSignal の静的メソッドである abort() を呼び出すことで作れます ( **リスト** 6)。引数で中止の理由を指定することもできます。

###### **▼リスト6 中止済みのAbortSignalを作る方法** 

**<mark>// 最初から中止済みのAbortSignal</mark> const signal = AbortSignal.abort();** **<mark>// 理由付き</mark> const signal2 = AbortSignal.abort("中止の理由");**

#### **一定時間後に中止するAbortSignalを作る** 

いわゆるタイムアウトとして、一定時間後に自動的に中止する AbortSignal は需要があります。そのため、これを簡単に作れる方法が用意されています。それが、 AbortSignal の静的メソッドである timeout() です( **リスト** 7)。

###### **▼リスト7 一定時間後に自動的に中止するAbortSignalを作る方法** 

- **<mark>// 1秒後に中止するAbortSignal</mark>** 

- **const signal = AbortSignal.timeout(1000);** 

#### **AbortSignalの合成** 

AbortSignal の合成というのは重要な操作です。これは、複数の AbortSignal を合成して、それらの中のどれかが中止されたら中止する AbortSignal を作るというものです。これは、 AbortSignal の静的メソッドである any() を使って作れます。ただし、 any() メソッドはかなり新しいため、仕様化されているもののまだ実際のブラウザでは使用できません。そのため、ここではまだ実際には使用できないものの、将来的に使用できるようになることを期待して紹介します。たとえば、 transferMoney() の例で、外部から渡された AbortSignal が中止された場合に加えて、 5 秒間のタイムアウトも設定したいとします。この場合、 **リスト** 8のように書けます。この例の場合、 combinedSignal は「 signal が中止されるか、または 5 秒経過する」という条件で中止されることになります。

**▼リスト8 複数のAbortSignalを合成する方法 async function transferMoney(signal: AbortSignal) { const combinedSignal = AbortSignal.any([ signal, AbortSignal.timeout(5000), ]); await fetch('/api/withdraw', { method: 'POST', body: JSON.stringify({ user: "Alice", amount: 1000 }), signal: combinedSignal, });** **<mark>(..略..)</mark> }**

###### ◆ ◆ ◆ 

以上が、現在用意されている AbortSignal の作成方法です。 AbortSignal が中止される条件によって作り方が異なることがわかりますね。最初に紹介した AbortController を用いて作る方法は、「こちらの好きなタイミングで中止させられる」という中止条件の AbortSignal を作る方法だったのだと理解しましょう。

##### **非同期処理のコンテキストを保持できる AsyncLocalStorage** 

ここからは、非同期処理と一緒に使うと役立つもう 1 つの機能、 AsyncLocalStorageを解説します。これはもともと Node.js の機能として登場しましたが、前述の WinterCG による仕様化も行われています。そのため、 Node.js 以外に Cloudflare Workers 環境でも利用可能です。また、 Deno でも Node.js 互換機能の 1 つとして実装が提供されており、これらの環境で利用可能となっています。一方、 AsyncLocalStorage は Web 仕様の一部ではないため、ブラウザでの実装はありません。

###### **AsyncLocalStorageを支える問題意識** 

AsyncLocalStorage は、非同期処理の実行コンテキストにおいて共有される値を保持する機能です。非同期処理の実行コンテキストというのはわかりにくい表現ですが、大雑把に言えば、同じ非同期コールスタックに属する非同期処理のことです。たとえば、 **リスト** 9 のようなコードを考えます。

###### **▼リスト9 同じ非同期コールスタックに属する非同期処理の例** 

**async function transferMoney(sender: string, receiver: string) { await withdraw(sender, 1000); await deposit(receiver, 1000); await notify(`${sender} sent 1000 yen to ${receiver}`); }**

この場合、 transferMoney が withdraw 、 deposit 、 notify の 3 つの非同期処理を順番に呼び出しています。このような、 async/await を交えた呼び出し-呼び出されの関係が非同期コールスタックです。では、非同期コールスタックの中のいろいろな場所で値を共有したいとしましょう。たとえば、一連の操作の中で「取引 ID 」を共有し、ログなどで使いたいとしましょう。一番単純な方法は、 **リスト** 10のように関数の引数として渡すことです。

###### **▼リスト10 関数の引数を使って取引IDを共有する方法** 

- **async function transferMoney(sender: string, receiver: string, transactionId: string) { await withdraw(sender, 1000, transactionId);** 

**await deposit(receiver, 1000, transactionId); await notify(`${sender} sent 1000 yen to ${receiver} (transactionId: ${transactionId})`); }**

この方法も悪くはありませんが、あらゆる関数呼び出しで取引 ID を取り回す必要があるため不便で、コードの変更を阻害します。そのため、引数で渡す以外の方法で値を共有したいですね。非同期処理でなければ、 **リスト** 11のようにグローバル変数を使うことで値を共有することもできました。

###### **▼リスト11 グローバル変数を使って取引IDを共有する方法** 

###### **globalThis.transactionId = "12345";** 

**<mark>// transferMoneyの処理中は対応するグローバル変数に取引IDが入っている</mark> transferMoney("Alice", "Bob"); globalThis.transactionId = null;**

しかし、非同期処理の場合はこのような方法は通用しません。なぜなら、非同期処理においては複数の処理が並行的に実行されることがあり、その場合グローバル変数はどの非同期処理のものかわからなくなってしまうからです。そこで、 AsyncLocalStorage を使うことで、それぞれの非同期処理(これが 1 つの非同期コールスタックに相当します)に対して 1 つ、値を保持することができます。

###### **AsyncLocalStorageの使い方** 

では、 AsyncLocalStorage の使い方を見ていきましょう。保持したい値 1 つに対して AsyncLocalStorage のインスタンスを作成します。そして、その run() メソッドを呼び出すことで、その内部の非同期コールスタックで保持される値を指定することができます。現在保持されている値は、 getStore() メソッドで取得できます。

**リスト** 12の例では transferMoney を呼び出す側と transferMoney 内部の間で直接的な値の受け渡しは行われていません。しかし、 AsyncLocalStorage を通じて値が共有されているため、外側で run() に供給された値を中で getStore() により取得することができるのです。今回は transferMoney の中で取引 ID を参照していますが、この方法では withdraw や deposit の中からも同様にして取引 ID を参照可能です。 AsyncLocalStorage を通じた値の共有は、非同期コールスタックが深くなる場合にとくに有利です。

- **▼リスト12 AsyncLocalStorageを使って取引IDを共有する方法** **<mark>// 取引ID用のAsyncLocalStorageのインスタンスを作成</mark>** 

- **const transactionIdStorage = new AsyncLocalStorage<string>();** 

**<mark>// この中で呼び出される非同期処理は、transactionIdStorageに保持された値を参照できる</mark> transactionIdStorage.run("12345", async () => { await transferMoney("Alice", "Bob"); });**

**async function transferMoney(sender: string, receiver: string) {** **<mark>// 非同期処理の内部ではこのように現在の取引IDを取得できる</mark> const transactionId = transactionIdStorage.getStore(); await withdraw(sender, 1000); await deposit(receiver, 1000); await notify(`${sender} sent 1000 yen to ${receiver} (transactionId: ${transactionId})`); }**

###### **AsyncLocalStorageの応用** 

以上で基本的な AsyncLocalStorage の使い方を学びました。ここからは、 AsyncLocalStorage を使った実践的な例を 2 つ見てみましょう。

#### **構造化ロギング** 

AsyncLocalStorage の使いどころとして、すでに見た例に近いものとしてはロギングの用途が挙げられます。とくに、構造化ロギング (単なる文字列ではなくオブジェクトのような形でログを記録すること)の場合は処理が進むごとにログの項目が足されていくことが多く、現在のログの項目を保持しておくのは AsyncLocalStorage の得意とするところです( **リスト** 13)。

**▼リスト13 ロギングにおけるAsyncLocalStorageの活用例** **<mark>// 現在のログ用メタデータを保持するAsyncLocalStorageのインスタンスを作成</mark> const logMetadataStorage = new AsyncLocalStorage<Record<string, unknown>> ();**

**logMetadataStorage.run({ transactionId: "12345" }, async () => { await transferMoney("Alice", "Bob"); });**

**<mark>/** ログを出力 */</mark> function log(message: string) { const logMetadata = logMetadataStorage.getStore(); console.log({ ...logMetadata, message }); }**

**async function transferMoney(sender: string, receiver: string) { const currentMetadata = logMetadataStorage.getStore(); log('Transferring money'); await withdraw(sender, 1000); await deposit(receiver, 1000); await notify(`${sender} sent 1000 yen to ${receiver}`); }**

ロギングというのはメインの処理と直交するものとして知られています。つまり、ログの処理というのはメインの処理に対しておまけのように付随して、かつどこにでも存在するものです。そのため、ログのためにあれこれと関数の引数が増えたりインターフェースが変わったりするのは望ましくなく、 AsyncLocalStorage のようなメインの処理に対して非侵襲的なしくみを使うのが良いでしょう。

#### **依存注入** 

いわゆる依存注入( Dependency Injection : DI )も AsyncLocalStorage と相性のいい使い方の 1 つです。とくにユニットテストのことを考えると、テスト対象のロジックに対して依存する外部の処理を差し替えられるというのは非常に便利です。引数などのインターフェースを変更することなく、必要なところにピンポイントで注入できるのが AsyncLocalStorage の強みです。簡単な具体例としては、現在時刻の取得が必要なロジックを考えてみましょう。テスト時に実際の時刻に依存するのは再現性などの観点から好ましくありません。そこで、現在時刻を取得する関数を注入することで、テスト時にはテスト用に固定された時刻を使用することができます。

AsyncLocalStorage を用いた注入であれば、コードを変更する必要があるのは実際に現在時刻を取得するところのみです。その周りに影響を及ぼすことなく、テスト用の実装を注入することができます。 **リスト** 14の例では、データを取得し、そのタイムスタンプが現在時刻から 1 分以上前であればエラーを投げるというロジックを実装しています。

- **▼リスト14 タイムスタンプが現在時刻から1分以上前ならエラーとする処理** **<mark>// 依存注入対応前の実装</mark>** 

- **async function getLatestDataAndVerify() { const latestData = await fetchLatestData(); if (latestData.timestamp < Date.now() - 1000 * 60) { throw new Error('Data is too old');** 

**}**

**}**

これを AsyncLocalStorage を用いて依存注入対応にすると、 **リスト** 15のようになります。

###### **▼リスト15 依存注入におけるAsyncLocalStorageの活用例** 

**const nowStorage = new AsyncLocalStorage<() => number>();** **<mark>// 注入された関数を取得(何も注入されていなければデフォルトのDate.nowを返す)</mark> const getNowFunction = () => nowStorage.getStore() ?? Date.now;**

**async function getLatestDataAndVerify() { const latestData = await fetchLatestData(); const nowFunction = getNowFunction(); if (latestData.timestamp < nowFunction() - 1000 * 60) { throw new Error('Data is too old');**

- **} }** 

テスト用に固定の時刻を返したうえで getLatestDataAndVerify を実行したい場合は、 **リスト** 16のように書けます。

###### **▼リスト16 テスト用に固定の時刻を返す方法** 

- **nowStorage.run(() => 1689585600000, async () => { await getLatestDataAndVerify(); });** 

このように、テスト対象の完全な外側から依存を注入できるのが AsyncLocalStorage の良いところです。

###### **AsyncLocalStorageの注意点** 

ここまで AsyncLocalStorage の使い道を説明してきました。しかし、その利用にあたっては注意しなければならない点もあります。

#### **コールバック地獄の再来!?** 

Promise 導入より前の非同期処理の様相は、俗にコールバック地獄と呼ばれていました。つまり、非同期処理の結果をコールバック関数として受け取るため、どんどんコードのネストが深くなってしまうということです。 Promise や async/await の登場によりネストが深くなる問題は改善されましたが、 AsyncLocalStorage を活用すると、再びコールバック地獄のようなコードになってしまうことがあります。たとえば、先ほどの例で各メソッドを呼び出す際にログのデータを追加したい場合、 **リスト** 17のコードのようになります。このように、非同期処理の一部分で AsyncLocalStorage の値を制御すると、そこにネストが発生します。つまり、 AsyncLocalStorage を多用すればするほどネストが深くなり、新たなコールバック地獄が生まれてしまうのです。 AsyncLocalStorage を使えば引数などを使わずに間接的にデータをやりとりできるのですが、その代償としてコールバック地獄が生まれるという欠点があるのです。

- **▼リスト17 AsyncLocalStorageにより発生するコールバック地獄の例** 

|**async function transferMoney(sender: string, receiver: string) {**<br>**const currentMetadata = logMetadataStorage.getStore();**<br>**await logMetadataStorage.run({ ...currentMetadata, operation: 'withdraw' }, async**<br>**() => {**<br>**await withdraw(sender, 1000);**<br>**});**<br>**await logMetadataStorage.run({ ...currentMetadata, operation: 'deposit' }, async ()**<br>**=> {**<br>**await deposit(receiver, 1000);**<br>**});**<br>**await logMetadataStorage.run({ ...currentMetadata, operation: 'notify' }, async ()**<br>**=> {**<br>**await notify(`${sender} sent 1000 yen to ${receiver}`);**<br>**});**<br>**}**|
|---|

Node.js ではネストを避けられる enterWith() というメソッドも用意されていますが、こちらにも別の欠点があるうえに、ほかの環境との相互運用性に乏しいのであまりお勧めしません。

##### **まとめ** 

この章では、実践的な非同期処理の実装に必要となる 2 つの機能として、 AbortSignal と AsyncLocalStorage を紹介しました。前者は、非同期処理の中止という頻出の処理を行うための標準的な方法です。 Web 標準においても、 fetch() を筆頭に多くの場面で AbortSignal を活用できます。後者は、一連の非同期処理の中でデータを共有するための機構で、同じ非同期コンテキスト内であれば同じ値を共有できるという、使いこなせば便利なものです。とくに DI を行う方法として、非侵襲的に DI 機構を追加できる点で優れています。非同期処理は現代的な JavaScript プログラミングにおいて欠かすことはできないものですが、やはり難しく考えなければならないことが多くあります。 1 つずつ身につけていきましょう。小杉山拓弥(こすぎやまたくや)株式会社LayerX <u>https://layerx.co.jp/ @itkq</u>

**第 1 章**

### **原則を正しく理解しようカオスエンジニアリング超入門** 

**「カオスエンジニアリング」という言葉自体の認知度に対して、その本質や重要性の浸透はいまだ十分とは言えません。第1章では、カオスエンジニアリングの誤解を払拭すべく、あらためてその基本や概要を押さえていきましょう。**

「カオスエンジニアリング」( Chaos Engineering )という言葉を聞いたことはあるでしょうか。誕生から 10 年ほど経過し、今となっては、一度は耳にしたことがある方も多いでしょう。筆者がカオスエンジニアリングを知ったときは、 Netflix ~~社~~ <u>注1</u> ~~の~~ Chaos Monkey というツールがセットだったことを強く記憶しています。なぜならそのツールは、本番環境のサーバをランダムにシャットダウンするという非常にキャッチーなものだったからです。しかしキャッチーだったがゆえに、カオスエンジニアリングの「本番環境でモノを壊す」という側面だけが広まる傾向が当時ありました。実際のところ、この側面だけでは本質をとらえきれていません。第 1 章では、カオスエンジニアリングとは何かについて、背景や誕生経緯に触れながら正しく理解することを目指します。実際の事例や、近年の技術トレンドとの関係も述べます。なお、 Netflix 社でカオスエンジニアリングに関わったエンジニアによる書籍『カオスエンジニアリング ─ 回復力のあるシステムの実践』<sup>[1]</sup> が最も包括的な情報と思われるため、その内容の多くを参考にしています。

##### **カオスエンジニアリングの提唱** 

「カオスエンジニアリング」という言葉を生み出し世間に広めたのは Netflix 社です。ここでは、カオスエンジニアリングが提唱され世間に広まるまでの過程について説明します( **表** 1)。

2008年:https://about.netflix.com/en/news/completing-the-netflix-cloud-migration 2010年:https://www.gremlin.com/chaos-monkey/ 2012年:

<u>https://github.com/Netflix/chaosmonkey/commit/0be459ae2ac64c834ee48c01a629cdc8b54 8ae98</u>

2015年頃:https://netflixtechblog.com/chaos-engineering-upgraded-878d341f15fa 2016年1月:https://github.com/chaos-eng/chaos- <u>eng.github.io/commit/c820b12e1315d59919b89813de8fba80b9a0cf0c</u> 2016年:https://www.gremlin.com/community/tutorials/chaos-engineering-the-history- <u>principles-and-practice/</u>

2017年:https://www.youtube.com/watch?v=rgfww8tLM0A 2018年:https://www.gremlin.com/chaos-conf/2018/ 2020年4月:https://learning.oreilly.com/library/view/chaos-engineering/9781492043850/ 2020年12月:https://youtu.be/jt-gV1YwmnI?t=4143 2022年:https://www.oreilly.co.jp/books/9784873119885/

#### **Chaos Monkeyの誕生** 

Netflix 社は 2008 年にデータセンターからクラウドへの移行を宣言しました。その背景には、データセンターに存在する大きなデータベースや垂直的にしかスケールしないコンポーネントが、スケーラビリティや可用性の単一障害点を持つアーキテクチャにロックインされてしまうというリスクがありました。したがって、水平スケールが可能なクラウドへ移行できれば、そのような単一障害点を減らせる見込みがありました。しかし、当時のクラウド( AWS : Amazon Web Services )は今より未成熟で、たとえばインスタンスが時々警告なく消滅することも起こっていました。このような障害は、巨大で高性能なマシンを扱うデータセンターでは珍しいことで、この特性の違いがクラウド移行への大きな障壁となりました( **表** 2)。

Netflix 社の特徴的なマネジメント哲学に、「すべてのエンジニアには裁量と責任を与え、仕事の達成の仕方は個々に任せる」というものがあります。この哲学が、クラウド移行における障壁を乗り越える助けとなりました。結果としてうまくいったアプリケーションが、 Chaos Monkey です。 Chaos Monkey は、本番クラスタ上のインスタンスをランダムに 1 つ選び、業務時間中のどこかで警告なしに停止するというシンプルなアプリケーションです。 Chaos Monkey によってインスタンスが停止し、事業や業務に負の影響が発生すると困るため、エンジニアはこの問題に対処する必要がありました。インスタンス障害に対する回復力をプロアクティブに検証する手法がマネジメント哲学に沿うものであり、エンジニアによる早期な問題解決を効果的に促しました。

#### **<u>規律の明文化</u>** 

Chaos Monkey の成功後、 AWS のリージョ ~~ン~~ <u>注2</u> ~~全~~ 体の障害をエミュレートする Chaos Kong も登場しました。このツールにより、利用リージョン全体が障害になったとしても別のリージョンにフェイルオーバーする訓練がなされ、実際に発生したリージョン障害にうまく対処できたそうです。これらのツールが成功した後、 2015 年にカオスエンジニアリングチームが発足しました。 Netflix 社内の人の多くは、カオスエンジニアリングとは「本番環境でわざと何かを壊すもの」という認識を持っていました。 Netflix 社のすべてのエンジニアは本番環境へのアクセス手段を持っていたため、その行為自体が価値を表しているとは言えませんでした。そこでチームは、議論の末に、「カオスエンジニアリングの原則」<sup>[2]</sup> を編み出しました。これによれば、カオスエンジニアリングとは **システムが本番環境における不安定な状態に耐える能力へ自信を持つためにシステム上で実験を行う訓練方法** です。

#### **<u>そして一般化へ</u>** 

Netflix 社は自分たちが定義したカオスエンジニアリングをさらに実践するため、コミュニティを発足させました。他社でも不具合の注入、障害テストなどの取り組みは存在しており、コミュニティは徐々に大きくなっていきました。

2016 年に、カオスエンジニアリングのマネージドサービスを提供する Gremlin 社が設立されました。 2018 年に、同社はカオスエンジニアリングに関するカンファレンスである Chaos Conf <u>注3</u> ~~を~~ 主催します。 2020 年には、 AWS がカオス実験を行うマネージドサービスの Fault Injection Simulato ~~r~~ <u>注4</u> ~~を~~ 発表しました。 Kubernetes 上でカオス実験を行うプラットフォームである Chaos Mes ~~h~~ <u>注5</u> ~~な~~ ど、今では OSS も多数存在します。このように、今日においてカオスエンジニアリングは、ソフトウェアエンジニアリングの世界で十分に認知されたものといってよいでしょう。

##### **カオスエンジニアリングとは何か** 

ここでは、 Netflix 社が公開した「カオスエンジニアリングの原則」<sup>[2]</sup> の中身に触れながら、カオスエンジニアリングとは何かを理解することを目指します。

#### **<u>そもそもカオスとは何を指すのか</u>** 

「カオスエンジニアリングの原則」において、明確に「カオス」の定義は行われていません。しかし、文脈から、カオスとは分散システムに存在するものと読み取ることができます。分散システムとは、ネットワークにより接続された複数のコンピュータによって構成されたシステムのことです。ネットワーク越しに、クライアントとサーバ間で 1 つのメッセージを往復させると、合計 8 個のステップが発生します注6 ~~。~~ これらのステップはすべて失敗し得るので、潜在的な失敗パターンが複雑になります。分散システム研究の第一人者である Leslie B. Lamport 氏によれば、「分散システムとは、そんな障害があるとは思ってもみなかった障害によって利用不能になるシステムである」と評価されます。日本語の日常会話では、「カオスな部屋」など無秩序でごちゃごちゃした状況を形容する表現で使われることが多いでしょう。一方、カオスエンジニアリングにおけるカオスとは、予測できない複雑性を表しており、物理学におけるカオス理論( Chaos theory )の文脈に近いと考えられます。カオスは分散システムに内在する、予測できない障害モードを引き起こすものと言えるでしょう。

#### **<u>カオス実験</u>** 

分散システムにおけるカオスのすべてを、人間が把握することは不可能です。なぜなら、「未知の未知( Unknown Unknowns )」な部分が存在するからです注7 ~~(~~ **図** 1)。これに対し、 **仮説を立て実験し、自分たちの知識を広げること** がカオスエンジニアリングの考え方です。「カオスエンジニアリングの原則」で説明される、カオスエンジニアリングの具体的な実験手順を書籍から引用します<sup>[1]</sup> 。

_1. システムの正常な振る舞いを示す測定可能な出力を、「定常状態」として定義することから始めます_

_2. 定義した「定常状態」が、制御グループと実験グループのいずれにおいても継続すると仮説を立てます_

_3. 実世界を反映する事象、すなわちサーバのクラッシュやハードドライブの誤作動、ネットワーク接続の切断などの変数を導入します_

_4. 制御グループと実験グループの間に定常状態に関する差異を探し、仮説の反証を試みます_

定常状態は、 CPU 使用率などシステムの内的なものではなく、外部から観測できる出力が望ましいとされています。 Netflix 社における定常状態は、「映像が開始する秒間の件数」である SPS ( Starts Per Second )です<sup>[3][4]</sup> 。また実験を観察するにあたり、観測された変化が実験由来のものでなかった場合は実験が無意味になりかねないため、注入するカオスの影響を受けない制御グループと、影響を受ける実験グループの 2 つを用意します。なお、実験をどのように実装するかはとくに決められていません。ここまで読んでいただければ、カオスエンジニアリングが「モノを壊す」ものではないと十分理解できたでしょう。なお「本番環境で実験する」という原則も存在しますが、これは「発展した」原則とされています。本番環境以外でも新しい知識を得られる可能性があるならば、ユーザーに悪影響を与えないまま実験できます。必ずしも本番環境で行う必要はありません。

#### **<u>カオスエンジニアリング≠テスト</u>** 

「カオスエンジニアリングの原則」では、カオスを注入することは実験であると述べられています。カオスエンジニアリングに対するよくある疑問の 1 つが、「テストと違うのか?」です。結論から言えば、実験とテストには性質の違いがあると考えられます。ソフトウェアエンジニアリングにおける代表的なテストである単体テストは、コードを書いたエンジニアが、実装したとおりの振る舞いをすることを保証するものです。アサーションはエンジニアの知識に基づいて書かれるものであり、テストの実行結果から新しい知識が得られることは厳密にはありません。一方実験では、前提として少なくとも不確実な問いがあり、実験結果によって不確実な部分を減らすことが可能です。すなわち新しい知識を得られていると言えます。続いて、結合テストを大量に書くことを考えてみます。結合テストは、複数のモジュール間の連携が意図どおりの挙動になっているかを確かめるものです。この場合でも、テストが人間の知識に基づくことを考えれば、新しい知識を獲得できるアプローチとは言えません。ただし、想定以上の負荷をかけ続けるストレステストなど、「テスト」と名は付いているものの実験的と言える手法もあります。ちなみに、カオス実験はカオステストと呼ばれることもあります。カオスエンジニアリングとは実験により自分たちのシステムへの自信を持つアプローチであり、そのためには出発点である「問い」が最も重要です。このプロセスは、典型的な単体テストなどのテストを行うプロセスとは異なると言えます。

##### **カオスエンジニアリングの実例** 

ここでは、実世界でどのようにカオスエンジニアリングが行われているかについて、 2 つの事例を紹介します注8 ~~。~~

#### **<u>ヤフー社の障害シナリオ</u>** 

国内で詳細が公開されている事例として、ヤフー社の Container as a Service ( CaaS )を対象とした本番環境の障害シナリオを紹介します注9 ~~。~~ ヤフー社では、システムのモダナイゼーションやマイクロサービス化、マルチリージョン/マルチ A ~~Z~~ <u>注10</u> ~~化~~ を進めた結果、システムが複雑化し、開発者や運用者のメンタルモデルと実際のシステムの挙動にギャップが存在することがわかりました。これに対し、起こり得る障害と想定される仮説をシナリオとして用意し、 Kubernetes 上に構築される Container as a Service のプラットフォームを対象にシナリオを実施しています。例として挙げられている実行シナリオは次のとおりです。

- ・「ワーカーノードがダウンしてもPod注11 ~~が~~ 別のノードで復活すること」を本番環境で週1回自動実行 

- ・「1つのAZがダウンしてもサービス提供が継続できること」を本番環境で半年に1回実施 

#### **Netflix社のChAP** 

高度な事例として、 Netflix 社の ChAP ( Chaos Automation Platform ~~)~~ <u>注12[</u> 4 ]というカオス実験の自動化プラットフォームを紹介します。これは障害注入( failure injection )と自動化されたカナリアリリースを組み合わせたものととらえることができます。カナリアリリースとは、新しいソフトウェアバージョンを本番環境にリリースする際にリスクを軽減させる手法で、変更を全体に展開した後、一部のユーザーにだけ適用することを指します注13 ~~。~~

ChAP はデプロイパイプラインに組み込まれており、制御グループと実験グループの 2 つのインスタンスを追加で起動します。デプロイするサービスに対して依存関係のあるマイクロサービスを自動的に選択し、「条件 X のもとでのマイクロサービス Y は定常状態を維持する」という実験を自動的に行います。分散システムにおける異常はレイテンシとしてモデリングできることが多いため、条件 X にはレイテンシが選択されることが多いそうです。実験期間に制御グループと実験グループの KPI (映像が開始する秒間の件数)が同じ程度に維持される場合は、リリースが全体に昇格しますが、 KPI から外れた場合は実験が直ちに中止され、実験グループへのトラフィックが流れなくなります。

**図** 2に ChAP の動作概念図を示します。これは API のリリースで Ratings システムの障害に耐える実験です。制御グループと実験グループを立ち上げ、 Gateway がそれぞれに全体の 0.5 %のリクエストをルーティングし、実験グループから Ratings システムへのリクエストに障害注入して KPI を観測します。

<u>https://netflixtechblog.com/chap-chaos-automation-platform-53e6d528371f#36c0</u>

ChAP によって、たとえばリトライポリシーの設定や高負荷時のフォールバック、サーキットブレーカーの設定といった耐障害性に自信を持つことができるそうです。 ChAP は Chaos Monkey と比較して、カオスエンジニアリングの発展した原則<sup>[2]</sup> である「実世界の事象を多様化させる」「爆発半径注14 ~~を~~ 抑える」「実験を自動化し継続的に実行する」が適用された発展型と言えます。

##### **オブザーバビリティ、DevOps、SREとの関係** 

ここでは、近年の技術トレンドとカオスエンジニアリングがどのように関係するかを考察します。とくに、オブザーバビリティ、 DevOps 、 SRE との関係について扱います。

#### **<u>近年のシステム進化傾向</u>** 

Netflix 社がカオスエンジニアリングを始めることになったきっかけは、データセンターからクラウドへの移行でした。クラウド化やマイクロサービスは、近年の技術トレンドになっています。

#### **クラウド化** 

AWS や Google Cloud などのパブリッククラウドは、インターネット越しにサーバなどのリソースや運用負荷が少ないマネージドサービスを提供します。 2023 年現在では、クラウドを活用して事業サービスを提供することが一般的になっています。自前でデータセンターを持つのと比較して、コスト効率の最適化、迅速で柔軟なスケーラビリティなどのメリットがあり、ビジネス上の優位性を獲得すると言われています。これは、 DevOps Research and

Assessment ( DORA )による「 Accelerate Stateof DevOps Report 」<sup>[5]</sup> からある程度明らかになっていることです。クラウド化により、水平スケール可能なアーキテクチャが促進され、より動的で回復力を必要とする環境になっています。サーバはワークロードに応じて増減することが当たり前で、動的なターゲットの監視が得意な Prometheus などのモニタリングツールが流行しています。また、ユーザーからサーバを隠蔽したサーバレスサービスや、定期メンテナンスなどの運用が不要なマネージドサービスも存在します。クラウドサービスのほとんどは SLA ( Service Level Agreement :サービスの品質保証契約)が設定されています。たとえば AWS で仮想コンピューティングを提供する Amazon EC2 サービスの月間の可用性は 99.99 %注15 ~~で~~ す。クラウドサービスを活用した自社サービスの信頼性は、クラウドサービスの信頼性に引きずられることに留意する必要があります。

#### **マイクロサービス** 

マイクロサービスアーキテクチャは、単一のアプリケーションから構成される小さなサービスを組み合わせ、サービス間通信を伴うスタイルのことです<sup>[6]</sup> 。マイクロサービスにおけるサービスは、疎結合( loosely-coupled )であり独立して開発とデプロイを行える特徴があります。クライアントサイド、バックエンド、データストアから構成される 3 層アーキテクチャに代表される、モノリスアーキテクチャとよく対比されます。マイクロサービスは、モノリスにおけるアプリケーションの複雑度やスケーラビリティ、デリバリーの遅さなどの問題を解決する場合がありますが、銀の弾丸ではありません<sup>[7]</sup> 。とくに、マイクロサービスは「カオス実験」の項で述べた分散システムの本質的な難しさを抱えており、そのぶん運用負荷が高くなります。自動リトライなどにより回復性を高めたり、グレースフルデグラデーション注 <u>16</u> ~~可~~ 能な設計をしたりする必要が出てきます。しかし、そのうえでも予期せぬ出来事はつきものです。システムに直接手を加えることなく、今何が起こっているのかを把握できるようにしておかなければ、運用し続けることは困難になるでしょう。

#### **<u>オブザーバビリティとの関係</u>** 

書籍『オブザーバビリティ・エンジニアリング』によると、オブザーバビリティの定義は「システムがどのような状態になったとしても、それがどんなに斬新で奇妙なものであっても、どれだけ理解し説明できるかを示す尺度」<sup>[8]</sup> です。筆者が一言で表すとするならば「本番環境のアプリケーションを手を加えずにデバッグする能力」になります。古典的なモノリシックシステムでは、創発的注17 ~~な~~ 複雑性があまりないため、シンプルなモニタリングで十分でした。モニタリングでは、 CPU 使用率の飽和など、既知のシステム問題にうまく対処できます。また、ダッシュボードやログを眺めることで、内部で何が起こっているか、目に見えない問題がどこで起こっているかの推論がある程度可能でした。一方、マイクロサービスアーキテクチャは分散システムであり、『オブザーバビリティ・エンジニアリング』にはたとえば次の特徴があると書かれています<sup>[8]</sup> 。

- _・手順書で定義できる既知の障害は、自動的に復旧できるモデルへと変化する。すなわちサービス障害とは、自動復旧できない未知の問題である_ 

- _・本番環境におけるリクエストは、ネットワークをまたぎ、複数のランタイムにまたがるため、デバッグが困難である_ 

未知の問題に対処するため、オブザーバビリティの出番となります。さまざまな情報を付与したイベントを、機械が扱いやすい形で収集しておき、イベントをフィルタしながら時系列順に結合することで、ある状況における分散システム上の挙動を詳細に追跡できるようになります。たとえば、ユーザー ID などのリクエスト情報や、コンテナ情報といったランタイム情報で絞り込み、エラーの発生箇所やレイテンシが高い処理を特定することができます。任意の問いをデバッグできるように、古典的なモニタリングのようにメトリクスを事前定義しておくのではなく、可能な限り解像度の高い情報を収集しておくことが必要です。オブザーバビリティを具体的に実践するには、ログ、メトリクス、トレース、プロファイルなどのテレメトリーを中央的なシステムに集約することが一歩目になります。 Honeycom ~~b~~ <u>注18</u> ~~や~~ Datado ~~g~~ <u>注19</u> などのソリューションの利用も選択肢として挙げられます。現在では、テレメトリーの出力について OpenTelemetr ~~y~~ <u>注20</u> ~~と~~ いうベンダー非依存の標準が存在し、これを実装することで任意のシステムへのテレメトリー送信が可能になっています。カオスエンジニアリングとオブザーバビリティは密接な関係にあります<sup>[9]</sup> 。「カオスエンジニアリングの原則」で意識されているのは分散システムであり、すなわちマイクロサービスということにもなります。そのうえで、定常状態を観測しながら、予期せぬ事象の解明を行うだけの能力を持っている必要があります。カオス実験を行う以前に、現在のシステムがどう振る舞っているかがわからなければ、カオスエンジニアリングを行うより先にオブザーバビリティを強化するべきです。オブザーバビリティなしでのカオスエンジニアリングは、単なる「カオス」になりかねません。

#### **DevOpsとの関係** 

DevOps とは、ソフトウェア開発手法のひとつで、ソフトウェアを迅速にビルド・テストする文化と環境により、安全かつ頻繁なリリースを可能にする組織構築を目指すものです注21 ~~。~~

DevOps のプラクティスのひとつである CI ( Continuous Integration 、継続的インテグレーション)は、メインとなるコードベースに対し、開発者がすべての変更を定期的に統合するものです注22 ~~。~~ 寿命の⻑いブランチは、コンフリクトやリグレッションバグが発生するリスクが高まってしまいます。そこで小さい変更を頻繁にマージし、自動ビルドや自動テストを行い、すばやくフィードバックを得ることで、品質向上やメンテナンスコストの削減、チームの生産性を高められます。一方、 CD ( Continuous Delivery 、継続的デリバリー)は、必要に応じて安全にすばやく変更をリリースする能力です注23 ~~。~~ CD は CI を拡張したものと考えることができ、 CI/CD とセットで表記されることがしばしばあります。 CD は能力であり、特定のプラクティスを実践すれば達成できるものではありませんが、 CI 以外の技術的な構成要素としてデプロイ自動化・疎結合なアーキテクチャ・監視とオブザーバビリティなどがあります。

Netflix 社における ChAP のように、デプロイパイプラインに組み込み自動化された検証を行う手法は、 CI/CD に続く **継続的ベリフィケーション(** CV **:** Continuous Verification **)** というカテゴリとして確立されつつあります。今日のソフトウェアは複雑であり、頻繁に変化するため、継続的に問題を検出することに価値があるためです。

#### **SREとの関係** 

SRE ( Site Reliability Engineering )とは、 Google が世間に広めた、信頼性を中心に置くシステムの開発運用手法です<sup>[10]</sup> 。 SRE は DevOps と混合されることもありますが、 Google は「 class SRE implements DevOps 」と表現してお ~~り~~ <u>注24</u> ~~、~~ SRE は DevOps が目指す目標の実現手段と言えます。

SRE の特徴的な哲学として、非現実的な 100 %の信頼性を諦める代わりに、システムの信頼性を示す指標( SLI : Service Level Indicator )とその目標値( SLO : Service Level Objective )を定義し、指標の数値によって合理的な行動を行うものがあります。指標が目標値を下回ってしまった場合、基本的にチームは機能開発よりも信頼性回復のための行動を優先することになります。 SLI はユーザーの体験を表す外面的な数値であることが多いですが、信頼性を効率良く改善していくためにはシステム内部のボトルネックの特定が不可欠であり、そのためにもオブザーバビリティがあるとよいでしょう。障害対応などのリアクティブなアプローチだけでなく、 SRE では、伝統的なシステム運用者と比較してソフトウェアエンジニアリングを重視し、プロアクティブなアプローチも行います。たとえば、トラフィックが増加し、サーバキャパシティが不足してからサーバを増設するという手作業があったとします。これは繰り返される単調な作業であり、「トイル」と呼ばれます。トイルは燃え尽き症候群などキャリアに負の影響を与え、かつ本質的な作業にかける時間を奪うため、トイルを削減するモチベーションがあります。なお Google の SRE は、トイルを 50 %以下に抑えるルールを運用しているそうです。先述の作業を考えると、たとえば、サーバの CPU 使用しきいち率を監視し、閾値を上回った場合(もしくは指定の閾値に収まるように)にロードバランサ下のサーバを自動的に増設する、オートスケーリングというアプローチを採ることができます注25 ~~。~~

SRE のプロアクティブなエンジニアリングの背景として、クラウド化を筆頭にインフラが抽象化され API でプログラマブルに操作可能になったことや、動的で柔軟にスケールするインフラの管理コストを抑えるためにソフトウェアによる自動化が重要になったことが関係していると筆者は考えています。カオスエンジニアリングは、繰り返し説明してきたとおり、自分たちのシステムに自信を持つための実践的なアプローチです。したがって、これは SRE のプロアクティブなアプローチの一種ともとらえられるでしょう。カオスエンジニアリングではシステムの定常状態を定義し、システムに失敗を注入したときの振る舞いを観察します。特定の失敗注入に対して、定常状態が変わらなければ、その失敗に対するシステムの回復力があるという自信を得ることができます。カオスエンジニアリングにおける定常状態は、 SRE における SLI/SLO の関係と似ています。定常状態の原則において、システムの内部属性ではなく、測定可能なシステム出力に焦点を当てることがあります。対して、良い SLI はユーザーの体験に連動するものとされており、考え方の類似性がうかがえます。また、 SLO の考え方は信頼性に対して守りの動き方だけでなく、攻めの動き方も促進させます。常に SLO を余裕で達成している場合、チームがリスクの高い変更をまったく取り入れていないととることもできます。リスクの高い変更とはたとえば、システムの大規模なマイグレーションです。組織のエラーバジェット注26 ~~の~~ ポリシーにもよりますが、エラーバジェットが余っている限りは、カオスエンジニアリングの実践が選択肢になり得るでしょう。

##### **カオスエンジニアリングの導入にあたって** 

ここでは、カオスエンジニアリングの導入前に考えたり知っておいたりすべきことについて筆者の考えを述べます。

#### **<u>導入対象は十分複雑か?</u>** 

「カオスエンジニアリングの原則」は広く定義されたもので、組織規模やシステム規模に限らず実行すること自体は可能です。ただし、創発的な複雑性がもたらされる分散システムを非常に意識しています。個人的には、大規模なマイクロサービスを運用している組織以外でカオスエンジニアリングを積極的に導入する動機は弱いと考えています。 DevOps や SRE の実践によるシステムの回復性や信頼性の向上に取り組み続けることはカオスエンジニアリングの前段階であり、多くの組織ではそれらで十分なのではないでしょうか。

#### **<u>導入タイミングは適切か?</u>** 

カオスエンジニアリングは、問題を早期に検出し修復できる可能性がありますが、本番環境で行う場合、ユーザーに迷惑をかける危険性という明確なデメリットがあります。したがって、周囲の納得を得ながら導入するために、そのタイミングは考慮すべきです。筆者が考える代表的な導入タイミングは 2 つあります。 1 つめは、予期せぬ問題が実際に発生した後です。これは Netflix 社の Chaos Monkey と同じパターンです。障害によりビジネスに損失が発生している場合、再発防止だけでなく、ほかの障害モードにも自信を持つ動機にもなり得ます。 2 つめは、新しいシステムの投入や、特大セールなどのビジネス上重要なイベントを控えている場面です。このようなタイミングでは不確実性を可能な限り減らすことが要求されるため、負荷試験などに加えて、実際に障害を注入して回復性を確認することも選択肢となります。いずれにしても、前述したとおり、もしオブザーバビリティが十分でない場合は先にオブザーバビリティを高めるべきです。

#### **<u>有用なスキルセット</u>** 

「カオスエンジニアリングの原則」では、実験対象をコントロールして爆発半径を抑えることや、定常状態を計測することが述べられています。実験の設計段階では、仮説を立てるために分散システムの基礎的な部分や、ネットワーク、データベースなどコンピューターサイエンスの知識が有用です。実験の作成では、ソフトウェアを実装したり活用したりする場合が多いため、一般的なソフトウェアエンジニアリングスキルがあるとよいでしょう。また、実験中に定常状態の観察や分析、アラートを上げるためのモニタリングやオブザーバビリティ、といった運用の知識が最低限必要になります。これらを総合すると、技術的側面としては、ソフトウェアエンジニアリングとシステムエンジニアリングを典型的とする SRE が近いと言えそうです。さらに、カオスエンジニアリングの高度な原則には「実験の自動化」があり、これも SRE の得意領域です。 SRE より広い目標を持った DevOps との相性も良いでしょう。技術面以外では、協調的なマインドセットや透明性を意識できるとよいでしょう。あなたがカオス実験を実施するエンジニアだとして、実験対象のチームから受け入れられなければ、組織としての成功から遠のいてしまいます。単に実験を行うだけの存在ではなく、ツールによるサポートや、教育的な協力が必要になります。また、実験自体やその結果と知見を組織に広く公開することも重要です。「障害がもし発生してしまったら」ではなく「障害に対してどう対処するか」という考え方を組織文化として根付かせることは、カオスエンジニアリングが果たす大きな役割だと考えています。

##### **まとめ** 

AWS をはじめとするパブリッククラウドが成熟し、自社で物理的なインフラを持たずにクラウド上でサービスを構築し提供する企業が一般的となりました。クラウドネイティブなアプローチは競争力を高めるという調査結果があります。クラウドネイティブの構成要素のひとつであるマイクロサービス化により、サービスを独立してデプロイできるアーキテクチャを構成し、サービスを自律的に運用できる組織を構築する事例をよく見かけます。マイクロサービスの運用では、分散システムの複雑性が、予期できない障害を引き起こしたり、その原因を突き止めることが困難な状況が発生したりします。このような状況下で事業成⻑を加速させるためには、本番環境で手を加えずにデバッグする能力であるオブザーバビリティの獲得や、頻繁かつ安全に変更をリリース可能な組織を目指す DevOps のプラクティス、信頼性を中心として開発運用する手法である SRE のアプローチが必要となります。さらに、未知の問題に対して仮説を立て失敗を起こす実験を行い、新しい知識を得るというプロアクティブなアプローチを Netflix 社が提唱しました。これをカオスエンジニアリングと呼びます。近年では、カオス実験を行うマネージドサービスや OSS が登場するなど、ソフトウェアエンジニアリングの世界において、カオスエンジニアリングは広く知られるものになりました。「カオスエンジニアリングの原則」を昇華させた継続的ベリフィケーションという新たなプラクティスが、カオスエンジニアリングの実用的手段として確立されつつあります。

#### **【参考文献】** 

- [1] Casey Rosenthal、Nora Jones 著、堀明子、松浦隼人訳『カオスエンジニアリング ─回復力のあるシステムの実践』オライリー・ジャパン、2022年6月 

- [2] "Principles of chaos engineering", https://principlesofchaos.org/ 

- [3] Netflix Technology Blog, "SPS: the Pulse of Netflix Streaming", <u>https://netflixtechblog.com/sps-the-pulse-of-netflix-streaming-ae4db0e05f8a</u> 

- [4] "The evolution of chaos engineering at Netflix", <u>https://d1.awsstatic.com/events/Summits/reinvent2022/NFX303_The-evolution-ofchaos-engineering-at-Netflix.pdf</u> 

- [5] DORA, "DevOps Research and Assessment", https://dora.dev/ 

- [6] Martin Fowler.com, "Microservices", 

<u>https://martinfowler.com/articles/microservices.html</u>

- [7] Susan J. Fowler 著、佐藤直生監訳、⻑尾高弘訳『プロダクションレディマイクロサービス ─運用に強い本番対応システムの実装と標準化』オライリー・ジャパン、2017年9月 

- [8] Charity Majors、Liz Fong-Jones、George Miranda 著、大谷和紀、山口能迪訳『オブザーバビリティ・エンジニアリング』オライリー・ジャパン、2023年1月 

- [9] Russ Miles, Chaos Engineering Observability, O’Reilly Media, Inc., 2019 

- [10] Betsy Beyer、Chris Jones、Jennifer Petoff、Niall Richard Murphy 編、澤田武男、関根達夫、細川一茂、矢吹大輔監訳、Sky株式会社玉川⻯司訳『SRE サイトリライアビリティエンジニアリング ─Googleの信頼性を支えるエンジニアリングチーム』オライリー・ジャパン、2017年8月 

###### 注2) データセンターをホストする地理的に離れた地域のこと。 

<u>https://docs.aws.amazon.com/ja_jp/AWSEC2/latest/UserGuide/using-regionsavailability-zones.html</u> (本文に戻る)

- 注8) 公開されている事例が次のページにまとめられているので、興味のある方はほかの例も見てみると発見があるかもしれません。 <u>https://github.com/ldomb/ChaosEngineeringPublicStories</u> (本文に戻る) 

注9) <u>https://www.youtube.com/watch?v=M7mjOdgTMb4、 https://techblog.yahoo.co.jp/entry/2023013030404798/</u> (本文に戻る)

- 注10) AZ(Availability Zone、アベイラビリティーゾーン)とは、リージョン内にある独立したデータセンターの集合単位のこと。 (本文に戻る) 

- 注11) Kubernertes上の最小のアプリケーション実行単位。 

   - (本文に戻る) 

- 注14) blast radiusの訳。影響範囲のことですが、障害の影響範囲を指す場合この言葉がよく使用されます。 

   - (本文に戻る) 

- 注16) あるコンポーネントが障害になったとしても、サービス全体が障害になることなく、そのコンポーネントを利用する機能以外は利用可能にすること。 (本文に戻る) 

- 注17) 自然と生じる複雑さや予期せぬ問題のこと。 

   - (本文に戻る) 

- 注25) 今ではごく一般的な手法ですが、説明のしやすさから例として挙げています。 (本文に戻る) 

- 注26) SLOから算出できる、期間内に損失可能な信頼性のこと。 (本文に戻る) 

立見祐介(たつみゆうすけ)ヤフー株式会社 <u>@cafenero_777</u>

**第 2 章**

### **不確実性に立ち向かうための段取りカオスエンジニアリングの進め方** 

**第2章では、カオスエンジニアリングを始めるにあたり、事前準備やシステム選定、仮設シナリオの作成をどのように進めればよいか解説します。ご自身の環境でカオスエンジニアリングを始める際の指標として、ぜひお役立てください。**

##### **はじめに** 

みなさんこんにちは! ヤフーのインフラを担当する立見祐介と申します。筆者は物理ネットワークインフラから IaaS を中心としたソフトウェアネットワークの設計、実装、そして運用までを幅広く手がけてきました。今回は、そんな経験を活かし、現在社内で推進しているカオスエンジニアリングプロジェクトのお話をします。さて、みなさんの担当するシステムが、今この瞬間に突如として障害に見舞われたとしたら自信を持って対処できるでしょうか? 本誌を手に取っているみなさんであれば、各種のテストやフェイルオーバー機構、メトリクスとアラートのしくみをすでに整えているでしょうから、こう問われても堂々とうなずく方も少なくないでしょう。しかしながら、モダンなシステムでは Kubernetes ( K8s )などの分散システムを利用するケースが増え、さまざまな外部システムが連携して動作することが常となりつつあります。それゆえに、自身のシステムが今現在、正しくデプロイされていて、意図どおりに機能していると自信を持って答えられる方は少ないかもしれません。筆者自身のシステム設計、実装、そして運用の経験からしても、この質問に対して 100 %の自信を持って「はい」と回答するのは容易ではありません。ふっしょくしかし、そんな不確実性を払拭する鍵となるのが、今筆者たちが取り組んでいる「カオスエンジニアリング」です。ヤフーではカオスエンジニアリングを実践し、システムの安定性向上に貢献してきました。本稿では、その経験をもとに、どのようにカオスエンジニアリングを進めればよいかを解説します。また、カオスエンジニアリングを進めるにあたり、ヤフーが何を考え、どう対応したのかも併せてお伝えします。みなさんが自身の組織でカオスエンジニアリングを実践する際、不確実性をコントロールし、システム安定性へとつなげる一助となれば幸いです。

##### **カオスエンジニアリングの事前準備** 

それではカオスエンジニアリングを実施するための準備を見ていきましょう。すでに第 1 章を読んだ方は、カオスエンジニアリングについてはなんとなくイメージが湧いたと思いますが、どのように実施すればよいかはまだ見当がつかないのではないでしょうか。筆者たちの場合は初めてのカオスエンジニアリングへの取り組みだったため、不明点や課題などをひとつひとつ明確化し解決しながら準備を進めました。

#### **<u>カオスエンジニアリングの進め方</u>** 

まずは大前提となるカオスエンジニアリングの進め方についてあらためて整理していきましょう。一番参考になるのは原典である「カオスエンジニアリングの原則( Principles of chaos engineering ) ~~」~~ <u>注1</u> ~~で~~ しょう。ただし、これだけでは実際の進め方がわかりづらいと思います。そこで、筆者たちがカオスエンジニアリングの進め方をステップバイステップで整理した際の図を **図** 1に示します。筆者たちが実践してきたカオスエンジニアリングも **図** 1に従っています。ここでは、カオスエンジニアリングを 3 つの大きなフェーズに分けています。

- 1 **準備フェーズ** :定常状態の定義と仮説シナリオの作成を行う 

- 2 **テストフェーズ** :仮説シナリオを実行し、対象システムや体制、またはシナリオ自体の改善を行う 

- 3 **継続フェーズ** :上記の結果の安定後、テスト実行の自動化を行い、継続的にこれらのカオスエンジニアリングのサイクルを繰り返すそれでは各フェーズごとに詳細を解説していきます。 

#### **1準備フェーズ:定常状態の定義と仮説シナリオの作成** 

最初に、「定常状態」( steady state )とは何か理解していきましょう。聞き馴染みのない、イメージしにくい言葉だと思いますが、簡単に言い換えると「安定して動いている状態」のことを指します。具体的には、システム全体の KPI が水準を満たしている状態や、たとえば HTTP レスポンスコード 200 が返信される割合が 99.9 %であったりレイテンシが 1 秒以内であったりといった個別の定量的な要素が条件を満たしている状態を指します。加えて定性的な観点からは、システムが冗⻑化されており、一部が壊れても意図した復旧機構が動作する状態か(フェイルオーバーするか)、障害発生時にアラートが作動する状態か、なども含まれます。次に、「仮説シナリオ」について見ていきましょう。これは具体的な障害シナリオを考え、それが起きた場合に対象システムがどのように動作するかをまとめたものです。たとえば、ある API に 10 秒間障害が起きた場合その API エンドポイントはほかにフェイルオーバーし、その結果サービスが一時的に中断して障害通知がメールや Slack などに通知される、といったシナリオです。実はこの仮説( hypothesis )という点がシナリオ作成を進めるにあたり重要です。というのも、この時点ではシナリオ作成者注2 ~~が~~ 対象システムの動作を理解注3 ~~し~~ ているかは不明だからです。そのため、まずやるべきことは現状の想定をありのままに書き出し、仮説シナリオの項目表をすべて埋めていくことです。

#### **2テストフェーズ:カオスエンジニアリン** 

#### **グの実践** 

次に、仮説シナリオを実際に実行します。これは「障害注入 ( Fault Injection )」「障害テスト実行」とも呼ばれます。このフェーズで最大限に注意すべき点は、まずは開発環境 ( development 環境)やステージング環境( staging 環境)などサービスに影響しない環境で障害テストを実行し、意図した動作結果をよく確認したうえで本番環境( Production 環境)で行うことです。なぜなら、実環境では想定外のシステム挙動が起き、予想以上に障害やサービスへの影響が出ることがあるからです。テストフェーズの実践後は、シナリオの作成者の認識と現実とのギャップを埋めていきます。仮説シナリオでの想定と、実際のシステムの挙動やアラート状況、 MTTD ( Mean Time To Detect :平均検出時間)、 MTTR ( Mean Time To Repair :平均復旧時間)の間に違いがあれば、それに基づいてシステム改修や対応ワークフローの改善を行います。場合によっては開発者や運用者の認識が間違っていただけのことがあります。その際は仮説シナリオの内容そのものを書き換えます。

#### **3継続フェーズ:カオスエンジニアリングのサイクル** 

カオスエンジニアリングはこれらをサイクルとして定期的に回すことが重要です。言い換えると、障害テストの実施と自動化を進めながら、「問題を発見し続ける経験学習のサイクル」を繰り返すことがカオスエンジニアリングと言っても過言ではありません。のちほど第 3 章で具体的な事例も紹介しますが、このサイクルを続けることで、本番環境の “ 変化 ” に気づくことができるので ~~す~~ <u>注4</u> ~~。~~ ◆ ◆ ◆

以上がカオスエンジニアリングの基本的なサイクルです。これらのステップを理解し、実際に適用することで、システムの動作や理解そのものの不確実性を減らし、システムの耐性を高めていくことができます。

#### **<u>チームや組織における準備</u>** 

カオスエンジニアリングをチームや組織として運用推進するにあたり、準備する項目を整理します。

#### **目的と評価指標の整理** 

すでに第 1 章で説明していますが、カオスエンジニアリングはこれまでの施策注5 ~~で~~ は改善しにくかったシステムを改善していく方法となります。そのため、従来の KPI だけではカオスエンジニアリング全体をカバーすることは難しいと筆者は考えています。また、各組織、対象システム、開発フェーズなどによって、最適な KPI は異なるでしょう。ここではその指標のヒントとなるアプローチを 2 つ紹介します。

1 つめのアプローチは、カオスエンジニアリングの準備と実践中に改善できた事項すべてを評価対象とする、というものです。具体的には、未知の問題が明らかになったこと(仮説シナリオとの大きなギャップを見つけたこと)や、問題発見から解決までの時間

( MTTD や MTTR )の短縮、アラート状況や対応ワークフローの改善を数字として置き換えて追っていくものです。また別のアプローチとして、ダウンタイムによる機会損失やブラきそんンドの毀損、また過去のインシデントの再発防止観点から KPI を設定することも考えられます。この視点では、おもに想定される機会損失の観点からそのインシデントが起きた場合の毀損コストとして算出し、それを追っていくものになります。しかしながら、筆者の考えでは、カオスエンジニアリングの真の評価値はエンジニアの「安心/安全」にあります。これは具体的な数値で測るのは難しい部分だと思いますが、たとえばユニットテストと同じように、本来ならばカオスエンジニアリングが実施されていないと不安に感じるべきかもしれません。常にシステムが想定どおりに動作しているという確証と安心感は開発者や運用者にとって重要です。なぜなら、システムが安全に動作していることの確証を持っていれば、意図しない動作の防止(インシデント防止)ができるだけでなく、開発スピード( Verocity )を安定的に向上させることができるはずだからです。カオスエンジニアリングの定量的な効果としては測りにくいものですが、実際は十分に体感できるものだと筆者たちは考えています。

#### **ツール選定** 

カオスエンジニアリングにおいて、障害テストを実行するツールは OSS から商用ソフトウェア、 SaaS までさまざまあります。たとえば、ソフトウェアとしては Chaos Monke ~~y~~ <u>注6</u> ~~や~~ Chaos Mes ~~h~~ <u>注 7</u> ~~、~~ Pumb ~~a~~ <u>注8</u> ~~、~~ Gremli ~~n~~ <u>注9</u> ~~な~~ どがあります。また、 CNCF ( Cloud Native Computing Foundation )ではさまざまなツールを活発的に開発しているようです注10 ~~。~~ パブリッククラウドでは Chaos Engineering as a Servic ~~e~~ <u>注11</u> ~~と~~ してカオスエンジニアリングがサポートされています。ご承知のとおり、ツールはあくまで手段であり、カオスエンジニアリングの本質ではありません。重要なのは対象システムで仮説シナリオを実行し、ギャップを認識することです。とはいえ、各種ツールは非常に便利ですので、利用者や組織に合った使いやすいものを選ぶとよいでしょう。ここでは各ツールの詳細説明はせずに、特徴や検討項目の簡単な紹介にとどめます。

- **・テスト対象のサポート:** ホスト環境(物理サーバやVMなど)、コンテナ環境(Docker/K8sなど)、アプリケーション 

   - (Application-level Fault Injection)などをサポートしているか 

- **・** OSS **か商用か:** サポート状況や利用料の違い 

- **・実行方法や管理方法:** YAML管理やWeb API連携による管理、パブリッククラウド上での各種リソースやダッシュボードとの連携など、自分たちに合った使い方ができるか 

- **・自動停止機能の有無:** テストそのものが安全に動作しなくなった場合に備え、自動でテストを停止する機能(デッドマンスイッチ)があるか。ない場合は止められる手段を自分たちで準備できるか 

- **・レポーティング機能:** 利用状況やシナリオ実施結果のレポートに求める充足性があるか 

#### **<u>対象システムの選定</u>** 

システム運用の観点から、カオスエンジニアリングは不確実性が大きいシステムに導入すると効果が大きいと言えます。具体的には次のような観点から、導入するシステムを選定するとよいかと思います。

- ・より安定した動作が求められるシステム 

- ・複雑なシステム 

- ・日々進化する(変化する)システム改善が必要なシステムや現行の運用方法ではなかなか安定しきれないシステム、さらなる安定化が求められるシステムに対して、既存の施策に加えてカオスエンジニアリングを導入することは効果が大きいと考えられます。また、 K8s に代表される分散システムや、外部と連携するシステムのように、人間がシステムの動作をすばやく把握するのが難しいシステムもカオスエンジニアリングの導入を検討すべき候補だと考えられます。さらに、日々進化し、アジャイル開発などの手法を用いたすばやいバージョンアップや変更が繰り返されるシステムもカオスエンジニアリングが有効だと考えられます。逆に言えば、ウォーターフォール型などの開発手法でガッチリと仕様に沿って開発された変更の少ないシステムに対しては、カオスエンジニアリングのメリットはそれほど見込めません。なぜなら、そのようなシステムは途中で仕様変更や改修が行われることが少ないため、あらためてカオスエンジニアリングを実施しても仕様と現実のギャップは小さいと想定されるからです注12 ~~。~~ 

また、ビジネスの観点からカオスエンジニアリングを始めることも考えられます。たとえば Netflix 社はクリスマスイブに大規模障害を引き起こしてしまったことから、カオスエンジニアリングを本格的にスタートした背景があります注13 ~~。~~

このように、各組織にはシステムの安定性に関してビジネス的な背景や課題があるはずです。そのため全社横断的なビジネス目線で重要なシステムを選定するのも良いアプローチだと言えます。また、そのような不確実性が大きいシステムは、すでにシステムのメトリクスやログ/アラートなどの監視および可視化がなされているかと思います。カオスエンジニアリング実施にあたっては、これらを指標として仮説シナリオの作成と実施を行います。このようにシステムが監視/可視化されていることはカオスエンジニアリング実践にあたり前提条件となります。そのため、カオスエンジニアリングの準備を進める中であらためて監視状況を整理し、仮説シナリオを作成していくのも良い方法の 1 つだと思います注14 ~~。~~

##### **仮説シナリオの作り方** 

さて、ここまではカオスエンジニアリングの進め方について紹介しました。続いて、選定したシステムに導入する仮説シナリオの作成を始めましょう。

#### **<u>仮説シナリオ作成</u>** 

仮説シナリオの具体的なまとめ方や表記方法はさまざまなバリエーションがあるかと思います。 **表** 1のような仮説シナリオの項目表を作成し、仮説シナリオの作成を進めていくとよいでしょう。この項目表はヤフー社内で実際に使っているものです。まずは、過去に故障した箇所や類似するインシデントに着目し、それらの中から 1 、 2 個を選び、項目表を順に埋めていくとスムーズに進められると思います。この項目表は、仮説シナリオを大きく 2 つの側面に分けて作成します。

- **・前半部分:** 何が、どのように壊れるか 

- **・後半部分:** それによりSLIがどのように変化するか、またはどのようにアラート通知/対応されるかこのような項目表を利用することで、障害内容やそれらが起きた場合の影響内容をもれなく記入/整理できます。また、カオスエンジニアリング実践状況が一覧できるため、周囲に説明するときも活用できます。繰り返しになりますが、これらの各項目内容は作成時点ではあくまで “ 仮説 ” です。実際には障害を引き起こしてこれらの仮説が正しいかどうかを確認していきます注15 ~~。~~ 

こうした仮説シナリオを作成する過程では、実際の開発環境で予備検証を行いながら、細かな挙動を確認し、仮説シナリオ一覧表を完成させていくことをお勧めします。システムの動きを理解し、 MTTD 、 MTTR 、アラートなどを整理/再評価する機会となるため、逆説的ではありますが、仮説シナリオのまとめ上げ自体が安定した運用に貢献すると考えられます。

#### **<u>仮説シナリオ作成の注意点</u>** 

仮説シナリオの作成には、障害時のシステムの挙動まで含めた実システムに対する詳細な理解を必要とします。これは一筋縄ではいかない作業です。システムの全体設計や実装に至るまでの深い理解がなければ、仮説シナリオの作成は困難でしょう。この課題に対しては、 2 つのアプローチをお勧めします。

1 つめは、そのシステムに詳しい人(キーパーソン)に仮説シナリオを作成してもらうという方法です。システムの詳細を理解している人は、障害時の挙動だけでなく、システムの潜在的な弱点注16 ~~も~~ 考慮した仮説シナリオを作り上げることができるからです。 2 つめのアプローチは、過去の実際に起きた障害注17 ~~を~~ もとに仮説シナリオを作成する方法です。この方法ならば、対象システムに対しての深い理解がなくとも、仮説シナリオを作成できます。さらに、過去の障害の再発防止策にもなり得ますので、この点は 1 つめのアプローチにはない大きな利点となります。前者はトップダウンのアプローチ、後者はボトムアップのアプローチとも言えるでしょう。実際には両方のアプローチを組み合わせて進めていくとよいでしょう。どのような方法で仮説シナリオを作成しても、システムの挙動が不明で埋められない項目が出てくることはあるかと思います。その場合でも、項目自体は暫定でもよいので埋めておくことが重要です。なぜなら、予備試験や実際のカオスエンジニアリングの作業を通じて、そのギャップが明らかになり、それによりシステムの適切な理解やシステム改修へとつながる可能性があるからです。さらに、どの種類の障害を起こすかという観点とは別に、 Blast Radius (システム障害範囲やユーザー影響範囲)を考慮に入れる必要があります。同じ障害シナリオでも、障害の深刻度合いや範囲によって障害の影響範囲が大きく変わることがあるからです。たとえば、特定の API が利用不可の時間が 1 秒と 1 分とでは、フェイルオーバーの動きも異なり、結果としてユーザーへの影響や SLI が大きく異なります。そのため、たとえば **表** 2のように同じシナリオであっても負荷をかける時間やその対象の数をレベル分けし、まずは小さな Blast Radius からテスト実行を始めるというアプローチをとるとよいでしょう注18 ~~。~~

このように、仮説シナリオ作成作業はなかなか大変である(工数が大きい)ことがわかります。そのため、仮説シナリオ自体もある程度テンプレート化し、各システムの担当者が再利用をすることで、これらの工数を低減することが見込めます注19 ~~。~~

◆ ◆ ◆

ここまで仮説シナリオの作成方法について紹介してきました。「カオスエンジニアリングの進め方」でも説明したとおり、カオスエンジニアリングで重要なのは実際の本番環境での実施、そして仮説シナリオとのギャップを埋めていくことです。そのため、大量の仮説シナリオを作成してしまい、それらを実行に移すことが難しくなってしまうという状況は避けたいところです。とくにカオスエンジニアリングに初めて携わる方は、まずは限定された数の仮説シナリオに絞り込み、それらを深く、そして継続的に実行することをお勧めします。そうすることで、カオスエンジニアリングの実践を通して、システムの安定性が徐々に向上していくことを実感できるはずです。

#### **仮説シナリオを活用したシステム動作の予備検** **<u>証</u>** 

仮説シナリオをもとにしたカオスエンジニアリングを本番環境で行う前に、必ず開発環境やステージング環境での詳細な動作検証をしておきましょう。ここで重視したいポイントは次のとおりです。

- ・障害を発生させるための手順をしっかりと確認する 

- ・必要に応じて復旧手順を準備/確認する 

- ・仮説シナリオ実行を緊急停止する基準(ロールバックする基準) を事前に決めておく 

- ・想定どおりの障害の規模になっているか、MTTDとMTTRがおおむね想定どおりであるかを確認する 

- ・監視すべき項目がすべて設定されているか、そしてアラートが確実に発火するかを確認する仮説シナリオを作成する前後で、これらの確認を何度も行うことになると思います。これらの一連の確認作業を通じてシステムの動作に対する理解が深まり、問題が生じた際の対処法も自然と身につくはずです。対処法が身についた状態で、自信を持って本番環境でのカオスエンジニアリングを実践していきましょう。注2) シナリオ自体も自動で作成させる、という研究もあるそうです。 <u>https://dl.acm.org/doi/10.1145/2987550.2987555</u> (本文に戻る) 

- 注3) 人間の理解ではなく、AI技術によってシステムを自律的に運用させるアプローチも研究されています。 <u>https://www.janog.gr.jp/meeting/janog51/aiml/</u> 

- (本文に戻る) 

- 注4) 対照的なものとしてリリース試験があげられます。リリース試験はリリース時点でのシステム動作の担保は可能ですが、リリース後(システム改修や外部システムの変更後)の動作を担保するものではありません。 (本文に戻る) 

- 注5) CI/CD、自動化、オブザーバビリティ向上、ペアプログラミング/モブプログラミング、マルチリージョンやマルチアベイラビリティーゾーンの分離、カナリアリリースなど。 (本文に戻る) 

注11) AWS Fault Injection SimulatorやAzure Chaos Studioがそれにあたります。 <u>https://aws.amazon.com/jp/fis/ https://azure.microsoft.com/ja-jp/products/chaos-studio</u> (本文に戻る)

- 注12) それでも、筆者はこのようなシステムに対してもカオスエンジニアリングを実践する価値はあると感じています。なぜなら、どのようなシステムであっても、「リリース後の仕様変更はない」「外的要因で障害は起きない」ということはあり得ないと考えているからです。 

   - (本文に戻る) 

- 注14) 本誌の読者であるみなさんならば、基本的な監視はすでにバッチリ取り組んでいると思います。もし足りない監視項目などがあれば、カオスエンジニアリングに取り組む中で併せて拡充していくとよいでしょう。 

   - (本文に戻る) 

- 注15) 仮説シナリオを立てずに障害を起こしてしまうとシステム状況や変化を追いにくくなり、本当に“カオス”になってしまいます! (本文に戻る) 

- 注16) システム設計や実装/運用を行った方であれば、そのシステムの弱点の1つや2つはすぐに思いつきますよね? 

   - (本文に戻る) 

- 注17) CPUへの高負荷やディスク容量の枯渇、サーバダウンやTLS証明書の期限切れなど、一般的によく起こり得ることだけで仮説シナリオを作成しても、十分にシステムの安定性/可用性を向上することができると思います。 (本文に戻る) 

- 注18) 繰り返しになりますが、Blast Radiusを小さくし過ぎて困る、ということはありません。いきなり大きなBlast Radiusでカオスエンジニアリングを行ってしまうと思わぬ挙動を引き起こしてしまい、本当に“カオス”な状況となってしまいます。 (本文に戻る) 

- 注19) ヤフー社内でいくつかのサービスに対しカオスエンジニアリングを実践した際、一番時間(工数)がかかったのが仮説シナリオ作成です。作成に苦労したという声も上がりました。そのためカオスエンジニアリングプロジェクトでは各サービスのシナリオを収集し、それらをテンプレート化して工数削減を推進しました。 (本文に戻る) 

立見祐介(たつみゆうすけ)ヤフー株式会社 <u>@cafenero_777</u>

# **第 3 章** 

##### **「継続」こそ成功の鍵** 

### **カオスエンジニアリングの効果と学び** 

**第3章では大規模システムにおける事例をもとに、カオスエンジニアリングをどのように進めたのか、その結果どのような効果があったのかを紹介します。カオスエンジニアリングがシステムだけにとどまらない取り組みであることがきっとわかるはずです。**

##### **はじめに** 

第 1 章でカオスエンジニアリングの概要を説明し、第 2 章でカオスエンジニアリングの導入について紹介しました。カオスエンジニアリングは具体的にどのような手法を指すのかつかんでもらえたかと思います。第 3 章では、現場におけるカオスエンジニアリングの実際と、カオスエンジニアリングの実施で得られた効果や学びについて、筆者の経験をもとに解説します。その中で筆者が直面した課題や、それらに対して何を考え、どう対応したのかも併せてお伝えしていきます。

##### **カオスエンジニアリングの準備** 

#### **<u>導入目的とシステム選定</u>** 

まず、第 2 章で説明したようなカオスエンジニアリングの進め方に沿って準備を進め、システム選定と仮説シナリオ作成を行います。ここで、カオスエンジニアリングを導入するにあたって、システム選定の条件をおさらいします。

- ・より安定した動作が求められるシステム 

- ・複雑なシステム 

- ・日々進化する(変化する)システム以上の条件に加え、実際に社内で開発/運用しているシステム状況やビジネス要件を考慮し、筆者たち(ヤフー)ではシステム選定の条件を次のように考えました。 

- ・ Kubernetes(K8s)のような分散システムや、多数の外部システムと連携して動作するシステムなど、全体の動作を正確に把握することが難しいシステム 

- ・高いSLA(Service Level Agreement)が求められるシステムや、 SLO(Service Level Objective)の改善が求められるシステム。とくに、ビジネスイベントや突発的なアクセスの増加にも耐えられる必要があるシステム注1 

- ・マルチアベイラビリティーゾーン(AZ)の物理インフラで動作するシステム 

- ・開発初期フェーズではなく、機能拡張や改善フェーズ、またはシステム刷新のタイミングにあるシステム 

K8s やマルチ AZ 構成など設計段階から分散構成を取り入れたシステムは高い安定性を維持できることが期待できます。しかし実際に本番運用を行うと想定外の挙動をすることがあります。ヤフーでもインシデントが起きてから弱点に気づいた、ということが実際にありました。そのため、筆者たちはこのような「本番環境でなければ気づけない課題を抱えている」システムを対象として選定を進めました。その結果、ヤフーでは、カオスエンジニアリングの対象を

IaaS ( Infrastructure as a Service )と PaaS ( Platform as a Service )に限定し、まずは PoC ( Proof of Concept )を進めることにしました。この PoC の中で仮説シナリオを作成し、開発環境で実際にシナリオを実行して、どのようにカオスエンジニアリングを進めていくかを順次学びました。とくに次の 3 点は、実際に手を動かしてあらためて注意する必要があると感じた項目になります。

- ・小規模な障害が予想以上の影響を及ぼす。たとえば、IaaSの特定のAPIにわずかな遅延(数十ms)を導入すると、エンドユーザー向けのAPIの応答時間が秒単位で遅延することがある 

- ・ MTTDやMTTRが思った以上にかかることがある 

- ・システム動作やアラート状況がそもそも明確でない場合もある。そのような場合は具体的なシナリオを作成し、開発環境で予備検証(動作確認)を行い現状を明確化するこの分析をもとに、ヤフーでは初めてカオスエンジニアリングを実施する対象として、 PaaS システムにフォーカスすることにしました。これは、多くのユーザーが利用するために高い SLA が求められるプラットフォームであり、システム刷新(マルチ AZ 対応)のタイミングで導入しやすく、そしてシステムの利用者数が今後大きく伸びる見通しがあった、という理由からです。 

#### **<u>社外の知見を学ぶ</u>** 

初めてカオスエンジニアリングを行う際は、他社の事例を参考にすると、より良い取り組みになると思います。たとえば、 Netflix 社のカオスエンジニアリングについての論文や書籍注2 ~~、~~ Chaos Conf 2020 の発表内容注3 ~~、~~ さまざまな会社のインシデント調査事例注4 ~~な~~ どが参考になると思います。筆者らも、これらの方法論や事例を参考にしつつ、ヤフーの運用状況や自身の経験と照らし合わせて取捨選択しながら取り入れました注5 ~~。~~ 資料の内容の中で、より安全に施策効果を向上させる観点から筆者がとくに重要視したのは次の点です。

- ・ Blast Radius(システム障害範囲やユーザー影響範囲)を慎重に広げながら仮説シナリオを拡充する。たとえば、障害範囲を Pod、API、Node、AZ、Regionのように徐々に広げていくことで、意図しない大きな障害を防ぐ 

- ・日中帯(対応できるエンジニアがいる時間帯)に実施することで、万が一の場合はすぐに対応できるようにしておく 

- ・カオスエンジニアリングの作業中に意図しない動きや想定以上の影響が出ることも想定しておく 

- ・ GameDa ~~y~~ <u>注6</u> ~~は~~ 施策の効果が見込めるため、事前に関係者と中止条件(想定外の事象発生時にロールバックさせる判断基準)を相談したうえで行う注7 

##### **実践カオスエンジニアリング** 

#### **<u>障害試験の概要</u>** 

前述のとおり、ヤフーでは PaaS システムをカオスエンジニアリングの対象としました。障害内容は、万が一の場合におけるサービスへの影響の度合いを考慮し、週に一度の Worker Node ダウン試験と、四半期に一度の AZ ダウン試験を行うというものです。

Worker Node ダウン試験では、 Worker Node が 1 台突然ダウンした場合でも、 PaaS システムがサービス提供をし続けられるかをテストします。この試験では Worker Node が急にダウンした場合でも、 PaaS システムが SLA を満たしながらサービスを提供し続けられるかどうかを確認します。また、四半期に一度行う AZ ダウン試験では、マルチ AZ 構成の中で 1 つの AZ がダウンした場合でも PaaS システムが想定どおりに動作するかを確認しました。つまり、通常時は複数 AZ をまたいで稼働しているシステムについて、 AZ1 つ分の Worker Node (約 400 台)を疑似的に落とすことで、システムの動作の確認を行ったということになります。筆者たちはこの AZ ダウン試験を GameDay として実施しました。この際には PaaS システムの担当者だけでなく、 IaaS などインフラ関係者やカオスエンジニアリングプロジェクトメンバーなど関係者全員が集結します。試験自体は、ツールやスクリプト、場合によっては手動操作を使って、事前に設計したシナリオに基づいて実行します。そして、試験終了後には、システムを適切に復旧させ、 SLI が正常かどうかを確認します。より詳しい内容については以前筆者が登壇した際の発表内容注8 ~~や~~ Yahoo! JAPAN Tech Blog の記 ~~事~~ <u>注9</u> ~~を~~ ご参照ください。

#### **<u>実施後にわかったシステムの改善点</u>** 

前述のとおり、筆者たちは仮説シナリオを実施する中で、 MTTD 、 MTTR 、監視状況など細かい差異だけでなく、シナリオの作成時には気づかなかったシステムの状態が把握できました。さらに、当初想定していたシステム起因の課題に限らず、人や組織の問題点も発見しました。おもな問題点は次のようなものです。

- ・デプロイの設定ミスによる想定外の動作。その結果本来期待したように動作しなかった 

- ・設計と運用の認識のずれ、および開発環境と本番環境とのずれ。とくにMTTDやMTTRは予想と大きく異なり、通知システムの処理に想定以上の時間がかかる場合もあった 

- ・メンバーの習熟度のずれ。復旧手順や復旧後の状態の想定などに個人差があったこのように、実際に本番環境でカオスエンジニアリングを行うことで、意外な事象を数多く発見できました。当初、本番環境でカオスエンジニアリングを行う効果に半信半疑だったメンバー注10 ~~も~~ 、初回でこのようなミスを見つけられたため、効果が確信に変わりました。また、システム修正後も本番環境でのカオスエンジニアリングを継続することで、以前起きた事象は起きない、と言い切ることができるようになりました。 

##### **カオスエンジニアリングで得られた学び** 

ここでは、カオスエンジニアリングの実施後にわかった改善点と、その際に得た学びなど、詳細について事例ベースで紹介します。とくに、システムそのものと、それを支える現場や組織に焦点を当てて進めていきます。

#### **<u>システムの舞台裏や想定とのずれ</u>** 

カオスエンジニアリングを実践し、対象システムが意図どおりに動作することはとても気持ちが良いものです。とかく、システムは正常に動くことが当たり前だと思われますが、耐障害性を向上させるためにさまざまな工夫や技工を施したシステムが、正常系も異常系も確かに設計どおりに本番環境で動いている、ということを確認みょうり /担保できるのはシステム設計者/運用者冥利に尽きます注11 ~~。~~

一方で、想定より MTTD に時間がかかることもあります。実際にダッシュボードの反映が遅い、アラートが飛んでくるまで思ったより時間がかかる、などといった現象も発生しました。とくに、カオスエンジニアリングや GameDay の作業中には、実際よりも時間が⻑ く感じる場合もあります。だからこそ、作業開始時間、サービスへの影響開始と終了時間、アラートの受信時間などを記録し、時間軸に沿って整理することで、事象を客観的に把握することが大切です。また、作業中に思いがけない動きが発生し、焦りを感じる場面もあるでしょう。アラートがなかなか飛んでこない、 Pod が生成されない、切り替わらないといった状況に直面したときです。そのような場合でもあらためて冷静に対応することが重要です。筆者の場合は声を掛け合いながら作業をやりなおすなどしました。そしてカオスエンジニアリングの継続フェーズに入り、 5 回目の AZ ダウン試験で初めてサービスへの影響が出た際に筆者たちは作業を即座に停止し、作業の切り戻しを行いました。この出来事では、カオスエンジニアリングを継続することがいかに重要か関係者全員が再認識しました。つまり、「これまでと同じやり方でやってきたから」という安心感は、ある程度の期間は有効ですが、同時に、同じ方法が通じなくなりインシデントが発生することも起き得る、ということです。カオスエンジニアリングを継続的に実践することで、本番環境の変化 (システム修正やユーザー利用方法、またその規模など)に気づけること、これが筆者たちヤフーでのカオスエンジニアリングプロジェクトの最大の知見となりました注12 ~~。~~

#### **<u>現場での実感</u>** 

**地味で「普通の作業」**

実際にカオスエンジニアリングを実践してみると、その作業は思った以上に地味であり、普通の運用作業と大差ありませんでしたが、この地味さこそが望ましい状況だと考えられます。なぜなら、通常時にうまく実行できない作業やシステム動作は、緊急時にもうまくいかない可能性が高いからです注13 ~~。~~

また、テスト作業は坦々と進み、実際の Worker Node ダウン試験は、ジョブを追加する感覚に近いものでしたし、事実、これは単なる Jenkins のジョブとして実行されていました。また AZ ダウン試験の場合も、通常の本番システムのメンテナンス作業と同じように進行しました。定期的にシナリオの実行サイクルを回せるようになると、その実行自体に慣れてしまい、気にもとまらなくなりました。実際の本番環境でシステムの動作確認が継続的に行われるというのは、非常に理想的な状態だと思います。このような作業は、システム運用者にとってはごく普通の運用作業です。当然ながら運用者のスキルや経験が問われます。今回ヤフーでカオスエンジニアリングに関わったメンバーは、システム設計初期の段階から携わってきたベテランが多く、また仮説シナリオの作成や事前のシステム理解、 GameDay の準備なども入念に行いました。そのため、これまでの経験が暗黙のうちに活かされ、結果的にテスト作業が “ 地味 ” なものになったのかもしれません。

#### **GameDayの雰囲気** 

初めての GameDay で AZ ダウン試験を行った際に、作業中の雰囲気の重要性を感じました。 GameDay には参加する関係者が多いため、試験中に意見を言いにくい雰囲気があったように思います。カオスエンジニアリングでは、システムが予想外の動作をした場合、その事象にいち早く皆で気づくことが重要です。しかし、これらの動作の原因が何であるか、誰がいつシステム改修を行ったのかを追求する際に、ともすれば犯人探しが始まってしまい不穏な空気になりがちです。このような状況は、カオスエンジニアリングや GameDay のオープンな環境で原因を追求する趣旨とは異なります。筆者たちも初めての GameDay であり、関係者同士が初対面の場合も多く、話し合いにくい雰囲気があったのも事実です。そのため、 GameDay 実施のフィードバックの時間を設け、 2 回目以降の GameDay ではよりカジュアルな形式でコミュニケーションを活発に行いました注14 ~~。~~

#### **当日のトラブル対応** 

GameDay 当日の作業直前に、仮説シナリオの影響範囲が予定よりも広いことに気づいた事例がありました。この影響範囲は事前の社きゅうきょ内アナウンスから漏れていたため、その日の試験は急遽延期することにしました。多くの関係者の予定を合わせて計画化した GameDay であっても、予期せぬインシデントが発生する可能性があるならば強行せずに、たとえ当日の実施直前であっても予定を変更するのは正しい判断だと考えます。なぜならカオスエンジニアリングはあくまで計画化された仮説シナリオの範囲(テスト対象やその内容、またそれらの影響を受けるユーザーなど)で実施することが大切で、「えいや」で障害を注入することはカオスエンジニアリングとは言えないからです。事実、もし直前の準備や確認作業を怠っていたら、意図せずしてサービスへの影響が広範囲に出ていた可能性があります。この経験から、すばやく異変に気づき意思決定するためには普段から計画や準備/確認を実践することが重要だと感じました。それと同時に、本番環境(=向こう側に多くのサービスやユーザーがい

###### る)で作業をすることの責任感や緊張感を忘れないことが重要であると再認識しました。 

#### **そしてデフォルトカオスへ** 

今回、社内で初めてのカオスエンジニアリングの取り組みとなるため、サービス稼働中の既存システムでの実施は思った以上にハードルが高かったのが実情でした。とくに、本番環境でカオスエンジニアリングを定常的に実践することは非常に勇気ある判断が必要でした。そのため、新規に立ち上げた PaaS システムを対象に、リリース当初からカオスエンジニアリングを実施することにしました。この方法で大切なのは、 PaaS システムは「定期的にカオスエンジニアリングを実施すること」をシステムのユーザーに事前に伝えることです。これにより、 PaaS システムの利用者(アプリケーションそご開発担当者)と運用状況とで認識の齟齬を減らすだけでなく、開発者にアプリケーションを冗⻑化するように強く促せました。まさに “ デフォルトカオス ~~”~~ <u>注15</u> ~~な~~ 状況を作り出すことで、システム自体の安定性向上だけでなく、システムを使うアプリケーションの安定性向上にも寄与するような環境を作り上げました。

#### **<u>相補的なアプローチの重要性</u>** 

筆者たちがカオスエンジニアリングを始めた当初は、組織のトップからカオスエンジニアリング推進を提言することで、本番環境での仮説シナリオ実施などさまざまな作業が非常に進めやすくなると考えていました。実際問題として本番環境でインシデントが起きた際の責任問題もあるため、トップダウンで推進するほうがやりやすいと思う局面が多いと感じます注16 ~~。~~ また、筆者たちの取り組みではシステム改善の一環としてカオスエンジニアリングをトップダウンで取り組んだことも事実です。しかし、実際にカオスエンジニアリングを実践してみると、トップダウンでの意思決定だけでなく、ボトムアップのアプローチも重要であることがわかりました。これらのアプローチを行ううえで、組織/人とのコミュニケーションは欠かすことができません。たとえば、仮説シナリオの作成やギャップを埋める際には、設計者や運用者と積極的に会話や議論を行い、システム設計およびその意図を正しく把握する必要があります。筆者たちは初回の GameDay の際、カオスエンジニアリングプロジェクトメンバーだけでなく、関連するシステム担当者たちと一緒に予備検証や作業方法、切り戻し方法といった詳細な準備を行い、ワンチームとして実践しました。このような現場でのコミュニケーションを通じた開発/運用の知見を活用することは、組織としてカオスエンジニアリングを進めるうえで必要不可欠なのです。そして何より、これらの取り組みが成り立つ背景には、「普段のシステム設計/運用業務をすべて自分たちで行っている」という筆者たちの自負があります。カオスエンジニアリングの実践と、普段の業務には共通する部分が数多くあるため、「私たちならきっとやれる」と自信を持って取り組むことができました注17 ~~。~~

このように、カオスエンジニアリングは、技術的な側面だけでなく、組織や人とも深く関わる取り組みと言えます。

##### **カオスエンジニアリングを活かす、その後のステップ** 

カオスエンジニアリングの実践を経た後、重要となるのは得られたフィードバックを的確に反映し、その結果をもとにカオスエンジニアリングのサイクル注18 ~~を~~ 回し続けることです。ここでは、具体的な効果として、ヤフーにおけるカオスエンジニアリングの実践結果と、その後の取り組みの広がりについて紹介していきましょう。

#### **<u>継続への工夫</u>** 

カオスエンジニアリングの活用により、システム設計の盲点や設定漏れ、監視の不足などが明らかになることはすでに触れたとおりです。加えて、システムだけでなく、組織としての動きにも改善点が見つかることでしょう。ヤフーでは次のような点に着目し、次回の実施に向けて準備を進めてきました。

- **・カオス実施の時間軸での整理:** 具体的に何にどれだけの時間がかかったかをタイムラインにまとめ、MTTDやMTTRなどのシステムの振る舞いや作業者の動きが意図どおりかを客観的に評価する 

- **・** GameDay **のブラッシュアップ:** GameDay当日のスムーズな進行や、意見を出しやすい環境作りなどを行う 

- **・システム再評価の視点:** カオスエンジニアリングを通じて得たフィードバックをもとに、運用の判断や対応が適切か、また、システムの実装や監視をどう改善すべきかを客観的に判断する注19 

- **・カオスエンジニアリングの継続的実施:** 障害注入や状況確認、動作確認を自動化(ツール化、スクリプト化)し、再現性良く繰り返せるように工夫する 

#### **<u>社内全体への情報共有</u>** 

カオスエンジニアリングの成果は、関係者だけでなく、プロジェクトメンバーやシステム利用者にも周知することが重要です。カオスエンジニアリングの実施は極論を言えば “ ユーザーが利用する稼働中のシステムを落とすこと ” です。これを許容してもらいつつ、システムの安定性向上に向けて互いの信頼感を築いていく必要があります。ヤフーでは次のように工夫することで、カオスエンジニアリングを具体的にどのように実施し、その結果がどうだったかをできるだけ数値を交えて共有しました。

- **・** Slack **チャンネルでの周知:** リアルタイム二結果を共有するため、利用者向けのSlackチャンネルなどを活用した 

- **・社内ポータルページでの公開:** 定常実施のカオスエンジニアリング結果を定期的に報告し、全社員がアクセス可能な状態にすることで、互いのシステムに対する理解を深めることに努めた 

##### **ヤフー社内における取り組みの広がり** 

ここでは、カオスエンジニアリングの実施による、システム以外の面での具体的な効果を紹介します(本節は性質上事例紹介となります)。

#### **カオスエンジニアリングの取り組みに対する評** **<u>価</u>** 

ヤフーでは、 Worker Node ダウン試験を週に一度、 AZ ダウン試験を半年に一度程度を目安に継続的にカオスエンジニアリング実践を行ってきました。前述のとおり、 AZ ダウン試験は 4 回目まではサービスに影響をまったく起こさず意図どおりのシステムが動作したのですが、 5 回目のときに初めて想定外の挙動が発生し、サービスに影響が出てしまいました。このとき、現場のエンジニアからは「見つかるべくして見つかった、良かった ! 」「まさにカオスエンジニアリングの効果だね ! 」といった言葉をもらいました。つまり、カオスエンジニアリングの施策効果をまさに目の前で実感し、これまでの設計や運用方法をもう一段向上させることができると確信した瞬間だったのです。また組織面からも、カオスエンジニアリングによって、未知のバグによるインシデントが GameDay の最中に見つかったことが評価され、部門賞を受賞できました注20 ~~。~~

このような現場の開発者や運用者の理解が社内のカオスエンジニアリング推進につながったことは言うまでもありません。他方、こうした挙動は本来であれば開発環境でのカオスエンジニアリング実践時に見つかるべき挙動であったため、 “ どのように本番環境でカオスエンジニアリングを実践していくか ” という運営的な課題が残りました。そのため、現在はカオスエンジニアリングの進め方を見直し、組織全体のプラクティス拡大に取り組んでいます。

#### **<u>カオスエンジニアリングへの関心増加</u>** 

当初は1つのシステムのみを対象として始まったカオスエンジニアリングの取り組みですが、成果が認められると同時に社内のエンジニアから試してみたいという声が増え、徐々に社内全体に広がりました。

#### **社内事例1** 

社内のあるサービスでは比較的大規模なシステムを開発/運用しており、すでにさまざまなシステム安定化施策を実施していました。しかし、最近さらに高い SLO が要求されるようになり、その対策方法を模索していました。ちょうど社内でカオスエンジニアリングプロジェクトの成果を共有していたため、当該サービスにプロジェクト活動を紹介し、既存の運用施策に加えてもう一段安定させられるようにカオスエンジニアリング導入を開始しました。このように、カオスエンジニアリングの導入タイミングがマッチし、また、既存施策にプラスアルファできる側面も大きな効果が期待されたため、現在 PoC に一緒に取り組んでいます。

#### **社内事例2** 

社内のエンジニアが、 Tech-Verse (ヤフー主催の技術イベント)のカオスエンジニアリングセッション、および Yahoo! JAPAN Tech Blog の記事を見つけ、直接声をかけてくれまし ~~た~~ <u>注21</u> ~~。~~

事例 1 とは違い、このエンジニアのチームはちょうど担当システムの安定化施策としてカオスエンジニアリングの組み込みを開始していました。そのため、すでに仮説シナリオを作成していたのですが、より具体的なアドバイスや知見が欲しいということで、情報交換を行いながらカオスエンジニアリング実践を一緒に進めています。

#### **社内事例3** 

社内の新規分散データストアプラットフォームシステムでもカオスエンジニアリングの導入が進められています。分散データストアのシステムではデータのレプリケーションがデータノード間で行われるため、レイテンシや利用可能なノード数、ディスクの空き容量などがとくに重要であり、これらを重点的に確認する仮説シナリオが作成されました。またこれらに加えて、ディスクの物理故障、ともほうもほうくに半死状態( Gray Failure )を Linux BPF で模倣する仮説シナリオも検討されています注22 ~~。~~

このように、当該システムの特徴に合わせたシナリオ作成を行い、実際に開発環境で予備実験を実施しました。すると、いくつかの動作認識差異やアラート不備などが見つかりました。一方で、この事例では本番環境での仮説シナリオはまだ実施していません。これは工数の問題もありますが、データの消失リスクがあることがおもな要因として挙げられます。そのため、単純なカオスエンジニアリングの実施だけではなく、より包括的な復旧手順の整備なども合わせて行う必要があります。カオスエンジニアリングは有効な手段ではありますが、当該システムの特徴をよく考え慎重に本番環境導入を進めることも必要だとこの事例からわかります。

◆ ◆ ◆

このように社内で関心が高まると、社内事例が増えていき、ノウハウも蓄積されます。

##### **新たなキャリアパスとしてのカオスエンジニアリング** 

筆者の考えでは、設計/運用経験が⻑い人は、カオスエンジニアリングに取り組むうえでとくに適した人材です。なぜなら、経験豊富な設計/運用者は、対象システムの挙動を把握することはもちろん、システムの設計者/運用者との対話を通じて課題を引き出し、カオスエンジニアリングの意義や、 unknown が known になること、監視や設計の抜け漏れが明らかになることといった効果を現場の感覚に基づいてまとめ上げることができるからです。⻑年の運用者としての経験/ノウハウを活かして取り組むことでシステムの信頼性と安定性を高めることができるため、カオスエンジニアリングは、スキルセットとしての新たな「武器」となり得ると考えています。

##### **まとめ** 

本稿ではカオスエンジニアリングの進め方について、筆者たちヤフーの実例を交えて紹介しました。カオスエンジニアリングを用いれば、システムの安定性向上に貢献できます。これは最前線のエンジニアにとっても良い経験だと言えるでしょう。筆者たちは、カオスエンジニアリングの取り組みの中で、対象システムの改善だけではなく、組織や人、またプロジェクト自体に対してもさまざまな工夫や改善を行ってきました。このような取り組みにおいては、現場でのコミュニケーションやシステム設計/運用経験が鍵となること、また改善を継続することで大きな効果を発揮すると伝わったかと思います。なお、カオスエンジニアリングは、海外はもとより、最近では国内でも徐々に広がっています注23 ~~。~~

みなさんの組織でもぜひカオスエンジニアリングを取り入れてみてください。本稿で共有したような、実践方法や工夫および実際の取り組みから得た経験や知見が少しでも読者のみなさんの糧になればと思います。

Let's Chaos Engineering Together!

注1) ヤフーの場合、PayPay祭などキャンペーンやニュース、天気のサービスがこれにあたります。 (本文に戻る) 注2) <u>https://learning.oreilly.com/library/view/chaosengineering/9781491988459/ https://www.oreilly.com/content/chaos-engineering/ https://arxiv.org/abs/1905.04648</u> (本文に戻る) 注3) <u>https://youtu.be/Lzv0UNzv4Po https://youtu.be/wwiSmgcBPVw</u>

###### (本文に戻る) 

注4) <u>https://www.microsoft.com/en-us/research/publication/how-to-fightproduction-incidents-an-empirical-study-on-a-large-scale-cloud-service/ https://queue.acm.org/detail.cfm?id=2655736</u> (本文に戻る) 注5) どの組織も似たような事例や悩みを抱えており、共感しながら読むことができます。 (本文に戻る)

- 注6) 関係者が全員集まり、カオスエンジニアリングをイベント形式で行う方法。 <u>https://www.gremlin.com/gameday/</u> 

- (本文に戻る) 

- 注7) ワイワイやりながらシステム改善を行いたかった、という筆者の思いもあります。 (本文に戻る) 

- 注10) 実は筆者もその1人でした。当初は1つぐらいしか見つからないだろう、と考えていましたが、実際は3つぐらい見つかったので、やはり効果があったと言わざるを得ませんでした。 (本文に戻る) 

- 注11) 読者のみなさんが「システムがいつでも意図したとおりに動くことを当然だと思わないでほしい」という思いに共感してくれると筆者は信じています。 (本文に戻る) 

- 注12) 筆者の考えでは、本番環境で継続してカオスエンジニアリングを実践し続けることが、本番環境を安全に保つほぼ唯一の対応策です。 (本文に戻る) 

- 注13) カオスエンジニアリング実践を避難訓練と同じように考えると納得できるかと思います。 (本文に戻る) 

- 注14) 実際に“次回はお祭り的にワイワイやろう”という社内フィードバックがありました。また、「確認しますー!」「了解ですー!」と声を掛けながら行うだけでも雰囲気改善に役に立ったと感じました。 (本文に戻る) 

- 注15) デフォルトカオスは筆者の造語となります。ここでは対象システムにカオスエンジニアリングを行うことを前提として設計/実装/運用/サービス提供を行う、という意味で使っています。 (本文に戻る) 

- 注16) 通常、これがカオスエンジニアリングを実践するうえで最も難しい点になると思います。 (本文に戻る) 

- 注17) まさにシステム設計/運用者の腕の見せどころですね。 (本文に戻る) 

- 注18) シナリオ実践後にシステムや体制、シナリオ自体を改善し、再度カオスエンジニアリングを実践すること。詳細は第2章を参照。 (本文に戻る) 

- 注19) 障害の内容や頻度、また開発状況によっては、システム改修は行わずにリスクを許容する場合もあります。もちろん、あらためてPoCを行い、耐障害性を向上させるようにシステム改修を行うこともあります。 

   - (本文に戻る) 

- 注20) ヤフーのインフラ部門として“施策効果があり、注目に値する案件”に贈られる賞となります。半年に一度選定されるこの賞は社内のオフィシャルなメッセージとして現場は受け止めています。 

   - (本文に戻る) 

- 注21) それぞれの詳細は注7、注8を参照してください。 

   - (本文に戻る) 

- 注22) Kernel Faultと呼ばれる手法です。通常、ディスクが故障した際にはシステムコールを経由してカーネル空間からアプリケーション(ユーザー空間)へI/Oエラーが送られます。このI/OエラーをBPFで模倣/発火させることで、アプリケーションやシステムが意図した動作をするか調べていきます。Chaos Meshなどのツールではこの機能がサポートされています。 

(本文に戻る)

注23) <u>https://cloudnativedays.jp/cndt2021/talks/1259 https://www.janog.gr.jp/meeting/janog52/lt03gameday/</u>

(本文に戻る)

###### **特別企画** 

#### **国産リレーショナルデータベース** 

## **Tsurugi** 

**潤沢なCPU/メモリを使い倒すため一から開発**

2023年7月10日、国内で開発されたオープンソースの次世代RDBのEarly Access版が公開されました。その名を「Tsurugi」と言います。従来のRDBは現代のメニーコア /大容量メモリといった資源を十分に活かしきれていないという課題認識をきっかけに開発されました。本記事では、GA版の一般公開に向けて準備が進められている Tsurugiの全容と性能の一端を紹介します。神林飛志(かんばやしたかし) 株式会社ノーチラス・テクノロジーズ

#### **Tsurugiについて** 

Tsurugi は、国産のオープンソースソフトウェア( OSS )の次世代リレーショナルデータベース( RDB )です。 5 年間の国(国立研究開発法人新エネルギー・産業技術総合開発機構: NEDO )の支援を経て、できあがった RDB になります。もともと有志の勉強会から始まったコミュニティ活動がベースで、国の予算を受けて、各⺠間企業((株)ノーチラス・テクノロジーズ、日本電気(株)( NEC ))・大学 (東京工業大学、慶應義塾大学、名古屋大学、大阪大学)・研究機関(国立天文台)等々が主体となって、さまざまな企業や関係者の協力のもとに開発されました。

OSS なので、誰でも自由に利用することができます。ライセンスは Apache 2.0 になります。商用サポートも提供されています。現在のところは希望する企業に個別に α 版を公開している段階で、リポジトリを含めた公開は 2023 年 9 月を予定しています(単純に人手不足で公開用の整備が間に合っていないだけです)。

#### **開発背景** 

「何をイマサラ RDB なのか? すでに枯れている技術ではないのか?」というのが、普通のエンジニアの印象だと思います。実際は、 RDB の技術は年々進歩しています。にもかかわらず既存の RDB では、このような新しい技術はなかなか取り入れられないのが実態です。これはさまざまな理由があると思いますが、主として、イノベーションのジレンマと、寡占化による競争緩和が大きいでしょう。とくに商用サポートのある RDB では新しい技術の採用は、なかなか難しい状況になっています。これら商用 RDB (含む OSS )のレイムダック化は、個々の RDB を統括する個々人・企業のマネジメントやエンジニアリングの能力が原因というよりも、そもそも商用サポートが行われるミドルウェアれいめいきの宿命のようなところがあります。黎明期には各種のソフトウェアで、製品が雨後の竹の子のように出て、競争が激化し、機能・内とうた容・差別化が推進され、結果として淘汰が進み、有力数社が残る、というのはどのカテゴリでもよく見る光景です。

RDB も黎明期には 20 弱もの製品、試験的なものや特殊なカテゴリのものまで包含すれば 50 は超える数のプロダクトが出てきたのは間違いないでしょう。ところが、競争や M&A の結果、 2023 年現在、アクティブな開発がされている RDB は、主として商用 RDB は Oracle

Database と SQL Server の 2 つ、 OSS では MySQL と PostgreSQL のこれまた 2 つの、計 4 つ程度にまで減りました。また、翻って我が国日本では「国産の RDB 」はメンテナンスモードである一部の RDB を除き、製品開発は実質ゼロ、というのが実態のようです。こうなってくると、勝ち残った既存勢力にとっては、競争に勝つために諸機能を追加開発し、場合によっては基礎アーキテクチャの変更も辞さないという「攻めの姿勢」は取りづらい。また、これまでの競争の中で機能追加や洗練化が進み、コードベースの肥大化や既存機能の互換性維持の圧力が深海 1 万メートルクラスの水圧でかかってきます。普通に身動きが取れません。結果、一部の例外を除き、既存 RDB にこの 20 年間の RDB の進歩、とくに環境変化による劇的なアーキテクチャの変更を伴う技術革新を取り込むのは極めて難しいということになります。

#### **実際のベンチマーク** 

理屈はともかく、「旧来の RDB と次世代の RDB で何がそんなに違うんだ」という話になると思いますので、まずは実際のベンチマークを見ていただくのが、わかりやすいかと思います。今回の Tsurugi プロジェクトで、実際の対象となった(株)アンデルセン様の業務系のバッチ処理を見てみましょう。これは Tsurugi のターゲットベンチマークになっているものです。このバッチ処理は詳細仕様・コード含めて OSS として Tsurugi とともに公開されるので、実際に読者が手元の環境で実行し、また、手を入れることも可能です。

##### **原価計算バッチ処理の特徴** 

一般にある DB のベンチマークと異なり、このバッチ処理は、比較的「実際の業務に近い」作りになっています。実際の原価計算システムのバッチ処理の部分を可能な限りそのまま抜き出して実装しています。したがって、ビジネスロジックは比較的複雑になっており、ユーザー企業の固有のロジックがそのまま実装されています。データモデルやテーブル数もかなり簡略化したとはいえ、ビジネスロジックに手を入れるには、ある程度の業務知識が必要です。現実のワークロードに当てはめた場合、「実際のところどうなるのか?」を検証する処理として有意なバッチ処理になっています。

##### **原価計算バッチ処理の概要** 

各製品の構成部品のデータが、継続的に更新・新規追加され、また、部品構成自体の変更が継続的に行われている状況下で、この部品データの新規追加・更新、部品構成の変更処理を止めることなく、全体あるいは個別に設定された製品群の各製品の原価・重量および所要量を計算する処理です。バッチ処理としては製品の原価・部品を構成する木構造について、 full scan または range scan を行う処理になっています。木構造をトラバース(走査)する原価計算・所要量計算は、従前の RDB のバッチ処理では時間コストがかかるうえに、一貫性担保が重要であるため、計算のもとになる構成部品データを、バッチ処理中に更新することは普通はできません。通常は、バッチ処理と更新処理を別計算で行うことが通例です。実際、アンデルセン様においても同様の運用になっています。なお、この構成データの更新と原価・所要量計算を同時に、かつ高速に行うことが可能であれば、リアルタイムでの必要原材料などの総量・総原価の算出が随時可能になります。また、原価計算/所要量計算が高速になることで、本番系とは別に同じ DB インスタンスをシミュレーション用に作成し、任意のタイミングで、部品原価・構成を変更し、必要に応じて原価・必要量シミュレーションを行うことも可能になります。

##### **原価計算バッチ処理ベンチマーク概要** 

ベンチマークの概要を以下に示します。詳細は省きますが、主要な処理と SQL を示します。まず ER は **図** 1のとおりです。処理の内容を **リスト** 1 **、** 2に示します。

###### **▼リスト1 バッチ処理の内容** 

**次の❶〜❸を1つのトランザクションとしてバッチ処理する ❶処理対象の工場・日付に対応する、製造品目マスターの全品目を取得する。取得した全品目について、品目構成マスターを再帰的に走査し、原料となる子孫品目をすべて取得する**

**select * from 製造品目マスター where 工場ID in (バッチ引数の工場) and バッチ引数の日付 between 開始日 and 終了日 select * from 品目構成マスター where 親品目ID=処理対象品目 and バッチ引数の日付 between 開始日 and 終了日**

- **❷木構造をもとに、重量・所要量・原価計算を実行する** 

- **・品目構成の木構造の全要素に対し重量計算を行う** 

- **・全木構造の重量計が決まったあとで、各要素の重量割合を計算する** 

- **・品目構成の木構造の全要素に対し所要量計算を行う** 

- **・所要量を決定する** 

- **(以上は取得したレコードから算出可能)** 

- **・品目構成の木構造の全要素に対し原価計算を行う** 

**1. 原価マスターを取得する**

**select * from 原価マスター where 工場ID=処理対象工場 and 品目ID=処理対象品目**

**2. 原価マスターから総原価を算出**

**3. 生産数あたりの原価を算出**

**4. 総製造原価を算出**

**5. 生産数あたりの製造原価を算出**

- **(以上は取得したレコードから算出可能)** 

- **❸結果テーブルに出力する** 

- **・品目構成の木構造の全要素を結果テーブルにINSERTする** 

**insert into 結果テーブル values(処理対象工場ID, バッチ日付, 製造品目マスター.品目ID, 木構造.品目ID, ......)**

###### **▼リスト2 オンライン処理の内容** 

**概略として2本のみ示す**

- **❶新規商品開発(select-insert)** 

- **・品目・品目構成マスター登録** 

**1. 品目IDを採番する**

**select max(品目ID) + 1 from 品目マスター**

**2. 品目マスターに登録する**

- **insert into 品目マスター values(採番した品目ID, ......)** 

**3. 品目構成マスターを登録する**

- **insert into 品目構成マスター values(品目ID, 採番した品目ID, ......)** 

- **・製造品目マスター登録** 

**insert into 製造品目マスター values(当工場ID, 採番した品目ID, ......)**

- **❷生産数の変更(select-insert/update)** 

**1. 品目IDを決める**

**select 品目ID from 品目マスター where 当日日付 between 開始日 and 終了日 and 品目種類=製品**

**2. 生産数を決める**

**3. 製造品目マスターを登録または更新する**

- **・製造品目マスターを取得する(upsert処理)** 

**select * from 製造品目マスター where 工場ID=当工場ID and 品目ID=対象品目ID and 当日日付 between 開始日 and 終了日**

**insert into 製造品目マスター values(当工場ID, 対象品目ID, 当日日付, 終了日, 新生産数)**

**update 製造品目マスター set 生産数=新生産数 where 工場ID=当工場ID and 品目 ID=対象品目ID and 当日日付 between 開始日 and 終了日**

バッチ処理の実行結果を見る前に、どのような実行を行い、評価を行っているかを示します。前提として、Isolation **レベル(トランザクション分離レベル) は** serializable **(直列化可能)とします。** また、対象処理のテーブルのサイズは **表** 1のとおりです。

#### **■1バッチ処理のみを実行** 

コンフリクト(データの衝突)を起こすオンライン処理などはいっさい行わない条件下で、バッチのみを実行します。バッチの実行処理時間を計測します。結果は **表** 2のとおりです。

#### **■2オンライン処理のみを実行** 

今度は逆に、同じくコンフリクトを起こすバッチ処理を行わずにオンライン処理のみを実行します。オンライン処理自体はそれぞれ、実行時間が短いものなので、すべての種類のオンライン処理をスレッド単位で、 begin→commit を繰り返し、各トランザクションの実行時間(遅延時間)を計測します。結果は **表** 3のとおりです。

#### **■3混合処理** 

バッチ処理をスタートさせると同時に、オンライン処理を2と同じように一⻫に放り込みます。バッチが完了した段階で、バッチ処理にかかった時間を1と比較し、また、各オンライン処理で遅延時間を2と比較してそれぞれの劣化度合いを見てみます。要するに、 **木構造のトラバースを行って処理しているフルバッチの最中に、その木構造を変更するオンライン処理をどんどん放り込むという構図になります。** もちろん、前述のように一貫性を担保するために分離レベルは serializable とします。そもそも木構造のトラバースは RDB では苦手な処理の 1 つです。結合処理を再帰的に行ってループを回し、深さ優先探査を順に行っていく処理で、普通に時間がかかります。そのうえ、この処理の最中に対象となる木構造が変更される処理を **同時に** 加えていきます。

##### **ベンチマーク(混合処理)の結果** 

分離レベルが serializable なので、従来の RDB であれば Lock を取りまくって、バッチ処理を終わらせ、その間オンライン処理は延々待たされるか、または逆にいつまで経ってもバッチが終わらないかの、おおむねどちらかになります。それでは Tsurugi ではどうなるか? まずバッチです。結果は **表** 4 のとおりです。劣化は 109.673 / 95.218 = 115.2 %で 15 %時間が延びています。次にオンラインです。結果は **表** 5のとおりです。止まるどころか、処理が完了したうえに、バッチ、オンラインともそこそこの劣化にとどまっています。オンラインにいたっては一部むしろパフォーマンスが向上しています注1 ~~。~~

次節以降、簡単に技術的な解説をしていきます。それらの技術は別に Tsurugi が新規に開発したものではありません(もちろん、いくつかは Tsurugi が開発した技術はあります)。この数十年の DB の開発で培われてきたものです。 Tsurugi はこれらの、この 20 年近くにわたる DB ・トランザクション技術・分散処理技術を総動員してできあがっている技術の集合体です。なお、大前提として、既存であろうと新世代であろうと、 ReadModify-Write が重なる場合は、必ず wait または abort が起きて、衝突するバッチ処理とオンライン処理は順序実行を強制されます(これはデータベースに限りません)。既存の Lock ベースでの RDB であれば後発で Lock を取ったほうのトランザクションが wait 処理になり、 Tsurugi の場合は abort and retry の処理になります。それでは、具体的に旧来の RDB と次世代の RDB の違いを見てきましょう。先に結論だけ言ってしまうと、

**旧世代の** RDB:Single Version/Lockベース/Diskベース/ incremental CC

であるのに対して、

**新世代の** RDB:MVCC/Timestampべース/In Memoryベース/ Epoch based CC

になります。

#### **Tsurugiのテクノロジー** 

##### **Multi-Version Concurrency Control** 

Tsurugi も含めた現代的 RDB の基本となる MVCC を “ ざっくり ” 解説します。 MVCC は Multi Version Concurrency Control (多版型同時実行制御)の略で、端的に言えば、トランザクションの一貫性を保ちながら、トランザクション処理のスループットを上げる技術です。複数の version を利用することで、トランザクションの並行性制御の許容度を高めます。従来の “ 貴重かつ少ない ”CPU /メモリの資源を「使い回す」ための Single Version のしくみと異なり、 MVCC は “ 豊富な ”CPU /メモリの資源を「使い倒す」ことが使命になっています。 MVCC 自体は非常に古い技術で、トランザクションの黎明期から研究されていたアルゴリズムです。ただ、残念ながら、その時代は資源、すなわち CPU とメモリに強い制約があり、 MVCC がパフォーマンスを出すには十分な環境が整わず、その結果、黎明期からしばらくの間、 MVCC は「使えない」の代名詞でした。ところが現状は大きくハードウェア環境が変わっています。 CPU はコア数の増加が顕著ですし、メモリもテラバイトクラスの DRAM が普通に使える時代です。 MVCC がその性能を発揮するには十分な時代になったと言えるでしょう。

Tsurugi をはじめとした現在の RDB では、この MVCC が基本になっています。 MVCC は説明するほうも、理解するほうも極めてハードルが高い技術なので、ここではイメージを簡略的に説明します。まず、 MVCC では「 **物理的に** 複数の状態が実際に “ 同時に ” 存在する」ことを許容します。この、複数の状態が「同時に物理的に存在する」ことが MVCC のスタート地点です。実際の前述のバッチ処理の例で言えば、「木構造が同時に複数存在できる」ということになります。厳密な説明はいったん置くとして、これは端的に表現すると、バッチ処理の対象になっているレコードと、もうひとつ別の “ 同じ ” レコードが “ 同時 ” に存在している、ということを指します(特定のレコードの、複数の “version” が同時に存在しているイメージでいいと思います)。まず、この「状態」が可能であれば、バッチ処理を行っている対象の木構造を維持したままで、同時に別の木構造を持つことが可能であるので、バッチ処理自体は邪魔されずに実行できる、ということはわかると思います。さて、この複数の状態、例えれば、「 DB 上に同一の、しかし内容の異なる、レコードが物理的に存在する」という状況は、論理的にはたんは破綻しているように見えます(ただし、物理的には破綻していないことに注意してください)。したがって、破綻しないように「事つじつま後的」に辻褄が合うような「リクツを考える」必要があります。リクツが整うのであれば、問題なく物理的かつ論理的に実行できている、ということになり、 commit ができます。ちなみに、リクツが見つからなかった場合は「やっぱり、論理的に破綻していますね」ということで、上記のバッチの例で言えば、オンライン側が abort されます。ですので、問題は、その「リクツとやらを、どうやって、いつ考えるのか」が問題になります。

- ・どうやってリクツを考えるのか? 

- ・いつリクツを考えるのか? 

を順を追って説明していきます。まず、「どうやってリクツを考えるのか?」ですが、この基準が「一貫性」になります。以下のパラグラフはかなり難しいことを言っていますので、興味のない方は積極的に読み飛ばしてください。まず一貫性の定義は serializable (直列化可能)になります。これは「個々の与えられたトランザクションのすべての物理的な read と同じ、 read を行う論理的なトランザクションの全順序が、その集合において、最低でも必ず 1 つ存在することができる」と定式化します。この条件においては、まず物理的な read は動かせません(所与)。このとき、 read ( r )と write ( w )の基本構造は、与えられた物理的な read を r2 、その read が読んでいる write を w1 とし、同時に存在している write を w3 と表記すると、この 3 者の構造の数学的表現 (グラフ)は、

((w1<r2),w3)

と表記できます。この構造を一般に multi-version conflict と呼びます ( “,” は anti-chain を表現していますが、 static なものではなく、 dynamic なものを想定しています。通常の order theory でいうところの anti-chain よりも広い relation になります)。上記の一貫性の定義は、この構造(( w1 < r2 ) ,w3 )を、 w3 < w1 < r2 または、 w1 < r2 < w3 の順序に整合的に解釈することができるトランザクションの順序を包含する順序が、対象となるすべてのトランザクションの順序の順序集合の集合において、最低でも必ず 1 つ存在することができる、ということと同値になります。よって、特定のトランザクション群がこの条件を満たす集合である、と判定することが「リクツを考える」ということになります。さらに、その順序集合が存在する場合(すなわちリクツがついた場合)は、論理的な全順序が取り得るので、(実際に取るかどうかは別として)「同時に実行する」ことができるということになります注2 ~~。~~

要は、リクツが合うように実行を制御するのではなく、とにかく同時実行して、その実行と同時に、リクツが合うように計算する (正確には合うようなリクツがあるかどうか計算して探す)スタイル、だと思ってもらえばいいかと思います(実は物理実行も多少はいじってますが、説明は割愛します)。

##### **Epoch based Concurrency Control** 

次に、「いつリクツを考えるのか?」の問題ですが、これも次世代 RDB の共通項であり、 Tsurugi の特徴の 1 つでもある epoch ベースと言われるものになります。

#### **■epochの定義** 

あまり定式化された定義はありませんが、一般には「トランザクション処理や、それにまつわる処理をまとめて実行する時間の単位」を指します。時間の単位なので、論理時間であることが普通ですが、実行時は普通に実時間にマッピングされます。この epoch の単位で「まとめて、リクツを考える」ことになります。 epoch の終了時点で、対象となるトランザクション群をまとめて、調整/判定を行います。この epoch で処理をまとめる、という方法は、端的に言えば全体のアーキテクチャを非同期処理に統合して、この連携のコストを最小限に抑えたうえで、それぞれのアトミックな時間/処理の単位でのスループットを最大化するしくみ、ということになるでしょう。もともとは、 epoch それ自体の出自は抽象的なものではなく、実装的なエンジニアリングの要請でした。 Concurrency Control では要するに「 group commit 」からの要請です。よく知られているとおり、「 group commit 」はとくに Disk ベース時代の DB ではスループットを稼ぐための手段でした。個々に commit 処理をして logging するよりも、まとめてやったほうが効率が良い、という実装上の工夫です。この限りにおいては別段、論理時間= epoch と言われる整理されたものではありません。ただ、時代の変遷とともに、 epoch 方式は効率が良い、ということが一般的になってきて、今やこの手法がメインストリームになりつつあります。一般にこの epoch を利用した Concurrency Control のしくみを Epoch based Concurrency Control ( ECC )と呼びます。 epoch の時間枠の中で、非同期で一貫性の判定をまとめて行います。たいていの場合は epoch の終了時に group commit と logging を行い、ユーザーサイドにまとめて commit を返すしくみを利用しています。

##### **LockベースとTimestampベース** 

さて、次に、旧来と現代の RDB の並行性制御の「実装」で最も異なる、順序制御の手法を説明します。一般に与えられたトランザクションの各命令の実行の順序指定や、一貫性の担保の実装はおおむね 2 種類に分かれます。 1 つは Lock 制御を利用した Lock ベースのものと、 Timestamp を利用した Timestamp ベースのものになります。

Lock ベースでは「先順」のものを処理している間に利用リソースに Lock をかけて「降順」のものを待たせるしくみになります。これにより各トランザクションの実行順序を決定し、「辻褄が合うように」しています。一方、 Timestamp ベースは各トランザクションに設定された Timestamp を見て判断したうえで、順番に実行させる、または優先度に応じて利用リソースへのアクセスの是非を判断するという順序制御を行い、これまた「辻褄が合うように」しています。なお、 Lock ベースであろうと、 Timestamp ベースであろうと順序制御という観点からは、まったく同じことができる、という点には注意してください。つまり論理的には違いはありません。前述の「リクツを合わせる」という観点から言うと、前者の Lock ベースの実装は、「辻褄が合うように」物理的な制御をしている方法になります。この場合は、前述の MVCC の考え方はとりません。リクツを合わせるのではなく、リクツに合うように物理制約をかける、という手法です。後者の Timestamp ベースは、「辻褄が合うように」物理的な制御を行うこともできますし、またそうではなくて、 Timestamp を基準にして「辻褄が合う」リクツを考える、ということもできます。 MVCC は原則として、 Timestamp ベースで処理をして事後的にリクツを考える( validation )という手法が主流です。 Tsurugi も同じです。現在の、技術の先端で研究・開発されている Concurrency Control は、ほぼすべて Timestamp ベースで、 Lock ベースのものはほとんどありません。他方、現在の、とくに旧来からの RDB は、ほぼすべて Lock ベースです。これは端的に言ってしまえば、 “ 現在の環境 ” では Timestamp ベースのほうがパフォーマンスが良く、使い勝手が良いからで、逆に Lock ベースが残ってしまっている理由は、これまた端的に言えば実装が簡易で制御が楽で、 “ 以前の環境 ” では十分にパフォーマンスが稼げたからです。

##### **DiskベースとIn Memoryベース** 

最後に、次世代 RDB の物理的な最大の、かつ共通の特徴になりますが、 In Memory ということがあります。「すべての処理をメモリで行う」という意味ですが、いろいろ誤解を招く表現です。そもそも、すべての処理をメモリで行うのは当たり前ではないですか?

すべからくコンピュータの CPU はメモリ上で処理を行うので、むしろ「すべての処理をメモリで行う」は当たり前で、むしろメモリ上で処理を行わない CPU はありません。

In Memory ベースの意味は「処理する可能性のあるすべてのデータはメモリ上」に持っているということです。 Tsurugi も当然、 Index およびデータ本体はすべてメモリ上にあります。結果、 DB 処理は高速になります。原則 logging 以外の Disk の読み書きはしません。「すべての処理をメモリで行う」のであればDBが落ちればデータ消えてしまいませんか?

これも大きな誤解があります。 Tsurugi に限らず現代的な In Memory DB は「別に DB が落ちてもデータは消えません」。たいていはトランザクションの log は Disk に永続化しています。この意味では Disk ベースの旧来の DB とメモリベースの新しい RDB の違いは、「要はデータを全部メモリに置いているか、いないか」の違いでしかありません。なぜ、このような誤解が生まれたかというと、 20 〜 30 年前の「 In Memory DB 」はデータのキャッシュをメモリに乗っけているだけ、という製品が実際に多かったからです。当時の「 In Memory DB 」は DB サーバが落ちるとデータが消えるという問題点があり、その印象が今に至るまで残っている、というのが理由でしょう。今ではこれは誤解になります。

#### **Tsurugiの構成** 

さて、これまでは Tsurugi の特徴のいくつかをかいつまんで、その背景とともに説明してきました。次に簡単ですが、 Tsurugi の全体像を説明しておきましょう。

Tsurugi は現代の RDB にふさわしく、従来の RDB のようなモノリシックな構造ではなく、コンポーネントベースでの構成的なアーキテクチャになっています( **図** 2)。このため、さまざまな介入ポイントがあると同時に、複数のインターフェースを同時に利用して、かつ一貫性を担保できるようになっています。以降、各コンポーネントについての簡単な解説を行います。

##### **PostgreSQLフロントエンド:Ogawayama** 

PostgreSQL をユーザーインターフェースとして利用する機能です。 PostgreSQL を利用しているユーザーが PostgreSQL の機能を活かしつつ、 Tsurugi を利用することが可能になります。

##### **高レベルJava-API:Iceaxe** 

Tsurugi に対する、細かい制御をアプリケーションから行うための Java の外部インターフェースです。 JDBC ではありませんが、その位置付けは JDBC と同様になります。 Tsurugi 本来の機能・パフォーマンスを利用するために、 JDBC 以上の高機能なライブラリになっています。

##### **低レベルJava-API:Tsubakuro** 

Iceaxe が外部から同期的に Tsurugi を扱う Java-API であるのに対して、 Tsubakuro はより低次で直接的に Tsurugi の取り扱いを可能にする、非同期 Java-API になります。より無駄のない効率的なしくみで直接 Tsurugi にアクセスする場合は、この Tsubakuro の API を利用します。

##### **Webサービス用API:Belayer** 

Web API を利用して、直接 Tsurugi にトランザクションとしてアクセスすることが可能です。とくに、外部から特定のフォーマット ( CSV 、 parquet )での大量のデータの登録・取り出しを行う際は、このインターフェースを利用すると便利です。大量のデータの一括での登録・ロード作業を行いながら、通常のインターフェースで SQL を発行して、一貫性を保ったまま Tsurugi を利用することができます。

##### **CLI:Tanzawa** 

Tsurugi に対して直接 SQL を実行できる SQL コンソールです。 ◆ ◆ ◆

以上が外部インターフェースとしてのコンポーネントです。これ以降は内部的な構成になります。とくに内部の各サービスは、 Tsurugi がメニーコア環境で最大限のパフォーマンスが出せるように実行環境を分散処理にターゲットを定めて、さまざまな最適化を実装しています。

##### **アプリケーション管理フレームワーク:Tateyama** 

Tsurugi 内部でのコンポーネント管理を行うマネジメントモジュールです。各構成サービスのライフサイクル管理を行います。

##### **ジョブスケジューラ:Jogasaki** 

つかさど Tsurugi 内部でのジョブ、タスクのスケジューリング機能を司るサービスモジュールです。メニーコア用にさまざまな最適化を行っています。

##### **SQL分散処理用コンパイラ:Mizugaki** 

与えられた SQL から実行計画を生成するコンパイラです。サービスモジュールとして提供されています。通常の SQL コンパイラと違って、分散環境下での実行を企図した実行計画を生成します。したがって単純なシーケンシャルな実行計画ではなく、 DAG (非循環有向グラフ)の形式の実行計画へ SQL 群がコンパイルされます。

##### **トランザクションエンジン:Shirakami** 

依頼されたトランザクションを実行します。実行アルゴリズムは、 MVCC ・ Timestamp ベースかつ、 Hybrid CC になっています。単純な処理は OCC ( Optimistic Concurrency Control :楽観的同時実行制御)で、バッチ処理のような⻑いトランザクションは LT ~~X~~ <u>注3</u> ~~で~~ 、というように複数のアルゴリズムを駆使して、 serializable にすべてのトランザクションを実行します。

##### **インメモリインデックス:Yakushima** 

Shirakami が内部に持つ Index tree で、インメモリでの Masstree 構造になっていて、基本的に B+ 木とトライ木の合成による構造を持っています。基本、楽観処理でトライ木の性格からレンジスキャンに優れています。

##### **ログストア:Limestone** 

非同期で WAL ( Write Ahead Logging :ログ先行書き込み)を制御するログストアです。 Tsurugi では従来の RDB のような、 logging = CC =トランザクションというモノリシックな実装方式になっていません。 DB の機能はストレージと CC に分かれつつある、という現代的な環境をベースに作られています。そのため、トランザクションエンジンと logging モジュールが分離されて実装されています。このため log の取得が極めて容易で、 CC エンジン側に手を入れる必要はいっさいなく、極めて低遅延で、安全に log を拡張・再利用することができます。

#### **Tsurugiの基礎性能** 

最後に Tsurugi のポテンシャルについて見ておきます。 Tsurugi の基礎性能のベンチマークとして、 YCSB-A <u>注4</u> ~~を~~ 見てみましょう( **表** 6 **、図** 3)。ワークロードは read/write で 50 %ずつです。

serializable な処理で、遅延でナノ秒、スループットで百万 tps のオーダーになっています。

MVCC ・ Timestamp で同時並行的にいろんなことができると言っても、その実行は現代的なハードウェアを使いこなす馬力があってこそ初めて可能になります。 Tsurugi がこれだけ処理が可能なのは、それ相応のパフォーマンスバックグラウンドを持つからです。上記のように、 Tsurugi については大量のコアと十分なメモリを与えた場合は、すでにハードウェアのパフォーマンスは人間の認識でりょうがきるスケールを凌駕します。外部連携レイヤですら、低遅延でマイクロ秒スケールの一貫性を持つトランザクションが可能になりつつある一方で、スループットは百万 tps の水準で達成可能です。これは結果として、 SQL レイヤと KVS レイヤでの大きなパフォーマンスギャップを生むことになります。

Tsurugi の開発では、マシーンインターフェースに近い KVS インターフェースの開発と公開は、ずっと先の予定でした。開発の終盤のパフォーマンスの作りこみの段階で、このパフォーマンスギャップが顕著になり、今後 SQL のパフォーマンス向上策は当然継続開発していくとしても埋まらない溝になるだろう、という結論になりました。結果、 KVS インターフェースの開発と公開は前倒しになっています。

Tsurugi のポテンシャルを活かすには、 SQL と KVS のインターフェースを同時に、どう使うのか?ということになるのかもしれません。

#### **Tsurugiの現在** 

冒頭で説明したように、 2023 年 7 月現在、 Tsurugi は希望するユーザー向けに Early Access 版( α 版)を提供しています。希望する方は次の URL からお申し込みください。

<u>https://www.tsurugidb.com/</u>

なお、 9 月の中旬にがんばって GA 版(無理なら β 版)をリリースする予定で、この段階でどんな方でも GitHub のリポジトリにアクセスできるようにします。なお、同時に、 Tsurugi の内容を詳細に解説した Tsurugi 本も出版される(これもまた予定)ので、そちらも読んでいただければより詳細な情報が手に入ると思います。

- 注1) これはTsurugiのタスクスケジューラの最適化によるものです。大量のタスクがあるほど最適化が効く余地が広がっています。 (本文に戻る) 

- 注2) このあたりの詳細を知りたい方は、2023年9月に発売が予定されているTsurugi本に詳細が記述されているので、そちらをあたることをお勧めします。 (本文に戻る) 

- 注3) LTXとはTsurugi独自のバッチ処理用のMVCCの実装プロトコルで、 MVTO(Multiversion Timestamp Ordering)の拡張になっています。詳しくは Tsurugi本を参照してください。 

   - (本文に戻る) 

- 注4) Yahoo! Cloud Service BenchmarkのWorkload A。 

   - (本文に戻る) 

#### **特別企画** 

### **マルウェア対策とエンドポイントセキュリティ** 

#### **攻撃手法を知り、対策を考える** 

##### **後編エンドポイントセキュリティの現実** 

昨今、「Emotet」というマルウェアの被害件数が急増しています。いったいどのようなマルウェアで、どのような危険性があるのでしょうか。本記事では、そうしたEmotetの実際のところを説明したうえで、マルウェアそのものについての対策を併せて考えます。この機会に、ぜひご自身の環境を振り返ってみてください。佐々木康介(ささきこうすけ)

##### **脅威対策は敵を知ることから** 

先月(本誌 2023 年 8 月号)は Emotet のことを解説しました。今回は Emotet と同等以上に注意すべきランサムウェア攻撃のことを説明しつつ、それらから守るための方法を記載します。現場のエンジニアはどうしても設計者の視点でセキュリティを考えがちです。不要なポートが開いていないか、権限設定や管理は適切か、脆弱性が放置されていないかなど、危険な部分が経験によってわかっているため、堅牢な設計や運用をすることは得意です。本誌の読者もそうした方が多いかもしれません。しかし、攻撃者のことを学ぶ機会はほとんどありません。今まではそれで良かったかもしれません。世界で一番サイバー攻撃を受けるのはアメリカでしたし、英語圏の⻄欧諸国に比べれば日本はサイバー攻撃を受ける機会が少ない状態が続きました。ですが、サイバー攻撃の件数は世界的に増加傾向にあり、攻撃者も自然な日本語で脅迫メッセージを送付するようになってきました。

##### **ランサムウェア犯罪組織とビジネス** 

ランサムウェアでもっとも存在感のあるグループは LockBit です。 LockBit というのは組織またはマルウェアの名称として使われます。先月の記事で、 SaaS 形式でランサムウェアサービスを提供する RaaS ( Ransomware as a Service )の説明と、それを利用して攻撃するアフィリエイターの説明をしました。

LockBit 組織は RaaS 提供者の中でもとくにビジネス色が濃い組織です。 Web サイトには自ら「最も古い国際的なランサムウェアグループであり、国や言語、年齢、宗教の垣根を越え地球上の誰であっても我々と仕事ができる」と記載しており、犯罪組織というより会社などの営利組織のようでもあります。また、彼らの倫理規定では医学、教育、慈善基金、社会サービス組織を攻撃してはならないとしています。実際にアフィリエイターが病院を攻撃してしまった際には謝罪して復号したこともあります。こうした特徴的な文化は LockBit に良心があるからというわけではなく、効率的にお金を集めるために都合がよいからです。彼らは儲かっている企業を攻撃して取引することが目的ですので、目立つことは本意ではありません。病院や社会インフラを攻撃して悪目立ちをしたくないのです。 LockBit の前に世界一のランサムウェア組織だったのは Conti です。彼らはウクライナ問題でロシア政府を支持してしまったことが致命的な仲間割れを引き起こし、組織内の会話のほとんどすべてが捜査機関にリークされて最終的に Conti は活動停止に追い込まれました。ロシア系犯罪組織はロシア政府と結び付きが強い場合もありますが、こうしたサイバー犯罪組織の実態はグローバル化が進んでいます。組織の構成員にはアメリカ人やウクライナ人もいるので国や思想に関して中立であることは巨大犯罪組織にとって合理的なことなのです。ロシア政府やロシアのサイバー犯罪者が目立つと、それを攻撃する勢力も誕生します。 RURansom というマルウェアは明確にロシアを標的にしており、脅迫状にはロシア政府への非難が記載されています。 RURansom という名称はランサムウェアらしいですし実際に暗号化攻撃をしますが暗号化されたファイルを復号する方法がないため、実態はワイパー型マルウェア(破壊を目的とするマルウェア)に分類されます。 LockBit のように、なるべく目立たないようにサイバー攻撃を行うことには自身を守る意味もあります。ランサムウェア攻撃は犯罪でありながら優れたビジネスのように大きく成功してしまっています。彼らは世界中のあらゆる企業よりも多くバグへの報奨金を支払います。バグハンターは正規ベンダーへ報告するよりも、ダークウェブに報告するほうが多くの賞金を手に入れられます。ランサムウェア組織はバグだけではなく、自分たちの犯罪ツールの改善についても賞金を支払います。犯罪が成功して資金が潤沢にある証拠です。攻撃を行うアフィリエイターには履歴書の提出を求めて面接を行います。もはや企業活動のようです。安全に犯罪活動を継続するために、お金と手間をかけていることがわかります。 LockBit がインタビューを受けることや企業へのアドバイスを求められてコメントをすることからも、彼らの活動にはゆとりを感じます。こうした攻撃者のことを知ると、攻撃の傾向を知ることに役立ちます。前述のとおり、ウクライナ問題により、⻄欧諸国がロシアにいる犯罪者を逮捕することが難しくなりました。ロシアにいる攻撃者からすれば従来より安全に犯罪行為に集中できるということになります。今の状況が変わらなければ、企業は攻撃されるリスクが高まっているのでセキュリティへの投資を増やして備えることを検討すべきでしょう。

IPA が発表した「情報セキュリティ 10 大脅威 2023 」注1 ~~に~~ は初めて「犯罪のビジネス化」がランキング入りしました。ここからもわかるとおり、犯罪ビジネスの脅威は大きくなっています。

##### **どうやってランサムウェアウイルスの感染を防ぐか** 

真っ先にやるべきことは水際で食い止めることです。ほとんどすべてのマルウェアは外部から侵入してきます。そのため、インターネットにつながっているサーバや PC などの端末を最優先で対策することになります。セキュリティパッチの適用やマルウェアを検知/ 除去できる製品を導入して、感染を防ぐことが大事です。ランサムウェア攻撃は VPN 機器の脆弱性から侵入するケースが多いことは先月説明しましたが、メール添付やフィッシングなどさまざまな手口があるので全方面気をつける必要もあります( **図** 1)。まずはあらゆる機器の脆弱性を放置せずに対応することが大事です。脆弱性を塞げば感染のリスクは大幅に下がります。多くの攻撃は既知の脆弱性を利用して行われるからです。マルウェアを検知/除去するためには、 EPP ( Endpoint Protection Platform )製品が必要です。これは有償無償問わず標準的なアンチウイルスソフトがあれば十分です。しかし、どんな EPP をもってしても感染を完全に防ぐことはできません。攻撃者はマルウェアスキャンをすり抜けるために解析を妨害する機能やあの手この手の技術を駆使して端末にマルウェアを感染させます。たとえば、端末を強制的にセーフモードで再起動するタイプのマルウェアもあります。セーフモードではほとんどのセキュリティ対策製品がまともに動作しません。このように、攻撃者はセキュリティ対策ソフトを無効化するための研究を怠りません。

EPP 製品の導入の次にやるべきことは、 EDR ( Endpoint Detection and Response )の導入です。 EPP が水際対策なら、 EDR は水際で食い止められなかったときの対策になります。 EDR は強力な機能がたくさんあるので後でもう一度詳しく説明します。ちなみに、 EPP/EDR は 1 つの製品として機能を併せ持つものが増えてきました。 EDR は必ず導入したい製品ではありますが、既存 EPP の置き換えが必要な場合は使用感や機能をよく確認しましょう。また、エンドポイント対策のネットワーク版を NDR と言います。さらに最近はそれらを併せ持って進化した XDR というものも生まれました。 XDR は、 EDR を PC やサーバなどのエンドポイントだけではなく、ネットワーク、クラウド、アプリケーションなどに拡張したことが特徴です。しかし、 XDR を使いこなすには EDR よりもさらに高い技術力と手厚い運用体制が必要です。機能が拡張していくのは良いことですが、何が必要かをしっかり考えてベンダーの言われるままに過剰投資にならないように注意しましょう。一般的な EDR でも使いこなすにはそれなりに大変です。大変なポイントの例として、 EDR/XDR には SIEM ( Security Information and Event Management )の機能を含む場合があります。 SIEM はさまざまなログを収集して相関分析を行うことで脅威の点と点を結んで線にすることができます( **図** 2)。 SIEM 特化製品はあらゆるログを収集しますから、「いつもと違う時間に入室」した人が「退職前日」に「機密情報にアクセス」した後に「大量に印刷」したことを検知してアラートを出すなど、複数のログを組み合わせて不審な行動を発見できます。プリンターのログだけなど 1 つの情報だけでは発見が難しい脅威を検知するには SIEM が有効です。 EDR の SIEM 機能はエンドポイントに限られるのが普通ですので、ネットワーク機器を含む XDR は有用かもしれません。しかし、すべてのネットワーク機器のログを収集するのは大変です(エンドポイントだけでも大変です)。製品導入時には機能や価格で選定しがちですが、導入運用を行う体制で使いこなせるものなのかも考慮しましょう。 SIEM は一例で、 EDR/XDR の導入運用は従来の EPP よりも複雑で高い技術力が要求されます。

##### **ランサムウェア攻撃とバックアップ** 

ランサムウェア攻撃を受けた際に、バックアップから復旧するという話題がよく挙がります。ランサムウェア攻撃に備えてバックアップを取得すべきという主張を見かけることもありますが、実際には容易ではありません。 7 〜 8 割はバックアップから復元できていないのが実情です。バックアップは障害から復旧するためのもので、ランサムウェア対策として設計されていないのがほとんどの組織の現実だと思います。感染時にデータが暗号化された段階で気づいて問題箇所をネットワークから隔離すれば被害は局所的に抑えられます。そうなると早期に復元できる可能性は高まります。バックアップが無事ならリストアすることで復旧も可能でしょう。実際にそうした例は少なくありません。攻撃を検知して封じ込めるしくみがあることを前提としてバックアップに頼るのは正しい形です。基本的にバックアップは最終手段であり頼り過ぎは厳禁です。システムが攻撃を受けてしまった際に、バックアップだけが無事とは通常は考えにくいです。なぜなら、ネットワークがつながっているとバックアップも攻撃されるからです( **図** 3)。ランサムウェア攻撃対策がされたバックアップソフトを使っていればバックアップは無事かもしれませんし、ネットワークから切り離された遠隔地に保管しているバックアップは無事かもしれません。ですが、攻撃による暗号化に至るまでにボットネットを構築するための潜伏期間が平均 1 週間程度あります。そうなると、バックアップの中にもマルウェアが潜んでいる可能性があるのです。いつからマルウェアが混入したのか調べることができなければ、マルウェアごとバックアップからリストアすることになりかねません。ネットワークから隔離された場所にバックアップがあったとしても、マルウェア検知ができなければ結局復旧は困難なのです。バックアップにマルウェアが混入した実例としては、 2023 年 7 月に名古屋港統一ターミナルシステム( NUTS )が LockBit によるランサムウェア攻撃を受けた際にはバックアップにも被害があったため復旧に時間がかかったと発表されたことがあります。

Windows PC の場合はクラウドにある OneDrive に自動的にバックアップされているかもしれませんが、こちらも同様に攻撃されます。 OneDrive は過去の変更履歴を記録していて正常時には任意の過去バージョンで復元できますが、今のランサムウェアにはこの過去バージョンファイルをも攻撃して破壊する機能があります。ただし、バックアップソリューションの提供者もランサムウェア攻撃がバックアップを破壊することはわかっているので、対策された製品を選ぶと安全です。たとえば Acronis のバックアップソリューションにはランサムウェア攻撃を検知/自動復元をする機能が備わっています。攻撃によりファイルが暗号化されると、そのプロセスを遮断し、暗号化されたファイルを自動的に復元します。しかし、攻撃者はこれからも復元を阻止する機能を進化させていくことは間違いありませんし、マルウェア対策は検知/除去が大事です。実際にランサムウェア攻撃を受けた際に、早めに検知できれば被害が抑えられるので復旧も簡単になります。 EDR を補う形でランサムウェア対策がされた有償のバックアップ製品で備えるのは良いことだと思います。プロのエンジニアでも「ネットワーク上で隔離された場所にバックアップがあれば安全」と考える人は多いです。しかし古いバックアップから復元するのは容易ではありませんし、遠隔地にあるテープバックアップのようなものは復元にも時間がかかります。筆者は攻撃を受けた際の復旧を考慮するのは大切だと思いますが、水際で感染しないようにすることや検知して被害を局所化して早期復旧できるように用意することのほうが優先度が高い対策だと言い続けています。

##### **EDRの重要性** 

検知が大事という説明をしましたが、 EPP の水際対策だけではなぜ不十分なのでしょうか。もう少し踏み込んで説明します。ランサムウェア攻撃を受けてデータを暗号化されたときにマルウェアをブロックするだけなら、 EPP でも可能かもしれません。しかし、 EPP だけではいつ感染したのか調査することはできません。 EDR であれば感染箇所をネットワークから隔離してから脅威の分析や影響範囲の解析を行うことができます。「ネットワークから隔離」と書きましたが、 EDR の機能で隔離した場合は隔離したまま感染したエンドポイントと EDR とで通信が可能です。エンドポイントにインストールしたエージェントと通信して、安全に脅威の情報を収集することもできます。いわゆるフォレンジック調査です。詳細な調査はメーカーで専門家が行うことになりますが、従来だったら外部の専門業者に高額な費用を支払って依頼するしかなかったところが、 EDR があれば飛躍的に調査が楽になります。調査の結果問題がなかった場合、 EDR の機能で速やかに復元を行うこともできます。さらに、脅威の原因となっているファイルやプロセスを組織内のエンドポイントから一括ブロックすることもできます。同じマルウェアに攻撃される可能性をシステム全体から排除できるのです。リアルタイムにすべてのエンドポイントの脅威を検知/隔離できることが EDR の強みですが、従来はそれをマンパワーで解決していました。大企業であれば調査対象が数百台、数千台となることもあります。 1 台ずつ対応するとなれば膨大な手間がかかります。ランサムウェア攻撃はとくに時間との勝負ですので、一括操作で対策ができる EDR が効果的です。

EDR が登場する前までは、そうした対策を SOC ( Security Operation Center )に外注して 24 時間 365 日体制でネットワークやデバイスを監視し、サイバー攻撃の検出や分析をしてもらうくらいしかできませんでした。脅威を検出するには高度な技術力が必要ですので、専門家に外注するしかなかったのです。今は EDR 製品などの発達で自社内に SOC 機能を備える会社も増えました。従来型の専門家が監視する形態をディープ SOC 、 SOC 機能を持つ製品を導入して自前で運用する形態をプライベート SOC と呼ぶこともあります。だんだんと難しい話になってきましたが、この難しさは EDR の欠点でもあります。 EDR の登場でいろいろ便利になったことは間違いありませんが、使いこなすにはディープ SOC ほどではないにしても専門性の高い技術者が必要です。最近の EDR は EPP の機能も備えており、 SIEM でもあり SOC でもあります。機能が豊富な分だけ使いこなすのが難しいのです。脅威の検出が発生した場合は迅速に正しく対応しなければなりませんが、それも経験や知識がないと困難です。結局セキュリティに強い業者に外注したほうがよかった、となりかねない製品です。さらに、 SOC の外注はできても、社内調査を完全に丸投げすることはできません。 CSIRT などの社内部署と連携して対応することが必要です。いずれにしても現代のサイバーセキュリティは、高機能な製品を導入しただけでは済みません。 EDR は複雑ですので、昔のアンチウイルスソフトの運用のような感覚では太刀打ちできません。昔はマルウェアもシンプルで今と比べると運用コストが大きいものではありませんでした。いろんな会社の現場で聞いた声の中には、セキュリティチームにエース人材が割り当てられないという嘆きもありました。利益にならないうえにどれだけやっても完璧にならないセキュリティが後回しにされるのはよくわかります。コア業務に一番優秀な人を割り当てるのは自然なことです。とはいえ、 EDR は従来のアンチウイルスソフトだけでは検知が難しい Emotet やランサムウェア攻撃の対策に非常に有効なソリューションですので、必要な運用体制を作って EDR だけでもしっかり活用していきたいものです。

##### **エンドポイントセキュリティ以外の観点** 

ここまでエンドポイントセキュリティのことを説明してきましたが、組織全体を守るためには別の観点も必要です。情報流出の原因は外部からの攻撃よりも、内部要因によるもののほうが多いのです。とくに日本の場合は攻撃されることが少ないため、 7 割は内部要因による情報流出と言われています。とくにクラウド基盤は知識や経験が浅くてもとりあえず使うことができてしまいますので、ヒューマンエラーのリスクは大きくなっています。実際、世界で一番多い情報流出は Amazon S3 の設定ミスで、クローズにすべき情報を誤って公開設定してしまうことです。 S3 に限らずクラウドの設定ミスは非常に多いです。 S3 のように直接情報が漏れてしまうものはとくに目立ちますが、アップデートの多いクラウド基盤をきちんと理解して正しく使うことは簡単ではありません。本誌の読者なら設定ミスをする可能性は低いかもしれませんが、組織全体ではどうでしょうか。人材不足のこのご時世、リテラシーが十分ではないエンジニアが変更作業を行うことは避けられないと思います。こうしたことはオンプレミスではそこまで問題になりませんでした。狭く深く作業分担されていましたし、機能や基盤 UI のアップデートは頻繁にはなかったからです。また、オンプレミスは社内に閉じた環境ですので、クリックのミスでインターネットに情報が公開されるということが起こりません。こうしたリスクは EDR でカバーすることはできません。エンドポイントセキュリティやネットワークセキュリティと違う観点のセキュリティ対策が必要になります。

##### **新連載** 

### **現場から学ぶAWSのクラウドセキュリティ** 

**第 1 回**

##### **クラウドセキュリティと責任共有モデル** 

**Amazon Web Services(AWS)に代表されるクラウドの利用は、今では当たり前になりました。しかし、利用が拡大するほど、運用面でセキュリティ対策が難しくなりがちです。本連載では、大規模なクラウド環境においてセキュリティ対策を担う現場のエンジニアが、その経験や実例から、AWSを安全に運用していくための知識やノウハウを伝授します。**

花塚亮祐(はなつかりょうすけ) 株式会社サイバーエージェントシステムセキュリティ推進グループ

@___hnts

株式会社サイバーエージェントシステムセキュリティ推進グループ

##### **はじめに** 

株式会社サイバーエージェントでセキュリティエンジニアをしている花塚です。筆者は「システムセキュリティ推進グループ」という会社全体に対する横断組織に所属しています。システムセキュリティ推進グループでは、日々さまざまなセキュリティに関する業務を行っており、その中でも筆者は、おもにパブリッククラウドにおける脅威検知に関するしくみや Cloud Security Posture Management に分類されるソフトウェアの開発・運用を行っています。

Amazon Web Services ( AWS )や Microsoft Azure 、 Google Cloud を代表とするパブリッククラウドの利用は、今では当たり前のように目にすると思います。ハードウェアを自前で調達することなく、パブリッククラウドが提供するサービスを組み合わせることで、より簡単に自分たちのサービスを開発・運用できるようになりました。一方で、クラウドセキュリティを考慮し、安全に運用していくことは難しいと感じている方も多いのではないでしょうか。とくに組織の規模が大きくなるにつれて、クラウドセキュリティを考慮した運用を行うことは、より難しくなっていくと考えられます。本連載では、大規模な AWS 環境のセキュリティ対策に携わる筆者の経験をもとに、現場の実例や悩みを散りばめながら、 AWS におけるセキュリティ対策を解説していきます注1 ~~。~~ 「情報セキュリティ」を一から説明するような内容ではないため、ほかの書籍などと補完しながら読んでいただければと思います。本連載の内容が、みなさんの AWS 環境をより安全に運用するためのヒントになれば幸いです。

##### **クラウドセキュリティとは** 

AWS に限った話に入る前に、本連載のタイトルにもある「クラウドセキュリティ」という用語がどのような意味を持つのか、ここであらためて整理をしてみましょう。まず、クラウドセキュリティがどのように定義されているのか、 Google Cloud と Gartner Glossary の記事の例を見てみます。

_( Google Cloud の記事_ _<u>注2</u> の例)_

_クラウドセキュリティとは、クラウド環境内のアプリケーション、データ、インフラストラクチャの保護に使用されるサイバーセキュリティポリシー、ベストプラクティス、制御、技術のことです。_

_クラウドセキュリティは、クラウドベースのアプリケーション、データ、インフラストラクチャを保護するための一連のサイバーセキュリティ対策です。_

_( Gartner Glossary の記事_ _<u>注3</u> の例、筆者訳)_

_クラウドセキュリティとは、クラウドコンピューティングのセキュリティ、コンプライアンス、その他の利用リスクをコントロールするために使用されるプロセス、メカニズム、サービスのことである。_

これらの例のとおり、クラウドセキュリティは広い意味を持ち、人によって定義が少し異なることがわかります。しかし整理すると、「クラウドセキュリティとは、 **クラウド環境内のアプリケーションやデータ、その他リソースを保護するためのセキュリティポリシーやベストプラクティス、テクノロジーの集合体** を意味する」と言えるでしょう。

##### **クラウドセキュリティと責任共有モデル** 

用語の意味を簡単に整理できたところで、次にクラウドセキュリティの話を進めるには、避けては通れない「責任共有モデル」という概念を解説していきます。

AWS を利用するユーザーは、クラウドセキュリティにおいて、何をすればいいのでしょうか? 仮に、 Web アプリケーションを AWS

で運用しているからといって、セキュリティ対策を AWS 側に丸投げすることはできません。一方で、ユーザーが意識しなくてもよい責任の範囲も存在します。

AWS 側の責任とユーザー側の責任が存在することは、「責任共有モデル」という概念で説明できます。

# **責任共有モデルとは** 

**責任共有モデル** とは、クラウドサービスプロバイダー( CSP )とユーザー側で、パブリッククラウドのサービス利用における責任を共有(分担)するという考え方です。責任共有モデルがあるおかげで、ユーザーは、オンプレミスを利用するときと比べて、セキュリティ対策を含む運用上の負担が軽減されることになります。 AWS の場合、 AWS 側とユーザー側それぞれのクラウドセキュリティの責任を次のように表しています注4 ~~。~~

- ・ AWSの責任:クラウド **の** セキュリティ(AWS responsibility Security of the Cloud) 

- ・お客様の責任:クラウド **内の** セキュリティ(Customer responsibility Security in the Cloud) 

**図** 1のように、「クラウド **の** セキュリティ」は、 AWS 側が AWS クラウドで提供するサービスのインフラストラクチャに対するセキュリティを指します。一方、「クラウド **内の** セキュリティ」は、ユーザー側が AWS クラウドのサービスを利用したことによって生じた範囲のセキュリティを指します。

<u>https://aws.amazon.com/compliance/shared-responsibility-model</u>

「責任共有モデル」という用語は知らずとも、「 Amazon EC2 Instance の管理をしたくないから、 AWS Lambda を使ってサーバレスな環境を構築しよう」といったように、無意識に責任共有モデルの概念を利用している方もいるかもしれません。

##### **さまざまなサービスごとの責任** 

AWS 側とユーザー側が担う責任は、サービスごとに異なります。次の 2 つの要点を押さえましょう。

- ・サービスの分類による責任範囲の違い 

- ・サービスごとの具体的な責任項目 

# **サービスの分類による責任範囲の違い** 

各サービスは、 IaaS 、 PaaS 、 SaaS という 3 つのモデルに分類されます( **表** 1)。それぞれのモデルに分類されるサービスは、 IaaS > PaaS > SaaS の順でユーザー側の責任範囲が大きくなっています( **図** 2)。この各モデルの責任範囲を参考にサービス選定を行うことで、セキュリティ対策を含む運用コストを考慮した意思決定が可能となります。

# **サービスごとの具体的な責任項目** 

例として Amazon S3 を見てみましょう。ドキュメン ~~ト~~ <u>注5</u> ~~で~~ は、ユーザー側のセキュリティにおける責任を次のように明示しています。

- _・オブジェクトの所有権および暗号化を含むデータの管理。_ 

- _・アセットの分類。_ 

- _・ IAM ロールおよびその他のサービス設定を使用し、適切な権限を適用したデータへのアクセスの管理。_ 

- _・ AWS CloudTrail または Amazon S3 の Amazon GuardDuty 等のディテクティブコントロールの有効化。_ 

Amazon S3 を利用する際は、これらの項目についてユーザー側で対策を行う必要があります。このようなサービスごとの責任項目を理解して対策することで、思わぬ事故を防げるようになります。「 AWS セキュリティドキュメント」注6 ~~で~~ は、サービスごとに、ユーザーが負う責任項目とベストプラクティスなどをまとめているドキュメントを提供していますので、ぜひ目を通してみてください。

##### **AWS側のセキュリティ対策の実態** 

責任共有モデルでは、 AWS 側とユーザー側が責任を共有すると説明しました。「クラウド **の** セキュリティ( Security of the Cloud )」と説明しているとおり、 AWS 側も責任を果たしてくれているようです。 AWS 側のセキュリティに関する実態を知りたい場合は、次のリソースを活用できます。

- ・「Risk and Compliance」のホワイトペーパー注7 

- <u>注8</u> 

- ・ AWS Artifac ~~t~~ 

また、世間を騒がせるような脆弱性が登場した際にも、 AWS が提供しているサービス自体に影響が出る場合は、 AWS 側が対応します。過去に AWS 側が対応した事例を挙げると、 Meltdown ( CVE2017-5754 )と Spectre ( CVE-2017-5715 、 CVE-2017-5753 )や Log4Shell ( CVE-2021-44228 )といった脆弱性がありまし ~~た~~ <u>注9</u> ~~。~~ このような対応の情報は、 AWS が提供しているセキュリティ速報 <u>注10</u> ~~で~~ 確認できますので、定期的にチェックしておきましょう。

###### **COLUMN** 

##### **セキュリティ対策の全体像** 

責任共有モデルを前提にすれば、ユーザーはサービスごとに明示されるセキュリティ対策をひとつひとつ実施していけばよさそうです。しかし、対策の過不足や効率性を考えると、セキュリティ対策の指針がほしいと思うかもしれません。とくに、組織全体でAWSのセキュリティを向上させるための施策を推進していく場合は、計画を立てるために指針が必要になると考えられます。米国国立標準技術研究所(NIST ~~)~~ <u>注A</u> ~~が~~ 策定したNIST Cybersecurity

Framework(以降、CSF ~~)~~ <u>注B</u> ~~は~~ 、 **表** Aの5つの機能を示し、各機能を支援するための具体的なセキュリティ管理策を提供しています。これを参照することで、1つのサービスだけに着目するのではなく、セキュリティ対策を大局的にとらえられるようになります。本来CSFは、具体的なセキュリティ管理策を知るためだけでなく、CSFを構成する複数の要素注C ~~を~~ うまく組み合わせて使いながら、組織のセキュリティ体制を継続的に改善していくためのフレームワークです。独立行政法人情報処理推進機構(IPA) によって日本語に翻訳された記事注D ~~が~~ ありますので、参照することをおすすめします。また、AWSは、CSFを導入したい(している)組織向けに「AWSクラウドにおけるNIST CSFへの準拠 ~~」~~ <u>注E</u> ~~と~~ いうホワイトペーパーを公開しています。ホワイトペーパーでは、識別、防御、検知、対応、復旧の各機能に対応するための説明があり、その対応時に使えるAWSサービスが紹介されています。⻑期的かつ戦略的にセキュリティ施策を進めていきたい方にとっての補完的なリソースとなるでしょう。注C) CSFは、コア、ティア、プロファイルの3つの要素から構成されています。 (本文に戻る)

##### **現実世界で起きているセキュリティインシデント** 

ここまで責任共有モデルを中心に話を進めてきました。ユーザー側が責任をまっとうしない場合、セキュリティインシデント(以降、インシデント)が発生する可能性がありますが、 AWS におけるインシデントは、実際に起きているのでしょうか? また、その原因は何なのでしょうか?

最近 AWS を利用し始めたばかりの方や、ある程度の期間利用しているがまだインシデントを経験していない方は、あまりイメージが湧かないかもしれません。そこで、実際に起きたインシデントに関する情報を少しのぞいてみましょう。

2022 年末、 DATADOG Security Lab ~~s~~ <u>注11</u> ~~が~~ 、「 A retrospective on public cloud breaches of 2022, with Rami McCarthy and Houston Hopkins ~~」~~ <u>注12</u> ~~と~~ いう記事を公開しました。この記事では、「 awscustomer-security-incidents ~~」~~ <u>注13</u> ~~や~~ その他の情報をもとに、 2022 年に起きたパブリッククラウドにおける( AWS 以外も含む)インシデントのおもな原因をまとめています。記事によると、おもな原因は次のように 4 つ紹介されています。

- _・公開された静的かつ⻑期間有効なクラウド認証情報_ 

- _・認証なしでインターネットに公開されているElasticsearchインスタンス_ 

- _・認証なしでパブリックからアクセスできるデータストレージサービス_ 

- _・ Server-Side Request Forgery(SSRF_ _~~)~~_ _<u>注14</u>_ _~~の~~ 脆弱性の悪用によるアプリケーションのクラウド認証情報の盗難_ 

- _(筆者訳)_ 

筆者は今までに複数のインシデントに遭遇したことがありますが、上記の原因は、過去の経験とも一致しているように感じます。とくに「公開された静的かつ⻑期間有効なクラウド認証情報」によるインシデントは、一度は Twitter やニュースなどで目にしたことがある方も多いのではないでしょうか。 GitHub リポジトリなどにクラウドの認証情報を誤ってコミットしてしまい、悪意のある第三者に利用されてしまうといったケースです。このような意外とシンプルかつある程度共通した原因によるインシデントが、日々現実世界では起きているのです。まだインシデントを経験していない方も、セキュリティに関するモニタリングをある程度の水準まで整えてみると、今まで気づくことができなかったインシデントが発覚するかもしれません。ここではユーザー側の責任範囲で起きたインシデントを紹介しましたが、 AWS 側( CSP 側)の責任範囲でインシデントが起こる可能性があるということも覚えておきましょう。

##### **まとめ** 

クラウドセキュリティに関する基本用語の説明や現実世界で起きているセキュリティインシデントの情報を紹介しました。今回の要点は次のとおりです。

- ・「責任共有モデル」という概念が存在し、AWS側とユーザー側で責任を分担している 

- ・ユーザーは責任共有モデルのもと、各サービスごとに明示された責任を理解し、対応する必要がある 

- ・意外とシンプルかつ、ある程度共通した原因によるインシデントが、日々現実世界では起きているとくに「責任共有モデル」の話は、 AWS を利用するうえでまずは理解しておきたい基本的かつ重要なトピックでした。「 AWS を利用しているから安全」というわけではなく、ユーザー側にも責任が存在し、対応すべき項目が存在するということがご理解いただけたでしょうか。ぜひ、この機会に「責任共有モデル」の理解を深めてみてください。前述したとおり、「公開された静的かつ⻑期間有効なクラウド認証情報」は、インシデントのおもな原因の 1 つで、誰もが一度は目にしたことがあるような身近なものです。次回は、この原因を防ぐことにつながる「 IAM ユーザーと認証情報」のお話をします。また、今後の本連載では、 AWS のサービス、 OSS 、 SaaS を活用したハンズオンも交えて話を進めていく予定ですので、お楽しみに。注1) 弊社には、複数のAWS Organizationsと約700のAWSアカウントがあります。 <u>https://developers.cyberagent.co.jp/blog/archives/41119/</u> (本文に戻る) 注9) 

<u>https://aws.amazon.com/jp/blogs/news/processor_speculative_execution_research _disclosure/,https://aws.amazon.com/jp/security/security-bulletins/AWS-2021-006/</u> (本文に戻る)

注13) AWSを利用している組織で起きた実際のセキュリティインシデントの一覧などを提供しているGitHubリポジトリ。 <u>https://github.com/ramimac/aws-customer-security-incidents</u> (本文に戻る)

### **機械学習を継続的に運用していくための MLOpsのすすめ** 

澁井雄介(しぶいゆうすけ) Launchable Inc.

**第2回 MLOpsのためのチームとスキルセット**

**機械学習が本格的に活用されるようになると、従来のソフトウェア開発スタイルでは対応が難しい課題が生まれました。その状況を打破するための新たな方法論が「MLOps」です。本連載では、 MLOpsの概要や背景といった基本的な部分から、システムに導入するための実践的なノウハウまで紹介します。**

**MLOpsのためのチームとスキルセットを考える**

こんにちは、澁井(しぶい)と申します。前回、本誌 2023 年 8 月号から機械学習を実用化し運用するために MLOps について執筆しています。筆者は本業では Launchable Inc. というアメリカのテックスタートアップで機械学習関連のソフトウェアエンジニアとして働きつつ、副業先の NOT A HOTEL でバックエンドエンジニア、 mybest で機械学習エンジニアとして働いています。

8 月号では、 MLOps のための方法論や機能について E- Commerce ( EC )の推薦システムを軸に説明しました。

MLOps ( Machine Learning Operations )とは機械学習( ML )を組み込んだシステムやプロダクトの開発、運用( DevOps )を円滑に行うための方法論です。 MLOps は DevOps の概念を機械学習に適用したものです。具体的には、データ収集、機械学習モデル開発、デプロイメント、更新、監視、評価など、機械学習およびシステムのライフサイクル全般を効率的に管理し改善することを目的としています。今回は MLOps を実現するためのスキルセットやチームのメンバー構成について説明します。

###### **必要なのはスピードと見極める能力** 

一般的なソフトウェア開発や DevOps がそうであるように、 MLOps や機械学習の実用化にも相応の専門性や技術力が必要になります。機械学習が世の中で注目され始めたのが 2010 年代半ばで、その方法論もまだ数年程度の歴史しかありません。しかも機械学習や AI は昨今の生成 AI や LL ~~M~~ <u>注1</u> ~~の~~ ように、 1 年前には不可能と思われていたことが突発的な技術革新によって可能になり、世界的なブームになることもまれではありません。必要なスキルセットは常に変化しています。そのため、 AI や機械学習を実用化するうえで必要なスキルセットは、新しい技術を身につけて取り入れるスピードと、不要な技術と見込みのある技術を見極める能力と言えます。こうした能力を身につける確実な方法があるかと問われれば、明確な回答は難しいでしょう。しかし筆者の経験から意見を述べると、前例のない技術を用いた新しいプロダクトを世の中に出し、運用し、成功と失敗を繰り返す経験は必要です。広い大地に一筋ついた道をたどり来るだけだと、道がなくなったところから足を踏み出すことができません。その中で失敗した技術選定や、そこからリカバリーする経験を通して、未知の技術を実用化するための見極める力と実装力が身についていくでしょう。話が逸れましたが、このように新しい技術がどんどん出てくる AI や機械学習の世界において、 MLOps を実現するために必要なスキルセットとチームメンバーの構成案を説明します。ただ単に一般的なことを説明してもおもしろくないので、今回は検索システムを開発するチームを例に説明します。

###### **検索システム** 

検索は多様なシーンで使われます。有名どころでは Web 検索の Google や Bing がありますし、 E-Commerce や SNS の中にも検索はあります。企業によっては社内データやドキュメント向けの検索システムもあるでしょう。必要な情報にすばやくアクセスできることはビジネスを効率化し、生活を便利にするために重要です( **図** 1)。一般的に検索で実行されていることは、テキストや条件を入力して関連するドキュメントや記事、データを得ることです。しかし検索対象の情報や検索条件がテキストに限定される必要はありません。たとえばファッション誌を見ていて似ている衣服を検索したいとき、テキストでその衣服を表現して検索するよりも、写真を撮って被写体に似ているものを検索するほうが効率的でしょう。画像に限らず音楽や動画についても同様のことが言えます。こうしたニーズに応えるためには、テキストだけでなく画像などのモーダル(情報の状態)を用いて検索する体験が求められます。今回は、多様なモーダルを用いた検索システムにおける機械学習の活用を考えます。

###### **多様なモーダルによる検索** 

テキストや画像、音声などを組み合わせたプロダクトとして SNS があります。 Twitter や Facebook のような SNS では、ユーザーがコンテンツを投稿し、公開します。コンテンツにはテキストや写真、動画、好きな音楽などが含まれることがあります。ユーザーは同時に閲覧者にもなり、他ユーザーの公開したコンテンツを閲覧します。 SNS でフォローしているユーザーや話題の投稿を検索するとき、多くの場合はキーワードやタグで検索すると思います。しかし自分の持っている画像や音声に似ているコンテンツを検索したい場合、そのコンテンツを説明する文章を書いて検索することは効率的といえません。というのも、「 1. 検索キーワードがそのコンテンツを適切に説明できるとは限らない」「 2. 投稿者がそのコンテンツに説明を添えているとは限らない」ためです。コンテンツからコンテンツを検索できるシステムがあると便利でしょう。画像から画像を検索するシステム(画像検索)を考えます( **図** 2)。自分の持っている写真やイラストを SNS の検索システムにアップロードし、似ている見た目の投稿画像を検索するしくみです。画像検索は、最近の Web 検索や E-Commerce でも提供されている技術になります。画像検索を実現するメジャーな方法はおおむね次の流れになります( **図** 3)。

1. 開発:検索対象の画像をインデックス化 

   - a. ディープラーニングで検索対象の画像を特徴量(画像の特徴を表した数値表現)に変換する 

   - b. 特徴量同士で近いものを検索する近傍探索インデックスを作成する 

2. デプロイ:画像検索システムをデプロイ 

   - a. 1.a.で使用したディープラーニングモデルと1.b.で作成した近傍探索インデックスをサーバに設置して、Web APIや非同期処理ジョブとしてデプロイする 

3. 推論:入力画像から似ている画像を検索 

   - a. 1.a.で使用したディープラーニングモデルを使用して、ユーザーの入力画像を特徴量に変換する 

   - b. 1.b.で作成した近傍探索インデックスから特徴量の近い画像を選定する 

   - c. 選定した画像をユーザーに返却する画像検索をユーザーに提供するユーザーインターフェースが必要になります。たとえばスマホで SNS を提供している場合、スマホアプリに画像検索機能を追加することになるでしょう。画像検索を用いた体験の作り方にも複数の方法が存在します。たとえば、ユーザーがスマホで写真を撮影して、その写真を SNS 画像検索機能にアップロードすると、似ている画像の投稿を検索して提示する、というものが考えられます。または SNS に投稿済みの画像 A から似ている画像を検索しておいて、画像 A を閲覧したユーザーに似ている画像をレコメンドする、という体験も可能でしょう。類似画像検索によって実現可能な体験はさまざまです。 

###### **特徴量による近傍探索** 

今回は画像を特徴量に変換して近傍探索するしくみをベースに書いていますが、同様の技術は画像以外でも活用可能です。テキストや音声においても、ディープラーニングモデルを使うことで、そのデータに内在する特徴を数値化できます。最近流行している GPT などの LLM ( Large Language Model )においても、テキストから特徴量を抽出して関連するコンテンツを検索する技術が活用されています。従来のテキスト検索では、検索対象のコンテンツはクエリの単語や類似語に一致する必要がありました。しかしクエリとコンテンツ双方を特徴量に変換することで、単語や類似語が一致しなくても、意味的に近いコンテンツを検索することが可能になります。特徴量を用いた近傍探索は今後さらに広まっていくでしょう。

###### **画像検索システムの実現に必要なスキルセットとチーム構成** 

ここまで画像検索システムのしくみについて概観してきました。続いて、画像検索システムを実現し、ユーザーに届けるためのスキルセットとメンバーを説明します。

###### **スキルセットの要件** 

企画段階では、どういう課題をどういうプロダクトで解決するのか、具体的な価値と要件を決めるプロダクトマネジャーは必須です。どんなに能力の高いエンジニアがそろっても、プロダクトに価値がなければ意味がありません。プロダクトマネジャーは重要なユーザー課題を選定し、解決策を策定して、プロダクトの成功の条件を決めます。今回は自社の SNS におけるコンテンツ検索を改善するため、画像検索というプロダクトを開発すると決めます。もちろん、プロダクトマネジャーだけでプロダクトに関するすべての事柄を決めるわけではありません。具体的な技術選定や可否の判断にはエンジニアの協力が必要です。とくに今回のような機械学習を用いたプロダクトの場合、解決したい課題が現実的な仕様(精度、スピード、コストなど)で解決できるのか、専門家の見解と検証が必要になるでしょう。ここで各種エンジニアの協力が必要になります。まず、ディープラーニングを含めた画像処理の技術は必須でしょう。また、一般的に画像データは容量が大きくなるため、画像を保存し、取得するためのストレージや基盤を扱うインフラやプラットフォームの技術も必要になります。加えて「多様なモーダルによる検索」項の「 2. デプロイ」で書いているように、画像検索システムをデプロイして起動するためのバックエンド開発が必要です。ディープラーニングの推論と近傍探索インデックスによる検索は多くの場合、テキスト検索やデータベースのクエリよりも遅いため、サービスレベル次第では高速化や迂回策が必要になります。テキストからコンテンツを検索することに慣れている人は多いでしょうが、画像などのコンテンツから検索する体験には慣れていない可能性があります。そうしたユーザーに画像検索を迷わず使ってもらうため、 UI デザインやフロントエンドの工夫が必須になります。もちろんですが、 MLOps ( Machine Learning Operations )である以上、機械学習の実用化とともに運用も必要なタスクになります。画像検索システムの運用にはどういう技術が必要でしょうか? SNS では投稿が増えればデータも増えるため、画像データ量も増えていきます。追加されていくデータを整理して、新たな近傍探索インデックスの作成に必要なワークフローエンジンを安定して稼働させる技術が必要になります。加えて、新たな近傍探索インデックスを本番システムにリリースするパイプラインが必要になります。さらには画像検索システムの監視通報や、障害発生時の復旧対応、そして画像検索の投資対効果の評価が必要です。まとめると、画像検索システムを実現するためには画像処理、インフラ、バックエンド、フロントエンド、 MLOps 、 QA のスキルセットを持ったメンバーが必要になります。

**メンバーとチーム**

それではメンバーのポジションと求められるスキルセットを具体的に整理します( **表** 1)。現実的には複数の領域を 1 人でカバーすることはまれではありませんし、逆に 1 つの役割を複数名のチームで担うこともあります。人数は企業やビジネスの規模しだいで上下することはご承知ください。こうして概観すると、画像検索システムを作るためには多様なスキルセットが必要で、それらを有するメンバーをチームとして構成する必要があることがわかります。異なる立場やスキルのメンバーをチームとして運営するためには、意思決定と運営(マネジメント)を担当する役割が必要になります( **表** 2)。

###### ◆ ◆ ◆ 

今回は機械学習を用いた画像検索をテーマにして、プロダクト開発に必要なスキルセットとメンバー、役割を整理しました。企業やチームによってアサイン可能なメンバーやスキルセットは多種多様であるため、今回説明した要件を満たすメンバーを集められるとは限らないでしょう。あくまで一般論としてのチーム構成として参考になると幸いです。なお、今回説明した画像検索システムの実装は、筆者が 2022 年に著した『現場で使える!機械学習システム構築実践ガイド』(翔泳社刊)注2 ~~で~~ サンプルコードを含めて説明しています。もし興味があればぜひご一読ください。次回は MLOps についてより技術的な視点から、機械学習を用いたプロダクト開発、運用に便利なツールやシステム構成を説明します。

> 注1) Large Language Modelの略称。巨大な言語モデルのことで、2022年後半から話題になっているChatGPTはその実用例。 

(本文に戻る)

#### **<mark>第2回位置情報に触れる・加工する(前編)</mark> 自分だけのマップアプリを作ろう!** 

### **位置情報エンジニアリングのすすめ** 

**「位置情報」は、アプリ、ゲーム、気象・災害、オープンデータ、デジタルツイン、DX などの昨今のトレンドを支える技術として、重要度が増しています。本連載では、位置情報技術の概観を学びつつ、具体的なアプリケーションを開発するための要素技術も紹介します。**

鈴木祐亮(すずきゆうすけ) 株式会社MIERUNE | Code for Sapporo <u>yuskesuzki @yuskesuzki</u>

###### **はじめに** 

株式会社 MIERUNE にてバックエンドのエンジニアとして勤務している鈴木です。連載 1 回目(本誌 2023 年 8 月号)を担当した井口に代わり、これから何回か連載を担当します。よろしくお願いします。前回は位置情報について周辺技術も含めた概要的な内容でした。第 2 回と第 3 回では位置情報が含まれるデータを実際に作ったり、位置情報を含むデータをさまざまな方法を用いて加工したりしてみます。とはいえ内容はそれほど難しくならないように努めますので、位置情報についてよくわからないという方も安心して読み進めていただければと思います。

###### **前回のおさらい** 

まずは前回のおさらいとなりますが、位置情報システム( GIS ) でおもに使われるベクトルとラスター、それぞれのデータの特徴、および、使われるデータフォーマットについて振り返ります。

# **ベクトルデータ** 

ベクトルデータは地図上に「点」「線」「面」の幾何学的形状を描くために、経緯度の値を始点・終点・頂点の座標として設定したデータです。次の 3 つの形状を扱います注1 ~~。~~

- ・点(ポイント:Point) 

- ・線(ライン:LineString) 

- ・面(ポリゴン:Polygon) 

ベクトルデータは座標情報をもとに描画を行うため、ユーザーが地図をどんどん拡大していっても、画像データのように拡大により形がギザギザに荒れることがありません( **図** 1)。ベクトルデータをデータベースで取り扱う場合によく使われるのが、オープンソースのデータベースソフトウェアである PostgreSQL の拡張プラグイン「 PostGIS ~~」~~ <u>注2</u> ~~で~~ す。 PostGIS データベースでは Geometry ~~型~~ <u>注3</u> ~~と~~ いう PostgreSQL の拡張データ型を用いて位置情報を管理します。 GIS で使うデータベースでは、位置情報を管理する列に位置情報をベクトルデータで保存するだけでなく、ほかの列に位置にひも付けたい関連情報もレコードで管理できます。ベクトルデータを扱うことのできるファイルフォーマットでも、 GIS データベースと同様、位置情報にひも付けたい情報を属性情報として関連付けることができます。ベクトルデータを扱うファイルフォーマットには次のようなものがあります。

- ・ GeoJSON:RFC 79467で規定された構造をもつJSON文字列。テキスト形式で扱いやすく、WebGISで標準的に利用されている 

- ・ ESRI Shapefile:商用のGISツールであるArkGISのために作られたベクトルデータ管理のファイル群。実体としてはshp/shx/dbfなど、異なる拡張子を持ったファイル群で、これらがすべてそろわないとデータの表示ができない 

- ・ GeoPackage:オープンソースのGISソフトQGIS ver3以降の標準ファイルフォーマット。ファイルデータベースSQLiteを拡張しており、SHPファイルのような複数のファイル群は扱わない 

# **ラスターデータ** 

地図上に航空写真や衛星写真など、写真自体を貼り付けたい場合、位置情報を含む画像ファイルであるラスターデータを用いることができます。ラスターデータには次のようなものがあります。

- ・ GeoTIFF:ビットマップ画像を格納するTIFF画像形式について、画像のヘッダ情報部に投影法、座標、楕円体、測地系などの位置情報を付与した画像形式のこと 

- ・ PNG/JPEG/WebP:Webコンテンツでもおなじみの画像形式。タイルと呼ばれるディレクトリ構造にまとめてWeb用の背景地図として利用したり、位置情報を格納したワールドファイルと呼ばれるテキストファイルと併せて作成・配置したりすることで、位置情報に対応した画像ファイルとして利用できる 

###### **COLUMN 位置情報を扱うデータベース製品「WKB」「WKT」について** 

位置情報に対応しているデータベースは、オープンソースのデータベースである PostgreSQL(PostGIS)/MySQL/MariaDB、商用製品のSQL Server/Oracleなどがあります。各データベース製品のGeometry型に格納するデータはバイナリ文字列の WKB(Well-Known Binary)で定義します。WKBはデータベースに格納するのに効率が良いですが、人間にとっては解読が困難な文字列です。このため人間が解読したり値を定義したりする際に使われるマークアップ言語のWKT(Well-Known Text) もあります。

###### **GeoJSONのデータ構造と特徴** 

ここからは実際に位置情報データを作ってみましょう。ここで作るのはベクトルデータの GeoJSON です。

GeoJSON は、表現する地物を 1 つだけ管理するか、複数管理するかによってデータ構造が異なります。

1 つの地物だけを管理する場合は、 Feature オブジェクトを使用します。 Feature オブジェクトは、内部に geometry オブジェクトと properties オブジェクトを持つコンテナです。地図上に描きたい形状は geometry オブジェクトで表現します。 geometry オブジェクトは type 属性と coordinates 属性を持ちます。 type 属性で、描く形状種類を点( Point )、線( LineString )、面 ( Polygon )の中から指定します。そして coordinates 属性で、 type で選択した形状の座標(経緯度の値)を指定します。描く形状によって coodinates 属性のリスト構造は次のように変わります( **リスト** 1)。

- ・ Point:経度・緯度の順で値を設定した座標リスト 

- ・ LineString:線の始点/終点を表す2点(Point)の座標リストのリスト 

- ・ Polygon:面の頂点となる座標(Point)を指定。2点以上描き、最後に始点の座標を指定する 

###### **▼リスト1 Featureオブジェクトの構造** 

**<mark>↓Pointの場合</mark> { "type": "Feature", "geometry": { "type": "Point", "coordinates":[ 139.770414664973885, 35.677812882188846 ] } }** **<mark>↓LineStringの場合</mark>**

**{ "type": "Feature", "geometry": { "type": "LineString", "coordinates": [ [138.7309, 35.3628], [138.8079, 35.1983], [139.0248, 35.2248] ] } }** **<mark>↓Polygonの場合</mark> { "type": "Feature", "geometry": { "type": "Polygon", "coordinates": [ [ [138.6199950499569,36.50168431819576], [138.6199950499569,35.93179561893332], [139.56347310951605,35.93179561893332], [139.56347310951605,36.50168431819576], [138.6199950499569,36.50168431819576]** **<mark>↑最初の座標に戻る</mark> ] ] } }**

properties オブジェクトには地物に関連させる情報を持たせます。 JSON として解釈できる形になっていれば、設定するキーと値はどんなものでもかまいません( **リスト** 2)。

**▼リスト2 Featureオブジェクトに含むpropertiesオブジェクトの構造**

**{ "type": "Feature", "geometry": {**

**"type": "Polygon", "coordinates": [[ [138.7309, 35.3628], [138.8079, 35.1983], [139.0248, 35.2248], [138.7309, 35.3628] ]] } "properties": { "description": "三角形になるよ" "descriptions": [ "頂点1", "頂点2", "頂点3" ] }, }**

複数個の地物を表現するときに使うのは、 FeatureCollection オブジェクトです。これは、複数個の Feature オブジェクトをリストとして持つコンテナです。 FeatureCollection オブジェクトを定義し、その内部に features リストが定義され、この中にひとつひとつの地物を表す Feature オブジェクトを追加していきます( **リスト** 3)。

###### **▼リスト3 FeatureCollectionの構造** 

**{ "type": "FeatureCollection", "features": [** **<mark>Featureオブジェクトのリスト</mark> ] }**

###### **COLUMN ポリゴンの細かなルール** 

ポリゴンには、頂点を結んだ線分を右回りに結んで指定するルール(Right-Hand Ruleといいます)があります。このルールに従わないとGeoJSONを処理するライブラリや処理系によっては面が正しく描けないこともあるので、指定するときには注意が必要です。また、ねじれた形(自己交差といいます)を組むと、後にGISツールなどで処理する際に問題が発生する場合があります。自己交差したポリゴンはQGIS などのGISツールを用いて修正できます。ポリゴン内部に穴を開けたい場合、 coordinatesのリストに開けたい穴のポリゴンを含めることで実現できます( **図** A **、リスト** A)。

###### **リストA 穴あきポリゴンの定義** 

**{ "type": "Feature", "geometry": { "type": "Polygon", "coordinates": [ [ [138.6199950499569,36.50168431819576], [138.6199950499569,35.93179561893332], [139.56347310951605,35.93179561893332], [139.56347310951605,36.50168431819576], [138.6199950499569,36.50168431819576] ], [ [138.87685818659048,36.32677626810349], [138.87685818659048,36.09162303783707], [139.32142899999553,36.09162303783707], [139.32142899999553,36.32677626810349],**

**[138.87685818659048,36.32677626810349]**

**]** **<mark>←ポリゴンに穴を開けたい場合、開けたい穴のポリゴン座標も定義する</mark> ]**

**} }**

###### **CSVで位置情報を扱ってみる** 

GeoJSON のデータ構造がわかったところで、次に位置情報のデータを集めてみます。ここまで説明した GeoJSON はテキストエディタで作るのはなかなかしんどいため、生成ツールを用いることにして、このツールに渡す位置情報を含む CSV ファイル作ってみることにします。

# **CSVファイルに経緯度を管理する列を設定** 

CSV ファイルに位置情報を埋め込む列を指定します。今回はいくつかの点データを作ろうと思うので、経度と緯度を示す列は 1 つずつ、それぞれ lon と lat という名前で列を定義します。

# **属性情報の列を決定** 

経緯度のデータだけでも地図上に点を置くことはできますが、何を示す点なのかわかりません。位置に結び付ける属性列も併せて定義します。今回は、筆者の好きな北陸新幹線の停車駅の位置情報を集めて、駅がどこにあるかを点で示すデータにしてみます。このデータの場合、属性に設定するのは、駅名と路線名、都道府県名、ひとまずはこんなところでしょう( **表** 1)。

# **経緯度情報をOpenStreetMapから集めてみる** 

経緯度の情報源はどうしましょうか。 Web で手軽に見られる Web 地図の代表格として Google Maps が挙げられますが、この地図に掲載されているデータは著作権が設定されているため、実は扱いに注意が必要なデータです注4 ~~。~~ このため、今回は全世界の有志たちの手により位置情報の登録やメンテナンスが日々行われているオープンデータの地図、 OpenStreetMap ( OSM )から経緯度情報を取得してみたいと思います。

OSM から経緯度情報を取得するには、次の手順で行います。

# **1. OSMを開く** 

お手持ちのブラウザを使って、 <u>https://www.openstreetmap.org/ を開</u> きます( **図** 2)。

# **2. 検索フォームに入力する** 

OSM の画面左上に検索フォームがあります。ここに「東京駅」と入力してみましょう。すると、 OSM に登録されている「東京駅」に関する検索結果の一覧が表示されます( **図** 3)。

# **3. 検索結果から適切なノードを選択し、経緯度情報をコピーする** 

「東京駅」の検索結果だと、バス停や地下鉄駅などいろいろありますが、 JR の東京駅になりそうな結果を一覧や地図画面から探してクリックしてみます。するとノード情報が表示されます。ここの「場所」欄に東京駅の経緯度が表示されているので、カーソルで選択してから値をコピーします( **図** 4)。コピーした値で、 CSV ファイルの lon と lat 列を埋めてみましょう( **表** 2)。

###### ◆ ◆ ◆ 

ほかの停車駅についても、同じようにして情報を集めていきます。北陸新幹線の停車駅について東京から終点までたどっていくと、 **リスト** 4のような CSV のデータができあがっていくでしょう。

**▼リスト4 北陸新幹線で停まる駅の経緯度情報となるCSVデータ lon,lat,駅名,路線,都道府県 139.7671861,35.6810912,東京駅,北陸新幹線,東京都 139.7765214,35.7123457,上野駅,北陸新幹線,東京都 139.6243304,35.9063869,大宮駅,北陸新幹線,埼玉県 139.3902414,36.1394555,熊谷駅,北陸新幹線,埼玉県 139.1794855,36.2188607,本庄早稲田駅,北陸新幹線,埼玉県 139.0126095,36.3223727,高崎駅,北陸新幹線,群馬県 138.8493964,36.3625792,安中榛名駅,北陸新幹線,群馬県 138.6351092,36.3425512,軽井沢駅,北陸新幹線,⻑野県 138.4643868,36.2781104,佐久平駅,北陸新幹線,⻑野県**

**138.2495261,36.3965369,上田駅,北陸新幹線,⻑野県 138.1888930,36.6427661,⻑野駅,北陸新幹線,⻑野県 138.3587460,36.8454839,飯山駅,北陸新幹線,⻑野県 138.2485139,37.0813089,上越妙高駅,北陸新幹線,新潟県 137.8617531,37.0433062,糸魚川駅,北陸新幹線,新潟県 137.4804050,36.8733083,黑部宇奈月温泉駅,北陸新幹線,富山県 137.2128269,36.7016185,富山駅,北陸新幹線,富山県 137.0120241,36.7269446,新高岡駅,北陸新幹線,富山県 136.6481510,36.5780366,金沢駅,北陸新幹線,石川県**

###### **CSVからGeoJSONに変換してみる** 

位置情報を含んだ CSV データがひとまず完成したので、このデータから GeoJSON に変換を行っていきます。 **リスト** 5のように Python で簡単なプログラムを組みます。

###### **▼リスト5 CSVからGeoJSONに変換するPythonプログラム** 

**import csv import json import copy** **<mark># csvファイル名</mark> csv_file = 'data.csv'** **<mark># geojsonファイル名</mark> geojson_file = 'data.geojson'**

**<mark># csvファイルを読み込む</mark> csv_data = csv.reader(open(csv_file, 'r'),quotechar='"')** **<mark># GeoJSONのヘッダー部分</mark>**

**<mark># GeoJSONのfeature_collectionを表すオブジェクト</mark> feature_collection = { "type": "FeatureCollection", "features": [] }**

**<mark># GeoJSONのfeatureを表すオブジェクト</mark> feature_tmpl = { "type": "Feature", "geometry": { "type": "Point", "coordinates": [0, 0] }, "properties": { "駅名": "", "路線": "", "都道府県": "" } }** **<mark># csvデータをGeoJSONに変換する</mark> for row in csv_data:** **<mark># csvデータの1行目の要素はヘッダーなのでスキップ</mark> if row[0] == 'lon': continue** **<mark># GeoJSONのフィーチャー部分</mark> feature = copy.deepcopy(feature_tmpl) feature['geometry']['coordinates'] =[float(row[0]), float(row[1])] feature['properties']['駅名'] = str(row[2]) feature['properties']['路線'] = str(row[3]) feature['properties']['都道府県'] = str(row[4]) feature_collection['features'].append(feature)**

**<mark># GeoJSONファイルを出力</mark> f = open(geojson_file, 'w') json.dump(feature_collection, f, ensure_ascii=False,indent=4) f.close()**

このプログラムを実行すると、 CSV ファイルから GeoJSON ファイルが生成されます。生成された GeoJSON ファイルをテキストエディタで開くと、 **リスト** 6のようなデータが記載されていることが確認できます。

###### **▼リスト6 CSVからGeoJSONに変換したデータ** 

**{ "type": "FeatureCollection", "features": [ { "type": "Feature", "geometry": { "type": "Point", "coordinates": [ 139.7671861, 35.6810912 ] }, "properties": { "駅名": "東京駅", "路線": "北陸新幹線", "都道府県": "東京都" } },** **<mark>(..略..)</mark> { "type": "Feature", "geometry": { "type": "Point", "coordinates": [ 136.648151, 36.5780366 ] }, "properties": { "駅名": "金沢駅", "路線": "北陸新幹線", "都道府県": "石川県" } } ] }**

###### **生成したGeoJSONを表示してみる** 

生成ツールによって CSV ファイルから生成した GeoJSON データを表示させてみましょう。ここではツールを使った 2 つの方法を紹介します。

# **QGISを使って表示する** 

QGIS はフリーに利用でき、オープンソースで開発が進められている地理情報システムのアプリケーションです。 <u>https://qgis.org/ja/site/forusers/download.html にアクセスすると各種</u> OS 版の QGIS 最新版のインストーラがダウンロードできます。お使いのマシンにインストール後、 QGIS を起動します。ウィンドウに GeoJSON ファイルをドラッグ & ドロップすると GeoJSON データの表示が行われますが、背景は白いままです。ここで、[ブラウザ]パネルの「 XYZ Tiles 」以下にある「 OpenStreetMap 」を、[レイヤ]パネルにある GeoJSON ファイル ( data )の下に追加することで、 OSM を背景地図にできます。これで **図** 5のように、生成した GeoJSON データが OSM で調べた経緯度の場所に表示されていることがわかります。

# **geojson.ioを使って表示する** 

geojson.io は Web ブラウザで GeoJSON データを表示・編集・保存するためのツールです。 <u>https://geojson.io/ にアクセスしたあと、右側ペ</u> インの GeoJSON 表示部に作成した GeoJSON ファイルをドラッグ & ドロップすると、 GeoJSON ファイルで定義したポイント一覧が表示されます( **図** 6)。

###### **おわりに** 

今回は位置情報フォーマットのふりかえり、および、 CSV にまとめた位置情報から GeoJSON を生成してみました。意外と位置情報を作るのは簡単だったなと思った方も多いかもしれません。第 3 回では今回と別の方法を用いて、位置情報を含むデータの加工や生成を行ってみたいと思います。お楽しみに。

- 注1) MULTIPOINT、MULTILINESTRING、MULTIPOLYGONといった、幾何学形状情報を複数束ねた派生形のデータ型もあります。 (本文に戻る) 

- 注2) PostgreSQLにオープンソースで開発されたGIS拡張モジュールを有効にさせたもの。 

   - (本文に戻る) 

- 注3) デカルト座標で座標を扱うGeography型というのもあります。 (本文に戻る) 

- 注4) Google Mapsのデータを商用利用する場合は、正しく契約を行わないとあとあと面倒なことになるので注意してください。 

(本文に戻る)

###### **エンジニア** 

## **心くすぐられる技術要素が満載新時代の分散型SNS Nostr** 

###### **第3回** 

##### **Nostrの関連サービスを見てみよう** 

鎌倉光臣(かまくらあきおみ) __@akiomik.github.io

今、分散型SNSが再び脚光を浴びています。その中のひとつ「Nostr」は、リレーと呼ばれる個々のサーバを使ってイベントを周知するという一風変わったしくみで実現されています。本連載では、仕様と実装の両面からその技術的なおもしろさを読み解いていきます。

##### **はじめに** 

Nost ~~r~~ <u>注1</u> ~~は~~ 分散 SNS の中でもひときわ分散した構成をとっています。そのため、 Nostr の関連サービスも通常の中央集権的なサービスとは異なったものが数多く存在します。今回は Nostr の関連サービスを見ていくことで、 Nostr を利用する際のイメージやしくみの理解を深めていただきたいと思います。

##### **Nostr特有のサービス** 

第 1 回・第 2 回(本誌 2023 年 7 月号・ 8 月号)と紹介してきたとおり、 Nostr はシンプルなリレー群と多機能なクライアントによって構成されており、データのやりとりは WebSocket を通して非同期的に行われます。中央的なサーバが存在しないことで可用性を高めていますが、必要なデータがどこにあるのかわからないというデメリットもあります。この節では、 Nostr の分散構成に由来する特有のサービスを見ていきます。

**nostr.com**

nostr.comは Nostr の公式サイトです。 nostr.com にはプロトコルに関するドキュメントだけでなく、プロフィールや投稿をシェアするための OGP ( Open Graph Protcol )対応のパーマリンク機能があり、「 https://nostr.com/:id 」のような URL でアクセスすることで利用できます。「 <u>:id 」の部分にユーザーの公開鍵(</u> Nostr では実質的にユーザー ID のように機能します)や投稿の ID などを入れることで、プロフィールや投稿の内容を外部のユーザーにシェアできるというものです。

Twitter のような中央集権的なサービスでは、こういった機能を提供するのは難しいことではありません。すべてのデータが手元にあるため、障害でも起きない限りはデータベースに 1 回問い合わせるだけで必要なデータを参照できることが保証されているといっていいでしょう。 OGP 対応のパーマリンク機能を実装する場合は、データベースから返ってきたデータを同期的に表示するだけで済みますし、データベースが保持していなければ存在しないデータであることも明白です。対照的に、分散的なサービスでは必要なデータが手元にすべてあるとは限りません。とくに Nostr の場合はすべてのデータを分散したリレーが保持しているため、問い合わせてみるまで取得できるかどうかはわかりません。場合によっては欲しているデータがどのリレーにあるかわからない場合もあります。また、非同期通信のためレスポンスをいつまで待てばいいのかわからない問題もあり、仮にデータが返ってきたとしても最新のものである保証すらありません。同様に、データが存在しないことを確かめる場合には、すべてのリレーからデータが返ってこないことを確認するしか方法がありません。これらの理由から、「可能な限り多くのリレーに問い合わせて、可能な限り待つ」というのが Nostr でデータを取得する際の基本戦略になります。この方針は、多少の取りこぼしがあっても問題なく、時間がかかったとしても見ている間に更新されれば問題ないような、 SNS のタイムライン表示などの場合にはうまくワークします。しかしながら、 OGP 対応のパーマリンク機能のように同期的にレスポンスを返したい場合には、タイムアウトを設定するなどのある種の妥協が必要になってきます。このように、分散的なサービスでは一般的な機能ひとつ取っても勝手が異なることが伝わるかと思います。

###### **nostr: post checker** 

nostr: post checke ~~r~~ <u>注2</u> ~~は~~ Nostr 上の投稿がどのリレーに存在するのかを確認できるサイトです( **図** 1)。日本の開発者の koteita ~~n~~ <u>注3</u> さんによって作成されています。

Nostr はそのしくみ上、同じリレーに接続した者同士だけがやりとりできます。ほとんどのユーザーは複数のリレーを併用しており、互いのリレーのどこかしらが重複しているため、どのリレーに何の投稿が存在するかを日常的に意識する機会は少ないです。しかし、リレーの障害時や通信環境が劣悪な場合にはリレーへの書き込みに失敗することもあり、意図せず投稿が届かなくなることも発生し得ます。このような場合には nostr: post checker を利用することで自分の投稿がどこに届いており、どこに届いていないのかを確認し、再送信などを行うことができます。また、 Nostr を始めたてのユーザーのフォローアップとして、うまく投稿できない場合の問題の切り分けなどにも利用されます。

###### **metadata.nostr.com** 

Nostr では、プロフィールやフォロー情報もほかの情報と同様に複数のリレーに分散して保存されます。すべてのリレーに最新のデータが保存されている保証はなく、古いデータが混在することもしばしばです。プロフィールやフォロー情報を更新する際に、クライアントが古いデータやキャッシュを参照してしまうと、不完全なデータで上書きされてしまい変更が飛んでしまう可能性があります。 metadata.nostr.co ~~m~~ <u>注4</u> ~~で~~ はこういった問題の対策として、プロフィールやフォロー情報のバックアップが可能です( **図** 2)。また、最新データを持っていないリレーの存在を逆手にとって、プロフィールやフォロー情報を古い状態にロールバックすることもできます。類似サービスにNostrFl ~~u~~ <u>注5</u> ~~と~~ いう hegur ~~o~~ <u>注6</u> ~~さ~~ んが開発したサービスがあり、こちらは日本のコミュニティ内でよく利用されています。

##### **発見系サービス** 

前述のとおり、 Nostr ではデータが分散しており中央的なサーバが存在しないため、横断的に何かを行うことが基本的に苦手です。代表的な Nostr クライアントである Damu ~~s~~ <u>注7</u> ~~が~~ ハッシュタグ検索しかサポートしていないように、主要クライアントも検索機能が充実しているとは言えない状況です。そのため、リレーやクライアントの機能を補う形で多くのサービスが存在しています。ここでは、 Nostr のユーザーによって日常的によく利用される、ユーザーや投稿を発見(開拓)するためのサービスを紹介します。

###### **Nostr.Band** 

Nostr.Ban ~~d~~ <u>注8</u> ~~は~~ 代表的な発見系サービスです。多くの一般的な SNS にあるような投稿検索機能はもちろん、話題のユーザーや投稿、さらには Nostr の DAU ( Daily Active Users )といった統計情報まで見ることができます。 Nostr はそのしくみ上アカウント作成が非常に簡単でスパム行為も行われやすいため、独自のアルゴリズムでスパム投稿を弾くなど Nostr.Band は技術面にも力を入れているようです。また、 API や検索用リレーの提供など開発者向け機能も充実しています。

###### **Nostr日本語検索ポータル** 

Nostr **日本語検索ポータ** **~~ル~~** <u>注9</u> ~~は~~ 、個人開発者の hoku <u>注10</u> ~~さ~~ んが作成している日本語検索に特化した検索サービスです。日本で Nostr が話題になった当初にリリースされ、今でも変わらず使われ続けているため、もしかしたら日本の Nostr ユーザーの間で一番利用されているサービスかもしれません。また、 Nostr の日本語タイムラインの盛り上がり度合い(流速)を可視化したグラフや、 Nostr 関連ニュースの表示といったユニークな機能もあります。

###### **Nostrends** 

Nostrend ~~s~~ <u>注11</u> ~~は~~ 話題の投稿を探せる拙作のサービスです( **図** 3)。リアクション( Twitter のいいねに相当)やリポスト( Twitter のリツイートに相当)が多く付いた投稿をチェックできます。総合的に話題の投稿が確認できる Nostr.Band と比較すると、日本リレー内での話題の投稿の発見機能があったり、週間の話題の投稿をサポートしていたりといった違いがあります。

Nostr が採用している WebSocket のリアルタイム性と親和性が高いからか、日本語圏のユーザーの間では Nostr をチャット的に利用する人が多く見受けられます。流速が速く話題が流れてしまうことも多いため、あとから話題を追いかけるときにも Nostrends が利用されています。

###### **Nosaray** 

変わり種として、かすてらふぃ注12 ~~さ~~ んが作成しているNosaray さかのぼ <u>注13</u> ~~と~~ いうサービスがあります。 Nosaray は、タイムラインを遡ったり、過去の任意時点のタイムラインを再現したりすることに特化しています。複数のリレーから過去のデータを漏れなく・効率よく投稿を取得するためにはリレーの挙動を熟知している必要があります。 nostrfetc ~~h~~ <u>注14</u> ~~と~~ いうライブラリは同じ作者による投稿を漏れなく取得のためのライブラリであり、 Nosaray にはこの技術が応用されています。

##### **マイクロブログSNS以外としてのNostr** 

Nostr は Twitter のようなマイクロブログ SNS のためのプロトコルとして開発がスタートし、それが現在でも中心的な利用方法であることは変わりません。しかし昨今では、マイクロブログ SNS にとどまらないさまざまな活用方法が模索されています。ここでは、マイクロブログ SNS 以外の用途として活用されている Nostr 関連サービスを見ていきましょう。

###### **GARNET** 

GARNE ~~T~~ <u>注15</u> ~~は~~ 日本の開発者の murakmi ~~i~~ <u>注16</u> ~~さ~~ んが作成している公開チャット専用の Web クライアントです( **図** 4)。 Nostr には公開チャットに関する仕様注17 ~~が~~ 定義されており、 GARNET はそれに従ってルームの作成や投稿などを行っています。チャットサービスは、 Nostr が採用している WebSocket のリアルタイム性の高さを活かしたユースケースの好例と言えるでしょう。

###### **Habla.news** 

Nostr 上に作られた著名なブログサービスの 1 つが verbirich ~~a~~ <u>注18</u> ~~さ~~ んによって開発されているHabla.new ~~s~~ <u>注19</u> ~~で~~ す。 Nostr にはブログとしての利用を想定した⻑文投稿作成のための仕様注20 ~~が~~ 定義されており、 Habla.news はこれを利用しています。マイクロブログ SNS 用の投稿とは異なり、タイトルを付けたり本文を Markdown で記述したりすることが可能です。また、執筆時点の数日前には Communities という Reddit のような機能が追加されました。これは、特定の話題を中心とした公開コミュニティを作成し、そのコミュニティに設定されたモデレータ(管理者)が承認したコンテンツのみを扱える機能で、 Nostr で策定中の仕様注21 ~~の~~ 先行実装です。そのほかにも、策定されたばかりのカスタム絵文字注22 ~~を~~ いち早く採用するなど、新しい機能を取り込むことどんよくにも貪欲な注目のサービスです。

**Nosli**

Nostr では用意された仕様をただ利用するだけでなく、既存の仕様を組み合わせたり応用したりすることでもさまざまなサービス開発が可能です。拙作のNosl ~~i~~ <u>注23</u> ~~と~~ いう Togetter ライクな投稿まとめサービス( **図** 5)はその一例で、前述の Habla.news が採用しているブログのしくみ( NIP-32 )をまとめの作成に流用しています。

Nostr を利用したサービス開発のメリットとして、サービス側がデータベースなどを用意する必要がなく、データの管理をリレーに任せられることが挙げられます。また、利用者側にも自分のデータが特定のサービスにロックインされないというメリットがあります。具体的には、 Nosli で作ったまとめを Habla.news で閲覧・編集するといったことが可能です。 Nostr の仕様を定めた NIPs <u>注24</u> ~~に~~ はさまざまなものがあり、すべてを把握するのは一苦労ですが、アイデアしだいではユニークなサービスを開発することも可能でしょう。

###### **さらなる発展** 

Nostr の可能性を信じている開発者たちによって、さまざまな活用方法が日々模索されています。たとえば、 WebSocket によるリアルタイム性を活かしたチェスやリバーシなどのゲーム開発は代表例の 1 つです。また、投げ銭のしくみを支払いに応用した、障害回復性の高い分散 E コマースシステムを構築するための規格も策定されています注25 ~~。~~ さらに、 Twitter 創業者であり Nostr の出資者でもあるジャック・ドーシーさんの要望で、 Nostr 上に GitHub ライクな Git ホスティングサービスを作るといった壮大な計画も進行しています注26 ~~。~~

「 Nostr は SNS ではなくプロトコルである」と説明されることがありますが、 Nostr はまさにさまざまな基盤となり得る可能性を秘めていると言えるでしょう。

##### **まとめ** 

今回取り上げたサービスはほんの一部であり、いくつか紹介したように日本の開発者によっても積極的にサービスが作られています。これらのサービスは GitHub で共有されている awesome-nost ~~r~~ <u>注27</u> や awesome-nostr-japa ~~n~~ <u>注28</u> ~~で~~ 確認することが可能です。 Nostr は通信の中心となるリレーがシンプルなぶん、さまざまなサービスを使い分けることで利便性やおもしろさも広がっていきます。幅広い楽しみ方のある Nostr のおもしろさが少しでも伝われば幸いです。たくさんのサービスが生まれる背景として、 Nostr はオープンプロトコルで拡張性が高く、エンジニアが遊べる余白が非常に大きい点が挙げられます。次回は、そんな Nostr の技術的な詳細やプロトコルの仕様についてより深く掘り下げていきます。

###### **Column** 

###### **NIP-07のすすめ** 

###### **Nostrにおける秘密鍵保護の重要性** 

Nostrにおいて、公開鍵はユーザーID、秘密鍵はパスワードのようなものと考えることができます。ただし、通常のパスワードと違ってNostrの秘密鍵は変更ができないため、流出しないようにとくに注意する必要があります。しかし、一般的にWebサービスには脆弱性などによる情報流出のリスクがつきものです。それはNostrクライアントや今回紹介したNostr関連サービスも例外ではありません。

Nostrでは、利便性を損なわずに秘密鍵流出リスクを下げることができる、NIP-07 と呼ばれるしくみを提供しています。

###### **NIP-07とは** 

NIP-0 ~~7~~ <u>注A</u> ~~は~~ 、Nostrが定める仕様(NIPs)の1つです。Webサービスとブラウザ拡張機能がやりとりするためのAPIを規定しています。次のように、ブラウザ拡張のみが秘密鍵を扱うことで秘密鍵流出のリスクを下げることができます。

- ・ユーザーはブラウザ拡張機能に秘密鍵を設定する 

- ・ブラウザ拡張機能はWebサービスに対して公開鍵の取得や署名を行うしくみを提供する 

- ・ Webサービスは必要に応じてそれらの処理を依頼するまた、おもなNIP-07対応ブラウザ拡張は権限管理も行えるため、プライバシー保護の観点からも有用です。たとえば「投稿は許可するが、ダイレクトメッセージの復号は許可しない」といった設定を行えます。 

###### **著名なNIP-07対応ブラウザ拡張** 

執筆時点では、著名なNIP-07対応ブラウザ拡張としてnos2x、Alby、Nostore などが存在しています。それぞれ次の特徴があります。ご利用の環境に応じて選択してみてください。

- **<u>注</u>** <u>B</u> 

- **・** nos2 ~~x~~ 

Nostrの作者であるfiatjafさ ~~ん~~ <u>注C</u> ~~に~~ よって開発されたブラウザ拡張。Chrome系ブラウザで利用可能

- **<u>注</u>** <u>D</u> 

- **・** Alb ~~y~~ 

Chrome系ブラウザやFirefoxで利用可能。さまざまなNostr向け機能が充実している総合的なブラウザ拡張

- **<u>注</u>** <u>E</u> 

- **・** Nostor ~~e~~ 

iOSやmacOSのSafariで利用可能。iOSアプリ形式で配布されている

###### **注意事項** 

NIP-07の注意点としては、ブラウザ拡張の利用が前提となっているため、ネイティブクライアントには対応していません。また、ブラウザ拡張自体に悪意や脆弱性がある可能性もあるため、秘密鍵が流出するリスクは依然として存在します。それでも、秘密鍵を直接利用するよりもリスクが下がることは間違いありません。今回紹介したNostr関連サービスを利用する際には、ぜひNIP-07を使ってみてください。

- 注3) NIP-05: <_@koteitan.github.io> 

   - (本文に戻る) 

- 注6) NIP-05: <heguro@heguro.com> 

   - (本文に戻る) 

- 注12) NIP-05: <_@c-stellar.net> 

   - (本文に戻る) 

- 注16) NIP-05: <_@txt.murakmii.dev> 

   - (本文に戻る) 

- 注18) NIP-05: <verbiricha@snort.social> 

   - (本文に戻る) 

### **Google Cloudを軸に実践する SREプラクティス** 

**信頼性を高めるサービス基盤の構築第 6 回 Renovateによる依存関係の更新**

**現代のソフトウェア開発で求められる「スピード」「高い柔軟性」「質の高いサービス」に応えるため、Googleから提唱されたSRE(Site Reliability Engineering)。本連載では、信頼性の高いサービスを提供するために、SREプラクティスを実践するエンジニア陣が、Google Cloudを軸に構築するサービス基盤やその技術を解説します。**

山田圭一(やまだけいいち)キャディ株式会社

<u>@kei711_</u>

小森裕介(こもりゆうすけ)キャディ株式会社 <u>@littleforest12</u>

<u>https://caddi-inc.com/</u>

#### **はじめに** 

前回(本誌 2023 年 8 月号)は Terraform と GitHub Actions で実践するインフラ CI/CD について解説しました。今回は Renovat ~~e~~ <u>注1</u> ~~を~~ 利用した、ツールやライブラリの依存関係更新について紹介します( **図** 1)。なぜ依存関係を更新する必要があるかという背景から、 Renovate のしくみの解説と利用方法、更新の運用を手軽に行うためにキャディで取り組んでいることを紹介します。

#### **なぜ依存関係を更新するのか** 

現代のアプリケーション開発において、私たちエンジニアはさまざまなツールやライブラリの利用を通して、先人の知恵を借り、効率的な開発を進めています。また、前回までで紹介した Terraform や GitHub Actions などのインフラ CI/CD の領域でも、なんらかの再利用のしくみを活用することで効率化しています。しかし、ツールやライブラリは絶えずアップデートされています。機能の追加やバグの修正、脆弱性への対策など、その理由はさまざまです。その中でも筆者らが依存関係の更新を重視する理由は、セキュリティと対応コストの 2 点です。セキュリティ観点では、ツールやライブラリの脆弱性やバグ修正の更新をいち早く検知・対応することが欠かせません。キャディでは、自社事業の基幹システムをフルクラウドで構築・運用しており、これらの放置は安定した価値提供を損ねることにつながるからです。対応コスト観点では、頻繁な対応によって、バージョン間の差分が小さいうちに更新できることを重視しています。そのため、 1 回あたりの更新対応のコストを下げることが可能です。また、 CHANGELOG にも常に目を通すことになるため、副次的に情報のキャッチアップにもつながります。

#### **なぜRenovateを使うか** 

依存関係の更新をサポートしてくれる主要なツールとしては、 Renovate のほかに GitHub で標準提供されている Dependabo ~~t~~ <u>注2</u> ~~が~~ あります。キャディでは、 2020 年に Renovate を採用するまでは、 Dependabot を一部で利用している程度でした。本連載で紹介しているように、筆者の所属する Platform グループでは、 Terraform や GitHub Actions を用いた IaC や CI/CD の高度化に取り組んでいます。これらにより依存するものが増えているため、前節のとおり依存関係の更新は必要です。一方で、事業の拡大を支えるための、本質的な価値提供にも集中する必要があります。このような背景に対するトイル削減の一環で、高いカスタマイズ性を持つ Renovate に魅力を感じ、利用を拡大しました。とくに、のちほど紹介する auto merge 、 Pull request ( PR )のグループ化、正規表現を利用しながら更新ルールをカスタマイズできる点が効果的だったととらえています。また、 Dependabot は利用をやめているわけではありません。一部の開発チームではセキュリティアラートを活用するなどして、 Renovate と共存しています。

Renovate でもセキュリティアラートを通知する設定はありますが、それぞれのツールでも得手不得手もあるため、開発者が最もメンテナンスしやすい方法を選択していく必要があると考えています。

#### **Renovateのしくみ** 

ここからは、 Renovate のしくみと設定方法について簡単に解説します( **図** 2)。さらに理解を深めたい方は公式ドキュメント注3 ~~を~~ 参照してください。

Renovate は依存関係を一元管理し、新しいバージョンがリリースされたときに自動的にファイルを更新します。そして GitHub や GitLab などのサポートされているプラットフォームにて、 Renovate によって PR や Merge request が作成されます。まず、 Renovate は依存関係の現状を把握するために各バージョンを確認します。 JavaScript の場合は package.json 、 Terraform の場合は terraform block の provider 定義など、各言語やツールに応じたファイルから取得します。次に、そのバージョンが最新であるかどうかを Renovate のルールに従って判定し、最新でない場合はバージョンを更新する PR を作成します。

#### **Renovateの設定** 

Renovate の挙動を理解するために欠かせない概念として、 **設定ファイル** と **マネージャー** があります。

##### **設定ファイル** 

Renovate は設定ファイルや環境変数によって挙動をカスタマイズできます。どんな依存関係にあるものをどんな頻度で更新するか、レビュアーを指定するか、 PR のラベルを指定するかなど、さまざまな設定が可能です。設定ファイルは、「 renovate.json 」「 .github/renovate.json 」「 .renovaterc 」として配置できたり、コメントが記載できるようにも拡張されたJSON5 **形** **~~式~~** <u>注4</u> ~~で~~ も記述できたりします。

**リスト** 1の設定例をもとに、簡単に紹介します。より詳細を理解したい方はドキュメント注5 ~~を~~ 参照ください。

###### **▼リスト1 renovate.json** 

###### **{** 

**"$schema": "https://docs.renovatebot.com/renovate-schema.json",** **<mark>//</mark>** <mark>1</mark> **"extends": [** **<mark>//</mark>** <mark>2</mark> **"config:base",** **<mark>//</mark>** <mark>5</mark> **":label(renovate)",** **<mark>//</mark>** <mark>6</mark> **":timezone(Asia/Tokyo)",** **<mark>//</mark>** <mark>7</mark> **], "schedule": ["after 1am and before 9am every weekday"],** **<mark>//</mark>** <mark>3</mark>

**"reviewers": ["team:reviewer-team", "kei711"],** **<mark>//</mark>** <mark>4</mark> **}**

$schema (1)は JSON Schema の指定です。この値により、エディタによっては設定名が補完されるようになります。 extends (2)は設定値のプリセットを指定します。 schedule (3)は cron 形式で実行スケジュールを指定します。 **リスト** 1の例では、平日の午前 1 時から午前 9 時の間に実行されます。 reviewers (4)はレビュアーを指定します。 GitHub や GitLab などの挙動に合わせて、グループや個人を指定できます。また、 **リスト** 1の例では Renovate で用意されているデフォルトプリセットの一部を指定しているため、こちらも紹介します。 config:base (5)は Renovate のデフォルト設定で、設定値は Renovate 自体に組み込まれていま ~~す~~ <u>注6</u> ~~。~~ :label (6)の設定により、作成される PR に特定のラベルを指定します。ここでは、 renovate というラベルを設定します。 :timezone (7)の設定により、 schedule で指定された実行スケジュールのタイムゾーンを指定します。なお、デフォルトプリセットの詳細はドキュメント注7 ~~を~~ 参照してください。このように、設定ファイルにより Renovate 自体の挙動を柔軟にカスタマイズできます。

##### **マネージャー** 

Renovate のマネージャーとは、各言語やツールに応じた処理が定義されたモジュールのことを指します。このマネージャーを通して、 Renovate が依存関係の解析や更新をします。たとえば、 JavaScript であれば npm 、 Terraform であれば terraform や terraformversion などのマネージャーがあります。

Renovate は初期設定でも多くのマネージャーを利用する設定となっています。詳細はドキュメント注8 ~~を~~ 参照してください。各マネージャーもドキュメントにて紹介されています。また、未設定だと利用されないマネージャーもあります。たとえば、 Argo CD はファイル構成が利用者に委ねられており正確な検知が難しいため、 **リスト** 2のように明示が必要です。

**▼リスト2 Argo CD向け設定の抜粋**

**<mark>(..略..)</mark> "argocd": { "fileMatch": [ "argocd/.+\\.ya?ml$", "applications/.+\\.ya?ml$" ] },**

Argo CD は、本連載第 1 回(本誌 2023 年 4 月号)で紹介した、 Kubernetes マニフェストを GitOps で管理するためのツールです。本連載でも以降の回で詳しく紹介する予定です。最初から用意されているマネージャーのほかにも、正規表現を利用して振る舞いを定義できる、 regex manage ~~r~~ <u>注9</u> ~~が~~ 存在します。こちらの詳細はキャディで利用している実例とともに、のちほど紹介します。

#### **Renovateの組み込み方法** 

ここからは、実際の使用方法を説明します。大きく分けて「 GitHub App の利用」「ローカルで実行」「 GitHub Actions などの環境で実行」の 3 パターンがありますが、ここでは最も手軽な GitHub App による方法を紹介します。

Renovate は Mend 社により、無償の GitHub App としても提供されています。ソースコードの管理に GitHub を利用している場合には、 GitHub の Marketplac ~~e~~ <u>注10</u> ~~か~~ ら GitHub App を導入して利用することで、手軽に Renovate を利用できます。

Marketplace から GitHub App をインストールし、依存関係を自動更新させたいリポジトリを選択します。そうすると、 Renovate の更新対象として選択したリポジトリにて、 Renovate の設定ファイルである renovate.json を作成する PR が自動作成されます。設定ファイルをリポジトリに配置することで、 Renovate の設定が完了し、自動で依存関係の更新が行われるようになります。

GitHub App の各リポジトリにおける GitHub App の動作状況は、ポータルサイト注11 ~~か~~ ら確認できます。 GitHub Organization を選択すると、 Installed Repositories として、 Renovate をインストールしたリポジトリの一覧と、それぞれのインストール日や最終実行時刻が表示されます。次に、リポジトリの行をクリックすると、 Recent Jobs のページが表示され、リポジトリ単位の実行状況が表示されます。さらに Job ごとの行をクリックすると、 Renovate が実行された際のログを、ログレベルや詳細情報の表示切り替えをしながら確認できます。もし Renovate の設定変更がうまく PR に反映されていない場合は、このログから状況を確認できます。

##### **Column RenovateのGitHub Appを利用する際の注意点** 

GitHub Appは手軽に使える反面、注意すべき点が2点あります。

1点目は、セキュリティ観点です。GitHub Appを導入するということは、アプリケーションを作成する場合のサプライチェーン攻撃の攻撃面が増えることを意味します。 RenovateのGitHub Appをインストールすると、自分たちが管理するソースコードへのアクセス権を与えることになります。そのため、このアクセス権が奪われた場合や、 GitHub Appに不正なコードが含まれている場合、それが自分たちのソースコードにも影響を及ぼす可能性があることを理解する必要があります。

2点目は、作業コストの観点です。全リポジトリを対象にRenovateを導入するオプションがありますが、リポジトリに導入したぶん、Renovateにより依存関係の更新PRが作成されます。作成されるPRが多過ぎるとメンテナンスを行いにくくなります。そのため、前述のように導入するリポジトリを選択することをお勧めします。また、セキュリティリスクにおいても、リスクを取れるリポジトリと取れないリポジトリもあるため、適切に選択をする必要があります。

#### **応用的な使い方** 

ここからは、更新の運用を手軽に行うために取り組んでいることをピックアップして紹介します。

##### **共通設定の定義と利用** 

Renovate の設定は renovate.json に記述しますが、リポジトリそれぞれで定義すると管理コストが非常に高くなります。実際、キャディでは管理するリポジトリが多く、設定の共通化で管理コストを下げています。共通設定の共有方法は複数ありますが、今回は手軽な GitHub で公開する方法を紹介します。ほかの共有方法や、 GitHub で公開する方法の詳細はドキュメント注12 ~~を~~ 参照してください。まず、 renovate の設定ファイルを共有するためのリポジトリを作成します。キャディでは、 renovate-config という名前で作成しています。次に、後述するプリセット名を省略した場合のため、

default.json に renovate の設定を記述します。また、言語や開発チームごとに共通設定を用意したい場合は <u>preset name.json</u> というような命名をします。たとえば、 go.json や team-platform.json5 のような形です。このように共通設定を用意したら、利用したいリポジトリの renovate.json にて、 **リスト** 3のように記述します。 <u>GITHUB_ORG は</u> caddijp のような組織名や kei711 のようなアカウント名に書き換えてください。

**リスト** 3の設定ファイルでは、次のように共通設定を参照します。

- 1 リポジトリ上のdefault.jsonを利用 

- 2 プリセット名を指定し、go.jsonを利用 

- 3 platform.json5を利用。JSON5形式の場合はプリセット名に拡張子を含める必要がある 

###### **▼リスト3 共通設定の利用例** 

|**{**<br>**"$schema": "https://docs.renovatebot.com/renovate-schema.json",**<br>**"extends": [**<br>**"github>GITHUB_ORG/renovate-config",**<br>**//**1|
|---|
|**"github>GITHUB_ORG/renovate-config:go",**<br>**//**2|
|**"github>GITHUB_ORG/renovate-config:team-platform.json5",**<br>**//**3<br>**]**<br>**}**|

また、 Git のタグやファイルパスも指定できます。詳細な例はドキュメント注13 ~~の~~ GitHub の項目を参照してください。なお、プライベートリポジトリで GitHub App の Renovate を利用する場合には、 renovate-config リポジトリもプライベートリポジトリにし、 GitHub App の導入も必要となる点に注意してください。

##### **設定ファイルの検証** 

Renovate には設定ファイルの構文が正常かどうかを確認するコマンドが用意されています。 **RENOVATE_CONFIG_FILE=renovate.json npx renovate-config-validator** のように実行することで確認できます。共通設定を管理するリポジトリの CI に設定しておくと安心して編集できるでしょう。

##### **GitHubでオートマージを利用する** 

Renovate の automerge を活用する場合は、設定ファイルの変更と、 GitHub や GitLab などにて設定しているマージ条件が満たされている必要があります。

GitHub では、リポジトリの設定で次の条件が満たされている必要があります。

- ・ Allow auto-mergeが有効になっている 

- ・ Branch Protection Rulesで設定されたマージ条件が満たされているたとえば、 CODEOWNERS のレビューを必須にしている場合には、更新対象のファイルを CODEOWNERS から除外するか、ルールをバイパスする設定を追加する必要があります。また、 Branch Protection Rules で PR の approve を必須としている場合には、 Renovate の PR を自動 approve してくれる GitHub App である 

- 「 renovate-approve 」を導入します。 approve の数などの必要に応じて 

- 「 renovate-approve2 」の GitHub Apps も導入してください。 

**リスト** 4は、セマンティックバージョニングされたツールで、 minor や patch の更新をオートマージする例です。設定ファイルでは、 packageRules ブロックで依存関係ごとに上書きできます。 matchPackageNames では対象を指定することで、特定の依存関係のみオートマージできます。また、必要に応じて ignoreTests でテストの実行を無視できます。

###### **▼リスト4 オートマージの設定例** 

**{ "platformAutomerge": true, "packageRules": [ { "automerge": true, "matchUpdateTypes": ["minor", "patch"],**

<!-- Start of picture text --> "matchPackageNames": [<br>         "kubernetes-sigs/kustomize",<br>         "mikefarah/yq"<br>       ],<br>       "ignoreTests": true<br>     }<br>   ]<br> }<br><!-- End of picture text -->

##### **グループ化により、依存関係をまとめて更新する** 

同じ用途のバージョンは、一度に更新したいことが多いかと思います。キャディでは前回までの連載で紹介しているとおり、 Terraform で Google Cloud の設定を IaC 化しています。

Google Cloud の設定をするための Terraform provider には、 google と google-beta の 2 種類があります。早く技術検証したい場合には googlebeta provider を利用することがあります。筆者らはこれらの provider をまとめて更新するための **リスト** 5の設定を利用しています。 matchManagers と matchPackageNames で対象となる依存関係を指定します。そして groupName を指定することで、 1 つの PR の中で一度にバージョンが更新されるようになります。

###### **▼リスト5 PRをグループ化する設定例** 

<!-- Start of picture text --> {<br>   "packageRules": [<br>     {<br>       "matchManagers": ["terraform"],<br>       "matchPackageNames": ["google", "google-beta"],<br>       "groupName": "Google Terraform providers"<br>     }<br>   ]<br> }<br><!-- End of picture text -->

##### **regexManagersによる独自の更新ルール定義** 

筆者らは、 Terraform による Google Cloud の設定処理を共通化するため、 GitHub Actions の Composite Action を作成しています。このとき、 terraform_version を渡す必要がありますが、このバージョンの指定方法では Renovate が更新してくれません( **リスト** 6)。

###### **▼リスト6 標準では更新対象外となる独自定義** 

**<mark>(..略..)</mark> - name: Setup Terraform and Auth Google Cloud uses: caddijp/gh-actions/terraform/setup_terraform@v0.21.0 with: workload_identity_provider: ${{ vars.GCP_WI_PROVIDER }} service_account: ${{ vars.GCP_WI_SERVICE_ACCOUNT }} working_directory: ./terraform terraform_version: 1.4.6**

そこで登場するのが regex manager です。対象ファイルと正規表現をもとに更新すべき対象を絞り込みし、依存するバージョンの公開先を指定することで一緒に更新してくれるようになります。 **リスト** 7の設定は、キャディで実際に利用している共通定義の一部を抜粋したものです。

###### **▼リスト7 独自の更新ルールを指示する設定** 

**{ "regexManagers": [ { "fileMatch": [** **<mark>//</mark>** <mark>1</mark> **"^\\.github/workflows/.*\\.ya?ml$", "^\\.circleci/config\\.ya?ml$" ], "matchStrings": [** **<mark>//</mark>** <mark>2</mark> **"terraform_version: +['\"]?(?<currentValue>[^'\" \\n]+?)['\"]?\\n"** **<mark>//</mark>** <mark>3</mark> **], "depNameTemplate": "hashicorp/terraform",** **<mark>//</mark>** <mark>4</mark> **"datasourceTemplate": "github-releases",** **<mark>//</mark>** <mark>5</mark> **"extractVersionTemplate": "^v(?<version>.*)$"** **<mark>//</mark>** <mark>6</mark>

**} ] }**

まず、 Renovate の更新対象となるファイルを1 fileMatch で指定します。キャディでは GitHub Actions のほか、 CircleCI も利用しているので、両方を指定しています。次の2 matchStrings では正規表現を指定し、 fileMatch で指定したファイルの中からマッチするものを探します。3 <currentValue> が Renovate 中で特殊に扱われているキャプチャグループ名です。 **terraform_version: 1.4.6** という表記のほかにも、 **terraform_version: '1.4.6'** のような表記、 **terraform_version: 1.4.6 # comment** のような表記のブレも吸収できるようにしています。次の456は、データソースの設定です。4 depNameTemplate と 5 datasourceTemplate により、 Terraform の GitHub Releas ~~e~~ <u>注14</u> ~~の~~ 情報をもとに最新バージョンを取得します。最後の6 extractVersionTemplate は、バージョンの表現を指定しています。 Terraform のバージョンは **v1.4.6** のように先頭が **v** から始まるタグの命名ルールです。ですが、キャディでは workflow 中のバージョンでは **v** を除いているため、記述方法に合わせるように先頭の **v** を除外しています。ほかにも特殊なキャプチャグループ名がありますので、興味がある方は regex manager のドキュメン ~~ト~~ <u>注15</u> ~~を~~ 参照してください。

#### **Renovateの更新運用の工夫** 

Renovate が自動的に PR を作成してくれるとはいえ、リポジトリ数が増えると差分を確認しながらマージするだけでも一苦労です。依存関係の更新が形骸化しないように、 Platform グループ設立から 2 年間試行錯誤してきました。ここからは筆者らが現在行っている運用の一部を紹介します。

##### **依存関係更新の運用** 

Renovate の更新 PR が溜まってしまうことを防ぎつつも、依存関係を更新していくには、習慣化するのが一番です。そこで筆者らは毎週 1 回 30 分カレンダーに Renovate 用の予定を登録しました。この時間内は必ず依存関係を更新するルールにしています。また、作業開始前に Renovate による PR があるリポジトリの URL を Slack に通知しています。このしくみを作ることにより、対象 PR を探しに行く手間をなくすようにしました。 **図** 3のようにリポジトリ単位でリポジトリの URL が投稿されるため、それぞれにリアクションができるようになります。筆者らは作業開始するリポジトリに対して「やります」のリアクションをしながら分担して作業を進めています。

##### **CIのみで利用するツールのpatch、minorは極力 automergeする** 

CI のみで利用するテスト、 Lint 、静的解析に関連するツールを自動更新しても大きく壊れることがなかったため、極力 automerge を利用するようにしています。ただし、 CD でも利用しているツールは自動でデプロイされると影響が大きく困るため、 automerge の対象から外しています。

##### **Renovate経由で作られたPRの通知を削減** 

筆者らは Slack の GitHub App を経由して、担当するリポジトリの PR を定期的に Slack に通知し、 PR マージまでのリードタイムを短くする取り組みをしています。この通知設定にて、 renovate のラベルが付いている PR を除外することで、 Renovate による通知疲れを低減させています。

Platform グループは横断組織であることから、認知負荷が高くなりがちなため、日ごろから通知を減らす努力をしています。

#### **おわりに** 

今回は依存関係の更新が必要な背景、 Renovate の解説、キャディでの取り組みについて紹介しました。連載の流れから、 Terraform と Renovate の組み合わせを中心に紹介をしてきましたが、今回紹介したものはアプリケーション開発でも同様に使えるものばかりです。みなさんの開発においても、依存関係の更新が楽になることを願っています。来月は Argo CD を利用した Kubernetes の CD について、キャディの事例をまじえながら紹介する予定です。

##### **「らしさ」の本質に迫る** 

### **なるほど納得/ Go 言語** 

崎原晴香(さきはらはるか) (H.Saki)

<u>@saki_engineer</u>

<u>https://zenn.dev/hsaki</u>

#### **<u>第 8 回</u> インターフェース(後編)** 

- **「Goらしさ」とは何でしょうか。本連載では、「Goらしさ」とはいったい何を指すのか、背景からじっくり考察し、「Goらしく」書くためのヒントを紹介します。第8回は、Goにおけるインターフェースの使い方をさらに掘り下げて紹介します。** 

前回(本誌 2023 年 8 月号)は、「 Go では型階層を廃して継承を取り入れなかったため、継承で担っていた 2 つの役割を別の方法で提供している」という話をしていました。

- ・型Tに、型Sと同様の機能を持たせるという拡張行為そのもの→ 埋め込みフィールド(embedded field)を用いることで使用可能になる委譲(delegation)で実現 

- ・異なる型である型Tと型Sを、同列に扱うための抽象化→インターフェースで実現今回はその後半「インターフェースを使って異なる型を抽象化することができる」について解説したうえで、インターフェースをうまく使う Tips を紹介します。 

##### **インターフェースによる抽象化** 

# **異なる型には代入できない** 

型階層によって「子となる型は親となる型と同じ性質を満たすべきである」という宣言がなされている場合には、子のインスタンスを親型の変数に代入できることがあります。たとえば **リスト** 1の Java のコードをご覧ください。 RunCounter 型は Runner 型を継承して作られており、いわば「 Runner を親とした型」です。そのため RunCounter 型のインスタンスである変数 r1 は、異なる型である Runner 型の変数 r2 に代入できています。

###### **▼リスト1 Javaではこんなコードでもコンパイルエラーにならない** 

**class Runner {** **<mark>(..略..)</mark> } class RunCounter extends Runner {** **<mark>(..略..)</mark> }**

**public class Main { public static void main(String[] args) throws Exception { RunCounter r1 = new RunCounter(); Runner r2 = r1; } }**

しかし型階層がない Go ではこれができません。埋め込みを行って「親子っぽく」したとしても、違う型である以上、 **リスト** 2のように Animal 型の変数に Dog 型の値を代入することができないので ~~す~~ <u>注 1</u> ~~。~~

###### **▼リスト2 Goでは違う型には代入できない** 

**type Animal struct {} type Dog struct { Animal**

**}**

**func main() { var a Animal a = Dog{} }** **<mark>// cannot use Dog{} (value of type Dog) as Animal value in assignment</mark>**

# **インターフェース=振る舞いによる同一視** 

ここで、先月号で引用した Rob Pike 氏の言葉をもう一度思い出してみましょう。

_What matters isn't the ancestor relations between things but what they can do for you._

_(訳)重要なのは型の家系上のつながりではなく、その型自身がプログラムの中でどのように振る舞うことができるかです。_

その型がどのように振る舞うかが重要であるとの言葉に表れているように、 Go ではある型が特定のインターフェースを実装しているか否かを、その型が持つメソッドセットを見て判定します。たとえば、 io パッケージ内に定義されている Writer インターフェース注2 ~~を~~ 見てください。

**type Writer interface { Write(p []byte) (n int, err error) }**

これは「 **Write(p []byte) (n int, err error)** メソッドさえ実装してある構造体であれば、 io.Writer インターフェースを実装していると扱われる」ということです。実際に io.Writer を実装している構造体は bytes.Buffe ~~r~~ <u>注3</u> ~~、~~ os.Fil ~~e~~ <u>注4</u> ~~、~~ net.TCPCon ~~n~~ <u>注5</u> ~~、~~ strings.Builde ~~r~~ <u>注6</u> ~~な~~ ど数多く存在しますが、それらすべてが io.Writer 型の変数に代入可能です( **リスト** 3)注7 ~~。~~

###### **▼リスト3 これはコンパイルエラーにならない** 

**var w io.Writer w = &bytes.Buffer{} w = &os.File{} w = &net.TCPConn{} w = &strings.Builder{}**

これが「メソッドによる振る舞いの一致によって異なる型を同一視する」という Go での抽象化のやり方です。ここでは「抽象化した型を継承しているんだから当然同じメソッドを持つよね」ではなく、「同じメソッドを持つのであればインターフェースを使って抽象化できるよね」という順序の逆転が起きており、かつこの順番をひっくり返したことがスケーラビリティ担保を目的とした Go の意図だと思っています。

##### **インターフェースのうまい使い方** 

ここからは、 Go でインターフェースを使うときによく使う考え方 /書き方を紹介していきます。

# **頻出命名法:「-er」** 

ある構造体が特定のインターフェースを満たしているかどうかは、その型がインターフェースのメソッドセットに含まれているメソッドをすべて実装しているかどうかだけで決まります。インターフェースはいわば特定のメソッド=振る舞いを持っているか?という部分だけを見て抽象化を行っているのです。そのため、振る舞い=動詞を使った名前、具体的には Reader/Writer/Closer といった「動詞+ -er 」という名前をインターフェースに付けると、役割を的確に表した名前になりやすいです( **リスト** 4)注8 ~~。~~

###### **▼リスト4 -erで命名された標準パッケージのインターフェース** 

**<mark>// io.Reader(https://pkg.go.dev/io#Reader)</mark> type Reader interface { Read(p []byte) (n int, err error) }**

**<mark>// io.Closer(https://pkg.go.dev/io#Closer)</mark> type Closer interface { Close() error }**

**<mark>// io.Writer(https://pkg.go.dev/io#Writer)</mark> type Writer interface { Write(p []byte) (n int, err error) }**

_By convention, one-method interfaces are named by the method name_ <u>注9</u> _plus an -er suffix or similar modification to construct an agent noun: (訳)慣例的に、 1 つのメソッドのみをメソッドセットに持つインターフェースは、そのたった 1 つのメソッド名に -er を付けた命名がされます。_

また、メソッドの数が 2 つ 3 つ程度でかつそれらがすべてシンプルな名前なのであれば、それらメソッド名をつなげてインターフェース名にするのもよく行われる手段です( **リスト** 5)。

###### **▼リスト5 動詞を2つ3つつなげてインターフェース名とした例** 

**<mark>// https://pkg.go.dev/io#ReadWriter</mark> type ReadWriter interface { Reader Writer }** **<mark>// https://pkg.go.dev/io#ReadWriteCloser</mark> type ReadWriteCloser interface { Reader Writer Closer }**

##### **Column is-aの関係を無理やりインターフェースで実現した例** 

一応、役割に着目しないis-aの関係をGoで無理やり実現することも可能です。 **リスト** AのGraphQLのスキーマでは、GitHub Projectというカンバンのカードを ProjectV2ItemContentオブジェクトとして定義しています。カードにできるのはIssueか PRという関係を、GraphQLのスキーマ定義記法ではunionでうまく記述できます。

###### **▼リストA GraphQLスキーマではis-aを無理なく記述できる** 

**union ProjectV2ItemContent = Issue | PullRequest**

**type Issue implements Node {** **<mark>(フィールド略)</mark> } type PullRequest implements Node {** **<mark>(フィールド略)</mark> }**

しかし、Goでは、「Issue/PR is ProjectV2ItemContent」という関係をスマートに表現する方法は存在しません。そのためgqlgenというコード自動生成ツールを使って生成した構造体定義では、「空メソッドであるIsProjectV2ItemContent()を持つものがProjectV2ItemContentに含まれる」という形で無理やりis-aの関係を表現しています( **リスト** B)。

**▼リストB Goでis-aを無理やり記述した例(gqlgenによる自動生成コード)**

**type ProjectV2ItemContent interface { IsProjectV2ItemContent() }**

**type Issue struct {**

**<mark>(..略..)</mark> } func (Issue) IsProjectV2ItemContent() {}** **<mark>// 空メソッド</mark> type PullRequest struct {**

**<mark>(..略..)</mark> } func (PullRequest) IsProjectV2ItemContent() {}** **<mark>// 空メソッド</mark>**

# **インターフェースは小さいほどよい** 

前項で、インターフェースに含まれるメソッドセットを 1 つ →2 つ →3 つ ...... と増やしていくパターンをチラリとお見せしました。このようにメソッドの数を増やしていくということは、そのインターフェースを満たすために必要になる条件がどんどん厳しくなっていくということと同義です。そうなると、せっかくインターフェースを使って抽象化しても、それを満たす/実装する具体型が全然表れないということにもなりかねません。

Go Proverbs <u>注10</u> ~~と~~ いう名言集の中には次のような言葉があります。

_The bigger the interface, the weaker the abstraction. (訳)インターフェースが大きくなるほど、より抽象度が薄れていきます。_

これは言い換えると、インターフェースは小さいほうがいい=メソッドセットは少ないほうがよりよい抽象化であるということです。

# **空インターフェースは避けよう** 

小さいインターフェースほどよいということは、メソッドセットを極限まで少なくした、つまり空のインターフェースはどうでしょうか。空インターフェースはメソッドセットの中身が空、つまりどんな型でも代入できるインターフェースです。 Go 1.18 から interface{} のエイリアスとして any 識別子が導入されており、こちらの名前のほうがその挙動が想像しやすいでしょうか。空インターフェースの利用は Go の静的型付けの恩恵を減らしてしまいますので、なるべく使用は避けましょう。 fmt.Println の引数のように「本当にどんな型でも受け入れないといけない」場合はしかたがありませんが ...... 。

_interface{} says nothing._

<u>注11</u> ──Go Proverbs

# **アドホックなインターフェースをどんどん作ろう** 

型階層/継承を前提とした場合には「先に抽象型の構造を決めておいて、具体型にそれを継承させる」というステップを踏む必要がありました。そのため抽象型の設計を行う時点で、どのような具体型を用意する予定でそれらのどの性質を抽象型に反映させるのかを事前によく考えなくてはいけないのです。しかし Go では「先に具体型だけ用意しておいて、後からメソッドに注目してインターフェースを切り出す」ということを容易に行うことができます。いわば、振る舞いに共通点を見いだしインターフェースという形で切り出すのは、用意する側ではなく使う側なのです。このように、「最初にその型が満たすインターフェースを宣言する」のではなく「はじめは抽象化のことを意識せずに具体型を実装し、そこに付属しているメソッドによって自動的に満たすインターフェース=抽象型が決まる」という Go のしくみは、多重継承のような複雑さを伴うことなく、 1 つの具体型を複数の異なる切り口で簡単に抽象化することができます。もし特定のユースケースのためだけの抽象化をしたくなったとしても、自分の親や子に変更を加えないといけない/影響が波及するかもしれないという心配をすることなく、そのユースケースのことだけを考えたニッチな用途の小さなインターフェースをどんどん切り出して OK なのです。

_Rather than requiring the programmer to declare ahead of time that two types are related, in Go a type automatically satisfies any interface that specifies a subset of its methods._

_Interfaces can be added after the fact if a new idea comes along or for testing-without annotating the original types. Because there are no explicit relationships between types and interfaces, there is no type hierarchy to manage or discuss._

_It takes some getting used to but this implicit style of type dependency_ <u>注12</u> _~~.~~ is one of the most productive things about Go_

_(訳) Go では、プログラマーに事前に 2 つの型には親子の関係があると宣言させるのではなく、メソッドセットのみからその型が満たすインターフェースが決まります。_

_テストや新しいユースケースのために、元の型定義に変更を加えることなく新しいインターフェースを実装させることができます。インターフェースと具体型の間にコードによる明示的な関係が定義されていないため、とある具体型が〇〇のインターフェースを満たすといった関係性を人間がメンテナンスする必要がないのです。_

_慣れるまでこの考え方に慣れるのは大変かもしれませんが、この暗黙的な型依存の関係は Go の生産性を生み出す源泉の 1 つなのです。_

# **インターフェースの実装有無チェック** 

Go のしくみでは、「とある具体型が特定のインターフェースを実装するかどうか」が、コードによる明示宣言ではなく、メソッドセットによって暗黙的に決定されてしまいます。そのため、「自分が作ったこの型は〇〇のインターフェースを満たしてほしいという意図で作ったけれども、本当にちゃんとインターフェースを実装できているだろうか?」と心配になることがあります。たとえば、「自分が作った MyFileWriter 型には io.Writer インターフェースを満たさせて、いずれ fmt.Fprintln 関数の第 1 引 ~~数~~ <u>注13</u> ~~に~~ 渡して使うんだ」という意図があった場合には、今も/そして将来ほかの人によって変更が加わったとしても io.Writer インターフェースを満たす関係を担保し続けたいと思うのではないでしょうか。そのような場合には、 **リスト** ~~6~~ <u>注14</u> ~~の~~ ようなイディオムが力を発揮します。もし MyFileWriter 型が io.Writer インターフェースを満たしていない場合には、 **リスト** 6のコードはコンパイルエラーとなります。

###### **▼リスト6 MyFileWriter型がio.Writerインターフェースを満たしていない例** 

**type MyFileWriter struct{}**

**var _ io.Writer = (*MyFileWriter)(nil)** **<mark>// インターフェースの実装チェック // cannot use (*MyFileWriter)(nil)</mark>** **<u><mark>(value of type *MyFileWriter) as io.Writer value in</mark></u>** **<mark>variable declaration: // *MyFileWriter does not implement io.Writer (missing method Write)</mark>**

逆に、「自分が作った NotFileWriter という構造体は io.Writer が必要になる場所で使われる想定ではないから、けっして io.Writer インターフェースを実装したくない」という心配は、 Go ではあまりすることはありません注15 ~~。~~

_Developers first learning about Go often worry about a type accidentally implementing an interface._

_Although it is easy to build hypotheticals, in practice it is unlikely that the same name and signature would be chosen for two incompatible_ <u>注16</u> _~~.~~ operations, and we have never seen it happen in real Go programs (訳) Go 初学者は意図しないインターフェースを実装してしまうことを心配することがあります。_

_確かにそういうことは理論上起こり得ますが、「まったく同じメソッドシグネチャがまったく無関係な場所で使われる」ということは考えづらく、経験的にもそのようなケースはまず見たことがありません。_

# **具体実装を隠すインターフェース** 

_If a type exists only to implement an interface and will never have exported methods beyond that interface, there is no need to export the type itself. Exporting just the interface makes it clear the value has no_ <u>注17</u> _~~.~~ interesting behavior beyond what is described in the interface_

_(訳)その型が特定のインターフェースを満たすためだけに設計されており、それ以外のメソッドを公開するつもりがないのであれば、型自体を非公開にしてしまってよいでしょう。その非公開型が満たすインターフェースのみを公開することで、「インターフェースが持つメソッド以外に重要な役割を持たない」ということを明確にすることができます。_

インターフェースだけを公開しているパターンとしてわかりやすいのは、 gRPC のストリームがあります。

gRPC のストリームの使い道といえば「サーバにメッセージを送る」「サーバからメッセージを受け取る」といった機能が主となります。それを実現するために mutex を使ったりコネクションを操作したりといった詳細な仕様を意識したいという方は少ないのではないでしょうか。そのため、 google.golang.org/grpc パッケー ~~ジ~~ <u>注18</u> ~~で~~ 公開されているのは ClientStream インターフェー ~~ス~~ <u>注19</u> ~~だ~~ けです。当然、それを満たす具体型注20 ~~が~~ パッケージ内部に実装されていますが、それは外部には非公開とすることで具体実装を隠しています。

**type ClientStream interface {** **<mark>(一部抜粋)</mark> SendMsg(m interface{}) error RecvMsg(m interface{}) error }**

> <u>注21</u> _~~.~~ Interfaces separate data from behavior_ 

##### **まとめ** 

Go のインターフェースは、異なる型を抽象化して扱うという需要が多い機能を提供しつつも、型階層を排したことでスケーラビリティを確保した Go の良さを損なわないようにうまく設計されています。それは次の Go 開発者の言葉にも表れていると思います。

_Avoiding the explicit association between interfaces and implementations allows Go programmers to define small, nimble, often ad hoc interfaces, rather than using them as foundation blocks in a complex type hierarchy._

_It encourages capturing relationships and operations as they arise during development, instead of needing to plan and define them all ahead of time._

_This especially helps with large programs, in which the eventual structure is much more difficult to see clearly when first beginning_ <u>注22</u> _~~.~~ development_

_(訳)インターフェースとそれを満たす具体型との間に明示的な関係を作らせないようにしたことによって、開発者は複雑な型階層の部品としてインターフェースを設計するのではなく、小さなアドホックインターフェースを気軽に作るといった使い方ができるようになりました。_

_これにより型やインターフェースの使い方を事前に綿密に計画しておくのではなく、開発していて必要になったときに抽象化/ 一般化したい操作をインターフェースとして切り出すことも可能です。_

_この性質は、最終的なプログラム構造をはじめに見通すことが困難である大規模プログラムの開発にとくに役立ちます。_

本稿が、 Go のインターフェースの設計思想と、それをうまく使えるようにするための理解の一助になっているとうれしいです。注1) コード出典はこちら→ <u>https://rakyll.org/typesystem/</u> (本文に戻る) 注7) The Go Playgroundでの実行はこちら→ <u>https://go.dev/play/p/ogHYd9rjKS6</u> (本文に戻る) 注8) 逆に「AnimalインターフェースにCat/Dog構造体」というようなis-aの関係はGoではやりづらいです......。 (本文に戻る) 注9) Effective Go

<u>https://go.dev/doc/effective_go#interface-names</u> (本文に戻る) 注12) Go FAQ - Why is there no type inheritance? <u>https://go.dev/doc/faq#inheritance</u> 重要部分だけを抜粋しています。 (本文に戻る) 注13) <u>https://pkg.go.dev/fmt#Fprintlnの定義にも記載されているとおり、第</u> 1引数にはio.Writerインターフェースが指定されています。 (本文に戻る)

注14) The Go Playgroundでの実行はこちら→

<u>https://go.dev/play/p/qNli4SujY9e</u> (本文に戻る)

注15) 現に、自作インターフェースではない他パッケージの公開インターフェースを自作の構造体が実装してしまうことを防止するようなやり方を筆者は思いつきません......。 (本文に戻る)

注16) The Go type system for newcomers

<u>https://rakyll.org/typesystem/</u> (本文に戻る)

注17) Effective Go

<u>https://go.dev/doc/effective_go#generality</u> (本文に戻る)

注20) <u>https://github.com/grpc/grpcgo/blob/2997e84fd8d18ddb000ac6736129b48b3c9773ec/stream.go#L507に実装</u> されています。 (本文に戻る)

注21) Go for gophers

<u>https://go.dev/talks/2014/go4gophers.slide#11</u> (本文に戻る)

注22) The Go Programming Language and Environment

<u>https://cacm.acm.org/magazines/2022/5/260357-the-goprogramming-language-and-environment/fulltext</u> (本文に戻る)

### **SD BOOK REVIEW** 

# **JavaScript Primer 改訂2版** 

azu、Suguru Inatomi 著 B5変形判/508ページ 3,900円+税アスキードワンゴ ISBN=978-4-04-893110-6

本書は、JavaScriptを一から学ぶ人に向けた入門書の改訂版だ。ECMAScript 2022までの新機能に基づいた解説が追加されており、またPromise/Async Functionベースで非同期処理を解説しているなど、これからJavaScriptを学ぶのに最適な内容になっている。前半は基本文法、機能の解説が中心。リファレンスと違って網羅性はないが、 JavaScriptという言語を理解するための要点をしっかり紹介している。現在でも使えるがあまり適切でない記法と比較しながら解説しており、JavaScriptのような歴史の⻑い言語を学ぶにあたっては、ひとつの正解をただ知るよりも深い学びが得られるだろう。後半では、Node.jsの使い方などに触れながら開発の例を紹介している。実際の開発でどうJavaScriptを使うのかイメージできるだろう。

**システム設計のセオリー II**

赤俊哉、川又眞綱、中⻄ 良明著 A5判/388ページ 3,200円+税リックテレコム ISBN=978-4-86594-363-4

開発を取り巻くインフラ環境は、オンプレミスの⻑い時代を経て、2010年代のクラウドの爆発的な普及によって大きく進化してきた。設計のセオリーも時代に合わせて変化してきている。そこで、2016年刊行の前著から7年を経て、クラウドベース開発を前提にした設計セオリーを全面刷新してまとめたのが本書だ。「環境がどうであろうと『システム設計の重要性』は不変です」と著者が述べるように、本書では各設計工程において「進化に対応すべきこと」「普遍的なこと」を明確化しながら、どんな開発手法でも最低限必要な原理原則を押さえていく。第1章ではクラウドベースにおける設計の位置付けを概観しているので、必要な工程の章から読み進めることもできるだろう。初学者はもちろん、設計の学びなおしとしても読んでおきたい1冊だ。

# **ソフトウェア開発にChatGPTは使えるのか?** 

小野哲著 B5変形判/336ページ 3,200円+税技術評論社 ISBN=978-4-297-13615-4

筆者の小野哲氏がChatGPTをソフトウェア開発に利用していることをSNSで担当が知り、興味を持ったのが書籍化のきっかけであった。アセンブラコードを読ませたらC のコードを生成した、条件を与えてER図やUML図をChatGPTに描かせてみた、などおもしろそうな知見ばかりである。これはプログラマやエンジニアに役立つ内容になると企画を立案、4月の会議で了承を得てゴールデンウィーク明けにはほぼ脱稿、6月に DTPと校正、7月に出版という超高速刊行になった。AIの話題は変化が激しいので、仮初な内容になるかと思いきや、ChatGPTのしくみも十分に説明、その他のプラグインやAPIの使い方も本質を解説してもらった。ChatGPTの恩恵はエンジニアのためにあるのでぜひご一読いただきたい。

# **基礎から学ぶコンテナセキュリティ** 

森田浩平著 B5変形判/224ページ 2,800円+税技術評論社 ISBN=978-4-297-13635-2

Dockerの普及、定着に伴い、開発/運用の現場で当たり前のようにコンテナを利用することが増えた。コンテナのメリットは、高速かつ軽量な仮想化環境を利用できることにある。一方で、見落とされがちなのがセキュリティだ。コンテナをデフォルトの状態で利用していると、セキュリティホールを突かれ、思わぬトラブルに遭遇し得る。ホストOSにまで被害が及んでしまうこともあるのだ。本書は、そうしたコンテナの弱点、すなわちどの部分に脆弱性が潜み、どの部分が攻撃対象になってしまうのかということを解説している。もちろんそれにとどまらず、コンテナ環境への攻撃を防ぎ、堅牢なコンテナ環境を作る方法も紹介しているので、心配はいらない。日ごろとくに注意せず、なんとなくコンテナを利用している人にこそ読んでほしい1冊だ。

### **AWS活用ジャーニー** 

###### **第12回** 

###### **AWS CloudFormation** 

武田隆志(たけだたかし)クラスメソッド株式会社 <u>https://dev.classmethod.jp/author/takeda-takashi/</u>

<u>@2017takeda @TAKEDA-Takashi</u>

AWSの環境を構築するとき、何もかもを手作業で行うのは現実的ではありません。今回紹介するCloudFormationを使えば、AWSリソースを自動で構築できるようになります。

#### **AWS CloudFormationとは** 

AWS CloudFormation (以下、 CloudFormation )とは、インフラストラクチャをコードとして扱い、 AWS およびサードパーティのリソースをプロビジョニング、管理するサービスです。インフラストラクチャをコードとして扱うことを Infrastructure as Code ( IaC )と呼びます。 IaC は、近年注目を集めている、開発( Development )と運用 ( Operations )を一体化させた DevOps と呼ばれる開発手法の核心的な部分を担い、開発と運用プロセスの一体化、効率化に貢献します。

CloudFormation および IaC には、次のようなメリットがあります。

- ・一貫性と再現性:繰り返し同じ環境を構築できる 

- ・スピードと効率:手作業とは異なり自動的にインフラストラクチャをデプロイできる 

- ・バージョン管理:プログラムのソースコードなどと同じようにバージョン管理できる 

- ・デバッグとテスト:環境を容易に複製できるため、問題のある環境を再現しデバッグやテストができる手作業による環境構築はミスもしやすく、また再構築に時間がかかります。 IaC はそういった問題を解決し、必要であれば同じ環境を複数作成できます。たとえば「本番環境」「ステージング環境」「開発環境」「個人検証環境」のように、開発や検証、テストのための環境も簡単に作成できるのです。またコード化することで、 CI/CD パイプラインから自動的にインフラストラクチャをデプロイできます。「 develop ブランチにマージされたら検証環境にデプロイする」「 main ブランチにマージされたらステージング環境にデプロイする」といったパイプラインを作成したとします。そうすると、開発者は自分たちで作業することなく、自動的にインフラストラクチャを展開、更新できます。 

##### **テンプレート** 

環境を構築する基となるファイルを「 CloudFormation テンプレート」あるいは「テンプレート」といいます。テンプレートはリソースの定義と依存関係を記述する、 JSON または YAML 形式のテキストファイルです( **リスト** 1)。

###### **▼リスト1 CloudFormationテンプレートの例(YAML形式)** 

**AWSTemplateFormatVersion: '2010-09-09' Description: Creates a new Amazon S3 bucket**

**Parameters: BucketNamePrefix: Description: The prefix of the S3 bucket name Type: String**

**Resources: MyS3Bucket: Type: 'AWS::S3::Bucket'**

**DeletionPolicy: Retain Properties: BucketName: !Sub '${BucketNamePrefix}-bucket' AccessControl: Private VersioningConfiguration: Status: Enabled**

**Outputs: BucketName: Description: The name of the newly created S3 bucket Value: !Ref MyS3Bucket**

一番重要なのが Resources セクションで、作成するリソースを定義する必須のセクションです。 MyS3Bucket は作成するリソースの論理 ID となり、テンプレート内ではこの論理 ID を用いて参照できます。 Type で作成するリソースのタイプを指定します。この例では AWS::S3::Bucket を指定しているため、 S3 バケットが作成されます。 DeletionPolicy はスタックを削除した際のリソースの挙動を指定します。通常であればスタックを削除すると、リソースも一緒に削除します。しかしスタックを削除しても残しておきたいリソースはあるでしょう。そんなとき DeletionPolicy に Retain を指定するとリソースを残してスタックを削除できます。 Properties でリソースの細かい設定をします。これは作成するリソースによって指定する値はさまざまなため、ドキュメントなどを参考に定義します。 **リスト** 1の例では、バケット名、 ACL 、バージョニングの指定をそれぞれしています。ほかの要素も気になるところですが、テンプレート全体の説明については後述します。作成したテンプレートは、 AWS CLI の validate-template コマンドを使用して検証できます( **図** 1)。テンプレートのローカルファイルまたは URL を指定します。

###### **▼図1 AWS CLIを使用してテンプレートを検証する** 

**↓検証をパスした場合の例**

**$ aws cloudformation validate-template --template-body file://cloudformationtemplate-ok.yml { "Parameters": [ { "ParameterKey": "BucketNamePrefix", "NoEcho": false, "Description": "The prefix of the S3 bucket name" } ], "Description": "Creates a new Amazon S3 bucket" }**

**↓検証をパスできなかった場合の例 $ aws cloudformation validate-template --template-body file://cloudformationtemplate-ng.yml An error occurred (ValidationError) when calling the ValidateTemplate operation: Template format error: Unresolved resource dependencies [BacketNamePrefix] in the Resources block of the template**

##### **スタック** 

テンプレートが作成できたら、そこから「 CloudFormation スタック」あるいは「スタック」と呼ばれるものを作成します( **図** 2)。「テンプレートとスタック」はちょうど「設計図と実体」という関係に似ています。スタックが作成されると、テンプレートに定義されていたリソースを順に作成していきます。スタックから作成されたリソースは、基本的には直接設定を変更してはいけません。リソースに変更が必要になった場合は、その箇所のテンプレートを修正します。そして修正したテンプレートを使用してスタックを更新します。スタックが更新されると、差分を検出し、リソースに変更を加えたり、リソースの新規作成や削除をしたりします。テンプレートを修正した際に、いきなり更新をかけてしまうと意図せずリソースの削除などが起きる可能性があります。そこで CloudFormation では「変更セット」という、新旧のテンプレートを比較してスタックがどのように変更されるのかという変更案を作成できます。これによって重要なリソースが削除や置換されないか、どのように変更されるのかを確認してから実際に変更を加えられます。問題ないと判断できたら、作成した変更セットを実行することで、スタックを更新できます。

CloudFormation で作成したリソースは手動で変更しないのが基本ということは前述しました。しかし運用をする中で意図せず設定が変わってしまうことはしばしばあります。リソースとテンプレートに差分があるまま運用を続けるのは、一貫性や信頼性が損なわれ好ましくありません。 CloudFormation では、「ドリフト検出」という機能を提供しており、この機能を使用することでリソースとテンプレートとの差分を検出できます。検出結果をもとにリソースとテンプレートを一致するように修正することで、より安定した運用を行えます。

##### **スタックセット** 

スタックはリージョナルリソースです。つまり異なるリージョンにリソースを作成する場合、それぞれのリージョンにスタックを作成する必要があります。また複数のアカウントに展開しようとした際も、やはりそれぞれのアカウントのそれぞれのリージョンにスタックを作成します。これは少し考えただけでも管理が煩雑になりそうです。

CloudFormation ではこのような状況で使用できる「スタックセット」という機能を提供しています。これは単一のオペレーションで複数のアカウントおよび複数のリージョンにスタックを作成できます。スタックセットは非常に便利な機能ですが、通常のスタックに比べて次のような点に注意が必要です。

- ・影響範囲 

- ・リソース制限 

- ・トラブルシューティング 

- ・ IAMロールの準備 

- ・テンプレート設計複数のアカウント、複数のリージョンでリソースを作成するため影響範囲の把握が難しくなります。意図しないスタックが作成されないように注意が必要です。また、 AWS のリソースにはクォータといういわゆる **上限** が設定されており、上限緩和申請などで引き上げられます。こういったことが一因となり、あるリージョンでは成功するが別のリージョンだと失敗することもあります。すべてのスタックを正しく作成するためには、エラーの原因を特定し、ほかのスタックに影響が出ないように修正する必要があります。これは単一のスタックの問題を解決するより難しいでしょう。前述したようにスタックセットは管理者となる単一のアカウントで、ターゲットとなる各アカウントのスタックを管理する機能です。 AWS でマルチアカウントといえば、必ず登場するのが IAM ロールです。スタックセットを使用するにはそれぞれのアカウントに次のロールを作成します。 

- ・管理者アカウント: 

   - AWSCloudFormationStackSetAdministrationRoleロール 

- ・すべてのターゲットアカウント: 

###### AWSCloudFormationStackSetExecutionRoleロール実はこのロールを作成するための CloudFormation テンプレートが AWS から提供されていま ~~す~~ <u>注1</u> ~~。~~ 通常はこのテンプレートを使用すれば十分でしょう。最後に、テンプレートも複数のスタックを作成することを前提に設計する必要があります。代表的な例として、 S3 バケットや IAM ロールなどのグローバルリソースが挙げられます。何も考えずにグローバルリソースを定義して複数リージョンにスタックを作成してしまうと、最初にスタックを作成したリージョンは成功するでしょうが、 2 つめ以降のリージョンでは競合して失敗してしまいます。そのためリソースの名前が重複しないように工夫したり、テンプレートを適切に分割し競合が起こらないようにスタックを作成したりします。 

#### **CloudFormationテンプレート詳解** 

**リスト** 1を例にテンプレートについて一部説明しましたが、ここからは全体像を見ていきましょう。テンプレートには定義できるいくつかの要素があります。おもな要素を紹介します。

##### **AWSTemplateFormatVersion** 

テンプレートフォーマットのバージョンを宣言します。執筆時点 ( 2023 年 7 月現在)で 2010-09-09 が唯一の指定可能な値です。

##### **Description** 

テンプレートに関するコメントを記述します。 0 〜 1,024 バイトのリテラル文字列です。記述した内容はマネジメントコンソールの一覧画面などでも表示されるため、わかりやすい内容を書いておくと運用時などに便利です。

##### **Metadata** 

テンプレートに関する追加情報を提供します。任意の情報を記述でき、たとえば作成するリソースの説明文を書いたりできます。また一部の機能はメタデータを使用するようになっており、次のメタデータキーが利用されます。

- ・ AWS::CloudFormation::Init 

- ・ AWS::CloudFormation::Interface 

- ・ AWS::CloudFormation::Designer 

それぞれ「 cfn-init ヘルパースクリプトの構成タスクの定義」「 CloudFormation コンソールの表示順」「 CloudFormation デザイナーのリソース配置」で使用されます。

##### **Parameters** 

スタックを作成または更新する際に入力値を受け取れます( **リスト** 2)。論理 ID (ここでは EnvName )とデータ型( Type )が必須で、そのほかのプロパティで入力値の制限などができます( **表** 1)。テンプレート内では論理 ID を指定して入力値を参照できます。それによってリソースの設定やスタックの挙動を動的に変更できます。また入力として受け取るパラメータとは別に、 CloudFormation が自動的に定義する擬似パラメータというものもあります( **表** 2)。動的に ARN ( Amazon Resource Name )を組み立てる場合などによく使用します。これらのパラメータを使用することで、異なるアカウントでもそのまま使用できるような移植性の高いテンプレートを作成できます。

###### **▼リスト2 パラメータEnvNameを定義する例** 

**Parameters: EnvName: Description: Environment name. Default: dev Type: String AllowedValues: - prod - dev ConstraintDescription: must specify prod or dev.**

##### **Mappings** 

キーと名前付きの値を対応付けて定義できます。よく使用される例として、リージョンごとに値が異なるものを定義するのに使用されます。代表的なものに EC2 の AMI ID があります( **リスト** 3)。定義したマッピング情報は Fn::FindInMap 関数を使用してテンプレート内で使用します。

###### **▼リスト3 MappingsでリージョンごとのAMI IDを定義する例** 

**Mappings: RegionMap: us-east-1: "AMI": "ami-06ca3ca175f37dd66" us-west-1: "AMI": "ami-0fd61683ae1a27a64" ap-northeast-1: "AMI": "ami-0d3bbfd074edd7acb"**

##### **Conditions** 

リソースの作成や設定される条件を定義します。定義した条件はリソースなどに関連付けます。たとえば、開発環境( dev )ではリソースを作成しないが本番環境( prod )では作成するといった条件付きの定義が可能です。 **リスト** 4では、パラメータ EnvName が prod かどうかを条件にし、 IsEnvProd という名前で条件を定義しています。定義した条件はリソース定義時に Condition プロパティを指定することで使用できます。

###### **▼リスト4 Conditionsで条件を定義する例** 

**Conditions: IsEnvProd: !Equals - !Ref EnvName - prod**

##### **Resources** 

スタックで管理するリソースを定義します。 **リスト** 1をもとに前節で解説したように、 Type で指定したリソースが作成されます。またリソース作成時に、ほかのリソースの物理 ID や属性の値を使用したい場合もよくあります。たとえばセキュリティグループと EC2 を CloudFormation で作成することを考えます。 EC2 インスタンスに SecurityGroups というプロパティがあり、そこに Ref 関数を使用してセキュリティグループを参照することで指定できます。 CloudFormation ではそのほかにも組み込み関数が提供されています ( **表** 3)。

##### **Outputs** 

スタック構築後に CloudFormation から出力される値を指定します。よく使われるものとしては、 DNS 名や EIP ( AWS Elastic IP アドレス)の値などが挙げられます。これらの値を出力しなかった場合、わざわざマネジメントコンソールなどでアクセスして作られたリソースを確認しなければいけません。出力に指定しておくことでこのような手間を軽減できます。

###### **ベストプラクティス** 

- テンプレートをバージョン管理する 

- テンプレートを使用する前に検証する 

- CloudFormationですべてのリソースを管理する(手動で変更しない) 

- スタック更新前に変更セットを作成する 

- 管理するリソースタイプのクォータを確認する 

- 擬似パラメータなどを使用し移植性を高める 

- 注1)・https://s3.amazonaws.com/cloudformation-stackset-templates-us-east- 

   - <u>1/master_account_role.template</u> 

- ・https://s3.amazonaws.com/cloudformation-stackset-templates-us-east- <u>1/managed_account_role.template</u> 

   - (本文に戻る) 

##### **“シェルを書いた”と主張したい** 

#### **魅惑の自作シェルの世界** 

##### **作ればわかるその役割と合理性** 

##### **<mark>第10回</mark>** 

**<mark>● パイプライン処理の実装(その2)</mark>**

上田隆一(うえだりゅういち) <u>@ryuichiueda</u>

**プログラミング言語のRustを使ってBashのクローンを自作することでシェルのしくみを学んでいきます。本連載を通して堂々と「(シェルスクリプトではなく)シェルを書いた」と言えるようになりましょう。**

##### **はじめに** 

前回(本誌 2023 年 8 月号)は、自作の Bash クローンシェル「寿司シェル(コマンド名: sush )」に、パイプラインの処理を実装しました。今回は、前回実装を後回しにした機能を実装していきます。

###### **環境と必要な前提知識** 

本稿記載のコードについては、 Ubuntu 22.04 LTS で cargo 1.65.0 を使ってビルドし、検証しています。本記事は斜め読みでも楽しんでもらえるように書いているつもりですが、

- ・ BashやZshなどの機能が多めのシェルの操作の経験と、基本的な用語の理解 

- ・プログラミングにおいて、構造体や、それに似たものを使った経験と、基本的な理解を前提に書いていますのでご了承ください。また、前回までに出てきた用語については、「 <u>https://b.ueda.tech/?page=sd_rusty_bash 」にま</u> 

とめてあります。そちらも参考にどうぞ。

##### **前回からの変更点** 

本連載のサンプルコードは、 shellgei/rusty_bash リポジトリ ( <u>https://github.com/shellgei/rusty_bash )の</u> sd から始まる名前のブランチにあります。今回は、ブランチ sd/202309_0 のコードに手を加えていきます。リポジトリを新規にダウンロード(クローン)したい人は、リポジトリを置きたいディレクトリで、 **図** 1のようにコマンドを実行しましょう。

###### **▼図1 リポジトリのダウンロードと動作確認の手順** 

**$ git clone https://github.com/shellgei/rusty_bash.git $ cd rusty_bash ←できたディレクトリに移動 $ git fetch ←ブランチの情報を取得 $ git checkout sd/202309_0 ←今回のブランチに移動 $ echo 'echo ko | rev' | cargo run ←動作確認 ok**

前回は、ブランチ sd/202308_3 で終わりました。 sd/202309_0 には次の 2 点の変更があります。

- ・コメントのサポート 

- ・バックスラッシュ( **\** )による入力途中での改行処理の変更それぞれについて説明します。 

**コメントのサポート**

Bash のシェルスクリプト(や、あまり需要はないですが端末)でコメントを記載するときは、行頭か空白のあとに **#** を書き、その後ろに書き込みます。この場合、 **#** から改行文字の手前までが、コメントとみなされます。シェルスクリプトでの例を示します。

**# コメントです。 echo やっほい #コメントです echo これは#コメントじゃない**

コメントについてはこれまで対応していませんでしたが、 sd/202309_0 では、次のように実装しました。

- ・単語(コマンドや引数の名前)を検出するための **scanner_word** メソッドで、文字列の先頭が **#** のときに単語扱いしないようにコードを追加 

- ・ feeder/scanner.rsに、 **scanner_comment** というメソッドを追加 

- ・単純コマンドとジョブのパースの際に、 **scanner_comment** を用いてコメントがないかスキャンこれらの実装については、 sd/202309_0 の src ディレクトリで、 

###### **$ grep scanner_comment ./ -Rn** 

と検索をかけて当該部分を探し、コードを読んでみてください。実装されたコードの挙動については、 **cargo run** して、 **図** 2のように入力してみると良いでしょう。以後、記事中の実装でも、コメントの対応が必要になります。

###### **▼図2 コメントの動作確認(sush)** 

**#こめんとですよー echo a #こめんとでーす**

**a**

**#こめんと**

**echo a#これはコメントではない**

**a#これはコメントではない**

**(#コメント**

**> echo a ) a**

- ※ 本記事中の絵文字はtwemojiを使用しています。 <u>https://twemoji.twitter.com/</u> 

"twemoji" by Twitter, Inc. and other contributors is licensed under CC BY 4.0. <u>https://creativecommons.org/licenses/by/4.0/deed.ja</u>

###### **バックスラッシュによる改行の処理** 

バックスラッシュについては、実装された挙動を先に **図** 3に示します。試すときは、 **\** の後ろですぐに改行を入れましょう。そうしないと単語の一部として認識されます。この挙動は、 Bash のものと同じです。 Bash の場合、 **\** による改行は、シングルクォートでクォートされた中身やコメント以外の箇所で、自由に挿入することができます。

###### **▼図3 バックスラッシュの挙動(sush)** 

###### **↓2番目の引数を次の行に記述** 

**echo ながーーい引数 \ > 次の引数**

**ながーーい引数次の引数**

**↓コマンドの名前の途中で改行**

**e\**

**> cho こんなところに改行入れるなこんなところに改行入れるな**

**↓「|&」の途中で改行 echo なぜ改行を入れる? |\ > & rev ?るれ入を行改ぜな**

実はこの機能、これまでのコード内にも実装されていたのですが、「 **\** +改行文字」が入力されたら無条件に削除し、次の行を読み込むという処理になっていました。ただ、これだとコメントやシングルクォート内で **\** を残さなければならない場合に対応できないので、 **scanner_** ○○内で、今何を読み込んでいるかに応じて対応を変えるように実装しなおしました。具体的には、 feeder/scanner.rs の

**scanner_word** 、 **scanner_blank** 、 **scanner_pipe** 内で「 **\** +改行文字」の削除と次の行の読み込みをするようにしました。実装は feeder/scanner.rs で読めます。また、 **リスト** 1にも例が出てきます。この変更で、関係各所の関数やメソッドにコードや引数を追加することになってしまい、少々不本意です注1 ~~。~~ もっと良い方法があれば、また扱いたいと思います。

##### **改行を含むパイプラインへの対応** 

さて、 **\** による改行とは別の機能として、 Bash ではパイプラインの途中で行を改めてコマンドを書けます。 sush でも対応しましょう。

###### **パイプラインと改行** 

まず、パイプライン中での改行について、 Bash で確認しておきましょう。 Bash では、 **図** 4のような書き方が許されています。パイプの後ろにも空白やコメントを入れることができます。 **図** 4は端末上での入力方法ですが、シェルスクリプトの場合も同様に空白、空行、コメントを置けます。とくに行を改めてコメントが書ける機能は、 **図** 4の 3 行目のように、パイプを流れるデータについて説明を入れるときに重宝します。

###### **▼図4 パイプライン中での改行の挙動(Bash)** 

**↓|の後ろや間に改行や空白、コメント**

> **$ seq 10  |   #こめんと** 

**# 1から10までの数字がパイプから出る**

**> head -n 5   |  # 上5行だけを出力 > > wc  # 改行数、単語数、バイト数を表示 5       5      10**

今のは Bash の話ですが、それ以前に開発されたいくつかのシェルでは、 **\** を付けないと改行できない場合があります注2 ~~。~~ そのため、互換性が必要なときには、

**echo 互換性が |\ awk '$0=$0"重要なこともある"'**

と書く場合もあります。また、これを積極的に利用して、次のようなシェルスクリプトを書く人もいるようです。

**echo 私は \ | awk '$0=$0"この書き方が"' \ | awk '$0=$0"いい"'**

###### **実装** 

以上をふまえて、パイプライン内の改行に対応しましょう。まず、 feeder/scanner.rs に、半角スペース、タブ、改行が連続する部分を検出するスキャナを実装します。もともと scanner.rs に存在している **scanner_blank** と処理が酷似するので、 **scanner_blank** も整理して **リスト** 1のように実装しましょう。

**▼リスト1 scanner_blankの改修とscanner_multiline_blankの実装(sd/202309_1ブランチのfeeder/scanner.rs)**

**14 fn scanner_chars(&mut self, charlist: &str, core: &mut ShellCore) -> usize { 15     let mut next_line = false; 16     let mut ans = 0; 17     for ch in self.remaining.chars() {**

**18         if &self.remaining[ans..] == "\\\n" {** **<mark>// 「\」による改行のチェック</mark> 19             next_line = true; 20             break;** **<mark>// ループを抜けて28行目で処理しなおし</mark> 21         }else if let Some(_) = charlist.find(ch) { 22             ans += 1; 23         }else{ 24             break; 25         } 26     } 27 28     if next_line && self.feed_and_connect(core){** **<mark>// 次の行を読んでやりなおし</mark> 29         return self.scanner_chars(charlist, core); 30     } 31     ans 32 }** **<mark>(..略..)</mark> 57 pub fn scanner_blank(&mut self, core: &mut ShellCore) -> usize { 58     self.scanner_chars(" \t", core) 59 } 60 61 pub fn scanner_multiline_blank(&mut self, core: &mut ShellCore) -> usize { 62     self.scanner_chars(" \t\n", core) 63 }**

14 〜 32 行目の **scanner_chars** は変更前の **scanner_blank** を書き換えて作ったもので、 **charlist** で与えられた文字列内にある文字が連続している部分の⻑さを返す関数です。 28 行目で使っている **feed_and_connect** は、先述の \ による改行への対応のためにブランチ sd/202309_0 で実装したものです。 **self.remaining** から後ろの 2 文字( **\** と改行文字)を削り、次の行を読んで **self.remaining** に連結します。 29 行目は、次の行が連結された **self.remaining** を自分自身に入力し、処理をイチからやりなおしています。

57 〜 59 行目の **scanner_blank** は、 **scanner_chars** にスペースとタブの 2 文字を渡すだけの関数として実装しなおしました。しばらく説明していなかったので補足ですが、 Rust のメソッドや関数は、 **return** がなくても最後にセミコロンなしで書いた式の値を返します。新たに実装した 61 〜 63 行目の **scanner_multiline_blank** は、 **scanner_chars** に、半角スペースと \ **t** に、 **\n** も加えて **scanner_chars** に渡します。

scanner.rs の変更が終わったら、いったん **cargo build** して、エラーがないか確認しておきましょう。前回から引き続きワーニングが出る状態ですが、次に進みます。今度は、パイプ間の空白、空行、コメントを取り込むための **Pipeline::eat_blank_and_comment** という関数を **リスト** 2のように実装します。ループの中で空白、空行とコメントを交互に読み込んでいき、何も読み込めなくなった時点で処理を終えるという実装になっています。

**▼リスト2 Pipeline::eat_blank_and_commentの実装(sd/202309_1ブランチの elements/pipeline.rs)**

|**65 fn**<br>**&mu**<br>**66**|**eat_blank_and_comment(feeder: &mut Feeder, ans: &mut Pipeline, core:**<br>**t ShellCore) {**<br>**loop {**|
|---|---|
|**67**|**let blank_len = feeder.scanner_multiline_blank(core);**|
|**68**|**ans.text += &feeder.consume(blank_len);**<br>**//空白、空行を削除**|
|**69**|**let comment_len = feeder.scanner_comment();**|
|**70**|**ans.text += &feeder.consume(comment_len);**<br>**//コメントを削除**|
|**71**|**if blank_len + comment_len == 0 {**<br>**//空白、空行、コメントがなければ出る**|
|**72**|**break;**|
|**73**|**}**|
|**74**|**}**|
|**75 }**||

最後に、 **リスト** 3のように、 **Pipeline::parse** 関数を書き換えます。前回も見た **man bash** に書いてある( **time** や **!** を除いた)パイプラインの構文「 **command [ [|||&] command2** ...... **]** 」の最初のコマンドを読み込んでいるのが 80 〜 82 行目、後ろの「パイプライン+コマンド」を読み込んでいるのが 84 〜 94 行目です。 84 行目でパイプが読み込めたら、まず **リスト** 2の **eat_blank_and_comment** を使ってコマンドまでの余白、コメントを除去します。その後、パイプの後ろには必ずコマンドがなければならないので、コマンドが読み込めるまで 85 〜 93 行目のループで追加の行を要求し続けます。これで **cargo run** して、 **図** 5のように改行を入れたパイプラインで動作確認しておきましょう。

**▼リスト3 Pipeline::parseの改修(sd/202309_1ブランチのelements/pipeline.rs)**

**77 pub fn parse(feeder: &mut Feeder, core: &mut ShellCore) -> Option<Pipeline> { 78     let mut ans = Pipeline::new(); 79 80     if ! Self::eat_command(feeder, &mut ans, core){** **<mark>// 最初のコマンド</mark> 81         return None; 82     } 83 84     while Self::eat_pipe(feeder, &mut ans, core){ 85         loop {** **<mark>// パイプの後ろでの処理</mark> 86             Self::eat_blank_and_comment(feeder, &mut ans, core); 87             if Self::eat_command(feeder, &mut ans, core){ 88                 break;** **<mark>// コマンドがあれば85行目のloopを抜けてパイプを探す</mark> 89             } 90             if ! feeder.feed_additional_line(core) {** **<mark>// 追加の行の要求</mark> 91                 return None; 92             } 93         } 94     } 95 96     Some(ans) 97 }**

###### **▼図5 パイプライン中での改行の挙動(sush)** 

**$ cargo run (まだワーニングが出ます) seq 10 | ←パイプの後ろに空白 > ←空行を入れてみる >  #コメコメ ←コメントを入れてみる**

**>      wc -l ←コマンドの前に空白 2**

###### **> grep 1 |** 

##### **複合コマンドをパイプライン中で使えるようにする** 

今度は、複合コマンドをパイプラインにつなげられるようにしましょう。

###### **丸括弧コマンド(サブシェル)への追加実装** 

丸括弧で囲むタイプの複合コマンド(本連載では丸括弧コマンドと読んでいます)については、少しの改修でパイプラインにつなげられます。まず **リスト** 4のように、 **ParenCommand::exec** で、引数 **pipe** を **Script** の **fork_exec** メソッドに渡します。

**▼リスト4 ParenCommand::execでの変更(sd/202309_2ブランチの elements/command/paren.rs)**

**14 fn exec(&mut self, core: &mut ShellCore, pipe: &mut PipeRecipe) { 15     match self.script {** **<mark>// script: 丸括弧の中身のスクリプト</mark> 16         Some(ref mut s) => s.fork_exec(core, pipe),** **<mark>// 引数にpipeを追加</mark>**

次に、 **Script** 側を変更します。 **リスト** 5の 28 行目のように、 **fork_exec** メソッドに引数 **pipe** を加えます。この **pipe** を通じ、プロセスがフォークしたあと、パイプを操作します。具体的には、 33 行目のように子側のプロセスで **pipe.connect()** を呼び出し、必要な場合はパイプの前のコマンドの出力を子のプロセスの入力へ連結します。また、 38 行目では、親側のプロセスで **pipe.parent_close()** を用い、不要なファイル記述子を閉じます。これらの処理は、前回に simple.rs で追加した処理と同じです。これで、 **cargo run** すると、 **図** 6のように丸括弧コマンドをパイプラインに挟めるようになります。

###### **▼図6 丸括弧コマンドをパイプラインにつなげる(sush)** 

**( echo たけやぶ ; echo やけてない ) | rev ぶやけたいなてけや ↓中にパイプがあっても後ろに改行があっても大丈夫 ( echo たけやぶ | nl ) | > rev ぶやけた  1**

###### **グループコマンドへの追加実装** 

今度は波括弧で囲った、 **{ echo a ; echo b ; }** のようなグループコマンドをパイプラインにつなげるようにしましょう。ただ、グループコマンドは前々回(本誌 2023 年 7 月号)で説明したとおりフォークしません。どうしましょう?

答えを言うと、「パイプラインにつながっているときはフォークする」ように実装します。 Bash でそうなっていることがわかる例を **図** 7に示します。例 1 ではグループコマンド内で定義した変数 **A** を外で **echo** すると、 **A** に代入した文字列が表示されます。一方、例 2 では、同様に定義した **B** の文字列が表示されません。

###### **▼図7 グループコマンドをパイプラインにつなげる(Bash)** 

**例1 $ { A=変数作ったよ ; } ; echo $A 変数作ったよ例2 $ { B=変数作ったよ ; } | echo ほんとかな ; echo $B ほんとかな ←echo $Bの結果の空行(Bは空文字)**

この理由は、例 2 のグループコマンドがフォークするからです。フォークすると **{}** 内が別のプロセスで実行されるため、内と外の変数 **B** は、異なるプロセスに存在するまったく別のものとなります。シェルのこの挙動、言語としては初心者殺しであまり褒められたものでもありませんが、プロセスを操作するための言語としては、当然と言えば当然です。ということで、「パイプにつながれていればフォークする」という処理を実装しましょう。まず **リスト** 6のように、 **PipeRecipe** に **is_connected** というメソッドを追加します。このメソッドは、

**PipeRecipe** のインスタンスの持ち主(コマンド)が、有効なファイル記述子( 0 以上の記述子)を持っていたら、ほかのコマンドと連結されていると判断して **true** を返します。

###### **▼リスト6 PipeRecipe::is_connectedの追加(sd/202309_3ブランチのpipe_recipe.rs)** 

**14 impl PipeRecipe {** **<mark>(..略..)</mark> 26     pub fn is_connected(&self) -> bool {** **<mark>// このメソッドを追加</mark> 27         self.recv != -1 || self.send != -1 || self.prev != -1 28     }**

次に、 **BraceCommand::exec** で **is_connected** を使ってフォークするかどうかを切り替える処理を書きます。 **リスト** 7に示します。 15 〜 18 行目は、 **self.script** (型: **Option<Script>** )から **Script** 型の中身を取り出して **script** という名前を付ける処理です。 20 〜 24 行目の処理では、グループコマンドがパイプとつながっていれば **fork_exec** を呼び出し、そうでなければ従来どおりフォークしない **exec** を呼び出しています。

###### **▼リスト7 BraceCommand::execの変更(sd/202309_3ブランチの elements/command/brace.rs)** 

|**13 i**<br>**14**<br>**15**|**mpl Command for BraceCommand {**<br>**fn exec(&mut self, core: &mut ShellCore, pipe: &mut PipeRecipe) {**<br>**let script = match self.script {**|
|---|---|
|**16**|**Some(ref mut s) => s,**|
|**17**<br>**18**<br>**19**|**_ => panic!("SUSH INTERNAL ERROR (BraceCommand::exec)"),**<br>**};**|
|**20**|**if pipe.is_connected() {**<br>**//パイプに連結されている**|
|**21**|**script.fork_exec(core, pipe);**<br>**//フォーク**|
|**22**|**}else{**<br>**//パイプに連結されていない**|
|**23**|**script.exec(core);**<br>**//フォークせず実行**|
|**24**|**}**|
|**25**|**}**|

これで、丸括弧コマンドと同様、パイプにつなげるようになります( **図** 8)。ビルド時のワーニングも消えます。

###### **▼図8 グループコマンドをパイプラインにつなげる(sush)** 

**$ cargo run echo 波括弧コマンドも | { cat ; echo 大丈夫 ; } | rev もドンマコ弧括波夫丈大**

##### **ビルトインコマンドもフォーク** 

今の話から類推できますが、ビルトインコマンドも、パイプでつないだ場合はフォークします。 sush もそういう仕様にしましょう。 **リスト** 8のように **Simple::exec** を変更します。

###### **▼リスト8 Simple::execの変更(sd/202309_4ブランチの elements/command/simple.rs)** 

|**20 i**|**mpl Command for SimpleCommand {**|
|---|---|
|**21**<br>**22**|**fn exec(&mut self, core: &mut ShellCore, pipe: &mut PipeRecipe) {**<br>**if ! pipe.is_connected() && core.run_builtin(&mut self.args) {**|
|**23**|**return;**<br>**// ↑パイプにつながっていないことの確認を追加**|
|**24**|**}**|
|**25**||
|**26**|**self.set_cargs();**|
|**27**|**match unsafe{unistd::fork()} {**|
|**28**|**Ok(ForkResult::Child) => {**|
|**29**|**pipe.connect();**|
|**30**|**if core.run_builtin(&mut self.args) {**<br>**// 30〜32行目を追加**|
|**31**|**core.exit();**|
|**32**|**}**|
|**33**||
|**34**|**match unistd::execvp(&self.cargs[0], &self.cargs) {**|

変更箇所の 1 点目は 22 行目で、フォークしないでビルトインコマンドを実行する場合を、パイプにつながれていない場合に限定しています。 2 点目は 30 〜 32 行目で、 27 行目にフォークしたあとにもう一度ビルトインコマンドを探して実行しようとしています。 22 行目で実行できなかった場合、この時点でビルトインコマンドが実行されます。コマンドの実行後は、 31 行目でプロセスを終わらせる必要があります。動作確認は、 **図** 9のようにホームディレクトリ以外で cd をパイプに挟んで実行し、ディレクトリが移動しないことを確認すると良いでしょう。

**▼図9 ビルトインをパイプにつなげる(sush)**

**cd | pwd ←パイプにcdをつなぐとディレクトリは移動しない**

**/home/ueda/GIT/rusty_bash/src/elements/command**

**cd ; pwd ←順番に実行するとcdでホームに移動**

**/home/ueda**

今回はここまでにします。次回は少しコードをリファクタリングしたあと、パイプ「 **|&** 」の機能を実装します。その後、リダイレクトの実装を開始します。

- 注1) Bashのコードがどうなっているかを解説すべきですが、こちらも不本意ながらややこしくてあきらめました。 

   - (本文に戻る) 

- 注2) dash、ashを試しましたが、どちらもパイプの後ろで「 **\** 」なしで改行できました。 

   - (本文に戻る) 

#### **<mark>Ansible</mark>** 

### **現場を支えるPlaybook** 

# **<mark>インフラ作業自動化&効率化アイデアレシピ</mark>** 

**<mark>第 10 回</mark>**

##### **正常性確認用Playbookの実装テクニック** 

さまざまなITインフラの現場でAnsibleとPlaybookが活躍しています。本連載では現場で実際に活用されるPlaybookに注目し、秘められた工夫やエピソードについて紹介します。第 10回では、Ansibleモジュールを利用して、インフラ検証試験を自動化するノウハウを紹介します。岡田淳(おかだじゅん)楽天グループ株式会社

#### **はじめに** 

クラウドインフラの構築において、品質の担保には非常に多くの工数を必要とします。とくに、プライベートクラウドの構築に関しては、データセンタでのサーバのラッキングや結線から始まり OS のインストール、仮想化ソフトウェアのインストール、アプリケーションが利用する設定の投入などに至るまで、非常に多くの作業が複数の部門にまたがって実施されます( **図** 1)。インフラ構築部門ではこの構築フェーズが完了したときに、今までの作業がすべて間違いなく行われており、アプリケーションが期待どおりに動作できるクラウドインフラを提供しているかどうかを担保する必要があります。この品質担保を行うための試験項目や観点は非常に多岐にわたります。このようなプライベートクラウドを 1,000 以上の複数拠点において構築する際に、確認項目の多い正常性確認(試験)をマニュアルで行うことは非効率/非現実的であり、運用コスト削減の観点からも自動化の必要があると言えるでしょう。本連載では、クラウドインフラに対する試験を Ansible によって自動化し、構築時間を劇的に短縮した例を取り上げます。今回紹介する Role や Task の分割はほかの試験用途でも応用できますので、参考にしてみてください。

#### **ユーザーの要求** 

前述のように、構築チームは複数の拠点に対して、複数の確認項目を持つ試験を実施しなければなりません。また、構築スケジュールに間に合うよう、正常性確認を含む作業を可能な限り短期間で完了しなければなりません。この背景から、「一度の Playbook の実行で、すべての確認項目に対する合否の結果を知りたい」という要求が発生します。つまり、ユーザー目線で欲しい Playbook は、実行すると **図** 2のように出力されるイメージです。

###### **▼図2 一度実行するだけですべての試験結果がわかるPlaybookのイメージ** 

**$ ansible-playbook -i inventory.yml check_all.yml ←単一のPlaybookを実行することで、すべての試験を流したい**

**(..略..) TASK [show_result : Assert all test results] ******************************* ↑すべての試験結果を1つのTaskで確認したい**

**ok: [host1] => { "changed": false, "msg": "All assertions passed" } fatal: [host2]: FAILED! => {  "assertion": "not (alert_check_assert |  d({\"failed\": false})) | json_query(\"failed\")", "changed": false, "evaluated_to": false, "msg": [ "Sanity Check failed", "Something went wrong during checking item ['alert_check']", "failed message is [['test1 is failed', 'infra_license: True', ‘k8s_controller_svc_status: True', ': True', 'k8s_kubelet_status: False', 'k8s_node: True', ‘k8s_pod_status: True', 'k8s_pod_age: True']]" ] } (..略..)**

**図** 2において、 host1 は “All assertions passed” と表示されています。これは正常性試験の結果、問題が発見されなかったことを示します。 host2 では、 alert_check_assert という項目で問題が発見されています。詳細なメッセージ( msg )を確認すると k8s_kubelet_status が False と表示されているため、このホストにおいて kubelet の状態が期待どおりになっていないとわかります。

#### **ユーザーの要求に対する実装上の課題** 

##### **1複数の確認項目とRole** 

前述のとおり、試験では複数の観点で複数の項目に対する試験を行います。すべての処理を 1 つの Role にまとめることも不可能ではありませんが、これでは凝集度が高く、メンテナビリティの低下の原因となります。またチーム開発では分担が難しくなります。

##### 2 **Playbookに求められる要件** 

構築チームには決まったスケジュールがあり、可能な限り簡単に、少ない手間で試験を実施し、異常を検知したいと考えています。つまり、 Role の分 Playbook をひとつひとつ実施するのではなく、一度の Playbook 実行でまとめて異常を検知したいと考えます。そのため、複数の Role を 1 つの Playbook から呼び出す必要があります。

##### **3失敗を前提としたTaskの実装** 

異常を検知するための Playbook を実装するためには、各 Task が失敗することを前提として実装を行わなければなりません。たとえば、本来 A である設定を確認するコマンドを発行したが、結果が B という値だった場合、何も工夫をしなければ Playbook は異常を検知してその時点で終了してしまいます。これでは、「一度の Playbook 実行で、すべての試験結果を知りたい」という要求を満たすことができません。

#### **Playbook実装時の工夫** 

これらの課題に対し、どのように工夫して Playbook や Role を実装したか紹介します。

##### **Roleの分割とマッピング** 

前述のとおり、正常性確認では複数の確認項目が存在します。そのため、 **表** 1のように確認項目ごとに Ansible の Role をマッピングします。また、これらの項目がそれぞれ問題なかったかどうかを表示する Role も必要です。これらを考慮すると Role の構成は **図** 3のようになります。

###### **▼図3 試験用Roleの構成** 

**roles ├─ check_epa_config ←各試験項目に対応するRole ├─ check_hw_error ├─ check_image_pull ├─ check_nic_status ├─ check_nw_connectivity ├─ check_pci_device ├─ check_sw_installed ├─ check_sw_version (..略..) └─ assert_all_results ←試験結果をまとめて表示するRole**

##### **Playbookの構成** 

「一度の Playbook 実行ですべての試験項目を確認したい」というユーザーからの要求に基づき、すべての Role を順次呼び出すよう

Play を記載します。また、すべての確認項目に対応する Role を呼び出したあとに、試験結果をまとめて表示する Role を呼び出します ( **リスト** 1)。

###### **▼リスト1 すべてのRoleを呼び出すPlaybook** 

**--- hosts: ‘all’ tasks: - name: ‘Include check_epa_config’ include_role: name: check_epa_config - name: ‘Include check_hw_error’ include_role: name: check_hw_error** **<mark>(..略..)</mark> - name: ‘Include assert_all_results include_role: name: assert_all_results**

##### **エラーを無視するTaskの実装** 

前述のとおり、正常性確認では期待どおりの結果でないことや、 Task 実行中のエラーを考慮する必要があります。ここでは、いったんエラーを無視するために ignore_errors モジュールを活用します。また、 Task の実行の成否を検査するためのフラグや変数を用意しておき、結果を格納していきます。なお、本項では、 1 つのソフトウェアバージョンを 1 つの Role で確認する例を取り上げますが、複数項目の確認を行う際には、必要に応じて複数の Task や変数を用意する必要があります。

# **■試験用RoleでのTaskの実装** 

確認結果の成否を示すフラグを用意します( **リスト** 2)。デフォルトの値は失敗を示す false としておきます。

**▼リスト2 defaults/main.yml**

**--flag_docker_version: false**

正常性確認処理を **リスト** 3に示します。これは、大きく 3 つの段階に分割できます。最初の Task (1)では、試験対象からの値を取得する処理を記載します。ここでは、 docker-ce というソフトウェアのバージョンを取得し、 actual_docker_version という変数に格納する例を記載します。また、値の取得処理自体が失敗する際に備え、 ignore_errors を true とし、その場合でも処理が止まらないようにします。

2 番めの Task (2)では、試験対象から取得した値が期待どおりかを確認し、フラグの値に成否を詰め込む処理を行います。期待どおりかどうかの成否は when 句で判定します。前段の値を取得する処理自体が失敗している可能性があるため、 actual_docker_version に値が入っていることを確認します。次に、試験対象から取得したソフトウェアバージョンが期待どおりかを判定します。この 2 つの条件にともに合致した場合に flag_docker_version の値を true に変更し、成功と判定します。

3 番めの Task (3)では、 role 全体での成功・失敗を assert モジュールを用いて検査します。 assert 節でのフラグの値が true であれば、問題なかった旨のメッセージを表示します。 false の場合には期待どおりでない旨を示すメッセージを表示します。また、その結果を check_sw_version_assert という変数に格納します。ここでも、失敗時に後続の処理を行うために ignore_errors を true に設定します。3では 1 つの正常性確認結果を示すフラグだけを検査していますが、 1 つの Role で複数の正常性確認を行う際には、 assert 節に複数のフラグを定義することで Role 全体での成功/失敗の判定が可能です。

# **■試験結果表示用のrole** 

すべての試験用 Role の後に、 Playbook から結果表示用の Role を呼び出します( **リスト** 4)。

###### **▼リスト4 tasks/main.yml** 

**--- name: 'Assert All test results' assert:**

###### **that:** 

**- 'not (check_sw_version_assert | d({"failed": false})) | json_query("failed")' fail_msg: [ 'Some tests went failed', 'failed role is {{ check_results | json_query("[?failed==`true`].role_name") }}', 'failed message is {{ check_results | json_query("[?failed==`true`].msg") }}' ] vars: check_results:**

**- '{{ (check_sw_version_assert | d({"failed": false})) | combine({"role_name": "check_sw_version"}) }}'**

この Role では、 assert モジュールを用いて、試験結果の成否判定を行います。 assert モジュールでは、試験用 Role の 3 番めの Task ( **リスト** 33)で格納した変数に failed という文字列が含まれるかどうかを判定します。複数の試験結果を表示する際には、リスト形式で変数を複数定義できる assert モジュールを使って、複数の Role で assert した結果を列挙します。また、 vars モジュールで失敗時のメッセージに表示するための変数 check_results を定義します。ここでは試験用 Role において assert モジュールで失敗判定がされた Role の Role 名を変数

check_results.role_name に追加します。これにより、ユーザーは「どの Role で、どんな失敗があったか」を知ることができます。最終的に、すべての試験用 Role を実行した後、 **図** 4のように出力され、まとめて試験結果を確認できます。

###### **▼図4 試験用Roleの実行結果** 

**TASK [assert_all_results : Assert ATP test check results]**

*************************************** 

**[WARNING]: Collection community.general does not support Ansible version 2.10.14**

**fatal: [localhost]: FAILED! => { "assertion": "not (check_sw_version_assert           |  d({\"failed\": false})) | json_query(\"failed\")",**

**"changed": false, "evaluated_to": false, "msg": [ "ATP check failed", "failed role is ['check_sw_version']", "failed message is [['expected version is: 23.0.5-1, actual version is 23.0.52']]" ] }**

#### **まとめ** 

筆者は Ansible を用いて試験を自動化するユースケースは少なくないと考えています。本連載ではその一例としてクラウドインフラの試験を自動化していく中でユーザー要求に応えるべく複数試験結果を一覧表示するためのテクニックを紹介しました。利用したモジュールは、 ignore_errors や assert 、 vars といった built-in の標準的なモジュールですが、組み合わせることで今回のような要件をコードとして表現することが可能です。もちろん、要件しだいではどのように実装を行うのが最適か変わるかと思いますし、 Serverspe ~~c~~ <u>注1</u> ~~や~~ ほかの試験ツールを用いることも選択肢として挙げられるでしょう。本記事が Ansible で自動試験の実装を検討されている方々のご参考になれば幸いです。

<u>@ryosuke927</u>

### **あなたのスキルは社会に役立つ** 

#### **エンジニアだからできる社会貢献** 

東日本大震災の発生直後に発足したHack For Japanや「市⺠が主体となって自分たちの街の課題を技術で解決するコミュニティ作り支援」を掲げるCode for Japanのメンバーを始めとして、日本各地で技術を活用した社会貢献活動が行われています。本連載では、防災や減災、地域の活性化や課題解決、そして人材育成など、「エンジニアだからできる社会貢献」の取り組みをお届けします。

##### **第141回** 

**AI技術と国会議事録が融合した「国会議事録検索(The Diet Search)プラグイン」**

●シニアプログラミングネットワーク代表小泉勝志郎(こいずみかつしろう)

近年、 AI 技術は急速に進化を遂げており、その中でも生成系 AI はとくに注目を浴びています。今回は、生成系 AI の中でも話題の ChatGPT を活用した、まさに革新的なプロジェクトを紹介します。それは、株式会社自動処理が開発した ChatGPT プラグイン「国会議事録検索( The Diet Search )プラグイン」です。 AI 技術と政治が交差する画期的な取り組みとして、大量の国会議事録から関連性の高い情報を探し出すというものです。国会の議論が一人一人の市⺠にも身近に感じられる時代の幕開けとなるかもしれません! 今回、株式会社自動処理の代表取締役である高木祐介氏にインタビューをしました。プラグイン開発の舞台裏と未来の可能性に迫ります。

#### **開発背景:社会に貢献するChatGPTプラグインの誕生** 

今や、人工知能( AI )技術は私たちの生活に欠かせない存在となっています。その中でも、 OpenAI によって開発された言語理解 AI である ChatGPT は、自然言語の理解と生成能力を活かし、数多くの応用分野で活躍しています。高木氏は、以前からこれらの進歩に注目していました。「 AI については数年開発を実施してきた。 ChatGPT については去年の 11 月のリリースタイミングで使ってみて、その可能性に大いに感銘を受けたんです。毎日実業務に使えるように試行錯誤を重ね、 3 月くらいからは AI のニュースを毎日調査し、情報発信も始めました」と語ります。その中で、 ChatGPT がプラグインを開発できる機能を実装することを知り、「これは自分で作ってみよう」と決意したのだそうです。既存の API をただ組み合わせるだけではなく、本当に社会に役立つ新たな価値を創造することを目指した結果生まれたのが、「国会議事録検索( The Diet Search )プラグイン」です。生成系 AI とオープンデータが融合し、国会の議論を広く一般に開放するという新たな道が切り開かれたのです。国会議事録検索( The Diet Search )プラグインは ChatGPT の GPT-4 のプラグインから使用可能です。 Plugin store で「 diet 」と検索し、インストールすると利用できます( **図** 1)。なお、国会議事録検索 ( The Diet Search )プラグイン自体は無料ですが、プラグインを利用するには ChatGPT の有料ユーザーである必要があります。

#### **プラグイン選択の理由と挑戦** 

**小泉** :では、プラグインの紹介から始めていただけますか? **高木** : 6 月 16 日に ChatGPT のプラグインとして「国会議事録検索 ( The Diet Search )プラグイン」を公開しました。これは、国⺠ の声と国会議事録をつなげるしくみを提供し、市⺠が疑問に思ったことが国会でどのように議論されているかを調査できるシステムです。

**図** 2は具体的な使用例です。「 60 歳で年金をもらうか、 65 歳で年金をもらうか迷っている」という質問に対して、国会で議論された事例を紹介しています注1 ~~。~~

- たとえば年金受給の選択について相談したら、年金を受けるル 

- ールを決める際にはいくつかの要素を考慮する必要があると教えてくれます。それで、最近の国会の議事録を検索して、年金について最新の議論や法律の変更を調べることができます。 

- **小泉** :国会と関係ないような日常的に思う疑問から、国会でどのような議論がされたのかを知ることができるなんて、本当にすばらしい行動ですね。 

- **高木** :そうですね、日常の素朴な疑問から国会でどういう議論がされているのか興味が持てるんですよ。 

- **小泉** :今回サービスではなくプラグインを採用しているのはなぜでしょうか。 

- **高木** : OpenAI から ChatGPT プラグインが公開され、 ChatGPT に外部データを取り込んで実行できるようになりました。それを見て、私は「プラグインを一度試してみたい」と思ったんですよ。それ以外にも、我々が作っている意見募集のシステムである「 IDEABOX ~~」~~ <u>注2</u> ~~は~~ 市⺠や行政の人々と対話するプラットフォームなので、将来的に関連付けることができると考えました。そして、 ChatGPT プラグインは最初に 80 個くらい公開されたんですけど、それがほとんどイケてなかった。単なる昔ながらのしくみで、なんとなく自社で持っている API とつなげた、みたいな状況。だから、何か新しい使い方ができるものを出したら、これはみんな喜んでくれるかなと思い、そこで作ったんです。システムはまだ発展途上ですが、 AI のチャットボットは 24 時間稼動しているわけですから、このシステムを使えば、国会の議論をすぐに調べることができます。 

- **小泉** :プラグインだと ChatGPT 側で多言語対応もやってくれますからね。 

- **高木** :海外の方でも日本の国会の情報をちゃんと調べられるので、これは非常にいいなと。 

- **小泉** :海外メディアが日本を報道するときに裏取りしやすくなりますよね。 

#### **なぜ国会議事録なのか?** 

- **小泉** :活用方法を聞いただけでワクワクしてきますね。言葉は悪いですが、これはかなり遊べそうですね。国会議事録を対象にしたのはなぜでしょう? 

- **高木** :もともと興味があって、シビックテックのようなものを実施しようと思っていました。政治とは何か、理解したいと思っていましたが、誰が何を言っているのか、どのような議論が行われているのかまったくわからなかったのです。選挙についてもよくわからない状況でしたが、これを使えば少し理解できるようになりました。というのも、先日引っ越しをしたんですよ。引っ越しのタイミングで選挙がありましたが、街頭演説をしている人が何をアピールしているのかまったくわからないまま投票しなければならなかったんです。その点を改善すべく、 ChatGPT のしくみを利用して、もう少し詳細に調査できるようにしたいと考え、開発に取り組みました。議員さんの中には自身を PR する場がない方も多いと思います。しかし、政治家は我々のために頑張っているのですから、その努力を誰かが見える化しなければならない。そうすることで、一般の人も政治家が何をしているのか具体的に調査し、確認することもうができるようになることが大切だと思います。うちは儲からないかもしれないけれども、みんなが幸せになるなら。 

**小泉** :それはすばらしい志ですね。今後はどういう方向への進化を考えていますか?

**高木** :県や市議会の情報が欲しいですよね。地方議会の情報は勝手にスクレイピングするわけにもいかないので。

#### **シビックテックと生成系AIの関係** 

**小泉** :シビックテックも生成系 AI を活用して変わるでしょうか? **高木** :今までシビックテックというと、正直なところ 1 日イベントなどで終わってしまっていたものが多かったんですね。完璧なプログラムを組むことができないということや、メンバーによるアイデアの良さなどで善しあしが決まるような状況がありました。しかし、生成 AI である ChatGPT を使うことによって、そのレベルが大きく上がると思います。だから、シビックテックという観点で言うと、 1 日のイベントで実現できることの質が大幅に向上しますね。最近のアイデアソンでは 2 時間ぐらい打ち合わせをしたあと、 10 分くらいでプレゼンテーション資料を作ることができました。要点をまとめるのが一瞬で終わるので、方向性を出すのがすごく簡単になります。

- **小泉** : ChatGPT というのはインターフェースの革命だと思うんですよね、自然言語で。 

- **高木** :そうそう、自然言語で、コンピューターと自然に話すことができる点が一番の革命だと思います。 ChatGPT を使うことによって高齢者でも自然言語で使えるようなシステムを考えるといった、これまでにはまったくなかったことが可能になりました。 

- だから話をして、その話をプログラムが勝手に解釈して実行す 

- る。これができれば、ハッカソンでやったことが非常に簡単に商用サービスレベルのソリューションになってくると思います。 

#### **これからのAI社会について** 

- **高木** :日本が AI に全振りする方向なのが、個人的にはうれしいというか、やはりそれは重要ですよね。 

- **小泉** :本当に。高木さんの話を聞くと、日本には逆転のチャンスがあると感じます。そのためには、たとえば肖像権などの守るべきものはしっかり守るべきですが、 AI を活用していくための地盤づくりは必要だと思います。そして、オープンデータについても、文脈のあるオープンデータ、たとえば議事録などはたくさんあるはずですから、それを公開してほしいですよね。過去のデータも意味があると思います。 

- **高木** : 30 年前はワープロが主流で、誰もがワープロがなくなるわけがないと思っていました。しかし、結局、ワープロはなくなってしまったんですよね。一方ラジオは、放送の主流がテレビへ移行した今もまだ残ってるじゃないですか。だから、それと同じように、 AI でやるところとそうじゃないところとが共存することになこんとん 

- るでしょう。今、混沌としていると感じていますけどね。 

**小泉** :ネットでもバズってましたね。

- 混沌といえば、たとえばプロンプトエンジニアリングというも 

- のが現在注目されているのは過渡期だからですよね。 AI による画像生成のプロンプトもそうなのですが、バッドノウハウ注3 ~~に~~ なるものだと思っています。 

- **高木** :そうなんですよ! 混沌としてるからこそ、結構変わってくると思うんですよ。 

#### **最後に** 

- **小泉** : Software Design の読者へメッセージがあればどうぞ。 

- **高木** :今回は、若い人向けに。今回の国会議事録検索( The Diet Search )プラグインを見て、こんなものもあればいいのにって感じたものを若い人が新しく何かまったく別なところから作ってくれるといいと思います。プラグインを見ると、まだまだ日本のものは少ししかないんですよ。 

- **小泉** :私も何か作ってみたいと思います! 

- **高木** :もっと良いものを頑張って作りたいと思います。 

- **小泉** :ほかに何かアピールしたいことがあればお願いします。 

- **高木** :動画のセミナーを開催します注4 ~~。~~ これは、新たな挑戦の一環として行うものです。 

- **小泉** :高木さん、本日はありがとうございました。 

**高木** :ありがとうございました。

- 注1) ここで行っているChatGPTとのやりとりの全文は、次のURLから確認できます。 <u>https://chat.openai.com/share/617e93fd-b18a-456f-ade1-8326f725b02a? fbclid=IwAR19BNjl4178Gdxz-REuKqw6OlmVg4E7a2o2MJYAsdIIuXmt_Z3Py51IM0</u> 

   - (本文に戻る) 

- 注3) その場の問題解決にはなっても、ほかでの応用があまり効かなかったり、今後も使える保証がなかったりする手段。 

   - (本文に戻る) 

- 注4) 次のURLで公開予定です。 

   - <u>https://www.youtube.com/playlist?list=PLV1kWaF2F78iaPUKuq-J9lGN5sAVxep0e</u> (本文に戻る) 

#### <mark>SD N</mark> EWS <mark>& P</mark> RODUCTS 

##### **「U-22プログラミング・コンテスト2023」応募受付開始** 

「U-22プログラミングコンテスト2023」の応募受付が開始された。U-22プログラミング・コンテストは、22歳以下(2001年4月2日生まれ以降)の若者により開発された「プログラミング」技術を活用したアプリケーションやサービスを審査するコンテスト。今年度で通算44回目の開催となる。募集するジャンルや言語は不問で、プログラミング技術を用いて実行可能にした、ほかのコンテストを含め未応募のオリジナル作品が審査対象。最終審査会は昭和大学上條記念館にて開催予定で、オンライン配信も予定されている。配信 URLは後日公式Webサイトにて発表されるとのこと。また、昨年に引き続き、翌年のU-22プログラミング・コンテストのキービジュアルを募集する「第2回 U-22キービジュアルコンテスト」が併催されており、こちらも募集期間は7 月3日から8月31日までとなる。

###### **CONTACT** 

U-22 **プログラミング・コンテスト実行委員会**

<u>https://u22procon.com/</u>

###### **マクニカ、** 

##### **AIを活用して既知の脆弱性を悪用する攻撃を迅速に発見する「Vectra Match」を日本国内で販売開始** 

(株)マクニカは、Vectra AI社のAIを活用して既知の脆弱性を悪用する攻撃を迅速に発見する「Vectra Match(ベクトラマッチ)」を日本国内で6月29日に販売開始した。

Vectra Matchは、独自のセキュリティ特化型AIを活用したシグネチャ検知(脅威の特徴やパターンを基にした検知)によって、エクスプロイトと呼ばれる既知の脆弱性を悪用する攻撃を迅速に発見する。既知の脆弱性を狙って外部から企業ネットワークの境界突破を試みる攻撃と、ネットワーク内で横展開を試みる攻撃双方を自動検知することで、効率的な対策が可能となる。また、すでに取り扱っているVectra NDRを組み合わせることで、シグネチャ検知を回避するような未知の攻撃への対応も可能となる。優先して対処すべきアラートを絞り込むことができ、インシデント発生時の調査やレスポンスの質が向上するだろう。さらに、Vectra MatchはVectra NDRと同一のアプライアンスで機能提供が可能なため、「複数アプライアンス管理による保守コスト」「製品トレーニング、運用ドキュメント整備などの教育コスト」「アラート運用、サポート問い合わせ対応などの運用コスト」などが削減できるという。

**CONTACT**

**(株)マクニカ**

<u>https://www.macnica.co.jp</u>

###### **NTTと早稲田大学、** 

##### **プログラム中の文字列抽出機能(正規表現)を自動修正する技術を世界に先駆けて実現** 

日本電信電話(株)(NTT)と早稲田大学は6月16日、文字列抽出に使われる正規表現の誤りを自動修正する技術を世界で初めて実現したと発表した。大半のプログラムで文字列抽出のために組み込まれている正規表現は、その動作の難解さから誤った記述が残され、システムの誤作動や情報漏えいの原因となっていた。本技術により、専門知識を持たない開発者でも正規表現の誤りを自動修正できるため、安全なサービスの実現が期待されている。本技術のポイントは次の3つだ。とくに1に関しては、ECMAScript 2023に完全準拠した正規表現エンジンの振る舞いを厳密に定義している。

1 正規表現を用いた文字列抽出を行うプログラムの振る舞いを厳密に定義

- 2 修正問題を形式化 

- 3 修正問題を解くアルゴリズムを考案本技術の詳細は、6月17〜22日開催のプログラミング言語分野の最難関国際会議 PLDI2023(Programming Language Design and Implementation)で発表された (https://dl.acm.org/doi/10.1145/3591287)。 

**CONTACT**

**日本電信電話(株)**

<u>https://group.ntt/jp/</u>

###### **MontaVista、** 

##### **「MVShield」によるCentOS 7サポートを2029年6月まで延⻑と発表** 

MontaVista社は6月27日、執筆時点で2024年6月30日に予定されているCentOS 7.9のサポート終了日以降も、CentOSの継続的なセキュリティアップデートと技術サポートを提供すると発表した。サポートは同社のサービス「MVShield」を通じて行う。 MVShieldは、CentOSを始めとするさまざまなLinux製品のサポートを提供するサービス。同サービスはユーザーによるCentOSの⻑期運用を支援するため、最短で2029年6月まで、CVEとバグの修正を適用したパッチを四半期ごとにリリースするほか、SLAベースの技術サポートを提供する。

CentOSのサポート終了後も、同社固有のCentOS 7.9ブランチをメンテナンスし続けるそうだ。新しいバージョンや他のディストリビューションからのバックポートが必要な場合も、同社の専門知識とリソースによりカーネルやパッケージにパッチを適用するとのこと。

REHL互換のLinuxディストリビューションとして多くの企業で利用されていたCentOSだが、開発コミュニティの方針転換によりサポートが終了することになった。CentOSを利用していた企業は対応に迫られている。

**CONTACT**

**モンタビスタソフトウエアジャパン(株)**

<u>https://mvista.com/</u>

###### **アクロニス、** 

**サイバー脅威レポート2023年上半期アップデート版を公開**

アクロニス社は7月6日、サイバー脅威レポートの上半期アップデート版「Mid-Year Cyberthreats Report, From Innovation to Risk: Managing the Implications of AI-driven Cyberattacks」を公開した。同レポートによると、2023年度第1四半期におけるランサムウェア事案の公表件数は809件で、ランサムウェアが一層主要なリスクとなっている。また、フィッシングやデータ盗難ソフトによる認証情報の窃取、機密情報への不正アクセスなどの被害件数も増大しており、とくに医療機関や軍事機密、大学組織が攻撃対象となったようだ。同社は、被害が増大している組織には、次のいずれかの状況があるとした。

- ・ゼロデイ脆弱性を検出できるようなセキュリティソリューションを導入していない 

- ・ソフトウェアアップデート未対応により、セキュリティ修正プログラムを適用していない 

- ・ Linuxサーバを十分に保護していない 

- ・適切な手順でデータをバックアップしていないこうした人為的なミスや設定不備によるリスクを防ぐことが組織にとっての急務になりそうだ。 

###### **CONTACT** 

**アクロニス・ジャパン(株)**

<u>https://www.acronis.com/ja-jp/</u>

##### **Google Cloud、 Cloud SQL Enterprise Plusエディションを発表** 

Googleは米国時間の7月12日、MySQLとPostgreSQL向けのCloud SQL Enterprise Plusエディションを発表した。Cloud SQLはMySQL、PostgreSQL、SQL Serverのワークロードを実行するためのGoogle Cloudのエンタープライズ対応フルマネージドデータベースサービス。今回発表された新エディションでは、次の3つの改良が加えられている。

- ・読み取りと書き込みの両方のパフォーマンスを大幅に高速化。MySQLでは現在のCloud SQLと比較して、読み取りスループットが最大3倍、書き込みレイテンシが最大2倍向上 

- ・計画的なメンテナンス作業におけるダウンタイムがほぼゼロに。メンテナンスも含めて 99.99%の可用性SLAを提供 

- ・データ保護機能を強化し、35日間のログ保持を実現 

Cloud SQL Enterprise Plusエディションは7月17日にはCloudコンソールで利用可能になる予定。SQL Server向けの同エディションも後日リリースが計画されている。

###### **CONTACT** 

**グーグル・クラウド・ジャパン合同会社** <u>https://cloud.google.com/</u>

### Reader's Link 

TwitterがXになりましたね。サービス名ってこんなに気軽に変わって良いものだったのかと驚きました。Webページ、書籍、さまざまなところで使われている「Twitter」をすべて「X」に変えるために、いったいどれだけの労働が発生するのでしょうか......。ちなみに、本誌の著者紹介に使っている表記は、いったん様子見ということで「Twitter」のままにしています。来月にはXからまたTwitterに戻っていたりするかもしれませんね。

#### **<u>7月号読者プレゼント当選者</u>** 

- 1 **ワイヤレスパーソナルイヤースピーカー「** nwm MBE001 **」** ken1flan様(東京都) 

- 2 USB **高速充電器「** OMNIA II 30W **」** 

   - 山⻄宏宜様(東京都) 

- 3 IPv6 **検定試験ノベルティマグカップ** 

   - 米山英謙様(愛知県) 

- 4 **『入門モダン** Linux **』** 

   - 小山勉様(東京都)、増田知賢様(東京都) 

- 5 **『ミュラー** Python **で学ぶ深層学習』** 鈴木友哉様(東京都)、ほまれ様(千葉県) 

- 6 **『実践** Svelte **入門』** 

   - 鈴木颯様(千葉県)、麻生太一様(東京都) 

- 7 **『[速習]ベイズの定理』** KenKen様(山形県)、福田和真様(神奈川県) 

※ 当選しているにもかかわらず、本誌発売日から1ヵ月経ってもプレゼントが届かない場合は、編集部(sd@gihyo.co.jp)までご連絡ください。アカウント情報の不備(本名が登録されていない、プレゼント応募後に住所が変更されたなど)によってお届けできないことがあります。2ヵ月以上連絡が取れない場合は、再抽選させていただくことがあります。

#### **2023年7月号についてたくさんのお便りありがとうございました!** 

**第1特集**

**gRPCで始めるWeb API開発**

**こちらを目的に購入しました。**

uparrow **さん/山形県**

**読みごたえがありました、プロジェクトで採用してみようと思いました。おさむさん/東京都**

**gRPCについてなんとなく知れてよかった。芦田悠さん/茨城県**

**参考になる。中村昭博さん/石川県**

**gRPCは気になっていたけどノータッチだった。今回、こうやってしっかりとまとまった形で学習できてよかった。** romeosheart **さん/⻑崎県**

**背景やほかとの比較が良かった。網羅性もあって知りたい内容が知れた。** tonono **さん/東京都**

**あまり目立たない縁の下の力持ちという印象が強いRPC、でも少しでも効率よくその性能を発揮させるための工夫が多々なされているんですね。南雲浩二さん/埼玉県**

**gRPCについてとても理解できた。これを読みたくて購入した。** boco **さん/埼玉県**

**Googleの開発したRPCフレームワークgRPCについて、背景から実装までガッツリ解説しました。その性能と利便性を理解してもらえたでしょうか。**

**第2特集**

**はじめようSvelte**

**ひ本特集に惹かれ購入。ReactやVueとの違いや基本構文を学ぶことができました。実際に Jamstackサイトを構築し、アプリケーションの作成方法を知ることができとても満足しています!**

mojako **さん/神奈川県**

**JavaScriptのフレームワークはいろいろあって、何を学ぶのが戦略的に正しいか悩む。カズさん/千葉県**

**Svelteについてわかりやすくまとまっていた。匿名希望さん/東京都**

**TauriのQuick Startの選択肢にSvelteKitがあり気になっていました。「All code is buggy」から始まる「Write less code」はなかなかおもしろいコンセプトだと思います。** yak_ex **さん/愛知県**

**参入障壁は下がりそうですが、原点となるReact、Vueの知識がないと本質をとらえた使い方ができないことがありそうだと感じました。** yasio **さん/静岡県**

**JavaScriptフレームワークはいろいろあるので、いざ使おうというときに迷ってしまいそうです。そんなときの参考にしたいと思います。** maplehills2566 **さん/埼玉県**

**また新しいJavaScriptフレームワークか......なんて思うなかれ。使ってみれば、 Svelteの個性と使い勝手がわかるはず。**

**特別企画**

**GoFデザインパターン歴史探訪**

**昔必死で勉強しました。**

ea6 **さん/大阪府**

**デザインパターンについて現代的観点から解説してもらえ、今後の勉強方針の参考になりました。** T. K **さん/千葉県**

**最近触れられない話題で原点回帰できました。**

###### **とーふやさん/神奈川県** 

###### **昔学んだ内容の再確認ができました。シトロンさん/東京都** 

**約30年前の書籍が初出のGoFデザインパターンですが、現代に活かせる部分を探していくのもまた一興ではないでしょうか。**

###### **短期連載** 

###### **ソフトウェアのセキュリティ&品質管理の新要件SBOM[最終回]** 

**必要だけど、まだ整ってないということがよくわかった。記載されていたツールを調べるきっかけになったのでありがとうございます。エースさん/神奈川県**

**連載は今月号で終わりですが、今後もSBOMについて取り上げていただきたいです。永作肇さん/東京都**

**興味深い。**

IT **ジャナリスト** 13 **さん/愛知県**

**学習になった。プロジェクトにも取り込みたい。石毛完知さん/千葉県**

**まだまだ議論すべきことも多いSBOMの課題などを解説しました。これからの展開にも注目ですね。**

###### **短期連載** 

###### **Denoで始めるサーバサイドTypeScript開発[最終回]** 

**ちょうど気になっていたFFIやWasmの記事でした。Rustの選択もモダン。OCRのサンプルもおもしろかったです。**

tanayasu **さん/兵庫県**

**最終回!! これまでありがとうございました! Denoを初めて触り、とても楽しかったです。ゴリラさんの今後の記事に期待しています! しゅうまさん/京都府**

**非常に文献も少ないので良い企画だと思う。キャンセル・ジュンさん/⻑崎県**

**WebフレームワークのFreshなど、周辺サービスも充実してきているDeno。エコシステムとしての進化も気になるところです。**

**連載**

**新時代の分散型SNS Nostr[1]**

**ちまた SNSの代表格であるTwitterの動向が不安定なこともあり、巷ではほかのSNSへの乗り換えなどが話題になっていますが、そもそもSNSを運営している裏側の技術というものをあまり意識したことがなかったので、本連載をきっかけにサービスの裏側という視点で物事を考えさせられるキッカケになりました。次号以降も楽しみにしています。** N/M **さん/兵庫県**

**新進気鋭のSNSプロトコルであるNostrについて解説する新連載。尖った部分も多いですが、エンジニア視点では興味深いポイントも多々あります。**

**連載**

###### **Stable Diffusionで学ぶ画像生成AIのしくみ[2]** 

**本誌を参考に画像生成AIを試してみたが、ソースコードの少なさに驚いた。そのしくみについては、本誌で大枠はイメージができたが複雑であり、継続して学んでいこうと思った。**

AA **さん/栃木県**

**使うのは驚くほど簡単ですが、そのしくみは驚くほど奥深いです。**

**連載**

**メールセキュリティ対策の現場[3]**

**Yahoo!メールのような国内でトップシェアを誇っているメールサービスのセキュリティ対策を公開してもらえるのはありがたいですね。他社のセキュリティ対策も気になります。とっぽさん/愛知県**

**メールセキュリティの最前線からしか得られない知見が学べます。**

**アンケートはhttps://gihyo.jp/magazine/support/questionnaireからご回答ください。コメントを掲載させていただいた方には、1,000円分のQUOカードをお送りします。み**

**なさまのご意見・ご感想をお待ちしております!**

##### SD Staff Room 

● 「やる気は自然に起きるものではない」という説を見かけました。やる気は行動を起こすことで初めて湧いてきて徐々にノッてくるものらしいです(諸説あり)。以来、やる気が出ないときは「まず5分だけやろう」とつぶやきつつ物事に着手しています。着手さえすれば継続できるので効果ありそうです。(よし)

うし ● 「うなぎの旬は秋から冬」ということは重々承知していますが、「土用の丑の日」というイベントに乗っかるのを毎年楽しみにしています。昨年、一昨年は諸事情によりうなぎにありつくことができなかったので、今年の思いはひとしおです。執筆時点でのこり4日。何事もなく当日を迎えられればよいのですが。(か)

● 今年の夏は暑すぎる。昔から東京都区内が生活圏ですが、こんなに連日危険な暑さが続くのは初めてかもしれません。あまり半袖は好みませんが今年は着っぱなし。日差しも強くサングラスが手放せず、飲み物の消費量も倍に。5ヵ月の子供のためにも室内の冷房はほぼ24時間稼働です。今夏の電気代が恐ろしい......。(に)

- 私用PCの買い替えを検討しています。今MacBook Proを使っているのですが、そのまま 

- 最新モデルに買い替えようとしたら......高い! こんなところでも円安の影響をひしひしと感じます。そういうわけで、今回はお財布事情的に結局Airを買うことになりそうです。グッバイ、Pro......。(や) 

● 『ぼっち・ざ・ろっく』の影響で最近小さなライブハウスにもちょくちょく足を運ぶようになりました。知らないバンドのライブにフラっと入る勇気はないのですが、頑張って探すと意外と有名どころのライブを見つけられて楽しいです。狭いとはすなわち音がデカいということ。ライブ後はしばらく耳鳴りが止まりません。(く)

- 我が家の小さなベランダガーデンの主は鉢植えのブルーベリーの木です。だいぶお年を 

- 召して古木のような風合いになっていますが、毎年たくさんの実をつけてくれるので夏の間は毎朝の水やりのついでに⻘紫色に完熟した果実を見つけては少しずつ収穫するのを楽しんでいます。たまってきた実で何を作ろうかな。(ま) 

本誌に記載の商品名などは、一般に各メーカーの登録商標または商標です。

### **PRESENT 読者プレゼントのお知らせ** 

『Software Design』をご愛読いただきありがとうございます。本誌サイト <u>https://sd.gihyo.jp/の「読者アンケート」からアクセスし、アンケートにご協力ください</u> (アンケートに回答するにはgihyo.jpへのお名前と住所のアカウント登録が必要となります)。ご希望のプレゼント番号を入力いただいた方の中から抽選でプレゼントを差し上げます。締め切りは2023 **年** 9 **月** 14 **日** です。プレゼントの発送まで日数がかかる場合がありますが、ご了承ください。ご記入いただいた個人情報は、プレゼントの抽選および発送以外の目的で使用することはありません。アンケートの回答は誌面作りのために集計いたしますが、それ以外の目的ではいっさい使用いたしません。入力いただいた個人情報は、作業終了後に責任を持って破棄いたします。

###### **有線マウス「REALFORCE RM1 MOUSE」** 

REALFORCEブランドの有線マウスです。REALFORCEキーボードと同様の静電容量無接点方式スイッチ(東プレスイッチ)をマウスの左右ボタンに採用。静音でソフトなクリック感が特⻑です。また、専用ソフトウェア(Windows、Mac専用)によって、ボタン割り当てやマクロ設定などをカスタマイズできます。インターフェースは有線(USB 2.0 Type-A)。ダークグレーとスーパーホワイトの2色を1名ずつにプレゼントします。 **提供元東プレ**

**<u>https://www.topre.co.jp/</u>**

**2名**

- **「Anker 737 MagGo Charger(3-in-1 Station)」** 

MagSafe対応のiPhone、Apple Watch、AirPodsに対応したワイヤレス充電ステーションです。上記の3種類の製品を同時にワイヤレス充電できます。付属のUSB PD対応USB急速充電器(30W)に、USB Type-Cケーブルで接続して使用します。 **提供元アンカー・ジャパン**

**<u>https://www.ankerjapan.com/</u>**

**1名**

**ノートPCクーラー「OWL-NTCDUAL106」**

デュアルファンを搭載したノートPCクーラーです。使用中のノートPCのUSB Type-Aポートに接続するだけで利用できます。スタンドの角度は5段階で調整可能です。15.6インチまでのノートPCに対応。

**提供元オウルテック**

**<u>http://www.owltech.co.jp/</u>**

**1名**

**JavaScript Primer 改訂2版**

**azu、Suguru Inatomi 著**

JavaScriptを一から学ぶ人に向けた入門書です(ECMAScript 2022に対応)。基本文法の解説だけでなく、変化の激しいJavaScriptを使うにあたって重要な、問題を自分で解決する技術にも触れています。

**提供元アスキードワンゴ**

**<u>https://asciidwango.jp/</u>**

**2名**

**システム設計のセオリー II**

- **赤俊哉、川又眞綱、中⻄ 良明著** 

現代の情報システムの設計で重要なセオリーを紹介する書籍です。数多くの開発現場を経験した筆者らの知見を基に体系化された、クラウドベース開発を前提としたシステム設計の手順を解説します。

**提供元リックテレコム**

**<u>https://www.ric.co.jp/</u>**

**2名**

**ソフトウェア開発にChatGPTは使えるのか? 小野哲著**

ChatGPTは開発に活用できるのか、という疑問に答える1冊です。プログラミングやリファクタリング、ドキュメント生成など、開発の各工程でChatGPTを効率的に利用する方法を紹介しています。

**提供元技術評論社**

**<u>https://gihyo.jp/book/</u>**

**2名**

###### **基礎から学ぶコンテナセキュリティ** 

**森田浩平著**

Dockerを題材にコンテナのセキュリティ対策を解説する書籍です。堅牢なコンテナイメージの作り方やコンテナランタイムのセキュアな運用方法、セキュアなコンテナ環境の構築方法を説明します。 **提供元技術評論社** **<u>https://gihyo.jp/book/</u>**

**2名**

紙版

Fujisan:https://www.fujisan.co.jp/sd e-hon:https://www.e-hon.ne.jp デジタル版 Gihyo Digital Publishing:https://gihyo.jp/dp

Fujisan:https://www.fujisan.co.jp/sd Amazon:https://www.amazon.co.jp/

##### **電子版書籍について** 

本書は紙の書籍『Software Design 2023年9月号』(ISBN0582709)を電子書籍化したものです。紙書籍とは一部レイアウトやデザインが異なります。本書の更新履歴や補足情報は技術評論社ウェブ <u>サイトをご参照ください。</u>

本書の一部または全部を著作権法の定める範囲を超え、無断で複写、複製、転載、テープ化、ファイルに落とすことを禁じます。造本には細心の注意を払っておりますが、万一、ページの乱れやページの抜け等がございましたら、小社デジタル事業部までお知らせください。

##### **電子版奥付** 

**書名** Software Design 2023年9月号 **電子版発行日** 2023年8月18日初版第1刷発行 **発行人** 片岡巌 **編集人** 吉岡高弘 **編集** 山﨑香 ⻄原康智山本紘彰栗木琢実 **編集アシスタント** 根岸真理子 **広告** 立山英樹 **発行所** 株式会社技術評論社東京都新宿区市谷左内町21-13 **電話** 03-3513-6150 販売促進部 03-3513-6180 デジタル事業部 **電子版製本** 株式会社グリープ <u>https://greep.jp/</u> ©2023 技術評論社 ISBN978-4-297-13454-9
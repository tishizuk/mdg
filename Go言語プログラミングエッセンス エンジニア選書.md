## **ご利用の前に必ずお読みください** 

本書は紙書籍『Go言語プログラミングエッセンス』 (ISBN978-4-297-13419-8)を元に製作した電子書籍です。紙書籍とはデザインやレイアウトが異なり、ご覧になる端末により表示が異なる場合があります。表示設定は端末の標準設定を推奨します。配信後に補足訂正等でデータの再配布を行う場合があります。更新方法は購入先の電子書店のヘルプ等をご確認ください。本書は情報提供のみを目的としており、掲載内容の運用結果について技術評論社および著者は一切の責任を負いません。掲載内容は特に断りのない限り執筆時点より以前の情報のため、変更される場合があります。特に、ソフトウェアはバージョンアップされる場合があり、本書での説明とは機能内容や画面図などが異なってしまうこともありえます。以上をあらかじめご承諾の上、ご利用をお願いします。本文中に記載されている製品の名称は、すべて関係各組織、各社の商標または登録商標です。 

- **●本書をお読みになる前に** 

- ・本書に記載された内容は、情報の提供のみを目的としています。したがって、本書を用いた運用は、必ずお客様自身の責任と判断によって行ってください。これらの情報の運用の結果について、技術評論社および著者はいかなる責任も負いません。 

- ・本書記載の情報は、2023年2月現在のものを掲載していますので、ご利用時には、変更されている場合もあります。 

- ・本書で紹介するソフトウェア/Webサービスはバージョンアップされる場合があり、本書での説明とは機能内容や画面図などが異なってしまうこともあり得ます。以上の注意事項をご承諾いただいたうえで、本書をご利用願います。これらの注意事項をお読みいただかずに、お問い合わせいただいても、技術評論社および著者は対処しかねます。あらかじめ、ご承知おきください。 

#### **●商標、登録商標について** 

本書に掲載した社名や製品名などは一般に各メーカーの商標または登録商標である場合があります。会社名、製品名などについて、本文中では、TM、C、Rマークなどは表示しておりません。 

# **はじめに** 

まず、本書をお手に取っていただいた方にお礼を申し上げます。本書はプログラミング言語 Go に興味のある方、 Go を触ってみて Go をもっと知りたいと思う方に、筆者が知る Go を、あらゆる方向からお伝えする 1 冊になっています。本書を読んで Go をもっと好きになっていただければと願うばかりです。筆者がプログラミング言語 Go を知ったのは、 Go 1.0 がリリースされるよりも前、オープンソースとして公開された直後でした。ソースコードをコンパイルするプログラミング言語でありながら、 Windows でも UNIX でも macOS でもほぼ同じソースを使うことができ、また高速に動作する、そんな特徴に心踊らされました。さらには、 Windows で起き得る文字コードの問題やファイルシステムの問題といった Windows 固有の問題にも、オフィシャルの開発者たちが積極的に対応し、限りなく他のプラットフォームと同じソースコードを使えるようにと努力してくれていることに、非常に感心しました。 

Go に興味が湧いた筆者は、標準ライブラリのソースコードを読み、多くのパッチを Go 本体に送りました。ベル研究所で UNIX を開発していた Rob Pike 氏や Russ Cox 氏、 gcc コンパイラのリンカーを開発していた Ian Lance Taylor 氏、 memcached の作者である Brad Fitzpatrick 氏といった、筆者にとってヒーローとも言えるすばらしい開発者のみなさんに、自分が書いたパッチをコードレビューしていただいた体験は、筆者の開発者体験の中でも大きな思い出になっています。筆者が Go にパッチを送り始めたあのころから比べると、 Go はとても大きくなりました。機能だけでなくコミュニティも大きくなりました。日本でも多くの企業が Go を採用し、多くのシステムが Go で書かれています。筆者も業務で多くのシステムを Go で書いてきました。なぜそこまで Go は、筆者やみなさんを魅了したのでしょうか。それは Go を採用した人たちそれぞれに別の答えがあると思います。もしかすると、本書を手にとったあなたは、まだ Go にそれほど興味を持っていないかもしれません。そのようなみなさんに、少しでも Go の良さを伝えることができれば幸いです。 Go がどのようにおもしろいのか、 Go を使って何ができるのか、そしてさまざまな開発シーンでどのように Go が活きてくるのか、本書で解説してみたいと思います。 Go は C 言語を意識して開発されたプログラミング言語であるため、本書にはいくつか C 言語との対比や、 C 言語に関する知識が前提となっている記述がありますが、なるべく C 言語をご存じない方にもわかりやすい解説を心掛けたつもりです。なお本書で扱う内容は、本書執筆時点( 2023 年 2 月)での情報です。今後変更されることもあります。紹介したツールやライブラリなども非推奨になっている場合があるかもしれません。また、紹介するベンチマーク結果などもライブラリの改善により執筆後に結果が変わってくる可能性があります。あらかじめご了承ください。 

2023 年 2 月 Google Developers Expert Go, mattn. 

# **謝辞** 

本書を執筆するにあたり、編集を担当して頂いた技術評論社さまに深く感謝いたします。また本書のレビューを担当して頂いた下記のみなさんに深く感謝いたします。山口能迪さん武内覚さん KoRoN さん 

**著者について** 

## mattn **(まっつん)** 

さまざまな OSS にコントリビュートを行う IT エンジニア。月刊誌「 Software Design 」にて連載記事「 Vim の細道」のほか、 Vim 、 Go 言語に関する記事を多数執筆。共著に『みんなの Go 』。 Google Developers Expert ( Go )。 

Twitter <u>: @mattn_jp</u> GitHub : <u>https://github.com/mattn</u> Blog <u>: https://mattn.kaoriya.net/</u> 

### **目次** 

### **<u>はじめに謝辞著者について</u>** 

## **<u>第1 章プログラミング言語Goとは</u>** 

- **<u>1.1 Goの簡単な歴史</u>** 

- **<u>1.2 Goの立ち位置</u>** 

- **<u>1.3 Goが利用される場面</u>** <u>標準でUTF-8をサポートマルチプラットフォーム並行処理の扱いやすさストリーム指向シングルバイナリ</u> 

- **<u>1.4 教育用途としてのGo</u>** 

- **<u>1.5 なぜGoが使われるのか</u>** <u>コンパイルの速さレビューのしやすさ周辺ツールの充実パッケージ公開の簡単さ libc非依存共同開発でのスキル差</u> 

- **<u>1.6 まとめ</u>** 

**<u>第2 章開発環境の準備 2.1 Goコンパイラのインストール</u>** <u>[COLUMN] GOROOT? GOPATH?</u> 

**<u>2.2 開発環境のセットアップ</u>** <u>Visual Studio Code Vim Goland</u> **<u>2.3 まとめ</u>** 

**<u>第3 章基本的な文法 3.1 プロジェクトフォルダ 3.2 Go言語の特徴 3.3 静的な型 3.4 基本的な構文</u>** <u>変数宣言 const(定数宣言) iota(列挙) 命名規則関数呼び出し条件分岐ループ配列とスライス文字列 map type(型宣言) struct(構造体) メソッドポインタ interface{}またはany コンストラクタインターフェース defer</u> **<u>3.5 goroutine(ゴルーチン)</u>** 

<u>channel(チャネル) 制御構文(select)</u> 

**<u>3.6 Goモジュールとは</u>** <u>go.modの配置 replace go install</u> **<u>3.7 プロジェクトレイアウト 3.8 lint文化</u>** <u>go vet revive golangci-lint</u> **<u>3.9 go fmt 3.10 まとめ</u>** <u>[COLUMN] おまじない</u> 

**<u>第4 章基本テクニックとベストプラクティス 4.1 ビルトイン関数</u>** <u>printとprintln panicとrecover [COLUMN] Explicit is better than implicit, Simple is better than complex init関数</u> 

**<u>4.2 パッケージ</u>** <u>fmtパッケージ logパッケージ encoding/jsonパッケージ netパッケージ timeパッケージ pathパッケージとpath/filepathパッケージ osパッケージ</u> 

<u>ioパッケージ contextパッケージ</u> 

### **<u>4.3 build constraints</u>** <u>ファイル名による指定 +build(またはgo:build)コメントによる指定 [COLUMN] マルチプラットフォーム向けアプリケーションの開発テクニック</u> 

**<u>4.4 cgo</u>** <u>pkg-configを使って複雑なコンパイルオプションに対処</u> 

- **<u>4.5 go:embed 4.6 Functional Options Pattern 4.7 Builder Pattern</u>** 

- **<u>4.8 internalパッケージ</u>** 

- **<u>4.9 Embedded struct</u>** 

- **<u>4.10 go run 4.11 まとめ</u>** 

**<u>第5 章 Webアプリケーションの開発に必要な要素</u>** 

**<u>5.1 net/http</u>** <u>基本の使い方ルーティング [COLUMN] Method Value ファイルのサーブ</u> 

- **<u>5.2 html/template</u>** <u>struct range/end if/else/end</u> 

<u>with/else/end template.HTML FuncMap template.Parse define/template</u> **<u>5.3 net/smtp 5.4 まとめ</u>** 

**<u>第6 章速いプログラムのためのテクニック 6.1 並行と並列 6.2 goroutine 6.3 channel 6.4 非同期パターン</u>** <u>ジェネレータ合流処理先着処理タイムアウト処理停止処理スロットリング(throttling)</u> **<u>6.5 まとめ</u>** 

**<u>第7 章テストにおけるテクニック 7.1 テストの考え方 7.2 Goのテストの基本</u>** <u>Table Driven Tests t.Skip/t.Skipf setup/teardown t.Short</u> 



<!-- Start of picture text -->
t.Parallel<br>テストデータ<br>カバレッジ<br>7.3  便利なテクニック<br>テンポラリディレクトリ<br>環境変数<br>7.4 Fuzzingテスト<br>7.5  まとめ<br>第8 章ベンチマークにおけるテクニック<br>8.1  「推測するな、計測せよ」<br>8.2  ベンチマークの基本<br>8.3  ベンチマークの比較<br>改善の前と後の比較<br>異なる実装の比較<br>8.4  プロファイリング<br>8.5  まとめ<br>第9 章 GoによるCLIアプリケーション開発<br>9.1  システムのコンセプト<br>9.2  データベース登録プログラム<br>[COLUMN] プログラムを作る手順<br>ZIPファイルのURL一覧を得る<br>ZIPファイルを展開する<br>データベースに登録する<br>9.3  データベース照会プログラム<br>9.4  テストの実装<br>9.5 CLI開発に役立つライブラリ<br><!-- End of picture text -->

**<u>9.6 フラグライブラリ</u>** <u>flagパッケージ urfave/cli alecthomas/kingpin spf13/cobra</u> **<u>9.7 端末制御ライブラリ</u>** <u>olekukonko/tablewriter mattn/go-runewidth jroimartin/gocui</u> **<u>9.8 まとめ第10 章 GoによるWebアプリケーション開発 10.1 システムのコンセプト 10.2 TODOアプリケーション</u>** <u>Webサーバリクエストハンドラテンプレート処理静的ファイルのサーブ</u> **<u>10.3 リマインダメール送信 10.4 Goにおけるフレームワーク</u>** <u>Gin Echo Micro Beego フレームワークの選定について</u> **<u>10.5 まとめ</u>** 

#### **<u>第11 章 GitHubでの開発における勘所</u>** 

**<u>11.1 ライブラリの仕様 11.2 プロジェクトの作成 11.3 パッケージ名 11.4 実装 11.5 バージョニング</u>** <u>[COLUMN] GitHubのワンポイントTips</u> **<u>11.6 テスト 11.7 ドキュメント</u>** <u>go docの使い方 godocの使い方 https://pkg.go.dev/で閲覧する</u> **<u>11.8 ディレクトリ構成 11.9 自動テスト 11.10 自動リリース 11.11 まとめ</u>** 

**<u>第12 章データベースの扱い方 12.1 database/sql</u>** <u>サードパーティドライバ Query/QueryRow Exec Prepare Begin/Commit/Rollback</u> **<u>12.2 ent/ent</u>** <u>OpenAPI連携 entgen</u> **<u>12.3 ORMベンチマーク 12.4 まとめ</u>** 

#### **<u>第13 章 Goとクラウドサービス</u>** 

**<u>13.1 Goがなぜ使われるのか 13.2 Google Cloud 13.3 AWS Lambda</u>** 

**<u>13.4 Oracle Cloud</u>** <u>[COLUMN] バイナリサイズを小さくする</u> **<u>13.5 まとめ</u>** 

### **<u>索引</u>** 



# **1.1 Goの簡単な歴史** 

いろいろなソフトウェアが OSS として提供されるのが当然になってきたころ、 Go は登場しました。 Go はもともと、 Google 社内のプログラミング環境を改善する目的として開発されたプログラミング言語でした。 Go の開発には、 C 言語を設計した世界的なエンジニアで知られる Kenneth Lane Thompson 氏(通称 Ken Thompson )、 UNIX の開発者であり UTF-8 の発案者でもある Robert C Pike 氏 (通称 Rob Pike )、高速な JavaScript エンジン V8 の開発者でもある Robert Griesemer 氏など、著名な人物が携わっています。 C 言語の構文を意識しつつ、より簡素な文法でコーディングできるように設計されています。実際に Go は、 Google の本番システムの一部で使用されており、多くの有名な企業やオープンソースプロジェクトでも使用されています。 

Go を使っている企業の一部を紹介すると、 Google 、 PayPal 、 American Express 、 Dropbox 、 Cloudflare 、 Facebook 、 Microsoft 、 Netflix 、 Salesforce 、 Twitter 、 Uber などがあります。日本の企業であれば、クックパッド、サイバーエージェント、サイボウズ、ディー・エヌ・エー、はてな、 LINE 、メルカリ、楽天、さくらインターネット、 Yahoo! JAPAN 、 ZOZO など、紹介しきれないほどあります。このほか、みなさんも恐らく一度は名前を聞いたことがあるであろう有名な企業のシステムの一部で、 Go は使われていま ~~す~~ <u>注1.1</u> ~~。~~ 

Go は 2009 年 11 月 10 日に公開され、 2012 年 3 月 28 日にバージョン 1.0 がリリースされました。当初の Go が目指すところは以下のようなものでした。 

- **JavaやC++のような静的型付け言語で、巨大なシステム** 

- **でもスケールする** 



**動的型付け言語のように生産性が高く、可読性も高い IDE(統合開発環境)を必要としない(ただしサポートはする)** 





### **ネットワーク処理が多重実行できる** 



おもしろい話としては、 Go というプログラミング言語を新たに設計したモチベーションに関して、 Go を設計した 3 名 <u>注</u> 全員が「 C++ が好きではなかったから」と言ったそうで ~~す~~ <u>1.2</u> ~~。~~ 

- **注** 1.1 <u>https://github.com/golang/go/wiki/GoUsers</u> (本文に戻る) 

- **注** 1.2 <u>https://commandcenter.blogspot.com/2012/06/less-isexponentially-more.html</u> 

(本文に戻る) 

# **1.2 Goの立ち位置** 

現代でもたくさんのプログラミング言語が存在しますが、昔からプログラミング言語はいくつも存在していました。ハードウェアの進化とともに、その時々のハードウェアを利用するための手段として新たにプログラミング言語が生まれ、利用されてきました。しかしソフトウェア技術の進化に伴い、ユーザーから望まれるシステムは次第に高度となり、ソースコードも比例して複雑になってきました。既存のプログラミング言語は、それが開発された当時のハードウェア専用に設計されたものであり、新しく登場したハードウェアにはマッチしないこともしばしばありました。当然ながら、それらは最新の CPU 命令やマルチコアをサポートできない、または有効活用できず、そういったプログラミング言語を使うことで開発者の生産性が落ちてしまうこともありました。現代ではマルチコア CPU や巨大な主記憶装置、膨大な規模のディスクストレージを搭載するハードウェアも見られるようになってきました。個人がこのようなハードウェアを所有していることも珍しくありません。マルチコア CPU や、大規模記憶装置(メモリやディスク)の要件にマッチすべく誕生したのが Go なのです。 

Go を少しでも触ったことがある方であればわかっていただけると思いますが、 Go はほかのプログラミング言語に比べると、とても「愚直で頑固」です。横着をすることができず、慣用句のようにお決まりのフレーズを書くことを要求される場面もあります。また一部の文法も独特で、以下のようなルールが存在します。 

**シンボルを公開するには名称の先頭を大文字にするソースコードのインデントにはタブ文字を使うコードフォーマッタは標準のものを利用し、形式の変更はできない** 







**例外(exception)がなくerrorオブジェクトを常に呼び出し元に返す** 



こういった一見、縛りとも思える言語仕様が肌に合わない人も実際にいます。しかしながら、これらにはちゃんと理由があります。言語を簡素に保つことで Go を使う開発者の間でもブレが起きないよう、かつ単純な方法でプログラミングできることを優先したのが現在の仕様です。さて、 Go はどのような環境で利用されているのでしょうか。 **表** 1.1に執筆時点で Go がサポートしている OS と CPU アーキテクチャを示します。多くの OS とアーキテクチャをサポートしているのがわかります。 Go ではこれらの OS や CPU アーキテクチャ向けの実行形式のバイナリファイル(実行ファイル)を生成する際、専用の環境を用意することなくコンパイルすることができます。たとえば、 Linux 上で Windows 向けの実行ファイルを生成することもできます。 

Go に関する多くの情報はオフィシャルサイ ~~ト~~ <u>注1.3</u> ~~か~~ ら確認することができます。オフィシャルが開発しているコンパイラとしては、 GC ( Garbage Collector とは別もの)と呼ばれるオリジナルの Go コンパイラのほかに、 gccgo と呼ばれる gcc フロントエンドも存在します。こちらは gcc がサポートするすべてのプロセッサに対応しています。さらには、マイクロコントローラや WebAssembly といった <u>注1.4</u> ~~と~~ い組み込み用途の実行ファイルを生成できる TinyG ~~o~~ 

うコンパイラも存在します( GC 自身も別の実装方法で WebAssembly をサポートしています)。以降の解説では、とくに明記しない限りは GC 、つまりオフィシャルサイト(注 1.3URL )からダウンロードできる Go コンパイラのバージョン 1.20 を使って説明します。 

**注** 1.3 <u>https://go.dev/</u> (本文に戻る) 

**注** 1.4 <u>https://tinygo.org/</u> (本文に戻る) 

# **1.3 Goが利用される場面** 

さて、 Go を使うとどのようなことができるのでしょうか。ソフトウェア開発プラットフォーム GitHub に置かれている、 Go を使ったプロジェクトの多くを大別すると、おおよそ以下にまとめられます。 







**CLI(Command Line Interface)** 

**TUI(Text User Interface) Webアプリケーション** 

CLI は find や grep といったコマンドライン形式のプログラムを指します。一方 TUI は、キャラクタ(文字)を使用して GUI のようなユーザーインターフェースを提供し、ボタンやテキスト入力、グラフ描画などを行うプログラムを指します。 

Go はこれらのプログラムを作るのに向いているとよく言われています。いったいどのような理由でそう考えられているのでしょうか。 

## **標準でUTF-8をサポート** 

最初に挙げられるのが、処理系として UTF-8 を標準で扱えることでしょう。 

C 言語は各プラットフォームに提供された C コンパイラに依存し、サポートしているエンコーディングが異なります。 Windows に限ってはソースコード内に記述される日本語メッセージを Shift_JIS で書かなければいけないコンパイラもいまだに存在します。 C 言語は標準では内部エンコーディングは規定されておらず、マルチバイト文字を扱うのであれば wchar_t 型を用いてプログラミングする必要があります。 wchar_t 型を使って処理するためには画面表示やファイル I/O など、すべての入出力処理に関してワイド文字向けの関数 ( Windows API )を呼ばなければなりません。 

Go はデフォルトで UTF-8 をサポートしているため、ソースコードが UTF-8 で記述されていたとしても、 Windows でのコンソール出力やファイル I/O で問題が起きることはありません。なお、最近でこそ多くのプログラミング言語が UTF-8 を標準でサポートしていますが、 Go が登場した 2009 年ごろ、サポート対象としているすべての OS を UTF-8 で統一できていたプログラミング言語は希少でした。 

## **マルチプラットフォーム** 

次に挙げられるのがマルチプラットフォームを意識したビルドのしくみと標準ライブラリです。 

C 言語では、標準ライブラリとはいっても特定のプラットフォーム向けには一部の機能が提供されていないことも多く、開発者を悩ませる問題がいくつか存在します。 Go ではそういった各プラットフォームに提供されるべき機能が極力、差異のないように標準ライブラリとして実装されています。 Linux 、 Windows 、 macOS など異なる環境向けでも、開発者がプラットフォームの違いをあまり意識することなく、同じソースコードを同じ手順でビルドし、ファイル I/O やネットワーク通信といった処理を実装することができ、かつそれらが同じ動作になるように実装されています。 

## **並行処理の扱いやすさ** 

Go の代名詞と言えるのが並行処理でしょう。一般的なプログラミング言語の多くは、スレッドを使って並行処理を実現しています。スレッドは OS が提供している機能で、手軽にマルチコアを利用できる手法ですが、その代わりにうまくプログラミングしないと安全ではない処理が実行されてしまう問題があります。たとえば、 1 つの変数を複数のスレッドから更新するようなケースでは、どのような結果になるかは保証されません。また、スレッドを使うことでプログラミングが極端に複雑になります。たとえばスレッドの終了を待ち合せたり、別のスレッドに対してメッセージを送信したりといった処理は、 C 言語で実装を行うとかなりの量のソースコードを書かなければなりません。さらにスレッドは意外とリソースを消費してしまいます。数千数万といった単位でスレッドを起動するには高価なハードウェアを必要とします。 Go は goroutine と呼ばれる、スレッドよりも軽い並行処理のしくみを提供しており、湯水のように実行することができます。また、並行処理で問題となる待ち合わせやメッセージ送信といった処理も簡単に実装できる機能を提供しています。もちろん、複数の並行処理から 1 つの変数を更新するような処理は、 Go においても危険であることに変わりありませんが、排他処理やアトミックに値を更新するといった必要な機能は標準ライブラリとして十分に用意されています。並行処理は難しく、スレッドを使った場合もうまく処理を分散するように設計しないと、使われる CPU コアに偏りが生じてしまいます。 Go はマルチコアが活かされるようにランタイムが設計されており、 CPU コアを効率的に使い切れるようになっています。 

## **ストリーム指向** 

Go の標準ライブラリは「ストリーム指向」を強くアピールしています。 io.Reader や io.Writer というインターフェース (抽象型)を使ってファイルや通信接続を抽象的に扱えます。たとえば、ネットワーク通信で受信した JSON を扱うケースを想像してください。プログラミング言語の実装の中には、一度受信したデータをメモリに格納してから JSON をパースするような設計になっているものがありますが、 Go ではネットワーク通信から JSON パースまでが一連のストリームとして扱えるようになっています。各レイヤが io.Reader で処理することでメモリを無駄に使わないように設計されています。 

## **シングルバイナリ** 

最後に挙げるのはシングルバイナリを生成できることです。 Go の標準パッケージはサードパーティライブラリに依存していません注1.5 ~~。~~ 

他のプログラミング言語の処理系であれば、 SSL/TLS の実装をたとえば OpenSSL に委ねていることも多いでしょう。また、 HTTP 通信をライブラリに委ねていることもあるでしょう。 Go ではそれらすべてが Go だけで書かれています。そのため、 Go のソースコードをビルドして実行ファイルを生成すると、依存関係がほぼない単一のバイナリとして生成されます。昨今では実行環境と開発環境とを分けて開発するのが当然になってきましたが、開発環境でビルドした実行ファイル 1 つを実行環境にコピーするだけでデプロイが完了するのはとても簡単で便利ですし、間違いも起きにくいのです。実行するために必要なランタイムや依存ライブラリを別途インストールする必要もありません。 

◆ ◆ ◆ 

これらの理由から Go は、マルチコアを活用でき、メモリに優しく、パフォーマンスを要求されるさまざまなケースでも複雑にならない文法で高度な実装をすることができるプログラミング言語と言えます。 Go がバックグラウンドで動作するサーバ処理を記述するのに使われることが多いのはそれが理由です。また、マルチプラットフォーム向けの標準ライブラリや、クロスコンパイルに⻑けていることから、複数のプラットフォーム向けに提供するコマンドラインアプリケーションを開発するのにも向いています。実際に、 Amazon Web Services や Oracle Cloud といったクラウドベンダーが提供しているクラウド専用のライブラリやコマンドラインツールも Go で作られています。 

Docker や Kubernetes 、 Terraform といったコンテナ技術で使われる多くのソフトウェアも Go で開発されています。さらには静的サイトジェネレータの Hugo 、 ArchLinux のパッケージマネージャ yay など、最近ではクライアント向けのコマンドラインプログラムにも Go が使われています。前述のように、 TinyGo を使えばマイクロコントローラ向けの処理を Go で書くこともできます。 Raspberry Pi Pico のように OS がない環境であっても、慣れ親しんだ Go で開発ができるのです。 

**注** 1.5 ただしネットワーク通信を行う場合は環境変数 CGO_ENABLEDを0に設定する必要があります。 

(本文に戻る) 

# **1.4 教育用途としてのGo** 

ここまで Go のソースコードを一度も示さずに、 Go がどういったプログラミング言語なのかを説明してきました。高度なことができるという説明から、もしかすると難しい言語を想像される方もおられるかもしれません。しかし、実は Go はとてもシンプルな言語なのです。一般的にプログラミングは、多くのキーワードを覚える必要があり、たとえば Java であれば 50 個ほどのキーワードを予約語として扱うことになりますが、 Go は予約語が 25 個しかありません。 

1 つの手続きに対して複数の書き方ができない程度に簡素化されているため、誰が書いてもおおよそ同じようなソースコードになるように設計されています。そのため、 Go の学習は他のプログラミング言語に比べて習得が早く、それでいて実際に動作するプログラムを作れるようになるまでの期間が短いと言われています。海外の大学ではコンピュータサイエンスの講義の中で Go を使っているところもあります。さまざまな環境で同じ動作をするという点、習得が早いという点で、教育に向いていると筆者は考えています。 

# **1.5 なぜGoが使われるのか** 

昨今、ビジネスソリューションとして開発されるシステムでは、以下のようなプログラミング言語が多く使われています。 











**Java C/C++** 

- **JavaScript Perl/Python/Ruby/PHP C#** 

これらのいずれもが、実は 20 年以上も昔に開発されたプログラミング言語です。それ以降もたくさんプログラミング言語は誕生してきていますが、一般的な開発業務として使われるまでにはいたっていません。しかし 2009 年に誕生した Go 言語は、わずか 10 年近くで一気に有名となり、多くの企業で使われるようになりました。これは Google が開発したからという理由で流行ったからなのでしょうか? もちろん Google が開発したことも理由の 1 つだと思いますが、すべてがそうだとは筆者は考えていません。 Go に人気が集まったことにはちゃんと理由があるのです。以降ではその理由を説明していきたいと思います。 

## **コンパイルの速さ** 

プログラミング言語には大きく 2 種類あります。 1 つはコンパイル型言語、もう 1 つはインタプリタ言語です。インタプリタ言語が実行時にソースコードを解釈して実行するのに対して、コンパイル型言語は事前にソースコードをコンパイルし、実行ファイルを生成します。この実行ファイルと呼ぶものの中には、 Java のようにランタイムを必要とするものもあれば直接実行できるものもあります。 

Go はコンパイル型のプログラミング言語です。 C 言語や C++ 、 Java や Rust といったコンパイル型のプログラミング言語は、コンパイル時にソースコードを解析し、最適化を行います。この最適化を行うには、ソースコードに記述されたあらゆる情報を収集し、少しでも速くなるようにいろいろな手法を試す必要があります。そのためコンパイルには多くの CPU リソースやメモリが消費され、コンパイル時間を⻑くしてしまいます。 C++ ではコンパイル時間を利用してコーヒーを淹れられる、といったジョークもあるほどです。 

Go は簡素な文法で設計されていることもあり、コンパイルがとても速いのです。「必要なのは実行時の速度なのだからコンパイルには多少時間がかかっても良い」と思う方もいますが、開発者がソースコードを少し変更しただけなのに多くの時間をコンパイルに取られてしまうことは、モチベーションを低下させてしまいます。何度もソースコードを直すであろう初級者は、それだけコンパイルに時間を大量に取られ、多くの時間を無駄にしてしまいます。 Go のコンパイラはこの多くの無駄な時間を節約することができます。また、最近では CI/CD (継続的インテグレーション/継続的デリバリー)や、 GitOps と呼ばれる開発手法が普及しています。ソースコードを修正して Git で push すると、テスト、リリースビルド、デプロイといった一連の自動化が行われます。この一連のフェーズが短ければ短いほど、開発サイクルを加速することができるのです。 

## **レビューのしやすさ** 

言語仕様が小さいことで得られるメリットはたくさんあります。世の中にはプログラムを書いた本人だけが読めるソースコードも少なくありません。プログラミング言語には、独特なイディオム(慣用句)を持ち合わせているものも多く、そのイディオムを知らないプログラマーが他の人が書いたソースコードを読むのは非常に困難になります。 

Go はシンプルな言語仕様であるため、難しい慣用句はありません。ソースコードレビューで困ることも少ないのです。また、ソースコードのインデントにはハードタブが用いられ、レビューの前にはコードフォーマッタを実行するという文化まであります。「インデントは 2 タブだ、いや 4 タブだ、ハードタブだ」といった自転車置き場議論をなくしてくれるのです。 

Go のコードフォーマッタは gofmt と呼ばれていますが、この例外を許さないというルールのおかげで生産性が上がったという声が多くみられるようになりました。この gofmt の成果に感化され、現在では他のプログラミング言語でも gofmt のようなコードフォーマッタが同梱されるようになってきました。 

## **周辺ツールの充実** 

プログラミング言語が流行る要素として筆者が重要だと考えているものに、周辺ツールがあります。プログラミング言語のコンパイラだけでなく、静的解析ツール、コード生成ツール、デバッガ、 Language Server など、周辺ツールが充実していることが開発者に大きなメリットを与えます。これらのツールは単純にコマンドラインから実行されるだけでなく、有志の手によりテキストエディタなどから便利に実行できるよう拡張機能が提供されることもあり、その相乗効果によってプログラミング言語の質が高められます。 

Go には、 Go 言語を解析するためのパッケージが標準ライブラリとして同梱されています。これにより開発者が静的解析ツールを自作できるようになっています。また、異なるプラットフォーム向けの実行ファイルを生成する方法が確立されており、簡単にクロスビルドすることもできます。すでに多くの静的解析ツールやクロスビルドツールがサードパーティにより作られています。そして、われわれ開発者はこれらを利用して CI/CD を実施しています。 Go はこういったエコシステムが高度にできあがっているプログラミング言語なのです。 

## **パッケージ公開の簡単さ** 

Go のパッケージおよびモジュールは VCS (バージョン管理システム)で公開することができます。 Go モジュールを、たとえば以下のように GitHub のホスト名とユーザー名 (もしくは Organization 名)を含んだ形式で記述することで、とくに特別なツールを必要とすることなくパッケージ/ モジュールを公開することができます。 

<u>https://github.com/mattn/go-hsd</u> 

標準でサポートしている VCS は次のとおりです。 



**GitHub** 







### **Bitbucket Launchpad OpenStack** 

一般的なプログラミング言語の多くは Maven 、 PyPI 、 npm 、 RubyGems 、 CPAN など、セントラルリポジトリにアカウントを用意し、そこに対してパッケージを登録したり、各 VCS へのリンクを登録したりといった作業が必要で、パッケージを公開する際のハードルが高くなっています。また、ちょっとした個人的なサンプルコードを公開する場合においても VCS のセットアップだけすればすぐにパッケージが公開できるため、初学者にとってもハードルが低く、開発しやすいという利点があります。 

## **libc非依存** 

C 言語で開発されたプログラミング言語や、ある言語が依存しているライブラリが C 言語で開発されている場合、それらは libc に依存しています。歴史上、 UNIX のシステムは C 言語で開発されることが多かったため、基本的なコマンドやライブラリそのものの開発にも C 言語が使われてきました。そして、それらの基本部分を集約して共通処理を集めたライブラリとして libc が提供されています。一見便利そうに思えるのですが、 libc を使うということは、実行ファイルを生成するために C 言語のコンパイラやリンカが必要になるということになります。これによりクロスコンパイル(別のターゲットの実行ファイルを生成すること)の難易度が高くなります。また libc を使うということは、 libc のバージョンに依存するということを意味します。 Linux のバージョンが上がると当然 libc のバージョンも上がります。これにより、たとえば Ubuntu でビルドした実行ファイルを Red Hat 系の Linux に持っていっても動かない、という問題が発生します。 

Go は libc に依存しません。執筆時点では、ネットワーク通信を行う一部の処理のみ libc を使うことがありますが、それ意外は libc に依存しません。よってクロスコンパイルを容易に行うことができ、かつ libc のバージョンにも非依存です。 Ubuntu でビルドした Go の実行ファイルを Red Hat 系の Linux に持っていっても、ほぼ問題なく動作するのです。 

## **共同開発でのスキル差** 

大きなシステムを開発する場合、複数人で作業することになります。当然ながら、開発者間のスキル差が開発の進捗に影響します。開発に使用している言語をマスターしている開発者もいれば、まだ学習中の開発者もいます。要員が足らなくなった際には、そのプログラミング言語にある程度、精通している開発者を追加しなければなりません。また保守を担当していた開発者がプロジェクトを抜けるといったケースでは、交代要員が必要になります。みなさんも、前任者が残したプログラムの小難しいテクニックに頭を悩まされたことが一度や二度はあると思います。 Go は文法がとてもシンプルで、表現できる範囲がそれほど広くありませんので、誰が書いても同じようなコードになりやすく、引き継ぎ時のリスクは低くなります。ただそれゆえに、高度な表現ができるプログラミング言語を多く扱ってきたスキルの高いプログラマーには、つまらないプログラミング言語となります。しかし Go を触ったことがなくても、それまでに他のプログラミング言語を触ったことがあるプログラマーであれば、それほど苦労することなく習得することができます。これも Go の魅力と言って良いと筆者は考えています。 

# **1.6 まとめ** 

Go 言語がどのようなプログラミング言語なのかを解説しました。表面上は簡素なプログラミング言語に見えますが、実は奥が深く、それでいてパワーを発揮できる良い言語だと筆者は考えます。後半では Go がなぜ使われるのかを解説しました。なぜ Go 言語がいろいろな場面で使われ始めているのか不思議に思っていた読者の方もいるかと思います。 Go はユーザーの利益を優先し成果を出すための言語であり、それでいて多くの開発者に対するハードルを下げた言語と言えるでしょう。 



# **2.1 Goコンパイラのインストール** 

Go のインストール方法は OS により異なります。ブラウザでオフィシャルサイトを開き、 Downloa ~~d~~ <u>注2.1</u> ~~ペ~~ ージに遷移してください。 

Windows の場合は、お使いのコンピュータのプロセッサ ( 386 または amd64 )に合った MSI インストーラをダウンロードしてインストールします。 

macOS の場合も、お使いのコンピュータのプロセッサ ( arm64 または amd64 )に合った pkg ファイルをダウンロードしてインストールします。また、 macOS の場合は Homebrew を使ってインストールすることもできます。 Homebrew を使ってインストールするには以下を実行します。 

$ brew install go 

Linux の場合でも、お使いのコンピュータのプロセッサ ( 386 または amd64 )に合った tar.gz ファイルをダウンロードしますが、その後、ご自分で tar.gz を展開してコンパイラへのパスを設定する必要があります。 

$ sudo rm -rf /usr/local/go 

$ sudo tar -C /usr/local -xzf go1.20.linux-amd64.tar.gz 

1 点注意として、必ず古い go ディレクトリを消してから tar.gz ファイル展開してください。古いファイルが残った状態でインストールすると誤動作が発生します。 Linux の場合はお使いの OS が提供するパッケージマネージャを使ってインストールすることもできます。たとえば Ubuntu Linux であれば、 apt を使って以下のようにインストールすることができます。 

#### $ sudo apt install golang-1.18 

ただしいずれの Linux ディストリビューションも、オフィシャルが提供しているコンパイラの最新バージョンよりも少し古いことが多いです。セキュリティフィックスを含むバグ修正や、性能改善が含まれていますので、可能な限りオフィシャルサイトから最新バージョンをインストールすると良いでしょう。 

Go に限らずですが、言語処理系のバージョンの違いによって起きる誤動作を懸念して、 goenv ( Python で言う venv 、 Ruby で言う rbenv )のような、バージョン管理ソフトウェアをお使いになる方もいます。これは筆者の個人的な意見なのですが、こういったバージョン管理ソフトウェアは、各ソフトウェアに対して必要なランタイムが固定されていないと動作しない、または動作の保証がないという問題を解決するのには有効ですが、 Go のようにビルドされてすでに静的にリンクされた実行ファイルに対してはほぼ意味がありません。つまり、コンパイラを古いバージョンに戻してコンパイルするメリットは薄いということです。また Go は、 Go 開発チームが下位互換性を保つことを決めた「 The Go1 Compatibility Rules 」と呼ばれているルールをもとに開発されています注2.2 ~~。~~ このルールにより Go の標準パッケージは仕様が固定されており、とても重要なバグ修正でもない限り関数シグネチャが変更されることはありません。ですので、 Go は常に新しいバージョンを使っておけば古いソースコードでも問題なくビルドできることが約束されています。また逆に、バージョン管理ソフトウェアを使うことで、脆弱性を含んだいくつものバージョンが更新されないまま残ってしまうこともあります。よって筆者は、 goenv のようなソフトウェアを使うのはお勧めしません。 

## **COLUMN** 

## **GOROOT? GOPATH?** 

古いバージョンのGoでは、GOROOTやGOPATHといった環境変数を設定する必要がありました。今もなお、その設定が必要だと思っている方がおられます。しかし最近のバージョンでは、どちらも設定する必要がありません。 

go getコマンドによってダウンロードされるソースコードは~/go/pkg配下に、go installコマンドによってインストールされる実行ファイルは~/go/binに格納されます。このディレクトリを変更したい場合はGOROOTや GOBINを設定します。通常の使い方をするのであれば、 GOROOTはもはや必要ありません。 

Goは2019年ごろからGoモジュールというパッケージの依存関係を解決するしくみを導入しましたが、それまではGOPATH内に置かれたローカルのパッケージを参照してビルドするしくみが利用されていました。また開発もGOPATHのフォルダ内で行われてきました。Goモジュールを利用するのであれば、もはや依存パッケージを手動でディレクトリ管理する必要はなく、開発ディレクトリもどこにあっても良いことになります。 

#### **注** 2.1 <u>https://go.dev/dl/</u> 

(本文に戻る) 

**注** 2.2 <u>https://go.dev/doc/go1compat</u> 

(本文に戻る) 

# **2.2 開発環境のセットアップ** 

基本的に、 Go は IDE がなくても開発できます。前述のとおり、 Go はシンプルな言語です。慣れてくると入力補完に頼らず開発できるようになります。もちろん、 IDE を使ったほうが効率的なのは間違いありません。以降では Microsoft 社が開発している Visual Studio Code と、古くから UNIX ユーザーに愛されているテキストエディタ Vim 、そして JetBrains 社が開発している Goland の、 3 つのセットアップ方法を紹介します。なお Go の Language Server である gopls は、各 IDE で自動(または手動)でインストールされる想定ですので、本書では解説しません。 

## **Visual Studio Code** 

Visual Studio Code は拡張可能なテキストエディタです( **図** 2.1)。オフィシャルサイト注2.3 ~~か~~ らインストーラをダウンロードしてインストールしてください。インストールした状態では Go は扱えません。 Go を扱うには拡張機能をインストールする必要があります。適当なディレクトリで拡張子 .go のファイルを開くと Go 拡張をインストールするように勧められます。あとは画面に従ってインストールしてください。 Go 拡張がインストールされると、 Go の開発に必要なコマンドラインツールも自動でインストールされます。 

## **Vim** 

Vim は古くから UNIX ユーザーに愛されるテキストユーザーインターフェースのテキストエディタです。キャラクターベースですので、 Visual Studio Code ほどグラフィカルではありませんが、基本的に Vim は Visual Studio Code の Go 拡張とほぼ同じことができるようになっています。 

Visual Studio Code の Go 拡張は、 Microsoft 社が仕様を取りまとめている Language Server Protocol (言語サーバプロトコル)を TypeScript で実装したソフトウェアです。 Language Server そのものは Go のオフィシャルが開発しており、その Go 専用の Language Server である gopls と Visual Studio Code が対話することで Go のプログラミングに便利な、入力補完や定義位置ジャンプといった機能を提供しています。 

Vim においても Language Server を扱うしくみがあります。 coc.nvim や vim-lsp という Vim プラグインを使うことで、 Go や Java 、 C# といったいろいろな Language Server を扱えるようになっています。以下では筆者が日常的に使っている vim-lsp のインストール手順を紹介します。 

Vim プラグイン prabirshrestha/vim-lsp と mattn/vim-lsp-settings の 2 つを使うことで Language Server を扱えるようになります。また hrsh7th/vim-vsnip と hrsh7th/vim-vsnip-integ を追加することで、補完の展開をさらに便利にすることができます。 

**リスト** 2.1はプラグインマネージャ vim-plug を使ってプラグインを定義するための書き方です。 vim-plug を導入して vimrc に書き足してください。 

#### **リスト2.1 vim-plugでGoのLanguage Server関係のプラグインを定義** 

Plug 'prabirshrestha/vim-lsp' Plug 'mattn/vim-lsp-settings' Plug 'hrsh7th/vim-vsnip' Plug 'hrsh7th/vim-vsnip-integ' 

また、 Language Server の操作をカスタマイズしたい場合には **リスト** 2.2を設定してください。 

**リスト2.2 Language Serverの操作設定** 

function! s:on_lsp_buffer_enabled() abort if &buftype ==# 'nofile' || &filetype =~# '^\(quickrun\)' || getcmdwintype() ==# ':' return 

endif " Language Serverが有効になったバッファに対する設定 setlocal omnifunc=lsp#complete " 以下は好みで設定 "nmap <buffer> gd <plug>(lsp-definition) "nmap <buffer> <f2> <plug>(lsp-rename) "nmap <buffer> <c-k> <plug>(lsp-hover) endfunction augroup vimrc_lsp_install autocmd! autocmd User lsp_buffer_enabled call s:on_lsp_buffer_enabled() augroup END 

Vim を起動して Go のファイルを開くと gopls のインストールを促されるので、:LspInstallServerコマンドを実行します。すると gopls のインストールが行われます。インストールが完了すると、 Visual Studio Code のように入力補完や定義ジャンプなどが扱えるようになります( **図** 2.2)。 



## **Goland** 

Goland は JetBrains 社が開発している Go 専用の IDE です。 Visual Studio Code や Vim では、 Go 開発チームが開発している Language Server を利用していますが、 Goland は独自に Go のソースコードを解析し、入力補完や定義ジャンプといった機能を提供しています。さすが商用製品だけあって、開発者が期待しているさまざまな機能が実装されています( **図** 2.3)。 



Goland は JetBrains 社のサイ ~~ト~~ <u>注2.4</u> ~~か~~ らダウンロードするか、 JetBrains Toolbox からインストールします。 Go 専用の IDE ですから、 Visual Studio Code や Vim のようにアドオンを追加する必要はありません。 

◆ ◆ ◆ 

筆者がこれら IDE 、テキストエディタを一通り使ったうえでの感想としては、 Goland の補完は精度が高く、また複数ある Go コンパイラをスイッチできたりなど、いろいろとかゆいところに手が届くようになっていると思います。 

**注** 2.3 <u>https://code.visualstudio.com/</u> (本文に戻る) 

**注** 2.4 <u>https://www.jetbrains.com/go/</u> (本文に戻る) 

# **2.3 まとめ** 

Go のインストール方法を解説しました。このほかにも、 Go を扱える開発環境はたくさんあります。ぜひ気に入った自分向きの開発環境を手に入れてください。 



# **3.1 プロジェクトフォルダ** 

Go の開発を始める際は、まず Go のプロジェクトフォルダを作成します。 

$ mkdir my-app $ cd my-app $ go mod init my-app 

次にテキストエディタで main.go というファイルを開き、 **リスト** 3.1の内容を入力します。 

**リスト3.1 main.go** 

package main import "fmt" 

func main() { <mark>|-></mark> fmt.Println("こんにちは世界") } 

※ <mark>|-></mark> 部分はタブ文字。以降のコードでは表記せず 

package や import 、 func は Go のキーワードです。 package はプログラムのパッケージ名を宣言します。パッケージとは関数や変数などを束ねる単位です。実行ファイルの場合はパッケージ名に main を使用します。 import は標準パッケージや外部のパッケージを取り込む際に使用します。 

func で関数宣言を行います。関数 main は、ビルドした際に実行エントリポイントとなります。取り込んだパッケージを参照し、そのパッケージ内のシンボルを参照するには.を使います。この例では fmt パッケージの Println という関数を呼び出しています。 fmt.Println はコンソールの標準出力にメッセージを出力する関数で、以降の解説で何度も登場します。ファイルを保存したらビルドします。 Visual Studio Code の場合は Ctrl - @ をタイプすると端末が起動するので( **図** 3.1)、そこで以下を入力します。 

#### go build 

Vim の場合は:terminalコマンドを実行すると Visual Studio Code と同様に端末が起動します。 Goland の場合は Alt - F12 で端末が起動します。コマンドを実行すると my-app という実行ファイル ( Windows の場合は my-app.exe )が生成されます。実行ファイルを実行すると「こんにちは世界」が表示されるはずです。最初に使用したコマンドgo mod initは Go モジュールの初期化を意味します。 Go モジュールについては後述します。 

# **3.2 Go言語の特徴** 

Go の基本的な構文を説明する前に、 Go がどのような言語仕様なのかを簡単に説明します。 













- **GC(Garbage Collector)がある静的な型を使うポインタを使う三項演算子はない継承はない例外はない** 

他のプログラミング言語に慣れている人の中には、三項演算子や継承や例外が Go にないことに戶惑う方がいるかもしれません。しかしこれにも理由があり、三項演算子や継承、例外はプログラムを余計に複雑にしてしまい、生産性を下げてしまうこともあるため、 Go ではあえて導入していないのです。例外については panic という、 C 言語で言うところの assert のようなものがあります。 recover 関数を使って panic を捕捉することで例外のようなことを実現することは可能ですが、例外と呼べる機能ではありません。 Go は panic/recover を例外機構として用意しておらず、スタックトレースや検査例外といったしくみも実現できません。 panic や recover は致命的なエラー時のみで使用します。 

# **3.3 静的な型** 

プログラミング言語は大きく分けて 2 種類あります。 1 つは静的型付け言語、もう 1 つは動的型付け言語です。静的型付け言語はコンパイル時に型が決定され、変数や関数を型で束縛して異なる型の値を再代入することを禁じ注3.1 ~~、~~ 型安全にする型システムです。かたや動的型付け言語は実行時まで型が決定せず、変数に異なる型の値を再代入できます。 Go は静的型付け言語です。異なる型への代入はコンパイル時にチェックされ、コンパイルエラーとなります。また、変換可能な異なる型への代入を行う際には明示的にキャスト (型変換)をしなければなりません。 

**注** 3.1 プログラミング言語によります。 (本文に戻る) 

# **3.4 基本的な構文** 

Go の基本的な構文を解説します。本書では Go のすべての構文については説明せず、簡単なアプリケーションを作成するうえで必要な構文を解説します。 Go は C 言語を意識して作られたプログラミング言語であるため、一部、 C 言語がわかる方向けに両者を対比する内容が書かれています。ですが C 言語がわからない方でも、おおよそはご理解いただけると思います。 

## **変数宣言** 

Go で変数を宣言するには var を使います。 

<mark>var n int</mark> 

Go には以下のビルトイン型があります。 















**interface{}(any) bool byte** 

**complex64、complex128** 

- **error** 

**float32、float64** 

**int、int8、int16、int32、int64** 







**rune、string uint、uint8、uint16、uint32、uint64 uintptr** 

上記の例では int 型の変数 n を宣言しています。宣言と初期値の代入を一度に行うこともできます。 

<mark>var n int = 1</mark> 

変数は型を保持します。同じ変数名で異なる型を代入することはできません。 

var n int n = 1 n = "foo" // コンパイルエラー同じ変数名で再度宣言することもできません。 

var n int n = 1 

var n string n = "foo" // コンパイルエラー 

※ ブロックスコープ({から始まり}で終わるコード)の中では同じ名前の変数を局所的に宣言できますなお、 Go ではセミコロン(;)は使いません。使うこともできますが、文を 1 行で書きたい場合にのみ使います。 

Go は他のプログラミング言語と異なり、変数 n を宣言しながらもそれが使われない場合にはコンパイルエラーとなります。一見この動作に戶惑う方もいますが、使われない変数が宣言されたままになるよりはコンパイル時にエラーになったほうが結果的には良いという、いかにも Go らしい仕様です。変数の宣言は var を省略して以下のように書くこともできます。 

x := 1 y := 1.2 n := 1 + (x + 2) * int(y) 

右辺のリテラルから型が明らかである場合には、左辺の変数の型が推測できるため、型を省略して記述することができます。上記の例では y は float64 型、 n は int 型として宣言されます。 

y は float64 型ですので int の計算で使う場合にはint(y)のようにキャストしなければなりません。一方、以下では n は float64 型の変数として宣言されます。 

x := 1 y := 1 n := 1.2 + (float64(x)+2)*float64(y) 

四則演算や、括弧の優先度などは C 言語と変わりませんが、 C 言語と異なり文字列は+演算子で結合することができます。 

<mark>s := "Hello, " + "World"</mark> 

## **const(定数宣言)** 

const を使うと定数を宣言できます。 

<mark>const x = 1</mark> 

変数と異なり、使われない場合でもコンパイルエラーになりません。 const は bool 、 int/int32/int64 や float32/float64 といった数値、複素数、文字列を宣言することができます。スライスや配列、 struct の値は宣言できません。たとえば、 C 言語では以下のように宣言することでイミュータブル(不変)な int の配列が宣言できます。 



#### **C** 

const int v[2] = {0, 1}; v[0] = 1; // コンパイルエラーまた、 Java では配列そのものをイミュータブルにすることはできますが、配列の要素は変更できます。 



#### **Java** 

final int v[] = { 0, 1 }; v[0] = 1; // OK 

Go では配列やスライス、 struct をイミュータブルにすることはできません注3.2 ~~。~~ 以下のソースを見てください。 

const x = 1 y := 1 f := 1.2 + (x + 2) * float64(y) 

ここでおもしろいことに気づくかもしれません。変数の場合、 x を使う際はfloat64(x)でキャストしなければなりませんでしたが、 const で宣言された x は型を明示しておらず、使われ方によって型が自動で決定されます。これを untyped constant と呼びます。使われる場所でそれぞれ型が決まるため、以下はコンパイルエラーになりません。 

const n = 1 x := 1 + n y := 1.2 + n 

## **iota(列挙)** 

Go には列挙型がありません。 const により定数を宣言することはできますが、 C 言語のような、ユニークな値を作ることができる enum は用意されていません。ただし iota を使うことで同様の定数を宣言することができます。 

const ( Apple = iota Orange Banana ) 

この場合、 Apple は 0 、 Orange は 1 、 Banana は 2 になります。 iota は都度評価されます。 

const ( Apple = iota + iota Orange Banana ) 

この場合、 Apple は 0 、 Orange は 2 、 Banana は 4 になります。 iota は登場するごとに新しい宣言が実行されるため、以下のように異なるルールを宣言することができます。 

const ( Apple = iota + iota Orange 

Banana = iota + 3 ) func main() { fmt.Println(Apple)  // 0 + 0 = 0 fmt.Println(Orange) // 1 + 1 = 2 fmt.Println(Banana) // 2 + 3 = 5 } 

型を指定して宣言することもできます。 

type Fruit int type Animal int const ( Apple Fruit = iota // Fruit(0) Orange Banana ) const ( Monkey Animal = iota // Animal(0) Elephant Pig ) 

前述のとおり、 const をまとめて記述すると初期値が連続してしまうため、型ごとに const 宣言を別にする必要があります。 

type Fruit int type Animal int const ( Apple Fruit = iota // Fruit(0) Orange             // Fruit(1) 

Banana             // Fruit(2) ) const ( Monkey Animal = iota // Animal(0) Elephant             // Animal(1) Pig                  // Animal(2) ) func main() { var fruit Fruit = Apple fmt.Println(fruit) fruit = Elephant // ここがコンパイルエラー fmt.Println(fruit) } 

型を指定して宣言することで、異なる型への代入を行った際にコンパイルエラーにすることができます。 ./fruit.go:26: cannot use Elephant (type Animal) as type Fruit in assignment ※ 訳:Animal型であるElephantをFruit型として代入することはできません 

## **命名規則** 

Go では一般的に変数に短い名前が付けられます。ただしこれには理由があります。 Go 開発チームの Russ Cox 氏のブログを引用します注3.3 ~~。~~ 

_> 名前の⻑さはその情報の中身を超えるべきではない。ローカル変数の場合、名前 i は index や idx といった情報を限_ 

_りなく即決に伝える。同様に i と j は、 i1 や i2 ( index1 や index2 はさらに良くない)よりもインデックスを名付けるための良いペアである。なぜならば、それらはプログラムを斜め読みするときに個別に伝えやすいからである。グローバルな名前は相対的により多くの情報を伝えなくてはならない。なぜならば、それらはいろんなコンテキストに現れるからである。短かったとしても、この正確な名前は acquire や take_ownership のような⻑ったらしいものよりも、より多くを伝えることができる。すべてを名前で伝える。_ 

Go では変数や関数の命名に Pascal Case が使われ、アンダースコア(_)は使われません。 update_user ではなく updateUser が使われます(ただし、絶対ではないので習慣ととらえてください)。パッケージ内で大文字から始まる型、グローバル変数、関数は、公開シンボルとして扱われます。以下のソースコードで、パッケージの外部から関数 UpdateUser を foo.UpdateUser()という形式で呼び出すことはできますが、グローバル変数 index を参照することはできません。 

package foo var index = 1 func UpdateUser() { // do something } 

## **関数呼び出し** 

Go では型名を、変数名や関数名のあとに記述します。 

func FindUser(name string) (*User, error) { user := findUesrFromGroup("my-group", name) if user == nil { return nil, ErrNotFound } return user, nil } 

これにも理由があります。 C 言語は昔に作られた言語仕様のため、いろいろなことが考慮されていませんでした。たとえば、変数宣言の際に型名を前に書くか後に書くか。 C 言語は前に型名を書きますが、これによりパース(構文解析)が難しくなる問題があります。まずは **図** 3.2を参照してください。 



※ 出典:http://c-faq.com/decl/spiral.anderson.html 

コンパイラは左から右へソースコードの確認を行います。この変数宣言をコンパイラが見つけたとき、 char だけではそれが変数宣言なのか関数宣言なのかがわかりません。そこでコンパイラは char という情報をメモリにストックしておかなければなりません。次に*を見つけてもそれが変数宣言なのか関数宣言なのかわかりません。さらに str を見つけてもそれは同じです。 str の次に[を見つけて初めて、それが変数宣言であるとわかります。それまでストックしておいた不確定なシンボル情報を次々に逆たどりし(1 → 4)、ようやくそれが char ポインタの配列の変数宣言であると判明します。これは Clockwise Spiral Rule と呼ばれ、 C 言語がいかにコンパイラに優しくない言語仕様なのかを示す際に使われる例えです。 Go では変数宣言に var を用いることで C 言語のような不確定なシンボル情報を保持する必要がありません。さて Go に話を戻します。 Go は C 言語と異なり多値を返すことができます。ユーザー情報とエラー情報を同時に返す関数を考えてみましょう。ユーザーが見つかった場合には error を nil で返却し、ユーザーが見つからなかった場合には error として ErrNotFound を返します。この関数を呼び出す際には、以下のようにして多値を受け取る必要があります。 

func FindUser(name string) (*User, error) { user, err := findUserFromList(users, name) if err != nil { return nil, err } return user, nil } func main() { user, err := FindUser("Bob") if err != nil { log.Fatal(err) } fmt.Println(user.Name) } 

Go ではエラーが発生し得る関数を呼び出した際には、まず err をチェックし、即座に呼び出し元に返却することが推奨されています。型名を変数名や関数名のあとに記述する仕様は、こういったケースが想定されているのです。 

## **条件分岐** 

if 文は括弧を必要としません。 if x == 2 && y == 3 { doSomething() } 

セミコロンを用いて以下のように書くこともできます。 

if user, err := userName(); err == nil { fmt.Println(user.Name) } else { log.Println(err) } 

if の中で宣言した変数 user や err は else でも使用することができます。 switch 文はほぼ C 言語と同じですが、 break を書く必要がありません。 

switch i { case 1: fmt.Println("one") case 2: fmt.Println("two") case 3, 4: fmt.Println("three or four") default: fmt.Println("other") } 

明示的に次の case に降下したい場合は fallthrough を使います。 

switch i { case 1: fmt.Println("one") case 2: fmt.Print("two or ") fallthrough // two or three or fourが表示される case 3, 4: fmt.Println("three or four") default: fmt.Println("other") } 

switch case には固定値ではなく式を書くこともできます。 

switch { case i > hit: fmt.Println("bigger than", hit) case i < hit: fmt.Println("less than", hit) case i == hit: fmt.Println("equal to", hit) } 

## **ループ** 

ループには for 文を使います。 for 文には 4 種類あります。 1 つめは C 言語形式のもの。 

for i := 0; i < len(s); i++ { // do something } 

2 つめは C 言語の while 文と同じもの。 

for i < 3 { // do something } 

### 3 つめは無限ループです。 

for { // do something } 

最後に map 、スライスをイテレートするものです。 map 、スライスについては後述します。 



#### **map** 

for k, v := range obj { // kはmapのキー、vは値 } 



#### **スライス** 

for i, v := range arr { // iはarrの添え字、vは値 } 

C 言語と同じように break でループを中断し、 continue でループを継続することができます。また、 Go には Labeled Break という記法があり、多重ループの内側から一気に外側のループまで抜けることができるようになっています。 

#### finished: 

for i := 0; i < 100; i++ { for j := 0; j < 100; j++ { if check(i, j) { break finished 

} } } 

これらの for ループとは別に、 channel (後述)の読み取りを for で書くことができます。 

for v := range ch { // do something } 

この場合、 channel がclose(ch)により閉じられると for ループが終了します。 

## **配列とスライス** 

Go には配列とスライスという、一見似ているが異なる 2 つの型が存在します。配列は固定⻑、スライスは可変⻑な配列と考えると良いでしょう。配列、スライスともに 0 から始まります。配列の⻑さを超えた位置を参照すると panic が発生します。 

var a [4]int a[0] = 1 

この宣言は int 型の、⻑さ 4 の配列を宣言しています。⻑さを変更することはできません。一方、スライスは⻑さを指定せずに宣言します。 

<mark>var a []int</mark> 

ただし、宣言するだけでは a には nil という特別な値が格納されます。そのまま要素にアクセスすると panic が発生します。スライスを作成するには make を使用します(変数宣言を兼ねています)。 

a := make([]int, 3) a[0] = 1 a[1] = 2 a[2] = 3 

また、スライスの初期化は初期値を代入することでも可能です。 

<mark>a := []int{1, 2, 3}</mark> 

多次元の配列やスライスを扱うこともできます。 

// 固定⻑2次元の配列 arr1 := [2][3]int { {1, 2, 3}, {4, 5, 6}, } // 2次元のスライス arr2 := [][]int { {1, 2, 3}, {4, 5, 6}, } 

スライスを伸⻑するには append を使います。 

a := []int{1, 2, 3} fmt.Println(a) // [1 2 3] が表示される a = append(a, 4, 5, 6) fmt.Println(a) // [1 2 3 4 5 6] が表示される 

fmt.Printf("a の⻑さは %d\n", len(a)) // 「a の⻑さは 6」が表示されるなお、配列およびスライスには、⻑さ len とは別に cap という容量を示す値があります。配列の場合、⻑さと容量が同じ値で固定されています。スライスの場合、 cap の値は常に len 以上になっています。これは append を使って伸⻑するごとにメモリを再確保しなくても良いように、予約された分を示しているからです。 

make は以下のように len と cap を指定することもできます。 

a := make([]int, 0, 100) for i := 0; i < 100; i++ { a = append(a, i) } 

append を繰り返し使用すると、 len が cap よりも大きくなるたびにメモリの再確保が行われ、パフォーマンスが悪くなってしまいます。事前に⻑さが判明しているのであれば、 len と cap の両方を指定するのが良いでしょう。この場合、ループを途中で抜けてしまっても len が 100 になることはありません。スライスは添え字で範囲を指定して部分参照することができます。以下の例は添え字 2 〜 4 を参照しています。 

fmt.Println(a[2:5]) // [2 3 4]が表示されるスライスから要素を削除する方法は 3 つあります。これから示す 3 つのコードは、どれも同じスライスの要素を削除するコードですが、以下の違いがあります。 

### **(1)新しく同じ型のスライスを用意して詰めなおす** 

### **(2)appendを使う (3)部分参照とcopyを使う** 

まず新しく同じ型のスライスを用意して詰めなおす方法 ( 1 )です。 

a2 := make([]int, 0, len(a)/2) for i := 0; i < len(a); i++ { if i % 2 == 0 { // 奇数は削除する a2 = append(a2, a[i]) } } a = a2 

あらかじめ cap を指定しておくことで無駄なアロケーションを防ぐことができます。次は append を使う方法( 2 )です。 

n := 50 a = append(a[:n], a[n+1:]...) // 添え字50を削除最後に部分参照と copy を使う方法( 3 )です。 

n := 50 a = a[:n+copy(a[n:], a[n+1:])] 

( 2 )( 3 )の 2 つはそれほど挙動が変わりませんが、 append を使う方法は 1 回だけアロケーションが発生するので、( 3 )のほうが望ましいです。 

## **文字列** 

前述のとおり、 Go の文字列は+で結合できます。 

s := "Hello " s += name 

文字列はバイト列で構成されているため、添え字を使ってアクセスできます。 

s := "Hello" fmt.Printf("%c", s[0]) // Hを表示ただし、 Go の string はイミュータブルですので、内容を書き換える際には以下のようにバイト列に変換する必要があります。 

s := "Hello" b := []byte(s) b[0] = 'h' // Helloをhelloに変更 s = string(b) // イミュータブルなので再代入が必要なおバイトサイズを超えた位置を参照すると panic が発生します。また Go には rune という型があり、以下のようにして Unicode のコードポイント列に変換することができます。 

s := "こんにちわ世界" rs := []rune(s) rs[4] = 'は' // こんにちわ世界をこんにちは世界に変更 s = string(rs) // イミュータブルなので再代入が必要固定⻑の配列は[:]という指定をすることでスライスに変換することができます。 

var b [4]byte b2 := b[:] // b2 はスライス 

`から`で囲うことで複数行のテキストを扱うことができます。 

var content = `複数行の文章からなるテキストです。 ` 

この場合、文字列内ではバックスラッシュによるエスケープの影響を受けませんので、\を出力したい場合に便利です。ただし以下の場合、最初の`の直後の改行も含まれることに注意しましょう。 

var content = ` 複数行の文章からなるテキストです。 ` 

## **map** 

map は順を持たないキーと値のペアです。キーは文字列以外の型も扱えます。 

<mark>var m map[string]int</mark> 

この宣言は string 型のキー、 int 型の値を持つ map を定義しています。ただし、宣言するだけでは m には nil という特別な値が格納されます。 map を作成するには make を使用します(変数宣言を兼ねています)。 

m := make(map[string]int) m["John"] = 21 m["Bob"] = 18 m["Mark"] = 33 

スライスと同様に cap を指定することができます。 

m := make(map[string]int, 10) m["John"] = 21 m["Bob"] = 18 m["Mark"] = 33 

また、 m の初期化はリテラルを使って初期値を代入することでも可能です。 

m := map[string]int{ "John": 21, "Bob":  18, "Mark": 33, } fmt.Println(m) // map[Bob:2 John:1 Mark:3]が表示される変数だけ宣言し、 make やリテラルを使って初期化していないままキーを参照したりアイテムを追加したりすると panic が発生します。 

map から要素を削除するには delete を使います。 

<mark>delete(m, "Bob")</mark> 

map のキーと値を列挙するには以下のように for-range を使うことができます。 

for k, v := range m { fmt.Printf("key: %v, value: %v\n", k, v) } 

なお map は順序を保持しません。また、この for-range は毎回異なる結果となります。ソートされたキーで列挙したい場合は、先にキーを取り出し、ソート後に for-range を実行する必要があります。 

keys := []string{} for k := range m { keys = append(keys, k) } sort.Strings(keys) for _, k := range keys { fmt.Printf("key: %v, value: %v", k, m[k]) } 

map は存在しないキーを指定すると、値の型のゼロ値が返ります。 

m := map[string]string { "foo": "bar", } fmt.Println(m["zoo"]) // 空文字列が表示されるキーが存在したかどうかを確認する場合は以下のように 2 つの値を受け取ります。 

v, ok := m["zoo"] if ok { // zooが存在すればokはtrueになる fmt.Println(v) } 

## **type(型宣言)** 

type で型に名前を付けることができます。 

<mark>type MyString stirng</mark> 

この場合、 MyString は string のように振る舞うことができます。 

package main import "fmt" type MyString string func main() { var m MyString m = "foo" fmt.Println(m) } 

元の string 型に戻すには以下のように型変換を行います。 

func main() { var m MyString m = "foo" s := string(m) fmt.Println(s) } 

## **struct(構造体)** 

C 言語と同様に struct で構造体の型を定義します。以下は struct による型定義に User という名前を付けることを意味しています。 

type User struct { Name string Age  int } var user User user.Name = "Bob" user.Age = 18 

型名や変数名、関数名などと同様に、 struct のフィールドについても他のパッケージから参照するには先頭を大文字にする必要があります。たとえば、以下の例では Name は他のパッケージから参照可能ですが、 age は参照できません。 

type User struct { Name string age  int } 

ここで注意ですが、標準パッケージである encoding/json を使い、この User struct の JSON 文字列を得る場合にもこのルールが適用されます。小文字である age は encoding/json から参照することができないため、生成される JSON に age は含まれなくなります。宣言と初期値の代入を一度に行うこともできます。 

user := User { Name: "Bob", Age:  18, } 

これは以下と同じです。 

var user User user.Name = "Bob" user.Age = 18 

関数の引数などに struct のデータを渡すと都度、コピーが行われます。コピーのオーバーヘッドをなくすのであれば、ポインタを使うと良いでしょう。 

func showName(user *User) { fmt.Println(user.wName) } func main() { user := User { Name: "Bob", Age:  18, } showName(&user) } 

ここでは&を使っています。これは参照と呼び、ポインタを受け取る関数に実体の変数を渡す際に利用します。ポインタについては後述します。 

## **メソッド** 

Go には C 言語と異なり、メソッドが導入されています。メソッドを説明する前にまず type を説明します。 



struct の説明で記述したように type は( 3 )で指定した型に対して、( 2 )で示す名前を付けます。これは struct に限りません。別名を付けた型にはメソッドを定義することができます。別名を付けていない int や string などのビルトインの型に対しては、メソッドを追加することはできません。 

func (v Value) Add(n Value) Value { return v + n } 

この関数は、型 Value に Add というメソッドを宣言し、引数 n とその Value 自身である v を足して返却するというものです。 v はレシーバと呼びます。以下のように実行することができます。 

func main() { v := Value(1) v = v.Add(2) fmt.Println(v) // 3が表示される } 

Value(1)は int の値 1 を Value 型にキャストしています。 

## **ポインタ** 

Go はポインタが扱えます。&を使ってポインタを得て*による実体の参照ができます。 

v := 1 p := &v 

*p = 2 fmt.Println(v) // 2が表示される以下の例を見てください。 

user := new(User) user.Name = "Bob" user.Age = 18 

struct を動的に生成する場合には new を使うことができ、実際、前述の struct の定義の例と似ていますが、この例の user は実はポインタです。 Go では実体のフィールド参照にもポインタのフィールド参照にも.を使います。 C 言語と同様に*でデリファレンスできます。前述の Value 型のメソッド Add で自己を変更するようにするには以下のように実装します。 

func (v *Value) Add(n Value) { *v += n // レシーバをデリファレンスして代入 } func main() { v := Value(1) v.Add(2)       // 自身を書き換える fmt.Println(v) // 3が表示される } 

つまり Go では型に対して、実体のレシーバに対するメソッドと、ポインタのレシーバに対するメソッドを宣言することができます。実体のレシーバの場合、自己を変更することができません。またメソッドを呼び出す際には、レシーバのコピーが生成されます。 

func (v Value) Add(n Value) { v += n // vはコピーされた値なので更新しても反映されない } 

C 言語の場合、関数内の変数のポインタを関数の戻り値として返すと、スタックを参照してしまいプログラムがクラッシュしてしまいますが、 Go はポインタが関数の外に返されるかどうかを自動で判断し、スタックからヒープに切り替えてくれます。これはエスケープ解析と呼ばれます。 

func Bob() *User { // 関数内の変数userのポインタを関数外へ返す user := User { Name: "Bob", Age:  18, } return &user } 

## **interface{}またはany** 

Go にはどの型の値でも格納できる型interface{}があります。以下のように扱うことができます。 

var v interface{} v = 1 v = "こんにちは世界" 

interface{}型の変数から値を取り出し、元の型に戻すには型アサーション( type assertion )を行う必要があります。 

var v interface{} 

v = 1 n := v.(int) v = "こんにちは世界" s := v.(string) 

間違った型で型アサーションを行うと panic が発生します。正しい型アサーションができなかったことを確認するには以下のように実行します。 

s, ok := v.(string) if !ok { fmt.Println("vはstringではない") } else { fmt.Printf("vは文字列 %q です\n", s) } 

これも 1 行で書くことができます。 

if s, ok := v.(string); !ok { fmt.Println("v は string ではない") } else { fmt.Printf("vは文字列 %q です\n", s) } 

どんな値でも引数として受け取れ、処理できる関数を実装するには型スイッチ( type switch )を使うことができます。 

func PrintDetail(v interface{}) { switch t := v.(type) { case int, int32, int64: 

fmt.Println("int/int32/int64 型:", t) case string: fmt.Println("string 型:", t) default: fmt.Println("知らない型") } } 

ただし、この型スイッチは以下のように type で定義された別の型( underling type )を扱うことはできません。 

type V int var v V = 123 PrintDetail(v) 

この場合、 V は int 、 int32 、 int64 のいずれにも該当しないため「知らない型」が表示されます。 

V を扱うには switch case に V そのものを含めなければなりません。 type 定義された型でも扱えるようにするためには reflect パッケージを使う必要があります( **リスト** 3.2)。 

#### **リスト3.2 reflectパッケージの使用例** 

package main 

import ( "fmt" "reflect" ) 

func PrintDetail(v interface{}) { rt := reflect.TypeOf(v) switch rt.Kind() { case reflect.Int, reflect.Int32, reflect.Int64: fmt.Println("int/int32/int64 型:", v) 

case reflect.String: fmt.Println("string型:", v) default: fmt.Println("知らない型") } } func main() { type V int var v V = 123 PrintDetail(v) } 

本書では reflect パッケージの使い方は説明しません。詳細は reflect パッケージのドキュメン ~~ト~~ <u>注3.4</u> ~~を~~ 参照ください。なお Go のバージョン 1.18 以降からinterface{}は any で置き換えられるようになりました。上記で記述しているinterface{} は any で置き換えて記述してもコンパイルすることができます。 

## **コンストラクタ** 

Go には C++ や Java のようなコンストラクタがありません。初期処理として struct の初期化を行い、その struct のポインタを返したいような場合は、 Go では習慣として New を接頭語に付けた関数を用意します。 

func NewUser(name string, age int) *User { return &User { Name: name, Age: age, } } 

この New はあくまで習慣であり、 Go コンパイラが New を特別扱いしているわけではありません。 

## **インターフェース** 

struct は実体を持つデータですが、 interface はメソッドを持つ型のインターフェースを定義できます。 

type Speaker interface { Speak() error } 

この Speaker は、メソッドに Speak を持つ型を表します。 

type Dog struct {} func (d *Dog) Speak() error { fmt.Println("BowWow") return nil } type Cat struct {} func (c *Cat) Speak() error { fmt.Println("Meow") return nil } 

このようにメソッド Speak を持つ型 Dog と Cat の値は、 Speaker 型の変数に代入することができます。 

func DoSpeak(s Speaker) error { return s.Speak() } 

func main() { dog := Dog{} DoSpeak(&dog) // BowWowが表示される cat := Cat{} DoSpeak(&cat) // Meowが表示される } 

Dog だけさらに別のメソッドを持っていてもかまいません。 

Go ではこのように、型そのものがインターフェース Speaker を実装していることを明示しなくても良い設計を採用しています。これをダック・タイピングと呼びます( 3.10 のコラム「おまじない」参照)。前述のinterface{}(または any )はつまり、「どんなメソッドを持っていても」どころか「メソッドを持っていなくても」代入可能な型ということになります。 

## **defer** 

defer は後処理を記述できます。 Go における後処理とは関数を抜ける際に実行される処理を defer で宣言することを意味します。 

package main import ( "fmt" "log" "os" ) 

func main() { f, err := os.Open("data.txt") if err != nil { log.Fatal(err) } defer f.Close() // 後で呼ばれる var b [512]byte n, err := f.Read(b[:]) if err != nil { log.Fatal(err) } fmt.Println(string(b[:n])) } os.Open はファイルを開く標準関数です。るとf.Close()はf.Close()はは main た、呼び出し時に使われる変数はキャプチャされます。 f, err := os.Open("data01.txt") if err != nil { log.Fatal(err) } defer f.Close() f, err = os.Open("data02.txt") if err != nil { log.Fatal(err) } defer f.Close() 

os.Open はファイルを開く標準関数です。 f.Close が呼ばれるまではシステムリソースを保持します。この処理を実行するとf.Close()はf.Close()はは main 関数が終了する際に実行されます。また、呼び出し時に使われる変数はキャプチャされます。この例を実行すると f は上書きされてしまいますが、どちらも defer 指定時の f がキャプチャされるため、それぞれの 

defer で正しくファイルが閉じられます。また defer は指定された順とは逆順に実行されます。 

func main() { defer fmt.Println("6") defer fmt.Println("5") defer fmt.Println("4") fmt.Println("1") fmt.Println("2") fmt.Println("3") } 

たとえば、このソースコードを実行すると 1 から 6 が順番に表示されます。ですので、ディレクトリとファイルを生成して処理を行ったあと、ファイルを閉じてディレクトリを削除するといった後処理も簡単に実装することができます。 

**リスト** 3.3では、たとえば、ディレクトリは作成できたけれどファイルを作ることができなかった、またはファイルに書き込むことができなかった、といった場合でも defer が呼び出されているので、確実にファイルディスクリプタを閉じることができ、かつディレクトリも確実に削除されます。 

#### **リスト3.3 deferでファイル処理の後処理を実装** 

func doSomething(dir string) error { err := os.Mkdir(dir, 0755) if err != nil { return err } defer os.RemoveAll(dir) 

f, err := os.Create(filepath.Join(dir, "data.txt")) if err != nil { return err } defer f.Close() 

// ファイルを使った処理 

} 

Windows では通常、ファイルハンドルが開かれた状態でディレクトリを削除することはできませんが、 defer を使うことでこの問題もおきません。 

1 点、気をつけないといけないのは、 defer は関数スコープで実行されるということです。以下のコードの defer はループごとには実行されません。すべての関数が終了する際に実行されてしまいます。 

for i := 0; i < 100; i++ { f, err := os.Open("data.txt") if err != nil { return err } defer f.Close() // do something } 

また、 defer には無名関数を渡すことができます( **リスト** 3.4)。 

#### **リスト3.4 deferに無名関数を渡す** 

func doSomething() { var n = 1 defer func() { fmt.Println(n) }() n = 2 } 

**リスト** 3.5のソースコードと動作が違う点に注意してください。 

#### **リスト3.5 deferに通常の関数を渡す** 

func doSomething() { var n = 1 defer fmt.Println(n) n = 2 } 

**リスト** 3.4では n はキャプチャされていません。ですので出力は 2 ですが、 **リスト** 3.5は n がキャプチャされているため 1 が出力されます。 defer は最後に実行されるため、 **リスト** 3.6と同じであると考えるとわかりやすいはずです。 

#### **リスト3.6 リスト3.4と同じ処理** 

func doSomething() { var n = 1 n = 2 func() { fmt.Println(n) // 2 }() } 

一方、無名関数を使わない場合は呼び出し時に n がキャプチャされるので **リスト** 3.7と同じになります。 

#### **リスト3.7 リスト3.5と同じ処理** 

func doSomething() { var n = 1 n = 2 

fmt.Println(1) // deferが実行されたときのnは1なので実質これと同じ } 

◆ ◆ ◆ 

そのほか、 Go の構文についてはドキュメン ~~ト~~ <u>注3.5</u> ~~を~~ 参照ください。 

**注** 3.2 <u>https://go.dev/blog/constants</u> (本文に戻る) 

**注** 3.3 <u>https://research.swtch.com/names</u> (本文に戻る) 

**注** 3.4 <u>https://pkg.go.dev/reflect</u> (本文に戻る) 

**注** 3.5 <u>https://go.dev/ref/spec</u> (本文に戻る) 

# **3.5 goroutine(ゴルーチン)** 

Go の特徴的な機能の 1 つに goroutine があります。 goroutine は Go のランタイムで管理された軽量なスレッドです。 

message := "hi" go sendMessage(message) 

goroutine を実行するには、関数呼び出しの前に go を付けるだけです。 goroutine は関数呼び出しに対して行われ、その呼び出し時には引数がキャプチャされます。ですので、以下のように message を変更しても呼び出される引数は変わりません。 

message := "hi" go sendMessage(message) message = "ho" 

ただし、無名関数を使う場合にはタイミングにより書き換えのほうが先に実行されてしまう可能性があります。 

message := "hi" // (1) go func() { sendMessage(message) // (2) }() message = "ho" // (3) 

( 1 )( 2 )( 3 )という順に実行されることを期待するかもしれませんが、 Go のランタイムスケジューラが( 3 )よりも( 2 )を先に実行するかはわかりません。以下の例では ho が 2 回出力されることがあります。 

func sendMessage(msg string) { println(msg) } func main() { message := "hi" go func() { sendMessage(message) }() message = "ho" time.Sleep(time.Second) println(message) time.Sleep(time.Second) } 

これは race condition と呼ばれ、 goroutine とその呼び出し元との間でデータ競合が起きていることを意味します。 

race condition は実行していても不整合が起きるまでは気づくことができませんが、 Go には race condition detector が付属されており、コンパイル時に -race を付けて実行することでランタイムが race condition を検出してくれます。 

$ go build -race main.go $ ./main ================== WARNING: DATA RACE (..略..) ================== 

ho ho 

goroutine は関数の中で実行しても動き続けます。逆に言うと、 main 関数の中で実行すると goroutine が実行中にもかかわらず main 関数の終了とともに強制終了してしまいます。 main 関数が goroutine の終了を待つためには sync パッケージを使う必要があります( **リスト** 3.8)。 

#### **リスト3.8 syncパッケージの使用例** 

package main import ( "fmt" "sync" ) func main() { var wg sync.WaitGroup wg.Add(1) // リファレンスカウンタを+1する go func() { defer wg.Done() // リファレンスカウンタを-1する // 重たい処理 }() // 別の重たい処理 wg.Wait() // リファレンスカウンタが0になるまで待つ } 

このように、重たい処理を並行して行えるのが goroutine です。一般的なプログラムは計算処理だけでなく、通信を行ったりファイルの I/O を行ったりと、 CPU を使用しない部分で多く時間を使います。その分だけ CPU は遊んでいることになります。そういった、 CPU を使わない時間に別の処理を実行できるという点が並行処理のメリットです。上の例では main 関数と goroutine の 2 つだけですが、必要に応じてさらに多くの goroutine を起動することで、より CPU を有効活用した処理が実装できます。 sync パッケージの使い方は後の章で解説します。なお goroutine をループ内で使う際には注意が必要です。 

var wg sync.WaitGroup for i := 0; i < 10; i++ { wg.Add(1) go func() { defer wg.Done() fmt.Println(i) }() } wg.Wait() 

さて、これを実行するとどう表示されるでしょうか。答えはこうです。 

10 2 10 10 10 10 10 10 6 10 

実行するごとに異なる結果が出ると思います。これは goroutine の中で参照している変数 i を fmt.Println で表示しようとしているときにはすでに、 for ループが終了してしまっている可能性があるからです。そうすると、ループ変数 i は 10 になります。ですので、結果に 10 が多くなっているのです。これを回避するにはループのスコープの中で新たな変数を宣言してやります(★部分)。 

var wg sync.WaitGroup for i := 0; i < 10; i++ { v := i // ← ★(i := i を使ってもかまいません) wg.Add(1) go func() { defer wg.Done() fmt.Println(v) }() } wg.Wait() 

これを実行すると期待どおり、個々の goroutine が正しいループ変数 i を参照できているのがわかると思います。 

0 1 5 3 4 9 7 8 6 2 

ただし、変数に代入するものが大きな struct や大きな byte 配列の場合もあります。そういった場合にはコピーにかかるコストが実行時間に影響してしまいます。 range を使ってループしている場合には、以下のように無名関数の引数として渡す方法もあります。 

for _, tt := range tests { wg.Add(1) go func(tt *Test) { defer wg.Done() fmt.Println(tt.name) }(&tt) } wg.Wait() また、ループカウンタを使って法でも良いでしょう。 for i := range tests { i := i wg.Add(1) go func() { defer wg.Done() fmt.Println(tests[i].name) }() } wg.Wait() 

また、ループカウンタを使って goroutine 内から参照する方法でも良いでしょう。 

goroutine の呼び出し元と goroutine 内で同じ変数を参照・更新する場合にも注意が必要です。前述のように、 race condition が発生した場合にはどのような結果になるかが保証されていません。データを保護するには sync.Mutex を使って保護する必要があります。たとえば、 **リスト** 3.9のプログラムでは 2000 が表示されないことがあります。 

#### **リスト3.9 race conditionが発生するプログラム** 

package main import ( "fmt" "sync" ) func main() { n := 0 var wg sync.WaitGroup wg.Add(2) go func() { defer wg.Done() for i := 0; i < 1000; i++ { n++ } }() go func() { defer wg.Done() for i := 0; i < 1000; i++ { n++ } }() wg.Wait() fmt.Println(n) } 

n++ は n の値に +1 するオペレータですが、元の n の値を得て +1 したタイミングで、他の goroutine がさらに +1 を実行してしまう可能性があるためです。このような race condition を避けるために sync.Mutex を利用します。 

**リスト** 3.10のプログラムは何回実行しても 2000 が正しく表示されます。 

#### **リスト3.10 リスト3.9にsync.Mutexを追加** 

func main() { n := 0 var mu sync.Mutex var wg sync.WaitGroup wg.Add(2) go func() { defer wg.Done() for i := 0; i < 1000; i++ { mu.Lock() n++ mu.Unlock() } }() go func() { defer wg.Done() for i := 0; i < 1000; i++ { mu.Lock() n++ mu.Unlock() } }() wg.Wait() 

fmt.Println(n) } 

## **channel(チャネル)** 

channel は goroutine に対してデータを送受信できるしくみです。まずは例を示します。 

#### **リスト3.11 channelの使用例** 

package main 

import "fmt" 

func server(ch chan string) { defer close(ch) ch <- "one" ch <- "two" ch <- "three" } 

func main() { var s string 

ch := make(chan string) go server(ch) 

s = <- ch 

fmt.Println(s) // oneが表示される s = <- ch 

fmt.Println(s) // twoが表示される s = <- ch fmt.Println(s) // threeが表示される 

} 

このプログラムを実行すると「 one two hree 」が画面に表示されます。 channel の作成には make を使います。 

<mark>ch := make(chan string)</mark> 

chan のあとに送信するデータの型を指定します。そして channel を閉じるには close を使います。 

<mark>close(ch)</mark> 

channel にデータを送信するためにはch <-を使います。また channel からデータを受信するためには<- chを使います。 channel からデータを受け取る処理に関しては for を使うことができます。 

for _, s := range ch { fmt.Println(s) } 

このループを終了させるためには goroutine 側で channel を閉じる必要があります。一見、単なるデータ受け渡しのしくみにしか見えないかもしれません。ですが、これが非同期処理に大きな影響をもたらします。たとえば 10 個の URL 文字列があるとします。それらが示す HTTP サーバ上の CSV ファイルをダウンロードし、すべてをデータベースに登録する処理を書く場合、みなさんであればどのように実装するでしょうか。 HTTP サーバからダウンロードする処理を 10 回まわして、それら CSV を読み込んでデータベースに登録する処理を書くのでしょうか。それとも、ダウンロードするごとにデータベースに登録するのでしょうか。実は HTTP サーバからダウンロードする処理は CPU をあまり使いません。また、データベースに登録する処理も CPU をあまり使いません。これらを並行で動作させることで、 CPU を有効に使い全体の処理時間を短くすることができます。 

goroutine と channel を使うことでそういった処理を簡単に実装できます( **リスト** 3.12)。 main 関数から goroutine を起動し、 10 個の URL を渡します。 goroutine ではループを使い HTTP サーバからダウンロードします。ダウンロードが完了したら都度、コンテンツを main 関数に渡します。 main 関数はダウンロードされたコンテンツを都度 goroutine から受け取って CSV をパースし、データベースに登録します。このように実装することで、 CSV ファイルをパースしたりデータベースに登録したりしている最中であっても HTTP サーバからのダウンロードが継続されることになります。 

#### **リスト3.12 goroutineとchannelを使った例** 

func downloadCSV(wg *sync.WaitGroup, urls []string, ch chan []byte) { defer wg.Done() defer close(ch) // 終わったら閉じる(5) // HTTPサーバからのダウンロード for _, u := range urls { resp, err := http.Get(u) if err != nil { log.Println("cannot download CSV: ", err) continue } b, err := ioutil.ReadAll(resp.Body) if err != nil { resp.Body.Close() log.Println("cannot read content: ", err) continue } 

resp.Body.Close() ch <- b // main関数にコンテンツを送信(3) } } func main() { urls := []string { "http://my-server.com/data01.csv", "http://my-server.com/data02.csv", "http://my-server.com/data03.csv", (..略..) } // バイト列を転送するためのchannelを作成(1) ch := make(chan []byte) var wg sync.WaitGroup wg.Add(1) go downloadCSV(&wg, urls, ch) //(2) // goroutineからコンテンツを受け取る(4) for _, b := range ch { r := csv.NewReader(bytes.NewReader(b)) for { records, err := r.Read() if err != nil { log.Fatal(err) } // レコードの登録 insertRecords(records) } } wg.Wait() } 

この例での channel を使ったデータのやりとりをまとめると以下の手順になります。 

**1. makeを使ってchannelを作成** 

**2. goroutineにchannelを渡す** 

**3. goroutineにてchannelへ送信** 

**4. main関数にてchannelから受信** 

**5. goroutineの終了時にchannelを閉じる** 

channel は基本、送信と受信それぞれをブロッキングします。つまり、 main 関数の for ループ( **リスト** 3.12 **(** 4 **)** ) は、 downloadCSV がコンテンツを送信( **リスト** 3.12 **(** 3 **)** ) するまでブロックします。コンテンツが届いていないのに for ループがまわり始めることはありません。逆に、 downloadCSV のコンテンツ送信( **リスト** 3.12 **(** 3 **)** )は、 main 関数が channel から読み取りを行わない限りブロックします。つまり双方が準備ができて初めてブロックが解けることになります。ここで、ダウンロード処理のほうが早く終わってしまう場合を考えます。 HTTP サーバがイントラネットにある場合にはデータベースへの登録処理よりも早く終わることになるでしょう。そういった場合、 downloadCSV での channel への送信の際に発生するブロックが、 main 側の受信の際に発生するブロックよりも⻑くなってしまうことになります。このような場合には、以下のように channel にバッファを持たせることができます。 

#### <mark>ch := make(chan string, 5)</mark> 

このバッファを 5 つ持った channel は、コンテンツが 5 つ登録されるまで送信がブロックされることはありません。また、コンテンツがいくつかバッファリングされている状態であれば、受信側もブロックされることはありません。うまく調整することで、非同期処理がさらに速くなります。ただし、ダウンロードしたコンテンツが、最も多い場合で 5 つ分メモリに保持されることになります。用途に合わせて調整する必要があります。 

goroutine と channel を使ったテクニックは、このほかにもたくさんあります。第 6 章で解説します。 

## **制御構文(select)** 

前述の switch 文とは別に、 Go には select 文という似た構文があります。たとえば、 2 つの channel を読み込むケースを考えます。前述のとおり、 channel の読み込みはデフォルトでブロッキングするので 1 つめの channel を待っている間に 2 つめの channel を読み込むことができません。そういった場合に select を使います。 

select { case v1 := <-ch1: // v1を使った処理 case v2 := <-ch2: // v2を使った処理 } 

このように記述すると、 ch1 または ch2 にデータが入ってくるまで両方を待つことができるため、効率が良くなります。また以下のように default を書くことで、 channel にデータが入っていない場合の処理も実装できます。この場合はブロッキングされません。 

select { case v := <-ch: // vを使った処理 default: // chからデータが入って来なかった } この実装は、複数の goroutine で重たい処理やブロッキングする処理を実行しつつ、 main 関数側でそれらの goroutine からデータ入力がない間にも他のことをしたいといったケースでとても便利です。 

for { select { case ev := <-keyCh: // キー入力があった case ev := <-recvCh: // 受信データがあった default: // 何も更新がないのでアニメーションを更新する } } 

# **3.6 Goモジュールとは** 

Go モジュールとは、複数のパッケージから構成された「依存を管理するためのしくみ」であり、そのしくみを利用する単位でもあります。ユーザーは、ユーザーが提供する複数のパッケージに対してそれが依存するパッケージを管理できます。本章冒頭の **リスト** 3.1を実行すると go.mod は以下のようになっています。 

module my-app go 1.20 

次に main.go を以下のように変更します。 

package main import ( "fmt" "github.com/mattn/go-runewidth" ) func main() { fmt.Println(runewidth.StringWidth("こんにちは")) } 

ここで my-app は go-runewidth に依存することになります。このまま実行してもコンパイルエラーが発生します。これは my-app が go-runewidth に依存しているにもかかわらず、 go.mod にそれが明記されていないからです。以下のコマンドを実行して go.mod を更新しましょう。 

#### $ go get github.com/mattn/go-runewidth 

すると go.mod が以下のように変更されるはずです。 

module my-app 

go 1.20 

require ( github.com/mattn/go-runewidth v0.0.13 // indirect github.com/rivo/uniseg v0.2.0 // indirect ) 

go-runewidth と uniseg が追加されました。これは gorunewidth が uniseg に依存しているためです。ソースコードに新しい外部のパッケージを取り込みたい場合は、このように go getを使って依存を解決する必要があります。バージョンを指定して以下のように実行することもできます。 

$ go get github.com/mattn/go-runewidth@v0.0.12 

最新バージョンにバグがあったり、破壊的な変更がされたりしている場合には、バージョンを指定するのが良いでしょう。このように、追加したパッケージや消したパッケージについて毎回コマンドを実行するのは面倒です。ましてや複数のパッケージを追加した場合には大変です。そのような場合は以下のコマンドを実行すると便利です。 

#### $ go mod tidy 

このコマンドを実行すると、追加されたり削除されたりしたパッケージをすべて解決しようとします。セマンティックバージョンの範囲の中で互換性のある最新バージョンが使われますので、もしかするとソースコードが期待しているバージョンと最新バージョンでインターフェース上の差異があるかもしれません。その場合は、前述のようにバージョンを指定しなおすと良いでしょう。 

go.mod に記述された Go のバージョンは、そのバージョンよりも後に導入された機能を拒否することを示します。このバージョンを修正する場合は以下を実行します。 

#### $ go mod edit -go=1.16 

go buildは通常、 go.mod と go.sum に書かれた依存情報により、必要なバージョンのモジュールをダウンロードしてビルドしますが、 go.mod と同じディレクトリにある vendor というディレクトリに置かれたモジュールのソースコードは優先して参照されます。たとえば、ネットワーク通信ができないような環境でビルドしたり、特定のモジュールにローカルでパッチを当ててビルドしたりしたいといったケースで vendor は便利です。以下のコマンドを実行すると、 go.mod と go.sum に書かれたモジュールが一括で vendor ディレクトリにダウンロードできます。 

$ go mod vendor 

## **<u>go.modの配置</u>** 

go.mod にはそのモジュールが参照するすべての依存パッケージが記載されます。たとえば、 examples ディレクトリにあるサンプルコードが、本体には必要ないパッケージを使っていた場合でも追記されます。パッケージ本体しか使わないのに、 examples にあるプログラムが参照しているからという理由だけで、パッケージの利用者がその依存物をダウンロードさせられるのは困ります。このような場合には、 examples ディレクトリの直下に go.mod を配置することで、パッケージの利用者に、 examples が利用する依存パッケージをダウンロードさせないようにできます。 



## **replace** 

さて、前述で examples に go.mod を配置する話をしましたが、こうすると examples が使うパッケージ本体( foobar.go ) も依存扱いになってしまいます。たとえばパッケージ本体に修正を入れたとしても、 examples の中では古いパッケージを参照したままになります。以下を実行し、 examples が参照している foobar は上位のディレクトリにあるものと同じであることを明示すると良いでしょう。 

$ go mod edit -replace github.com/mattn/foobar=.. 

これを実行すると go.mod に以下が追記されます。 

module examples 

require ( github.com/mattn/foobar v0.0.1 // indirect ) 

replace github.com/mattn/blahblah = .. 

こうすることで、 examples で使われる blahblah は、 github.com/mattn/blahblah からではなくローカルディレクトリにあるものが使われます。まだgit pushしていない、ローカルで開発中のソースコードでも実行できるので有用です。 

## **<u>go install</u>** 

go installコマンドを使うと、パッケージをビルドしてインストールできます。パッケージをインストールすると、以降そのパッケージを使ったプロジェクトをビルドする際に、再度ビルドされることがありません。また、 main パッケージを含んだパッケージをインストールすると、実行ファイルがインストールされます。コマンドラインプログラムを配布する際には、実行ファイルそのものを配布することもできますが、 Go コンパイラをインストールしているユーザーに対しては、このgo installコマンドを使ってインストールしてもらうこともできます。 

$ go install github.com/mattn/twty@latest 

「 latest 」はバージョンを意味します。特定バージョンのコマンドをインストールする場合は以下のようになります。 

$ go install github.com/mattn/twty@v0.0.12 

go getコマンドはカレントディレクトリにある go.mod を変更しますが、go installは go.mod を変更することはありません。 

# **3.7 プロジェクトレイアウト** 

Go のプロジェクトのディレクトリ構成にはいろいろと流儀があります。すべての流儀を紹介することはできませんので一例を紹介します。以下のスタイルはその中でもよく使われるスタイルで、筆者も使っています。プロジェクトのルートディレクトリにパッケージの本体のソースコードを置き、パッケージ分割が必要な場合はディレクトリを作成してサブパッケージとし、実行ファイルを提供する場合は cmd 配下に実行ファイル名のディレクトリを作成して main.go を置くスタイルです。 

Go コンパイラはgo buildを引数なしで実行した場合、カレントディレクトリ名をもとに実行ファイル名を決定します。カレントディレクトリに go.mod がある場合、 go.mod に記載されたパスのファイル名部分が実行ファイル名に使われます。 cmd/blahblah のように go.mod が存在しない場合はディレクトリ名が使われます。上記の例であれば blahblah が実行ファイル名になります。 

# **3.8 lint文化** 

Go にはコンパイル時の警告がありません。正常にコンパイルされるか、コンパイルエラーか、どちらかしかありません。ただし Go では lint により、ソースコードを静的解析して、コーディング上で相応しくない書き方を検出するのが一般的です。他のプログラミング言語では lint を通すことがユーザーの選択に委ねられる状況と比べると、 Go では lint を通すことがマナーの一種と扱われており、これは文化と言って良いと筆者は考えています。 

## **<u>go vet</u>** 

go vetは Go に標準で付属する静的解析ツールです。 

package main import ( "fmt" ) func main() { fmt.Printf("%s %s", "hello") } 

上記のソースコードがあるディレクトリでgo vetを実行すると静的解析した結果が表示されます。 

$ go vet # go-book/vet ./main.go:8:2: fmt.Printf format %s reads arg #2, but call has 1 arg 

この例は fmt.Printf に指定した書式が 2 つの引数を要求しているのに対し、 1 つしか指定されなかったことを検出しています。 

lint を使うと関数のコメントが記述されていないことに関しても警告が出力されます。 

## **revive** 

Go はもともと標準で lint 機能をサポートしており、 golint と呼ばれていました。⻑い間、 Go のユーザーに使われてきましたが、保守が難しくなり 2020 年 6 月に開発継続を断念しました。ひとまずgo vetと golint だけ通しておけば一定の品質は確保できるという理由でデファクトスタンダードであったため、多くの Go ユーザーが CI で利用してきました。それ以降、サードパーティ製の lint ツールがいくつか誕生しました。その 1 つが reviv ~~e~~ <u>注3.6</u> ~~で~~ す。 revive は golint の後継と言って良いでしょう。これまでの golint の機能や性能を維持しているだけでなく、 6 倍速くなりカスタマイズも可能になっています。 

## **<u>golangci-lint</u>** 

もう 1 つ紹介するのが golangci-lin ~~t~~ <u>注3.7</u> ~~で~~ す。 Go の静的解析ツールは本当にたくさん存在するのですが、それらをひとまとめにして実行してくれるツールです。 Go のプロジェクトを GitHub Actions のような CI でテストする際にはよく使われます。 

**注** 3.6 <u>https://github.com/mgechev/revive</u> (本文に戻る) 

**注** 3.7 <u>https://github.com/golangci/golangci-lint</u> (本文に戻る) 

# **3.9 go fmt** 

go fmtも Go らしい文化です。 Go では標準でソースコードフォーマッタが用意されています。 

Go のフォーマッタには設定項目がありません。いろいろなプログラミング言語でコードフォーマッタが提供されていますが、その多くには設定項目が用意されています。ユーザーの好みに合わせてインデント幅や、括弧の位置などが変更できます。しかしそれらを許してしまうと、無駄な手直しが発生したり、自転車置き場の議論に発展したりしかねません。 1 つに決まったルールを皆が認め、それを使い続けることにより、大切な時間を無駄に消費することが少なくなる、ということを我々はgo fmtに教えてもらったと筆者は思っています。 

# **3.10 まとめ** 

Go の基本的な文法、標準ライブラリやツールを使ったベストプラクティスを解説しました。おおよそ Go がどのような文法を扱い、どのように開発していくのかを理解いただけたと思います。これらは普段 Go を使ううえで必要になる要素です。何度も使っていく内に手に馴染んでくると思います。 

**COLUMN おまじない** 

ダック・タイピングは便利ではあるものの、逆に型がインターフェースを実装しているかの保証はしてくれません。もちろん、そのインターフェースを利用するコードがあればコンパイルエラーを起こしてくれますが、提供するものがライブラリである場合にはテストを実行しないと気づけないことになります。そこで、おまじないとして次のようにインターフェースIの型を持つ変数_に型fooのポインタ型nilを代入しておくことで、わざわざコンパイルをしなくても、IDEがエラーを検知して間違いに気づくことができます(_は特別な変数であり、使用することはできません)。 



<!-- Start of picture text -->
 package zoo<br> var _ I = (*foo)(nil)<br> type I interface {<br>     doSomething()<br> }<br> type foo struct {<br> }<br> func (f *foo) doSomething() {<br>このようにしておくことで、万が一インターフェース<br>に変更が入ったとしてもすぐに気づくことができます。<br><!-- End of picture text -->



# **4.1 ビルトイン関数** 

## **<u>printとprintln</u>** 

どちらも Go のビルトイン関数です。引数の値を表示しますが、標準エラー( stderr )に対する出力となります。 

<mark>println("Hello World")</mark> 

Go では print や println はあくまでデバッグ用のメッセージ出力関数という位置付けになっています。標準出力( stdout ) に出力したい場合は fmt パッケージを使います。デバッグや、 fmt を import するのも面倒になる程度の(人に伝えるためだけの)書き捨てコードでは print/println が使われますが、本番のソースコードには残すべきではありません。 

## **<u>panicとrecover</u>** 

Go には C 言語の assert に相当する panic 関数があります。例外を発生させてプログラムを強制停止します。ただしこの例外は、言語仕様として例外をサポートしているプログラミング言語、たとえば Java や C# の例外と同じ機能ではありません。あくまでプログラムを強制停止するためのものです。通常は「これ以上プログラムが動作してはいけない」といったケースでのみ使います。当然、ユーザーに正しいエラーメッセージを伝えるのには向きません。 

if mustExists(file) { panic("The file must exists. Something wrong.") } 

このコードを実行するとスタックトレースが出力されます。 

panic: The file must exists. Something wrong. goroutine 1 [running]: main.main() /home/mattn/dev/go-sandbox/panic/main.go:10 +0x27 exit status 2 

Go では復旧可能な例外と復旧不可能な例外があります。下記のようなゼロ除算や nil ポインタのデリファレンス、配列の境界外アクセスなどはランタイムで panic が呼び出されます。 



#### **ゼロ除算** 

var n int println(1 / n) 



#### **nilポインタのデリファレンス** 

var p *int println(*p) 



#### **配列の境界外アクセス** 

var a [2]int n := 2 println(a[n]) 

これらの panic は recover により復帰することができます。 recover は defer の中で呼び出さなければなりません。 

package main import "fmt" func main() { defer func() { if e := recover(); e != nil { fmt.Println(e) } }() var a [2]int n := 2 println(a[n]) } 

このプログラムを実行すると、配列の境界外アクセスによりランタイムで panic が発生し、 main 関数が強制終了しようとします。 defer により後処理が実行されますが、 recover によりランタイム panic を捕捉して復帰することができます。 recover から返される値はinterface{}型( any )であり、何が格納されているかはわかりません。 **リスト** 4.1のように、文字列を引数に panic 関数を呼び出した場合には recover から返される値には文字列が格納されます。 

#### **リスト4.1 recoverから返される値には文字列が格納される** 

package main import "fmt" func main() { defer func() { if e := recover(); e != nil { 

fmt.Printf("%[1]T: %[1]s\n", e) // string: my error } }() panic("my error") } 

一方、ランタイム panic の場合は error 型の値が格納されます ( **リスト** 4.2)。 

**リスト4.2 recoverから返される値にはerror型の値が格納される** package main import "fmt" func main() { defer func() { if e := recover(); e != nil { fmt.Printf("%[1]T: %[1]s\n", e) // runtime.boundsError: runtime error: index out of range [2] with length 2 } }() var a [2]int n := 2 println(a[n]) } 

通常、意図しない panic が起き得るということは、そのプログラムにはバグがあることを意味するため、不用意に recover を呼び出すべきではありません。プログラムは落ちるべくして落ちたほうが良く、それを止めるべきではありません。 nil ポインタをデリファレンスしてしまう可能性があるのであれば事前にポインタが nil かをチェックすべきですし、ゼロ除算が行われる可能性があるのであれば事前に除数がゼロかをチェックすべきですし、配列境界外にアクセスしてしまう可能性があるのであれば事前に配列サイズをチェックすべきです。そしてそういった、プログラマーが意図しない値を受け取り得る関数は戻り値に error を含むべきです。 

## **COLUMN** 

# **Explicit is better than implicit, Simple is better than complex** 

たとえば、引数として2つの値を取り、浮動小数点の値として割り算した結果を表示するプログラムを考えてみてください。いくつエラーが発生する可能性があるでしょうか。 

1つだと思った方は、RubyかPerlをメインで扱っておられるプログラマーかもしれません。以下のRubyのソースコードを見てください。 

<mark>puts ARGV[0].to_f / ARGV[1].to_f</mark> 

よくプログラムは短いほうが良いと力説される方がいます。本当にそうでしょうか? このプログラムに引数を与えなかった場合はどうなりますか? 1つだけ与えた場合はどうなりますか? 数値でない引数を与えた場合はどうなりますか? 除数が0の場合はどうなりますか? Goの答えは **リスト** 4.Aです。 

#### **リスト4.A 引数を割り算し浮動小数点の値として結果を表示するプログラム** 

#### package main 

import ( "errors" "fmt" "os" "strconv" ) func divide(a, b float64) (float64, error) { if b == 0 { return 0, errors.New("Divisor must not be zero") } return a / b, nil } 

func main() { if len(os.Args) != 3 { 

fmt.Fprintln(os.Stderr, "Number of arguments must be 2") os.Exit(1) } a, err := strconv.ParseFloat(os.Args[1], 64) if err != nil { fmt.Fprintf(os.Stderr, "First argument must be float value: %v", err) os.Exit(1) } b, err := strconv.ParseFloat(os.Args[2], 64) if err != nil { fmt.Fprintf(os.Stderr, "Second argument must be float value: %v", err) os.Exit(1) } result, err := divide(a, b) if err != nil { fmt.Fprintf(os.Stderr, "Invalid argument: %v", err) 

os.Exit(1) } fmt.Println(result) } 

面倒に感じる方もいるかもしれません。もしかすると、この冗⻑とも思えるソースコードに拒否反応を示す方もいるかもしれません。しかし考えてみてください。たとえばこのプログラムを知らない人に渡して実行してもらったとしましょう。ユーザーがどんな引数でコマンドを実行するかはわかりません。すべての発生し得るエラーに対してユーザーに原因を伝えたいのであれば、すべてのエラーを明示的に捕捉すべきです。 

GoではZen of Pytho ~~n~~ <u>注4.A</u> ~~に~~ 含まれる2つの考え方を重視しています注4.B ~~。~~ 

**Explicit is better than implicit** 明示的であることは暗黙的であることよりも優れている 



**Simple is better than complex** 単純であることは複雑であることよりも優れているここで言う単純(Simple)は簡単(Easy)とは異なります。言語仕様が単純であるからこそ実装するのが簡単になるというのがGoの狙いです。もちろんGoでも、上記のRubyと同じようなプログラムを実装することはできます( **リスト** 4.B)。 

#### **リスト4.B リスト4.Aを短くしたプログラム(エラーは十分に伝えられず)** 

package main 

import ( 

"fmt" "os" "strconv" ) 

func main() { defer func() { if e := recover(); e != nil { fmt.Fprintln(os.Stderr, e) } }() a, _ := strconv.ParseFloat(os.Args[1], 64) b, _ := strconv.ParseFloat(os.Args[2], 64) fmt.Println(a / b) } 

ずいぶん短くなりました。しかし、発生し得るエラーをユーザーに伝えることはできなくなりました。Goはエラーを明示的にハンドリングすることの重要さを、言語仕様として伝えています。エラーが起き得る関数では errorを返すべきです。では、Goは毎回このようなプログラムを書かなければならないのか、と億劫に感じる方もいるかもしれません。うまくアイデアを絞り出すことができたら、プログラムは短くなるかもしれませんね( **リスト** 4.C)。 

**リスト4.C リスト4.Aを短くしたプログラム(エラーを十分に伝える)** 

package main 

import ( "errors" "fmt" "os" 

"strconv" 

) 

func divide(a, b float64) (float64, error) { if b == 0 { 

return 0, errors.New("Divisor must not be zero") } return a / b, nil } 

func exitIf(err error, message string) { if err != nil { fmt.Fprintf(os.Stderr, "%s: %v", message, err) os.Exit(1) 

} } 

func main() { if len(os.Args) != 3 { fmt.Fprintln(os.Stderr, "Number of arguments must be 2") os.Exit(1) } a, err := strconv.ParseFloat(os.Args[1], 64) exitIf(err, "First argument must be float value") b, err := strconv.ParseFloat(os.Args[2], 64) exitIf(err, "Second argument must be float value") result, err := divide(a, b) exitIf(err, "Invalid argument") fmt.Println(result) } 

**注** 4.A <u>https://peps.python.org/pep-0020/</u> (本文に戻る) 

**注** 4.B <u>https://go.dev/talks/2012/zen.slide#12</u> (本文に戻る) 

## **init関数** 

Go には init という特殊な関数が存在します。 init 関数は、そのパッケージに含まれるグローバル変数が初期化されたタイミングで自動的に呼ばれます。 

package main 

var name = "John" func init() { println("Hi! " + name) } func main() { println("Hello! " + name) } 

このソースコードを実行すると「 "Hi! John" 」のあと「 "Hello! John" 」が表示されます。また、 import するパッケージも同様です。 

package main import ( _ "my-init/init1" _ "my-init/init2" _ "my-init/init3" ) 

func main() { } 

import している init1 、 init2 、 init3 にそれぞれ init 関数がある場合、 main 関数が呼ばれる前にそれらが呼び出されます。この init 関数は import されるパッケージ側で作用するため、ブランク import とともに使われることがあります。ブランク import は import したプログラムソース内ではそのパッケージを明示的に使用しないが、 import することで機能を利用することを宣言する目的で使われます。たとえば、 Go でデータベースを利用する際には database/sql パッケージと、データベースドライバを import する必要がありますが、以下のようにドライバをブランク import して利用します。 

import ( "database/sql" _ "github.com/lib/pq ) 

pq では init 関数を利用して import された際に sql.Register 関数を使用してドライバ「 postgres 」を登録しています。以下は pq のソースの一部です。 

func init() { sql.Register("postgres", &Driver{}) } 

# **4.2 パッケージ** 

## **fmtパッケージ** 

fmt パッケージはアプリケーションを開発する際に、恐らく一番よく使うパッケージでしょう。 Go で数値を文字列に変換するためには strconv パッケージを使用しますが、もっと簡単に行う方法として fmt パッケージが使われます。 

x := 1 s := fmt.Sprint(x) 

また、書式を使うこともできます。 

x := 1 s := fmt.Sprintf("%05d", x) 

標準出力にそのまま出力することもできます。 

x := 1 fmt.Printf("%05d\n", x) 

C 言語を扱ったことがある方であれば馴染み深いと思います。開いたファイルに対して出力することもできます。 

f, err := os.Create("output.dat") if err != nil { 

panic(err) } defer f.Close() fmt.Fprintf(f, "COUNT %05d\n", count) 

fmt パッケージの書式により、 struct の中の情報を出力することもできます( **リスト** 4.3)。 

**リスト4.3 structの中身をfmtパッケージで出力** package main import ( "fmt" ) type F struct { Name string Age  int } func main() { f := &F{ Name: "John", Age:  20, } fmt.Printf("%v\n", f) } 

実行すると以下が出力されます。 

&{John 20} 

**リスト** 4.3の書式を以下のように変更してみましょう。 

<mark>fmt.Printf("%+v\n", f)</mark> 

フィールド名とその内容が出力されました。 

&{Name:John Age:20} 

さらに書式を変更してみましょう。 

<mark>fmt.Printf("%#v\n", f)</mark> 

struct 名も出力されるようになりました。 

&main.F{Name:"John", Age:20} 

型名を出力したい場合には %T を使います。 

fmt.Printf("%T\n", f) fmt.Printf("%T\n", *f) 

以下のように出力されます。 

*main.F main.F 

このように fmt パッケージには、 C 言語の printf にはない便利な書式が用意されています。 



**Stringerインターフェース** 

Go では Stringer インターフェースを使うことで、 fmt パッケージの関数に与えた引数を明示的な文字列として出力することができます。 Stringer インターフェースは以下の構造になっています。 

type Stringer interface { String() string } 

型としてインターフェースを実装しておくと、 fmt パッケージのいくつかの関数に引数として与えた際に、出力される文字列をカスタマイズすることができます( **リスト** 4.4)。 

#### **リスト4.4 Stringerインターフェースの使用例** 

package main import ( "fmt" ) type F struct { Name string Age  int } func (f *F) String() string { return fmt.Sprintf("NAME=%q, AGE=%d", f.Name, f.Age) } func main() { f := &F{ Name: "John", Age:  20, } fmt.Printf("%v\n", f) } 

このソースコードを実行すると以下が出力されます。 NAME="John", AGE=20 

Go で enum を実現するには型付きの const を使うことが多いのですが、 **リスト** 4.5のような switch 文を毎回書くのはとても手間ですし、抜け漏れが発生する恐れがあります。 

#### **リスト4.5 型付きのconstでenumを実現** 

type Fruit int const ( Apple Fruit = iota Orange Banana ) func (i Fluit) String() string { switch i { case Apple: return "Apple" case Orange: return "Orange" case Banana: return "Banana" } return fmt.Sprintf("Fruit(%d)", i) } 

このような手間を省くために、 Go には stringer というコマンドが用意されています。以下の手順でインストールして下さい。 

$ go install golang.org/x/tools/cmd/stringer@latest 

**リスト** 4.5のソースコードのファイル名が fluit.go である場合には以下のように実行します。 

#### $ stringer -type Fruit fruit.go 

-type で指定している Fluit は型名です。実行すると **リスト** 4.6のソースコード fruit_string.go が出力されます。 

**リスト4.6 リスト4.5からstringerコマンドで生成した fruit_string.go** 

// Code generated by stringer -type Fruit fruit.go; DO NOT EDIT package main import "fmt" const _Fruit_name = "AppleOrangeBanana" var _Fruit_index = [...]uint8{5, 11, 17} func (i Fruit) String() string { if i < 0 || i >= Fruit(len(_Fruit_index)) { return fmt.Sprintf("Fruit(%d)", i) } hi := _Fruit_index[i] lo := uint8(0) if i > 0 { lo = _Fruit_index[i-1] } return _Fruit_name[lo:hi] } 

このファイルを一緒にビルドすると、 switch 文を自分で実装することなく const の値が文字列で表示されます。毎回コマンドの実行方法を調べなくてもいいように、go generateで実行されるようにしておくと良いでしょう。 

<mark>//go:generate stringer -type Fruit fruit.go</mark> 

上記のように go:generate を記述しておくと、以下のコマンドで、記述したコマンドが実行されます。 

$ go generate 

## **logパッケージ** 

log パッケージは簡単なログのしくみを提供します。日付と文字列のみが出力されます。他のプログラミング言語にある、 INFO/WARN/ERROR といったログレベルもサポートしていません。また出力形式も JSON などは選べません。それらが必要な場合は、サードパーティライブラリを使う必要があります。 

log パッケージでよく使う関数は以下の 2 種類です。 





**log.Print(Printf、Println) log.Fatal(Fatalf、Fatalln)** 

log パッケージは標準エラーに出力しますが、 SetOutput を使うことで出力先をファイルに変更することもできます。 

f, err := os.OpenFile(filename, os.O_APPEND|os.O_WRONLY|os.O_CREATE, 0600) if err != nil { panic(err) } log.SetOutput(f) log.Println("app started") 

log パッケージの出力関数は、改行で終了していない文字列を渡すと必ず改行が付け足されるようになっています。 Printf はフォーマット付きの出力を行います。 

<mark>log.Printf("cannot open file: %v", err)</mark> 

fmt パッケージにある Printf ( Sprintf )と扱いは同じですので、フォーマットの書式については fmt パッケージのドキュメントを参照してください。また、 Fatal はスタックトレースを出力したあとに、プログラムが強制終了するようになっています。この動作(強制終了すること)に戶惑う方が多いようですが、これは Fatal という名前が他のログ出力ライブラリのログレベルを連想してしまうからだと思われます。 log パッケージにはログレベルのようなものは存在せず、単純にログ出力を行う Print/Printf/Println と、ログ出力とともにプログラムを強制終了する Fatal/Fatalf/Fatalln が存在するという認識をしておけば問題ありません。プログラムの設定ファイルの読み込みや初期処理など、強制的に終了させても良いケースの場合に使うと便利です。 

## **encoding/jsonパッケージ** 

JSON をエンコード/デコードするには encoding/json パッケージを使用します。 Go の JSON の取り扱い方は、他のプログラミング言語と比べて特徴的です( **リスト** 4.7)。 

#### **リスト4.7 JSONパッケージの使用例** 

package main import ( "encoding/json" "fmt" ) var content = ` 

{ "species": "ハト", "decription": "岩にとまるのが好き", "dimensions": { "height": 24, "width": 10 } } ` type Dimensions struct { Width  int `json:"width"` Height int `json:"height"` } type Data struct { Species     string     `json:"species"` Description string     `json:"description"` Dimensions  Dimensions `json:"dimensions"` } func main() { var data Data err := json.Unmarshal([]byte(content), &data) if err != nil { log.Fatal(err) } } 

Go の struct には「タグ」と呼ばれるメタ情報を付与することができます。タグは空白で区切られた複数の属性を持つことができます。 

<u>属性名1:"属性値1" 属性名2:"属性値2"</u> 

encoding/json はこのタグを参照し、 JSON のフィールド名を取り扱います。上記の例では、 Dimensions の Width には JSON 上では width という属性名が割り当てられます。 Go では外部のパッケージに対してフィールドを公開するためには先頭を大文字にしないといけませんが、このタグを使うことでオブジェクトのキー名が小文字の JSON も扱えるようになります。通常、固定の JSON 文字列をパースする場合には json.Unmarshal を使いますが、ネットワーク通信のストリームやファイルなどを扱う場合はデコーダを使います。 

f, err := os.Open("input.json") if err != nil { log.Fatal(err) } defer f.Close() var data Data err := json.NewDecoder(f).Decode(&data) if err != nil { log.Fatal(err) } 

これにより、巨大なデータであっても入力データとパース済みの struct の両方にメモリが確保されることがありません。 json.Decoder はストリームを扱うことができるため、たとえば NDJSON ( Newline Delimited JSON )をネットワークで受信するときに便利です。 

var data Data dec := json.NewDecoder(f) scanner := bufio.NewScanner(conn) for scanner.Scan() { line := scanner.Text() 

// 行ごとにJSONをデコード err := dec.Decode(&data) if err != nil { break } doSomething(&data) } 

## **netパッケージ** 

Go はネットワークを扱いやすいプログラミング言語と言われています。それは、一般的にネットワーク通信はストリームを扱うことが多く、またすべての個別のエラーに対して個々の異なる対処を求められることが多いからです。たとえば、接続エラーに対してはリトライ、送受信は再接続、など要件が異なってきます。たとえばこれらが例外を送出する場合、個々に try/catch といった例外をハンドルするコードが必要になりますが、 Go のように愚直にエラーを都度処理する文法は、そういったネットワークの処理を書くのに向いていると言って良いでしょう。またサーバアプリケーションの場合、 Accept 後には個々のクライアントと個別に通信することになります。そういった場合には Go の goroutine がとても役立ちます( **リスト** 4.8)。 

#### **リスト4.8 netパッケージの使用例** 

package main import ( "fmt" "log" "net" ) 

func handleData(conn net.Conn) { defer conn.Close() 

var b [512]byte for { n, err := conn.Read(b[:]) if err != nil { break } fmt.Print(string(b[:n])) } } func main() { lis, err := net.Listen("tcp", ":8888") if err != nil { log.Fatal(err) } defer lis.Close() for { conn, err := lis.Accept() if err != nil { log.Fatal(err) } go handleData(conn) } } 

このサーバアプリケーションでは接続数が制限されていませんが、第 3 章で述べたとおり goroutine は軽量スレッドとして動作するため、接続数が多くなってもスレッドによる実装ほどパフォーマンスが劣化しません。これはサーバアプリケーションに限る話ではありませんが、通信を行うアプリケーションでは多くのケースで想定外のエラーが発生し得ます。 







### **受信ポートが開けない相手のクライアントが接続を切った受信中にエラーが発生した** 

サーバアプリケーションが強制終了してしまわないようにするには、あらゆる対応をする必要があります。 Go では EINTR による中断の対処や EAGAIN による再試行は標準ライブラリの中で行われるため、リソースの解放が必須であるケースを除いては、 Accept が失敗した際には log.Fatal で強制終了してもかまいません。 

## **timeパッケージ** 

time パッケージには時刻や経過時間を扱うのに便利な関数が含まれています。 



### **time.Format/time.Parse** 

time パッケージは Go の数ある一風変わった仕様の中でも、特徴的なものとなっています。他のプログラミング言語では日付時刻の書式にyyyy/MM/dd HH:mm:ssを使うことが多いと思います。 Go ではあえてこの書式を使わず、見た目から書式を推測できる仕様になっています。たとえば、現在の日付時刻を表示するプログラムは以下になります。 

now := time.Now() fmt.Println(now.Format("2006/01/02 15:04:05")) 

初めて見た方はビックリすると思います。これは英語圏の人たちが、月/日/年という順で日付を表記するところから決められていますが、数字それぞれには意味がなく、「 1 月 2 日 3 時 4 分 5 秒 2006 年( 3 時は「 15 時」表記)」と連番になっており、おおよそ **表** 4.1のように理解すると覚えやすくなっています。取り扱いづらそうに見えますが、多くの場合は time パッケージに標準で用意されている固定の書式名を使うことができます( **表** 4.2)。 



Parse や Format のフォーマット書式として以下のように使用します。 

package main import ( "fmt" "time" ) 

func main() { now := time.Now() 

fmt.Println(now.Format(time.RFC3339)) } 

Format と同様に、日付や時刻の文字列をパースする際にもこの書式を使います。 

var s = "2022/12/25 07:42:38" d, err := time.Parse("2006/01/02 15:04:05", s) 

yyyy/MM/dd HH:mm:ssであっても実際は「あれ、月が大文字だっけ、時刻が大文字だっけ?」や「 hh は小文字だっけ、大文字だっけ?」といったように、実は覚えられていないことの方が多いのです。その点、 Go のこの独特な書式は 2006 と 01 を覚えておけば、あとは連番になっているという記憶だけで書式を思い出せます。 

Go の time パッケージのこの独特なフォーマットは、他のプログラミング言語が使用するyyyy/MM/dd hh:mm:ss や%Y/%m/%dと大きく異なるため、議論されることがあります。これは筆者の考えですが、 MM と mm のどちらが月でどちらが分なのか、 12 時間記法はどうだったか、タイムゾーンはどうやって記述するかといった覚えることの量は、 Go の書式と比べてもそれほど変わらない、もしくは Go のほうが少ないと感じています。また、見た目がそのままレイアウトになっているので、表示がイメージしやすい、画面に表示される際の幅が考慮しやすいといったメリットもあると思っています。 



### **time.Duration** 

time パッケージには Duration という型が用意されています。時刻ではなく経過時間を保持できる型です。 3s は 3 秒間、 4m は 4 分間、 5h は 5 時間、といった感じに扱うことができます。 time.ParseDuration でパースすることもできます。 

d, err := time.ParseDuration("3s") if err != nil { log.Fatal(err) } fmt.Println(d) // 3s d, err = time.ParseDuration("4m") if err != nil { log.Fatal(err) // 4m0s } fmt.Println(d) d, err = time.ParseDuration("5h") if err != nil { log.Fatal(err) } fmt.Println(d) // 5h0m0s time.Duration は四則演算も可能です。 d, err := time.ParseDuration("3s") if err != nil { log.Fatal(err) } fmt.Println(d * 3) // 9s 

time.Duration は四則演算も可能です。 

Duration 型の定数は time.Nanosecond を「 1 」として、以下のように定義されています。 

const ( Nanosecond  Duration = 1 Microsecond       = 1000 * Nanosecond Millisecond       = 1000 * Microsecond Second            = 1000 * Millisecond Minute            = 60 * Second 

Hour              = 60 * Minute ) 

3 秒であればtime.Second * 3と書くことができます。この 3s や 4m といった記法は、ユーザーから経過時間を指定してもらう際にとても便利で、たとえば UNIX の sleep コマンドにも採用されています。 

$ sleep 3s 

## **<u>pathパッケージとpath/filepathパッケージ</u>** 

Go を扱ううえで、筆者がとても重要視しているパッケージです。 path パッケージは URL など仮想的なパスを扱う場合に使います。一方 path/filepath は物理的なパスを扱う場合に使います。物理的なパスの操作に path パッケージを使ってはいけません。逆に仮想的なパスの操作に path/filepath パッケージを使ってはいけません。 

path パッケージには **表** 4.3の関数が用意されています。いずれも、実際に物理的なファイルが存在しなくても処理が可能な関数です。一方 path/filepath パッケージはこれらに加え、物理パスの操作に必要な **表** 4.4の関数が用意されています。 



path パッケージのパス区切りは常に/です。一方 path/filepath パッケージのパス区切りは OS により異なります。 Windows であればバックスラッシュです。 filepath.Clean を呼び出すと、パスの中に含まれる/は OS パスセパレータで置換されます。 

println(filepath.Base(`C:/path/to/file.txt`)) // file.txt 

println(filepath.Dir(`C:/path/to/file.txt`)) // C:\path\to 

println(filepath.Clean(`C:/path/to/..\file.txt`)) // C:\path\file.txt 

println(filepath.Ext(`C:/path/to/file.txt`)) // .txt 

println(filepath.IsAbs(`C:\path\to\file.txt`)) // true println(filepath.IsAbs(`.\file.txt`)) // false 

println(filepath.Join(`C:/path`, `to/file.txt`)) // C:\path\to\file.txt 

absolute, err := filepath.Abs(`../file.txt`) if err == nil { 

println(absolute) // カレントディレクトリがC:\path\toであれば C:\path\file.txt } absolute, err := filepath.Rel(`C:\path`, `C:\path\to\file.txt`) if err == nil { println(absolute) // to\file.txt } 

println(filepath.VolumeName(`C:\path\to\file.txt`)) // C: println(filepath.VolumeName(`\\server\share\file.txt`)) // \\server\share // UNIXでは空文字列が返りますこのように、 filepath パッケージの変換関数は、自動で OS パスセパレータにより整形された値を返します。 filepath.ToSlash と filepath.FromSlash を使うと、 OS パスセパレータと/セパレータを変換することができます。 UNIX では何も起きません。 

s := `C:\path\to\file.txt` s = filepath.ToSlash(s) println(s) // C:/path/to/file.txt s = filepath.FromSlash(s) println(s) // C:\path\to\file.txt 

指定ディレクトリ配下のファイルを再帰的に探索する場合は WalkDir を使います( **リスト** 4.9)。 

**リスト4.9 path/filepathパッケージの使用例(WalkDirを利用)** package main import ( "fmt" "io/fs" 

"log" "os" "path/filepath" ) func main() { files := []string{} cwd, err := os.Getwd() if err != nil { log.Fatal(err) } err = filepath.WalkDir(cwd, func(path string, info fs.DirEntry, err error) error { if err != nil { return err } if info.IsDir() { return nil } files = append(files, path) return nil }) if err != nil { log.Fatal(err) } fmt.Println(files) } 

このソースコードはカレントディレクトリ配下のファイル一覧を返します。ディレクトリ配下の探索をスキップしたい場合はエラーとして fs.SkipDir を返します。以下の例は、ディレクトリが.で始まる隠しディレクトリの場合は探索を行わないようにするコードです。 

if info.IsDir() { if info.Name()[0] == '.' { return fs.SkipDir } return nil } 

filepath.WalkDir と似た動作をする filepath.Walk があります。こちらは filepath.WalkDir が実装される前に存在していた関数です。 filepath.Walk は検索した各エントリに対して os.Lstat を呼び出す実装になっているため、 filepath.WalkDir を使ったほうがパフォーマンスが良くなります。 

Windows ではドライブレターや UNC パスのホスト名、シェア名がパスの一部に含まれます。 path パッケージはそれらを知りませんし、パスセパレータは/のみ扱います。 path.Base や path.Dir 、 path.Join は/を判定していますし、 path.Clean も/だけを考慮します。パスセパレータとして/と\の両方を扱える Windows のパスを path パッケージで扱ってしまうと不正なパスが生成されてしまいます。 

Windows でも Linux でも macOS でもおおよそ動作するプログラムを作りたいのであれば、正しく path/filepath パッケージを使うよう心掛けてください。 

## **osパッケージ** 

os パッケージは OS に対する命令を扱うパッケージです。このパッケージは OS の動作に依存する度合がとくに強く、一部の関数は特定の OS でしか提供されない、または提供されていても動作しないものもあります。 os.Create や os.Open 、 

os.Remove/os.RemoveAll といったファイル操作やディレクトリ操作の関数が用意されています。ファイルやディレクトリを作成し、何かの操作を行ったあとに削除するといった一連の流れでよく使われます。 

f, err := os.Open("file.txt") if err != nil { return fmt.Errorf("cannot open file: %v", err) } defer f.Close() // 処理 

err := os.Mkdir(dirname, 0755) if err != nil { return fmt.Errorf("cannot create directory: %v", err) } defer os.RemoveAll(dirname) 

// 処理 

## **ioパッケージ** 

io パッケージはその名のとおり I/O を担当するパッケージです。 Go がストリーム処理をする場合、それらはほぼすべて io.Reader または io.Writer を実装しています。 io.Reader/io.Writer の定義は以下のとおりです。 

type Reader interface { Read(p []byte) (n int, err error) } 

type Writer interface { 

Write(p []byte) (n int, err error) } 

たとえば、 io.Reader かつ io.Writer は、 io.ReadWriter として定義されています。 

type ReadWriter interface { Reader Writer } 

その他、合成して定義されたインターフェースがたくさん用意されています。 fmt.Fprint の第 1 引数は io.Writer を取ります。また io.Copy の第 1 引数は io.Writer を、第 2 引数は io.Reader を取ります。ですので、以下のように自分で作成した struct などに Write メソッドを実装すると、 fmt.Fprint や io.Copy の引数として渡すことができます。 

type Foo struct { (..略..) } func (f *Foo) Write(p []byte) (n int, err error) { (..略..) } func main() { var foo Foo io.Copy(&foo, os.Stdin) } 

**contextパッケージ** 

context パッケージは Go の非同期アプリケーションを開発するうえで重要なパッケージです。これまで解説してきた goroutine の使い方は、 goroutine 側が主導権を持ち、自ら終了するものでした。しかし、たとえば goroutine を起動した側が goroutine を中断したい場合もあります。とくに一定の条件で goroutine 処理をキャンセルしたい場合や、タイムアウトで goroutine をキャンセルしたい場合などです。 goroutine 側で自ら経過時間を判定することもできますが、本来は goroutine を起動した側が制御すべきです。この機能を提供するのが context パッケージです( **リスト** 4.10)。 

#### **リスト4.10 contextパッケージの使用例** 

package main import ( "context" "fmt" "sync" "time" ) func f(ctx context.Context, wg *sync.WaitGroup) { defer wg.Done() for { select { case <-ctx.Done(): // 中断 return default: } fmt.Println("goroutine: 処理") time.Sleep(1 * time.Second) } 

} func main() { var wg sync.WaitGroup wg.Add(1) ctx, cancel := context.WithCancel(context.Background()) go f(ctx, &wg) time.Sleep(10 * time.Second) cancel() wg.Wait() } 

context.Context は習慣的に関数の第 1 引数として渡され、下層の関数に引き渡されます。タイムアウトの場合は **リスト** 4.11になります。 

**リスト4.11 contextパッケージの使用例(タイムアウト処理)** package main import ( "context" "fmt" "sync" "time" ) func f(ctx context.Context, wg *sync.WaitGroup) { defer wg.Done() for { select { case <-ctx.Done(): // 中断 return default: } 

fmt.Println("goroutine: 処理") time.Sleep(1 * time.Second) } } 

func main() { var wg sync.WaitGroup wg.Add(1) 

ctx, cancel := context.WithTimeout(context.Background(), time.Duration(5*time.Second)) defer cancel() go f(ctx, &wg) 

wg.Wait() } 

context.Context を作成する方法としては 2 つあります。 





### **context.Background() context.TODO()** 

既存の処理を context.Context を使った実装に置き換えていくのは少し大変です。呼び出し元の関数から渡された ctx をそのまま渡して良いのか、それとも新しい 

context.Background()を渡して良いのか、しばらく検討する必要があるでしょう。「 context.Context を引数に足したけれど、まだDone()を確認するような処理は実装していない」といった場合は、context.TODO()を渡しておき、少しずつ context.Background()に置き換えていくと良いでしょう。 

# **4.3 build constraints** 

Go にはマルチプラットフォーム向けのアプリケーション開発が楽になるようなしくみがあります。その 1 つが build constraints です。 C 言語のように #ifdef や #endif でプラットフォームの各実装を分けて書けるのは、確かに小さい範囲であればソースコードの見通しは良いのですが、大きくなると可読性も悪く、バグも発生しやすくなります。 

build constraints は Go のソースコードをビルドする際に、対象のソースコードを含めるかどうかを決める「条件」を指定できます。 Go では次の 2 つの規則に基づいてビルドに含むソースコードを判別しています。 





### **ファイル名による指定** 

**+build(またはgo:build)コメントによる指定** 

## **ファイル名による指定** 

次のファイルが置かれているディレクトリでgo buildを実行すると、 Windows では command.go と command_windows.go がビルドに含まれます。また Linux では command.go と command_linux.go がコンパイルされます。 





**command.go command_windows.go** 



### **command_linux.go** 

実際には次のルールにマッチしたファイルがコンパイル対象となります。 

file_${GOOS}.go file_${GOARCH}.go file_${GOOS}_${GOARCH}.go 

GOOS 、 GOARCH は Go が認識する環境変数です。通常は未指定ですが、指定することでその OS やアーキテクチャ向けの実行ファイルを生成することができます。 GOOS および GOARCH には **表** 4.5の値が設定可能です。現在 Go が認識している GOOS や GOARCH を知りたい場合にはgo envを実行すると確認できます。前述のとおり、特定の OS や CPU アーキテクチャの場合のみビルドにソースコードを含めたいといった場合に、このファイル名による指定が使用できます。それぞれの OS 向けのソースファイル( command_windows.go と command_linux.go など)を準備すれば良いことになり、存在しない GOOS はビルドされない、つまりビルドエラーが発生するということになります。 

## **+build(またはgo:build)コメントによる指定** 

もう 1 つはコメントによる指定です。ファイルの先頭にコメントとして +build (または go:build )を書いておくことで、ソースコードがビルドに含まれるかどうかを条件で指定できます。ファイル名による指定では「含まれる」条件しか指定できませんが、このコメントによる指定では「含まれない条件」も指定することができます。形式は以下のとおりです。 

//go:build <u>タグ</u> // +build タグ 

package main 

注意しなければならないのは、このコメントと package の間には必ず 1 行、空行を入れなければならないことです。以前までは +build だけ記述すれば良かったのですが、 Go 1.16 から go:build が推奨となりました。本書では古い記述方法は解説しません。古い Go のバージョンをサポートする予定がないのであれば、 go:build だけ指定しても良いでしょう。タグとその例の代表的なものを **表** 4.6にいくつか紹介します。複数のタグを AND 条件で指定したい場合には&&を、 OR 条件を指定したい場合は||を、 NOT を指定したい場合は!を使用します。()を使って複数の条件式を書くことができます。たとえば windows かつ 386 の場合は以下になります。 

<mark>//go:build windows || 386</mark> 

linux 以外、かつ 386 もしくは arm の場合は以下になります。 

<mark>//go:build !linux && (386 || arm)</mark> 

なお、 linux を指定した場合は android も対象に含まれます。 android を含まない linux を指定する場合には以下を記述してください。 

<mark>//go:build linux && !android</mark> 

これらの値は、 Go コンパイラが判別するプリセットです。上記のビルドタグはユーザーが定義することもできます。たとえばgo:build catと書かれた cat.go とgo:build dogと書かれた dog.go があり、以下のようになっていたとします。 



#### **cat.go** 

//go:build cat 

package animal 

func Name() string { return "Cat" } 



#### **dog.go** 

//go:build !cat 

package animal 

func Name() string { return "Dog" } 

これらのソースコードは-tags catフラグを付けてビルドすると cat.go が有効になり、 dog.go は無効になります。 

#### $ go build -tags cat 

逆に-tags catを付けずにビルドすると dog.go が有効になり、 cat.go は無効になります。特別な値を持つタグとしては ignore があります。 ignore を指定するとgo buildコマンドの対象から外されます。 

<mark>//go:build ignore</mark> 

このタグが付けられたファイルをビルドするには、ファイルを直接指定してgo build file.goを実行するか、go run file.goで実行することになります。このタグを付けたファイルは、タグを付けられていないファイルと干渉しないため、プロジェクトのサンプルコードとして同じディレクトリ内に、複数個の main 関数を持ったソースコードを置いておくのに使われます。 

## **COLUMN** 

# **マルチプラットフォーム向けアプリケーションの開発テクニック** 

Goはマルチプラットフォーム向けに優れたパッケージ構成になっていますが、すべてがそうとは限りません。 LinuxやWindows、macOS向けの特別な処理も当然ながら必要になります。このような場合に、各OS名で分割したファイル名を使用します。パッケージ本体には、各OSでの実装を呼び出す共通のコードを書きます。 

package foo 

func DoSomething() { doSomething() } 

そして_windows.goや_linux.goで各OS専用のコードを実装します。 



#### **foo_windows.go** 

package foo 

func doSomething() { 

// Windows専用のコード } 



#### **foo_linux.go** 

package foo func doSomething() { // Linux専用のコード } 



#### **foo_darwin.go** 

package foo func doSomething() { // macOS専用のコード } 

こうすることで、_linux.goではLinuxだけに存在する関数を呼び出したり、_windows.goではWindows APIを呼び出したり、macOSではObjective-Cの実装を個別に書いたりすることができます。また、#ifdefで起きがちなコンパイルエラーも起きにくいというメリットがあります。ここで勘の良い方は気づいたかもしれませんが、GoにはMakefileのようなものがありません。go buildはディレクトリ直下のファイルがビルドされ、go build ./...は指定したディレクトリ直下のファイルがビルドされます。どのファイルがビルドされ、またどのファイルがビルドされないかは、ファイル名やbuild tagによって決まります。 

# **4.4 cgo** 

cgo は Go から C 言語を扱うためのしくみです。 Go の標準ライブラリは機能が豊富で、 OpenSSL 相当の暗号ライブラリまで同梱されていますが、 C 言語で実装された既存の資産はたくさんあります。これらを Go から呼び出せるようにしたものが cgo です。 

**リスト** 4.12のソースコードは、 mylib.h で宣言された mylib_update_data という関数を Go から呼び出すものです。go buildを実行すると libmylib.a がリンクされます。 

**リスト4.12 cgoの使用例** 

package main /* #include <stdlib.h> #include <mylib.h> // cgo: LDFLAGS -lmylib // cgo: CFLAGS -I/usr/local/include */ import "C" import "unsafe" 

func main() { s := "index.dat" ptr := C.CString(s) 

defer C.free(unsafe.Pointer(ptr)) C.mylib_update_data(ptr) } 

//で始まる部分に書かれているのは cgo に与える環境変数で、 CFLAGS ( C++ は CXXFLAGS )でヘッダファイルの検索パスを、 LDFLAGS などでリンカへのフラグを渡すことができます。 

## **pkg-configを使って複雑なコンパイルオプションに対処** 

Windows 、 Linux 、 macOS それぞれでビルドできるようにするためには、 OS により異なるコンパイルオプションを扱わなければなりません。たとえばある C 言語のライブラリをリンクしたい場合、ライブラリ自身だけでなく、そのライブラリが必要としている別のライブラリもリンクしなければなりません。 Windows であれば、ライブラリが user32.dll に依存する場合は -luser32 を指定する必要がありますし、 Linux でリアルタイム拡張ライブラリをリンクする際には -lrt を指定しなければなりません。 

cgo では次のように各環境により異なる CFLAGS や LIBS を個別にソースコード上に記述できます。 

package mylib // #cgo CFLAGS: -DPNG_DEBUG=1 // #cgo amd64 386 CFLAGS: -DX86=1 // #cgo LDFLAGS: -lpng // #include <png.h> import "C" 

Windows と Linux でライブラリのファイル名が異なる場合には対応できますが、ライブラリがバージョンによって異なるライブラリファイル名を提供しているといったような複雑な構成の場合(たとえば古いバージョンでは -lxxx だけ、新しいバージョンでは -lxxx 、 -lyyy など)には対応できなくなります。そこで、 cgo では pkg-config を扱えるようになっています。ライブラリとして配布されるオープンソースプロジェクトでは拡張子に .pc を持つファイルが同梱されていることがあります。ライブラリをインストールすると pkg-config コマンドを使用し、そのライブラリを含めたコンパイルやリンクに必要なオプションを知ることができます。次のコマンドのように実行すると、コンパイルオプションを取得できます。 

$ pkg-config --cflags png # コンパイルオプション $ pkg-config --libs png   # リンクオプション 

pkg-config が必要なビルドオプションを教えてくれるので、 Go の開発上では OS の違いによるコンパイルオプションなどを気にする必要がありません。以下のように記述することで pkg-config を使うことができます。 

package mylib // #cgo pkg-config: png // #include <png.h> import "C" 

ソースコードにライブラリが格納されているディレクトリや、 OS により異なるコンパイルオプションを指定しなくても良くなるので、ソースコードをきれいに保つことができます。 

# **4.5 go:embed** 

プログラムを開発していると、実行ファイルを配布すると同時に、動作に必要なその他のファイルも同梱しなければならないことがあります。多くの場合は tar や ZIP などを使って配布しますが、それでも展開したあとの扱いは楽ではありません。とくにファイルが複数個ある場合には面倒になります。 

Go では go:embed というマジックコメントを使うことで、実行ファイルに他のファイルを埋め込むことができるようになっています。これまでも statik や go-assets 、 packr 、 go.rice というツールを使うことで、任意のファイルをバイナリ表現された Go のソースコードとして取り込み、一緒にビルドすることでファイルの同梱が実現できていましたが、 Go 1.16 からはそういった外部のツールを使う必要がなくなりました。 

**リスト** 4.13のプログラムは Web サーバを起動します。ブラウザから http://localhost:8989/ にアクセスすると画像が表示されるプログラムです。 

#### **リスト4.13 go:embedの使用例** 

package main 

import ( 

_ "embed" "net/http" 

"github.com/labstack/echo/v4" ) 

//go:embed static/logo.png var contents []byte 

func main() { 

e := echo.New() 

e.GET("/", func(c echo.Context) error { return c.Blob(http.StatusOK, "image/png", contents) }) e.Logger.Fatal(e.Start(":8989")) } 

変数 contents は宣言しているだけで何も代入していませんが、コメントに書かれている go:embed のパスがコンパイル時に読み取られ、プログラム実行時にバイト列として格納されます。先ほど列挙したこれまでのツールを使う場合、 static/logo.png を更新するたびにコマンドを実行してソースコードを更新する必要がありましたが、 go:embed では必要ありません。ファイルが見つからなかった場合はコンパイルエラーになります。注意するのは embed パッケージをブランク import するという点だけです。扱えるのはバイト列だけではありません。文字列も扱えます( **リスト** 4.14)。 

#### **リスト4.14 go:embedで文字列を扱う** 

package main 

import ( _ "embed" "fmt" 

) 

//go:embed message.txt var message string func main() { fmt.Println(message) } 

さらにはディレクトリも扱えます( **リスト** 4.15)。 

**リスト4.15 go:embedでディレクトリを扱う** package main import ( "embed" "io" "log" "os" "path" ) //go:embed static var local embed.FS func main() { fis, err := local.ReadDir("static") if err != nil { log.Fatal(err) } for _, fi := range fis { in, err := local.Open(path.Join("static", fi.Name())) if err != nil { log.Fatal(err) } out, err := os.Create("embed-" + path.Base(fi.Name())) if err != nil { log.Fatal(err) } 

io.Copy(out, in) out.Close() in.Close() log.Println("exported", "embed-"+path.Base(fi.Name())) } } 

このプログラムを実行すると、ビルド時に static というディレクトリ内に置かれたファイル(例: logo.png )が、実行時に embed-logo.png として生成されます。 embed.FS 型で提供されるファイルシステムは仮想的なパスを扱うため、前述のとおり path/filepath パッケージではなく path パッケージで扱う必要があります。埋め込んだファイルシステムを Web サーバから配信することもできます。 http.FS を使うことで embed.FS ファイルシステムを http.FileServer 経由でサーブすることができます( **リスト** 4.16)。 

#### **リスト4.16 go:embedでファイルシステムを扱う** 

package main 

import ( "embed" "net/http" "github.com/labstack/echo/v4" ) //go:embed static var local embed.FS 

func main() { e := echo.New() e.GET("/", echo.WrapHandler(http.FileServer(http.FS(local)))) 

e.Logger.Fatal(e.Start(":8989")) } 

このソースをビルドすると、シングルバイナリでありながら、コンテンツをサーブできる Web サーバができあがります。できあがった実行ファイルを実際に動作させるサーバに 1 つだけ配置すればデプロイが完了します。 

# **4.6 Functional Options Pattern** 

Go には関数のデフォルト引数がありません。これは Go らしい割り切りで、関数の引数とは多くのケースでは固定値ではないという点、かつ関数の引数がデフォルト値を持つということはその値がいつ計算されるべきかユーザーに明示されなければならないという点から、コードを理解するのを難しくするという理由で意図的に採用されていません。 Go にも可変個引数はあります。 

func doSomething(args ...string) { for _, arg := range args { fmt.Println(arg) } } func main() { doSomething("Hello", "World!", "Golang") } 

しかし、型が固定になってしまいます。もちろん interface{}( any )を使って型の異なる可変個引数を表現することもできますが、関数が扱える型をコンパイル時に固定することができません。そこで使うのが Functional Options Pattern です。 Functional Options Pattern を使うことで、開発者はその関数の利用者に対して、開発者が限定した型のみを可変個で渡せる関数を提供できます。 

### たとえば **リスト** 4.17の関数を用意します。 

#### **リスト4.17 ホスト名とポート番号を受け取り、Server structを返す** 

package server type Server struct { host string port int } func New(host string, port int) *Server { return &Server{ host: host, port: port, } } func (s *Server) Start() error { // do something return nil } 

この関数はホスト名とポート番号を受け取り、 Server struct を返します。 Server struct は Start メソッドを提供し、何かしらの処理を開始できます。利用者側はおおよそ **リスト** 4.18のように実装するでしょう。 

#### **リスト4.18 リスト4.17を使う実装** 

package main import ( "log" "github.com/mattn/awesome-server/server" ) 

func main() { svr := server.New("localhost", 8888) if err := svr.Start(); err != nil { log.Fatal(err) } } 

ここで、たとえばこの svr に対して以下の機能を足したいとします。 





### **接続タイムアウトロガー** 

Server struct に公開フィールドとして Timeout や Logger を足したり、 SetTimeout や SetLogger メソッドを追加したりすることはできます。しかし、利用者にタイムアウト値やロガーを任意のタイミングで変更させたくはありません。できれば New で指定した初期値から変更させないように制限したいですね。ここで利用するのが Functional Options Pattern です。まず **リスト** 4.17の Server struct を以下のように変更します。 

package server import ( "log" "time" ) type Server struct { host    string port    int timeout time.Duration 

logger  *log.Logger } 

次に、 timeout を受け取り、設定・更新する関数をクロージャとして返す WithTimeout を実装します。 

func WithTimeout(timeout time.Duration) func(*Server) { return func(s *Server) { s.timeout = timeout } } 

logger も同様です。 

func WithLogger(logger *log.Logger) func(*Server) { return func(s *Server) { s.logger = logger } } 

そして func (*Server)に別名を付けます。 

<mark>type Option func(*Server)</mark> 

あとは、この Option を New の引数で可変個に受け取れるようにします。 

func New(host string, port int, options ...Option) *Server { svr := &Server{ host: host, port: port, } for _, opt := range options { opt(svr) } 

return svr } 

### 全体では **リスト** 4.19のようになります。 

#### **リスト4.19 リスト4.17をFunctional Options Patternに則って変更** 

package server 

import ( "log" "time" ) type Server struct { host    string port    int timeout time.Duration logger  *log.Logger } 

type Option func(*Server) 

func WithTimeout(timeout time.Duration) func(*Server) { return func(s *Server) { s.timeout = timeout } } func WithLogger(logger *log.Logger) func(*Server) { return func(s *Server) { s.logger = logger } } 

func New(host string, port int, options ...Option) *Server { svr := &Server{ 

host: host, port: port, } for _, opt := range options { opt(svr) } return svr } func (s *Server) Start() error { if s.logger != nil { s.logger.Println("server started") } // do something return nil } 

呼び出し側は **リスト** 4.20のように実装することができます。 

**リスト4.20 リスト4.19を使う実装** 

package main import ( "log" "os" "time" "github.com/mattn/awesome-server/server" ) func main() { f, err := os.Create("server.log") if err != nil { log.Fatal(err) } 

defer f.Close() logger := log.New(f, "", log.LstdFlags) svr := server.New("localhost", 8888, server.WithTimeout(time.Minute), server.WithLogger(logger), ) if err := svr.Start(); err != nil { log.Fatal(err) } } 

タイムアウト値やロガーがオプションとして渡せるようになりました。この他にも必要なオプションを増やしたい場合には、以下の手順でオプションを増やすことができます。 





**Server structにフィールドを追加 WithXXXを実装** 

# **4.7 Builder Pattern** 

Functional Options Pattern と対比される実装パターンです。 Builder Pattern はメソッドチェーンを用いることで Build メソッドに与える初期値を決定する方法です。 

#### **リスト4.21 リスト4.17をBuilder Patternに則って変更** 

package server 

import ( "log" "time" ) 

type Server struct { param serverParam } 

type serverParam struct { host    string port    int timeout time.Duration logger  *log.Logger } 

func NewBuilder(host string, port int) *serverParam { return &serverParam{host: host, port: port} } 

func (sb *serverParam) Timeout(timeout time.Duration) *serverParam { sb.timeout = timeout return sb } func (sb *serverParam) Logger(logger *log.Logger) *serverParam { sb.logger = logger return sb } func (sb *serverParam) Build() *Server { svr := &Server{ param: *sb, } return svr } func (s *Server) Start() error { if s.param.logger != nil { s.param.logger.Println("server started") } // do something return nil } 

実行方法は以下のとおりです。 

#### **リスト4.22 リスト4.21を使う実装** 

package main 

import ( "log" "os" "time" 

"github.com/mattn/awesome-server/server" ) func main() { f, err := os.Create("server.log") if err != nil { log.Fatal(err) } defer f.Close() logger := log.New(f, "", log.LstdFlags) svr := server.NewBuilder("localhost", 8888). Timeout(time.Minute). Logger(logger). Build() if err := svr.Start(); err != nil { log.Fatal(err) } } 

Functional Options Pattern の場合は New 関数に与えるパラメータとして WithXXX <u>を用意する方法を取りましたが、</u> Builder Pattern では生成した Builder ( serverParam )に対してメソッドによりパラメータを付与し、 Build メソッドにて実際の Server インスタンスを作成します。どちらも似た機能を提供しますが、 Builder Pattern は途中の設定を他の機能に委ねることができます。 

builder := server.NewBuilder("localhost", 8888) configureServer(builder) svr := builder.Build() 

メソッドチェーンは Go ではあまり使われないため、ユーザーによって好みが分かれることがあります。どちらの方法も、あとからオプションを追加したいケースでは有効です。ただ、どちらも若干、実装量が増えてしまいがちです。 Functional Options Pattern を簡略化する形で以下のように書かれることもあります。 

svr := server.New(host, port, server.Config { Timeout: time.Minute, Logger: nil, }) 

# **4.8 internalパッケージ** 

Go でパッケージ外に変数や関数を公開するには、シンボル名の先頭を大文字にする必要があります。公開か非公開かが一目瞭然でとてもシンプルなルールですが、問題が起きてしまうことがあります。たとえばライブラリを開発中に、内部処理のためにパッケージを分けしたとします。ライブラリの利用者は、ソースコードに含まれている便利な関数をライブラリの機能の一部だと思って使ってしまうことがあります。しかし、開発者はそれが使われていることなど知らずにインターフェースを変更することもあります。こういったことが起きないように、 Go には internal パッケージと呼ばれるしくみがあります。このディレクトリ構成では、 cmd/awesome/main.go は internal/awesome にある関数を呼び出すことができますが、 zoo/internal/bored にある関数は呼び出せません。さらには、このパッケージの外からは internal/awesome も zoo/internal/bored も参照することができません。 

package main import ( "log" "github.com/mattn/awesome-lib/internal/awesome" // boredは参照できない //"github.com/mattn/awesome-lib/zoo/internal/bored" ) func main() { if err := awesome.DoAwesome(); err != nil { log.Fatal(err) } // boredの関数は使えない //if err := bored.DoBored(); err != nil { //    log.Fatal(err) //} } 

参照することができるのは、 internal パッケージと同列、もしくはその配下のソースコードからのみになります。この internal 配下にパッケージを置くことで、意図せず利用者が参照してしまう事故を防げます。 

# **4.9 Embedded struct** 

以下のように記述することで struct を合成することができます。 

type Attr struct { Name string Age  int } type Teacher struct { Attr Subject string } type Student struct { Attr Score int } 

この場合、 Teacher は Name 、 Age 、 Subject のフィールドを持ちます。また Student は Name 、 Age 、 Score のフィールドを持ちます。初期化は以下のように行います。 

teacher := Teacher { Attr: Attr{ Name: "John Schwartz", Age:   43, }, 

Subject: "Math", } student := Student { Attr: Attr{ Name: "Robert Smith", Age:   17, }, Score: 87, } 

合成されたフィールドは以下のように参照できます。 fmt.Println(teacher.Name, teacher.Subject) fmt.Println(student.Name, student.Score) 

また、同じフィールドを持つ struct を複数合成した場合は、先に埋め込まれた struct が優先されます。 

type Attr struct { Name string Age  int } type AttrEx struct { Name string } type Teacher struct { Attr AttrEx Subject string } 

この場合、 teacher.Name のように参照することはできませんが、以下のように埋め込まれた struct 名を使って参照する 

### ことができます。 

teacher := Teacher{ Attr: Attr{ Name: "John Schwartz", Age:  43, }, AttrEx: AttrEx{ Name: "JS", }, Subject: "Math", } fmt.Println(teacher.Attr.Name) fmt.Println(teacher.AttrEx.Name) 

Attr や AttrEx にメソッドがある場合には、 teacher から呼び出すこともできます( **リスト** 4.23)。 

**リスト4.23 別のstructからフィールドの持つメソッドを呼び出す** type Attr struct { Name string Age  int } func (a Attr) String() string { return fmt.Sprintf("%s(%d)", a.Name, a.Age) } type AttrEx struct { Name string } func (a AttrEx) String() string { return fmt.Sprintf("(a.k.a. %s)", a.Name) } 

type Teacher struct { Attr AttrEx Subject string } func main() { teacher := Teacher{ Attr: Attr{ Name: "John Schwartz", Age:  43, }, AttrEx: AttrEx{ Name: "JS", }, Subject: "Math", } // John Schwartz(43) (a.k.a. JS) fmt.Println(teacher.Attr.String(), teacher.AttrEx.String()) } 

この機能は一見、継承のように見えますが、埋め込んだ struct から埋め込まれた struct を参照することはできません。委譲(コンポジション)としての継承は実現できます( **リスト** 4.24)。 

**リスト4.24 委譲としての継承** 

package main import ( "fmt" ) type Walker struct { Name string 

} 

func (w *Walker) Walk() { fmt.Printf("%s is walking\n", w.Name) } type Runner struct { Walker } func NewRunner(name string) *Runner { return &Runner{Walker{Name: name}} } func (r *Runner) Run() { fmt.Printf("%s is running\n", r.Name) } func main() { runner := NewRunner("John") runner.Walk() // John is walking runner.Run()  // John is running } 

runner.Walk()は、さも Runner にある Walk メソッドを呼び出しているように見えますが、実際にはrunner.Walker.Walk() を呼び出すシュガーになっています(runner.Walker.Walk() の呼び出しも可能です)。 

# **4.10 go run** 

これまで、ソースをビルドして実行するためにgo buildコマンドを紹介しましたが、以下のように実行することでビルドと実行を併せて行うことができます。 

$ go run main.go 

go runには go ファイルが複数置かれたディレクトリも指定できます。 

$ go run ./upload 

さらにモジュールパスをバージョン付きで指定し実行することもできます。 

$ go run github.com/mattn/longcat@v0.0.4 

この例(ジョークコマンド)を実行すると端末に、縦に伸びた猫の画像が表示されます。たとえば、インターネット上に置かれている資料をスクレイピングし、 Go のソースコードを生成するツールを作成するためのコマンドを用意しておくと、その資料が更新されるたびにgo generateコマンドを実行するだけでソースコードを最新にすることができますが、その実行をgo runにしておくと、ビルドする手間も省けてとても便利です。 

# **4.11 まとめ** 

Go の基本的なテクニックと、それに関連するベストプラクティスを解説しました。これらを自然と扱えるようになれば、 Go の開発がさらに速く、そして高度になってきます。これらを丸暗記する必要はありません。困ったときには Go のドキュメントを参照しながら開発してください。 Go のドキュメントは分量も多く、関数の挙動に関しても詳細に書かれています。パッケージのドキュメントでは意外と知らなかった機能を新発見することもあるので、目を通してみることをお勧めします。 



# **5.1 net/http** 

net/http は標準で用意されているパッケージです。これだけで Web アプリケーションを開発することができます。 Go の多くの Web アプリケーションフレームワークの実装が、内部では net/http を使っています。 

## **基本の使い方** 

net/http による簡単な Web サーバの実装を **リスト** 5.1に示します。 

**リスト5.1 net/httpの使用例** 

package main import ( "fmt" "net/http" ) func main() { http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { fmt.Fprintf(w, "Hello World!") }) http.ListenAndServe(":8080", nil) } 

http.HandleFunc を使ってリクエストハンドラを設定し、 http.ListenAndServe でサーバを起動しています。とても簡単な仕様です。ハンドラの引数 w は型から想像できるとおり、レスポンスを書き込むための io.Writer です。 r にはリクエスト情報が格納されています。 

http.HandleFunc は複数呼び出すことができます。呼び出し順を考慮する必要はありません。ただ、 Web アプリケーションフレームワークのように、 GET や POST といったリクエストメソッドごとにハンドラを登録するのではない点に注意が必要です。 

GET と POST を区別したい場合には以下のように r.Method を確認する必要があります。 

http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { switch r.Method { case http.MethodGet: // GET時の処理 default: } }) 

http.ResponseWriter は io.Writer であるため、 Go の io.Writer を受け取る多くの関数を呼び出すことができます。 

http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { contents := forecast() w.Write([]byte(contents)) }) 

たとえば、 io.Copy でファイルの内容を出力することもできます。 

http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { f, err := os.Open("/path/to/content.txt") if err != nil { http.Error(w, err.Error(), http.StatusInternalServerError) return } defer f.Close() io.Copy(w, f) }) 

## **ルーティング** 

http.Handle と http.HandleFunc の違いは第二引数が func かどうかだけであり、 http.HandlerFunc でキャストすることができます。以下の 2 つは等価です。 

http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { fmt.Fprintf(w, "Hello World!") }) 

http.Handle("/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { fmt.Fprintf(w, "Hello World!") })) 

関数のシグネチャが同じであれば引数として渡すことができます。 

func myHandler(w http.ResponseWriter, r *http.Request) { fmt.Fprintf(w, "Hello World!") } 

func main() { http.HandleFunc("/", myHandler) http.ListenAndServe(":8080", nil) } 

Go のメソッド値( Method Value 、次のコラム参照)を使うことで http.HandleFunc にメソッドを渡すことができます。 

type MyContext struct { db *sql.DB } func (m *MyContext) handle(w http.ResponseWriter, r *http.Request) { // m.dbを使った処理 } func main() { myctx := NewMyContext() http.HandleFunc("/", myctx.handle) (..略..) } 

こうすることで、 NewMyContext でデータベース接続を db に設定し、 handle の中でその db を使った処理を実装することができます。 

**COLUMN Method Value** 

少し混乱するかもしれませんが、Goのメソッド呼び出しは、第一引数にレシーバに持った関数の呼び出しと同 

### 義であると言えます。 

package main 

import "fmt" 

type I int 

func (i I) Add(n int) I { return i + I(n) } func main() { var n I = 0 n = n.Add(1).Add(2) fmt.Println(n) // 1 + 2 = 3 } 

これはintの別名IにAddというメソッドを定義し、引数の値と足し算をして返す関数です。メソッドAddは値として変数に取ることができます。 

add := n.Add fmt.Println(add(3)) // 3 + 3 = 6 

n.Addにaddという名前を付けることで、レシーバを指定せずに呼び出すことができます。さて、このn.Addの型を確認してみましょう。 

<mark>fmt.Printf("%T\n", n.Add) // func(int) main.I</mark> 

期待していたとおりだと思います。ではI.Addはどうでしょうか? 

<mark>fmt.Printf("%T\n", I.Add) // func(main.I, int) main.I</mark> 

引数が2つになりました。これはレシーバが第一引数にあることを意味しています。つまり、以下の2つのAddは同じ意味になるということになります。 

n = 1 fmt.Println(n.Add(2)) fmt.Println(I.Add(n, 2)) 

ちまた前述のように、 net/http は巷の Web アプリケーションフレームワークのように、 GET や POST といったメソッドを制限したハンドラを登録したり、パラメータを受け取れたりするようなルーティング機能を持っていません。すべて自分で実装する必要があります。ベースを net/http で構築し、ルーティング機能のみサードパーティライブラリを使うこともできます。 julienschmidt/httproute ~~r~~ <u>注5.1</u> ~~を~~ 使うと、 net/http を使いつつ、ルーティングだけを差し替えられるため、少しだけ高度な Web アプリケーションを構築することができます( **リスト** 5.2)。 

#### **リスト5.2 julienschmidt/httprouterによるルーティング** 

package main 

import ( "fmt" "net/http" "log" "github.com/julienschmidt/httprouter" ) 

func Index(w http.ResponseWriter, r *http.Request, _ httprouter.Params) { fmt.Fprint(w, "Welcome!\n") } func Hello(w http.ResponseWriter, r *http.Request, ps httprouter.Params) { fmt.Fprintf(w, "hello, %s!\n", ps.ByName("name")) } func main() { router := httprouter.New() router.GET("/", Index) router.GET("/hello/:name", Hello) log.Fatal(http.ListenAndServe(":8080", router)) } 

また、 gocraft/we ~~b~~ <u>注5.2</u> ~~を~~ 使っても同様のことができます ( **リスト** 5.3)。 

**リスト5.3 gocraft/webによるルーティング** package main 

import ( "fmt" "net/http" "strings" "github.com/gocraft/web" ) type AppContext struct { HelloCount int } 

func (c *AppContext) SetHelloCount(rw web.ResponseWriter, req *web.Request, next web. NextMiddlewareFunc) { c.HelloCount = 3 next(rw, req) } 

func (c *AppContext) SayHello(rw web.ResponseWriter, req *web.Request) { fmt.Fprint(rw, strings.Repeat("Hello ", c.HelloCount), "World!") } 

func main() { router := web.New(AppContext{}). // Create your router Middleware(web.LoggerMiddleware).// Use some included middleware 

Middleware(web.ShowErrorsMiddleware). // ... Middleware((*AppContext).SetHelloCount). // Your own middleware! 

Get("/", (*AppContext).SayHello)           // Add a route http.ListenAndServe("localhost:3000", router) // Start the server! } 

ブラウザから http://localhost:3000/ にアクセスすると、更新するたびに「 Hello 1 」のカウントが足されます。 router は http.Handler を実装しており、 net/http に注入する形でルーティングを構成することができます。さて、なぜルーティングのためにフレームワークを使わなければならないのでしょうか。それはルーティングという機能そのものが複雑だからです。複雑さを知っていただくために、簡単なルーティングを実装してみます。ルーティングに必要な要素は以下の 3 つです。 







### **どのHTTPメソッドかリクエストURIとマッチさせるためのしくみパスパラメータと関数呼び出し** 

簡単な例として実装するため、正規表現を使います。まず、パスパラメータとそれを渡す関数の型を定義します。 

type routerParam map[string]string 

type routerFunc func(routerParam, http.ResponseWriter, *http.Request) 

ルータが扱うマッチ要素に持つ情報は以下のとおりです。 

type routerItem struct { method  string matcher *regexp.Regexp fnc     routerFunc } 

type router struct { items []routerItem } 

リクエストメソッドが method と一致し、かつリクエスト URI が正規表現のパターン matcher と一致していれば、 fnc を呼び出すというものです。 router は最終的には http.ListenAndServe の第二引数として渡すので、 http.Handler を実装しなければなりません。巷によくあるルーティングのように GET や POST も実装します( **リスト** 5.4)。 

#### **リスト5.4 フレームワークを使わずルーティングを実装** 

func (rt *router) GET(prefix string, fnc routerFunc) { rt.items = append(rt.items, routerItem{ 

method:  http.MethodGet, matcher: regexp.MustCompile(prefix), fnc:     fnc, }) } func (rt *router) POST(prefix string, fnc routerFunc) { rt.items = append(rt.items, routerItem{ method:  http.MethodPost, matcher: regexp.MustCompile(prefix), fnc:     fnc, }) } func (rt *router) ServeHTTP(w http.ResponseWriter, r *http.Request) { for _, v := range rt.items { // リクエストメソッドの一致、リクエストURIがマッチ if v.method == r.Method && v.matcher.MatchString(r.RequestURI) { // パスパラメータの取得 match := v.matcher.FindStringSubmatch(r.RequestURI) param := make(routerParam) for i, name := range v.matcher.SubexpNames() { param[name] = match[i] } v.fnc(param, w, r) return } } http.NotFound(w, r) } 

このように実装すると、サーバの構築は **リスト** 5.5のようになります。 

#### **リスト5.5 リスト5.4に対応するサーバの実装** 

func main() { rt := router{} // 先頭から見て/だけで終わる文字列 rt.GET(`^/$`, func(p routerParam, w http.ResponseWriter, r *http.Request) { fmt.Fprintln(w, "Hello") }) // 先頭から見て/で始まり英数字だけが続いて終わる文字列 rt.GET(`^/(?P<name>\w+)$`, func(p routerParam, w http.ResponseWriter, r *http. Request) { fmt.Fprintf(w, "Hello: %v\n", p["name"]) }) // 先頭から見て/apiで終わる文字列 rt.POST(`^/api$`, func(p routerParam, w http.ResponseWriter, r *http.Request) { w.Header().Set("content-type", "text/json") fmt.Fprintln(w, `{"status": "OK"}`) }) http.ListenAndServe(":8080", &rt) } 

実際に起動して curl でアクセスしてみます。 

$ curl http://localhost:8080/xxx.html 404 page not found $ curl http://localhost:8080/ Hello $ curl -X POST http://localhost:8080/api {"status": "OK"} 

$ curl http://localhost:8080/mattn Hello: mattn 

おおよそ期待どおりに動作しました。一見簡単そうに見えるかもしれませんが、この実装には以下の問題があります。 









### **登録が正規表現のため読みづらい** 

**マッチがそれほど速くない** 

**GETやPOST以外のメソッドに対応していない /mattn/../のようなパスを補正しない** 

これらの問題を解決するには多くのコードを必要とします。安全のためにも、枯れた実装を使うことをお勧めします。 

## **ファイルのサーブ** 

ファイルをサーブしたい場合には http.FileServer を使います。 

<mark>http.Handle("/", http.FileServer(http.Dir("./static")))</mark> 

この場合、 static というディレクトリにあるファイルがサーブされます。つまり ./static/index.html が 

http://localhost:8080/index.html としてサーブされます。 ./static/index.html を http://localhost:8080/public/index.html としてサーブしたい場合には少し注意が必要です。以下のようにしても期待どおりにはなりません。 

<mark>http.Handle("/public/", http.FileServer(http.Dir("./static")))</mark> 

これは、 /public/index.html というリクエストに対してファイルサーバが ./public/static/index.html というファイルを探してしまうためです。 http.StripPrefix を用いて /public/ を取り除く必要があります。 

http.Handle("/public/", http.StripPrefix("/public/", http.FileServer(http.Dir("./static")))) 

StripPrefix の第一引数は第二引数に対して作用するのではなく、リクエスト URI に対して作用する点を留意しておきましょう。 

FileServer を使用する場合、 Content-Type は自動で設定されます。もし Go が用意する MIME タイプに該当しないファイルをサーブしたい場合には、自分でファイルのサーブ処理を書く必要があります。 

fileserver := http.StripPrefix("/public/", http.FileServer(http.Dir("./static"))) http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { if path.Ext(r.URL.Path) == ".xls" { w.Header().Set("Content-Type", "application/vnd.ms-excel") } fileserver.ServeHTTP(w, r) }) 

カスタマイズしたい拡張子がたくさんある場合には map を用意しておくと良いでしょう。 

mimemap := map[string]string{ ".xls":  "application/vnd.ms-excel", ".xlsx": "application/vnd.openxmlformatsofficedocument.spreadsheetml.sheet", ".ppt":  "application/vnd.ms-powerpoint", 

".pptx": "application/vnd.openxmlformatsofficedocument.presentationml.presentation", ".doc":  "application/msword", ".docx": "application/vnd.openxmlformatsofficedocument.wordprocessingml.document", } http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { if typ, ok := mimemap[path.Ext(r.URL.Path)]; ok { w.Header().Set("Content-Type", typ) } fileserver.ServeHTTP(w, r) }) 

**注** 5.1 <u>https://github.com/julienschmidt/httprouter</u> (本文に戻る) 

**注** 5.2 <u>https://github.com/gocraft/web</u> (本文に戻る) 

# **5.2 html/template** 

html/template は Go に標準で用意されたテンプレートエンジンです。独自の記法により制御構文や関数呼び出しができます。 **リスト** 5.6が html/template を扱う際の基本の形です。 

#### **リスト5.6 html/templateの基本形** 

package main import ( "html/template" "log" "os" ) func main() { tmpl := `{{.}}` // ←★ t := template.Must(template.New("").Parse(tmpl)) err := t.Execute(os.Stdout, "Hello, World!") if err != nil { log.Fatal(err) } } 

★部分の.は Execute に渡された値、または with および range で束縛される値を示します。 Go の html/template は値が出力されるコンテキストにより自動でエスケープ方法が変わります。たとえば下記のテンプレートを見てください。 

<mark><a href="{{.}}">link</a></mark> 

このテンプレートに Execute で<script>alert(1)</script>を渡すと、以下のように URL エスケープされて出力されます。 

<a href="%3cscript%3ealert%281%29%3c/script%3e">link</a> 

また以下のテンプレートを見てください。 

<mark><a href="/add">{{.}}</a></mark> 

この場合は以下のように HTML エスケープされて出力されます。 

<a href="/add">&lt;script&gt;alert(1)&lt;/script&gt;</a> 

**struct** 

struct を Execute に渡すと、 .Field のようにフィールドの値が参照できます。 

type User struct { Name string } user := User{Name: "Bob"} err := t.Execute(os.Stdout, user) 

以下のテンプレートで実行すると Bob が表示されます。 

<mark>{{.Name}}</mark> 

## **range/end** 

配列やスライスを Execute に渡すと、 range を使ってループさせることができます。 

values := []string{"Hello", "World"} err := t.Execute(os.Stdout, values) 

以下のテンプレートで実行すると、 

{{range .}} <p>{{.}}</p>{{end}} 

以下のように出力されます。 

<p>Hello</p> <p>World</p> 

range ブロックの中のスコープはその要素になります。また、 index を使うことでインデックスを指定できます。 

<mark>{{index . 1}}</mark> 

このテンプレートで実行すると「 World 」と表示されます。 

## **if/else/end** 

html/template では制御構文が扱えます。 

type User struct { Age  int 

Name string } user := User{Age: 21, Name: "Bob"} err := t.Execute(os.Stdout, user) 

テンプレートでは以下のように指定します。 {{if gt .Age 20}} {{.Name}} is older than 20 {{else}} {{.Name}} is not older than 20 {{end}} 

扱える評価式は **表** 5.1のとおりです。 



## **with/else/end** 

with を使うことでブロック内のスコープを変更できます。 type Employee struct { Name string } type Company struct { Employees []Employee 

} company := Company{ Employees: []Employee{ {Name: "Bob"}, {Name: "Mike"}, }, } err := t.Execute(os.Stdout, company) 

以下のテンプレートで実行すると「 Bob 」が表示されます。 

{{with index .Employees 0}} {{.Name}} {{end}} 

with は値が空( 0 や""、 nil )であった場合には else を取ることができます。 

{{with .}} {{.}} {{else}} Not Found {{end}} 

with や range はコンテキスト内だけで有効な変数に値を代入することもできます。 

{{with $v := index .Employees 0}} {{$v.Name}} {{end}} 

## **template.HTML** 

前述のように、 html/template はデフォルトでエスケープされます。<や>を含んだ文字列は &lt; や &gt; に変換され、 HTML 属性値の中ではパーセントエスケープされます。しかし、時には HTML タグをそのまま出力したいケースもあります。そういった場合には template.HTML を使います。 

tmpl := `<div>{{.}}</div>` t := template.Must(template.New("").Parse(tmpl)) err := t.Execute(os.Stdout, template.HTML(`<b>HTML</b>`)) 

このコードを実行すると以下が出力されます。 

<div><b>HTML</b></div> 

また、 script タグの中で JavaScript を出力するのであれば template.JS を使います。 

tmpl := `<script>{{.}}</script>` t := template.Must(template.New("").Parse(tmpl)) err := t.Execute(os.Stdout, template.JS(`alert(" <script>1</script>")`)) 

このソースコードを実行すると以下が出力されます。 

<script>alert("<script>1</script>")</script> 

## **FuncMap** 

html/template では自作の関数を追加することができます。たとえば time.Time の値を独自の書式で出力するのであれば、 **リスト** 5.7のように FuncMap を使って Funcs を設定します。 

#### **リスト5.7 html/templateで自作の関数を追加** 

package main 

import ( "html/template" "log" "os" "time" ) 

func main() { t := template.New(""). Funcs(template.FuncMap{ "FormatDateTime": func(format string, d time.Time) string { if d.IsZero() { return "" } return d.Format(format) }}) tmpl := `{{FormatDateTime "...." .}}` // 実行するテンプレート t = template.Must(t.Parse(tmpl)) err := t.Execute(os.Stdout, time.Now()) if err != nil { log.Fatal(err) } } 

以下のテンプレートで実行すると日本語の日付が出力されます。 

{{FormatDateTime "2006年01月02日" .}} 

## **template.Parse** 

template.Parse は文字列を受け取りますが、ファイルを指定することもできます。 

t := template.Must(template.New("").ParseFiles( "template/index.html.tmpl", "template/description.html.tmpl", "template/login.html.tmpl", )) 

またワイルドカード文字列を使って読み込むこともできます。 

t := template.Must(template.New("").ParseGlob("template/*.tmpl")) 

## **define/template** 

テンプレートには名前が付けられます( **リスト** 5.8)。 

#### **リスト5.8 body.html.tmpl** 

{{define "body"}} {{.}} 

{{end}} 

このテンプレートは **リスト** 5.9のように他のテンプレートから呼び出すことができます。 

#### **リスト5.9 index.html.tmpl** 

{{define "index"}} ここからが本文です。 {{template "body" .}} ここまでが本文です。 {{end}} 

リスト 5.8 、 5.9 を template ディレクトリに配置し、以下のコードを実行すると、 

t := template.Must(template.New("").ParseGlob("template/*.tmpl")) err := t.ExecuteTemplate(os.Stdout, "index", "これは本文です。") 

以下が出力されます。ここからが本文です。これは本文です。ここまでが本文です。 

{{template "body" .}}の.で引数を渡せることに注目してください。各テンプレートの命令({{と}}で囲まれる部分)は、先頭または末尾に-を付けることで、前にある改行や後にある改行を無効にすることができます。たとえば、前述の **リスト** 5.9を以下のように変更してみます。 

{{define "index"}} ここからが本文です。 {{- template "body" .}} ここまでが本文です。 {{end}} 

すると以下が出力されます。ここからが本文です。これは本文です。ここまでが本文です。後ろにも付けてみましょう。 

{{define "index"}} ここからが本文です。 {{- template "body" . -}} ここまでが本文です。 {{end}} すると無駄な改行が削除されます。ここからが本文です。これは本文です。ここまでが本文です。さらに **リスト** 5.8を以下に変更してみましょう。 {{define "body"}} {{- . -}} {{end}} 

すると出力から改行がなくなり、 1 行で出力されます。ここからが本文です。これは本文です。ここまでが本文です。 

# **5.3 net/smtp** 

Go には net/smtp パッケージと net/mail パッケージがあり、メールの送信を行うことができます。 net/smtp は SMTP プロトコルの送受信( RFC-5321 )を、 net/mail はメッセージフォーマット( RFC-5322 )に関するライブラリです。ただ、 net/mail のメッセージフォーマットはバイト列のみを扱う簡易なものですので、添付ファイルなどを扱うには物足りません。 enmim ~~e~~ <u>注5.3</u> ~~を~~ 使うと、こういった類の問題の多くを解決することができます( **リスト** 5.10)。 

#### **リスト5.10 enmimeの使用例** 

package main 

import ( "net/smtp" "github.com/jhillyerd/enmime" ) 

func main() { smtpHost := "my-mail-server:25" smtpAuth := smtp.PlainAuth( "example.com", "example-user", "example-password", "auth.example.com") 

sender := enmime.NewSMTP(smtpHost, smtpAuth) 

master := enmime.Builder(). From("送信太郎", "taro@example.com"). Subject("メールのタイトル"). Text([]byte("テキストメール本文")). HTML([]byte("<p>HTML メール<b>本文</b></p>")). AddFileAttachment("document.pdf") 

msg := master.To("宛先花子", "hanako@example.com") err := msg.Send(sender) if err != nil { log.Fatal(err) } } 

ただしメールは UTF-8 エンコーディングのまま送信されます。一部の日本のモバイル端末では、いまだに ISO-2022-JP のみ扱うものが存在し、受信すると文字化けしてしまうことがあります。そういった場合は enmime は使えません。以下のように、 mime/multipart を使い、マルチパートを作成、本文は base64 でエンコードします。また Subject ヘッダは iso-2022-jp でエンコードし、 base64 でエンコードした形式を用いなくてはなりません( **リスト** 5.11)。 

#### **リスト5.11 mime/multipartの使用例** 

package main import ( "bytes" "encoding/base64" "fmt" "io" "log" 

"mime/multipart" "net/smtp" "net/textproto" 

"golang.org/x/text/encoding/japanese" ) func split(s string, n int) []string { ss := []string{} rs := []rune(s) l := len(rs) sub := "" for i, r := range rs { sub = sub + string(r) if (i+1)%n == 0 { ss = append(ss, sub) sub = "" } else if i+1 == l { ss = append(ss, sub) } } return ss } func header(s string) string { var buf bytes.Buffer iso2022jp := japanese.ISO2022JP.NewEncoder() for i, line := range split(s, 13) { if i != 0 { buf.WriteString("\r\n ") } buf.WriteString("=?iso-2022-jp?B?") input := []byte(line) b, err := iso2022jp.Bytes(input) if err != nil { b = input } 

buf.WriteString(base64.StdEncoding.EncodeToString(b)) buf.WriteString("?=") 

} return buf.String() } func body(b []byte) []byte { return []byte(base64.StdEncoding.EncodeToString(b)) } func main() { smtpHost := "my-mail-server:25" smtpAuth := smtp.PlainAuth( "example.com", "example-user", "example-password", "auth.example.com") var buf bytes.Buffer mw := multipart.NewWriter(&buf) mh := textproto.MIMEHeader{} mh.Set("Content-Type", "text/plain; charset=iso-2022-jp") mh.Set("Content-Transfer-Encoding", "base64") part, err := mw.CreatePart(mh) if err != nil { log.Fatal(err) } _, err = part.Write(encodeBody([]byte("メール本文"))) if err != nil { log.Fatal(err) } mh = textproto.MIMEHeader{} mh.Set("Content-Type", "text/html; charset=utf-8") mh.Set("Content-Transfer-Encoding", "base64") part, err = mw.CreatePart(mh) if err != nil { 

log.Fatal(err) } _, err = part.Write(encodeBody([]byte("<p>メール<b>本文</b> </p>"))) if err != nil { log.Fatal(err) } mw.Close() boundary := mw.Boundary() var body bytes.Buffer body.WriteString( fmt.Sprintf("From: %s <%s>\n", header("送信太郎"), "taro@example.com")) body.WriteString( fmt.Sprintf("To: %s <%s>\n", header("宛先花子"), "hanako@example.com")) body.WriteString( fmt.Sprintf("Content-Type: multipart/alternative; boundary=%s\n", boundary)) body.WriteString( fmt.Sprintf("Subject: %s\n", header("メールのタイトル"))) body.WriteString("\n") io.Copy(&body, &buf) err = smtp.SendMail( smtpHost, smtpAuth, "taro@example.com", []string{"hanako@example.com"}, body.Bytes()) if err != nil { log.Fatal(err) } } 

もちろん、日本語の題名を送信するのに quoted-printable 形式も使えますが、日本語だけで構成される Subject や本文の場合は base64 エンコーディングのほうが小さくなるため、 base64 決め打ちでも良いでしょう。 

**注** 5.3 <u>https://github.com/jhillyerd/enmime</u> 

(本文に戻る) 

# **5.4 まとめ** 

Go の net/http パッケージを使用して Web アプリケーションを作成する方法を、また net/smtp やサードパーティライブラリを使用してメールを送信する方法を解説しました。いずれも Go でウェブアプリケーションを作成するうえでは必要になる機能のため、実際に手を動かして使い方を覚えると良いでしょう。とくに、 html/template はいろいろな応用方法があります。ぜひマスターできるようにしましょう。 



# **6.1 並行と並列** 

具体的な実装の紹介の前に、まず並行と並列について説明します。ある 2 つの処理を実行するとき、 2 つの処理をそれぞれ別の作業者が担当したとします。これを並列と言います。並列は個々の作業者がその処理に 100% 注力します( **図** 6.1)。一方、 2 つの処理をフェーズごとに区切り、 1 人の作業者が都度処理したとします。これを並行と言います。並行では作業者がフル回転で作業することはできません( **図** 6.2)。一見、並列のほうが早く処理が終わると思ってしまうかもしれません。しかし、そうとも限りません。コンピュータで実行する処理は計算処理だけではありません。プログラムが処理にかける時間の多くではファイルやネットワークに対する I/O が伴います。ファイルやネットワークの I/O は非同期プログラミングを行わない限りブロッキングされます。つまり並列は一見、作業者がフル回転していそうに見えて、待ち時間が多く発生するのです。効率良く作業することで、並行のほうが結果的に早く作業が終わる場合があります。さらに複数の作業者が並列で作業する際は、作業者間でコミュニケーションが必要な場合もあります。そういった場合、コミュニケーションの間に手を止める必要があり、効率がどんどん落ちます。これらの問題を解決するのが goroutine と channel です。 

# **6.2 goroutine** 

第 3 章にて、 Go には goroutine という軽量のスレッドがあることを説明しました。 goroutine をうまく使うことで、プログラムをより速くすることができます。ただし気をつけなければならないのは、 goroutine が効果を出すのは I/O が絡むケースだということです。メモリや CPU のみを使う計算処理を並行処理したとしても、それほど効果を発揮しません。 pthread で実装した C 言語での実装と比較すると、少し遅くなることがあります。 

goroutine は「ファイル I/O やネットワーク通信でブロックしている間にもランタイムが選んだ CPU コアで別の処理を並行して行うことで単純な並列処理よりも効率的に処理を行う」を主題にしているランタイムの機能です。よって並行処理を行う部分の見極めが重要となります。 

goroutine は軽量スレッドと呼ばれていますが、実際にスレッドが常に起きているわけではありません。たとえば **リスト** 6.1のプログラムを見てください。 

#### **リスト6.1 goroutineの使用例** 

package main 

import ( "sync" "time" ) 

func doSomething(wg *sync.WaitGroup) { defer wg.Done() time.Sleep(100 * time.Second) } func main() { var wg sync.WaitGroup for i := 0; i < 100; i++ { wg.Add(1) go doSomething(&wg) } wg.Wait() } 

これを以下のコマンドでビルドして実行します。 

$ go build -o goroutine-test main.go $ ./goroutine-test 

別の端末を開き、以下のコマンドを実行します。 

$ ps a -o pid,nlwp,args | grep goroutine-test | grep -v grep 55179    6 ./goroutine-test 

実行結果のうち、 2 カラムめがスレッド数になります。 4 コア CPU のハードウェアで実行しました。 100 個の goroutine を起動しましたが、実際のスレッド数は 6 個です。では次に、先ほどの **リスト** 6.2を以下のように変更してみましょう。 

**リスト6.2 リスト6.1を負荷が高くなるよう変更** package main import ( "fmt" 

"sync" ) func doSomething(wg *sync.WaitGroup, id int) { defer wg.Done() for i := 0; i < 10000; i++ { fmt.Printf("%d\n", id) } } func main() { var wg sync.WaitGroup for i := 0; i < 100; i++ { wg.Add(1) go doSomething(&wg, i) } wg.Wait() } 

少しだけ負荷の高い処理になります。これを実行して再度スレッド数を確認してみてください。同じく 6 になっているのがわかると思います。これは、実際に 6 つしか並行処理していないわけではなく、 Go のランタイムが効率的に CPU コアを使っているのです。つまり goroutine が CPU の 1 コアを占有しないのです。このことを確かめるために、同様の処理を行う C 言語のプログラム( **リスト** 6.3)を実行して比較してみます。 

main.go にて fmt.Println や fmt.Printf が呼び出している os.File.Write は標準出力をロックし、システムコールwrite(2) の完了を確認しているため、 C 言語におけるfflush(3)相当の処理を行っています。これにより標準出力が混ざりあうことはなく、期待する出力が行われます。 Go と C 言語で計測をフェアにするために、ここでは C 言語のほうに 

flockfile/funlockfile による stdout のロック、また fflush によるバッファのフラッシュを追加しています。 

#### **リスト6.3 リスト6.2と同様の処理を行うC言語のプログラム** 

#define _OPEN_THREADS #include <pthread.h> #include <stdlib.h> #include <stdio.h> #include <unistd.h> void *thread(void *arg) { pthread_t self = pthread_self(); int i = 0; for (i = 0; i < 10000; i++) { flockfile(stdout); printf("%ld\n", self); fflush(stdout); funlockfile(stdout); } pthread_exit(0); } int main() { pthread_t thid[100]; int i; void *ret; for (i = 0; i < 100; i++) { if (pthread_create(&thid[i], NULL, thread, NULL) != 0) { perror("pthread_create() error"); exit(1); } } for (i = 0; i < 100; i++) { if (pthread_join(thid[i], NULL) != 0) { perror("pthread_create() error"); 

exit(3); } } } 

C 言語のソースコードは以下のようにコンパイルして実行します。 

$ gcc main.c $ ./a.out 

top コマンドなどで確認すると、スレッドが 100 個実行され、 CPU 使用率が振り切ってしまうことが分かると思います。また前述の ps コマンドで確認すると、確かに 100 個のスレッドが起動しているのが確認できます。 4 コアの CPU で 100 個ものスレッドを起動すると、コンテキストスイッチ(スレッドを停止してコアに別のスレッドを割り当てる処理)が多く発生し、負荷が高くなってしまいます。以下のコマンドを実行して、この処理の実行に何秒かかるのかを確認してみてください。 

$ time ./a.out > /dev/null real    0m12.018s user    0m8.500s sys  0m37.901s 

およそ 12 秒かかりました。では Go のプログラムでも同じ計測をしてみましょう。 

$ time ./main1 > /dev/null real    0m8.940s user    0m11.409s sys  0m8.839s 

Go のほうが C 言語よりも 3 秒ほど早く終了しました。この結果に驚く人もいるかもしれません。 Go では 4 コアの中で最低限必要な 6 スレッドを起動し、その中で Go のランタイムスケジューラが各 goroutine を小さな処理の単位で切り替えつつ CPU コアに割り当てながら実行することで、コアを有効に使うことができたことを意味しています。注意すべき点として、 goroutine は 1 つの CPU コアに固定されないということがあります。 Go だけで実装する場合には問題になりませんが、第 4 章で紹介した cgo ( Go から C 言語を扱うための方法)を使うと、 1 つの goroutine 内でスレッド ID がころころ変わることがあります。もちろんすべてのケースにおいて C 言語よりも Go が速くなるわけではありません。スレッド数がそれほど必要ないケースでは当然、 C 言語の実装のほうが早く終了します。逆に言うと、並行処理をたくさん行う必要があるケースでは、 Go は CPU コアを有効に活用できる処理を簡単に実装できるということになります。 goroutine は並行かつ並列と言えるのです。第 1 章で Go は goroutine を「湯水のように実行することができます」と書いたのは、このことを意味しています。 

# **6.3 channel** 

前述のとおり、並列処理においては作業者の間でコミュニケーションが必要な場合があります。コミュニケーションにも種類があります。 





### **相手の応答を待たなければならない対話相手にいつか伝わればいい程度の通知** 

これらをうまく扱うことができるのが channel です。第 3 章からサンプルコードを引用します( **リスト** 6.4)。 

**リスト6.4 goroutineとchannelを使った例(第3章リスト3.12と同コード)** 

func downloadCSV(wg *sync.WaitGroup, urls []string, ch chan []byte) { defer wg.Done() defer close(ch) // 終わったら閉じる(5) 

// HTTP サーバからのダウンロード for _, u := range urls { resp, err := http.Get(u) if err != nil { log.Println("cannot download CSV: ", err) continue } b, err := ioutil.ReadAll(resp.Body) if err != nil { 

resp.Body.Close() log.Println("cannot read content: ", err) continue } resp.Body.Close() ch <- b // main関数にコンテンツを送信(3) } } func main() { urls := []string { "http://my-server.com/data01.csv", "http://my-server.com/data02.csv", "http://my-server.com/data03.csv", (..略..) } // バイト列を転送するためのchannelを作成(1) ch := make(chan []byte) var wg sync.WaitGroup wg.Add(1) go downloadCSV(&wg, urls, ch) // (2) // goroutineからコンテンツを受け取る(4) for _, b := range ch { r := csv.NewReader(bytes.NewReader(r)) for { records, err := r.Read() if err != nil { log.Fatal(err) } // レコードの登録 insertRecords(records) } 

} wg.Wait() } 

まず、 **リスト** 6.4 **(** 1 **)** ではバイト列を転送するための channel を作成します。 make にバッファサイズを指定していないので、 channel への送信処理と channel からの受信処理は待ち合せることになります。次に **リスト** 6.4 **(** 2 **)** では作成した URL の一覧と channel を引数に与え、 goroutine を起動します。ここからは main 関数と downloadCSV の並行処理となります。 downloadCSV では URL 一覧を 1 つずつループさせ、 http.Get を使ってコンテンツをダウンロードします。 **リスト** 6.4 **(** 3 **)** でこのダウンロードしたバイト列を channel に送信しています。 

main 関数の **リスト** 6.4 **(** 4 **)** では channel を for で使用しています。 downloadCSV から送信されたバイト列が得られるので、それを bytes.Reader に与えて CSV の読み取りを開始し、レコード登録処理を行います。 

downloadCSV ではすべての URL を処理しきるとdefer close(ch)により channel が閉じられます( **リスト** 6.4 **(** 5 **)** )。それに連動して main 関数の for ループ( **リスト** 6.4 **(** 4 **)** )が終了します。ブロッキングが起きるhttp.Get(u)や 

ioutil.ReadAll(resp.Body)、場合によっては insertRecords の間にもそれぞれの goroutine が動き続けるため、高速に処理が完了します。 

# **6.4 非同期パターン** 

第 3 章では、 goroutine と channel 、そして sync.WaitGroup を使った基本的な並行処理の書き方を解説しました。本章では、これらを使った非同期の並行処理のテクニックを紹介します。 

goroutine と channel 、そして sync.WaitGroup だけで複雑なことを行うのは難しいように感じるかもしれません。しかし、これらを使ってたくさんの非同期パターンを実装できます。本来は、 goroutine を正常に終了させるために context を使って処理を中断し、 sync.WaitGroup を使って goroutine の終了を待ち合わせるべきですが、本章では要点を押さえるためにあえて省略します。 

## **ジェネレータ** 

**リスト** 6.5の処理は第 3 章にも登場しました。 

**リスト6.5 goroutineとchannelを使ったジェネレータの例** package main 

import ( "fmt" "time" ) 

func generator(msg string) <-chan string { ch := make(chan string) go func() { for i := 0; ; i++ { ch <- fmt.Sprintf("%s %d", msg, i) time.Sleep(time.Second) } }() return ch } func main() { ch := generator("Hello") for i := 0; i < 5; i++ { fmt.Println(<-ch) } } 

このソースコードを実行すると、以下の出力が 1 秒ごとに表示されます。 

Hello 0 Hello 1 Hello 2 Hello 3 Hello 4 

関数 generator の中で goroutine を起動し、 goroutine から返された channel を main 関数で受信します。ここで着目してほしいのは、関数宣言にある<-chanです。このように chan を宣言することで、返される chan が受信専用であることを明示できます。返された chan に対して送信を行うとコンパイルエラーになります。なお第 3 章で説明したとおり、 channel は main 側が受信を行わない限り、送信側がブロックするようになっています。ですので無限ループになりますが、 time.Sleep を削除しても負荷が掛かるわけではありません。 

## **合流処理** 

複数の generator を 1 つの channel にまとめる手法です( **リスト** 6.6)。 

#### **リスト6.6 合流処理の例** 

package main import ( "fmt" "time" ) 

func fanIn(ch1, ch2 <-chan string) <-chan string { new_ch := make(chan string) go func() { for { new_ch <- <-ch1 } }() go func() { for { new_ch <- <-ch2 } }() return new_ch } 

func generator(msg string) <-chan string { ch := make(chan string) go func() { for i := 0; ; i++ { ch <- fmt.Sprintf("%s %d", msg, i) time.Sleep(time.Second) } }() return ch } 

func main() { ch := fanIn(generator("Hello"), generator("Bye")) for i := 0; i < 10; i++ { fmt.Println(<- ch) } } 

2 つの処理の実施状況を混ぜて画面に表示する場合などに便利です。 

## **先着処理** 

合流と似ていますが、こちらは select を使うことで goroutine が 1 つ少なくなるのが特徴です( **リスト** 6.7)。 

#### **リスト6.7 先着処理の例** 

package main import ( "fmt" "time" ) func main() { ch := fanIn(generator("Hello"), generator("Bye")) for i := 0; i < 10; i++ { fmt.Println(<- ch) } } // fanIn is itself a generator func fanIn(ch1, ch2 <-chan string) <-chan string { // receives two read-only channels new_ch := make(chan string) 

go func() { for { select { case s := <-ch1: new_ch <- s case s := <-ch2: new_ch <- s } } }() return new_ch } func generator(msg string) <-chan string { // returns receive-only channel ch := make(chan string) go func() { // anonymous goroutine for i := 0; ; i++ { ch <- fmt.Sprintf("%s %d", msg, i) time.Sleep(time.Second) } }() return ch } 

なお、この select は ch1 または ch2 に入力があるまでブロックしますが、 default を足すことで、双方に入力がない場合の処理を書くことができます。 

for { select { case s := <-ch1: new_ch <- s case s := <-ch2: new_ch <- s default: update_progressbar() } } 

たとえば、プログレスバーの更新などに使うと良いでしょう。 

## **タイムアウト処理** 

**リスト** 6.8のプログラムは generator のループ内の処理が 1 秒以上かかると強制的に終了するものです。実行時の状況しだいでは 10 回ループするまでに途中で終了してしまうかもしれません。 

#### **リスト6.8 タイムアウト処理の例** 

package main import ( "fmt" "time" ) func main() { ch := generator("Hi!") for i := 0; i < 10; i++ { select { case s := <-ch: fmt.Println(s) case <-time.After(1 * time.Second): fmt.Println("Waited too long!") return } } } func generator(msg string) <-chan string { // returns receive-only channel ch := make(chan string) 

go func() { // anonymous goroutine for i := 0; ; i++ { ch <- fmt.Sprintf("%s %d", msg, i) time.Sleep(time.Second) } }() return ch } 

また、ループ 1 つのタイムアウトではなく、全体をタイムアウトさせたい場合には **リスト** 6.9のように実装します。 

**リスト6.9 リスト6.8を全体をタイムアウトさせるよう変更** 

package main import ( "fmt" "time" ) func main() { ch := generator("Hi!") timeout := time.After(5 * time.Second) for i := 0; i < 10; i++ { select { case s := <-ch: fmt.Println(s) case <-timeout: fmt.Println("5s Timeout!") return } } } func generator(msg string) <-chan string { // returns receive-only channel 

ch := make(chan string) go func() { // anonymous goroutine for i := 0; ; i++ { ch <- fmt.Sprintf("%s %d", msg, i) time.Sleep(time.Second) } }() return ch } 

この generator には goroutine を停止する処理がありません。 channel の送信がブロッキングするため、ループが回り続けることはありませんが、 goroutine は生き続けます。つまり、 generator を必要としなくなったあとは goroutine がメモリに残り続ける goroutine リークが発生します。本節の 2 つの例は main 関数が終了するとともに goroutine が停止するため、単独のプログラムとしては goroutine リークが起きることはありませんが、ライブラリとして何度か使う場合には goroutine リークが発生します。安全に goroutine を停止したい場合は、次の「停止処理」節を参考にするか、第 4 章で紹介した context パッケージを使うべきです。 

## **停止処理** 

これまでに説明した generator は、プログラム停止時に goroutine がリーク(実際にはプログラム終了時に停止)します。呼び出すたびにリークするため、このコードはライブラリとして使う場合には採用できません。 **リスト** 6.10のプログラムは generator に対して停止指示を出せるようにしています。 

#### **リスト6.10 停止処理の例** 

package main import ( "fmt" "math/rand" ) func main() { quit := make(chan bool) ch := generator("Hi!", quit) for i := rand.Intn(50); i >= 0; i-- { fmt.Println(<-ch, i) } quit <- true } func generator(msg string, quit chan bool) <-chan string { // returns receive-only channel ch := make(chan string) go func() { // anonymous goroutine for { select { case ch <- fmt.Sprintf("%s", msg): // nothing case <-quit: fmt.Println("Goroutine done") return } } }() return ch } 

停止指示を行ったあと、後処理が必要な場合には、さらに quit から応答をもらうと良いでしょう( **リスト** 6.11)。 

#### **リスト6.11 リスト6.10に後処理を追加** 

package main import ( "fmt" "math/rand" ) func main() { quit := make(chan string) ch := generator("Hi!", quit) for i := rand.Intn(10); i >= 0; i-- { fmt.Println(<-ch, i) } quit <- "Bye!" fmt.Printf("Generator says %s", <-quit) } func generator(msg string, quit chan string) <-chan string { // returns receive-only channel ch := make(chan string) go func() { // anonymous goroutine for { select { case ch <- fmt.Sprintf("%s", msg): // nothing case <-quit: quit <- "See you!" return } } }() return ch } 

確実に goroutine の停止を待つのであれば、前述の sync.WaitGroup を使うのが良いでしょう。ここまでの非同期パターンの実装例は Google I/O 2012 のトーク「 Go Concurrency Patterns 」で紹介されたものを参考にしています注6.1 ~~。~~ 

## **スロットリング(throttling)** 

goroutine はとても軽量であるため、「湯水のように」使うことができますが、それゆえに goroutine を生成し過ぎてしまい、その goroutine の中で行われる I/O がボトルネックになってしまったり、その対象に負荷を掛けてしまったりする場合があります。たとえば、ある Web サーバから 100 個の JSON ファイルをダウンロードするケースを考えてみてください。 1 個の JSON ファイルをダウンロードするのに 6 秒かかる場合、合計で 600 秒もかかってしまいます。これを 100 個の goroutine で同時にダウンロードすれば、計算上は 6 秒で完了することになります (実際にはそうなりませんが)。しかしながら、同時に 100 個もダウンロードを要求された Web サーバにはかなりの負荷が掛かってしまいます。 JSON の生成にメモリを使用するのであれば、サーバ側でスラッシング注6.2 ~~が~~ 発生し、シーケンシャルにダウンロードするよりも遅い結果になり得る場合もあります。こんなときに必要なのが、用途に合わせて頻度や速度を調節する「スロットリング」で、サーバが処理できる程度に調節/制御を行います。まずはシーケンシャルな例( **リスト** 6.12)を見てみましょう。 

#### **リスト6.12 WebサーバからJSONをダウンロードする処理(シーケンシャル)** 

package main 

import ( "fmt" "time" ) 

func downloadJSON(u string) { // download JSON println(u) time.Sleep(time.Second * 6) } 

func main() { before := time.Now() for i := 1; i <= 100; i++ { u := fmt.Sprintf("http://example.com/api/users?id=%d", i) downloadJSON(u) } fmt.Printf("%v\n", time.Since(before)) } 

これは終了までに 600 秒かかります。次に、 goroutine を生成して並列にします( **リスト** 6.13)。 

#### **リスト6.13 WebサーバからJSONをダウンロードする処理(並列)** 

package main 

import ( "fmt" "sync" "time" 

) func downloadJSON(u string) { // download JSON println(u) time.Sleep(time.Second * 6) } func main() { before := time.Now() var wg sync.WaitGroup for i := 1; i <= 100; i++ { wg.Add(1) i := i go func() { defer wg.Done() u := fmt.Sprintf("http://example.com/api/users?id=%d", i) downloadJSON(u) }() } wg.Wait() fmt.Printf("%v\n", time.Since(before)) } 

100 個の goroutine がいっきに生成されるので、計算上は 6 秒で終了します。しかし、スロットリングされていないので、サーバに 100 個のリクエストが送信されてしまいます。そこで goroutine の生成を 20 個に制限します。バッファ付きの chan が、バッファを超える要素を送信しようとしたときにブロックする特性を利用します( **リスト** 6.14)。 

#### **リスト6.14 WebサーバからJSONをダウンロードする処理(スロットリング)** 

package main 

import ( "fmt" "sync" "time" ) func downloadJSON(u string) { // download JSON println(u) time.Sleep(time.Second * 6) } 

func main() { before := time.Now() limit := make(chan struct{}, 20) var wg sync.WaitGroup for i := 1; i <= 100; i++ { wg.Add(1) i := i go func() { // limit が20よりも多いとブロックする limit <- struct{}{} // ←★ defer wg.Done() u := fmt.Sprintf("http://example.com/api/users?id=%d", i) downloadJSON(u) <-limit // limit から抜き出す }() } wg.Wait() 

fmt.Printf("%v\n", time.Since(before)) } 

これにより、 Web サーバには同時に 20 個しかリクエストが送信されないようになります。処理時間は、 1 回あたり 6 秒かかる 100 個のリクエストが 20 個ずつ処理されるので、計算上は 30 秒となります。 

limit に追加しているstruct{}は空の struct を意味します( **リスト** 6.14★部分)。サイズは 0 バイトのため、バッファ数が多くなったとしてもメモリを多く消費することはありません。 

- **注** 6.1 <u>https://github.com/kevchn/go-concurrency-patterns</u> (本文に戻る) 

- **注** 6.2 物理メモリが不足している状況でページの書き換えが頻繁に起きることでシステムの動作が極端に遅くなること。 (本文に戻る) 

# **6.5 まとめ** 

goroutine と channel を使い、速いプログラムを書くためのテクニックを紹介しました。 Go の並列処理では、いろいろな文法や機能を覚える必要がなく、 goroutine と channel だけでいろいろなパターンを実装できることを理解いただけたと思います。組み合わせしだいではこの他にも新しいパターンを実装できるでしょう。 



# **7.1 テストの考え方** 

一般的に、ソフトウェアにおけるテストには大きく 2 つの意味があります。 





### **品質を保証するため安全に壊すため** 

品質を保証するとは、テストが通っていることを他者に明示することにより、そのライブラリの使用者や開発の依頼元に対して品質の良さをアピールすることです。一方、安全に壊すためとは、一度書き上げたソースコードに対して変更を行う際に、テストがあることによって安心してコードに手を加えられることを指します。「壊す」というと聞こえが悪いのですが、これは悪いことではなく、リファクタリングをする際にエラーを出してくれることで、知らない間に仕様を壊すことなく、安全にリファクタリングできる効果があります。 

# **7.2 Goのテストの基本** 

Go でテストを書くには、 _test.go で終わるファイルのソースコードにテストコードを実装します。 **リスト** 7.1がテスト対象のコード、 **リスト** 7.2がテストコードです。 

#### **リスト7.1 テスト対象のコード** 

package hsd func StringDistance(lhs, rhs string) int { return Distance([]rune(lhs), []rune(rhs)) } func Distance(a, b []rune) int { dist := 0 if len(a) != len(b) { return -1 } for i := range a { if a[i] != b[i] { dist++ } } return dist } 

#### **リスト7.2 テストコード** 

package hsd 

import ( "testing" ) 

func TestStringDistance(t *testing.T) { got := StringDistance("foo", "foh") want := 2 if got != want { t.Fatalf("expected: %v, got %v:", want, got) } } 

※ 後述しますが、このテストは間違っています 

Go のテストは関数名を Test から始め、引数に *testing.T を持つ関数が実行されます。詳細はドキュメント注7.1 ~~を~~ 参照してください。テストの結果が失敗したことを明示するには 2 種類の関数を使い分けます。 





### **t.Fatal/t.Fatalf t.Error/t.Errorf** 

t.Fatal と t.Fatalf はその時点でテストを中断します。また t.Error と t.Errorf はエラーを明示したあともテストを続行します。いずれも、 fmt.Print や fmt.Printf と使い方は同じです。エラーを示す際には、期待値と結果の両方を出すと良いでしょう。このケースでは、 want を期待していたが got を得た、ということを示しています。さて、このコードはとても簡単なテストです。 2 つの文字列間の距離注7.2 ~~を~~ 得る StringDistance 関数へのテストで、文字列 foo と foh の距離 got を得て、期待値 want と比較します。テストを実行してみましょう。 

$ go test --- FAIL: TestStringDistance (0.00s) hsd_test.go:11: want 2, but 1: FAIL exit status 1 FAIL    github.com/mattn/go-hsd 0.294s 

このテストではあえて間違った期待値「 2 」を書いてあるのでテストは失敗します。正しいテスト( **リスト** 7.3)に修正して実行すると以下のようになります。 

#### **リスト7.3 リスト7.2を正しいテストに修正** 

package hsd import ( "testing" ) func TestStringDistance(t *testing.T) { got := StringDistance("foo", "foh") want := 1 if got != want { t.Fatalf("expected: %v, got %v:", want, got) } } 

=== RUN   TestStringDistance --- PASS: TestStringDistance (0.00s) PASS ok      github.com/mattn/go-hsd 0.291s 

テストをする際には、パッケージ名の指定に 2 つの方法があります。 



**テスト対象と同じパッケージ名称でテストコードを書く** 

### **テスト対象とは別のパッケージ名称でテストコードを書く** 



前者はパッケージ名称が同じですので、 private 関数もテストすることができます。一方、後者はあえて別名を付けることにより、 private な変数や関数にアクセスできないテストを書くことができるため、このパッケージの使用者と同じ実装をサンプルコードとして明示することができます。どちらを選んでもかまいませんが、前者でないとテストできないものがあることに注意してください。 

## **Table Driven Tests** 

Go では Table Driven Tests を推奨しています( **リスト** 7.4)。難しそうな名前が付いていますが、これは単純にテストケースをテーブルとしてまとめたものです。 

#### **リスト7.4 Table Driven Testsの実装例** 

package hsd 

import ( "reflect" "testing" ) 

func TestStringDistance(t *testing.T) { tests := []struct { name string lhs  string rhs  string want int }{ {name: "lhs is longer than rhs", lhs: "foo", rhs: "fo", want: -1}, 

{name: "rhs is shorter than rhs", lhs: "fo", rhs: "foo", want: -1}, {name: "No diff", lhs: "foo", rhs: "foo", want: 0}, {name: "1 diff", lhs: "foo", rhs: "foh", want: 1}, {name: "2 diffs", lhs: "foo", rhs: "fhh", want: 2}, {name: "3 diffs", lhs: "foo", rhs: "hhh", want: 3}, {name: "multibyte", lhs: "あいう", rhs: "あいえ", want: 1}, } 

for _, tc := range tests { got := StringDistance(tc.lhs, tc.rhs) if !reflect.DeepEqual(tc.want, got) { t.Fatalf("%s: expected: %v, got: %v", tc.name, tc.want, got) } } } 

このように、テーブルで入力条件と期待値をまとめておくことでテストの抜けを見つけやすくなり、テストを追加するのも簡単になります。また網羅性も良くなります。テーブルに持たせる項目はおおよそ以下になります。 







**テストの名前(name) 入力(この場合はlhsやrhs) 期待値(want)** 

とくにテスト名( name )は重要です。テーブルが多くなると、どのテストで失敗したかわからなくなります。もちろん、すべてのコードが Table Driven Tests でテストできるわけではありません。境界値チェックやバリエーションチェックが必要な関数に向いています。このようにテストをしっかりと書いておくことで、以降リファクタリングを行った際に、その変更が間違ったものであることに一早く気づくことができます。このことが「安全に壊す」なのです。 

<u>注</u> Table Driven Tests に関しては Deve Cheney 氏のブログ記 ~~事~~ <u>7.3</u> ~~が~~ 参考になります。なおリファクタリングをする前には必ず、git commitしておきましょう。 

## **t.Skip/t.Skipf** 

テストによっては特定の OS では実施できないものもあります。そういった場合には t.Skip または t.Skipf を使うことができます。 

func TestReadData(t *testing.T) { if runtime.GOOS != "windows" { t.Skipf("skipping on %v", runtime.GOOS) } // テストコード } 

このテストを Linux で実行すると、以下のようにスキップされたことが表示され、テストが続行していることがわかります。 

=== RUN   TestSimple --- PASS: TestSimple (0.00s) === RUN   TestReadData foo_test.go:19: skipping on linux --- SKIP: TestReadData (0.00s) PASS ok      foo     0.291s 

## **setup/teardown** 

テストを行う際に、前処理と後処理を書きたい場合があります。これを行うには TestMain を実装します( **リスト** 7.5)。 

#### **リスト7.5 テストの前処理と後処理** 

package main import ( "log" "os" "testing" ) func TestMain(m *testing.M) { log.Println("Before") ret := m.Run() log.Println("After") os.Exit(ret) } func TestA(t *testing.T) { log.Println("TestA running") } func TestB(t *testing.T) { log.Println("TestB running") } 

このテストを実行すると以下のようにテストが実行されます。 

$ go test 2022/12/25 19:06:50 Before 

2022/12/25 19:06:50 TestA running 2022/12/25 19:06:50 TestB running PASS 2022/12/25 19:06:50 After ok      go-book/testmain        0.437s 

TestMain は *testing.M を引数に取る必要があります。前処理と後処理の最中にエラーが発生した場合には、 os.Exit で異常終了したことを伝える必要があります。終了コード「 0 」以外で終了する際には log.Fatal を使うと便利です。 

func TestMain(m *testing.M) { if err := setup(); err != nil { log.Fatal("failed to setup:", err) } ret := m.Run() if err := teardown(); err != nil { log.Fatal("failed to teardown:", err) } os.Exit(ret) } 

## **t.Short** 

テストをたくさん実装していると、テストの中にも簡易なテストと詳細なテストといった区別が出てきます。詳細なテストをすべて実行すると物凄く時間がかかります。 Go では簡易なテストのみを実行するためのオプションが用意されています。 

$ go test -short 

-short を付けて実行した場合にはtesting.Short()が true を返します。各テストで -short のときにはスキップしたい場合には、以下のようにt.SkipNow()を呼び出します。 

func TestA(t *testing.T) { if testing.Short() { t.SkipNow() } log.Println("TestA running") } 

**リスト** 7.5の TestA を上記で書き換えて再度実行すると、以下のように TestA がスキップされます。 

$ go test -short 2022/12/25 19:28:39 Before 2022/12/25 19:28:39 TestB running PASS 2022/12/25 19:28:39 After ok      go-book/testmain        0.380s 

## **t.Parallel** 

たとえば **リスト** 7.6の関数があったとします。 

**リスト7.6 重い処理を行う関数** 

package calc import "time" func Add(a, b int) int { // 物凄く重たい足し算 result := a + b 

time.Sleep(3 * time.Second) // 重たい処理 return result } 

これをテストする **リスト** 7.7のテストコードを用意します。 

#### **リスト7.7 リスト7.6に対するテストコード** 

package calc import ( "testing" ) func TestAdd(t *testing.T) { tests := []struct { name string lhs  int rhs  int want int }{ {name: "test1", lhs: 0, rhs: 1, want: 1}, {name: "test2", lhs: 1, rhs: -1, want: 0}, {name: "test3", lhs: 2, rhs: 1, want: 3}, } for _, test := range tests { got := Add(test.lhs, test.rhs) if got != test.want { t.Errorf("%v: want %v, but %v:", test.name, test.want, got) } } } 

実行してみましょう。 

$ go test -v # vオプションは詳細表示 === RUN   TestAdd --- PASS: TestAdd (9.02s) PASS ok      calc    9.356s 

詳細は省きますが、とにかく遅い関数 Add のテストを 3 つ書いたら合計で 9 秒かかってしまいました。テストを追加するたびに遅くなるのではたまりません。そこで使えるのが t.Parallel です( **リスト** 7.8)。 

**リスト7.8 リスト7.6に対するテストコード(t.Parallelを利用)** 

func TestAdd(t *testing.T) { tests := []struct { name string lhs  int rhs  int want int }{ {name: "test1", lhs: 0, rhs: 1, want: 1}, {name: "test2", lhs: 1, rhs: -1, want: 0}, {name: "test3", lhs: 2, rhs: 1, want: 3}, } for _, test := range tests { test := test t.Run(test.name, func(t *testing.T) { t.Parallel() got := Add(test.lhs, test.rhs) if got != test.want { t.Errorf("%v: want %v, but %v:", test.name, test.want, got) } }) 

} } 

このように実行することで、テストが並行して実行されるようになります。 

=== RUN   TestAdd === RUN   TestAdd/test1 === PAUSE TestAdd/test1 === RUN   TestAdd/test2 === PAUSE TestAdd/test2 === RUN   TestAdd/test3 === PAUSE TestAdd/test3 === CONT  TestAdd/test1 === CONT  TestAdd/test3 === CONT  TestAdd/test2 --- PASS: TestAdd (0.00s) --- PASS: TestAdd/test2 (3.01s) --- PASS: TestAdd/test3 (3.01s) --- PASS: TestAdd/test1 (3.01s) PASS ok      calc    3.359s 

並行でテストが実行されたので 3 秒で終わるようになりました。とても便利ですが、 2 つ注意しないといけないことがあります。最初の注意点としては「 Table Driven Tests 」節で述べたとおり、必ずテストにユニークな名前を付けることです。並行で実行するのですから順序どおりには起動しません。また順序どおりに終了することもありません。どれが 2 つめのテストになるのかなどもわかりません。雑な名前でも良いので、見ただけでどのテストかわかるものを付けておきましょう。もう 1 つの注意点は、 Table Driven Tests を並行で実行する際にはループの中で test 変数を束縛することです。 

for _, test := range tests { test := test // ← ★ t.Run(test.name, func(t *testing.T) { t.Parallel() got := Add(test.lhs, test.rhs) if got != test.want { t.Errorf("%v: want %v, but %v:", test.name, test.want, got) } }) } 

★部分の処理を入れないと、タイミングによってはすべてのテストが tests に含まれる最後の項目を参照してしまいます。 

## **テストデータ** 

テストをする際に、外部のファイルを使うことがあります。このような場合には testdata というディレクトリにテストデータを置くことが多いです。 Go から提供されるいろいろなツールでは、この testdata というディレクトリを無視するようになっています。同じように Go のツールは.や_で始まるファイルやディレクトリも無視するようになっています。実際にテストデータを testdata ディレクトリに置いてテストを実施するには、 filepath.Glob を使うのが便利です( **リスト** 7.9)。 

#### **リスト7.9 filepath.Globの使用例** 

import ( "io/ioutil" "path/filepath" "testing" 

"github.com/google/go-cmp/cmp" ) func TestDoSomething(t *testing.T) { fns, err := filepath.Glob("testdata/*.dat") if err != nil { t.Fatal(err) } for _, fn := range fns { t.Log(fn) // 入力データを読む b, err := ioutil.ReadFile(fn) if err != nil { t.Fatal(err) } // 関数呼び出し got := doSomething(string(b)) // .datを.outに入れ替えて結果データを読み込む b, err = ioutil.ReadFile(fn[:len(fn)-4] + "out") if err != nil { t.Fatal(err) } want := string(b) if diff := cmp.Diff(want, got); diff != "" { t.Errorf(diff) } } } 

Go に標準で用意されている方法で want と got を比較しても良いのですが、何がどう違っていたのかが理解しづらい場合もあります。そこでこの例では比較に google/go-cmp を使いました。 go-cmp を使うと比較が行えるだけでなく、比較した結果が diff 形式で得られます。このテストを実行すると、以下のように diff 形式でエラーが表示されます。 

{ -     "value": 20 +     "value": 21 } 

## **カバレッジ** 

テストの進捗を示す指標としてカバレッジがあります。テストを実行した際に、テスト対象の本体プログラムが、テストによりどれくらいパスしたかを網羅率という数値で表します。これにより、どこをまだテストしていないのかを簡単に知ることができます。注意しなければならないのは、これはあくまでテストの進捗を示す値であり、本体プログラムの品質を表すものではないということです。ゆえに、カバレッジが高ければ品質も高いとは言えないことに注意してください。 

Go のテストスイートにはテストカバレッジを得るしくみがあります。まずは簡単にテストのカバレッジ率を得てみます。 

$ go test -v -cover === RUN   TestDistance --- PASS: TestDistance (0.00s) PASS 

github.com/mattn/go-jsd coverage: 95.3% of statements ok      github.com/mattn/go-jsd 0.332s 

このテストは 95.3% の網羅率であることが分かります。どこがまだテストを実施されていないのかを知るには、 cover プロファイルというファイルを生成する必要があります。 

$ go test -v -cover -coverprofile=cover.out 

このコマンドを実行するとテストが実行され、カレントディレクトリに cover.out というファイルが生成されます。 

mode: set github.com/mattn/go-jsd/jsd.go:3.46,5.2 1 1 github.com/mattn/go-jsd/jsd.go:7.26,8.13 1 1 github.com/mattn/go-jsd/jsd.go:8.13,10.3 1 1 github.com/mattn/go-jsd/jsd.go:11.2,11.11 1 1 github.com/mattn/go-jsd/jsd.go:14.26,15.13 1 1 github.com/mattn/go-jsd/jsd.go:15.13,17.3 1 1 github.com/mattn/go-jsd/jsd.go:18.2,18.11 1 1 github.com/mattn/go-jsd/jsd.go:21.35,22.32 1 1 github.com/mattn/go-jsd/jsd.go:22.32,23.23 1 1 (..略..) 

各行を何回パスしたかといった情報が格納されています。これだけでは見づらいですね。この cover プロファイルを可視化できるツールが用意されています。 

$ go tool cover -html=cover.out -o cover.html 

このコマンドを実行すると cover.out を解析して cover.html というファイルが生成されます。ブラウザで開くと **図** 7.1のような画面が表示されます。白黑の紙面ではわかりにくいですが、緑で表示されている行がパスしたコード、赤で表示されている行がパスしていないコードです。この表示を見ると、以下の部分がパスしていないことが分かります。 

func Distance(lhs, rhs []rune) float64 { rl1, rl2 := len(lhs), len(rhs) if rl1 == 0 || rl2 == 0 { return 0 // ← ★ } if rl1 == rl2 && isSame(lhs, rhs) { 

return 1 } 

これは、引数である lhs と rhs の、どちらかが空の場合を表すコードです。★の箇所が実行されるテストが用意されていないということです。テストコードを見ると確かに、空文字列のテストが欠けているのがわかります。 

func TestDistance(t *testing.T) { var tests = []struct { name string lhs  string rhs  string want float64 }{ {"test001", "こんにちわ世界", "こんにちわ世界", 1}, {"test002", "こんにちわ世界", "こにゃちわ世界", 0.876}, {"test003", "こんにちわ世界", "こにゃにゃちわ世界", 0.813}, {"test004", "こんにちわ世界", "こんばんわ世界", 0.742}, {"test005", "こんにちわ世界", "こんにちわ", 0.838}, {"test006", "こんにちわ世界", "こんばんわ", 0.453}, } 

以下のテストを足して再度テストしてみます。 

{"test007", "こんにちわ世界", "", 0}, 

$ go test -v -cover === RUN   TestDistance --- PASS: TestDistance (0.00s) PASS github.com/mattn/go-jsd coverage: 100.0% of statements ok      github.com/mattn/go-jsd 0.360s 

テストカバレッジが 100% になりました。このように、 Go のテストスイートにはテストカバレッジをサポートするためのツールが充実しています。 

CodeCo ~~v~~ <u>注7.4</u> ~~と~~ いう Web サービスを使用すると、このカバレッジを取得して Web 上できれいに表示してくれます( **図** 7.2)。さらに CodeCov ではバッジも提供されており、 GitHub の README.md に、 CodeCov から発行される以下のような記述を加えるだけで、見栄えよくカバレッジ率を確認することができます( **図** 7.3)。 

[![Codecov](https://codecov.io/gh/mattn/gorunewidth/branch/master/graph/badge.svg)] (https://codecov.io/gh/mattn/go-runewidth) 



使い勝手が良いので筆者もよく使います。なお cover.out にはテストコード以外のコードが含まれています。つまり、モックのコードも含まれてしまいます。go tool coverを実行する前に、 grep などを使って事前に取り除いておくと良いでしょう。 

$ go test -v -cover -coverprofile cover.tmp $ grep -v _mock.go cover.tmp > cover.out $ go tool cover -html=cover.out -o cover.html 

**注** 7.1 <u>https://pkg.go.dev/testing@go1.20.0</u> 

(本文に戻る) 

**注** 7.2 <u>https://ja.wikipedia.org/wiki/ハミング距離</u> 

(本文に戻る) 

**注** 7.3 <u>https://dave.cheney.net/2019/05/07/prefer-table-driven-tests</u> (本文に戻る) 

**注** 7.4 <u>https://about.codecov.io/</u> (本文に戻る) 

# **7.3 便利なテクニック** 

testing パッケージにはテストコードを書く際に便利な関数がたくさん用意されています。以下では筆者がとくに便利だと思うものを紹介します。 

## **テンポラリディレクトリ** 

テストでは一時的に作業ディレクトリが必要な場合もあります。そういった場合は、テストコードの中でディレクトリを作って defer で消すのも良いですが、 testing パッケージにはすでにそれが用意されています( **リスト** 7.10)。 

#### **リスト7.10 t.TempDir()の使用例** 

package profile_test 

import ( "path/filepath" "testing" ) 

func TestCreateProfile(t *testing.T) { dir := t.TempDir() filename := filepath.Join(dir, "profile.json") got, err := CreateProfile(filename) if err != nil { t.Fatal(err) 

} want := true if got != want { t.Fatalf("want %v, but %v:", want, got) } } 

このt.TempDir()は何度も呼び出すことができます。また TestCreateProfile が終了すると自動でテンポラリディレクトリが削除されます。とても便利ですので積極的に使うべきです。 

## **環境変数** 

Go 1.17 からt.Setenv()が使えるようになっています。これは環境変数に依存した処理をテストする際に便利です( **リスト** 7.11)。 

**リスト7.11 t.Setenv()の使用例** 

package cmd_test 

import ( "os/exec" "path/filepath" "testing" ) func TestCreateProfile(t *testing.T) { t.Setenv("DATABASE_URL") err := doSomething() if err != nil { t.Fatalf("cannot do something: %v", err) } } 

t.TempDir()と同様に、テスト関数の終了とともに環境変数が元に戻されます。 

# **7.4 Fuzzingテスト** 

Go には Fuzzing テストのためのツールが用意されています。 Fuzzing テストとは、開発者が準備したテストの入力データではなく、開発者が予期しないであろうランダムで無効なデータを用いてテストする手法です。たとえば、「 int のスライスを引数に受け取るがスライスの ⻑さを 3 のときしか期待していない関数」は、スライスの⻑ さが 2 のときには panic が発生してしまいます。また、カンマセパレートされた文字列を受け取る関数に、 1 つもカンマが含まれていない文字列を渡した場合にどのような動作になるかは、テストを書いてみないと検証できません。本体プログラムでは当然考慮済みだと思ってテストを実装していると、そういった例外的な入力値のテストの実装が漏れることもあります。 Fuzzing テストはこのような想定外の入力を与えることで、リリース後に起き得る未知のバグを未然に防ぐ目的で使われます。 

Fuzzing テストの実装は簡単です( **リスト** 7.12)。 

**リスト7.12 Fuzzingテストの例** package something_test 

import ( "testing" ) 

func FuzzDoSomething(f *testing.F) { 

f.Add("test&&&") f.Fuzz(func(f *testing.T, s string) { doSomething(s) }) } 

Fuzzing テストの関数名は Fuzz という接頭語を持ちます。また通常のテストでは testing.T 、ベンチマークでは testing.B のポインタを引数に取りますが、 Fuzzing テストでは testing.F のポインタを受け取ります。以下を実行するとテストが開始されます。 

$ go test -fuzz FuzzDoSomething 

このまま実行すると、 Fuzzing テストはバグが見つかってクラッシュするまで終了しません。 -fuzztime を指定して終了時間を指定します。 

$ go test -fuzz FuzzDoSomething -fuzztime 10s 

この指定では 10 秒経過するとテストが終了します。 f.Add は Fuzzing テストに与えるコーパス(つまりパターン集のシード)です。これを与えることにより、どのような数値や文字列で Fuzz 関数が呼び出されるかが決定されます。実際にはテストのカバレッジが通るであろう値を入れておくと、その近辺の値を使って呼び出してくれるようになっています。 

Fuzz 関数に渡す関数の引数(第 2 引数以降)には、以下の型を指定できます。 

string, []byte int, int8, int16, int32/rune, int64 uint, uint8/byte, uint16, uint32, uint64 

float32, float64 bool 

複数の引数を取る関数に対しては、以下のように Add と Fuzz で受け取る引数をそれぞれ指定します。 

func FuzzDoSomething(f *testing.F) { f.Add(3, "test&&&") f.Fuzz(func(f *testing.T, i int, s string) { doSomething(i, s) }) } 

Fuzzing テストでは testdata ディレクトリにテストで使用したパターンが格納されています。テストを実行してクラッシュが起きた場合には、その内容を確認してプログラムを修正することができます。 

$ go test -fuzz FuzzDoSomething -fuzztime 10s 5 test fuzz: elapsed: 0s, gathering baseline coverage: 0/7 completed fuzz: elapsed: 0s, gathering baseline coverage: 7/7 completed, now fuzzing with 8 workers fuzz: minimizing 31-byte failing input file fuzz: elapsed: 0s, minimizing --- FAIL: FuzzDoSomething (0.26s) --- FAIL: FuzzDoSomething (0.00s) main_test.go:17: testing.go:1485: panic: runtime error: index out of range [1] with length 1 goroutine 226 [running]: runtime/debug.Stack() /home/mattn/dev/go/src/runtime/debug/stack.go:24 +0x9e testing.tRunner.func1() 

/home/mattn/dev/go/src/testing/testing.go:1485 +0x1f6 panic({0x1024000, 0xc000116048}) /home/mattn/dev/go/src/runtime/panic.go:884 +0x213 github.com/mattn/go-fuzz-example_test.doSomething(...) /home/mattn/go/src/github.com/mattn/go-fuzz-testingexample/main_ test.go:8 github.com/mattn/go-fuzzexample_test.FuzzDoSomething.func1(0xc0001a0000, {0x1170598, 0x1}) /home/mattn/go/src/github.com/mattn/go-fuzz-testingexample/main_test .go:18 +0x9d reflect.Value.call({0x1002580?, 0x10442e8?, 0xe6ea96?}, {0x1033938, 0x4}, {0xc00019a060, 0x2, 0x2?}) 

/home/mattn/dev/go/src/reflect/value.go:586 +0xb07 reflect.Value.Call({0x1002580?, 0x10442e8?, 0x1149d00?}, {0xc00019a060?, 0x1032f20?, 0xc000112040?}) /home/mattn/dev/go/src/reflect/value.go:370 +0xbc testing.(*F).Fuzz.func1.1(0x0?) /home/mattn/dev/go/src/testing/fuzz.go:335 +0x3f3 testing.tRunner(0xc0001a0000, 0xc0001a2000) /home/mattn/dev/go/src/testing/testing.go:1576 +0x10b created by testing.(*F).Fuzz.func1 /home/mattn/dev/go/src/testing/fuzz.go:322 +0x5b9 Failing input written to testdata/fuzz/FuzzDoSomething/def578230616f8b9 To re-run: go test -run=FuzzDoSomething/def578230616f8b9 FAIL exit status 1 FAIL    github.com/mattn/go-fuzz-example        0.713s 

以下の行に着目してください。 

Failing input written to testdata/fuzz/FuzzDoSomething/def578230616f8b9 

これはテストで試したコーパスが記載されているファイル名です。内容は以下のとおりです。 

go test fuzz v1 string("\xff") 

スタックトレースの中身から doSomething に問題があることが分かります。修正し、同じコーパスを使って再度テストを実行します。 

$ go test -run=FuzzDoSomething/def578230616f8b9 PASS 

ok      github.com/mattn/go-fuzz-example        0.332s 

うまく修正できたことが確認できました。必要に応じてこのパターンを個別のテストに起こしておくと良いでしょう。 

func TestDoSomethingWithFF(t *testing.T) { doSomething("\xff") } 

また-fuzz FuzzDoSomethingを付けて実行して他に問題がないかも確認しておきます。 

$ go test -fuzz FuzzDoSomething -fuzztime 10s 5 test 

fuzz: elapsed: 0s, gathering baseline coverage: 0/4 completed fuzz: elapsed: 0s, gathering baseline coverage: 4/4 completed, now fuzzing with 8 workers 

fuzz: elapsed: 3s, execs: 357375 (118780/sec), new interesting: 3 (total: 7) 

fuzz: elapsed: 6s, execs: 750183 (131113/sec), new interesting: 3 (total: 7) 

fuzz: elapsed: 9s, execs: 1117395 (122327/sec), new interesting: 4 (total: 8) 

fuzz: elapsed: 11s, execs: 1243338 (81424/sec), new interesting: 4 (total: 8) PASS 

ok      github.com/mattn/go-fuzz-example        10.878s 

このように Go の Fuzzing テストは問題検出の改善のイテレーションを繰り返すことができるようになっています。 

# **7.5 まとめ** 

Go によるテストの方法と、便利なテクニック、 Fuzzing テストについて解説しました。テストは 1 回限りではなく繰り返し行うことが重要です。新しい機能を足した際にテストも足しておくことを習慣づけることで、リファクタリングの際に安心してソースコードを壊すことができるようになります。 



# **8.1 「推測するな、計測せよ」** 

本節名となっているこの言葉は、 Go の生みの親の 1 人である Rob Pike 氏によるものです。パフォーマンスを改善する前には計測が必要です。あるボトルネックを改善して速くなったと思っていたら、実は修正により別の箇所を遅くしてしまっていた、なんてことは当たり前に起きます。改善前と改善後を何度も比較し続けることが、速度改善のコツです。計測し終えるまでは、スピードハックを入れるべきではありません。 

# **8.2 ベンチマークの基本** 

Go のベンチマークはgo testと似たしくみになっています。以下の something パッケージの DoSomething 関数を計測したいとします。 

package something 

func DoSomething() { // 処理 } 

ソースコードが something.go である場合、以下の something_test.go ( **リスト** 8.1)を用意します(ファイル名のプレフィックスを合わせる必要はありませんが、そろっていたほうが便利です)。 

**リスト8.1 something.goのベンチマークを取るコード (something_test.go)** 

package something 

import ( "testing" ) 

func BenchmarkDoSomething(b *testing.B) { for i := 0; i < b.N; i++ { DoSomething() 

} } 

### シェルから以下を実行します。この結果の意味は、左の BenchmarkDoSomething-8 が実行したベンチマーク、真ん中の 5 が試行回数、右が 1 オペレーションあたりにかかった時間です。試行回数は 5 に決まっているわけではなく、一定時間内に実行された回数になります。なお、-bench DoSomethingはベンチマーク名を指定するためのフラグで、 BenchmarkXXX の「 XXX 」部分を指定するための正規表現パターンです。すべてのベンチマークを実行するのであれば以下で良いでしょう。 

#### $ go test -bench . 

Go のベンチマークで計測できるのは処理時間だけではありません。メモリアロケーション量も計測できます。そのためには以下のコマンドを実行します。 



- -benchmem を付けることで一処理あたりのメモリアロケー 

- ション数が得られます。 B/op は 1 回のオペレーションで何バイトのメモリ確保が実行されたかを、 allocs/op は 1 回のオペレーションで何回メモリ確保が実行されたかを示しています。 

# **8.3 ベンチマークの比較** 

ここからはベンチマークの比較方法について紹介します。ある実装の改善前と改善後、そして似た処理の違う実装同士を比較します。 

## **改善の前と後の比較** 

ベンチマークはそのあとに行うパフォーマンスの改善に役立ちます。パフォーマンス改善は単純な作業ではありません。思いつく施策を幾度も試すことで、改善前よりも悪くなってしまうこともあります。そうした改善の中、都度ベンチマークを得て本当に改善しているのかを確認するのが重要です。例として **リスト** 8.2の関数のベンチマークを取ってみます。 

#### **リスト8.2 メモリアロケーションの効率が悪い例** 

package something 

import "fmt" 

func makeSomething(n int) []string { var r []string for i := 0; i < n; i++ { r = append(r, fmt.Sprintf("%05d 何か", i)) 

} return r } 

指定した個数ぶん「 00000 何か」というスライスを作成して返すだけの関数です。このソースコードはスライスを都度 append しているため、メモリアロケーションの効率が悪い例としてよく紹介されます。 **リスト** 8.3のベンチマークコードを実装して計測します。 

#### **リスト8.3 リスト8.2のベンチマークコード** 

package something 

import "testing" 

func BenchmarkMakeSomething(b *testing.B) { b.ResetTimer() for i := 0; i < b.N; i++ { _ = makeSomething(1000) } } 

ベンチマークを計測する際には、ベンチマークを得るたびに発生する「揺らぎ」を考慮する必要があります。実行回数が少な過ぎると揺れが大きくなり過ぎてしまい、ベンチマークを取る意味がなくなってしまいます。 -count で回数を指定します。リスト 8.2 の makeSomething を **リスト** 8.4のように修正します。 

#### **リスト8.4 リスト8.2を改善** 

func makeSomething(n int) []string { r := make([]string, n, n) for i := 0; i < n; i++ { r[i] = fmt.Sprintf("%05d 何か", i) } return r } 

あらかじめ必要な⻑さのスライスを用意して代入するソースコードです。スライスが都度、伸⻑しないので効率が良くなりました。再度ベンチマークを取ります。 



1op あたりのメモリ使用量が半分程度であることが分かります。また 1op あたりの実行速度も少ないことが分かります。 2 つのベンチマークを before/after として比較するために benchstat をインストールします。 

$ go install golang.org/x/perf/cmd/benchstat@latest 

old.log と new.log を指定します。 



1op あたりの実行時間が -45.33% 、メモリアロケーションサイズが -47.03% 、メモリアロケーション回数が -0.63% になりました。なお、改修の前後でベンチマークを比較するには、前後で同じ条件でなければなりません。 

## **異なる実装の比較** 

ベンチマークを得るモチベーションのもう 1 つは、同じ目的のために用意された異なる実装方式を比較することです。例として、 Go で有名なデータフレームを 3 種類使ってベンチマークを比較してみます。比較するデータフレームライブラリは以下のとおりです。 







**github.com/go-gota/gota github.com/rocketlaunchr/dataframe-go github.com/tobgu/qframe** 

また比較の条件は以下のとおりです。 







**iris.csvをインポートすること出力する列をsepal_lengthとspeciesに絞ること CSVデータを(メモリへ)エクスポートすること** 

ベンチマークを取るコードは **リスト** 8.5のとおりです。 

**リスト8.5 3つのデータフレームのベンチマークコード** package df_test import ( "bytes" "context" "io/ioutil" "testing" "github.com/go-gota/gota/dataframe" "github.com/rocketlaunchr/dataframe-go/exports" "github.com/rocketlaunchr/dataframe-go/imports" "github.com/tobgu/qframe" ) func BenchmarkQFrame(b *testing.B) { bs, err := ioutil.ReadFile("iris.csv") if err != nil { b.Fatal(err) } b.ReportAllocs() b.ResetTimer() for i := 0; i < b.N; i++ { var out bytes.Buffer qf := qframe.ReadCSV(bytes.NewReader(bs)) qf = qf.Select("sepal_length", "species") err = qf.ToCSV(&out) if err != nil { b.Fatal(err) } 

} } func BenchmarkGota(b *testing.B) { bs, err := ioutil.ReadFile("iris.csv") if err != nil { b.Fatal(err) } b.ReportAllocs() b.ResetTimer() for i := 0; i < b.N; i++ { var out bytes.Buffer df := dataframe.ReadCSV(bytes.NewReader(bs)) df = df.Select([]string{"sepal_length", "species"}) err = df.WriteCSV(&out, dataframe.WriteHeader(true)) if err != nil { b.Fatal(err) } } } func BenchmarkDataframeGo(b *testing.B) { bs, err := ioutil.ReadFile("iris.csv") if err != nil { b.Fatal(err) } b.ReportAllocs() b.ResetTimer() for i := 0; i < b.N; i++ { var out bytes.Buffer df, err := imports.LoadFromCSV(context.Background(), bytes.NewReader(bs)) if err != nil { b.Fatal(err) } df.RemoveSeries("sepal_width") df.RemoveSeries("petal_length") 

df.RemoveSeries("petal_width") err = exports.ExportToCSV(context.Background(), &out, df) if err != nil { 

b.Fatal(err) } } } 

同じ目的でも実装方法が異なるのがおもしろいですね。ベンチマークを取ってみます。 



qframe が一番速く、メモリアロケーションの量も少ないことが分かります。当然ですが、ベンチマークを取る際には、各ライブラリがほぼ同じ目的で同じ実装をしていることを確認すべきです。このように、ライブラリの選定をする際には、使い勝手の良さだけでなくベンチマークを取ってみてパフォーマンスの良いものを選ぶことも大事です。もちろん、⻑期的なメンテナンスがされるか否かも重要な要素になりますが。 

Go はこういったベンチマークを簡単に得られるしくみを提供していることがおわかりいただけた思います。 

# **8.4 プロファイリング** 

Go のテストスイートにはプロファイルを得るしくみも用意されています。 **リスト** 8.6の、とにかくメモリを確保する重たいプログラムのプロファイルを取得してみます。 

#### **リスト8.6 メモリを大量に確保する重たいプログラム** 

package main 

import ( "sync" ) 

func heavyFunc(wg *sync.WaitGroup) { defer wg.Done() s := make([]string, 3) for i := 0; i < 1000000; i++ { s = append(s, "magical pandas") } } 

func main() { var wg sync.WaitGroup wg.Add(1) go heavyFunc(&wg) wg.Wait() } 

このプログラムは、⻑さの決まったスライスに文字列を追加し続ける処理です。 **リスト** 8.7のように変更します。 

#### **リスト8.7 リスト8.6を、プロファイリングを取るよう変更** 

package main 

import ( "flag" "log" "os" "runtime/pprof" "sync" ) func heavyFunc(wg *sync.WaitGroup) { defer wg.Done() s := make([]string, 3) for i := 0; i < 1000000; i++ { s = append(s, "magical pandas") } } var cpuprofile = flag.String("cpuprofile", "", "write cpu profile to `file`") var memprofile = flag.String("memprofile", "", "write memory profile to `file`") 

func main() { flag.Parse() if *cpuprofile != "" { f, err := os.Create(*cpuprofile)  // (1) if err != nil { log.Fatal("could not create CPU profile: ", err) } defer f.Close() if err := pprof.StartCPUProfile(f); err != nil { 

log.Fatal("could not start CPU profile: ", err) } defer pprof.StopCPUProfile() // (2) } var wg sync.WaitGroup wg.Add(1) go heavyFunc(&wg) wg.Wait() if *memprofile != "" { f, err := os.Create(*memprofile) // (3) if err != nil { log.Fatal("could not create memory profile: ", err) } defer f.Close() //runtime.GC() if err := pprof.WriteHeapProfile(f); err != nil { log.Fatal("could not write memory profile: ", err) } } } 

処理本体の前に CPU プロファイルの開始( **リスト** 8.7 **(** 1 **)** )と defer による停止の予約( **リスト** 8.7 **(** 2 **)** ) を、本体処理のあとにヒープのプロファイル出力( **リスト** 8.7 **(** 3 **)** )を、それぞれ実行します。 CPU プロファイルを得るには以下の手順でプログラムを起動します。 

$ ./実行ファイル -cpuprofile cpu.prof 

プログラムが終了すると、カレントディレクトリに cpu.prof が生成されます。 CPU プロファイル結果を参照するには以下のコマンドを実行します。 



• flat は「関数の中で他の関数を呼び出した時間」を含まな 

• い結果です。 sum はスタック履歴での flat 累計です。 cum は「関数の中で関数を呼び出した時間」を含んだ結果です( **図** 8.1)。この結果を見ると、 CPU 負荷が高かった上位の関数はランタイムの処理だとわかります。以下のようにフラグを指定することで、 cum でソートした結果を表示できます。ランタイムによるものを除くと、 heavyFunc が一番、 CPU 負荷が高かったことになります。同様にメモリのプロファイルも参照します。メモリプロファイルを得るには以下の手順でプログラムを起動します。 

$ ./実行ファイル -memprofile mem.prof 

プログラムが終了すると、カレントディレクトリに mem.prof が生成されます。メモリプロファイル結果を参照するには以下のコマンドを実行します。 



heavyFunc が一番、メモリ負荷が高かったようです。 -png フラグを付けると呼び出しの相関がわかります。以下の手順で CPU プロファイルの相関グラフを生成できます。 

$ go tool pprof -png cpu.prof > out.png 

このコマンドを実行するためには graphvis の dot コマンドが必要です。あらかじめインストールしておいてください。実行すると out.png ( **図** 8.2)が生成されます。画像の上部に main.heavyFunc があります( **図** 8.3)。 



### さらに以下のコマンドを実行します。 

$ go tool pprof -http=:8080 cpu.prof 

すると Web ブラウザが起動し、相関だけでなくフレームグラフも表示できます( **図** 8.4)。どの関数がどれだけ処理時間を使っているのかがわかりやすく表示されています。また[ Source ]を選択することで、コードの中でどの部分に処理時間がかかっているのかを簡単に知ることができます( **図** 8.5、8.6)。今回の例では append に多くの処理時間を使っているのがわかります。ただし今回、プロファイルを得るためにプログラムに多くの処理を追加してしまいました。 Go にはこれをもう少し簡単にするためのしくみがあります。それが net/http/pprof です。以下のように net/http/pprof をブランク import (第 4 章参照)し、 goroutine の中で Web サーバを起動します。また、プログラムがすぐに終了してしまわないように処理をループにしておきます( **リスト** 8.8)。 

#### **リスト8.8 net/http/pprofの使用例** 

package main import ( "log" "net/http" _ "net/http/pprof" "sync" ) func heavyFunc(wg *sync.WaitGroup) { defer wg.Done() s := make([]string, 3) for i := 0; i < 1000000; i++ { s = append(s, "magical pandas") } } func main() { go func() { log.Println(http.ListenAndServe("localhost:6060", nil)) }() for { var wg sync.WaitGroup wg.Add(1) go heavyFunc(&wg) wg.Wait() 

} } 

このプログラムを起動したあと、別の端末から以下を実行します。 

$ curl -s http://localhost:6060/debug/pprof/profile > cpu.prof 

プロファイルの取得には 30 秒かかります(待てない場合は ?seconds=N を付与)。 

cpu.prof が生成されたあとの流れは前述のとおりです。 net/http/pprof にはこのほかにも計測可能な項目がたくさん提供されています。 











**http://localhost:6060/debug/pprof/heap http://localhost:6060/debug/pprof/block http://localhost:6060/debug/pprof/goroutine http://localhost:6060/debug/pprof/threadcreate http://localhost:6060/debug/pprof/mutex** 

このように、 Go はプロファイルを得るしくみも標準で用意されています。これらは Go でコンパイルされるアプリケーションだけでなく Go 本体のプロファイリングにも使用され、常にパフォーマンスの改善に取り組んでいます。 

# **8.5 まとめ** 

Go でベンチマークやプロファイルを得る方法を紹介しました。今までアプリケーションのパフォーマンス改善に関して難易度が高いと諦めてきた方も、きっと Go のツールでは簡単に取り組めると思います。 



⻘空文庫(https://www.aozora.gr.jp/) 

# **9.1 システムのコンセプト** 

本プログラムは **表** 9.1の 2 つのプログラムで実現します。コードは筆者の GitHub <u>注9.1</u> ~~で~~ 公開しています。 

**注** 9.1 <u>https://github.com/mattn/aozora-search</u> (本文に戻る) 

# **9.2 データベース登録プログラム** 

本プログラムで扱う技術(パッケージ)は **表** 9.2になります。まずは⻘空文庫のテキストをダウンロードします。 Go でインターネットサイトの情報をダウンロードして解析するライブラリとしては、 goquery が有名です。スクレイピングできるライブラリは他にも存在しますが、 goquery はバランスが良く、多くのユーザーから使われています。まずは以下のコマンドを実行してプロジェクトを初期化します。 

$ go mod init github.com/mattn/aozora-search go: creating new go.mod: module github.com/mattn/aozorasearch 

※ 実行される際は「mattn」の部分をご自身のGitHubに置き換えてくださいまずは芥川龍之介(⻘空文庫内では「芥川⻯之介」)の作品リストを得るプログラムを作りましょう。 goquery を使うのでgo getを使ってパッケージをインポートします 

$ go get github.com/PuerkitoBio/goquery 

プログラムのディレクトリを作成し、 main.go をエディタで開きます。 

$ mkdir -p cmd/aozora-collector $ vim cmd/aozora-collector/main.go 

## **COLUMN プログラムを作る手順** 

まずは直近のゴールを決定しましょう。プログラムを作るうえで大事なのは、いきなり最終的なゴールを決めないことです。プログラムは小さな処理や関数を寄せ集めて作られます。そしてそれらの小さな関数は、単一で動作すべきなのです。プログラムが起動したら実行されるmain関数を少しずつ修正していき、まとまった単位で関数に切り出していくことでプログラムができあがります。main関数にすべての処理を書いても良いのですが、そうするとテストがしづらくなります。テストだけではありません。たとえば特定の関数だけを実行して動作確認したい場合でも、すべての処理手順を実行しなければなりません。プログラムを完成させる近道だと思い、こまめに関数分けしましょう。 

## **ZIPファイルのURL一覧を得る** 

⻘空文庫では小説のテキストファイルを ZIP ファイルで提供しており、直近のゴールとして必要なのは、その ZIP ファイルの URL 一覧の取得です。作者の情報ページに書かれている作品リストから、個々の詳細ページをたどり、 ZIP ファイルの URL を集める処理を実装しましょう。おそらく今後、タイトルと詳細ページ、および ZIP の URL が必要になるため、それらをまとめる Entry という struct を用意します( **リスト** 9.1)。 

**リスト9.1 作品情報をまとめるstruct「Entry」** package main 

import ( "fmt" "log" "github.com/PuerkitoBio/goquery" ) type Entry struct { AuthorID string Author   string TitleID  string Title    string InfoURL  string ZipURL   string } func findEntries(siteURL string) ([]Entry, error) { // 処理 } func main() { 

listURL := "https://www.aozora.gr.jp/index_pages/person879.html" 

entries, err := findEntries(listURL) if err != nil { log.Fatal(err) } for _, entry := range entries { fmt.Println(entry.Title, entry.ZipURL) } } 

リスト 9.1 で使う、 goquery で URL から DOM ( Document Object Model )オブジェクトを作成する findEntries は **リスト** 9.2のように実装します。 

**リスト9.2 URLからDOMオブジェクトを作成するfindEntries関数** func findEntries(siteURL string) ([]Entry, error) { doc, err := goquery.NewDocument(siteURL) if err != nil { return nil, err } // 処理 return nil, nil // ひとまず両方nilを返しておく } 

⻘空文庫の作品一覧の HTML を見ると以下のようになっています(執筆時点の HTML であり今後変更される可能性があります)。 

<h2><a name="sakuhin_list_1">公開中の作品</a></h2> 

<ol> <li><a href="../cards/000879/card4872.html">愛読書の印象 

</a> (新字旧仮名、作品ID:4872) </li> <li><a href="../cards/000879/card16.html">秋</a> (新字旧仮名、作品ID:16) </li> 

<li><a href="../cards/000879/card178.html">芥川⻯之介歌集 </a> (新字旧仮名、作品ID:178) </li> 

<li><a href="../cards/000879/card15.html">アグニの神</a> (新字旧仮名、作品ID:15) </li> 

<li><a href="../cards/000879/card43014.html">アグニの神</a> (新字新仮名、作品ID:43014) </li> <li><a href="../cards/000879/card3804.html">悪魔</a> (新字旧仮名、作品ID:3804) </li> 

(..略..) </ol> 

作成した doc の Find メソッドに CSS セレクタを渡すことで、指定したノードを得ることができます。 **リスト** 9.3のようにリンク URL 一覧を得ます。 

**リスト9.3 リンクURL一覧を取得する** 

doc.Find("ol li a").Each(func(n int, elem *goquery.Selection) { println(elem.Text(), elem.AttrOr("href", "")) }) 

これを実行すると以下のテキストが出力されます。愛読書の印象 ../cards/000879/card4872.html 秋 ../cards/000879/card16.html 芥川⻯之介歌集 ../cards/000879/card178.html アグニの神 ../cards/000879/card15.html 

アグニの神 ../cards/000879/card43014.html 悪魔 ../cards/000879/card3804.html 浅草公園 ../cards/000879/card21.html (..略..) 

⻘空文庫の ZIP ファイルへの URL は以下の形式になっています。 

https://www.aozora.gr.jp/cards/6桁の作者ID/card作品ID.html 

この場合、芥川⻯之介の作者 ID は 000879 、作品 ID は上記で得た cardXXXX の XXXX 部分(例: 4872 )になります。先ほどの、 href 属性から得た概要ページの相対パスに含まれている情報を見ると、その両方が得られるのがわかります。正規表現を使って抜き出しましょう。リスト 9.3 を **リスト** 9.4のように修正して、 ZIP ファイルが含まれる詳細ページへの URL を得ます。 

#### **リスト9.4 リスト9.3に詳細ページへのURLを正規表現で抜き出す処理を追加** 

pat := regexp.MustCompile(`.*/cards/([0-9]+)/card([0-9]+).html$`) doc.Find("ol li a").Each(func(n int, elem *goquery.Selection) { token := pat.FindStringSubmatch(elem.AttrOr("href", "")) if len(token) != 3 { return } pageURL := fmt.Sprintf("https://www.aozora.gr.jp/cards/%s/card%s.html", token[1], token[2]) println(pageURL) }) 

詳細ページの HTML では table タグの中に ZIP ファイルへのパスが含まれています。扱いやすいように **リスト** 9.5、9.6 のようにプログラムを変更します。 

#### **リスト9.5 リスト9.4にfindAuthorAndZIP(リスト9.6)を追加** 

pat := regexp.MustCompile(`.*/cards/([0-9]+)/card([0-9]+).html$`) doc.Find("ol li a").Each(func(n int, elem *goquery.Selection) { token := pat.FindStringSubmatch(elem.AttrOr("href", "")) if len(token) != 3 { return } pageURL := fmt.Sprintf("https://www.aozora.gr.jp/cards/%s/card%s.html", token[1], token[2]) author, zipURL := findAuthorAndZIP(pageURL) // 作者とZIPファイルのURLを得る println(zipURL) }) 

#### **リスト9.6 作者とZIPファイルのURLを得る関数のひな形** 

func findAuthorAndZIP(siteURL string) (string, string) { doc, err := goquery.NewDocument(siteURL) if err != nil { return "", "" } zipURL := "" // 処理 return zipURL } 

ZIP の URL は download という class 属性の付いた table タグの中に含まれる a タグで、かつ拡張子が .zip のものになります。 **リスト** 9.7のようにリスト 9.6 を変更しましょう。 

#### **リスト9.7 リスト9.6を実装する** 

func findAuthorAndZIP(siteURL string) (string, string) { doc, err := goquery.NewDocument(siteURL) if err != nil { return "", "" } author := doc.Find("table[summary=作家データ] tr:nth-child(1) td:nth-child(2)").Text() zipURL := "" doc.Find("table.download a").Each(func(n int, elem *goquery.Selection) { href := elem.AttrOr("href", "") if strings.HasSuffix(href, ".zip") { zipURL = href } }) return author, zipURL } 

### これを実行すると以下が出力されます。 

./files/4872_ruby_20864.zip ./files/16_ruby_344.zip ./files/178_ruby_2210.zip ./files/15_ruby_904.zip ./files/43014_ruby_17392.zip ./files/3804_ruby_27189.zip ./files/21_ruby_1427.zip ./files/43361_ruby_17690.zip ./files/17_ruby_377.zip ./files/14_ruby_1261.zip ./files/60574_txt_71363.zip (..略..) 

相対パスですので扱いが面倒です。 net/url パッケージを使って相対 URL から絶対 URL に変更しましょう。得られた zipURL は引数 siteURL からの相対パスです。まずは siteURL をパースします。 

u, err := url.Parse(siteURL) if err != nil { return "" } 

siteURL のディレクトリ部分は path パッケージの path.Dir 関数を使って得られるので、そのディレクトリと相対パスである zipURL を path.Join 関数を使ってパス結合します。 

<mark>path.Join(path.Dir(u.Path), zipURL)</mark> 

これを u.Path に戻し、 String メソッドを使って元の URL 文字列に戻します。 zipURL がはじめから URL 形式だった場合も想定しておきましょう( **リスト** 9.8)。 

#### **リスト9.8 リスト9.7に相対URLから絶対URLに変換する処理を追加** 

func findAuthorAndZIP(siteURL string) (string, string) { log.Println("query", siteURL) doc, err := goquery.NewDocument(siteURL) if err != nil { return "", "" } 

author := doc.Find("table[summary=作家データ]:nth-child(1) tr:nth-child(2) td:nthchild(2)").Text() 

zipURL := "" 

doc.Find("table.download a").Each(func(n int, elem *goquery.Selection) { href := elem.AttrOr("href", "") if strings.HasSuffix(href, ".zip") { zipURL = href } }) if zipURL == "" { return author, "" } if strings.HasPrefix(zipURL, "http://") || strings.HasPrefix(zipURL, "https://") { return author, zipURL } u, err := url.Parse(siteURL) if err != nil { return author, "" } u.Path = path.Join(path.Dir(u.Path), zipURL) return author, u.String() } 

### これを実行すると以下が出力されます。 

https://www.aozora.gr.jp/cards/000879/files/4872_ruby_20864.zip https://www.aozora.gr.jp/cards/000879/files/16_ruby_344.zip https://www.aozora.gr.jp/cards/000879/files/178_ruby_2210.zip https://www.aozora.gr.jp/cards/000879/files/15_ruby_904.zip https://www.aozora.gr.jp/cards/000879/files/43014_ruby_17392.zi p https://www.aozora.gr.jp/cards/000879/files/3804_ruby_27189.zip https://www.aozora.gr.jp/cards/000879/files/21_ruby_1427.zip https://www.aozora.gr.jp/cards/000879/files/43361_ruby_17690.zi p https://www.aozora.gr.jp/cards/000879/files/17_ruby_377.zip 

https://www.aozora.gr.jp/cards/000879/files/14_ruby_1261.zip (..略..) 

findEntries 関数に **リスト** 9.9を追加して、 findAuthorAndZIP から得た Entry 一覧を main 関数に戻すようにします。 

#### **リスト9.9 findEntries関数(リスト9.2)に追加する処理** 

entries := []Entry{} doc.Find("ol li a").Each(func(n int, elem *goquery.Selection) { token := pat.FindStringSubmatch(elem.AttrOr("href", "")) if len(token) != 3 { return } title := elem.Text() pageURL := fmt.Sprintf("https://www.aozora.gr.jp/cards/%s/card%s.html", token[1], token[2]) author, zipURL := findAuthorAndZIP(pageURL) // 作者とZIPファイルのURLを得る if zipURL != "" { entries = append(entries, Entry{ AuthorID: token[1], Author:   author, TitleID:  token[2], Title:    title, SiteURL:  siteURL, ZipURL:   zipURL, }) } }) return entries, nil 

この時点で、このプログラムが以下の構成になっていることに注目してください。 

### **最終的な処理は深く呼び出した関数の中では行わないエラーが起きた場合はその場で強制終了せずmainに戻す** 





これが Go でうまくプログラムを作るうえでの重要なポイントになります。書き捨ての小さなスクリプトレベルのプログラムであれば途中で panic を起こしたり log.Fatal で強制終了したりしても良いと思いますが、のちのちメンテナンスしていくプログラムでは足かせになってしまいます。とくにライブラリを開発している場合には重要で、今までエラーを返していなかった関数について、エラーを返すために、関数定義を変えなければならなくなります。その関数を使っていたライブラリユーザーに対して非互換な変更を強制することになってしまいます。はじめの設計時からエラーを返す想定をしておくことが非常に重要で、こうしておくことで関数を単独で検証することができるようになります。今回の例であれば findZipURL や findEntries を試験したくなった際には、 main から直接呼び出して簡単に確認することができます。 

## **ZIPファイルを展開する** 

次は得られた ZIP ファイルへの URL から、実際に ZIP ファイルを展開する処理を実装しましょう。 main 関数のループを **リスト** 9.10に変更しましょう。 

**リスト9.10 リスト9.1のmain関数のループを変更** 

for _, entry := range entries { content, err := extractText(entry.ZipURL) if err != nil { log.Println(err) continue } fmt.Println(entry.SiteURL) fmt.Println(content) } 

main 関数から呼び出す extractText 関数の定義も作っておきます。 

func extractText(zipURL string) (string, error) { // 処理 } 

ZIP ファイルの中には拡張子 txt のファイルが含まれています。 archive/zip パッケージを使ってコンテンツを得ます。 archive/zip はバイト列を Reader として読み込ませる必要があるので、まずは net/http パッケージを使ってコンテンツをダウンロードし、バイト列を得るまでを実装します。 

resp, err := http.Get(zipURL) if err != nil { return "", err } defer resp.Body.Close() b, err := ioutil.ReadAll(resp.Body) if err != nil { return "", err } 

変数 b にダウンロードした ZIP ファイルの内容が格納されました。 bytes.NewReader を使って ZIP ファイルを読み込みます。 

r, err := zip.NewReader(bytes.NewReader(b), int64(len(b))) if err != nil { return "", err } 

r にはファイル一覧が保持されているため、これをループして拡張子が .txt のファイルを探します。見つけたらコンテンツを得て返します。 

for _, file := range r.File { if path.Ext(file.Name) == ".txt" { f, err := file.Open() if err != nil { return "", err } b, err := ioutil.ReadAll(f) f.Close() if err != nil { return "", err } return string(b), nil } } 

これで ZIP のコンテンツが得られました。ただし実行するとわかりますが、コンテンツは ShiftJIS です。 Go はプログラムの内部では UTF-8 で扱いたいので、エンコーディングを変換しましょう。 Go には標準パッケージとは別に、よく使われるであろう便利なパッケージが数多く用意されています ( **表** 9.3)。このうち、今回は x/text に含まれる 

golang.org/x/text/encoding/japanese パッケージを使ってエンコーディング変換を行います。 extractText 全体は **リスト** 9.11になります。 

#### **リスト9.11 extractTextを実装する** 

func extractText(zipURL string) (string, error) { resp, err := http.Get(zipURL) if err != nil { return "", err } defer resp.Body.Close() b, err := ioutil.ReadAll(resp.Body) if err != nil { return "", err } r, err := zip.NewReader(bytes.NewReader(b), int64(len(b))) 

for _, file := range r.File { if path.Ext(file.Name) == ".txt" { f, err := file.Open() if err != nil { return "", err } b, err := ioutil.ReadAll(f) f.Close() if err != nil { return "", err } b, err = japanese.ShiftJIS.NewDecoder().Bytes(b) if err != nil { return "", err } return string(b), nil } } return "", errors.New("contents not found") } 

## **データベースに登録する** 

さて、ここからは別のフェーズになります。いったん、これまでのプログラムを忘れ、新規にテンポラリのプロジェクトを作って、データベースの登録部分のみを実装し、それを本プロジェクトに肉付けしていくと良いでしょう。データベースはどの RDBMS でも良いですが、お手軽でかつ全文検索が可能な SQLite 3 を選びました。 Go では database/sql パッケージを使って SQLite 3 のデータベースを開くには **リスト** 9.12のように実装します。 

#### **リスト9.12 SQLite 3のデータベースを開くひな形** 

package main import ( "database/sql" "log" _ "github.com/mattn/go-sqlite3" ) func main() { db, err := sql.Open("sqlite3", "database.sqlite") if err != nil { log.Fatal(err) } defer db.Close() // 処理 } 

まずはデータベースの設計を行います。著者やタイトル、 URL といった情報を扱い、さらに全文検索を行うのであれば、以下の 2 つのテーブルが必要になります。 





### **著者情報テーブルコンテンツ情報** 

これらに加えて、全文検索のために FTS4 ( Full Text Search version 4 )を扱う仮想テーブルを用意します( **リスト** 9.13)。 

**リスト9.13 リスト9.12をもとにデータベースの設計処理を実装** package main import ( "database/sql" 

"log" _ "github.com/mattn/go-sqlite3" ) func main() { db, err := sql.Open("sqlite3", "database.sqlite") if err != nil { log.Fatal(err) } defer db.Close() queries := []string{ `CREATE TABLE IF NOT EXISTS authors(author_id TEXT, author TEXT, PRIMARY KEY (author_id))`, `CREATE TABLE IF NOT EXISTS contents(author_id TEXT, title_id TEXT, title TEXT, content TEXT, PRIMARY KEY (author_id, title_id))`, `CREATE VIRTUAL TABLE IF NOT EXISTS contents_fts USING fts4(words)`, } for _, query := range queries { _, err = db.Exec(query) if err != nil { log.Fatal(err) } } // 処理 } 

今回は芥川⻯之介の情報を全文検索する部分のみを実装するため、サンプルデータを登録します。 

b, err := os.ReadFile("ababababa.txt") if err != nil { 

log.Fatal(err) } b, err = japanese.ShiftJIS.NewDecoder().Bytes(b) if err != nil { log.Fatal(err) } content := string(b) res, err := db.Exec(`INSERT INTO contents(author_id, title_id, title, content) values (?, ?, ?, ?)`, "000879", "14", "あばばばば", content, ) if err != nil { log.Fatal(err) } docID, err := res.LastInsertId() 

最後の行で docID を得ているのは、 contents_fts に登録する際の docid として使うためです。 FTS4 では自動的に docid というカラムが付与されます。通常は autoincrement されますが、これを明示的に指定しています。なお『あばばばば』の作品データは <u>https://www.aozora.gr.jp/cards/000879/card14.html から事前にダ</u> ウンロードしておいてください。 FTS4 のカラム words には、小説テキストを分かち書きした文字列を登録しましょう。 <u>注</u> Go で分かち書きされた文字列を得るのであれば、 kagom ~~e~~ <u>9.2</u> ~~が~~ 使えます。 

kagome は Pure Go で開発されている日本語形態素解析器です。 MeCab IPADIC と UniDIC 、 2 つの辞書を扱うことができます。日本語の形態素解析をするプログラムとして、筆者もよく利用します。 kagome を使って分かち書き文字列を取得し、 contents_fts テーブルに登録するには **リスト** 9.14のように実装します。前述した docID を使う必要があります。 

**リスト9.14 分かち書き文字列を取得してテーブルに登録する** t, err := tokenizer.New(ipa.Dict(), tokenizer.OmitBosEos()) if err != nil { log.Fatal(err) } seg := t.Wakati(content) _, err = db.Exec(` INSERT INTO contents_fts(docid, words) values(?, ?) `, docID, strings.Join(seg, " "), ) if err != nil { log.Fatal(err) } 

最後に正しく動作しているか確認しましょう。以下のクエリを実行して、レコードが得られることを確認します。 

query := "虫 AND ココア" rows, err := db.Query(` SELECT a.author, c.title FROM contents c INNER JOIN authors a ON a.author_id = c.author_id INNER JOIN contents_fts f ON c.rowid = f.docid 

AND words MATCH ? `, query) if err != nil { log.Fatal(err) } defer rows.Close() for rows.Next() { var author, title string err = rows.Scan(&author, &title) if err != nil { log.Fatal(err) } fmt.Println(author, title) } 

「あばばばば」が表示されれば正解です。これで、 SQLite 3 を使った全文検索のデータベース登録処理が実装できました。リスト 9.13 、 14 も合わせて、本体に組み込みます( **リスト** 9.15)。 

**リスト9.15 芥川⻯之介の作品リストをデータベースに登録するプログラム** 

func setupDB(dsn string) (*sql.DB, error) { db, err := sql.Open("sqlite3", dsn) if err != nil { return nil, err } queries := []string{ `CREATE TABLE IF NOT EXISTS authors(author_id TEXT, author TEXT, PRIMARY KEY (author_id))`, `CREATE TABLE IF NOT EXISTS contents(author_id TEXT, title_id TEXT, title TEXT, content TEXT, PRIMARY KEY (author_id, title_id))`, 

`CREATE VIRTUAL TABLE IF NOT EXISTS contents_fts USING fts4(words)`, } for _, query := range queries { _, err = db.Exec(query) if err != nil { return nil, err } } return db, nil } func addEntry(db *sql.DB, entry *Entry, content string) error { _, err := db.Exec(` REPLACE INTO authors(author_id, author) values(?, ?) `, entry.AuthorID, entry.Author, ) if err != nil { return err } res, err := db.Exec(` REPLACE INTO contents(author_id, title_id, title, content) values(?, ?, ?, ?) `, entry.AuthorID, entry.TitleID, entry.Title, content, ) if err != nil { return err } docID, err := res.LastInsertId() if err != nil { 

return err } t, err := tokenizer.New(ipa.Dict(), tokenizer.OmitBosEos()) if err != nil { return err } seg := t.Wakati(content) _, err = db.Exec(` REPLACE INTO contents_fts(docid, words) values(?, ?) `, docID, strings.Join(seg, " "), ) if err != nil { return err } return nil } func main() { db, err := setupDB("database.sqlite") if err != nil { log.Fatal(err) } defer db.Close() listURL := "https://www.aozora.gr.jp/index_pages/person879.html" entries, err := findEntries(listURL) if err != nil { log.Fatal(err) } log.Printf("found %d entries", len(entries)) for _, entry := range entries { 

log.Printf("adding %+v\n", entry) content, err := extractText(entry.ZipURL) if err != nil { log.Println(err) continue } err = addEntry(db, &entry, content) if err != nil { log.Println(err) continue } } } 

本当はもっとうまく作者をまとめ、 1 回で 1 レコードのみ INSERT INTO すべきですが、⻘空文庫に登録されている作者情報は単著によるものだけではありません。たとえば別の作者の作品で、芥川⻯之介は翻訳者として登場する場合もあります。今回は芥川⻯之介の作者情報 URL からスクレイピングを開始しましたが、他の作者からスクレイピングを始めても芥川⻯之介は登場します。実行後に登録された作者情報テーブルは以下のようになりました。 

001086|ゴーチェテオフィル 000338|フランスアナトール 001085|イエイツウィリアム・バトラー 002016|ダ・ヴィンチレオナルド 000879|芥川 ⻯之介作者数 5 人、作品の数は 378 作品となりました。他の作者情報からさらにスクレイピングすることもできますが、本書ではここまでとします。 

**注** 9.2 <u>https://github.com/ikawaha/kagome</u> 

(本文に戻る) 

# **9.3 データベース照会プログラム** 

次はこのデータベースを照会するプログラムを作っていきましょう。照会プログラムの仕様は **表** 9.4とします。プログラムのディレクトリを作成し、 main.go をエディタで開きます。 

$ mkdir -p cmd/aozora-search $ vim cmd/aozora-search/main.go 

Go にはサブコマンドを扱うために便利なパッケージがたくさんありますが、ここではそれらを使わずに実装してみます。これは本章後半で解説するコマンドラインパーサが、どれくらい効率的で機能性が高いのか、それらを使わないプログラムがどれだけ大変なのかを理解していただくためです。表 9.4 のコマンドラインの仕様を愚直に実装すると **リスト** 9.16のようになります。 

#### **リスト9.16 照会プログラムのコマンドをコマンドラインパーサを使わず実装** 

func main() { var dsn string flag.StringVar(&dsn, "d", "database.sqlite", "database") flag.Usage = func() { fmt.Print(usage) } flag.Parse() if flag.NArg() == 0 { flag.Usage() os.Exit(2) } db, err := sql.Open("sqlite3", dsn) if err != nil { log.Fatal(err) } defer db.Close() switch flag.Arg(0) { case "authors": err = showAuthors(db) case "titles": if flag.NArg() != 2 { flag.Usage() os.Exit(2) } err = showTitles(db, flag.Arg(1)) case "content": if flag.NArg() != 3 { flag.Usage() os.Exit(2) } err = showContent(db, flag.Arg(1), flag.Arg(2)) case "query": 

if flag.NArg() != 2 { flag.Usage() os.Exit(2) } err = queryContent(db, flag.Arg(1)) } if err != nil { log.Fatal(err) } } 

一見、わかりやすいようにも見えますが、サブコマンドが増えるたびにこの switch 関数が増えるのはメンテナンスコストが高いのです。また、このソースコードではデータベース接続文字列を指定するフラグ -d だけをグローバルなフラグとして扱いますが、サブコマンドに対するフラグが必要となった場合には実装が複雑になります。今回実装したデータベース照会プログラムではサブコマンドがそれほど多くありませんが、こういったプログラムをちゃんと実装するのであれば、コマンドラインパーサを扱うのが良いでしょう。 

# **9.4 テストの実装** 

テストを実装しましょう。 findEntries および findAuthorAndZIP 、 extractText のテストをします。このプログラムは HTTP クライアントを使って⻘空文庫から情報を取得します。そのまま実行結果を比較するテストを書いてしまうと、テストのたびに⻘空文庫にアクセスします。⻘空文庫のサーバに負荷を掛けてしまいますし、インターネットにアクセスできない状況ではテストが失敗してしまいます。 Go には HTTP を使ったプログラムのテストを簡単にするためのしくみが用意されています( **リスト** 9.17)。 

**リスト9.17 FindEntriesに対するテストコードのひな形** 

func TestFindEntries(t *testing.T) { ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http. Request) { // サーバのコード })) defer ts.Close() // テストコード } 

このコードはテストサーバを起動するコードです。 ts.URL にアクセスすることでテストサーバにアクセスできます。 http.Client が必要であればts.Client()を呼び出すことで得られます。 aozora-collector の場合「サーバのコード」と書かれている部分に、⻘空文庫のサーバを疑似するコードを書くことになります。ただし、 findEntries は www.aozora.gr.jp にアクセスするように作者情報の URL を作っています。 **リスト** 9.18 のように pageURLFormat をグローバル変数にすることで、テストコードから作品情報の URL 書式を変更できるようにしておきます。 

#### **リスト9.18 findEntriesの変更個所** 

var pageURLFormat = 

"https://www.aozora.gr.jp/cards/%s/card%s.html" 

func findEntries(siteURL string) ([]Entry, error) { (..略..) 

doc.Find("ol li a").Each(func(n int, elem *goquery.Selection) { (..略..) 

pageURL := fmt.Sprintf(pageURLFormat, token[1], token[2]) author, zipURL := findAuthorAndZIP(pageURL) // ZIPファイルのURLを得る 

### テストコードは **リスト** 9.19のように書き換えます。 

#### **リスト9.19 TestFindEntrie(リスト9.17)の変更個所** 

func TestFindEntries(t *testing.T) { 

tmp := pageURLFormat pageURLFormat = ts.URL + "/cards/%s/card%s.html" defer func() { pageURLFormat = tmp }() // 処理 } 

こうすることで、テストコードの場合だけ、テストサーバを⻘空文庫のサーバとしてアクセスできるようになります。/にアクセスした場合は作者情報を、それ以外は作品情報を返却するには **リスト** 9.20のように実装します。 

#### **リスト9.20 最終的なTestFindEntrieの実装** 

ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { fmt.Println(r.URL.String()) if r.URL.String() == "/" { w.Write([]byte(` <table summary="作家データ"> <tr><td class="header">作家名:</td><td><font size="+2"> テスト太郎</font></td></tr> 

<tr><td class="header">作家名読み:</td><td>テスト太郎 </td></tr> <tr><td class="header">ローマ字表記:</td><td>Test, Taro</td></tr> </table> <ol> <li><a href="../cards/999999/card001.html">テスト書籍 001</a></li> <li><a href="../cards/999999/card002.html">テスト書籍 002</a></li> 

<li><a href="../cards/999999/card003.html">テスト書籍 003</a></li> </ol> `)) } else { pat := regexp.MustCompile(`.*/cards/([0-9]+)/card([09]+).html$`) token := pat.FindStringSubmatch(r.URL.String()) w.Write([]byte(fmt.Sprintf(` 

<table summary="作家データ"> <tr><td class="header">作家名:</td><td><font size="+2"> テスト太郎</font></td></tr> <tr><td class="header">作家名読み:</td><td>テスト太郎 </td></tr> <tr><td class="header">ローマ字表記:</td><td>Test, Taro</td></tr> </table> <table border="1" summary="ダウンロードデータ" class="download"> <tr> <td><a href="./files/%[1]s_%[2]s.zip">%[1]s_% [2]s.zip</a></td> </tr> </table> `, token[1], token[2]))) } })) defer ts.Close() tmp := pageURLFormat pageURLFormat = ts.URL + "/cards/%s/card%s.html" defer func() { pageURLFormat = tmp }() got, err := findEntries(ts.URL) if err != nil { t.Error(err) return } 

最後に findEntries を呼び出して得たエントリ一覧をチェックします。 

want := []Entry{ { AuthorID: "999999", Author:   "テスト太郎", TitleID:  "001", Title:    "テスト書籍001", SiteURL:  ts.URL, ZipURL:   ts.URL + "/cards/999999/files/999999_001.zip", }, { AuthorID: "999999", Author:   "テスト太郎", TitleID:  "002", Title:    "テスト書籍002", SiteURL:  ts.URL, ZipURL:   ts.URL + "/cards/999999/files/999999_002.zip", }, { AuthorID: "999999", Author:   "テスト太郎", TitleID:  "003", Title:    "テスト書籍003", SiteURL:  ts.URL, ZipURL:   ts.URL + "/cards/999999/files/999999_003.zip", }, } if !reflect.DeepEqual(want, got) { t.Errorf("want %+v, but got %+v", want, got) } 

これで findEntries と findAuthorAndZIP のテストができました。次に extractText のテストです。 extractText は ZIP ファイルをダウンロードしてその ZIP ファイルに含まれるテキストファイルを展開して返却するので、事前にテストデータとして 

ZIP ファイルを用意しておき、テストサーバをファイルサーバとして振る舞わせます。「テストデータ」と書かれたテキストファイル(ファイル名: example.txt )を ZIP で圧縮し、 testdata というディレクトリの中に置いておきます。テストコードは **リスト** 9.21になります。 

#### **リスト9.21 extractTextに対するテストコード** 

func TestExtractText(t *testing.T) { ts := httptest.NewServer(http.FileServer(http.Dir("."))) defer ts.Close() 

got, err := extractText(ts.URL + "/testdata/example.zip") if err != nil { t.Fatal(err) return } want := "テストデータ\n" if want != got { t.Errorf("want %+v, but got %+v", want, got) } } 

このようにすることで、⻘空文庫のサーバと同じように ZIP ファイルがダウンロードされ、中のテキストファイルがコンテンツして展開されるまでをテストできます。 ◆ ◆ ◆ 完成したコマンド CLI アプリケーションのコマンドをいくつか試してみました( **図** 9.1)。 



# **9.5 CLI開発に役立つライブラリ** 

Go は CLI ( Command Line Interface )のアプリケーションが作りやすいプログラミング言語と言われています。 Go 言語そのものが UNIX 文化の中で生きてきた人たちによって作られていることも大きいですが、 Go 本体や周辺ツールの多くが小さいコマンドラインプログラムの集合で構成されている点、そしてそれらを扱うサードパーティアプリケーションもまたコマンドラインで提供されることが多い点が理由であろうと筆者は考えています。 

Go にはたくさんの CLI 向けライブラリがそろっています。本章後半ではそれらのうち、とくによく使われているものを紹介していきます。 

# **9.6 フラグライブラリ** 

CLI の開発では、コマンドに渡されたフラグを処理する実装(コマンドインパーサ)が必要ですが、 Go ではそれを効率的に行える便利なライブラリが多くあります。本章前半の CLI アプリケーションはそれらライブラリを使わず実装したので、サブコマンドを実装するために以下のような switch 文を書く必要がありました。 

switch flag.Arg(0) { case "authors": ... case "titles": ... case "content": ... case "query": ... } 

本節で紹介するライブラリを使うことで、ソースコードをきれいに記述することができるようになります。ソースコードがきれいに分割できるので、テストもしやすくなります。 

## **flagパッケージ** 

こちらは Go に標準で用意されているフラグパーサです。フラグを扱うための基本的な機能のみを提供するもので、サブコマンドなどは扱えません。コマンドライン引数として、フラグと入力文字列だけを扱うようなプログラムを作る際に便利です。 



### **使い方** 

**リスト** 9.22に簡単な例を示します。 

**リスト9.22 flagパッケージの使用例(flag.Intの場合)** package main import ( "flag" ) func main() { max := flag.Int("max", 255, "max value") name := flag.String("name", "somthing", "my name") flag.Parse() println(*name, *max) } 

このプログラムを実行すると「 something 255 」が表示されます。続いて -h を付けて実行します。 

$ ./main -h Usage of ./main: -max int max value (default 255) -name string my name (default "somthing") 

max は int 型のポインタ、 name は string 型のポインタですが、どちらも nil になることはありません。flag.Parse()が実行されたタイミングで引数が解析され、不正な引数(たとえば max に文字列)が渡された場合には、プログラムを中断して使用方法を表示します。値の妥当性はチェックされないので開発者自身が値のチェックを行わなければなりません。扱える型には Bool 、 Float64 、 Int 、 Int64 、 Uint 、 Uint64 と、 encoding.TextMarshaler 、 time.Duratoin があります。 

flag.Int は int 型のポインタを返しますが、ポインタでない値がほしいときもあります。そういった場合には Int ではなく IntVar を使います( **リスト** 9.23)。 

#### **リスト9.23 flagパッケージの使用例(flag.IntVarの場合)** 

package main import ( "flag" ) func main() { var name string var max int flag.IntVar(&max, "max", 255, "max value") flag.StringVar(&name, "name", "", "my name") flag.Parse() // 処理 } 

Int と IntVar がペアであるように、 String と StringVar 、 Bool と BoolVar など対応するメソッドがあります。 flag パッケージがフラグを解析したあとの残りの引数は、 flag.Args から得ることができます。 

func main() { var name string var max int 

flag.IntVar(&max, "max", 255, "max value") flag.StringVar(&name, "name", "", "my name") flag.Parse() 

for _, arg := range flag.Args() { fmt.Println(arg) } } 

直接 N 番目の引数を得たい場合はflag.Arg(3)のように指定することもできます( 0 始まりです)。 

flag パッケージでは引数が与えられなかったときにエラーにするしくみが提供されていません。もし引数が与えられなかった場合にエラーにするのであれば、 flag.NArg を使ってチェックします。 

if flag.NArg() == 0 { flag.Usage() os.Exit(1) } 



### **flag.Duration/flag.DurationVar** 

筆者が flag パッケージの中で好きな機能の 1 つです。第 4 章で time.Duration を紹介しましたが、これを flag として受け取ることができます( **リスト** 9.24)。 

**リスト9.24 flagパッケージの使用例(flag.Durationの場合)** package main 

import ( 

"flag" "time" ) 

func main() { var sleep time.Duration flag.DurationVar(&sleep, "sleep", time.Second, "sleep time") flag.Parse() 

time.Sleep(sleep) 

} 

このように実装すると、コマンドラインフラグに 3s や 4m 、 5h といった経過時間を渡すことができます。 

$ ./main -sleep 3s 



### **flag.Text/flag.TextVar** 

encoding.TextMarshaler は MarshalText を、 encoding.TextUnmarshaler は UnmarshalText メソッドを持ったインターフェースです。 

type TextMarshaler interface { MarshalText() (text []byte, err error) } 

type TextUnmarshaler interface { UnmarshalText(text []byte) error } 

これらのインターフェースを実装した型を flag パッケージで扱うことができます。たとえば net.IP は IP アドレスを表現する型ですが、両方のインターフェースを実装しています。よって IP アドレスを得るコマンドラインフラグを **リスト** 9.25のように実装することができます。 

#### **リスト9.25 flagパッケージの使用例(flag.TextVarの場合)** 

package main 

import ( "flag" "fmt" "net" ) func main() { var ip net.IP flag.TextVar(&ip, "ip", net.IPv4(127, 0, 0, 1), "ip address") flag.Parse() 

fmt.Println(ip) } 

執筆時点で encoding.TextMarshaler と encoding.TextUnmarshaler を実装している型は以下のとおりです。 









**encoding/gobパッケージのTextGobber型 timeパッケージのTime型 netパッケージのIP型 math/bigパッケージのInt、Float、Rat型** 

time.Time を使う場合は RFC 3339 のフォーマットで以下のように指定する必要があります。 

$ ./main -t 2022-11-26T08:23:05Z07:00 

もちろん、 encoding.TextMarshaler と 

encoding.TextUnmarshaler を両方実装した型を用意すれば、自分で作った型を TextVar に渡すこともできます。 



### **テストの書き方** 

flag パッケージは簡素に書けてちょっとしたプログラムを書くには便利な反面、以下のような面もあります。 

### **テストが書きづらい** 



**複数のパッケージでflag.Parseを実行できないフラグの定義と値の妥当性チェック処理がソース上でまとめづらい** 





Go でプログラムを書く際に筆者がよく使う方法として、 run という関数を用意するというものがあります。別に関数名は run でなくてもかまいませんが、処理の決まりとしては、プログラム名を除いた引数文字列の配列と、終了コードを返すために int の戻り値を取る点があります( **リスト** 9.26)。 

#### **リスト9.26 関数runの実装** 

package main 

import ( "flag" "fmt" "os" ) 

var ( commandLine = flag.NewFlagSet(os.Args[0], flag.ExitOnError) ) 

func run(args []string) int { max := commandLine.Int("max", 255, "max value") name := commandLine.String("name", "", "my name") if err := commandLine.Parse(args); err != nil { fmt.Fprintf(os.Stderr, "cannot parse flags: %v\n", err) 

} if *max > 999 { fmt.Fprintf(os.Stderr, "invalid max value: %v\n", *max) return 1 } if *name == "" { fmt.Fprintln(os.Stderr, "name must be provided") return 1 } // 正常処理 return 0 } func main() { os.Exit(run(os.Args[1:])) } 

flag.Int や flag.String 、 flag.Parse はグローバル変数に格納されたフラグセットを使って処理されるため、複数回実行されるテストではフラグが重複定義されてしまいます。結果として、テストは常にエラーになってしまいます。 run を作っておけば **リスト** 9.27のように、 commandLine をグローバル変数に置き、テストコードから毎回書き換えることができます。 

#### **リスト9.27 runを利用したCLIアプリのテストコード** 

package main 

import ( "flag" "os" "testing" ) 

func TestFlagVar(t *testing.T) { tests := []struct { name string args []string want int }{ {name: "test1", args: []string{"-name", "foo"}, want: 0}, {name: "test2", args: []string{"-name", "foo", "-max", "1000"}, want: 1}, {name: "test3", args: []string{"-name", "", "-max", "123"}, want: 1}, } for _, tt := range tests { tt := tt t.Run(tt.name, func(t *testing.T) { commandLine = flag.NewFlagSet(os.Args[0], flag.ExitOnError) if got := run(tt.args); got != tt.want { t.Errorf("%v: run() = %v, want %v", tt.name, got, tt.want) } }) } } 

## **urfave/cli** 

urfave/cl ~~i~~ <u>注9.3</u> ~~は~~ 筆者が最もよく使うコマンドラインパーサです。 urfave/cli はもともとは codegangsta/cli として開発されていましたが、メンテナーが移行された経緯があります。 

urfave/cli はアプリケーションを示す cli.App とそれに追加するフラグ( Flags )、コマンド( Commands )で構成されます ( **リスト** 9.28)。 

#### **リスト9.28 urfave/cliの使用例** 

package main import ( "os" "github.com/urfave/cli/v2" ) func main() { app := cli.NewApp() app.Flags = []cli.Flag{ &cli.StringFlag{ Name:    "config", Aliases: []string{"c"}, Usage:   "Load configuration from `FILE`", }, } app.Name = "score" app.Usage = "Show student's score" app.Run(os.Args) } 

### このプログラムを実行すると以下が表示されます。 

$ ./実行ファイル NAME: score - Show student's score USAGE: score [global options] command [command options] [arguments...] COMMANDS: list     list students help, h  Shows a list of commands or help for one command 

GLOBAL OPTIONS: --config FILE, -c FILE  Load configuration from FILE --help, -h              show help (default: false) 

app.Flags はグローバルなフラグとして作用します。また、 urfave/cli はサブコマンドを取ることができます( **リスト** 9.29)。 

#### **リスト9.29 urfave/cliの使用例(サブコマンド対応)** 

func main() { app := cli.NewApp() app.Flags = []cli.Flag{ &cli.StringFlag{ Name:    "config", Aliases: []string{"c"}, Usage:   "Load configuration from `FILE`", }, } app.Commands = []*cli.Command{ { Name:  "list", Usage: "list students", Flags: []cli.Flag{ &cli.BoolFlag{ Name:  "json", Usage: "output as JSON", Value: false, }, }, Action: cmdList, }, } app.Name = "score" app.Usage = "Show student's score" 

app.Run(os.Args) } 

Flags や Commands は構造化されており、新しく追加したサブコマンド list に対するフラグ -json を扱うことができます。 

#### $ ./実行ファイル list -json 

list コマンドが指定されると Action 、つまり cmdList が呼び出されます。 cmdList は以下のように実装することができます。 

func cmdList(c *cli.Context) error { if c.Bool("json") { return listStudentsAsJSON() } return listStudents() } 

この c には、 app に足した -config の値( FILE )も c.String("config")から参照することができます。 

$ ./実行ファイル -c fukuoka.cfg list -json 

urfave/cli にはこのほかにも、 CLI アプリケーションを作るうえで必要な機能がたくさんそろっています。 

## **alecthomas/kingpin** 

alecthomas/kingpi ~~n~~ <u>注9.4</u> ~~は~~ 標準パッケージの flag に近い形式で、コードによるフラグ定義を行うスタイルのライブラリで 

### す( **リスト** 9.30)。 

#### **リスト9.30 alecthomas/kingpinの使用例** 

package main 

import ( "os" "strings" "gopkg.in/alecthomas/kingpin.v2" ) var ( app      = kingpin.New("score", "Show student's score") debug    = app.Flag("debug", "Enable debug mode.").Bool() serverIP = app.Flag("server", "Server address.").Default("127.0.0.1").IP() 

register     = app.Command("register", "Register a new user.") registerNick = register.Arg("nick", "Nickname for user.").Required().String() registerName = register.Arg("name", "Name of user.").Required().String() 

post        = app.Command("post", "Post a message to a channel.") postImage   = post.Flag("image", "Image to post.").File() postChannel = post.Arg("channel", "Channel to post to.").Required().String() postText    = post.Arg("text", "Text to post.").Strings() ) 

func main() { switch kingpin.MustParse(app.Parse(os.Args[1:])) { // Register user case register.FullCommand(): println(*registerNick) 

// Post message case post.FullCommand(): if *postImage != nil { } text := strings.Join(*postText, " ") println("Post:", text) } } 

サブコマンドに対して引数を 2 つ取るような形式であっても、柔軟に対応できるようになっています。 urfave/cli の場合は、それが呼び出す Action の形式に合わせる必要があり、少しテストがしづらい面もありますが、 kingpin の場合は自らが switch で分岐していることから、 urfave/cli よりもテストがしやすい形になっていると思います。 

## **spf13/cobra** 

spf13/cobr ~~a~~ <u>注9.5</u> ~~は~~ 付属する cobra コマンドを使ってボイラーテンプレートが生成できる、とても人気がある便利なライブラリです。 cobra を使うには、 cobra-cli をインストールします。 

$ go install github.com/spf13/cobra-cli@latest 

まずは以下を実行してボイラーテンプレートコードを生成します。 

$ cobra-cli init -l MIT -a "Go言語好き太郎 golang@example.com>" 

毎回指定するのが面倒であれば、以下の YAML を ~/.cobra.yaml として保存しておくと良いでしょう。 

--license: MIT author: Go言語好き太郎 golang@example.com> 

これにより、次回からはcobra-cli initでフラグを指定しなくても良くなります。コマンドを実行すると以下のディレクトリ構造が作成されます。 

. ├── LICENSE ├── cmd │   └── root.go ├── go.mod ├── go.sum └── main.go 

このままビルドすると、プログラムの実行結果は以下になります。 

$ ./main 

A longer description that spans multiple lines and likely contains examples and usage of using your application. For example: 

Cobra is a CLI library for Go that empowers applications. This application is a tool to generate the needed files to quickly create a Cobra application. 

この定義は cmd/root.go に記述されています。 

// rootCmd represents the base command when called without any subcommands var rootCmd = &cobra.Command{ 

Use:   "cobraapp", Short: "A brief description of your application", Long: `A longer description that spans multiple lines and likely contains 

examples and usage of using your application. For example: 

Cobra is a CLI library for Go that empowers applications. This application is a tool to generate the needed files to quickly create a Cobra application.`, // Uncomment the following line if your bare application 

// has an action associated with it: // Run: func(cmd *cobra.Command, args []string) { }, } 

コマンド引数なしで起動した場合にはこの部分が実行されます。サブコマンドを追加するには以下を実行します。 

$ cobra-cli add update 

このコマンドを実行すると cmd/update.go が生成されます。./main updateを実行したときの処理は以下の部分に記述します。 

// updateCmd represents the update command var updateCmd = &cobra.Command{ Use:   "update", Short: "A brief description of your command", Long: `A longer description that spans multiple lines and likely contains examples 

and usage of using your command. For example: 

Cobra is a CLI library for Go that empowers applications. This application is a tool to generate the needed files 

to quickly create a Cobra application.`, Run: func(cmd *cobra.Command, args []string) { fmt.Println("update called") }, } 

cmd/update.go の init 関数を **リスト** 9.31のように変更すると、 --foo と --toggle (省略形は -t )というフラグが追加されます。 

**リスト9.31 spf13/cobraでフラグを追加(cmd/update.goのinit関数を編集)** 

func init() { rootCmd.AddCommand(updateCmd) 

updateCmd.PersistentFlags().String("foo", "", "A help for foo") updateCmd.Flags().BoolP("toggle", "t", false, "Help message for toggle") } 

### 以下のような実行結果となります。 

$ ./main update -h A longer description that spans multiple lines and likely contains examples 

and usage of using your command. For example: 

Cobra is a CLI library for Go that empowers applications. This application is a tool to generate the needed files to quickly create a Cobra application. 

Usage: cobraapp update [flags] Flags: --foo string   A help for foo 

-h, --help         help for update -t, --toggle       Help message for toggle 

update コマンド実行時に --foo の値を取るには、 updateCmd を **リスト** 9.32のように変更します。 

**リスト9.32 spf13/cobraで--fooフラグの値を取得 (cmd/update.goのupdateCmdを編集)** 

var updateCmd = &cobra.Command{ Use:   "update", Short: "A brief description of your command", Long: `A longer description that spans multiple lines and likely contains examples and usage of using your command. For example: 

Cobra is a CLI library for Go that empowers applications. This application is a tool to generate the needed files to quickly create a Cobra application.`, 

Run: func(cmd *cobra.Command, args []string) { if toggle, err := cmd.Flags().GetBool("toggle"); err == nil { println("toggle", toggle) } if foo, err := cmd.Flags().GetString("foo"); err == nil { println("foo", foo) } fmt.Println("update called") }, } 

spf13/cobra はこのように、 cobra-cli を使って便利に開発を進めることができます。 

**注** 9.3 <u>https://github.com/urfave/cli</u> (本文に戻る) 

**注** 9.4 <u>https://github.com/alecthomas/kingpin</u> 

(本文に戻る) 

**注** 9.5 <u>https://github.com/spf13/cobra</u> 

(本文に戻る) 

# **9.7 端末制御ライブラリ** 

CLI アプリケーションの中には、端末を制御して見栄えの良い UI (ユーザーインターフェース)を提供するものもあります。これら TUI ( Text User Interface )向けのライブラリを使うことで、端末向けのアプリケーションが簡単に実装できます。 Go は内部エンコーディングが UTF-8 であり、 Windows や Linux 、 macOS など異なるプラットフォームでもそれほど差異なく実装できるしくみがいろいろとそろっています。 

## **olekukonko/tablewriter** 

olekukonko/tablewrite ~~r~~ <u>注9.6</u> ~~を~~ 使うと、端末で簡単に表を作ることができます( **リスト** 9.33)。 

#### **リスト9.33 olekukonko/tablewriterの使用例** 

package main 

import ( "os" "github.com/olekukonko/tablewriter" ) func main() { data := [][]string{ 

{"A", "The Good", "500"}, {"B", "The Very very Bad Man", "288"}, {"C", "The Ugly", "120"}, {"D", "The Gopher", "800"}, } table := tablewriter.NewWriter(os.Stdout) table.SetHeader([]string{"Name", "Sign", "Rating"}) for _, v := range data { table.Append(v) } table.Render() } 

これを実行すると、端末で以下のように表示されます。デフォルトでは数字は右寄せ、それ以外は左寄せで表示されます。カラムごとにアライメントを変更したい場合は SetColumnAlignment を使用します。 

table := tablewriter.NewWriter(os.Stdout) table.SetHeader([]string{"Name", "Sign", "Rating"}) table.SetColumnAlignment([]int{ tablewriter.ALIGN_CENTER, tablewriter.ALIGN_DEFAULT, tablewriter.ALIGN_DEFAULT, }) 

### リスト 9.33 で上記のように書き換えると、以下のように表示されるようになります。またヘッダの色を変更したい場合は SetHeaderColor を使います。 

table.SetHeaderColor( tablewriter.Colors{tablewriter.Bold, tablewriter.BgGreenColor}, tablewriter.Colors{tablewriter.FgHiRedColor, tablewriter.Bold, tablewriter.BgBlack 

Color}, tablewriter.Colors{tablewriter.BgRedColor, tablewriter.FgWhiteColor}, ) 

上記を加えると、白黑の紙面ではわかりにくいですが、 **図** 9.2のように表示されるようになります。また表に合計欄を追加したい場合は、 SetFooter を使います。 

table.SetFooterAlignment(tablewriter.ALIGN_RIGHT) table.SetFooter([]string{ "", "", "427.0", }) 



olekukonko/tablewriter を使うと、このように見栄えの良い表を簡単に実装することができます。 

## **mattn/go-runewidth** 

日本語といった、 1 文字の幅がアスキー文字よりも大きな文字を扱う場合、単純な実装をしてしまうと表示が崩れてしまいます。そのような場合に便利なのが mattn/go-runewidth です。これは前述の olekukonko/tablewriter でも使われています。たとえば、端末に表示する文字列にアスキー文字で下線を引きたい場合、文字列の幅を知る必要があります。そのような場合に使えるのがこのライブラリです( **リスト** 9.34)。 

#### **リスト9.34 mattn/go-runewidthの使用例** 

package main 

import ( "fmt" "strings" "github.com/mattn/go-runewidth" ) func main() { s := "Go言語でCLIアプリケーション作成" fmt.Println(s) width := runewidth.StringWidth(s) fmt.Println(strings.Repeat("~", width)) } 

### これを実行すると **図** 9.3が表示されます。 



CJK ( Chinese 、 Japanese 、 Korean )で扱われる、 East Asian Ambiguous Width (東アジアのあいまい文字幅)も扱えるようになっているため、いろいろな CLI/TUI ライブラリから使用されています。それだけでなく、指定文字幅で文字列をカットしたり折り返したりすることができる API がそろっています。 

#### <mark>fmt.Println(runewidth.Wrap(s, 11))</mark> 

上記のように設定することで、指定幅での折り返しが可能です。 

Go言語でCLI アプリケーション作成 

## **<u>jroimartin/gocui</u>** 

jroimartin/gocui は Go で端末向けアプリケーションを作成するためのライブラリの 1 つです。画面を構成するためのビュー制御や、キーに対するアクションを登録するためのしくみが提供されています。 **リスト** 9.35を実行すると **図** 9.4の画面が表示されます。 

#### **リスト9.35 jroimartin/gocuiの使用例** 

package main import ( "fmt" "log" "github.com/jroimartin/gocui" ) func main() { g, err := gocui.NewGui(gocui.OutputNormal) if err != nil { log.Panicln(err) } defer g.Close() g.ASCII = true g.SetManagerFunc(layout) // CTRL-C で終了 

if err := g.SetKeybinding("", gocui.KeyCtrlC, gocui.ModNone, quit); err != nil { log.Panicln(err) } if err := g.MainLoop(); err != nil && err != gocui.ErrQuit { log.Panicln(err) } } func layout(g *gocui.Gui) error { s := `Go is an open source programming language that makes it simple to build secure, scalable systems.` maxX, maxY := g.Size() if v, err := g.SetView("hello", maxX/2-21, maxY/2-2, maxX/2+21, maxY/2+2); err != nil { if err != gocui.ErrUnknownView { return err } fmt.Fprintln(v, s) } return nil } func quit(g *gocui.Gui, v *gocui.View) error { return gocui.ErrQuit } 



Ctrl - C で終了できます。 

### ◆ ◆ ◆ 

gocui のほかにも、 gizak/termu ~~i~~ <u>注9.7</u> ~~や~~ rivo/tvie ~~w~~ <u>注9.8</u> ~~な~~ ど、端末でありながらリッチな画面を表示できるライブラリがたくさんあります。ぜひ試していただき、自分にあったライブラリを探してください。 

#### **注** 9.6 <u>https://github.com/olekukonko/tablewriter</u> 

(本文に戻る) 

#### **注** 9.7 <u>https://github.com/gizak/termui</u> 

(本文に戻る) 

#### **注** 9.8 <u>https://github.com/rivo/tview</u> 

(本文に戻る) 

# **9.8 まとめ** 

Go で CLI アプリケーションを開発する手順を解説しました。この例はあくまで、数ある CLI アプリケーション開発手法の 1 つでしかありません。しかしプログラムができあがるまでの大筋の雰囲気を味わっていただけたと思います。また、テストの重要さや Go のテストスイート、各種ライブラリの便利さが少しでもみなさんにお伝えできたのならば幸いです。テストを書いて、バグの少ないプログラムを開発しましょう。 



# **10.1 システムのコンセプト** 

本プログラムの構成は **表** 10.1のとおりです。データベースには PostgreSQL を使います。コードは筆者の GitHub <u>注10.1</u> ~~で~~ 公開しています。 

**注** 10.1 <u>https://github.com/mattn/todo-notify</u> (本文に戻る) 

# **10.2 TODOアプリケーション** 

まずは Web アプリケーション本体を作成します。フレームワークとしては labstack/echo を使い、テンプレートエンジンとしては html/template を使います。データベース操作は ORM の uptrace/bun を使います。今回は html/template を使う前提ですので SPA ( Single Page Application )ではありません。ルーティングは **表** 10.2のとおりです。 



## **Webサーバ** 

まずは以下のコマンドを実行してプロジェクトを初期化します。 

$ go mod init github.com/mattn/todo-webapp go: creating new go.mod: module github.com/mattn/todo-webapp 

エディタで main.go を開き **リスト** 10.1を実装します。 echo のひな形になります。 

#### **リスト10.1 TODOアプリのひな形** 

package main import ( "net/http" "github.com/labstack/echo/v4" ) func main() { e := echo.New() e.GET("/", func(c echo.Context) error { return c.String(http.StatusOK, "") }) e.Logger.Fatal(e.Start(":8989")) } 

以下を実行してパッケージを取得します。 

$ go mod tidy 

最初に TODO 一覧を作ります。 TODO アプリで扱う項目は、本文、完了フラグ、作成日時、期日くらいです。 struct で表現するのであれば以下になります。 

type Todo struct { ID        int64 Content   string Done      bool Until     time.Time CreatedAt time.Time } 

ORM の bun でこの Todo を扱うためには、 bun.BaseModel を内包している必要があります。また、 bun が各フィールドをテーブルとどのように紐づけるかを示すためにタグを設定する必要があります。 bun から扱えるように **リスト** 10.2のように変更します。まず、 bun.BaseModel はこの struct と実際のテーブルの紐づきを定義しています。テーブル名は todos 、省略名は t です。 t は SQL で複数のテーブルを扱う際に付けられる別名です。以下で示す t や u がそれにあたります。 

SELECT t.id, u.name FROM todos t, users u WHERE t.user_id = u.id ORDER BY t.id 

なお、今回作成するアプリケーションはマルチユーザー対応ではありませんので、 users テーブルは作成しません。 CreatedAt 、 UpdatedAt 、 DeletedAt は bun の機能を使って作成日時、更新日時、削除日時(論理削除)を実装するためのものです。 

### それではリスト 10.2 を使い、 PostgreSQL に接続してテーブルを自動で作成するところまでを実装します( **リスト** 10.3)。環境変数 DATABASE_URL に PostgreSQL の接続文字列を設定しておいてください。このプログラムを実行すると、データベースに todos というテーブルが自動で作成されます。 



bun には、構造を変更した際に実際のテーブルも変更してくれるマイグレーション機能もあります。 go-bun/bun-starterkit の実装が参考になりま ~~す~~ <u>注10.2</u> ~~。~~ 

## **リクエストハンドラ** 

HTTP リクエストに対するデータの処理を行います。まずは TODO アプリで必要なデータベース操作を解説します。 bun で Todo の一覧を得るには以下を実行します。 

var todos []Todo ctx := context.Background() 

err := db.NewSelect().Model(&todos).Order("created_at").Scan(ctx) if err != nil { return err } 

とても直感的ですね。リスト 10.2 のような 

bun:",soft_delete"タグを付けたフィールドを持つ struct の場合は、 deleted_at カラムが NULL のレコードのみが抽出されます。次に、データベースへの登録の処理は以下を実行します。 

_, err = db.NewInsert().Model(&todo).Exec(ctx) if err != nil { return err } 

更新の場合は以下を実行します。 

var orig Todo 

err = db.NewSelect().Model(&orig).Where("id = ?", todo.ID).Scan(ctx) if err != nil { c.Error(errors.New("not found")) return nil } orig.Done = todo.Done 

_, err = db.NewUpdate().Model(&orig).Where("id = ?", todo.ID).Exec(ctx) 

更新の場合は、すでにあるレコードを取得し、必要な項目のみ上書き更新します。こうすることで、画面から送信される項目に Todo のすべてを乗せる必要がなくなります。最後に削除は以下を実行します。 

_, err = db.NewDelete().Model(&todo).Where("id = ?", todo.ID).Exec(ctx) 

以上の処理をまとめた、 echo のリクエストハンドラに対するデータベース処理は **リスト** 10.4のとおりです。 

**リスト10.4 echoのリクエストハンドラに対するデータベース処理** 

e := echo.New() 

e.GET("/", func(c echo.Context) error { var todos []Todo ctx := context.Background() err := 

db.NewSelect().Model(&todos).Order("created_at").Scan(ctx) if err != nil { e.Logger.Error(err) return c.Render(http.StatusBadRequest, "index", Data{ Errors: []error{errors.New("Cannot get todos")}, }) } return c.Render(http.StatusOK, "index", Data{Todos: todos}) }) e.POST("/", func(c echo.Context) error { var todo Todo // フォームパラメータをフィールドにバインド errs := echo.FormFieldBinder(c). Int64("id", &todo.ID). 

String("content", &todo.Content). Bool("done", &todo.Done). CustomFunc("until", customFunc(&todo)). BindErrors() if errs != nil { e.Logger.Error(err) return c.Render(http.StatusBadRequest, "index", Data{Errors: errs}) } else if todo.ID == 0 { // IDが0のときは登録 ctx := context.Background() if todo.Content == "" { err = errors.New("Todo not found") } else { _, err = db.NewInsert().Model(&todo).Exec(ctx) if err != nil { e.Logger.Error(err) err = errors.New("Cannot update") } } } else { ctx := context.Background() if c.FormValue("delete") != "" { // 削除 _, err = db.NewDelete().Model(&todo).Where("id = ?", todo.ID).Exec(ctx) } else { // 更新 var orig Todo err = db.NewSelect().Model(&orig).Where("id = ?", todo.ID).Scan(ctx) if err == nil { orig.Done = todo.Done _, err = db.NewUpdate().Model(&orig).Where("id = ?", todo.ID).Exec(ctx) } 

} if err != nil { e.Logger.Error(err) err = errors.New("Cannot update") } } if err != nil { return c.Render(http.StatusBadRequest, "index", Data{Errors: []error{err}}) } return c.Redirect(http.StatusFound, "/") }) 

リクエストフォームパラメータのバインドには echo.FormFieldBinder を使っています。本来、 echo は struct のフィールドタグを下記のように設定することで、 c.Bind を使って POST フォームを struct にバインドすることができます。とても簡単で便利です。ただし、今回は TODO の期限を入力するために HTML5 の<input type="date">タグを用いて日時入力を行います。この input タグが POST で送信してくるのは「 2022-11-03T15:42 」といった日時の形式であるため、 echo が使っている time.Parse の日付書式ではパースすることができません。そこで、カスタムバインダを使ってレイアウトを指定して Bind する必要があります。さらに、入力される日時文字列にはタイムゾーンが付与されていません。 customFunc の実装では、 **リスト** 10.5のように JST を付与してパースすることで日本時間で扱えるようにします。 

#### **リスト10.5 customFuncの実装** 

func customFunc(todo *Todo) func([]string) []error { return func(values []string) []error { if len(values) == 0 || values[0] == "" { return nil } dt, err := time.Parse("2006-01-02T15:04 MST", values[0]+" JST") if err != nil { return []error{echo.NewBindingError("until", values[0:1], "failed to decode time", err)} } todo.Until = dt return nil } } 

Int64 や String がアドレスを渡して書き換えるインターフェースになっているリスト 10.4 と異なり、 customFunc には戻り値や参照引数で更新された値を返すしくみがありません。そこで更新対象の変数の参照を引数に取りつつ、クロージャを返す関数にすることで、見た目のきれいな実装にしています。 

## **テンプレート処理** 

echo でテンプレートを扱うには Render メソッドを持った実装を e.Renderer に設定する必要があります。今回は日付を「 2006-01-02 15:04 」の形式で表示するためのテンプレート関数 FormatDateTime を **リスト** 10.6のように実装しました。 

**リスト10.6 FormatDateTimeの実装** 

type Template struct { templates *template.Template } func (t *Template) Render(w io.Writer, name string, data interface{}, c echo.Context) error { return t.templates.ExecuteTemplate(w, name, data) } func formatDateTime(d time.Time) string { if d.IsZero() { return "" } return d.Format("2006-01-02 15:04") } (..略..) e.Renderer = &Template{ templates: template.Must(template.New(""). Funcs(template.FuncMap{ "FormatDateTime": formatDateTime, }).ParseFS(templates, "templates/*")), } 

こうすることで、 echo からは以下のように index テンプレートに value を渡し、 FormatDateTime 関数を呼び出せるようになります。 

<mark>return c.Render(http.StatusBadRequest, "index", value)</mark> 

<mark>{{FormatDateTime .}}</mark> 

サーバ側の処理ではエラーが発生する箇所がいくつかありますが、本アプリケーションではエラーを画面の一部に表示したいと思います。サーバ側が処理した正常な結果、または複数のエラーを一度に返せるように以下の struct を実装します。 

type Data struct { Todos  []Todo Errors []error } 

Todos は取得した TODO アイテムの一覧、 Errors はサーバ側で発生した error の一覧です。これをテンプレートに渡すことでブラウザに TODO 一覧を表示したり、入力エラーを表示したりすることができます。テンプレートの実装は **リスト** 10.7のとおりです。 

#### **リスト10.7 TODOアプリのテンプレート** 

{{define "index"}} <!DOCTYPE html> <html lang="en"> <head> <meta charset="utf8" /> <meta name="viewport" content="width=device-width,initialscale=1" /> <title>Todo List</title> <link rel="stylesheet" type="text/css" href="/static/style.css" media="all" /> </head> <body> 

<h1>TODO</h1> <!-- エラーの一覧 --> {{range .Errors}} <p><em class="error">{{.}}</em></p> {{end}} <!-- TODO の一覧 --> <ul class="todo-overview-list"> {{range .Todos}} <li> <form action="/" method="post"> <input type="hidden" name="id" value="{{.ID}}" /> <input type="hidden" name="done" value="{{if .Done}}0{{else}}1{{end}}" /> <p class="{{if .Done}}todo-done{{end}}">{{.Content}} <span class="controls"> {{if .Until}}{{FormatDateTime .Until}}{{end}} <input type="submit" id="update" name="update" class="update" value="{{if .Done}}未完了{{else}}完了{{end}}" /> <input type="submit" id="delete" name="delete" class="delete" value="削除" /> </span> </p> </form> </li> {{end}} </ul> <!-- 入力エリア --> <form action="/" method="post" id="add-todo"> <input type="hidden" name="id" value="" /> <p> <input type="text" class="content" name="content" 

placeholder="What you should do? " autofocus required /> <span class="controls"> <input type="datetime-local" class="until" name="until" value="" /> <input type="submit" class="add" value="登録" /> </span> </p> </form> </body> </html> {{end}} 

## **静的ファイルのサーブ** 

HTML にはインライン CSS を持たせず、外部の CSS ファイルを使用しています。 echo で静的ファイルを扱う場合には以下のように実装することで簡単に実現できます。 

<mark>e.Static("/static", "assets")</mark> 

ただし、今回はデプロイを楽にするために embed にこだわりました。 template ファイルだけでなく、 CSS ファイルもすべて実行ファイルに埋め込んでしまいたいので、 embed されたファイルシステムを配信するようにしました。 

staticFs, err := fs.Sub(static, "static") if err != nil { log.Fatal(err) } fileServer := http.FileServer(http.FileSystem(http.FS(staticFs))) e.GET("/static/*", echo.WrapHandler(http.StripPrefix("/static/", fileServer))) 

これにより、ビルドされた実行ファイル 1 つをコピーするだけで、どこでも動作するようになります。これで Web アプリケーションが実装できました( **図** 10.1)。 



**注** 10.2 <u>https://github.com/go-bun/bun-starter-kit</u> (本文に戻る) 

# **10.3 リマインダメール送信** 

この TODO アプリケーションには TODO の期限が用意されています。これを使い、 TODO を忘れないようにリマインダメールを送信するアプリケーションを作ります。まずは以下のコマンドを実行してプロジェクトを初期化します。 

$ go mod init github.com/mattn/todo-notify go: creating new go.mod: module github.com/mattn/todo-notify 

エディタで main.go を開き **リスト** 10.8を実装します。 echo のひな形になります。以下を実行してパッケージを取得します。 

$ go mod tidy 

リマインダとして送信したいものは、 Todo struct のうち、 until が NULL ではなく done が false のものです。よって Todo の抽出は **リスト** 10.9の実装になります。 

**リスト10.9 リスト10.8のmain関数を変更** 

func main() { sqldb, err := sql.Open("postgres", os.Getenv("DATABASE_URL")) if err != nil { log.Fatal(err) } defer sqldb.Close() db := bun.NewDB(sqldb, pgdialect.New()) defer db.Close() // TODO 抽出 var todos []Todo ctx := context.Background() err = db.NewSelect().Model(&todos).Order("created_at").Where("until is not null" ).Where("done is false").Scan(ctx) if err != nil { log.Fatal(err) } if len(todos) == 0 { return } メール送信 from := mail.Address{Name: "TODO Reminder", Address: os.Getenv("MAIL_FROM")} var buf bytes.Buffer buf.WriteString("From: " + from.String() + "\r\n") buf.WriteString("To: " + os.Getenv("MAIL_TO") + "\r\n") buf.WriteString("Subject: TODO Reminder\r\n") buf.WriteString("\r\n") buf.WriteString("This is your todo list\n\n") for _, todo := range todos { fmt.Fprintf(&buf, "%s %s\n", todo.Until, todo.Content) 

} smtpAuth := smtp.PlainAuth( os.Getenv("MAIL_DOMAIN"), os.Getenv("MAIL_USER"), os.Getenv("MAIL_PASSWORD"), os.Getenv("MAIL_AUTHSERVER"), ) err = smtp.SendMail( os.Getenv("MAIL_SERVER"), smtpAuth, from.Address, []string{os.Getenv("MAIL_TO")}, buf.Bytes()) if err != nil { log.Fatal(err) } } 

net/mail パッケージと net/smtp パッケージを使い、抽出した Todo をメール送信します( **リスト** 10.10)。 

#### **リスト10.10 メール送信プログラム** 

from := mail.Address{Name: "TODO Reminder", Address: os.Getenv("MAIL_FROM")} var buf bytes.Buffer buf.WriteString("From: " + from.String() + "\r\n") buf.WriteString("To: " + os.Getenv("MAIL_TO") + "\r\n") buf.WriteString("Subject: TODO Reminder\r\n") buf.WriteString("\r\n") buf.WriteString("This is your todo list\n\n") for _, todo := range todos { fmt.Fprintf(&buf, "%s %s\n", todo.Until, todo.Content) } smtpAuth := smtp.PlainAuth( 

os.Getenv("MAIL_DOMAIN"), os.Getenv("MAIL_USER"), os.Getenv("MAIL_PASSWORD"), os.Getenv("MAIL_AUTHSERVER"), ) err = smtp.SendMail( os.Getenv("MAIL_SERVER"), smtpAuth, from.Address, []string{os.Getenv("MAIL_TO")}, buf.Bytes()) if err != nil { log.Fatal(err) } 

これを cron で 1 日 1 回実行するなどすれば、未完了で期限付きの Todo がメール送信されます( **図** 10.2)。 



crontab に登録する場合は以下のように記述すると良いでしょう。 

MAIL_FROM=todo-from@example.com MAIL_TO=todo-to@example.com MAIL_DOMAIN=example.com MAIL_USER=user 

MAIL_PASSWORD=password MAIL_AUTHSERVER=auth.example.com MAIL_SERVER=mail.example.com DATABASE_URL=db.example.com 0 0 * * * /path/to/todo-notify 

# **10.4 Goにおけるフレームワーク** 

Web アプリケーションを開発する際は、 Go 標準で用意されている net/http を使うだけで、おおよそのアプリケーションを開発することができます。 net/http は HTTP を扱ううえで必要な機能をそろえていますが、一般的な Web アプリケーションフレームワークが持ち合わせるような以下の機能を持っていません。 









**簡素なサーバ向けRESTfull API パラメータを受け取れるルーティンググループ化されたルーティング形式化されたミドルウェア** 

Web アプリケーションフレームワークを導入し、そのフレームワークが引いたレールの上に乗って開発するほうが、アプリケーション構築をスムーズに進められたり、コード量を少なくすることができたり、フレームワークが提供する拡張を簡単に導入したりすることができます。また、セキュリティ問題が発生した場合はフレームワーク側が対応してくれるなどのメリットもあります。 

Go にもたくさんのフレームワークがあります。著名な Go のフレームワークの一覧を示します( **表** 10.3)。 



Micro <u>https://micro.dev/</u> Gin <u>https://gin-gonic.com/ja/</u> Echo <u>https://echo.labstack.com/</u> Fiber <u>https://github.com/gofiber/fiber</u> Go Kit <u>https://gokit.io</u> Gorilla <u>https://www.gorillatoolkit.org/</u> Beego <u>https://github.com/beego/beego</u> 

このほかにも Go にはたくさんの Web フレームワークがあり、好みによって選択することができます。 

## **Gin** 

とても人気の高い Web アプリケーションフレームワークです。 API サーバを実装するのに向いています。 Go の他の Web フレームワークと比較してもパフォーマンスが良く、開発も活発です。 

## **Echo** 

Labstack 社が開発する Web アプリケーションフレームワークです。高いパフォーマンスと拡張性、ミニマリスト向けという点を売りにしており、多くのユーザーから使われています。本書でも何度か登場しています。 HTTP フレームワークとして必要な機能は一通りそろっており、 CORS ( CrossOrigin Resource Sharing )や CSRF ( Cross-Site Request Forgeries )といった機能を簡単に足すことができるミドルウェアも充実しています注10.3 ~~。~~ とくにドキュメントが良く整備されており、開発の際に困ることはほぼありません。 

## **Micro** 

Micro は Web アプリというよりもバックエンドサービスを開発するためのフレームワークで、 RPC で通信するサービスアプリケーションを構築することができます。 Micro は gomicro という CLI を提供しており、簡単にサービスを作成できます。 go-micro は以下の手順でインストールします。 

$ go install github.com/go-micro/cli/cmd/go-micro@latest 

まず以下のコマンドを実行し、 helloworld サービスを作成します。 

$ go-micro new service helloworld 

$ cd helloworld 

$ make init proto update tidy 

※ 残念ながら、執筆時点ではgo-microはWindowsで正しくビルドすることができませんそしてサービスを起動します。 

$ go-micro run 

handler/helloworld.go は **リスト** 10.11のような実装になっています。 

#### **リスト10.11 handler/helloworld.go** 

type Helloworld struct{} 

func (e *Helloworld) Call(ctx context.Context, req 

*pb.CallRequest, rsp *pb.Call Response) error { logger.Infof("Received Helloworld.Call request: %v", req) rsp.Msg = "Hello " + req.Name return nil } 

受け取った引数から Name を取り出し、 Hello を付けて返すサンプルコードになっています。コマンドラインからこの Call を呼び出すには以下を実行します。 

$ go-micro call helloworld Helloworld.Call '{"name": "John"}' {"msg":"Hello John"} 

次に別の端末を開いて、今度は呼び出し側を作成します。 

$ go-micro new client helloworld $ cd helloworld-client $ cp -r ../helloworld/proto . # protoを持ってくる $ make tidy 

実装は **リスト** 10.12のようになります。 

**リスト10.12 helloworld-client/main.go** 

c := pb.NewHelloworldService(service, srv.Client()) 

for { // Call service rsp, err := c.Call(context.Background(), &pb.CallRequest{Name: "John"}) if err != nil { 

logger.Fatal(err) } logger.Info(rsp) time.Sleep(1 * time.Second) } 

以下のコマンドでクライアントを実行します。 

$ go-micro run 2022-12-14 09:20:36  file=helloworld-client/main.go:36 level=info msg:"Hello John" 2022-12-14 09:20:37  file=helloworld-client/main.go:36 level=info msg:"Hello John" 2022-12-14 09:20:38  file=helloworld-client/main.go:36 level=info msg:"Hello John" 2022-12-14 09:20:39  file=helloworld-client/main.go:36 level=info msg:"Hello John" 2022-12-14 09:20:40  file=helloworld-client/main.go:36 level=info msg:"Hello John" 2022-12-14 09:20:41  file=helloworld-client/main.go:36 level=info msg:"Hello John" 

クライアントを **リスト** 10.13のように変更するとストリーム形式の RPC も実行できます。 

**リスト10.13 リスト10.12をストリーム形式のRPC対応に** 

stream, err := c.ServerStream(context.Background(), &pb.ServerStreamRequest{Count: 3}) if err != nil { logger.Fatal(err) } defer stream.Close() for i := 0; i < 3; i++ { 

var value map[string]int err = stream.RecvMsg(&value) if err != nil { break } fmt.Println(value["count"]) } 

Micro を使って service と RPC 通信できていることが確認できました。このように Micro は、バックエンドサービスを開発する際に必要な RPC を簡単に取り扱うことができるようになっています。 

## **Beego** 

Beego は Go の初期から存在する有名な Web フレームワークで、多くのユーザーから使われています。 Beego は ORM や admin (管理者用のライブラリ)、 config (設定ファイル)、 task (タスク管理)、 i18n (国際化)といった、 Web アプリケーションに限らない、システムを構築するうえで必要になるであろうあらゆるものがそろっています。とくに task には、 cron 形式で処理を実行する際に便利なライブラリがあります。 **リスト** 10.14のコードでは 3 秒ごとに tk1 がログ出力されます。たとえば、夜間バッチ処理を実行する際に便利です。 

#### **リスト10.14 Beegoのtaskの使用例** 

package main 

import ( "context" "time" 

"github.com/beego/beego/v2/core/logs" "github.com/beego/beego/v2/task" ) func main() { // create a task tk1 := task.NewTask("tk1", "0/3 * * * * *", func(ctx context.Context) error { logs.Info("tk1") return nil }) // check task err := tk1.Run(context.Background()) if err != nil { logs.Error(err) } // add task to global todolist task.AddTask("tk1", tk1) // start tasks task.StartTask() defer task.StopTask() // wait 12 second time.Sleep(12 * time.Second) } 

さらに Beego のロガーにはファイルサイズや行数でローテートするしくみも用意されており、本格的なアプリケーションを作成するのに向いています( **リスト** 10.15)。 

#### **リスト10.15 Beegoのlogsの使用例** 

package main 

import ( "github.com/beego/beego/v2/core/logs" ) func main() { log := logs.NewLogger(10000) log.SetLogger(logs.AdapterMultiFile, `{ "filename":"./foo.log", "daily":true, "maxlines":10000, "rotate":true }`) for i := 0; i < 100; i++ { log.Info("Hello, World!") } } 

このソースコードを実行すると、以下のように日付と連番が付与されたログファイルが生成されます。 

foo.log foo.2022-12-07.001.log foo.2022-12-07.002.log foo.2022-12-07.003.log (..略..) 

## **フレームワークの選定について** 

Beego に限らず、 Go の Web アプリケーションフレームワークは一見、フルスタックフレームワークのように見えますが、機能のほとんどは切り離しが可能で、これらの中の一部品を使って個別にアプリケーションを作成することが可能になっています。 

Go は一般的に Web API の開発に向いていると言われており、フレームワークは軽量なものから中規模のものが多いです。とはいえ大規模な開発に向いていないのかというと、そういうわけでもありません。 Go の Web フレームワークは net/http が提供する http.Handler のしくみに沿って作られていることが多く、そのしくみが簡素なインターフェースを提供するがゆえに、フレームワークのミドルウェアも自然と簡素に見えている、という一面があります。各フレームワークごとに特色があります。ルーティングに重きを置いていたり、パラメータの取得に重きを置いていたりと、用途はさまざまです。ご自分にあったフレームワークを選んでください。表 10.3 の中で、 Fiber だけは net/http ではなく fasthttp というライブラリをベースに作られています。 net/http より速いですが、 net/http と互換性がないため、 net/http の機能を使っている他のライブラリとの連携には細工が必要になります。フレームワークを選定する際には、使い勝手にも考慮が必要ですが、やはりベンチマーク計測も考慮すべきです。 HTTP のベンチマークを取るツールはたくさんありますが、筆者のお勧めは以下の 2 つです。 





**<u>https://github.com/ddosify/ddosify https://github.com/rakyll/hey</u>** 

どちらも HTTP プロトコルだけでなく、 DNS の名前引きや接続時間も計測できます。 ddosify は TLS 通信にかかった時間も計測できます( **図** 10.3)。フレームワークは一度使うと多少なりともロックインされることになります。パフォーマンスだけではなく、運用するうえでの取り扱いやすさなどを考え、自分にとってベストなフレームワークを選んでください。 

#### **注** 10.3 <u>https://echo.labstack.com/middleware/</u> 

(本文に戻る) 

# **10.5 まとめ** 

Web アプリケーションフレームワークの echo 、 ORM の bun を使って TODO Web アプリケーションを作成しました。また、リマインダメールを送信するプログラムも作りました。また、後半ではそれ以外のフレームワークを紹介しました。難しいと思われがちな Go の Web アプリケーションも、フレームワークを使うことで簡単に実装できることがおわかりいただけたと思います。こういった Go のアプリケーションをしばらく運用してみるとわかると思いますが、 Go のスタティックバイナリは運用ととても相性が良く、実行中に OS のパッケージをアップグレードしてもほぼ干渉しません。 Go は他のライブラリを参照していないので、ソフトウェアのバージョンがアップデートされても影響しないからです。ここも Go が実務で使われている理由の 1 つです。 



# **11.1 ライブラリの仕様** 

本章の解説では、リポジトリ名を以下と仮定します。 

<u>https://github.com/mattn/go-hsd</u> 

ユーザーから渡された 2 つの文字列から、文字列の距離を求めるライブラリを作ります。なるべく解説が簡単になるよう、ハミング距離注11.1 ~~を~~ 例にします。ハミング距離は 2 つの同じ⻑さの文字列の距離を計算するアルゴリズムで、以下の「距離の公理」を満たすよう実装します。 

dist(a, b) = dist (b, a) 

dist(a, b) ≦ dist(a, c) + dist(b, c) 

**注** 11.1 <u>https://ja.wikipedia.org/wiki/ハミング距離</u> (本文に戻る) 

# **11.2 プロジェクトの作成** 

まずはプロジェクトを初期化します。プロジェクトのディレクトリを作成し、go mod initを実行します。 

$ mkdir go-hsd 

$ cd go-hsd 

$ go mod init github.com/mattn/go-hsd 

実行すると以下の go.mod が生成されます。 

module github.com/mattn/go-hsd 

go 1.20 

go mod initの引数にはモジュール名を指定します。 Go ではモジュール名として、 VCS のリポジトリ名と、そのリポジトリ名からの相対パスで作られる「モジュールパス」で指定することができます。これは第 3 章で紹介したgo getに際して意味をなします。 

go getを実行すると、 Go はパッケージをダウンロードしますが、デフォルトでは Semantic Versioning でタグ付けされた最終のバージョンが取得されます。これは最終のコミットではありません。タグが設定されていない場合は v0.0.0 とすることで最終コミットが得られます。本章ではまだパッケージを import していないので、たとえば以下のgo getは実行しても意味がありません。 

$ go get github.com/fatih/color 

# **11.3 パッケージ名** 

Go ではパッケージ名は小文字の 1 単語で名付けることが推奨されています。たとえば base64 にエンコーディングするためのパッケージは encoding_base64 でも encodingBase64 でもなく、 encoding/base64 であるべきです。また Go のパッケージ名には_を使えますが、一般的には使いません。使うのはテストコードを実装する際のパッケージ名 foo_test くらいです。勘違いしやすいのですが、 go.mod の先頭に記述されているのはモジュール名です。モジュール名とパッケージ名は一致していなくてもかまいません。本章で作成するライブラリを GitHub 上で公開したとして、ユーザーが以下のように import したとしても、ソースコード内では hsd として扱えます。 

package main import ( "github.com/mattn/go-hsd" ) 

もし気になるのであれば以下のように別名を付けて import するのでも良いでしょう。 

package main 

import ( 

hsd "github.com/mattn/go-hsd" ) 

# **11.4 実装** 

それでは実装を始めましょう。テキストエディタを起動し、ファイルを作成します。ファイル名は前述のとおり、 1 単語で表します。 Hamming String Distanc の頭文字を取り hsd.go とします( **リスト** 11.1)。 

**リスト11.1 hsd.go** package hsd func StringDistance(lhs, rhs string) int { return Distance([]rune(lhs), []rune(rhs)) } func Distance(a, b []rune) int { dist := 0 if len(a) != len(b) { return -1 } for i := range a { if a[i] != b[i] { dist++ } } return dist } 

ソースコードはとても簡単です。ビルドしてみましょう。 

$ go build 

エラーなくビルドされたはずです。この時点で、ソースコードを git リポジトリに追加してコミットしておくと良いでしょう。 

$ git init 

$ git add go.mod go.sum main.go 

$ git commit -am 'first import' 

さてgo buildを実行してディレクトリの中を見ても、オブジェクトファイルなどが存在しないことに気づくと思います。 main 関数があるコマンドラインアプリケーションと異なり、ライブラリの場合、ビルド結果はカレントディレクトリに生成されません。ビルド結果は以下のコマンドを実行した際に表示されるキャッシュディレクトリに格納されています。 

$ go env GOCACHE 

ただし通常は見にいくことはありません。ディスクが圧迫してきたら以下のコマンドでクリアしても良いでしょう。 

$ go clean --modcache 

このコマンドを実行すると、ユーザーが保持している build や test のキャッシュをすべて削除してくれます。 

# **11.5 バージョニング** 

Go のモジュールは、リポジトリにgit pushし、タグ付けすることで公開となります。タグはセマンティックバージョンでなければなりません。ここまでの説明では、まだ GitHub リポジトリを作っていません。まずは GitHub リポジトリを作成しましょう。 <u>https://github.com/new をブラウザで開き、リポ</u> ジトリを作ります( **図** 11.1)。リポジトリ名は前述のとおり go-hsd としました。 

## **COLUMN** 

## **GitHubのワンポイントTips** 

リポジトリの作成にはGitHub社が開発しているghコマンド注11.A ~~を~~ 利用すると便利です。 

$ gh repo create 

プライベートリポジトリを作成するのであれば以下を実行します。 

$ gh repo create --private 

ghコマンドを使うと、リポジトリの作成だけでなく pull-requestのブランチをチェックアウトすることもできます。 

$ gh pr checkout 353 

とても便利に使えるのでインストールしておくと良いでしょう。 

**注** 11.A <u>https://github.com/cli/cli</u> 

(本文に戻る) 

リポジトリを作成したらローカルリポジトリでリモートを登録し、 push します。 

- $ git branch -M main 

$ git remote add origin git@github.com:mattn/go-hsd.git 

$ git push -u origin main 

※ 前述のgh repo createを実行するとこの手順が省略できます。ブラウザからリポジトリを作成する際に、 Public にするか Private にするかを聞かれます。これは各個人の開発スタイルによる話ですが、他のユーザーに OSS を公開する際には、ある程度動く状態になってから公開したいと思う人と、途中段階でも公開してしまいたいと思う人の 2 種類がいます。前者であれば Private でリポジトリを作成しておき、git pushしつつ、ある程度動くようになってからリポジトリを Public に変更するという方法を取ると良いでしょう。 

Go のプロジェクトで git のタグを打つということは、他のユーザーがgo getした際の「最新バージョンを固定する」ことを意味します。先進的な実装を main ブランチには取り込みたいけれどユーザーには安定バージョンを使ってほしい、といった場合には git タグを打つと良いでしょう。以下のように実行してソースとタグを push します。 

- $ git push origin main 

- $ git tag v0.0.1 

- $ git push --tags 

また、自分のプロジェクトが依存しているモジュールのセマンティックバージョンを更新するには以下を実行します。 

$ go get -u モジュール名これを実行すると、指定したモジュールのマイナーバージョン、およびパッチレベルバージョンが最新に更新されます。 go.mod で管理しているすべてのモジュールを更新するのであれば以下を実行します。 

$ go get -u ./... 

ただしgo get -uは、モジュールのメジャーバージョンアップは行いません。メジャーバージョンアップをするには以下を実行します。 

$ go mod edit -replace=モジュール名@v=モジュール名@v 

go modの中で Go のメジャーバージョンを指定する方法には 2 通りあり、パッケージの作者は使い分けることができます。 1 つはこのgit modを使ってモジュールのバージョンを指定する方法。もう 1 つはパス形式で指定する方法です。メジャーバージョンに限り、ソースコード内でモジュール名の後ろにメジャーバージョンのパスを指定して import することができます。 

#### <mark>import "github.com/labstack/echo/v4"</mark> 

この場合、プロジェクト内で使っているパッケージがメジャーバージョンアップした際にはソースコードを修正して v5 に変更することになります。なお、バージョンを下げる場合もメジャーバージョンアップのときと同じです。go mod edit -replaceを使って古いバージョンを指定することになります。 

# **11.6 テスト** 

当然ですがテストを書きましょう。 

package hsd import ( "testing" ) func TestStringDistance(t *testing.T) { got := StringDistance("foo", "foh") want := 1 if got != want { t.Fatalf("expected: %v, got %v:", want, got) } } 

テストが動作することも確認しておきます。 

=== RUN   TestStringDistance --- PASS: TestStringDistance (0.00s) PASS ok      github.com/mattn/go-hsd 0.291s 

ここでは簡単にテストを書きましたが、 Table Driven Test を使ってきちんとテストを実装するには第 7 章を参照してください。 

# **11.7 ドキュメント** 

パッケージを公開する場合、利用者に対してその使い方などを説明する必要があります。リポジトリに README.md などを含めておくのでも良いのですが、 Go にはドキュメントを公開するしくみが用意されています。例として **リスト** 11.2のソースコードを用意します。 

#### **リスト11.2 go docのためのドキュメントの例** 

// Package foo provide functions for example. package foo 

// Add returns a plus b. func Add(a, b int) int { return a + b } // Sub returns a minus b. func Sub(a, b int) int { return a - b } 

package の上段に書かれているコメント行は、パッケージを説明するために使われます。また関数名の上段に書かれているコメントは関数を説明するために使われます。どちらも複数行でもかまいません。 

Go にはドキュメントを表示する方法が 3 つあります。 

## **<u>go docの使い方</u>** 

Go をインストールすると標準でgo docコマンドを利用できます。パッケージのディレクトリのルートディレクトリで実行すると、以下が表示されます。 

$ go doc package foo // import "github.com/mattn/foo" 

Package foo provide functions for example. 

func Add(a, b int) int func Sub(a, b int) int 

関数 Add のドキュメントを参照したい場合には以下を実行します。 

$ go doc Add package foo // import "." func Add(a, b int) int Add returns a plus b. 

コマンドラインから利用できるため、 Vim のようなテキストベースのエディタを利用している人には便利でしょう。 

## **<u>godocの使い方</u>** 

Go にはもう 1 つ、ドキュメント閲覧用コマンドが存在します。それが godoc です。 godoc は、もともとは標準で付属していたツールです。以下のコマンドでインストールします。 

$ go install golang.org/x/tools/cmd/godoc@latest 

パッケージのディレクトリのルートディレクトリで実行すると、 Web サーバが起動します。 

$ godoc 

ブラウザで http://localhost:6060 を開くと標準パッケージとサードパーティのパッケージのドキュメントが表示されます ( **図** 11.2)。 



## **https://pkg.go.dev/で閲覧する** 

GitHub にリポジトリを作成し、 Go のパッケージを push するとパッケージがインデックスされ、ミラーリングされます。これによりgo getコマンドでパッケージを取り寄せる際は、内部的にはミラーサーバからパッケージのソースがダウンロードされることになります。さらに、インデックスされたパッケージは Go オフィシャルのパッケージドキュメントサイトでドキュメントを閲覧できるようになります。たとえば、筆者が公開しているパッケージ go-colorable のドキュメントは <u>https://pkg.go.dev/github.com/mattn/go-colorable から参照するこ</u> とができます。ただし、開発中にドキュメントを参照する用途であれば、最近の IDE では入力中に表示されるポップアップにより、その関数のドキュメントが表示されるので( **図** 11.3)、簡単なものであればブラウザを起動するまでもありません。 



# **11.8 ディレクトリ構成** 

ディレクトリ構成はユーザーによってさまざまなスタイルがあります。どれが正しいということはありませんが、筆者は Go のオフィシャルが提供するスタイル、または Go のコミッタが使っているスタイルを参考にするのをお勧めします。 Go のオフィシャルやコミッタの人たちが使うスタイルは、おおよそ以下のようなディレクトリ構成です。このモジュールのディレクトリ構成では、リポジトリのルートディレクトリでパッケージを提供し、 cmd/tool1 や cmd/tool2 でそのパッケージを使用したツールを提供します。まず、 foo.go がパッケージ本体です。そして foo_test.go がテストコード。そして example_test.go が Example 形式のテストコードです。 Example 形式のテストは通常のテストと少し異なります。 

package foo import ( "fmt" ) func ExampleFoo() { result := Add(1, 2) fmt.Printf("Add(1, 2) is %v\n", result) // Output: Add(1, 2) is 3 } 

Example テストは fmt.Println や fmt.Printf などを実行して実際に標準出力された文字列が、コメント行// Output:の中身と同じかどうかでテストの成功失敗が判定されます。それだけではなく、 Example テストはドキュメントとしても活用されます。 Go のミラーサーバにインデックスされると、 Example テストは <u>https://pkg.go.dev/ で実行が可能になりま</u> す。 <u>https://pkg.go.dev/github.com/mattn/anko@v0.1.9/vm は筆者</u> が開発しているスクリプト言語 Anko のドキュメントページですが、 Example を[ Run ]ボタンで実行することができます。これはソースコードを少し変更して実際に試してみるのにも便利です( **図** 11.4)。 



(https://pkg.go.dev/) 

# **11.9 自動テスト** 

Go に限らずですが、ローカルで開発した場合にはビルドされるのはその開発端末での結果になります。当然ながら Windows でビルドすれば Windows 向けのソースコードだけがビルド対象になります。前述のソース構成では、ビルドに使われるのは以下のうち foo.go と foo_windows.go だけです。 









**foo.go** 

**foo_linux.go foo_windows.go foo_test.go** 

たとえば、 Windows と Linux の双方を共通的に呼び出す関数のインターフェースを変更し、コンパイルエラーの出た Windows のソースコードだけを修正した場合、開発者は Linux のビルドが壊れてしまったことに気づくことができません。それは、テストコードがあったとしても、です。そこで便利なのが GitHub Actions による継続的インテグレーション( CI : Continuous Integration )です。 GitHub Actions は GitHub 社が提供する CI/CD のためのプラットフォームであり、 GitHub ユーザーであれば基本、無料で使うことができます。 GitHub Actions を使うことで、手元に Linux や macOS を用意しなくても、 GitHub が提供する Windows 、 Linux 、 macOS 上でテストを実施することができます。 

GitHub Actions を使うにはまず、リポジトリに以下のフォルダを作成します。 

$ mkdir -p .github/workflows 

次に **リスト** 11.3の YAML ファイル ( .github/workflows/test.yaml )を作成します。 

#### **リスト11.3 CIのためのGitHub Workflow** 

name: Test on: push: branches: - main pull_request: branches: - main jobs: test: strategy: matrix: os: [windows-latest, macos-latest, ubuntu-latest] go: ["1.17", "1.18", "1.19", "1.20"] runs-on: ${{ matrix.os }} steps: - uses: actions/checkout@v2 - uses: actions/setup-go@v2 with: go-version: ${{ matrix.go }} - run: go test ./... - run: go test -bench . -benchmem 

このファイルを git で追加して GitHub にgit pushするだけで準備は完了です。この GitHub Workflow を準備すると、以降は main ブランチに対して push するだけで各 OS 上でテストが実行されます。 

# **11.10 自動リリース** 

開発が進んだら、パッケージが提供するコマンドラインアプリケーションを配布したくなるはずです。できればユーザーには、 Go コンパイラでビルドする手間を与えずに、ビルドされた実行ファイルだけを配布したいものです。そこで使うのが GitHub Actions による継続的デリバリー ( CD : Continuous Delivery )です。 GitHub Actions を使うことで、手元でビルドしなくても GitHub のサーバ上で実行ファイルをビルドし、リリース物としてダウンロード可能にすることができます。前述したフォルダ .github/workflows に、 **リスト** 11.4の YAML ファイル( .github/workflows/release.yaml )を作成します。 

#### **リスト11.4 CDのためのGitHub Workflow** 

name: Release on: push: tags: - 'v*' 

jobs: release: name: Release runs-on: ubuntu-latest steps: 

- name: Checkout uses: actions/checkout@v2 with: fetch-depth: 0 - name: Setup Go uses: actions/setup-go@v2 with: go-version: 1.x - name: Run GoReleaser uses: goreleaser/goreleaser-action@v2 with: version: latest args: release --rm-dist env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 

このファイルを git で追加し、 GitHub にgit pushするだけで準備は完了です。この GitHub Workflow を準備すると、以降はタグを push するだけで、ビルドされた実行ファイルが GitHub リポジトリにある、 Release にタグを打ったセマンティックバージョンごとにアップロードされます。実際にリリースする場合は以下のように、git tagを実行し、 GitHub に push します。 

$ git push origin main $ git tag v0.0.5 $ git push --tags 

あとは GitHub Actions がタグの付与を検知し、ビルドされた実行ファイルが GitHub リリース物としてアップロードされます。 

# **11.11 まとめ** 

GitHub を使った Go の開発方法を解説しました。 GitHub で開発すると、他のユーザーから pull-request をもらったり、バグを報告してもらえたりなど、たくさんのメリットが得られます。 GitHub を使ったことがない読者の方は、ぜひアカウントを作って OSS を公開してみてください。 



# **12.1 database/sql** 

Go の database/sql パッケージは RDBMS を抽象的に表現したインターフェースを提供しています。ですので database/sql だけでは何もできません。 database/sql が提供する driver インターフェースに従って開発された「データベースドライバ」を使う必要があります。 Go オフィシャルとしてはいずれのドライバも開発していません。 Go からデータベースを扱うには、サードパーティ製のドライバを使う必要があります。 

## **サードパーティドライバ** 

SQL データベースを扱うプログラムでは database/sql の import と、ドライバとなるパッケージのブランク import (_を付ける)が必要になります。複数のデータベースドライバを扱うのであれば、それぞれの import が必要となります。 Go から扱える SQL データベースドライバの一覧は公式の Wik ~~i~~ <u>注12.1</u> ~~か~~ ら参照できます。たとえば、 SQLite 3 ひとつ取ってもいくつかドライバがあります。現状、 SQLite 3 を扱う際のドライバとしては、筆者の go-sqlite3 が一般的に使われているようです( cgo を使っているのでビルドには C コンパイラが必要です)。 

Wiki の中に[*]のマークがついているものは、 Go オフィシャルチームが動作確認を行っているドライバです( **表** 

12.1)。 database/sql の修正を行う際に、サードパーティドライバの動作を壊してしまわないかを確認しています。筆者のドライバもその対象となっています。 



MySQL <u>https://github.com/go-sql-driver/mysql、 https://github.com/ziutek/mymysql</u> PostgresSQL(pure Go) <u>https://github.com/lib/pq、 https://github.com/jackc/pgx</u> SQLite(uses cgo) <u>https://github.com/mattn/go-sqlite3</u> 

筆者の経験上、 MySQL であれば github.com/go-sqldriver/mysql 、 PostreSQL であれば github.com/lib/pq を使っておくと、情報量も多く、問題が発生することが少ないと思います。 

Wiki の SQLite 3 ドライバの中には pure go と書かれているドライバ modernc.org/sqlit ~~e~~ <u>注12.2</u> ~~が~~ あります。こちらのドライバはビルド時に C コンパイラが不要です。 SQLite 3 の C 言語のソースコードを中間コードのままコンパイルし、 Go のコードに落とし込んだうえで VM として実行することにより、 C コンパイラを不要にしています。ただし、 VM として動作する仕様上、 go-sqlite3 などネイティブなドライバに比べて処理速度が落ちてしまいます。また、 SQLite 3 が持っているいくつかの機能をサポートしていないという点に注意が必要です。第 9 章で作成したプログラムでは SQLite 3 の FTS4 という全文検索機能を使いました。本書の執筆時点で modernc.org/sqlite 

は FTS4 をサポートしていないため、使用できませんでした。このように、複数あるデータベースドライバの中から用途に合ったドライバを選ぶ必要がありますが、 database/sql のインターフェースから使えるドライバを選ぶことで、データベース操作は統一されたコードで書くことができるようになりますし、たとえば PostgreSQL で動いていたプログラムを SQLite 3 でも動作するように変更する際にも、それほど大きな修正が必要になることはありません。 

## **Query/QueryRow** 

**リスト** 12.1の例では PostgreSQL を扱うプログラムを想定しています。 

**リスト12.1 database/sqlのQueryの使用例** package main 

import ( "database/sql" "log" _ "github.com/lib/pq" ) 

func main() { db, err := sql.Open("postgres", "postgres://postgres:postgres@dbserver/database") if err != nil { log.Fatal(err) } defer db.Close() 

id := 3 rows, err := db.Query(`SELECT name, age FROM users WHERE id < $1`, id) if err != nil { log.Fatal(err) } defer rows.Close() for rows.Next() { var name string var age int err = rows.Scan(&name, &age) if err != nil { log.Fatal(err) } fmt.Printf("name=%v, age=%v\n", name, age) } } 

SELECT を実行する場合には、まず Query でステートメントを作成し、 Next を呼び出してレコードをフェッチ、 Scan で値を抽出する流れになります。もしフェッチの件数が 1 件の場合には QueryRow を使います( **リスト** 12.2)。 

#### **リスト12.2 database/sqlのQueryRowの使用例** 

var name string var age int err = db.QueryRow(`SELECT name, age FROM users WHERE id = $1`, id).Scan(&name, &age) if err != nil { log.Fatal(err) } fmt.Printf("name=%v, age=%v\n", name, age) 

この場合、 Scan の失敗だけでなく QueryRow で抽出結果がない場合にもエラーとなります。 

Query/QueryRow はバインドする値を引数で与え、 Scan は抽出結果を Go の値として取れますが、型がマッチしている必要があります。おおよそ型変換は行われますが、数値型の抽出結果を string で Scan して値が得られるかどうかは、データベースドライバしだいです。 

Query/QueryRow 、いずれも context を取る関数が用意されているため、遅いクエリを途中でキャンセルすることもできます(ドライバにより実装していない場合もあります)。 

ctx, cancel := context.WithTimeout(context.Background(), 3*time.Second) defer cancel() rows, err := db.QueryContext(ctx, `SELECT name, age FROM users WHERE id < $1`, id) if err != nil { log.Fatal(err) } 

## **Exec** 

INSERT や DELETE を実行する場合は Exec を使います( **リスト** 12.3)。 

**リスト12.3 database/sqlのExecの使用例** 

err := db.Exec(`UPDATE users SET name = $1 WHERE id = $2`, name, id) if err != nil { log.Fatal(err) } 

Query/QueryRow と同じく ExecContext が用意されているのでキャンセルを処理できます。 

## **Prepare** 

複数のクエリや実行を扱いたい場合のために、ステートメントを先に作成しておくことができる Prepare があります( **リスト** 12.4)。 

**リスト12.4 database/sqlのPrepareの使用例** 

stmt, err := db.Prepare(`SELECT name, age FROM users WHERE id < $1`) if err != nil { log.Fatal(err) } err = stmt.QueryRow(id).Scan(&name, &age) if err != nil { log.Fatal(err) } fmt.Printf("name=%v, age=%v\n", name, age) 

この場合、すでにパースされたステートメントを使いまわすことができます。 2 回目以降の Query/QueryRow 実行時に SQL のパースの必要がないため、パフォーマンスを良くすることができます。 

## **Begin/Commit/Rollback** 

database/sql はトランザクションも扱えます。複数の更新を行ったあと、 Commit したり Rollback したりすることができます( **リスト** 12.5)。 

**リスト12.5 database/sqlでトランザクションを扱う** tx, err := db.Begin() if err != nil { return err 

} defer tx.Rollback() result, err := tx.Exec(`INSERT INTO users(name, age) VALUES($1, $2)`, "Fred", 41) if err != nil { return err } if affected, err := result.RowsAffected(); err != nil { return err } else if affected == 0 { return errors.New("no record affected") } tx.Commit() 

db.Exec ではなく tx.Exec を実行しなければならないことに留意してください。おおよそいずれの RDBMS でも動作しますが、トランザクションレベルのサポート状況は各 RDBMS により異なります。トランザクションは扱う前に、ドライバのサポート状況を調べておくと良いでしょう。 

**注** 12.1 <u>https://github.com/golang/go/wiki/SQLDrivers</u> (本文に戻る) 

**注** 12.2 <u>https://modernc.org/sqlite</u> (本文に戻る) 

# **12.2 ent/ent** 

Ent ( ent/ent )は Facebook 社が開発している ORM です。高度に構造化されており、モデルとクライアントを分離しつつ、 IDE と親和性の良いインターフェースを提供しています。 

ent でデータベースを扱うにはまずスキーマを定義する必要があります。 ent のツールを使ってまずはディレクトリ構造を作成します。 

$ mkdir todo $ cd $_ 

$ go mod init todo 

そして以下を実行すると **リスト** 12.6のファイル ( ent/schema/todo.go )が生成されます。 

$ go run -mod=mod entgo.io/ent/cmd/ent init Todo 

**リスト12.6 ent/schema/todo.go** 

package schema 

import "entgo.io/ent" 

// Todo holds the schema definition for the Todo entity. type Todo struct { ent.Schema 

} // Fields of the Todo. func (Todo) Fields() []ent.Field { return nil } // Edges of the Todo. func (Todo) Edges() []ent.Edge { return nil } 

この Fields に **リスト** 12.7のようにフィールドを足していきます。 

**リスト12.7 リスト12.6の// Fields of the Todo.部分を変更** 

func (Todo) Fields() []ent.Field { return []ent.Field{ field.Text("text"). NotEmpty(), field.Time("created_at"). Default(time.Now). Immutable(), field.Enum("status"). NamedValues( "InProgress", "IN_PROGRESS", "Completed", "COMPLETED", ). Default("IN_PROGRESS"), field.Int("priority"). Default(0), } } 

プロジェクトフォルダのルートから以下を実行します。 

$ go generate ./ent 

すると ent ディレクトリの中に、 Todo を操作するための CRUD API が生成されます。あとは ent.Open でクライアントを作成し、 Todo を操作できるようになります( **リスト** 12.8)。 

#### **リスト12.8 CRUD APIの処理を実装** 

client, err := ent.Open("postgres", "postgres://postgres:postgres@localhost/test?ssl mode=disable") if err != nil { log.Fatal(err) } defer client.Close() // スキーマ作成 (マイグレーション) err := db.Schema.Create(context.Background() if err != nil { log.Fatalf("failed creating schema resources: %v", err) } // Todoを列挙 for _, e := range client.Todo.Query().AllX(context.Background()) { fmt.Println(e.Text) } // Todoを作成 _, err = client.Todo.Create().SetText("Go言語を学ぶ").Save(context.Background()) if err != nil { log.Fatalf("failed creating a todo: %v", err) } 

## **OpenAPI連携** 

Ent は Go の静的解析のしやすさを活かし、いろいろな周辺ツールと連携できます。たとえば、 OpenAPI 仕様の API も生成できます。 ent が生成したディレクトリの中で ent/generate_openapi.go というファイルを作り、 **リスト** 12.9のようにして保存します。 

#### **リスト12.9 ent/generate_openapi.go** 

//go:build ignore // +build ignore package main import ( "log" "entgo.io/contrib/entoas" "entgo.io/ent/entc" "entgo.io/ent/entc/gen" ) func main() { ex, err := entoas.NewExtension() if err != nil { log.Fatalf("creating entoas extension: %v", err) } err = entc.Generate("./schema", &gen.Config{}, entc.Extensions(ex)) if err != nil { log.Fatalf("running ent codegen: %v", err) } } 

さらに ent/generate.go を開いて以下のように修正します( 2 つめの go:generate を追加)。 

package ent //go:generate go run -mod=mod entgo.io/ent/cmd/ent generate ./schema //go:generate go run -mod=mod generate_openapi.go 

以下のコマンドを実行すると openapi.json が生成されます。 

$ go generate ./ent 

生成された ent/openapi.json は以下のとおりです。 

{ "openapi": "3.0.3", "info": { "title": "Ent Schema API", "description": "This is an auto generated API description made out of an Ent schema definition", "version": "0.1.0" }, "paths": { "/todos": { "get": { "tags": [ "Todo" ], "summary": "List Todos", "description": "List Todos.", "operationId": "listTodo", ... 

生成された openapi.json を使って Web API を作ってみます。以下のコマンドを実行して oapi.go を生成します。 

$ go install github.com/deepmap/oapi-codegen/cmd/oapicodegen@latest 

$ oapi-codegen -package main -generate server -old-config-style ent/openapi.json > oapi.go 

あとは生成された oapi.go と同じディレクトリに、 **リスト** 12.10の Web サーバのソースを置けば、それだけでデータベースのテーブルを CRUD できる Web API が完成します。 

**リスト12.10 oapi.goに対応するWebサーバ** 

package main 

import ( "context" "database/sql" "encoding/json" "errors" "log" "os" "todo/ent" "todo/ent/todo" "github.com/labstack/echo/v4" _ "github.com/lib/pq" ) type Api struct { client *ent.Client } type ListTodoParams struct { 

Page         *int ItemsPerPage *int } 

func (a *Api) ListTodo(ctx echo.Context, params ListTodoParams) error { page := 0 if params.Page != nil { page = *params.Page } itemsPerPage := 5 if params.ItemsPerPage != nil { itemsPerPage = *params.ItemsPerPage } ees, err := a.client.Todo.Query(). Order(ent.Desc(todo.FieldID)). Offset(page * itemsPerPage). Limit(itemsPerPage). All(context.Background()) if err != nil { log.Println(err) return echo.ErrBadRequest } return ctx.JSON(200, ees) } func (a *Api) CreateTodo(ctx echo.Context) error { var ee ent.Todo err := json.NewDecoder(ctx.Request().Body).Decode(&ee) if err != nil { log.Println(err) return echo.ErrBadRequest } e := a.client.Todo.Create(). SetText(ee.Text). SetStatus(ee.Status). SetPriority(ee.Priority) 

if !ee.CreatedAt.IsZero() { e.SetCreatedAt(ee.CreatedAt) } if ee2, err := e.Save(context.Background()); err != nil { log.Println(err) return echo.ErrBadRequest } else { ee = *ee2 } return ctx.JSON(200, ee) } 

func (a *Api) DeleteTodo(ctx echo.Context, id int) error { e := a.client.Todo.DeleteOneID(int(id)) err := e.Exec(context.Background()) if err != nil { log.Println(err) return echo.ErrBadRequest } return nil } func (a *Api) ReadTodo(ctx echo.Context, id int) error { e, err := a.client.Todo.Get(context.Background(), int(id)) if err != nil { if errors.Is(err, sql.ErrNoRows) { return echo.ErrNotFound } log.Println(err) return echo.ErrBadRequest } return ctx.JSON(200, e) } func (a *Api) UpdateTodo(ctx echo.Context, id int) error { var ee ent.Todo err := json.NewDecoder(ctx.Request().Body).Decode(&ee) 

if err != nil { log.Println(err) return echo.ErrBadRequest } e := a.client.Todo.UpdateOneID(int(id)). SetText(ee.Text). SetStatus(ee.Status). SetPriority(ee.Priority) if ee2, err := e.Save(context.Background()); err != nil { log.Println(err) return echo.ErrBadRequest } else { ee = *ee2 } return ctx.JSON(200, ee) } func main() { client, err := ent.Open("postgres", os.Getenv("DATABASE_URL")) if err != nil { log.Fatalf("failed opening connection to sqlite: %v", err) } client.Schema.Create(context.Background()) e := echo.New() myApi := &Api{client: client} RegisterHandlers(e, myApi) e.Static("/", "static") e.Logger.Fatal(e.Start(":8989")) } 

このほかにも Ent には、 ER 図やドキュメントを生成できるコード生成処理がいくつかあります。詳しくは Ent のドキュメントサイト注12.3 ~~を~~ 参照してください。 

## **entgen** 

Ent は空のデータベースからコードによるスキーマを定義し、そこからテーブルを構築するフレームワークですが、既存のテーブルと連携することもできます。筆者が開発している entge ~~n~~ <u>注12.4</u> ~~を~~ 使うと、既存のテーブルから Ent のスキーマを生成することができます。 

**注** 12.3 <u>https://entgo.io/ja/docs/getting-started/</u> (本文に戻る) 

**注** 12.4 <u>https://github.com/mattn/entgen</u> 

(本文に戻る) 

# **12.3 ORMベンチマーク** 

### **表** 12.2に挙げる ORM のベンチマークを計測しました。 



Beego <u>https://github.com/beego/beego</u> GORM <u>http://gorm.io/</u> XORM <u>http://xorm.io/</u> go-pg/pg <u>https://github.com/go-pg/pg</u> go-gorp/gorp <u>https://github.com/go-gorp/gorp</u> upper/db <u>https://github.com/upper/db</u> volatiletech/sqlboiler <u>https://github.com/volatiletech/sqlboiler</u> gocraft/dbr <u>https://github.com/gocraft/dbr</u> sqlx <u>http://jmoiron.github.io/sqlx/</u> genmai <u>https://github.com/naoina/genmai</u> ent <u>https://entgo.io/</u> bun <u>https://bun.uptrace.dev/</u> 

各ライブラリでの RDBMS のサポート状況は以下のマークで示しています。 

○:サポート △:サポートしているがサードパーティ  ×:未サポート操作は以下の 5 つです。 











**単一レコードのINSERT 複数レコードのINSERT 単一レコードのUPDATE 単一レコードのSELECT 複数レコードのSELECT** 

以下の環境で計測しました。 

**ベンチマーカー** 



<u>https://github.com/mattn/orm-benchmark</u> **計測したサーバ** 



Windows 11(32GB) / Memory 2.5GHz / Ryzen 5 

結果は以下のとおりです。結果を見ると raw 、つまり database/sql のまま扱うのが一番効率が良く、次点で beego_orm を使うのが良いようです。筆者がよく使う bun のような構造化に寄せた ORM は速くはないので、どこまで安全性や利便性を優先するかのトレードオフと言えるでしょう。とくに、高度に構造化された ORM である ent は利便性が高い反面、パフォーマンスは優れておらず、とくに複数行の SELECT には弱い傾向があるようです (執筆時点の 2022 年 11 月での計測結果)。 

ORM に限らず、プロジェクトで扱うライブラリの選定は、利便性やパフォーマンスだけではなく、今後⻑くメンテナンスされるかどうかも大きなポイントになるため、総合的な判断が必要です。 

# **12.4 まとめ** 

Go の database/sql の使い方と、サードパーティの ORM ライブラリの使い方を解説しました。 ORM 選びは利用者の好き嫌いも影響しますが、トレンドを追って複数個を扱えるようにしておくと、用途によって使い分けることができます。 



# **13.1 Goがなぜ使われるのか** 

Go はクラウドサービスでよく利用されています。理由はおもに以下に絞られます。 







### **シングルバイナリでデプロイがしやすいクロスコンパイルが容易メニーコア環境でスケールする** 

これらはクラウドサービスの利用者だけでなく、クラウドサービスの提供者としてもうれしい話で、 SDK として Go のライブラリを提供することにより多くのユーザーに対して、スケールのしやすさをアピールすることができます。せっかくクラウドサービス側が CPU コア数を変更できるしくみを提供したとしても、利用者側が動かすアプリケーションが対応していなければ利用してもらえません。著名なクラウドサービスとしては Google Cloud 、 Amazon Web Service ( AWS )、 Microsoft Azure 、 Oracle Cloud などがありますが、いずれのサービスにおいても Go を利用することができます。 

# **13.2 Google Cloud** 

Google Cloud で一番簡単に Go を利用する方法は Cloud Run でしょう。事前に gcloud CLI をインストールしてログインしておく必要があります注13.1 ~~。~~ 

まずgo mod initを実行して go.mod を生成します。 

module github.com/mattn/google-cloud-run-example go 1.19 

そして **リスト** 13.1の簡単なアプリケーションを実装します。 

**リスト13.1 Google Cloud用のサンプルアプリ** package main 

import ( "fmt" "log" "net/http" "os" ) 

func main() { log.Print("starting server...") http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) { 

fmt.Fprintln(w, "Hello World") }) port := os.Getenv("PORT") if port == "" { port = "8080" } log.Printf("listening on port %s", port) if err := http.ListenAndServe(":"+port, nil); err != nil { log.Fatal(err) } } 

net/http で作ることができる Web サーバそのままです。あとは gcloud を使って以下を実行するだけです。 

$ gcloud run deploy 

数分すればインターネットから接続可能な Web サービスがデプロイされます( **図** 13.1)。 



Google Cloud Run の良いところは、とくに専用のフレームワークなどを必要としないところです。ローカル PC でもコンパイルして実行できるので、デバッグもしやすいです。また google/ko を使うともっと簡単にデプロイすることができます。 

$ gcloud run deploy --image=$(ko build ./cmd/app) 

Google Cloud では Cloud Run だけでなく、 Google Cloud Functions や、 Google Kubernetes Engine ( GKE )など、 Go が利用可能なクラウドサービスがたくさんあり、またそれらを Go から制御できる SDK も提供されています。 GitHu ~~b~~ <u>注13.2</u> ~~で~~ サンプルが提供されています。 

#### **注** 13.1 <u>https://cloud.google.com/sdk/docs/install?hl=ja</u> 

(本文に戻る) 

**注** 13.2 <u>https://github.com/GoogleCloudPlatform/golang-samples</u> (本文に戻る) 

# **13.3 AWS Lambda** 

AWS でも Go が利用できます。たとえば、 AWS Lambda で簡単に Go が扱えます。 AWS Lambda の場合は、実行ファイルを ZIP で圧縮してその ZIP でアップロードする方式と、 Amazon ECR に push されたコンテナイメージを Lambda から参照する方式の 2 つがあります。 

ZIP アップロード方式を使う場合は build-lambda-zip コマンドを使うと便利です。 

$ go install github.com/aws/aws-lambda-go/cmd/build-lambdazip@latest 

GOOS=linux 、 GOARCH=amd64 でビルドした実行ファイルを ZIP ファイルに格納します。 **リスト** 13.2のように Makefile で手順化しておくと便利です。 

**リスト13.2 AWS LambdaへのZIPアップロードのMakefile** 

.PHONY: all update clean 

all: main.zip 

main.zip: main build-lambda-zip -output main.zip main 

main: main.go GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -v 

-ldflags '-d -s -w' -a -tags netgo -installsuffix netgo -o main main.go 

update: main.zip 

aws lambda update-function-code --function-name hello --zipfile fileb://main.zip 

clean: 

rm -f main.zip main 

ビルドした Zip ファイルを Lambda にアップロードする前に、 AWS マネージメントコンソールから Lambda 関数を作成しておきます( **図** 13.2)。作成直後はハンドラ名が関数名と同じ「 hello 」になっていますので「 main 」に変更してください(関数名を「 main 」に変えないように注意)。あとはmake updateでデプロイできます。 Lambda を起動するには以下を実行してください。 

$ aws lambda invoke --function-name hello output.json 

またコンテナイメージ方式を使う場合は、前述の google/ko を使ってデプロイすることができます。 

$ aws lambda update-function-code \ --function-name=my-function-name \ 

--image-uri=$(ko build ./cmd/app) 

Go はスクリプト言語のようにランタイムを別途インストールする必要がなく、ビルドした実行ファイルをコンテナイメージに直接置くだけで良いため、デプロイ処理がずいぶん楽になります。 

AWS のマネージメントコンソールでメモリ割り当てを変更すると CPU コア数も連動します。 Go はその設定内容に応じて、パラメータなどを変更しなくてもアプリケーションがスケールするようになっています。 

# **13.4 Oracle Cloud** 

Oracle Cloud でも Go は利用できます。以降では Oracle Cloud Function での利用方法を解説します。 Oracle Cloud Functions を利用するためには Fn Project が提供する fn コマン ~~ド~~ <u>注13.3</u> ~~を~~ 使います。 

fn コマンドを使ってプロジェクトを作成します。 

$ fn init --runtime go hello Creating function at: ./hello Function boilerplate generated. func.yaml created. $ cd hello 

生成された func.go は **リスト** 13.3のようになっています。 

**リスト13.3 func.go** 

package main 

import ( "context" "encoding/json" "fmt" "io" "log" fdk "github.com/fnproject/fdk-go" 

) 

func main() { fdk.Handle(fdk.HandlerFunc(myHandler)) } 

type Person struct { Name string `json:"name"` } 

func myHandler(ctx context.Context, in io.Reader, out io.Writer) { p := &Person{Name: "World"} json.NewDecoder(in).Decode(p) msg := struct { Msg string `json:"message"` }{ Msg: fmt.Sprintf("Hello %s", p.Name), } log.Print("Inside Go Hello World function") json.NewEncoder(out).Encode(&msg) } 

Oracle Cloud Functions は、 Fn Project の仕様をベースに構築されているため、 fdk というライブラリを使用してアプリケーションを開発する必要があります。この関数を実行するにはアプリケーションを Oracle Cloud に登録する必要がありますが、 Oracle Cloud Functions は執筆時点で fn コマンドからのアプリケーション登録を行うことができず、 Web コンソール画面から作成します。ここでは my-app という名前にしました。あとは以下のコマンドを実行してデプロイするだけです。 

$ fn -v deploy --app my-app 

作成した Function を実行するには以下を実行します。 

$ fn invoke my-app hello {"message": "Hello World"} 

fn コマンドは標準入力を扱えるので、以下のようにファイルから JSON を入力して実行することもできます。 

$ cat input.json { "name": "mattn" } 

$ fn invoke my-app hello < input.json {"message":"Hello mattn"} 

以下のコマンドを実行した結果に含まれる fnproject.io/fn/invokeEndpoint が指す URL が、トリガーとなるエンドポイントになります。 

#### $ fn inspect function my-app hello 

**COLUMN バイナリサイズを小さくする** 

Goはlibcに依存しない実行ファイルを生成します。そのためシングルバイナリであり、必然的に実行ファイルのバイナリサイズが大きくなります。デプロイが楽である、libcに依存しないことで異なるディストリビューションでも動作するなどのメリットもありますが、少しでもバイナリサイズが小さいに越したことはありません。とくにコンテナイメージに格納する実行ファイルは、可搬性がありながらも可能な限りサイズが小さいほうが有利です。以下のDockerfileでは、Goが生成する実行ファイルからデバッグ情報やパス情報を取り除き、ビルド後の実行ファイルをUPXで圧縮することで小さくしています。 

FROM golang:1.18-alpine AS build-env 

WORKDIR /app COPY go.mod . COPY go.sum . 

RUN apk add --no-cache upx || \ go version && \ go mod download COPY . . 

RUN CGO_ENABLED=0 go build -buildvcs=false -trimpath - ldflags '-w -s' -o /go/bin/myapp RUN [ -e /usr/bin/upx ] && upx /go/bin/myapp || echo FROM scratch 

COPY --from=build-env /go/bin/myapp /go/bin/myapp ENTRYPOINT ["/go/bin/myapp"] 

マルチステージでscratchに対してアプリケーションの実行ファイルのみを配置することで、動作に不要なコンパイラなども除外しています。一見、地道な作業のように見えますが、コンテナイメージをpullする時間の短縮にも直結し、ひいてはアプリケーションの起動時間にも影響します。もしかすると年間コストにも影響するかもしれませんね。 

**注** 13.3 <u>https://github.com/fnproject/fn</u> (本文に戻る) 

# **13.5 まとめ** 

クラウドサービスで Go を使う方法を紹介しました。各クラウドサービスでの Go の利用方法は執筆時点のものであり、これからさらに Go が利用できる範囲が広がっていくものと思われます。 Go はクラウドサービスで有利に働く優秀なランタイムを持ったプログラミング言語です。いずれのクラウドサービスも、 Go を簡単に扱えるようになっています。エンドユーザーにとってもクラウドサービス運営側にとっても相性の良い Go はこれからも使われ続けていくと思います。 

# **索引** 

**記号** & * ` ` +build <- ch **A** alecthomas/kingpin any append archive/zip AWS Lambda **B** Beego beego_orm benchstat break build constraints Builder Pattern **C** cap CD 

cgo ch <- channel CI CLIアプリケーション CodeCov const context.Context contextパッケージ continue copy CORS CSRF 

### **D** 

database/sql default defer define/template delete 

### **E** 

Echo Embedded struct encoding.TextMarshaler encoding.TextUnmarshaler encoding/japanese encoding/json enmime ent/ent entgen error Exec 

### **F** 

fallthrough Fiber filepath.Glob filepath.WalkDir flag.Duration flag.DurationVar flag.Int flag.IntVar flag.Text flag.TextVar flagパッケージ fmt fmt.Println fmtパッケージ for FTS4 FuncMap Functional Options Pattern Fuzzingテスト 

### **G** 

ghコマンド Gin GitHub GitHub Actions gizak/termui go build go doc go env go fmt go get 

go install go mod edit go mod init go mod tidy go run go test go test -bench go test -benchmem go tool cover go vet go.mod go.sum go:build go:embed GOARCH GOBIN go-bun/bun-starter-kit GOCACHE gocraft/web godoc Goland golangci-lint Google Cloud google/go-cmp GOOS GOPATH goquery GOROOT goroutine go-sql-driver/mysql go-sqlite3 Goコンパイラ Goの開発環境 Goモジュール 

Go言語 **H** html/template http.FileServer https://pkg.go.dev/ **I** if import init関数 interface{} internalパッケージ io.Reader io.Writer iota ioパッケージ 

**J** 

jroimartin/gocui julienschmidt/httprouter kagome Labeled Break labstack/echo len lib/pq libc 

**K** 

**L** 

lint log.Fatal log.Print logパッケージ 

### **M** 

main map mattn/go-runewidth Method Value Micro mime/multipart modernc.org/sqlite net/http net/http/pprof net/mail net/smtp netパッケージ 

### **N** 

### **O** 

olekukonko/tablewriter OpenAPI Oracle Cloud ORM os.Open osパッケージ 

**P** 

package 

panic Pascal Case path/filepath pathパッケージ pkg-config Prepare print println Query QueryRow quit race condition range recover reflectパッケージ replace revive rivo/tview run rune select SetColumnAlignment SetFooter SetHeaderColor SetOutput spf13/cobra 

### **Q** 

### **R** 

### **S** 

SQLite 3 string Stringerインターフェース struct switch sync.WaitGroup syncパッケージ t.Error t.Errorf t.Fatal t.Fatalf t.Parallel t.Setenv t.Skip t.Skipf t.TempDir Table Driven Tests template.HTML template.Parse testdata The Go1 Compatibility Rules time.Duration time.Format time.Parse time.Time timeパッケージ TinyGo TUI type 

**T** 

**U** 

underling type untyped constant uptrace/bun urfave/cli UTF-8 **V** var Vim Visual Studio Code **W** Webアプリケーションフレームワーク Webサーバ **Z** Zen of Python **い** 委譲イミュータブルインターフェース **え** エラー **か** カバレッジ型アサーション型スイッチ型宣言関数呼び出し 

**く** クラウドサービス **け** 継承 **こ** 構造体合流処理コマンドラインパーサコンストラクタコンパイル型言語 **し** ジェネレータ実行ファイル自動テスト自動リリース条件分岐シングルバイナリ **す** 推測するな、計測せよストリーム指向スライススロットリング **せ** 制御構文静的な型先着処理 

**た** タイムアウト処理ダッグ・タイピング端末制御ライブラリ **て** データベースドライバ停止処理定数宣言ディレクトリ構成テストテンプレート処理 **と** ドキュメントトランザクション **は** バージョニングバイナリ配列パッケージ **ひ** ビルトイン関数 **ふ** ファイルのサーブフラグライブラリブランクimport 

#### プロジェクトフォルダプロファイリング 

||**へ**|
|---|---|
|並行<br>並列<br>変数宣言<br>ベンチマーク||
||**ほ**|
|ポインタ||
||**ま**|
|マルチプラットフォーム||
||**め**|
|命名規則<br>メソッド<br>メモリアロケーション||
||**も**|
|文字列||
||**り**|
|リクエストハンドラ||
||**る**|
|ルーティング<br>ループ||
||**れ**|
|列挙||



**カバーデザイン** トップスタジオデザイン室(轟木亜紀子) **本文設計・組版** マップス石田昌治 **編集** 中田瑛人 



## **電子版書籍について** 

本書は紙の書籍『Go言語プログラミングエッセンス』 (ISBN978-4-297-13419-8)を電子書籍化したものです。紙書籍とは一部レイアウトやデザインが異なります。本書の更新履歴や補足情報は技術評論社ウェブサイトをご参照ください。本書の一部または全部を著作権法の定める範囲を超え、無断で複写、複製、転載、テープ化、ファイルに落とすことを禁じます。造本には細心の注意を払っておりますが、万一、ページの乱れやページの抜け等がございましたら、小社デジタル事業部までお知らせください。 

## **電子版奥付** 

**書名** Go言語プログラミングエッセンス **電子版発行日** 2023年3月9日初版第1刷発行 **著者** mattn **発行者** 片岡巌 **発行所** 株式会社技術評論社東京都新宿区市谷左内町21-13 **電話** 03-3513-6150 販売促進部 03-3513-6180 デジタル事業部 **電子版製本** 株式会社グリープ <u>https://greep.jp/</u> 

©2023 mattn ISBN978-4-297-13420-4
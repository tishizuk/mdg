[第1章序章:Juliaの世界観と本書のねらい(汎用から計算科学まで)](cEZ.xhtml)

[1. 読者像と到達目標](cEZ.xhtml#a3M0)

[2\. Juliaが解く課題:二言語問題の解消と再現性](cEZ.xhtml#a3M1)

[3. 設計思想の核:多重ディスパッチ・JIT・LLVM](cEZ.xhtml#a3M2)

[3.1 多重ディスパッチ:型に応じて最適なメソッドを選ぶ](cEZ.xhtml#a3M3)

[3.2 JITコンパイル:最初にコンパイル、次回から速い](cEZ.xhtml#a3M4)

[3.3 LLVM:低レベル最適化を言語が享受](cEZ.xhtml#a3M5)

[4. 汎用言語としてのJulia:日常の開発力を底上げする](cEZ.xhtml#a3M6)

[4.1 書きやすさ:動的言語の表現力](cEZ.xhtml#a3M7)

[4.2 エラー処理とテスト:信頼性の確保](cEZ.xhtml#a3M8)

[4.3 環境管理:再現性の標準化](cEZ.xhtml#a3M9)

[4.4 可視化・レポーティング:伝わる成果物](cEZ.xhtml#a3MA)

[5. 計算科学・数値解析に向いた言語機能:正確さ・速さ・拡張性](cEZ.xhtml#a3MB)

[5.1 配列とブロードキャスト:短く書いて速い](cEZ.xhtml#a3MC)

[5.2 浮動小数点と数値安定性:意識的に扱う](cEZ.xhtml#a3MD)

[5.3 微分方程式・最適化・根探し:表現と解法の橋渡し](cEZ.xhtml#a3ME)

[5.4 型安定性:性能の前提条件](cEZ.xhtml#a3MF)

[6. 仕様記述言語としてのJulia:DSLで構造をコード化する](cEZ.xhtml#a3MG)

[6.1 ドメイン固有の型設計](cEZ.xhtml#a3MH)

[6.2 マクロによる宣言的記述](cEZ.xhtml#a3MJ)

[6.3 多重ディスパッチで仕様を選択する](cEZ.xhtml#a3MK)

[7. 再現可能性とプロジェクト設計:環境・設定・成果物の統制](cEZ.xhtml#a3MM)

[7.1 環境のピン留め](cEZ.xhtml#a3MN)

[7.2 乱数・設定値の管理](cEZ.xhtml#a3MP)

[7.3 成果物の生成パイプライン](cEZ.xhtml#a3MR)

[7.4 テスト・プロパティ検査・ベンチマーク](cEZ.xhtml#a3MS)

[8. 汎用から高度計算までを一気通貫で扱う意義](cEZ.xhtml#a3MT)

[9. 実践的な作法:小さく始めて、正しく速く](cEZ.xhtml#a3MU)

[第2章環境構築と基本操作:インストール・REPL・Pkg・VS Code・Jupyter](cEZ.xhtml#a3MV)

[1. 公式配布の導入と初期確認](cEZ.xhtml#a3MW)

[1.1 パス設定と起動オプション](cEZ.xhtml#a3MX)

[2\. REPLの基本操作とモード(?, ;, ]/Pkg)](cEZ.xhtml#a3MY)

[2.1 基本操作とショートカット](cEZ.xhtml#a3MZ)

[2.2 ヘルプモード(?)](cEZ.xhtml#a3N0)

[2.3 シェルモード(;)](cEZ.xhtml#a3N1)

[2.4 Pkgモード(])](cEZ.xhtml#a3N2)

[3\. Pkgで作る再現可能なプロジェクト(Project.toml/Manifest.toml)](cEZ.xhtml#a3N3)

[3.1 Project.tomlの役割](cEZ.xhtml#a3N4)

[3.2 Manifest.tomlの役割](cEZ.xhtml#a3N5)

[3.3 典型的なワークフロー](cEZ.xhtml#a3N6)

[3.4 Pkgの主なコマンド](cEZ.xhtml#a3N7)

[3.5 コード側からのPkg操作](cEZ.xhtml#a3N8)

[4\. VS Code拡張の導入と実践的な使い方](cEZ.xhtml#a3N9)

[4.1 実行ファイルの設定](cEZ.xhtml#a3NA)

[4.2 REPL統合とセル実行](cEZ.xhtml#a3NB)

[4.3 変数ビューとプロット](cEZ.xhtml#a3NC)

[4.4 Lint・フォーマット・テスト](cEZ.xhtml#a3ND)

[5\. Jupyterノートブック(IJulia)の連携方法](cEZ.xhtml#a3NE)

[5.1 IJuliaの導入](cEZ.xhtml#a3NF)

[5.2 ノートブックの起動](cEZ.xhtml#a3NG)

[5.3 環境の一致を保つ](cEZ.xhtml#a3NH)

[5.4 ノートブックの構造化](cEZ.xhtml#a3NJ)

[6. 再現性を強化する初期設定(startup.jl, Random.seed!, artifacts)](cEZ.xhtml#a3NK)

[6.1 startup.jlでの初期化](cEZ.xhtml#a3NM)

[6.2 乱数と設定の集中管理](cEZ.xhtml#a3NN)

[6.3 Artifacts.tomlによるデータの配布](cEZ.xhtml#a3NP)

[7. よくある躓きの回避とトラブルシューティング](cEZ.xhtml#a3NR)

[7.1 REPLとプロジェクトの不一致](cEZ.xhtml#a3NS)

[7.2 互換性(compat)の不足](cEZ.xhtml#a3NT)

[7.3 ビルドやバイナリ依存の失敗](cEZ.xhtml#a3NU)

[7.4 Jupyterカーネルの選択ミス](cEZ.xhtml#a3NV)

[7.5 スレッド数の未設定](cEZ.xhtml#a3NW)

[7.6 乱数の未固定](cEZ.xhtml#a3NX)

[8. 実務を意識した最初のセットアップの例](cEZ.xhtml#a3NY)

[9. 補足:REPLでの軽快な探索を支えるテクニック](cEZ.xhtml#a3NZ)

[第3章文法入門:型・変数・制御構文・関数・スコープ(◎基本を最短で)](cEZ.xhtml#a3P0)

[1. 基本型とリテラル](cEZ.xhtml#a3P1)

[1.1 数値型](cEZ.xhtml#a3P2)

[1.2 文字・文字列](cEZ.xhtml#a3P3)

[1.3 論理値と比較](cEZ.xhtml#a3P4)

[1.4 欠損と空](cEZ.xhtml#a3P5)

[1.5 範囲・集合的なリテラル](cEZ.xhtml#a3P6)

[2. コレクション型:タプル・配列・辞書・集合](cEZ.xhtml#a3P7)

[2.1 タプルとNamedTuple](cEZ.xhtml#a3P8)

[2.2 配列(Array)とベクトル/行列](cEZ.xhtml#a3P9)

[2.3 辞書(Dict)と集合(Set)](cEZ.xhtml#a3PA)

[2.4 コピーとビュー、浅い/深いコピー](cEZ.xhtml#a3PB)

[3. 変数・型注釈・型変換](cEZ.xhtml#a3PC)

[3.1 代入と定数](cEZ.xhtml#a3PD)

[3.2 型注釈・型アサーション](cEZ.xhtml#a3PE)

[3.3 型安定性の意識](cEZ.xhtml#a3PF)

[4. 制御構文:条件分岐・反復・内包・短絡](cEZ.xhtml#a3PG)

[4.1 条件分岐](cEZ.xhtml#a3PH)

[4.2 反復(for/while)とbreak/continue](cEZ.xhtml#a3PJ)

[4.3 内包表記・生成器](cEZ.xhtml#a3PK)

[4.4 ブロードキャストと融合](cEZ.xhtml#a3PM)

[5. 関数定義:引数、可変長、キーワード、無名関数、doブロック](cEZ.xhtml#a3PN)

[5.1 基本の関数定義と戻り値](cEZ.xhtml#a3PP)

[5.2 可変長引数(variadic)とスプラット](cEZ.xhtml#a3PR)

[5.3 キーワード引数とデフォルト値](cEZ.xhtml#a3PS)

[5.4 無名関数とdoブロック記法](cEZ.xhtml#a3PT)

[5.5 関数内のスコープと性能の注意](cEZ.xhtml#a3PU)

[6. スコープ規則:global/local/let、ソフトスコープの挙動](cEZ.xhtml#a3PV)

[6.1 関数内のスコープ](cEZ.xhtml#a3PW)

[6.2 トップレベルのソフトスコープ(REPLなど)](cEZ.xhtml#a3PX)

[6.3 letで閉じたローカルを作る](cEZ.xhtml#a3PY)

[6.4 localとglobalの明示](cEZ.xhtml#a3PZ)

[7. 例外処理:try/catch/finally、throw/rethrow、@assert](cEZ.xhtml#a3R0)

[7.1 基本構文](cEZ.xhtml#a3R1)

[7.2 例外の送出と再送出](cEZ.xhtml#a3R2)

[7.3 アサーションと事前条件](cEZ.xhtml#a3R3)

[8. 実用パターン:入力検証、API設計、ドキュメンテーション、イディオム](cEZ.xhtml#a3R4)

[8.1 入力検証と型ガード](cEZ.xhtml#a3R5)

[8.2 APIの階層化(位置+キーワード)](cEZ.xhtml#a3R6)

[8.3 ドキュメント文字列(docstring)](cEZ.xhtml#a3R7)

[8.4 破壊的更新と非破壊的操作の区別](cEZ.xhtml#a3R8)

[8.5 デストラクチャリングとパターン](cEZ.xhtml#a3R9)

[8.6 例:基礎文法を組み合わせた小規模タスク](cEZ.xhtml#a3RA)

[9. 例外ケースと境界の扱い:欠損、境界チェック、比較の落とし穴](cEZ.xhtml#a3RB)

[9.1 missingの伝播とskipmissing](cEZ.xhtml#a3RC)

[9.2 境界チェックと@inbounds](cEZ.xhtml#a3RD)

[9.3 浮動小数点の比較](cEZ.xhtml#a3RE)

[10. 基礎文法の運用原則(記述のみ)](cEZ.xhtml#a3RF)

[第4章型システムと多重ディスパッチ:抽象型・構造体・パラメトリック型](cEZ.xhtml#a3RG)

[1. 多重ディスパッチの要点](cEZ.xhtml#a3RH)

[2. 抽象型の設計:拡張点を定義して柔軟性を確保する](cEZ.xhtml#a3RJ)

[3. 構造体でのデータ表現:不変と可変、具体的なフィールド型](cEZ.xhtml#a3RK)

[4. パラメトリック型:汎用性と型安全を同時に得る](cEZ.xhtml#a3RM)

[5. メソッドテーブルの読み解き:methods・@which・型の包含関係](cEZ.xhtml#a3RN)

[6. 計算科学で効く設計パターン](cEZ.xhtml#a3RP)

[6.1 タグディスパッチ:性質で切り替える](cEZ.xhtml#a3RR)

[6.2 トレイトパターン:能力で分岐する](cEZ.xhtml#a3RS)

[6.3 関数バリア:動的を局所化してJIT最適化を助ける](cEZ.xhtml#a3RT)

[6.4 インターフェース準拠:AbstractArray等の最小要件を満たす](cEZ.xhtml#a3RU)

[7. アンチパターンと注意点:型安定・型海賊・ユニオンの罠](cEZ.xhtml#a3RV)

[7.1 型安定性の破壊](cEZ.xhtml#a3RW)

[7.2 型海賊(type piracy)](cEZ.xhtml#a3RX)

[7.3 大きなUnion型](cEZ.xhtml#a3RY)

[8. 具体例:数値表現の拡張、誤差・単位・精度の扱い](cEZ.xhtml#a3RZ)

[8.1 単位付き量の型](cEZ.xhtml#a3S0)

[8.2 誤差付き値の型](cEZ.xhtml#a3S1)

[8.3 精度の切り替え](cEZ.xhtml#a3S2)

[9. データ構造とアルゴリズムの協調:疎行列、特殊行列、演算のオーバーロード](cEZ.xhtml#a3S3)

[10. メソッド設計の実務:キーワード、破壊的!、ドキュメント](cEZ.xhtml#a3S4)

[10.1 位置引数+キーワードで拡張性](cEZ.xhtml#a3S5)

[10.2 破壊的更新の明示](cEZ.xhtml#a3S6)

[10.3 ドキュメント文字列](cEZ.xhtml#a3S7)

[11. 実践例:モデル仕様を型で表し、解法をメソッドで差し替える](cEZ.xhtml#a3S8)

[12. 実務的原則(記述のみ)](cEZ.xhtml#a3S9)

[第5章配列と線形代数:ブロードキャスト・ビュー・BLAS/LAPACK](cEZ.xhtml#a3SA)

[1\. N次元配列の基礎:形状・軸・アクセスと再配置](cEZ.xhtml#a3SB)

[2. ブロードキャストと融合:ドットで短く書いて速くする](cEZ.xhtml#a3SC)

[3. ビューとコピーの選択:view/@viewsで無駄を減らす](cEZ.xhtml#a3SD)

[4. 低レベルループ最適化:eachindex/@inbounds/@simdの要点](cEZ.xhtml#a3SE)

[5\. BLASによる密行列の高速演算:LinearAlgebraの基本](cEZ.xhtml#a3SF)

[6\. LAPACKによる分解:lu/cholesky/qr/svd/eigenの実務](cEZ.xhtml#a3SG)

[7. 疎行列:SparseArraysで巨大問題を現実的に扱う](cEZ.xhtml#a3SH)

[8. 数値安定性と精度:丸め・条件数・アルゴリズム選択](cEZ.xhtml#a3SJ)

[9. 実戦レシピ:高速な配列処理と線形代数の統合](cEZ.xhtml#a3SK)

[9.1 移動平均・移動標準偏差(ビュー+ブロードキャスト)](cEZ.xhtml#a3SM)

[9.2 最小二乗回帰(QR分解)](cEZ.xhtml#a3SN)

[9.3 正定値系の解(Cholesky)](cEZ.xhtml#a3SP)

[9.4 疎行列の構築と積](cEZ.xhtml#a3SR)

[9.5 三角解とインプレース更新](cEZ.xhtml#a3SS)

[10. 運用の注意:測定・前処理・契約の明示](cEZ.xhtml#a3ST)

[第6章性能チューニングと計測:型安定性・アロケーション削減・プロファイリング](cEZ.xhtml#a3SU)

[1. 性能チューニングの基本姿勢と測定の原則](cEZ.xhtml#a3SV)

[2. 型安定性の確認方法:@code_warntypeと関数バリア](cEZ.xhtml#a3SW)

[2.1 @code_warntypeで推論結果を確認する](cEZ.xhtml#a3SX)

[2.2 フィールド型・コンテナ型の具体化](cEZ.xhtml#a3SY)

[2.3 関数バリアで動的を局所化](cEZ.xhtml#a3SZ)

[2.4 グローバル変数の回避](cEZ.xhtml#a3T0)

[3. ベンチマーク:BenchmarkTools.@btimeと実務的使い方](cEZ.xhtml#a3T1)

[3.1 基本の計測パターン](cEZ.xhtml#a3T2)

[3.2 セットアップとティアダウン](cEZ.xhtml#a3T3)

[3.3 @allocatedでアロケーション量を取得](cEZ.xhtml#a3T4)

[4. メモリアロケーションの可視化と削減](cEZ.xhtml#a3T5)

[4.1 破壊的更新と事前確保](cEZ.xhtml#a3T6)

[4.2 ビューでコピー回避](cEZ.xhtml#a3T7)

[4.3 形状と融合の活用](cEZ.xhtml#a3T8)

[4.4 コンテナの型安定とAnyの回避](cEZ.xhtml#a3T9)

[4.5 データレイアウト:SoAとAoS](cEZ.xhtml#a3TA)

[5. プロファイラの使い方:Profileでホットスポットを特定する](cEZ.xhtml#a3TB)

[5.1 基本操作](cEZ.xhtml#a3TC)

[5.2 サンプリング頻度とクリア](cEZ.xhtml#a3TD)

[5.3 観察のポイント](cEZ.xhtml#a3TE)

[6. ホットスポットの特定から修正まで:段階的手順](cEZ.xhtml#a3TF)

[7. コード生成の読み方:@code_typed・@code_llvm・@code_native](cEZ.xhtml#a3TG)

[7.1 @code_typed(型付きIR)](cEZ.xhtml#a3TH)

[7.2 @code_llvm(LLVM IR)](cEZ.xhtml#a3TJ)

[7.3 @code_native(ネイティブアセンブリ)](cEZ.xhtml#a3TK)

[8. 具体チューニング事例:移動統計の最適化](cEZ.xhtml#a3TM)

[8.1 素朴実装(毎回サブ配列をコピー)](cEZ.xhtml#a3TN)

[8.2 最適化実装(ビュー・事前確保・.=融合)](cEZ.xhtml#a3TP)

[8.3 計測](cEZ.xhtml#a3TR)

[9. 並列・マルチスレッドの計測と注意点](cEZ.xhtml#a3TS)

[9.1 スレッド化の基本](cEZ.xhtml#a3TT)

[10\. I/O・BLAS・LAPACKの活用によるアルゴリズム的最適化](cEZ.xhtml#a3TU)

[11. 解析系ユーティリティ:ロガー・アサート・契約](cEZ.xhtml#a3TV)

[12. ケース:ヒストグラム計算の段階的改善](cEZ.xhtml#a3TW)

[12.1 素朴版(push!でバケットに追加)](cEZ.xhtml#a3TX)

[12.2 改善版(事前計算・関数バリア・型安定)](cEZ.xhtml#a3TY)

[12.3 計測](cEZ.xhtml#a3TZ)

[13. レイテンシ(初回遅延)とTTFXの最適化:事前コンパイル](cEZ.xhtml#a3U0)

[14. 低レベル最適化の留意点:@fastmath・ポインタ操作の慎重な適用](cEZ.xhtml#a3U1)

[15. 結果の検証と性能回帰の監視:テストとベンチの併走](cEZ.xhtml#a3U2)

[16. 運用箇条(記述のみ)](cEZ.xhtml#a3U3)

[第7章メタプログラミング入門:Expr・マクロ・@generatedの設計術](cEZ.xhtml#a3U4)

[1. 式とASTの基本:Expr・クォート・アンクォート](cEZ.xhtml#a3U5)

[2. マクロの基礎:展開タイミング・ハイジーン・esc](cEZ.xhtml#a3U6)

[3. マクロ設計パターン:ロギング・計測・コンテキスト挿入](cEZ.xhtml#a3U7)

[3.1 計測マクロ(@timeitのスケルトン)](cEZ.xhtml#a3U8)

[3.2 コンテキスト挿入(リソース管理)](cEZ.xhtml#a3U9)

[3.3 式の書き換え(条件付挿入)](cEZ.xhtml#a3UA)

[4. @generated関数の基礎:型に応じたコード生成](cEZ.xhtml#a3UB)

[4.1 代表例:NTuple{N,T}の和をアンロール](cEZ.xhtml#a3UC)

[4.2 制約と注意](cEZ.xhtml#a3UD)

[4.3 Valで型レベルの選択](cEZ.xhtml#a3UE)

[5. 生成と保守のトレードオフ:いつマクロ・いつ関数](cEZ.xhtml#a3UF)

[6\. DSLへの橋渡し:宣言的仕様をコードへ落とす](cEZ.xhtml#a3UG)

[7. 式変換の定石:パターン・再帰・postwalk風処理](cEZ.xhtml#a3UH)

[8. デバッグ・検証:@macroexpand・@code_lowered・@code_typed](cEZ.xhtml#a3UJ)

[9. 実務的注意事項:副作用・安全・バージョン耐性](cEZ.xhtml#a3UK)

[第8章乱数と確率・統計:Random・Distributions・モンテカルロ(◇欄数種の管理)](cEZ.xhtml#a3UM)

[1. 再現性の要と乱数の役割](cEZ.xhtml#a3UN)

[2. 乱数生成器の選択と基本操作](cEZ.xhtml#a3UP)

[3. 乱数種の固定と管理:テスト・検証・配布のために](cEZ.xhtml#a3UR)

[4. 基本分布のサンプリング:一様・正規・指数・離散分布](cEZ.xhtml#a3US)

[5. 統計的推定の基礎:点推定・区間推定・簡易検定](cEZ.xhtml#a3UT)

[5.1 点推定・区間推定](cEZ.xhtml#a3UU)

[5.2 簡易検定(例:1標本t検定)](cEZ.xhtml#a3UV)

[6. モンテカルロ法の実装パターン:積分・打ち落とし法・重要度サンプリング](cEZ.xhtml#a3UW)

[6.1 モンテカルロ積分(単純サンプリング)](cEZ.xhtml#a3UX)

[6.2 打ち落とし法(幾何学的近似)](cEZ.xhtml#a3UY)

[6.3 重要度サンプリング(分散縮減)](cEZ.xhtml#a3UZ)

[7. 大規模サンプリングの並列化:スレッド・分散と再現性](cEZ.xhtml#a3V0)

[7.1 スレッド並列(Threads.@threads)](cEZ.xhtml#a3V1)

[7.2 分散(複数プロセス・複数マシン)](cEZ.xhtml#a3V2)

[8\. RNG品質のセルフチェック:分布・相関・ヒストグラム](cEZ.xhtml#a3V3)

[9. 応用例:ブートストラップ・ベイズ推論・モンテカルロ最適化](cEZ.xhtml#a3V4)

[9.1 ブートストラップ(再標本化)](cEZ.xhtml#a3V5)

[9.2 ベイズ推論(乱数による事後分布近似の発想)](cEZ.xhtml#a3V6)

[9.3 モンテカルロ最適化(確率的探索)](cEZ.xhtml#a3V7)

[10. 運用・設計の指針:再現性・性能・保守性](cEZ.xhtml#a3V8)

[第9章可視化の作法:Plots・Makieで伝わる図を作る(三角見せる数値)](cEZ.xhtml#a3V9)

[1. 可視化の基本姿勢:伝えるための設計と再現性](cEZ.xhtml#a3VA)

[2\. Plots.jlの基礎:ライン・散布・ヒストグラム・ヒートマップ](cEZ.xhtml#a3VB)

[3. テーマとレイアウト最適化:一貫したスタイルと紙面設計](cEZ.xhtml#a3VC)

[4. 相関・分布の可視化:散布・六角ビン・密度・相関行列](cEZ.xhtml#a3VD)

[5. 時系列と不確実性:リボン・移動統計・ログスケール](cEZ.xhtml#a3VE)

[6. ヒートマップ・等高線:色スケール・正規化・色覚配慮](cEZ.xhtml#a3VF)

[7\. 3D可視化の実務:Makieでのlines・scatter・surface・volume](cEZ.xhtml#a3VG)

[8\. Makieのインタラクティブ:Observables・スライダ・リンク](cEZ.xhtml#a3VH)

[9. 論文・レポート品質への仕上げ:解像度・形式・注釈・一貫性](cEZ.xhtml#a3VJ)

[10. 大規模データの可視化:過密回避・要約・多段表現](cEZ.xhtml#a3VK)

[11. 再現性の仕組み:スタイル固定・乱数種・出力パイプライン](cEZ.xhtml#a3VM)

[12. 実践レシピ:相関行列+各ペア散布、3Dサーフェス+断面](cEZ.xhtml#a3VN)

[12.1 相関行列とペア散布の組版](cEZ.xhtml#a3VP)

[12.2 3Dサーフェスと断面の統合(Makie)](cEZ.xhtml#a3VR)

[13. 運用上の注意:凡例の簡潔化・注釈の最小化・尺度の誤解防止](cEZ.xhtml#a3VS)

[第10章データ処理:DataFrames・CSV・Arrowで整える(丸前処理大全)](cEZ.xhtml#a3VT)

[1. 方針と再現可能な前処理の設計](cEZ.xhtml#a3VU)

[2. 取り込みの基本:CSVから正しく読む](cEZ.xhtml#a3VV)

[3. 初期検査:形状・型・欠損・基本統計](cEZ.xhtml#a3VW)

[4. 型変換:文字列からDate/DateTime、数値への安全な変換](cEZ.xhtml#a3VX)

[5. 欠損処理:削除・代入・宣言的制御](cEZ.xhtml#a3VY)

[6. クリーニング:文字列の正規化・カテゴリ化・重複処理](cEZ.xhtml#a3VZ)

[7. 列操作とインプレース更新:select!/transform!/combine](cEZ.xhtml#a3W0)

[8. 結合:joinファミリで外部データと統合](cEZ.xhtml#a3W1)

[9. 集計:groupby/combineでロバストな要約](cEZ.xhtml#a3W2)

[10. 時系列の前処理:ソート・欠損補間・移動統計](cEZ.xhtml#a3W3)

[11. メモリ効率の工夫:不要列削除・インプレース・カテゴリ化・文字列プーリング](cEZ.xhtml#a3W4)

[12. カラム指向フォーマット:Arrowで高速・再現性・相互運用](cEZ.xhtml#a3W5)

[13. 前処理パイプラインの設計:関数分割・検証・ログ](cEZ.xhtml#a3W6)

[14. 検証可能性:不変条件・キー整合・数値的妥当性](cEZ.xhtml#a3W7)

[15. 書き出し:CSV/Arrowの選択とエクスポート設計](cEZ.xhtml#a3W8)

[16. 大規模データの実務:部分読み・段階出力・分割処理](cEZ.xhtml#a3W9)

[17. 監査とドキュメント化:処理履歴・スキーマ・依存環境](cEZ.xhtml#a3WA)

[18. 総合例:入口から出力までを一気通貫で(スクリプト骨子)](cEZ.xhtml#a3WB)

[第11章微分方程式と数値解析:DifferentialEquations・積分・根探し](cEZ.xhtml#a3WC)

[1. 設計指針:問題の記述、アルゴリズム選択、検証可能性](cEZ.xhtml#a3WD)

[2\. ODEの基本:問題定義、解法、出力の取り扱い](cEZ.xhtml#a3WE)

[3. 剛性の扱い:症状、選択方針、陰的法](cEZ.xhtml#a3WF)

[4. イベント検知:連続/離散コールバックの設計](cEZ.xhtml#a3WG)

[5. パラメータ推移と感度:パラメータ掃引、補間比較、指標化](cEZ.xhtml#a3WH)

[6\. SDE(確率微分方程式):ノイズの扱い、再現性、並列化](cEZ.xhtml#a3WJ)

[7. 数値積分:QuadGKで一次元積分を高精度に](cEZ.xhtml#a3WK)

[8. 非線形方程式の解法:NLsolveで根探し](cEZ.xhtml#a3WM)

[9. 誤差評価・検証:トレランス、参考解、保存則、不変量](cEZ.xhtml#a3WN)

[10. 時間刻み・安定性:固定刻み、適応刻み、ステップ診断](cEZ.xhtml#a3WP)

[11. 実践例:Lotka–Volterra 系(イベント・パラメータ掃引・検証)](cEZ.xhtml#a3WR)

[12\. ODEと積分・根探しの連携:到達時刻の解法、確率計算](cEZ.xhtml#a3WS)

[13. 数値安定性・スケーリング:前処理、スケール合わせ、境界条件](cEZ.xhtml#a3WT)

[14. 計算効率:saveat、スパース/密、ベクトル化、インプレース](cEZ.xhtml#a3WU)

[15. テスト・ロギング・再現性:品質保証の枠組み](cEZ.xhtml#a3WV)

[16. 補足:二分法・ニュートン法の自作スケルトン(安全設計)](cEZ.xhtml#a3WW)

[17. 参考的ユースケース:剛性+イベント+根探しの複合](cEZ.xhtml#a3WX)

[18. 実装上の注意まとめ(記述のみ)](cEZ.xhtml#a3WY)

[第12章最適化と数理計画:Optim・JuMPで特(◇製薬と目的)](cEZ.xhtml#a3WZ)

[1. 設計指針:目的・制約・データ・再現性を軸に最適化を構成する](cEZ.xhtml#a3X0)

[2. 連続最適化(無制約):Optim.jlで勾配・準ニュートン法を使う](cEZ.xhtml#a3X1)

[3. 連続最適化(境界付き):Fminboxで上下限を課す](cEZ.xhtml#a3X2)

[4. 実例:ロジスティック回帰の損失最小化(L2正則化)](cEZ.xhtml#a3X3)

[5\. JuMPでの線形計画(LP):宣言的モデル化とソルバ連携](cEZ.xhtml#a3X4)

[6\. MIP(混合整数計画):0-1意思決定の導入](cEZ.xhtml#a3X5)

[7\. QP/最小分散ポートフォリオ:二次目的+線形制約](cEZ.xhtml#a3X6)

[8. 非線形制約(NLP):@NLobjective・@NLconstraintでモデリング](cEZ.xhtml#a3X7)

[9. 現場データとの結合:DataFramesからJuMPへ安全に渡す](cEZ.xhtml#a3X8)

[10. 感度解析:パラメータ掃引・双対値・比較の再現性](cEZ.xhtml#a3X9)

[11. 実験の設計:停止条件・時間制限・ギャップ管理・ログ](cEZ.xhtml#a3XA)

[12. モデル安全性:ビッグMの慎重な使用・スケーリング・弾性目的](cEZ.xhtml#a3XB)

[13. ウォームスタート・分割統治・緩和解の活用](cEZ.xhtml#a3XC)

[14\. NLPでの初期値・スケール・正則化:収束安定化の技法](cEZ.xhtml#a3XD)

[15. 連続最適化と数理計画の橋渡し:ハイブリッド設計](cEZ.xhtml#a3XE)

[16. 品質保証:単体テスト・可行性検査・数値検証](cEZ.xhtml#a3XF)

[17. 再現可能性:環境固定・データバージョニング・レポート自動化](cEZ.xhtml#a3XG)

[18. 総合ケース:需要充足の生産・配分(LP→MIP→感度)](cEZ.xhtml#a3XH)

[19. 実装スニペット集:よく使うJuMP/Optimの補助操作](cEZ.xhtml#a3XJ)

[20. 実務の心得(記述のみ)](cEZ.xhtml#a3XK)

[第13章並列・分散・GPU:Threads・Distributed・CUDA(三角スケールさせる)](cEZ.xhtml#a3XM)

[1. 設計指針:CPUスレッド・分散プロセス・GPUの役割分担と再現性](cEZ.xhtml#a3XN)

[2. スレッド並列の基本:Threads.@threads、競合回避、偽共有の抑制](cEZ.xhtml#a3XP)

[3. スレッド並列の乱数:スレッドローカルRNG、再現性の確保](cEZ.xhtml#a3XR)

[4. タスク並行と軽量スレッド:@spawn/@sync、@async/Channel](cEZ.xhtml#a3XS)

[5. 分散処理:Distributedでワーカー管理、pmap/@distributedの使い分け](cEZ.xhtml#a3XT)

[6\. GPU基礎:CUDA.jlのCuArrayとブロードキャスト、独自カーネル](cEZ.xhtml#a3XU)

[7\. GPUでの乱数と再現性:現実的な運用パターン](cEZ.xhtml#a3XV)

[8. メモリ転送の最適化:バッチ、ピン留め、レイアウトと融合](cEZ.xhtml#a3XW)

[9. 統合レシピ:Threads・Distributed・CUDAのモンテカルロと検証](cEZ.xhtml#a3XX)

[10. 性能計測の指針:BenchmarkTools、同期とイベント、統計の読み方](cEZ.xhtml#a3XY)

[11. 分散の運用:ワーカーライフサイクル、データ配置、障害耐性の基本](cEZ.xhtml#a3XZ)

[12\. GPUカーネル設計の一歩先:ストライド、境界チェック、インプレース合成](cEZ.xhtml#a3Y0)

[13. 仕様化とドキュメント:乱数・分割・計測の規則を明文化](cEZ.xhtml#a3Y1)

[14. よくある落とし穴と回避策(記述のみ)](cEZ.xhtml#a3Y2)

[15. 実務指針(記述のみ)](cEZ.xhtml#a3Y3)

[第14章科学計算の実践:ModelingToolkitで使用奇術師徳(丸記述から買いへ)](cEZ.xhtml#a3Y4)

[1. 設計指針:記述(仕様)から解(数値)へ](cEZ.xhtml#a3Y5)

[2. 記号・数値ハイブリッドの基本構文](cEZ.xhtml#a3Y6)

[3\. ODEモデルの定義から解法まで(Lotka–Volterra を例に)](cEZ.xhtml#a3Y7)

[4. サブシステム合成:再利用可能なモデルを積み木で組む](cEZ.xhtml#a3Y8)

[5\. DAE(微分代数方程式)の定式化:制約付き系の扱い](cEZ.xhtml#a3Y9)

[6. イベント・切替:物理的境界と政策介入を仕様記述する](cEZ.xhtml#a3YA)

[7. 記号的ヤコビアンとコード生成:高速化の基礎](cEZ.xhtml#a3YB)

[8. 感度解析:パラメータに対する応答の定量化](cEZ.xhtml#a3YC)

[9. モデル同定:Optimization.jlでパラメータを推定する](cEZ.xhtml#a3YD)

[10. 仕様記述としてのモデル言語化:検証可能性を高める](cEZ.xhtml#a3YE)

[11. 実務の前処理・後処理:データ整合・可視化・成果物](cEZ.xhtml#a3YF)

[12. ステディステート・根探し:定常解の計算](cEZ.xhtml#a3YG)

[13. 数値安定性とスケーリング:無次元化のすすめ](cEZ.xhtml#a3YH)

[14. チーム開発のための構成:モジュール化とテスト](cEZ.xhtml#a3YJ)

[15. 実例レシピ:質点バネダンパの同定ワークフロー(抜粋)](cEZ.xhtml#a3YK)

[16. 高度機能への入口:PDE離散化と標準ライブラリ](cEZ.xhtml#a3YM)

[17. 性能運用の要点:ヤコビ・イベント・剛性・ログ](cEZ.xhtml#a3YN)

[18. 失敗の切り分け:一致性・スケール・可視化で原因を掴む](cEZ.xhtml#a3YP)

[19. 再現可能なプロジェクト雛形(骨子)](cEZ.xhtml#a3YR)

[20. 実務の心得(記述のみ)](cEZ.xhtml#a3YS)

[第15章機械学習と確率的プログラミング:Flux・MLJ・Turing](cEZ.xhtml#a3YT)

[1. 設計指針:データ→前処理→モデル→学習→検証→配布の一気通貫と再現性](cEZ.xhtml#a3YU)

[2. 再現性の基礎:乱数種・環境・ログ・出力グリッド](cEZ.xhtml#a3YV)

[3. データ前処理の基本:スケーリング・エンコーディング・分割](cEZ.xhtml#a3YW)

[4\. Fluxによる深層学習:モデル定義・損失・オプティマイザ・学習ループ](cEZ.xhtml#a3YX)

[5. 特徴量前処理と評価の設計:パイプライン化・早期停止・モデル選択](cEZ.xhtml#a3YY)

[6\. MLJによる統一インターフェース:モデル選択・前処理・チューニング](cEZ.xhtml#a3YZ)

[7\. Turingによるベイズ推論:モデル化・MCMC・事後予測](cEZ.xhtml#a3Z0)

[8\. Flux×Turingの橋渡し:確率的深層学習の最小例](cEZ.xhtml#a3Z1)

[9. 乱数並列時の再現性:スレッド・プロセス・GPU](cEZ.xhtml#a3Z2)

[10. 評価指標と検証:ホールドアウト・交差・時系列の注意](cEZ.xhtml#a3Z3)

[11. モデル配布:重み・前処理・メタデータの一体化](cEZ.xhtml#a3Z4)

[12. 実戦レシピ:Fluxでの分類、MLJでの比較、Turingでの不確実性評価](cEZ.xhtml#a3Z5)

[12.1 Fluxで二値分類(簡略版)](cEZ.xhtml#a3Z6)

[12.2 MLJでベースライン比較](cEZ.xhtml#a3Z7)

[12.3 Turingで閾値の不確実性評価(概念)](cEZ.xhtml#a3Z8)

[13. ハイパーパラメータ最適化:探索空間・評価・停止条件](cEZ.xhtml#a3Z9)

[14. パイプラインの品質保証:テスト・プロファイリング・監査](cEZ.xhtml#a3ZA)

[15. 実務上の注意:ドリフト対策・分布外検知・堅牢化](cEZ.xhtml#a3ZB)

[16. 実務指針(記述のみ)](cEZ.xhtml#a3ZC)

[第16章 DSL設計と仕様記述言語としてのJulia(◇領域固有の文法を作る)](cEZ.xhtml#a3ZD)

[1. 目的と要件定義:領域固有言語(DSL)で何を実現するか](cEZ.xhtml#a3ZE)

[2. 記法設計方針:宣言的中心・命令的補助・最小核の徹底](cEZ.xhtml#a3ZF)

[3. 型設計:抽象型・構造体・パラメトリック型で語彙を固定する](cEZ.xhtml#a3ZG)

[4. マクロ設計:衛生・esc・gensym・ブロック構文](cEZ.xhtml#a3ZH)

[5. 文法の最小例:目的・制約・イベントを宣言する](cEZ.xhtml#a3ZJ)

[6. 検証可能な仕様記述:型チェック・値域・単位・一貫性](cEZ.xhtml#a3ZK)

[7. 中間表現と実行計画:コンパイルステップの設計](cEZ.xhtml#a3ZM)

[8. シミュレータ連携:仕様から数値へ(ODE・イベントの橋渡し)](cEZ.xhtml#a3ZN)

[9. エラーメッセージ:出典・位置・対処法を明示する](cEZ.xhtml#a3ZP)

[10. 保守性と拡張性のバランス:小さな核・プラグイン・直交性](cEZ.xhtml#a3ZR)

[11. 成功・失敗から学ぶ設計指針](cEZ.xhtml#a3ZS)

[12. 仕様→最適化・シミュレーション連携の最小レシピ](cEZ.xhtml#a3ZT)

[13. メタプログラミングによる最適化:@generated・コード生成の適用範囲](cEZ.xhtml#a3ZU)

[14. 品質保証:テスト・プロファイリング・ドキュメント・バージョニング](cEZ.xhtml#a3ZV)

[15. 実務の前処理・後処理:データ・可視化・成果物の一体化](cEZ.xhtml#a3ZW)

[16. 拡張設計パターン:タグ・トレイト・インターフェース準拠](cEZ.xhtml#a3ZX)

[17. 運用設計:プロジェクト骨子とワークフロー](cEZ.xhtml#a3ZY)

[18. 具体例:制約付き二変数最小化DSLの小さな完成形(雛形)](cEZ.xhtml#a3ZZ)

[19. 性能・再現性・保守の要点(記述のみ)](cEZ.xhtml#a400)

[20. 運用箇条(記述のみ)](cEZ.xhtml#a401)

[第17章パッケージ開発・テスト・ドキュメント:PkgTemplates・Documenter](cEZ.xhtml#a402)

[1. 方針:再現可能・保守可能・拡張可能な「Juliaパッケージ」を設計する](cEZ.xhtml#a403)

[2. 雛形作成:PkgTemplatesで初期セットアップを自動化する](cEZ.xhtml#a404)

[3. パッケージの最小公開API:型・関数・ドキュメント文字列](cEZ.xhtml#a405)

[4. 単体テスト:Testで正しさと境界を押さえる](cEZ.xhtml#a406)

[5. ベンチテスト:BenchmarkTools・PkgBenchmarkで性能回帰を監視する](cEZ.xhtml#a407)

[6. ドキュメント生成:DocumenterでAPIとチュートリアルを自動構築](cEZ.xhtml#a408)

[8. バージョニングと互換性管理:Project.tomlの[compat]を徹底する](cEZ.xhtml#a409)

[9. 変更管理:CHANGELOG・破壊的変更の告知・移行ガイド](cEZ.xhtml#a40A)

[10. 登録と配布:レジストリ登録・タグ付け・リリース](cEZ.xhtml#a40B)

[11. ドキュメント実務:構成・ナラティブ・API・例・FAQ](cEZ.xhtml#a40C)

[12. テストの拡張:プロパティ・隠れバグ検知・カバレッジ](cEZ.xhtml#a40D)

[13. 性能の運用:ベンチ計測・ボトルネックの特定・回帰検知](cEZ.xhtml#a40E)

[14. 開発フロー:dev運用・Revise・ローカルCI](cEZ.xhtml#a40F)

[15. エラーメッセージと例外設計:診断可能な失敗にする](cEZ.xhtml#a40G)

[16. 秘密情報とライセンス:配布時の安全と適法性](cEZ.xhtml#a40H)

[17. 実務チェックリスト(記述のみ)](cEZ.xhtml#a40J)

[18. 参考スニペット:Release準備の一連コマンド例(ローカル)](cEZ.xhtml#a40K)

[19. 実務上の注意(記述のみ)](cEZ.xhtml#a40M)

[第18章プロジェクト運用と再現可能性:環境・依存関係・成果物管理(△長期運用)](cEZ.xhtml#a40N)

[1. 原則:長期運用で壊れないしくみ(再現性・可観測性・自動化)](cEZ.xhtml#a40P)

[2. 環境ピン留め:Project.tomlとManifest.tomlをチームの単位にする](cEZ.xhtml#a40R)

[3. 互換性固定と依存監査:[compat]・pin・resolveで予期せぬ更新を防ぐ](cEZ.xhtml#a40S)

[4. 実行入口の標準化:ブートストラップとランナー](cEZ.xhtml#a40T)

[5. 乱数種と設定値の管理:入口で固定、関数にはrngを渡す](cEZ.xhtml#a40U)

[6. 成果物パイプライン:図・表・数値・レポートをコードで一括生成](cEZ.xhtml#a40V)

[7. ロギング・プロベナンス:入力・設定・環境・ハッシュを記録する](cEZ.xhtml#a40W)

[8. データ・バイナリ資源の再現:Artifacts.tomlで資源を宣言する](cEZ.xhtml#a40X)

[9. テスト戦略:正しさ・再現・プロパティ・性能回帰](cEZ.xhtml#a40Y)

[10. レビューと品質保証:PRテンプレート・チェックリスト・コード規約](cEZ.xhtml#a40Z)

[11\. CI(継続的インテグレーション):テスト・パイプライン・成果物の検証](cEZ.xhtml#a410)

[12. バージョニング・リリース:セマンティックバージョニングと変更記録](cEZ.xhtml#a411)

[13. セキュリティ・秘密情報:ENVとシークレット管理](cEZ.xhtml#a412)

[14. 入出力の標準化:ファイル命名・フォーマット・ディレクトリ規約](cEZ.xhtml#a413)

[15. 起動オプションと性能:-t・キャッシュ・中間成果物の再利用](cEZ.xhtml#a414)

[16. 失敗時の切り分け:環境・設定・入力・コードの4象限で診断](cEZ.xhtml#a415)

[17. チーム運用:レビュー・PRテンプレート・運用チェックリスト](cEZ.xhtml#a416)

[18. 実務スニペット:小さな「一括生成」コマンドを整備する](cEZ.xhtml#a417)

[19. 長期保守の心得(記述のみ)](cEZ.xhtml#a418)

[第19章ケーススタディ:金融・材料・気象・ライフサイエンスの実例](cEZ.xhtml#a419)

[1. 再現可能なケーススタディの設計(共通フレーム)](cEZ.xhtml#a41A)

[2. 金融:平均–分散ポートフォリオの最適化と簡易バックテスト](cEZ.xhtml#a41B)

[2.1 データ取得と前処理(合成リターン系列)](cEZ.xhtml#a41C)

[2.2 モデル化(JuMPで平均–分散最小化)](cEZ.xhtml#a41D)

[2.3 可視化(構成比・効率フロンティア)](cEZ.xhtml#a41E)

[2.4 検証(簡易バックテスト)](cEZ.xhtml#a41F)

[3. 材料:1D熱拡散(熱方程式)の離散化・推定・可視化](cEZ.xhtml#a41G)

[3.1 問題設定(合成材料棒の熱拡散)](cEZ.xhtml#a41H)

[3.2 可視化(温度分布の時間発展)](cEZ.xhtml#a41J)

[3.3 検証(質量保存的観点・安定性)](cEZ.xhtml#a41K)

[4. 気象:Lorenz-63 による擬似大気力学・感度と可視化](cEZ.xhtml#a41M)

[4.1 モデル化(Lorenz-63)](cEZ.xhtml#a41N)

[4.2 可視化(軌道・相図)](cEZ.xhtml#a41P)

[4.3 検証(パラメータ感度)](cEZ.xhtml#a41R)

[5. ライフサイエンス:SIR 感染モデルの推定・検証](cEZ.xhtml#a41S)

[5.1 モデル化(SIR 方程式)](cEZ.xhtml#a41T)

[5.2 合成観測と前処理](cEZ.xhtml#a41U)

[5.3 同定(最小二乗)](cEZ.xhtml#a41V)

[5.4 検証と可視化](cEZ.xhtml#a41W)

[6\. 4ケースの統合パイプラインとスモークテスト](cEZ.xhtml#a41X)

[7. ドメイン別の品質保証観点(チェックリスト)](cEZ.xhtml#a41Y)

[8. 実運用のための成果物規約(命名・フォーマット)](cEZ.xhtml#a41Z)

[9. 付記:コード断片の関数化(再利用性の向上)](cEZ.xhtml#a420)

[10. 運用指針(記述のみ)](cEZ.xhtml#a421)

[第20章付録:REPL技法・標準ライブラリ索引・用語集(丸参照しやすく)](cEZ.xhtml#a422)

[1\. REPLのモード早見(すぐ使える最小装備)](cEZ.xhtml#a423)

[2\. REPLショートカット&「手が止まらない」操作集](cEZ.xhtml#a424)

[3. 環境運用の要点(Project.toml/Manifest.toml)](cEZ.xhtml#a425)

[4. 標準ライブラリ索引(用途と主要APIの目印)](cEZ.xhtml#a426)

[5. 用語集(現場頻出ワードの短義と落とし穴)](cEZ.xhtml#a427)

[6\. REPLでの計測・診断・編集(最短ループ)](cEZ.xhtml#a428)

[7. 標準ライブラリ実用断片(すぐ貼って使える)](cEZ.xhtml#a429)

[8. トラブルシューティング(よくある落とし穴と脱出)](cEZ.xhtml#a42A)

[9. 参考資料・コミュニティ入口(名前索引)](cEZ.xhtml#a42B)

[10. 実務チェックリスト(REPL・標準・再現の総まとめ)](cEZ.xhtml#a42C)

---

# 第1章序章:Juliaの世界観と本書のねらい(汎用から計算科学まで)

# 1. 読者像と到達目標

本書は、汎用プログラミングの基礎を持ちながら科学技術計算や数値解析に踏み込みたい方、あるいは既存の言語で性能と再現性の両立に悩む方を主な読者として想定しています。研究者、エンジニア、データサイエンティスト、シミュレーション担当、最適化・オペレーションズリサーチの実務者など、領域は問いません。到達目標は明確です。第一に、Juliaを汎用言語として日常業務に使えるだけの基礎体力を身につけます。第二に、数値線形代数・微分方程式・最適化・可視化などの計算科学領域で、Juliaの言語機能とライブラリを「設計意図に沿って」使いこなせるようになります。第三に、仕様記述言語として、問題の構造や仮定、制約、評価指標をコードへ過不足なく落とし込む技法を学び、チームで共有・検証できる再現可能なプロジェクト運用を確立します。

この章では、Juliaの設計思想を掘り下げます。多重ディスパッチ、JITコンパイル、LLVMによる低レベル最適化という核を理解し、それらが「書きやすさ」と「速さ」を両立させる仕組みであることを、具体例を交えながら確認します。また、汎用プログラミングと高度な計算科学を同一言語で扱う意義を、プロトタイピングから本番運用までの流れの中で整理します。最後に、仕様記述言語としての側面を解説し、Juliaがドメイン固有言語(DSL)の土台として機能する理由を示します。

* * *

# 2\. Juliaが解く課題:二言語問題の解消と再現性

現場では、試作(分析・検証)と本番(性能・保守)を別言語で行う「二言語問題」が頻出します。例えば、プロトタイピングをPython/R/Matlabで行い、最終的な高速化をC/C++/Fortranで書き直す、という流れです。この分業は、仕様の齟齬、検証可能性の低下、移植コストの増大、テストの重複などを招きます。Juliaは、動的言語らしい表現力と、コンパイル言語並みの性能を同時に満たす設計により、同じコードベースで試作から本番までを一貫させます。

加えて、再現性は研究・実務の生命線です。Juliaではプロジェクト環境を独立管理し、依存関係やバージョンを固定できます。乱数の種や設定値をコードと環境に明示的に縛ることで、結果の再生成が容易になります。

# 乱数の再現性:種を固定  
using Random  
Random.seed!(1234)  
rand(3) # 実行のたびに同じ結果が得られる

プロジェクト環境は、作業ディレクトリでアクティベートし、依存を再現する流れが基本です。

# REPLのPkgモード(`]`で入る)例  
# ] activate .  
# ] instantiate  
# ] status

この仕組みは、計算科学の長期的・共同的な活動において、分析の「再現可能性」と成果物の「検証可能性」を確保します。言語レベルの表現力とツールレベルの再現性が揃うことで、仕様→実装→実験→評価→配布のライフサイクルを破綻なく回せるのが、Juliaの大きな強みです。

* * *

# 3. 設計思想の核:多重ディスパッチ・JIT・LLVM

Juliaの設計の核心は、(1) 多重ディスパッチ、(2) JITコンパイル、(3) LLVMによる最適化の三位一体にあります。これらを理解すると、なぜ「短いコードが速く、保守しやすいのか」が腹落ちします。

## 3.1 多重ディスパッチ:型に応じて最適なメソッドを選ぶ

多重ディスパッチは、関数呼び出しを実引数の型組み合わせに応じて動的に解決する仕組みです。オブジェクト指向言語の単一ディスパッチ(受け手オブジェクトの型でメソッド選択)とは異なり、すべての引数型が選択に関与します。数値計算では、Float64・BigFloat・複素数・配列・疎行列・ユーザー定義の数値型など多様な表現に対して、一つの関数名で最適な実装を提供できます。

# 同じ関数名fが引数の型ごとに最適化されたメソッドを持つ  
f(x::Int) = x + 1  
f(x::Float64) = x + 1.0  
f(x::Complex) = real(x) + 1 + im*imag(x)  


f(10) # -> 11 (Intに最適)  
f(10.0) # -> 11.0 (Float64に最適)  
f(1 + 2im) # -> 2 + 2im (Complexに最適)

抽象型・具象型・パラメトリック型を組み合わせることで、拡張性と性能を両立します。

abstract type NumberLike end  
struct MyFloat{T<:AbstractFloat} <: NumberLike  
v::T  
end  


# 抽象型を受ける汎用実装  
g(x::NumberLike) = "generic"  


# 具体型を受ける高速実装  
g(x::MyFloat{Float64}) = x.v + 0.5  


g(MyFloat(1.0)) # 1.5(専用メソッドが選ばれる)

この仕組みにより、アルゴリズムを「型で編成」できます。新しい数値表現を導入しても、関数名はそのままにメソッドを追加すればよく、既存コードを壊しません。計算科学における「表現の多様性」を受け止めるために、多重ディスパッチは合理的な選択です。

## 3.2 JITコンパイル:最初にコンパイル、次回から速い

JuliaはJIT(Just-In-Time)コンパイルにより、関数が初めて特定の型で呼ばれるタイミングでコンパイルし、以後はコンパイル済みのネイティブコードを再利用します。これにより、REPLでの探索的作業と、長時間のバッチ計算の双方で、最適化された実行が得られます。初回呼び出し時のコンパイル遅延は設計上存在しますが、長い計算や反復タスクではオーバーヘッドが相対的に小さくなります。型が安定する設計を心がけると、JITの恩恵を最大化できます。

## 3.3 LLVM:低レベル最適化を言語が享受

JuliaはLLVM(低レベル仮想マシン)をバックエンドに持ち、ベクトル化・インライン展開・レジスタ割り当てなどの最適化を受けます。これにより、数値ループや配列演算を高水準の表現で書いても、低レベル最適化が自動的に適用され、C/C++に近い性能が得られます。ユーザーは「どう計算するか」を高い抽象度で書き、コンパイラに「どう速くするか」を委ねられます。

* * *

# 4. 汎用言語としてのJulia:日常の開発力を底上げする

Juliaは科学計算に強いだけの特殊言語ではありません。日常の業務アプリ、スクリプト、データ処理、ツール作成にも十分使えます。以下の観点で、汎用言語としての実力を確認します。

## 4.1 書きやすさ:動的言語の表現力

可読性の高い文法、柔軟なコレクション型、直感的な配列操作、複数戻り値、パイプ演算子やドット演算による簡潔な記述など、日々の作業を軽量にします。

# 複数戻り値で安全な分解  
function stats(xs)  
n = length(xs)  
μ = sum(xs) / n  
σ = sqrt(sum((x - μ)^2 for x in xs) / (n - 1))  
return μ, σ  
end  


mean, std = stats([1,2,3,4,5])

## 4.2 エラー処理とテスト:信頼性の確保

例外処理やテストの仕組みを備え、失敗を早期に検知して品質を守ります。

# 例外処理  
try  
x = parse(Int, "not a number")  
catch e  
@warn "parse failed" error=e  
end

## 4.3 環境管理:再現性の標準化

プロジェクトごとに環境を分離し、依存関係を固定して、誰がどこで実行しても同じ結果が得られる状態を用意できます。これは汎用開発でも強力です。設定ファイルやデータとコードを同じレポジトリで管理し、CIの導入でビルド・テストを自動化することで、分析の信頼性が向上します。

## 4.4 可視化・レポーティング:伝わる成果物

図表・レポートの生成をコード化しておくと、再実行だけで最新の成果物を出力できます。分析の「説明責任」を果たすために、可視化のパイプラインは重要です。Juliaでは、図のスタイル・レイアウト・配色の一貫性を保ちながら、数値と図を同じ環境で生成でき、レポートの再現性が確保できます。

* * *

# 5. 計算科学・数値解析に向いた言語機能:正確さ・速さ・拡張性

科学技術計算の現場では、性能だけでなく、数値安定性・誤差管理・表現の拡張性が同等に重要です。Juliaは、言語機能レベルでこれらを支える設計を持ちます。

## 5.1 配列とブロードキャスト:短く書いて速い

配列を対象とした要素ごとの演算は、ドット演算で簡潔に書けます。融合(fusing)により、不要な中間配列の生成を避け、メモリアロケーションを減らして高速化します。

# ブロードキャストの融合例  
x = rand(10_000)  
y = rand(10_000)  
z = @. sin(x) + cos(y) # @. は行内の演算にドットを自動付与

ビュー(@views)を使えば、部分配列操作での余計なコピーを防げます。疎行列や多次元配列も標準で扱え、BLAS/LAPACKとの連携で信頼性の高い線形代数計算が可能です。

## 5.2 浮動小数点と数値安定性:意識的に扱う

浮動小数点は有限精度の近似表現であり、演算順序や丸め誤差の影響を受けます。最小二乗計算、固有値、条件数の評価などでは、アルゴリズム選択が安定性に直結します。Juliaでは、Float64を基本に、必要に応じて高精度のBigFloatを使う戦略が現実的です。型で精度を切り替えながら、同じ関数名で異なる精度に対して最適なメソッドを用意できるのは、多重ディスパッチの利点です。

sum_stable(xs::AbstractVector{Float64}) = sum(xs) # 高速・一般的  
sum_stable(xs::AbstractVector{BigFloat}) = sum(xs) # 高精度志向

## 5.3 微分方程式・最適化・根探し:表現と解法の橋渡し

微分方程式は、モデルの仕様(状態・パラメータ・外力・境界条件)を明確に記述し、適切な解法(刻み、誤差制御、剛性判定、イベント処理)を選ぶ設計が重要です。Juliaでは、状態ベクトルやパラメトリック型を使い、モデルを型安全に表現できます。最適化や根探しでも、目的関数・制約・初期値・停止条件・評価指標を仕様としてコード化することで、検証可能性が高まります。

# 根探しの小例(二分法のスケルトン)  
function bisect(f, a, b; tol=1e-8, maxiter=10_000)  
fa, fb = f(a), f(b)  
fa*fb < 0 || error("bracket must have opposite signs")  
for _ in 1:maxiter  
c = (a + b)/2  
fc = f(c)  
if abs(fc) < tol  
return c  
elseif fa*fc < 0  
b, fb = c, fc  
else  
a, fa = c, fc  
end  
end  
return (a + b)/2  
end  


bisect(x -> x^3 - 2, 0.0, 2.0) # 立方根の近似

## 5.4 型安定性:性能の前提条件

JITとLLVMの恩恵を最大化するには、型安定性が鍵です。「同じ関数が常に同じ型を返す」設計により、コンパイラが最適化しやすくなります。動的に異なる型を返す関数は、ボックス化や分岐のオーバーヘッドを招きます。戻り値の型を揃える、コレクションの要素型を固定する、計算途中で不要な型変換を避ける、といった作法が性能を大きく左右します。

# 悪い例:条件で戻り値の型が変わる  
h(x) = x > 0 ? x : "negative" # Int か String を返す → 型不安定  


# 良い例:戻り値の型を数値で統一  
h_fixed(x) = x > 0 ? x : -x # 常に数値を返す → 型安定

* * *

# 6. 仕様記述言語としてのJulia:DSLで構造をコード化する

仕様記述言語とは、問題の構造(対象、関係、仮定、制約、評価)を表現するための文法と語彙を備えた言語のことです。Juliaは、マクロ・多重ディスパッチ・パラメトリック型・メタプログラミングといった機能により、具体的な領域に最適化されたDSLを容易に設計できます。これにより、モデル・アルゴリズム・実験設定を「説明可能な形」でコード化し、チームで共有・レビュー・再利用できます。

## 6.1 ドメイン固有の型設計

ドメインオブジェクトを型で表すと、誤用を防ぎ、アルゴリズムの適用条件を型で表現できます。例えば、単位つき量や区間、制約集合などを型で表現すれば、計算の安全性が高まります。

# 区間型の例:仕様としての範囲を明示する  
struct Interval{T<:Real}  
lo::T  
hi::T  
function Interval(lo::T, hi::T) where {T<:Real}  
lo <= hi || error("lo must be <= hi")  
new{T}(lo, hi)  
end  
end  


contains(I::Interval, x) = I.lo <= x <= I.hi

## 6.2 マクロによる宣言的記述

マクロは、コードをコードで変換する仕組みです。DSLの表現を、実行可能なJuliaコードへ展開できます。宣言的に仕様を書き、裏で検証・ログ・可視化を自動挿入する、といった使い方が可能です。

# ミニDSL:目的関数と制約を宣言する(デモ用に簡略化)  
macro model(ex)  
# 仕様をタグ付きの式に包む(本格用途では検証・展開を実装)  
return :(println("model spec: ", $(QuoteNode(ex))))  
end  


@model begin  
objective = x^2 + y^2  
constraint1 = x + y <= 1  
constraint2 = x >= 0  
end

この例は最小限ですが、「仕様を書く→検証する→解く」の一連の流れを言語機能で支援できることを示しています。複雑な最適化や微分方程式でも、マクロと型の組み合わせで、宣言的に問題構造を記述し、解法選択やログ、可視化を自動的に差し込めます。

## 6.3 多重ディスパッチで仕様を選択する

仕様に応じて解法を切り替えるには、多重ディスパッチが有効です。例えば、StiffODE型とNonStiffODE型に応じて、ステップ幅制御やメソッドを変える、といった設計が自然に書けます。

abstract type ODEModel end  
struct StiffODE <: ODEModel; end  
struct NonStiffODE <: ODEModel; end  


solve(m::StiffODE, u0, tspan) = "use implicit method"  
solve(m::NonStiffODE, u0, tspan) = "use explicit method"  


solve(StiffODE(), [1.0, 0.0], (0.0, 1.0))

仕様(型)を追加すれば、メソッドを足すだけで拡張できます。解法選択のロジックが分散せず、保守性を保ったまま性能も確保できます。

* * *

# 7. 再現可能性とプロジェクト設計:環境・設定・成果物の統制

計算科学・データ分析では、結果が再生成できることが前提です。Juliaでは、言語・ツールの両面で再現可能性を確保できます。

## 7.1 環境のピン留め

プロジェクトごとにProject.tomlとManifest.tomlで依存関係を固定します。これにより、時間が経っても、他の開発者が別の環境でも、同じバージョンのライブラリで同じコードを実行できます。再現性の破壊要因を環境レベルで封じます。

## 7.2 乱数・設定値の管理

乱数は、統計・モンテカルロ・ベイズ推論・シミュレーションなどで広く使われます。Random.seed!で種を固定し、乱数生成器を明示的に選ぶことで、並列時の独立性と再現性を両立します。設定値(刻み幅、許容誤差、停止条件、評価指標、可視化スタイル)は、コード内にハードコードするのではなく、設定ファイルや構造体で集中管理すると、変更の影響を可視化しやすくなります。

# 設定の集中管理例  
struct SolverConfig  
tol::Float64  
maxiter::Int  
step::Float64  
end  


cfg = SolverConfig(1e-8, 10_000, 0.01)

## 7.3 成果物の生成パイプライン

図・表・数値・ログ・レポートを、同じコードベースから自動生成するパイプラインを構築します。これにより、データ更新や仕様変更のたびに、人手による再加工を避け、品質の一貫性を保てます。「コードから一括生成する」のが鉄則です。

## 7.4 テスト・プロパティ検査・ベンチマーク

単体テストはもちろん、数値アルゴリズムでは性質(単調性、保存量、境界条件の遵守など)を検査するプロパティテストが有効です。性能については、入力規模やデータ分布を変えたベンチマークを用意し、回帰(性能劣化)を検知できる状態を維持します。テストは仕様の一部です。仕様を満たしていることをコードで主張し、その証拠を自動化して残します。

* * *

# 8. 汎用から高度計算までを一気通貫で扱う意義

同じ言語で、汎用プログラミングと高度な計算科学を扱う意義は実務に直結します。第一に、仕様の整合性が保たれます。分析の段階で書いた関数やモデルが、そのまま性能チューニングを受けて本番で動くため、意図と実装の距離が縮まります。第二に、学習コストが低減します。言語をまたぐ移植やバインディングの罠に悩む時間を削減し、アルゴリズム・検証・可視化に集中できます。第三に、チームの協調性が高まります。仕様を型とマクロでコード化し、再現可能なプロジェクトとして共有することで、レビュー・再利用・派生研究・運用移管が滑らかになります。

さらに、Juliaは「表現と性能」の間のトレードオフを緩和します。高水準の抽象で書いても、LLVMの低レベル最適化とJITの型特化が後段で効くため、実装者は「問題に集中」できます。多重ディスパッチは、アルゴリズムを型で整理する思考を促し、仕様の追加・変更を局所化します。これは、科学技術計算の長期運用における保守性の源泉です。

最後に、仕様記述言語としてのJuliaは、ドメイン固有の概念を「語彙」と「文法」に昇華し、チームの共通言語をコードとして定着させます。設計(仕様)→実行(計算)→評価(可視化・検証)→配布(成果物)のループを、一つの生態系で回すことができるため、研究・実務の両方で成果を持続的に積み上げられます。

* * *

# 9. 実践的な作法:小さく始めて、正しく速く

最後に、Juliaを使い始めるうえでの実践的な作法をまとめます。これらは、汎用プログラミングから計算科学まで通底する「正しく速い」ための基礎体力です。

  * 型を意識する: 引数・戻り値・コレクションの要素型を設計し、型安定性を保つことで、JITとLLVMの最適化が効きます。
  * 抽象に名前を与える: 抽象型・パラメトリック型で、ドメイン概念を明示化します。仕様の「語彙」を型として定義します。
  * メソッドを分ける: 多重ディスパッチで、型ごとに最適なメソッドを用意します。条件分岐ではなく、メソッドテーブルで整理します。
  * 配列演算を融合する: ブロードキャストで短く書いて速くし、ビューや疎行列を適切に使います。
  * 乱数と設定を固定する: Random.seed!と構造化された設定管理で、再現性を担保します。
  * 環境をピン留めする: Project.toml/Manifest.tomlを使い、依存関係を固定します。プロジェクトごとの分離を徹底します。
  * 成果物をコード化する: 図・表・レポートはコードから生成します。人手の加工を無くし、説明責任を果たします。
  * テストと検証を並走させる: 単体テストに加え、性質の検査とベンチマークで品質と性能を守ります。



これらの作法は、Juliaの設計思想と相性が良く、短期の生産性と長期の保守性を両立します。汎用プログラミングの快適さを保ちつつ、計算科学の厳密さに耐えられるコードベースを築くための基本指針として、ぜひ意識的に取り入れてください。

* * *

# 第2章環境構築と基本操作:インストール・REPL・Pkg・VS Code・Jupyter

# 1. 公式配布の導入と初期確認

Juliaの導入は、公式配布を利用するのが最も確実です。Windows/macOS/Linuxのいずれでも、配布されたバイナリをインストールまたは展開するだけで使い始められます。インストール後は、端末(ターミナル/PowerShell)やアプリランチャからjuliaを起動できます。最初に、Julia本体と標準ライブラリが正常に動作しているか、バージョンやビルド情報を確認します。

# バージョンとビルド情報の確認  
versioninfo()

versioninfo()は、Juliaのバージョン、OS、CPU、LLVMのバージョン、スレッド数などを表示します。数値計算や並列処理に絡む設定が反映されているかを、ここで目視確認しておくと後のトラブルを避けられます。

## 1.1 パス設定と起動オプション

julia実行ファイルへのパスが通っていない場合は、OSの環境変数PATHへ追加します。端末から以下のように起動して、基本的な起動オプションも覚えておくと便利です。

# バージョンのみ表示  
julia -v  


# ヘルプと主なオプション一覧  
julia \--help  


# スレッド数を自動設定して起動(v1.5以降)  
julia -t auto  


# 現在のディレクトリの環境(Project.toml)を有効化して起動  
julia \--project=.

スレッド数は-t(または\--threads)で指定するか、環境変数JULIA_NUM_THREADSでも設定できます。数値解析やデータ処理でCPUを活用する場面が多いので、versioninfo()に表示されるThreadsの値が期待通りか確認してください。

* * *

# 2\. REPLの基本操作とモード(?, ;, ]/Pkg)

JuliaのREPL(Read–Eval–Print Loop)は、探索・試作・デバッグを高速に回すための中心的なUIです。REPLは、通常のコード入力に加えて、3つの専用モードを持ちます。

  * ? ヘルプモード
  * ; シェルモード
  * ] Pkgモード



これらは1文字入力するだけで、各モードに切り替わります。モードを抜けるにはBackspaceを押します。

## 2.1 基本操作とショートカット

REPLでは履歴が保存され、上下矢印で過去の入力を辿れます。補完(Tab)も有効で、関数名や変数名を途中まで打ったらTabで候補を展開できます。重要なショートカットは次のとおりです。

  * Ctrl+C 実行の中断
  * Ctrl+D REPLの終了
  * Tab 補完
  * Shift+Enter(VS Codeのターミナルなど) 改行



## 2.2 ヘルプモード(?)

?を入力すると、ヘルプモードに切り替わります。関数や型のドキュメント文字列(docstring)をその場で確認できます。

?println

ヘルプはパッケージの関数にも適用でき、関数の使い方や引数の意味を把握するのに役立ちます。

## 2.3 シェルモード(;)

;を入力すると、シェルモードになります。ここではOSのコマンドをそのまま実行できます。ディレクトリの確認やgit操作など、開発作業をREPLから離れずに行えます。

;pwd  
;ls -la  
;git status

## 2.4 Pkgモード(])

]を入力すると、パッケージ管理のPkgモードに切り替わります。環境の作成・有効化・依存の追加/更新/削除・状況確認・事前コンパイルなどの操作を、対話的に実行できます。

] activate .  
] status  
] add DataFrames CSV  
] precompile

Pkgモードでは、プロジェクト環境を明示的にactivateするのが基本です。activate .はカレントディレクトリのProject.tomlを環境として有効化します。もし存在しない場合は、空の環境が作成されます。以降、その環境へ依存関係が記録されます。

* * *

# 3\. Pkgで作る再現可能なプロジェクト(Project.toml/Manifest.toml)

Juliaはプロジェクト単位の環境分離を標準機能として備えています。これにより、分析コードと依存関係を一緒に管理でき、他者や将来の自分が同じ環境を再構築して同じ結果を再現できます。中心となるファイルはProject.tomlとManifest.tomlです。

## 3.1 Project.tomlの役割

Project.tomlはプロジェクトのメタ情報と、直接依存するパッケージ(トップレベルの依存)を記述します。主な項目はname、uuid、version、[deps]、[compat]などです。[deps]には依存パッケージ名とそのUUIDが記録され、[compat]でバージョンの互換範囲を指定できます。

name = "MyProject"  
uuid = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"  
version = "0.1.0"  


[deps]  
DataFrames = "a93c6f00-e57b-5684-b7b6-d8193f3e46c0"  
CSV = "336ed68f-0bac-5ca0-87d4-7b16caf5d00b"  


[compat]  
julia = "1.10"  
DataFrames = "1"  
CSV = "0.10"

[compat]は再現性と保守性の要で、将来の更新で破壊的変更を取り込まないよう制御できます。

## 3.2 Manifest.tomlの役割

Manifest.tomlは、依存グラフを展開した「完全なスナップショット」です。直接依存だけでなく、その依存が依存するパッケージまで含め、具体的なバージョンと取得元(レジストリ/リポジトリ/アーティファクト)が記録されます。これがあると、他環境でinstantiateするだけで全く同じ依存関係が復元されます。

] instantiate

instantiateはManifest.tomlに従い、必要なパッケージをダウンロード・ビルドし、環境を完全に再構築します。Manifest.tomlは生成物に近く、通常は手動編集せず、Pkgに管理させます。

## 3.3 典型的なワークフロー

新しいプロジェクトの開始から、依存固定、共有までの例を示します。

# 新規ディレクトリを作る  
mkdir myproj && cd myproj  


# REPLを起動し、Pkgモードで環境を有効化  
julia \--project=.  
# ] activate .  
# ] add DataFrames CSV Plots  
# ] status  
# ] precompile

コードと環境ファイルをVCS(git)で共有すれば、他者はclone後にinstantiateするだけで同じ環境を再現できます。

git init  
git add Project.toml Manifest.toml src notebooks  
git commit -m "initial setup"

## 3.4 Pkgの主なコマンド

  * ] activate <path> 環境の有効化
  * ] add <pkg> 依存の追加
  * ] rm <pkg> 依存の削除
  * ] update 依存の更新
  * ] status 依存の一覧
  * ] precompile 事前コンパイル
  * ] resolve 互換性に基づく解決の再計算
  * ] pin <pkg> 特定バージョンへの固定
  * ] dev <path or url> 開発中パッケージを直接参照



pinは意図的にバージョンを固定したいときに有用です。計算結果の検証や長期プロジェクトで、ライブラリの更新に伴う挙動変化を避けられます。

## 3.5 コード側からのPkg操作

REPLのPkgモードに加えて、コードからPkg標準ライブラリを使って操作することもできます。自動セットアップスクリプトやCIで役立ちます。

using Pkg  
Pkg.activate(".")  
Pkg.instantiate()  
Pkg.precompile()

* * *

# 4\. VS Code拡張の導入と実践的な使い方

VS Codeは、Julia開発に最適化された拡張機能を提供します。拡張「Julia」を導入すると、REPL統合、コード補完、Lint、プロファイリング、プロット、テスト連携などが使えます。インストール後、拡張の設定でJulia実行ファイルへのパスを確認します。

## 4.1 実行ファイルの設定

VS Codeの設定(Settings)でJulia: Executable Pathにjuliaのパスを指定します。複数バージョンを併用する場合、プロジェクトごとに設定を分けたいときは、ワークスペース設定に記録します。

## 4.2 REPL統合とセル実行

Julia: Start REPLで統合REPLが起動します。ソース内の# %%でセルを区切ると、セル単位の実行が可能になり、試行錯誤が高速化します。

# %%  
using DataFrames, CSV  


df = CSV.read("data.csv", DataFrame)  


# %%  
describe(df)

選択範囲実行、行ごとの実行、エラー位置へのジャンプなど、探索時に役立つ操作が整備されています。

## 4.3 変数ビューとプロット

拡張は、現在のセッションの変数一覧を可視化するビューを提供します。DataFrameや配列を確認しながら、PlotsやMakieで生成した図をプレビューできます。計算結果と可視化を同環境で扱えるのは、試作のスピードと再現性を高めます。

## 4.4 Lint・フォーマット・テスト

静的解析(Lint)やコードフォーマッタとの連携、Pkg.test()の実行をIDEから行えます。数値アルゴリズムの開発では、テストを小刻みに回す習慣が品質を支えます。VS Codeのタスクやlaunch.jsonを活用すれば、起動オプション(\--project=.や-t auto)をIDE側で固定でき、環境差異を減らせます。

* * *

# 5\. Jupyterノートブック(IJulia)の連携方法

Jupyterは、対話的な分析、説明のためのノート作成、可視化の配布に適したツールです。JuliaからJupyterを使うにはIJuliaパッケージを導入します。

## 5.1 IJuliaの導入

プロジェクト環境をactivateした上で、IJuliaを追加します。

] activate .  
] add IJulia

## 5.2 ノートブックの起動

REPLから次のように起動できます。既存のJupyterが検出される場合はそれを使い、未インストールなら必要なコンポーネントのセットアップが促されます。

using IJulia  


# ノートブックを起動  
notebook()  


# JupyterLabを起動  
jupyterlab()

ブラウザが開き、NewからJuliaカーネルを選ぶと、Juliaのコードセルを作成・実行できます。

## 5.3 環境の一致を保つ

ノートブックからもプロジェクト環境に乗ることが重要です。セルの先頭で、明示的にPkg.activate(".")を行うと、ノートブックがプロジェクトのProject.toml/Manifest.tomlを参照するようになります。

using Pkg  
Pkg.activate(".")

これにより、VS CodeやREPLで使っているのと同じ依存・バージョンで実行でき、図・表・レポートの再現性が担保されます。

## 5.4 ノートブックの構造化

ノートブックは実験ログや説明を含められる一方、順序依存の副作用が溜まりやすい特徴があります。以下の作法を守ると、再現性が高まります。

  * セルの冒頭で乱数の種を固定する
  * 主要な設定値を1か所にまとめるセルを用意する
  * データの読み取り/前処理/可視化/結果出力をセクション化する
  * Pkg.status()やversioninfo()の出力をノート冒頭に記録する



using Random  
Random.seed!(20240101)  


versioninfo()

* * *

# 6. 再現性を強化する初期設定(startup.jl, Random.seed!, artifacts)

再現可能性は、環境・設定・データの3要素で成立します。Juliaには、初期化の仕組みやアーティファクト管理が備わっています。

## 6.1 startup.jlでの初期化

~/.julia/config/startup.jlは、REPLやスクリプト起動時に読み込まれる初期化ファイルです。よく使うパッケージの読み込みやプロンプト設定、Revise.jlの導入などを記述しておくと、生産性が上がります。

# ~/.julia/config/startup.jl の例  
try  
using Revise  
catch  
@warn "Revise not available; add it in your environment"  
end  


# 好みのプロンプトや表示設定などをここに記述

Reviseは、ソースコードの変更をREPLへ即時反映できるため、関数の修正を何度も再読み込みせずに試せます。

## 6.2 乱数と設定の集中管理

乱数の種(Random.seed!)は、毎回の実行開始時に明示的に設定します。設定値(許容誤差、最大反復回数、図のスタイルなど)は構造体や辞書で一元管理し、ノートやスクリプトの先頭でインスタンス化します。

using Random  
Random.seed!(1234)  


struct Config  
tol::Float64  
maxiter::Int  
style::Symbol  
end  


cfg = Config(1e-8, 10_000, :paper)

## 6.3 Artifacts.tomlによるデータの配布

外部データやバイナリ資源を再現性高く扱うには、Juliaのアーティファクト機構を使えます。Artifacts.tomlに、チェックサム付きでデータを宣言すると、Pkgが取得・キャッシュを管理します。これにより、プロジェクト配布時に「どのデータを使ったか」がコード化され、再実行時に同じ資源が自動入手されます。

# Artifacts.toml の概念例(実際はuuidやchecksumを指定)  
[mydata]  
git-tree-sha1 = "..."

アーティファクトの取得はコードから行えます。

using Pkg.Artifacts  


path = artifact"mydata"  
# path 以下のファイルを読み取りに使う

* * *

# 7. よくある躓きの回避とトラブルシューティング

環境構築と基本操作では、いくつかの典型的な躓きがあります。以下の観点をチェックすると、多くの問題を予防できます。

## 7.1 REPLとプロジェクトの不一致

REPLを素の状態で起動し、Project.tomlのあるディレクトリで作業していると思い込むと、実はグローバル環境で依存が解決されてしまうことがあります。必ず\--project=.で起動するか、REPLでPkg.activate(".")またはPkgモードで activate .を実行し、「現在のプロジェクト」が意図通りか確認してください。] statusで環境が指しているパスも表示されます。

## 7.2 互換性(compat)の不足

Project.tomlに[compat]がないと、更新時に大きくバージョンが上がって挙動が変わることがあります。最低限、juliaの互換範囲と主要依存の互換を記入します。] updateの前後で] statusの差分を確認し、変更が計算結果に影響しないか、テストやベンチで検証します。

## 7.3 ビルドやバイナリ依存の失敗

一部のパッケージは、OSネイティブのビルドやバイナリ資源を必要とします。] buildを試す、versioninfo()の出力でOS/CPU/LLVMを確認する、ネットワーク環境がアーティファクト取得を阻害していないか見る、といった基礎確認が有効です。CIや別マシンで再現が取れるかを確かめるのも、切り分けに役立ちます。

## 7.4 Jupyterカーネルの選択ミス

Jupyterを起動したものの、Pythonカーネルでノートを書いてしまい、Juliaのコードが動かない、という取り違えが起きがちです。NewやKernelメニューでJuliaカーネルが選ばれているか、ノートの右上に表示されるカーネル名を必ず確認します。ノート内ではPkg.status()やversioninfo()を出力して、環境の一致を記録しておくと安心です。

## 7.5 スレッド数の未設定

並列化を試みても速くならないとき、Threads.nthreads()を確認すると1のままである、ということがあります。起動時に-t autoまたは-t <数>で指定する、VS Codeのlaunch.jsonで-t autoを含める、シェルの環境変数JULIA_NUM_THREADSを設定する、といった対策を取ります。

Threads.nthreads()

## 7.6 乱数の未固定

モンテカルロや学習の再実験で、結果が毎回揺れて比較が困難になる場合、冒頭でRandom.seed!が設定されていない可能性があります。ノートやスクリプトの先頭で、意図した種を明示的に設定します。並列の場合は、スレッドごとの独立性と再現性を保つための生成器設計も考慮します。

* * *

# 8. 実務を意識した最初のセットアップの例

最後に、実務でそのまま使える最小の雛形を示します。ディレクトリ構成、環境ファイル、起動手順、Jupyter連携を一気通貫で整えます。

# ディレクトリを作成  
mkdir jlproj && cd jlproj  
mkdir src notebooks data reports  


# REPLをプロジェクト環境で起動  
julia \--project=.

# REPL内:Pkgモードで基本依存を導入  
] activate .  
] add DataFrames CSV Plots IJulia Random  


# 事前コンパイル  
] precompile

# src/main.jl の例  
using Random, DataFrames, CSV, Plots  


Random.seed!(20240101)  


df = DataFrames.DataFrame(a = 1:10, b = rand(10))  
CSV.write("reports/sample.csv", df)  


plot(df.a, df.b; xlabel="a", ylabel="b", title="sample")  
savefig("reports/plot.png")

# notebooks/analysis.ipynb を作る前に、Jupyterを起動  
using IJulia, Pkg  
Pkg.activate(".")  
notebook()

ノートの先頭セルで、環境と設定の確認を行います。

using Pkg, Random  
Pkg.activate(".")  
Random.seed!(20240101)  
versioninfo()  
Pkg.status()

この最小セットアップにより、コード・環境・成果物の3点が一致し、いつでも再生成可能な体制が整います。VS Codeでは、ワークスペース設定でJulia: Executable Pathを指定し、タスクやlaunch.jsonで\--project=.と-t autoを固定しておくと、IDEからの起動でも再現性が保たれます。

* * *

# 9. 補足:REPLでの軽快な探索を支えるテクニック

日々の作業をさらに快適にするための小技を挙げます。いずれも再現性を損なわず、探索速度を高めるものです。

  * ドット演算の即時検証: 配列演算やブロードキャストの癖をREPLで確認します。



x = rand(5); y = rand(5)  
@. sin(x) + y^2

  * @timeと@btimeの使い分け: ざっくり計測は@time、厳密なベンチはBenchmarkToolsの@btimeを使います。



using BenchmarkTools  
@btime sum(rand(10_000));

  * edit/includeの併用: ソースを編集(edit("src/main.jl"))してinclude("src/main.jl")でロード、Reviseを併用すれば即時反映できます。
  * ;シェルモードでの周辺作業: データの配置確認、gitの操作、lsで成果物の一覧をチェックするなど、REPLから離れずに完結できます。
  * ?ヘルプでの関数探索: 未知の関数はまず?でドキュメントを確認し、最小例をREPLで回してから本番コードへ移植します。



これらのテクニックを、プロジェクト環境の枠組みの中で運用することが重要です。常にactivate済みのプロジェクトで試し、依存関係や設定をファイルへ記録しながら進めることで、探索で得た知見をそのまま再現性のある成果に接続できます。

* * *

# 第3章文法入門:型・変数・制御構文・関数・スコープ(◎基本を最短で)

# 1. 基本型とリテラル

Juliaの文法は、数値・文字列・論理値などの基本型を自然なリテラルで扱えるように設計されています。各型の特徴を把握し、必要に応じて型変換・厳密比較・欠損の扱いを正しく選ぶことが、数値解析やデータ処理の堅牢性につながります。

## 1.1 数値型

Juliaは複数の数値表現を持ち、演算時に安全な型昇格(プロモーション)を行います。

  * 整数: Int(環境依存のビット幅)、Int8/Int16/Int32/Int64、符号なしはUInt*
  * 浮動小数点: Float16/Float32/Float64
  * 高精度: BigInt(任意精度整数)、BigFloat(任意精度浮動小数点)
  * 複素数: Complex{T}



# 整数と浮動小数点  
a = 42 # Int  
b = 3.14 # Float64  
c = 1//3 # Rational{Int}(有理数)  
z = 1 + 2im # Complex{Int}  


# 高精度  
big_i = BigInt(2)^100  
big_f = BigFloat("3.1415926535897932384626")  


# 型昇格(安全なプロモーション)  
r = 2 + 3.0 # Int + Float64 → Float64  
typeof(r) # → Float64

浮動小数点では、NaN(非数)やInf(無限大)が生じることがあります。これらは比較の挙動が通常と異なるため、局所的なガードや検査関数の活用が重要です。

x = 0.0/0.0 # NaN  
y = 1.0/0.0 # Inf  


isnan(x) # true  
isfinite(y) # false

## 1.2 文字・文字列

  * 文字: Charはシングルクォートで表し、Unicodeを扱えます。
  * 文字列: Stringはダブルクォートで表します。改行やタブなどはエスケープで指定します。



ch = 'α' # Char  
s = "hello, Julia" # String  
s2 = "line1\nline2" # 改行を含む  


# 文字列補間  
name = "Julia"  
msg = "Hello, $name!"

文字列はイミュータブルです。性能が重要な場面ではIOBufferで逐次書き出すか、Stringの事前割り当てを避ける設計を検討します。

## 1.3 論理値と比較

論理値はBool(true/false)です。比較は==(値の比較)と===(同一性、型とオブジェクトの同一)を使い分けます。浮動小数点のNaNは自分自身とも等しくない点に注意してください。

true && false # 短絡AND  
true || false # 短絡OR  


1 == 1.0 # true(値の比較)  
1 === 1.0 # false(型が異なる)  


NaN == NaN # false  
isequal(NaN,NaN) # true(NaN同士を等しいと見なす比較)

## 1.4 欠損と空

  * missing: データの欠損値。統計・データフレーム文脈で用います。
  * nothing: 値が存在しないことを表すユニット型。戻り値が無い/意図的に空を返す設計に使います。



m = missing  
n = nothing  


ismissing(m) # true  
isnothing(n) # true

## 1.5 範囲・集合的なリテラル

範囲は効率的なイテレータであり、配列を生成せずに走査できます。

r1 = 1:10 # 1から10まで  
r2 = 0:2:10 # ステップ2の範囲 0,2,4,6,8,10  


collect(r2) # 配列化が必要ならcollect

# 2. コレクション型:タプル・配列・辞書・集合

実務では、データの詰め合わせ方・更新の仕方で性能と保守性が大きく変わります。不変(immutable)と可変(mutable)の違い、ビューとコピーの違いを意識して使い分けます。

## 2.1 タプルとNamedTuple

  * Tuple: 異種混在を許す不変の順序付きコレクション。
  * NamedTuple: フィールドに名前を持つ不変タプル。軽量なレコードとして有用です。



t = (1, "a", 3.0)  
nt = (x=1, y=2.0, label="pt")  


t[2] # "a"  
nt[:y] # 2.0  
nt.y # 2.0(ドットアクセス)

タプルはイミュータブルであり、要素の差し替えはできません。その代わり、コンパイラ最適化が効きやすく、引数パッキングや複数戻り値で活躍します。

## 2.2 配列(Array)とベクトル/行列

Array{T,N}は可変のN次元配列です。1次元はVector{T}、2次元はMatrix{T}の別名を持ちます。

v = [1,2,3] # Vector{Int}  
M = [1 2 3; 4 5 6] # 2×3 Matrix{Int}  
A = Array{Float64}(undef, 2, 2) # 未初期化の配列  


# 要素アクセスと更新  
v[2] # 2  
v[2] = 20  


# 末尾操作  
push!(v, 99)  
pop!(v)  


# 範囲・スライス・ビュー  
using LinearAlgebra  
B = M[:, 2] # コピー  
@views C = M[:, 2] # ビュー(コピーを避ける)

配列生成の基本は内包表記とブロードキャストです。中間配列の生成を避け、演算を融合させることで高速化します。

x = collect(1:10)  
y = rand(10)  
z = [xi^2 + yi for (xi, yi) in zip(x, y)] # 内包表記  


@. y = sin(x) + cos(y) # ブロードキャストの融合

疎行列やview、@inbounds、@simdなどのヒントは、数値演算のホットスポットで効果的です。ただし境界チェックを外す場合は、正しさの検証を先に済ませておくのが原則です。

## 2.3 辞書(Dict)と集合(Set)

  * Dict{K,V}: キーと値のマップ。可変。
  * Set{T}: 要素の重複のない集合。可変。



d = Dict("a"=>1, "b"=>2)  
d["a"] = 10  
haskey(d, "c") # false  
get(d, "c", 0) # 無ければデフォルト0  


s = Set([1,2,2,3]) # {1,2,3}  
push!(s, 5)  
in(2, s) # true

辞書の反復はペアで受け取れます。計算の正確性が重要な場面では、キー型や値型を固定して型安定性を保つことが性能に直結します。

for (k, v) in d  
println("key=$k value=$v")  
end

## 2.4 コピーとビュー、浅い/深いコピー

  * copy: 浅いコピー。一次元要素は共有されることがある。
  * deepcopy: 深いコピー。ネストした構造を含めて独立に複製。



A = [[1,2],[3,4]]  
B = copy(A)  
C = deepcopy(A)  


A[1][1] = 99  
B[1][1] # 99(浅いコピーなので共有)  
C[1][1] # 1  (深いコピーなので独立)

# 3. 変数・型注釈・型変換

変数への代入・スコープ・型注釈は、性能と可読性に関わります。特にグローバル変数は型不安定の原因になりやすく、関数内でのローカルに落とし込む設計が推奨されます。

## 3.1 代入と定数

x = 10  
x += 5  


const PI2 = 2 * π # 定数(再代入できない)

constはグローバルに有効です。定数はコンパイラ最適化の対象になりやすく、再代入を防ぎます。

## 3.2 型注釈・型アサーション

  * 変数の型注釈: 読解性向上・意図の明示
  * 型アサーション: 実行時に型を確認して変換/エラー



y::Int = 3 # 変数注釈(代入値がIntでなければエラー)  
z = 3.0::Float64 # アサーション(右辺の型確認)  


# 明示的型変換  
v = Int(3.0) # 3  
w = Float64(3) # 3.0

## 3.3 型安定性の意識

戻り値やコレクションの要素型を一定に保ちます。条件分岐で異なる型を返す設計は避け、外側で型を統一するか、抽象型を設計して多重ディスパッチで整理します。

# 悪い例(型不安定)  
f(x) = x > 0 ? x : "neg"  


# 良い例(型安定)  
g(x) = x > 0 ? x : -x

# 4. 制御構文:条件分岐・反復・内包・短絡

制御構文は可読性と性能に直結します。Juliaでは、if/elseif/elseとfor/whileを基本に、内包表記・短絡評価・三項演算子を組み合わせて簡潔に書けます。

## 4.1 条件分岐

x = 7  
if x % 2 == 0  
println("even")  
elseif x % 3 == 0  
println("multiple of 3")  
else  
println("other")  
end  


# 三項演算子  
msg = (x > 0) ? "positive" : "non-positive"

論理演算は短絡評価です。副作用や高コスト計算を含む条件では、短絡による早期終了を活かせます。

cond = expensive() && cheap() # expensive()がfalseならcheap()は呼ばれない

## 4.2 反復(for/while)とbreak/continue

# for での走査  
sumx = 0  
for i in 1:100  
sumx += i  
end  


# while  
i = 0  
while i < 5  
println(i)  
i += 1  
end  


# break/continue  
for i in 1:10  
if i == 5  
continue  
elseif i == 8  
break  
end  
println(i)  
end

反復処理では、範囲やイテレータを直接回す設計が配列化を避け、メモリ効率が良くなります。

## 4.3 内包表記・生成器

配列や辞書、集合は内包表記で生成できます。可読性と速度の両面で優れます。

A = [i^2 for i in 1:10 if i%2==0] # 配列内包  
D = Dict(i => i^2 for i in 1:5) # 辞書内包  
S = Set(i^2 for i in 1:5) # 集合生成  


# 生成器(遅延評価)  
using Statistics  
μ = mean(i^2 for i in 1:1_000_000) # 配列を作らずに平均

## 4.4 ブロードキャストと融合

要素ごとの演算はドットで書き、融合により中間配列を省いて高速化します。

x = rand(10_000)  
y = rand(10_000)  
@. x = sin(x) + 2*y

必要に応じて@viewsや@inboundsを併用しますが、正しさの確認が先決です。

@views @inbounds for i in 2:length(x)-1  
x[i] = (x[i-1] + x[i] + x[i+1]) / 3  
end

# 5. 関数定義:引数、可変長、キーワード、無名関数、doブロック

Juliaの関数は多重ディスパッチを前提に設計されています。引数の型や数に応じて最適なメソッドを選べるため、柔軟かつ高速です。

## 5.1 基本の関数定義と戻り値

# 単純な関数  
function add(a::Number, b::Number)  
return a + b  
end  


# 戻り値省略(最後の式が返る)  
square(x) = x^2  


# 複数戻り値(タプル)  
function stats(xs::AbstractVector{<:Real})  
n = length(xs)  
μ = sum(xs) / n  
σ = sqrt(sum((x - μ)^2 for x in xs) / (n - 1))  
return μ, σ  
end  


mean, std = stats(rand(100))

## 5.2 可変長引数(variadic)とスプラット

sum_all(xs...) = sum(xs) # 可変長  
sum_all(1,2,3,4)  


# スプラットでタプルや配列を展開  
args = (1,2,3)  
sum_all(args...)

## 5.3 キーワード引数とデフォルト値

キーワードは可読性・拡張性を高め、順序依存を解消します。

function smooth(xs; window=5, method=:mean)  
# window, method はキーワード引数  
# 実装は仮  
return xs  
end  


smooth(rand(10); window=3, method=:median)

位置引数+キーワード引数の併用で、インターフェースを明確にできます。

## 5.4 無名関数とdoブロック記法

匿名関数は短いコールバックに便利です。イテレーションや高階関数でよく使われます。

f = x -> x^3  
map(x -> x^2, 1:5)  


# doブロック(第一引数が関数のコールバックになる)  
open("test.txt", "w") do io  
write(io, "hello")  
end

## 5.5 関数内のスコープと性能の注意

グローバル変数に依存する関数は型不安定になりやすく、性能が低下します。必要な値は引数で渡すか、constで型を固定します。

# 悪い例(グローバルに依存)  
factor = 2  
bad(x) = x * factor # REPLでは型不安定になりがち  


# 良い例(引数化)  
good(x; factor=2) = x * factor

# 6. スコープ規則:global/local/let、ソフトスコープの挙動

スコープは「どの識別子がどこから見えるか」の規則です。Juliaのスコープは、関数・let・内包・try/catch等で新しいローカルスコープを作ります。if/for/whileは、関数内では新しいスコープを作りません(同じ関数のローカル)。ただし、グローバルコンテキスト(REPLやトップレベル)ではソフトスコープが働き、ループ内の代入はglobal宣言が必要になる場合があります。

## 6.1 関数内のスコープ

function demo()  
x = 0 # 関数のローカル  
for i in 1:3  
x += i # 同じローカルxを更新  
end  
return x  
end  


demo() # 6

## 6.2 トップレベルのソフトスコープ(REPLなど)

REPLやスクリプトのトップで、for内に新しい変数へ代入する場合はglobalが必要になることがあります。

# REPLトップレベルの例  
x = 0  
for i in 1:3  
global x  
x += i  
end  
x # 6

## 6.3 letで閉じたローカルを作る

letは明示的に新しいローカルスコープを作り、初期値を束縛します。クロージャや一時的な隠蔽に有用です。

let x = 100  
println(x) # 100  
end  
# ここではxは見えない

## 6.4 localとglobalの明示

スコープの境界で変数の所属を明示したい場合は、local/global宣言を使います。読みやすさと正確さが向上します。

function counter()  
local cnt = 0  
for _ in 1:10  
cnt += 1  
end  
return cnt  
end

# 7. 例外処理:try/catch/finally、throw/rethrow、@assert

例外処理は、失敗を検知し、復旧やログ出力、リソース解放を正しく行うための仕組みです。数値計算では、領域外・収束失敗・不正な入力などに対して明確な例外を投げ、呼び出し側でハンドリングする設計が有効です。

## 7.1 基本構文

try  
x = parse(Int, "42")  
y = parse(Int, "not a number")  
catch e  
@warn "parse failed" error=e  
finally  
println("cleanup if needed")  
end

finallyは例外の有無に関わらず実行され、ファイルクローズやロック解放などの後処理に使えます。

## 7.2 例外の送出と再送出

function sqrt_strict(x::Real)  
x < 0 && throw(DomainError(x, "x must be nonnegative"))  
return sqrt(x)  
end  


try  
sqrt_strict(-1)  
catch e  
# ログを出した後、そのまま外へ伝播  
@error "domain error" error=e  
rethrow()  
end

標準の例外型(ArgumentError, DomainError, BoundsErrorなど)を適切に使うと、呼び出し側のハンドリングが簡潔になります。

## 7.3 アサーションと事前条件

@assertは開発時の不変条件の確認に有用です。失敗時はAssertionErrorが発生します。

function normalize(v::AbstractVector{<:Real})  
s = sum(abs.(v))  
@assert s != 0 "zero vector cannot be normalized"  
return v ./ s  
end

本番向けには、アサーションと例外を使い分けます。ユーザー入力の検証は例外で扱い、内部不変条件の破壊検知はアサーションで素早く気づけるようにします。

# 8. 実用パターン:入力検証、API設計、ドキュメンテーション、イディオム

文法の運用は設計次第で可読性・性能・再現性が大きく変わります。ここでは、基礎文法を活かす実務的イディオムをまとめます。

## 8.1 入力検証と型ガード

引数に対して具体的な型制約を与え、異常系は早期に例外とします。抽象型の受け口と具体型の最適化を併用するのがJuliaらしい設計です。

abstract type Sampleable end  
struct Normal <: Sampleable  
μ::Float64  
σ::Float64  
end  


function sample(n::Int, d::Normal)  
@assert n > 0 "n must be positive"  
return d.μ .+ d.σ .* randn(n)  
end

## 8.2 APIの階層化(位置+キーワード)

位置引数で最小限の仕様を受け、キーワード引数で拡張します。将来の拡張に強いAPIになります。

function solve(f; tol=1e-8, maxiter=10_000, method=:bisect, a=0.0, b=1.0)  
if method == :bisect  
return bisect(f, a, b; tol=tol, maxiter=maxiter)  
else  
throw(ArgumentError("unknown method"))  
end  
end

## 8.3 ドキュメント文字列(docstring)

関数・型の直前に文字列リテラルを置くと、?ヘルプで参照できるドキュメントになります。例と注意点を含めると、チームでの再利用性が向上します。

"""  
bisect(f, a, b; tol=1e-8, maxiter=10_000)  


二分法で`f`の根を探索します。`a`と`b`は符号が逆である必要があります。  
"""  
function bisect(f, a, b; tol=1e-8, maxiter=10_000)  
fa, fb = f(a), f(b)  
fa*fb < 0 || throw(ArgumentError("bracket must have opposite signs"))  
for _ in 1:maxiter  
c = (a + b)/2  
fc = f(c)  
if abs(fc) < tol  
return c  
elseif fa*fc < 0  
b, fb = c, fc  
else  
a, fa = c, fc  
end  
end  
return (a + b)/2  
end

## 8.4 破壊的更新と非破壊的操作の区別

関数名の末尾に!を付ける慣習は、引数を破壊的に更新することを示します。数値解析では、メモリの再利用とAPIの明示性を両立できます。

function normalize!(v::AbstractVector{<:Real})  
s = sum(abs.(v))  
s == 0 && throw(DomainError(v, "zero vector"))  
@. v = v / s  
return v  
end

## 8.5 デストラクチャリングとパターン

タプルやNamedTupleからの多値受け取りは、安全で可読性の高いデータアクセス方法です。

pt = (x=1.0, y=2.0, label="A")  
x, y, label = pt.x, pt.y, pt.label

## 8.6 例:基礎文法を組み合わせた小規模タスク

以下は、基礎文法の総合例です。入力検証、キーワード引数、例外処理、ブロードキャスト、内包表記、複数戻り値をひとつにまとめています。

"""  
moving_stats(xs; window=5)  


`xs`の移動平均と移動標準偏差を返します。`window`は奇数で正の値。  
"""  
function moving_stats(xs::AbstractVector{<:Real}; window::Int=5)  
window > 0 && isodd(window) || throw(ArgumentError("window must be positive odd"))  
n = length(xs)  
half = (window - 1) ÷ 2  
μs = Vector{Float64}(undef, n)  
σs = Vector{Float64}(undef, n)  
@views for i in 1:n  
lo = max(1, i - half)  
hi = min(n, i + half)  
seg = xs[lo:hi]  
μ = sum(seg) / length(seg)  
σ = sqrt(sum((x - μ)^2 for x in seg) / max(1, length(seg)-1))  
μs[i] = μ  
σs[i] = σ  
end  
return μs, σs  
end  


μ, σ = moving_stats(rand(100); window=7)

# 9. 例外ケースと境界の扱い:欠損、境界チェック、比較の落とし穴

数値・データの現場では、境界や欠損、比較の罠で誤差が増幅することがあります。文法レベルで正しい選択を習慣化します。

## 9.1 missingの伝播とskipmissing

missingは演算に伝播します。集計時はskipmissingで取り除くか、coalesceで代替値に置き換えます。

xs = [1,2,missing,4]  
sum(skipmissing(xs)) # 7  
ys = coalesce.(xs, 0) # [1,2,0,4]

## 9.2 境界チェックと@inbounds

境界チェックは安全性に不可欠ですが、ホットループではオーバーヘッドになります。正しさが確認できた箇所に限り@inboundsで外します。

function diff!(y::Vector{Float64}, x::Vector{Float64})  
@assert length(y) == length(x)  
@inbounds for i in 2:length(x)  
y[i] = x[i] - x[i-1]  
end  
y[1] = NaN  
return y  
end

## 9.3 浮動小数点の比較

浮動小数点の等値比較は、許容誤差を考慮したisapproxの使用が安全です。

isapprox(0.1 + 0.2, 0.3; atol=1e-12, rtol=1e-8)

# 10. 基礎文法の運用原則(記述のみ)

  * 型は明示し、戻り値・コレクションの要素型を安定させます。
  * 可変と不変を意識し、ビューとコピーを使い分けます。
  * 条件分岐・内包・ブロードキャストで簡潔に書き、計測と検証を欠かしません。
  * 引数設計は位置+キーワードで拡張性を確保します。破壊的操作は!で明示します。
  * スコープはglobal/local/letを正しく用い、REPLのソフトスコープの挙動を理解します。
  * 例外は標準型で投げ、try/catch/finallyと@assertで失敗を制御します。
  * 欠損・境界・浮動小数点の罠を避けるため、skipmissing・@inbounds・isapprox等のツールを活用します。



これらの原則は、Juliaの文法を最短で戦力化するための基本姿勢です。文法の機能を単独で覚えるだけでなく、組み合わせて「正しく速い」コードを書く練習を重ねてください。

* * *

# 第4章型システムと多重ディスパッチ:抽象型・構造体・パラメトリック型

# 1. 多重ディスパッチの要点

Juliaの核となる特徴は多重ディスパッチです。関数名は1つでも、引数の型の組み合わせごとに最適なメソッドが選ばれます。計算科学では、実数・複素数・高精度・疎行列・ユーザー定義数値型など多様な表現に対し、同じ関数名で性能と正確さの両立を図れます。

# 基本例:引数の型ごとにメソッドを用意  
f(x::Int) = x + 1  
f(x::Float64) = x + 1.0  
f(x::Complex) = real(x) + 1 + im*imag(x)  


f(10) # 11  
f(10.0) # 11.0  
f(1 + 2im) # 2 + 2im

複数引数でも型組み合わせに応じたメソッドを選べます。数値プロモーションや混合精度を安全に扱うために、promote_ruleやconvertと連携する設計が有効です。

# 型組み合わせで最適化:行列とベクトルの例(概念)  
mul(A::AbstractMatrix{Float64}, x::AbstractVector{Float64}) = A * x  
mul(A::AbstractMatrix{BigFloat}, x::AbstractVector{BigFloat}) = A * x

メソッド選択の確認には@whichが便利です。

@which f(10.0) # 呼ばれるメソッドの特定

多重ディスパッチは「条件分岐を型システムへ押し出す」設計手法です。アルゴリズムの分岐をifで散在させるのではなく、型ごとにメソッドテーブルへ整理することで、保守性と拡張性が向上します。

* * *

# 2. 抽象型の設計:拡張点を定義して柔軟性を確保する

抽象型はインスタンスを持たない型で、インターフェースの境界を定義します。計算科学では「何を受け入れるか」を抽象型で宣言し、具体的な表現は具象型で差し替えられるように設計します。

# 抽象型の定義  
abstract type Distribution end  


# 具体型は後で追加  
struct Normal <: Distribution  
μ::Float64  
σ::Float64  
end  


struct Uniform <: Distribution  
a::Float64  
b::Float64  
end

抽象型を受け口にした関数は、拡張に強くなります。最小限の共通操作をインターフェースとして定義し、具体型がそれを満たすようにします。

# インターフェースとなる関数群(例)  
mean(d::Distribution) = error("not implemented")  
var(d::Distribution) = error("not implemented")  


mean(d::Normal) = d.μ  
var(d::Normal) = d.σ^2  
mean(d::Uniform) = (d.a + d.b) / 2  
var(d::Uniform) = (d.b - d.a)^2 / 12

抽象型は階層化できます。過度な入れ子は避けつつ、「型で語彙を作る」イメージでドメイン概念を整理すると、後の拡張で破綻しにくくなります。

abstract type ODEModel end  
abstract type StiffnessTag end  
struct Stiff <: StiffnessTag end  
struct NonStiff <: StiffnessTag end

* * *

# 3. 構造体でのデータ表現:不変と可変、具体的なフィールド型

構造体(struct)はデータの入れ物です。Juliaには不変structと可変mutable structがあり、性能と安全性の観点から、基本は不変を選び、必要な場面でのみ可変を使います。

# 不変:基本はこれを優先  
struct State  
x::Float64  
y::Float64  
end  


# 可変:シミュレーションで場を更新する等、意図がある場合  
mutable struct Accumulator  
sum::Float64  
n::Int  
end

フィールド型は具体化するのが原則です。Anyや未注釈のフィールドは型不安定を招きます。

# 望ましくない例:フィールドがAny  
mutable struct BadBox  
value  
end  


# 望ましい例:フィールド型を具体化  
mutable struct GoodBox{T}  
value::T  
end

構造体はコンストラクタを定義して整合性を保証できます。

struct Interval{T<:Real}  
lo::T  
hi::T  
function Interval(lo::T, hi::T) where {T<:Real}  
lo <= hi || throw(ArgumentError("lo must be <= hi"))  
new{T}(lo, hi)  
end  
end

パフォーマンスを意識する場合、isbits(固定サイズ)の構造体はメモリ局所性が良く、配列に詰めても効率的です。可変構造体や参照型フィールドが多い設計は、メモリアクセスの分散を招くため、データレイアウトの選択に注意します。

* * *

# 4. パラメトリック型:汎用性と型安全を同時に得る

パラメトリック型は、型変数T等でフィールド型やふるまいを一般化します。汎用性を高めつつ、具体型としてインスタンス化されるため、型安定性と性能を確保できます。

# Tで要素型をパラメータ化  
struct Vec2{T<:Real}  
x::T  
y::T  
end  


norm(v::Vec2{T}) where {T<:Real} = sqrt(v.x^2 + v.y^2)

where句で制約を与え、受け入れる型を明確化します。複数パラメータや依存関係を持たせれば、ドメイン制約を型で表現できます。

struct Sample{T<:Real, RNG}  
value::T  
rng::RNG  
end  


function step!(s::Sample{T,RNG}) where {T<:Real, RNG}  
# rngの型に応じた処理を記述可能  
return s  
end

パラメトリック型は**不変(invariant)**です。Vector{Int}はVector{Number}のサブタイプではありません。この性質により、要素型の誤用を防げます。抽象型を使う場合は、AbstractVector{<:Real}のようにパラメトリック抽象型で受けるのが一般的です。

# 受け口としての抽象型  
f(xs::AbstractVector{<:Real}) = sum(xs)

パラメトリック関数も同様に、引数の型に束縛された高速なメソッドが生成されます。

g(x::T, y::T) where {T<:Real} = x + y

* * *

# 5. メソッドテーブルの読み解き:methods・@which・型の包含関係

メソッドがどのように解決されるかを理解するには、メソッドテーブルを確認します。

methods(f) # 関数fの全メソッド一覧  
@which f(1.0) # 呼び出しで選ばれる具体メソッド

シグネチャに現れる型の包含関係は、具体型がより特殊、抽象型がより一般の関係です。複数メソッドが候補になると、最も特殊なものが選ばれます。曖昧(ambiguity)がある場合は警告が出ることがあり、明示的に片方へ寄せるメソッドを追加して解消します。

h(x::Number, y::Int) = x + y  
h(x::Int, y::Number) = x + y  
# h(1, 2) は曖昧になり得る → 専用メソッドで解消  
h(x::Int, y::Int) = x + y

異種混合の演算を安全に扱うには、promoteを活用します。promote_ruleを定義すると、独自型と他型の混合演算でも自然な共通型へ揃えられます。

struct MyFloat{T<:AbstractFloat}  
v::T  
end  


Base.convert(::Type{MyFloat{T}}, x::T) where {T<:AbstractFloat} = MyFloat{T}(x)  
Base.promote_rule(::Type{MyFloat{T}}, ::Type{T}) where {T<:AbstractFloat} = MyFloat{T}  


+(a::MyFloat{T}, b::MyFloat{T}) where {T<:AbstractFloat} = MyFloat{T}(a.v + b.v)  


# 異種を合わせる  
a = MyFloat(1.0)  
a + 2.0 |> x -> x.v # 3.0(promoteでMyFloat{Float64}へ揃う)

メソッド探索の視覚化や型の包含関係の把握は、設計の正しさの確認に不可欠です。特に数値ライブラリでは、境界条件・精度・表現の違いを型で分離し、曖昧なメソッド選択が発生しないようにします。

* * *

# 6. 計算科学で効く設計パターン

多重ディスパッチと型システムを活かした設計パターンをいくつか挙げます。いずれも「仕様を型で表し、解法をメソッドで差し替える」思想です。

## 6.1 タグディスパッチ:性質で切り替える

問題の性質(剛性、境界条件の種類、精度方針など)をタグ型で表し、解法を切り替えます。

abstract type BoundaryCondition end  
struct Dirichlet <: BoundaryCondition end  
struct Neumann <: BoundaryCondition end  


apply!(u, ::Dirichlet, value) = (u .= value; u)  
apply!(u, ::Neumann, flux) = (u[2:end-1] .+= flux; u)

## 6.2 トレイトパターン:能力で分岐する

トレイトは「型が持つ能力」を外部関数で返し、分岐に使う手法です。

# トレイト  
has_fastpath(::Type{T}) where {T} = false  
has_fastpath(::Type{Float64}) = true  


function kernel(x)  
if has_fastpath(typeof(x))  
# 高速経路  
return x * 2  
else  
# 汎用経路  
return 2 * float(x)  
end  
end

## 6.3 関数バリア:動的を局所化してJIT最適化を助ける

入出力は抽象型やAnyでも、内部の計算を具体型へpromoteし、関数境界で型を安定化させます。JITの最適化が効きやすくなります。

function outer(x, y)  
# ここでは抽象的  
x2, y2 = promote(x, y)  
return inner(x2, y2) # innerは具体型で高速  
end  


inner(x::Float64, y::Float64) = x + y

## 6.4 インターフェース準拠:AbstractArray等の最小要件を満たす

独自配列型を設計するときは、size, getindex, setindex!, IndexStyleなど最小インターフェースを満たすことで、既存の生態系と互換性が取れます。

struct DiagonalView{T<:Real, M<:AbstractMatrix{T}} <: AbstractVector{T}  
A::M  
end  
Base.size(d::DiagonalView) = (min(size(d.A,1), size(d.A,2)),)  
Base.getindex(d::DiagonalView, i::Int) = d.A[i,i]  
Base.IndexStyle(::Type{DiagonalView}) = Base.IndexLinear()

* * *

# 7. アンチパターンと注意点:型安定・型海賊・ユニオンの罠

## 7.1 型安定性の破壊

条件分岐で戻り値の型が揺れると、最適化が効きません。戻り値は揃えます。

bad(x) = x > 0 ? x : "neg" # 型不安定  
good(x) = x > 0 ? x : -x # 型安定

## 7.2 型海賊(type piracy)

他人の型に対して、他人の関数のメソッドをグローバルに追加する行為は、意図せぬ振る舞いの変更を招きます。自分の名前空間内の関数、または自分の型に対する拡張に留めます。

# 悪い例(想定外の副作用を生む)  
# Base.+(x::SomeExternalType, y::Int) = ...

## 7.3 大きなUnion型

Union{T1,T2,...}が巨大になるとコンパイル負荷が増えます。抽象型やパラメトリック型で受ける、関数バリアで具体化する、などで負荷を抑えます。

* * *

# 8. 具体例:数値表現の拡張、誤差・単位・精度の扱い

## 8.1 単位付き量の型

物理量の安全性を高めるには、単位を型へ織り込みます。誤用の防止と可視化時の一貫性に役立ちます。

struct Quantity{T<:Real, U}  
v::T  
end  


Base.:+(a::Quantity{T,U}, b::Quantity{T,U}) where {T<:Real,U} = Quantity{T,U}(a.v + b.v)  
# 異単位の加算は未定義 → 型が守ってくれる

## 8.2 誤差付き値の型

計測誤差や数値誤差を伝播させる設計は、解析の信頼性を高めます。

struct WithErr{T<:Real}  
v::T  
e::T  
end  


Base.:+(a::WithErr{T}, b::WithErr{T}) where {T<:Real} =  
WithErr{T}(a.v + b.v, sqrt(a.e^2 + b.e^2))

## 8.3 精度の切り替え

同じアルゴリズムでFloat64とBigFloatを使い分けるには、パラメトリック型の受け口が有効です。

function poly_eval(coeffs::AbstractVector{T}, x::T) where {T<:AbstractFloat}  
# Horner 法  
acc = zero(T)  
@inbounds for c in reverse(coeffs)  
acc = muladd(acc, x, c)  
end  
return acc  
end  


poly_eval(Float64[1,2,3], 0.1)  
poly_eval(BigFloat[BigFloat(1),BigFloat(2),BigFloat(3)], BigFloat("0.1"))

* * *

# 9. データ構造とアルゴリズムの協調:疎行列、特殊行列、演算のオーバーロード

特殊な構造を持つデータは、型で特殊性を表すことで計算を最適化できます。

# 対角行列ビュー(例)  
struct Diag{T<:Real, M<:AbstractMatrix{T}} <: AbstractMatrix{T}  
A::M  
end  
Base.size(D::Diag) = size(D.A)  
Base.getindex(D::Diag, i::Int, j::Int) = (i == j) ? D.A[i,i] : zero(T) where {T}

この設計により、演算子の実装側でDiagを検出して、より軽い計算経路へ切り替えることができます。多重ディスパッチは、構造に応じたアルゴリズム選択を自然に表現します。

# 例:ベクトルとの積を特殊化  
*(D::Diag{T}, x::AbstractVector{T}) where {T<:Real} = D.A[diagind(D.A)] .* x

* * *

# 10. メソッド設計の実務:キーワード、破壊的!、ドキュメント

## 10.1 位置引数+キーワードで拡張性

最小の仕様を位置引数で受け、追加設定はキーワードで受けます。

function solve_ode(f, u0, tspan; method=:auto, reltol=1e-6, abstol=1e-12)  
# 実装は省略(概念)  
return u0  
end

## 10.2 破壊的更新の明示

!付きの関数名は「引数を更新する」契約を明示します。メモリ再利用とAPIの透明性に役立ちます。

function axpy!(y::AbstractVector{T}, a::T, x::AbstractVector{T}) where {T<:Real}  
@inbounds @. y = a * x + y  
return y  
end

## 10.3 ドキュメント文字列

?ヘルプで参照できるdocstringに、契約・前提・例・副作用を記述します。

"""  
axpy!(y, a, x)  


`y ← a*x + y` を破壊的に実行します。`y`と`x`は同じ長さである必要があります。  
"""

* * *

# 11. 実践例:モデル仕様を型で表し、解法をメソッドで差し替える

最後に、型システムと多重ディスパッチを総合した小例を示します。微分方程式の仕様(剛性、境界条件、精度方針)を型で表し、解法選択・更新則をメソッドで切り替えます。

# 仕様の語彙  
abstract type ODEModel end  
struct RHS{F} <: ODEModel  
f::F  
end  


abstract type Stepper end  
struct ExplicitEuler <: Stepper end  
struct ImplicitEuler <: Stepper end  


abstract type PrecTag end  
struct FP64 <: PrecTag end  
struct BigP <: PrecTag end  


# 精度タグに応じて型を決める(トレイト風)  
eltype_for(::Type{FP64}) = Float64  
eltype_for(::Type{BigP}) = BigFloat  


# ステッパごとの更新則  
function step!(u::AbstractVector{T}, m::RHS{F}, h::T, ::ExplicitEuler) where {T<:AbstractFloat,F}  
@inbounds @. u = u + h * m.f(u)  
return u  
end  


function step!(u::AbstractVector{T}, m::RHS{F}, h::T, ::ImplicitEuler) where {T<:AbstractFloat,F}  
# 実務では非線形解法を呼ぶ。ここでは簡略化した固定点反復のスケルトン  
u_new = copy(u)  
for _ in 1:10  
@inbounds @. u_new = u + h * m.f(u_new)  
end  
@. u = u_new  
return u  
end  


# ドライバ:関数バリアで精度と型を確定  
function solve(m::RHS{F}, u0, tspan; stepper::Stepper=ExplicitEuler(), prec::Type{P}=FP64, h=0.01) where {F,P<:PrecTag}  
T = eltype_for(prec)  
u = T.(u0) # 型を確定  
t = T(tspan[1])  
t_end = T(tspan[2])  
while t < t_end  
step!(u, m, T(h), stepper)  
t += T(h)  
end  
return u  
end  


# 使用例  
f(u) = [-u[1]]  
m = RHS(f)  
u_final = solve(m, [1.0], (0.0, 1.0); stepper=ExplicitEuler(), prec=FP64, h=0.1)  
u_final_big = solve(m, [BigFloat(1)], (BigFloat(0), BigFloat(1)); stepper=ImplicitEuler(), prec=BigP, h=BigFloat("0.1"))

この例では、仕様(右辺関数、ステッパ、精度方針)を型で表し、更新則を多重ディスパッチで差し替えています。solveは関数バリアで型を確定し、JIT最適化を効かせつつ、引数の変更で計算戦略を安全に切り替えています。

* * *

# 12. 実務的原則(記述のみ)

  * 抽象型で受け口を定義し、具象型で最適化します。インターフェースは最小限に。
  * 構造体のフィールド型は具体化し、基本は不変structを選びます。可変は意図がある場合のみ。
  * パラメトリック型で汎用性を持たせ、whereで制約を明示します。抽象受け口はAbstractVector{<:Real}等を用います。
  * 多重ディスパッチで条件分岐を型へ押し出し、メソッドテーブルで整理します。曖昧は専用メソッドで解消します。
  * promote_rule/convertで混合型演算を安全に統制します。関数バリアで型を安定化します。
  * タグ・トレイト・インターフェース準拠で、構造や能力に応じた解法選択を表現します。
  * 型海賊を避け、名前空間の契約を守ります。戻り値の型安定を徹底します。
  * データ構造の特殊性(対角、疎、バンド等)は型で表し、演算を特殊化して高速化します。



これらの原則は、Juliaの型システムと多重ディスパッチを計算科学の現場で活かすための基本姿勢です。仕様を型で記述し、解法をメソッドで差し替える設計により、正確さ・速度・保守性を同時に満たすコードベースを構築できます。

* * *

# 第5章配列と線形代数:ブロードキャスト・ビュー・BLAS/LAPACK

# 1\. N次元配列の基礎:形状・軸・アクセスと再配置

Juliaの配列はArray{T,N}で表され、要素型Tと次元Nを持ちます。一次元はVector{T}、二次元はMatrix{T}の別名が用意されています。形状の把握と安全な反復は、性能と正確さの両面で重要です。

# 生成と基本操作  
v = [1, 2, 3] # Vector{Int}  
M = [1 2 3; 4 5 6] # 2×3 Matrix{Int}(行は;、列は空白で区切る)  
A = Array{Float64}(undef, 2, 2, 3) # 2×2×3 Array{Float64} 未初期化  


size(M) # (2, 3)  
length(M) # 6  
axes(M) # (Base.OneTo(2), Base.OneTo(3))  
eltype(M) # Int  


M[1, 3] # 1行3列の要素(列優先のメモリ配置)  
M[:, 2] # 2列のコピー(ベクトル)

Juliaは列優先のメモリ配置です。連続アクセスが速くなるため、列方向の走査か、eachindexによる抽象イテレーションが推奨されます。

# 形に依存しない安全な反復  
s = 0.0  
for i in eachindex(v)  
@inbounds s += v[i]  
end

次元の入れ替えや再配置は、permutedims、PermutedDimsArray、reshapeを使います。reshapeはデータをコピーせず、ビュー的に形状だけを変えるのが原則です。

B = reshape(v, 3, 1) # 3×1 Matrix(コピーなし)  
C = peruted = permutedims(M, (2,1)) # 軸入れ替え(コピーあり)  
D = PermutedDimsArray(M, (2,1)) # 軸ビュー(原則コピーなし)

多次元の座標にはCartesianIndex/CartesianIndicesを使うと可読性が高く、境界計算を誤りにくくなります。

for I in CartesianIndices(A)  
@inbounds A[I] = sum(Tuple(I)) # インデックスの各成分の和を代入  
end

部分次元に対する安全な抽出はselectdimが便利です。可読性の高い「次元名」風の記述で意図を明確にできます。

row2 = selectdim(M, 1, 2) # 第1次元(行)の2番目 → 2行目のビュー  
col3 = selectdim(M, 2, 3) # 第2次元(列)の3番目 → 3列目のビュー

* * *

# 2. ブロードキャストと融合:ドットで短く書いて速くする

要素ごとの演算は、ドット記法で簡潔に書けます。Juliaはドット演算を融合(fusing)し、中間配列の生成を避けて高速化します。

x = rand(10_000); y = rand(10_000)  
@. x = sin(x) + 2*y # @. は行内の演算へ一括でドット付与

関数にもドットを付けて、要素ごとの適用が可能です。

f(u, v) = u^2 + v^2  
u = rand(5); v = rand(5)  
w = f.(u, v) # 要素ごとのf

ドット代入(.=)は左辺が既存配列なら再利用され、メモリアロケーションを削減できます。

z = similar(u) # 形状・型を揃えた未初期化配列  
z .= sin.(u) .+ cos.(v) # 代入側のメモリを再利用

スカラーと配列の混合や次元拡張にはRefが有効です。ブロードキャストの「引数をスカラー扱い」にできます。

off = Ref(0.1)  
@. u = u + off[] # 等価:u .+= 0.1

ユーザー定義型に対しても、broadcastableで「要素として扱うか/展開するか」を制御できます。多くの場合、Refで十分です。

形状不一致はエラーの原因です。axesで軸を合わせる、必要ならreshapeで形状を揃えてからブロードキャストします。

a = rand(3,1); b = rand(1,4)  
C = a .+ b # 3×4 に自動ブロードキャスト

* * *

# 3. ビューとコピーの選択:view/@viewsで無駄を減らす

部分配列の抽出は、デフォルトではコピーです。頻繁な切り出しで性能が問題となる場合、viewまたは@viewsを用いてビューに切り替え、コピーを避けます。

M = rand(1000, 1000)  


# コピー(安全だが重い)  
col = M[:, 500]  


# ビュー(軽い)  
col_view = view(M, :, 500)  


# 大きなコードで一括適用  
@views for j in 1:size(M,2)  
v = M[:, j] # ここはビューに展開される  
v .*= 2  
end

ビューは元配列のメモリを参照します。元配列の寿命と整合性を保つ設計が前提です。破壊的操作(!)はビューにも及びます。

ビューとselectdimを組み合わせると、意図が読みやすく、境界計算の誤りが減ります。

@views row2 = selectdim(M, 1, 2) # 2行目ビュー  
row2 .+= 1.0

* * *

# 4. 低レベルループ最適化:eachindex/@inbounds/@simdの要点

ループで細かい制御を行う場合、配列の形に依存しないeachindexを使うと、最適なインデックス順序で走査されます。

function scale!(x::AbstractVector{<:Real}, a::Real)  
@inbounds @simd for i in eachindex(x)  
x[i] = a * x[i]  
end  
return x  
end

  * @inboundsは境界チェックを外します。事前に正しさが保証されているホットループのみで使います。
  * @simdはベクトル化のヒントです。同一配列への依存関係がない単純ループで効果が見込めます。
  * @fastmathは数値的再配置の許容(厳密なIEEE754則を緩める)です。数値安定性に影響するため、意図・許容誤差を明示できる場面でのみ使用します。



多次元ループはCartesianIndicesで安全に書けます。

function laplace2d!(U::AbstractMatrix{<:Real})  
@views @inbounds for I in CartesianIndices(U)[2:end-1, 2:end-1]  
i, j = I.I  
U[i, j] = (U[i-1, j] + U[i+1, j] + U[i, j-1] + U[i, j+1]) / 4  
end  
return U  
end

* * *

# 5\. BLASによる密行列の高速演算:LinearAlgebraの基本

LinearAlgebra標準ライブラリはBLAS/LAPACKに接続し、高性能な線形代数演算を提供します。特に密行列の積・解法はBLASの対象です。

using LinearAlgebra  


# 行列×ベクトル・行列×行列  
A = rand(1000, 1000)  
x = rand(1000)  
y = A * x # gemv 相当  
B = rand(1000, 1000)  
C = A * B # gemm 相当  


# インプレース演算:既存メモリを再利用  
C = similar(A)  
mul!(C, A, B) # C = A*B を代入(メモリ再利用)

ベクトル間のドット積やAXPYもBLASです。

dot(x, x) # 内積  
axpy!(2.0, x, y) # y ← 2.0*x + y

構造付き行列ラッパはアルゴリズム選択を最適化します。対角・三角・対称/エルミートを明示すると、不要計算を避けられます。

D = Diagonal(rand(5))  
L = LowerTriangular(rand(5,5))  
S = Symmetric(rand(5,5)) # 対称(上三角を利用)  


y = D * x # 要素ごとの乗算に最適化  
z = L \ x # 前進代入(trsv)  
w = S * x # syrk等に最適化される

連立一次方程式は\で解きます。正方・長方に応じ、適切な分解が選ばれます。

b = rand(1000)  
x = A \ b # 一般にはLU分解(正方)/QR(長方)を利用

分解の再利用(複数右辺を解く)では、因数化オブジェクトを保持します。

F = lu(A) # LU分解を保持  
X = F \ b # 使い回しで高速

* * *

# 6\. LAPACKによる分解:lu/cholesky/qr/svd/eigenの実務

分解は計算の中間表現であり、繰り返し計算や安定性確保に有効です。Juliaは分解結果をオブジェクトとして返し、演算子\で連携できます。

A = rand(1000,1000)  
b = rand(1000)  


# LU分解(一般正方)・ピボット付き  
F_lu = lu(A)  
x = F_lu \ b  


# Cholesky(対称正定値)  
K = Symmetric(rand(1000,1000)); K = K*K' + I # 正定値の生成例  
F_chol = cholesky(K)  
x = F_chol \ b  


# QR分解(長方形、最小二乗)  
M = rand(1200, 1000)  
F_qr = qr(M)  
x_ls = F_qr \ b[1:1200] # 過剰決定の最小二乗解  


# SVD(特異値・ランク評価・低ランク近似)  
F_svd = svd(M)  
σ = F_svd.S # 特異値  
U = F_svd.U; Vt = F_svd.Vt  


# 固有分解(正方)  
F_eig = eigen(Symmetric(rand(200,200)))  
λ = F_eig.values  
V = F_eig.vectors

例外・チェックの扱いも重要です。cholesky(A)は非正定値で例外が発生します。事前にSymmetricで性質を宣言し、必要ならcheck=falseでスキップした上で別途検証します(検証はユーザー側の責任になります)。

try  
F = cholesky(Symmetric(A))  
x = F \ b  
catch e  
# 正定値でない等の失敗  
# ここで代替経路(例えばルー分解)へ切り替える  
x = (lu(A) \ b)  
end

分解を活用すると、複数右辺、前処理、条件数評価などの高次処理が簡潔に記述できます。

# 条件数の概算(2ノルム近似)  
using LinearAlgebra  
condA = cond(A) # 大きいほど数値的に不安定

* * *

# 7. 疎行列:SparseArraysで巨大問題を現実的に扱う

巨大でほとんどがゼロの行列は、疎行列で表現します。メモリと計算量を劇的に削減できます。

using SparseArrays  


# 座標形式から生成  
I = [1, 2, 3, 4]; J = [1, 2, 3, 4]; V = [10.0, 20.0, 30.0, 40.0]  
S = sparse(I, J, V, 4, 4) # 4×4 SparseMatrixCSC{Float64}  


# 零の削除・要素数  
dropzeros!(S)  
nnz(S) # 非零要素数  


# 疎行列×ベクトル・疎×密  
x = rand(4)  
y = S * x  


# 対角疎行列  
Sd = spdiagm(0 => rand(1000)) # 主対角のみ

疎行列の連立解は、行列の構造(対称・正定・帯幅等)に応じた分解が選ばれます。密への変換は最後の手段です。手続き的な逐次挿入は高コストになることがあるため、座標ベクトルでまとめてspalloc/sparseを使うのが定石です。

# 座標を一括でsparseへ  
I = Int[]  
J = Int[]  
V = Float64[]  
# ... ここで座標と値を詰める ...  
S = sparse(I, J, V, m, n)

疎の反復アルゴリズム(例えば共役勾配法など)を用いる場面もありますが、ここでは標準ライブラリ内で完結する基本演算に留めます。性質がわかっている場合はSymmetric等のラッパで宣言し、最適な解法の選択を支援します。

* * *

# 8. 数値安定性と精度:丸め・条件数・アルゴリズム選択

線形代数計算の品質は、アルゴリズム選択とデータの性質に強く依存します。以下の要点を意識してください。

  * 条件数:cond(A)が大きいと、入力の微小変化が解に大きく影響します。スケーリング、正則化、アルゴリズム変更(QR/SVD)で緩和します。
  * ピボット:LU分解では部分ピボットが一般に安全です。ピボット無しは高速でも不安定になり得ます。
  * 最小二乗:過剰決定系は正規方程式よりもQR分解ベースの解が安定です。JuliaのA \ bは長方形でQRを選ぶのが原則です。
  * 対称正定値:この性質があるならSymmetric(A)を明示してcholeskyを使うと高速・安定です。
  * 丸め誤差:muladd(a, b, c)はa*b + cの融合演算で丸めの影響を低減します(環境・最適化に依存)。
  * 比較:浮動小数点の等値比較はisapprox(x, y; rtol, atol)で行います。閾値は問題に応じて設定します。
  * 精度切り替え:Float64を基本とし、必要に応じてBigFloatに切り替えます。アルゴリズムの安定性が精度を補うことも多いため、まず安定な手法を選びます。



# isapprox の使用例  
isapprox(0.1 + 0.2, 0.3; atol=1e-12, rtol=1e-8)  


# muladd の使用例(内積の加算で丸め減衰の一手)  
s = 0.0  
for i in eachindex(x, y)  
s = muladd(x[i], y[i], s)  
end

データのスケーリング(列の標準化、単位合わせ)も効果的です。極端に異なる桁の値を同じ計算へ入れると、丸め誤差が顕著になります。

* * *

# 9. 実戦レシピ:高速な配列処理と線形代数の統合

ここでは、配列操作・ブロードキャスト・ビュー・BLAS/LAPACKを組み合わせた実用的なパターンを示します。

## 9.1 移動平均・移動標準偏差(ビュー+ブロードキャスト)

function moving_stats!(μ::AbstractVector{Float64}, σ::AbstractVector{Float64},  
xs::AbstractVector{<:Real}; window::Int=5)  
window > 0 && isodd(window) || throw(ArgumentError("window must be positive odd"))  
n = length(xs); half = (window - 1) ÷ 2  
@views @inbounds for i in 1:n  
lo = max(1, i-half); hi = min(n, i+half)  
seg = xs[lo:hi]  
m = sum(seg) / length(seg)  
μ[i] = m  
σ[i] = sqrt(sum((x - m)^2 for x in seg) / max(1, length(seg)-1))  
end  
return μ, σ  
end  


xs = rand(10_000)  
μ = similar(xs, Float64); σ = similar(xs, Float64)  
moving_stats!(μ, σ, xs; window=7)

## 9.2 最小二乗回帰(QR分解)

using LinearAlgebra  


# デザイン行列(先頭列は1で切片)  
n, p = 10_000, 5  
X = [ones(n) rand(n, p-1)]  
β_true = [1.0, 0.5, -0.2, 0.0, 2.0]  
y = X * β_true .+ 0.01 .* randn(n)  


F = qr(X) # 分解の再利用が可能  
β_est = F \ y # 最小二乗解

QRは正規方程式より安定です。分解を再利用すると、複数のyに対して高速に解を得られます。

## 9.3 正定値系の解(Cholesky)

using LinearAlgebra  


# SPDの例:A = B'B + λI  
B = rand(2000, 1000)  
A = Symmetric(B'B + 1e-3I) # 正定値に調整  
b = rand(2000)  


F = cholesky(A)  
x = F \ b

SPDを宣言することで、choleskyが最適な経路を選びます。小さな正則化は条件数の改善に寄与します。

## 9.4 疎行列の構築と積

using SparseArrays  


m, n = 10_000, 10_000  
I = rand(1:m, 200_000)  
J = rand(1:n, 200_000)  
V = rand(200_000)  
S = sparse(I, J, V, m, n)  


x = rand(n)  
y = S * x # 疎×密の積(メモリ効率が高い)

座標の一括投入で、高速に疎行列を生成します。逐次挿入は行列の再構築コストが高くなりがちです。

## 9.5 三角解とインプレース更新

using LinearAlgebra  


L = LowerTriangular(rand(1000,1000))  
b = rand(1000)  
x = similar(b)  
ldiv!(x, L, b) # x ← L \ b をインプレースで計算

ldiv!は既存メモリへ解を格納し、アロケーションを抑えます。反復処理やオンライン処理で有効です。

* * *

# 10. 運用の注意:測定・前処理・契約の明示

  * 測定:@timeで概算、より厳密な計測はベンチマーク環境で行い、ホットスポットに@inbounds/@simdを適用します。
  * 前処理:配列の形状と軸を明確にし、axesベースの反復を優先。部分抽出は@viewsでビュー化し、破壊的操作は!で契約を明示します。
  * 数値契約:Symmetric/Hermitian/Diagonal/UnitUpperTriangular等のラッパで構造を宣言すると、適切なアルゴリズムが選ばれます。宣言は正しい性質がある場合のみ行います。
  * 安定性:condで不安定性の兆候を把握し、QR/SVDへ切り替える、スケーリングや正則化を行うなどの手当てをします。
  * メモリ:大規模計算ではインプレース(mul!/ldiv!/.=)を優先し、アロケーションの発生箇所を減らします。



これらの原則を徹底することで、配列処理と線形代数が滑らかにつながり、Juliaの強みである「短く書いて速い」を実務で安定して引き出せます。

* * *

# 第6章性能チューニングと計測:型安定性・アロケーション削減・プロファイリング

# 1. 性能チューニングの基本姿勢と測定の原則

性能改善は「測って、原因を特定して、最小介入で直す」というサイクルで進めます。まず、正しさを確認したコードに対してのみ最適化します。測定は以下の原則を守ると安定します。

  * ウォームアップ: JuliaはJITで初回呼び出し時にコンパイルします。ベンチ前に一度呼び出しておき、コンパイル遅延を除きます。
  * 再現性: 乱数や入力データを固定します。Random.seed!で種を設定し、ベンチ時に毎回同じ入力を使います。
  * 環境統一: \--project=.でプロジェクト環境をアクティベートし、依存のバージョン差を避けます。Threads.nthreads()を確認し、意図した並列数で計測します。
  * 計測単位の明確化: 処理あたりの時間、スループット、メモリバイト数、アロケーション回数など、評価指標を事前に決めます。
  * ホットスポット優先: 全体の遅延より、消費時間の大半を占める関数(ホットスポット)から手を入れます。プロファイルで当たりをつけます。



これらの原則のもと、型安定性、アロケーション削減、プロファイリング、コード生成の読み解きへ進みます。

* * *

# 2. 型安定性の確認方法:@code_warntypeと関数バリア

Juliaの最適化は型情報に依存します。型安定性とは「関数の戻り値や中間値の型が、入力型から決定可能でブレない」状態です。型が揺れると、ボックス化や動的ディスパッチが入り、実行速度が低下します。

## 2.1 @code_warntypeで推論結果を確認する

@code_warntype f(args...)は、コンパイラの型推論結果を色付き表示します。::AnyやUnionでハイライトされる箇所は、型不安定の疑いがあります。

# 悪い例:戻り値がIntかStringで揺れる  
bad(x) = x > 0 ? x : "neg"  


@code_warntype bad(1)

この場合、badは型不安定です。戻り値の型を揃えるか、上位で分岐します。

# 良い例:常に数値を返す  
good(x) = x > 0 ? x : -x  
@code_warntype good(1)

## 2.2 フィールド型・コンテナ型の具体化

構造体のフィールドに型注釈が無い、Vector{Any}等の非具体的コンテナを使うと、型が揺れます。フィールド型は具体化し、コンテナの要素型は可能な限り具体にします。

# 望ましくない  
mutable struct Box  
value  
end  


# 望ましい(パラメトリック)  
mutable struct Box{T}  
value::T  
end  


# コンテナは具体型で  
xs = Vector{Float64}(undef, 1000)

## 2.3 関数バリアで動的を局所化

APIの入り口では抽象型でも、内部でpromoteや具体型への変換を行い、関数境界で型を確定させます。これにより内側のルーチンが型安定になり、JIT最適化が効きます。

# 外側は抽象、内側は具体  
function outer(x::Real, y::Real)  
x2, y2 = promote(x, y) # ここで型を揃える  
return inner(x2, y2)  
end  


inner(x::Float64, y::Float64) = muladd(x, y, 0.0)  
inner(x::Float32, y::Float32) = muladd(x, y, 0.0f0)

## 2.4 グローバル変数の回避

グローバル変数はREPLやスクリプトのトップレベルでは推論が難しく、型不安定の原因になります。値は引数で渡す、設定はconstで固定する等で対処します。

# 悪い例  
factor = 2  
badmul(x) = x * factor  


# 良い例  
goodmul(x; factor::Int=2) = x * factor

* * *

# 3. ベンチマーク:BenchmarkTools.@btimeと実務的使い方

正確な計測にはBenchmarkToolsを使います。@btimeは最良時間とアロケーション量を出し、@benchmarkは分布統計を返します。

## 3.1 基本の計測パターン

using BenchmarkTools  


# 入力を外で作り、$で挿入(グローバル変数の影響を避ける)  
x = rand(10_000)  
@btime sum($x) # $で変数を評価済みにする  


# 直前にウォームアップ  
sum(x)  
@btime sum($x)

ポイント: $を付けると、ベンチ中の変数解決に伴うアロケーションを避けられます。グローバルで直接参照すると、推論が崩れて誤計測になりがちです。

## 3.2 セットアップとティアダウン

@benchmarkはsetup引数で前処理を指定できます。ベンチごとに新鮮な入力を用意し、破壊的操作の影響を回避します。

# 毎回新しい入力でmapを計測  
res = @benchmark begin  
y = similar($x)  
@. y = sin($x)  
end

BenchmarkTools.DEFAULT_PARAMETERS.samples等で試行回数や最小時間を調整できます。短い関数は反復を多く、長い関数は試行回数を少なくするなど、対象に合わせて設定します。

## 3.3 @allocatedでアロケーション量を取得

粗い確認にはBase.@allocatedを使えます。関数呼び出しに伴うメモリ割当バイト数を返します。

# 配列のコピーが発生していないか確認  
bytes = @allocated sum(x)  
println(bytes)

* * *

# 4. メモリアロケーションの可視化と削減

アロケーションはGC負荷とキャッシュ効率悪化の原因です。以下の原則で削減します。

## 4.1 破壊的更新と事前確保

配列・ベクトルの結果は、事前に確保したバッファへ書き込みます。関数名末尾に!を付け、契約(破壊的)を明示します。

function axpy!(y::AbstractVector{T}, a::T, x::AbstractVector{T}) where {T<:Real}  
@inbounds @simd @. y = a * x + y  
return y  
end  


x = rand(10_000); y = rand(10_000)  
@btime axpy!($y, 2.0, $x) # 0 alloc を目指す

ベクトル・辞書にはsizehint!で容量の見当を示します。再確保の回数が減ります。

v = Int[]  
sizehint!(v, 100_000)  
for i in 1:100_000  
push!(v, i)  
end

## 4.2 ビューでコピー回避

部分配列の抽出はデフォルトでコピーです。viewまたは@viewsでビューに切り替えます。

A = rand(1_000, 1_000)  
@views for j in axes(A, 2)  
col = A[:, j] # ビュー  
col .*= 2  
end

## 4.3 形状と融合の活用

ブロードキャストの.=代入は左辺のメモリを再利用します。中間配列を作らない式にまとめます。

x = rand(10_000); y = rand(10_000)  
@btime begin  
@. x = sin(x) + 2*y  
end # 中間を避けて1パスで更新

## 4.4 コンテナの型安定とAnyの回避

Vector{Any}や混在型のUnionは、各要素の動的ディスパッチとボックス化を誘発します。要素型を具体にし、必要なら抽象型の受け口で外側を包み、内側で具体へ落とす関数バリアを使います。

# 望ましい  
xs = Vector{Float64}(undef, 100_000)

## 4.5 データレイアウト:SoAとAoS

大量の構造体の配列(AoS: Array of Struct)より、各フィールドごとに配列を分ける(SoA: Struct of Arrays)と、列方向の連続アクセスができ、キャッシュ効率が上がることがあります。

# AoS:座標の配列  
struct Pt  
x::Float64  
y::Float64  
end  
pts = [Pt(rand(), rand()) for _ in 1:100_000]  


# SoA:各フィールドの配列  
xs = rand(100_000); ys = rand(100_000)  
@inbounds @simd @. xs = xs + 2*ys

* * *

# 5. プロファイラの使い方:Profileでホットスポットを特定する

標準ライブラリProfileはサンプリング型プロファイラです。どの関数がどれだけ時間を消費したか、スタックトレースの分布で示します。

## 5.1 基本操作

using Profile  


Profile.clear()  
@profile begin  
# 計測したい処理  
heavy_work()  
end  
Profile.print() # 結果をテキストで表示

Profile.print()はスタックの頻度を表示します。行番号や関数名から、どのループ・どの呼び出しが支配的かを把握します。

## 5.2 サンプリング頻度とクリア

Profile.init(n, delay)でバッファサイズやサンプリング間隔を調整できます。計測前にはProfile.clear()で過去の記録を消去します。

Profile.init(10_000, 0.001) # バッファを大きく、間隔を短く  
Profile.clear()

## 5.3 観察のポイント

  * 深いスタックで頻度が高い行がホットスポットです。そこへ@btimeを当て、型不安定なら直します。
  * GC時間が支配的なら、アロケーション削減に注力します。
  * I/O待ちが多い場合、バッチ化や非同期化を検討します(計算の最適化とは別軸)。



* * *

# 6. ホットスポットの特定から修正まで:段階的手順

  1. プロファイルで重い場所を特定。
  2. 最小再現例を作り、@btimeで時間とアロケーションを測る。
  3. **@code_warntype**で型不安定を確認し、戻り値・フィールド・コンテナ型を安定化。
  4. アロケーション削減(ビュー、.=, 事前確保、sizehint!)。
  5. 低レベルヒント(@inbounds, @simd)は正しさ確認後に適用。
  6. アルゴリズム切り替え(密→BLAS、長方→QR、SPD→Choleskyなど)。
  7. 再測定して改善を確認。回帰テスト・ベンチで品質を維持。



* * *

# 7. コード生成の読み方:@code_typed・@code_llvm・@code_native

最終的な機械語に近い視点でボトルネックを見抜くこともできます。

## 7.1 @code_typed(型付きIR)

コンパイラの型付き中間表現(SSA)を表示します。Anyやinvokeのパターンから動的ディスパッチの痕跡を掴めます。

@code_typed good(1)

## 7.2 @code_llvm(LLVM IR)

ベクトル化、インライン展開、muladdの融合など、低レベル最適化が適用されているかを確認できます。

@code_llvm good(1)

## 7.3 @code_native(ネイティブアセンブリ)

最終的なアセンブリを表示します。過度に追いすぎると移植性やバージョン依存の罠が増えるため、明確な目的(例えばループがベクトル化されているか、分岐が整理されているか)に絞って使用します。

@code_native good(1)

* * *

# 8. 具体チューニング事例:移動統計の最適化

移動平均・移動標準偏差を、素朴実装と最適化実装で比較します。

## 8.1 素朴実装(毎回サブ配列をコピー)

function moving_stats_naive(xs::AbstractVector{<:Real}, window::Int)  
n = length(xs); half = (window - 1) ÷ 2  
μ = Vector{Float64}(undef, n)  
σ = Vector{Float64}(undef, n)  
for i in 1:n  
lo = max(1, i-half); hi = min(n, i+half)  
seg = xs[lo:hi] # コピーが発生  
m = sum(seg) / length(seg)  
s2 = sum((x - m)^2 for x in seg) / max(1, length(seg)-1)  
μ[i] = m; σ[i] = sqrt(s2)  
end  
return μ, σ  
end

## 8.2 最適化実装(ビュー・事前確保・.=融合)

function moving_stats_opt!(μ::AbstractVector{Float64}, σ::AbstractVector{Float64},  
xs::AbstractVector{<:Real}, window::Int)  
window > 0 && isodd(window) || throw(ArgumentError("window must be positive odd"))  
n = length(xs); half = (window - 1) ÷ 2  
@views @inbounds for i in 1:n  
lo = max(1, i-half); hi = min(n, i+half)  
seg = xs[lo:hi] # ビュー(コピー無し)  
m = sum(seg) / length(seg)  
μ[i] = m  
acc = 0.0  
@inbounds @simd for j in eachindex(seg)  
d = seg[j] - m  
acc = muladd(d, d, acc) # 融合乗算加算で丸め影響軽減  
end  
σ[i] = sqrt(acc / max(1, length(seg)-1))  
end  
return μ, σ  
end

## 8.3 計測

using BenchmarkTools  
xs = rand(100_000)  
μ = Vector{Float64}(undef, length(xs))  
σ = similar(μ)  


# ウォームアップ  
moving_stats_naive(xs, 7)  
moving_stats_opt!(μ, σ, xs, 7)  


@btime moving_stats_naive($xs, 7) # アロケーション多いはず  
@btime moving_stats_opt!($μ, $σ, $xs, 7) # 0〜少量のアロケーション

期待される改善は、アロケーションと時間の双方です。@allocatedでも粗く差を確認できます。

* * *

# 9. 並列・マルチスレッドの計測と注意点

並列化は最後の手段です。単一スレッドでのアルゴリズム最適化と型安定化を済ませてから、Threads.@threadsを適用します。

## 9.1 スレッド化の基本

using Base.Threads  


function scale_threads!(x::Vector{Float64}, a::Float64)  
@threads for i in eachindex(x)  
@inbounds x[i] = a * x[i]  
end  
return x  
end

注意点:

  * 偽共有(false sharing): スレッドが近接データを頻繁に書き換えるとキャッシュラインの奪い合いが発生します。チャンク分割で緩和します。
  * 乱数: スレッドごとに独立な生成器を用意し、再現性を確保します。



using Random  
rngs = [MersenneTwister(1234 + i) for i in 1:nthreads()]  
@threads for i in eachindex(x)  
tid = threadid()  
x[i] = rand(rngs[tid])  
end

  * 同期コスト: 軽いタスクはスレッド化で逆に遅くなることがあります。バッチ化して粗粒度に分割します。



ベンチでは、スレッド数を固定して比較し、@btimeのセットアップで入力を毎回リセットします。

* * *

# 10\. I/O・BLAS・LAPACKの活用によるアルゴリズム的最適化

計測でホットスポットが線形代数なら、LinearAlgebra経由でBLAS/LAPACKに任せます。自前ループより高速で、キャッシュフレンドリーです。

using LinearAlgebra  
A = rand(2000, 2000); x = rand(2000)  
@btime $A * $x # BLASのgemv

連立解は\、分解はlu/cholesky/qr/svdを選択し、性質の宣言(Symmetric, Hermitian, Diagonal, UpperTriangular)で最適経路へ誘導します。I/Oがボトルネックなら、バッチ読み書きとメモリ再利用でスループットを上げます。

* * *

# 11. 解析系ユーティリティ:ロガー・アサート・契約

性能調査の過程で、入力の妥当性や境界条件を破っていないか確認するため、アサートやログを入れます。本番では削る前提でも、開発時は手厚くしておくとバグの切り分けが容易です。

using Logging  


function normalize!(x::AbstractVector{<:Real})  
s = sum(abs.(x))  
@assert s != 0 "zero vector cannot be normalized"  
@. x = x / s  
@debug "normalized with sum=$s"  
return x  
end

@assertは内在的不変条件の検知に、例外は入力不正の通知に使い分けます。性能目的で@inboundsを付けたループには、上流で境界チェックを入れておくのが安全です。

* * *

# 12. ケース:ヒストグラム計算の段階的改善

## 12.1 素朴版(push!でバケットに追加)

function hist_naive(xs::AbstractVector{<:Real}, nbins::Int)  
lo, hi = minimum(xs), maximum(xs)  
binw = (hi - lo) / nbins  
bins = fill(0, nbins)  
for x in xs  
b = clamp(Int(floor((x - lo) / binw)) + 1, 1, nbins)  
bins[b] += 1  
end  
return bins  
end

## 12.2 改善版(事前計算・関数バリア・型安定)

function hist!(bins::Vector{Int}, xs::AbstractVector{<:Real}, lo::Real, hi::Real)  
nbins = length(bins)  
T = float(promote_type(eltype(xs), typeof(lo), typeof(hi)))  
loT, hiT = T(lo), T(hi)  
binw = (hiT - loT) / T(nbins)  
@inbounds for x in xs  
xt = T(x)  
b = Int(floor((xt - loT) / binw)) + 1  
if 1 <= b <= nbins  
bins[b] += 1  
end  
end  
return bins  
end  


function hist_fast(xs::AbstractVector{<:Real}, nbins::Int)  
lo, hi = minimum(xs), maximum(xs)  
bins = zeros(Int, nbins)  
return hist!(bins, xs, lo, hi)  
end

## 12.3 計測

using BenchmarkTools  
xs = rand(1_000_000)  
@btime hist_naive($xs, 100) # 参考  
@btime hist_fast($xs, 100) # 改善後

改善点は、型安定化(同一の浮動型に揃える)、事前確保と破壊的更新、境界チェックの局所化です。さらにスレッド分割して各スレッドでローカルバケットを集計し、最後にリダクションする戦略でスケールします(偽共有を避けるためローカル配列を持つのが定石)。

* * *

# 13. レイテンシ(初回遅延)とTTFXの最適化:事前コンパイル

性能(スループット)とは別に、初回実行までの遅延(TTFX: time-to-first-execution)を短縮したい場面があります。Pkg.precompile()で依存パッケージの事前コンパイルを行い、REPLの起動直後の遅延を抑えられます。自作パッケージでは、パブリックAPIに対するprecompileステートメントを配置し、よく使うメソッドのコンパイルを先に済ませます。

using Pkg  
Pkg.precompile()

※TTFX短縮はスループットの改善とは別軸です。ベンチマークではウォームアップ実行でJIT遅延を除去します。

* * *

# 14. 低レベル最適化の留意点:@fastmath・ポインタ操作の慎重な適用

@fastmathは浮動演算に関する厳密なルール(NaN伝播、丸め順序)を緩め、最適化余地を広げますが、数値安定性に影響し得ます。誤差許容が明確な場面でのみ適用します。

@fastmath @inbounds @simd for i in eachindex(x, y)  
s = muladd(x[i], y[i], s)  
end

ポインタ操作(unsafe_*)は安全性に関わるため、標準のインデックスやeachindexで十分な性能が出る限り避けます。正しさ>速度の順序を守ります。

* * *

# 15. 結果の検証と性能回帰の監視:テストとベンチの併走

最適化のあと、数値結果が変わっていないかをテストで確認します。性能はベンチで回帰検知します。

# 単体テスト例(概念)  
using Test  
@test isapprox(sum(rand(10)), sum(rand(10)); rtol=1e-12) == false # 乱数は一致しない例  


# ベンチマークを定期的に記録(概念)  
using BenchmarkTools  
t = @belapsed axpy!($y, 2.0, $x)  
@info "axpy! elapsed" time=t

CIにベンチを組み込む場合、揺らぎに強い指標(大きめの入力・十分な反復)で比較し、しきい値の設定を慎重に行います。

* * *

# 16. 運用箇条(記述のみ)

  * 型安定性の確認は@code_warntype、戻り値・フィールド・コンテナ型を具体化し、関数バリアで内側を安定化します。
  * 時間は@btime、分布は@benchmarkで測定し、$挿入で誤計測を避けます。ウォームアップと再現性を徹底します。
  * アロケーション削減は、ビュー、.=融合、事前確保、sizehint!、破壊的!で行い、GC負荷を下げます。
  * プロファイラProfileでホットスポットを見つけ、ピンポイントに最適化します。GC・I/O・計算を切り分けます。
  * 低レベルヒント(@inbounds, @simd, @fastmath)は正しさ確認後に適用し、数値安定性への影響を意識します。
  * BLAS/LAPACKで密行列演算を任せ、構造の宣言(Symmetric等)で最適な分解を選びます。
  * 並列は最後の手段。偽共有や乱数の再現性に注意し、粗粒度の分割でスケールさせます。
  * テストとベンチを併走させ、性能回帰を監視します。数値結果の検証を最優先にします。



以上の作法を体系的に適用することで、Juliaの「短く書いて速い」を安定して引き出し、実務に耐える性能と保守性を両立できます。

* * *

# 第7章メタプログラミング入門:Expr・マクロ・@generatedの設計術

# 1. 式とASTの基本:Expr・クォート・アンクォート

Juliaのメタプログラミングは「コードをデータとして扱い、変換してから実行する」思想に基づきます。Juliaのコードは抽象構文木(AST)に変換され、ASTはExprというオブジェクトで表現されます。Exprは以下の3要素を持ちます。

  * head: 式の種類(例::call、:block、:if)
  * args: 引数列(サブツリー)
  * メタデータ(行番号など、表示時には省略されることがあります)



最も手軽なAST生成は「クォート(quote)」です。:( ... )やquote ... endで、コード断片を実行せずに式オブジェクトへ変換できます。

ex = :(1 + 2 * x) # Expr(:call, :+, 1, Expr(:call, :*, 2, :x))  
typeof(ex) # Expr  
dump(ex) # 構造の確認

式内部の変数を埋め込みたいときは「アンクォート(スプライス)」を使います。$varで値を式中へ展開できます。

op = :+  
lhs = :y  
rhs = 42  
ex = :($op($lhs, $rhs)) # -> :(+(y, 42))

式を手作業で組み立てる場合は、Exprコンストラクタを使います。

ex = Expr(:call, :+, :x, 10) # :(x + 10)

ASTの表示をS式風に確認するにはMeta.show_sexprが便利です。

using InteractiveUtils  
Meta.show_sexpr(stdout, :(a*b + c))  
# (:call, :+, (:call, :*, :a, :b), :c)

文字列からASTへ変換するにはMeta.parseを使います。パーサレベルの変換なので、動的な構文生成や簡易DSLの入り口に役立ちます。

ex = Meta.parse("sin(x) + cos(y)")

ASTを直接評価するにはevalがありますが、評価はモジュールのグローバルで行われる点に注意してください(ローカルでは無効)。通常はmacroや@generatedでコンパイル時に展開・生成し、evalは最小限の用途に留めるのが保守的です。

* * *

# 2. マクロの基礎:展開タイミング・ハイジーン・esc

macroはパース後・型推論前の段階で、入力ASTを別のASTへ変換して差し込みます。展開後のコードは通常の関数と同様にコンパイルされます。基本形は以下のとおりです。

macro sayhello(name)  
# name は式(Symbol など)として渡される  
return :(println("hello ", $name))  
end  


@sayhello :world # hello world

マクロは「衛生的(hygienic)」であるべきです。つまり、マクロ内部で導入する識別子(変数名)が、呼び出し側ユーザーのスコープと衝突しないようにします。衝突回避にはgensym(一意なシンボル生成)が有効です。

macro pow2(ex)  
tmp = gensym(:tmp)  
return quote  
local $(tmp) = $(esc(ex))  
$(tmp) * $(tmp)  
end  
end  


x = 3  
@pow2 x # 9

ここで重要なのがescです。esc(ex)は「呼び出し元のスコープへ式を逃がす」操作で、ユーザーの変数を正しく参照させるために使います。原則として、ユーザー由来の識別子や式はescし、マクロ内部で導入した識別子はgensym等で衛生的に生成します。

macro badpow(ex)  
# escしないと、ユーザーの変数を別スコープとして扱って誤動作し得る  
return :( $(ex) * $(ex) )  
end

実際の展開結果は@macroexpandで確認できます。マクロの動作確認や衛生性の検証に必須です。

@macroexpand @pow2 x

* * *

# 3. マクロ設計パターン:ロギング・計測・コンテキスト挿入

マクロは「記法の糖衣」と「反復定型の自動挿入」に向いています。高度なアルゴリズム本体は関数へ寄せ、マクロは周辺処理(ログ・計測・契約チェック)に絞ると保守性が高まります。

## 3.1 計測マクロ(@timeitのスケルトン)

macro timeit(tag, ex)  
t = gensym(:t)  
return quote  
local $(t) = time()  
local _res = begin  
$(esc(ex))  
end  
println("[", $(esc(tag)), "] elapsed = ", time() - $(t), " s")  
_res  
end  
end  


@timeit "step1" begin  
s = sum(rand(10^6))  
end

計測対象をbegin ... endで受けると、複数文を含むブロックにも適用しやすくなります。副作用変数はローカルgensymで衛生化します。

## 3.2 コンテキスト挿入(リソース管理)

doブロックを受ける形式は「コンテキスト」を自然に挿入できます。例えば、乱数種の固定や一時的な設定変更をブロック範囲で安全に適用します。

macro with_seed(seed, ex)  
return quote  
local _rng_bak = copy(Random.default_rng())  
try  
Random.seed!($(esc(seed)))  
$(esc(ex))  
finally  
Random.seed!(Random.default_rng(), UInt(_rng_bak.seed))  
end  
end  
end  


@with_seed 1234 begin  
println(rand())  
end

注意:乱数生成器の扱いは環境やバージョンにより差があり得ます。実務では生成器を引数で受けて関数に渡す設計がより安全です。ここでは構図の例示に留めています。

## 3.3 式の書き換え(条件付挿入)

式パターンを検出して書き換える例です。

macro guard_nonneg(ex)  
# ex が代入式 `lhs = rhs` のとき、rhs に max(0, rhs) を挿入する  
if ex.head == :(=) && length(ex.args) == 2  
lhs, rhs = ex.args  
new_rhs = :(max(0, $(rhs)))  
return :( $(lhs) = $(new_rhs) )  
else  
error("@guard_nonneg requires assignment form")  
end  
end  


@guard_nonneg y = -3 # y = max(0, -3) → y = 0

パターンマッチ時はex.headとex.argsの整合性を厳密にチェックして、不正入力の早期失敗を徹底します。

* * *

# 4. @generated関数の基礎:型に応じたコード生成

@generated関数は引数の型情報のみを用いて、返すべきコード(AST)を決める仕組みです。実行時値に依存してはなりません。向いている場面は、型パラメータに基づくコード展開(タプル長に応じたループ展開、ビット幅で分岐する低レベル処理など)です。

## 4.1 代表例:NTuple{N,T}の和をアンロール

@generated function sumtuple(t::NTuple{N,T}) where {N,T<:Real}  
# N 個の要素を足す式を生成する  
# 例:N=3なら :(t[1] + t[2] + t[3])  
terms = [:(t[$i]) for i in 1:N]  
ex = reduce((a,b)->:( $a + $b ), terms)  
return ex  
end  


sumtuple((1.0, 2.0, 3.0)) # 6.0

このように、型パラメータNに応じてループを消し込み、JITが完全に展開する形へできます。

## 4.2 制約と注意

  * 型のみ参照可能:@generatedの本体では、値(実引数の中身)へアクセスしてはいけません。
  * 純粋性:副作用のある処理を入れると、コンパイルキャッシュと相互作用して非決定的挙動を招きます。
  * デバッグ性:生成コードの可視化に@macroexpandは使えないため、@code_loweredや@code_typedで観察します。



@code_lowered sumtuple((1.0,2.0,3.0))  
@code_typed sumtuple((1.0,2.0,3.0))

## 4.3 Valで型レベルの選択

Val{K}は値を型へ持ち上げるユーティリティで、@generatedの代わりに「軽い分岐」を行う場面で有効です。

f(::Val{true}, x) = x^2  
f(::Val{false}, x) = abs(x)  


f(Val(true), -3) # 9  
f(Val(false), -3) # 3

Valはコンパイラが分岐を消し込めるため、ifより高速になる場合があります。ただし、過度に多用するとメソッドの増殖でコンパイル負荷を招きます。

* * *

# 5. 生成と保守のトレードオフ:いつマクロ・いつ関数

メタプログラミングは強力ですが、保守性と可読性を犠牲にしがちです。以下の原則が有効です。

  * 関数優先:可能な限り通常の関数・多重ディスパッチ・パラメトリック型で解決します。
  * マクロは境界操作:ログ・計測・契約・コンテキストなど、周辺の繰り返しに限定します。
  * @generatedは型駆動最適化のみ:ループ展開・型パラメータ依存の低レベル分岐。ロジック本体は通常関数で表現します。
  * 明示的なインターフェース:マクロ引数はASTで来るため、契約違反の入力を明確にエラーにします。
  * 展開結果の確認:@macroexpand・@code_typedで確認し、CIに簡単な検証を入れます。



小規模・明確な効果(可読化・DRY・性能)以外では、生成処理を控えるのが長期運用では賢明です。

* * *

# 6\. DSLへの橋渡し:宣言的仕様をコードへ落とす

マクロとExprを組み合わせると、宣言的仕様を「実行可能なJuliaコード」へ展開する簡易DSLが作れます。ここでは最小の例として「最小化問題の仕様」を記述し、実行時は簡素なランダム探索に落とすスケルトンを示します。

# 仕様を包む構造体(実装はあくまで例)  
struct MiniProblem  
objective::Expr  
constraints::Vector{Expr}  
end  


function eval_objective(obj::Expr, vars::Dict{Symbol,Float64})  
# 極めて簡略な評価(安全性や網羅性は本例では扱わない)  
# 例::(x^2 + y^2) を vars の値で評価  
x = vars[:x]; y = vars[:y]  
return eval(Expr(:block, :(x = $x), :(y = $y), obj))  
end  


macro minimize(ex)  
# 受け口:@minimize begin ... end  
# ブロック中で objective = ..., constraint: x + y <= 1 のような式を拾う  
ex.head == :block || error("@minimize requires a block")  


obj = nothing  
cons = Expr[]  
for stmt in ex.args  
if stmt.head == :(=) && stmt.args[1] == :objective  
obj = stmt.args[2]  
elseif stmt isa Expr  
push!(cons, stmt)  
end  
end  
obj === nothing && error("objective must be defined")  


# 実行コードへ展開(例:乱択探索で制約を満たす点を探す)  
return quote  
local _prob = MiniProblem($(esc(obj)), $(cons))  
local best = Inf  
local best_vars = Dict{Symbol,Float64}(:x=>0.0, :y=>0.0)  
for _ in 1:10_000  
# 単純なサンプル([-1,1]範囲)  
local x = 2rand() - 1  
local y = 2rand() - 1  
# 制約チェック(例として x + y <= 1 のみ対応)  
local ok = true  
for c in $cons  
# c が :(x + y <= 1) の形だと仮定  
if c.head == :(<=)  
local lhs, rhs = c.args[1], c.args[2]  
local val = eval(Expr(:block, :(x = $x), :(y = $y), lhs))  
ok &= (val <= rhs)  
end  
end  
if ok  
local val = eval_objective($(esc(obj)), Dict(:x=>x, :y=>y))  
if val < best  
best = val  
best_vars[:x] = x  
best_vars[:y] = y  
}  
end  
end  
(; best, best_vars)  
end  
end  


# 使用例(非常に単純化された仕様)  
res = @minimize begin  
objective = x^2 + y^2  
x + y <= 1  
end  


println(res)

本例は発想のデモであり、実務で使うには安全・汎用性・性能の観点から多くの強化が必要です。ポイントは「仕様を式として受け取り、実行可能な形へ自動展開する」流れです。より本格的には、MacroTools.jlなどのユーティリティやパーサ生成器、検証ロジックを組み合わせます。

* * *

# 7. 式変換の定石:パターン・再帰・postwalk風処理

複雑な式の部分書き換えには、再帰的にツリーを辿る処理が必要です。標準にはpostwalk等のユーティリティは含まれないため、自前で定義します。

# 部分式を再帰的に置換する簡易ユーティリティ  
function rewrite(ex::Any, f::Function)  
if ex isa Expr  
newargs = [rewrite(arg, f) for arg in ex.args]  
newex = Expr(ex.head, newargs...)  
return f(newex)  
else  
return f(ex)  
end  
end  


# 使用例:sin -> cos へ書き換え(デモ)  
ex = :(sin(x) + sin(y))  
ex2 = rewrite(ex) do node  
if node isa Expr && node.head == :call && node.args[1] == :sin  
Expr(:call, :cos, node.args[2])  
else  
node  
end  
end  


ex2 # :(cos(x) + cos(y))

式変換では「構造を壊さない」「型推論を阻害しない」ことが重要です。展開結果の@code_typedで型安定性を確認し、必要ならescやgensymを組み合わせます。

* * *

# 8. デバッグ・検証:@macroexpand・@code_lowered・@code_typed

メタプログラムは目に見えないコードを生成するため、展開結果の検証が不可欠です。

  * @macroexpand: マクロ展開結果のASTを表示(型推論前)。
  * @code_lowered: 下位中間表現(lowered)を表示。
  * @code_typed: 型付きSSAフォームを表示(推論後)。
  * @code_llvm・@code_native: より低レベルの最適化・アセンブリを観察。



@macroexpand @guard_nonneg z = -5  
@code_lowered sumtuple((1.0,2.0,3.0))  
@code_typed sumtuple((1.0,2.0,3.0))

展開結果が「意図通りで、型が安定している」ことを確認し、テストスイートへ最小の例を組み込みます。メタプログラムは小さく検証可能であることが品質の鍵です。

* * *

# 9. 実務的注意事項:副作用・安全・バージョン耐性

最後に、メタプログラミングを実務で運用する際の要点を記します。

  * 副作用最小化:@generatedは型だけへ依存し、外部状態を読みません。マクロも展開時の副作用を避け、生成コードへ明確に押し出します。
  * 安全な評価:evalの使用は最小限に。どうしても必要ならモジュールを明示し、入力式の検証(フィルタリング)を徹底します。
  * スコープの正確性:ユーザー式はescで呼び出し元へ返し、導入識別子はgensymで一意化します。
  * 可読性保持:ロジック本体は関数に置き、マクロは糖衣・ラッパに限定します。@generatedは狙い撃ちの最適化のみ。
  * バージョン耐性:AST構造やlowered表現はJuliaのバージョンで変わる可能性があります。MacroTools等の抽象化レイヤを使うと保守が楽になります。
  * 計測と検証:展開結果の性能はBenchmarkTools.@btime、型は@code_warntypeで確認し、CIに軽いメタ展開テストを含めます。



メタプログラミングは、仕様記述と最適化の橋渡しを担う強力な道具です。適切な衛生と検証のもと、最小限・高効果の変換を設計することで、表現力と性能を同時に押し上げられます。コードは「読みやすく、差し替えやすく、検証しやすく」を満たす構造へ保ち、メタレイヤはその補助に徹するのが長期運用の定石です。

* * *

# 第8章乱数と確率・統計:Random・Distributions・モンテカルロ(◇欄数種の管理)

# 1. 再現性の要と乱数の役割

乱数は、統計的推定、最適化、ベイズ推論、シミュレーション、数値積分など広範な場面で不可欠です。実務で最も重要なのは再現性です。乱数が関与する計算では、同じコード・同じ環境・同じ乱数種であれば、同じ結果が得られる設計にしなければ検証・比較・品質保証が破綻します。Juliaでは、乱数生成器(RNG)と乱数種(seed)を明示的に管理し、プロジェクト環境とともに固定することで、この要件を満たします。

  * 乱数生成器はRandom標準ライブラリで提供され、MersenneTwisterなどのジェネレータを選択できます。加えて、エコシステムにはXoshiro系やPCG系といった最新のジェネレータを利用可能なものもあります(採用は環境やパッケージ選択によります)。
  * 生成器はオブジェクトとして扱い、関数へ明示的に渡すのが原則です。これにより、状態の共有や意図しない使い回しを避け、再現性とテスト容易性が高まります。
  * 並列化(スレッド・プロセス)時は、各ワーカーに独立な生成器(かつ再現性のため固定可能なシード)を配布します。



以下では、ジェネレータの選択、乱数種の固定、分布からのサンプリング、推定・検定、モンテカルロ法の実装パターン、並列サンプリングまで、実用上の要点を具体例中心に整理します。

* * *

# 2. 乱数生成器の選択と基本操作

乱数生成器は、擬似乱数列の品質(周期、均一性、相関)と速度、メモリ使用量に関わります。Juliaでは、標準のRandomモジュールから代表的な生成器を利用できます。

  * MersenneTwister(seed): 長周期で古典的に広く使われるジェネレータ。再現性管理が容易で、配列サンプリング等に十分な品質。
  * Xoshiro系(例:Xoshiro256**など)やPCG系:速度・統計的品質に優れ、最新の実装が利用可能。選択はプロジェクトの要件(速度・品質・安定性)に応じます。環境によりパッケージ導入が必要な場合があります。



基本の使い方は以下のとおりです。

using Random  


# 1) グローバルの既定生成器の種を固定(手軽だが、並列・大規模では推奨しない)  
Random.seed!(1234)  
rand() # U(0,1) の一様乱数  
rand(1:6) # 1..6 の一様整数(サイコロ)  


# 2) 生成器をオブジェクトとして作り、明示的に渡す(推奨)  
rng = MersenneTwister(1234)  
rand(rng) # rng から生成  
rand(rng, 1:6)  


# 3) ベクトル・行列に対して  
rand(rng, 5) # 5要素ベクトル  
rand(rng, Float64, 2, 3) # 2×3 行列  


# 4) 生成器の複製(同じ状態から列を再現)  
rng2 = copy(rng)  
rand(rng, 3) # 3個生成  
rand(rng2, 3) # 同じ列が得られる(copy直後なら)

生成器を局所化して使う(引数で受け渡す)ことで、関数のテスト容易性と再現性が高まります。複雑な処理では、生成器をstructや設定オブジェクトに含めて管理するのが定石です。

* * *

# 3. 乱数種の固定と管理:テスト・検証・配布のために

乱数種の管理は再現性の要です。以下の原則を守ると、テストと運用が安定します。

  * 固定の入口を作る:スクリプト・ノートブック・関数の冒頭で、seedを明示的にセットします。
  * 生成器を渡す:グローバル既定生成器への依存を避け、rngを引数で渡します。
  * 状態を保存・復元:長期・比較実験では、生成器の状態を必要に応じて保存・復元します(copyや独自シリアライズ)。
  * 並列時の独立性:スレッドごと・プロセスごとに異なる種を割り当て、列の重複や相関を避けます(seed + threadid()等の方式)。



例:

using Random  


# 入口で種を固定  
const GLOBAL_SEED = 20240101  


function setup_rng(seed::Int=GLOBAL_SEED)  
return MersenneTwister(seed)  
end  


# 乱数を使う処理は常にrngを受け取る  
function simulate_once(rng::AbstractRNG; n=1000)  
# 例:単純なコイン投げの平均  
xs = rand(rng, 0:1, n) # 0/1 の一様  
return sum(xs) / n  
end  


rng = setup_rng()  
simulate_once(rng)  


# 状態の複製で同一列を再試行  
rng_a = setup_rng()  
rng_b = copy(rng_a)  
simulate_once(rng_a) # A  
simulate_once(rng_b) # A と同じ系列に基づく再現

注意:既定生成器(Random.GLOBAL_RNG)へRandom.seed!を多用すると、別の関数や並列タスクと状態を共有してしまい、意図しない相関や再現性崩れを招きます。実務では生成器の明示渡しを基本としてください。

* * *

# 4. 基本分布のサンプリング:一様・正規・指数・離散分布

確率分布からのサンプリングは、RandomとDistributions(エコシステムの代表的パッケージ)を組み合わせるのが一般的です。Distributionsを使うと、パラメトリックな分布をオブジェクトとして扱え、randでサンプルが得られます。

using Random  
using Distributions  


rng = MersenneTwister(42)  


# 連続分布  
dN = Normal(0.0, 1.0)  
x = rand(rng, dN) # N(0,1)  
xs = rand(rng, dN, 10_000) # 1万サンプル  


dE = Exponential(2.0) # rate=2  
y = rand(rng, dE)  


# 離散分布  
dB = Binomial(10, 0.3) # n=10, p=0.3  
k = rand(rng, dB)  


dP = Poisson(5.0)  
λk = rand(rng, dP)  


# 一様分布(区間)、整数一様  
dU = Uniform(-1.0, 1.0)  
z = rand(rng, dU)  
i = rand(rng, 1:6) # 整数一様(サイコロ)

混合分布やベクトル化も簡潔に扱えます。

d_mix = MixtureModel([Normal(-2,1), Normal(2,1)], [0.4, 0.6])  
xs = rand(rng, d_mix, 1000)

必要に応じてpdf/logpdf/cdf/quantileを用いて、評価・可視化・検証に使います。

pdf(dN, 0.0) # 密度  
cdf(dN, 1.96) # 累積分布  
quantile(dN, 0.975) # 上側2.5%点

* * *

# 5. 統計的推定の基礎:点推定・区間推定・簡易検定

サンプルから母数(平均・分散・尤度)を推定する処理は頻出です。ここでは最小限のツールで安全に扱う方法を示します。

## 5.1 点推定・区間推定

正規近似を前提とした母平均の95%信頼区間(分散既知/未知)を例示します。

using Statistics  


function ci_mean(xs::AbstractVector{<:Real}; level=0.95)  
n = length(xs)  
μ = mean(xs)  
σ = std(xs) # 不偏標準偏差  
# 正規近似のZ値(両側)  
α = 1 - level  
z = 1.959963984540054 # 0.975 の標準正規分位(必要ならquantile(Normal(), 0.975)で取得)  
half = z * σ / sqrt(n)  
return (μ - half, μ + half)  
end  


rng = MersenneTwister(0)  
xs = rand(rng, Normal(10.0, 2.0), 200)  
ci = ci_mean(xs)

より厳密な扱い(小標本)ではt分布を用いる方法もあります。分布依存の推定はDistributionsで分位を得れば、同様の形で実装できます。

## 5.2 簡易検定(例:1標本t検定)

標本平均がある値μ0と等しいかを検定します。

function ttest_one_sample(xs::AbstractVector{<:Real}, μ0::Real)  
n = length(xs)  
μ = mean(xs)  
s = std(xs) # 不偏標準偏差  
t = (μ - μ0) / (s / sqrt(n))  
# ここでは近似的に大標本でZとして扱う例(厳密にはt分布の分位を用いる)  
p_two = 2 * (1 - cdf(Normal(), abs(t)))  
return (t=t, p=p_two)  
end  


rng = MersenneTwister(1)  
xs = rand(rng, Normal(0.2, 1.0), 1000)  
res = ttest_one_sample(xs, 0.0)

検定の実務では、仮定・分布・検出力・多重比較・効果量など、設計上の論点が多数あります。Juliaでは、Distributionsや補助パッケージを組み合わせることで、より厳密な検定を構築できます。

* * *

# 6. モンテカルロ法の実装パターン:積分・打ち落とし法・重要度サンプリング

モンテカルロ法は、乱数に基づく近似計算の総称です。典型的な用途を3つ示します。

## 6.1 モンテカルロ積分(単純サンプリング)

区間[a,b]上の積分∫ f(x) dxを一様サンプリングで近似します。

function mc_integrate_1d(f, a::Real, b::Real; n::Int=100_000, rng=MersenneTwister(0))  
xs = rand(rng, Uniform(a, b), n)  
vals = f.(xs)  
return (b - a) * mean(vals)  
end  


mc_integrate_1d(x -> exp(-x^2), -1.0, 1.0)

分散を減らすには、分層サンプリング・重要度サンプリングへ進みます。

## 6.2 打ち落とし法(幾何学的近似)

単位円面積やπの近似で頻用される方法です。

function estimate_pi(n::Int; rng=MersenneTwister(0))  
xs = rand(rng, Uniform(-1, 1), n)  
ys = rand(rng, Uniform(-1, 1), n)  
inside = sum(@. xs^2 + ys^2 <= 1.0)  
return 4.0 * inside / n  
end  


estimate_pi(1_000_000)

## 6.3 重要度サンプリング(分散縮減)

目標の積分がE[g(X)]で、Xの分布p(x)に対し、別の提案分布q(x)からサンプルを取り、重みw = p/qで補正します。

using Distributions  


function importance_sampling(f, p::Distribution, q::Distribution; n::Int=100_000, rng=MersenneTwister(0))  
xs = rand(rng, q, n)  
w = pdf.(p, xs) ./ pdf.(q, xs)  
return mean(w .* f.(xs))  
end  


p = Normal(0, 1)  
q = Normal(0, 2) # tail を厚くして稀事象に強く  
res = importance_sampling(x -> x^2, p, q)

重要度サンプリングは、提案分布の選択が鍵で、支持範囲・尾の厚さ・計算容易性がトレードオフになります。

* * *

# 7. 大規模サンプリングの並列化:スレッド・分散と再現性

モンテカルロは独立試行の和が本質であり、並列化との相性が良好です。ただし、再現性を保つために生成器の管理を慎重に行います。

## 7.1 スレッド並列(Threads.@threads)

各スレッドに独立な生成器を配布し、重複を避けます。シードは規則的にずらして割り当てます。

using Random  
using Base.Threads  


function estimate_pi_threads(n::Int, rng_seed::Int=0)  
# スレッドごとに独立RNGを用意  
rngs = [MersenneTwister(rng_seed + i) for i in 1:nthreads()]  
counts = zeros(Int, nthreads())  
@threads for t in 1:nthreads()  
rng = rngs[threadid()]  
local_inside = 0  
# 各スレッドあたりの試行数  
m = n ÷ nthreads()  
xs = rand(rng, Uniform(-1,1), m)  
ys = rand(rng, Uniform(-1,1), m)  
@inbounds @simd for i in eachindex(xs)  
local_inside += (xs[i]^2 + ys[i]^2 <= 1.0)  
end  
counts[threadid()] = local_inside  
end  
inside = sum(counts)  
return 4.0 * inside / ( (n ÷ nthreads()) * nthreads() )  
end  


estimate_pi_threads(10_000_000, 1234)

注意点:

  * グローバル生成器の共有は避ける。rand()を直接呼ぶと、内部で共有状態に依存し競合や相関の原因となる。
  * 偽共有を避けるため、スレッドローカルの集計配列を用いて、最後にリダクションする。
  * 乱数種配置は仕様化し、ドキュメントへ明記しておく(将来の再現性と比較のため)。



## 7.2 分散(複数プロセス・複数マシン)

プロセス並列(Distributed)や複数ホストでは、プロセスID・ホストIDに基づいて種を割り当てます。生成器の初期化と結果の収集(リダクション)を、明確なプロトコルで記述します。ネットワーク越しのデータ転送や順序依存が再現性へ影響するため、実行順序に依存しない集計(可換・結合的な集計関数)を選ぶのが安全です。

* * *

# 8\. RNG品質のセルフチェック:分布・相関・ヒストグラム

生成器の品質検査は専用フレームワークが有効ですが、現場での軽いセルフチェックも役に立ちます。

  * 一様性の確認:[0,1]上でヒストグラムの偏りがないかを見る。
  * 自己相関の簡易検査:x[i]とx[i+k]の相関を確認し、顕著な構造がないかを見る。
  * 変換の安定性:一様乱数からのBox–Muller法などで正規乱数を作り、平均・分散・歪度・尖度が期待に近いか確認。



using Statistics  


function quick_uniform_check(rng::AbstractRNG; n=1_000_000, bins=50)  
xs = rand(rng, n)  
# ヒストグラムの相対頻度を粗く確認  
counts = zeros(Int, bins)  
@inbounds for x in xs  
b = clamp(Int(floor(x * bins)) + 1, 1, bins)  
counts[b] += 1  
end  
freqs = counts ./ n  
μ = mean(freqs)  
σ = std(freqs)  
return (; μ, σ, freqs)  
end  


rng = MersenneTwister(123)  
res = quick_uniform_check(rng)

本格的な検証には、連続・離散の統計的適合度検定、スペクトル解析、専用のテストスイートを用います。実務では「採用する生成器の品質が十分である」という前提を守りつつ、自分の処理の使い方(並列・サンプリング方式)で不自然な相関を誘発していないか確認する視点が重要です。

* * *

# 9. 応用例:ブートストラップ・ベイズ推論・モンテカルロ最適化

乱数の典型的応用を3つ示します。

## 9.1 ブートストラップ(再標本化)

母集団の分布が非既知でも、標本からの再標本化で推定量の分布を近似します。

using Random, Statistics  


function bootstrap_mean(xs::AbstractVector{<:Real}; B::Int=10_000, rng=MersenneTwister(0))  
n = length(xs)  
μs = Vector{Float64}(undef, B)  
@inbounds for b in 1:B  
idx = rand(rng, 1:n, n) # 置換ありサンプル  
μs[b] = mean(@view xs[idx])  
end  
return μs  
end  


rng = MersenneTwister(123)  
xs = rand(rng, Normal(0,1), 100)  
bs = bootstrap_mean(xs; rng=rng)  
# 区間推定などに用いる

## 9.2 ベイズ推論(乱数による事後分布近似の発想)

MCMCなど本格的な手法は専門パッケージに譲るとして、乱数サンプリングと重み付け(重要度サンプリング)で事後期待値を近似する考え方は、簡易モデルでも役に立ちます。前述の重要度サンプリングの枠組みで、尤度と事前分布を組み合わせて重みを構成します。

## 9.3 モンテカルロ最適化(確率的探索)

離散・連続の広域探索で、ランダム近傍・ランダムリスタート・焼きなましの素朴版など、乱数は探索の多様性を支えます。生成器の管理が探索再現性の鍵であり、同じ種・同じ設定値で結果が再現できる構造にしておくことが肝要です。

* * *

# 10. 運用・設計の指針:再現性・性能・保守性

最後に、乱数と確率・統計・モンテカルロを運用するための実務指針をまとめます(記述のみ)。

  * 生成器は明示渡し:rngを常に引数で受け取り、グローバル既定生成器への依存を避けます。
  * 種を仕様化:seedは設定値として外出しし、実験ログ・レポートに記録します。比較の再現が容易になります。
  * 並列の独立性:スレッド・プロセスごとに独立な生成器を用意し、種の割り当て規則を明文化します。重複・相関を避けます。
  * サンプリング契約:分布パラメータの妥当性(正の尺度・範囲)をチェックし、契約違反を早期に例外とします。
  * 分散縮減:単純サンプリングの分散が大きい問題では、分層・重要度・低差乱数(環境により採用)へ段階的に移行します。
  * 測定と検証:乱数の品質を簡易にチェックし、推定・検定の仮定(分布・独立性・標本数)を明示します。
  * 可読性の保持:統計的ロジックは関数化し、乱数生成は入口で一度に構成して渡す。DSL化・マクロ化は周辺の定型に留めます。
  * 成果物の再生成:乱数種・設定値・依存環境をプロジェクトに固定し、図・表・レポートはコードから一括生成します。



これらを徹底することで、Juliaの表現力と性能を活かしながら、乱数・確率・統計・モンテカルロを信頼性高く運用できます。実験の設計段階から「乱数の再現性」を最優先に据え、並列化・分布選択・分散縮減といった手段を適切に組み合わせることが、実務で成果を積み上げるための基本姿勢です。

* * *

# 第9章可視化の作法:Plots・Makieで伝わる図を作る(三角見せる数値)

# 1. 可視化の基本姿勢:伝えるための設計と再現性

可視化は「事実を誤りなく、簡潔に、比較可能に」伝えるための工程です。JuliaではPlots.jlとMakieが主要な選択肢で、前者は宣言的で簡潔、後者は高速・高機能・インタラクティブ表現に強みがあります。可視化設計では次の原則を徹底します。

  * 目的の明文化: 相関の把握、分布の形の確認、推移の比較、空間構造の提示など、図の目的を一行で明記します。
  * スケール選択: 線形/対数/シンメトリックなど、データの範囲と粒度に合う軸スケールを選びます。
  * マッピングの一貫性: 色・形・サイズ・透明度は意味を持たせ、一図内かつ図集全体で統一します。
  * 注釈と凡例: タイトル、軸ラベル、単位、凡例、注記は最小限で過不足なく。数式はLaTeXStringsで整えます。
  * 再現性: スタイルや乱数種、出力解像度、ファイル形式をコードで固定し、誰が実行しても同じ図が得られる状態にします。



以下、Plots.jlとMakieを用いた主要プロット、レイアウトとテーマ、相関・分布の表現、3D・インタラクティブ、論文品質への仕上げまでを具体例で示します。

* * *

# 2\. Plots.jlの基礎:ライン・散布・ヒストグラム・ヒートマップ

Plots.jlは統一APIで複数バックエンドを扱えます。まずは基本のプロットを押さえます。

using Plots  
# バックエンド(既定はGR)。必要なら明示指定:  
gr() # 代表例。plotlyjs() 等も選べます  


# ラインプロット  
x = 0:0.1:10  
y = @. sin(x) * exp(-0.1x)  
plot(x, y; xlabel="x", ylabel="sin(x)·exp(-0.1x)", label="decay", lw=2, color=:blue)  


# 散布図  
x2 = rand(1000); y2 = 0.5x2 .+ 0.1randn(1000)  
scatter(x2, y2; xlabel="x", ylabel="y", label="samples", ms=3, alpha=0.6)  


# ヒストグラム(密度正規化)  
histogram(y2; bins=50, normalize=:pdf, xlabel="y", ylabel="density", color=:orange)  


# ヒートマップ  
A = [sin(i/10) * cos(j/10) for i in 1:100, j in 1:100]  
heatmap(A; colorbar=true, c=:viridis, xlabel="j", ylabel="i", clims=(-1,1))

複数系列の重ね合わせ、サブプロットの基本も押さえます。

# 複数系列をまとめて  
plot(x, sin.(x); label="sin", lw=2)  
plot!(x, cos.(x); label="cos", lw=2, ls=:dash)  


# サブプロット(レイアウト)  
p1 = plot(x, sin.(x); label="sin")  
p2 = plot(x, cos.(x); label="cos")  
p3 = scatter(x, sin.(x) .+ 0.1rand(length(x)); label="noisy")  
plot(p1, p2, p3; layout=(3,1), size=(800,600))

保存はsavefigで行い、解像度や形式は用途に合わせます。

savefig("fig_line.png") # ラスター  
savefig("fig_line.pdf") # ベクター(論文向け)

* * *

# 3. テーマとレイアウト最適化:一貫したスタイルと紙面設計

図集全体の一貫性は、説得力を高めます。Plots.jlではthemeや既定値の上書きでスタイルを固定できます。

# ベーステーマの設定  
default(fontfamily="Helvetica", guidefont=12, legendfont=10, tickfont=10, lw=2, framestyle=:box)  


# カラーパレットを固定  
palette(:Dark2)  


# タイトル・注釈・凡例の位置  
plot(x, y; title="Damped sine", legend=:topright, annotation=(5,0.3, "peak"), xlim=(0,10), ylim=(-1,1))

複雑なレイアウトは@layoutで定義します。色分布図+断面プロットのような組版を簡潔に記述できます。

l = @layout [a{0.7w} b; c d]  
pA = heatmap(A; c=:viridis, colorbar=true)  
pB = plot(A[:,50]; label="col 50", color=:red)  
pC = plot(A[50,:]; label="row 50", color=:blue)  
pD = histogram(vec(A); bins=50, normalize=:pdf, label="density")  
plot(pA, pB, pC, pD; layout=l, size=(900,700))

軸リンクや補助線も活用します。

p1 = scatter(x2, y2; xlabel="x", ylabel="y", label="", alpha=0.5)  
p2 = histogram2d(x2, y2; nbins=(40,40), c=:magma, xlabel="x", ylabel="y")  
plot(p1, p2; layout=(1,2), link=:both)

LaTeX風表記を用いた数式の軸ラベルや注釈は、読者の理解を助けます。

using LaTeXStrings  
plot(x, sin.(x); xlabel=L"\theta\,[\mathrm{rad}]", ylabel=L"\sin(\theta)")

* * *

# 4. 相関・分布の可視化:散布・六角ビン・密度・相関行列

相関と分布はデータ理解の要です。オーバープロットを避け、構造を伝える図を選びます。

# 散布図+回帰線(簡易)  
x = rand(2000); y = 0.7x .+ 0.1randn(2000)  
p = scatter(x, y; ms=3, alpha=0.4, label="")  
# 最小二乗の直線を重ねる  
A = [ones(length(x)) x]; β = A \ y  
plot!(x, A*β; lw=2, color=:red, label="OLS")  


# 六角ビン  
hexbin(x, y; gridsize=40, c=:viridis, xlabel="x", ylabel="y", colorbar=true)  


# カーネル密度推定(1D)  
using StatsPlots  
density(y; xlabel="y", ylabel="density", label="KDE", color=:green)  


# 相関行列ヒートマップ  
using Statistics  
X = hcat(randn(1000), randn(1000), randn(1000) .+ 0.5randn(1000))  
C = cor(X)  
heatmap(C; xticks=(1:3, ["x1","x2","x3"]), yticks=(1:3, ["x1","x2","x3"]),  
c=:coolwarm, clims=(-1,1), colorbar=true, title="correlation")

大規模散布では透明度(alpha)、ビン化(hexbin/histogram2d)、輪郭線(contour)が有効です。因果の示唆や外れ値の可視化には注釈や選択的ハイライトを併用します。

* * *

# 5. 時系列と不確実性:リボン・移動統計・ログスケール

時系列は推移・周期性・不確実性の表現が重要です。

t = 1:300  
y = cumsum(0.1 .+ 0.05randn(length(t)))  
μ = [mean(y[max(1,i-10):min(end,i+10)]) for i in eachindex(t)]  
σ = [std(y[max(1,i-10):min(end,i+10)]) for i in eachindex(t)]  


# リボン(±σ帯)  
plot(t, μ; ribbon=σ, color=:steelblue, label="mean ± σ", xlabel="t", ylabel="value")  
plot!(t, y; color=:gray, alpha=0.4, label="raw")  


# ログスケール(指数的成長や広範囲)  
plot(t, abs.(y) .+ 1; yaxis=:log, xlabel="t", ylabel="log(value)", label="")

不確実性は帯(ribbon)、区間棒(errorbar)、箱ひげ(boxplot)などで表現します。比較では同一スケール・同一窓幅・同一色規則で統一します。

* * *

# 6. ヒートマップ・等高線:色スケール・正規化・色覚配慮

ヒートマップは色の選択が肝要です。連続量には知覚等間隔のパレット(viridis等)、符号対称にはcoolwarmなどを用います。

heatmap(A; c=:viridis, clims=(minimum(A), maximum(A)), colorbar=true)  
contour(A; levels=10, linewidth=1.0, color=:black)  


# 零中心のZスコア正規化例(視覚的比較に)  
Am = mean(vec(A)); As = std(vec(A))  
Az = @. (A - Am) / As  
heatmap(Az; c=:balance, clims=(-3,3), colorbar=true, title="z-normalized")

欠損値(missing)は色バー外の扱いになるため、前処理で補間・マスク化を検討します。色覚バリアフリーの配色(青→橙系など)を優先し、凡例に数値範囲を明記します。

* * *

# 7\. 3D可視化の実務:Makieでのlines・scatter・surface・volume

Makieは高機能3D・インタラクティブ可視化に強みがあります。まずは基本図形です。

using GLMakie # 3D/インタラクティブ。静的出力はCairoMakieも選べます  


# 3D曲線  
x = range(0, 10, length=500)  
lines(x, sin.(x), cos.(x); linewidth=2, color=:blue, axis=(type=Axis3, xlabel="x", ylabel="y", zlabel="z"))  


# 3D散布  
xs = rand(1000); ys = rand(1000); zs = rand(1000)  
scatter(xs, ys, zs; markersize=5, color=zs, colormap=:viridis)  


# サーフェス(等高面)  
u = range(-2, 2, length=200); v = range(-2, 2, length=200)  
z = [exp(-(ui^2 + vi^2)) for ui in u, vi in v]  
surface(u, v, z; colormap=:plasma, shading=true)  


# ボリュームレンダリング(例示、データは小規模)  
vol = rand(64,64,64)  
volume(vol; colormap=:inferno, transparency=true)

カメラ操作、ライティング、保存も重要です。

fig = Figure(resolution=(800,600))  
ax = Axis3(fig[1,1]; xlabel="x", ylabel="y", zlabel="z")  
lines!(ax, x, sin.(x), cos.(x))  
fig  
# 画像保存(PNG/SVG/PDF)  
save("fig_3d.png", fig)

3Dは視点で情報が変わるため、固定視点と回転可能なビューを併用し、主要断面や投影を並置すると理解が進みます。

* * *

# 8\. Makieのインタラクティブ:Observables・スライダ・リンク

インタラクティブは探索に強力です。Observablesで動的更新が可能です。

using GLMakie  


fig = Figure(resolution=(800,400))  
ax = Axis(fig[1,1]; xlabel="x", ylabel="y")  
x = range(0, 10, length=500)  
k = Node(1.0) # パラメータのObservable  
y = lift(k) do kk  
@. sin(kk*x)  
end  
lines!(ax, x, y, color=:blue)  


# スライダUI  
s = Slider(fig[2,1], range=0.5:0.1:3.0, startvalue=1.0, label="k")  
on(s.value) do v  
k[] = v  
end  


fig

軸リンク、ツールチップ、選択イベントも容易です。

ax2 = Axis(fig[1,2]; xlabel="x", ylabel="y")  
scatter!(ax2, x, rand(length(x)); markersize=6)  
hidedecorations!(ax2, grid=false)  


# ツールチップ(簡易)  
data = rand(100)  
ax3 = Axis(fig[2,2])  
sc = scatter!(ax3, 1:100, data)  
on(events(ax3).mouseposition) do mp  
# 実務では最近傍点を取り、注釈を移動する処理を組みます  
end

操作の記録(スライダ値、選択状態)をコードで保持すると、探索の再現性も確保できます。

* * *

# 9. 論文・レポート品質への仕上げ:解像度・形式・注釈・一貫性

仕上げでは、視認性・印刷適正・引用可能性を満たすよう整えます。

  * 解像度: ラスターは十分なDPI(例:300以上)を確保。ベクター形式(PDF/SVG)は線や文字の鮮明さが必要な図に適します。
  * サイズ: 柱幅・紙面幅に合わせたsize指定で、文字の大きさ・間隔・余白のバランスを調整します。
  * フォント: 表題/軸/凡例のフォントを統一。数式はLaTeXStringsで表記し、単位は角括弧[]に揃えます。
  * 注釈: 図中の注釈は最小限・簡潔に。外れ値や重要な領域は矢印や囲みで強調し、凡例で記号の意味を明示します。
  * 色覚配慮: 2~3系列の比較なら色+線種、複数系列では色覚安全パレット+形状(点マーカー)で識別します。
  * 書式: ファイル名・番号付け・キャプションを管理し、レポート生成(PDF/HTML)で自動挿入します。



# Plots: サイズ・フォント・エクスポート  
default(size=(800,600), guidefont=12, tickfont=10, legendfont=10)  
plot(x, y; title="Damped sine", xlabel="x [s]", ylabel="y [a.u.]", lw=2)  
savefig("fig1_damped_sine.pdf")

MakieではCairoMakieでベクター出力が容易です。

using CairoMakie  
fig = Figure(resolution=(600,400))  
Axis(fig[1,1]; xlabel="x [s]", ylabel="y [a.u.]")  
lines!(range(0,10,length=500), sin.(range(0,10,length=500)))  
save("fig2_lines.svg", fig)

* * *

# 10. 大規模データの可視化:過密回避・要約・多段表現

大量点・高解像度格子は、過密表示による誤読の原因になります。次を優先します。

  * ビン化: hexbin/histogram2dで密度を表現し、色バーで量を示す。
  * 間引き: 時系列は等間隔サンプリングではなく極値保持間引き(可視上の最大・最小)を使うと誤解が減る。
  * 要約: 尺度別の統計量(中央値・四分位)を示し、全体像を保ちます。
  * 多段表現: 概観(密度)+詳細(選択点の散布)を並置。インタラクティブではズーム連動で詳細図を更新。



# 2Dヒストグラム例  
x = randn(200_000)  
y = 0.5x .+ 0.2randn(200_000)  
histogram2d(x, y; nbins=(200,200), c=:magma, colorbar=true)

必要であれば、グリッドに集計後にheatmapで表示し、補間せずに離散パターンを見せます。透明度(alpha)で重なりを示す手も有効です。

* * *

# 11. 再現性の仕組み:スタイル固定・乱数種・出力パイプライン

図の再生成は品質保証の一部です。コードに次を組み込みます。

  * スタイル固定: default(...)やテーマ関数でフォント・色・線種を固定。
  * 乱数種: 試験用データやデモで乱数を使う場合はRandom.seed!(...)または生成器を明示渡し。
  * 出力パイプライン: 図番号・ファイル名・形式を一元管理し、レポート生成時に自動挿入。



using Random  
Random.seed!(20240101)  


# 図の一元生成  
function save_plot(p::Plots.Plot, name::String; dir="reports/figs")  
isdir(dir) || mkpath(dir)  
savefig(joinpath(dir, name))  
end  


p = plot(x, y; title="example")  
save_plot(p, "fig_example.pdf")

Makieでも同様にラッパを用意し、出力先・形式・解像度を統一します。

* * *

# 12. 実践レシピ:相関行列+各ペア散布、3Dサーフェス+断面

## 12.1 相関行列とペア散布の組版

using Plots, Statistics  


# データ  
X = hcat(randn(1000), randn(1000) .+ 0.5randn(1000), randn(1000) .+ 0.2randn(1000))  
names = ["x1","x2","x3"]  
C = cor(X)  


# 相関行列  
pC = heatmap(C; c=:coolwarm, clims=(-1,1),  
xticks=(1:3, names), yticks=(1:3, names),  
title="correlation matrix", colorbar=true)  


# ペア散布(上三角)  
p12 = scatter(X[:,1], X[:,2]; ms=3, alpha=0.4, xlabel="x1", ylabel="x2", label="")  
p13 = scatter(X[:,1], X[:,3]; ms=3, alpha=0.4, xlabel="x1", ylabel="x3", label="")  
p23 = scatter(X[:,2], X[:,3]; ms=3, alpha=0.4, xlabel="x2", ylabel="x3", label="")  


l = @layout [a{0.4h}; b c d]  
plot(pC, p12, p13, p23; layout=l, size=(900,900))  
savefig("fig_pairs.pdf")

## 12.2 3Dサーフェスと断面の統合(Makie)

using CairoMakie  


u = range(-3, 3, length=200)  
v = range(-3, 3, length=200)  
z = [sin(√(ui^2 + vi^2)) / (1 + ui^2 + vi^2) for ui in u, vi in v]  


fig = Figure(resolution=(900,600))  
ax3 = Axis3(fig[1,1]; xlabel="u", ylabel="v", zlabel="z")  
surface!(ax3, u, v, z; colormap=:viridis, shading=true)  


# 断面(v=0)  
ax2 = Axis(fig[1,2]; xlabel="u", ylabel="z")  
plot!(ax2, u, z[:, findfirst(==(0.0), round.(v; digits=1))]; color=:red, label="v=0")  


# 断面(u=0)  
ax2b = Axis(fig[2,2]; xlabel="v", ylabel="z")  
plot!(ax2b, v, z[findfirst(==(0.0), round.(u; digits=1)), :]; color=:blue, label="u=0")  


fig  
save("fig_surface_sections.pdf", fig)

* * *

# 13. 運用上の注意:凡例の簡潔化・注釈の最小化・尺度の誤解防止

最後に実務で頻出する落とし穴に触れておきます(記述のみ)。

  * 凡例の過多: 系列が多い場合、凡例を図外に出す/短縮名を使う/説明はキャプションへ移すなどで視認性を保ちます。
  * 注釈の肥大化: 図中に文章を詰め込まない。矢印や囲みは最小限、詳細は本文・キャプションへ。
  * 軸にゼロを含めない: バーや面積の比較では軸のゼロ省略が誤解を招きます。必要に応じゼロを含む表示に。
  * 色スケールの不一致: 図間比較ではclimsを固定し、色バーの範囲を統一します。
  * 透明度での偽の重なり: 高透明度は暗い背景で誤解を生むことがあります。背景とライン色のコントラストを再確認します。
  * 3Dの歪み: 3Dでは視点・パースで量感が歪み得ます。2Dの断面・投影を併置し、誤解を減らします。



これらを抑えることで、Plots.jlとMakieを用いた可視化は、短く書いて速く、かつ論文・レポート品質に耐える「見せる数値」へと仕上がります。

* * *

# 第10章データ処理:DataFrames・CSV・Arrowで整える(丸前処理大全)

# 1. 方針と再現可能な前処理の設計

データ処理は「正しく読み、正しく整え、正しく検証し、同じ処理が何度でも再現できるようにする」ことが最優先です。JuliaのDataFrames.jl、CSV.jl、Arrow.jlは、テーブルデータの取り込みから変換・欠損処理・結合・集計・出力までを一貫して支える生態系です。ここでは以下の設計原則を徹底します。

  * 明示的なスキーマ: 列名・型・欠損の扱いをコードで規定し、暗黙の自動推論に依存しすぎないようにします。
  * 関数化・段階化: 前処理を小さな純粋関数へ分解し、引数で入出力を受け渡すことでテスト容易性と再現性を高めます。
  * 検証内蔵: 各ステップに検証(行数・キー重複・値域・欠損率)を組み込み、異常を早期に検出します。
  * カラム指向フォーマットの活用: CSVでの取り込み後、長期保管・高速再読込にはArrowを活用し、ゼロコピー・列指向の利点を得ます。
  * メモリ効率: 不要列の早期削除、カテゴリ化、インプレース更新、文字列のプーリング活用でアロケーションを抑えます。



以降、具体的なコードとパターンで実践的に説明します。必要なパッケージを先に読み込みます。

using DataFrames  
using CSV  
using Arrow  
using Dates  
using Statistics

* * *

# 2. 取り込みの基本:CSVから正しく読む

最も一般的な入口はCSVです。CSV.readはファイルを解析し、DataFrameへ変換します。型や日付の書式、欠損表現を明示すると、後工程が安定します。

# 取り込みの基本  
df = CSV.read("data/raw/sales_2024.csv", DataFrame;  
header=true, # 1行目をヘッダとして扱う  
delim=',', # 区切り文字(既定は',')  
missingstring="NA", # 欠損の文字表現  
dateformat="yyyy-mm-dd") # 日付文字列の書式

列ごとの型を明示する場合はtypesを使います。自動推論に任せると、混在データで意図と異なる型になることがあります。

schema = Dict(  
"date" => Date,  
"store_id" => Int,  
"sku" => String,  
"units" => Int,  
"price" => Float64  
)  
df = CSV.read("data/raw/sales_2024.csv", DataFrame; types=schema, dateformat="yyyy-mm-dd")

巨大ファイルを全読みせずに行を逐次処理したい場合は、CSV.Fileを使ってイテレータとして扱えます(必要に応じて集計だけ行い、素材のDataFrame化を避けてメモリ節約)。

f = CSV.File("data/raw/sales_2024.csv"; dateformat="yyyy-mm-dd", missingstring="NA")  
total_units = 0  
for row in f  
total_units += row.units  
end

列選択は取り込み時に行うと効率的です。不要列を後で落とすより、早い段階で取り込まないことが最善です。

df = CSV.read("data/raw/sales_2024.csv", DataFrame;  
select=["date", "store_id", "sku", "units", "price"])

* * *

# 3. 初期検査:形状・型・欠損・基本統計

取り込み直後に、全体の形・型・欠損の分布を把握します。後工程でのエラーを未然に防げます。

# 形状・型の確認  
size(df) # (行数, 列数)  
names(df) # 列名のベクトル  
eltype.(eachcol(df)) # 列ごとの要素型  


# 概要統計(数値列中心)  
describe(df)

欠損の可視化は簡便に列ごとの欠損率を算出します。

# 欠損率の算出  
function missing_ratio(df::DataFrame)  
ratios = map(col -> count(ismissing, col) / length(col), eachcol(df))  
return DataFrame(name=names(df), miss_ratio=ratios)  
end  


missing_ratio(df)

バリデーションの一例として、キーの重複・値域チェックを入れます。

# キー重複(store_id, sku, dateの三つ組で一意か)  
g = groupby(df, [:store_id, :sku, :date])  
dup = combine(g, nrow => :n)  
filter(:n => >(1), dup) # 1より大きければ重複  


# 値域チェック(unitsは非負、priceは正)  
@assert all(@. df.units >= 0) "unitsに負の値があります"  
@assert all(@. df.price > 0) "priceに0以下の値があります"

* * *

# 4. 型変換:文字列からDate/DateTime、数値への安全な変換

日付文字列のパターンが複数混在していることがあります。変換関数を用意し、安全にmissingへフォールバックします。

# 安全にDateへ変換(複数パターン対応)  
function parse_date_safe(s::AbstractString)  
for fmt in ("yyyy-mm-dd", "dd/mm/yyyy", "yyyy/mm/dd")  
try  
return Date(s, dateformat"$(fmt)")  
catch  
end  
end  
return missing  
end  


df.date = map(s -> s === missing ? missing : parse_date_safe(s), df.date)

数値変換も同様に安全化します。

# 文字列の数値を安全にFloat64へ  
function parse_float_safe(x)  
if x === missing  
return missing  
elseif x isa AbstractString  
try  
return parse(Float64, replace(x, ',' => '')) # 千区切り除去  
catch  
return missing  
end  
else  
return Float64(x)  
end  
end  


df.price = parse_float_safe.(df.price)

* * *

# 5. 欠損処理:削除・代入・宣言的制御

欠損の扱いは、削除(完全行のみ)、代入(中央値・平均・前方/後方補間)、**構造化(欠損フラグ列の付与)**の選択があります。目的に応じて使い分けます。

# 欠損行の削除(指定列のみ)  
df_clean = dropmissing(df, [:units, :price])  


# 一括削除(全列、データ損失に注意)  
df_complete = dropmissing(df)  


# 欠損代入(価格はカテゴリごとの中央値で置き換え)  
g = groupby(df, :sku)  
df_filled = combine(g) do sdf  
med = median(skipmissing(sdf.price))  
sdf.price = coalesce.(sdf.price, med)  
sdf  
end  


# 欠損フラグの追加(モデル入力用に有用)  
df.is_missing_price = ismissing.(df.price)

恒久的に欠損を禁止し、型からMissingを外すときはdisallowmissing!を使います(欠損が残っているとエラー)。事前にすべて埋めてから適用します。

# 欠損が残っていないことを確認したうえで  
@assert count(ismissing, df.price) == 0  
disallowmissing!(df, :price) # :price列の型からMissingを除去

* * *

# 6. クリーニング:文字列の正規化・カテゴリ化・重複処理

文字列は余分な空白・大小文字・全半角混在などの揺れが性能や品質に影響します。正規化を関数化して適用します。

# 文字列正規化の例  
normalize_str(s) = s === missing ? missing :  
strip(replace(lowercase(s), r"\s+" => " ")) # 余分空白を1個へ  


df.sku = normalize_str.(df.sku)  
df.store_id = df.store_id # 数値はそのまま

カテゴリ化は、繰り返しが多い文字列を効率的に保持し、比較・結合を高速化します。

using CategoricalArrays  


df.sku = categorical(df.sku) # Pooledカテゴリ(メモリ効率)  
df.store_id = categorical(df.store_id) # 必要に応じて

重複行の処理は、キーに基づいて代表行を選ぶルール(最新日付・最大unitsなど)を明示します。

# 同じ(store_id, sku, date)で複数行ある場合、unitsが最大の行を採用  
g = groupby(df, [:store_id, :sku, :date])  
df_dedup = combine(g) do sdf  
sdf[argmax(sdf.units), :] # 最も売れた行を採用  
end

* * *

# 7. 列操作とインプレース更新:select!/transform!/combine

DataFrames.jlの基本は「列を選ぶ、作る、要約する」の3系統です。インプレース版(末尾に!)はメモリ効率に優れます。

# 列の選択・並べ替え・改名(インプレース)  
select!(df, [:date, :store_id, :sku, :units, :price])  
rename!(df, Dict(:units => :qty, :price => :unit_price))  


# 新列の作成・変換(ByRowで行単位処理)  
transform!(df, [:qty, :unit_price] => ByRow((q, p) -> q * p) => :amount)  


# 既存列の正規化(Zスコア)  
μ = mean(df.amount); σ = std(df.amount)  
transform!(df, :amount => ByRow(x -> (x - μ) / σ) => :amount_z)

関数の引数に選択子(Not, Between, Cols)を使うと柔軟な指定が可能です。

# 数値列すべてを標準化して別名で追加  
num_cols = names(df, Number)  
transform!(df, num_cols .=> ByRow(x -> (x - mean(df[!, num_cols])) / std(df[!, num_cols])) .=> (c -> Symbol(c, :_z)))

(上の例は概念的です。実務では列ごとの平均・分散を事前に計算し、列専用に適用してください。)

* * *

# 8. 結合:joinファミリで外部データと統合

複数テーブルの統合は、キーの型・正規化・欠損の扱い、結合種(inner, left, right, outer, semi, anti)の選択が重要です。

# 参照テーブルの読み込み  
df_sku = CSV.read("data/ref/sku_master.csv", DataFrame)  
df_store = CSV.read("data/ref/store_master.csv", DataFrame)  


# キー正規化(文字列なら正規化関数を適用)  
df_sku.sku = normalize_str.(df_sku.sku)  


# 左外部結合(販売データにマスタ属性を付与)  
df = leftjoin(df, df_sku, on=:sku)  
df = leftjoin(df, df_store, on=:store_id)  


# 結合後の欠損チェック(参照にないキーの検出)  
missing_rows = filter(:category => ismissing, df)

半結合(semi)は、左テーブルの行で右テーブルにキーが存在するものだけを残し、反結合(anti)は存在しないものだけを抽出します。参照整合性の検査に有用です。

semi = semijoin(df, df_sku, on=:sku) # skuがマスタにある行  
anti = antijoin(df, df_sku, on=:sku) # skuがマスタにない行(要対処)

* * *

# 9. 集計:groupby/combineでロバストな要約

グループ集計は、粒度(キー)と指標(合計・平均・中央値・分散など)を明示します。複数指標は=>を並べて指定できます。

# 店舗×SKUの月次合計  
df.date_month = Date.(firstdayofmonth.(df.date)) # 月の先頭へ丸め  
g = groupby(df, [:store_id, :sku, :date_month])  
agg = combine(g,  
:qty => sum => :qty_sum,  
:amount => sum => :amount_sum,  
:amount => mean => :amount_mean,  
nrow => :n  
)  


# 比率・単価の派生  
transform!(agg, [:amount_sum, :qty_sum] => ByRow((a, q) -> q == 0 ? 0.0 : a / q) => :avg_unit_price)

ピボット(列↔行の入れ替え)は、長いデータのままが基本ですが、レポート用途で必要ならunstack/stackを使います。

# SKUを列へ展開(店舗×月の表にSKU別合計を並べる)  
wide = unstack(agg, [:store_id, :date_month], :sku, :qty_sum)

* * *

# 10. 時系列の前処理:ソート・欠損補間・移動統計

時系列は順序が重要です。ソート・連続性の検査・簡単な補間・移動統計を備えます。

sort!(df, [:store_id, :sku, :date]) # ソート  


# 連続日チェック(ギャップ検出)  
function detect_gaps(sdf::DataFrame)  
d = sdf.date  
gap_idx = findall(i -> i > 1 && d[i] != d[i-1] + Day(1), 2:length(d))  
return gap_idx  
end  


gap_rows = combine(groupby(df, [:store_id, :sku])) do sdf  
DataFrame(gap_index=detect_gaps(sdf))  
end  


# 移動平均(窓幅wの中心移動平均)  
function moving_mean(v::AbstractVector{<:Real}, w::Int)  
n = length(v); half = (w - 1) ÷ 2  
out = similar(v, Float64)  
@inbounds for i in 1:n  
lo = max(1, i - half); hi = min(n, i + half)  
out[i] = mean(@view v[lo:hi])  
end  
return out  
end  


df.qty_ma7 = combine(groupby(df, [:store_id, :sku])) do sdf  
DataFrame(qty_ma7=moving_mean(sdf.qty, 7))  
end |> x -> vcat(x..., cols=:union) |> _.qty_ma7 # 列の結合(概念)

(実務では、各グループへtransformで一括適用するヘルパー関数を用意すると簡潔に書けます。)

* * *

# 11. メモリ効率の工夫:不要列削除・インプレース・カテゴリ化・文字列プーリング

大規模データでは、不要列の早期削除とインプレース更新が最も効きます。

# 早期に不要列を落とす  
select!(df, [:date, :store_id, :sku, :qty, :unit_price, :amount])  


# 列の更新はtransform!で(新列追加もインプレース)  
transform!(df, :amount => ByRow(x -> clamp(x, 0, typemax(Float64))) => :amount)  


# 文字列のカテゴリ化でメモリ削減(skuの重複が非常に多い場合)  
df.sku = categorical(df.sku)

CSV.jlは内部で文字列プーリングを活用し、同じ文字列を共有してメモリ削減を図ります。取り込み後に大量のString変換を行うと、この利点が失われるため、不要なコピーを避けます(文字列をすぐに置換・結合するより、正規化後にカテゴリ化する方が効率的です)。

* * *

# 12. カラム指向フォーマット:Arrowで高速・再現性・相互運用

Arrow.jlは列指向フォーマットへの入出力を提供します。ゼロコピーに近い再読込・他言語との相互運用(Python/R/Java/Go等)が容易です。

# DataFrameをArrowへ書き出し  
Arrow.write("data/processed/sales.arrow", df)  


# Arrowからの高速読み込み(ゼロコピー的)  
tbl = Arrow.Table("data/processed/sales.arrow")  
df2 = DataFrame(tbl) # 必要ならDataFrameへ

複数テーブルをまとめて書くことも可能です(読み出し時に選択)。再読込の高速性と確定スキーマにより、検証可能な前処理パイプラインの中間生成物として最適です。

* * *

# 13. 前処理パイプラインの設計:関数分割・検証・ログ

再現可能な前処理は、入口→正規化→欠損処理→結合→派生→検証→出力を関数に分割し、各関数に検証を内蔵します。

# 入口  
function load_sales(path::AbstractString)  
CSV.read(path, DataFrame; header=true, missingstring="NA", dateformat="yyyy-mm-dd")  
end  


# 正規化  
function normalize_sales(df::DataFrame)  
df.sku = normalize_str.(df.sku)  
df.date = Date.(df.date) # 書式が統一されている前提  
df  
end  


# 欠損処理  
function fill_missing_price!(df::DataFrame)  
g = groupby(df, :sku)  
combine(g) do sdf  
med = median(skipmissing(sdf.unit_price))  
sdf.unit_price = coalesce.(sdf.unit_price, med)  
sdf  
end |> (x -> df .= vcat(x..., cols=:union)) # インプレース反映(概念)  
df  
end  


# 結合  
function attach_master!(df::DataFrame, df_sku::DataFrame, df_store::DataFrame)  
leftjoin!(df, df_sku, on=:sku)  
leftjoin!(df, df_store, on=:store_id)  
df  
end  


# 派生  
function derive_amount!(df::DataFrame)  
transform!(df, [:qty, :unit_price] => ByRow(* ) => :amount)  
df  
end  


# 検証  
function validate!(df::DataFrame)  
@assert all(@. df.qty >= 0)  
@assert all(@. df.unit_price > 0)  
df  
end  


# 出力(Arrow)  
function save_arrow(df::DataFrame, path::AbstractString)  
Arrow.write(path, df)  
end

(上のcombine→vcatは概念的なインプレース反映の例です。実装では戻り値のDataFrameを返す関数形に揃える方が簡潔です。)

実務では、ログ(何行読み、何列残し、欠損率、検出した重複数)を記録し、レポート生成に含めると品質保証に役立ちます。

* * *

# 14. 検証可能性:不変条件・キー整合・数値的妥当性

前処理で破ってはいけない不変条件をコード化します。

# 不変条件の一例  
function invariants(df::DataFrame)  
@assert eltype(df.qty) <: Integer  
@assert eltype(df.unit_price) <: AbstractFloat  
@assert count(ismissing, df.qty) == 0  
@assert count(ismissing, df.unit_price) == 0  
@assert all(@. df.amount >= 0)  
df  
end  


# キー整合の検査  
function check_keys(df::DataFrame, df_sku::DataFrame)  
anti = antijoin(df, df_sku, on=:sku)  
@assert nrow(anti) == 0 "未知SKUがあります"  
df  
end

数値的妥当性(極端な外れ値・単位・桁)も検査します。

# 極端値の検出(価格の上位1%)  
q99 = quantile(df.unit_price, 0.99)  
outliers = filter(:unit_price => >(q99), df)  


# 単位・桁の整合(例:amountはqty×unit_priceに一致するか)  
@assert all(@. isapprox(df.amount, df.qty * df.unit_price; rtol=1e-12, atol=1e-12))

* * *

# 15. 書き出し:CSV/Arrowの選択とエクスポート設計

出力形式は用途によって使い分けます。

  * CSV: 人間可読・他ツール連携に広く使える。解像度や型情報の保持は限定的。
  * Arrow: スキーマを保ちながら高速に読み書き。中間生成物・長期保管・相互運用に適する。



# CSVへ書き出し(列順とヘッダを固定)  
CSV.write("reports/clean/sales_clean.csv", df)  


# Arrowへ書き出し(中間成果物として)  
Arrow.write("data/processed/sales.arrow", df)

* * *

# 16. 大規模データの実務:部分読み・段階出力・分割処理

メモリに収まらないデータでは、以下の戦略を取ります。

  * 部分読みと段階出力: 期間・店舗単位でファイルを分割し、処理後に結合。
  * 逐次集計: CSV.Fileを用いて行イテレータで走査し、グループ集計だけ行って出力。
  * 列選択・早期削除: 取り込み時に必要列のみ選ぶ、処理初期に不要列を落としてメモリを確保。



# 期間で分割読み(例:月ごとにファイルがある)  
months = Date(2024,1):Month(1):Date(2024,12)  
agg_all = DataFrame(store_id=Int[], sku=String[], date_month=Date[], qty_sum=Int[], amount_sum=Float64[])  


for m in months  
path = "data/raw/sales_$(Dates.month(m)).csv"  
dfm = CSV.read(path, DataFrame; dateformat="yyyy-mm-dd", missingstring="NA", select=["date", "store_id", "sku", "units", "price"])  
rename!(dfm, Dict(:units => :qty, :price => :unit_price))  
dfm.date_month = Date.(firstdayofmonth.(dfm.date))  
g = groupby(dfm, [:store_id, :sku, :date_month])  
agg = combine(g, :qty => sum => :qty_sum, [:qty, :unit_price] => ByRow((q, p) -> q * p) => :amount)  
agg = combine(groupby(agg, [:store_id, :sku, :date_month]), :qty_sum => sum, :amount => sum => :amount_sum)  
append!(agg_all, agg, promote=true)  
end  


CSV.write("reports/agg/sales_monthly.csv", agg_all)

* * *

# 17. 監査とドキュメント化:処理履歴・スキーマ・依存環境

前処理の品質保証には、処理履歴のログ(読込件数、削除件数、欠損代入件数、結合で失われた行数)、スキーマ定義(列名・型・許容値域)、依存環境(Project.toml内のパッケージ・バージョン)を成果物に添えることが重要です。これにより、将来の再現・比較・監査が容易になります。

# スキーマ定義(コード内・別ファイルでも)  
schema = DataFrame(  
name = ["date", "store_id", "sku", "qty", "unit_price", "amount"],  
type = ["Date", "Int", "String/Categorical", "Int", "Float64", "Float64"],  
rule = ["yyyy-mm-dd", ">=0", "normalized", ">=0", ">0", "qty*unit_price"]  
)  
CSV.write("reports/docs/schema.csv", schema)

* * *

# 18. 総合例:入口から出力までを一気通貫で(スクリプト骨子)

最後に、ここまでの要点をまとめた骨子スクリプトの例を示します。プロジェクトのsrc/pipeline.jl等に置き、\--project=.で起動します。

using DataFrames, CSV, Arrow, Dates, Statistics, CategoricalArrays  


normalize_str(s) = s === missing ? missing : strip(replace(lowercase(s), r"\s+" => " "))  
function load_sales(path::AbstractString)  
CSV.read(path, DataFrame; header=true, missingstring="NA", dateformat="yyyy-mm-dd")  
end  


function process_sales(input_csv::AbstractString, sku_csv::AbstractString, store_csv::AbstractString, output_arrow::AbstractString)  
df = load_sales(input_csv)  
# 整形  
rename!(df, Dict(:units => :qty, :price => :unit_price))  
df.sku = normalize_str.(df.sku)  
# 欠損処理  
g = groupby(df, :sku)  
df = combine(g) do sdf  
med = median(skipmissing(sdf.unit_price))  
sdf.unit_price = coalesce.(sdf.unit_price, med)  
sdf  
end  
# 結合  
df_sku = CSV.read(sku_csv, DataFrame)  
df_store = CSV.read(store_csv, DataFrame)  
df.sku = normalize_str.(df.sku); df_sku.sku = normalize_str.(df_sku.sku)  
df = leftjoin(df, df_sku, on=:sku)  
df = leftjoin(df, df_store, on=:store_id)  
# 派生  
transform!(df, [:qty, :unit_price] => ByRow(* ) => :amount)  
# 検証  
@assert all(@. df.qty >= 0)  
@assert all(@. df.unit_price > 0)  
@assert all(@. df.amount >= 0)  
# 不要列削除・カテゴリ化  
select!(df, [:date, :store_id, :sku, :qty, :unit_price, :amount, :category])  
df.sku = categorical(df.sku)  
# 出力  
Arrow.write(output_arrow, df)  
return df  
end  


df_final = process_sales("data/raw/sales_2024.csv", "data/ref/sku_master.csv", "data/ref/store_master.csv", "data/processed/sales.arrow")

この骨子に、前述の検証・ログ・集計・レポート生成(CSVや可視化)を重ねていけば、「前処理大全」としての実務に耐える再現可能なパイプラインが完成します。重要なのは、型・欠損・結合・集計・検証を明示し、インプレース更新と列指向フォーマットで効率化することです。これらの原則をコードへ織り込むことで、データ品質と処理性能の両立が可能になります。

* * *

# 第11章微分方程式と数値解析:DifferentialEquations・積分・根探し

# 1. 設計指針:問題の記述、アルゴリズム選択、検証可能性

微分方程式・数値積分・根探しは「仕様の明確化→適切な数値法の選択→誤差と妥当性の検証→再現可能な実行」の流れで設計します。JuliaではDifferentialEquations.jlで常微分方程式(ODE)・確率微分方程式(SDE)・イベント検知・剛性問題に対応でき、QuadGK.jlで高精度の一次元積分、NLsolve.jlで非線形方程式の解法が利用できます。基本方針は次のとおりです。

  * 仕様の記述: 状態変数、パラメータ、初期条件、区間、必要な出力粒度(saveat)、検証のための不変量・物理制約。
  * 数値法の選択: 非剛性⇔剛性、非線形の強さ、ノイズの有無、イベントの有無、精度要件に応じて明示的/陰的・次数・誤差制御の有無を選択。
  * 誤差の管理: reltol/abstol、ステップ制御、参考解との比較、収束テスト、単体テスト(不変量、境界条件、保存則)。
  * 再現性: 乱数種、パラメータ、バージョン、出力粒度、ファイル形式の固定。コード内に検証ロジックを内蔵します。



* * *

# 2\. ODEの基本:問題定義、解法、出力の取り扱い

基本的な1次の常微分方程式はODEProblem(f, u0, tspan, p)で記述し、solveで解きます。ここでは指数減衰+正弦外力の例を示します。

using DifferentialEquations  


# 右辺関数:du/dt = -α*u + β*sin(ω*t)  
function f!(du, u, p, t)  
α, β, ω = p  
du[1] = -α*u[1] + β*sin(ω*t)  
end  


u0 = [0.0]  
tspan = (0.0, 10.0)  
p = (0.5, 1.0, 2.0π)  


prob = ODEProblem(f!, u0, tspan, p)  


# 非剛性向けの高精度・誤差制御つき解法(5次)  
sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10, saveat=0.01)  


# 補間による連続出力  
u_at_7p5 = sol(7.5) # t=7.5 の補間値  
u_grid = sol[1, :] # 保存タイムステップの第1成分  
t_grid = sol.t

実務では、出力粒度(saveat)を要件に合わせて固定し、後工程(集計・可視化・比較)の再現性を確保します。補間sol(t)は便利ですが、比較時には同一グリッドで評価するのが安全です。

* * *

# 3. 剛性の扱い:症状、選択方針、陰的法

剛性(stiffness)は、急激に変化する成分が含まれているため、明示的法では極端に小さいステップを強制される状態です。症状としては「ステップが極端に縮む」「非剛性法で発散または非現実的な解」「緩慢な成分の時間スケールに比して過剰な計算量」などが挙げられます。剛性が疑われる場合は陰的法(Rosenbrock系、BDF系など)を選びます。

# 剛性例:van der Pol の強制振動(μ が大きいと剛性傾向)  
function vdp!(du, u, p, t)  
μ = p  
du[1] = u[2]  
du[2] = μ*(1 - u[1]^2)*u[2] - u[1]  
end  


u0 = [2.0, 0.0]  
tspan = (0.0, 30.0)  
μ = 100.0 # 剛性が強い設定  


prob = ODEProblem(vdp!, u0, tspan, μ)  


# 剛性向け陰的法(Rosenbrock、BDFなど)  
sol_stiff = solve(prob, Rodas5(); reltol=1e-8, abstol=1e-10, saveat=0.01)

陰的法は1ステップで非線形方程式を解く必要があり、計算コストは上がりますが、安定性向上による総計算量の削減が見込めます。剛性の判定は自動で完全には行えないため、問題の性質・パラメータ・経験則に基づき選択してください。

* * *

# 4. イベント検知:連続/離散コールバックの設計

イベント(境界到達、符号反転、衝突など)がある系では、コールバックで条件式と作用を記述します。例として「高さが0に到達したら速度を反転・減衰させる」簡易跳ね返りモデルを示します。

using DifferentialEquations  


# 状態:u = [height, velocity]  
g = 9.81  
c = 0.8 # 反発係数  


function fall!(du, u, p, t)  
du[1] = u[2]  
du[2] = -g  
end  


# 連続コールバック:高さが0に到達するゼロ交差を検知  
condition(u, t, integrator) = u[1] # 0 を跨ぐ  
function affect!(integrator)  
integrator.u[1] = 0.0  
integrator.u[2] = -c * integrator.u[2]  
end  
cb = ContinuousCallback(condition, affect!; rootfind=true)  


u0 = [10.0, 0.0]  
prob = ODEProblem(fall!, u0, (0.0, 5.0))  
sol = solve(prob, Tsit5(); callback=cb, reltol=1e-9, abstol=1e-12, saveat=0.01)

イベントの検知では、rootfind=trueで正確な到達時刻へステップを調節可能です。離散イベント(特定時刻やステップごと)はDiscreteCallbackで表現します。コールバック内では物理的制約と整合性(高さを負にしない、エネルギーの扱い)を保つよう更新します。

* * *

# 5. パラメータ推移と感度:パラメータ掃引、補間比較、指標化

パラメータ依存を評価するときは、同一グリッドでの指標比較(最大値、到達時刻、L2距離)を使います。

function peak_value(sol)  
maximum(sol[1, :])  
end  


params = [0.2, 0.5, 1.0, 2.0]  
peaks = Float64[]  


for α in params  
p = (α, 1.0, 2.0π)  
prob = ODEProblem(f!, [0.0], (0.0, 10.0), p)  
sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10, saveat=0.01)  
push!(peaks, peak_value(sol))  
end

補間値sol(t)で連続関数として扱えますが、比較は同一時刻集合で行うのが確実です。必要に応じてsaveatで統一グリッドを指定します。

* * *

# 6\. SDE(確率微分方程式):ノイズの扱い、再現性、並列化

SDEは確率過程を含む系をモデル化します。例として幾何ブラウン運動(GBM)を示します。

using DifferentialEquations, Random  


# dX = μ*X*dt + σ*X*dW  
function f_sde!(du, u, p, t)  
μ, σ = p  
du[1] = μ*u[1]  
end  
function g_sde!(du, u, p, t)  
μ, σ = p  
du[1] = σ*u[1]  
end  


u0 = [1.0]  
tspan = (0.0, 1.0)  
p = (0.1, 0.2)  


# 乱数生成器の固定(再現性)  
rng = MersenneTwister(1234)  


prob = SDEProblem(f_sde!, g_sde!, u0, tspan, p)  


sol = solve(prob, EM(); dt=1e-3, adaptive=false, rng=rng, saveat=0.01)

SDEでは数値法の弱/強収束特性を意識し、刻み幅dtを管理します。並列モンテカルロ(多試行)はEnsembleProblemで行い、各試行に独立な乱数生成器を割り当てて、再現性を保ちます。

ensemble = EnsembleProblem(prob)  
sols = solve(ensemble, EM(); dt=1e-3, adaptive=false, trajectories=100,  
rng=MersenneTwister(1234), saveat=0.01)  
# 期待値経路  
mean_path = reduce(+, (sol.u for sol in sols)) ./ length(sols)

* * *

# 7. 数値積分:QuadGKで一次元積分を高精度に

QuadGKはGauss–Kronrod則に基づく1次元積分の高精度ライブラリです。有限区間・無限区間に対応します。

using QuadGK  


# 有限区間  
f(x) = exp(-x^2)  
val, err = quadgk(f, -1.0, 1.0; rtol=1e-10, atol=1e-12)  


# 無限区間([0, ∞))  
g(x) = x^2 * exp(-x)  
val_inf, err_inf = quadgk(g, 0.0, Inf; rtol=1e-10, atol=1e-12)  


# 対称な無限区間((-∞, ∞))  
h(x) = exp(-x^2)  
val_all, err_all = quadgk(h, -Inf, Inf)

被積分関数が特異(端点で発散)・急峻な場合は、区間分割や変数変換で滑らかさを高めると良い結果が得られます。quadgkは自動分割・誤差見積もりを持ちますが、特異点の位置がわかっている場合は事前に避けるのが定石です。

* * *

# 8. 非線形方程式の解法:NLsolveで根探し

NLsolveはスカラー・ベクトルの非線形方程式を解きます。スカラー関数には二分法の自作も有効ですが、一般系ではヤコビ不要の手法から始めます。

using NLsolve  


# 1変数:f(x) = cos(x) - x の根  
f1(x) = cos(x) - x  
res1 = nlsolve(x -> f1(x[1]), [0.5]) # 初期推定 0.5  
xstar = res1.zero[1]  


# 多変数:Lotka–Volterra の定常点(例)  
function F!(F, x)  
# x = [prey, predator]  
α, β, δ, γ = (1.5, 1.0, 1.0, 3.0)  
F[1] = α*x[1] - β*x[1]*x[2]  
F[2] = δ*x[1]*x[2] - γ*x[2]  
end  


res2 = nlsolve(F!, [1.0, 1.0])  
x_eq = res2.zero

nlsolveはf!(インプレース)とf(非インプレース)の両方に対応します。難しい問題ではヤコビ行列の提供やスケーリングが有効です。収束判定、最大反復、ステップ制御をオプションで設定します。

res = nlsolve(F!, [1.0, 1.0]; ftol=1e-12, xtol=1e-12, maxiters=10_000)

* * *

# 9. 誤差評価・検証:トレランス、参考解、保存則、不変量

誤差管理の実務的指針を列挙します。

  * トレランス: ODE/SDEはreltol/abstolで目標精度を指定。相対誤差が小さな値ではabstolが支配的になります。量のスケールに合わせて設定してください。
  * 参考解: 高精度解(より厳しいトレランス/高次数法)を基準に、低精度解との差をL2/L∞で評価します。



sol_ref = solve(prob, Vern9(); reltol=1e-12, abstol=1e-14, saveat=0.001)  
sol_test = solve(prob, Tsit5(); reltol=1e-6, abstol=1e-8, saveat=0.001)  
err = maximum(abs.(sol_test[1, :] .- sol_ref[1, :]))

  * 保存則・不変量: ハミルトン系のエネルギー、質量保存、非負制約など、モデルの不変条件を検査します。コールバックや反復の節目でアサートを入れます。



function check_nonneg(sol)  
@assert all(@. sol[1, :] >= 0.0) "負値が発生"  
end

  * イベント整合: 連続コールバックの到達時刻、前後での物理量の整合性(連続/不連続の仕様通りか)を確認します。



* * *

# 10. 時間刻み・安定性:固定刻み、適応刻み、ステップ診断

刻み制御は精度・安定性・計算量の要です。

  * 固定刻み: SDEや離散時間モデルではadaptive=falseとdt固定が有効。
  * 適応刻み: ODEの誤差制御つき法では自動刻み調整が基本。イベント前後の挙動を診断するため、dense=trueやsaveatで確認します。
  * ステップ診断: sol.destats(解法統計)で拒否ステップ数、関数評価数、線形代数呼び出し回数などを確認し、チューニングします。



sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10)  
sol.destats # 統計構造(実行環境に依存して表示)

* * *

# 11. 実践例:Lotka–Volterra 系(イベント・パラメータ掃引・検証)

捕食–被食モデルを総合例として扱います。

using DifferentialEquations  


# dx/dt = αx - βxy  
# dy/dt = δxy - γy  
function lv!(du, u, p, t)  
α, β, δ, γ = p  
x, y = u  
du[1] = α*x - β*x*y  
du[2] = δ*x*y - γ*y  
end  


u0 = [1.0, 1.0]  
tspan = (0.0, 25.0)  
p = (1.5, 1.0, 1.0, 3.0)  


prob = ODEProblem(lv!, u0, tspan, p)  
sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10, saveat=0.01)  


# 不変量(例):正の領域維持  
@assert all(@. sol[1, :] >= 0.0) && all(@. sol[2, :] >= 0.0)  


# パラメータ掃引(αを変化)  
alphas = 0.5:0.5:2.5  
peaks = Float64[]  
for α in alphas  
p = (α, 1.0, 1.0, 3.0)  
sol = solve(remake(prob; p=p), Tsit5(); reltol=1e-8, abstol=1e-10, saveat=0.02)  
push!(peaks, maximum(sol[1, :]))  
end

イベントを導入する場合は、例えば「捕食者が一定閾値を下回ったら保護措置で増加率を一時変更」などをDiscreteCallbackで表現できます。

# 閾値イベント:y < y_min で一時的にγを小さく  
y_min = 0.2  
function condition_y(u, t, integrator)  
u[2] - y_min  
end  
function affect_y!(integrator)  
α, β, δ, γ = integrator.p  
integrator.p = (α, β, δ, γ*0.5) # 一時的に死亡率を半減  
end  
cb = ContinuousCallback(condition_y, affect_y!)  


sol_ev = solve(prob, Tsit5(); callback=cb, reltol=1e-8, abstol=1e-10)

イベントの設計では政策介入・制約をコード化し、介入が妥当な時刻・状態でしか発動しないことを検証します。

* * *

# 12\. ODEと積分・根探しの連携:到達時刻の解法、確率計算

ODEと積分・根探しを組み合わせて、到達時刻(閾値到達時間)や期待値を計算する例を示します。

# 到達時刻:u(t) が閾値θへ達する最小 t を求める  
θ = 0.8  
f_t(t) = sol(t)[1] - θ  


# 区間内で符号が反転していることを確認し、二分法で近似  
function bisect(f, a, b; tol=1e-10, maxiter=10_000)  
fa, fb = f(a), f(b)  
fa*fb < 0 || error("bracket must have opposite signs")  
for _ in 1:maxiter  
c = (a + b)/2  
fc = f(c)  
if abs(fc) < tol  
return c  
elseif fa*fc < 0  
b, fb = c, fc  
else  
a, fa = c, fc  
end  
end  
return (a + b)/2  
end  


t_hit = bisect(f_t, 0.0, 10.0)

補助的な期待値(例えば到達までのコストの期待値)は、補間関数とquadgkの組み合わせで定義できます。

using QuadGK  
cost_density(t) = exp(-0.1*t) # 重みの例  
val_cost, err_cost = quadgk(t -> cost_density(t)*sol(t)[1], 0.0, t_hit)

* * *

# 13. 数値安定性・スケーリング:前処理、スケール合わせ、境界条件

非線形や剛性で難しい問題は「スケール合わせ」と「前処理」が効果的です。

  * スケール合わせ: 状態・パラメータを同程度のオーダに揃えると、reltol/abstolの設定が自然になります。
  * 境界条件の扱い: 初期値と境界値が厳密に満たされているか検証。イベントやコールバックで境界の内側へ投影する処理を設計します。
  * 前処理: 根探しでヤコビ行列のスケーリング、ODEで固有スケールを意識した無次元化を行うと収束が安定します。



* * *

# 14. 計算効率:saveat、スパース/密、ベクトル化、インプレース

  * saveatの最適化: 必要な時刻のみ保存。補間に頼る場合はdense=trueを考慮します。
  * 線形代数の効率: 剛性法では線形系の解が多数発生。行列構造(対称、疎、バンド)がわかっているなら宣言し、効率を上げます。
  * 関数のインプレース: f!(du, u, p, t)形式でアロケーションを削減。状態や右辺のベクトルを再利用します。



* * *

# 15. テスト・ロギング・再現性:品質保証の枠組み

  * テスト: 不変量、イベント時刻、参考解との差、根の残差‖F(x*)‖などを@testで検証。
  * ロギング: 解法統計、刻みの最小値、拒否ステップ数、イベント発生回数を記録。
  * 再現性: 乱数種、トレランス、パラメータ、出力粒度を明示。バージョンをProject.tomlで固定。



using Test  
@test sol.retcode == :Success  
@test maximum(abs.(sol[1, :] .- sol_ref[1, :])) < 1e-5

* * *

# 16. 補足:二分法・ニュートン法の自作スケルトン(安全設計)

汎用ライブラリに加えて、仕様に合わせた簡易求解器を自作したい場合の骨子です。

# 二分法(区間に1つの単純根、連続関数)  
function bisect_safe(f, a, b; tol=1e-12, maxiter=10_000)  
fa, fb = f(a), f(b)  
fa*fb < 0 || error("bracket must have opposite signs")  
for _ in 1:maxiter  
c = (a + b)/2  
fc = f(c)  
if abs(fc) < tol  
return c  
end  
if fa*fc < 0  
b, fb = c, fc  
else  
a, fa = c, fc  
end  
end  
return (a + b)/2  
end  


# ニュートン法(導関数あり・初期値が良好なとき)  
function newton(f, df, x0; tol=1e-12, maxiter=10_000)  
x = x0  
for _ in 1:maxiter  
dfx = df(x)  
dfx == 0 && error("zero derivative")  
x_new = x - f(x)/dfx  
if abs(x_new - x) < tol  
return x_new  
end  
x = x_new  
end  
return x  
end

自作する場合も、収束しないケースの扱い(最大反復、ブレーク条件、例外)、初期値・スケーリング、検証を必ず組み込みます。

* * *

# 17. 参考的ユースケース:剛性+イベント+根探しの複合

剛性系(化学反応)、イベント(閾値到達で反応停止)、根探し(定常点探索)を一体で扱う設計例の骨子です。

# 反応系(剛性傾向)  
function rxn!(du, u, p, t)  
k1, k2 = p  
# u = [A, B, C]  
du[1] = -k1*u[1]  
du[2] = k1*u[1] - k2*u[2]  
du[3] = k2*u[2]  
end  


u0 = [1.0, 0.0, 0.0]  
tspan = (0.0, 10.0)  
p = (100.0, 50.0) # 大きな速度定数で剛性傾向  


prob = ODEProblem(rxn!, u0, tspan, p)  


# C が閾値 θ に達したら反応停止(u[2] の生成を止めるなど)  
θ = 0.8  
cond(u, t, integrator) = u[3] - θ  
function affect!(integrator)  
k1, k2 = integrator.p  
integrator.p = (k1, 0.0) # k2=0 へ  
end  
cb = ContinuousCallback(cond, affect!)  


sol = solve(prob, Rodas5(); callback=cb, reltol=1e-9, abstol=1e-12, saveat=0.001)  


# 定常点の検出(t→∞ での近似。根探しでF(u*)=0 を求める)  
using NLsolve  
function steady_F!(F, x)  
du = similar(x)  
rxn!(du, x, p, 0.0)  
F .= du  
end  
res = nlsolve(steady_F!, sol.u[end]) # 終端値を初期推定に  
u_star = res.zero

* * *

# 18. 実装上の注意まとめ(記述のみ)

  * ODE/SDEの右辺はインプレースf!で書き、アロケーションを減らします。
  * 剛性が疑われる場合は陰的法を第一候補にし、トレランスを適切に設定します。
  * イベントはContinuousCallback/DiscreteCallbackで仕様化し、到達時刻の精密化(rootfind=true)を使います。
  * 積分はquadgkで誤差を把握し、特異点は区間分割・変数変換で回避します。
  * 根探しはnlsolveで初期値とスケーリングを工夫し、残差・収束の検証を行います。
  * 不変量・保存則・非負制約をテストに組み込み、検証可能性を高めます。
  * 乱数を使う場合(EMなど)は生成器・種を固定し、並列時は独立な生成器を配布して再現性を保ちます。
  * 出力は同一グリッドで比較可能にし、補間は可視化や補助的評価に用います。



これらの作法を体系的に適用することで、Juliaにおける微分方程式・数値積分・根探しは、正確さ・速度・再現性を同時に満たす堅牢なワークフローへと整えられます。

* * *

# 第12章最適化と数理計画:Optim・JuMPで特(◇製薬と目的)

# 1. 設計指針:目的・制約・データ・再現性を軸に最適化を構成する

最適化は「目的関数」「制約」「データ」「ソルバ」の4要素を、再現可能で検証可能な形で組み合わせる営みです。Juliaでは、連続最適化はOptim.jlで手軽に扱え、線形/整数/非線形の数理計画はJuMPで宣言的にモデル化できます。以下の原則を徹底します。

  * 目的関数: 最小化/最大化の基準を定量化し、単位・スケール・凸性の把握を明示します。正則化(L2/L1)やペナルティの設計もここに含めます。
  * 制約: 実務上の境界条件・資源制約・ロジック制約(if/then型)を数理的に表現し、可行領域を定義します。等式/不等式/整数(0-1)などの型を明確にします。
  * データ: 外部データ(CSV/DataFrames)を「列の型・単位・前処理」を含めて確定し、モデルに安全に流し込みます。
  * ソルバ連携: 連続最適化は勾配法/準ニュートン法/境界付き最適化を選択、数理計画はLP/MIP/NLP/QPのソルバを選択し、属性(停止条件・時間制限・許容誤差)を設定します。
  * 再現性: プロジェクト環境・ソルババージョン・乱数種・前処理を固定し、ログと検証を残します。



* * *

# 2. 連続最適化(無制約):Optim.jlで勾配・準ニュートン法を使う

Optim.jlは無制約最適化の入り口として最適です。目的関数だけで始められ、勾配・ヘッセ行列があれば収束が加速します。自動微分はForwardDiffで簡単です。

using Optim  
using ForwardDiff  


# 例:2変数のRosenbrock関数(非凸だが谷に沿って最適へ)  
rosenbrock(x) = (1.0 - x[1])^2 + 100.0 * (x[2] - x[1]^2)^2  


x0 = [-1.2, 1.0]  


# 準ニュートン:LBFGS(自動微分で勾配を供給)  
res = optimize(rosenbrock, x0, LBFGS(); autodiff = :forward)  
xstar = Optim.minimizer(res)  
fstar = Optim.minimum(res)

勾配・ヘッセを明示することで、Newton法も使えます。

∇f(x) = ForwardDiff.gradient(rosenbrock, x)  
∇2f(x) = ForwardDiff.hessian(rosenbrock, x)  
resN = optimize(rosenbrock, ∇f, ∇2f, x0, Newton())

運用のポイント

  * スケーリング:変数のオーダを揃え、勾配の振れを抑えます。単位系を意識します。
  * 初期値:非凸では初期値依存が大きいので、複数の初期値で試行し、最良解を採用します。
  * 停止条件:勾配ノルム/ステップ長/相対改善率をログで確認し、Optim.Optionsで上限反復や許容値を設定します。



* * *

# 3. 連続最適化(境界付き):Fminboxで上下限を課す

多くの実務では「変数は非負」「範囲内」という単純制約があります。Fminboxで上下限を付けられます。

lower = [0.0, 0.0]  
upper = [2.0, 2.0]  
res_box = optimize(rosenbrock, lower, upper, x0, Fminbox(LBFGS()); autodiff=:forward)  
xstar_box = Optim.minimizer(res_box)

Fminboxは境界へ投影しつつ探索するため、単純な箱型制約に適します。非線形等式/不等式制約はOptim.jlでは直接扱えないため、NLPソルバ(後述のJuMP+Ipopt)を選びます。

* * *

# 4. 実例:ロジスティック回帰の損失最小化(L2正則化)

連続最適化の代表例として、二値分類のロジスティック回帰を学習します。データはX∈R^{N×d}、ラベルy∈{0,1}。損失は対数損失+L2正則化です。

using Optim, ForwardDiff, Statistics  


# X: N×d, y: N  
function logreg_loss(w, X, y; λ=1e-2)  
N, d = size(X)  
s = 0.0  
@inbounds for i in 1:N  
z = dot(@view X[i, :], w)  
# 安定なロジスティック損失(y∈{0,1})  
s += (z >= 0 ? (log1p(exp(-z)) + (1-y[i]) * z)  
: (log1p(exp(z)) - y[i] * z))  
end  
s += λ * sum(abs2, w)  
return s  
end  


# デモ用データ(実務ではCSV/DataFramesから)  
N, d = 500, 20  
X = randn(N, d); true_w = randn(d)  
y = @. (sigmoid(dot(true_w, X[1,:])) > 0.5) ? 1 : 0 # 概念例(実務で適切に生成)  


w0 = zeros(d)  
f(w) = logreg_loss(w, X, y; λ=1e-2)  


res = optimize(f, w0, LBFGS(); autodiff=:forward)  
ŵ = Optim.minimizer(res)

ポイント

  * 正則化の強さλは検証データで選定します。
  * 特徴量の標準化(平均0・分散1)で収束が安定します。
  * 交差検証は別スクリプトで行い、再現可能な乱数種・分割を固定します。



* * *

# 5\. JuMPでの線形計画(LP):宣言的モデル化とソルバ連携

JuMPは数理計画のモデル言語です。LPは「線形目的+線形制約」で構成され、汎用ソルバ(HiGHS/GLPKなど)で解けます。例:生産計画(製品別数量の決定)。

using JuMP  
using HiGHS  


# データ(実務ではCSVから)  
profit = [8.0, 5.0, 6.0] # 製品別利益  
A = [1.0 2.0 1.0; 3.0 0.0 2.0] # 資源使用係数(資源×製品)  
b = [100.0, 120.0] # 資源上限  
n = length(profit); m = length(b)  


model = Model(HiGHS.Optimizer)  
@variable(model, x[1:n] >= 0) # 生産量  
@objective(model, Max, sum(profit[j]*x[j] for j in 1:n))  
@constraint(model, [i in 1:m], sum(A[i,j]*x[j] for j in 1:n) <= b[i])  


optimize!(model)  


xstar = value.(x)  
obj = objective_value(model)

診断・感度  
LPでは線形制約の**双対値(shadow price)**が取れます(ソルバが対応していれば)。

dual_vals = [dual( JuMP.constraint_by_name(model, string("constraint[$i]")) ) for i in 1:m] # 参照方法は実装に応じて  
# あるいは、@constraint作成時に名前を付け、dual(c[i])で取得

双対値は「資源上限を微小に緩めたときの目的関数の改善率」を意味し、資源の価値評価や投資優先度の示唆に使えます。

* * *

# 6\. MIP(混合整数計画):0-1意思決定の導入

整数変数を使うと、施設配置/配車/選択問題などの離散意思決定を表現できます。例:0-1ナップサック。

using JuMP, HiGHS  


value = [10.0, 4.0, 9.0, 12.0]  
weight = [5.0, 3.0, 4.0, 7.0]  
W = 10.0  
n = length(value)  


model = Model(HiGHS.Optimizer)  
@variable(model, z[1:n], Bin)  
@objective(model, Max, sum(value[i]*z[i] for i in 1:n))  
@constraint(model, sum(weight[i]*z[i] for i in 1:n) <= W)  


optimize!(model)  
zstar = value.(z)  
obj = objective_value(model)

実務のコツ

  * 時間制限・最良許容ギャップを設定します(ソルバ属性)。
  * ウォームスタート:近似解やヒューリスティクスから初期値を入れるset_start_value。
  * モデルの強化:有効不等式、弱いビッグMの見直し、対称性の回避(対称構造は探索を増やす)。



* * *

# 7\. QP/最小分散ポートフォリオ:二次目的+線形制約

平均–分散ポートフォリオは、二次目的(分散)と線形制約(合計1・非負・期待収益閾値)で表せます。NLPソルバ(Ipopt)かQPソルバを用います。

using JuMP, Ipopt  


μ = [0.05, 0.08, 0.12] # 期待収益  
Σ = [0.10 0.02 0.01; 0.02 0.08 0.03; 0.01 0.03 0.15] # 共分散  
μ_target = 0.09  
n = length(μ)  


model = Model(Ipopt.Optimizer)  
@variable(model, x[1:n] >= 0)  
@constraint(model, sum(x) == 1.0)  
@constraint(model, sum(μ[i]*x[i] for i in 1:n) >= μ_target)  
@objective(model, Min, sum(Σ[i,j]*x[i]*x[j] for i in 1:n, j in 1:n))  


optimize!(model)  
xstar = value.(x)  
risk = objective_value(model)

注意

  * Σは対称正定値が望ましい。数値安定性のため、小さなεIで正則化することがあります。
  * 期待収益制約を掃引し、効率的フロンティアを描くと意思決定が明確になります。



* * *

# 8. 非線形制約(NLP):@NLobjective・@NLconstraintでモデリング

物理/工学では非線形制約が自然です。JuMPのNL機能とIpoptで解きます。

using JuMP, Ipopt  


model = Model(Ipopt.Optimizer)  
@variable(model, x >= 0)  
@variable(model, y >= 0)  


# 例:非線形目的(エネルギー最小)+非線形制約(幾何条件)  
@NLobjective(model, Min, (x - 1)^2 + (y - 2)^2)  
@NLconstraint(model, x * y >= 2.0) # 双曲線領域  


optimize!(model)  


xstar, ystar = value(x), value(y)

運用のコツ

  * 初期値はset_start_valueで与えると収束が安定します。
  * スケーリング:変数・制約のスケールを揃えて、勾配の振れを抑えます。
  * 収束診断:termination_status(model)・objective_value(model)・制約違反量を確認します。



* * *

# 9. 現場データとの結合:DataFramesからJuMPへ安全に渡す

データは型・単位を確定してからモデルに渡します。例:製品別データからLPを構築。

using CSV, DataFrames, JuMP, HiGHS  


df = CSV.read("data/products.csv", DataFrame)  
# 列: name, profit, a1, a2, capacity1, capacity2 ...  


profit = Float64.(df.profit)  
A = hcat(Float64.(df.a1), Float64.(df.a2))' # 資源×製品(行列化)  
b = [df.capacity1[1], df.capacity2[1]]  


model = Model(HiGHS.Optimizer)  
@variable(model, x[1:length(profit)] >= 0)  
@objective(model, Max, sum(profit[j]*x[j] for j in 1:length(profit)))  
@constraint(model, sum(A[1,j]*x[j] for j in 1:length(profit)) <= b[1])  
@constraint(model, sum(A[2,j]*x[j] for j in 1:length(profit)) <= b[2])  


optimize!(model)  
solution = value.(x)

ベストプラクティス

  * データ検証(非負・範囲・欠損)を前処理で行い、モデルに入る前に不正を弾きます。
  * 列名・単位・スキーマを固定し、複数ファイルの整合性をチェックします。



* * *

# 10. 感度解析:パラメータ掃引・双対値・比較の再現性

意思決定のロバスト性を評価します。LPなら双対値、NLPならパラメータ掃引で影響を測ります。

targets = 0.06:0.01:0.12  
risks = Float64[]  
for μt in targets  
model = Model(Ipopt.Optimizer)  
@variable(model, x[1:n] >= 0)  
@constraint(model, sum(x) == 1.0)  
@constraint(model, sum(μ[i]*x[i] for i in 1:n) >= μt)  
@objective(model, Min, sum(Σ[i,j]*x[i]*x[j] for i in 1:n, j in 1:n))  
optimize!(model)  
push!(risks, objective_value(model))  
end

再現性の工夫

  * 同一モデル・同一ソルバ・同一属性で走らせ、比較は同じグリッド(同じパラメータ集合)で行います。
  * MIPではランダム化ヒューリスティクスがあるソルバもあるため、乱数種や決定論モードを設定します(ソルバ依存)。



* * *

# 11. 実験の設計:停止条件・時間制限・ギャップ管理・ログ

実務では「時間内に最良解」「許容ギャップ以内に停止」という現実的な運用が重要です。ソルバ属性で設定します(属性名はソルバごとに異なるため、ドキュメントを参照し、コードに明記します)。

using JuMP, HiGHS  
model = Model(HiGHS.Optimizer)  
# 例:時間制限・最良許容ギャップ(ソルバ依存)  
# set_optimizer_attribute(model, "time_limit", 60.0)  
# set_optimizer_attribute(model, "mip_rel_gap", 0.01)

ログ

  * 目的値・停止理由・反復数・ギャップ・探索ノード数(MIP)を記録します。
  * 入力パラメータ(ターゲット収益・能力上限・正則化強度)を一緒に保存し、比較可能にします。



* * *

# 12. モデル安全性:ビッグMの慎重な使用・スケーリング・弾性目的

論理制約(if/then)で使うビッグMは、過大だと緩い境界(弱い定式化)になり、数値も不安定になります。Mの下限を理論/データから導出して最小化します。目的に弾性項(違反ペナルティ)を入れて、不可解な無可行を避ける設計も有効です。

using JuMP, HiGHS  
model = Model(HiGHS.Optimizer)  
@variable(model, x >= 0)  
@variable(model, y, Bin)  
M = 100.0 # データから根拠ある最小Mを設定  
@constraint(model, x <= M * y) # y=0ならx=0、y=1ならx自由(Mまで)

* * *

# 13. ウォームスタート・分割統治・緩和解の活用

MIPでは、LP緩和の解やヒューリスティクスからの初期解を与えると探索が速くなります。

# 緩和を解いてxの実数解を得て、丸めた初期解を入れる(概念)  
# set_start_value(z[i], round(Int, x_relaxed[i]))

大規模問題は、分割統治(時間・地域・製品で分解)、列生成、ラグランジュ緩和など高度手法も視野に入ります。まずはスコープを絞り、可視化/集計で構造を把握します。

* * *

# 14\. NLPでの初期値・スケール・正則化:収束安定化の技法

非線形では、初期値とスケールが生命線です。

  * 変数の初期値を可行域の内部に置く(境界の直上/直下に置くと勾配が尖る)。
  * 目的/制約を無次元化し、オーダを合わせる。
  * 小さな正則化(例:Σ + εI)でヘッセ行列の条件数を改善。
  * ペナルティ法(制約違反にペナルティ)で可行性へ誘導。



* * *

# 15. 連続最適化と数理計画の橋渡し:ハイブリッド設計

実務では、連続の調整(係数推定など)と離散の意思決定(採用/不採用)が絡みます。

  1. 連続パラメータをOptim.jlで推定。
  2. 推定結果をJuMPへ渡してMIPを解く。
  3. 目的値が悪ければパラメータを再学習(外ループ最適化)。



このハイブリッドで、データ駆動のパラメータと実務制約の意思決定を統合できます。

* * *

# 16. 品質保証:単体テスト・可行性検査・数値検証

  * 可行性:primal_status(model)・制約違反量・変数境界の満足をチェック。
  * 最適性:LPなら双対ギャップ、MIPならMIPギャップ。NLPはKKT条件の残差。
  * テスト:小規模インスタンスで期待解を@test、境界ケース(ゼロ能力・高ターゲット)で安定動作を検証。



using Test  
# @test termination_status(model) == MOI.OPTIMAL  
# @test objective_value(model) ≈ expected_value atol=1e-6

* * *

# 17. 再現可能性:環境固定・データバージョニング・レポート自動化

  * Project.toml/Manifest.tomlでパッケージ/ソルバのバージョン固定。
  * 入力データのバージョン管理(スキーマ・生成日・集計方法)を記録。
  * 出力(解ベクトル・目的値・双対値・ログ)をファイルへ保存し、レポート生成(PDF/HTML)を自動化。
  * 乱数要素を使う場合(MIPヒューリスティクスなど)は種を固定(ソルバ依存属性)。



* * *

# 18. 総合ケース:需要充足の生産・配分(LP→MIP→感度)

  1. LPで「生産量と配分」を連続変数で最適化(容量上限、輸送コスト、需要充足)。
  2. 施設の稼働/休止を0-1変数で追加しMIP化(固定費・最小稼働量)。
  3. 需要ベース/能力上限を掃引し、費用曲線と双対値で投資優先度を評価。
  4. 時間制限・ギャップ許容で現実的に運用、候補解を可視化し意思決定へ。



この一連の流れを、JuMPのモデルとDataFramesのデータで統一し、Optim.jlで必要な連続パラメータ(例えばコスト係数の校正)を事前に推定してから投入すれば、実務で通用する一気通貫の最適化パイプラインになります。

* * *

# 19. 実装スニペット集:よく使うJuMP/Optimの補助操作

# JuMP: 変数の初期値  
set_start_value(x[1], 0.5)  


# JuMP: 解の取得と丸め  
xsol = value.(x)  
xbin = round.(Int, xsol) # MIPのヒューリスティック案には注意  


# JuMP: ステータス・目的・違反量  
using MathOptInterface  
const MOI = MathOptInterface  
term = termination_status(model)  
obj = objective_value(model)  


# Optim: 統計・停止条件  
res = optimize(f, x0, LBFGS(); autodiff=:forward)  
# Optim.converged(res), Optim.iterations(res), Optim.minimum(res), Optim.minimizer(res)  


# Optim: オプション(最大反復・許容)  
# optimize(f, x0, LBFGS(); autodiff=:forward, iterations=10_000, g_tol=1e-8)

* * *

# 20. 実務の心得(記述のみ)

  * モデルはシンプルから始め、段階的に制約を増やす。突然の複雑化は数値と探索を不安定にする。
  * 目的関数と制約のスケールを揃え、ソルバの勾配・残差評価が健全になるよう設計する。
  * 可視化・集計で解の構造を理解し、対称性や冗長制約を取り除く。
  * 時間制限・ギャップ許容を設定し、意思決定に十分な質の解で早く止める。
  * 乱数を使う場面では種を固定し、比較は同一設定・同一データで行う。
  * 失敗(無可行・数値不安定)は、前処理・スケーリング・緩和・初期値で手当する。
  * ドキュメント化(目的・制約・データ・属性・結果)と再現スクリプト整備は、長期運用の生命線。



これらを一貫して適用することで、Optim.jlとJuMPを中核とするJuliaの最適化基盤は、連続・離散・非線形の多様な課題に対して、速く・正しく・再現可能な解を提供します。

* * *

# 第13章並列・分散・GPU:Threads・Distributed・CUDA(三角スケールさせる)

# 1. 設計指針:CPUスレッド・分散プロセス・GPUの役割分担と再現性

並列化は「どこを並べるか」「どう同期を避けるか」「どう再現性を保つか」の設計で決まります。Juliaでは、同一プロセス内のCPUスレッド(Threads)で細粒度の計算を高速化し、複数プロセス(Distributed)で粗粒度のタスクをスケールさせ、GPU(CUDA.jl)で大量の同型演算を一括処理します。基本方針は次のとおりです。

  * 分割: 独立な要素・ブロック・試行に分け、各ワーカーが排他なく処理できる粒度に切ります。
  * 同期最小化: 共有書き込みを避け、ローカル集計→最後にリダクション(加算・結合)する形にします。
  * メモリ局所性: 連続アクセス・チャンク分割でキャッシュ/帯域の効率を上げます。
  * 再現性: 乱数・設定値・データ分割規則を「仕様化」してコードに明示。CPUスレッド・プロセス・GPU各レイヤで一貫したルールを守ります。
  * 計測優先: 並列化の前後で必ず計測し、スレッド/プロセス/デバイス転送のオーバーヘッドを可視化します。



以下、Threads・Distributed・CUDAを順に、実用コードと設計要点で掘り下げます。

* * *

# 2. スレッド並列の基本:Threads.@threads、競合回避、偽共有の抑制

CPUスレッドは同一メモリ空間を共有します。データ競合(同じ場所への同時書き込み)は厳禁です。要点は「読み取り共有・書き込み局所化・最後に縮約」です。

using Base.Threads  


# 要素ごとの変換(書き込み先は独立なので競合なし)  
function scale!(x::Vector{Float64}, a::Float64)  
@threads for i in eachindex(x)  
@inbounds x[i] = a * x[i]  
end  
return x  
end  


# リダクション(各スレッドがローカルに集計して最後に和)  
function sum_threads(x::Vector{Float64})  
partial = zeros(Float64, nthreads())  
@threads for i in eachindex(x)  
@inbounds partial[threadid()] += x[i]  
end  
return sum(partial)  
end

競合回避の原則

  * 共有配列への書き込みは「各スレッドが互いに異なる領域」へ限定します(上のpartial[threadid()]など)。
  * 原子操作(Threads.atomic_add!等)は、衝突が稀なカウンタ用途なら有効ですが、頻繁な原子更新はボトルネックになります。
  * **ロック(ReentrantLock)**は最小範囲で。広域ロックは並列効果を殺します。
  * 偽共有(false sharing): スレッドローカルの部分和配列は「連続要素を異なるスレッドが更新しない」ため概ね安全ですが、狭い配列に全スレッドが書くと同一キャッシュライン争奪が起きます。必要ならパディング(ntupleで余分な要素を挿入)で緩和します。
  * チャンク分割: ループ本体が重い場合、手動でstart:endを割り当てるとスケジューリングのオーバーヘッドを下げられます。



* * *

# 3. スレッド並列の乱数:スレッドローカルRNG、再現性の確保

グローバルRNG(既定のRandom.GLOBAL_RNG)の共有は、並列で相関や競合の原因になります。各スレッドに独立なRNGを配布し、初期化規則を固定します。

using Random, Base.Threads  


# スレッドごとに独立なRNGを用意(種の規則を仕様化)  
function rngs_for_threads(seed::Int)  
return [MersenneTwister(seed + i) for i in 1:nthreads()]  
end  


function montecarlo_pi_threads(n::Int; seed::Int=0)  
rngs = rngs_for_threads(seed)  
counts = zeros(Int, nthreads())  
@threads for t in 1:nthreads()  
rng = rngs[threadid()]  
local_inside = 0  
# スレッドあたりの試行数(端数は最後に加算)  
m = n ÷ nthreads()  
xs = rand(rng, m)  
ys = rand(rng, m)  
@inbounds @simd for i in 1:m  
local_inside += (xs[i]^2 + ys[i]^2 <= 1.0)  
end  
counts[threadid()] = local_inside  
end  
inside = sum(counts)  
return 4.0 * inside / ( (n ÷ nthreads()) * nthreads() )  
end

再現性の要点

  * 種を「仕様値」としてコードか設定ファイルに固定します。
  * 分割規則(各スレッド何試行か)を固定します。
  * 乱数の生成・消費の順序は実行ごとに不変であるべきです(早期終了・条件分岐の内部で乱数を消費しないなど)。



* * *

# 4. タスク並行と軽量スレッド:@spawn/@sync、@async/Channel

計算の粒度がバラつく場合は、@spawnでタスクを投げ、@syncで待ち合わせると負荷分散が自然になります。

# タスク並行:ばらつきのある仕事を自動分配  
function map_spawn(f, shards)  
@sync begin  
results = Vector{Any}(undef, length(shards))  
for (i, s) in enumerate(shards)  
@spawn begin  
results[i] = f(s)  
end  
end  
return results  
end  
end

I/Oや待ちのある処理には@asyncとChannelでパイプライン化すると、CPUの待機時間を隠せます。数値演算主体の章では詳細は割愛し、タスク分配の要点のみ押さえます。

* * *

# 5. 分散処理:Distributedでワーカー管理、pmap/@distributedの使い分け

複数プロセス(複数CPUコア/複数ノード)で粗粒度タスクを並べます。ワーカー起動とコード配布はDistributedで行います。

using Distributed  
addprocs(4) # ローカルに4ワーカー追加  


@everywhere using Random  


# 各ワーカーで仕事(π推定)を実行し、結果を集約  
@everywhere function estimate_pi_trials(n::Int, seed::Int)  
rng = MersenneTwister(seed)  
inside = 0  
xs = rand(rng, n); ys = rand(rng, n)  
@inbounds @simd for i in 1:n  
inside += (xs[i]^2 + ys[i]^2 <= 1.0)  
end  
return 4.0 * inside / n  
end  


# pmap(可変重いタスク向け):各ワーカーへ種と試行数を配布  
seeds = 1:32  
res = pmap(s -> estimate_pi_trials(1_000_000, s), seeds)  
mean(res)

@distributedはループ本体が均質で、簡単なリダクションをしたい場面に有効です。

# @distributed で総和を計算  
tot = @distributed (+) for s in seeds  
estimate_pi_trials(1_000_00, s) # 集約は足し算(他にmax/minなども可能)  
end  
avg = tot / length(seeds)

分散の要点

  * @everywhereで、必要な関数・型・usingを全ワーカーへ配布します。
  * シリアライズに注意:クロージャや大きなデータの配布は転送コストになります。データはワーカー側でロードする設計が効率的です。
  * SharedArray(単一ノード)で読み取り共有を行い、集計はローカル→縮約にします。
  * 乱数再現性: ワーカーIDに基づく種の規則(seed + myid())を固定し、分割数・順序を変えないようにします。



* * *

# 6\. GPU基礎:CUDA.jlのCuArrayとブロードキャスト、独自カーネル

大量の同型演算はGPUへ載せると効果的です。基本はデータをデバイスへ移す→ブロードキャストで融合→ホストへ戻すです。

using CUDA  


# データをGPUへ  
x = rand(Float32, 10_000_000)  
y = rand(Float32, 10_000_000)  
dx = CuArray(x)  
dy = CuArray(y)  


# ブロードキャストは融合され、暫定配列を作らず高速  
dz = @. sin(dx) + 2f0 * dy  


# 結果をホストへ(必要時のみ)  
z = Array(dz)  


# スカラー禁止(誤ってデバイスメモリへスカラーアクセスすると遅い)  
CUDA.allowscalar(false)

独自カーネルでストライドアクセス・共有メモリなどを制御できます(まずはシンプルな例から)。

# 要素ごとの加算カーネル  
function vadd_kernel!(out, a, b)  
i = (blockIdx().x - 1) * blockDim().x + threadIdx().x  
if i <= length(out)  
@inbounds out[i] = a[i] + b[i]  
end  
return  
end  


n = length(dx)  
dz2 = similar(dx)  
threads = 256  
blocks = cld(n, threads)  
@cuda threads=threads blocks=blocks vadd_kernel!(dz2, dx, dy)

GPUの原則

  * 転送最小化: ホスト↔デバイス間のコピーは高コスト。まとめて転送し、デバイス内で演算を連鎖させます。
  * 融合: ブロードキャスト・mul!等のインプレース演算を使い、中間メモリを減らします。
  * スカラー禁止: CUDA.allowscalar(false)で誤用検知。ループで1要素ずつCuArrayを読む/書くのは避けます。
  * 精度・型: Float32が基本。必要時のみFloat64。整数・論理の混在に注意し、型を揃えます。



* * *

# 7\. GPUでの乱数と再現性:現実的な運用パターン

GPU内で乱数を直接生成する方法もありますが、再現規則・シード管理・ストリーム/カーネル間の消費順序を整える必要があり、設計が難しい場面があります。実務での堅実なやり方は次の2つです。

  1. CPUで乱数を生成してからGPUへ転送(種を固定し、生成順序を統一)。
  2. 乱数配列を事前生成し、カーネルには乱数を引数で渡す(消費数が固定される)。



using Random, CUDA  


# 1) CPU側で固定種・固定順序の乱数を準備  
rng = MersenneTwister(1234)  
r = rand(rng, Float32, 10_000_000)  
dr = CuArray(r) # まとめて転送  


# 2) カーネルは dr[i] を消費する設計に  
function kernel_with_noise!(out, a, noise)  
i = (blockIdx().x - 1) * blockDim().x + threadIdx().x  
if i <= length(out)  
@inbounds out[i] = a[i] + noise[i]  
end  
end  


da = CuArray(rand(Float32, 10_000_000))  
do = similar(da)  
threads = 256; blocks = cld(length(da), threads)  
@cuda threads=threads blocks=blocks kernel_with_noise!(do, da, dr)

この設計なら、種→乱数列→転送→消費までが固定され、再現性を保てます。GPU内生成を使う場合は、ストリームごとの種管理・消費順序の規約化が必要です(ここでは詳細は割愛し、再現性重視の運用を推奨します)。

* * *

# 8. メモリ転送の最適化:バッチ、ピン留め、レイアウトと融合

転送の一般則は「少なく・大きく・先に」です。

  * 少なく: 演算に必要なデータは一括でデバイスへ。結果はまとめてホストへ。途中経過の往復は避けます。
  * 大きく: 細かいコピーを乱発せず、まとまったチャンクで移します。
  * 先に: 前処理(型変換・正規化・フィルタ)は可能な限りホストで済ませ、デバイスでは演算に集中します。
  * 融合: @.で演算を一式にまとめ、dz .= f.(dx) .+ g.(dy)のように中間を作らない形にします。
  * レイアウト: AoS(構造体配列)よりSoA(配列構造体)がベターな場面が多いです。フィールドごとに配列を持つと連続アクセスになり帯域効率が上がります。



* * *

# 9. 統合レシピ:Threads・Distributed・CUDAのモンテカルロと検証

スケールさせる典型例として、モンテカルロの多試行をThreads/Distributed/GPUで実装し、同一仕様下で比較できる形に整えます。

# 仕様値  
const SEED = 20240101  
const N = 10_000_000  


# 単一スレッド  
function mc_pi_single(n::Int, seed::Int)  
rng = Random.MersenneTwister(seed)  
xs = rand(rng, n); ys = rand(rng, n)  
inside = 0  
@inbounds @simd for i in 1:n  
inside += (xs[i]^2 + ys[i]^2 <= 1.0)  
end  
return 4.0 * inside / n  
end  


# スレッド  
mc_pi_threads(N; seed=SEED) # 前述関数を流用  


# 分散(ワーカーごとに独立種で集計)  
using Distributed  
addprocs(4)  
@everywhere using Random  
@everywhere function mc_pi_worker(n::Int, seed::Int)  
rng = MersenneTwister(seed)  
xs = rand(rng, n); ys = rand(rng, n)  
inside = 0  
@inbounds @simd for i in 1:n  
inside += (xs[i]^2 + ys[i]^2 <= 1.0)  
end  
return inside  
end  
parts = 32  
per = N ÷ parts  
tot_inside = @distributed (+) for k in 1:parts  
mc_pi_worker(per, SEED + k)  
end  
pi_dist = 4.0 * tot_inside / (per * parts)  


# GPU(乱数はCPU生成→転送)  
using CUDA  
rng = MersenneTwister(SEED)  
xs = rand(rng, Float32, N); ys = rand(rng, Float32, N)  
dx = CuArray(xs); dy = CuArray(ys)  
dflag = similar(dx, Int32)  


function inside_kernel!(flag, x, y)  
i = (blockIdx().x - 1) * blockDim().x + threadIdx().x  
if i <= length(flag)  
@inbounds flag[i] = (x[i]^2 + y[i]^2 <= 1f0) ? 1 : 0  
end  
end  


threads = 256; blocks = cld(N, threads)  
@cuda threads=threads blocks=blocks inside_kernel!(dflag, dx, dy)  
CUDA.synchronize()  
inside_gpu = sum(Array(dflag))  
pi_gpu = 4.0 * inside_gpu / N

この3系統は「仕様(種・分割・消費順序)」を固定して比較できます。Threadsは共有メモリ・偽共有の罠、Distributedは転送コスト・ワーカー起動のオーバーヘッド、GPUは転送とデバイス帯域の支配をそれぞれ確認し、対象と規模に応じて最適な層を選びます。

* * *

# 10. 性能計測の指針:BenchmarkTools、同期とイベント、統計の読み方

並列計測の基本は「同期して測る」「小さすぎる対象を測らない」です。

using BenchmarkTools  


# スレッド計測(小さすぎるとスレッド起動オーバーヘッドに支配される)  
x = rand(10_000_000)  
@btime scale!($x, 2.0) # ウォームアップ後に測定  


# 分散計測(全体を同期して壁時計時間を測る)  
using Distributed  
function time_dist()  
t0 = time()  
tot = @distributed (+) for k in 1:32  
mc_pi_worker(1_000_00, SEED + k)  
end  
synchronize()  
return time() - t0, tot  
end  


# GPU計測(カーネル起動は非同期なので、同期して測る)  
function time_gpu(N)  
rng = MersenneTwister(SEED)  
dx = CuArray(rand(rng, Float32, N))  
dy = CuArray(rand(rng, Float32, N))  
dflag = similar(dx, Int32)  
threads = 256; blocks = cld(N, threads)  
t0 = time()  
@cuda threads=threads blocks=blocks inside_kernel!(dflag, dx, dy)  
CUDA.synchronize()  
return time() - t0  
end

読み方の要点

  * 起動オーバーヘッド: 小さい問題は並列化しないほうが速いことが多いです。規模の閾値を決めます。
  * スピードアップ: 単一スレッド対比の倍率で評価。スレッド数やワーカー数に比例しないなら、ボトルネック(同期・メモリ帯域・分割不均衡)を探ります。
  * アロケーション: @btimeの出力でバイト数が増えていないか。インプレース化・融合で減らします。
  * GPU: カーネルがメモリ帯域支配か演算支配かを意識します(簡易診断は問題の演算密度から推定)。複数カーネルを一つにまとめると帯域効率が上がります。



* * *

# 11. 分散の運用:ワーカーライフサイクル、データ配置、障害耐性の基本

  * ライフサイクル: addprocsはコストがあります。タイトなループ内で起動/停止しない。セッション開始時に起動して使い回します。
  * データ配置: 大きなデータは各ワーカーがローカルに読み込む設計(パスとスキーマを共有)。pmapは引数転送が重いので、引数は軽く。
  * SharedArray: 単一ノードなら読取り中心のデータをSharedArrayで共有し、書き込みはローカル→縮約。
  * 障害耐性: 長時間の分散計算では、失敗時のリトライ・ログ・部分結果の保存を設計します(本章では詳細実装は割愛し設計原則のみ)。



* * *

# 12\. GPUカーネル設計の一歩先:ストライド、境界チェック、インプレース合成

カーネル実装でまず守るべきは「境界の安全」「連続アクセス」「融合」です。

# 2演算を1カーネルに合成(融合)  
function fused_kernel!(out, a, b)  
i = (blockIdx().x - 1) * blockDim().x + threadIdx().x  
if i <= length(out)  
@inbounds out[i] = sin(a[i]) + 2f0 * b[i]  
end  
end  


threads = 256; blocks = cld(length(dx), threads)  
@cuda threads=threads blocks=blocks fused_kernel!(dz2, dx, dy)

  * 境界の安全: i <= length(out)でチェック。@inboundsは正しさが確認済みの範囲でのみ付与。
  * 連続アクセス: 隣接要素を同じワープ(32スレッド)で処理し、帯域効率を確保。
  * インプレース: similarや再利用でアロケーションを抑えます。



* * *

# 13. 仕様化とドキュメント:乱数・分割・計測の規則を明文化

並列は「同じ構成で同じ結果が出る」ことが最重要です。以下をドキュメント化し、コードに刻みます。

  * 乱数: CPUスレッド(seed + threadid())、分散(seed + myid())、GPU(CPU生成→転送)などの規則。
  * 分割: チャンクサイズや配布方法(ブロック割り、ラウンドロビン、負荷推定に基づく動的分配)。
  * 計測: ウォームアップ、同期点、入力規模、指標(時間・アロケーション・スピードアップ)の定義。
  * 停止条件: 時間予算、反復上限、誤差許容など。
  * 環境: JULIA_NUM_THREADS、versioninfo()、GPUデバイス情報の記録。



* * *

# 14. よくある落とし穴と回避策(記述のみ)

  * 共有書き込み:スレッドが同じ配列要素へ書く設計は必ず壊れます。ローカル集計→縮約に改めます。
  * 原子過多:高頻度の原子更新はスレッドを序列化します。設計から原子を排除し、小さなローカルバッファを持ちます。
  * 小さすぎる並列:オーバーヘッドに負けます。単一スレッドに戻すか、粒度を上げます。
  * GPUスカラー:CuArrayに対する1要素の読み書きは遅く、禁止します。ブロードキャスト/カーネル融合に寄せます。
  * 転送乱発:ホスト↔デバイスの往復で遅くなります。転送はまとめ、演算はデバイス内で連鎖。
  * 乱数順序の揺れ:条件分岐の中で乱数を消費すると順序が揺れます。乱数生成と消費を外側で固定します。



* * *

# 15. 実務指針(記述のみ)

  * CPUはスレッドで「細粒度・メモリ共有の計算」を、Distributedで「粗粒度・タスク分配」を、GPUで「同型演算の一括処理」を担わせます。
  * 共有書き込みを避け、ローカル集計→縮約のパターンを徹底します。
  * 乱数の再現性はレイヤごとにルール化し、種と分割の仕様をコードに明記します。
  * 転送は最小化し、融合・インプレースで中間メモリを削減します。
  * 計測は同期点を設け、起動オーバーヘッドを意識して規模を選びます。
  * 環境・種・分割・計測指標をドキュメント化し、誰が実行しても同じ結論が得られる体制を整えます。



この設計と運用で、Threads・Distributed・CUDAを「正しく・速く・再現可能」に使い分け、Juliaの並列計算を現場水準へスケールさせられます。

* * *

# 第14章科学計算の実践:ModelingToolkitで使用奇術師徳(丸記述から買いへ)

# 1. 設計指針:記述(仕様)から解(数値)へ

科学計算の現場で強いモデルは、記号・数値のハイブリッドとして設計され、仕様をそのままコードに移し替えられるものです。JuliaのModelingToolkit.jl(以下、MTK)は、変数・パラメータ・微分演算子・方程式を宣言的に記述し、DifferentialEquations.jl(以下、DE)へ渡して高速・高精度に解けるよう設計されています。基本方針は次のとおりです。

  * 仕様の宣言:状態変数・パラメータ・時間・微分・代数制約・イベントを、式(~)で明示します。
  * 構造化:サブシステムを積み木のように合成し、再利用・検証を容易にします。
  * 簡約:structural_simplifyで自動の記号的整理(不要方程式削除、インデックス低減、代入展開)を行い、解きやすい形へ変換します。
  * 数値化:ODEProblem/DAEProblem/SteadyStateProblem等へ落とし込み、DEのソルバで解きます。
  * 感度・同定:自動微分・随伴法で感度を得て、Optimization.jlと組み合わせてパラメータ同定を行います。
  * 検証:不変量・境界・次元・単位、初期一致性をコードで検査し、再現可能なプロジェクトとして運用します。



* * *

# 2. 記号・数値ハイブリッドの基本構文

MTKでは、変数・パラメータ・微分演算子を宣言し、方程式を~で結びます。

using ModelingToolkit  


@variables t x(t) y(t) # 時間tに依存する状態  
@parameters α β δ γ # パラメータ  
D = Differential(t) # d/dt 演算子  


eqs = [  
D(x) ~ α*x - β*x*y,  
D(y) ~ δ*x*y - γ*y  
]  
sys = ODESystem(eqs, t, [x, y], [α, β, δ, γ])

  * @variables:未知の時間依存変数を宣言します。
  * @parameters:定数パラメータを宣言します。
  * Differential(t):時間微分演算子を得ます。
  * ~:方程式(左辺と右辺の同値)を表します。
  * ODESystem:常微分方程式系としてシステムを構築します(DAEはDAESystem)。



この記号的モデルから、structural_simplifyで整理された数値的モデルを生成し、DEへ渡します。

* * *

# 3\. ODEモデルの定義から解法まで(Lotka–Volterra を例に)

using ModelingToolkit, DifferentialEquations  


@variables t x(t) y(t)  
@parameters α β δ γ  
D = Differential(t)  


eqs = [  
D(x) ~ α*x - β*x*y,  
D(y) ~ δ*x*y - γ*y  
]  
@named lv = ODESystem(eqs, t, [x, y], [α, β, δ, γ])  


sys = structural_simplify(lv) # 記号的簡約  
u0 = [x => 1.0, y => 1.0] # 初期値  
p = [α => 1.5, β => 1.0, δ => 1.0, γ => 3.0]  
tspan = (0.0, 25.0)  


prob = ODEProblem(sys, u0, tspan, p) # 数値問題へ変換  
sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10, saveat=0.01)  


# 出力の参照  
t_grid = sol.t  
x_vals = sol[x]  
y_vals = sol[y]

要点

  * @namedで名前の付いたシステムを作ると、サブシステム合成時に参照が明快になります。
  * structural_simplifyは、冗長方程式や代入可能な関係を整理して、ソルバに相性の良い形へ変換します。
  * sol[var]で状態の時系列を取り出せます(saveatで出力グリッドを統一すると比較が容易)。



* * *

# 4. サブシステム合成:再利用可能なモデルを積み木で組む

モデルを部品化して合成できます。例として、2個の質点バネダンパ(直列)を合成します。

using ModelingToolkit, DifferentialEquations  


@variables t x1(t) v1(t) x2(t) v2(t)  
@parameters m1 c1 k1 m2 c2 k2 f(t)  
D = Differential(t)  


eqs1 = [  
D(x1) ~ v1,  
m1*D(v1) ~ f - c1*v1 - k1*x1 - k2*(x1 - x2) - c2*(v1 - v2)  
]  


eqs2 = [  
D(x2) ~ v2,  
m2*D(v2) ~ k2*(x1 - x2) + c2*(v1 - v2) - (k2*x2 + c2*v2)  
]  


@named s1 = ODESystem(eqs1, t, [x1, v1], [m1, c1, k1, k2, c2, f])  
@named s2 = ODESystem(eqs2, t, [x2, v2], [m2, c2, k2])  


sys = structural_simplify(ODESystem(s1.eqs ∪ s2.eqs, t,  
[x1, v1, x2, v2], [m1, c1, k1, m2, c2, k2, f]))  


u0 = [x1 => 0.0, v1 => 0.0, x2 => 0.0, v2 => 0.0]  
p = [m1 => 1.0, c1 => 0.1, k1 => 10.0,  
m2 => 1.0, c2 => 0.1, k2 => 5.0, f => 1.0]  
tspan = (0.0, 10.0)  
prob = ODEProblem(sys, u0, tspan, p)  
sol = solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10)

要点

  * サブシステムの方程式集合を結合し、一体でstructural_simplifyします。
  * connectなどの高位API(標準ライブラリの回路・機械系コネクタ)も活用可能ですが、まずは生の式合成から慣れると理解が深まります。



* * *

# 5\. DAE(微分代数方程式)の定式化:制約付き系の扱い

代数制約を含む系(例:ペンデュラムの幾何制約)をDAEとして記述します。

using ModelingToolkit, DifferentialEquations  


@variables t x(t) y(t) vx(t) vy(t) λ(t) # λはラグランジュ乗数  
@parameters m g L  
D = Differential(t)  


eqs = [  
D(x) ~ vx,  
D(y) ~ vy,  
m*D(vx) ~ -λ*x,  
m*D(vy) ~ -λ*y - m*g,  
x^2 + y^2 ~ L^2 # 幾何制約(代数方程式)  
]  


@named pend = ODESystem(eqs, t, [x, y, vx, vy, λ], [m, g, L])  
sys = structural_simplify(pend)  


u0 = [x => L, y => 0.0, vx => 0.0, vy => 0.0, λ => 0.0]  
du0 = [D(x) => 0.0, D(y) => 0.0, D(vx) => 0.0, D(vy) => 0.0, D(λ) => 0.0] # 一致性が必要  
p = [m => 1.0, g => 9.81, L => 1.0]  
tspan = (0.0, 5.0)  


prob = DAEProblem(sys, du0, u0, tspan, p)  
sol = solve(prob, DFBDF(); reltol=1e-8, abstol=1e-10, saveat=0.01)

要点

  * DAEではu0とdu0(微分値)の一致性が必要です(代数制約と運動方程式を同時に満たす)。不一致だと発散やエラーにつながります。
  * structural_simplifyがインデックス低減を図るため、安定性が向上します。
  * 剛性傾向が強ければ、陰的法(DFBDFなど)を選びます。



* * *

# 6. イベント・切替:物理的境界と政策介入を仕様記述する

モデルに境界到達や離散介入があるときは、DEのコールバックで仕様化します。例:バウンス(高さ0で速度反転)。

using DifferentialEquations  


g = 9.81; c = 0.8  
function fall!(du, u, p, t)  
du[1] = u[2]  
du[2] = -g  
end  


condition(u, t, integrator) = u[1] # 高さが0を跨ぐ  
function affect!(integrator)  
integrator.u[1] = 0.0  
integrator.u[2] = -c * integrator.u[2]  
end  
cb = ContinuousCallback(condition, affect!; rootfind=true)  


u0 = [10.0, 0.0]  
tspan = (0.0, 5.0)  
prob = ODEProblem(fall!, u0, tspan)  
sol = solve(prob, Tsit5(); callback=cb, reltol=1e-9, abstol=1e-12, saveat=0.01)

要点

  * イベントは「条件」と「作用」を分離して設計します。
  * rootfind=trueで到達時刻を精密化できます。
  * モデル側(MTK)で仕様、ソルバ側(DE)でイベントを扱うのが実務上の分担として自然です。



* * *

# 7. 記号的ヤコビアンとコード生成:高速化の基礎

MTKは記号的にヤコビアン・質量行列などを自動導出できます。ソルバへ渡すとステップ毎の線形代数が高速化されます。

using ModelingToolkit, DifferentialEquations  


@variables t u(t)  
@parameters a  
D = Differential(t)  
eqs = [D(u) ~ -a*u]  
@named sys = ODESystem(eqs, t, [u], [a])  


sys_simpl = structural_simplify(sys)  


# ODEFunctionへ変換(記号的ヤコビアンを含む)  
f_ode = ODEFunction(sys_simpl)  
u0 = [u => 1.0]  
p = [a => 0.5]  
tspan = (0.0, 10.0)  


prob = ODEProblem(f_ode, u0, tspan, p)  
sol = solve(prob, Rosenbrock23(); reltol=1e-10, abstol=1e-12)

要点

  * 記号ヤコビアンは陰的法や剛性系で効果が大きいです。
  * build_functionで純Julia関数へコード生成し、外部最適化との連携や埋め込みにも応用できます。



* * *

# 8. 感度解析:パラメータに対する応答の定量化

出力y(t; p)のパラメータ感度∂y/∂pを、随伴法や自動微分で計算できます。SciMLSensitivity.jlを経由してDEから指定します。

using ModelingToolkit, DifferentialEquations, SciMLSensitivity  


@variables t x(t) y(t)  
@parameters α β δ γ  
D = Differential(t)  
eqs = [D(x) ~ α*x - β*x*y,  
D(y) ~ δ*x*y - γ*y]  
@named lv = ODESystem(eqs, t, [x, y], [α, β, δ, γ])  
sys = structural_simplify(lv)  


u0 = [x => 1.0, y => 1.0]  
p = [α => 1.5, β => 1.0, δ => 1.0, γ => 3.0]  
tspan = (0.0, 20.0)  


prob = ODEProblem(sys, u0, tspan, p)  
tobs = 0:0.1:20  


sol = solve(prob, Tsit5(); saveat=tobs,  
sensealg = InterpolatingAdjoint()) # 随伴感度  


# 感度を取得(例:xに対するαの感度)  
# 実務ではSciMLSensitivityのAPIでパラメータごと・出力指標ごとに取り出します

要点

  * 感度は同定の前工程として、どのパラメータが目的指標に効いているかの判断材料になります。
  * 随伴法は長時間・多数パラメータで計算量を抑えられます。



* * *

# 9. モデル同定:Optimization.jlでパラメータを推定する

観測データに最も整合的なパラメータを最適化で求めます。損失関数は観測とモデル出力の差の平方和などを使います。

using ModelingToolkit, DifferentialEquations, SciMLSensitivity  
using Optimization, OptimizationOptimJL  


# モデル(Lotka–Volterra)  
@variables t x(t) y(t)  
@parameters α β δ γ  
D = Differential(t)  
eqs = [D(x) ~ α*x - β*x*y,  
D(y) ~ δ*x*y - γ*y]  
@named lv = ODESystem(eqs, t, [x, y], [α, β, δ, γ])  
sys = structural_simplify(lv)  


u0 = [x => 1.0, y => 1.0]  
tspan = (0.0, 20.0)  
tobs = 0:0.2:20  


# 観測データ(実務ではCSVから読み、前処理・スケーリングを済ませます)  
x_obs = rand(length(tobs)) # 例示用ダミー  
y_obs = rand(length(tobs))  


function loss(pvec)  
p = [α => pvec[1], β => pvec[2], δ => pvec[3], γ => pvec[4]]  
prob = ODEProblem(sys, u0, tspan, p)  
sol = solve(prob, Tsit5(); saveat=tobs, sensealg=InterpolatingAdjoint(),  
reltol=1e-8, abstol=1e-10)  
x_pred = Array(sol[x])  
y_pred = Array(sol[y])  
return sum((x_pred .- x_obs).^2) + sum((y_pred .- y_obs).^2)  
end  


p0 = [1.5, 1.0, 1.0, 3.0]  
optf = OptimizationFunction((θ, _) -> loss(θ), autodiff=:forward)  
optprob = OptimizationProblem(optf, p0)  
res = optimize(optprob, LBFGS(); maxiters=500)  
p̂ = res.minimizer

要点

  * sensealgを指定して随伴法を活用し、高速化します。
  * 正則化(L2等)で過適合を抑えます。観測ノイズモデルを設計し、尤度最大化へ拡張することも可能です。
  * 検証データによる汎化誤差評価を行い、推定の妥当性を確認します。



* * *

# 10. 仕様記述としてのモデル言語化:検証可能性を高める

仕様(仮定・制約・評価指標)をコードへ刻みます。

  * 不変量チェック:質量保存・非負制約・幾何制約等を@assertで検査。
  * 次元・単位の明示:スケール・無次元化の規則をコメント・コードへ明文化。
  * 初期一致性:DAEのu0/du0が制約を満たすことを事前検査。
  * APIの統一:solve_model(params; saveat, solver, tol)のように、入口を揃える。
  * ドキュメント文字列:方程式・パラメータの意味・範囲をdocstringで記し、?ヘルプで参照できるようにする。



仕様をコードに落とすことで、レビュー・再利用・自動化テストが容易になります。

* * *

# 11. 実務の前処理・後処理:データ整合・可視化・成果物

  * データ整合:DataFrames/CSVで列型・欠損・単位・スケーリングを確定。
  * 出力固定:saveatで比較可能な同一グリッドを採用。
  * 可視化:Plots/Makieで図を生成し、ラベル・単位・凡例を統一。
  * 成果物:Arrowで列指向に保存し、再読込・相互運用を高速化。
  * ログ:パラメータ・ソルバ・誤差・収束統計を記録(sol.destats等)。



* * *

# 12. ステディステート・根探し:定常解の計算

定常点はSteadyStateProblemやNLsolveで求められます。MTKから代数的にf(u)=0を取り出し、初期推定から解きます。

using ModelingToolkit, NonlinearSolve  


@variables x y  
@parameters α β δ γ  
eqs = [  
α*x - β*x*y ~ 0,  
δ*x*y - γ*y ~ 0  
]  
@named steady = NonlinearSystem(eqs, [x, y], [α, β, δ, γ])  
sys = structural_simplify(steady)  


p = [α => 1.5, β => 1.0, δ => 1.0, γ => 3.0]  
u0 = [x => 1.0, y => 1.0]  


prob = NonlinearProblem(sys, u0, p)  
sol = solve(prob, NewtonRaphson())  
x_eq = sol[x]; y_eq = sol[y]

要点

  * 定常点を出発点に、周辺の線形化・安定性解析へ繋げられます。
  * ODEの長時間解から終端値を初期推定にして非線形解法を回す手も実用的です。



* * *

# 13. 数値安定性とスケーリング:無次元化のすすめ

非線形・剛性が強い系では、スケール合わせ(無次元化)が特効薬です。

  * 状態・パラメータのオーダを揃え、reltol/abstolが自然な値になるよう設計します。
  * 小さな正則化(Σ + εI等)で条件数を改善します。
  * 記号的簡約後も、数値出力の桁と軸スケールを確認し、テストを通します。



* * *

# 14. チーム開発のための構成:モジュール化とテスト

  * モジュール化:MyModel.jlにモデル定義、solve.jlに求解関数、fit.jlに同定関数を分割。
  * Project.toml固定:依存パッケージ・バージョンを固定し、instantiateで再現性を確保。
  * テスト:不変量・境界・感度の符号・定常解の残差を@testで検査。
  * CI:主要パラメータセットで簡易ベンチ・検証を自動実行。
  * ドキュメント:方程式・パラメータ表・単位・データ前処理の仕様をdocs/へ。



* * *

# 15. 実例レシピ:質点バネダンパの同定ワークフロー(抜粋)

  1. モデル記述(バネ・ダンパ・外力):



@variables t x(t) v(t)  
@parameters m c k f(t)  
D = Differential(t)  
eqs = [ D(x) ~ v,  
m*D(v) ~ f - c*v - k*x ]  
@named msd = ODESystem(eqs, t, [x, v], [m, c, k, f])  
sys = structural_simplify(msd)

  2. 測定データの取り込み・スケーリング:



using CSV, DataFrames  
df = CSV.read("data.csv", DataFrame)  
tobs = df.time; x_obs = df.disp  
f_in = t -> 1.0*sin(2π*1.0*t) # 試験の入力力(仕様化)  
u0 = [x => x_obs[1], v => 0.0]  
p0 = [m => 1.0, c => 0.1, k => 10.0, f => f_in]

  3. 損失関数と最適化:



function lossθ(θ)  
p = [m => θ[1], c => θ[2], k => θ[3], f => f_in]  
prob = ODEProblem(sys, u0, (tobs[1], tobs[end]), p)  
sol = solve(prob, Tsit5(); saveat=tobs)  
x_pred = Array(sol[x])  
return sum((x_pred .- x_obs).^2)  
end  
optf = OptimizationFunction((θ,_) -> lossθ(θ), autodiff=:forward)  
optprob = OptimizationProblem(optf, [1.0, 0.1, 10.0])  
res = optimize(optprob, LBFGS())  
θ̂ = res.minimizer

  4. 検証:不変量・残差・再現図の生成、レポート出力。



# 再走・図出力(省略)

* * *

# 16. 高度機能への入口:PDE離散化と標準ライブラリ

MTKには、PDEを離散化(空間差分・自動離散)してODE系へ落とすユーティリティもあります(問題に応じ、空間離散を明示し、MTKのODEとして解くのが基本)。また、ModelingToolkitStandardLibraryには電気回路・機械系などの標準コネクタ・コンポーネントが用意されており、connectで接続してシステム合成が可能です。まずは小さな例から構文・流儀を掴み、必要に応じて標準ライブラリを取り込みます。

* * *

# 17. 性能運用の要点:ヤコビ・イベント・剛性・ログ

  * ヤコビの提供:陰的法・剛性で効果が高い。ODEFunction(sys)で供給。
  * イベントの粒度:到達時刻精密化は計算量増を伴うため、必要最小限に。
  * 剛性:Rodas5/DFBDF等を第一候補に、reltol/abstolを調整。
  * ログ:sol.destatsで関数評価数・拒否ステップ等を記録し、性能回帰を検知します。



* * *

# 18. 失敗の切り分け:一致性・スケール・可視化で原因を掴む

  * DAE初期不一致:u0/du0が代数制約を満たさない。制約から初期加速度・乗数を導出し直します。
  * スケール不整合:状態値が極端に大・小で誤差制御が効かない。無次元化・正則化。
  * イベント密集:切替が多すぎ、ステップが極端に縮む。仕様を緩和するか、近似モデルへ切替。
  * データ整合:観測列・単位・時刻対が不一致。前処理で統一し、saveatグリッドを揃える。
  * 可視化不足:生時系列・残差・フェーズ平面・パラメトリック図で挙動を点検します。



* * *

# 19. 再現可能なプロジェクト雛形(骨子)

# src/model.jl  
module MyModel  
using ModelingToolkit  
export build_system  


function build_system()  
@variables t x(t) y(t)  
@parameters α β δ γ  
D = Differential(t)  
eqs = [D(x) ~ α*x - β*x*y,  
D(y) ~ δ*x*y - γ*y]  
@named lv = ODESystem(eqs, t, [x, y], [α, β, δ, γ])  
return structural_simplify(lv)  
end  
end

# src/solve.jl  
using .MyModel, DifferentialEquations  
function solve_lv(p; u0=(1.0,1.0), tspan=(0.0,25.0), saveat=0.01)  
sys = MyModel.build_system()  
u0map = Dict(sys.states[1] => u0[1], sys.states[2] => u0[2])  
pmap = Dict(sys.params[1] => p[1], sys.params[2] => p[2],  
sys.params[3] => p[3], sys.params[4] => p[4])  
prob = ODEProblem(sys, u0map, tspan, pmap)  
return solve(prob, Tsit5(); reltol=1e-8, abstol=1e-10, saveat=saveat)  
end

# src/fit.jl  
using Optimization, OptimizationOptimJL, SciMLSensitivity  
function fit_lv(obs_t, obs_x, obs_y; p0=[1.5,1.0,1.0,3.0])  
sys = MyModel.build_system()  
u0map = Dict(sys.states[1] => 1.0, sys.states[2] => 1.0)  
function lossθ(θ)  
pmap = Dict(sys.params[1] => θ[1], sys.params[2] => θ[2],  
sys.params[3] => θ[3], sys.params[4] => θ[4])  
prob = ODEProblem(sys, u0map, (obs_t[1], obs_t[end]), pmap)  
sol = solve(prob, Tsit5(); saveat=obs_t, sensealg=InterpolatingAdjoint(),  
reltol=1e-8, abstol=1e-10)  
return sum((Array(sol[sys.states[1]]) .- obs_x).^2) +  
sum((Array(sol[sys.states[2]]) .- obs_y).^2)  
end  
optf = OptimizationFunction((θ,_) -> lossθ(θ), autodiff=:forward)  
optprob = OptimizationProblem(optf, p0)  
res = optimize(optprob, LBFGS())  
return res.minimizer  
end

  * Project.toml/Manifest.tomlで依存を固定し、instantiateで再現可能に。
  * test/で簡易検証を提供。不変量・残差・パラメータ境界をチェック。



* * *

# 20. 実務の心得(記述のみ)

  * 仕様はコードに刻み、方程式・制約・単位・初期条件・イベント・評価指標を明文化します。
  * structural_simplifyを通してから解き、ヤコビを供給できる場合は供給します。
  * DAEは一致性が命。初期値・微分初期値の組を代数制約と整合させます。
  * 感度→同定→検証の順で進め、正則化・交差検証・可視化で妥当性を支えます。
  * 出力グリッド・乱数種・ソルバ属性・環境を固定し、誰がいつ実行しても同じ結論に到達できる体制を整えます。



この流儀で、ModelingToolkitを中核に「記述から解へ」を一気通貫で運用でき、科学計算の仕様記述言語としてのJuliaの強みを最大限に引き出せます。

* * *

# 第15章機械学習と確率的プログラミング:Flux・MLJ・Turing

# 1. 設計指針:データ→前処理→モデル→学習→検証→配布の一気通貫と再現性

機械学習と確率的プログラミングを現場で安定運用するためには、(1) データの取り込みと前処理、(2) モデル定義、(3) 学習とハイパーパラメータ管理、(4) 検証と評価、(5) 追試可能な配布・再利用、という流れを明確に分け、各段階に再現性の仕組み(乱数種、環境固定、出力フォーマット)を組み込むことが重要です。Juliaでは、深層学習はFlux.jl、統一インターフェースでの機械学習はMLJ.jl、ベイズ推論やMCMCはTuring.jlで扱えます。本章では、これらを「同じプロジェクト内で」連携できるように、実用的なコードと作法に絞って解説します。

* * *

# 2. 再現性の基礎:乱数種・環境・ログ・出力グリッド

  * 乱数種: Random.seed!でセッション開始時に固定し、DataLoaderやshuffleの挙動も種に依存させます。並列時はスレッド・プロセスごとに決定論的ルール(例: seed + threadid())を適用します。
  * 環境: Project.toml/Manifest.tomlでパッケージバージョンを固定し、\--project=.で起動します。
  * ログ: ハイパーパラメータ、学習曲線、評価指標、モデルのサイズ、乱数種、日付を記録します。
  * 出力: モデルと重みは可搬な形式(BSON/JLSO)で保存し、メタデータ(前処理のパイプライン定義、特徴量一覧)を同梱します。



using Random  
Random.seed!(20240101)

* * *

# 3. データ前処理の基本:スケーリング・エンコーディング・分割

数値特徴量はスケーリング(標準化・最小最大)、カテゴリはエンコーディング(one-hot、ターゲットエンコード)、文字列は正規化(大小・空白・全半角など)を一貫して行います。学習・検証・テストに分割し、リークを防ぎます。

using DataFrames, Statistics  


# 例:標準化(学習集合の統計量で揃える)  
function standardize!(df::DataFrame, cols::Vector{Symbol})  
μ = Dict{Symbol,Float64}()  
σ = Dict{Symbol,Float64}()  
for c in cols  
μ[c] = mean(skipmissing(df[!, c]))  
σ[c] = std(skipmissing(df[!, c]))  
df[!, c] = ((df[!, c] .- μ[c]) ./ max(σ[c], eps()))  
end  
return (μ, σ) # テスト適用用に返す  
end

データ分割は乱数種固定で行い、IDシャッフルの順序が毎回同一になるようにします。

* * *

# 4\. Fluxによる深層学習:モデル定義・損失・オプティマイザ・学習ループ

Flux.jlは「書いたとおりに動く」シンプルなAPIです。基本は、Chainでモデルを定義し、損失関数、オプティマイザ(ADAMなど)を選び、gradientで勾配計算してパラメータを更新します。

using Flux, Random  


Random.seed!(20240101)  


# 例:多層パーセプトロン(回帰)  
model = Chain(  
Dense(20, 64, relu),  
Dense(64, 64, relu),  
Dense(64, 1) # 出力1  
)  


loss(ŷ, y) = Flux.Losses.mse(ŷ, y)  
opt = Flux.Adam(1e-3)  


# ダミーデータ(実務ではDataLoaderでバッチ化)  
X = rand(Float32, 20, 1024) # 特徴量 20次元 × サンプル数  
Y = rand(Float32, 1, 1024)  


ps = Flux.params(model)  


for epoch in 1:50  
grads = gradient(ps) do  
ŷ = model(X)  
loss(ŷ, Y)  
end  
Flux.Optimise.update!(opt, ps, grads)  
@info "epoch=$epoch mse=$(loss(model(X), Y))"  
end

ポイント

  * バッチ学習: Flux.Data.DataLoaderでバッチ化し、shuffle=trueでも種固定で順序再現。
  * 正則化: Dropout、L2(重み減衰)、早期打ち切り(検証損失の悪化で停止)。
  * GPU: CUDA.jlがロードされていればgpu(model)やgpu(X)でデバイスへ移動(積み替えは最小限に)。
  * 評価: 検証集合でRMSE/MAE/Accuracy/AUCなどを定期的に計測。



using Flux: DataLoader  
loader = DataLoader((X, Y), batchsize=64, shuffle=true)  
for epoch in 1:50  
for (xb, yb) in loader  
grads = gradient(ps) do  
loss(model(xb), yb)  
end  
Flux.Optimise.update!(opt, ps, grads)  
end  
end

* * *

# 5. 特徴量前処理と評価の設計:パイプライン化・早期停止・モデル選択

評価の品質を守るために、前処理→モデル→評価を「ひとつの関数」にまとめ、検証でのみ使う乱数要素(シャッフル)も固定し、リーク防止のため学習集合の統計量でのみ適用します。早期停止は「検証損失が連続して改善しない回数」を閾値として止めるのが実務的です。

struct EarlyStopper  
patience::Int  
best::Float64  
count::Int  
end  


function update!(es::EarlyStopper, val::Float64)  
if val < es.best - 1e-8  
es.best = val; es.count = 0  
else  
es.count += 1  
end  
return es  
end

* * *

# 6\. MLJによる統一インターフェース:モデル選択・前処理・チューニング

MLJ.jlは、異なる学習器を共通インターフェースで扱い、Tableデータ、パイプライン、評価指標、交差検証、ハイパーパラメータ最適化を統合します。Fluxモデルもラップ可能ですが、まずは既製の学習器(決定木、ランダムフォレスト、ロジスティック回帰など)で手順を掴むと良いです。

using MLJ  


# データ(DataFrame)をMLJのテーブルへ  
X_tbl = MLJ.table(df[:, Not(:target)])  
y = df.target  


# モデルの選択(例:決定木)  
DecisionTreeRegressor = @load DecisionTreeRegressor pkg=DecisionTree  
tree = DecisionTreeRegressor(max_depth=5)  


# パイプライン(標準化→モデル)  
Standardizer = @load Standardizer pkg=MLJModels  
pipe = Standardizer() |> tree  


mach = machine(pipe, X_tbl, y)  
fit!(mach)  


# 予測と評価  
yhat = predict(mach, X_tbl) |> MLJ.unwrap  
r2 = MLJ.r_squared(yhat, y)

交差検証とグリッドサーチ

r = range(tree, :max_depth, lower=3, upper=12)  
self_tuning = TunedModel(model=pipe,  
resampling=CV(nfolds=5, shuffle=true),  
measure=rms,  
ranges=[r],  
tuning=Grid(resolution=10),  
acceleration=CPUThreads())  
mt = machine(self_tuning, X_tbl, y)  
fit!(mt)  
best_model = fitted_params(mt).best_model

ポイント

  * 前処理の一体化: Standardizerなどをパイプに含めて、学習・推論で同じ処理を適用。
  * 評価指標: 回帰ならrms/r_squared/mae、分類ならaccuracy/aucなど。
  * 再現性: CV(..., shuffle=true)の乱数種はセッション種に依存するため、冒頭で固定。



* * *

# 7\. Turingによるベイズ推論:モデル化・MCMC・事後予測

Turing.jlは確率モデルを直接記述し、NUTS/HMC/GibbsなどのMCMCで事後分布をサンプリングします。例として、ベイズ線形回帰を示します。

using Turing, Random, Statistics, Distributions  


Random.seed!(20240101)  
Turing.setadbackend(:forwarddiff) # ADの選択(状況に応じて)  


# 合成データ  
N = 500  
x = rand(N); true_a = 2.0; true_b = -0.5; σ = 0.2  
y = true_a .* x .+ true_b .+ σ .* randn(N)  


@model function bayes_linreg(x, y)  
a ~ Normal(0, 5) # 事前(傾き)  
b ~ Normal(0, 5) # 事前(切片)  
σ ~ truncated(Normal(0, 1), 0, Inf)  
for i in eachindex(x)  
y[i] ~ Normal(a * x[i] + b, σ)  
end  
end  


model = bayes_linreg(x, y)  
chain = sample(model, NUTS(), 2_000; discard_adapt=true)  


# 事後要約  
a_post = mean(chain[:a])  
b_post = mean(chain[:b])  
σ_post = mean(chain[:σ])

事後予測

# 新しいxでの予測分布(事後パラメータサンプルで生成)  
x_new = 0.3  
ys = [rand(Normal(chain[:a][i]*x_new + chain[:b][i], chain[:σ][i])) for i in 1:length(chain[:a])]  
mean_pred = mean(ys); interval = quantile(ys, [0.025, 0.975])

ポイント

  * 事前分布はドメイン知識に基づき、過度に広い/狭い事前は避けます。
  * NUTS設定: アダプテーション期間やステップ数、質量行列の推定などを調整。
  * 収束診断: gelman_rubin_diagnostic、トレースプロット、effective_sample_sizeで混合と有効サンプル数を確認。
  * 再現性: Random.seed!の固定に加え、並列サンプル時はチェーンごとに種をずらして規則化。



* * *

# 8\. Flux×Turingの橋渡し:確率的深層学習の最小例

小さなネットワークの重みに事前分布を置いて、事後推論することも可能ですが、計算量が増えるため、まずは線形/浅いモデルで感触を掴みます。実務では変分推論(VI)や事後近似を用いてスケールさせます(ここではMCMCの最小骨子のみに留めます)。

* * *

# 9. 乱数並列時の再現性:スレッド・プロセス・GPU

  * スレッド: スレッドローカルにMersenneTwister(seed + threadid())を配布。データの分割規則を固定。
  * 分散: ワーカーID(myid())に基づく種を配布し、pmapの順序を固定(入力リスト順)。
  * GPU: 乱数列はCPUで生成→GPUへ転送し、消費順序を固定(カーネル内での生成は複雑化しやすい)。



* * *

# 10. 評価指標と検証:ホールドアウト・交差・時系列の注意

  * ホールドアウト: 学習/検証/テストの3分割で最小限のバイアスチェック。
  * 交差検証: CV(nfolds=k)で安定性評価。ハイパーパラメータ選択に活用。
  * 時系列: 時系列はシャッフル禁止・時系列分割(rolling)で検証。リーク防止のため、未来情報を使わない。
  * クラス不均衡: AUC、適合率/再現率、F1、ROC曲線を併用し、しきい値最適化を行います。



* * *

# 11. モデル配布:重み・前処理・メタデータの一体化

配布時は、(1) モデル構造(コード)、(2) 重み(BSONやJLSO)、(3) 前処理パイプライン(スケーラの平均・分散、エンコーダのカテゴリ辞書)、(4) 乱数種・バージョン・依存関係、を一体化します。

using BSON: @save, @load  


# 保存  
@save "model.bson" model μ σ # 例:標準化の統計量も保存  


# 復元  
@load "model.bson" model μ σ

MLJのパイプラインはsaveで一括保存し、推論時に同じ前処理を適用します。

* * *

# 12. 実戦レシピ:Fluxでの分類、MLJでの比較、Turingでの不確実性評価

## 12.1 Fluxで二値分類(簡略版)

using Flux, Random  
Random.seed!(20240101)  


model = Chain(Dense(10, 32, relu), Dense(32, 2), softmax)  
loss(ŷ, y) = Flux.Losses.logitcrossentropy(ŷ, y)  
opt = Flux.Adam(1e-3)  


X = rand(Float32, 10, 2048)  
Y = Flux.onehotbatch(rand(0:1, 2048), 0:1)  


for epoch in 1:30  
grads = gradient(Flux.params(model)) do  
loss(model(X), Y)  
end  
Flux.Optimise.update!(opt, Flux.params(model), grads)  
end

## 12.2 MLJでベースライン比較

using MLJ  
@load LogisticClassifier pkg=MLJLinearModels  
clf = LogisticClassifier()  
mach = machine(clf, MLJ.table(X'), argmax.(eachcol(Y))) # ラベルへ戻す  
fit!(mach)  
yhat = predict(mach, MLJ.table(X'))  
acc = MLJ.accuracy(yhat, argmax.(eachcol(Y)))

## 12.3 Turingで閾値の不確実性評価(概念)

分類のスコアにノイズモデルを重ね、最適閾値の分布を推定する...といった拡張も可能です。まずは外生的に閾値を動かし、再現性ある指標(AUC)で比較し、必要ならベイズ的な枠組みに移行します。

* * *

# 13. ハイパーパラメータ最適化:探索空間・評価・停止条件

  * 探索空間: 学習率、層幅、正則化強度、バッチサイズ、ドロップアウト率など。
  * 評価: 交差検証の平均指標、分布のばらつき。
  * 停止: 時間制限、試行数上限、改善が止まったら終了。
  * 記録: 試行ごとの設定・指標・モデルサイズ・乱数種を記録し、後で再現できるようにします。



MLJのTunedModelや、Fluxでは自前ループ+Grid/Random/Bayesian探索の実装で対応します。

* * *

# 14. パイプラインの品質保証:テスト・プロファイリング・監査

  * テスト: 前処理の単体テスト(標準化の平均0・分散1になるか)、学習の再現(同じ種で同じ損失推移)、推論の再現(同じ入力で同じ出力)。
  * プロファイリング: 学習時間、アロケーション、GPUメモリ占有、I/O待ちを計測。
  * 監査: データの欠損率・分布変化(ドリフト)・ラベル品質・モデルの公平性指標(必要に応じて)。



* * *

# 15. 実務上の注意:ドリフト対策・分布外検知・堅牢化

  * ドリフト: 本番データの分布が学習時とズレたら、再学習やオンライン更新で対応。
  * 分布外検知: 異常値に弱いモデルには、一貫した前処理と外れ値ロバストな指標を用意。
  * 堅牢化: L2正則化、データ拡張(画像・音声・テキストでの適切な拡張)、アンサンブルで安定性向上。



* * *

# 16. 実務指針(記述のみ)

  * 乱数種・環境・ハイパーパラメータを固定し、ログと成果物を必ず残します。
  * 前処理とモデルを一体化したパイプラインを設計し、学習時の統計量を推論時にも適用します。
  * Fluxでの深層学習は、バッチ・正則化・早期停止・評価を組み合わせ、GPU転送の最小化を徹底します。
  * MLJでベースライン比較とハイパーパラメータ探索を行い、指標のばらつきを評価します。
  * Turingで不確実性を明示化し、事前・事後・事後予測を一体で記述して推論します。
  * モデル配布では、重み・前処理・メタデータ・乱数種・依存関係のセットを提供し、追試可能性を担保します。



この流儀により、Flux・MLJ・Turingのそれぞれの強み(表現力・統合インターフェース・不確実性の扱い)を一つのプロジェクトの中で活かし、現場水準の品質と再現性を両立できます。

* * *

# 第16章 DSL設計と仕様記述言語としてのJulia(◇領域固有の文法を作る)

# 1. 目的と要件定義:領域固有言語(DSL)で何を実現するか

領域固有言語(DSL)は、ある分野の専門概念(対象・関係・仮定・制約・評価)を簡潔で誤解のない表現で記述し、そのまま実行・検証・可視化・配布へつなげるための言語です。Juliaは、強力な型システムと多重ディスパッチ、メタプログラミング(Expr・マクロ・@generated)を備えており、DSLを軽量かつ高性能に設計できます。要件定義では次を明確にします。

  * 語彙(何を表すか):対象、属性、関係、制約、イベント、目的、評価指標。
  * 文法(どう書くか):宣言的(仕様を記述)/命令的(手順を記述)、ブロック構造、式の形。
  * 意味論(どう解釈するか):「仕様→中間表現→実行計画→結果」の変換規則。
  * 検証可能性(どう誤りを検出するか):型・値域・単位・一貫性・可観測性・再現性。
  * 連携(どう外部とつなぐか):シミュレータ・最適化器・可視化器・データ源。
  * 運用(どう保守するか):拡張性・後方互換性・バージョニング・ドキュメント・テスト。



本章では、Juliaの型・マクロ・メタプログラミングを駆使して、検証可能な仕様記述言語を作るための設計指針と具体例を示します。

* * *

# 2. 記法設計方針:宣言的中心・命令的補助・最小核の徹底

DSLは、宣言的に「仕様」を書けることを優先します。命令的なユーティリティは補助に留め、核となる仕様は式の集合で表します。

  * 宣言的:objective = ...、constraint: ...、event: when ... do ... のような表現。
  * 最小核:目的・制約・パラメータ・データ・モデル構造のごく基本セットから始める。
  * 拡張点:プラグイン式に演算子や構文糖衣を足せるフック(型・マクロ)を用意する。
  * 直交性:仕様の各概念が互いに独立(直交)で、組み合わせが爆発しないように設計。



悪い例は、糖衣に偏りすぎて意味的曖昧さを招くこと/構文の自由度が高すぎて解析が困難になることです。まず「語彙と意味」を型で固め、上に薄い記法を乗せます。

* * *

# 3. 型設計:抽象型・構造体・パラメトリック型で語彙を固定する

DSLの語彙は型で表現します。抽象型でインターフェースを定義し、具象型で表現と最適化を担います。パラメトリック型で汎用性と型安全を両立します。

# 目的・制約・イベントの語彙  
abstract type Objective end  
abstract type Constraint end  
abstract type EventSpec end  


# 目的関数(最小化/最大化)  
struct Minimize <: Objective  
expr::Expr  
end  
struct Maximize <: Objective  
expr::Expr  
end  


# 代表的制約  
struct Eq <: Constraint  
lhs::Expr  
rhs::Expr  
end  
struct Le <: Constraint  
lhs::Expr  
rhs::Expr  
end  
struct Ge <: Constraint  
lhs::Expr  
rhs::Expr  
end  


# イベント仕様  
struct When <: EventSpec  
cond::Expr # 条件式(ゼロ交差など)  
act::Expr # 作用(更新・介入)  
end  


# モデル全体(仕様のコンテナ)  
struct SpecModel  
objective::Objective  
constraints::Vector{Constraint}  
events::Vector{EventSpec}  
params::Dict{Symbol,Any}  
end

Exprを保持するのは、宣言と実行計画の生成を分けるためです。後段で型化・展開・実行に落とします。代替案として、式を直接型付き表現(ASTの独自型)へ解析しておくと、検証や最適化が容易になります。

* * *

# 4. マクロ設計:衛生・esc・gensym・ブロック構文

マクロはパース後・型推論前に式を変換する仕組みです。ユーザー式を受け、衛生的に展開し、SpecModelを組み立てるスケルトンを用意します。

macro spec(ex)  
# ブロック専用  
ex.head == :block || error("@spec は begin ... end ブロックで使います")  
objective = nothing  
constraints = Expr[]  
events = Expr[]  
params = Dict{Symbol,Any}()  


for stmt in ex.args  
if stmt.head == :(=) && stmt.args[1] == :objective  
objective = stmt.args[2]  
elseif stmt isa Expr && stmt.head == :call && stmt.args[1] == :constraint  
push!(constraints, stmt)  
elseif stmt isa Expr && stmt.head == :call && stmt.args[1] == :event  
push!(events, stmt)  
elseif stmt.head == :(=) && stmt.args[1] == :param  
# 例:param = (:alpha => 0.1) のような入力を期待  
pair = stmt.args[2]  
params[pair.args[1]] = pair.args[2]  
end  
end  
objective === nothing && error("objective がありません")  


# Objective 型へ包む  
obj_node = :(Minimize($(esc(objective)))) # 例では最小化に固定(実務では切替)  
cons_nodes = [ :(Le($(esc(c.args[2])), $(esc(c.args[3])))) for c in constraints ] # constraint(lhs <= rhs)  
evt_nodes = [ :(When($(esc(e.args[2])), $(esc(e.args[3])))) for e in events ]  
# SpecModel の生成  
return :(SpecModel($obj_node, $cons_nodes, $evt_nodes, $(esc(params))))  
end

衛生の要点:

  * ユーザー式・識別子は必ずescして呼び出し元スコープへ返す。
  * マクロ内部で導入する識別子はgensymで一意化する(衝突防止)。
  * 論理チェック(期待される式形でなければ明確にerror)を行い、曖昧さを避ける。



実務では、MacroTools.jl的な構文支援があると便利ですが、まずは自前で式形を確かめる基本を徹底します。

* * *

# 5. 文法の最小例:目的・制約・イベントを宣言する

最小の宣言的文法を定義する関数(ダミー)を用意して、マクロが拾えるようにします。

# ダミーのマーカー関数(@spec が拾う)  
constraint(lhs, rhs) = :(lhs <= rhs) # プレースホルダ  
event(cond, act) = :(when cond then act) # プレースホルダ  


# 使用例  
spec = @spec begin  
objective = x^2 + y^2  
constraint(x, 1.0) # "x <= 1.0" のつもり(簡素化した例)  
constraint(y, 2.0)  
event(x - 0.5, :(x = 0.5)) # 条件: x-0.5 のゼロ交差、作用: x=0.5へ投影  
param = (:alpha => 0.1)  
end

この時点ではSpecModelが得られただけです。次に検証→中間表現→実行計画へ進みます。

* * *

# 6. 検証可能な仕様記述:型チェック・値域・単位・一貫性

仕様が「実行に耐える」ために、検証を内蔵します。最低限のチェックを定義します。

# 単純な検証(拡張の入口)  
struct ValidationError <: Exception  
msg::String  
end  


function validate(spec::SpecModel)  
# 目的が式か  
spec.objective isa Objective || throw(ValidationError("objective が不正です"))  
# 制約は少なくとも1つ  
isempty(spec.constraints) && throw(ValidationError("制約がありません"))  
# パラメータ値の範囲(例)  
for (k,v) in spec.params  
k == :alpha && !(0.0 <= v <= 1.0) && throw(ValidationError("alpha の範囲が不正"))  
end  
return spec  
end

強化案:

  * 単位付き量を型に織り込む(Quantity{T,U})ことで単位不整合を型レベルで検出。
  * 値域・境界・不変量(非負・保存則・整合条件)の検査を宣言可能にし、実行前に自動チェック。
  * 代数的矛盾(同値式の重複・不可能条件)を記号的に検出(段階的に導入)。



* * *

# 7. 中間表現と実行計画:コンパイルステップの設計

SpecModelを、実行機構に近い中間表現へ変換する層を用意します。ここで、型化・簡約・最適化を行います。

# 例:目的・制約を関数へ落とす  
struct CompiledSpec  
objective::Function # x,y→cost  
constraints::Vector{Function} # x,y→Bool(可行性)  
callback::Function # state→state(イベント作用)  
params::Dict{Symbol,Any}  
end  


function compile(spec::SpecModel)  
validate(spec)  
# 目的関数(簡略例:x,y を引数に取る)  
obj = let ex = spec.objective::Minimize  
(x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), ex.expr))  
end  
# 制約関数群  
cons = map(spec.constraints) do c  
if c isa Le  
lhs, rhs = c.lhs, c.rhs  
(x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), :($(lhs) <= $(rhs))))  
elseif c isa Ge  
lhs, rhs = c.lhs, c.rhs  
(x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), :($(lhs) >= $(rhs))))  
elseif c isa Eq  
lhs, rhs = c.lhs, c.rhs  
(x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), :($(lhs) == $(rhs))))  
else  
(x,y) -> false  
end  
end  
# イベント(簡略例)  
cb = if isempty(spec.events)  
st -> st  
else  
ev = spec.events[1]  
(st::NamedTuple) -> begin  
cond = eval(Expr(:block, :(x=$(st.x)), :(y=$(st.y)), ev.cond))  
if cond == 0  
# 作用を適用(x = 0.5 など)  
xnew = eval(Expr(:block, :(x=$(st.x)), :(y=$(st.y)), ev.act))  
(; x = xnew, y = st.y)  
else  
st  
end  
end  
end  
return CompiledSpec(obj, cons, cb, spec.params)  
end

本例は最小の雛形です。実務では、evalを減らし、式→ネイティブ関数のコード生成(build_function相当)へ寄せることで高速化・安全化します。

* * *

# 8. シミュレータ連携:仕様から数値へ(ODE・イベントの橋渡し)

仕様をシミュレータへ渡すために、実行計画を更新則・イベント則へ落とす関数を用意します。簡略例として、x,yの更新とイベントの適用を行うステップ関数を設計します。

# ステップ更新(仮:勾配降下風)  
function step!(cs::CompiledSpec, state::NamedTuple; η=0.1)  
# 勾配はデモ目的で有限差分(実務では自動微分へ)  
x, y = state.x, state.y  
fx = cs.objective(x, y)  
dfdx = (cs.objective(x + 1e-6, y) - fx) / 1e-6  
dfdy = (cs.objective(x, y + 1e-6) - fx) / 1e-6  
x_new = x - η * dfdx  
y_new = y - η * dfdy  
# イベント適用  
st2 = cs.callback((; x=x_new, y=y_new))  
return st2  
end  


function feasible(cs::CompiledSpec, state::NamedTuple)  
all(f -> f(state.x, state.y), cs.constraints)  
end  


# 実行例  
st = (; x=0.9, y=1.5)  
for k in 1:100  
st = step!(compile(spec), st)  
feasible(compile(spec), st) || break  
end

実際のシミュレータ(ODE・最適化・制約充足)に渡すには、シグネチャ整備(f!(du,u,p,t)など)とイベントコールバック(条件・作用分割)が重要です。DSL側では、仕様→シミュレータAPIへの写像を一箇所に集約します。

* * *

# 9. エラーメッセージ:出典・位置・対処法を明示する

DSLの品質はエラーメッセージで決まります。何が・どこで・なぜ・どう直すかを示す方針を徹底します。

  * 何が:alpha は [0,1] にない、objective が未定義、constraint の形が不正。
  * どこで:式のスパン(LineNumberNode)を保持し、ブロック内の位置を示す。
  * なぜ:期待された文法・型・値域を明確に記載。
  * どう直すか:有効な例・既定値・利用可能なオプションを提示。



マクロ展開時に、式と位置情報をラップしておくと、エラーに位置を付せます。

* * *

# 10. 保守性と拡張性のバランス:小さな核・プラグイン・直交性

成功するDSLは「小さな核」を保ち、拡張はプラグインで分離します。

  * 核:目的・制約・イベント・パラメータ・検証・コンパイル。
  * 拡張:追加の演算子・構文糖衣・ドメイン固有の検証ルール・最適化。
  * 直交性:拡張が核の他要素へ副作用を持たない(影響範囲が局所)。
  * プラグイン登録:register!(dsl_extension)のように明示的に拡張を有効化。



無秩序な拡張は互換性を崩します。後方互換は固定し、破壊的変更はメジャーバージョンでのみ許容します(セマンティックバージョニング)。

* * *

# 11. 成功・失敗から学ぶ設計指針

成功例に通底する要素:

  * 語彙と意味の型化:抽象型・具象型・パラメトリック型でインターフェースを固定。
  * 宣言的中心:仕様を式で書き、命令は補助へ。
  * 検証内蔵:型・値域・単位・整合性のチェックを初期化時に行う。
  * メッセージ品質:エラーが読みやすく、修正指針が明確。
  * テスト:不変量・境界・互換性・パフォーマンスの回帰を監視。



失敗例に通底する要素:

  * 型海賊:他人の型へ他人の関数をグローバル拡張(予測不能な挙動)。
  * 曖昧文法:複数解釈が可能な構文で誤解・解析困難。
  * 過度な糖衣:短期的快適だが意味を隠し、検証と最適化を阻害。
  * 互換性軽視:拡張で既存仕様を壊し、再現性が崩れる。
  * 計測不足:性能・メモリ・起動遅延・エラー率の把握がない。



* * *

# 12. 仕様→最適化・シミュレーション連携の最小レシピ

DSLから最適化へ渡す場合の雛形(目的・制約を関数へ落とす):

# 評価関数(最小化)  
function cost(cs::CompiledSpec, x::Float64, y::Float64)  
return cs.objective(x, y)  
end  


# 可行性チェック  
function feasible(cs::CompiledSpec, x::Float64, y::Float64)  
return all(f -> f(x, y), cs.constraints)  
end  


# 乱暴な座標降下(例)  
function solve_min(cs::CompiledSpec; x0=0.0, y0=0.0, η=0.01, maxiter=10_000)  
x, y = x0, y0  
best = (x=x, y=y, c=cost(cs, x, y))  
for k in 1:maxiter  
# 最急降下の簡易近似  
c0 = cost(cs, x, y)  
dfdx = (cost(cs, x+1e-6, y) - c0)/1e-6  
dfdy = (cost(cs, x, y+1e-6) - c0)/1e-6  
x -= η*dfdx; y -= η*dfdy  
if feasible(cs, x, y)  
c1 = cost(cs, x, y)  
if c1 < best.c  
best = (x=x, y=y, c=c1)  
end  
else  
# 反動で戻すなどの戦略  
x += η*dfdx; y += η*dfdy  
end  
end  
return best  
end  


best = solve_min(compile(spec))

DSLからシミュレーションへ渡すときは、step!・イベント・状態型(構造体)を整え、ループ・停止条件・ログ・可視化を組み合わせます。仕様側で状態・更新・制約・イベントを宣言し、ブリッジ層でシミュレータAPIへ写像するのが定石です。

* * *

# 13. メタプログラミングによる最適化:@generated・コード生成の適用範囲

@generatedは型パラメータに依存するコード展開に向きます(タプル長に応じた展開・固定サイズ演算のアンロールなど)。DSLでは、静的に決まる部分(固定次元の線形代数・演算テンプレート)に限って使用し、動的値に依存する生成は避けます。

@generated function sum_tuple(t::NTuple{N,Float64}) where {N}  
ex = reduce((a,b)->:( $a + $b ), [:(t[$i]) for i in 1:N])  
return ex  
end

適用指針:

  * 速度の要が静的構造にあるときのみ採用。
  * 生成コードの検証(@code_typed・@code_llvm)とベンチで回帰監視。
  * 過度なメソッド増殖・巨大Unionの生成を避ける(コンパイル負荷になる)。



* * *

# 14. 品質保証:テスト・プロファイリング・ドキュメント・バージョニング

  * テスト:文法・検証・コンパイル・連携の単体テストを用意。仕様の最小例・境界例・エラー例を網羅。
  * プロファイリング:@time・BenchmarkTools.@btimeで起動遅延・実行時間・アロケーションを計測。
  * ドキュメント:語彙・意味・文法・例・拡張点・エラー一覧・FAQを整備。docstringで?ヘルプも提供。
  * バージョニング:セマンティックバージョニング(MAJOR.MINOR.PATCH)。破壊的変更はメジャーで告知・移行ガイド同梱。



* * *

# 15. 実務の前処理・後処理:データ・可視化・成果物の一体化

  * データ前処理:DataFrames/CSVで型・単位・欠損の整合を確保。仕様のparamsへ安全に渡す。
  * 可視化:Plots/Makieで仕様→結果を一体で図化。凡例・軸・単位を統一し、再生成可能なスクリプトにする。
  * 成果物:Arrowで中間生成物を列指向保存。仕様・コンパイル結果・ログ・図を同梱して配布。



* * *

# 16. 拡張設計パターン:タグ・トレイト・インターフェース準拠

DSLの拡張は、タグディスパッチとトレイトが有効です。例えば、制約の種類・解法の選択をタグで切り替えます。

abstract type SolverTag end  
struct FastGrad <: SolverTag end  
struct Robust <: SolverTag end  


solve(cs::CompiledSpec, st; ::FastGrad) = solve_min(cs; x0=st.x, y0=st.y, η=0.05)  
solve(cs::CompiledSpec, st; ::Robust) = solve_min(cs; x0=st.x, y0=st.y, η=0.005)  


# トレイト(能力判定)  
has_event(::CompiledSpec) = true # 例(本来はeventsの有無を見る)

インターフェース準拠:独自配列型や状態型を設計する場合、Juliaの標準インターフェース(getindex・iterate・sizeなど)を満たすことでエコシステムと自然に接続できます。

* * *

# 17. 運用設計:プロジェクト骨子とワークフロー

  * src/分割:syntax.jl(文法・マクロ)/types.jl(語彙型)/compile.jl(中間表現)/bridge.jl(シミュレータ連携)/validate.jl(検証)。
  * test/:例・境界・エラー・性能のテスト。
  * docs/:仕様書・例・チュートリアル・拡張ガイド。
  * Project.toml/Manifest.toml:依存固定・再現性確保。
  * CI:テスト・ベンチ・ドキュメント生成を自動化。



* * *

# 18. 具体例:制約付き二変数最小化DSLの小さな完成形(雛形)

下記は、これまでの断片をひとつにまとめた最小DSLの雛形です。学習目的の簡易版として参照ください。

module MiniDSL  
export @spec, SpecModel, compile, validate, solve_min  


# --- 語彙型 ---  
abstract type Objective end  
abstract type Constraint end  
abstract type EventSpec end  


struct Minimize <: Objective  
expr::Expr  
end  
struct Le <: Constraint  
lhs::Expr; rhs::Expr  
end  
struct When <: EventSpec  
cond::Expr; act::Expr  
end  


struct SpecModel  
objective::Objective  
constraints::Vector{Constraint}  
events::Vector{EventSpec}  
params::Dict{Symbol,Any}  
end  


# --- マーカー関数(文法素片) ---  
constraint(lhs, rhs) = (:constraint, lhs, rhs)  
event(cond, act) = (:event, cond, act)  


# --- マクロ ---  
macro spec(ex)  
ex.head == :block || error("@spec は begin ... end ブロック用です")  
objective = nothing  
constraints = Expr[]  
events = Expr[]  
params = Dict{Symbol,Any}()  


for stmt in ex.args  
if stmt.head == :(=) && stmt.args[1] == :objective  
objective = stmt.args[2]  
elseif stmt isa Expr && stmt.head == :tuple && stmt.args[1] == :constraint  
push!(constraints, :(Le($(esc(stmt.args[2])), $(esc(stmt.args[3])))))  
elseif stmt isa Expr && stmt.head == :tuple && stmt.args[1] == :event  
push!(events, :(When($(esc(stmt.args[2])), $(esc(stmt.args[3])))))  
elseif stmt.head == :(=) && stmt.args[1] == :param  
pair = stmt.args[2]  
params[pair.args[1]] = pair.args[2]  
end  
end  
objective === nothing && error("objective がありません")  
return :(SpecModel(Minimize($(esc(objective))), $constraints, $events, $(esc(params))))  
end  


# --- 検証 ---  
struct ValidationError <: Exception  
msg::String  
end  
function validate(spec::SpecModel)  
isempty(spec.constraints) && throw(ValidationError("制約がありません"))  
spec.objective isa Minimize || throw(ValidationError("objective の型が不正"))  
if haskey(spec.params, :alpha)  
a = spec.params[:alpha]  
0.0 <= a <= 1.0 || throw(ValidationError("alpha は [0,1]"))  
end  
return spec  
end  


# --- コンパイル(簡易) ---  
struct CompiledSpec  
objective::Function  
constraints::Vector{Function}  
callback::Function  
params::Dict{Symbol,Any}  
end  


function compile(spec::SpecModel)  
validate(spec)  
obj = let ex = (spec.objective::Minimize).expr  
(x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), ex))  
end  
cons = map(spec.constraints) do c  
(c::Le; (x,y) -> eval(Expr(:block, :(x=$(x)), :(y=$(y)), :($(c.lhs) <= $(c.rhs)))))  
end  
cb = isempty(spec.events) ? (st->st) : let e = spec.events[1]  
(st::NamedTuple) -> begin  
cond = eval(Expr(:block, :(x=$(st.x)), :(y=$(st.y)), e.cond))  
if cond == 0  
xnew = eval(Expr(:block, :(x=$(st.x)), :(y=$(st.y)), e.act))  
(; x=xnew, y=st.y)  
else  
st  
end  
end  
end  
return CompiledSpec(obj, cons, cb, spec.params)  
end  


# --- 最小化(座標降下の簡易例) ---  
function solve_min(cs::CompiledSpec; x0=0.0, y0=0.0, η=0.01, maxiter=10_000)  
x, y = x0, y0  
best = (x=x, y=y, c=cs.objective(x, y))  
for k in 1:maxiter  
c0 = cs.objective(x, y)  
dfdx = (cs.objective(x+1e-6, y) - c0)/1e-6  
dfdy = (cs.objective(x, y+1e-6) - c0)/1e-6  
x -= η*dfdx; y -= η*dfdy  
st = cs.callback((; x=x, y=y))  
x, y = st.x, st.y  
all(f -> f(x,y), cs.constraints) || break  
c1 = cs.objective(x, y)  
if c1 < best.c  
best = (x=x, y=y, c=c1)  
end  
end  
return best  
end  


end # module  


using .MiniDSL  


# 使用例  
spec = @spec begin  
objective = x^2 + y^2  
constraint(x, 1.0)  
constraint(y, 2.0)  
event(x - 0.5, :(x = 0.5))  
param = (:alpha => 0.1)  
end  


cs = MiniDSL.compile(spec)  
ans = MiniDSL.solve_min(cs; x0=0.9, y0=1.5)

この雛形は、仕様→検証→コンパイル→実行の流れの骨格です。実務では、evalの削減・自動微分・イベントの連続検知・実シミュレータへの橋渡し・高度な検証(単位・不変量)などを段階的に追加します。

* * *

# 19. 性能・再現性・保守の要点(記述のみ)

  * 性能:build_function相当で式からネイティブ関数へ。@generatedは静的構造にのみ適用。アロケーション削減・融合・インプレースを徹底。
  * 再現性:プロジェクト環境・乱数・前処理・出力グリッド・ソルバ属性を固定。仕様・検証・ログ・成果物を同梱。
  * 保守:核の直交性・プラグイン拡張・後方互換・セマンティックバージョニング。ドキュメントとテストを先行。



* * *

# 20. 運用箇条(記述のみ)

  * 核は「語彙の型化・宣言的文法・検証・コンパイル・連携」に限定し、拡張はプラグインで局所化します。
  * マクロの衛生・明確なエラー・一意な識別子で、読みやすさと安全性を確保します。
  * 仕様は必ず検証可能にし、単位・値域・不変量・初期一致性をコード化します。
  * 実行計画は、中間表現→最適化(関数化)→シミュレータ橋渡しへ段階設計します。
  * 成功例に学び、曖昧文法・型海賊・過度な糖衣・互換性軽視を避けます。



この指針に従うことで、Juliaを仕様記述言語として活用し、領域固有の文法をもつDSLを正しく・速く・検証可能に設計・運用できます。

* * *

# 第17章パッケージ開発・テスト・ドキュメント:PkgTemplates・Documenter

# 1. 方針:再現可能・保守可能・拡張可能な「Juliaパッケージ」を設計する

パッケージ開発は、(1) 雛形の標準化、(2) 正しさを保証するテスト群、(3) 性能特性を把握するベンチ、(4) 読み手のためのドキュメント、(5) CIとリリース、(6) バージョニングと互換性管理、(7) 登録・配布までを一気通貫で設計します。JuliaではPkgTemplates.jlで雛形を自動生成し、Test・BenchmarkTools・PkgBenchmarkで品質の検証、Documenter.jlでAPIドキュメントを構築、GitHub ActionsでCIを組み、Project.tomlの[compat]で互換性を固定します。以下、具体的手順と作法を示します。

* * *

# 2. 雛形作成:PkgTemplatesで初期セットアップを自動化する

雛形は「コード・テスト・ドキュメント・CI・ライセンス」を同梱した最小完全形が望ましいです。PkgTemplatesでプロジェクト骨子を作ります。

julia> using PkgTemplates  


julia> t = Template(  
user="YourGitHubID",  
authors=["Your Name <you@example.com>"],  
license="MIT",  
julia_version=v"1.10",  
dir=joinpath(homedir(), ".julia", "dev"),  
plugins=[  
GitHubActions(), # CI  
Documenter{GitHubActions}(),  
Codecov(), # 任意:カバレッジ  
CompatHelper(), # 任意:互換性PR  
TagBot() # リリースタグ自動化  
],  
);  


julia> generate("AwesomePackage", t)

生成後の標準構成(例):

AwesomePackage/  
├─ Project.toml  
├─ src/  
│ └─ AwesomePackage.jl  
├─ test/  
│ └─ runtests.jl  
├─ docs/  
│ ├─ Project.toml  
│ ├─ make.jl  
│ └─ src/  
│ └─ index.md  
├─ .github/workflows/  
│ ├─ CI.yml  
│ └─ TagBot.yml  
└─ LICENSE

  * src/:公開API(モジュール)を定義します。
  * test/:単体テスト・プロパティテスト・統合テストの入口です。
  * docs/:DocumenterでAPIとチュートリアルを自動生成します。
  * .github/workflows/:CIとドキュメントの自動デプロイ設定を含みます。



* * *

# 3. パッケージの最小公開API:型・関数・ドキュメント文字列

公開APIは「薄いが頑丈」であることが理想です。型・関数にドキュメント文字列(docstring)を必ず付与します。

module AwesomePackage  


export addthree, Accumulator  


"""  
addthree(x::Number, y::Number, z::Number) -> Number  


3つの数値`x, y, z`を足し合わせて返します。  
引数は`Number`であれば混合型でも受け付けます(必要に応じて`promote`)。  
"""  
function addthree(x::Number, y::Number, z::Number)  
x2, y2, z2 = promote(x, y, z)  
return x2 + y2 + z2  
end  


"""  
Accumulator{T}  


数値を累積する軽量な可変構造体。  
`push!`で値を追加し、`sum`と`count`を保持します。  
"""  
mutable struct Accumulator{T<:Real}  
sum::T  
count::Int  
end  


Accumulator{T}() where {T<:Real} = Accumulator{T}(zero(T), 0)  


"""  
push!(acc::Accumulator{T}, x::T) -> Accumulator{T}  


値`x`を累積します。戻り値は`acc`自身です。  
"""  
function Base.push!(acc::Accumulator{T}, x::T) where {T<:Real}  
acc.sum += x  
acc.count += 1  
return acc  
end  


end # module

ドキュメント文字列はDocumenterでそのままAPIリファレンスへ出力可能です。例を含むと、doctestで実行検証もできます。

* * *

# 4. 単体テスト:Testで正しさと境界を押さえる

テストは「正常系」「境界」「異常系」を網羅します。@testと@test_throwsを組み合わせます。

using Test  
using AwesomePackage  


@testset "addthree" begin  
@test addthree(1, 2, 3) == 6  
@test addthree(1.0, 2, 3) ≈ 6.0  
@test addthree(-1, -2, 3) == 0  
end  


@testset "Accumulator" begin  
acc = Accumulator{Float64}()  
@test acc.sum == 0.0  
@test acc.count == 0  
push!(acc, 1.5)  
@test acc.sum ≈ 1.5  
@test acc.count == 1  
end

プロパティテスト(性質検査)では、単調性・閉包性・不変量を検査します。簡易にはRandomでランダム値を生成し、性質を確認します。

using Random  


@testset "Properties" begin  
rng = MersenneTwister(1234)  
for _ in 1:1000  
a, b, c = rand(rng), rand(rng), rand(rng)  
@test addthree(a, b, c) ≥ max(a, b, c) # (例)非負の範囲では最大値以上  
end  
end

注意:性質の正当性はドメインに依存します。無条件の不等式は誤りになり得るため、検査式は仕様通りに記述してください。

* * *

# 5. ベンチテスト:BenchmarkTools・PkgBenchmarkで性能回帰を監視する

性能は「速さだけでなくアロケーション量」も重要です。BenchmarkToolsで関数単位の計測、PkgBenchmarkでバージョン間の性能比較を行います。

using BenchmarkTools  
using AwesomePackage  


x = rand(10_000); y = rand(10_000); z = rand(10_000)  


@btime addthree($x[1], $y[1], $z[1])  


# アロケーション量の粗確認  
bytes = @allocated addthree(1.0, 2.0, 3.0)  
@info "allocated bytes = $bytes"

PkgBenchmarkの基本構成(パッケージ直下にbenchmark/benchmark.jlを用意):

# benchmark/benchmark.jl  
using AwesomePackage, BenchmarkTools  


suite = BenchmarkGroup()  
suite["addthree"] = @benchmarkable addthree($(rand()), $(rand()), $(rand()))  
suite

実行例(比較):

julia> using PkgBenchmark  
julia> results = benchmarkpkg("AwesomePackage") # 現行 vs 登録版の比較  
julia> export_markdown(results, "benchmark_results.md")

ベンチはCIで定期実行し、性能回帰(遅くなる・アロケーション増)が検出されたら原因を追います。

* * *

# 6. ドキュメント生成:DocumenterでAPIとチュートリアルを自動構築

docs/make.jlでサイト構築の設定を行います。最小例:

# docs/make.jl  
using Documenter  
using AwesomePackage  


DocMeta.setdocmeta!(AwesomePackage, :DocTestSetup, :(using AwesomePackage); recursive=true)  


makedocs(  
modules=[AwesomePackage],  
sitename="AwesomePackage.jl",  
clean=true,  
format=Documenter.HTML(),  
pages=[  
"Home" => "index.md",  
"API" => "api.md",  
],  
)  


# GitHub Pages等にデプロイする場合  
deploydocs(  
repo="github.com/YourGitHubID/AwesomePackage.jl",  
devbranch="main",  
)

索引ページ(docs/src/index.md)には、パッケージ概要・インストール・最小例・設計思想を記述します。APIページ(docs/src/api.md)で@docsを用いてdocstringから自動生成します。

# API  


```@docs  
AwesomePackage.addthree  
AwesomePackage.Accumulator

**doctest**:`DocMeta.setdocmeta!`の設定により、`jldoctest`ブロック内のコードがテストされます。例:  


```markdown  
# 使い方  


```jldoctest  
julia> using AwesomePackage  


julia> addthree(1, 2, 3)  
6

`PkgTemplates`のドキュメントプラグインを使うと、CIで自動ビルド・デプロイが整います。  


\---  


# 7. 継続的インテグレーション(CI):テスト・ドキュメント・ベンチを自動化する  


`GitHub Actions`の最小CI例(`/.github/workflows/CI.yml`):  


```yaml  
name: CI  
on:  
push:  
branches: [ "main" ]  
pull_request:  


jobs:  
test:  
runs-on: ubuntu-latest  
steps:  
\- uses: actions/checkout@v4  
\- uses: julia-actions/setup-julia@v2  
with:  
version: '1'  
\- uses: julia-actions/cache@v2  
\- name: Install dependencies  
run: julia --project -e 'using Pkg; Pkg.instantiate()'  
\- name: Run tests  
run: julia --project -e 'using Pkg; Pkg.test(coverage=true)'  
docs:  
runs-on: ubuntu-latest  
steps:  
\- uses: actions/checkout@v4  
\- uses: julia-actions/setup-julia@v2  
with:  
version: '1'  
\- name: Build docs  
run: julia --project=docs -e 'using Pkg; Pkg.instantiate(); include("docs/make.jl")'

  * テストジョブ:Pkg.test()で単体・プロパティテストを実行、カバレッジ収集。
  * ドキュメントジョブ:docs環境でmake.jlを実行してビルド。
  * ベンチジョブ:必要に応じて別ワークフローでPkgBenchmarkを実行し、差分をレポートします。



Secrets(DOCUMENTER_KEY等)を設定すると、GitHub Pagesへの自動デプロイが可能です。

* * *

# 8. バージョニングと互換性管理:Project.tomlの[compat]を徹底する

互換性は「自分のパッケージのversion」と「依存パッケージの[compat]」の二軸で管理します。Semantic Versioningに従い、破壊的変更はメジャー、後方互換の追加はマイナー、修正はパッチでバージョンを進めます。

Project.tomlの例:

name = "AwesomePackage"  
uuid = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"  
version = "0.3.1"  


[deps]  
Documenter = "xxxx..."  
BenchmarkTools = "yyyy..."  


[compat]  
julia = "1.10"  
Documenter = "1"  
BenchmarkTools = "1"

  * juliaのバージョン範囲を固定します(例:"1.10")。
  * 依存パッケージのメジャー範囲を固定し、破壊的変更取り込みを防ぎます。
  * CIでPkg.status()を出力し、依存の差分をログに残します。



* * *

# 9. 変更管理:CHANGELOG・破壊的変更の告知・移行ガイド

公開パッケージは利用者の資産です。破壊的変更は避け、やむを得ない場合は以下を徹底します。

  * CHANGELOG.mdに「Added/Changed/Deprecated/Removed/Fixed」の分類で記録。
  * Deprecated期間を設け、警告を出して移行を促す(警告は一度だけ表示する設計が望ましい)。
  * 移行ガイドで旧APIと新APIの対応表を示し、サンプルコードを添えます。



* * *

# 10. 登録と配布:レジストリ登録・タグ付け・リリース

一般配布にはレジストリ登録が必要です。実務の流れ(概要):

  1. リポジトリを公開し、Project.tomlにname/uuid/versionを記入。
  2. タグ(例:v0.1.0)を作成。
  3. レジストリの登録手続きを実行(Registrator等を利用)。
  4. TagBotでリリースタグに応じたリリースノートを自動生成。
  5. 利用者は] add AwesomePackageでインストール可能になります。



バージョン更新時は、[compat]の更新・CHANGELOGの追記・タグ付け・CI合格を確認してから登録します。

* * *

# 11. ドキュメント実務:構成・ナラティブ・API・例・FAQ

  * 構成:index(概要)→getting started(最小例)→tutorials(深掘り)→api(完全なリファレンス)→faq(よくある質問)。
  * ナラティブ:設計意図・仕様・制約・注意点を文章で説明し、コードは短く・正確に。
  * API:docstringから自動生成し、シグネチャ・説明・例・戻り値・例外を明記。
  * 例:doctestで実行可能な最小例に揃え、CIで破綻なくテストされる状態を維持。
  * FAQ:インストールエラー、互換性、既知の限界、パフォーマンスのコツを載せます。



* * *

# 12. テストの拡張:プロパティ・隠れバグ検知・カバレッジ

  * プロパティテスト:仕様的性質(可逆性、単調性、保存量)をランダム入力で検証。
  * 隠れバグの検知:境界値(0・Inf・NaN・空配列・巨大配列)・異種型・ミュータブル/イミュータブルを取り合わせる。
  * カバレッジ:CIでカバレッジを集計し、閾値を設定(過度に高すぎる閾値は逆効果)。重要分岐を優先的に網羅します。



* * *

# 13. 性能の運用:ベンチ計測・ボトルネックの特定・回帰検知

  * 計測:@btimeでホットスポットを特定、@allocatedでアロケーションも監視。
  * 最適化:型安定・.=融合・ビュー・インプレース・@inbounds(正しさ確認後)を適用。
  * 回帰検知:PkgBenchmarkでバージョン比較を自動化(CIで前回リリースと比較)。



* * *

# 14. 開発フロー:dev運用・Revise・ローカルCI

  * ローカル開発:] dev AwesomePackageで作業ディレクトリへリンク、Revise.jlで関数変更をREPLに即時反映。



using Revise  
using AwesomePackage

  * ローカルCI:Pkg.test()・docs/make.jl・ベンチをローカルで回し、PR前に品質を担保。
  * ブランチ戦略:mainへ直接コミットしない、PRベース運用・レビュー必須・CI必須を徹底。



* * *

# 15. エラーメッセージと例外設計:診断可能な失敗にする

  * 例外型:ArgumentError/DomainError/BoundsErrorなど標準を優先。独自型は必要最小限。
  * メッセージ品質:原因・値・期待範囲・対処の一行を含める。



x ≥ 0 || throw(DomainError(x, "x must be nonnegative"))

  * 失敗の再現性:ランダム要素のある処理は種を固定し、失敗が再現可能であることを保証。



* * *

# 16. 秘密情報とライセンス:配布時の安全と適法性

  * 秘密情報:APIキー・トークンはENVで渡し、コードに直書きしない。CIのsecretsを利用。
  * ライセンス:LICENSEをプロジェクト直下に配置。依存パッケージのライセンス互換性も確認。
  * 著作権表示:ドキュメント・ソースのヘッダに年・著作者を明記。



* * *

# 17. 実務チェックリスト(記述のみ)

  * 雛形:PkgTemplatesでCI・Docs・ライセンス込みの最小完全形を生成したか。
  * API:公開関数・型にdocstringを付け、DocumenterでAPIページが生成できるか。
  * テスト:正常・境界・異常・プロパティ・カバレッジの各観点を押さえているか。
  * ベンチ:主要関数の時間・アロケーションを測り、回帰を検知できる仕組みがあるか。
  * CI:テスト・ドキュメント・(必要なら)ベンチが自動で回り、PRに必須化されているか。
  * 互換性:[compat]が埋められ、破壊的変更を避け、CHANGELOGに記録されているか。
  * 配布:タグ・登録・リリースノート・ドキュメント公開が整っているか。



* * *

# 18. 参考スニペット:Release準備の一連コマンド例(ローカル)

# 1) テストとドキュメントをローカルで確認  
julia \--project -e 'using Pkg; Pkg.test()'  
julia \--project=docs -e 'using Pkg; Pkg.instantiate(); include("docs/make.jl")'  


# 2) バージョンを更新(Project.toml)  
# version = "0.3.2" へ変更、CHANGELOG.mdを追記  


# 3) タグ付け(シェル)  
git add .  
git commit -m "Release v0.3.2"  
git tag v0.3.2  
git push \--follow-tags  


# 4) CI完了後、レジストリ登録手続きを実行(ワークフローやBotに従う)

* * *

# 19. 実務上の注意(記述のみ)

  * APIは小さく、意味論を明確に。糖衣に偏らず、型・契約(不変量)を優先します。
  * ドキュメントは「使い方ファースト」。APIの網羅性より、最小例・チュートリアル・FAQを丁寧に。
  * テストは正しさ>速度。ベンチは速度の根拠であり、パッチで壊れやすい箇所を守る盾です。
  * CIは「壊れる前に止める」。PRに必須化し、失敗の原因がログで即時把握できるように。
  * バージョニングは約束事。互換性を尊重し、破壊的変更は慎重に告知・移行ガイドを添える。
  * 登録・配布は利用者の体験。タグ・リリースノート・ドキュメントが揃っていて初めて「使える」になります。



以上の流儀で、PkgTemplates・Documenter・Test・BenchmarkTools・PkgBenchmark・CIを組み合わせれば、Juliaパッケージを正しく・速く・説明可能な形で開発・配布できます。

* * *

# 第18章プロジェクト運用と再現可能性:環境・依存関係・成果物管理(△長期運用)

# 1. 原則:長期運用で壊れないしくみ(再現性・可観測性・自動化)

長期運用のプロジェクトでは、誰が、いつ、どの環境で、どの入力を使って、どの設定で実行しても、同じ成果物(数値・図表・レポート)が再生成できることが前提になります。Juliaでは、(1) 環境ピン留め(Project.toml/Manifest.toml)、(2) 依存関係の監査と互換性固定([compat])、(3) 乱数種と設定値の集中管理、(4) 成果物の生成パイプラインのコード化、(5) ロギングとプロベナンス(由来)記録、(6) テスト・レビュー・CIの運用を組み合わせ、再現可能性と可観測性を担保します。以降、手順と実用コードで要点を示します。

* * *

# 2. 環境ピン留め:Project.tomlとManifest.tomlをチームの単位にする

プロジェクト直下にProject.toml/Manifest.tomlを置き、必ずその環境を起点に作業します。

# REPLでプロジェクト環境を起動  
julia \--project=.  


# Pkgモード(`]`)での基本操作  
# ] activate .  
# ] instantiate  
# ] status

  * Project.toml:パッケージ名・UUID・バージョン・直接依存([deps])・互換性([compat])。
  * Manifest.toml:依存グラフの完全スナップショット(間接依存を含む)。手編集しない。



新しいメンバーは、リポジトリをclone→julia --project=.→] instantiateで同じ環境を再構築できます。開発・検証・本番で環境を分離(例:/env/dev・/env/prod)し、\--project=env/prodで本番を固定する運用も有効です。

* * *

# 3. 互換性固定と依存監査:[compat]・pin・resolveで予期せぬ更新を防ぐ

Project.tomlの[compat]は安全装置です。最低限、juliaと主要依存を固定します。

[compat]  
julia = "1.10"  
DataFrames = "1"  
CSV = "0.10"  
Plots = "1"

  * ] update:互換範囲内で更新。更新前後で ] status の差分をログへ保存。
  * ] pin SomePkg:特定依存を固定し、更新から除外。
  * ] resolve:互換性の衝突を再解決。



CIでPkg.status()の出力を成果物と一緒にアーカイブしておくと、いつどの依存で実行したかを検証できます。

* * *

# 4. 実行入口の標準化:ブートストラップとランナー

「誰でも同じ手順で実行できる」入口として、ブートストラップとランナーの2本を用意します。

# scripts/bootstrap.jl  
using Pkg  
Pkg.activate(".")  
Pkg.instantiate()  


# 実行環境の記録  
using InteractiveUtils  
open("run_meta/versioninfo.txt", "w") do io  
redirect_stdout(io) do  
versioninfo()  
end  
end  


# 依存一覧の記録  
open("run_meta/status.txt", "w") do io  
redirect_stdout(io) do  
Pkg.status()  
end  
end

# scripts/run.jl  
using Pkg; Pkg.activate(".")  
# 設定・乱数種の集中管理  
include("src/config.jl")  
cfg = load_config("config/default.toml") # テキスト設定にしてもよい  


# パイプラインの実行(後述)  
include("src/pipeline.jl")  
run_pipeline(cfg) # 図・表・数値・レポートを一括生成

運用手順は 1 行で統一します。

julia \--project=. scripts/bootstrap.jl && julia \--project=. scripts/run.jl

* * *

# 5. 乱数種と設定値の管理:入口で固定、関数にはrngを渡す

乱数を使う処理は必ず入口で種を固定し、下位にはrngを明示渡しします。設定は構造体かTOML/YAMLで集中管理します。

# src/config.jl  
module Config  
using Random  


struct AppConfig  
seed::Int  
report_dir::String  
plot_theme::Symbol  
tol::Float64  
maxiter::Int  
end  


function load_config(path::AbstractString)  
# 例として固定値(実務ではTOML読み取り)  
return AppConfig(20240101, "reports", :default, 1e-8, 10_000)  
end  


function mk_rng(cfg::AppConfig)  
return MersenneTwister(cfg.seed)  
end  


end # module

# 使用例  
using .Config  
cfg = Config.load_config("config/default.toml")  
rng = Config.mk_rng(cfg)  


# 下位関数は rng::AbstractRNG を受け取る  
function sample_some(rng::AbstractRNG; n=1000)  
return rand(rng, n)  
end

並列時は、スレッド・プロセスごとに規則化した種(例:seed + threadid()・seed + myid())を割り当て、消費順序が揺れない設計にします。

* * *

# 6. 成果物パイプライン:図・表・数値・レポートをコードで一括生成

成果物(figures/・tables/・numbers/・reports/)はコードから一括生成し、同じ入力で同じ出力になるようにします。

# src/pipeline.jl  
module Pipeline  
using CSV, DataFrames, Plots  


function ensure_dirs(cfg)  
for d in (cfg.report_dir, joinpath(cfg.report_dir, "figures"),  
joinpath(cfg.report_dir, "tables"),  
joinpath(cfg.report_dir, "numbers"))  
isdir(d) || mkpath(d)  
end  
end  


function gen_tables(cfg, df::DataFrame)  
out = combine(groupby(df, :category), :value => mean => :value_mean)  
CSV.write(joinpath(cfg.report_dir, "tables", "value_mean.csv"), out)  
return out  
end  


function gen_figures(cfg, df::DataFrame)  
default(); # 必要ならテーマ固定  
p = histogram(df.value; bins=50, xlabel="value", ylabel="count", title="Value Distribution")  
savefig(joinpath(cfg.report_dir, "figures", "hist_value.png"))  
return p  
end  


function gen_numbers(cfg, df::DataFrame)  
open(joinpath(cfg.report_dir, "numbers", "summary.txt"), "w") do io  
println(io, "count=$(nrow(df))")  
println(io, "mean=$(mean(df.value))")  
println(io, "std=$(std(df.value))")  
end  
end  


function run_pipeline(cfg)  
ensure_dirs(cfg)  
df = CSV.read("data/input.csv", DataFrame)  
gen_tables(cfg, df)  
gen_figures(cfg, df)  
gen_numbers(cfg, df)  
return nothing  
end  


end # module

  * 図のテーマ・サイズ・フォントは固定(同一出力のため)。
  * 表はCSV.writeでバイナリ差分も追跡可能。
  * 数値はnumbers/配下へテキストで保存し、CIで閾値比較できます。



* * *

# 7. ロギング・プロベナンス:入力・設定・環境・ハッシュを記録する

どの入力で、どの設定で、どの環境で、どのコードから生成したかを、成果物と同じディレクトリに記録します。

# src/provenance.jl  
module Provenance  
using SHA  


function file_sha256(path::AbstractString)  
open(path, "r") do io  
return bytes2hex(sha256(read(io)))  
end  
end  


function record_provenance(cfg; input_path="data/input.csv")  
open(joinpath(cfg.report_dir, "run_meta.txt"), "w") do io  
println(io, "seed=$(cfg.seed)")  
println(io, "input=$(abspath(input_path)) sha256=$(file_sha256(input_path))")  
println(io, "plot_theme=$(cfg.plot_theme)")  
println(io, "tol=$(cfg.tol) maxiter=$(cfg.maxiter)")  
# バージョン情報は bootstrap で出力済み(versioninfo/status)  
end  
end  


end # module

パイプラインの末尾や入口でrecord_provenanceを呼び、入力ファイルのハッシュと設定を保存します。これにより、外部差し替え(データ改変)の検知が可能になります。

* * *

# 8. データ・バイナリ資源の再現:Artifacts.tomlで資源を宣言する

外部データ(固定データセット・モデル重み・バイナリツール)はArtifacts.tomlで管理します。チェックサム付きで宣言し、artifact"名前"で取得します。

# Artifacts.toml(概念例)  
[my_dataset]  
git-tree-sha1 = "abcdef1234567890..." # コンテンツID

using Pkg.Artifacts  
path = artifact"my_dataset"  
df = CSV.read(joinpath(path, "dataset.csv"), DataFrame)

これにより、取得元と内容が固定され、他メンバーやCIでも同じ資源を参照できます。大規模データは分割・サブセットを作り、作業再現を優先します。

* * *

# 9. テスト戦略:正しさ・再現・プロパティ・性能回帰

テストは通常のTestに加え、再現性と性能回帰をカバーします。

# test/runtests.jl  
using Test  
using .Config, .Pipeline, .Provenance  


@testset "reproducible pipeline" begin  
cfg = Config.AppConfig(20240101, "reports_test", :default, 1e-8, 10_000)  
# 簡易入力を作る  
using CSV, DataFrames, Random  
rng = Random.MersenneTwister(cfg.seed)  
df = DataFrame(category = rand(rng, ["A","B","C"], 1000),  
value = rand(rng, 1000))  
CSV.write("data/input.csv", df)  


Pipeline.run_pipeline(cfg)  
# 出力の存在  
@test isfile(joinpath(cfg.report_dir, "figures", "hist_value.png"))  
@test isfile(joinpath(cfg.report_dir, "tables", "value_mean.csv"))  
@test isfile(joinpath(cfg.report_dir, "numbers", "summary.txt"))  
end

  * 正しさ:数値妥当性(例:平均が期待範囲)・表の行数など。
  * 再現性:同じ種で同じ出力(ファイルのハッシュ一致など)。
  * プロパティ:単調性・保存量・単位など仕様に基づく性質検査。
  * 性能回帰:BenchmarkTools/PkgBenchmarkで閾値監視。



* * *

# 10. レビューと品質保証:PRテンプレート・チェックリスト・コード規約

チームレビューでは、再現性に関わる観点をチェックリストにします。

  * Project.tomlの[compat]は更新されているか。
  * 入口スクリプト(bootstrap.jl/run.jl)で再現手順が壊れていないか。
  * 乱数種を入口で固定し、下位はrng渡しになっているか。
  * 成果物がコード化され、手作業の「貼り付け」工程がないか。
  * ログ・プロベナンス記録が更新され、入力ハッシュが残っているか。
  * テスト・ベンチ・CIが通過しているか。



PRテンプレートに上記のチェック項目を含めると、長期運用での事故が減ります。

* * *

# 11\. CI(継続的インテグレーション):テスト・パイプライン・成果物の検証

GitHub Actionsでテスト・ドキュメント・パイプラインの最小実行を自動化します。

# .github/workflows/ci.yml(概略)  
name: CI  
on:  
push:  
branches: [ "main" ]  
pull_request:  


jobs:  
test:  
runs-on: ubuntu-latest  
steps:  
- uses: actions/checkout@v4  
- uses: julia-actions/setup-julia@v2  
with: { version: '1' }  
- uses: julia-actions/cache@v2  
- run: julia --project -e 'using Pkg; Pkg.instantiate(); Pkg.test()'  


pipeline-smoke:  
runs-on: ubuntu-latest  
steps:  
- uses: actions/checkout@v4  
- uses: julia-actions/setup-julia@v2  
with: { version: '1' }  
- run: julia --project -e 'using Pkg; Pkg.instantiate()'  
- run: julia --project=. scripts/run.jl  
- run: test -f reports/figures/hist_value.png && echo "ok"

  * 「スモークテスト」的にパイプラインを一度回し、成果物の存在を検証。
  * 大規模生成は時間制限に配慮してスキップ版(サンプル入力)を用意。



* * *

# 12. バージョニング・リリース:セマンティックバージョニングと変更記録

長期運用では、バージョンを厳密に管理します。

  * MAJOR.MINOR.PATCHでセマンティックバージョニング。
  * 破壊的変更はメジャーのみ、移行ガイドを用意。
  * CHANGELOG.mdに「Added/Changed/Deprecated/Removed/Fixed」を明記。
  * 依存更新は[compat]の範囲内で。外れる更新は、上流変更の影響をレビューの上で慎重に。



* * *

# 13. セキュリティ・秘密情報:ENVとシークレット管理

APIキー・トークンはコードに直書きしないで、ENVから渡します。

api_key = get(ENV, "MY_API_KEY", nothing)  
api_key === nothing && error("MY_API_KEY is not set")  


# 取得したキーを使う処理...

CIでもsecretsを設定し、ENVへ注入します。プロベナンス記録には秘密を出力しない設計を徹底します。

* * *

# 14. 入出力の標準化:ファイル命名・フォーマット・ディレクトリ規約

成果物は、命名規約・フォーマット・ディレクトリを固定します。

  * reports/figures/fig_<topic>.png(図はPNG、論文用はPDF/SVGも)。
  * reports/tables/tab_<topic>.csv(表はCSVで差分追跡可能)。
  * reports/numbers/<metric>.txt(KPIなどはテキスト化)。
  * run_meta(環境・依存・設定・ハッシュの記録)。



この規約をドキュメント化し、レビューで運用を徹底します。

* * *

# 15. 起動オプションと性能:-t・キャッシュ・中間成果物の再利用

長期運用では、性能も重要です。起動時にスレッドを指定し、中間成果物(加工済みデータ)を再利用して全体時間を短縮します。

# CPUスレッドを自動設定  
julia \--project=. -t auto scripts/run.jl

  * 重い前処理は一度だけ実行してArrowやJLD2で保存し、以降のパイプラインでは再読込。
  * 「前処理バージョン」や入力ハッシュに紐付けたキャッシュキーを設計し、整合性が崩れたら自動再生成。



* * *

# 16. 失敗時の切り分け:環境・設定・入力・コードの4象限で診断

障害時の切り分けを定型化します。

  1. 環境:versioninfo/statusの差分、[compat]逸脱、instantiate漏れ。
  2. 設定:種・トレランス・パス・閾値。run_metaの記録と比較。
  3. 入力:入力のハッシュ差・列型の変更・欠損率の変化。
  4. コード:最近のコミット差分。テスト/ベンチ回帰の検知。



「どれが変わったか」を先に確定できると、復旧が速くなります。

* * *

# 17. チーム運用:レビュー・PRテンプレート・運用チェックリスト

運用のチェックリスト例(PRテンプレートに含めます):

☐ Project.tomlの[compat]が保守範囲内で更新された

☐ scripts/bootstrap.jl/scripts/run.jlで再現手順が維持されている

☐ 乱数種の固定・rng渡しの原則が守られている

☐ 成果物の命名・格納規約に従っている

☐ run_meta(環境・依存・設定・ハッシュ)が出力される

☐ テスト・CIが通過(スモークパイプライン含む)

☐ CHANGELOGに変更内容が追記された(破壊的変更なし)

* * *

# 18. 実務スニペット:小さな「一括生成」コマンドを整備する

現場で使いやすいよう、一括生成のシェルタスクやMakefile相当を用意します。

# build.sh(概略)  
set -e  
julia \--project=. scripts/bootstrap.jl  
julia \--project=. -t auto scripts/run.jl  
echo "done."

夜間バッチや定期レポートはこのスクリプトをスケジューラに登録し、失敗時通知(メール/ChatOps)を追加すると運用が安定します。

* * *

# 19. 長期保守の心得(記述のみ)

  * すべての成果物はコードから再生成できるようにし、手作業工程を排除します。
  * 環境・依存・設定・入力のプロベナンスを成果物と一緒に保存し、比較可能にします。
  * 互換性とバージョンは約束事。[compat]とCHANGELOGを必ず更新し、破壊的変更は避けます。
  * 乱数・閾値・トレランスは設定に外出しして、レビュー可能にします。
  * テスト・ベンチ・CIで回帰を早期検知し、壊れる前に止める運用にします。
  * チームの「入口」を統一(bootstrap→run)し、誰でも同じ手順で成果物を出せる状態を維持します。



この流儀を継続すると、Juliaプロジェクトは長期運用でも再現可能性と品質を保ち、安心して積み上げられる資産になります。

* * *

# 第19章ケーススタディ:金融・材料・気象・ライフサイエンスの実例

# 1. 再現可能なケーススタディの設計(共通フレーム)

ケーススタディは「データ取得→前処理→モデル化→可視化→検証→成果物出力」を同一環境で繰り返せるように設計します。ここでは各ドメインで共通する運用骨子を先に整え、以降の章でそれぞれのミニプロジェクトに当てはめます。データは法的・運用上の理由から合成データを用います(再現性の確保のため、乱数種・設定値を固定します)。

# 共通: 環境起動(REPL): julia --project=.  
# 依存(例): DataFrames, CSV, Plots, DifferentialEquations, JuMP, Optimization, SciMLSensitivity  


# scripts/bootstrap.jl  
using Pkg, InteractiveUtils  
Pkg.activate("."); Pkg.instantiate()  
open("run_meta/versioninfo.txt","w") do io; redirect_stdout(io) do; versioninfo(); end; end  
open("run_meta/status.txt","w") do io; redirect_stdout(io) do; Pkg.status(); end; end  


# src/config.jl  
module Config  
using Random  
struct AppConfig  
seed::Int  
outdir::String  
end  
load_config() = AppConfig(20240101, "reports")  
mk_rng(cfg::AppConfig) = MersenneTwister(cfg.seed)  
end

以降の各ケーススタディでは、cfg = Config.load_config()/rng = Config.mk_rng(cfg)を入口に、reports/figures・reports/tables・reports/numbersへ成果物を出力します。図のスタイル・解像度は固定し、同じ入力から同じ出力が得られることを検証します。

* * *

# 2. 金融:平均–分散ポートフォリオの最適化と簡易バックテスト

## 2.1 データ取得と前処理(合成リターン系列)

リターン系列(多資産)を合成し、日次の期待収益と共分散行列を推定します。乱数種固定で再現可能です。

using Random, DataFrames, CSV, Statistics  
using .Config  
cfg = Config.load_config(); rng = Config.mk_rng(cfg)  


# 合成: 3資産の日次リターン(1年=252営業日)  
n, T = 3, 252  
μ_true = [0.0002, 0.0001, 0.0003] # 真の平均(例)  
Σ_true = [0.0004 0.0001 0.0000; 0.0001 0.0003 0.0001; 0.0000 0.0001 0.0005] # 真の共分散(例)  


# 多変量正規に基づく合成  
using LinearAlgebra  
L = cholesky(Σ_true).L  
Z = randn(rng, T, n)  
R = Z*L' .+ μ_true' # T×n 行列  
dates = collect(1:T)  
df = DataFrame(date = dates, asset1 = R[:,1], asset2 = R[:,2], asset3 = R[:,3])  
isdir("reports/tables") || mkpath("reports/tables")  
CSV.write("reports/tables/returns.csv", df)  


# 推定  
μ_hat = [mean(df.asset1), mean(df.asset2), mean(df.asset3)]  
Σ_hat = cov(Matrix(df[:, [:asset1,:asset2,:asset3]]))

## 2.2 モデル化(JuMPで平均–分散最小化)

期待収益の下限を設定して分散(リスク)を最小化します。非負・合計1の制約を課します。

using JuMP, Ipopt  
μ_target = 0.00015  
n = length(μ_hat)  
model = Model(Ipopt.Optimizer)  
@variable(model, w[1:n] >= 0)  
@constraint(model, sum(w) == 1.0)  
@constraint(model, sum(μ_hat[i]*w[i] for i in 1:n) >= μ_target)  
@objective(model, Min, sum(Σ_hat[i,j]*w[i]*w[j] for i in 1:n, j in 1:n))  
optimize!(model)  
w_star = value.(w)  
risk_star = objective_value(model)

## 2.3 可視化(構成比・効率フロンティア)

構成比棒グラフと、μ_targetを掃引した効率フロンティアを出力します。

using Plots  
isdir("reports/figures") || mkpath("reports/figures")  


# 構成比  
bar(["asset1","asset2","asset3"], w_star; legend=false, title="weights", ylim=(0,1))  
savefig("reports/figures/weights.png")  


# フロンティア  
targets = range(0.00005, 0.00035; length=20)  
risks = Float64[]  
for μt in targets  
m = Model(Ipopt.Optimizer)  
@variable(m, w[1:n] >= 0)  
@constraint(m, sum(w) == 1.0)  
@constraint(m, sum(μ_hat[i]*w[i] for i in 1:n) >= μt)  
@objective(m, Min, sum(Σ_hat[i,j]*w[i]*w[j] for i in 1:n, j in 1:n))  
optimize!(m)  
push!(risks, objective_value(m))  
end  
plot(targets, risks; xlabel="target μ", ylabel="variance", title="efficient frontier")  
savefig("reports/figures/frontier.png")

## 2.4 検証(簡易バックテスト)

最適ウェイトで1日先リターンをシミュレートし(合成)、累積の推移を可視化します。ここでも種固定で再現します。

# 合成1日先の帰結  
ret_port = sum(w_star .* μ_true) # 期待値近似(合成)  
# 実現累積(単純合成)  
cum = cumsum(Matrix(df[:,2:4]) * w_star)  
plot(1:T, cum; xlabel="day", ylabel="cumulative", title="cumulative portfolio")  
savefig("reports/figures/cumulative.png")  


# 数値出力  
isdir("reports/numbers") || mkpath("reports/numbers")  
open("reports/numbers/portfolio.txt","w") do io  
println(io, "risk_star=$(risk_star)")  
println(io, "weights=$(w_star)")  
end

* * *

# 3. 材料:1D熱拡散(熱方程式)の離散化・推定・可視化

## 3.1 問題設定(合成材料棒の熱拡散)

1D 熱方程式 ∂u/∂t = α ∂2u/∂x2 を有限差分で離散化し、温度分布を時間発展させます。境界はディリクレ(端部温度固定)とします。

using DifferentialEquations  
# 空間離散  
Nx, L = 100, 1.0  
dx = L/(Nx-1)  
α = 1e-4  


# ラプラシアン離散(内部点のみ)  
function laplace!(du, u, p, t)  
@inbounds for i in 2:Nx-1  
du[i] = α*(u[i-1] - 2u[i] + u[i+1])/dx^2  
end  
# 境界: 端は固定  
du[1] = 0.0; du[end] = 0.0  
end  


# 初期温度(中央高温のガウス)  
x = range(0, L; length=Nx)  
u0 = @. exp(-((x-0.5)^2)/(2*0.05^2))  
tspan = (0.0, 10.0)  


prob = ODEProblem(laplace!, u0, tspan)  
sol = solve(prob, TRBDF2(); reltol=1e-8, abstol=1e-10, saveat=0.1)

## 3.2 可視化(温度分布の時間発展)

using Plots  
isdir("reports/figures") || mkpath("reports/figures")  


# スナップショット  
for (k,t) in enumerate(sol.t)  
if k % 10 == 1  
plot(x, sol.u[k]; xlabel="x", ylabel="temperature", title="t=$(round(t,digits=2))")  
savefig("reports/figures/heat_t$(k).png")  
end  
end  


# 2D ヒートマップ (x-t)  
U = hcat(sol.u...) # Nx × Nt  
heatmap(sol.t, x, U; xlabel="t", ylabel="x", title="temperature field", colorbar=true)  
savefig("reports/figures/heatmap.png")

## 3.3 検証(質量保存的観点・安定性)

熱方程式は「質量保存」ではなく「滑らか化」ですが、数値的にエネルギーが減衰していくことを確認します。

E = [sum(abs2, sol.u[k]) for k in eachindex(sol.u)]  
plot(sol.t, E; xlabel="t", ylabel="||u||^2", title="energy decay")  
savefig("reports/figures/energy_decay.png")

* * *

# 4. 気象:Lorenz-63 による擬似大気力学・感度と可視化

## 4.1 モデル化(Lorenz-63)

Lorenz-63 系のパラメータを固定し、時間発展を解きます。カオス的性質に注意しつつ、種・設定を固定して可視化します。

using DifferentialEquations  
σ, ρ, β = 10.0, 28.0, 8/3  
function lorenz!(du,u,p,t)  
du[1] = σ*(u[2]-u[1])  
du[2] = u[1]*(ρ - u[3]) - u[2]  
du[3] = u[1]*u[2] - β*u[3]  
end  
u0 = [1.0, 1.0, 1.0]  
tspan = (0.0, 50.0)  
prob = ODEProblem(lorenz!, u0, tspan)  
sol = solve(prob, Tsit5(); reltol=1e-9, abstol=1e-12, saveat=0.01)

## 4.2 可視化(軌道・相図)

using Plots  
plot(sol.t, hcat(sol[1,:], sol[2,:], sol[3,:]); label=["x" "y" "z"], xlabel="t", ylabel="value", title="Lorenz-63")  
savefig("reports/figures/lorenz_timeseries.png")  


plot(sol[1,:], sol[3,:]; xlabel="x", ylabel="z", title="phase: x-z", legend=false)  
savefig("reports/figures/lorenz_phase_xz.png")  


plot(sol[2,:], sol[3,:]; xlabel="y", ylabel="z", title="phase: y-z", legend=false)  
savefig("reports/figures/lorenz_phase_yz.png")

## 4.3 検証(パラメータ感度)

パラメータをわずかに変化させたときの差分を可視化し、カオス性に伴う発散傾向を確認します。

ρ2 = 27.9  
sol2 = solve(remake(prob); p=nothing, Tsit5(); reltol=1e-9, abstol=1e-12, saveat=0.01)  
# 差分 (初期条件一致)  
Δ = abs.(sol[1,:] .- sol2[1,:])  
plot(sol.t, Δ; xlabel="t", ylabel="|Δx|", title="sensitivity to ρ")  
savefig("reports/figures/lorenz_sensitivity.png")

* * *

# 5. ライフサイエンス:SIR 感染モデルの推定・検証

## 5.1 モデル化(SIR 方程式)

人口は定数、感染・回復率をパラメータとし、観測系列(合成)から同定します。

using DifferentialEquations  
N = 1_000_000  
function sir!(du,u,p,t)  
S, I, R = u  
β, γ = p  
du[1] = -β*S*I/N  
du[2] = β*S*I/N - γ*I  
du[3] = γ*I  
end  


# 初期値  
I0, R0 = 100.0, 0.0  
S0 = N - I0 - R0  
u0 = [S0, I0, R0]  
tspan = (0.0, 160.0)  
p_true = [0.3, 0.1]  


prob = ODEProblem(sir!, u0, tspan, p_true)  
sol = solve(prob, Tsit5(); reltol=1e-9, abstol=1e-12, saveat=1.0)

## 5.2 合成観測と前処理

観測ノイズを加えて「観測感染者数」を作り、データフレームに保存します(乱数種固定)。

using Random, DataFrames, CSV  
rng = Config.mk_rng(cfg)  
I_series = Array(sol[2,:])  
obs = I_series .+ 50 .* randn(rng, length(I_series)) # 合成雑音  
obs = clamp.(obs, 0, Inf) # 非負  
df = DataFrame(day = 0:length(I_series)-1, I_obs = obs)  
CSV.write("reports/tables/sir_obs.csv", df)

## 5.3 同定(最小二乗)

β, γ を最小二乗で推定します。SciMLSensitivityの随伴法で高速化可能ですが、ここでは単純化します。

using Optimization, OptimizationOptimJL  


function lossθ(θ)  
prob2 = ODEProblem(sir!, u0, tspan, θ)  
sol2 = solve(prob2, Tsit5(); saveat=1.0, reltol=1e-8, abstol=1e-10)  
I_pred = Array(sol2[2,:])  
return sum((I_pred .- df.I_obs).^2)  
end  


θ0 = [0.2, 0.05]  
optf = OptimizationFunction((θ,_) -> lossθ(θ), autodiff=:forward)  
optprob = OptimizationProblem(optf, θ0)  
res = optimize(optprob, LBFGS(); maxiters=500)  
θ_hat = res.minimizer

## 5.4 検証と可視化

推定パラメータで再計算し、観測との整合を図・数値で確認します。

prob_hat = ODEProblem(sir!, u0, tspan, θ_hat)  
sol_hat = solve(prob_hat, Tsit5(); saveat=1.0)  


plot(df.day, df.I_obs; label="obs", xlabel="day", ylabel="I", title="SIR fit")  
plot!(df.day, Array(sol_hat[2,:]); label="fit")  
savefig("reports/figures/sir_fit.png")  


open("reports/numbers/sir_fit.txt","w") do io  
println(io, "θ_hat=$(θ_hat)")  
println(io, "loss=$(lossθ(θ_hat))")  
end

* * *

# 6\. 4ケースの統合パイプラインとスモークテスト

各ミニプロジェクトの入口をまとめて実行できるようにします。スモークテストでは「成果物が生成されたか」を確認します。

# scripts/run.jl  
using .Config  
cfg = Config.load_config()  
isdir(cfg.outdir) || mkpath(cfg.outdir)  
isdir("reports/figures") || mkpath("reports/figures")  
isdir("reports/tables") || mkpath("reports/tables")  
isdir("reports/numbers") || mkpath("reports/numbers")  


# 1) 金融  
include("src/case_finance.jl") # 上記コードを関数化しておく  
# 2) 材料  
include("src/case_materials.jl")  
# 3) 気象  
include("src/case_weather.jl")  
# 4) ライフサイエンス  
include("src/case_life.jl")  


println("done.")

CI(スモーク)例:

# .github/workflows/smoke.yml  
name: Smoke  
on:  
push:  
branches: [ "main" ]  
pull_request:  
jobs:  
run:  
runs-on: ubuntu-latest  
steps:  
- uses: actions/checkout@v4  
- uses: julia-actions/setup-julia@v2  
with: { version: '1' }  
- run: julia --project -e 'using Pkg; Pkg.instantiate()'  
- run: julia --project scripts/run.jl  
- run: test -f reports/figures/weights.png  
- run: test -f reports/figures/heatmap.png  
- run: test -f reports/figures/lorenz_phase_xz.png  
- run: test -f reports/figures/sir_fit.png

* * *

# 7. ドメイン別の品質保証観点(チェックリスト)

  * 金融:[互換性]推定(μ・Σの時点ずれなし)、非負・合計1の満足、ターゲット収益と実現の差、分散値の再計算一致。
  * 材料:境界条件の満足(端部更新なし)、安定ソルバ選択(剛性対応)、エネルギー減衰の一貫性。
  * 気象:パラメータ・初期値の記録(感度検証用)、時間刻みの適切性(適応刻み)、相図の再現性(同一種)。
  * ライフ:非負制約(S, I, R ≥ 0)、人口保存(S+I+R = N)、推定値の妥当(範囲・単位)、残差の分布。



テストコードでは、これらを@testで網羅し、失敗時のログ(設定・種・依存)をrun_metaに記録しておきます。

* * *

# 8. 実運用のための成果物規約(命名・フォーマット)

  * 図:reports/figures/<case>_<topic>.png(論文用はSVG/PDFも併用)。
  * 表:reports/tables/<case>_<topic>.csv(差分追跡容易)。
  * 数値:reports/numbers/<case>_<metric>.txt(KPI)。
  * メタ:run_meta/versioninfo.txt・run_meta/status.txt(環境・依存)・reports/numbers/<case>_hash.txt(入力ハッシュ)。



上記をREADMEに明文化し、レビューで運用を徹底します。

* * *

# 9. 付記:コード断片の関数化(再利用性の向上)

例示コードは関数化しておくと、パラメータ掃引やベンチが容易になります。金融のフロンティア計算を関数化する例:

function efficient_frontier(μ::Vector{Float64}, Σ::Matrix{Float64}, targets::AbstractVector{Float64})  
using JuMP, Ipopt  
n = length(μ)  
risks = Float64[]  
for μt in targets  
m = Model(Ipopt.Optimizer)  
@variable(m, w[1:n] >= 0)  
@constraint(m, sum(w) == 1.0)  
@constraint(m, sum(μ[i]*w[i] for i in 1:n) >= μt)  
@objective(m, Min, sum(Σ[i,j]*w[i]*w[j] for i in 1:n, j in 1:n))  
optimize!(m)  
push!(risks, objective_value(m))  
end  
return risks  
end

* * *

# 10. 運用指針(記述のみ)

  * 合成データであっても、種・設定・環境を固定して完全再現を保証します。
  * 「一括生成」スクリプトとCIスモークで、壊れる前に止める仕組みを維持します。
  * 図・表・数値はコードから出力し、手作業工程を排除します。
  * 各ドメイン固有の検証観点(制約・保存・安定性)をテストで明文化し、レビューで徹底します。
  * パラメータ・入力・依存のプロベナンスを成果物と並置し、比較可能性を担保します。



以上の設計で、金融・材料・気象・ライフサイエンスの各ミニプロジェクトを、汎用から高度計算まで一貫して扱える再現可能な環境として運用できます。

* * *

# 第20章付録:REPL技法・標準ライブラリ索引・用語集(丸参照しやすく)

# 1\. REPLのモード早見(すぐ使える最小装備)

JuliaのREPLは「読んで(Read)評価して(Eval)印字する(Print)」を高速に回すための作業台です。3つの専用モードを正しく使い分けると、探索・検証・実行までの往復が劇的に速くなります。

  * ?(ヘルプモード):関数・型・マクロのdocstringをその場で参照します。終了はBackspace。



?println  
?LinearAlgebra

  * ;(シェルモード):OSコマンドをREPLから実行します。終了はBackspace。



;pwd  
;ls -la  
;git status

  * ](Pkgモード):パッケージ管理を対話的に操作します。終了はBackspace。



] activate .  
] instantiate  
] add DataFrames CSV  
] status  
] precompile

補助のREPLコマンドやリテラルも覚えておくと便利です。

# ヘルプなしでもdocstringを参照  
@doc println  


# 型情報・メソッド解決  
@which sum([1,2,3]) # どのメソッドが呼ばれるか  
methods(sum) # 関数のメソッド一覧  


# コード生成・最適化の確認(型推論・LLVM・ネイティブ)  
@code_warntype sqrt(3.0)  
@code_llvm sqrt(3.0)  
@code_native sqrt(3.0)

* * *

# 2\. REPLショートカット&「手が止まらない」操作集

探索を止めないキーバインド・操作をまとめます。実装/検証の往復で指が迷わないよう、体で覚えるのが最短です。

  * 入力編集: 

    * ↑/↓:履歴移動
    * Tab:補完(関数名・変数・ファイル名)
    * Ctrl+A/Ctrl+E:行頭/行末へ移動
    * Ctrl+K:行末まで削除
  * 実行制御: 

    * Ctrl+C:実行の中断(無限ループ/待機の脱出)
    * Ctrl+D:REPL終了
  * ショートスニペット:



# 編集中のファイルへジャンプ(VS Code連携前提)  
@edit sum([1,2,3])  


# 型・構造の中身をすぐ確認  
dump((a=1, b=2.0))  


# 実行時間の粗計測と厳密計測  
@time sum(rand(10^6))  
using BenchmarkTools  
@btime sum($rand(10^6))

Pkgモードの最小セット(迷ったらこれだけ覚える):

] activate . # カレントのProject.tomlで環境を有効化  
] instantiate # Manifest.tomlに従って依存を復元  
] add Foo Bar # 依存を追加  
] status # 依存とバージョンの一覧  
] precompile # 事前コンパイル(TTFX短縮)  
] test # パッケージテストの実行(プロジェクトがパッケージ構造の場合)  
] update # 互換範囲で更新  
] pin Foo # 特定バージョンへ固定(解除は `] free Foo`)

* * *

# 3. 環境運用の要点(Project.toml/Manifest.toml)

再現可能性の核になるのが環境ピン留めです。迷わない定型手順を用意します。

# プロジェクトを起動したら最初に走らせる(定型)  
julia \--project=.  
# REPLで  
] activate .  
] instantiate  
] status

  * Project.toml:パッケージ名・UUID・バージョン・直接依存([deps])・互換性([compat])。
  * Manifest.toml:依存グラフの完全スナップショット(間接依存も含む)。手編集しないのが原則。



互換性固定の最小例(Project.toml抜粋):

[compat]  
julia = "1.10"  
DataFrames = "1"  
CSV = "0.10"  
Plots = "1"

CIやレポートの冒頭で環境情報を記録すると復旧が速くなります。

using Pkg, InteractiveUtils  
versioninfo()  
Pkg.status()

* * *

# 4. 標準ライブラリ索引(用途と主要APIの目印)

Juliaは多数の標準ライブラリを同梱しています。日常利用で「ここから当たる」目印だけを押さえておくと迷いません。

  * LinearAlgebra:行列・ベクトル・分解(lu/cholesky/qr/svd/eigen)、BLAS/LAPACK連携



using LinearAlgebra  
A = rand(5,5); b = rand(5)  
x = A \ b  
F = cholesky(Symmetric(A'A) + I)

  * SparseArrays:疎行列(CSR)、spdiagm/sparse/nnz



using SparseArrays  
S = spdiagm(0 => rand(1000))  
nnz(S)

  * Statistics:mean/std/median/cor/cov



using Statistics  
mean(rand(1000)); std(rand(1000))

  * Random:乱数生成器・seed!・分布一様乱数



using Random  
rng = MersenneTwister(20240101)  
rand(rng, 1:6)

  * Dates:日時型・書式・加減算



using Dates  
d = Date("2024-12-31"); d + Day(7)

  * Printf:整形出力(@printf/@sprintf)



using Printf  
@printf "pi=%.5f\n" π

  * Markdown:Markdown生成(docstring・レポート補助)
  * Distributed:複数プロセス・addprocs・@distributed・pmap



using Distributed  
addprocs(4)  
@distributed (+) for i in 1:10^6  
1  
end

  * Sockets:ネットワークソケット・簡易TCP/UDP
  * Logging:@info/@warn/@error/@debug



using Logging  
@info "start"; @warn "careful"; @error "fail"

  * Test:単体テスト・@test/@test_throws/@testset



using Test  
@test 1 + 1 == 2

  * InteractiveUtils:versioninfo/@which/methods/@edit
  * REPL:REPL拡張・システム統合(高度用途)
  * Pkg:パッケージ管理API(コード内から操作)



using Pkg  
Pkg.activate("."); Pkg.instantiate()

  * Serialization:Juliaオブジェクトの直列化(同一バージョン前提)
  * DelimitedFiles:readdlm/writedlm(簡易CSV/TSV)
  * FileWatching:ファイル変更の監視
  * Mmap:メモリマップI/O
  * LibGit2:Git操作(内部用)
  * Libdl:動的ライブラリローダ(FFI)
  * Unicode:文字種・正規化・カテゴリ判定



補足:BaseとCoreは言語の核(Baseは日常API、Coreは言語の内部機能)。直接触れるよりは上記標準ライブラリ経由で必要機能へアクセスするのが一般的です。

* * *

# 5. 用語集(現場頻出ワードの短義と落とし穴)

  * 多重ディスパッチ:引数型の組み合わせで最適なメソッドを選ぶ仕組み。設計を「型に押し出す」のがコツ。
  * 型安定性:関数の戻り値・中間値の型が推論可能でブレないこと。@code_warntypeで確認、Anyや巨大Unionは避ける。
  * パラメトリック型:Type{T}に型変数を持たせる。汎用性と型安全を両立。AbstractVector{<:Real}のような受け口が便利。
  * ブロードキャスト:f.(xs)や@.で要素ごと演算を融合(中間配列を作らず高速化)。
  * ビュー:view(A, :, 2)や@viewsで部分配列をコピーせず参照。破壊的操作は元配列へ反映される。
  * イミュータブル/ミュータブル:structは不変、mutable structは可変。基本は不変を選ぶのが保守的。
  * JIT/LLVM:初回に型特化でコンパイル、LLVM最適化を享受。ウォームアップと型安定設計が鍵。
  * REPL:探索と検証の作業台。?/;/]のモードを使い分ける。
  * 環境:Project.toml/Manifest.tomlで依存を固定。\--project=.起動が基本。
  * Artifacts:外部資源(データ・バイナリ)をチェックサム付きで宣言・取得する仕組み。
  * 事前コンパイル(precompile):依存のコンパイルを前倒しして初回遅延(TTFX)を短縮。
  * マクロ衛生:escとgensymで識別子衝突を避ける。ユーザー式はesc、内部導入はgensym。
  * Expr:抽象構文木(AST)。メタプログラミングの基本単位。過度に直接evalするより関数生成で安全化。
  * @generated:型パラメータに応じたコード生成。静的構造に限定適用、動的値依存は不可。
  * タスク/チャネル:軽量並行(@async/Channel)。I/Oの待ち合わせやパイプラインに有効。
  * GC:ガーベジコレクション。アロケーション削減(.=融合・ビュー・インプレース)で負荷軽減。
  * BLAS/LAPACK:密行列演算の高速化。mul!・ldiv!のインプレースが効く。
  * ODE/SDE:常/確率微分方程式。DifferentialEquations.jlで解く。剛性なら陰的法。
  * DSL:領域固有言語。語彙を型化、仕様を宣言、検証・連携・生成へ橋渡し。
  * AD(自動微分):ForwardDiffなど。最適化・同定・勾配計算で多用。
  * JuMP:数理計画の宣言的モデリング。LP/MIP/NLP/QPをソルバ連携で解く。
  * MLJ:機械学習の統一インターフェース。前処理・評価・チューニングを一式で。
  * Flux:深層学習の最小API。Chain/Dense/ADAMで素早く試作。
  * Turing:確率的プログラミング(ベイズ/MCMC)。事前・事後・事後予測を一気通貫。



* * *

# 6\. REPLでの計測・診断・編集(最短ループ)

最短ループを体に入れます。困ったらこの順番で当たると早いです。

# 1) 型推論は安定しているか  
@code_warntype yourfunc(args...)  


# 2) どのメソッドが選ばれているか  
@which yourfunc(args...)  


# 3) 時間とアロケーションは妥当か(粗→厳密)  
@time yourfunc(args...)  
using BenchmarkTools  
@btime yourfunc($args...)  


# 4) 行きたい定義へジャンプ(VS Code連携)  
@edit yourfunc(args...)

パフォーマンスの勘所:

  * .=で左辺を再利用(中間配列を避ける)
  * mul!・ldiv!のインプレース
  * @views+@inbounds(正しさ検証後)
  * 型を揃える(promote)・Anyを排除
  * ホットループを具体型に落とす(関数バリア)



* * *

# 7. 標準ライブラリ実用断片(すぐ貼って使える)

# Statistics: 欠損混じりの集計  
using Statistics  
xs = [1,2,missing,4]  
mean(skipmissing(xs)) # 欠損を除いて平均  
coalesce.(xs, 0) # 欠損を0に置換  


# Dates: パーサと加算  
using Dates  
Date("2025-01-01") + Day(30)  


# Logging: 重要イベントを記録  
using Logging  
@info "start batch" job_id=123  
@warn "retry" attempt=2  
@error "failed" reason="timeout"  


# Distributed: 粗粒度の総和(単ノードでもOK)  
using Distributed  
addprocs(4)  
@distributed (+) for i in 1:10^6  
1  
end  


# Test: 境界と異常系  
using Test  
@testset "div" begin  
@test 6 ÷ 3 == 2  
@test_throws DivideError 1 ÷ 0  
end

* * *

# 8. トラブルシューティング(よくある落とし穴と脱出)

  * グローバル環境で依存が混ざる:必ず\--project=.で起動、activate .→instantiate→status。
  * Anyで遅い:@code_warntypeで赤い箇所を潰す。戻り値を揃える、コンテナ要素型を具体に。
  * 並列なのに遅い:共有書き込み・偽共有を疑う。ローカル集計→縮約へ設計変更。
  * GPUが遅い:スカラーアクセス禁止(CUDA.allowscalar(false))。転送をまとめ、ブロードキャストで融合。
  * TTFXが長い:Pkg.precompile()、頻用関数へprecompile声明。ウォームアップ実行。
  * 乱数が揺れる:入口でseed!固定。スレッド/プロセスはseed + id規則で配布。消費順序が分岐で変わらない設計に。



* * *

# 9. 参考資料・コミュニティ入口(名前索引)

リンクは省略し、名前だけを案内します。検索・ブックマークの起点にしてください。

  * ドキュメント: 

    * Juliaの公式ドキュメント
    * Pkgの使用ガイド
    * 標準ライブラリ(LinearAlgebra/Statistics/Random/Distributed/Dates/Test)
    * Documenter.jlのユーザーガイド
  * 主要パッケージ: 

    * DataFrames.jl・CSV.jl・Arrow.jl
    * Plots.jl・Makie.jl
    * DifferentialEquations.jl・SciMLSensitivity.jl
    * JuMP.jl・ソルバ(Ipopt・HiGHSなど)
    * Flux.jl・MLJ.jl・Turing.jl・ForwardDiff.jl
  * 開発運用: 

    * PkgTemplates.jl・BenchmarkTools.jl・PkgBenchmark.jl
    * Revise.jl
  * コミュニティ: 

    * Julia Discourse
    * JuliaのGitHub Organization
    * Zulip(Julia)
    * 主要会議・ミートアップ(JuliaCon など)



* * *

# 10. 実務チェックリスト(REPL・標準・再現の総まとめ)

☐ \--project=.で起動し、activate→instantiate→statusを定型化した。

☐ ?/;/]の3モードを使い分け、@which/@code_warntype/@btimeを手癖にした。

☐ 標準ライブラリの入口(LinearAlgebra/Statistics/Random/Distributed/Dates/Test)を把握した。

☐ 乱数種・設定値の固定、成果物の自動生成(図・表・数値)をコード化した。

☐ ログ・プロベナンス(versioninfo/Pkg.status/入力ハッシュ)の記録を整えた。

☐ テスト・ベンチ・CIの最小構成を持ち、回帰に気づける体制にした。

☐ 迷ったときの最短ループ(診断→ジャンプ→修正→計測)を身につけた。

以上を常用すれば、REPLでの探索から、標準ライブラリの実用、再現可能な成果物の生成まで、Juliaを「参照しやすく・壊れにくい」作業環境として運用できます。

![](image_rsrc4JB.jpg)
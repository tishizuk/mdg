本作品は、横書き表示での閲覧を推奨いたします。縦書き表示にした際には、表示が一部くずれる恐れがあります。

ご利用になるブラウザまたはビューワにより、表示が異なることがございます。

---

![](image_rsrcAG5.jpg)

---

カバーイラスト:ウラベロシナンテ

カバーデザイン:末吉亮(図工ファイブ)

---

●本書の執筆にあたって,以下の計算機環境を利用しています.

Ubuntu Desktop 20.04(AMD64)

Julia 1.6, 1.7, 1.8

●サポートページURL

<https://bicycle1885.org/julia-taizen/>

本書に掲載されているサンプルプログラムやスクリプト,およびそれらの実行結果や出力などは,上記の環境で再現された一例です.本書の内容に関して適用した結果生じたこと,また,適用できなかった結果について,著者および出版社は一切の責任を負えませんので,あらかじめご了承ください.

●本書に記載されている情報は,2023年1月時点のものです.

●本書に記載されているウェブサイトなどは,予告なく変更されていることがあります.

●本書に記載されている会社名,製品名,サービス名などは,一般に各社の商標または登録商標です.なお,本書では,TM, ®, ©マークを省略しています.

---

# まえがき

新しいプログラミング言語を使い始めるきっかけは人によってさまざまです。筆者がJuliaを使い始めたのは、タンパク質分子の周囲のポテンシャルを計算するためでした。最初にPythonで書いたプログラムはあまりに遅かったので、一部をC++に書き換え、Boost経由でPythonから呼び出していました。こうすると確かに性能は出るものの、ビルドやデバッグの困難さに辟易としたことを覚えています。

そんなときに見つけたのがJuliaでした。JuliaはC++のように高速でありながらPythonのように手軽に使え、私にとっては革命的なプログラミング言語でした。当時のJuliaはまだ開発版だったこともあり、起動するだけで何秒もかかり、不具合も多く、破壊的な変更も頻繁に導入されました。それでも今日まで10年以上に渡ってJuliaを使い続けてきたのは、高速さや手軽さだけではない、言葉にし切れない魅力をJuliaに見出してきたからです。それはJuliaを通して見える計算機の面白さであったり、Juliaをより良いものにしようと奮闘するコミュニティの情熱であったりします。

本書は、そのようなJuliaの魅力を、より多くの人に知ってもらいたいとの想いから執筆されました。それと同時に、Juliaを通して知識や興味を広げてもらえるよう、データの表現やコンパイラの動作なども扱っています。執筆にあたっては要点をなるべく簡潔に記述するよう努めたつもりですが、知っておいてほしいことを余さず含めた結果、約600ページにもなってしまいました。

本書を読むにあたりJuliaの前提知識は必要ありません。意欲的な中高生や大学生であれば、十分読み進められるでしょう。他のプログラミング言語を習得している技術者や研究者であれば、Juliaの要点を短時間で習得できる体系的かつ網羅的な内容になっているはずです。すでにJuliaを使っている方にとっても、知識の補完や参照用として大いに役立つと考えています。科学技術計算への応用を考慮し、一般的なプログラミング言語の書籍に比べて数値計算を重点的に扱っています。

本書は4部で構成されます。プログラミングの初心者でも理解できるよう、基本的な事柄から解説が始まり、最終的には、実践的な開発ができる内容になっています。全体を読み通すには相当の時間がかかりますが、導入にあたる第1部を読むだけでもJuliaの概要を把握し、使い始められるようになっています。第2部はデータの表現と言語の特徴を解説しており、本書の根幹をなす部分でもあります。Juliaの大きな特徴である多重ディスパッチやメタプログラミングもここで扱っています。第3部は科学技術計算や並行計算など、標準ライブラリを中心としたライブラリの紹介です。第4部ではパッケージ管理や高速化など、Juliaで開発を進めるにあたって知っておくと役立つ知識を紹介しています。さらに、開発に関わる周辺知識を付録としてまとめています。

各章はその章で扱う重要な概念の説明から始まり、具体例や詳細な解説がそれに続く構成になっています。各項は短く、トピックがわかりやすいように配慮されていますので、必要な部分を拾い読みするときにも便利です。実際に手を動かして実験しやすいよう、本文にはコードと実行結果の例を豊富に示しています。読者の皆様からの質問や説明が不足した部分があれば、適宜サポートページで補っていく予定です。

本書の刊行にあたり、本書の原稿に貴重な助言をくださった黒木玄さん、村田賢太さん、門脇宗平さん、ごまふあざらしさん、山本拓都さん、きゅあれもねーどさん、古田悠馬さんには、この場を借りて感謝を申し上げます。また、5年もの長きにわたり本書の刊行のためにご尽力いただいた講談社サイエンティフィクの横山真吾さんにも、厚く御礼を申し上げます。自分自身を育ててくれたJuliaへの限りなく大きな恩を、本書で少しでも返せていたら幸甚です。

2023年4月

佐藤建太

---

目次

[まえがき](c1Y.xhtml)

[第1部導入](cC8.xhtml)

[第1章 Julia観光](cCH.xhtml)

[1.1 本書の目的と構成](cCH.xhtml#a960)

[1.2 プログラミング言語としてのJulia](cCH.xhtml#a961)

1.2.1 最初のコード例/1.2.2 特徴的な機能/1.2.3 実行時コンパイル

[1.3 科学技術計算環境としてのJulia](cCH.xhtml#a962)

1.3.1 高い実行性能/1.3.2 豊富な型と拡張性/1.3.3 Julia自身で書かれたライブラリ/1.3.4 数値計算ライブラリ/1.3.5 パッケージ管理システム

[1.4 Juliaの開発とコミュニティ](cCH.xhtml#a963)

1.4.1 オープンなライセンスと開発体制/1.4.2 豊富な科学技術計算用のパッケージ/1.4.3 Juliaを支えるコミュニティ

[第2章環境構築](cK9.xhtml)

[2.1 サンプルコード](cK9.xhtml#a965)

[2.2 Juliaのインストール](cK9.xhtml#a966)

2.2.1 Juliaのライセンス/2.2.2 Juliaupを使ったインストール/2.2.3 Juliaのダウンロード/2.2.4 Windowsでのインストール/2.2.5 macOSでのインストール/2.2.6 Linuxでのインストール/2.2.7 ソースコードからのインストール/2.2.8 インストールの確認

[2.3 対話的実行環境](cK9.xhtml#a967)

2.3.1 対話的実行環境の基本操作/2.3.2 補完と履歴/2.3.3 ![](image_rsrcAG6.jpg)記法による文字の入力/2.3.4 ヘルプモード/2.3.5 パッケージ管理モード/2.3.6 コマンドモード

[2.4 スクリプトファイルの実行](cK9.xhtml#a968)

2.4.1 スクリプトファイルの形式/2.4.2 ターミナルからの実行/2.4.3 REPLへの読込み/2.4.4 コマンドライン引数の実行

[2.5 Juliaの開発環境](cK9.xhtml#a969)

2.5.1 Visual Studio Code/2.5.2 JupyterLab/2.5.3 テキストエディタのプラグイン

[第3章ファーストステップ](cXU.xhtml)

[3.1 REPLで実験](cXU.xhtml#a96B)

3.1.1 簡単な計算/3.1.2 エラー/3.1.3 実行順序/3.1.4 オブジェクトと変数/3.1.5 基本的なオブジェクト/3.1.6 オブジェクトの比較/3.1.7 コレクション/3.1.8 型/3.1.9 入出力

[3.2 基本構文](cXU.xhtml#a96C)

3.2.1 分岐・反復・関数定義/3.2.2 具体例:素数を判定する関数/3.2.3 具体例:素数を列挙する関数/3.2.4 条件分岐構文のバリエーション/3.2.5 関数定義構文のバリエーション/3.2.6 型の定義

[3.3 オセロを作ろう](cXU.xhtml#a96D)

3.3.1 オセロモジュールの定義/3.3.2 石と操作関数の定義/3.3.3 マスの位置/3.3.4 オセロ盤/3.3.5 石の打ち方/3.3.6 プレイヤ/3.3.7 ゲーム

[第2部データと言語の基礎](c1JN.xhtml)

[第4章数値と算術](c1JX.xhtml)

[4.1 真偽値](c1JX.xhtml#a96G)

4.1.1 真偽値の演算/4.1.2 数値としての真偽値

[4.2 整数](c1JX.xhtml#a96H)

4.2.1 整数の表現/4.2.2 整数の型/4.2.3 整数の表記/4.2.4 整数の算術演算/4.2.5 整数のビット演算

[4.3 有理数](c1JX.xhtml#a96J)

[4.4 浮動小数点数](c1JX.xhtml#a96K)

4.4.1 浮動小数点数の表現/4.4.2 浮動小数点数の正規化/4.4.3 浮動小数点数の形式/4.4.4 丸めと誤差/4.4.5 浮動小数点数の型/4.4.6 浮動小数点数の出力に関する注意/4.4.7 浮動小数点数の比較/4.4.8 浮動小数点数の関数/4.4.9 可変サイズの浮動小数点数

[4.5 複素数](c1JX.xhtml#a96M)

[4.6 定数の無理数](c1JX.xhtml#a96N)

[4.7 数値の演算](c1JX.xhtml#a96P)

4.7.1 算術演算子の優先順位/4.7.2 昇格/4.7.3 数学関数/4.7.4 数学関数の戻り値の型/4.7.5 数学関連のパッケージ

[第5章コレクション](c251.xhtml)

[5.1 代表的なコレクション](c251.xhtml#a96S)

5.1.1 タプル/5.1.2 範囲/5.1.3 配列/5.1.4 辞書/5.1.5 集合/5.1.6 ジェネレータ

[5.2 コレクションの操作](c251.xhtml#a96T)

5.2.1 イテレーション/5.2.2 検索/5.2.3 変換/5.2.4 集約/5.2.5 複製

[5.3 コレクションの特性と使い分け](c251.xhtml#a96U)

5.3.1 コレクションの不変性と可変性/5.3.2 タプルと配列の使いどころ/5.3.3 外部ライブラリの選択肢

[第6章文字列](c2FB.xhtml)

[6.1 文字の表現](c2FB.xhtml#a96W)

6.1.1 コードポイント/6.1.2 文字のリテラル/6.1.3 文字の操作/6.1.4 文字の分類

[6.2 文字列の表現](c2FB.xhtml#a96X)

6.2.1 文字列のリテラル/6.2.2 文字列の補間/6.2.3 文字列の比較/6.2.4 エンコーディング/6.2.5 エンコーディングの有効性/6.2.6 文字の取得/6.2.7 部分文字列/6.2.8 文字列の正規化

[6.3 正規表現](c2FB.xhtml#a96Y)

6.3.1 正規表現の基本/6.3.2 文字列の抽出/6.3.3 高度な正規表現の機能

[6.4 文字列処理](c2FB.xhtml#a96Z)

6.4.1 数値のパース/6.4.2 文字列の検索/6.4.3 文字列の変換/6.4.4 文字列の結合と分解/6.4.5 テキストの入出力/6.4.6 効率的な文字列の構築

[第7章スコープと構文](c315.xhtml)

[7.1 変数](c315.xhtml#a971)

7.1.1 変数の命名規則/7.1.2 任意の変数名を作るマクロ/7.1.3 変数の代入

[7.2 スコープ](c315.xhtml#a972)

7.2.1 スコープの基本概念/7.2.2 グローバルスコープ/7.2.3 定数の定義/7.2.4 ローカルスコープ/7.2.5 ローカルスコープの入れ子/7.2.6 let文による変数のシャドーイング/7.2.7 begin文によるブロックの構築/7.2.8 変数の存在確認/7.2.9 スコープの明示的な指定/7.2.10 対話的環境でのスコープ

[7.3 演算子](c315.xhtml#a973)

7.3.1 積の省略/7.3.2 関数の演算子/7.3.3 優先順位と結合性

[7.4 条件分岐](c315.xhtml#a974)

7.4.1 短絡評価/7.4.2 条件演算子とifelse関数/7.4.3 if文

[7.5 反復](c315.xhtml#a975)

7.5.1 while文/7.5.2 for文/7.5.3 反復構文の制御/7.5.4 無条件ジャンプ

[7.6 例外処理](c315.xhtml#a976)

7.6.1 catchブロックでの例外の捕捉/7.6.2 finallyブロックでの後処理/7.6.3 elseブロック/7.6.4 ブロックの省略と実行順序

[7.7 関数定義](c315.xhtml#a977)

7.7.1 関数定義の基本/7.7.2 可変長引数/7.7.3 オプショナル引数/7.7.4 キーワード引数/7.7.5 引数のスプラッティング/7.7.6 デフォルトの評価/7.7.7 クロージャ/7.7.8 無名関数/7.7.9 無名関数の後置/7.7.10 引数の部分適用

[7.8 型注釈](c315.xhtml#a978)

7.8.1 式の型注釈/7.8.2 変数の型注釈/7.8.3 関数の型注釈

[7.9 モジュール](c315.xhtml#a979)

7.9.1 モジュールの定義/7.9.2 既定のモジュール/7.9.3 using文/7.9.4 import文/7.9.5 モジュール名の相対指定/7.9.6 export文/7.9.7 ベアモジュール

[第8章型システム](c3S5.xhtml)

[8.1 型の階層](c3S5.xhtml#a97B)

[8.2 パラメトリック型](c3S5.xhtml#a97C)

8.2.1 パラメトリック型の例/8.2.2 パラメトリック型の意義/8.2.3 パラメータの制約/8.2.4 パラメトリック型の不変性とその例外

[8.3 ユニオン型](c3S5.xhtml#a97D)

[8.4 型の別名](c3S5.xhtml#a97E)

[8.5 抽象型の定義](c3S5.xhtml#a97F)

[8.6 構造体型](c3S5.xhtml#a97G)

8.6.1 構造体型の定義/8.6.2 構造体型定義の例/8.6.3 パラメトリックな構造体型/8.6.4 シングルトン

[8.7 プリミティブ型の定義](c3S5.xhtml#a97H)

[8.8 不変性と可変性](c3S5.xhtml#a97J)

[8.9 型のリフレクション](c3S5.xhtml#a97K)

8.9.1 型の性質/8.9.2 構造体型のフィールド情報

[第9章多重ディスパッチ](c434.xhtml)

[9.1 メソッド](c434.xhtml#a97N)

9.1.1 関数とメソッドの関係/9.1.2 メソッドの多重ディスパッチ/9.1.3 総称関数とビルトイン関数

[9.2 メソッドの定義](c434.xhtml#a97P)

9.2.1 引数の数によるディスパッチ/9.2.2 引数の型によるディスパッチ/9.2.3 ディスパッチの優先順位/9.2.4 パラメータを持つメソッドのディスパッチ/9.2.5 型によるディスパッチ/9.2.6 メソッドを持たない関数の定義

[9.3 コンストラクタ](c434.xhtml#a97R)

9.3.1 内部コンストラクタ/9.3.2 外部コンストラクタ/9.3.3 パラメータ付きコンストラクタ/9.3.4 不完全な初期化/9.3.5 オブジェクト解放時の処理

[9.4 多重ディスパッチを使った機能拡張](c434.xhtml#a97S)

9.4.1 他モジュールにある関数の拡張/9.4.2 Baseモジュールにある関数の拡張/9.4.3 型変換の実装/9.4.4 オブジェクト表示の実装/9.4.5 イテレータの実装/9.4.6 演算子の拡張

[9.5 トレイト](c434.xhtml#a97T)

[第10章メタプログラミング](c4G7.xhtml)

[10.1 実行の仕組み](c4G7.xhtml#a97V)

10.1.1 動的コンパイル/10.1.2 構文解析/10.1.3 評価

[10.2 抽象構文木の表現と操作](c4G7.xhtml#a97W)

10.2.1 シンボル/10.2.2 式の表現/10.2.3 式の補間/10.2.4 式のコンストラクタ/10.2.5 ブロックのクォート

[10.3 動的なコード生成](c4G7.xhtml#a97X)

[10.4 マクロ](c4G7.xhtml#a97Y)

10.4.1 マクロ呼出し/10.4.2 標準ライブラリのマクロ/10.4.3 マクロ定義の基本/10.4.4 衛生的なマクロ/10.4.5 変数のエスケープ/10.4.6 実用的なマクロの実装例/10.4.7 非標準文字列リテラルの定義

[10.5 生成関数](c4G7.xhtml#a97Z)

10.5.1 生成関数の定義/10.5.2 生成関数の応用例

[10.6 マクロと生成関数の定義に関する注意](c4G7.xhtml#a980)

[第3部ライブラリ](c4W2.xhtml)

[第11章科学技術計算](c4WA.xhtml)

[11.1 多次元配列](c4WA.xhtml#a983)

11.1.1 多次元配列の概要/11.1.2 多次元配列の構築/11.1.3 多次元配列の連結/11.1.4 多次元配列の変形/11.1.5 整数を使った2つのインデクシング/11.1.6 インデクシング関数/11.1.7 範囲や配列を使ったインデクシング/11.1.8 インデクシングの注意/11.1.9 部分配列/11.1.10 ベクトル化演算/11.1.11 ブロードキャスト/11.1.12 フュージョン/11.1.13 ビット配列

[11.2 疑似乱数](c4WA.xhtml#a984)

11.2.1 基本的な疑似乱数生成関数/11.2.2 その他の疑似乱数生成関数/11.2.3 再現性のある疑似乱数生成

[11.3 線形代数](c4WA.xhtml#a985)

11.3.1 ベクトルと行列の表現/11.3.2 ベクトルと行列の演算/11.3.3 行列分解

[11.4 疎な配列](c4WA.xhtml#a986)

11.4.1 疎なベクトルと行列の構築と基本操作/11.4.2 疎行列の内部構造

[11.5 統計](c4WA.xhtml#a987)

11.5.1 統計関数/11.5.2 多次元配列の統計量/11.5.3 欠損値の取り扱い/11.5.4 統計関係の外部パッケージ

[第12章並行プログラミング](c5F8.xhtml)

[12.1 並行プログラミングの基礎](c5F8.xhtml#a989)

12.1.1 並行・並列・分散/12.1.2 プロセス・スレッド・コルーチン/12.1.3 タスク/12.1.4 マルチタスキング/12.1.5 タスクのスケジューリング/12.1.6 マクロを使った非同期実行と同期/12.1.7 チャネルを使ったタスク間の通信

[12.2 並列プログラミング](c5F8.xhtml#a98A)

12.2.1 並列プログラミングの概要/12.2.2 マルチスレッディングのインタフェース/12.2.3 具体例:マンデルブロ集合描画の並列化/12.2.4 ロックによる排他制御/12.2.5 ロックの種類と注意点/12.2.6 コンディションによる同期/12.2.7 マクロによるアトミック操作

[12.3 分散プログラミング](c5F8.xhtml#a98B)

12.3.1 Juliaのプロセス間通信/12.3.2 処理のリモート実行/12.3.3 データとコードの共有/12.3.4 高レベルのリモート実行インタフェース/12.3.5 複数ノードでの分散処理のセットアップ

[第13章システムプログラミング](c5Y6.xhtml)

[13.1 ファイルシステム](c5Y6.xhtml#a98D)

13.1.1 パスの操作/13.1.2 ファイルシステムのクエリ関数/13.1.3 ファイルシステムの操作関数/13.1.4 一時ファイルと一時ディレクトリの管理

[13.2 ストリーム](c5Y6.xhtml#a98E)

13.2.1 標準ストリーム/13.2.2 パイプラインとリダイレクト/13.2.3 ファイル/13.2.4 入出力関数

[13.3 シリアライゼーション](c5Y6.xhtml#a98F)

13.3.1 Serialization.jl/13.3.2 JSON/13.3.3 CSV/13.3.4 HDF5

[13.4 日付と時刻](c5Y6.xhtml#a98G)

13.4.1 時間の基本操作/13.4.2 時間の計算/13.4.3 時間と文字列の変換/13.4.4 時刻の変換/13.4.5 タイムゾーン

[13.5 ロギング](c5Y6.xhtml#a98H)

13.5.1 ログのレベルと出力/13.5.2 ログ出力の制御/13.5.3 ロガーの設定

[第14章外部プログラムの実行](c6A2.xhtml)

[14.1 外部プログラム実行の基本](c6A2.xhtml#a98K)

14.1.1 単純なコマンドの作成/14.1.2 コマンドの実行/14.1.3 出力の捕捉/14.1.4 シェルの機能に関する注意

[14.2 パイプライン](c6A2.xhtml#a98M)

14.2.1 単純なパイプラインの構築/14.2.2 パイプラインのオプション/14.2.3 パイプラインのエラー処理

[14.3 発展的な外部プログラム実行](c6A2.xhtml#a98N)

14.3.1 コマンドの環境設定/14.3.2 外部プログラムの非同期実行/14.3.3 実行中プロセスとの通信/14.3.4 コマンドリテラルの詳細

[第15章他のプログラミング言語との連携](c6GX.xhtml)

[15.1 C との連携](c6GX.xhtml#a98R)

15.1.1 C との連携の概要/15.1.2 共有ライブラリの作成/15.1.3 共有ライブラリにあるシンボルの取得/15.1.4 C と互換性のある型/15.1.5 グローバル変数の読み書き/15.1.6 関数呼出し/15.1.7 オブジェクトの保護/15.1.8 配列の受け渡し/15.1.9 構造体の受け渡し/15.1.10 具体例:XZ Utilsのラップ

[15.2 Pythonとの連携](c6GX.xhtml#a98S)

15.2.1 PyCall.jlのインストール/15.2.2 Python関数の呼出し/15.2.3 Pythonモジュールの利用/15.2.4 Pythonコードの埋め込み/15.2.5 配列の受け渡し

[15.3 R との連携](c6GX.xhtml#a98T)

15.3.1 RCall.jlのインストール/15.3.2 R 関数の呼出し/15.3.3 型の変換/15.3.4 R のREPL

[第4部開発](c6XA.xhtml)

[第16章パッケージ管理と開発](c6XJ.xhtml)

[16.1 パッケージの基礎概念](c6XJ.xhtml#a98W)

16.1.1 パッケージの概要/16.1.2 リビジョンとバージョン/16.1.3 セマンティックバージョニング/16.1.4 環境/16.1.5 レジストリ/16.1.6 アーティファクト/16.1.7 デポ

[16.2 パッケージの管理](c6XJ.xhtml#a98X)

16.2.1 パッケージ管理モード/16.2.2 環境の切り替え/16.2.3 パッケージの追加・更新・削除・固定/16.2.4 Pkg.jlのAPI/16.2.5 環境の再現/16.2.6 バージョンの制約/16.2.7 環境のスタック

[16.3 パッケージの開発](c6XJ.xhtml#a98Y)

16.3.1 パッケージの生成/16.3.2 依存パッケージの追加/16.3.3 分割による構造化/16.3.4 ユニットテスト/16.3.5 ドキュメント/16.3.6 パッケージの公開/16.3.7 パッケージ開発の補助ツール

[第17章開発実践ガイド](c79X.xhtml)

[17.1 コーディングスタイル](c79X.xhtml#a990)

[17.2 命名に関するガイドライン](c79X.xhtml#a991)

17.2.1 変数の命名/17.2.2 関数とマクロの命名/17.2.3 型とモジュールの命名/17.2.4 命名に困ったときは

[17.3 フォーマッティング](c79X.xhtml#a992)

17.3.1 改行/17.3.2 空白/17.3.3 丸括弧の使用/17.3.4 インデント/17.3.5 モジュールの利用

[17.4 アンチパターン](c79X.xhtml#a993)

17.4.1 不必要に強い型注釈/17.4.2 戻り値の型が不安定な関数/17.4.3 型の乗っとり/17.4.4 一貫性のない引数の順序

[17.5 実行モデル](c79X.xhtml#a994)

17.5.1 ソースコードからマシンコードまでの表現/17.5.2 ソースコードが実行されるまで

[17.6 型推論](c79X.xhtml#a995)

17.6.1 型推論の概要/17.6.2 型の不安定性の診断/17.6.3 型の不安定性が生じやすいケース

[第18章性能の改善](c7N0.xhtml)

[18.1 事前準備](c7N0.xhtml#a997)

18.1.1 実行環境の用意/18.1.2 プログラムのバージョン管理/18.1.3 テストとデータの用意

[18.2 ベンチマーク](c7N0.xhtml#a998)

18.2.1 経過時間・ユーザCPU時間・システムCPU時間/18.2.2 コマンドラインツール/18.2.3 標準ライブラリのツール/18.2.4 BenchmarkTools.jlパッケージ

[18.3 プロファイル](c7N0.xhtml#a999)

18.3.1 実行時間のプロファイル/18.3.2 メモリ割当てのプロファイル/18.3.3 イベントのプロファイル

[18.4 コンパイラの活用](c7N0.xhtml#a99A)

18.4.1 型推論と最適化/18.4.2 コンパイラへのヒント

[18.5 多重ディスパッチを使ったテクニック](c7N0.xhtml#a99B)

18.5.1 パラメータのエンコーディング/18.5.2 特殊な構造を持つ配列

[18.6 メモリ割当て](c7N0.xhtml#a99C)

18.6.1 いつメモリ割当てが発生するか/18.6.2 不変な構造体型か可変な構造体型か/18.6.3 配列のメモリ割当て削減/18.6.4 文字列の分解と結合

[18.7 CPUの性能を引き出す](c7N0.xhtml#a99D)

18.7.1 キャッシュ/18.7.2 多次元配列のメモリレイアウト/18.7.3 パイプライン/18.7.4 分岐予測と投機的実行/18.7.5 具体例:Base64符号化

[18.8 並列化](c7N0.xhtml#a99E)

18.8.1 高速化の限界/18.8.2 タスクの粒度/18.8.3 フォールスシェアリング/18.8.4 MapReduceパターン/18.8.5 ネスト並列化

[付録 A Markdown](c888.xhtml)

[付録 B TOML](c8BX.xhtml)

[付録 C GitとGitHubの基本](c8EH.xhtml)

[用語一覧](c8K8.xhtml)

---

リスト目次

[1.1 挿入ソートの関数定義](cCH.xhtml#a9RN) insertionsort.jl

[1.2 エイトクイーンパズル](cCH.xhtml#aADF) nq/nqueens.jl

[1.3 主成分分析による次元削減](cCH.xhtml#a9W0) pca.jl

[2.1 簡単なスクリプト](cK9.xhtml#aA0V) hello.jl

[2.2 引数をとるスクリプト](cK9.xhtml#a9PE) helloargs.jl

[3.1 素数判定関数](cXU.xhtml#a9B6) isprime.jl

[3.2 素数列挙関数](cXU.xhtml#aAA0) primes.jl

[6.1 文字列を1文字ずつ出力する関数](c2FB.xhtml#a9DR) printchars.jl

[7.1 最後の要素を返す関数](c315.xhtml#aA0M) lastitem.jl

[7.2 フィボナッチ関数](c315.xhtml#aA85) fib.jl

[7.3 最小値関数](c315.xhtml#a9H9) min.jl

[8.1 周期表](c3S5.xhtml#a9TE) periodictable.jl

[8.2 構造体型のフィールドを表示する関数](c3S5.xhtml#aA6P) reportfields.jl

[9.1 円を表現する構造体型](c434.xhtml#aA69) circle.jl

[9.2 RGB色モデル](c434.xhtml#a9Y8) rgb.jl

[9.3 正規分布](c434.xhtml#a9AT) normal.jl

[9.4 ノードの構造体型](c434.xhtml#a9JD) node.jl

[9.5 スタック構造体型の定義](c434.xhtml#a9RK) stack.jl

[9.6 スタックの操作関数](c434.xhtml#a9XW) stack.jl

[9.7 短い文字列型](c434.xhtml#a9W6) shortstring.jl

[9.8 オブジェクトの表示](c434.xhtml#aAFG) shortstring.jl

[9.9 ビット回転イテレータ](c434.xhtml#a9E6) bitrotator.jl

[9.10 コンポジションと演算子](c434.xhtml#a9X1) composition.jl

[9.11 トレイトを使ったディスパッチ](c434.xhtml#aA72) trait.jl

[10.1 コード生成による関数定義](c4G7.xhtml#a9DW) msb.jl

[10.2 問題のあるマクロ定義](c4G7.xhtml#a9FZ) check.jl

[10.3 式を繰り返し評価するマクロ](c4G7.xhtml#aA9W) repeat.jl

[10.4 実行時間を計測するマクロ](c4G7.xhtml#a9EJ) elapsed.jl

[10.5 文字列補間マクロ](c4G7.xhtml#a9J7) fstring.jl

[10.6 生成関数の例](c4G7.xhtml#aAFM) genfun.jl

[10.7 n-1 次多項式を計算する生成関数](c4G7.xhtml#aA09) polynomial.jl

[11.1 疎行列を作成する関数](c4WA.xhtml#a9V6) mysparse.jl

[12.1 たらい回し関数](c5F8.xhtml#aA39) tarai.jl

[12.2 生産者・消費者パターン](c5F8.xhtml#aAF5) producer-consumer.jl

[12.3 誤った並列カウンタ](c5F8.xhtml#a9XU) counter-bad.jl

[12.4 マンデルブロ集合の計算](c5F8.xhtml#a9P4) mb/mandelbrot.jl

[12.5 マンデルブロ集合の逐次描画](c5F8.xhtml#a9MN) mb/plot-seq.jl

[12.6 マンデルブロ集合の並列描画](c5F8.xhtml#a9UH) mb/plot-par1.jl

[12.7 ロックを使った並列カウンタ](c5F8.xhtml#a9XX) counter-lock.jl

[12.8 マルチスレッドでのイベント通知](c5F8.xhtml#a9BP) notification.jl

[12.9 アトミック操作を使ったカウンタ](c5F8.xhtml#a9SR) counter-atomicadd.jl

[12.10 アトミック型の定義](c5F8.xhtml#aA0T) atomic.jl

[12.11 ワーカーと接続するスクリプト](c5F8.xhtml#a9HB) dist/addworkers.jl

[13.1 現在のディレクトリ以下を探索するプログラム](c5Y6.xhtml#a9S5) walkpwd.jl

[13.2 入力をタイトルケースに変換するスクリプト](c5Y6.xhtml#aA9H) titlecase.jl

[13.3 JSON.jlの使用例](c5Y6.xhtml#aA7H) json.jl

[13.4 ファイルへのロギング](c5Y6.xhtml#a9YP) logging.jl

[14.1 topコマンドのロガー](c6A2.xhtml#a9CX) toplogger.jl

[15.1 単純な共有ライブラリのソースコード](c6GX.xhtml#a9EC) voyager.c

[15.2 メモリの比較関数](c6GX.xhtml#a9F9) memcmp.jl

[15.3 単純な C のソート関数](c6GX.xhtml#a9PW) sort.c

[15.4 Pythonコードの埋め込み](c6GX.xhtml#aA1K) textwrap.jl

[16.1 Example.jlを使うスクリプト](c6XJ.xhtml#a9HA) myproject/hello.jl

[16.2 パッケージのテンプレート](c6XJ.xhtml#a9WJ) pkgtemplate.jl

[16.3 ソースコードの分割](c6XJ.xhtml#a9T4) Othello/src/Othello.jl

[16.4 パッケージのテストファイル](c6XJ.xhtml#a9PB) Othello/test/runtests.jl

[16.5 docstringの例](c6XJ.xhtml#a9BV) message.jl

[17.1 モジュールのインデント](c79X.xhtml#aA66) PrimeTools.jl

[17.2 強すぎる引数の型注釈](c79X.xhtml#a9ZU) isin.jl

[17.3 戻り値の型が不安定な総和関数](c79X.xhtml#a9E3) sum-unstable.jl

[17.4 単純なループ関数](c79X.xhtml#a9UP) loop.jl

[17.5 戻り値の型が安定している総和関数](c79X.xhtml#a9AX) sum-stable.jl

[18.1 円の半径から直径・円周・面積を計算するスクリプト](c7N0.xhtml#aA9B) calccircle.jl

[18.2 リスト18.1の簡易テスト](c7N0.xhtml#aA11) calccircle-test.jl

[18.3 実行時間のプロファイル](c7N0.xhtml#a9X7) sleeper.jl

[18.4 メモリ割当てのプロファイル](c7N0.xhtml#a9RA) allocator.jl

[18.5 長さが4の固定長ベクトル](c7N0.xhtml#aA1S) vec4.jl

[18.6 階数が1以下の行列](c7N0.xhtml#a9ER) rank1matrix.jl

[18.7 簡単なデータベース](c7N0.xhtml#aAC9) database.jl

[18.8 文字列を分割する関数](c7N0.xhtml#a9GF) split.jl

[18.9 行優先の各行最小値関数](c7N0.xhtml#aAFN) rowmins.jl

[18.10 列優先の各行最小値関数](c7N0.xhtml#a9EV) rowmins.jl

[18.11 ソート済み配列のマージ](c7N0.xhtml#aA2K) sortmerge.jl

[18.12 Base64のASCII文字への変換関数](c7N0.xhtml#aAD2) b64/hex2char.jl

[18.13 Base64のエンコード関数(バージョン1)](c7N0.xhtml#aACS) b64/encode1.jl

[18.14 Base64のエンコード関数(バージョン2)](c7N0.xhtml#a9D5) b64/encode2.jl

[18.15 Base64のエンコード関数(バージョン3)](c7N0.xhtml#aAC4) b64/encode3.jl

[18.16 マンデルブロ集合の並列描画(改善版)](c7N0.xhtml#aABK) mb/plot-par2.jl

[18.17 フォールスシェアリングの例](c7N0.xhtml#a9F8) fs/parsum.jl

[18.18 ヒストグラムの計算(逐次実行)](c7N0.xhtml#a9KJ) hist/histogram-serial.jl

[18.19 ヒストグラムの計算(ロック)](c7N0.xhtml#aAC7) hist/histogram-lock.jl

[18.20 ヒストグラムの計算(MapReduce)](c7N0.xhtml#aA83) hist/histogram-mapreduce.jl

[18.21 エイトクイーンパズルの並列化(フラット)](c7N0.xhtml#aADC) nq/nqueens-flat.jl

[18.22 エイトクイーンパズルの並列化(ネスト)](c7N0.xhtml#a9PS) nq/nqueens-nested.jl

---

# ![](image_rsrcAG7.jpg)

---

# ![](image_rsrcAG8.jpg)

2012年2月14日、「なぜJuliaを作ったのか」[*1](cCH.xhtml#a9KR)と題されたブログ記事[*2](cCH.xhtml#a9KS)とともにJuliaの最初のバージョン0.1が公開されました。創始者の4人[*3](cCH.xhtml#a9KT)が新しいプログラミング言語を開発した動機が綴られたその記事は、次の一文で始まります。

一言でいえば、私たちは欲張りだからだ[*4](cCH.xhtml#a9SK)。

この一文には、彼らが新しいプログラミング言語に込めた思いがよく反映されています。Juliaは数値計算でもテキスト処理でも、日々のちょっとした処理の自動化からスーパーコンピュータを使った大規模な計算まで、1つの言語で対応できるよう設計されています。それまでの計算環境では、人間と計算機の間にはいくつものプログラミング言語が挟まれていました。例えば、重い計算は実行性能に優れた C やFortranで記述してライブラリ化し、それを R やPythonのような手軽で扱いやすい動的言語から呼び出す使い方は、現在では一般的です。しかし、これでは開発者が1つのプロジェクトで複数の言語を扱う必要があり、ソフトウェアの複雑さが増してしまいます。利用者がソフトウェアを使うときにも、コンパイラや依存ライブラリの細かな違いによって、インストールすらうまくいかないことも少なくありません。

さらに深刻なのは、開発者と利用者が異なるプログラミング言語を使うことで生じる分断です。使う言語が違うことで、ライブラリの開発者は利用者が使うインタフェースの設計を疎かにし、利用者はインタフェースの背後で起きている計算の理解を諦めてしまいがちになります。Juliaの創始者たちが新しいプログラミング言語で解決しようとしているのは、まさにこうした問題です。

Juliaは、実行性能に優れ、対話的な実行も可能な処理系を使って、この問題に挑んでいます。Juliaの性能は入念に設計された言語仕様と最新のコンパイラ技術に支えられており、高速なコンパイル言語に匹敵する性能を発揮します。それでいて、コードのコンパイルは実行時に自動的に行われるので、Pythonや R などの動的言語のように対話的にも使えます。もちろん、スクリプトファイルを用意すれば、重いバッチ処理をコマンドラインから実行するのも簡単です。開発者は性能と利便性の両立のために複数の言語間を行ったり来たりする必要はありませんし、利用者はJuliaさえ知っていればインタフェースからコアの計算まで理解できます。人間と計算機、さらには開発者と利用者の間を継ぎ目なくつなぐのが、Juliaという言語なのです。

一方で、Juliaは既存のソフトウェアを捨てるべきと主張しているわけではありません。広く使われているライブラリには積み重ねてきた信頼と実績があり、それらをすべて書き直すことは非現実的です。すでにあるものをうまく利用し、つなぎ合わせるための機能をJuliaは備えています。実際、Juliaの標準ライブラリは、OpenBLAS[*5](cCH.xhtml#a9JT)やPCRE[*6](cCH.xhtml#a9JU)など、多数の高品質なライブラリ上に成り立っています。これらの標準ライブラリは、基盤となるライブラリの難しい部分をうまく隠しつつ、高レベルで実用的な機能を提供しています。さらに、他言語で書かれたライブラリをコンパイル済みのバイナリファイルとして配布できる優れたパッケージングシステムのおかげで、利用者がそれらを自分でコンパイルする必要はありません。いまあるものを活かしつつ、新しい価値を与えることもJuliaの重要な目的の1つです。

# 1.1 本書の目的と構成

本書はJuliaの知識を前提としません。Juliaは比較的新しい言語ですので、本書の執筆時点では日本語の資料や書籍はあまり多くありません。ですので、本書で初めてJuliaを学ぶ読者のために、基礎から実践まで幅広いトピックを取り扱っています。ただし、条件分岐・反復・関数などのプログラム一般の基本的概念には、ある程度馴染みがあることを想定しています。

Juliaに興味を持って本書を手にとった方は、きっと科学技術計算に役立つ新しいプログラミング言語としてJuliaに興味を持った方がほとんどでしょう。本書はそうした人たちに向けて書かれています。したがって、一般的なプログラミング言語の書籍より、やや科学技術寄りの内容を多く取り扱っています。具体的には、理数系の学生や、科学技術計算・データ解析などを専門とする技術者や研究者を主なターゲットとしています。

本書は大きく4つのパートに分かれます。本章を含む第1部は、Juliaの導入です。第2部で言語の基礎的な構文・型システム・機能を解説します。続く第3部は、標準ライブラリを中心としたライブラリの紹介です。最後の第4部は、パッケージ管理と開発・性能改善などの実践的な知識を扱います。

本章では、詳細には立ち入らず、Juliaがどのようなプログラミング言語であり、どのような利点があるのかを紹介します。特に、他のプログラミング言語と比較した共通点・相違点を見ていくことで、Juliaの特徴を浮き彫りにしていきます。まずは本章でJuliaを一巡り観光して、そのよさを納得してもらい、以降の章でJuliaの世界に移り住むために必要な知識を提供しようというわけです。

本章では実際にコードを書く必要はありませんが、第2章で説明するJuliaのインストールを先に済ませておけば、手元で動作を確認しながら読むこともできます。また、経験者にわかりやすいようにPython, R, MATLABなど他のプログラミング言語の用語を使った説明も一部ありますが、もし知らなくても本書の他の部分を読むのには差し支えありません。

# 1.2 プログラミング言語としてのJulia

まずは簡単なJuliaのコードを例として、構文と実行の仕組みをごく簡単に紹介します。Juliaは他のプログラミング言語を使ったことがある人にとっては親しみやすい言語ですので、ここでは基礎から概念を積み上げていくのではなく、例を使って直感的に説明していきます。

# 1.2.1 最初のコード例

まずはJuliaという言語がどのような見た目をしているかを知ってもらうために、例としてリスト1.1に配列の要素を昇順に整列(ソート)する insertionsort 関数のコードを載せました。このアルゴリズムは挿入ソート(insertion sort)として知られる基本的なアルゴリズムです。最初の例としては少し高度ですが、動作の詳細はさておき、まずはJuliaのコードを眺めてみてください。

![](image_rsrcAG9.jpg)

プログラミングの経験があれば、リスト1.1は説明なしでもかなり理解できるのではないでしょうか。1行目の # から始まる行はコメントです。2行目の function から13行目の end までが関数の定義です。end はコードのかたまり、すなわちブロック(block)の終了位置を表すキーワードです。4行目の for と6行目の while で始まるブロックも同じように end で終わります。関数本体では、最初に入力として渡された配列 xs をコピーし、4~11行目のfor文を使ったループでソートします。for文の内側にある6~9行目のwhile文では、i 番目の要素である x を挿入する正しい位置を探し、そこに x を10行目の xs[i] = x で挿入しています。ソートが完了した配列は、関数の最後(12行目)で呼出し元に返されます。

リスト1.1の例から、Juliaは他のプログラミング言語と構文的な共通点を多く持っていることがわかります。例えば for や while などのキーワードは多くのプログラミング言語と同じ意味を持ちますし、end でブロックの終わりを表すのもMATLABやRubyなどで見られます。プログラミング経験が少しあれば、Juliaの基礎構文は容易に習得できます。なお、本書のコードは読みやすさのためにインデントを行っていますが、Juliaではこれは必須ではありません。

# 1.2.2 特徴的な機能

今度はJuliaの機能に目を向けてみましょう。Juliaは次のような特徴を持つプログラミング言語です。

# 動的なプログラムの実行

大まかな分類をすれば、Juliaは動的言語(dynamic language)という種類のプログラミング言語になります。PythonやMATLABも同じ動的言語の仲間です。動的言語の処理系は、プログラムのコード全体をあらかじめ読み込んでから実行するのではなく、コードを順次読み込みながら実行できます。例えばJuliaの対話的実行環境では、ユーザが入力したコードをすぐさま実行し、計算結果を表示してくれます。簡単な四則演算はもちろん、関数を定義して呼び出すなど、Juliaの機能はすべてこの対話的実行環境でも使えます。

![](image_rsrcAGA.jpg)

# Unicodeのサポート

Juliaは標準でUnicode[*7](cCH.xhtml#aA99)をサポートしていますので、ASCIIの基本的な英数字や記号だけでなく、日本語の文字や数学記号も扱えます。例えば、次のように日本語のテキストを出力できたり、ギリシャ文字の変数や数学記号も使用できます。

![](image_rsrcAGB.jpg)

Juliaではギリシャ文字や数学記号を多用するので、多くの開発ツールは入力を補助する機能を持っています。例えば、上記の εは![](image_rsrcAGC.jpg)varepsilon の直後にTabキーを押下すると入力できます。

# 多重ディスパッチを使ったメソッドの選択

多重ディスパッチはJuliaの根幹をなす非常に重要な機能です。多重ディスパッチを一言で説明すれば、関数呼出しで実行される処理が引数の数や型によって動的に決まる機能です。

例を使って説明しましょう。Juliaには rand という乱数を生成する関数があります。この関数は rand() のように引数を与えずに呼び出すと0と1の間の数値を1つランダムに返しますが、rand(2, 3) のように2つの整数を渡して呼び出すと0と1の間のランダムな数値で埋まった行列を返します。さらに、rand([2, 3, 5]) のように配列を渡して呼び出すと、配列の要素を1つランダムに返します。

![](image_rsrcAGD.jpg)

これらはいずれも「乱数を生成して返す」という抽象的な振る舞いは共通していますが、その具体的な処理内容はまったく異なるので、別々の定義が必要になります。Juliaでは、関数の具体的な処理を引数の型に応じて個別に定義できます。つまり、1つの関数には特定の型に特化した具体的なメソッドがあり、どのメソッドが実行されるかは関数に渡した引数の型で決まるのです。この仕組みが多重ディスパッチです。多重ディスパッチの仕組みは、x + y や print(x) などのような基本的な関数を含む、ほとんどすべての関数で使われています。多重ディスパッチのおかげで、Juliaの機能拡張や、特定の型に特化した最適化を容易に行えます。

# マクロによるメタプログラミング

マクロは少し説明が難しい機能です。やや抽象的に説明すれば、マクロとはソースコードを本来の意味とは異なる意味に変換する機能です。具体例として、ある条件が満たされているかをチェックする @assert マクロの使用例を示します。@ 記号がマクロの呼出しを示す構文です。

![](image_rsrcAGE.jpg)

@assert マクロは与えられた条件式が満たされていれば何もしませんが、そうでなければこの例のように例外(エラー)を送出します。例外のメッセージをよく見ると、条件式![](image_rsrcAGF.jpg)自体がメッセージに含まれていることがわかります。これは、マクロが式の評価値ではなく、式自体を受けとっているからこそ実現できる機能です。評価値のみが渡される関数では、これと同じような機能は実現不可能です。マクロはこのような単純な用途以外にも、最適化のヒントや特殊な文字列リテラルの導入など、さまざまな場面で使われます。

# 1.2.3 実行時コンパイル

動的言語の対比として、静的言語(static language)と分類されるプログラミング言語もあります。代表的なのは C やFortranといった、プログラムを構成するソースコードをあらかじめ処理系に与え、コンパイルという処理により実行可能なファイル[*8](cCH.xhtml#a9XS)を生成してから実行する言語です。静的言語では、ソースコードはすべてコンパイル時に与えられ、動的言語のように実行時にソースコードを受けとって実行する機能はありません。一方で、コンパイル時にソースコードを詳細に解析して、問題点を実行前に見つけたり、より高速に実行できるよう最適化を行えたりします。

さて、Juliaは動的言語に分類されると先ほど述べましたが、実は静的言語のような高度なコンパイル機能を持っています[*9](cCH.xhtml#a9JA)。プログラムの実行時に必要に応じてコンパイルを行うので、このような仕組みを一般に JIT(just-in-time)[*10](cCH.xhtml#a9JB)コンパイルと呼びます。Juliaの処理系は高機能なJITコンパイラを内蔵しており、ソースコードが与えられると、それを逐次的にコンパイルしながら実行します。JITコンパイルのおかげで、Juliaは動的言語ながら静的言語に劣らない高速な計算が可能です。

# 1.3 科学技術計算環境としてのJulia

Juliaは汎用プログラミング言語であり、科学技術計算専用言語ではありませんが、科学技術計算で有用なように設計されてきたことは事実です。ここでいう科学技術計算とは、統計・機械学習・最適化・シミュレーションなどの数値や配列を中心とした計算のことを指しています。本節では、Juliaがどういった点で科学技術計算に向いているのかを紹介します。

# 1.3.1 高い実行性能

長時間実行されることが多い科学技術計算では、いうまでもなく実行性能が非常に重要です。Juliaは開発当初から効率的なJITコンパイルが可能なように設計されているので、科学技術計算の要求性能にも応えてくれます。例えば、Juliaは実行時に型推論という処理を行い、コード中の変数や関数の戻り値の型をあらかじめ推論します。すると、推論した型の情報を使って、他の動的言語では難しいレベルの強力な最適化が可能になります。

具体例として、簡単なマイクロベンチマークの結果を見てみましょう。ここでは、エイトクイーンパズル(eight queens puzzle)として知られる問題を3つの言語(Julia, C++, Python)で解き、計算時間を比較します。この問題は、チェス盤に8個のクイーンを互いに攻撃せず置く配置が何通りあるかを数え上げます。チェスのクイーンは縦・横・斜めに制限なく動けるので、同じ行・列・対角線上に複数のクイーンは置けません。図1.1に1つの解を示します。

![](image_rsrcAGG.jpg)

通常の8×8のチェス盤では、サイズが小さすぎて一瞬で解けてしまい、比較になりません。したがって、ここでは N×N に一般化したチェス盤に N 個のクイーンを置きます。Juliaの実装例をリスト1.2[*11](cCH.xhtml#a9TA)に示します[*12](cCH.xhtml#a9TB)。反復・分岐・関数呼出しなどをバランスよく含むので、ライブラリに依存しない処理系の性能がわかります。

![](image_rsrcAGH.jpg)

ベンチマークの結果を図1.2に示します[*13](cCH.xhtml#a9UE)。縦軸はチェス盤のサイズを表し、横軸の値が小さいほど実行時間が短く、性能がよいことを示しています。C++ではGCCの2つの最適化オプション -O1 と -O3 で計測しています。チェス盤のサイズ N が小さいとき、Juliaの実行時間はC++とPythonの間にあり、C++と比較すると遅いですが、サイズ N が大きくなるにつれてJuliaとC++の差は小さくなり、最終的にはほとんど差がなくなりました。すべてを同時に比較できる N=15 のとき、実行時間はJuliaが24.1秒、C++の -O1 と -O3 がそれぞれ28.7秒と25.4秒、Pythonが1923.5秒でした[*14](cCH.xhtml#a9UF)。Juliaは実行時にコンパイルを行うので、実行時間が短いときはコンパイルのオーバーヘッドが大きいですが、実行時間が長くなるにつれてコンパイルの相対的なコストは低減し、C++に近い性能が得られるようになります。

![](image_rsrcAGJ.jpg)

# 1.3.2 豊富な型と拡張性

科学技術計算にはさまざまな型が不可欠です。Juliaはこれらの基本的な型を豊富に提供しており、多様な問題に対応できます。例えば、整数については8ビットから128ビットまで5種類のサイズの整数型を符号付き・符号なしの両方で用意しています。これらに加えて任意精度の整数型も用意しているので、整数だけでも合計11種類の型があります[*15](cCH.xhtml#aAEC)。浮動小数点数についても半精度・単精度・倍精度の3種類と任意精度の浮動小数点数の型が利用可能です。さらに複素数や有理数など、特定の用途で必要な数値型も標準ライブラリで提供しています。多くのスクリプト言語が2~3種類程度しか数値を表現する型を持たないのと比べると、Juliaがいかに数値データを重視しているかがわかるでしょう。

基本的な数値型に加えて、配列の型も標準ライブラリで提供しています。この型は、1次元配列であるベクトルや2次元配列である行列を含む、任意の多次元の配列を表現できます。さらに、配列は前述した数値型を含む型をパラメータとしてとれるジェネリック(generic)な型なので、特定の型の要素に特化した配列、例えば単精度浮動小数点数の行列などを作るのも容易です。

既存の型で問題をうまく扱えないときには、新しい型を定義して機能を拡張するという選択肢もあります。実際、標準ライブラリの複素数を表現する Complex 型は、次のように定義されています。

![](image_rsrcAGK.jpg)

ここで注目してもらいたいのは、複素数のような基本的な数値の型もJuliaで定義されているということです。さらに、この複素数を使った演算も次のようにJuliaで定義されています[*16](cCH.xhtml#aAFA)。しかも、Juliaで定義したことによる性能上のペナルティはまったくありません。つまり、Pythonなどの動的言語では、このような基本的な型や演算は効率のため C などの低レベルなプログラミング言語で実装することが多いですが、JuliaではJuliaで書いても最高の性能が実現できます。

![](image_rsrcAGM.jpg)

# 1.3.3 Julia自身で書かれたライブラリ

Juliaを支える標準ライブラリの大半はJulia自身で記述されています。例を挙げれば、指数関数や辞書型の実装はすべてJuliaで記述されています。これは、Juliaの表現力と性能の高さを実証しています。また、外部パッケージもJuliaのみで記述されているものがほとんどです。よく設計されたパッケージは、Juliaで書かれていても、十分満足できる性能を示します。

ライブラリがJuliaで書かれている最大の利点は、Juliaさえわかってしまえば実装を確認するのも修正するのも心理的な負担が小さくなることです。ライブラリの利用者がちょっとした関数のアルゴリズムを確認しようと思っても、コアの計算が C やC++で書かれていれば、それを読み解くのは容易ではありません。一方、上から下までJuliaだけで書かれていれば、ソースコードの理解はずっとやりやすくなります。ソースコードを表示する @less というマクロがあるので、ソースコードを探しにいく必要すらありません。

![](image_rsrcAGN.jpg)

# 1.3.4 数値計算ライブラリ

Juliaの標準ライブラリには、配列の型だけでなく、その演算も豊富に用意されています。線形代数を例にとれば、行列 A とベクトル x の積 Ax は A * x だけで計算できますし、線形方程式 Ax=b の解 x は A ![](image_rsrcAGC.jpg) b だけで計算できます。リスト1.3には主成分分析(principal component analysis, PCA)による次元削減の実装例を示します。機能豊富な標準ライブラリのおかげで、コメントを除けば実質的に7行のコードで主成分分析が実装できています。

![](image_rsrcAGP.jpg)

こうした計算の裏側では、OpenBLASなどの実績のある高速なライブラリが使われています。背後にあるライブラリの使用方法を知らなくても、利用者は高レベルなインタフェースで計算を記述できるので、実装にかかる負担を大幅に削減できます。さらに、細かい制御ができる低レベルなインタフェースを使って計算を効率化する選択肢もあります。

これらのほかにも、Juliaは次のような既存の有名な数値計算ライブラリの機能を標準で提供しています。つまり、数学関数・乱数・行列分解・疎行列・高精度計算といった数値計算でよく使われるツールは、標準ライブラリでかなりカバーされているのです。

●OpenBLAS

内積や行列積などの計算ライブラリ

●OpenLibm

さまざまな数学関数を提供するライブラリlibmの実装

●dSFMT

高速な疑似乱数生成器

●LAPACK

線形方程式系や固有値問題のルーチン

●SuiteSparse

疎行列の線形代数ルーチン

●GMP

任意精度の整数計算

●MPFR

高精度な浮動小数点数の計算

# 1.3.5 パッケージ管理システム

Juliaの標準ライブラリにはPkg.jlというパッケージ管理ツールが付随しています。Pkg.jlはパッケージの追加・更新・削除といった基本的な機能に加えて、プロジェクトに固有のパッケージ環境を設定する機能を備えています。この機能を使えば、そのプロジェクトで使うパッケージのバージョンを完全に固定できるので、例えば開発用の環境をそのままサーバで再現したり、計算実験の再現性を確保したりするときに非常に役に立ちます。さらに、Pkg.jlはコンパイル済みの共有ライブラリを管理する機能も備えているので、外部ライブラリ[*17](cCH.xhtml#a9F5)に依存するJuliaのパッケージをインストールするとき、ほとんどの利用者は自前でその外部ライブラリをコンパイルする必要はありません。

# 1.4 Juliaの開発とコミュニティ

Juliaがいかに優れていても、自由に使えなければ意味がありません。また、プログラミング言語を選ぶうえで、そのコミュニティも重要な因子です。本節では、Juliaがどのように開発されていて、どのような人たちが使っているのかを紹介します。

# 1.4.1 オープンなライセンスと開発体制

ソフトウェアのような知的財産は、一定のライセンスのもとで使用が許可されます。Julia処理系のソースコードは、MITライセンスという寛容なオープンソースソフトウェアライセンスのもとで利用できます。MITライセンスのもとでは、利用の目的(商用・非商用など)を問わず無償でソフトウェアを使用でき、改変したソースコードを公開する義務もありません。この文化を継承し、第三者が公開するJuliaのパッケージでも、MITやBSDなど制約の少ないライセンスがよく用いられます。

ライセンスと同様、Julia自体の開発も非常にオープンであり、いつでも誰でも開発に参加できます。ソースコードはGitHubのレポジトリ[*18](cCH.xhtml#a9Z1)で公開されており、このレポジトリは開発の中心地でもあります。GitHubのアカウントさえあれば、バグなどの報告や修正パッチの提供などもできます。もちろん修正が取り入れられるかは内容次第ですが、開発の実績がないことを理由に拒否されることはありません。2022年5月現在までに1,200人以上のコントリビュータが5万回以上のコミットをしています。Juliaでは、多くの人が自由に開発に参加することで、品質と開発の透明性を向上させています。

開発の中心にいるのは最初のバージョンを公開した創始者らですが、それ以外にも大学の研究者など、多種多様な人々が開発に大きく関わっています。また、開発の中心メンバはJuliaHub[*19](cCH.xhtml#a9WM)という会社を立ち上げ、Juilaに関するコンサルティングや技術支援などのサービスを提供しています。ほかにも、NumFOCUS[*20](cCH.xhtml#a9WN)というオープンソース・オープンサイエンスを支援する非営利団体が2014年から継続的にJuliaプロジェクトを支援しています。

# 1.4.2 豊富な科学技術計算用のパッケージ

Juliaには科学技術計算に使えるパッケージが豊富にあります。7,000個以上あるパッケージはJuliaHub[*21](cCH.xhtml#a9XP)で一覧できます。このパッケージリストを眺めてみると、科学技術計算用パッケージの多さに驚かされるでしょう。その中でも、例えば微分方程式によるシミュレーションを行うDifferentialEquations.jlや、最適化のモデリングができるJuMP.jlはとりわけ人気のパッケージです。

対話的な実行環境として、標準機能であるREPL以外にも、IJulia.jlというブラウザ上での対話的な実行を可能にするパッケージもあります。これはJupyterというPythonや R などの対話的な実行開発環境をブラウザ上に構築するプロジェクトのJuliaインタフェースです。最近では、同様の試みとしてPluto.jlというツールの開発も活発化しています。Pluto.jlはリアクティブプログラミング(reactive programming)という、変数や関数の変更を自動的に検知して結果に反映するプログラミングパラダイムを採用しています。結果を確認しながら対話的にプログラミングできるこれらのツールは、データ解析や教育などの分野で広く活用されています。

# 1.4.3 Juliaを支えるコミュニティ

Juliaの開発と同様、疑問の解決やパッケージなどの開発も、その多くをJuliaユーザのコミュニティが支えています。Juliaについて技術的な質問があれば、Discourseのフォーラム[*22](cCH.xhtml#aAD7)が役に立ちます。Juliaの開発者たちも頻繁に現れますので、ここで聞けばたいていの疑問は解決します。新しいパッケージのアナウンスや興味深い議論も展開されるので、時々のぞいてみるとよいでしょう。また、Juliaの公式サイト[*23](cCH.xhtml#aAD8)に主要なコミュニティの紹介がありますので、気になる方は確認してみてください。

Juliaのパッケージには専門のコミュニティが開発・保守しているものも多くあります。例えば、JuliaMath[*24](cCH.xhtml#a9AB)という数学関係専門のコミュニティでは、素数の計算に関するPrimes.jlパッケージや、特殊関数を提供するSpecialFunctions.jlパッケージなどが有志によって開発されています。ほかにも、統計・機械学習・最適化・生物学などを専門にしているコミュニティがあり、それぞれその分野での重要なパッケージを開発しています。

まとめ

本章では、Juliaの創始者たちがJuliaを開発するにいたった動機とその特徴を紹介しました。科学技術計算のコミュニティがJuliaに惹きつけられている理由を少し理解できたのではないかと思います。Juliaは巨大な組織が資本を投下して開発した言語ではなく、既存のソフトウェアに満足しない研究者や技術者たちが自分たちの思いを具現化するために作った言語です。このようなコミュニティでは、開発者と利用者の垣根はいっそう低くなります。本章の説明ではまだ納得しきれない方でも、これからJuliaを実際に使ってみれば、非常に優れた言語であることを理解してもらえるはずです。

注

[*1](cCH.xhtml#aA02) 原文:Why We Created Julia.[*2](cCH.xhtml#a9HU) <https://julialang.org/blog/2012/02/why-we-created-julia>[*3](cCH.xhtml#a9BW) Jeff Bezanson, Stefan Karpinski, Viral Shah, Alan Edelman[*4](cCH.xhtml#a9MH) 原文:In short, because we are greedy.[*5](cCH.xhtml#aABS) 行列積などを計算する高速な数値計算ライブラリです。[*6](cCH.xhtml#aAE3) 文字列のマッチングに使う正規表現ライブラリです。[*7](cCH.xhtml#a9PV) 世界中の文字を計算機で扱う方法を定めた標準的な規格です。「ユニコード」と読みます。[*8](cCH.xhtml#a9YG) コンパイラが生成するファイルには人間が書いたソースコードは通常含まれておらず、代わりにそのプログラムを実行する計算機に向けた命令列(マシンコード)が含まれています。[*9](cCH.xhtml#a9M4) より正確にいえば、言語の特徴というよりその処理系の特徴です。現在のJuliaは実質的に1つしか実装がないので、ここでは言語と処理系の実装を同一視しています。[*10](cCH.xhtml#aA82) 元々は製造業で使われていた用語のようです。JITは「ジット」と読みます。[*11](cCH.xhtml#aABZ) より高速なアルゴリズムもあると思いますが、処理系の比較が目的なので、アルゴリズムが言語間で共通していれば十分です。[*12](cCH.xhtml#a9K1) 変数名に使っているfileはチェス用語で縦の列、rankは横の行を意味します。[*13](cCH.xhtml#aA9Y) 計測には次の処理系を使用しました。Julia 1.7.2, Python 3.10.4, GCC 11.3.1[*14](cCH.xhtml#a9NX) Pythonの N=16 は実行に時間がかかりすぎたため計測していません。[*15](cCH.xhtml#a9MR) 真偽値も1ビットの整数のように振る舞うので、これも加えれば整数型は12種類になります。[*16](cCH.xhtml#a9JC) real と imag はそれぞれ複素数の実部と虚部を取得する関数です。[*17](cCH.xhtml#aAG2) ここでは C やFortranなどで書かれたJuliaの標準ライブラリに含まれていないものを指しています。[*18](cCH.xhtml#aA0W) <https://github.com/JuliaLang/julia>[*19](cCH.xhtml#aA4S) <https://juliahub.com/>[*20](cCH.xhtml#a9CA) <https://www.numfocus.org/>[*21](cCH.xhtml#aA5D) <https://juliahub.com/ui/Home>[*22](cCH.xhtml#a9Y6) <https://discourse.julialang.org/>[*23](cCH.xhtml#a9FT) <https://julialang.org/community/>[*24](cCH.xhtml#a9RS) <https://github.com/JuliaMath>

---

# ![](image_rsrcAGR.jpg)

動的言語であるJuliaは、さまざまな環境で開発・実行できます。標準的なターミナルでの開発はもちろん、ウェブブラウザ上でコードを編集・実行するJupyterや、テキストエディタと実行環境が一体になった統合開発環境のようなソフトウェアもいくつかあります。

本章では、以降の章を読むにあたって必要な開発実行環境の構築を説明します。紹介するすべての環境を構築する必要はありませんが、最初に紹介するJulia本体のインストールとターミナルでの実行は最低限必要です。テキストエディタの設定やその他の開発環境の構築は、必要に応じて整えれば問題ありませんので、関係ない部分は読み飛ばしてしまっても差し支えありません。

# 2.1 サンプルコード

本書のサンプルコードはサポートページ(<https://bicycle1885.org/julia-taizen/>)で配布されています。特に言及のない限り、サンプルコードはすべてパブリックドメインで自由に利用可能です。

# 2.2 Juliaのインストール

JuliaはWindows, macOS, Linuxの3種類のオペレーティングシステム(operating system, OS)を公式にサポートしています。ほとんどのユーザはこれらのいずれかを利用しているでしょうから、本節ではそのインストールの方法を説明します。

# 2.2.1 Juliaのライセンス

Juliaの本体であるコンパイラは、MITライセンスのもとで制限なく使用・複製・改変・配布などをする権利が認められています。MITは寛容的なオープンソースライセンスですから、ほとんどの人はライセンスの内容に同意できるでしょう。また、Juliaとともに使用されるライブラリについても、同様の権利がそれぞれのオープンソースライセンスのもとで認められています。つまり、商用・非商用などの用途を問わず、ユーザは無料でJuliaを使用・配布できます。しかし、こうしたJuliaの利用に関しては無保証であり、生じる結果についてJuliaの開発者(著作権者)は一切の責任を負わないことに注意してください。Juliaのライセンスについては、ライセンスファイル[*1](cK9.xhtml#a9NU)に詳細が記載されていますので参照してください。

# 2.2.2 Juliaupを使ったインストール

Juliaup[*2](cK9.xhtml#a9YK)はJulia公式の管理ツールです。Juliaupを使うとコマンドを1つ実行するだけでJuliaのインストールが完了します。最新版への更新や複数バージョン切替えも簡単にできるので、Juliaupの使用を推奨します。

Juliaupをインストールするには、ターミナルで次のコマンドを実行します。表示されるガイドに従いデフォルト設定でインストールすれば、最新版のJuliaがインストールされ、julia コマンドの実行に必要な PATH 環境変数の設定[*3](cK9.xhtml#a9G7)も完了します。

![](image_rsrcAGS.jpg)

Juliaupで特定のバージョンをインストールするには、juliaup add 1.6 のように juliaup add コマンドを実行します。続けて juliaup default 1.6 を実行すると、julia コマンドがそのバージョンのJuliaになります。現在の状態は、次のように juliaup status コマンドで確認できます。

![](image_rsrcAGT.jpg)

juliaup コマンドの他の機能については、juliaup help コマンドの表示やJuliaupのドキュメントを参照してください。

# 2.2.3 Juliaのダウンロード

Juliaupを使わずインストールするには、公式が配布しているコンパイル済みのバイナリファイルを手動でダウンロードする必要があります。ここではJulia 1.6.6を使ってダウンロードとインストールの仕方を説明しますが、バージョンは適宜読み替えてください。特別な事情がない限り、本書を読んでいる時点での最新版を推奨します。Juliaのバイナリファイルは、Juliaの公式サイト(<https://julialang.org/>)にあるダウンロードページ[*4](cK9.xhtml#a9B3)からダウンロードできます。ダウンロードページのスクリーンショットを図2.1に示します。

![](image_rsrcAGU.jpg)

macOSを使用している場合、Homebrewというパッケージ管理ツールを使用して公式バイナリをダウンロード・インストールする手順もあります。Homebrewを使ってJuliaをインストールしたい場合には、ここでJuliaのバイナリをダウンロードする必要はありませんので、ダウンロード手順を飛ばして直接macOSでのインストール手順に移ってください。

図2.1に示すように、Current stable releaseという表に書かれているのが、現在入手できる最新の安定版です。使用しているプラットフォームに合わせて、適切なものを選択し、バイナリをダウンロードしてください。例えば、64ビット版のWindowsを使用している場合は、64-bitと書かれたリンクからWindows向けバイナリをダウンロードできます。ソースコードからビルド・インストールする場合は、Tarball with dependenciesをダウンロードするのがよいでしょう。

同じダウンロードページでは、比較的長期間にわたってサポートされるLTS(long-term support)版も提供されています。執筆時点でのLTS版はJulia 1.6であり、本書の説明はJulia 1.6を基本としていますが、Julia 1.7や1.8で加えられた重要な機能にも言及しています。LTS版に最新の機能は追加されませんが、不具合の修正は随時行われています[*5](cK9.xhtml#a9TZ)。

Juliaのダウンロードが完了したら、Juliaのインストールを行い、ターミナルから julia コマンドが使えるように設定します。続く各OS向けのインストール手順に移ってください。なお、各OS向けのより詳しい説明が公式サイト[*6](cK9.xhtml#a9P5)にもあります。

# 2.2.4 Windowsでのインストール

インストール手順はWindows 10を想定しています。Windowsでは、コマンドプロンプトではなく、Windows Terminal[*7](cK9.xhtml#aABE)などの高機能なターミナルを使うことが強く推奨されます。手元にない場合は、事前にMicrosoft Storeを通じてWindows Terminalをインストールしておくとよいでしょう。

まずはダウンロードしたインストーラのファイルをダブルクリックし、ウィンドウに表示されるインストール手順に従ってください。このとき、インストール先のパスを記録しておいてください。なお、インストーラの最後の段階では、パスに julia コマンドを追加するか選択するチェックボックスが表示されます。ここにチェックを入れた場合はパスを手動で設定する必要はありません。

Juliaを更新するときなどのために、パスを手動で設定する方法を紹介しておきましょう。ターミナルを開き、次のコマンドを入力すると、環境変数を編集するウィンドウが開きます。

![](image_rsrcAGV.jpg)

開いたウィンドウのユーザの環境変数から PATH を選択し、インストール時に記録したパスをディレクトリ名の bin まで追記します。例えば、ユーザ名が Julius だとすると、典型的なパスは C:![](image_rsrcAGC.jpg)Users![](image_rsrcAGC.jpg)Julius![](image_rsrcAGC.jpg)AppData![](image_rsrcAGC.jpg)Local![](image_rsrcAGC.jpg)Programs![](image_rsrcAGC.jpg)Julia-1.6.6![](image_rsrcAGC.jpg)bin のようになるはずです。

# 2.2.5 macOSでのインストール

macOSでインストールするには2つの方法があります。1つは、2.2.3項でダウンロードした.dmgファイルからインストールする方法です。もう1つは、Homebrewを使ってコマンドラインからダウンロード・インストールを行う方法です。どちらでもmacOS向け公式バイナリをインストールすることになりますので、Juliaの機能に違いはありません。

まずは.dmgファイルからインストールする方法を説明します。ダウンロードした.dmgファイルを開くと、図2.2のようなウィンドウが開きます。ここでJuliaのアイコンをApplicationsと書かれたディレクトリアイコンの上にドラッグ・ドロップすると、Juliaがインストールされます。

![](image_rsrcAGW.jpg)

デフォルトでインストールされる場所は、普段ターミナルを使ってプログラムを使用している人には便利な場所とはいえませんので、Juliaを julia コマンドを打つだけで実行できるようにしましょう。これは普段やっている方法で構いませんが、例えば次のように ~/.local/bin や /usr/local/bin などすでに PATH 環境変数にある場所に、インストールされた julia コマンドへのシンボリックリンクを張ります。

![](image_rsrcAGX.jpg)

次にHomebrewを使ったインストール方法を説明します。Homebrew自体はインストール済みであることを前提としますので、あらかじめHomebrewのインストール手順[*8](cK9.xhtml#aA2E)に従ってインストールを済ませておいてください。JuliaはHomebrew Caskというバイナリ形式で配布されています。インストール作業はとても簡単で、ターミナルで

![](image_rsrcAGY.jpg)

を実行すれば、最新版のダウンロードとインストールを一度に行ってくれます。

# 2.2.6 Linuxでのインストール

LinuxのバイナリはTAR形式で配布されています。ダウンロードしたTARファイルをどこか安定的なディレクトリに置き、次のように tar コマンドを使って展開してください。

![](image_rsrcAGZ.jpg)

すると julia-1.6.6 というディレクトリができ、その中の bin ディレクトリに julia コマンドが置かれています。後はmacOSの場合と同じように、PATH 環境変数にあるディレクトリに julia コマンドへのシンボリックリンクを張るか、julia-1.6.6/bin/ を PATH 環境変数に加えます。

# 2.2.7 ソースコードからのインストール

ソースコードからのインストールはそれほど難しくありませんが、Juliaのコンパイルには結構な時間がかかります。また、環境の違いなどが原因でコンパイル中に問題が発生することもありますので、可能ならバイナリをダウンロードしてインストールすることをおすすめします。それでもソースコードからインストールする必要がある場合には、TAR形式のソースコードをダウンロードした後に、以下のコマンドでJuliaのビルドとインストールが可能です。

![](image_rsrcAH0.jpg)

インストール先のディレクトリなどをデフォルトの設定から変更するときは、展開したディレクトリにある README.md ファイルの指示に従って Make.user ファイルを編集してください。

# 2.2.8 インストールの確認

Juliaが正しくインストールされているかを確認するには、ターミナルを起動して julia --version コマンドを実行します。パスの設定を変更した後にターミナルの再起動が必要な場合もありますので注意してください。次のようにインストールしたJuliaのバージョンが表示されれば、インストールは完了しています。

![](image_rsrcAH1.jpg)

# 2.3 対話的実行環境

Juliaのインストールが終わったら、実際に実行して動作することを確認しましょう。最初に紹介する対話的実行環境は、Juliaの基本を学ぶには最適な環境です。また、プログラムの開発中にも頻繁に使うので、対話的実行環境の使い方は必ず覚えてください。

# 2.3.1 対話的実行環境の基本操作

まずはJuliaの対話的実行環境でのコードの実行方法を説明します。対話的実行環境では、ユーザが入力したJuliaのコードを処理系が読み込み、すぐさまそのコードを評価して結果を出力します。読込み(read)・評価(eval)・出力(print)という一連の動作を繰り返すループ(loop)なので、それぞれの頭文字をとったREPL[*9](cK9.xhtml#aAB3)という略称がよく使われます。本書でもこのように呼ぶことにします。

REPLを起動するには、ターミナルで julia コマンドを実行します。すると、図2.3に示すようにJuliaのロゴやバージョンなどの情報が出力されます。

![](image_rsrcAH2.jpg)

いまターミナルは、julia> の後にJuliaのコードが入力できる状態になっています。これをプロンプト(prompt)といいます。プロンプトにコードを入力した後にキーボードのEnterキーを押すと、コードがJuliaの処理系に読み込まれて実行され、計算結果が表示されます。例として、sin(0.1) / 0.1 とプロンプトに入力し、Enterキーを押してみましょう。次のようにREPLは数式 sin(0.1)/0.1 の計算結果を表示し、次のプロンプトを表示します。

![](image_rsrcAH3.jpg)

次はプロンプトに ans とだけ入力し、Enterキーを押してみましょう。

![](image_rsrcAH4.jpg)

直前に実行した sin(0.1) / 0.1 の計算結果が再び表示されました。ans は直前の計算結果を保持する特殊な名前で、REPLの中でだけ使えます[*10](cK9.xhtml#a9DD)。こうした名前を変数と呼びます。

次のようにコードの最後にセミコロン(;)を置くと、計算結果は表示されなくなります。計算結果の出力を抑えたいときは、この方法を使うとよいでしょう。

![](image_rsrcAH5.jpg)

REPLを終了するには、プロンプトにコードが入力されていない状態でキーボードのCtrlキーと D キーを同時押し(Ctrl+D)するか、exit() を実行します。exit 関数はJuliaの実行を停止する関数です。REPLを終了すると、元のシェルに戻ってきます。

なお、REPLにJuliaのサンプルコードなどをコピーアンドペーストするとき、julia> のプロンプトが付いたままでも構いません。REPLは先頭のプロンプトを検出して自動的に削除します。

# 2.3.2 補完と履歴

REPLには名前の一部から候補を表示する機能が付いています。例えば、プロンプトに si と途中まで入力した段階でTabキーを2回連続で押すと、次のように sign や sin など、si から始まる名前が一覧表示されます。候補が1つしかないときは、一度Tabキーを押した時点で名前の残りが補完されます。この機能は補完(completion)と呼ばれます。なお、←以降は説明なので、実際には入力しません。

![](image_rsrcAH6.jpg)

関数名と開き括弧、例えば sign( の直後に同じくTabキーを押すと、その関数のメソッド一覧が表示されます。メソッドについては[第9章](c434.xhtml)で説明しますが、この機能は関数の機能を調べるときに重宝します。なお、本書では表示やコードの一部を省略するとき、次のように三点リーダ(...)を使います。

![](image_rsrcAH7.jpg)

REPLでJuliaを試していると、以前実行したコードをもう一度実行したくなることもあるでしょう。REPLはコードの実行履歴を保存し、再度実行する機能を提供しています。これは履歴(history)の補完と呼ばれます。例えばプロンプトに何も入力されていない状態でCtrl+Pキー(もしくは↑キー)を押すと、直前に実行したコードがプロンプトに表示されます。そこでEnterキーを押すと、そのコードを再実行できます。

![](image_rsrcAH8.jpg)

Ctrl+Pキーは押した数だけ過去にさかのぼれます。押しすぎて行きすぎてしまったときは、Ctrl+Nキー(もしくは↓キー)を押せばCtrl+Pとは逆方向に、つまり最近入力したコマンドのほうに動きます。履歴はファイルに保存されるので、REPLを再起動した後でも履歴をたどれます。

コードが途中まで入力されている状態でCtrl+Pキーを押すと、前方一致するコードを履歴から検索します。少し前のコードを再度実行したいときには、Ctrl+Pキーを何度も押さなくて済むので便利です。前方一致ではなく、部分一致するコードを検索したいときはCtrl+Rを使います。Ctrl+Rを押下すると、REPLが逆方向検索モードに移行し、過去の履歴から入力に部分一致するコードを探します。Ctrl+PとCtrl+Nで履歴を前後に移動できるのは履歴の補完のときと同様です。

プロンプトに一度入力したコードを編集するときは、Ctrl+B(もしくは←キー)でカーソル位置を左に、Ctrl+F(もしくは→キー)で右に移動できます。Backspace(もしくはCtrl+H)を押下すれば1文字削除できます。練習として、履歴から sin(0.1) / 0.1 を補完し、次のように編集してみましょう。

![](image_rsrcAH9.jpg)

REPLで使える便利なキーバインディングを表2.1にまとめます。表2.1では特に使用頻度が高いもののみを紹介していますが、REPLで使えるキーバインディングの完全なリストは公式ドキュメント[*11](cK9.xhtml#a9K9)にあります。

![](image_rsrcAHA.jpg)

# 2.3.3 ![](image_rsrcAHB.jpg)記法による文字の入力

Juliaではギリシャ文字や数学記号などの文字もソースコードで頻繁に使います。それらの文字を入力するときに便利な![](image_rsrcAG6.jpg)記法[*12](cK9.xhtml#aA8Z) をここで簡単に紹介しましょう。

プロンプトで![](image_rsrcAGC.jpg)pi と入力した直後にTabキーを押すと、![](image_rsrcAGC.jpg)pi がπというギリシャ文字に変換されます。ちなみに、πは円周率を表す定数としてあらかじめ定義されています。

![](image_rsrcAHC.jpg)

ギリシャ文字のほかにも、![](image_rsrcAGC.jpg)in を ∈ に変換したり、![](image_rsrcAGC.jpg)sqrt を✓に変換したりできます。これらの記法は![](image_rsrcAG6.jpg)から受け継いだ記法なので、すでに![](image_rsrcAG6.jpg)に慣れている読者にとっては馴染みのあるものでしょう。変換できる文字のリストは非常に長いので、一覧表は公式ドキュメント[*13](cK9.xhtml#aA40)に譲ります。また、後述するヘルプモードでも![](image_rsrcAG6.jpg)の入力方法を調べられます。

# 2.3.4 ヘルプモード

プログラミング中に ans や sin の機能を忘れてしまったときはどうすればよいでしょうか。インターネットで検索したり、公式ドキュメントのウェブページを探したりするのも1つの方法ですが、もっと簡便な方法があります。

julia> プロンプトに何も入力されていない状態で ? キーを押すと、プロンプトが緑色の julia> から黄色の help?> に変わります。これはヘルプモード(help mode)と呼ばれ、コードの実行はせず、関数名などを入力してそのドキュメントを参照するときに使います[*14](cK9.xhtml#a9CT)。例えば、? キーを押して help?> プロンプトにした後、ans と入力してEnterキーを押すと、ans の説明が出力されます。

![](image_rsrcAHD.jpg)

ヘルプモードから元のJuliaモードに戻すには、何も入力されていない状態でBackspaceキーを押下してください。プロンプトが help?> から julia> に戻ります。

ヘルプモードにはドキュメントをキーワード検索する機能もあります。プロンプトにキーワードを入力するとき、ダブルクォーテーション(")でキーワードを囲むと、ドキュメント全体からそのキーワードの検索をします。例えば次のように "cube" というキーワードを入力して検索すると、三乗根(cube root)を計算する cbrt 関数がヒットします。

![](image_rsrcAHE.jpg)

![](image_rsrcAG6.jpg)記法の入力方法を調べるのにもヘルプモードが使えます。例えば、![](image_rsrcAHF.jpg)を入力する方法を調べるには、次のようにヘルプモードに![](image_rsrcAHF.jpg)と入力します。すると、これがREPLでは x![](image_rsrcAGC.jpg)hat の後にTabキーで入力できることがわかります。

![](image_rsrcAHG.jpg)

# 2.3.5 パッケージ管理モード

Juliaのパッケージ管理もREPLで行います。REPLのプロンプトで ] キーを押すと、プロンプトが julia> から pkg> に変化します。この状態をパッケージ管理モード(package management mode)と呼び、プロンプトにコマンドを入力してパッケージ管理を行えます。例えば、Example.jlというパッケージ[*15](cK9.xhtml#aA2R)をインストールするには、次のように add コマンドを使います。

![](image_rsrcAHH.jpg)

パッケージ管理モードのコマンドには、ほかにもパッケージを更新する update コマンド・削除する remove コマンド・状態を表示する status コマンドなどがあります。詳しくは[第16章](c6XJ.xhtml)で扱います。元のJuliaモードに戻す方法はヘルプモードと同様です。

# 2.3.6 コマンドモード

REPLの使用中にちょっとしたコマンドを実行したいとき、; キーでREPLをコマンドモードに変えられます。プロンプトは赤色の shell> に変化し、コマンドを実行できるようになります。

![](image_rsrcAHJ.jpg)

Bashなど本物のシェルと比べると、REPLのコマンドモードはかなり機能が制限されるので注意してください。元のJuliaモードに戻す方法はヘルプモードと同様です。

# 2.4 スクリプトファイルの実行

REPLでの実行は、簡単な計算をしたり短いコードの動作を確認したりするときには大変便利ですが、コードの再実行や編集がしにくいので、大きなプログラムの開発には向きません。また、REPLでの実行は個人での利用を前提としているので、他の人とコードを共有することもできません。本節では、ファイルに保存されたコードを実行する方法と、それをREPLに読み込む方法を解説します。

# 2.4.1 スクリプトファイルの形式

Juliaのソースコードを収めたファイルを本書ではスクリプトファイル(script file)や単にスクリプトと呼びます。Juliaではスクリプトの拡張子に .jl を使うのが慣習です。それ以外の拡張子にしてもJuliaが処理するうえでは問題ありませんが、エディタがJuliaで書かれたソースコードだと認識できない可能性もあるので .jl を使いましょう。

Juliaのスクリプトは、必ずUTF-8でエンコーディングされたテキストファイルにしてください。それ以外のエンコーディング、例えばShift-JISやEUC-JPには対応していません。UTF-8は現在最もよく使われるエンコーディングの1つですが、環境によってはデフォルトのエンコーディングがUTF-8になっていないこともあるので、エディタの設定を確認してください。

# 2.4.2 ターミナルからの実行

英語と日本語でメッセージを出力する簡単なプログラムのソースコードをリスト2.1に示します。#! から始まる1行目のコメントは特にシバン(shebang)と呼ばれます[*16](cK9.xhtml#aA6A)。シバンは # から始まるので、Julia処理系にはコメントとして無視されます。まずはテキストエディタを使ってこのコードを書き、hello.jl という名前のスクリプトとして保存しましょう。

![](image_rsrcAHK.jpg)

ファイルに保存されたコードを実行するには、REPLと同じ julia コマンドを使います。julia コマンドにスクリプトへのパスを渡せば、ファイルに保存されたプログラムが実行されて次のようにメッセージが表示されます。

![](image_rsrcAHM.jpg)

シバンを使えば、次のように julia コマンドを直接使わなくても実行できるので、ユーザはスクリプトの中身がどの言語で書かれているかを気にする必要はありません。ただし、この方法はスクリプトの実行権限が必要なことに注意してください。

![](image_rsrcAHN.jpg)

シバンにオプションを渡せば、プログラムの動作を制御できます。例えば、次のように env コマンドに -S を渡し、julia コマンドに \--threads 4 を渡せば、プログラムを4スレッドで実行できます[*17](cK9.xhtml#a9SP)。こうすると、プログラムを実行するたびにオプションを渡す面倒がないので、覚えておいて損はないテクニックです。

![](image_rsrcAHP.jpg)

スクリプトにコマンドラインから引数を渡すときは、ARGS 変数を使用します。ARGS は配列というデータ構造になっており、1番目の引数は ARGS[1]、2番目の引数は ARGS[2] などのように参照できます。リスト2.2のスクリプトの実行例を次に示します。

![](image_rsrcAHR.jpg)

![](image_rsrcAHS.jpg)

# 2.4.3 REPLへの読込み

スクリプトを実行してからREPLを起動すると、スクリプト内の変数や関数などをREPLに取り込めるので便利です。次の例では、julia コマンドの実行時に \--load オプション(短縮形は -L)を使ってスクリプトを実行してからREPLを起動しています。Juliaのロゴの表示を抑えるために、\--quiet オプション(短縮形は -q)も同時に渡していることに注意してください。

![](image_rsrcAHT.jpg)

REPLを立ち上げた後でスクリプトを読み込む方法もあります。次の例で使われている include 関数は、スクリプトのパスを文字列として受けとると、その中身を実行します。

![](image_rsrcAHU.jpg)

\--load オプションはJuliaの起動時にしか使えませんでしたが、include 関数はREPLを立ち上げた後でも呼び出せるので、より柔軟性があります。エディタでJuliaのスクリプトを編集し、REPLでそれを読み込み、動作を確認して気に入らないところがあればエディタでスクリプトを修正するという繰り返しは、Juliaのような動的言語では一般的な開発スタイルです。プログラムのプロトタイプを作成する段階では非常に有効な開発方法でしょう。なお、include 関数は、REPLでファイルを読み込む用途以外にも、大きなプログラムを複数のファイルに分割して管理するときなどにも使用します。

# 2.4.4 コマンドライン引数の実行

コマンドライン引数としてソースコードを直接実行するには、次のように \--eval オプション(短縮形は -e)を使います。

![](image_rsrcAHV.jpg)

\--eval オプションの代わりに \--print オプション(短縮形は -E)を用いると、println 関数を使わなくても結果を出力できます。

![](image_rsrcAHW.jpg)

# 2.5 Juliaの開発環境

Juliaでプログラムを開発するには、最低限テキストエディタとJuliaの処理系の2つがあれば十分ですが、Visual Studio CodeやJupyterLabを活用すれば、より開発がしやすくなります。本節では、これらのツールでどのようなことができるかを簡単に紹介するに留めますので、具体的な使い方はそれぞれのドキュメントやチュートリアルなどを参照してください。

# 2.5.1 Visual Studio Code

Visual Studio Code(VS Code)はMicrosoft社が開発している統合開発環境[*18](cK9.xhtml#aACD)です。Windows, macOS, Linuxがサポートされているクロスプラットフォームな開発環境であり、商用・非商用ともに無料で使えます[*19](cK9.xhtml#aACE)。VS Codeは公式サイト[*20](cK9.xhtml#aACF)で配布されています。インストール方法は本書では割愛しますので、公式サイトを参照してください。

VS CodeにはJulia用の拡張[*21](cK9.xhtml#aAC8)があります。この拡張はVS Code内のマーケットプレイスからインストールできます。拡張をインストールすると、Juliaのシンタックスハイライトが有効化されてコードが読みやすくなるだけでなく、REPLのような![](image_rsrcAG6.jpg)による入力補助機能も追加されるので、Juliaのソースコードにギリシャ文字や数学記号などを入力しやすくなります。

拡張の機能で特に便利なのが、REPLとの連携機能です。図2.4にVS CodeのREPLとの連携機能を使用している様子を示しています。VS Codeのウィンドウ下部にあるREPLには、すぐ上のエディタに表示しているソースコードを読み込んでおり、ソースコードを編集しながら関数などの動作をREPLですぐに確かめられます。また、コードの実行結果をインラインで表示する機能も提供しています。

![](image_rsrcAHX.jpg)

VS CodeのJulia拡張には、これらのほかにもデバッガとの連携やドキュメントの表示などさまざまな開発支援機能があり、現在も活発に開発が続けられています。使用するツールに強いこだわりがなければ、Juliaの学習や開発にはVS Codeを使うのがよいでしょう。

# 2.5.2 JupyterLab

JupyterLabは、ウェブブラウザを通じてJuliaやPythonなどを対話的に実行できる環境です。普通のテキストベースのターミナルとは異なり、ウェブブラウザの技術を活用して表や画像の表示など、より高機能なインタフェースを提供しています。JupyterLabはノートブックと呼ばれるファイルにコードとその実行結果を保存します。コードのみならず結果もセットで保持されるので、データ解析や教育用途で特に有用です。具体的にどのようなことができるかはnbviewer[*22](cK9.xhtml#aACM)を見るのが参考になるでしょう。

JupyterLabはPythonのパッケージとして配布されているので、pipやCondaなど、Pythonの標準的なパッケージ管理ツールがあれば簡単にインストールできます。詳細はドキュメント[*23](cK9.xhtml#a9DU)に譲りますが、次のいずれかのコマンドでインストールできます[*24](cK9.xhtml#a9DV)。

![](image_rsrcAHY.jpg)

JuliaをJupyterLabで使うには、JupyterLabに加えてIJulia.jlというパッケージが必要です。IJulia.jlは次のようにJuliaのパッケージ管理ツールを使ってインストールできます。IJulia.jlのインストールはREPLのパッケージ管理モードで次のコマンドを実行すれば完了です。

![](image_rsrcAHZ.jpg)

JupyterLabとIJulia.jlをインストールしたら、ターミナルから jupyter lab コマンドを実行するとJupyterLabのサーバが起動します。何もオプションを付けないで実行すると、ウェブブラウザが立ち上がって、図2.5のような画面が表示されるでしょう。この画面で実行したい言語のアイコンをクリックすると、そのカーネルが起動し、ウェブブラウザを通して実行できるようになります。また、画面の左に表示されている区画には、現在のディレクトリにあるファイルが一覧表示されます。ここに保存したノートブックがあれば、それをクリックして編集・実行できます。

![](image_rsrcAJ0.jpg)

# 2.5.3 テキストエディタのプラグイン

VS CodeとJupyterLabのほかにも、Vim, Emacs, Sublime Textなどのテキストエディタ用プラグインが有志[*25](cK9.xhtml#aA7E)によって開発されています。これらのプラグインは、JuliaのシンタックスハイライトやREPLにあるような![](image_rsrcAG6.jpg)による入力補助機能を持っています。例えば、Vim用のプラグインをインストールすると、インサートモードで![](image_rsrcAGC.jpg)sigma に続けてTabキーを押すと σ に変換されます。プラグインを使わなくてもJuliaのコードは書けますが、プラグインを使えばより快適に開発できるでしょう。

まとめ

本章では、Juliaの実行環境と開発環境のセットアップを解説し、REPLの使い方を紹介しました。開発環境の設定は凝り始めると終わりがありませんが、本書を読むうえではJuliaの実行と簡単なテキスト編集ができるようになっていれば十分です。特に、REPLを使ってコードを実行してみたり、ヘルプモードでドキュメントを確認したりすることは、Juliaの学習を進めるうえで非常に重要です。こうした癖を早い段階で付けておくとよいでしょう。

注

[*1](cK9.xhtml#aA7B) <https://github.com/JuliaLang/julia/blob/master/LICENSE.md>[*2](cK9.xhtml#a9Z8) <https://github.com/JuliaLang/juliaup>[*3](cK9.xhtml#a9GR) 執筆時点では、fishでは設定されないので、fish_add_path コマンドなどで ~/.juliaup/bin/ を PATH 環境変数に追加する必要があります。[*4](cK9.xhtml#aADD) <https://julialang.org/downloads/>[*5](cK9.xhtml#aA79) 新しい安定版が出ると、LTS版を除く古い安定版はサポートされなくなります。例えば、Julia 1.8が最新の安定版であるとすると、Julia 1.7はサポートされなくなり、Julia 1.6と1.8のみがサポートされます。サポートが終わったバージョンは、不具合の修正などが行われないので注意してください。詳しい説明はJulia’s Release Process(<https://julialang.org/blog/2019/08/release-process/>)にあります。[*6](cK9.xhtml#aABB) <https://julialang.org/downloads/platform/>[*7](cK9.xhtml#a9TF) <https://github.com/microsoft/terminal>[*8](cK9.xhtml#aAAV) <https://brew.sh/>[*9](cK9.xhtml#aA23) 「レプル」と読みます。[*10](cK9.xhtml#a9TN) answerの短縮形だと覚えておけば忘れにくいでしょう。[*11](cK9.xhtml#a9JE) <https://docs.julialang.org/en/v1/stdlib/REPL/#Key-bindings>[*12](cK9.xhtml#aAB6) Donald Knuth氏が開発した![](image_rsrcAJ1.jpg)をもとにLeslie Lamport氏が開発した組版処理システムです。数式の記述に優れ、多くのユーザがいることから、Juliaは![](image_rsrcAG6.jpg)の記法をUnicode文字の入力補助に採用しています。本書の原稿も![](image_rsrcAG6.jpg)を使って書かれています。[*13](cK9.xhtml#a9MZ) <https://docs.julialang.org/en/v1/manual/unicode-input/>[*14](cK9.xhtml#a9S9) この機能は起動直後のメッセージにも書かれているので、注意深く読んでいた方はすでに気がついていたかもしれません。[*15](cK9.xhtml#aA35) 本書では、Juliaのパッケージに文章内で言及するとき、パッケージであることをわかりやすくするため、Foo.jlのようにパッケージ名の最後に接尾辞.jlを付けます。[*16](cK9.xhtml#a9YS) LinuxやmacOSなどのUnix系OSでは、スクリプトの最初の行にある #! から始まるシバンを認識して実行する処理系を切り替えられます。[*17](cK9.xhtml#a9GV) 並列計算については[12.2節](c5F8.xhtml#a98A)で説明します。[*18](cK9.xhtml#aA4R) 同社が開発しているVisual Studioと名前が似ていますが別のソフトウェアです。[*19](cK9.xhtml#a9F2) ソースコード自体はMITライセンスで配布されていますが、ソフトウェアとしてのライセンスは<https://code.visualstudio.com/license>で規定されています。[*20](cK9.xhtml#a9F6) <https://code.visualstudio.com/>[*21](cK9.xhtml#a9UU) <https://www.julia-vscode.org/>[*22](cK9.xhtml#aABH) <https://nbviewer.jupyter.org/>[*23](cK9.xhtml#aA1X) <https://jupyterlab.readthedocs.io/en/stable/getting_started/installation.html>[*24](cK9.xhtml#aA4C) IJulia.jlにはJupyterLabをインストールする機能があるので、IJulia.jlを先にインストールし、IJulia.jlを読み込んでから jupyterlab 関数を実行しても構いません。ただし、この場合はPython環境が独自に構築されるようです。[*25](cK9.xhtml#aA3F) <https://github.com/JuliaEditorSupport/>

---

# ![](image_rsrcAJ2.jpg)

本章では、Juliaに初めて触れる読者向けに、基礎の基礎をチュートリアル形式で紹介します。本章の目的は、Juliaのプログラミング言語としての特徴を俯瞰することにあります。以降の章の内容と一部重複しますが、初めにJuliaの知識を広く浅く仕入れておけば、すぐにJuliaを使い始められますし、本書全体の見通しもよくなります。

3.1節ではREPLを使って実験しながら初歩的な機能を学びます。続く3.2節では、構造を持ったより複雑な構文の扱い方を簡単に紹介します。最後の3.3節では、有名なボードゲームであるオセロの実装を通して、実際のコードの書き方を説明します。

# 3.1 REPLで実験

REPL([2.3節](cK9.xhtml#a967))はコードを書いてすぐに実行結果を確かめられるので、Juliaを覚えるにはうってつけの環境です。まずは、REPLを使っていろいろなコードを試しながら、Juliaの動作を覚えていきましょう。サンプルコードをコピーするだけではなく、値を変えたり、普段使っている言語との共通点や相違点を比較したりしながら、REPLで遊んでみてください。ターミナルで julia コマンドを実行し、REPLを立ち上げましょう。

# 3.1.1 簡単な計算

まずは簡単な計算の方法を確認しましょう。整数の足し算なら、2 + 3 のようにプロンプトに入力した後、Enterキーを押下してコードを実行します。いくつかの例を次に示します。なお、ハッシュ(#)の位置から行末まではコメント(comment)[*1](cXU.xhtml#a9Y0)であり、コードの動作に影響しませんので、入力する必要はありません。

![](image_rsrcAJ3.jpg)

関数を使って計算するには丸括弧(())を使います。例えば、23を5で割った商と余りを計算するには、div 関数と rem 関数を次のように使います。

![](image_rsrcAJ4.jpg)

このような関数の使い方を関数呼出し(function call)と呼びます。このとき関数に渡される値を引数[*2](cXU.xhtml#aAFZ)(argument)と呼び、計算結果を戻り値(return value)と呼びます。関数呼出し div(23, 5) では、23 と 5 が引数であり、4 が戻り値です。

Juliaでは + や * も普通の関数です。したがって、+ 関数や * 関数といった呼び方をします。多くの関数は f(x, y) の形式でしか呼び出せませんが、これらの関数は演算子として x + y という呼出し方もできます[*3](cXU.xhtml#a9K7)。

ところで、上のようなREPLの表示そのままの書き方は、プロンプトと改行が紙幅の無駄遣いになるので、本書では次のようにコメントとして #> の後に結果を書くことにします。

![](image_rsrcAJ5.jpg)

計算結果以外のターミナルに出力されるテキストは、次のように #> の代わりに ## を使って書くことにします。同様に、エラーメッセージは #! を使って書くことにします。これら以外のコメントは説明のためのコメントです。なお、1行が長くなりすぎるときは直後の行に書くこともあります。

![](image_rsrcAJ6.jpg)

Juliaの説明に戻りましょう。割り算にはもう1つ、/ という関数があります。この関数の戻り値は次のように小数を含むことがあります。

![](image_rsrcAJ7.jpg)

/ 関数の戻り値は浮動小数点数という数値です。浮動小数点数は計算機で実数を近似的に表現するときに使われます。Juliaでは整数と浮動小数点数を区別して扱います。例えば、整数 4 と浮動小数点数 4.0 は区別されます。

Juliaには数学関数も多数用意されています。平方根 sqrt・正弦関数 sin・余弦関数 cos・指数関数 exp・対数関数 log の使用例を次に示します。これらの関数も浮動小数点数を返します。

![](image_rsrcAJ8.jpg)

数値とその演算については[第4章](c1JX.xhtml)で扱います。特に、整数については[4.2節](c1JX.xhtml#a96H)、浮動小数点数については[4.4節](c1JX.xhtml#a96K)で詳しく扱います。

# 3.1.2 エラー

REPLなどでコードを実行していると、次のような出力に出くわすことがあるでしょう。

![](image_rsrcAJ9.jpg)

出力の最初の行に ERROR と表示されていることからもわかるように、これはコードの実行中に問題が発生し、実行が完了できなかったことを意味しています。このような状況をエラー(error)が発生したといいます。ERROR の後には、発生したエラーの種類とメッセージが続きます。これを例外(exception)と呼び、この例では DomainError 例外が送出されたといいます。DomainError 例外は数学関数に定義域外の値を渡したことを表します[*4](cXU.xhtml#a9ZX)。

Stacktrace: の後にエラーが発生した経緯が表示されます。これをスタックトレース(stacktrace)と呼びます。スタックトレースは、どの関数がどの順序で呼び出されたのかを表し、エラーの原因を突き止めるときに役立つ情報を提供します。上にある関数ほど例外が送出された場所に近いことを意味します。[...] の直後には関数名が、@ の直後にはソースコードのファイル名と行番号が表示されていることにも注目してください。本書では以降スタックトレースを省略します。

# 3.1.3 実行順序

REPLにおいてコードは入力した順に実行されます。一度実行を開始したコードは、それが終了するか途中でエラーが発生して中断されるまで待たなければなりません。例えば、REPLで sleep(360) を実行すると、360秒間待つことになります。このときCtrl+Cを押下すると、InterruptException 例外が送出されて実行が中断されます。

![](image_rsrcAJA.jpg)

コードをスクリプト([2.4節](cK9.xhtml#a968))に書いた場合、コードは上から順に実行されます。上の行の実行が終わるまで、下の行の実行が始まることはありません[*5](cXU.xhtml#aAE2)。したがって、次の例で「ソースを入れる」が「お湯を捨てる」より前に表示されることはありません。

![](image_rsrcAJB.jpg)

なお、同じ行に複数の式を並置することはできません。複数の式を続けて実行するには、上のように改行で区切って別の行に置きます。ただし、改行の代わりにセミコロンを使って、次のように書く方法もあります。

![](image_rsrcAJC.jpg)

# 3.1.4 オブジェクトと変数

プログラミングではオブジェクト(object)という言葉をよく使います。その意味はプログラミング言語によって微妙に異なるのですが、Juliaでは計算や操作の対象になりうるものはすべてオブジェクトであり、値という言葉の同義語として使います。例えば、ここまでに登場した整数・浮動小数点数・関数・例外はオブジェクトです。以降、真偽値・文字列・配列・型・モジュールなどのオブジェクトが登場します。

オブジェクトをソースコード中に記述する記法をリテラル(literal)と呼びます。整数や浮動小数点数はリテラルで記述できますが、そうでないオブジェクトを扱うには、それをソースコード中で参照するための名前が必要です。この名前を変数(variable)と呼びます。次のコード例では、15 と 1 がリテラルであり、x, y, sqrt, + が変数です。

![](image_rsrcAJD.jpg)

このコード例の x = 15 は「以降では変数 x を使って整数のオブジェクト 15 を参照する」という宣言です。この操作を代入(assignment)と呼び、このことを変数 x をオブジェクト 15 に束縛(bind)するといいます。この例を実行した後の変数とオブジェクトの束縛関係を図3.1に示します。図中の左側がプログラム中の変数で、右側がオブジェクトの世界です。変数からオブジェクトへ伸びる矢印が「変数をオブジェクトに束縛する」という関係を表しています。変数からオブジェクトに向かう矢印の印象から「変数がオブジェクトを指す」のように表現することもあります。

![](image_rsrcAJE.jpg)

複数の変数を同じオブジェクトに束縛することもあります。例えば、次の2つの代入を実行すると、x と y は同じオブジェクト 42 を指します。このとき、変数 x と y は互いに別名(alias)になっているといいます。

![](image_rsrcAJF.jpg)

関数に記号の別名が付いていることがあります。例えば、剰余を計算する rem 関数には % という別名があり、次のように呼び出せます。

![](image_rsrcAJG.jpg)

計算と代入を同時に行う記法も用意されています。例えば、次のように x += 1 と書くと、x = x + 1 と同じ効果が得られます。

![](image_rsrcAJH.jpg)

変数や代入について詳しくは[7.1節](c315.xhtml#a971)で扱います。

# 3.1.5 基本的なオブジェクト

オブジェクトにはさまざまな種類があり、それぞれ特定の用途で使用されます。ここでは、整数と浮動小数点数以外の頻繁に使われる基本的なオブジェクトについて説明します。

# 真偽値

true と false はそれぞれ真と偽を表すオブジェクトです。両者を合わせて真偽値と呼びます。真偽値は「x の値が10より大きいか」や「x と y は同じオブジェクトか」など、何らかの条件が成立するか否かを表現するときに使われます。

![](image_rsrcAJJ.jpg)

! 関数は真偽値を反転させます。この関数は次のように使います。

![](image_rsrcAJK.jpg)

真偽値の演算は[4.1節](c1JX.xhtml#a96G)で詳しく扱います。

# nothing

nothing は何もないことを表すオブジェクトです[*6](cXU.xhtml#a9J9)。戻り値が必要ない関数など、具体的な値が重要でないときに使われます。なお、REPLに nothing と入力しても何も表示されません。

![](image_rsrcAJM.jpg)

isnothing 関数はオブジェクトが nothing かを判定する関数です。引数が nothing なら true を返し、そうでなければ false を返します。真偽値を返す関数の多くはこの関数のように is で始まる名前が付いています。

![](image_rsrcAJN.jpg)

# 文字と文字列

テキストを扱うために、文字や文字列を表現するオブジェクトが用意されています[*7](cXU.xhtml#a9JV)。次のように文字をシングルクォート(')で囲むと文字のオブジェクトが作られ、ダブルクォート(")で囲むと文字列のオブジェクトが作られます。

![](image_rsrcAJP.jpg)

文字や文字列には漢字などの文字も使えます。

![](image_rsrcAJR.jpg)

文字に整数を足したり引いたりすると別の文字になります。例えば英字 a の次の文字は b なので、'a' + 1 は 'b' になります。

![](image_rsrcAJS.jpg)

数値などを文字列化するには string 関数を使うのが基本です。string 関数は複数の引数も受けとれ、文字列への変換と同時に文字列の結合にも使えます。

![](image_rsrcAJT.jpg)

文字列には $( と ) で囲んだ部分に書いたコードの評価値を補間する機能があります。単純な変数だけなら、丸括弧は省略して $ だけで構いません。

![](image_rsrcAJU.jpg)

文字と文字列については[第6章](c2FB.xhtml)で詳しく扱います。

# シンボル

シンボルは変数名などの名前を表現するオブジェクトです。次のように変数の前にコロン(:)を付けて表記します。シンボルは[第10章](c4G7.xhtml)のメタプログラミングで特によく使われます。

![](image_rsrcAJV.jpg)

# 3.1.6 オブジェクトの比較

数値は == 関数や != 関数による等価比較ができます。比較の結果は true または false の真偽値で返されます。つまり、x == y は x と y の指すオブジェクトが等価のとき true を返し、それ以外のときは false を返します。x != y とすると、x == y と真偽値が反転します。

![](image_rsrcAJW.jpg)

同様に、< 関数と > 関数で大小比較ができます。数学の記法と同じなので、詳しい説明は不要でしょう。また、等号(=)が付いた <= 関数と >= 関数も用意されており、それぞれ数学記号の![](image_rsrcAJX.jpg)と![](image_rsrcAJY.jpg)に対応します。

![](image_rsrcAJZ.jpg)

x < y の結果を ! 関数で反転するとき、!x < y ではなく !(x < y) と書く必要があることに注意してください。

![](image_rsrcAK0.jpg)

!=, <=, >= の3つの比較関数には、それぞれ ≠, ![](image_rsrcAK1.jpg), ![](image_rsrcAK2.jpg)という別名が定義されています。≠を入力するには、![](image_rsrcAGC.jpg)ne(not equalの短縮)の直後にTabキーを押します。![](image_rsrcAK1.jpg)と![](image_rsrcAK2.jpg)はそれぞれ![](image_rsrcAGC.jpg)le(less than or equal toの短縮)と![](image_rsrcAGC.jpg)ge(greater than or equal to の短縮)で入力できます。

![](image_rsrcAK3.jpg)

2つのオブジェクトが同一かを判定する === 関数があります[*8](cXU.xhtml#a9V4)。例えば、整数 0 と浮動小数点数 0.0 はともに数のゼロを表現しますが、異なるオブジェクトなので === 関数で区別できます。=== 関数の否定形は !== 関数です。

![](image_rsrcAK4.jpg)

# 3.1.7 コレクション

標準ライブラリには、複数のオブジェクトを収めるオブジェクトであるコレクションがいくつか用意されています。コレクションに収められた各オブジェクトは要素と呼ばれます。ここで紹介するタプル・範囲・配列・辞書の4つは特に使用頻度が高く、重要なコレクションです。コレクション一般について詳しくは[第5章](c251.xhtml)で扱います。

# タプル

タプルは複数のオブジェクトを順序付きでまとめるコレクションです。タプルには、各要素に名前がない名前無しタプルと、名前がある名前付きタプルがあります。

![](image_rsrcAK5.jpg)

タプルは[5.1.1項](c251.xhtml#aA4V)で詳しく説明します。

# 範囲

範囲は等間隔の列を表現するコレクションです。数列や文字の列を表現するときに使われます。

![](image_rsrcAK6.jpg)

連続する要素の間隔はデフォルトで1ですが、コロンの間に要素の間隔を指定できます。次の例は2ずつ増える整数の範囲です。

![](image_rsrcAK7.jpg)

範囲は[5.1.2項](c251.xhtml#a9D6)で詳しく説明します。

# 配列

配列は複数のオブジェクトを順序付きでまとめるコレクションです。タプルに似ていますが、配列は次元を持ち、後から要素を変更可能などの違いがあります。1次元配列はベクトル、2次元配列は行列とも呼ばれます。要素数が多いときは、タプルより配列のほうが適しています。

![](image_rsrcAK8.jpg)

配列は[5.1.3項](c251.xhtml#aA0P)と[11.1節](c4WA.xhtml#a983)で詳しく説明します。

# 辞書

辞書はオブジェクトのペアを順序なしで保持するコレクションです。辞書の要素であるペアは => を使って作られます。ペア key => val の key をキー、val を値と呼びます。辞書はオブジェクトの対応関係を保持し、キーから値を検索するときによく使われます。

![](image_rsrcAK9.jpg)

辞書は[5.1.4項](c251.xhtml#aA3B)で詳しく説明します。

# コレクションの操作

タプルや配列が収めるオブジェクト(要素)は、次のように角括弧([])を使って参照できます。名前無しタプルと配列では、要素の位置、すなわち要素が何番目にあるかを表す整数で特定の要素を参照します。最初の要素が1番目であることに注意してください[*9](cXU.xhtml#a9UG)。

![](image_rsrcAKA.jpg)

2次元配列(行列)では、行番号と列番号を使って要素の位置を指定できます。

![](image_rsrcAKB.jpg)

同様に、名前付きタプルや辞書でも角括弧が使えます。名前付きタプルでは、要素を位置または名前で参照できます。辞書では、キーとペアになる値を参照できます。

![](image_rsrcAKC.jpg)

名前付きタプルの要素を名前で参照するときは、次のようにドット(.)を使う記法も用意されています。movie[:title] よりこちらのほうが短いのでよく使われます。

![](image_rsrcAKD.jpg)

配列と辞書では、次のように等号を使って後から要素を更新したり追加したりできますが、タプルではできません。

![](image_rsrcAKE.jpg)

配列の末尾には push! 関数を使って次のように要素を追加できます。Juliaには、このように引数を更新する関数の名前に ! を付ける慣習があります。

![](image_rsrcAKF.jpg)

コレクションにある値が要素として含まれるかを知るには、in 関数を使います。in 関数は特別に x in X という呼出し方が許されています[*10](cXU.xhtml#aA3R)。in 関数には∈(![](image_rsrcAGC.jpg)in)という別名があり、その否定形である ∉(![](image_rsrcAGC.jpg)notin)も用意されています。

![](image_rsrcAKG.jpg)

辞書にあるキーが存在するかを知るには haskey 関数を使います。

![](image_rsrcAKH.jpg)

# 3.1.8 型

型はオブジェクトの表現方法を決める重要な概念です。オブジェクトは型によってグループ分けされ、オブジェクトに適用可能な操作もその型によって決まります。Juliaには、整数の型・浮動小数点数の型・文字列の型などがあります。

オブジェクトの型は typeof 関数で取得できます。まずはここまでに登場した代表的なオブジェクトの型を見てみましょう。

![](image_rsrcAKJ.jpg)

コレクションの型はやや複雑です。例えば次のように、型名の後に波括弧({})で別の型が表示されます。

![](image_rsrcAKK.jpg)

コレクションなど一部の型は、交換可能なパラメータを持ちます。このような型はパラメトリック型([8.2節](c3S5.xhtml#a97C))と呼ばれます。例えば、Tuple{Int64, Float64} というタプルの型は、第1要素が Int64 型で第2要素が Float64 型である Tuple 型を意味します。パラメータは型だけでなく整数などになることもあります。例えば、Array{Int64, 2} という配列の型は、要素が Int64 型の2次元 Array 型であることを意味しています。

型には利便性のため別名が付いていることがあります。例えば、Int 型は実行するプラットフォームによって Int32 型か Int64 型の別名になっています[*11](cXU.xhtml#a9DJ)。パラメータ T に対して Vector{T} 型と Matrix{T} 型は、それぞれ T 型の要素を持つ1次元配列と2次元配列の別名になっています。

![](image_rsrcAKM.jpg)

型システム一般については[第8章](c3S5.xhtml)で説明します。

# 3.1.9 入出力

テキストを出力するには print 関数か println 関数を使います。println 関数は最後に改行文字を出力します。

![](image_rsrcAKN.jpg)

これらの関数は複数の引数も同時に出力できます。

![](image_rsrcAKP.jpg)

標準入力から1行を受けとるには、readline 関数を使います。

![](image_rsrcAKR.jpg)

入出力について詳しくは[13.2節](c5Y6.xhtml#a98E)で扱います。

# 3.2 基本構文

少しでもプログラミングの経験があれば、Juliaの基本的な構文は簡単に理解できるものがほとんどです。構文的にはMATLABやLuaとよく似ていますが、PythonやJavaScriptしか知らなくても、Juliaの動作は類推できるでしょう。本節では、まず基本的な構文を紹介し、続いて簡単な関数を定義して実用的な使い方を確認します。最後に構文に関する注意点をいくつか解説します。

# 3.2.1 分岐・反復・関数定義

最初に基本的な構文を説明します。これらの構文は以降の例で頻繁に使います。

# 分岐

プログラムの処理はif文を使うと実行時の条件に応じて分岐できます。次のコードは「x は偶数」という条件が成立したときのみメッセージを出力します。ここで使っている iseven 関数は引数が偶数かを判定する関数であり、end はif文の終了位置を表すキーワードです。

![](image_rsrcAKS.jpg)

if文などの条件分岐については[7.4節](c315.xhtml#a974)で詳しく扱います。[3.2.4項](cXU.xhtml#a9AH)でもいくつかの書き方を説明します。

# 反復

プログラムの一部を繰り返し実行する反復には、for文かwhile文を使います。反復はループとも呼ばれます。次のコードはfor文と範囲を使って1から5までの整数を出力します。

![](image_rsrcAKT.jpg)

while文を使う場合は次のように書きます。条件式 i ![](image_rsrcAK1.jpg) 5[*12](cXU.xhtml#a9Z2)が true である限り、while と end の間のコードが反復実行されます。

![](image_rsrcAKU.jpg)

反復については[7.5節](c315.xhtml#a975)で詳しく扱います。

# 関数定義

特定の手続きに名前を付けて再利用するには、関数を定義するのが便利です。関数は次のように function キーワードを使って定義できます。この例では double が関数名であり、x が第1引数です。return キーワードの後に書かれている 2 * x が戻り値です。

![](image_rsrcAKV.jpg)

関数定義については[7.7節](c315.xhtml#a977)で詳しく扱います。[3.2.5項](cXU.xhtml#a9BD)でもいくつかの書き方を説明します。

# 3.2.2 具体例:素数を判定する関数

実用的な関数定義を見てみましょう。リスト3.1の isprime 関数は、与えられた引数 n が素数かどうかを判定する関数です。整数2, 3, ..., ![](image_rsrcAKW.jpg)の中に n を割り切る約数があるかをfor文で探索しています。なお、この![](image_rsrcAKW.jpg)は![](image_rsrcAKX.jpg)を満たす最大の整数であり、isqrt 関数はそれを計算する標準ライブラリの関数です。整数 i が n の約数であるかは、n を i で割ったときの剰余 n % i が0になるかで判定します。ループで約数が見つかれば、return false を実行して関数は false を返します。このように、return があると関数の途中からでも値を返せます。もし最後まで約数が見つからなければ、n は素数と判断して true を返します。

![](image_rsrcAKY.jpg)

読みやすさのために、if文やfor文などの内側のブロックでは、リスト3.1のように空白文字4個でインデントをするのが標準的なスタイルです[*13](cXU.xhtml#aA7S)。これは必須ではありませんが、慣習には従うとよいでしょう。

この関数を試すには、関数定義を isprime.jl ファイルに書いて保存し、次のようにREPLで include 関数を実行して読み込みます。

![](image_rsrcAKZ.jpg)

# 3.2.3 具体例:素数を列挙する関数

もう1つ別の関数の実装例を見てみましょう。n 以下のすべての素数を列挙するエラトステネスのふるい(the sieve of Eratosthenes)を実装してみます。エラトステネスのふるいでは、まず2以上 n 以下の自然数のリストを用意し、p=2 とします。次に、リストに含まれる p より大きい p の倍数(2p, 3p, ... )をすべて削除した後、リストの中から p より大きい最小の自然数(これは素数)を選び、それを次の p とします。この操作を p が![](image_rsrcAKW.jpg)以下である限り続けると、最終的にリストには n 以下の素数のみが残ります。リスト3.2の primes 関数は、n 以下の素数を列挙するエラトステネスのふるいの実装例です。

![](image_rsrcAM0.jpg)

この関数で整数のリスト list の初期化に使っている trues 関数は、true で埋められた指定サイズの配列[*14](cXU.xhtml#aA9T)を返します。findnext 関数と findall 関数は、それぞれ配列から次の true の位置を返す関数とすべての true の位置を返す関数です。次のようにREPLでこれらの関数の動作を確認しておくとよいでしょう。

![](image_rsrcAM1.jpg)

while で始まり end で終わるwhile文は、条件式 p ![](image_rsrcAK1.jpg) isqrt(n) が成立する限りは何度でもブロック内のコードを繰り返し実行します。while文の内側にあるfor文では、ループの範囲を 2p:p:n と書いています。これは、2p から始めて n を超えない範囲で p ごとにループするということです。2p は奇妙に見えるかもしれませんが、積を計算する関数である * の短縮記法であり、2 * p を意味します。

primes.jl ファイルに関数定義を保存し、実際に primes 関数を使ってみましょう。30以下の素数は、次のように primes(30) で列挙できます。結果の正しさはリスト3.1の isprime 関数を使えば確認できるでしょう。

![](image_rsrcAM2.jpg)

さて、primes 関数は正しく動いているようですが、実際にアルゴリズムがどう動いているかはわかりにくいかもしれません。そういうときは @show マクロ[*15](cXU.xhtml#a9HJ)が役に立ちます。このマクロは次のように与えられたコードとその結果を同時に出力するので、primes 関数にあるwhile文の内側で変数が変化していく様子を観察するときに役立ちます。

![](image_rsrcAM3.jpg)

primes 関数のコードを編集し、次のように @show マクロの前にある # を削除してください。なお、ここでは配列をコンパクトに表示するため bitstring 関数を使っていますが、変数 list を直接使っても問題ありません。

![](image_rsrcAM4.jpg)

編集したファイルを保存し、include 関数で再度 primes 関数を読み込んでから実行すると、p と list の内容が表示されます。list は true の位置が 1、false の位置が 0 で表示されることに注意してください。list はすべて 1 で初期化されていますが、p = 2 のときに list の2より大きい偶数(2の倍数)がすべて 0 になり、p = 3 のときに3より大きい3の倍数がすべて 0 になり、というように、ふるいにかけられて list にある素数の候補を示す 1 が減っていく様子がわかるでしょう。

![](image_rsrcAM5.jpg)

# 3.2.4 条件分岐構文のバリエーション

条件分岐の構文にはいくつか書き方のバリエーションがあり、どれもよく使われるので、ここで簡単に紹介します。より体系的な説明は[7.4節](c315.xhtml#a974)で行います。

if キーワードとともに else キーワードを使うと、次のように条件が成立しなかったときの処理も記述できます。

![](image_rsrcAM6.jpg)

分岐を追加するときは次のように elseif キーワードを使います。elseif キーワードはいくつでも使えます。

![](image_rsrcAM7.jpg)

Juliaには条件演算子というコロン(:)と疑問符(?)を使った条件分岐の構文があります。条件演算子は cond ? x : y のように書き、条件式 cond が true のときは式 x を、false のときは式 y を評価して返します。条件演算子は次のように条件式によって評価する式を切り替えるときに使います。

![](image_rsrcAM8.jpg)

もう1つJuliaで頻繁に使われるのが、短絡評価を使った方法です。短絡評価の演算子には、対になる && と || の2種類があります。&& 演算子は cond && x のように書き、条件式 cond が true のとき式 x を評価して返しますが、false のときは x を評価せず false を返します。|| 演算子は cond || x のように書き、条件式 cond が false のとき式 x を評価して返しますが、true のときは x を評価せず true を返します。少しわかりにくいですが、両者の動作が正反対になっていることをよく確認してください。

短絡評価の演算子は、次のように2つ以上の条件式を組み合わせるときに使うのが一般的です。

![](image_rsrcAM9.jpg)

前者は次のように1つの式でも書けます。

![](image_rsrcAMA.jpg)

さらに、短絡評価の演算子をif文の短縮記法として使うこともよくあります。次の短絡評価を使った書き方とif文を使った書き方のペアは、それぞれまったく同じ動作をします。

![](image_rsrcAMB.jpg)

ここではif文・条件演算子・短絡評価の3つの条件分岐の書き方を紹介しました。どの書き方も非常によく使うので、必ず覚えておいてください。

# 3.2.5 関数定義構文のバリエーション

関数定義の構文もいくつかバリエーションがあるので、ここで少し追加説明をしましょう。より体系的な説明は[7.7節](c315.xhtml#a977)で行います。

関数定義には、function で始まる方法とその短縮記法があります。短縮記法では function も end も使わず、関数本体を等号の後に記述します。それぞれの方法で偶数かを判定する iseven 関数を書くと次のようになります。

![](image_rsrcAMC.jpg)

さらに、関数定義では return を省略でき、省略すると最後の式が戻り値になります。したがって、上の2つの書き方はそれぞれ次のようにも書けます。

![](image_rsrcAMD.jpg)

本書では、function を使った通常の関数定義ではわかりやすさのため return を省略せず、短縮表記では戻り値がほとんど自明なので return を省略することにします。

もう1つ関数定義で重要なのは、関数が受けとれる引数の型を指定できることです。例えば次のように関数 f を定義すると、この関数は Int 型以外の引数をとれません。

![](image_rsrcAME.jpg)

このような引数の型注釈は、非常に重要な機能である多重ディスパッチと密接に関わっています。Juliaでは、関数に渡された引数の数と型に応じて、複数ある実装を切り替えられます。関数の持つ個々の実装はメソッドと呼ばれます。例えば、次のコードは関数 g に5つの異なるメソッドを定義しています。最後のメソッドには引数の型指定がないので、あらゆる型のオブジェクトを受けとれるメソッドです。

![](image_rsrcAMF.jpg)

g 関数を異なる型の引数で呼び出してみると、次のように別々のメソッドが呼び出されていることがわかるでしょう。

![](image_rsrcAMG.jpg)

このような多重ディスパッチの動作を解説するには、現段階ではまだ予備知識が足りません。詳細は[第9章](c434.xhtml)で説明しますので、ここでは1つの関数が複数のメソッドを持てるということと、呼び出されるメソッドは関数に渡された引数の数と型で決まるということだけ覚えておいてください。

# 3.2.6 型の定義

ユーザも新しい型を定義できます。最もよく使うのは構造体型([8.6節](c3S5.xhtml#a97G))という型です。構造体型のオブジェクトである構造体は、フィールドというオブジェクトを保持するためのスロットを持ちます。次の Circle 型は、cx, cy, r の3つのフィールドを使って円(circle)の図形を表現する構造体型の定義です。::Float64 はフィールドが保持する値を Float64 型に制限するという意味の型に関する注釈です。

![](image_rsrcAMH.jpg)

ある型のオブジェクトは、コンストラクタ([9.3節](c434.xhtml#a97R))という型名と同名の関数で作成できます。また、フィールドが保持するオブジェクトは次のようにドットで参照できます。

![](image_rsrcAMJ.jpg)

# 3.3 オセロを作ろう

Juliaの実践的な書き方を知るには、実際のプログラムを見るのが一番です。本節では、国民的ボードゲームであるオセロ(Othello)をJuliaで実装します。これから実装するオセロは、2人のプレイヤに人間のユーザと人工知能(artificial intelligence, AI)を設定できます。人工知能といっても石を置ける場所にランダムに置くだけですが、少し工夫すれば、それなりに強いものにもできるでしょう。

オセロを選んだ理由は、日本での知名度が高く、ルールが単純であることです。それでいて盤面のデータ構造や人工知能の実装などは工夫次第で面白いものが作れます。本節でもオセロの説明をしますが、そもそもオセロをまったく知らない方は、インターネットなどで検索し、ゲームのルールを確認しておいてください[*16](cXU.xhtml#a9NN)。

コードは説明のために簡易化したりはせず、標準的な書き方をしています。まだ説明していない機能も使いますが、適宜説明を加えますので安心してください。ここでは動作の概要がつかめれば大丈夫です。以降の説明はサンプルコードの Othello.jl ファイルをモニタに表示しながら読むことをおすすめします。

# 3.3.1 オセロモジュールの定義

これから実装するオセロは、空白行やコメントも合わせて300行程度あります。こうしたそれなりに長いコードは、コードを分割して管理する単位であるモジュールにまとめると便利です。

モジュールの定義は次のように module で始まります。ここでは、Othello という名前のモジュールを定義します。その後の export に続く名前は、モジュールのユーザが使うAPI(application programming interface)です。

![](image_rsrcAMK.jpg)

モジュールの定義は end で終えます。以降で説明するコードは、この module と end の間に書かれます。モジュールについては[7.9節](c315.xhtml#a979)で解説します。

Othello モジュールを読み込んでインタフェースの play 関数を呼び出すと、次のように人工知能とのゲームが開始されます。move> と表示されたプロンプトに、例えば f5 と入力してEnterキーを押すと、f5の位置に黒石が置かれて人工知能の手番に移ります。

![](image_rsrcAMM.jpg)

# 3.3.2 石と操作関数の定義

まずはオセロの石(disk)を扱う機能を実装します。石には黒面と白面があり、石をひっくり返すことで色を変えられるのはご存知のとおりです。これらに加え、石が置かれていない空きマスを表す特別な石も定義しておきます。

石を表す型には Disk という名前を付けることにします。Disk 型は文字を表す Char 型の別名とします。あわせて空き・黒石・白石も次のように定義しておきます[*17](cXU.xhtml#a9P1)。なお、背景の暗いエディタやターミナルでは文字を白で表示することが多いので、白と黒が反転して見えるかもしれませんが、ここでは紙面上の表示を優先しています。

![](image_rsrcAMN.jpg)

変数の定義に const と付けると、変数とオブジェクトの束縛は変更できなくなります[*18](cXU.xhtml#aADP)。これは厳しい制約ですが、間違いを発見しやすくなるので、可能ならば(グローバル)変数には const を付けるべきです。こうした変数は特に定数([7.2.3項](c315.xhtml#a9PY))とも呼ばれます。

石の種類を判定する関数を定義しておきましょう。毎回 == 関数を使ってもよいですが、次のように専用の関数を用意しておいたほうが、コードが簡潔で読みやすくなることが多いです。

![](image_rsrcAMP.jpg)

ちなみに、関数定義の引数に付いている ::Disk などの型注釈は、ここで示すコード例ではほとんどの場所で不要です。つまり、型注釈を取り払ってもオセロゲームの実行に支障はないのですが、コードのわかりやすさのために付けています。型注釈を付けるべきか否かは設計の根幹にも関わる重要な問題ですが、議論するにはJuliaの知識がそれなりに必要ですので、ここではあまり深入りしないようにします。

石をひっくり返す関数も定義しておきましょう。黒石を白石に、白石を黒石に変換する flip 関数は、次のように条件演算子を使って定義できます。黒石と白石以外の石が flip 関数に引数として渡されたときには、例外を送出してエラーを知らせます。throw 関数は例外を送出する関数であり、ArgumentError は例外オブジェクトを作る関数(コンストラクタ)です。

![](image_rsrcAMR.jpg)

# 3.3.3 マスの位置

8×8のオセロ盤の初期配置は図3.2のようになっています。盤上の各列には a から h までの英字が、各行には1から8までの数字が割り振られています。図では列 f と行5をハイライトしています。盤上の石を置ける枠(マス)は、この英字と数字のペアで指定されます。例えば、黒石を持つ先手のプレイヤは列 f と行5が交差するf5の位置に石を置くと、黒石で挟まれたe5の白石をひっくり返せます。

![](image_rsrcAMS.jpg)

オセロの8×8の盤面にあるマスは、列を表す英字と行を表す数字のタプルで表現することにします。例えばf5を ('f', 5) と表します。このタプルは何度も使うので、次のように Position という別名を付けておきます。また、盤のサイズ N と行の範囲 ROWS と列の範囲 COLS もあると便利なので定義しておきます。

![](image_rsrcAMT.jpg)

Position で表現された位置を数のインデックス(添字)に変換する pos2idx 関数と、ある位置が盤上に存在するかを判定する isonboard 関数も定義しておきます。

![](image_rsrcAMU.jpg)

関数の動作をREPLで確認しておきましょう。

![](image_rsrcAMV.jpg)

# 3.3.4 オセロ盤

次は土台となるオセロ盤を定義します。オセロ盤に石を置いたり盤面を表示したりする操作もここで定義します。

# オセロ盤の定義

まずはオセロ盤を定義します。オセロ盤は2次元配列(行列)を使って表現すると便利なのですが、そのまま使うのではなく、新しく定義する構造体型を通して行列を扱うと、オセロ盤の操作をうまく抽象化できます。今回は盤の石を1つの行列で管理するので、次のように Board 型のフィールドは1つで十分です。

![](image_rsrcAMW.jpg)

次は Board 型のオブジェクトを作るコンストラクタを定義します。コンストラクタは次のように型と同じ名前の関数として定義します。

![](image_rsrcAMX.jpg)

このコンストラクタで使っている fill 関数は、同じオブジェクトで埋め尽くされた配列を作ります。fill 関数の第1引数には配列を埋めるオブジェクトを、第2引数には配列のサイズを指定します。オセロ盤は DISK_EMPTY で埋め尽くした後に、初期配置としてd5とe4に黒石を、d4とe5に白石を置いています。

# 石の参照と設置

次のような書き方でオセロ盤 board に石を参照・設置できると便利なので、その操作を定義します。実は、この記法は前述のコンストラクタ内ですでに使っています。

![](image_rsrcAMY.jpg)

標準ライブラリである Base モジュールには、getindex 関数と setindex! 関数があります。Board 型にこれらの関数のメソッドを定義すると、上記の記法が可能になります。詳細は[9.4.6項](c434.xhtml#a9K2)で説明しますが、メソッドの定義は次のように書けます。

![](image_rsrcAMZ.jpg)

この ... という書き方は引数のスプラッティング([7.7.5項](c315.xhtml#a9FN))という記法です。スプラッティングを使うと、pos2idx(pos) の返すタプルが次のように個別の引数として展開されて渡されます。

![](image_rsrcAN0.jpg)

# 盤面の表示

続いて、オセロ盤の状態を表示するメソッドを定義します。[9.4.4項](c434.xhtml#a9S0)で説明するように、Base モジュールの show 関数がオブジェクトの表示方法を決めます。

次のメソッド定義を見てください。第1引数の output は出力先を決めるストリーム([13.2節](c5Y6.xhtml#a98E))というオブジェクトです。ストリームを print 関数の第1引数に渡すと、そこに第2引数以降のオブジェクトが出力されます。countdisks 関数はすぐ後で定義します。

![](image_rsrcAN1.jpg)

次の countdisks 関数は盤上の石を数えます。ここで使っているfor文の書き方は、入れ子になった2つのfor文の代わりに1つのfor文で済ませる記法([7.5.2項](c315.xhtml#a9XF))です。この関数の戻り値は名前無しのタプルでもよいですが、名前付きタプルにしておくと、戻り値を受けとる側のコードがわかりやすくなります。

![](image_rsrcAN2.jpg)

オセロ盤の操作もREPLで確認しておきましょう。

![](image_rsrcAN3.jpg)

# 3.3.5 石の打ち方

オセロ盤ができたので、今度は石の打ち方に関する規則を実装していきます。

# ひっくり返せる石の取得

まずは、ひっくり返せる石を探す flips 関数です。この関数は、位置 pos に自分の石 disk を置いたときに挟まれる相手の石が方向 dir にあれば、ペアになる自分の石の位置をタプルで返し、ひっくり返せる石がなければ nothing を返します。

![](image_rsrcAN4.jpg)

この関数で説明が必要なのは、pos .+ dir という書き方でしょう。これは、ベクトル化演算([11.1.10項](c4WA.xhtml#a9T1))という、ベクトルやタプルで対応する要素ごとの計算を行う演算の記法です。例えば、(1, 2) .+ (3, 4) という演算では、2つのタプルの第1要素と第2要素がそれぞれ足し合わされ、(4, 6) というタプルができます。

位置 pos は英字と数字からなるタプルであったことを思い出してください。方向 dir を -1, 0, +1 からなる2要素のタプルとすれば、次のように pos .+ dir で pos の周囲のマスを表現できます[*19](cXU.xhtml#a9WR)。

![](image_rsrcAN5.jpg)

今回の例では、pos .+ dir は (pos[1] + dir[1], pos[2] + dir[2]) と同じですが、わざわざ要素ごとに + を書かなくてもよいので、表記を少し短縮できます。

# 有効手の判定

次に、オセロ盤のある位置に石を置けるかをチェックする isvalidmove 関数を定義します。オセロのルールでは、ある位置に石を置いたとき、1つ以上ひっくり返せる相手の石があれば、その手は有効です。定義は次のとおりです。

![](image_rsrcAN6.jpg)

この関数は最初に引数で指定された位置 pos が盤上にあり、かつその位置に他の石がまだ置かれていないことをチェックします。この2つの条件が満たされなければ無効な手なので、関数は早々に false を返します。

続く部分では盤上のすべての方向について、ひっくり返せる石があるかを探索しています。書き方がやや技巧的なので丁寧に解説しましょう。all 関数の関数呼出し all(f, X) はコレクション X の各要素に関数 f を適用し、すべての結果が true であれば true を返します。

![](image_rsrcAN7.jpg)

したがって、all(isnothing, X) は X の全要素が nothing であるかを判定しています。isvalidmove 関数の場合、この X がジェネレータ([5.1.6項](c251.xhtml#aACV))というコレクションになっています。ジェネレータは for キーワードを使ってオブジェクトを生成する機能を持っています。例えば、次の dirs は盤上の8方向を生成するジェネレータです[*20](cXU.xhtml#aACW)。

![](image_rsrcAN8.jpg)

まとめると、all(isnothing, flips(..., dir) for dir in dirs) は「すべての方向にひっくり返せる石がない」ことを意味するので、先頭に真偽値反転の ! 関数が付いた !all(...) は「いずれかの方向にひっくり返せる石がある」ことを確認しているわけです。これが有効手を判定するロジックです。

# 石の設置

以上の関数を組み合わせると、盤上に石を置いて相手の石をひっくり返す move! 関数が次のように定義できます。関数名が ! で終わっているのは、これがオセロ盤である引数 board の状態を更新する関数だからです。for文の中にある continue はループの残りの部分をスキップする命令([7.5.3項](c315.xhtml#a9EH))です。

![](image_rsrcAN9.jpg)

初期配置の盤面のf5に黒石を置いて、e5の石がひっくり返されることを確認してみましょう。

![](image_rsrcANA.jpg)

# 3.3.6 プレイヤ

次に人間と人工知能の2種類のプレイヤを実装しますが、その前にプレイヤ全般を表す Player 型を次のように定義します。この型は抽象型と呼ばれ、型を階層化([8.1節](c3S5.xhtml#a97B))して管理する役割があります。

![](image_rsrcANB.jpg)

各プレイヤは次の2つの関数のメソッドを定義することとします。

name 関数プレイヤの名前を文字列で返す

move 関数プレイヤの次の手の位置(投了[*21](cXU.xhtml#aA8W)するときは nothing)を返す

これらの関数は、各プレイヤがどう振る舞うべきかを抽象的に規定します。すなわち、関数の引数と戻り値の形式だけが決まっており、具体的な手続きはプレイヤの開発者に一任されています。後に実装するゲームのロジックは、name 関数と move 関数のみを通してプレイヤと関わり、関数の実装には関知しません。このように抽象的な振る舞いだけを規定しておけば、プレイヤの実装をゲームのその他の部分から分離して管理できます。例えば、人工知能のアルゴリズムを変更する場合、move 関数の内部の実装のみを変更するだけでよく、ゲームの他のコードに変更を加える必要はありません。同様に、新しいプレイヤを定義することも容易にできます。この仕組みは多重ディスパッチ([第9章](c434.xhtml))の主題です。

# ユーザ

ユーザ(人間)のプレイヤを実装します。ユーザはオセロ盤と同じように構造体型を使って定義します。User 型は文字列の名前を持ちます。<: Player は User 型が Player 型の下位に位置する型であることを表しています。struct の直前に書かれている文字列は、docstring([16.3.5項](c6XJ.xhtml#a9SM))と呼ばれるドキュメントです。

![](image_rsrcANC.jpg)

User 型に名前を取得する name 関数と、ユーザに次の手を入力させてその手を返す move 関数を実装しましょう。move 関数は、次のように定義する prompt 関数を使ってユーザの入力を受けとり、それが f5 のような正しい手の形式かを正規表現([6.3節](c2FB.xhtml#a96Y))を使ってチェックし、正しい形式ならそれを返します。もし、ユーザがCtrl+Dを押下、もしくは resign と入力すると、投了を示すために nothing を返します。入力が正しくなければ、ユーザが正しい手を入力するまで何度でもループします。

![](image_rsrcAND.jpg)

ここでは strip 関数と lowercase 関数を入力された文字列の正規化に利用しています。次のように strip 関数は前後の空白を取り除き、lowercase 関数はすべての文字を小文字に変換します。こうした文字列処理の関数は[6.4節](c2FB.xhtml#a96Z)で紹介します。

![](image_rsrcANE.jpg)

# 人工知能

ユーザと同様、RandomAI というプレイヤも定義します。これは盤上の有効手をすべて列挙し、その中からランダムに手を選びます。つまり、isvalidmove 関数と push! 関数を使って有効手を配列 moves に集め、最後に rand 関数([11.2節](c4WA.xhtml#a984))を使って配列からランダムな手を選んでいます。今回は簡単な実装ですが、盤面の評価関数などで手を重み付けするなどの工夫をすれば、自分でオセロAIが作れます。

![](image_rsrcANF.jpg)

moves 変数の初期化に使っている Position[] は、要素の型が Position 型である空の配列を作成する構文です。このように書くと、目的の型以外の要素が配列に紛れ込むのを防止できます[*22](cXU.xhtml#a9E2)。

# 3.3.7 ゲーム

最後に、石・盤面・プレイヤなど、これまでに定義した構成要素を組み合わせて、ゲームを駆動するロジックを実装します。

# 対局結果

ゲームの結果を保持する構造体型を定義します。次の Result 型は、ゲームの最終盤面と白黒それぞれのプレイヤを保持します。また、ゲームがどちらかの投了(resign)で終了した場合、そのプレイヤの石の色も保持します。

![](image_rsrcANG.jpg)

resigned フィールドに使われているユニオン型([8.3節](c3S5.xhtml#a97D))は、複数の型のうち、どの型のオブジェクトも保持できます。今回は Union{Disk, Nothing} なので、resigned フィールドには Disk 型のオブジェクトか Nothing 型の nothing を保持できます。投了したプレイヤがいなければ、nothing を保持することになります。投了以外では盤上の石を数えれば対局結果がわかるので、明示的に勝者を保持する必要はないことに注意してください。

Board 型のときと同じように、Base.show 関数に Result 型のオブジェクトを表示するメソッドを定義しましょう。このメソッドはオセロ盤にある石の数を数えて表示したり、白黒どちらが勝ったのか(あるいは引き分けたのか)を表示したりします。

![](image_rsrcANH.jpg)

石の数を表示するにあたり、標準ライブラリのPrintf.jlパッケージの機能を使っています。パッケージ([第16章](c6XJ.xhtml))は特定の機能をモジュールとして提供します。ここで使っている using キーワード([7.9.3項](c315.xhtml#a9FP))は、モジュールから特定の機能を読み込む宣言です。例えば、ここで読み込んでいる @printf マクロは、フォーマットを文字列で指定し、そのフォーマットに従って整形したテキストを出力します。@printf マクロは関数内で定義された report 関数内で呼び出されています。これはクロージャ([7.7.7項](c315.xhtml#a9HD))と呼ばれます。すぐ後で少し複雑なクロージャの例を紹介します。

投了していない場合、盤上の石の数を数えて勝者を決めますが、そこでは cmp 関数を使っています。cmp(a, b) は a が b より小さければ -1 を、大きければ +1 を、同じ値であれば 0 を返します。

投了があったとき、resigned の石の色が黒または白であることを @assert マクロで確認しています。このマクロは、与えられた式が true に評価されないと、次のように AssertionError 例外を送出します。投了できるのは黒または白のどちらかしかありませんから、それ以外の値、例えば DISK_EMPTY などの不正な値が入ってないことを確認しているわけです。これは必須ではありませんが、バグなどをいち早く発見するのに役立ちます。

![](image_rsrcANJ.jpg)

# 対局

いよいよゲームを開始する play 関数を定義します。この関数はゲームを駆動する重要な関数です。play 関数はプレイヤを指定するためにキーワード引数([7.7.4項](c315.xhtml#a9F0))という名前付きの引数 black, white をとります。オセロでは黒石を持ったプレイヤ black が先手です。デフォルトは black が User、white が RandomAI なので、ユーザと人工知能のゲームになります。play 関数はゲームが終われば Result 型のオブジェクトを返します。getmove 関数はプレイヤが有効手を提示するまで move 関数を呼び出します。

![](image_rsrcANK.jpg)

![](image_rsrcANM.jpg)

play 関数のデフォルトのプレイヤ名は、次の randname 関数で生成しています。

![](image_rsrcANN.jpg)

play 関数の変数 nextdisk はクロージャに束縛されています。クロージャは関数の一種ですが、内部に状態を保持でき、呼び出すたびに内部状態に応じた結果を返します。すぐ後で実装を説明しますが、nextdisk のクロージャは、前回打った石の色を内部状態として持っており、呼び出されるたびに次の手番の石を返します。どちらの石も打てなければ nextdisk は nothing を返します。次の手番を決める処理は少し複雑なのですが、クロージャに状態を閉じ込めて手続きを抽象化してしまえば、play 関数の実装はこのように簡単になります。

# プレイヤの交替

次に打つプレイヤの石の色を決めるロジックは alternatedisks 関数に実装されています。オセロでは原則として黒石と白石を交互に打ちますが、有効手がないときには相手に手番を渡さなければいけないので、次のプレイヤが石を置けるかあらかじめチェックする必要があり、単純な黒と白の交替より処理が少し複雑です。

![](image_rsrcANP.jpg)

初期配置での動作例を次に示します。黒と白の石を交互に返していることがわかります。

![](image_rsrcANR.jpg)

この関数の実装はやや技巧的なので、詳しく説明しましょう。alternatedisks 関数の返すクロージャは、内部状態として石の色を表すオブジェクトを変数 next に保持しています。クロージャが呼び出されるたび、クロージャ内部では next を参照してその色の石を置く場所が盤面にあるかを canmove 関数で確認します。もしなければ、石の色を変えて再試行し、それでも石を置く場所がなければ nothing を返して終局します[*23](cXU.xhtml#a9CV)。置ける石の色が決まればクロージャはその石の色を返しますが、その前に next の石の色を変えて次の呼出しに備えます。

ここまで書いたら、最後に end を書いて Othello モジュールの定義を終了します。

![](image_rsrcANS.jpg)

まとめ

本章では、REPLを使った実験から始め、基本的な構文と機能を概観し、オセロが遊べるプログラムの実装まで紹介しました。オセロのコードはあえて手加減せず実践的な書き方をしたので、初学者には難しかったかもしれません。しかし、以降の章で説明するJuliaの基礎を押さえてから読み直せば、コードの詳細までわかるようになるはずです。今回はJuliaの実装例を示すのが目的だったので、人工知能の実装は可能な限り単純なものにしましたが、Juliaに慣れてきたら実装を工夫して強くしてみるのも一興でしょう。

注

[*1](cXU.xhtml#a9NY) 本書では使いませんが、#= と =# で囲まれた部分もコメントとして無視されます。[*2](cXU.xhtml#a9RZ) 「ひきすう」と読みます。[*3](cXU.xhtml#a9FC) 一般的ではありませんが、+(2, 3) のような呼出し方も可能です。[*4](cXU.xhtml#a9AN) エラーメッセージでも説明されているように、log(complex(-1)) と書けば、定義域が複素数に拡張された対数を計算できます。[4.7.4項](c1JX.xhtml#a9AP)の説明も参照してください。[*5](cXU.xhtml#a9XH) ただし、@async マクロ([12.1.6項](c5F8.xhtml#a9N8))のように独立した処理を並行して実行する機能もあります。[*6](cXU.xhtml#aAEV) Pythonの None やJavaScriptの null に相当します。[*7](cXU.xhtml#a9Y5) Juliaでは文字と文字列は厳密に区別され、文字は1文字の文字列ではないことに注意してください。PythonやJavaScriptなどのように、文字を長さ1の文字列で表現する言語もあります。[*8](cXU.xhtml#a9KA) Pythonの is やJavaScriptの === に相当します。別名は ≡(![](image_rsrcAGC.jpg)equiv)であり、その否定形として ≢(![](image_rsrcAGC.jpg)nequiv)もあります。[*9](cXU.xhtml#aA8A) PythonやJavaScriptでは、最初の要素は0番目です。[*10](cXU.xhtml#a9X4) もちろん in(x, X) という呼出し方もできます。[*11](cXU.xhtml#a9EP) 現在使われているデスクトップ用やサーバ用の計算機では、ほとんどの場合 Int64 型の別名です。[*12](cXU.xhtml#a9Z3) x <= 5 と書いても同じです。[*13](cXU.xhtml#aACB) 本書ではUnicodeでコードポイントがU+0020の文字のみを空白文字(space)と呼んでいます。タブ文字や改行などを含むより広い意味では単に空白(whitespace)と呼んでいます。[*14](cXU.xhtml#a9CF) 正確にいえば、汎用的な Array 型の配列ではなく、ビット配列という真偽値に特化した配列を生成します。ビット配列については詳しくは[11.1.13項](c4WA.xhtml#a9CG)を参照してください。[*15](cXU.xhtml#a9WY) 現段階でマクロとは何かを端的に説明するのは困難です。マクロを含むメタプログラミング一般については[第10章](c4G7.xhtml)で解説します。特にマクロについては[10.4節](c4G7.xhtml#a97Y)で説明します。[*16](cXU.xhtml#aADR) 次のサイトにルールの簡単な説明があります。<http://www.othello.org/lesson/lesson/rule.html>[*17](cXU.xhtml#a9PK) '![](image_rsrcAGC.jpg)u25cf' はUnicode文字のコードポイントを表します。'●' と直接文字を入力することもできますが、入力方法がわかりにくいので、ここではASCII文字のみで入力できるようにしています。文字のリテラルについては[6.1.2項](c2FB.xhtml#a9PM)で解説します。[*18](cXU.xhtml#a9RF) 取り替えられることもありますが、期待しない動作をする可能性があるのですべきではないでしょう。少なくとも警告は表示されます。[*19](cXU.xhtml#aA0E) ただし、(0, 0) の方向だけはあらかじめ排除する必要があります。[*20](cXU.xhtml#a9H3) ジェネレータの全要素を配列として取り出したければ、collect 関数が使えます。例えば collect(dirs) を実行すると、方向を示す8個のタプルが配列として得られます。[*21](cXU.xhtml#a9P9) 負けを認めて対局を終えることです。[*22](cXU.xhtml#a9PT) 型の異なるオブジェクトを push! 関数で配列に入れようとすると、型の変換が起こり、変換に失敗するとエラーが発生します。[*23](cXU.xhtml#a9TV) 例えば、盤上の石がすべて同じ色になると、双方ひっくり返せる相手の石がないので新たに置ける石はありません。もちろん、盤面が完全に埋まった場合も新たな石は置けません。

---

# ![](image_rsrcANT.jpg)

---

# ![](image_rsrcANU.jpg)

本章では、最も基本的な型である数値型とその計算について解説します。Juliaではさまざまな数値型を目的に応じて使い分けるので、Pythonなど他のスクリプト言語に比べるとやや状況が複雑ですが、全体的には一貫性があるので、慣れてしまえばそれほど難しくは感じないはずです。

Juliaはデータの型を暗黙的に変換することはありませんが、1 + 0.1 のように複数の型が混在する計算では、昇格という一見すると暗黙の型変換に見える動作が発生します。昇格は数値の計算で特によく使われるので、本章で扱います。

# 4.1 真偽値

真偽値(boolean value)は、true か false のいずれかの値になりうるデータです。true はある条件が成立することを、false は成立しないことを表すために使われます。例えば、< や >= などの不等号は、次のように true か false を返します。

![](image_rsrcANV.jpg)

真偽値の型は true も false も同じ Bool 型です。これは値の型を調べる typeof 関数を使えばわかります。

![](image_rsrcANW.jpg)

# 4.1.1 真偽値の演算

真偽値の演算で最も重要なのは論理演算(logical operation)です。論理演算は真偽値で閉じた演算であり、!, |, & の3つの演算子(関数)が特に重要です。

否定 ! は true と false を反転させる演算です。これらの演算の実行例を次に示します。

![](image_rsrcANX.jpg)

論理和 | は2つの真偽値のうち、一方または両方が true のときのみ true を返し、論理積 & は両方が true のときのみ true を返します[*1](c1JX.xhtml#a9BG)。

![](image_rsrcANY.jpg)

これらのほかに、否定論理和 nor・否定論理積 nand・排他的論理和 xor も用意されています。これらの関数にはそれぞれ ∇(![](image_rsrcAGC.jpg)nor), ∧(![](image_rsrcAGC.jpg)nand), ∨(![](image_rsrcAGC.jpg)xor)という別名が付いており、x∨y のように中置して呼び出せます。

# 4.1.2 数値としての真偽値

真偽値は数値とはやや異なるようにも見えますが、順序や算術に関する規則が定義されているので、2つの値のみをとりうる数値型とみなせます。言い換えれば、true は数値の1に、false は数値の0に対応します。例えば、真偽値どうしの加算は次のように整数になります。

![](image_rsrcANZ.jpg)

# 4.2 整数

整数(integer)は-3, -2, -1, 0, 1, 2, 3 のような小数を持たない数直線上の離散的な数値です。Juliaでは整数を表現する型が複数用意されており、用途に応じて適切な型を選択できます。ただし、ほとんどの場面で使われるのは Int という型の整数です。本節では、まず整数を表現する一般的な方法を説明し、続いて標準ライブラリにある整数の型と表記を紹介します。最後に、整数の算術とビット操作に関わる演算を解説します。

# 4.2.1 整数の表現

計算機の内部では、整数は2進法、つまり0と1からなるビット列で表現されます。例えば、整数の19は 19=1・24+0・23+0・22+1・21+1・20 なので、2進法で表すには下線部を集めて 100112 になります。なお、2進法表記であることを明示するため数値の右下に2を添えています。さらに、計算機では2のべき乗ビット数である8, 16, 32, 64ビットなどが扱いやすいので、左側から必要なビット数を0で埋めます。100112 を8ビットで表現するなら、0を3つ補い 000100112 と表現します。0以上の値を表現するときに既定のビット数をすべて使う整数の表現を、符号なし整数(unsigned integer)と呼びます。

0以上の整数(符号なし整数)ではこのように簡単なのですが、負の整数も含めて表現しようとするとやや事情が複雑です。負の整数の表現は、絶対値が同じ正の整数のビットをすべて反転し、1を足して作ります。こうした整数の表現を2の補数表現(two’s complement representation)と呼びます[*2](c1JX.xhtml#aAFB)。8ビット表現なら、-19の2の補数表現は 111011012 です。この一見奇妙な表現方法は、絶対値が同じ正負の数の和を固定ビット数に収めると、ちょうどゼロになることを考えれば自然に思えるでしょう。例えば、図4.1に示すように、19+(-19)=000100112+111011012=1000000002 であり、8ビット幅からあふれた左端の1を削除して 000000002、つまりゼロになります。このように負の整数も表現できるようにした整数の表現を符号付き整数(signed integer)と呼びます。

![](image_rsrcAP0.jpg)

概念上の整数は無限に存在しますが、固定サイズのビット数を使って表現できる整数はその一部だけです。n ビットで表現できる符号なし整数は0から 2n-1 まで、符号付き整数は-2n-1 から 2n-1-1 までの範囲です。また、2の補数表現を使った場合、0以上の整数では左端のビットが0であり、負の整数では1であるという特徴があります。

# 4.2.2 整数の型

Juliaでは整数の型を非常に細かく使い分けられます。例えば、Pythonでは標準で使える整数型は int 型のみですが、Juliaには実に11種類もの整数型が標準で用意されています。こういうとひるませてしまいそうですが、デフォルトの整数型である Int 型以外はあまり使われないので安心してください。

整数を表現する型は「符号の有無」と「サイズ(精度)」の2つの独立した軸で分類できます。[4.2.1項](c1JX.xhtml#a9MG)で説明したように、符号がない(表現できない)整数を符号なし整数と呼び、符号がある(表現できる)整数を符号付き整数と呼びます。符号なし整数は同じサイズの符号付き整数と比べ、負の整数が表現できない代わりに0以上の整数を2倍多く表現できます。

整数のサイズには小さいものから順に8, 16, 32, 64, 128ビットと5種類の固定サイズがあり、さらにサイズが可変なものもあります。固定サイズの符号付き整数の型名は Int、符号なし型名は UInt で始まり、その後にサイズを表すビット数が続きます。表現する整数によってサイズが変わる整数は符号付きの BigInt 型のみで、対応する符号なしの型はありません。表4.1に整数型を精度と符号の有無で分類した表を示します。なお、Bool 型の真偽値も整数のように振る舞うのでこの表に加えています。

![](image_rsrcAP1.jpg)

Int 型のサイズは計算機によって異なり、32ビット計算機では Int 型のサイズは32ビット、64ビット計算機ではサイズは64ビットです[*3](c1JX.xhtml#a9YM)。つまり、Int 型は32ビット計算機では Int32 型の、64ビット計算機では Int64 型の別名になっています。符号なしの UInt 型も同様です。次の例は64ビット計算機で Int 型が Int64 型の別名になっていることを示しています。

![](image_rsrcAP2.jpg)

固定サイズの整数型は表現できる整数の範囲が限られる一方で、可変サイズの整数に比べて計算機で効率的に扱えるという利点があります。Juliaでは計算効率のために、固定サイズの Int 型が主に使われます。固定サイズの整数が表現できる下限と上限は、それぞれ typemin 関数と typemax 関数で取得できます。次の例は16ビット整数が表現できる下限と上限の値です。

![](image_rsrcAP3.jpg)

# 4.2.3 整数の表記

ソースコード中で整数を表記するリテラルには2, 8, 10, 16進法の4種類があります。10進法リテラルは日常で使う整数の表記と同じ 0 から 9 までの数字が1つ以上連続する列です。-123 のように先頭にマイナス(-)を付けると、絶対値が同じ負の整数を表します。01234 のように 0 から始まっていても、先頭の 0 が無視されるだけで10進法のリテラルであることに変わりはありません[*4](c1JX.xhtml#aADB)。

10進法リテラルの表記は基本的に Int 型の値になりますが、Int 型で表せないほど絶対値が大きな値のリテラルは Int128 型など、より表現範囲が広い値になります。例えば、64ビット計算機では 263-1=9223372036854775807 まで Int 型(Int64 型)で表現できますが、それより大きくなると次のように Int128 型の値になります。負の数の場合も同様です。

![](image_rsrcAP4.jpg)

さらに Int128 型でも表せないほど絶対値が大きくなると BigInt 型に切り替わります。

![](image_rsrcAP5.jpg)

絶対値が小さい整数であっても Int128 型や BigInt 型を強制したいときは、次のような int128"..." や big"..." という表記も使えます。

![](image_rsrcAP6.jpg)

2進法リテラルは 0b で始まり、0 か 1 の数字が続きます。8進法リテラルは 0o で始まり、0 から 7 までの数字が続きます。16進法リテラルは 0x で始まり、0 から 9 までの数字か a から f までの英字が続きます。a から f は対応する大文字の A から F でも意味は変わりません。10進法リテラルは符号付き整数と解釈されるのに対し、これらのリテラルは符号なし整数と解釈されます。例えば、0b101010, 0o52, 0x2a は、それぞれ2, 8, 16進法表記で42にあたる符号なし整数のリテラルです。

![](image_rsrcAP7.jpg)

符号なし整数では、値ではなくリテラルの長さによって UInt8 型から UInt128 型まで変化します。例えば16進法のリテラルなら1桁が4ビットに相当するので、桁数×4ビットを収められる最小サイズの符号なし整数の型が選ばれます。16進法の例を次に示しますが、2進法や8進法でも同様です。

![](image_rsrcAP8.jpg)

[9.3節](c434.xhtml#a97R)で紹介するコンストラクタを使えば、ある値から目的の型に変換するのも容易です。次の例は、UInt8 型の 0x2a を UInt32 型に変換しています。

![](image_rsrcAP9.jpg)

位どりをして読みやすくするために、リテラルにアンダースコア(_)を挿入できます。アンダースコアは連続して挿入できないので注意してください。

![](image_rsrcAPA.jpg)

# 4.2.4 整数の算術演算

整数の和・差・積・冪の演算はそれぞれ次のように書けます。

![](image_rsrcAPB.jpg)

整数の計算結果が、その型で表現できる範囲に収まらないときは注意が必要です。例えば Int64 型で表現できる最大値に1を加えると、typemin(Int64) に等しい値になります。この振る舞いは[4.2.1項](c1JX.xhtml#a9MG)で説明した2の補数表現で期待される動作です。組合せの計算など非常に大きくなりうる整数を扱うときは BigInt 型を使用したほうがよいでしょう。

![](image_rsrcAPC.jpg)

整数の除算はやや注意が必要です。/ 関数の戻り値は割り切れる整数どうしの演算であっても必ず浮動小数点数になります[*5](c1JX.xhtml#aA7M)。

![](image_rsrcAPD.jpg)

ユークリッド除法(Euclidean division)による商の計算は div 関数を使い、剰余の計算は rem 関数を使います。div 関数と rem 関数にはそれぞれ ÷(![](image_rsrcAGC.jpg)div)と % という別名が付いています[*6](c1JX.xhtml#aA56)。これらは別名なので動作は変わりませんが、+ 関数などと同様に中置記法で呼び出せます。商と剰余を同時に得るには divrem 関数を使います。

![](image_rsrcAPE.jpg)

ゼロによる整数の除算は DivideError 例外が送出されます。

![](image_rsrcAPF.jpg)

divrem 関数と似た関数に fldmod 関数があります。どちらの関数も次のように2つの整数 x と y に対する商 q と剰余 r を返すのですが、動作に微妙な違いがあります。

![](image_rsrcAPG.jpg)

両者に共通する性質は x=q・y+r(|r|<|y|) を満たすことです。しかし、r≠0 のとき、r の符号は divrem 関数では x と同符号になるのに対し、fldmod 関数では y と同符号になります。このとき q は r に応じて一意に決まります。次の例で動作を確認してください。

![](image_rsrcAPH.jpg)

div 関数と fld 関数、rem 関数と mod 関数の違いも同様です。なお、x と y が同符号のときは両者に差はありません。

% 関数は第2引数に整数の型 T をとれます。T で表現可能な整数の数を n とすると、x % T は n を法として x と合同[*7](c1JX.xhtml#a9A9)な T 型の整数を返します。実行例を次に示します。

![](image_rsrcAPJ.jpg)

整数でよく使われる算術関数を表4.2に示します。なお、この表のほとんどの関数は浮動小数点数など整数以外の数でも同様に使えます。詳しくはドキュメントを参照してください。

![](image_rsrcAPK.jpg)

# 4.2.5 整数のビット演算

整数、特に符号なし整数の型は、ビット列で表現されるデータ、すなわちバイナリデータを表現するときによく用いられます。ビット演算(bitwise operation)は、4.2.1項で説明したように整数を0と1で表現されたビット列として見たとき、個々のビットに対して行う演算を指します。標準ライブラリに用意されているビット演算を表4.3にまとめます。ビット数を数える演算も同じ表に含めました。

![](image_rsrcAPM.jpg)

ビット演算の動作を確認するには bitstring 関数が便利です。この関数は与えられたデータのビット列を文字列で返します。

![](image_rsrcAPN.jpg)

シフト演算はやや複雑なので少し説明を加えましょう。標準ライブラリでは、左シフト・右シフト・論理右シフトの3種類のシフト演算が定義されています。一番単純な左シフト x << n では、n が0以上のとき、x のビットがすべて n ビット左にスライドし、空席になった右端の n ビットは0で埋められます。n が負のときはビットがスライドする方向が逆になる右シフト x >> -n と等価です。

n が0以上のとき、右シフト x >> n と論理右シフト x >>> n はともにすべてのビットを右にスライドしますが、空席になった左端の埋め方に違いがあります。右シフトは x が0以上であれば0で、負であれば1で左端の空席を埋めますが、論理右シフトは常に0で埋めます。したがって、x が符号付き整数のとき、右シフトは x の符号を変えませんが、論理右シフトでは x が負のときに1ビット以上シフトすると結果は0以上の整数になります。次の例で両者の違いを確認してください。

![](image_rsrcAPP.jpg)

n が負の数のとき、右シフトと論理右シフトはともに左シフト x << -n と等価です。x が0以上のとき、右シフトと論理右シフトはともに0で左端を埋めるので動作に差はありません。

# 4.3 有理数

有理数(rational number)は整数の既約分数[*8](c1JX.xhtml#aA81)で表現できる数です。Juliaは有理数を表現する Rational 型を定義しています。有理数の記法は、// を使って分子と分母を分けて記述します。例えば 2//3 は分子が2、分母が3の有理数です。分子と分母はそれぞれ numerator 関数と denominator 関数で取得できます。Rational 型はパラメトリック型([8.2節](c3S5.xhtml#a97C))と呼ばれる種類の型であり、分母と分子の型が整数型でパラメータ化されています。

![](image_rsrcAPR.jpg)

有理数には次のように基本的な演算が定義されています。整数とは異なり、除算 / が有理数で閉じていることに注意してください。

![](image_rsrcAPS.jpg)

有理数型の値は、整数や浮動小数点数では正確に表現できない一部の実数を正確に表現できます。例えば、1/3という数は、整数はもちろん、2進法や10進法の浮動小数点数を使っても正確に表現できませんが、Rational 型では 1//3 で容易に表現できます。

# 4.4 浮動小数点数

浮動小数点数(floating-point number)は、実数を計算機で近似的に扱うための数値です。0と1の間にある小数や円周率などの数は、整数の型で表現することはできませんが、浮動小数点数を使えば十分に近い値で表現できます。また、64ビットの整数でも表現できない大きな値(例えばアボガドロ定数[*9](c1JX.xhtml#a9VW)である6.02214076×1023)も、浮動小数点数で近似的に表現できます。科学技術計算では浮動小数点数の使用機会は非常に多く、数値計算をするうえで最も重要な型といってもよいでしょう。

ここで「近似的に」や「十分に近い値で」表現できるなどと微妙な言い回しをしたのには理由があります。実は、整数と異なり、浮動小数点数の表す値と本当に表したい値との間には誤差(error)が存在することがあります。無限に存在する実数を現実の計算機にある限られた資源で厳密に表現することはほとんど不可能なので、浮動小数点数はある程度の誤差を許容した近似的な表現をします。運よく目的の数値を浮動小数点数で誤差なく表現できたとしても、それらを使った計算の結果が浮動小数点数で誤差なく表現できるとは限りません。浮動小数点数の世界では、その表現にも計算にも誤差が入り込むことがほとんどです。

浮動小数点数は初学者に限らずわかりにくいようで、Juliaコミュニティ内でも浮動小数点数の誤解に起因する質問がよく見受けられます。Juliaのユーザは浮動小数点数が絡む数値計算を行う機会が多いでしょうから、本書では浮動小数点数の一般論を特に詳しく説明します。すでにIEEE 754準拠の浮動小数点数をよく知っている方は、4.4.5項からJuliaにおける浮動小数点数の扱いを解説しますので、一般論を飛ばしてそこから読み始めても構いません。

# 4.4.1 浮動小数点数の表現

まずはJuliaに限定せず、浮動小数点数の一般的な話から始めましょう。数学の言葉を使って浮動小数点数を定義します。浮動小数点数には基数(radix)βと精度(precision)p の2つのパラメータがあります。基数βは2以上の整数、精度 p は1以上の整数です。p が精度と呼ばれる理由はすぐにわかります。これら2つのパラメータのもとに、ある整数の組 (M, e) を使って次の式で表される数 x を(有限の)浮動小数点数と呼びます[*10](c1JX.xhtml#aA3E)。

![](image_rsrcAPT.jpg)

ここで、M は |M|<βp を満たし、・は積を表します。M と e はそれぞれ整数仮数(integral significand)と指数(exponent)と呼ばれています。指数 e には最小値 emin と最大値 emax を定める制約 emin![](image_rsrcAJX.jpg)e![](image_rsrcAJX.jpg)emax が付くことが多いです。

具体例を考えてみましょう。基数と精度をそれぞれβ=10 と p=3 とします。このとき、M は |M|<103=1000、すなわち3桁以下の整数 {-999, -998, ..., 998, 999} のいずれかです。このように、p が M のとりうる値の数を決定するので、p は精度と呼ばれているわけです。ひとまず M=123と固定して考えると、e=1 に対応する浮動小数点数 x は x=123・101-3+1=12.3 です。同じようにして、e=-2, -1, 0, 1, 2, 3 で計算してみると、対応する浮動小数点数はそれぞれ x=0.0123, 0.123, 1.23, 12.3, 123, 1230 となります。この例からわかるように、指数 e によって浮動小数点数の小数点(decimal point)の位置が決まります。

今度は e を固定して M を動かしてみましょう。e=1 と固定して M を123から124にすると、対応する浮動小数点数 x は12.3から12.4に変化します。実は、この2つの値の間にある値(例えば12.34)は、同じパラメータ (β, p) の浮動小数点数では表現できません。もし12.34を表したいなら、精度を増やして p=4 以上にする必要があります。次に、e=2 と固定して M を同じように動かすと、対応する浮動小数点数 x は123から124に変化します。図4.2の数直線に示すように、浮動小数点数 x の変化は e=1 のとき+0.1なのに対して、e=2 では+1になります。同じように e=0 なら変化は+0.01になり、e=3 なら+10になります。この浮動小数点数の変化量は、ulpという誤差の評価に使われる単位と深く関係しています。

![](image_rsrcAPU.jpg)

いま説明したように、浮動小数点数の指数 e は、整数仮数の最小変化(M から M+1)が浮動小数点数をどれだけ変化させるかを決定しています。つまり、e が大きいほど浮動小数点数の絶対値が大きくなり、かつ最小変化の幅も大きくなります。言い方を変えれば、浮動小数点数は絶対値が小さいほど変化の刻み幅が細かく緻密であり、大きくなるにつれ刻み幅が大きく大雑把になります。この性質は浮動小数点数がよく使われる科学技術計算にとって合理的な性質です。例えば、原子間距離(10-10 メートルオーダ)を測るには日常で用いないような細かい目盛りが必要ですが、惑星間距離(1010 メートルオーダ)を測るには1メートル程度の違いはほとんど問題にならないでしょう。浮動小数点数は、スケールがまったく異なる数値を現実的な細かさで表現できる優れた表現方法です。

# 4.4.2 浮動小数点数の正規化

説明のために、浮動小数点数の定義を少し修正します。といっても、先ほどの定義と本質的に変わるところは何もありません。まず、整数仮数 M から符号を取り出すため、M![](image_rsrcAJY.jpg)0 のとき0となり、M<0 のとき1となる整数 s を定義します。次に、負でない数 m を m=|M|・β-p+1 と定義します。すると、先ほどの浮動小数点数 x の定義は次のように書き換えられます。

![](image_rsrcAPV.jpg)

こちらの3つの整数の組 (s, m, e) を使った定義を採用すると、M が s と m の2つの数に分解され、m は 0![](image_rsrcAJX.jpg)m<βを満たす数になります。s は符号(sign)、m は仮数(significand)[*11](c1JX.xhtml#aA1F) と呼ばれます。

パラメータが (β, p)=(2, 3) のとき、仮数 m がとりうる値は次の8通りのみです。2進法表記であることをわかりやすくするため、値の右下に小さい2を付けています。

![](image_rsrcAPW.jpg)

ここで、1つ面倒な問題があります。浮動小数点数 x=1.002 を (s, m, e) の3つ組で表そうとすると、次の3つの方法があることがわかります。

![](image_rsrcAPX.jpg)

つまり、符号 s=0 は確定なのですが、1.002・20=0.102・21=0.012・22 であるために、同じ数を表す m と e の組合せが3通り発生しています。同様のことは1.102=0.112・21 でも起きています。同じ数を表すときに複数の方法があるのは、計算機で数を扱ううえではあまり好ましくないので、表現を工夫する必要があります。それが正規化(normalization)です。

浮動小数点数では、m が最大になるような m と e の組合せのみを値の表現に使います。上の例でいえば、x=1.002 は3つのうち一番上の (s, m, e)=(0, 1.002, 0)、つまり x=(-1)0・1.002・20 で表現します。このとき、m![](image_rsrcAJY.jpg)1 となる浮動小数点数を正規化数(normal number)と呼び、0<m<1 となる浮動小数点数は非正規化数(subnormal number)[*12](c1JX.xhtml#aA5Y)と呼びます。m=0 に対応する浮動小数点数のゼロは正規化数でも非正規化数でもありません。また、各浮動小数点数について正規化数と非正規化数は1通りの表現しかありませんが、ゼロのみ符号 s によって2通りの表現方法があることに注意してください。

非正規化数は数直線上の原点(ゼロ)付近にのみ存在し、正規化数はその外側に存在します。パラメータ β=2, p=3, emin=-1, emax=2 のすべての浮動小数点数と、符号 s および指数 e の値を図4.3の数直線上に図示します。中央に(正負の)ゼロがあり、ゼロ周辺のオレンジ色で示した値が非正規化数で、その外側の青色で示した値が正規化数です。正の非正規化数と正の正規化数の最小値は、ともに指数 e が emin=-1 で、仮数 m がそれぞれ 0.012 と 1.002 のときであり、値はそれぞれ 0.012・2-1=0.125と0.012・2-1=0.5 です。また、浮動小数点数は等間隔に並んでいるわけではなく、正規化数は指数 e が1増えると隣り合う浮動小数点数の間隔がβ=2 倍になっていることにも注目してください。このように、ゼロから絶対値が離れるほど浮動小数点数の間隔が広がり、近似がより大雑把になっていきます。

![](image_rsrcAPY.jpg)

浮動小数点数を使った計算は、計算結果が正規化数で表現できないことがあります。計算結果が非正規化数になることをアンダーフロー(underflow)と呼びます。同様に、計算結果が正規化数の範囲を外側に超えてしまうことをオーバーフロー(overflow)と呼びます。

# 4.4.3 浮動小数点数の形式

基数βが10である10進法の浮動小数点数のほうが人間にはわかりやすいのですが、計算機ではβ=2 に対応する2進法の浮動小数点数のほうが扱いやすく、いくつか便利な性質も備えています。実際、Juliaの標準ライブラリにある浮動小数点数は、2進法の浮動小数点数のみです[*13](c1JX.xhtml#a9CP)。ここでは、2進法の浮動小数点数のエンコーディング形式について考えてみましょう。

浮動小数点数の標準規格であるIEEE 754では、2進法の浮動小数点数について、16, 32, 64, 128ビットの4種類のサイズの形式を規定しています。32ビットと64ビットの形式は特に重要で、それぞれ単精度(single precision)と倍精度(double precision)と呼ばれています。また、16ビットと128ビットの形式は、それぞれ半精度(half precision)と4倍精度(quadruple precision)と呼ばれることがあります。いずれのサイズでも、1つの浮動小数点数は図4.4に示すような (S, E, T) 3つからなるビット列の組でエンコーディングされています。ビット数はそれぞれ1, w, p-1であり、それぞれ符号・指数・仮数をエンコーディングしています。例えば、32ビットの単精度浮動小数点数なら w=8, p-1=23 であり、符号も含めた合計ビット数は1+w+(p-1)=w+p=32 となります。他のサイズの浮動小数点数に関しても同様です。精度 p を表すビット列 T の長さを p-1と書く理由はすぐに説明します。標準規格で規定されている2進法の浮動小数点数のビット数を表4.4に示します。

![](image_rsrcAPZ.jpg)

![](image_rsrcAR0.jpg)

符号 s のエンコーディング S は、s=0 なら S=0 に s=1 なら S=1 になるだけなので簡単です。指数 e のエンコーディングには、負の数を表現するために若干のトリックが入ります。すなわち、指数をエンコードするビット列 E を符号なし整数として解釈した後、バイアス(bias)と呼ばれる定数 b をその値から引いた値が指数 e です[*14](c1JX.xhtml#a9TH)。例えば、バイアス b=127のとき、指数の w=8 ビットのエンコーディング E が00000110というビット列なら、000001102=6 なので 6-b=-121 が指数 e です。このようにビット列 E は正規化数の指数 e のエンコーディングに使われますが、すべて0かすべて1のときのみ特別な解釈がなされます。E がすべて0のときは、エンコーディングしている浮動小数点数はゼロもしくは非正規化数と解釈されます。すべて1のときは、後で紹介する無限大やNaNといった特殊な値を収めるために使われます。

仮数 m のエンコーディング T には、さらに面白いトリックがあります。表現する浮動小数点数 x が正規化数のとき、仮数 m は m![](image_rsrcAJY.jpg)1 なので、2進法では先頭の数字が1である m=1.dd...d2(d は0か1)という形式になることがわかります。x が正規化数かどうかは前述したとおりビット列 E を見ればわかるので、m の先頭の1はビット列 T に含める必要はなく、小数点以下の dd...d2 のみを保持すればよいのです。非正規化数もしくはゼロのとき、同様に m=0.dd...d2 となるので、やはり先頭の0をビット列 T に含める必要はありません。結果として、精度を1ビット多く確保できます。ビット列 T の長さが p ではなく p-1となっているのは、このように仮数の先頭ビットを省略しているからです。

ビット列 E がすべて1のときを保留していましたので説明しましょう。このとき、浮動小数点数はビット列 T がすべて0か否かによって次の2種類の特殊な値を表現します。

・ビット列 T がすべて0のとき

絶対値が無限大(infinity)の値を表します。符号ビット S が0のときは正の無限大+∞、1のときは負の無限大-∞になります。任意の有限の浮動小数点数 x について、大小関係-∞<x<+∞が成立します。例えば、正負の無限大はそれぞれ1/0と-1/0を計算すると出現します。

・それ以外のとき

有効な数ではないことを示す値を表します。NaN(Not a Number)もしくは非数と呼ばれます。例えば、0/0や![](image_rsrcAR1.jpg)などの計算で出現します。

これらの特殊な値は[4.4.1項](c1JX.xhtml#aA2P)で説明した有限の浮動小数点数とあわせて、浮動小数点数全体を構成します。

# 4.4.4 丸めと誤差

本節の最初に、浮動小数点数には誤差がつきまとうと説明しました。ここまでに説明したとおり、浮動小数点数は飛び飛びの離散的な値をとります。図4.3の数直線上に示したパラメータβ=2, p=3 の浮動小数点数には、2つの隣り合う浮動小数点数の間を埋める浮動小数点数は存在しません。IEEE 754で規定されている浮動小数点数は精度 p が大きいので、この図に比べてずっと密度が高いですが、それでも表現できない実数は2つの浮動小数点数の間に依然として存在します。

近似的に表現できる浮動小数点数へ実数を変換する操作を丸め(rounding)といいます。ここでは、実数 x を丸めた浮動小数点数を![](image_rsrcAR2.jpg)(x) と表記することにします。単に丸めといっても規格では5種類もの丸めが定義されていますが、最もよく使われるのは、偶数への最近接丸め(rounding to nearest, ties to even)です。この丸めはデフォルトの丸めとなったおり、実数を最も近くにある浮動小数点数へと丸めますが、実数が2つの隣り合う浮動小数点数のちょうど中間に位置するときには、仮数の最後が偶数(β=2 のときは0)である方向へと丸めます。図4.5に浮動小数点数で正確に表現できない実数 x と y の丸め動作の概略を示します。図中の実数 x は左側の浮動小数点数のほうが近いので左側へと丸められていますが、y はどちらの浮動小数点数からも等距離なので仮数の最後のビットが0である右側へと丸められています。

![](image_rsrcAR3.jpg)

日常で使う10進法表記の小数には、有限精度の2進法浮動小数点数で正確に表せるものとそうでないものがあります[*15](c1JX.xhtml#aACK)。例えば、0.125は2進法の浮動小数点数でも容易に表現できますが、0.1は2進法で表現しようとすると0.0001100110011...2 と0.02 の後に 00112 が限りなく続き、精度 p をいくら大きくしても正確に表現できません。したがって、2進法の浮動小数点数では、小数0.1は丸められると真の値との間に誤差が発生します。例えば、単精度浮動小数点数(p=24)なら、0.100000001490116119384765625という0.1に近い値に丸められます。また、1/3や![](image_rsrcAR4.jpg)など、10進法でも2進法でも有限の桁数では誤差なく表現できない実数もあります。こうした、表したい実数とそれに最も近い浮動小数点数の間の誤差を表現誤差(representation error)と呼ぶことがあります。

もう1つ重要な誤差の原因が、演算による誤差です。つまり、浮動小数点数の演算の結果が浮動小数点数で表現できないとき、その結果は近い値に丸められて誤差が発生します。例えば、10進法で精度 p=3 の浮動小数点数 1.23・102 と2.84・100 を足すとき、指数を揃えて計算すると 1.23・102+0.0284・102=1.2584・102 となりますが、この結果は p=3 桁の精度を超えるので正確に表現できず、丸められて![](image_rsrcAR2.jpg)(1.2584・102)=1.26・102 となります。これはつまり、計算機で行う演算 x+y は、実際のところ丸めを含んだ![](image_rsrcAR2.jpg)(x+y) を意味するということです。演算に丸めがつきまとうせいで、任意の実数 x, y, z については成立する (x+y)+z=x+(y+z) や (x+y)+z=(x+z)+y などの基本的な性質が浮動小数点数では成立しません[*16](c1JX.xhtml#aA4H)。

IEEE754規格では、加減乗除と平方根の演算について、浮動小数点数はあたかも無限の精度であるかのように計算した後で丸めるのと同じ結果を返すと定義されています。さらに、加算と乗算を融合したfused multiply-add(FMA)演算も定義されています。これは3つの浮動小数点数 x, y, z について、丸めは最後の1回のみ、つまり![](image_rsrcAR2.jpg)(x・y+z) を計算します。加算と乗算を別々に計算する![](image_rsrcAR2.jpg)(![](image_rsrcAR2.jpg)(x・y)+z) に比べて丸めが1回で済むので、結果的に演算の誤差が小さくなることがあります。

丸めやその他の計算によって生じる誤差に関連して、2つの重要な用語を紹介します。1つ目はulp(unit in the last place)という単位です。ulpは隣り合う2つの浮動小数点数の間隔の広さを1とする単位です。これは、仮数の最後の桁の重み(仮数の最後の桁が1増えたときの変化)に対応します。間隔の広さは絶対値によって変化するので、実数 x のulpは ulp(x) のように関数[*17](c1JX.xhtml#a9EU)として表します。ulpは単純な関数や操作の結果生じる誤差を測る単位として頻繁に使われます。例えば、最近接丸めでは、丸めの結果生じる誤差は0.5ulp以下(丸められる実数が2つの浮動小数点数のちょうど中間地点にあるときが誤差最大の0.5ulp)です。また、ulp(1)=β-p+1 の値を特別に計算機イプシロン(machine epsilon)と呼びます。2つ目の重要な用語は相対誤差(relative error)です。誤差のない非ゼロの実数 x とその近似値![](image_rsrcAR5.jpg)の間の誤差 |x-![](image_rsrcAR5.jpg)| を |x| で割った値| (x-![](image_rsrcAR5.jpg))/x| が相対誤差です。相対誤差はulpで測るのが難しい複雑な計算でよく使われます。

# 4.4.5 浮動小数点数の型

浮動小数点数の一般論はここまでにして、ここからはJuliaにおける浮動小数点数の扱いを見ていきます。Juliaは浮動小数点数の型を4種類定義しています。そのうち3つが標準規格で定義されている16, 32, 64ビットの浮動小数点数に対応し、それぞれ Float16, Float32, Float64 と型名が付いています[*18](c1JX.xhtml#a9Y9)。Juliaはこれら固定サイズの浮動小数点数に加えて、可変サイズの浮動小数点数である BigFloat 型も定義しています。BigFloat 型は固定サイズの浮動小数点数型に比べて計算コストはかかりますが、より高い精度の近似が可能です。

浮動小数点数のリテラルは整数と区別されます。まず、小数点(.)が入る数値のリテラルは、浮動小数点数です。例えば 0 は整数ですが、0.0 は小数点が入るので浮動小数点数のリテラルです。また、指数表記(exponential notation)も浮動小数点数です。指数表記では e(または E)の左に仮数、右に指数を10進法で記します。次の4つの値は Float64 型のリテラルの例です。

![](image_rsrcAR6.jpg)

小数点の前後の数字を省略したリテラルも浮動小数点数です。また、指数表記では小数点も省略できます。次の4つの値は Float64 型のリテラルの例です。

![](image_rsrcAR7.jpg)

整数の場合と同様に、位どりのためなどにアンダースコア(_)を挿入できます。

![](image_rsrcAR8.jpg)

浮動小数点数には正負2種類のゼロがあり、それぞれ次のように表記します。なお、+ の付かない 0.0 は正のゼロです。

![](image_rsrcAR9.jpg)

正負の無限大とNaNはそれぞれ次のように表記します。

![](image_rsrcARA.jpg)

符号なし整数と同様、Float64 には16進法表記があります。10進法表記と異なり、精度が十分あれば値を誤差なく表記できます。0x から始まり、仮数が続いて、p(または P)の後に指数を続けます[*19](c1JX.xhtml#a99Y)。10進法の指数表記と異なり、p(または P)の後の指数は2のべき乗(つまり基数は2)であることに気を付けてください。

![](image_rsrcARB.jpg)

Float32 型の表記には、指数表記の e(または E)の代わりに f(または F)を使います。次の3つの値は Float32 型のリテラルの例です。

![](image_rsrcARC.jpg)

無限大とNaNはそれぞれのサイズが付いた名前があります。Inf と NaN はそれぞれ Inf64 と NaN64 の別名です。

![](image_rsrcARD.jpg)

無限大とNaNを除けば、Float16 型の値を記載するリテラルは用意されていませんので、他の値から Float16 型の値へ変換する必要があります。BigFloat 型については[4.4.9項](c1JX.xhtml#aAEM)で扱います。

# 4.4.6 浮動小数点数の出力に関する注意

REPLのプロンプトに 0.1 を入力し、値を表示させると 0.1 と出力されます。これは当然のようですが、0.1という実数は2進法の浮動小数点数では正確に表現できない値です。いうなれば、この表示には若干のごまかしがあります。Float64 型で表現できる0.1に最も近い値は0.1000000000000000055511151231257827021181583404541015625ですが、REPLでこれをすべて出力するのはあまりに煩わしいので、通常は丸めた結果が同じ値になる桁数最小の値が出力されます[*20](c1JX.xhtml#a9U4)。

浮動小数点数の表す値を10進法で正確に出力するには、標準ライブラリのPrintf.jlパッケージにある @printf マクロを使います。このマクロは、表示する形式と浮動小数点数の値をとり、指定された形式に従って出力します。例えば、次のように %.30f と形式を指定すると、小数点以下の30桁までを出力します。

![](image_rsrcARE.jpg)

@printf マクロは f の代わりに e を使うと指数表記になります。ドット(.)の後の整数は表示精度(桁数)です(デフォルトの精度は6)。また、@sprintf マクロを使うと結果を文字列で返します。

![](image_rsrcARF.jpg)

浮動小数点数の小数点以下の表示桁数を調整するために round 関数を使う例を見かけますが、これは推奨されません。10進法での丸め処理は正確に行えないからです。代わりに @printf マクロや @sprintf マクロを使用してください。

# 4.4.7 浮動小数点数の比較

浮動小数点数の比較には少し複雑な側面があります。浮動小数点数の比較が、誤差を許容できるかという問題を含んでいるからです。ゼロやNaNの扱いはさらに比較を複雑にしています。

浮動小数点数では符号ビットが異なるにもかかわらず、== 関数は正負のゼロを同じ値とみなします。一方で、ビット列が同一の NaN どうしを同じ値とみなしません。

![](image_rsrcARG.jpg)

isequal 関数を使うと、これらの結果は逆になります。

![](image_rsrcARH.jpg)

こうした微妙な問題があるので、ゼロやNaNを判定するときには専用の関数を使うほうが間違いにくいです。ゼロの判定には iszero 関数、NaNの判定には isnan 関数が用意されています。

![](image_rsrcARJ.jpg)

正負のゼロ、およびNaNと他の浮動小数点数の比較では、順序が定義されていません。結果として、< 関数によるこれらの値の大小比較は常に false を返します[*21](c1JX.xhtml#a9BY)。次の結果は < を > に取り替えても変わりません。

![](image_rsrcARK.jpg)

順序が定義されていないと浮動小数点数の配列をソートするとき不便なので、isless というゼロやNaNにも一貫した順序を付ける関数が定義されています。実際、配列を整列する sort 関数の大小比較は、isless 関数がデフォルトの比較関数です。

![](image_rsrcARM.jpg)

最後に、isapprox 関数を紹介しましょう。isapprox(x, y) 関数は2つの引数 x と y がだいたい同じ値かを返す関数です。厳密な一致を要求しないことが多い浮動小数点数では、このような関数は役に立ちます。まずは簡単な例を見てみましょう。1.0は1.0-10-8 と同じ値と判定されていますが、1.0-10-7 とは異なる値と判定されています。

![](image_rsrcARN.jpg)

大雑把にいえば、isapprox 関数のデフォルトの動作は、仮数の左から約半分が一致すれば true を返すように設定されています。倍精度浮動小数点数は10進法で約16桁の精度に対応する仮数を持つので、上の例ではその約半分にあたる8桁目付近に分岐点があることを示しています。また、isapprox 関数には ≈(![](image_rsrcAGC.jpg)approx)という別名の関数が用意されています。その否定である![](image_rsrcARP.jpg)(![](image_rsrcAGC.jpg)napprox)関数も用意されています。

![](image_rsrcARR.jpg)

だいたいこのような理解でも十分なのですが、理解を深めるためにもう少し説明しましょう。isapprox 関数は絶対的(absolute)な誤差と相対的(relative)な誤差の2つの基準で値が近いかを判定します。誤差はそれぞれ atol キーワード引数[*22](c1JX.xhtml#aA8X) [*23](c1JX.xhtml#aA8Y)と rtol キーワード引数で指定されます。そして isapprox(x, y) は、abs(x - y) ![](image_rsrcAK1.jpg) max(atol, rtol * max(abs(x), abs(y))) をチェックしています。atol がデフォルト値のゼロであるとき、T を x と y の型とすると、rtol はデフォルトで sqrt(eps(T)) に設定されています。ここで、sqrt は平方根を計算する関数で、eps は浮動小数点数型 T の計算機イプシロンを返す関数です。したがって、デフォルトの isapprox 関数は次のように相対的な差が計算機イプシロンεの平方根以下かを判定していることになります。

![](image_rsrcARS.jpg)

isapprox 関数を使うときはゼロ付近の動作には注意してください。例えばゼロと最小の正の浮動小数点数(約5.0・10-324)を比べたときにすら、isapprox 関数は false を返します。isapprox 関数でゼロに近いかを判定するには、atol キーワード引数をうまく設定する必要があります。

![](image_rsrcART.jpg)

# 4.4.8 浮動小数点数の関数

浮動小数点数を扱ううえで重要な関数を表4.5に示します。整数の算術関数として紹介した表4.2にある多くの関数も浮動小数点数の関数として使用できます。数学関数については[4.7.3項](c1JX.xhtml#a9MM)を参照してください。いくつか注意が必要な関数について解説を加えます。

![](image_rsrcARU.jpg)

round 関数のデフォルトの動作は、小数点以下の値を四捨五入した整数を浮動小数点数として返します。引数が2.5など2つの整数のちょうど中間に位置するとき、整数が偶数になる方向に変換します。小数の処理をどう行うかは、第2引数に渡す丸めモード(rounding mode)によって変化します。デフォルトの丸めモードは RoundNearest です。表4.6に丸めモードの一覧を示します[*24](c1JX.xhtml#a9BJ)。

![](image_rsrcARV.jpg)

使い方は次のとおりです。デフォルトの動作より RoundNearestTiesAway や RoundNearestTiesUp 丸めモードのほうが日本語の四捨五入に近い動作をします。

![](image_rsrcARW.jpg)

eps 関数は引数なしで呼び出すと、倍精度浮動小数点数の計算機イプシロンを返します。浮動小数点数の型を引数として与えると、その型の計算機イプシロンを返します。浮動小数点数を引数として与えると、ulp(x) の値を返します。

![](image_rsrcARX.jpg)

# 4.4.9 可変サイズの浮動小数点数

BigFloat 型は他の浮動小数点数の型と異なりサイズが可変です。Float64 型などより計算コストはかかりますが、より高い精度の計算ができます。BigFloat 型はGNU MPFRライブラリ[*25](c1JX.xhtml#a9HS)の上に実装されています。

BigFloat 型の値を作るには、big"..." の中に浮動小数点数のリテラルを記述します。

![](image_rsrcARY.jpg)

ほかにも BigFloat 型のコンストラクタを使う方法もありますが、コンストラクタに渡される引数はすでに丸められているので BigFloat 型の精度を下回ることがあることに注意してください。例えば、BigFloat(0.1) と書くと実数0.1はあらかじめ Float64 型の浮動小数点数に丸められるので、big"0.1" より精度が下がります。

![](image_rsrcARZ.jpg)

BigFloat 型のデフォルトの精度は precision 関数で取得できます。BigFloat 型のデフォルトの精度と丸め処理は setprecision 関数と setrounding 関数で変更できます。

![](image_rsrcAS0.jpg)

# 4.5 複素数

Juliaは標準で Complex という複素数を表現する型を定義しています。虚数単位 i は im で表します。例えば複素数 1.0+2.3i は次のように書けます。

![](image_rsrcAS1.jpg)

あるいは complex 関数を使ってもよいでしょう。

![](image_rsrcAS2.jpg)

Complex 型は Rational 型と同じパラメトリック型([8.2節](c3S5.xhtml#a97C)を参照)と呼ばれる種類の型で、実部と虚部が Int64 型や Float64 型など別の数値型でパラメータ化されています[*26](c1JX.xhtml#a9G6)。

![](image_rsrcAS3.jpg)

複素数の実部と虚部はそれぞれ real 関数と imag 関数で取得します。reim 関数を使えば実部と虚部を同時に取り出せます。

![](image_rsrcAS4.jpg)

複素共役は conj 関数で取得します。

![](image_rsrcAS5.jpg)

複素数には通常の数と同様に演算が定義されています[*27](c1JX.xhtml#a9GT)。

![](image_rsrcAS6.jpg)

ただし、絶対値関数 abs は引数が複素数であっても戻り値は複素数になりません。数学的には、複素数 x=u+vi の絶対値は![](image_rsrcAS7.jpg)で定義されます。

![](image_rsrcAS8.jpg)

# 4.6 定数の無理数

標準ライブラリには数学定数がいくつか定義されています。円周率を表す定数 pi は次のように表現されます。また、pi の別名としてπ(![](image_rsrcAGC.jpg)pi)を定義しています。

![](image_rsrcAS9.jpg)

こうした定数の無理数を表現するために、Irrational 型という型が用意されています。

![](image_rsrcASA.jpg)

定数の無理数は、計算時に適切な浮動小数点数の近似値に変換されます。したがって、多くの場合は浮動小数点数の変数と同じように計算式に組み込めます。

![](image_rsrcASB.jpg)

明示的に数値型に変換するにはコンストラクタに定数を渡します。

![](image_rsrcASC.jpg)

円周率 pi を含む定数は、MathConstatns モジュールで定義されています。円周率以外の定数は使用頻度が低いため、MathConstatns.golden のように MathConstatns モジュールを接頭辞にして参照する必要があります。MathConstants モジュールに定義されている定数を表4.7にまとめます。

![](image_rsrcASD.jpg)

# 4.7 数値の演算

本節では、数値の型に限定されない一般的な数値の演算を紹介します。

# 4.7.1 算術演算子の優先順位

数式と同じように、Juliaの演算子には計算の優先順位があります。例えば、a + b * c は最初にある加算 + より先に乗算 * を行います。同様に、a * b ^ c は * より先に ^ を計算します。

![](image_rsrcASE.jpg)

+, -, * などは比較演算子より優先順位が高く、シフト演算子より優先順位が低いです。

![](image_rsrcASF.jpg)

より詳しくは[7.3.3項](c315.xhtml#aA7K)で扱います。

# 4.7.2 昇格

1 + 2.0 のように複数の型が混在する数値の演算では、特定の規則に則って値が変換されます。昇格規則(promotion rule)とは、異なる型を持つ複数の値を1つの型に集約する規則を指します。昇格規則に従って値の型を変換することを昇格(promotion)と呼びます。昇格は四則演算を含む多くの計算で使われます。例えば、1 + 2.0 を計算すると 3.0 になりますが、これは Int 型の 1 が加算の前に Float64 型の 1.0 に昇格し、2.0 が加算された結果です。昇格のいくつかの例を次に示します。

![](image_rsrcASG.jpg)

昇格規則は値ではなく型によって決まります。例えば、Int 型と Float64 型の値を昇格した結果はいつでも同じです。値がどう昇格されるかを知るには promote 関数を使います。次のように、promote 関数は引数を昇格して結果をタプルで返します。

![](image_rsrcASH.jpg)

promote_type 関数を使えば、型のレベルで昇格規則を確認することもできます。次のように、promote_type 関数は複数の型が昇格してどの型になるかを返します。

![](image_rsrcASJ.jpg)

昇格により、値はより表現の幅が広い型に変換されます。例えば、Int32 型と Int64 型の値を昇格するとき、前者は後者で完全に表現できる(表現の幅が広い)ので後者が選ばれます。同様に、Float32 型と Float64 型の値を昇格するときは後者が選ばれます。昇格で型が拡張される例をいくつか示します。

![](image_rsrcASK.jpg)

昇格規則には注意が必要な微妙なケースもあります。例えば、Int8 型と UInt8 型の昇格を考えると、互いに表現できない値がありますが、ビット列はそのままで UInt8 型に昇格します。ただし、Int8 型の値が負の数のときは UInt8 型に対応する値が存在しないので InexactError 例外が送出されます。

![](image_rsrcASM.jpg)

さらに、Int64 型と Float64 型の昇格でも互いに表現できない値がありますが、Int64 型の値は Float64 型の近似値に昇格します。昇格の結果は必ずしも無誤差ではないということに注意してください。

型の変換を明示的に行うには、oftype 関数や convert 関数を使います。oftype 関数は第1引数に変換先の型の値を指定し、convert 関数は第1引数に変換先の型を直接指定します。どちらも変換対象の値は第2引数に指定します。

![](image_rsrcASN.jpg)

# 4.7.3 数学関数

zero 関数と one 関数は、それぞれ引数と同じ型の加法と乗法の単位元を返します。これらの関数は型も引数にとれます。引数に応じた単位元を返すので、しばしば変数の初期化に使われます。

![](image_rsrcASP.jpg)

標準ライブラリには基本的な数学関数が多数用意されています。すでに紹介したものと一部重複しますが、代表的な数学関数の名前を表4.8に列挙します[*28](c1JX.xhtml#aA9U) [*29](c1JX.xhtml#aA9V)。各関数の詳細については関数のドキュメントを参照してください。

![](image_rsrcASR.jpg)

# 4.7.4 数学関数の戻り値の型

sin 関数や sqrt 関数などの通常実数について定義されている関数は、引数として固定サイズの整数を与えると Float64 型の戻り値を返します。

![](image_rsrcASS.jpg)

一方、引数が BigInt の場合は BigFloat 型の戻り値を返します。

![](image_rsrcAST.jpg)

引数が浮動小数点数の場合、引数と同じ型の戻り値を返します。

![](image_rsrcASU.jpg)

平方根など、一部の関数は戻り値が複素数になりうる複素関数としても振る舞います。しかし、Juliaの数学関数は性能のため戻り値の型が引数の型で決まるように設計されているので、例えば sqrt 関数に負の値を渡すと複素数を返すのではなく DomainError 例外が発生します。このような場合は引数をあらかじめ複素数に変換しておく必要があります。

![](image_rsrcASV.jpg)

# 4.7.5 数学関連のパッケージ

標準ライブラリにはない基礎的な数学関連パッケージを表4.9に示します。

![](image_rsrcASW.jpg)

まとめ

本章では数値型とその演算を解説しました。多数の数値型が登場しましたが、中心になるのは Bool 型の真偽値・Int 型の整数・Float64 型の浮動小数点数の3つだけです。バイナリデータを扱うときは UInt8 型もよく使われますが、他の型は特殊な用途以外ではあまり使われないので、初めのうちはそれほど気にする必要はありません。また、軽く扱われがちな浮動小数点数については特に詳しく説明したので、基本的な性質をよく確認し、奇妙な動作に出くわしたときは参考にしてください。昇格規則も慣れないうちはハマりやすいので注意してください。

注

[*1](c1JX.xhtml#aA5S) 論理和と論理積の演算子は短絡評価([7.4.1項](c315.xhtml#aA5T))に使う || と && に似ていますが、評価の仕方が異なりますので注意してください。例えば、短絡評価される x || y は x が true に評価されれば y は評価されませんが、x | y は x の評価値にかかわらず y も常に評価されます。[*2](c1JX.xhtml#a9GX) 歴史的には1の補数表現など別の表現方法もありますが、今日ではまず使われません。Juliaはそうした古い計算機をサポートしていないので、2の補数表現だけ考えれば十分です。[*3](c1JX.xhtml#a9GW) C の int 型の精度は、32ビット計算機でも64ビット計算機でも32ビットであることが多いですが、Juliaではそうではありませんので注意してください。[*4](c1JX.xhtml#a9BA) C やC++では、0 から始まる整数リテラルは8進法ですので、違いに注意してください。[*5](c1JX.xhtml#a9UC) これはPythonと同じ振る舞いですが、C やC++とは異なります。[*6](c1JX.xhtml#a9TU) Pythonの % はJuliaの mod 関数に相当します。後述するように、mod 関数と rem 関数とは微妙に動作が異なる場合があるので注意してください。[*7](c1JX.xhtml#a9H1) 整数 x, y が整数 n![](image_rsrcAJY.jpg)2 を法として合同であるとは、x と y をそれぞれ n で除した剰余が等しいことを指します。詳しくは合同算術(modular arithmetic)の資料を参照してください。[*8](c1JX.xhtml#a9CY) 分母と分子に共通の約数が存在しない分数の表現です。[*9](c1JX.xhtml#aADG) 1モル(mol)という単位に含まれる分子の数です。[*10](c1JX.xhtml#aA4P) この式で表せない(有限でない)浮動小数点数も一部ありますが、毎回「有限の」と断るのは煩雑なので、文脈から明らかなときは省略します。[*11](c1JX.xhtml#a9JJ) mantissaと呼ばれることもありますが、現在ではsignificandと呼ぶのが普通でしょう。[*12](c1JX.xhtml#a9HG) IEEE754-2008でdenormal numberから改称されました。[*13](c1JX.xhtml#aACU) 10進法の固定長浮動小数点数を扱うためのDecFP.jlというパッケージがあります。[*14](c1JX.xhtml#aA54) 2の補数表現ではなくバイアスを使うことで、浮動小数点数の比較がビット列の単純な比較になるという利点があるようです[*15](c1JX.xhtml#a9NA) 逆に有限精度の2進法の小数は、十分な桁数があれば10進法でも表現できます。[*16](c1JX.xhtml#a9TC) Gaucheの開発者である川合史朗氏のブログ記事(<http://blog.practical-scheme.net/shiro/20160513-flonumquestion>)に、関連した面白い問題があります。[*17](c1JX.xhtml#aA0H) 数学的な ulp(x) の定義には細かいバリエーションがいくつかありますが、ここでは、実数 x について、|x|∈[βe, βe+1) のとき![](image_rsrcASX.jpg)と定義します。これは正規化数でのJuliaの慣習とも一致します。[*18](c1JX.xhtml#a9FY) 規格上は128ビットも定義されていますが、標準ライブラリには実装されていないので本書では扱いません。[*19](c1JX.xhtml#a9UW) e や E では16進法の整数表記と区別できなくなるので、代わりに p(または P)が使われます。[*20](c1JX.xhtml#a9BN) Julia 1.6ではRyuという浮動小数点数を文字列化するアルゴリズムが使われています。Ryuは文字列化した浮動小数点数を再度浮動小数点数に変換すると元の浮動小数点数に戻る最短の表示をする性質があります。[*21](c1JX.xhtml#a9PJ) これは、浮動小数点数 x が x > 0 のようにある範囲にあることを確認する条件式の成立を防ぐためだと考えられます。[*22](c1JX.xhtml#aA46) キーワード引数は引数の名前と値を同時に指定する方法です。[7.7.4項](c315.xhtml#a9F0)で詳しく扱います。[*23](c1JX.xhtml#a9DT) 接尾辞の tol は許容量(tolerance)を意味します。[*24](c1JX.xhtml#aA28) RoundFromZero はごく一部でしか実装されていないので、混乱を避けるため一覧には載せていません。[*25](c1JX.xhtml#a9AJ) <https://www.mpfr.org/>[*26](c1JX.xhtml#aA3D) Complex{Float64} 型には ComplexF64、Complex{Float32} 型には ComplexF32 のように別名が付いています。[*27](c1JX.xhtml#a9DF) 複素数の演算は仮に虚部が0でも戻り値は常に複素数です。[*28](c1JX.xhtml#a9ND) sin 関数など三角関数の引数はラジアン単位が標準ですが、sind のように接尾辞 d が付いた度単位の引数をとる関数や、sinpi のように接尾辞 pi が付いた円周率倍の引数をとる関数も用意されています。[*29](c1JX.xhtml#aA6Y) 完全なリストは<https://docs.julialang.org/en/v1/base/math/>を参照してください。

---

# ![](image_rsrcASY.jpg)

プログラムで扱うオブジェクトは、何かしらの構造を持つ別のオブジェクトに収めて扱うと便利なことがあります。こうした「オブジェクトを収めるオブジェクト」はコレクション(collection)と総称されます。標準ライブラリにはさまざまなコレクション用の型が用意されているので、コレクションの特性を考慮して目的に応じた使い分けが必要です。さらに、コレクションを操作する関数は非常に多いので把握が大変です。本章では、まず代表的なコレクションを概説し、続いてコレクションを操作する関数を説明します。さらに、for文とともに使われる重要な概念であるイテレーションについても本章で扱います。最後に、コレクションの特性に応じた使い分けについて解説します。

# 5.1 代表的なコレクション

まずは標準ライブラリにある代表的なコレクションを紹介しましょう。本節では、各コレクションの簡単な特徴と作成方法のみを解説し、コレクションに対する操作や特性の説明を次節以降に回します。なお、文字列も文字のコレクションとみなせますが、[第6章](c2FB.xhtml)で詳しく解説するので本章では扱いません。

# 5.1.1 タプル

タプル(tuple)は、複数の値をまとめて扱うための最もシンプルなコレクションです。例えば、オセロの例([3.3節](cXU.xhtml#a96D))で盤上の位置を表現するときに用いた ('f', 5) は、文字と整数からなるタプルです。タプルに含まれる要素には順序があり、タプルを構成する個々の要素が同じでも、要素の順序が違えば異なるタプルです。

タプルには Tuple 型の名前無しタプル(unnamed tuple)と NamedTuple 型の名前付きタプル(named tuple)の2種類があります。単にタプルといったときには通常名前無しタプルのみを指すこともありますが、ほとんどの性質はどちらにも共通するので、両者を区別せずタプルと呼ぶことも多いです。本書では特に注意が必要なときのみ「名前無しタプル」と「名前付きタプル」の用語を使います。

タプルを作成するには、次のように丸括弧で全体を囲み、構成する個々の要素をコンマ(,)で区切ります。ただし、名前無しタプルでは(要素が1つもない場合を除き)丸括弧を省略できます。名前付きタプルの場合は各要素の前に等号(=)で名前を付けます。

![](image_rsrcASZ.jpg)

通常最後の要素の後にコンマは不要ですが、要素が1つのタプルを作るときは必要です[*1](c251.xhtml#aAEU)。また、要素が1つもない空のタプルは ()(名前無しタプル)または (;)(名前付きタプル)で作れます。

![](image_rsrcAT0.jpg)

既存の変数からタプルを構築することもできます。例えば、x... のようにタプルの内側に記述すると、x の要素がタプルの要素として展開されます。同様に、; x, y のように記述すると、変数名がそのまま要素名となった名前付きタプルが構築されます。同様の記法は、関数呼出しで引数を渡すときにも使えます([7.7.5項](c315.xhtml#a9FN)を参照)。

![](image_rsrcAT1.jpg)

タプルの各要素は角括弧([])を使って参照できます。角括弧の内側には参照する要素の位置を書きます。最初の要素の位置は1番目であることに注意してください[*2](c251.xhtml#aA53)。例えば、名前無しタプルの場合は次のとおりです。

![](image_rsrcAT2.jpg)

数値に加え、角括弧内では begin と end という特別なキーワードが使えます。それぞれ最初の要素と最後の要素の位置を示します。これらのキーワードを含む計算もできます。

![](image_rsrcAT3.jpg)

名前付きタプルの場合、位置に加えて名前でも要素を参照できます。これは次の3通りの記法がありますが、特に理由がなければ最も短い最初の記法が選ばれます。

![](image_rsrcAT4.jpg)

なお、タプルの要素を別の要素に取り替えたり、タプルに要素を追加したり、要素を削除したりはできません。

# 5.1.2 範囲

範囲(range)は離散的な値の列を表現するコレクションです。主に数列を表現するときに使われますが、ほかに文字の範囲なども作れます。範囲を表現する型はいくつもありますが、基本的な性質は共通しており、細かい型の違いを意識して使う場面はほとんどないので、本章では型の違いには立ち入りません。

範囲はコロン(:)を使って start:stop または start:step:stop の形式で作成します。start:stop は start から stop まで1ずつ増加する範囲を表現し、start:step:stop は start から stop まで step ずつ増加(step が負なら減少)する範囲を表現します[*3](c251.xhtml#a9FB)。

![](image_rsrcAT5.jpg)

範囲は整数のみでなく、次のように浮動小数点数の範囲[*4](c251.xhtml#aA51)や文字の範囲も表現できます。

![](image_rsrcAT6.jpg)

コロンを使った記法以外に、range 関数を使っても範囲を作れます。range 関数には第1引数の開始値 start のほかに、終了値 stop・長さ length・ステップ幅 step のうち2つを指定します。この3つのうち指定していない引数は他の引数から自動的に計算されます。ステップ幅を省略した場合は、デフォルトで step = 1 になります。また、stop はキーワード引数ではなく第2引数としても渡せます。

![](image_rsrcAT7.jpg)

なお、Julia 1.7以降で range 関数の使い方が拡張されているので、詳しくは最新のドキュメントを参照してください[*5](c251.xhtml#aA8C)。

配列(5.1.3項)と異なり、範囲は個別の要素を保持しているわけではないので、とてつもなく長い範囲も一瞬で作成できます[*6](c251.xhtml#aA6D)。

![](image_rsrcAT8.jpg)

範囲もタプルと同様に角括弧を使って要素を参照できます。範囲のステップ幅は step 関数で取得できます。タプルと同様、範囲も要素の更新や追加・削除はできません。

# 5.1.3 配列

配列(array)は複数のオブジェクトをまとめて扱うためのコレクションです。タプルに似ていますが、配列には次元という概念があり、1次元配列はベクトルの表現として、2次元配列は行列の表現として使われます。なお、ここではオブジェクトを保持する基本的なコレクションとしての性質のみを解説しますので、多次元配列としての詳しい解説は[11.1節](c4WA.xhtml#a983)、線形代数での扱いについては[11.3節](c4WA.xhtml#a985)を参照してください。

配列を表現する代表的な型は Array 型です。このうち、1次元と2次元の配列は特によく使うため、それぞれに Vector と Matrix という別名が付けられています。

1次元配列(ベクトル)は次のように角括弧([])を使って作成します。タプルと同じように、最後の要素の後のコンマはあってもなくても構いません。

![](image_rsrcAT9.jpg)

2次元配列(行列)は各要素を次のように表形式で並べます。1次元配列の場合と異なり、コンマで要素を分けませんので注意してください。

![](image_rsrcATA.jpg)

配列もタプルや範囲と同じように角括弧を使って要素を参照できます。配列がタプルや範囲と異なるのは、次のように次元数と同じ数だけ添字を使える点です。

![](image_rsrcATB.jpg)

角括弧を使った方法以外に、関数を使って配列を作成する方法もあります。次に示す zeros 関数と ones 関数は、それぞれ第1引数に指定した型の0と1で初期化された配列を作ります。fill 関数は第1引数に指定した値で初期化された配列を作ります。より詳しくは[11.1.2項](c4WA.xhtml#a9PP)で扱います。

![](image_rsrcATC.jpg)

配列の次元数・サイズ(各次元の長さ)・要素数は、それぞれ次のように ndims 関数・size 関数・length 関数で取得できます。size 関数が返すのは次元数と同じ長さのタプルですが、第2引数に整数を渡すとその次元の長さを返します。

![](image_rsrcATD.jpg)

配列の要素は次のように更新できます。

![](image_rsrcATE.jpg)

存在しない要素の位置を参照しようとすると、次のように BoundsError 例外が送出されます。

![](image_rsrcATF.jpg)

1次元配列は次のように末尾や先頭に要素を追加したりできます[*7](c251.xhtml#aA5H)。表5.1に配列の基本的な操作をまとめます[*8](c251.xhtml#aA5J)。なお、表中の [!] は配列を更新する関数と更新しない関数の両方があることを意味します。

![](image_rsrcATG.jpg)

![](image_rsrcATH.jpg)

なお、配列といったとき、広義には Array 型のオブジェクトだけでなく、AbstractArray 型の下位型([8.1節](c3S5.xhtml#a97B))のオブジェクト全体を指します。これには、範囲([5.1.2項](c251.xhtml#a9D6))・ビット配列([11.1.13項](c4WA.xhtml#a9CG))・疎行列([11.4節](c4WA.xhtml#a986))などが含まれます。

# 5.1.4 辞書

辞書(dictionary)は、あるオブジェクトから別のオブジェクトへの対応関係を保持するコレクションです。辞書にはキー(key)と呼ばれる種類のオブジェクトと、値(value)と呼ばれる種類のオブジェクトがあり、辞書はキーと値のペアを複数保持します。現実の辞書が見出し語から意味の説明を引くときに使うように、Juliaの辞書もキーに対応する値を引くときに使います。

辞書は Dict 型で表現されます[*9](c251.xhtml#a9YT)。ほかにもいくつか辞書を表現する型がありますが、使用機会はあまり多くないので説明を割愛します。辞書を作る専用の構文(リテラル)は用意されていませんが、次のように Dict 型のコンストラクタとオブジェクトのペアを作る => 関数を使うのが一般的です。

![](image_rsrcATJ.jpg)

キーと値のペアやタプルがすでに配列などに入っているときは、それをそのまま Dict コンストラクタに渡せます。

![](image_rsrcATK.jpg)

空の辞書を作成するときは Dict() と書きます。また、次のように波括弧({})の内側にキーの型と値の型を書けます。

![](image_rsrcATM.jpg)

辞書では、角括弧の構文でキーに対応する値を引けます。キーが辞書にない場合、KeyError 例外が送出されます。

![](image_rsrcATN.jpg)

そもそもキーが辞書にあるかを知るには haskey 関数を使います。また、要素の追記や更新ができます。

![](image_rsrcATP.jpg)

キーを使って辞書内のペアを削除するには delete! 関数を使います。

![](image_rsrcATR.jpg)

辞書に存在しないキーを引いたとき、例外を送出するのではなくデフォルトを返すようにするためには、デフォルトを第3引数にとる get 関数を使います。また、そのとき同時にそのデフォルトを辞書に記録するには get! 関数を使います。

![](image_rsrcATS.jpg)

表5.2に辞書の基本的な操作を示します。

![](image_rsrcATT.jpg)

# 5.1.5 集合

集合(set)は配列のように複数の値を保持するコレクションですが、配列と異なり要素に順序はなく、同じ値の要素は1つの集合に1つまでしか保持できません。言い換えれば、集合はちょうど辞書のキーのみを集めたようなコレクションです。

よく使われる集合の型は Set 型です[*10](c251.xhtml#a9ZJ)。Set コンストラクタに配列など別のコレクションを渡して集合を作ります。

![](image_rsrcATU.jpg)

集合にはいくつかの集合演算が定義されています。集合の操作を表5.3にまとめます。なお、多くの場合、これらの操作は集合以外の配列や範囲でも動作します。

![](image_rsrcATV.jpg)

![](image_rsrcATW.jpg)

集合には、Set 型のほかに、整数の集合に特化した BitSet という型もあります。詳しくはドキュメントを参照してください。

# 5.1.6 ジェネレータ

ジェネレータ(generator)は少し特殊なコレクションです。ジェネレータは、直接的にオブジェクトを保持しているのではなく、必要に応じてオブジェクトを生成(generate)するコレクションです。その意味で、ジェネレータは範囲に似ています。

ジェネレータの型は Base.Generator 型ですが、その詳細はやや複雑なので、あまり型を意識しないで使うのがよいでしょう。ジェネレータを作成するには for キーワードを使います。ジェネレータは必要になったときに初めて要素のオブジェクトを生成するので、莫大な(あるいは無限の)要素を持つジェネレータも生成できます。

![](image_rsrcATX.jpg)

ジェネレータには生成するオブジェクトに条件を課すフィルタを付けられます。フィルタはジェネレータの最後に if キーワードを使って指定します。

![](image_rsrcATY.jpg)

さらに、ジェネレータはデカルト積(Cartesian product)を生成するときにも使えます。デカルト積のジェネレータは、フラットなものとネストしたもので次のように書き方が少し異なります。両者の具体的な違いはこのあとで示します。

![](image_rsrcATZ.jpg)

ジェネレータはそれ自体がオブジェクトです。本章の以降で紹介する関数の多くは、次のようにジェネレータを引数としてとれます。

![](image_rsrcAU0.jpg)

ジェネレータを丸括弧ではなく角括弧の内側に書くと配列を生成します。この構文を特に配列の内包表記(comprehension)と呼ぶことがあります。

![](image_rsrcAU1.jpg)

デカルト積を生成するジェネレータの違いは次のように配列の内包表記で現れます。フラットなジェネレータが1次元配列であるのに対し、ネストしたジェネレータが2次元配列であることに注意してください。

![](image_rsrcAU2.jpg)

ジェネレータを使ってタプル・辞書・集合などを生成することもできます。

![](image_rsrcAU3.jpg)

# 5.2 コレクションの操作

前節では標準ライブラリにある代表的なコレクションについて、その基本的な操作を説明しました。本節では、多くのコレクションに共通する操作を紹介します。

# 5.2.1 イテレーション

コレクションなどのオブジェクトに含まれる全要素を重複なく取り出す操作をイテレーション(iteration)と呼びます[*11](c251.xhtml#a9TT)。このとき、そのオブジェクトはイテレータ(iterator)と呼ばれます。配列の各要素の出現回数をカウントするなど、コレクションの全要素に何らかの処理をすることはよく行われるので、イテレーションはプログラミングに必要不可欠です。

本章で扱ったコレクションはすべてイテレータとして機能します。イテレータからはfor文を使って次のように要素を逐一取り出して処理できます。

![](image_rsrcAU4.jpg)

例として、イテレーションを使って配列(イテレータ)の要素の数を数えるコードを示します。

![](image_rsrcAU5.jpg)

イテレーションの仕方は関数を使って変えられます。例えば、辞書 dict をそのままイテレーションするとキーと値のペアに関するイテレーションになりますが、次のように keys 関数や values 関数を適用してイテレーションすると、それぞれキーのみのイテレーションと値のみのイテレーションになります。これは、keys 関数と values 関数が、それぞれキーと値専用のイテレータを作成するからです。

![](image_rsrcAU6.jpg)

表5.4にイテレータとともによく使われる関数を示します。特に、eltype 関数や length 関数は、それぞれ要素の型と数を取得するときによく使われます。collect 関数はイテレータを配列に変換するときによく使われます。これらの関数の使用例を次に示します。

![](image_rsrcAU7.jpg)

![](image_rsrcAU8.jpg)

foreach 関数は関数をイテレータの各要素に適用します。戻り値は nothing なので、出力などの副作用がある関数とともに使われます。

![](image_rsrcAU9.jpg)

より発展的なイテレータの関数は Iterators モジュールに定義されていますので、詳しくはドキュメント[*12](c251.xhtml#a9MU) を参照してください。イテレータの実装方法は[9.4.5項](c434.xhtml#a9MV)で解説します。

# 5.2.2 検索

コレクションから特定の条件を満たす要素やその位置を検索する関数を表5.5に示します。各関数の詳しい動作はドキュメントを参照してください。ここでは、findall 関数の使用例を示します。findall 関数は、配列にある true の位置を探すときによく使われます。また、第1引数に関数を渡すと、その関数を第2引数のコレクションの各要素に適用して、true を返す要素の位置を返します[*13](c251.xhtml#aA41)。

![](image_rsrcAUA.jpg)

![](image_rsrcAUB.jpg)

なお、Julia 1.7で導入された2引数をとる argmin 関数は、位置ではなくコレクションの要素を返すことに注意してください。位置が必要な場合は findmin 関数が有用です。argmax 関数についても同様です。

![](image_rsrcAUC.jpg)

# 5.2.3 変換

コレクションの要素を変換する関数を表5.6に示します。map! のように最後に ! が付く関数は、引数として渡されたコレクションを直接変換します。ここではよく使われる関数について、例を挙げて説明します。

![](image_rsrcAUD.jpg)

filter 関数と map 関数はともに第1引数に関数をとります。filter 関数は、引数の関数を要素に適用して true を返すものを返します。map 関数は、引数の関数を要素に適用してその結果を返します。

![](image_rsrcAUE.jpg)

sort 関数は要素を昇順に整列します。rev = true をキーワード引数として渡すと、要素を降順に整列します。要素の大小比較にはデフォルトで isless 関数が使われますが、lt キーワード引数を使えば変更できます。ほかにもいくつか動作を変えるキーワード引数が用意されているので、詳しくはドキュメントを参照してください。

![](image_rsrcAUF.jpg)

# 5.2.4 集約

コレクションの要素を集約して1つの結果を得る関数を表5.7に示します。集約の基本となる関数は reduce 関数です。reduce 関数は第1引数に2引数関数をとり、それをコレクションの要素間に適用します。次の例を確認してください。

![](image_rsrcAUG.jpg)

![](image_rsrcAUH.jpg)

reduce(⊕, A) の結果は、第1引数の2引数関数 ⊕ に結合則(associativity)が成立することを仮定していることに注意してください。すなわち、x ⊕ (y ⊕ z) と (x ⊕ y) ⊕ z が等しくなければなりません。これは reduce 関数では ⊕ を A の要素に適用する順序が決まっていないからです。先ほどの例では ⊕ が + 関数や | 関数であり、結合則が成立しているので問題ないわけです[*14](c251.xhtml#aA08)。一方、foldl 関数と foldr 関数は特定の順序で関数 ⊕ を適用するので、結合則が成立しなくても問題ありません。これらはそれぞれ次のように左と右から関数を適用します。

![](image_rsrcAUJ.jpg)

mapreduce 関数は、変換の map 関数と集約の reduce 関数を合成したような関数です。すなわち、変換用の関数 f と集約用の関数 ⊕ を引数にとり、f で変換した値を ⊕ で集約します。mapfoldl 関数や mapfoldr 関数も、それぞれ map 関数と foldl 関数や foldr 関数の合成と考えられます。また、all 関数や sum 関数などの集約関数は、第1引数に変換関数をとれます。これはコレクションの要素を変換しながら集約したいときに便利です。

![](image_rsrcAUK.jpg)

これらの集約関数は、初期値として init キーワード引数をとれます。init 引数が設定されないと、イテレータ A の最初の要素が代わりに使われます。A が空になりうるときは一般に init 引数が必要です。

# 5.2.5 複製

コレクションを複製する関数を表5.8に示します。

![](image_rsrcAUM.jpg)

copy 関数は次のようにコレクションを複製するので、複製元のコレクション A に加えた操作は複製先のコレクション B に反映されません。

![](image_rsrcAUN.jpg)

ただし、copy 関数はコレクションの要素は複製しないことに注意してください。つまり、複製元の要素と複製先の要素は共有されています。したがって、次のように複製元のコレクションの要素 A[1][1] に加えた操作は、複製先のコレクションの要素 B[1][1] にも反映されます。コレクションの要素まで含めて複製を作るには、copy 関数の代わりに deepcopy 関数を使います。deepcopy 関数はオブジェクト内のオブジェクトまで再帰的に複製します。

![](image_rsrcAUP.jpg)

# 5.3 コレクションの特性と使い分け

コレクションはそれぞれ特性に違いがあり、適切なものを目的に応じて選択する必要があります。本節では、使い分けの指針となるコレクションの特性を解説します。

# 5.3.1 コレクションの不変性と可変性

要素の追加・更新・削除など、コレクションの要素が変わる操作ができないコレクションを不変(immutable)なコレクションと呼びます。そうでないコレクションは可変(mutable)なコレクションです。本章で紹介したコレクションでは、タプル・範囲・ジェネレータが不変なコレクションであり、配列・辞書・集合が可変なコレクションです。なお、独自の不変な配列を提供しているパッケージ[*15](c251.xhtml#aAB1)などもありますので、配列ならば常に可変というわけではありません。

タプルなどの不変なコレクションでは、setindex! 関数などで要素を入れ替える操作はできません。したがって、要素をコレクションに追加したり、要素の順番を入れ替えたりするなどの操作が必要な場合は、タプルや範囲の代わりに配列を使う必要があります。同様のことは名前付きタプルと辞書にもいえます。

![](image_rsrcAUR.jpg)

注意してほしいのは、「コレクションが不変性を持つ」ことは「そのコレクションの要素が不変性を持つ」ことを意味しないということです。例えば、可変な配列を要素に持つ次のようなタプルがあるとき、その配列に要素を加えたりする操作はまったく問題ありません。

![](image_rsrcAUS.jpg)

# 5.3.2 タプルと配列の使いどころ

タプルと配列(特に1次元配列)はほとんど同じ構造を持ちますが、タプルは不変なコレクションなので要素を変更できません。すると常に配列を使えばよいのではないかと考えるかもしれませんが、タプルには配列にはない重要な特性がいくつかあります。

まず、タプルは非常に軽量なオブジェクトです。多くの場合、要素数があらかじめ決まっているタプルを作成するコストはほとんどありません。例えば、次の2つの関数の実装を比べると、タプルを返す前者のほうが一般に効率的です。

![](image_rsrcAUT.jpg)

また、タプルは要素の型が互いに同じではない場合にも向いています。詳しくは[第18章](c7N0.xhtml)などで説明しますが、Juliaの処理系はオブジェクトの型を正確に推論できるときに強力な最適化を行います。タプルの型は個々の要素の型を記録しているので、各要素の型が異なっていても問題になりません。一方、配列の型は要素の型を個別には記録していないので、型が異なる要素が同じ配列にあると最適化がうまくできないことがあります。

逆に、要素数が実行するまで決まらないとき、要素数が多いとき、要素の型がすべて同じであるときなどは、タプルより配列のほうが適切なことが多いでしょう。特に、数値計算などで大規模な浮動小数点数のデータを扱うときは、ベクトルや行列としても扱える配列が好まれます。

同様のことは名前付きタプルと辞書の選択でもいえます。名前付きタプルも非常に軽量で要素の型を個別に記録しています。したがって、要素数と名前があらかじめ決まっているときや、要素の型が互いに異なるときには、辞書ではなく名前付きタプルの使用が推奨されます。

# 5.3.3 外部ライブラリの選択肢

本章で紹介した標準ライブラリのコレクション以外にも、外部ライブラリには多種多様な用途のコレクションが用意されています。その中で特に重要なパッケージがDataStructures.jlです。DataStructures.jlには、スタック・キュー・両端キュー(deque)といった、有用ながら標準ライブラリには含まれていないコレクション(データ構造)をはじめとする多数のコレクションが用意されています。

StaticArrays.jlはサイズが固定の配列(特にベクトルや行列)に特化した処理を提供することで、コンパイラによる強力な最適化を支援します。したがって、サイズがあらかじめ決まっている比較的小さな配列を扱うときには、StaticArrays.jlの提供する配列を使えば計算が大幅に高速化することがあります。

表形式のデータを収めるときに使うデータフレームはDataFrames.jlから提供されています。DataFrames.jlはデータフレームの実装としてJuliaコミュニティで最もよく使われています。

まとめ

本章では、タプル・範囲・配列・辞書・集合・ジェネレータの6つのコレクションを中心に説明をしました。イテレータはfor文とともに使われることが多く、以降の章でも何度も登場する重要な概念です。コレクションの使い分けは必要不可欠でありますが、慣れないと難しいものでもあります。変換や集約操作を簡潔に行うには、本章で扱った関数をよく把握しておくとよいでしょう。不変性と可変性はコレクションを選ぶときの重要な概念なので、よく理解しておいてください。タプルと配列は混同されて不適切に使われがちですので特に注意が必要です。

注

[*1](c251.xhtml#a9HR) コンマがないと他の構文と区別できないのが理由です。[*2](c251.xhtml#aABT) Pythonなど、いくつかの言語では0番目から始まります。[*3](c251.xhtml#a9ZF) Pythonの範囲と異なり、終端を含むことに注意してください。[*4](c251.xhtml#a9VM) 浮動小数点数には表現誤差([4.4.4項](c1JX.xhtml#a9VN)を参照)があることに注意してください。[*5](c251.xhtml#a9RW) 例えば、Julia 1.7で range(0, stop = 1, length = 51) は range(0, 1, 51) とも書けるようになりました。[*6](c251.xhtml#a9J8) Python 2の range 関数や R のコロン演算子は範囲内の全要素を収めたリストや1次元配列を作ります。Juliaの範囲やPython 3の range 関数が作る範囲とは性質が異なるので注意してください。[*7](c251.xhtml#a9U6) 2次元以上の配列ではできません。[*8](c251.xhtml#a9K3) 集合とみなしたときの操作は[5.1.5項](c251.xhtml#a9K4)の表5.3に示しています。[*9](c251.xhtml#a9DY) Dict 型の実装はハッシュテーブル(hash table)です。キーは hash 関数でハッシュ化されます。[*10](c251.xhtml#a9MK) Set 型の実装は Dict 型と同様です。[*11](c251.xhtml#a9XB) コレクションに限らず、例えばテキストファイルから1行ずつ文字列を読む操作もイテレーションといいます。[*12](c251.xhtml#a9A5) <https://docs.julialang.org/en/v1/base/iterators/>[*13](c251.xhtml#aA7Z) 使用例の >(16) は引数の部分適用([7.7.10項](c315.xhtml#aA80)を参照)の例です。[*14](c251.xhtml#aAFW) 一般に2引数関数で結合則は成立しません。例えば、減算の - 関数や除算の / 関数などでは、普通は結合則が成立しません。1 - (2 - 3) と (1 - 2) - 3 は結果が異なることを確認してください。また、浮動小数点数のときは丸めの影響で + 関数でも結合則は成立しませんので注意が必要です。[*15](c251.xhtml#a9X3) 例えば、StaticArrays.jlパッケージの SArray 型は不変な配列です。

---

# ![](image_rsrcAUU.jpg)

文字列の処理は、今日のプログラミングでは必須の機能です。自然言語を扱える実用的なプログラミング言語は、まず間違いなく文字列のための型を用意しています。科学技術計算を主なターゲットとして開発されたJuliaも例外ではなく、開発初期から文字列を表現する型を提供してきました。本章では、Juliaで文字や文字列がどう表現されているのかを最初に説明し、その後で文字列処理に必要不可欠な部分文字列・正規表現・検索・変換などの扱い方を解説します。

# 6.1 文字の表現

文字列について解説する前に、まずは文字列と関係の深い文字(character)について説明をします。Juliaでは文字と文字列は厳密に使い分けられており、別々の型で表現されています。

計算機で扱う文字の一式を文字集合といいます。JuliaはUnicode(ユニコード)で定義された文字集合をサポートしています。Unicodeとは、文字や文字列を扱うための国際的な標準規格です。Unicodeはかつて乱立していた文字集合と符号化方式を統一し、現在のウェブでは事実上の覇者[*1](c2FB.xhtml#aA7N)となっています。

# 6.1.1 コードポイント

Unicodeの各文字には、コードポイント(code point)と呼ばれる0以上 10FFFF16 以下の整数が一意に割り当てられています。コードポイントはU+から始まる4桁か6桁の16進法の整数で表されます。例えば、英語アルファベットの最初の文字「a」には、U+0061というコードポイントが割り当てられており、漢字の「響」という文字にはU+97FFというコードポイントが割り当てられています。なお、本書ではコードポイントを示すときには、16進法を示す下付きの16を省略することにします。文字とコードポイントの対応関係はUnicodeの規格で定められており、勝手に新しい文字を割り当てることはできません。

Juliaには Char という文字を表現する型があります。Char 型の値は32ビットの固定長で、1つの値がUnicodeのコードポイントと一対一で対応しています。ただし、Char 型の値にコードポイントが直接収められているわけではなく、主に効率のためにUTF-8([6.2.4項](c2FB.xhtml#a9FK))という方式で変換されたビット列が収められているので注意してください。

# 6.1.2 文字のリテラル

基本的な文字のリテラルは、次のようにシングルクォート(')で文字を囲みます。REPLでは文字の後にコードポイントと[6.1.4項](c2FB.xhtml#a9Z7)で説明するカテゴリの情報が表示されます。

![](image_rsrcAUV.jpg)

改行などの制御文字は、C やPythonなどと同じようにバックスラッシュ(![](image_rsrcAGC.jpg))でエスケープした表現が使えます。NUL文字[*2](c2FB.xhtml#aA1M)は '![](image_rsrcAGC.jpg)0'、改行文字は '![](image_rsrcAGC.jpg)n' といった具合です。また、![](image_rsrcAGC.jpg)x や![](image_rsrcAGC.jpg)0 に続けて、それぞれ16進法と8進法で整数を指定できます。例えば、'![](image_rsrcAGC.jpg)t', '![](image_rsrcAGC.jpg)x09', '![](image_rsrcAGC.jpg)011' はすべてタブ文字(U+0009)を表します。

さらに、次のように![](image_rsrcAGC.jpg)u(または![](image_rsrcAGC.jpg)U)の後に16進法のコードポイントを続けると、あらゆるUnicodeの文字がリテラルで表現できます。表現したい文字が直接入力しにくいときは、文字集合の一覧表などを引いて、このようにコードポイントを直接使うとよいでしょう。

![](image_rsrcAUW.jpg)

# 6.1.3 文字の操作

文字と整数は、文字のコードポイントを使って加減演算ができます。

![](image_rsrcAUX.jpg)

文字の大小関係はコードポイントを使って比較されます。

![](image_rsrcAUY.jpg)

uppercase 関数と lowercase 関数でそれぞれ大文字と小文字への変換ができます。大文字と小文字の判定はそれぞれ isuppercase 関数と islowercase 関数で可能です。

![](image_rsrcAUZ.jpg)

convert 関数[*3](c2FB.xhtml#aA61)を使うと、コードポイントに対応する整数と Char 型のオブジェクトの間を相互に変換できます。

![](image_rsrcAV0.jpg)

コンストラクタを使っても同じことができます。

![](image_rsrcAV1.jpg)

また、codepoint 関数を使ってもコードポイントを取得できます。

![](image_rsrcAV2.jpg)

# 6.1.4 文字の分類

REPLで文字を表示したとき、図6.1の下線部の表示が気になった方がいるかもしれません。これは一般カテゴリ(general category)と呼ばれるもので、Unicodeで各文字についてその分類が定められています。

![](image_rsrcAV3.jpg)

一般カテゴリは、図6.2に示すように2文字でその分類を表します。

![](image_rsrcAV4.jpg)

1文字目が大分類で2文字目が小分類です。表6.1に示した文字の判別関数には、この一般カテゴリを使って文字を分類するものがあります。

![](image_rsrcAV5.jpg)

例えば、islowercase 関数は渡された文字がLlに分類されるときのみ true を返します。各関数の詳しい動作はドキュメントを参照してください。

![](image_rsrcAV6.jpg)

# 6.2 文字列の表現

文字列(string)は、人間や機械が読み書きするテキストを扱うデータです。文字と異なり、文字列は可変長の型です。長さゼロの空文字列から、数ギガバイトにいたるような長さのテキストまで、文字列は任意の長さのテキストを表現できます。

標準ライブラリでは、String 型が文字列を表現する代表的な型です。Char 型と同様、String 型も Unicode に準拠していますので、英語や日本語を含む世界中の言語のテキストを統一的に扱えます。

String 型の文字列は不変です。すなわち、文字列の末尾に別の文字列を連結したり一部を書き換えたりすることはできず、文字列オブジェクトを作った時点で表現するテキストの内容は固定されます。文字列を逐次的かつ効率的に構築するには、[6.4.6項](c2FB.xhtml#a9EX)で説明する IOBuffer 型を使うのが一般的です。

# 6.2.1 文字列のリテラル

基本的な文字列のリテラルは、次のようにダブルクォート(")で囲んだ表記です。文字列リテラルは複数行にわたっても問題ありません。

![](image_rsrcAV7.jpg)

文字のリテラルと同様、文字列のリテラルでもバックスラッシュ(![](image_rsrcAGC.jpg))を使ったエスケープが使用できます。

![](image_rsrcAV8.jpg)

文字列のダブルクォートは文字列の初めと終わりに3つ連ねることもできます。こうすると、次のように文字列中のダブルクォートをエスケープする必要はありません。

![](image_rsrcAV9.jpg)

文字列のリテラルには、raw が先頭に付いたraw文字列リテラル(raw string literal)というものがあります。この文字列は通常の文字列リテラルとは異なり、バックスラッシュがあってもエスケープ処理を行いません。例えば、raw"![](image_rsrcAGC.jpg)n" は改行1文字の文字列ではなく、バックスラッシュの後に英字「n」が続く2文字の文字列 "![](image_rsrcAGC.jpg)![](image_rsrcAGC.jpg)n" になります。raw 文字列リテラルの使用機会はあまり多くありませんが、例えばWindowsのパスなど、バックスラッシュを多用する文字列では便利です。

![](image_rsrcAVA.jpg)

# 6.2.2 文字列の補間

文字列リテラルの中に変数などの式を書いて、全体を文字列に変換することを文字列補間(string interpolation)といいます。補間する式は $( と ) で囲み、文字列リテラルに埋め込みます。

![](image_rsrcAVB.jpg)

補間できるのは変数のみではなく、関数呼出しなどを使った式も記述できます。文字列に埋め込まれるオブジェクトは string 関数を使って文字列の表現に変換されるので、次のように数値を埋め込むことも可能です。

![](image_rsrcAVC.jpg)

補間する式が単純な変数のときには、"✓$x" のように丸括弧を省略できます。また、raw文字列リテラルでは $ はドル記号の文字と解釈されるので補間は行われません。

# 6.2.3 文字列の比較

文字列の等価性を判定する == 関数が定義されています。大文字と小文字は異なる文字として扱われます。

![](image_rsrcAVD.jpg)

文字列は辞書式順序(lexicographical order)に従って順序付けられています。すなわち、先頭から順に文字を比較して文字列の順序を決定します。

![](image_rsrcAVE.jpg)

Unicodeの文字列を正しく比較するときには、文字列の正規化が必要になることがあります。文字列の正規化については[6.2.8項](c2FB.xhtml#a9NC)で解説します。

# 6.2.4 エンコーディング

今度は文字列の中身に目を向けてみましょう。少し高度な話題ですが、頭の片隅に入れておくとさまざまな問題を回避できます。

前述したように、文字列ではUnicodeが定める文字集合が使えます。その文字の並びを計算機で扱えるようにバイト列に変換する方法をエンコーディング(encoding)と呼びます。1つの文字に対応するコードポイントはU+0000からU+10FFFFまでの値をとりうるので、コードポイントを表現しようとすると2バイト(16ビット)では少し足りず、4バイト(32ビット)必要になります[*4](c2FB.xhtml#a9M5)。素朴に考えると、文字列は4バイトのコードポイントを並べてエンコーディングすればよいように思えますが、実はより効率のよい方法があります。

そのような効率のよいエンコーディングの1つがUTF-8です。Unicodeのエンコーディングとしては最もよく使われています。例えば、"Julia 言語" という7文字からなる文字列をUTF-8でエンコーディングすると、図6.3に示すように、UTF-8では11バイトでエンコーディングできます。コードポイントをそのまま並べるエンコーディング[*5](c2FB.xhtml#a9S2)では4×7=28バイト必要なので、UTF-8を使えば約60%もメモリを節約できます。

![](image_rsrcAVF.jpg)

UTF-8の大きな利点は、ASCII(アスキー)という伝統的なエンコーディング方式と互換性があるということです。つまり、ASCIIでエンコーディングした文字列はそのままUTF-8としても扱えるのです。ASCIIは1文字が1バイトに対応する単純なエンコーディング方式であり、英数字と一部の記号[*6](c2FB.xhtml#aADZ)しかエンコーディングできませんが、長い歴史があり現在でもさまざまな場面で使われています。UTF-8がASCIIの上位互換であるおかげで、Juliaの世界では実質UTF-8にのみ気を配ればよいのです。

UTF-8はASCIIより広範な文字集合を扱えるという利点がありますが、それに伴う難しさもあります。ASCIIは1文字を1バイトでエンコーディングする単純な方式ですが、UTF-8では1文字のエンコーディングに使うバイト数に幅があり、最小1バイトから最大4バイトまで使います。これを可変長エンコーディング(variable-length encoding)と呼びます。ある文字を何バイトでエンコーディングするかは文字によって決まっています。例えば、ギリシャ文字やキリル文字は1文字あたり2バイト、日本語のひらがなや漢字は3バイトを使うことが多いですが、例外もあります。UTF-8は優れたエンコーディングではあるのですが、ASCIIと比べると1文字1バイトというわかりやすい関係が崩れるので、少し注意が必要です。以降でもう少し詳しく説明します。

ある文字列が何文字で構成されているかという情報は、length 関数で取得できます。

![](image_rsrcAVG.jpg)

他方、ある文字列が何バイトで構成されているかという情報は、sizeof 関数で取得できます。

![](image_rsrcAVH.jpg)

"Julia" はASCII文字のみからなる5文字の文字列なのでUTF-8でも5バイトでエンコーディングされていますが、漢字を含む "Julia 言語" は7文字で11バイトです。これは "言語" の2文字がそれぞれ3バイトで合計6バイトであり、5バイトの "Julia" と合わせると全体で5+6=11バイトになるからです。sizeof 関数の戻り値が length 関数の戻り値の倍数にならないのは、UTF-8のような可変長エンコーディングの特徴です。

# 6.2.5 エンコーディングの有効性

UTF-8のエンコーディングでは、すべてのバイト列が有効な文字列というわけではありません。例えば、0xc0 と 0xaf の並びはUTF-8において無効な文字列です。String 型は無効なエンコーディングの文字列を作ることを禁止しておらず、外部から与えられたデータの有効性をチェックしないので、文字列オブジェクトが無効な文字列である可能性もあります。

文字列のエンコーディングの有効性をチェックするときには isvalid 関数を使います。この関数は次のようにUTF-8として有効な文字列のときのみ true を返します。

![](image_rsrcAVJ.jpg)

# 6.2.6 文字の取得

エンコーディングは文字列の構成要素である各文字を取り出すときに関係してきます。まずはASCII文字のみからなる文字列の場合を説明しましょう。"Julia" という文字列から4文字目の 'i' を取り出すには、次のように書けます。

![](image_rsrcAVK.jpg)

ASCII文字のみからなる文字列のときには、図6.4のように1文字と1バイトが対応しているので、文字の取得は単純です。

![](image_rsrcAVM.jpg)

一方、漢字を含む "Julia言語" という文字列から7文字目の '語' を取り出そうとしても、先ほどのようにはいかずエラーが発生します。

![](image_rsrcAVN.jpg)

ここで '語' を取り出すには、 s[7] ではなく s[9] と書く必要があります。

![](image_rsrcAVP.jpg)

そのわけは、文字列に対する添字の位置は文字単位ではなくバイト単位で指定する必要があるからです。そして、ある文字を取得するには、添字をその文字の最初のバイト位置に揃える必要があります。図6.5は "Julia言語" という文字列をUTF-8でエンコーディングしたバイト列です。この図からわかるように、文字列の7文字目にある '語' という文字を取り出すには、その文字が始まる9バイト目を指定する必要があるわけです。

![](image_rsrcAVR.jpg)

このように、文字列から何文字目を取り出すという操作は、文字列の中身によって正しい位置が変わるので、ASCII文字のみからなる文字列とわかっているとき以外はおすすめしません。代わりに、後続文字の位置を取得する nextind 関数と、先行文字の位置を取得する prevind 関数を使うことを推奨します。

![](image_rsrcAVS.jpg)

文字列の添字の最初と最後の位置はそれぞれ firstindex 関数と lastindex 関数で取得できるので、例えば文字列を1文字ずつスラッシュで区切って出力するコードはリスト6.1のように書けます。使用例は次のとおりです。

![](image_rsrcAVT.jpg)

![](image_rsrcAVU.jpg)

なお、文字列は文字のイテレータ([5.2.1項](c251.xhtml#a9WT))なので、1文字ずつ取り出す操作だけなら次のように簡単に書けます。

![](image_rsrcAVV.jpg)

また、文字列を構成するバイト列を直接参照するときには codeunit 関数を使います。

![](image_rsrcAVW.jpg)

# 6.2.7 部分文字列

文字列 s の一部を切り出すには s[start:stop] という書き方ができます。なお、このときも start と stop の位置を文字の最初のバイト位置に揃える必要があります。

![](image_rsrcAVX.jpg)

この書き方は新しくメモリを確保し、そこに元の文字列のデータをコピーして、新しい String 型の文字列を作ります。この処理は十分に高速ですが、それでも繰り返し実行すると無視できないコストになります。そこでコピーを作らない部分文字列がよく使われます。

部分文字列(substring)とは、コピーを作らずに元の文字列の連続する範囲を切り出せるデータ構造です。部分文字列は文字列のコピーを避けてメモリ割当てを削減する目的で使われます。標準ライブラリでは SubString という型が部分文字列を表現するときに使われます。ほとんどの場面で SubString 型は String 型と同じように振る舞うので、部分文字列の存在を意識することはあまりありませんが、String 型と異なる型があるということは頭の片隅には入れておくとよいでしょう。

SubString コンストラクタを使えば、次のように String 型の文字列から指定した範囲の部分文字列を構築できます。同じことは @view マクロでも実現できます[*7](c2FB.xhtml#a9W8)。元の文字列をコピーする必要がないので、先の書き方に比べて高速です。

![](image_rsrcAVY.jpg)

部分文字列がなぜコピーを必要としないかは、部分文字列の仕組みがわかると納得できるので、内部構造を簡単に説明しましょう。SubString 型のオブジェクトには、次の3つの情報(フィールド)が記録されています。

1\. 元の文字列を参照する string フィールド

2\. 部分文字列の開始位置を保持する offset フィールド

3\. 部分文字列の長さを保持する ncodeunits フィールド

SubString オブジェクトの内部構造を図6.6に模式的に示します。このように、SubString オブジェクトは元の文字列への参照・開始位置・長さの情報のみを保持し、中身のバイト列を直接保持していないので、元の文字列がどれほど長くても、部分文字列の構築は非常に軽量です。

![](image_rsrcAVZ.jpg)

[6.4.3項](c2FB.xhtml#a9C2)で紹介する chomp 関数など、一部の文字列処理関数は部分文字列を返します。chomp 関数は文字列の末尾にある改行を取り除きますが、文字列は不変なので改行文字を直接取り除くことはできません。もちろん、末尾の改行文字以外の部分をコピーして新しい文字列を作ることもできますが、代わりに部分文字列を使えば、元の文字列をコピーせずに済みます。

![](image_rsrcAW0.jpg)

以降、特別に区別が必要ないときは、表現を簡潔にするため部分文字列も含めて文字列と呼びます。

# 6.2.8 文字列の正規化

さて、ここまでは「文字」という言葉を日常的な意味で使ってきましたが、直感に反した振る舞いをすることがあります。例えば、length("![](image_rsrcAW1.jpg)") の値は何になるでしょうか。実は、この値は1になることも2になることもあります。

![](image_rsrcAW2.jpg)

この2つの文字列は、どちらも英字「a」(U+0061)の上にサーカムフレックス(U+0302)が付いたものですが、前者はこれを合成済み文字(precomposed character)で表した1文字(U+00E2)からなる文字列であるのに対し、後者は通常の文字と結合文字(combining character)の組合せで表した文字列です。すなわち、Unicodeでは表す文字列が等価であっても、表現の方法が複数ある場合があるのです。

同じような例は日本語でも作れます。例えば、ひらがな「が」(U+304C)はひらがな「か」(U+304B)と濁点(U+3099)をつなげた文字としても表現できます。

![](image_rsrcAW3.jpg)

このようなとき、文字列の見た目が同じであるにもかかわらず、s1 == s2 は false を返します。これは、Juliaの文字列比較はUTF-8でエンコードされたバイト列を比較するからです。

Unicodeでは、正規化(normalization)という等価な文字列の表現を等しくする処理が定義されています。Unicodeで定義されている正規化形式には、Normalization Form D(NFD)・Normalization Form C(NFC)・Normalization Form KD(NFKD)・Normalization Form KC(NFKC)の4つの形式があります。それぞれの詳細を説明するのは本書の範囲を超えるので、例えば“Unicode Standard Annex #15”[*8](c2FB.xhtml#aABN)などを参照してください。

Unicode文字列の正規化は、標準ライブラリであるUnicode.jlパッケージから提供されています[*9](c2FB.xhtml#a9BB)。先の例で s2 をNFCで正規化すると s1 と同じ表現になります。逆に s1 をNFDで正規化すると s2 と同じ表現になります。

![](image_rsrcAW4.jpg)

NFCとNFKCの違いは次のような場合に見られます。「平成30年」をNFCで正規化しても変化がありませんが、NFKCで正規化すると「平成」(U+337B)は「平」と「成」の2文字に分割され、全角数字はASCIIの半角数字に変換されます。

![](image_rsrcAW5.jpg)

Juliaは外部から受けとったテキストを勝手に正規化することはありませんので、必要ならばプログラマが明示的に行わなければなりません。もしUnicodeの文字列を扱っていて奇妙な問題[*10](c2FB.xhtml#a9JZ)に遭遇したら、文字列の正規化を思い出してください。

# 6.3 正規表現

正規表現(regular expression)は、文字列があるパターンを含むかを判定したり、文字列にある特定のパターンを抽出・変換したりするのに使える便利なプログラミング言語です。例えば次のようなパターンが正規表現で記述できます。

●4桁の整数("1234", "8101" など)

●1文字以上の連続する空白(" ", "![](image_rsrcAGC.jpg)t![](image_rsrcAGC.jpg)n" など)

●T または t で始まり、s で終わる単語("This", "thesaurus" など)

こうしたパターンをJuliaのコードのみで表現しようとするとやや面倒ですが、正規表現を使えば、先の3つのパターンはそれぞれ次のように簡潔に記述できます。

●r"![](image_rsrcAGC.jpg)d{4}"

●r"![](image_rsrcAGC.jpg)s+"

●r"[Tt]![](image_rsrcAGC.jpg)w*s"

現代的なプログラミング言語の多くは正規表現を備えており、Juliaも標準ライブラリで正規表現を提供しています。正規表現はソースコードに埋め込まれて使われますが、Juliaとは独立したプログラミング言語です。真核生物の細胞内で共生するミトコンドリアのように、正規表現はJuliaを含むさまざまなプログラミング言語に取り込まれた特異なプログラミング言語だといえます[*11](c2FB.xhtml#aA7W)。

正規表現の文法にはいくつもバリエーションがありますが、Juliaが採用したのはPerlで使われているPerl Compatible Regular Expression(PCRE)という正規表現なので、すでにPerlで馴染みがある人はすぐに覚えられるでしょう。PythonやRubyで採用されている正規表現と微妙な違いはありますが、基本は共通しています。

正規表現はそれだけで1冊の本が書けるほど豊富な機能を持ちます。また、正規表現は理論的にも実装的にも非常に興味深い対象なのですが、本節では文字列の処理でよく使われる代表的な機能を紹介するに留めます。詳細については、Juliaが使用しているPCRE2ライブラリのドキュメント[*12](c2FB.xhtml#a9HM)やその他の書籍を参考にしてください。

# 6.3.1 正規表現の基本

正規表現では、あるパターンを正規表現で記述し、そのパターンが対象の文字列に現れるか、現れるとしたらどこに現れるのかなどの検索が行えます。こうした操作を広い意味でマッチング(matching)と呼びます。

正規表現のパターンは、r"..." のように文字列リテラルの前に r を付けて記述します[*13](c2FB.xhtml#a9X0)。次の occursin 関数はパターンと検索対象の文字列を受けとり、そのパターンが文字列中に出現(マッチ)するかを真偽値で返します。なお、引数の順序が逆の contains 関数もあります。まずは最もシンプルな、普通の文字のみからなるパターンで試してみましょう。

![](image_rsrcAW6.jpg)

普通の文字のみからなるパターンは面白みがありません。正規表現では、特殊な意味を持つメタ文字(metacharacter)を使って複雑なパターンを記述できます。パイプ(|)はメタ文字の一種であり、p1|p2 のように書くと p1 か p2 のどちらかにマッチします。次の例では、パターンは Julia か Python を含む文字列にマッチします。

![](image_rsrcAW7.jpg)

パイプを含む次の12文字が正規表現のメタ文字一覧です。ただし、後述するように [ と ] の間ではメタ文字が一部異なります。正規表現のパターンにこれらの文字を見つけたら、特別な意味があると考えてください。

![](image_rsrcAW8.jpg)

メタ文字の意味は次のとおりです。

エスケープ ![](image_rsrcAGC.jpg)

バックスラッシュは(![](image_rsrcAGC.jpg))さまざまなエスケープ処理に使われます。例えば、メタ文字と同じ文字にマッチするパターンを記述するには、その文字をバックスラッシュでエスケープします。

![](image_rsrcAW9.jpg)

メタ文字のエスケープ以外には、次のような用途でも使われます[*14](c2FB.xhtml#a9YU)。

●![](image_rsrcAGC.jpg)n, ![](image_rsrcAGC.jpg)r, ![](image_rsrcAGC.jpg)t などのASCII文字のエスケープ

●![](image_rsrcAGC.jpg)s (空白の文字), ![](image_rsrcAGC.jpg)d (10進法数字), ![](image_rsrcAGC.jpg)w (単語文字)

●![](image_rsrcAGC.jpg)xhh の16進法コードポイント(hh がコードポイント)

先頭と末尾 ^ $

キャレット(^)は文字列の先頭、ドル($)は文字列の末尾にマッチします。これらは文字自体ではなく位置にマッチングする特殊なパターンです。

![](image_rsrcAWA.jpg)

任意の文字 .

ドット(.)は改行以外の任意の文字にマッチします。例えば、次のパターンにある3つのドット(...)は、1つ目の Julia では uli に、2つ目の Jenga では eng にマッチします。3つ目は最後の文字が一致しないのでマッチしません。

![](image_rsrcAWB.jpg)

なお、デフォルトでは改行文字にマッチしないので注意してください。

![](image_rsrcAWC.jpg)

文字クラス [

文字の集合をパターンとし、集合に含まれるどの文字にもマッチします。文字の集合を文字クラス(character class)と呼びます。例えば、[abc] というパターンは a, b, c のどの文字にもマッチします。ハイフン(-)を使うと、連続する文字の範囲を文字集合にできます。例えば、[a-z] というパターンは英字の a から z までのどの文字にもマッチします。[A-Za-z0-9] のように、複数の範囲をまとめて書くこともできます。

![](image_rsrcAWD.jpg)

[ の直後に ^ を付けると、文字集合は補集合になります。例えば、[^0-9] は 0 から 9 の数字以外の文字にマッチします。また、[ と ] の間では、メタ文字は![](image_rsrcAGC.jpg) ^ - [ ] のみになります。ただし、^ は [ の直後でのみメタ文字です。

選択 |

パイプ(|)で区切られたパターンのいずれかにマッチするパターンを作ります。例えば、foo|bar は foo もしくは bar にマッチします。3つ以上のパターンは foo|bar|baz のような書き方もできます。

グループ ()

パターンをまとめて1つのパターンを作ります。例えば、次のように google, microsoft, netflix をパイプで区切ってグループ化すると、.com の直前のみが入れ替えられるようになります。もし ( と ) を外すと、google や microsoft という文字列にもマッチするようになります。

![](image_rsrcAWE.jpg)

量化子 * + ? {}

量化子(quantifier)は直前のパターンを何回繰り返すかを指定して、新しいパターンを作ります。アスタリスク(*)は0回以上、プラス(+)は1回以上、クエスチョン(?)は0回か1回、波括弧({)は回数を整数で指定できます。最初の3つの量化子については、次の例で動作を確認してください。なお、AB*C は (AB)*C ではなく、A(B*)C と解釈されることに注意してください。+ と ? も同様です。

![](image_rsrcAWF.jpg)

{ は } とペアになり、次のように繰り返しの最小回数と最大回数を指定できます。

●p{m} はパターン p をちょうど m 回繰り返すパターン

●p{m,} はパターン p を m 回以上繰り返すパターン

●p{m,n} はパターン p を m 回以上 n 回以下繰り返すパターン

なお、* は {0,} と、+ は {1,} と、? は {0,1} と等価ですので、機能的には *, *, + は冗長なのですが、短くわかりやすいのでよく使われます。

以上のメタ文字が提供する機能を組み合わせると、かなり複雑なパターンも正規表現で記述できます。具体例として、16進法整数のリテラルにのみマッチする正規表現を次に示します。なお、位どりに挿入できるアンダースコア(_)は先頭や末尾に現れたり、連続して現れたりすることができないことに注意してください。

![](image_rsrcAWG.jpg)

# 6.3.2 文字列の抽出

occursin 関数は、正規表現のパターンが文字列にマッチするかどうかだけを返しますが、現実にはパターンにマッチした文字列を取り出したいことがあります。そんなときに使うのが match 関数です。

match 関数は occursin 関数と同じように正規表現のパターンと文字列を引数にとりますが、マッチングが成功したときには RegexMatch 型のオブジェクト(マッチオブジェクト)を返します。パターンにマッチする文字列が見つからないときには nothing を返します。マッチオブジェクトはパターンが文字列のどの部分にマッチしたかを記録しているので、興味のある部分を文字列から抽出できます。郵便番号を抽出する次の例を見てください。

![](image_rsrcAWH.jpg)

match 関数の返すマッチオブジェクトには、match フィールドにパターンが最初にマッチした文字列、offset フィールドにはその文字列のバイト位置が収められています。

match 関数は第3引数にマッチングの開始位置をとれます。次にマッチする位置を見つけたいときは、match(pattern, text, m.offset + 1) のようにすればよいでしょう。nothing を返すまでこれを繰り返せば、すべてのマッチする文字列が得られます。

さて、郵便番号は前半3桁と後半4桁の2つの成分に分けられます。成分をそれぞれ別々に取り出したいとき、郵便番号全体を抽出した後分割してもよいのですが、もっとよい方法があります。それは、前半と後半の成分をそれぞれ丸括弧で囲ってグループ化し、マッチオブジェクトの captures フィールドからそれぞれにマッチした文字列を取り出す方法です。言葉で説明するより次の例を見たほうが早いでしょう。

![](image_rsrcAWJ.jpg)

この例からわかるように、マッチオブジェクトは丸括弧でグループ化されたパターンにマッチした文字列を captures フィールドに、その文字列の位置を offsets フィールドに配列として保持しています。captures フィールドの値は、m[1] や m[2] のようにその番号でも参照できます。パターン全体が複数の成分から構成されるようなときは、このように各成分を丸括弧でグループ化しておくと、後から分解する手間が省けます。

グループが多くなり番号で識別しづらいときには、名前付きグループが使えます。これは、(?<name>pattern) のようにグループに < と > の間に書いた名前を付けられる機能です。グループの名前でマッチした文字列を参照できるので、コードが読みやすくなります。次のURLを分解する例[*15](c2FB.xhtml#a9GD)で動作を確認してください。

![](image_rsrcAWK.jpg)

# 6.3.3 高度な正規表現の機能

ここでは正規表現の少し高度な話題を取り上げます。使用頻度はさほど高くないので、初めて読む場合は読み飛ばしても差し支えないでしょう。

# 正規表現の修飾子

正規表現の構文や機能の一部は、修飾子(modifier)を付けることで変更できます。修飾子は正規表現リテラルの後に付けます。例えば、i を修飾子として正規表現リテラルに付けると、大文字小文字の違いを無視してマッチします。

![](image_rsrcAWM.jpg)

修飾子の一覧を表6.2に示します。

![](image_rsrcAWN.jpg)

# 先読みと後読み

先読み(lookahead)パターンは、パターンマッチングを先(右)まで続けるが、そのマッチング結果を捨てるパターンです。先読みパターンは (?=pattern) と書き、次のように他のパターンに続けて書きます。このような書き方は、あるパターンが後続する特定のパターンを見つけ出したいが、その後続するパターン自体には興味がない場合に便利です。

![](image_rsrcAWP.jpg)

同じように、後読み(lookbehind)パターンは、パターンマッチングを後(左)に続けるが、そのマッチング結果を捨てるパターンです。後読みパターンは (?<=pattern) と書き、次のように他のパターンより前に書きます。

![](image_rsrcAWR.jpg)

= を ! に変えると否定パターンが作れます。先読みと後読みでは、この否定パターンのほうが利用機会が多いでしょう。例えば、次のパターンは mutable が前に付かない struct にマッチします。

![](image_rsrcAWS.jpg)

# 最短マッチング

量化子はデフォルトで最長のマッチングをしますが、量化子に ? を付けると最短マッチングを行います。

![](image_rsrcAWT.jpg)

# PCRE2の詳細

Juliaが使っているPCRE2の機能は、本書ですべてを紹介するにはあまりに膨大かつ複雑です。PCRE2で使える構文の完備なドキュメントはpcre2pattern man page[*16](c2FB.xhtml#aAD4)にあります。本書で紹介しきれなかった機能については、そちらを参照してください。

# 6.4 文字列処理

本節では、ここまでに説明した文字・文字列・正規表現を使って、実用的な文字列処理の方法を解説します。

# 6.4.1 数値のパース

parse 関数は数値の型と文字列を引数にとり、指定された型の数値を返す関数です。ユーザの入力したデータや、テキストファイルから数値を読み込むときなどに使われます。次の例を見れば、動作は明らかでしょう。

![](image_rsrcAWU.jpg)

文字列に不正な値があり正しくパースできないとき、parse 関数は ArgumentError 例外を送出します。例外は[7.6節](c315.xhtml#a976)で説明する方法でも捕捉できますが、tryparse 関数を使えばもっと手軽です。tryparse 関数は、パースが失敗すると、例外を送出する代わりに nothing を返します。

![](image_rsrcAWV.jpg)

# 6.4.2 文字列の検索

contains 関数は、特定のパターンが文字列に出現するかどうかを返します。なお、ここでは検索する文字・文字列・正規表現をすべてパターンと呼びます。

![](image_rsrcAWW.jpg)

occursin 関数は、contains 関数と引数の順序が逆です。また、startswith 関数と endswith 関数は、それぞれパターンが先頭と末尾に現れるかを返します。これらの引数の順序は英語の自然な語順に則っていることに注意してください。つまり、

●“text contains pattern”なので contains(text, pattern)

●“pattern occurs in text”なので occursin(pattern, text)

●“text starts with pattern”なので startswith(text, pattern)

●“text ends with pattern”なので endswith(text, pattern)

という引数の順になっているということです。いくつかの例を次に示します。

![](image_rsrcAWX.jpg)

パターンの出現回数をカウントするには count 関数を使います[*17](c2FB.xhtml#aA4A)。

![](image_rsrcAWY.jpg)

パターンの開始位置が必要なときには、findfirst 関数を使います。パターンが存在しないときは nothing を返します。

![](image_rsrcAWZ.jpg)

findnext 関数を使うと、特定の位置から検索を開始できます。次のように nothing を返すまで繰り返し呼び出せば、文字列中のすべてのパターンを網羅できます。

![](image_rsrcAX0.jpg)

findfirst 関数と findnext 関数には、それぞれ対になる findlast 関数と findprev 関数があります。findlast 関数と findprev 関数は、文字列を逆方向に(右から左に)検索します。残念ながら、逆方向の検索では正規表現をパターンとして使えません。

![](image_rsrcAX1.jpg)

findall 関数を使うと、パターンのすべての出現位置をベクトルで取得できます[*18](c2FB.xhtml#a9MW)。

![](image_rsrcAX2.jpg)

これらの検索関数には、第1引数に文字をとって真偽値を返す関数(表6.1を参照)をパターンとして受けとれるものがあります。このとき、関数が true を返した文字がマッチする文字になります。

![](image_rsrcAX3.jpg)

# 6.4.3 文字列の変換

replace 関数は、文字列に出現する特定のパターンを別の文字列に置換する最も一般的な関数です。変換は pattern => 変換後の形式で指定します。

![](image_rsrcAX4.jpg)

グループ化したパターンにマッチした文字列には、=> の右で順に![](image_rsrcAGC.jpg)1, ![](image_rsrcAGC.jpg)2 などで参照できます。このとき、=> の右の文字列は s"..." のように s を付けて置換文字列(substitution string)という特殊な文字列にする必要があります。

![](image_rsrcAX5.jpg)

strip 関数は文字列の両端にある空白を取り除く関数です。取り除かれる空白は isspace 関数で決まるので、狭義の空白文字(U+0020)だけでなく、タブ文字(U+0009)や改行文字(U+000A)も含まれます。strip 関数の派生として、左端のみから取り除く lstrip 関数と、右端のみから取り除く rstrip 関数があります。また、これらの関数には取り除く文字を指定するオプションもありますので、詳しくはそれぞれの関数のドキュメントを参照してください。

![](image_rsrcAX6.jpg)

chomp 関数は文字列の右端にある改行(![](image_rsrcAGC.jpg)n か![](image_rsrcAGC.jpg)r![](image_rsrcAGC.jpg)n)を取り除く関数です。rstrip 関数と異なり、改行以外の空白は取り除かれないので注意してください。

![](image_rsrcAX7.jpg)

lpad 関数と rpad 関数は、それぞれ文字列の左端と右端を空白文字で埋めた、固定長の文字列を作ります。数値などを右揃えや左揃えで出力するときなどに便利です。

![](image_rsrcAX8.jpg)

文字列の変換関数を表6.3に示します。詳細な動作はそれぞれの関数のドキュメントを参照してください。

![](image_rsrcAX9.jpg)

# 6.4.4 文字列の結合と分解

string 関数は引数のオブジェクトを文字列に変換し、それらを結合した文字列を作ります。引数の数に制限はありません。文字や文字列どうしであれば string 関数の代わりに * 関数でも結合できます。

![](image_rsrcAXA.jpg)

同じ文字や文字列を複数回繰り返して結合するには ^ 関数を使います。

![](image_rsrcAXB.jpg)

join 関数は配列やタプルの要素を結合して1つの文字列にします。第2引数に文字(列)を渡すと、その区切り文字(列)で結合します。加えて第3引数に文字(列)を渡すと、最後のペアのみその区切り文字(列)で結合します。

![](image_rsrcAXC.jpg)

split 関数は文字列を空白で分割します。第2引数にパターンを渡すと、そのパターンがマッチする部分で分割します。

![](image_rsrcAXD.jpg)

split 関数には2つのキーワード引数があります。limit キーワード引数は、分割する最大数を決定します。keepempty キーワード引数は、分割した後の空文字列を保持するかどうかを決定します。

![](image_rsrcAXE.jpg)

また、文字列の右端から分割し始める rsplit 関数も用意されています。右端から limit キーワード引数で指定した回数だけ分割したいときに使えます。

ファイルパスの操作については専用の関数が用意されています。[13.1.1項](c5Y6.xhtml#aA16)で解説しますので、そちらを参照してください。

# 6.4.5 テキストの入出力

一般的なデータの入出力は[13.2節](c5Y6.xhtml#a98E)で解説しますが、テキストの入出力をここで簡単に紹介します。テキストファイルを1行ずつ読み込む処理は eachline 関数を使うと便利です。この関数はファイルパスやストリームを引数にとり、1行ずつ文字列を読み込むイテレータを返します。改行文字はデフォルトで自動的に取り除かれます。

![](image_rsrcAXF.jpg)

readline 関数は最初の行を読んで返し、readlines 関数は全行を一度に読んで文字列の配列を返します。read 関数は次のように使うと改行を含むファイル全体を1つの文字列として返します。

![](image_rsrcAXG.jpg)

テキストの出力は print 関数か println 関数を主に使います。両者の違いは、後者が改行も出力する点だけです。

![](image_rsrcAXH.jpg)

print 関数と println 関数は、引数のオブジェクトを文字列化して出力するので、文字列のみならず数字や配列もそのまま渡せます。これらの関数はいくつでも引数をとれるので、出力したいオブジェクトごとに関数を呼び出す必要はありません。

![](image_rsrcAXJ.jpg)

# 6.4.6 効率的な文字列の構築

標準ライブラリには、IOBuffer という型のストリーム([13.2節](c5Y6.xhtml#a98E))があります。IOBuffer 型のオブジェクトは他のストリームと異なり、入出力用のストリームではありません。IOBuffer 型のストリームに書き込まれたデータはメモリ上のバッファに保持され、ターミナルにもファイルにも書き出されません。

IOBuffer 型の主な用途は文字列の構築です。文字列の補間・string 関数・join 関数など文字列を作る方法はいくつもありますが、IOBuffer 型を使えば文字列の逐次的な構築が可能です。

IOBuffer 型のオブジェクトは print 関数などで書き込まれたテキストのデータを記録しています。書き込んだデータを最後に take! 関数で取り出し、String コンストラクタで文字列化すれば、目的の文字列が構築できるというわけです。

![](image_rsrcAXK.jpg)

IOBuffer 型を使った文字列構築の優れた点は、中間データを最小限にしか必要としない点です。文字列は不変なので、文字列を作った後に書き換えることはできません。代わりに、IOBuffer が保持するメモリ上のバッファに書き込み、そのバッファを文字列に変換すると、中間の文字列を作らず効率的に新しい文字列を構築できます。

まとめ

本章では、文字と文字列に関わる機能を紹介しました。また、文字列処理で重要な正規表現にも触れました。文字列は単純なデータのようにも見えますが、その裏には多種多様な文字を扱うための工夫があり、それほど単純ではありません。特に、文字列を「文字の配列」程度に考えて不用意に添字を使ってしまうのは、よくあるバグの原因です。文字列がどう計算機で表現されているかをよく知れば、Juliaの文字列の表現方法や操作関数が自然に思えてくるはずです。

注

[*1](c2FB.xhtml#aA0B) 2021年1月の統計(<https://www.w3.org/International/questions/qa-who-uses-unicode>)によると、約96%のウェブサイトがUnicodeの符号化方式であるUTF-8を採用しています。[*2](c2FB.xhtml#aABW) C で文字列の終端位置を表現するのに使われる文字です。Juliaの文字列は終端位置を別の方法で持つので、特別な意味はありません。[*3](c2FB.xhtml#a9GN) convert 関数をユーザ定義型に拡張する方法を[9.4.3項](c434.xhtml#a9GP)で扱います。[*4](c2FB.xhtml#a9RG) 原理的には3バイトでも足りますが、計算機では少々扱いにくいサイズです。[*5](c2FB.xhtml#aAC6) UTF-32と呼ばれます。[*6](c2FB.xhtml#a9ZZ) ASCIIの文字一覧:<https://ja.wikipedia.org/wiki/ASCII>[*7](c2FB.xhtml#aACZ) @view マクロは配列にも使えます。詳しくは[11.1.9項](c4WA.xhtml#aA9E)を参照してください。[*8](c2FB.xhtml#aA6M) <https://www.unicode.org/reports/tr15/>[*9](c2FB.xhtml#a9WC) 内部の処理はutf8proc(<https://github.com/JuliaStrings/utf8proc>)というライブラリが担っています。[*10](c2FB.xhtml#a9W5) 例えば、macOSのファイルシステムである HFS+ では、ファイルパスがNFDのように正規化されていることがあり、ファイルパスを扱うときには注意が必要です。[*11](c2FB.xhtml#a9YH) 「Scheme:マクロの危険」<https://practical-scheme.net/wiliki/wiliki.cgi?Scheme%3A%E3%83%9E%E3%82%AF%E3%83%AD%E3%81%AE%E5%8D%B1%E9%99%BA>[*12](c2FB.xhtml#a9G9) <https://www.pcre.org/current/doc/html/index.html>[*13](c2FB.xhtml#aA6E) Regex 型のコンストラクタを呼び出して正規表現オブジェクトを作る方法もありますが、記述も長く非効率なので、正規表現のパターンを動的に生成する必要がない限り使われません。[*14](c2FB.xhtml#aA64) これらは全機能のごく一部です。詳しくは[https://www.pcre.org/current/doc/html/pcre2pattern.html#SEC5](https://www.pcre.org/current/doc/html/pcre2pattern.html#SEC5)を参照してください。[*15](c2FB.xhtml#aAAF) このURLに対する正規表現は、説明のため極めて単純化したものです。[*16](c2FB.xhtml#aAFU) <https://www.pcre.org/current/doc/html/pcre2pattern.html>[*17](c2FB.xhtml#a9S1) 文字に対する count 関数のメソッドはJulia 1.7で導入されました。[*18](c2FB.xhtml#aA6R) 文字に対する findall 関数のメソッドはJulia 1.7で導入されました。

---

# ![](image_rsrcAXM.jpg)

ここまでの章では、標準ライブラリにある基本的な型とその操作方法を解説してきました。本章から、Juliaでプログラムを書くために必要な言語の機能について詳細に解説します。本章では、最初の言語機能として変数とスコープの概念を紹介し、続いて基本構文について説明します。

# 7.1 変数

Juliaではあらゆるオブジェクトに名前を付けて参照できます。数値・文字列・配列といったデータはもちろん、型・関数・モジュールにも名前が付きます。そうしたオブジェクトに付く名前を変数(variable)と呼びます。変数は識別子(identifier)と呼ばれることもあります。

# 7.1.1 変数の命名規則

変数の名前(変数名)には、ソースコードに書けるものと書けないものがあります。例えば、英字とアンダースコア(_)からなる my_name という変数名は許可されていますが、間に空白文字が入る my name やハイフン(-)が入る my-name のような変数名は許可されていません。代表的なものとして、次のような変数名はすべて有効な変数名です。

●somename, SomeName, SOME_NAME などの英字とアンダースコアからなる名前

●tp53, h2o, rush! などの先頭以外に数字や ! を含む名前

●α, β, γなどのギリシャ文字からなる名前

●すわわ, 浮雲などのひらがな・カタカナ・漢字からなる名前

●+, *, ∨などの記号

最後の記号のみからなる変数名は奇妙に思えるかもしれませんが、実際に許可されている変数名です。例えば、次のコードはまったく問題なく動作します。

![](image_rsrcAXN.jpg)

もちろん、すべての記号が変数名として許されているわけではありません。例えばハッシュ(#)はコメントの開始位置を示すときに使われるので、変数名としては使えません。また、+somename+ や ** などのように、単独では変数名として有効でも、それらをつなげた変数名は有効ではないことがあります。記号の変数名は一部が特例として許されていると考えたほうがよいでしょう。

また、if や end などの構文で使われる名前はキーワード(keyword)または予約語(reserved word)と呼ばれ、変数名には使えません[*1](c315.xhtml#aADH)。Juliaでキーワードとなっている名前は次のとおりです。

![](image_rsrcAXP.jpg)

アンダースコアのみからなる名前(_ や __ など)も許可されていません。アンダースコアのみからなる名前は、次のように変数の代わりに置いて、値を読み捨てるときに用いられます。「変数を使い忘れているのではなく、意図的に使っていない」という意図を示すときに使うとよいでしょう。

![](image_rsrcAXR.jpg)

# 7.1.2 任意の変数名を作るマクロ

すでに述べた変数名の規則は、ソースコードにそのまま書くときの規則でした。Juliaには任意の変数名を作る var"..." マクロが用意されています。このマクロで書いた変数は、空白や記号なども含め、ほとんどすべての変数名を作れます。例えば、次のように普通には許可されていない空白文字を含む some name という変数名が作れます。

![](image_rsrcAXS.jpg)

[第15章](c6GX.xhtml)で見るように、この機能は異なる変数命名規則を持つ他のプログラミング言語を扱うときの利便性のために用意されています。例えば、R は data.frame のように変数名にドット(.)を使うことを許しているので、Juliaから参照するときは var"data.frame" のように使えます。

# 7.1.3 変数の代入

x = 42 のように、等号(=)を使って変数にオブジェクトを束縛することを変数の代入(assignment)と呼びます。代入は新しい変数を定義することもあれば、既存の変数の束縛を書き換えることもあります。どちらになるかは文脈に依存し、それを理解するには[7.2節](c315.xhtml#a972)で説明するスコープの理解が必要ですが、まずはスコープに関係しない代入の構文的な側面を先に説明しておきます。

最も単純な代入は、次のように1組の変数とオブジェクトを結びつける代入です。このとき、左辺の変数は右辺のオブジェクトに束縛され、代入後は変数を通じてオブジェクトを参照できるようになります。

![](image_rsrcAXT.jpg)

複数の変数への代入は一度に行えます。次の例では、代入を3回行ったときと同じ効果が得られます。

![](image_rsrcAXU.jpg)

この方法は、次のように関数が返すタプルの要素を別々の変数に代入したり、束縛された値を交換したりするときによく使われます。

![](image_rsrcAXV.jpg)

また、次のように複数の変数に同じオブジェクトを束縛する代入方法もあります。

![](image_rsrcAXW.jpg)

Julia 1.7で導入された便利な代入方法を紹介します。次のように (; foo, bar) = x と記述すると、x の2つのプロパティ foo と bar を同名の変数に代入する動作と等しくなります。これは構造体([8.6節](c3S5.xhtml#a97G))のフィールドの一部を関数内で変数に取り出すときに特に便利です。

![](image_rsrcAXX.jpg)

代入に使う等号は、直前に演算子をとると演算と代入を同時に行えます。これを複合代入演算(compound assignment operator)と呼ぶことがあります。次の例では、+= を使って加算と代入を同時に行っています。これは x = x + 2 と同じ動作をします。あくまで関数を適用した後に代入するという2つの操作の短縮記法であり、オブジェクト自体を書き換える操作ではないことに注意してください。

![](image_rsrcAXY.jpg)

代入について、1つ注意をしておきましょう。単純な代入は、左辺にある変数を右辺にあるオブジェクトに束縛するだけで、他の変数の束縛には一切関知しませんし、新しいオブジェクトを生成したりもしません[*2](c315.xhtml#a9A8)。次のコード例では、最初に x と y は同じオブジェクト 0 に束縛されていますが、x = 1 を実行した後では x と y は異なるオブジェクトに束縛されています。x = 1 という代入は y に何ら影響を及ぼしません。この様子を図7.1に示します。

![](image_rsrcAXZ.jpg)

![](image_rsrcAY0.jpg)

# 7.2 スコープ

変数には、その変数を参照可能なコード上の範囲があります。この参照可能な有効範囲のことを、その変数のスコープ(scope)といいます。特に、Juliaはレキシカルスコープ(lexical scope)と呼ばれるスコープ方式を採用しています。レキシカルスコープでは、ソースコードのみから変数のスコープを判断できます[*3](c315.xhtml#aAD1)。

スコープの話は小難しく、そのうえあまり面白いとはいえませんが、プログラミングをするうえでは必須の知識です。初めて本節を読むときには、Juliaを起動して説明を1つ1つ確かめながら読み進めることをおすすめします。スコープの動作を誤解して時間を浪費しないためにも、本節でスコープの挙動を確実に理解しておきましょう。

# 7.2.1 スコープの基本概念

細かい説明は後回しにして、最初にスコープに関わる用語を概説しておきます。変数のスコープは次の2種類に分類されます。

グローバルスコープ(global scope)

モジュール全体に参照範囲が及ぶスコープです。グローバルスコープはいかなるスコープにも包含されません。グローバルスコープを持つ変数をグローバル変数(global variable)と呼びます。

ローカルスコープ(local scope)

ソースコードの特定のブロック内に参照範囲が限定されるスコープです。ローカルスコープはグローバルスコープや他のローカルスコープに包含されます。ローカルスコープを持つ変数をローカル変数(local variable)と呼びます。

ある変数がどちらの種類の変数になるかは、原則として、どこで最初の代入が行われたかによって決まります。詳細は以降で述べますが、for キーワードや function キーワードなどで始まり、対になる end キーワードで終わるブロック内で初めて代入された変数はローカル変数であり、それ以外はグローバル変数です。また、module キーワードを使えば、独立したグローバルスコープを作成できます[*4](c315.xhtml#aA4Z)。[7.2.9項](c315.xhtml#aA50)で説明する global キーワードと local キーワードを使えば、特定の種類の変数に強制することも可能です。

具体例を通して、スコープの構造を確認しましょう。図7.2左のコードを見てください。

![](image_rsrcAY1.jpg)

一番上の module キーワードから一番下の end キーワードの間が1つのグローバルスコープです。これをコメントで示されているように G と呼ぶことにします。G の内側には、function キーワードから始まり end キーワードで終わるローカルスコープ L1 と L2 があります。さらに、L2 の内側には、for キーワードから始まり end キーワードで終わるローカルスコープ L3 と L4 があります。それぞれのスコープの先頭では1つずつ変数が定義されており、このコードには1つのグローバル変数 g と4つのローカル変数 l1 ~ l4 があることになります。

図7.2右には、スコープの包含関係をグラフで抽象的に示しています。入れ子になったスコープでは、グラフの矢印をたどって、包含関係の外側にあるスコープの変数を参照できます。例えば、L1 と L2 は G に包含されているので G の変数 g を参照できますし、L3 と L4 は L2 に包含されているので L2 の変数 l2 を参照できます。さらに、L3 と L4 は G にも間接的に包含されているので G の変数も参照できます。一方、スコープの意味から当然ですが、スコープの外側からはそのスコープの変数は参照できません。例えば、L1 は L2 の外側にあるので、L1 のどの位置からも L2 の変数 l2 や、さらに内側のスコープの変数 l3 と l4 は参照できません。

ここでは、スコープが入れ子の構造をとることと、スコープが変数の参照できる範囲を制限することを説明しました。以降では、グローバルスコープとローカルスコープの動作を詳しく解説します。

# 7.2.2 グローバルスコープ

グローバルスコープを作る要素は次の2つです。

1\. デフォルトモジュールの Main

2\. module キーワードや baremodule キーワードの作るモジュール

1つ目のデフォルトモジュールについてはここまで説明していませんでしたが、実はすでに何度も使っています。REPLやスクリプトは、デフォルトモジュールである Main モジュールのスコープ内にあるのです。Main モジュールはJuliaが暗黙的に作るモジュールなので、Main モジュールのスコープはユーザのソースコードとは無関係に最初からそこにあります。コードを書き始めるより前に module Main がすでにあって、そのグローバルスコープにコードを書いていると考えてもよいでしょう。

2つ目はユーザが明示的に作るグローバルスコープです。module キーワードまたは baremodule の位置から対になる end までスコープが続きます。Main モジュールは自動で作られ、baremodule キーワードはめったに使われないので、実質的に module キーワードのみがグローバルスコープを明示的に作る要素だと思ってよいでしょう。

グローバル変数はそのグローバルスコープと紐付くモジュールに属します。グローバル変数はスコープ内で直接参照できるのはもちろん、モジュールを経由しても参照できます。また、異なるモジュールに属するグローバル変数は、同じ名前で定義しても別の変数です。

![](image_rsrcAY2.jpg)

グローバルスコープで注意が必要なのは、グローバルスコープどうしは入れ子にできないということです[*5](c315.xhtml#a9ZT)。つまり、グローバルスコープを含むスコープというものは存在しません。これは入れ子にできるローカルスコープとは異なる性質です。次の例を見てください。デフォルトモジュールである Main モジュールで定義したグローバル変数 x は、その内側で定義された Sub モジュールからは直接参照できません。

![](image_rsrcAY3.jpg)

グローバル変数をそのスコープの外から参照するには、モジュールを経由する必要があります。例えば、先の例では Main モジュールを経由して Main.x とすれば、Sub モジュールの内側でも Main モジュールのグローバル変数 x を間接的に参照できます。ただし、この方法が使えるのは、Main モジュールがどこからでも参照できる性質を持った特殊なモジュールだからです。それ以外のモジュールにあるグローバル変数を参照するには、using文([7.9.3項](c315.xhtml#a9FP))などを使って、そのモジュールや変数を取り込む必要があります。

# 7.2.3 定数の定義

グローバル変数には、束縛が変化しないという制約を付けられます。この制約が付いたグローバル変数を特に定数(constant)と呼びます。定数を使うと、制約を受ける代わりに、コンパイラによる最適化の恩恵を受けやすくなります。また、ソースコードを読む人にとっても、変数の指すオブジェクトが変わらないことがわかれば、プログラムの動作を把握しやすくなる利点があります。

定数の定義には const キーワードを使います。定数名は慣習的に大文字を使うことが多いです。

![](image_rsrcAY4.jpg)

現在、このように定数にできるのはグローバル変数だけです。ローカル変数を const キーワードで定数にしようとすると構文エラーになります。あるグローバル変数が定数かは isconst 関数で確認できます。

# 7.2.4 ローカルスコープ

グローバルスコープとは対照的に、ローカルスコープを作る構文は多数あります。ローカルスコープを作る構文は次のとおりです。それぞれの構文については、以降で説明します。

●反復

for, while

●例外処理

try, catch, finally

●関数定義

function, do, ->(無名関数)

●構造体型定義

struct, mutable struct

●その他

let, macro, ジェネレータ

let文を例にローカル変数の定義を見てみましょう。let文はローカルスコープを作るための構文です[*6](c315.xhtml#aA42)。let文の作るローカルスコープは let キーワードから始まり、対になる end で終わります。このスコープ内で定義したローカル変数をスコープ外から参照する方法はありません。

![](image_rsrcAY5.jpg)

ローカルスコープを作る構文は多数あり、すべてを覚えるのは大変なので、むしろローカルスコープを作らない次の3種類の例外的な構文を覚えたほうが実用的です。

1\. if文 if, elseif, else

2\. ブロック begin

3\. モジュール定義 module, baremodule

3番目のモジュール定義はグローバルスコープを作るための構文なので、当然ながらローカルスコープは作りません。間違えやすいのが、if キーワードなどのif文と begin キーワードです。if文は使用機会も多いので、特に注意してください。次のように、if や begin から end の間で定義された変数はブロックの外からでも参照できます。

![](image_rsrcAY6.jpg)

# 7.2.5 ローカルスコープの入れ子

グローバルスコープは他のスコープに包含されませんが、ローカルスコープはグローバルスコープや他のローカルスコープに包含されます。このときの動作が少しわかりにくいので、注意深く説明します。

まずはグローバルスコープに包含されたローカルスコープを考えましょう。ローカルスコープを作る構文内での代入は、それを包含するグローバルスコープに同名の変数がすでに存在しても、新しいローカル変数を定義します。したがって、次の2つの変数 x は、名前こそ同じですが互いに独立した変数です。

![](image_rsrcAY7.jpg)

このようにローカルスコープで新しいローカル変数が定義され、グローバル変数を更新しないのは、ある意味で安全のための機能です。同名のグローバル変数を書き換えないように注意深くプログラミングするより、新しいローカル変数を定義してしまうほうがずっと簡単です。

続いて、他のローカルスコープに包含されたローカルスコープを考えます。ローカルスコープどうしの入れ子は、先ほどとは異なった動きをします。ローカルスコープの内側で別のローカルスコープを作り、内側のスコープで外側のスコープにあるローカル変数と同名のローカル変数を定義しようとしても、新しい変数にはなりません。言い換えれば、内側のスコープは外側のスコープにあるローカル変数を継承します。先ほどのコードと次のコードの動作の違いをよく確認してください。

![](image_rsrcAY8.jpg)

ローカルスコープどうしの入れ子でローカル変数を継承するのは、一見奇妙に見えるかもしれませんが、実用的な理由があります。例えば、次のような配列要素の最小値を返す minimum 関数を定義したと考えましょう。簡単のため、エラー処理などは省略しています。function と for の作るローカルスコープが入れ子になっていることに注意してください。もしローカル変数の継承がなかったら、for文の内側の res 変数はfor文のブロック内でのみ有効な変数になってしまうので、この関数は常に初期値の xs[begin] を返すでしょう。minimum 関数がこのように簡潔に書けるのは、ローカル変数の継承があるおかげです。

![](image_rsrcAY9.jpg)

まとめると、ローカルスコープでの代入は、外側のグローバルスコープには影響しませんが、外側のローカルスコープには影響します。なお、for の直後の変数(この例では変数 i)は動作が異なるので、詳しくは[7.5.2項](c315.xhtml#a9XF)を参照してください。

# 7.2.6 let文による変数のシャドーイング

let文には、すでにあるローカル変数を隠し、同名の新しいローカル変数を定義する機能があります。これを変数のシャドーイング(shadowing)といいます。例えば、次のように let キーワードの直後に改行せず x = ... と記述すると、この変数 x は外側の変数を継承せず、内側のlet文内でのみ使える新しいローカル変数の定義になります。

![](image_rsrcAYA.jpg)

等号の右側で外側のスコープの変数を参照できます。これはコードの途中で一時的に変数を切り替えたいときに使えます。

![](image_rsrcAYB.jpg)

let キーワードの後には let x = ..., y = ... のようにコンマで分けて複数の変数を定義することもできます。定義は左から順に評価され、例えば y = x * 2 のように、先に定義された変数を後の変数の定義で参照できます。

# 7.2.7 begin文によるブロックの構築

begin文はコードのかたまり(ブロック)を作るための構文です。begin キーワードで始まり、end キーワードで終わります。begin文は次のようにマクロに渡す式を1つにまとめるときなどによく使われます。

![](image_rsrcAYC.jpg)

[7.2.4項](c315.xhtml#a9AD)で述べたように、begin文はローカルスコープを作らないことに注意してください。ただし、マクロとともに使われると、そのマクロがスコープを作る可能性はあります。

# 7.2.8 変数の存在確認

コードのある場所で変数が定義されているかは、isdefine 関数か @isdefined マクロで確認できます。isdefined 関数はグローバル変数専用であり、第1引数にモジュールをとり、第2引数に変数名をシンボルでとります。@isdefine マクロはローカル変数にも使え、変数名のみを引数にとります。次の例では、x がグローバル変数として、y がローカル変数として定義されています。

![](image_rsrcAYD.jpg)

# 7.2.9 スコープの明示的な指定

ここまでに説明したように、変数のスコープは定義された場所で決まるのが基本ですが、global キーワードと local キーワードを使うと、変数のスコープを明示して、このデフォルトの動作を変更できます。

global キーワードは、後に続く変数がグローバル変数であることを明示し、その変数を現在のスコープとその内側のスコープにグローバル変数として導入します。例えば、次のように global キーワードを使うと、通常はローカル変数の定義である代入がグローバル変数の更新に変わります。global x の有無で動作がどう変わるかを確認してください。

![](image_rsrcAYE.jpg)

対して、local キーワードは後に続く変数がローカル変数であることを明示します。

![](image_rsrcAYF.jpg)

さらに、local キーワードは、ローカル変数を未初期化のまま導入するときにも使えます。リスト7.1の lastitem 関数は、for文のブロックが1回以上実行されたときのみ変数 last に値が束縛されます。

![](image_rsrcAYG.jpg)

![](image_rsrcAYH.jpg)

なお、global x, y のように複数の変数も指定できます。また、global キーワードや local キーワードによるスコープの明示と代入は、次のように同時に行えます。

![](image_rsrcAYJ.jpg)

# 7.2.10 対話的環境でのスコープ

ここまではソースコードをファイルに保存して実行したときの動作を説明しましたが、REPLのような対話的な実行環境では、利便性のためスコープの動作が少し異なることがあります。ここでその動作を解説しておきましょう。まずは次の2つのコードを比べてください。

![](image_rsrcAYK.jpg)

両者は最終的な変数 x の値が異なります。対話的環境ではfor文の中でグローバル変数 x を書き換えているのに対し、非対話的環境ではfor文の中で新しいローカル変数 x を定義しているので、グローバル変数 x には影響がないわけです。

これがどのような仕組みになっているかを詳しく説明しましょう。Juliaのローカルスコープは、ソフトスコープ(soft scope)とハードスコープ(hard scope)に分けられます。for, while, try はソフトスコープを作る構文ですが、function, let など他のローカルスコープを作る構文はハードスコープを作ります。対話的環境において、ソフトスコープはそのスコープ内で変数に代入するとき、その変数と同名の変数がすぐ外側のグローバルスコープにあれば、新しいローカル変数は作らず、そのグローバル変数を更新します[*7](c315.xhtml#aA7X)。これが先ほどの動作の違いが生じる理由です。他方、ハードスコープは対話的環境でも非対話的環境でも動作に差はなく、グローバル変数の有無にかかわらず常に新しいローカル変数を定義します。

ローカル変数に代入するときの動作を図7.3に示します。ただし、最初の分岐にある「現在のスコープで変数 x は定義されている」とは、global キーワードでローカルスコープに変数が導入されている場合を含みます。

![](image_rsrcAYM.jpg)

# 7.3 演算子

: や + など、何らかの演算(operation)を行う構文に使われる記号を演算子(operator)と呼びます。また、この演算の対象となるものを被演算子(operand)と呼びます。例えば、x + 1 では、+ が演算子であり、x と 1 が被演算子です。

Juliaにおいて、多くの演算子は関数でもあります[*8](c315.xhtml#a9TW)。例えば、+ という演算子は x + 1 と書くことが一般的ですが、+(x, 1) のように通常の関数呼出しのように使うこともできますし、foldl(+, 0:9) のように引数として関数呼出しに渡すこともできます。

被演算子が1つの演算子を単項演算子と呼び、被演算子が2つの演算子を二項演算子と呼びます。例えば、- 関数は -x では単項演算子ですが、x - y では二項演算子です。

演算子はここまでにもいくつか説明してきましたが、本節では演算子に関する重要な概念をいくつか紹介します。なお、多重ディスパッチを使って演算子の動作を定義する方法については、[9.4.6項](c434.xhtml#a9K2)を参照してください。

# 7.3.1 積の省略

2つの式の積を計算するとき、積の関数 * を省略できます。例えば、2 * x や 3 * (x + 1) は次のように簡潔に記述できます。数学の記法に似たこの短縮記法は、Juliaでは頻繁に使われます。

![](image_rsrcAYN.jpg)

数値リテラルに限らず、より広い範囲で * 関数の短縮記法が使えます。例えば、(x + 1)x は (x + 1) * x と等価な短縮記法です。一方、x(x + 1) は関数呼出し構文と解釈されるので、x * (x + 1) と等価ではありません。この短縮記法が実際に使われるのは、2n のような数値リテラルとの積か、リスト1.3で使用したような行列 A の転置 A' との積 A'X がほとんどです。

# 7.3.2 関数の演算子

関数に適用できる演算子がいくつかあります。! 演算子は、関数に前置すると、関数の真偽値を反転する関数を作ります。これは次のように使用できます。

![](image_rsrcAYP.jpg)

![](image_rsrcAR2.jpg)(![](image_rsrcAGC.jpg)circ)演算子は2つの関数を合成した関数を作ります。つまり、(f![](image_rsrcAR2.jpg)g)(x) は f(g(x)) と同じ結果を返します。

![](image_rsrcAYR.jpg)

|> 演算子を使うと、関数に渡す引数を関数より前に書けます。つまり、x |> f は f(x) と同じ結果を返します。f(g(x)) の代わりに x |> g |> f と書けるので、複数の関数を連続して適用するときに書きやすくなることがあります。

![](image_rsrcAYS.jpg)

# 7.3.3 優先順位と結合性

演算子には優先順位(precedence)と結合性(associativity)という性質があります。複数の演算子がある式において、まずは優先順位の高い演算子から被演算子とグループになり、優先順位が同じ場合は結合性の向きでグループが決まります。例えば、算数で習ったように、2+3×4は3×4の部分をまず計算し、その結果に2を加えます。掛け算を足し算より先に計算するのは、×が+より優先順位が高い演算子なので、3×4が優先的にグループを作るからです。同様に、1+2-3 は1+2を先に計算しますが、これは+と-が同じ優先順位を持ち、+が左結合的[*9](c315.xhtml#aA9P)だからです。

これらはJuliaの計算式でも同じです。例えば、* が + より高い優先順位を持つので、2 + 3 * 4 は 2 + (3 * 4) と等価になりますし、+ と - は優先順位が等しく左結合的なので 1 + 2 - 3 は (1 + 2) - 3 と等価になります。演算子の優先順位と異なる式を組み立てるには、次のように丸括弧を使って演算の順位を明示します。

![](image_rsrcAYT.jpg)

複数の比較を同時に行うとき、比較演算子は次のように連続して使用できます。このとき、結合性は存在せず、全体を1つの演算と考えることに注意してください。例えば、0 < x < 5 は (0 < x) < 5 や 0 < (x < 5) とは異なる意味を持ちます。

![](image_rsrcAYU.jpg)

演算子の優先順位と結合性の一覧表を表7.1 に示します[*10](c315.xhtml#a9V5)。演算子は優先順位が高いほうから並べられています。すべてを覚える必要はありませんが、前述の算術に使われる演算子の優先順位や、: は - より優先順位が低いので 1:n-1 は 1:(n-1) と等価であることなどは頻繁に使われるので、記憶しておくとよいでしょう。

![](image_rsrcAYV.jpg)

# 7.4 条件分岐

プログラムにおいて、条件に応じて実行するパスを動的に変更する機能は必要不可欠です。この仕組みを条件分岐(conditional branch)と呼びます。Juliaには、次の3つの構文が条件分岐のために用意されています。

1\. 短絡評価の && と ||

2\. 条件演算子の :?

3\. if文(if, elseif, else などからなる文)

本節では、これらの構文を順に解説します。

# 7.4.1 短絡評価

短絡評価(short-circuit evaluation)は、条件式の評価結果に応じて別の式を評価するか否かを制御する仕組みです。&& と || が短絡評価の演算子として用意されています。cond && x は cond が true に評価されたとき、かつそのときに限り x を評価し、その結果を返します。cond || x は逆に cond が false に評価されたとき、かつそのときに限り x を評価し、その結果を返します。次の例では、1つ目と4つ目の println 関数だけ実行されます。

![](image_rsrcAYW.jpg)

どちらの演算子も、条件式 cond は必ず Bool 型の真偽値に評価されなければなりません。C やPythonなどのように、条件式が整数や文字列になることは許されていないので注意してください。これは他の条件分岐構文でも同様です。

短絡評価は x < 0 || x > 10 のように、2つ以上の条件式を合成するときにも使われます。この式は x が0より小さいか10より大きいとき、かつそのときに限り true となります。なお、&& も || も右結合ですが、&& は || より優先順位が高いことに注意してください。例えば、x && y || z は (x && y) || z を意味します。次の例は、ある年がうるう年[*11](c315.xhtml#a9FA)かを判定する関数の定義です。

![](image_rsrcAYX.jpg)

# 7.4.2 条件演算子と ifelse 関数

条件演算子(conditional operator)は、条件式の評価結果に応じて評価する式を変える演算子です[*12](c315.xhtml#aA7J)。条件演算子は cond ? x : y のように書き、cond が true のときは x を、false のときは y を評価します。

![](image_rsrcAYY.jpg)

条件演算子は次のように入れ子にして、最初に満たされた条件に付随する式を評価する分岐を記述するときにもよく使われます。

![](image_rsrcAYZ.jpg)

条件演算子と似たものに ifelse 関数があります。ifelse 関数は、3つの値を引数にとる関数であり、最初の引数が true のときは第2引数を、false のときは第3引数を返します。

![](image_rsrcAZ0.jpg)

条件演算子との違いは、ifelse 関数は関数なので引数として与えられた3つの式を常に評価することです。結果として、引数の式に副作用があるとき、次のような違いが見られます。

![](image_rsrcAZ1.jpg)

# 7.4.3 if文

if文は条件分岐の標準的な構文です。書き方のバリエーションがいくつかあるので、順に説明します。

最も単純なif文は、if cond block end のように書き、分岐条件 cond が true のとき、かつそのときに限り block を評価します。短絡評価の && 演算子と等価ですが、if文は block に複雑なコードを書くときに向いています。なお、if cond, block, end の3つは同じ行にも書けますが、次のように別々の行に書くのが一般的です。

![](image_rsrcAZ2.jpg)

if文では、else に続けて分岐条件が false のとき、かつそのときに限り実行されるコードを書けます。これは条件演算子と等価です。

![](image_rsrcAZ3.jpg)

さらにif文では、elseif に続けて cond のほかに分岐条件を追加できます。elseif は if の後にいくつでも書けますが、分岐条件が最初に true と評価されたブロックのコードのみが実行される点に注意してください。すべての分岐条件が false となったときのみ、else に続くブロックが実行されます。

![](image_rsrcAZ4.jpg)

なお本書ではif文と呼んでいますが、Juliaのif文は値を返せるので正確には式です。x = if ...のように値を受けとるコードもたまに見られます。しかし他のプログラミング言語の慣習から、本書ではif文で統一しています。以降で説明する文も同様です。

# 7.5 反復

条件分岐と並んで反復(iteration)もプログラムには欠かせない要素です。反復はループ(loop)とも呼ばれます。Juliaには次の2つの構文が反復のために用意されています。

1\. while文

2\. for文

さらに、反復するブロック内でのみ使える continue キーワードと break キーワードがあります。本節では、これらの構文を順に解説します。

# 7.5.1 while文

while文は while cond block end のように書き、条件 cond が true に評価される限り何度でも block を繰り返し評価します。簡単な使用例は次のとおりです[*13](c315.xhtml#a9ES)。

![](image_rsrcAZ5.jpg)

7.5.3項で説明するように、continue キーワードと break キーワードを使うと、ブロックの実行を細かく制御できます。

# 7.5.2 for文

for文は for x in iter block end と書き、イテレータ iter から要素を取り出しながら変数 x を要素に束縛し、block を実行します。イテレータから取り出す要素がなくなったとき、for文は終了します。x はローカル変数になりますが、後述するように動作が通常のローカル変数定義とは少し異なります。また、in の部分は等号(=)や∈(![](image_rsrcAGC.jpg)in)に取り替えても構いません。どれを使っても動作はまったく同じです。簡単な使用例を示します。

![](image_rsrcAZ6.jpg)

for文では、次のようにイテレータを複数指定できます。こうすると、要素のすべての組合せ(デカルト積)を列挙できます。

![](image_rsrcAZ7.jpg)

for文で気を付けるべきことは、for キーワードの直後の変数は、ローカル変数であるにもかかわらず、外側のスコープにある同名のローカル変数を継承しないということです。これは通常のローカル変数の動作とは異なります。通常のローカル変数と同じ動作をさせるには、次のように for キーワードの後に outer と書きます。

![](image_rsrcAZ8.jpg)

# 7.5.3 反復構文の制御

while文とfor文には、そのブロック内でのみ使える特殊なキーワードがあります。continue キーワードと break キーワードがそれです。continue キーワードは continue より先にあるブロックの残りを実行せず、反復の先頭に戻る命令です。break キーワードは反復をただちに終了する、つまりループから抜ける命令です。

まずは continue キーワードを取り上げます。例えば、次のようにループの変数が偶数のときだけ print 関数の呼出しをスキップできます。

![](image_rsrcAZ9.jpg)

次は break キーワードです。break キーワードは、実行された時点で残りのすべての処理を無視してfor文の外側へ抜けます。

![](image_rsrcAZA.jpg)

while文やfor文を入れ子にした場合、break キーワードは書かれた場所から見て最も内側にあるループのみを抜けます。ただし、次のように1つのfor文に複数のイテレータを指定した場合、内側のループだけでなくfor文全体を抜けるので注意してください。

![](image_rsrcAZB.jpg)

# 7.5.4 無条件ジャンプ

Juliaには無条件ジャンプ(unconditional jump)を行うマクロが用意されています。@goto name を実行すると、@label name にすぐさま移ります。ほとんどの場合はfor文やwhile文で代替可能なので使用機会はそれほど多くありませんが、リスト1.2のように入れ子になったループの内側から脱出する制御などが簡潔に書けるので、まれに使われます。

# 7.6 例外処理

プログラムの実行中には、想定外のエラーが発生する可能性があります。例えば、開こうとしたファイルが存在しないときや、誤って範囲外の配列要素を参照しようとしたとき、発生した問題を通知するため例外(exception)が送出されます。

例外が発生すると、Juliaはその場で一度プログラムの実行を中断し、その関数の呼出し元に例外の発生を伝えます。通常、この例外の伝播は、関数呼出しの連鎖をたどっていき、最後にはエラーメッセージとともにプログラムを停止させます。本節で説明するtry文は、この例外の伝播を制御する構文です。

try文には構文にいくつかのバリエーションがありますので、順を追って紹介していきましょう。

# 7.6.1 catchブロックでの例外の捕捉

catchブロックを伴う単純なtry文は、try block1 catch block2 end のように書きます。try文はまず block1 を実行し、例外が発生しなければtry文はそこで完了して対になった end 以降が実行されます。このとき、block2 は実行されません。もし block1 の実行中に例外が発生したら、block1 の実行をそこで中断して block2 を実行します。意図的に例外を発生させる error 関数を使って、この動作を確認してみましょう。

![](image_rsrcAZC.jpg)

block1 のエラー前というメッセージは表示されているのに、エラー後は表示されていないことに注目してください。これは、後者のメッセージを出力する前に error 関数の呼出しで例外が発生し、そこで block1 の実行が中断されたからです。

先の例では、どのような例外が発生したのか外からはまったくわかりません。catchブロックを catch ex block end と書くと、変数 ex をtryブロックで発生した例外に束縛し、block 内で例外を参照できるようになります。これは次のように発生した例外をユーザに警告メッセージとして表示するときに便利です。

![](image_rsrcAZD.jpg)

あるいは、catchブロック内で rethrow 関数を呼び出すと、捕捉した例外をそのまま再送出できます。catchブロック内で例外が発生したときに後処理し、それから例外を再送出するパターンはよく見かけられます。または、次のように対応できる例外のみを選別し、それ以外は再送出するというパターンもあります。

![](image_rsrcAZE.jpg)

# 7.6.2 finallyブロックでの後処理

ある処理の最後に、その処理が成功したかどうかにかかわらず別の処理(後処理)を行いたい状況があります。具体例として、ファイルを開いて何かしらの処理をする状況を考えましょう。ファイルを開いた後、処理の途中で何らかのエラーが発生すると、ファイルは開いたままの状態になってしまいます。ファイルの状態はOSで管理されており、同時に開けるファイルの数には限りがありますので、例外発生の有無にかかわらず、ファイルは処理が終わったら確実に閉じるべきです。一般に、リソースを確保してから処理をするとき、処理中に例外が発生してもしなくても、割り当てたリソースを解放する必要があるのです。この例では、ファイルを開くことがリソースの確保にあたり、ファイルを閉じることがリソースの解放にあたります。

try文のfinallyブロックは、そうしたリソース解放の後処理をするにはうってつけの場所です。catchブロックは例外が発生したときのみ実行されるのに対し、finallyブロックは例外の発生にかかわらず、必ず実行されるからです。例えば、finallyブロックにファイルを閉じる処理を記述しておくと、開かれたファイルを確実に閉じられます[*14](c315.xhtml#a9FX)。

![](image_rsrcAZF.jpg)

# 7.6.3 elseブロック

tryブロックで例外が発生しなかったとき、かつそのときのみ実行されるelseブロックがJulia 1.8で導入されました。elseブロックは、次のようにcatchブロックの直後に記述します。

![](image_rsrcAZG.jpg)

# 7.6.4 ブロックの省略と実行順序

try文にはcatchブロックとfinallyブロックの片方もしくは両方を記述できますが、どちらか一方は必要です。ただし、catchブロックを省略すると、例外が発生しても無視されるので注意してください。

catchブロックとfinallyブロックの順番はどちらが先でも構いませんが、実行順序は常にtry→catch(else)→ finallyの順です。finallyブロックを先に記述してもcatchブロックより先に実行されることはないので注意してください。

![](image_rsrcAZH.jpg)

# 7.7 関数定義

関数(function)は手続きを抽象化する強力な手段です。関数は引数(argument)[*15](c315.xhtml#aA5F)をとり、何らかの処理を実行して、最終的に戻り値(return value)を呼出し元に返します。本節では、関数を定義する構文について詳しく解説します。なお、多重ディスパッチは関数と密接に関係しますが、ここに含めるとあまりに長くなってしまうので、[第9章](c434.xhtml)で改めて解説しています。

# 7.7.1 関数定義の基本

関数定義の構文には、function キーワードを使う構文と使わない構文があります。両者の機能に差はありませんが、複数行からなる複雑な関数は前者で書き、1行で書ける単純な関数は後者を使うのが一般的な使い分けです。本書では、関数の複雑さに応じて両者を使い分けます。

function キーワードを使った関数定義の基本的な構文は次のとおりです。name は関数名、arg1 と arg2 は関数の引数、block が実行される関数の本体です。

![](image_rsrcAZJ.jpg)

もう1つは数学的な関数定義の記法に似た構文です。

![](image_rsrcAZK.jpg)

関数の戻り値 ret は、return キーワードを使って return ret と指定します。戻り値を省略して単に return とだけ書くと、関数は nothing を返します。関数は return が実行されるとただちに値を返し、その関数の残りの部分は実行されません。return を省略すると、関数の最後の式が戻り値になります。

フィボナッチ数(Fibonacci number)[*16](c315.xhtml#a9DG)を計算する簡単な例で関数定義を確認しましょう。リスト7.2の実装は関数内で自分自身を呼び出す再帰関数(recursive function)の例になっています。この1行関数定義では return を省略していることに注意してください。

![](image_rsrcAZM.jpg)

変数への代入のときと同様、次のように1つの引数を複数の変数へ分離して代入できます。それほど使用頻度は高くありませんが、要素数の決まっているタプルなどを受けとるときに便利です。

![](image_rsrcAZN.jpg)

# 7.7.2 可変長引数

引数の数が特に定まっていない関数も定義できます。例えば、標準ライブラリにある min 関数は引数のうち最小の値を返す関数ですが、1つ以上の引数をいくつでも渡せます。このような引数を可変長引数(variadic arguments, varargs)と呼びます。

![](image_rsrcAZP.jpg)

可変長引数は args... のように引数の後にドット(.)を3つつなげて指定します。可変長引数は関数内部でタプルとして扱えるので、for文で要素を1つずつ取り出したり、添字を使った要素を参照したりすることが可能です。なお、可変長引数は通常の引数より前には置けません。リスト7.3に上記の min 関数の実装例を示します。

![](image_rsrcAZR.jpg)

# 7.7.3 オプショナル引数

関数の引数 arg には、何らかのデフォルト val を arg = val の形で設定できます。デフォルト(default)は既定値とも呼ばれます。関数呼出しのとき、デフォルトが設定された引数は省略できます。このような引数をオプショナル引数(optional argument)と呼びます。オプショナル引数はいくつでも指定できますが、通常の引数より前には置けません。次の例では、引数 d がオプショナル引数です。

![](image_rsrcAZS.jpg)

オプショナル引数は次に紹介するキーワード引数と似ていますが、名前ではなく関数に渡された引数の位置(引数が何番目か)でそれぞれの引数を区別することに注意してください。関数呼出しのときに increment(1, d = 2) のようなキーワード引数で使われる記法は使用できません。なお、デフォルトの評価については[7.7.6項](c315.xhtml#aAF4)で説明します。

# 7.7.4 キーワード引数

これまでに説明した引数は、第1引数や第2引数など、関数に渡される引数の位置が意味を持ちました。これは位置引数(positional argument)と呼ばれます。それに対して、位置ではなく名前で引数を指定するキーワード引数(keyword argument)というものがあります。キーワード引数には、オプショナル引数と同様にデフォルトも指定できます。

デフォルトを持つキーワード引数は arg = val の形で指定しますが、引数リストの区切りにコンマ(,)でなくセミコロン(;)を使うのが特徴です。セミコロンより前の引数は位置引数に、後の引数はキーワード引数になります。例えば、次のように書くと arg1 は通常の引数ですが、arg2 はオプショナル引数であり、kwarg1 と kwarg2 はキーワード引数です。

![](image_rsrcAZT.jpg)

次の例をオプショナル引数の例と比較してください。

![](image_rsrcAZU.jpg)

キーワード引数のデフォルトは省略できますが、省略するとその引数を渡さない関数呼出しはエラーになります。

![](image_rsrcAZV.jpg)

関数定義だけでなく、キーワード引数を渡して関数を呼び出すときにもセミコロンが使えます。こうすると、:d => 4 のようなペアを使ったキーワード引数の指定が許されます。

![](image_rsrcAZW.jpg)

さらに、変数名とキーワード引数名が一致すれば、次のような短縮記法が使えます。

![](image_rsrcAZX.jpg)

任意個数のキーワード引数を受けとるには、次のように ... を使って関数を定義します。

![](image_rsrcAZY.jpg)

# 7.7.5 引数のスプラッティング

関数呼出しのときにタプルなどのコレクションに入った要素を個別の引数として展開することを引数のスプラッティング(splatting)[*17](c315.xhtml#aA4U)と呼びます。引数のスプラッティングには、args... のようにドットを3つつなげた構文を使います。これはタプルを構築するときに使われる構文([5.1.1項](c251.xhtml#aA4V)を参照)と共通しています[*18](c315.xhtml#aA4W)。

次の例では、任意の数の引数をとれる println 関数に3つの値が入ったタプルをスプラットして渡しています。これはタプルの要素を個別の引数として渡すのと等価です。

![](image_rsrcAZZ.jpg)

キーワード引数でも同じようにスプラットが使えます。このとき、スプラットする引数は名前付きタプルにすると便利です。

![](image_rsrcB00.jpg)

# 7.7.6 デフォルトの評価

オプショナル引数とキーワード引数には、関数呼出しで引数が渡されなかったときに代用されるデフォルトを指定できます。デフォルトには整数や文字列などの単純なリテラルだけでなく、任意の式を指定できます。デフォルトの評価について、把握しておくべきことを具体例を通して確認しましょう。

デフォルトとして指定された式は、関数呼出しのたびに評価されます[*19](c315.xhtml#aAAY)。例えば、次のように乱数を生成する rand 関数の呼出しをデフォルトにすると、関数を呼び出すたびに異なる値が返ります。

![](image_rsrcB01.jpg)

デフォルトは左から右の順で評価され、後続の引数はそれより前の引数を参照できます。

![](image_rsrcB02.jpg)

なお、実際に引数が渡されたとき、そのデフォルトは評価されません。

![](image_rsrcB03.jpg)

# 7.7.7 クロージャ

関数は関数定義内でも定義できます。このとき、内側の関数から外側の関数のローカル変数を参照できます。このような仕組みをクロージャ(closure)といいます。さらに、内側の関数をオブジェクトとして返すことも可能です。言葉で説明してもわかりにくいので、具体例を示しましょう。次の makeadder 関数は、和を計算する adder 関数を定義して返します。このとき返す関数オブジェクトのことをクロージャと呼ぶこともあります。makeadder 関数に渡される引数 x は adder 関数の中で使われており、最初に makeadder 関数に渡す値によって adder 関数の動作は変化します。

![](image_rsrcB04.jpg)

もう少し面白い例を見てみましょう。次の makecounter 関数は、呼び出すたびに戻り値が1ずつ増える counter 関数を返します。このクロージャでは、状態を保持する変数としてローカル変数 n を使っているわけです。

![](image_rsrcB05.jpg)

前の例に続けて、新しいカウンタ another を作ってみます。すると、このカウンタは counter とは独立にカウントを続けます。このように、makecounter 関数は呼び出されるたびに独立したクロージャを返します。

![](image_rsrcB06.jpg)

クロージャを使ったテクニックは面白いのですが、実際に使うことはあまりないかもしれません[*20](c315.xhtml#a9D7)。実用的には、以降の章で解説するように、構造体などのオブジェクトに状態を保持し、そこに関数やメソッドを定義することが多いです。なお、クロージャは[3.3.7項](cXU.xhtml#a9D8)でも使用しています。

# 7.7.8 無名関数

ここまでに紹介した関数にはすべて名前がありましたが、名前は必要ない場合もあります。例えば、1ヶ所でしか使わない関数には、名前が必要ないことが多いでしょう。Juliaには名前を持たない関数を作る機能があり、こうした関数は無名関数(anonymous function)と呼ばれます。

無名関数を作るには、args -> body という構文を使います。args が関数の引数で、body が本体です。例えば、引数が3より大きいときのみ true を返す無名関数は x -> x > 3 と記述でき、次のように呼び出せます。

![](image_rsrcB07.jpg)

2引数以上の無名関数も同様です。

![](image_rsrcB08.jpg)

無名関数の使いどきは、名前を付けて抽象化するほどではないが、関数がほしいときです。特に、[5.2節](c251.xhtml#a96T)で紹介した filter 関数や map 関数などの第1引数にとる関数とともに無名関数がよく使われます。

![](image_rsrcB09.jpg)

無名関数も普通のオブジェクトですので、変数を束縛できます。これは[7.7.1項](c315.xhtml#a9PG)の名前が付いた関数定義とはまったく異なる仕組みなので、混同しないようにしてください。

![](image_rsrcB0A.jpg)

無名関数の定義が複数行からなるときには、通常の関数定義から関数名を除いた次の構文で無名関数を作れます。

![](image_rsrcB0B.jpg)

この記法を使うと、[7.7.7項](c315.xhtml#a9HD)で紹介した makecounter 関数が次のように少し簡潔に書けます。

![](image_rsrcB0C.jpg)

# 7.7.9 無名関数の後置

filter 関数や map 関数などの第1引数に関数をとる関数では、do キーワードを使った構文で無名関数を後置できます。無名関数の中身が長くなるとき、-> で作る無名関数に処理を詰め込むより、無名関数の後置を使って次のように do ... end の中に書いたほうが読みやすくなります。第1引数に無名関数を渡す構文と機能的な差はありません。

![](image_rsrcB0D.jpg)

無名関数の後置は、ファイルを開く open 関数とともに使われることがよくあります。open 関数は、ファイルに対する処理を行う関数を第1引数にとり、その処理が終わればファイルを閉じます。この書き方は close 関数を直接呼び出す必要がないので簡単です。詳細は[13.2.3項](c5Y6.xhtml#a9JH)で解説します。

# 7.7.10 引数の部分適用

一部の関数では、引数の部分適用(partial application)が行えます。部分適用とは、関数の一部の引数を固定して新しい関数を作成することを指します。例えば、2引数をとる > 関数は、第2引数が固定された新しい関数が作れます。つまり、>(y) は x -> x > y と同じ動作をする関数を作ります。

![](image_rsrcB0E.jpg)

部分適用を使うと、先の例のように便利な関数が手軽に作れます。現在、in, occursin, contains, isequal, ==, >, <, >=, <= など、一部の比較関数が部分適用に対応しています。

# 7.8 型注釈

本節では、型の情報を式や変数などに付与する型注釈(type annotation)について説明します。型注釈にはいくつかの機能がありますが、構文は共通しているので混同しないように注意してください。

# 7.8.1 式の型注釈

式には、2つのコロン(:)を使って次のように型注釈を付けられます。

![](image_rsrcB0F.jpg)

誤った型注釈は実行時に検出され、TypeError 例外が送出されます。

![](image_rsrcB0G.jpg)

式の型注釈はJuliaの処理系に型に関するヒントを与えます。すなわち、コンパイラが高度な最適化を行えるように型に制約を与えることで、性能が改善することがあります。ただし、コンパイラは型を文脈から推論する機能([17.6節](c79X.xhtml#a995)を参照)を持っているので、ほとんどの場合において型注釈は不要です。

# 7.8.2 変数の型注釈

新しい変数を定義するときにも型注釈[*21](c315.xhtml#aA2M)が使えます。変数定義で x::T のように型注釈を行うと、変数 x が別の値に束縛されるたびに型 T へと変換しようとします[*22](c315.xhtml#aA2N)。したがって、変数の型が制限され、次のように変換ができない操作をしようとすると、InexactError 例外が送出されます。

![](image_rsrcB0H.jpg)

Julia 1.6と1.7ではローカル変数でのみ型注釈が行えましたが、Julia 1.8からはグローバル変数でも型注釈が行えるようになりました。

# 7.8.3 関数の型注釈

関数では、引数と戻り値に対して型注釈を付けられます。引数に型注釈を付ければ、関数に渡される引数に制約が課され、関数に予期せぬ型の引数が与えられたときに、関数を実行する前にエラーを報告します。これは、[第9章](c434.xhtml)で扱う多重ディスパッチを実現するために必要な機能です。

引数の型注釈は、関数定義をするときに x::T の形式で指定します。例えば、次の greet 関数の定義は、引数 name に String 型の制約を課しています[*23](c315.xhtml#a9PC)。

![](image_rsrcB0J.jpg)

この関数は引数が String 型のときには問題なく呼び出せますが、それ以外の型のときには呼び出せず、MethodError 例外が送出されます。

![](image_rsrcB0K.jpg)

同様に、戻り値の型注釈は f(...)::T の形式で指定します。このように型注釈を行うと、関数 f で計算された戻り値は最後に型 T に変換されます。この機能が必要な場面は多くありませんが、return が複数ある関数で戻り値の型が定まっていることを保証したいときや、コードを読む人にヒントを与えたいときなどに使うこともあります。次に示す関数 f64 は、引数 x を Float64 型に変換して返します。

![](image_rsrcB0M.jpg)

この関数は、例えば Int 型の引数を渡すと型が変換されて Float64 型の値が返りますが、文字列から Float64 型への変換はできませんので、文字列を引数として渡すとエラーが発生します。

![](image_rsrcB0N.jpg)

# 7.9 モジュール

モジュール(module)は、複数のグローバル変数をまとめて分割管理する単位です。変数の名前を分ける空間なので、このような仕組みは名前空間(namespace)とも呼ばれます。モジュールの名前空間で分けられた変数は、互いに干渉しません。つまり、モジュールはそれぞれ別の名前空間を持ち、別のモジュールに同名の変数があっても何も問題ないわけです。本節では、モジュールの定義と利用方法を解説します。

# 7.9.1 モジュールの定義

モジュールの定義は module name body end のように書きます。name がモジュールの名前で、body がモジュールの内容です。body には関数定義や型定義などのコードを自由に書けます。この部分で定義されたグローバル変数はそのモジュールの名前空間に属します。モジュールに属する各変数は、スコープの外からドット(.)を使って参照できます。

次のコード例では、2つのモジュール America と Sweden を定義しています。同名の関数 greet がそれぞれのモジュールに定義されていますが、名前空間が異なるので、それぞれ独立した関数であることに注意してください。モジュール名は慣習として大文字から始め、module の直下はインデントをしないスタイルが一般的です。

![](image_rsrcB0P.jpg)

モジュールの定義は入れ子にもできます。内側のモジュールは、外側のモジュールを通じて参照できます。なお、ここでは構造を見やすくするためにインデントをしています。

![](image_rsrcB0R.jpg)

# 7.9.2 既定のモジュール

Juliaには、起動直後から存在するモジュールがいくつかあります。次の3つは、そうしたモジュールのうち、ソースコードのどこからでも参照でき、特に重要な機能を持つモジュールです。

Main モジュール

module キーワードがなくても暗黙的に使われるデフォルトのモジュールです。[7.2.2項](c315.xhtml#a9AZ)でも取り上げました。REPLやスクリプトに書いたコードは、モジュールを何も指定しなければ Main モジュールに属します。Main モジュールに書いたモジュール定義も、Main モジュールの子モジュールです。このことは、現在有効なモジュールを取得する @__MODULE__ マクロを使えば確認できます。

![](image_rsrcB0S.jpg)

Base モジュール

標準ライブラリの大部分を構成する重要なモジュールです。何気なく使っている + 関数や println 関数などは、このモジュールで定義されています。Base モジュールの提供する機能は標準ライブラリの基盤となっているので、このモジュールがなければほとんど何もできません。そのため、Base モジュールの機能はその多くが Main モジュールに取り込まれ、起動直後から使えるようになっています。また、Base モジュール自身も Main モジュールに取り込まれているので、Main モジュールで Base モジュールの関数などを上書きしてしまっても、次のように Base モジュールを経由して元の関数を参照できます。

![](image_rsrcB0T.jpg)

Core モジュール

Juliaのビルトイン機能を提供する最も低レベルなモジュールです。ここでいうビルトイン機能とは、Julia自身では定義できない機能を指します[*24](c315.xhtml#aAFY)。Base モジュールは Core モジュールの上に構築されています。Core モジュールを実際に使うことはほとんどありませんが、低レベルな機能が必要なときにまれに使います。

# 7.9.3 using文

using文は他モジュールの変数を現在のモジュールに取り込みます。取り込まれた変数は、モジュール名を付けなくても参照できるようになります。

using文では、using キーワードの後にモジュールを指定します。表4.7で一覧にした数学定数を、using文で Base.MathConstants モジュールから取り込んでみましょう。Main モジュールには存在しなかった e や golden といった数学定数が、次のように Main モジュールに取り込まれます。

![](image_rsrcB0U.jpg)

このように using の後にモジュール名のみを指定するとそのモジュールのデフォルトの変数を取り込みますが、モジュールから取り込む変数を指定することもできます。モジュール名の後にコロン(:)を付け、取り込む変数を name1, name2, ... のように列挙してください。次の例では、e と golden のみを指定し、eulergamma は取り込みません。

![](image_rsrcB0V.jpg)

また、as を使って次のように別名を付けられます。

![](image_rsrcB0W.jpg)

このほか、using Foo, Bar, ... のように複数のモジュールを同時に取り込むこともできます。

基本的な構文を説明したところで、using文の動作をもう少し詳しく説明しましょう。取り込む変数を指定せず using Foo とだけ書いたとき、Foo モジュール自身と、モジュールが指定するデフォルトの変数が取り込まれます。このときに取り込まれる変数のリストは、そのモジュール内にあるexport文で指定されています。詳しくは、[7.9.6項](c315.xhtml#a9SW)で説明します。Main モジュールや module キーワードで定義したモジュール内では実質的に using Base が実行された状態で、Base モジュールがエクスポートしている機能が使えるようになっています。

using文でコロンの後に取り込む変数を指定する書き方は、意図しない変数が現在のモジュールに取り込まれることを防止できます。このとき取り込む変数は、export文で指定された変数以外も指定可能です。また、次のようにモジュール名自身を指定し、そのモジュール名のみを取り込むこともできます。この例では、統計関係の標準ライブラリであるStatistics.jlパッケージ([11.5節](c4WA.xhtml#a987)を参照)を使っています。

![](image_rsrcB0X.jpg)

# 7.9.4 import文

import文はusing文と同様の機能を提供しますが、動作の詳細が異なります。import Foo と書いたとき、Foo という変数のみを現在のモジュールに取り込み、そのモジュールがエクスポートする変数は取り込みません。これは using Foo: Foo と同じ動作です。一方、import Foo: f1, f2, ... と書いたとき、指定された名前のみ現在のモジュールに取り込むのはusing文と同じですが、関数を拡張するときの動作が異なります。詳しくは[第9章](c434.xhtml)で説明する多重ディスパッチの知識が必要なので、[9.4節](c434.xhtml#a97S)で解説をします。

# 7.9.5 モジュール名の相対指定

using文とimport文では、モジュールの指定方法が2種類あります。1つはモジュール名のみを指定する方法です。この方法では、パッケージマネジャがモジュールの探索を行います。例えば、using Statistics を実行すると、パッケージマネジャは Statistics モジュールの定義を環境([16.1.4項](c6XJ.xhtml#a9CC))から見つけて読み込みます。

もう1つはモジュールの相対パスを指定する方法です。相対パス指定は、対象のモジュールがパッケージマネジャで管理されていないときに使用します。ここではこの仕組みを詳しく見ていきましょう。

相対パス指定では、モジュール名の前にドットが1つ以上付きます。.Foo のようにドットが1つのときは、自身のモジュールから見た子モジュールの名前を意味します。これは次の例を見たほうがわかりやすいでしょう。モジュール Parent のusing文は子である Child モジュールを相対パスで指定しています。

![](image_rsrcB0Y.jpg)

相対パスはドットの数を重ねるごとに親のモジュールをたどります。例えば、ドットが2つなら、図7.4に示すように自身 Me の親である Parent から見た別の子、つまり姉妹の Sister モジュールの名前を指定できます。

![](image_rsrcB0Z.jpg)

![](image_rsrcB10.jpg)

# 7.9.6 export文

export文は、using文を使ったときにデフォルトで取り込む名前を指定します。export キーワードを使って次のようにモジュール定義の内側に書きます[*25](c315.xhtml#a9YN)。

![](image_rsrcB11.jpg)

なお、エクスポートされてない名前であってもモジュール名を経由して参照可能なことに注意してください。

![](image_rsrcB12.jpg)

# 7.9.7 ベアモジュール

モジュール定義のときに module キーワードではなく baremodule キーワードを使うと、Base モジュールを取り込まないベアモジュール(bare module)を作れます。ベアモジュールは Base モジュールが提供する機能を持っていないので、ほとんど何もできません。ベアモジュールを使用する機会はほとんどありませんが、Base モジュール自身を始めとして、ごく一部のモジュールの定義で使われています。

まとめ

本章では変数のスコープと基本的な構文を解説しました。特に理解が難しいのはローカルスコープの動作です。しばらく使って慣れてくると自然に思えるのですが、特にローカルスコープが入れ子になったときの動作は、初めのうちは誤解しやすいようです。REPLではソフトスコープとハードスコープの区別が必要なことも注意すべきポイントです。また、let文など他言語にない機能は避けがちですが、実際はよく使うので確実に理解しておくべきでしょう。関数定義での引数の扱いも、使いやすいインタフェースを設計するときには重要な知識ですので、よく理解しておくべきです。なお、型を定義する構文については第8章で扱っています。

注

[*1](c315.xhtml#aA3C) 例外的に、マクロ名やプロパティ名としては使えます。[*2](c315.xhtml#a9Z4) 例えばC++のコピー代入演算子のようなことは起きません。[*3](c315.xhtml#aA6K) ただし、マクロを使うとプログラムを書き換えられるので、例外的な事象は起こりえます。[*4](c315.xhtml#aA94) 7.9節でモジュールの作る名前空間について説明します。[*5](c315.xhtml#a9S6) モジュールの入れ子と混同しないよう注意してください。モジュール自体は入れ子にできても、モジュールに付随するグローバルスコープは互いに独立しており、包含関係はありません。[*6](c315.xhtml#a9NK) let文の機能については[7.2.6項](c315.xhtml#a9NM)でも説明しています。[*7](c315.xhtml#a9G5) 対話的環境で一部の構文のスコープの規則が変わることを奇妙に感じる方もいると思います。実際、Julia 1.0がリリースされたとき、一貫性のためソフトスコープとハードスコープの区別は一度廃止されたのですが、長い議論のすえ、Julia 1.5で復活しました。対話的環境のような「書き捨て」処理をする場所では、グローバル変数を書き換える機会が多く、for文などでいちいち global キーワードを使ってスコープを明示しなければならないのは不便であり、初学者には混乱のもとにもなっていたことが復活の大きな理由です。一貫性より利便性を優先した仕様ともいえます。[*8](c315.xhtml#aA4E) = や && などの関数ではない演算子もあります。[*9](c315.xhtml#aA5M) 厳密には、Juliaの + と * は非結合的です。例えば、x + y + z は +(+(x, y), z) でなく +(x, y, z) と解釈されます。ただし、デフォルトの実装が左結合的に評価するので、実用上は左結合的と考えて差し支えありません。[*10](c315.xhtml#a9W1) 実用的と思われる演算子のみを示しています。完全なものはJuliaの実装(src/julia-parser.scmファイル)を参照する必要があります。[*11](c315.xhtml#aA12) 原則は西暦が4の倍数である年がうるう年ですが、例外的に400の倍数でない100の倍数の年はうるう年ではありません。[*12](c315.xhtml#a9FM) 3つの式をとるので、三項演算子(ternary operator)と呼ばれることもあります。[*13](c315.xhtml#a9VD) [7.2.10項](c315.xhtml#a9VE)で説明したように、REPLでは global i は不要です。[*14](c315.xhtml#a9JG) [13.2.3項](c5Y6.xhtml#a9JH)では do キーワードを使った別の方法を紹介しています。[*15](c315.xhtml#aAAA) 関数定義に現れる引数を仮引数(parameter)、呼出しに現れる引数を実引数(argument)と呼んで区別することもありますが、Juliaではどちらも単に引数(argument)と呼ぶことがほとんどです。[*16](c315.xhtml#aAES) フィボナッチ数 Fn (n![](image_rsrcAJY.jpg)1) は、F1=1, F2=1, Fn=Fn-1+Fn-2 (n![](image_rsrcAJY.jpg)3) で定義される整数です。[*17](c315.xhtml#aA3P) 日本語に訳しづらい英単語ですが、湿ったものをピチャッと飛び散らせるというような意味を持ちます。[*18](c315.xhtml#aA8D) 可変長引数とも構文が類似していますが、可変長引数は関数定義の構文であるのに対し、こちらは関数呼出しの構文であるという違いに注意してください。[*19](c315.xhtml#aA97) Pythonでは関数定義のときにデフォルトの値が決定するので、Juliaとは動作が異なります。[*20](c315.xhtml#a9X5) [17.6.3項](c79X.xhtml#a9X6)で説明するように、性能の劣化を引き起こしやすいという問題もあります。[*21](c315.xhtml#a9UZ) この場合には型宣言(type declaration)という語が使われることが多いですが、本書では特に区別せず型注釈と呼ぶことにします。[*22](c315.xhtml#aAD5) 一方、前述した式の型注釈では、型を変換しようとしないことに注意してください。例えば、x::T = 0 は変数 x の型注釈で T 型へと変換されますが、x = 0::T は式 0 の型注釈で変換は起きません。なお、型の変換は convert 関数で行われます。[*23](c315.xhtml#aA2D) ただし、引数の型注釈は、前述した変数の型注釈と異なり、変数に特定の型を強制しません。例えば、x::Int という引数の型注釈があったとき、関数内で x = 0.1 などと変数 x を Int 型以外の値に束縛することは許されています。[*24](c315.xhtml#a9HF) Core.Compiler モジュールなど、Core モジュールの一部はJulia自身で書かれています。[*25](c315.xhtml#a9W9) エクスポート対象の定義より前に書いても後に書いても、複数回書いても機能に差はありません。

---

# ![](image_rsrcB13.jpg)

Juliaの型システム(type system)は、他のスクリプト言語と比較するとやや複雑です。これは、Juliaが多重ディスパッチをプログラミングの中心的なパラダイムとし、プログラムの実行効率を重視していることと深く関係しています。本章では、Juliaの型システムを基礎から解説し、第9章の多重ディスパッチへとつなげます。

# 8.1 型の階層

Juliaで扱うすべてのオブジェクトは型を持ちます。あるオブジェクトの型は、typeof 関数を使って取得できます。例えば、typeof(42) の戻り値は Int 型であり、typeof("foo") の戻り値は String 型です。一方で、Juliaには typeof 関数の戻り値にはなりえない型も存在します。例えば、標準ライブラリにある Integer という型は、どのオブジェクトを持ってきても typeof 関数の戻り値にはなりません。このように、Juliaで使われる型には typeof 関数の戻り値になりえる型となりえない型の2種類が存在し、前者を具象型(concrete type)といい、後者を抽象型(abstract type)といいます。先の例でいえば、Int は 42 というオブジェクトの型なので具象型であり、Integer は抽象型です。ある型 T のオブジェクトを T のインスタンス(instance)と呼びます。例えば、42 は Int 型のインスタンスです。この言葉を使えば、具象型はインスタンスを作れる型であり、抽象型はインスタンスを作れない型とも言い換えられます。

インスタンスが存在しない抽象型には、上位概念として他の型をまとめる役割があります。他の型をまとめるとき、抽象型はその型の上位型(supertype)[*1](c3S5.xhtml#aA4K)といいます。具体例で説明しましょう。32ビット符号付き整数の Int32 型と64ビット符号付き整数の Int64 型はともに具象型ですが、それらをまとめる上位型として Signed 型という抽象型が存在します。名前が示すとおり、Signed 型はビット数といった具体的な表現の違いを無視し、抽象的に符号付き整数の型を意味します。ある型の直接的な上位型は次のように supertype 関数という関数で取得できます。図8.1に符号付き整数型の関係を示します。

![](image_rsrcB14.jpg)

![](image_rsrcB15.jpg)

上位型とは逆の関係を表す下位型(subtype)[*2](c3S5.xhtml#aAB8) という言葉があると便利なので、定義しておきましょう。S 型が T 型の上位型であるとき、T 型は S 型の下位型です。例えば、Signed 型は Int32 型の上位型なので、Int32 型は Signed 型の下位型といえます。同様に Int64 型も Signed 型の下位型です。なお、上位型と下位型は2つの型の関係性を表す言葉です。つまり、「S 型は T 型の上位型である」という相対的な関係性はいえても、「S 型は上位型である」というような絶対的ないい方はできません。

supertype 関数とは逆に、ある型の下位型を取得する subtypes という関数があります[*3](c3S5.xhtml#a9U3)。ある型の直上の上位型は常に1つですが、直下にある下位型は1つとは限らないので subtypes 関数の名前は複数形です。この関数は次のように下位型の配列を返します。

![](image_rsrcB16.jpg)

あるオブジェクトがある具象型のインスタンスであるかどうかは、isa 関数を使って確認できます。x isa T は、型 T が x の型であるか、その上位型であるときのみ true を返します。なお、isa(x, T) のように関数呼出しの構文で使われることもあります。

![](image_rsrcB17.jpg)

抽象型にはさらに上位の抽象型が存在することがあります。例えば、Signed 型の上位型は一般的な整数を表す Integer という抽象型です。Integer 型のさらに上位型は一般的な実数を表す Real という抽象型です。このように、型は多層的な階層構造をとります。この型の階層の最も下層に多数の具象型があり[*4](c3S5.xhtml#a9N2)、最上位には Any という抽象型があります。具体例として、一般的な数を表す Number 型以下の型の階層を図8.2に示します。具象型は数が多いので一部を省略しています。Number 型の上位型は Any 型です。

![](image_rsrcB18.jpg)

ある型が別の型の上位型であるかどうかは、<: 関数を使って確認できます。T <: S は、型 S が型 T の上位型であるか、型 S と型 T が同じ型であるときのみ true を返します。例えば Integer 型は Int64 型の上位型ですが、AbstractFloat 型はそうではありません。また、AbstractFloat 型は Float64 型の上位型ですが、Integer 型はそうではありません。したがって、これらの型に <: 関数を適用すると次のようになります。

![](image_rsrcB19.jpg)

型階層の最上位にある Any 型はすべての型の上位に位置します。したがって、T <: Any は T がどんな型であっても true になります。

![](image_rsrcB1A.jpg)

# 8.2 パラメトリック型

先ほど説明した Int や Integer などの型は、最も単純な部類の型です。Juliaにはより複雑な型があり、本節で紹介するパラメトリック型(parametric type)はそのような型の1つです。パラメトリック型は文字通りパラメータ(parameter)を持つ型であり、パラメータを取り替えてさまざまな型を表現できます。

# 8.2.1 パラメトリック型の例

まずは標準ライブラリにあるパラメトリック型の具体例をいくつか紹介しましょう。[4.5節](c1JX.xhtml#a96M)で見た複素数を表す Complex 型はパラメトリック型の例です。この型は、パラメータ T を使って Complex{T} と書き、T 型の実部と虚部からなる複素数を表現します。例えば、Complex{Float32} 型は単精度浮動小数点数を使って複素数を表現し、Complex{Float64} 型は倍精度浮動小数点数を使って複素数を表現します。

![](image_rsrcB1B.jpg)

[第5章](c251.xhtml)で見たコレクションの多くもパラメトリック型です。例えば、1次元配列(ベクトル)を表す Vector{T} 型は、保持する要素の型をパラメータ T として持ち、辞書を表す Dict{K, V} 型はキーの型 K と値の型 V の2つのパラメータを持ちます。

![](image_rsrcB1C.jpg)

パラメトリック型のパラメータに代入できるのは型に限りません。例えば、Array{Float32, 3} のように、Array 型の2番目のパラメータには配列の次元数を表す整数が入ります。この型は単精度浮動小数点数の3次元配列を表しています。ただし、どんなオブジェクトでもパラメータに代入できるわけではなく、Bool 型や Int 型など一部の型のオブジェクトに限られます[*5](c3S5.xhtml#a9CM)。

Vector や Dict のような具象型[*6](c3S5.xhtml#a9G2)のパラメトリック型だけではなく、抽象型のパラメトリック型も存在します。例えば、AbstractVector 型は Vector 型の上位型である抽象型です。表8.1に代表例をまとめます。

![](image_rsrcB1D.jpg)

# 8.2.2 パラメトリック型の意義

パラメトリック型が持つ利点の1つは、パラメータの値を入れ替えることで操作や構造が共通する型を抽象化して扱える点です。もしパラメトリック型がなかったとしたら、例えば、辞書はキーと値の型の組合せごとに別々の型を用意しなければなりません。辞書に対する操作や構造は共通しているのに、個別の型を用意しなければならないのは不便です。パラメトリック型があれば、型の一部のみをパラメータとして交換可能にし、その他の部分は共通化できます。

型がパラメータを持つことにより、効率の面でもさまざまな利点があります。パラメータ T が具象型のとき、Complex{T} 型のメモリサイズを例に考えてみましょう。あるオブジェクトが Complex{T} という型を持つとわかれば、そのオブジェクト(ここでは複素数)の実部と虚部は T 型であると保証できます。これにより、もし T 型のサイズが固定されていれば、Complex{T} 型のオブジェクトが使うメモリサイズもわかります。Complex{Float32} ならメモリサイズは実部と虚部を合わせて64ビットですし、Complex{Float64} なら128ビットです。もし複素数型がパラメトリック型でなかったとしたら、実部と虚部の型を固定するか、複数の型の値も収められるような工夫が必要になります。前者は柔軟性に欠けますし、後者はポインタなどが必要になり非効率的です。

さらに、パラメトリック型にはプログラムの実行効率でも利点があります。例えば Vector{T} 型の配列を引数にとり、全要素をソートする関数を書くとき、その関数内では isless 関数などで要素の大小比較を繰り返すでしょう。大小比較の具体的な方法は要素の型によって異なる(例えば T が Int64 型ならビット列の比較ですが、String 型なら辞書順の比較になる)ので、もし配列から要素を取り出すまでその型がわからないとすると、比較のたびに適切な実装を探し出さなければなりません。しかし要素の型があらかじめわかっていれば、Juliaは関数を実行する前にどう大小比較をすればよいのかを決定できるので、コンパイラを使って効率的なコードを生成できます。

# 8.2.3 パラメータの制約

パラメトリック型のパラメータには制約をかける方法があります。パラメータの制約は[第9章](c434.xhtml)で解説する多重ディスパッチで使用するので、関係する構文も含め、ここで基本から説明します。

Juliaにはパラメトリック型のパラメータを明示的に扱う構文が用意されています。この構文は次のように where に続けてパラメータを記述します[*7](c3S5.xhtml#aA5G)。パラメータは何を使っても(例えば T の代わりに U にしても)意味に差はありません。

![](image_rsrcB1E.jpg)

パラメトリック型のパラメータが未定のとき、その型は任意のパラメータの値を持つ型の上位型として振る舞います。例えば、Vector{T} where T は Vector{Int64} や Vector{String} など、任意の Vector{...} の上位型です。

![](image_rsrcB1F.jpg)

単に where T と書いたとき、パラメータ T は任意の型になりえますが、パラメータに制約をかける方法もあります。制約をかけるには <: を使ってパラメータの上位型を指定します。制約をかけると、次のようにパラメトリック型を一部の範囲に限定できます。

![](image_rsrcB1G.jpg)

このようにパラメータに制約をかける記法はよく使われるので、次のように where を使わない短縮記法も用意されています。

![](image_rsrcB1H.jpg)

なお、パラメトリック型のパラメータ部分を除いた記法は、where を使った記法の短縮形と考えられます。

![](image_rsrcB1J.jpg)

パラメータが複数あるときは次のように where を重ねるか、パラメータを波括弧でまとめます。どちらの書き方でも意味に差はありません。

![](image_rsrcB1K.jpg)

また、一部のパラメータのみを固定して他のパラメータを未定のままにできます。ただし、2番目以降のパラメータを固定するときは where が必要です。

![](image_rsrcB1M.jpg)

# 8.2.4 パラメトリック型の不変性とその例外

Vector{S} 型と Vector{T} 型について、T が S の上位型であるとき、Vector{T} も Vector{S} の上位型であると考える人がいるかもしれません。例えば、Int64 <: Integer なので Vector{Int64} <: Vector{Integer} が成立すると予想するのは自然な発想ですし、一部のプログラミング言語では実際そのようになっていますが、Juliaでは一部の例外を除き成立しません。これをパラメトリック型のパラメータに対する不変性(invariance)と呼びます。

![](image_rsrcB1N.jpg)

ただし、次の関係は成立することに注意してください。

![](image_rsrcB1P.jpg)

パラメトリック型の不変性には、1つ重要な例外があります。それはタプル型のパラメータです。タプル型においては、S <: T であるとき Tuple{S} <: Tuple{T} が成立します。不変性に対し、これをタプル型のパラメータに対する共変性(covariance)と呼びます。タプル型のパラメータに対する共変性はパラメータが複数ある場合でも成立します。

![](image_rsrcB1R.jpg)

余談ですが、同じパラメータ T を N 個持つタプル型には、NTuple{N, T} という別名が付けられています。

![](image_rsrcB1S.jpg)

# 8.3 ユニオン型

ユニオン型(union type)[*8](c3S5.xhtml#a9YX)は、複数の型を合併した型です。型を数学的な集合のようなものと考えれば、ユニオン型は和集合(union)とみなせるでしょう。

ユニオン型は Union{S, T, ...} と書きます。このユニオン型は S や T の上位型になります。S や T の順序は影響しません。なお、Union{T} 型は T 型と等価です。

![](image_rsrcB1T.jpg)

ユニオン型は複数の型を特定の目的のために暫定的にまとめる抽象的な型の表現として使われます。特に、nothing や missing([11.5.3項](c4WA.xhtml#a9CZ))を要素として持ちうるコレクションの要素の型として頻繁に用いられます。例えば、欠損値 missing は他の型とは独立したオブジェクトなので、次のような Vector{String} 型のコレクションには欠損値を収められません[*9](c3S5.xhtml#a9D0)。そこで、要素の型を Union{String, Missing} 型とし、要素が欠損値かもしれないとコレクションに教えておく必要があるわけです。

![](image_rsrcB1U.jpg)

# 8.4 型の別名

頻繁に使う型に別名を付けると便利なことがよくあります。例えば、Int 型はその実行環境で最も効率的に扱える整数型(Int32 型か Int64 型)の別名になっています。さらに、単精度と倍精度の複素数型にはそれぞれ ComplexF32 と ComplexF64 という別名が付けられていますが、これらは次のように定義されています。

![](image_rsrcB1V.jpg)

パラメトリック型は一部をパラメータ化した別名を付けることも可能です。例えば、Vector 型や Matrix 型は Array 型の別名として次のように定義されています[*10](c3S5.xhtml#aAB4)。

![](image_rsrcB1W.jpg)

ユニオン型も型の別名によく使われます。例えば、[4.4節](c1JX.xhtml#a96K)で説明したIEEE754規格で定義されている浮動小数点数の型は、次のようにユニオン型としてまとめ、別名を付けて扱うと便利なことがあります。

![](image_rsrcB1X.jpg)

# 8.5 抽象型の定義

必要に応じてユーザは自分で抽象型を定義できます。以降で説明する構造体型やプリミティブ型をまとめる上位型として、抽象型は広く使われています。

抽象型の定義は、次のように abstract type で始まり、end キーワードで終わります[*11](c3S5.xhtml#aA5R)。[ と ] の間は省略可能です。Name が定義する抽象型の名前で SuperType が上位型です。

![](image_rsrcB1Y.jpg)

<: SuperType を省略すると自動的に Any 型直下の下位型として定義されます。例えば、図8.2に示した Number 型以下の抽象型の一部は、次のように定義できます。

![](image_rsrcB1Z.jpg)

慣習として、抽象型の名前は AbstractVector 型や AbstractString 型などのように、Abstract で始まることが多いです[*12](c3S5.xhtml#aAC5)。例えば、木構造のノード(node)を表現する抽象型を定義するとしたら、次のように AbstractNode とするのが一般的でしょう。

![](image_rsrcB20.jpg)

パラメータを持つ抽象型も定義できます。次のように型名の後ろにパラメータを続けます。パラメータの名前は何でも構いません。パラメータをとるように上の例を修正すると、次のように書けます。

![](image_rsrcB21.jpg)

さらに、パラメータに制約をかけるときは次のように書けます。

![](image_rsrcB22.jpg)

抽象型はインスタンスを作れないので、抽象型を定義しただけではあまり用途がありません。抽象型は、これから説明する構造体型やプリミティブ型など、インスタンスを作れる具象型を下位型として定義して使います[*13](c3S5.xhtml#aABM)。

# 8.6 構造体型

構造体型(struct type, composite type)は、複数のオブジェクトをまとめて抽象化するための標準的な仕組みです。標準ライブラリでは、複素数の Complex 型や辞書の Dict 型など、実に多様な型が構造体型として定義されています。ユーザが定義する構造体型もビルトインの構造体型と区別なく扱われ、性能も遜色ありません。

構造体型のオブジェクト(構造体)にはフィールド(field)と呼ばれるオブジェクトを収めるスロットがあり、各フィールドに1つずつオブジェクトを収められます。フィールドには個別の名前があり、その名前を通じてフィールドに収められたオブジェクトを参照できます。例えば、Complex 型の複素数 3.1 - 2.4im は、図8.3に示す実部 re と虚部 im の2つのフィールドを持つ構造体です。

![](image_rsrcB23.jpg)

![](image_rsrcB24.jpg)

# 8.6.1 構造体型の定義

構造体型の定義は次のように struct キーワードもしくは mutable struct で始まり、end キーワードで終わります。[ と ] で囲んだ部分は省略可能です。Name は定義する型名で、SuperType は定義する型の上位型です。field1 や field2 で示されるフィールドの数は任意です。::T1 や ::T2 は各フィールドの型注釈を示します。

![](image_rsrcB25.jpg)

struct で定義される構造体型は不変(immutable)な構造体型といい、mutable struct で定義される構造体型は可変(mutable)な構造体型といいます。不変な構造体型のオブジェクトは、フィールドが保持するオブジェクトを別のオブジェクトに変更できません。可変な構造体型のオブジェクトはその逆で、フィールドのオブジェクトを別のオブジェクトに変更できます[*14](c3S5.xhtml#aA3X)。構造体型の不変性については、[8.8節](c3S5.xhtml#a97J)で改めて説明します。

<: SuperType を省略すると、抽象型の定義と同様に Any 型直下の下位型になります。特別な設計意図がない限りは省略して問題ありません。

フィールドの型注釈は、各フィールドが保持するオブジェクトに対する型の制約です。すなわち、ある field フィールドが field::T と定義されたとすると、そのフィールドのオブジェクトは T 型かその下位型のオブジェクトでなければなりません。フィールドの型注釈を省略すると、::Any という型注釈が付いたものと等価になり、任意の型のオブジェクトがそのフィールドに保持できます。

フィールドの型注釈を付けると、プログラムの設計やプログラマの意図がわかりやすくなる利点があります。また、フィールドの型が決定できればコンパイラがプログラムを最適化できる機会も増えて性能向上につながるので、可能な限り指定することが推奨されます。

# 8.6.2 構造体型定義の例

単純な構造体型を定義して動作を確認しましょう。次の ChemElem 型は、化学の元素を表現する構造体型です。

![](image_rsrcB26.jpg)

ChemElem 型には、次の2つのフィールドを定義しました。

●原子番号(atomic number)を保持する Int 型の number フィールド

●元素記号(chemical symbol)を保持する String 型の symbol フィールド

構造体型には、インスタンスを作るための関数であるコンストラクタ(constructor)が自動的に作られます。このコンストラクタを特にデフォルトコンストラクタ(default constructor)と呼びます。デフォルトコンストラクタ以外のコンストラクタについては[9.3節](c434.xhtml#a97R)で説明します。デフォルトコンストラクタの名前は型名と同じです。デフォルトコンストラクタに渡す引数の順序は、構造体型定義のフィールドの順序に合わせます。

![](image_rsrcB27.jpg)

作成した元素オブジェクトは println 関数などで出力できます。

![](image_rsrcB28.jpg)

元素オブジェクトの各フィールドに保持されたオブジェクトは、次のようにドット(.)を使って参照できます。

![](image_rsrcB29.jpg)

struct キーワードで定義した構造体は不変なので、フィールドのオブジェクトを変更できません。例えば、次のようにフィールドを書き換えようとすると例外が発生します。

![](image_rsrcB2A.jpg)

元素の原子番号と元素記号がプログラムの実行中に変わることはまず考えられないので、ChemElem 型が不変であることは実用上問題にはならないでしょう。むしろ、誤ってフィールドを書き換えてしまうバグを防げるので好都合といえます。

# 8.6.3 パラメトリックな構造体型

構造体型はパラメトリック型としても定義できます。次の構造体型は、フィールドの型がパラメータ化されているので、任意の型のオブジェクトをフィールドに保持できますが、x と y が(抽象型を含む)同じ型のオブジェクトであるという制約が付きます。パラメータがどの型になるかは、デフォルトコンストラクタに渡された引数から推論されます。

![](image_rsrcB2B.jpg)

次のようにパラメータの型を指定したコンストラクタ呼出しもできます。

![](image_rsrcB2C.jpg)

<: を使ってパラメトリック型のパラメータに制約をかけられます。制約が課された構造体型は、その制約に反するオブジェクトの作成が不可能になります。例えば、大小比較ができる数の型になるように、T <: Real という制約をかけると次のようになります。

![](image_rsrcB2D.jpg)

# 8.6.4 シングルトン

シングルトン(singleton)はフィールドを1つも持たない不変な構造体型を指します。このような構造体型は1種類のオブジェクトしか表現できないので、シングルトンのオブジェクトはどう作っても互いに区別不可能なオブジェクト、すなわち同一のオブジェクトになります。

![](image_rsrcB2E.jpg)

オブジェクトが存在すること自体に意味がある特殊な値を作るときにシングルトンは使われます。標準ライブラリでは、nothing や missing などがシングルトンのオブジェクトです。実際、missing は標準ライブラリで次のように定義されています[*15](c3S5.xhtml#a9C1)。

![](image_rsrcB2F.jpg)

可変な構造体型ではこのようなシングルトンの構造体型にはなりません。可変な構造体型は、コンストラクタを呼び出すたびに異なるオブジェクトを作るからです。

# 8.7 プリミティブ型の定義

プリミティブ(primitive)型は、その名のとおり原始的な型です。プリミティブ型のオブジェクトは構造体のようなフィールドを持たず、固定サイズのビット列で値を表現します。また、プリミティブ型のオブジェクトは不変です。標準ライブラリにある数値型の多くはプリミティブ型です。例えば、Float64 型は64ビットのビット列で倍精度の浮動小数点数を表現するプリミティブ型です。ユーザも新しいプリミティブ型を定義できますが、ほとんどの場合において不変な構造体型か、本節で説明する @enum マクロで代用できるので、直接的にプリミティブ型を定義する機会はほとんどありません[*16](c3S5.xhtml#aA8N)。

プリミティブ型の定義は次のように primitive type で始まり、end キーワードで終わります。Name は定義する型名で、SuperType はその上位型です。他の型定義と同様に、<: SuperType は省略可能であり、省略したときは <: Any と等価です。nbits は値を表現するビット列の長さで、8の倍数を指定できます。

![](image_rsrcB2G.jpg)

次の例は、生物の塩基配列を構成するデオキシリボ核酸(deoxyribonucleic acid, DNA)の1文字(1塩基)を表現するプリミティブ型を定義しています。生物の塩基配列を構成するDNAは、基本的にACGTの4種類[*17](c3S5.xhtml#aAD9)しかとりえないので、最小サイズの8ビットで十分表現できます。

![](image_rsrcB2H.jpg)

プリミティブ型にはデフォルトコンストラクタが存在しないので、そのままではオブジェクトを作れませんが、reinterpret 関数を使うと別のオブジェクトから変換できます。reinterpret 関数はビット列の表現を変えずに、指定された型のオブジェクトに変換します。変換元のオブジェクトには、プリミティブ型と同じサイズのオブジェクトを使います。DNA 型は8ビットなので、同じサイズの UInt8 型の値から DNA 型の値を以下のように作ります。

![](image_rsrcB2J.jpg)

プリミティブ型は @enum マクロを使うとより手軽に定義できます。@enum マクロはプリミティブ型と同時にその型を持つ定数も定義します。次の例は、先の DNA 型と同様のプリミティブ型を @enum マクロで定義する例です。DNA_A などは DNA 型の定数として使用できます。

![](image_rsrcB2K.jpg)

@enum マクロで定義した型は特に列挙型(enum型)とも呼ばれ、主に C の列挙型をラップするときに使われます。詳しくは[15.1節](c6GX.xhtml#a98R)を参照してください。

# 8.8 不変性と可変性

コレクションの不変性と可変性に関しては[5.3.1項](c251.xhtml#a9VR)で簡単に解説しましたが、ここで不変性と可変性について再確認をしておきます。

[8.6.2項](c3S5.xhtml#a9J5)で定義した ChemElem 型に続けて、リスト8.1のような元素の周期表を表現する PeriodicTable 型を定義してみます。

![](image_rsrcB2M.jpg)

PeriodicTable は不変な構造体型なので、後から新しい元素を追加できないと考えるかもしれませんが、実際には可能です。

![](image_rsrcB2N.jpg)

この操作が可能な理由は不変性の意味を正しく理解すればわかります。構造体型の不変性とは、フィールドが保持するオブジェクトが不変であるという意味ではなく、フィールドとオブジェクトの束縛関係が不変であるという意味です。周期表の例では、周期表の各フィールドが保持するオブジェクト(配列)の内容が変わっただけで、オブジェクトが別のオブジェクトに入れ替わったわけではありません。配列自体は不変なオブジェクトではありませんから、フィールドの配列に新しい要素を追加することはまったく問題ないのです。

一方、フィールドのオブジェクトを別のオブジェクトに取り替えようとする次のような操作はできません。フィールドに代入する操作はフィールドとオブジェクトの束縛関係を変えてしまうからです。

![](image_rsrcB2P.jpg)

もし elements フィールドの元素リストを空にしたいのなら、フィールドとオブジェクトの結びつきを変えないようにしなければなりません。例えば empty! 関数を使えば、これは可能です。

![](image_rsrcB2R.jpg)

可変な構造体のフィールドは他のオブジェクトに交換可能ですが、プログラムの動作を把握しづらくなる傾向にあります。また、制約の弱さからコンパイラの最適化を妨げることも考えられます。安全性や効率の観点から、可能ならば不変な構造体型の使用が推奨されます。構造体型を定義する構文が(意図的に mutable と付けない限り)不変な構造体型をデフォルトとしているのには、こうした意図があります。

# 8.9 型のリフレクション

リフレクション(reflection)とは、プログラムが自身の情報を得る手法です。Juliaのような動的言語では、リフレクションが比較的多用されます。リフレクションは、[第10章](c4G7.xhtml)で扱うメタプログラミングでも重要です。本節では、型に関するリフレクションを解説します。

# 8.9.1 型の性質

[8.1節](c3S5.xhtml#a97B)で説明したように、型は抽象型と具象型に大別できます。ある型がそのどちらに属すかは、isabstracttype 関数と isconcretetype 関数でわかります。

![](image_rsrcB2S.jpg)

パラメトリック型は、パラメータを代入しないと具象型と判別されないことに注意してください。

![](image_rsrcB2T.jpg)

ismutable 関数はオブジェクトが可変かどうかを判定します[*18](c3S5.xhtml#a9EE)。

![](image_rsrcB2U.jpg)

isbitstype 関数は、型が不変で、他のオブジェクトへの参照を保持しないオブジェクトを作るかを判定します。この関数が true を返す型のオブジェクトは一般に非常に軽量です。

![](image_rsrcB2V.jpg)

これらのほか、構造体型かを判定する isstructtype 関数、プリミティブ型かを判定する isprimitivetype 関数などがあります。詳しくは各関数のドキュメントを参照してください。

# 8.9.2 構造体型のフィールド情報

構造体型のフィールドに関する情報は重要です。標準ライブラリには、field から始まるフィールド情報を得る関数がいくつか定義されています。リスト8.2に示す関数は、フィールドの数を取得する fieldcount 関数、フィールドの名前を取得する fieldname 関数、フィールドの型を取得する fieldtype 関数を使って、構造体型の情報を出力する関数です。

![](image_rsrcB2W.jpg)

実行例は次のとおりです。ChemElem 型の定義は[8.6.2項](c3S5.xhtml#a9J5)を参照してください。

![](image_rsrcB2X.jpg)

これらのほか、フィールドの名前と型をタプルで返す fieldnames 関数と fieldtypes 関数、構造体先頭からフィールドまでのオフセットを返す fieldoffset 関数が用意されています。

まとめ

本章ではJuliaの型システムについて解説しました。インスタンスを作れない抽象型は上位型として他の型をまとめ、インスタンスを作れる具象型はオブジェクトの表現方法を決定するということをよく理解しておいてください。また、パラメトリック型は他のスクリプト言語ではあまり見かけませんが、Juliaでは非常に重要な役割を果たしますので必ず確認しておいてください。struct キーワードで定義される構造体型は、型を定義するときに最もよく使う方法です。構造体型に関しては、不変性の意味を誤解しないよう注意してください。コンストラクタの定義方法は多重ディスパッチとともに第9章で解説します。

注

[*1](c3S5.xhtml#a9ZS) supertypeには「基本型」という訳語をあてるのが一般的ですが、原語との対応や意味のわかりやすさを重視して、本書ではこのように呼びます。[*2](c3S5.xhtml#a9R9) subtypeには「派生型」という訳語をあてるのが一般的ですが、上位型(supertype)と同じ理由で本書ではこのように呼びます。[*3](c3S5.xhtml#aACT) これは標準ライブラリにあるInteractiveUtils.jlパッケージの関数ですが、REPLはInteractiveUtils.jlを自動で読み込むので、明示的にパッケージを読み込まなくても使えます。[*4](c3S5.xhtml#aA87) 具象型は他の型の上位型にはなりえません。[*5](c3S5.xhtml#a9WE) より正確には、シンボル、isbits 関数が true を返すオブジェクト、それらのタプルがパラメータとして使えます。[*6](c3S5.xhtml#aADT) パラメータに値を代入すると、具象型になるパラメトリック型もここでは具象型と考えています。例えば、Vector{Int} は具象型なのでVector型を具象型に分類しています。[8.9.1項](c3S5.xhtml#aADU)の説明も参考にしてください。[*7](c3S5.xhtml#aA32) この where はキーワードではないので変数としても使えます。[*8](c3S5.xhtml#aA05) Juliaのドキュメントではtype unionという表記も見られますが、同じものです。[*9](c3S5.xhtml#aA2J) この例でユニオン型を使わず String[] で cities を初期化してしまうと、欠損値を追加するときエラーになります。[*10](c3S5.xhtml#a9R2) パラメトリック型では、const がなくても定数になりますが慣習的に const を付けています。[*11](c3S5.xhtml#aA2U) abstract type は2語セットでキーワードのように扱われ、それぞれ個別にはキーワードではありません。abstract も type も個別に普通の変数名として使えます。[8.6.1項](c3S5.xhtml#aA2V)で扱う mutable も同様にキーワードではありません。[*12](c3S5.xhtml#a9KD) Realなどはこの慣習に従っていませんが、数に関わる型はすでに数学的に意味のある名前があるので、その名前を使っています。[*13](c3S5.xhtml#a9NR) 既存の型を後から定義した抽象型の下位型にすることはできません。[*14](c3S5.xhtml#a9U0) 可変な構造体型の任意のフィールドを const キーワードで不変にする機能がJulia 1.8で追加されました。不変な構造体型と同様に、不変なフィールドは別のオブジェクトに変更できません。[*15](c3S5.xhtml#a9AU) nothing はJuliaの処理系内部でも使われる基礎的なオブジェクトなので、missing と同じように定義されているわけではありません。[*16](c3S5.xhtml#a9XM) 効率や性能の面からも不変な構造体型ではなくプリミティブ型を使う理由はありません。[*17](c3S5.xhtml#a9EK) それぞれアデニン(adenine)・シトシン(cytosine)・グアニン(guanine)・チミン(thymine)の頭文字です。[*18](c3S5.xhtml#a9DH) isimmutable 関数もありますが、現在は非推奨です。

---

# ![](image_rsrcB2Y.jpg)

多重ディスパッチはJuliaの根幹をなす重要な概念です。整数の加算や文字列の出力などの基本機能から、行列積の計算や統計処理などの高レベルな機能まで、Juliaのプログラムでは至るところで多重ディスパッチが使われています。

多重ディスパッチを一言で説明すれば、関数を呼び出すときに引数の型に応じて実行する実装(メソッド)を動的に選ぶ機能といえます。こういってしまうとあまりに単純に聞こえますが、実のところ非常に柔軟で応用範囲の広い機能です。本章の目的は、この簡潔な説明が意味するところを実例を見ながら深く理解することです。

# 9.1 メソッド

本書ではここまで関数(function)という語を特に注意せず使ってきましたが、実はその裏にメソッド(method)という重要な概念があります。関数とメソッドは、Juliaでははっきり区別されて使われる用語です。多重ディスパッチを正しく理解するには、この2つの言葉を使い分けられるようになるのが早道ですので、まずはこれらの言葉の意味を解説しましょう。

# 9.1.1 関数とメソッドの関係

Juliaにおいて関数とは、処理を抽象化したものです。つまり、ある処理を誰がどうやるのかという詳細はさておき、「これこれという操作をする」ということを抽象的に表現したものが関数です。具体例がないと話が見えてこないと思うので、* 関数で説明しましょう。* 関数は、「何かと何かをかける(乗算する)」という抽象的な操作を表現しています。これは、何をかけるのか、どうやるのかという細かいことは無視した、抽象的な表現です。例えば 2 * 3 という整数の乗算と 2.0 * 3.0 という浮動小数点数の乗算は、CPUのレベルでは異なる命令で実行されます[*1](c434.xhtml#a9M6)が、Juliaのレベルではその違いは無視してどちらも * 関数で表現されます。同様に、複素数の積や行列積なども同じ * 関数で抽象化されています。

一方、メソッドとは関数が表現する操作の具体的な実装です。言い換えれば、関数が隠した処理の実務を担うのがメソッドです。この様子を模式的に図9.1に示します。* 関数の例でいえば、整数どうしの積や浮動小数点数どうしの積を計算する方法を具体的に定義しているのがメソッドです。言い換えれば、関数は引数の型に依存しないのに対し、メソッドは引数の型に応じて個別に用意されます。

![](image_rsrcB2Z.jpg)

標準ライブラリにはどれくらいのメソッドがあるでしょうか。methods 関数を使うと、引数として与えられた関数に実装されているメソッドの一覧(メソッドリスト、あるいはメソッドテーブル)が得られます。基本的な4つの関数について、Julia 1.6でメソッド数を数えてみると次のようになりました。これらの関数には実に多くのメソッドが紐付いていることがわかります。

![](image_rsrcB30.jpg)

# 9.1.2 メソッドの多重ディスパッチ

関数呼出しで実際に使われるメソッドは、メソッドリストの中から引数の型に応じて決められます。この仕組みを多重ディスパッチ(multiple dispatch)と呼びます。メソッドを定義する方法と多重ディスパッチがメソッドを決定する仕組みについて詳しくは[9.2節](c434.xhtml#a97P)で具体例を使って説明しますが、ここで概要を簡単に述べておきましょう。

多重ディスパッチのディスパッチ(dispatch)という言葉には、英語で「何かをどこかへ送る」という意味があります。Juliaの関数呼出しでは、関数に渡された引数がメソッドへと送られます。このとき、どのメソッドへ送られるかは引数の数と型によって決まります。引数の数の違いも型の違いと考えれば、シンプルに引数の型によって決まるといってもよいでしょう。Juliaの多重ディスパッチではすべての引数の型がその順序にかかわらずメソッドの決定に等しく影響するという点が重要です。つまり、複数の引数が影響するという意味で、多重(multiple)的なディスパッチなのです。

多重の持つ意味は、そうでないディスパッチと比較するとよりわかりやすいでしょう。例として、Pythonのディスパッチを取り上げます。Pythonはシングルディスパッチ(single dispatch)を行うプログラミング言語です。すなわち、obj.func(a, b, ...) のように書くと、obj の型のみでどのメソッドが実行されるかが決まり、他の引数(a や b など)はメソッドの選択には影響しません。もし他の引数の型に応じて実行する処理を切り替えたければ、呼び出されたメソッドの中で分岐処理を書く必要があります。一方、Juliaの多重ディスパッチでは、関数に渡された引数は最初の引数から最後の引数までメソッドのディスパッチに等しく影響します。これがシングルディスパッチと多重ディスパッチの大きな違いです。

# 9.1.3 総称関数とビルトイン関数

[9.1.1項](c434.xhtml#aA13)では関数とメソッドの関係を説明しましたが、実は用語の使い方がほんの少し不正確でした。そこで関数と呼んでいたものは、より正確にいうと総称関数(generic function)と呼ばれるものです。総称関数はメソッドを複数持てるタイプの関数であり、多重ディスパッチでメソッドを定義し、機能を拡張できます。Juliaには総称関数以外の関数もありますが、普段使う関数の大半は総称関数ですので、あえて広義の関数と総称関数を使い分けるのも煩わしさがあります。したがって、本書では特に混乱が生じないと考えられるときには、総称関数も単に関数と呼びます。

総称関数でない関数で代表的なものは === 関数です。この関数は2つのオブジェクトが区別不可能かどうかを判定する(区別不可能なら true を返す)関数ですが、総称関数ではなく、ビルトイン関数(built-in function)と呼ばれるタイプの関数です。ビルトイン関数は最も基本的な関数であり、Julia自身では定義できません。また、多重ディスパッチでの拡張もできません。ビルトイン関数はJuliaの低レベルの機能を提供する関数なので、一般のユーザが使う機会があるものはあまり多くありませんが、=== 関数のほか、isa, <:, isdefined, ifelse, getfield, setfield! などの関数は、一般のユーザも使う機会の多いビルトイン関数です[*2](c434.xhtml#a9C4)。ある関数が総称関数かビルトイン関数かは、次のようにREPLに入力すればわかります。

![](image_rsrcB31.jpg)

# 9.2 メソッドの定義

メソッドを定義する構文は、[7.7節](c315.xhtml#a977)で説明した関数を定義する構文と変わりません。実は、関数を初めて定義したとき、同時にその関数の最初のメソッドを定義しているのです。次の例でいえば、foo 関数の定義と同時に、foo 関数の最初のメソッドが1つ定義されます。

![](image_rsrcB32.jpg)

関数に新しいメソッドを定義することは、関数のオーバーロード(overload)とも呼ばれます。しかし、他のプログラミング言語のオーバーロードとの混同[*3](c434.xhtml#a9GY)を防ぐために、本書ではこの用語を使わないことにします。

# 9.2.1 引数の数によるディスパッチ

まずは最もシンプルな多重ディスパッチから説明しましょう。多重ディスパッチでは、受けとる引数の数が異なるメソッドは別々のメソッドとみなされます。例えば、引数を1つもとらない foo() メソッドを定義した後、引数を1つとる foo(x) メソッドを定義すると、新たなメソッドが foo 関数のメソッドリストに追加されます。

![](image_rsrcB33.jpg)

foo 関数を呼び出すと、引数の数に応じて異なるメソッドが実行されます。

![](image_rsrcB34.jpg)

メソッドが定義されていない数の引数を渡すと、MethodError 例外が発生します。

![](image_rsrcB35.jpg)

引数の数が適合するメソッドを新しく定義すれば、この関数呼出しは成功します。

![](image_rsrcB36.jpg)

このように、メソッドを追加定義すれば、関数の動作を拡張できるわけです。

[7.7.2項](c315.xhtml#aA9Z)で説明した可変長引数を使うと、任意の数の引数をとるメソッドを定義できます。次の args... は1個以上の引数でなく、0個以上の引数に対応することに注意してください。

![](image_rsrcB37.jpg)

# 9.2.2 引数の型によるディスパッチ

多重ディスパッチには引数の数だけでなく、引数の型も影響します。引数の数の違いを型の数の違いと考えてしまえば、すべては引数の型の違いに帰着できます。メソッド定義のときに引数に型注釈([7.8.3項](c315.xhtml#a9AV))を付けると、関数を呼び出すときに引数の数が合致していたとしても、制約に適合しないメソッドは呼び出せません。なお、ここで使っている repr 関数はオブジェクトを文字列化するための関数です[*4](c434.xhtml#a9AW)。

![](image_rsrcB38.jpg)

型注釈を付けずにメソッドを定義した場合は、::Any という型注釈を付与したのと同等とみなされ、引数の型が何であってもそのメソッドを呼び出せます。一般に、a::T という引数の型注釈について、a isa T が true となるとき、つまり引数 a が型 T かその下位型のオブジェクトであるときのみ、その制約をパスできます。そして、渡された引数がメソッドのすべての制約をパスできれば、そのメソッドを呼び出せます。

![](image_rsrcB39.jpg)

bar(x) を定義した後に bar(x::Int) を定義しても、前者のメソッドが取り消されるわけではないことに注意してください。引数の数や型の制約が異なれば、メソッドの定義は互いに独立しています[*5](c434.xhtml#a9AF)。

# 9.2.3 ディスパッチの優先順位

関数に渡された引数が複数のメソッドと適合するときはどうなるでしょうか。引数の型が複数のメソッドと適合するとき、最も制約の強いメソッドが選ばれます。例えば、Int と Integer では前者のほうが制約が強い(Int <: Integer)ので、Int 型の引数に対しては前者のメソッドが使われます。次の例では、baz 関数に制約の強さが異なる3つのメソッドが定義されていますが、この関数を呼び出すと型が適合するメソッドの中で最も制約が強いメソッドが呼び出されていることがわかります。メソッド定義の順序は影響しません。例えば、次の3つのメソッドを逆順に定義しても動作は変わりません。

![](image_rsrcB3A.jpg)

メソッドに複数の型注釈があるときは、すべての型注釈が等しくメソッドの決定に影響します。特定の位置にある引数の型を優先することはありません。

![](image_rsrcB3B.jpg)

型に関する制約の組合せによっては、どのメソッドを呼び出すか1つに決定できないことがあります。特に、型注釈が抽象型のときには、このようなメソッド選択の曖昧性が発生しやすいです。先の例はあえてそのような曖昧性が生じるように定義したので、例えば次のように Int 型と Float64 型の引数の組合せを渡すと、メソッドを決定できないというエラーが発生します。

![](image_rsrcB3C.jpg)

この場合、エラーメッセージにヒントとして示されているように、先の2つのメソッドよりさらに制約の強いメソッドを定義すれば曖昧性を排除できます。

![](image_rsrcB3D.jpg)

# 9.2.4 パラメータを持つメソッドのディスパッチ

型の一部をパラメータ化することで、より強力な多重ディスパッチが可能になります。例として、2つの引数をとるメソッドを定義するときに、2つの引数が同じ型であるという制約をかけたいとしましょう。qux(x::Int, y::Int) のように具体的な型で制約を課せば Int 型についてはこの制約を実現できますが、それ以外の型では使えません。型の数だけ実装が同じメソッドを定義するというのも現実的とはいえません。

このようなとき、パラメトリック型の構文([8.2.3項](c3S5.xhtml#aAFT))で使った where を使えば、引数の型をパラメータ化して、次のような書き方で引数の型が同じという制約がついたメソッドを定義できます。

![](image_rsrcB3E.jpg)

型注釈の型がパラメトリック型のときにも、同様にしてパラメータに制約をかけられます。次の例では、第2引数の配列の要素は第1引数と同じ型であるという制約をかけています。

![](image_rsrcB3F.jpg)

パラメータ自体に制約をかけるときには、where T <: S のように書きます。例えば、qux(x::Vector{T}) where T <: Real とメソッドを定義すれば、このメソッドが受けとる配列の要素は Real 型かその下位型でなければなりません。この場合はパラメータを使わずに qux(x::Vector{<:Real}) と書いても意味は同じです。また、パラメータが複数あるときには、where S where T か、より短く where {S, T} のように書きます。

# 9.2.5 型によるディスパッチ

Juliaでは型もオブジェクトなので、関数を呼び出すときに型を引数として渡せます。例えば、文字列をパースする parse 関数やデータの変換を行う convert 関数は、最初の引数に型をとります。

型を引数とするディスパッチを行うには、Type{T} という型のコンストラクタを使います。T isa Type{T} は true になるので、引数として型 T が渡されると ::Type{T} という型注釈に適合します。なお、型によるディスパッチでは渡された引数は使わないので、省略するのが普通です。

![](image_rsrcB3G.jpg)

標準ライブラリの parse 関数では、概念的には次のように parse 関数のメソッドが定義されているわけです。

![](image_rsrcB3H.jpg)

# 9.2.6 メソッドを持たない関数の定義

関数はメソッドが存在しないと呼び出せませんが、応用的な用途で関数のみが必要なときもあります。例えば、あるモジュールで入出力に関して一定のプロトコル(protocol)[*6](c434.xhtml#a9U9)を定めた関数群を定義し、そのモジュールのユーザはその関数に自分の型に対応したメソッドを定義し、機能を拡張する場合などがあります。この場合、そのモジュールではメソッドを定義する必要はなく、[9.4節](c434.xhtml#a97S)で紹介するように、モジュールの外部から関数にメソッドを追加して機能を実装します。

メソッドを持たない関数は、次のように function キーワードで定義できます。関数名の後に引数のリストがないことに注目してください。この時点ではまだメソッドがないので、この関数を呼び出す手段がありませんが、後から新しいメソッドを定義すれば使えるようになります。

![](image_rsrcB3J.jpg)

# 9.3 コンストラクタ

コンストラクタ(constructor)はオブジェクトを生成するための関数です。struct キーワードなどを使って構造体型を定義すると、デフォルトコンストラクタが自動で作られますが、通常の関数と同じようにコンストラクタにも新しいメソッドを定義できます。自分でコンストラクタのメソッドを定義すれば、一部の引数をオプショナルにしたり、引数をチェックしたりするなど、便利な機能を追加できます。多重ディスパッチのおかげで、複数あるコンストラクタを引数の型に応じて自在に切り替えられます。

コンストラクタには、次の3つの種類があります。

1\. 自動的に作られるデフォルトコンストラクタ([8.6.2項](c3S5.xhtml#a9J5))

2\. new 関数を呼び出す内部コンストラクタ(9.3.1項)

3\. new 関数を呼び出さない外部コンストラクタ(9.3.2項)

デフォルトコンストラクタは、構造体型を定義すると自動で作られるコンストラクタです。後の2つのコンストラクタは、new という特殊な関数を呼び出すかどうかで区別されます。本書では、前者を内部コンストラクタ(inner constructor)、後者を外部コンストラクタ(outer constructor)と呼ぶことにします。

# 9.3.1 内部コンストラクタ

まずは内部コンストラクタを説明しましょう。構造体型の内部コンストラクタは、struct キーワードと end キーワードの間で定義します。この関数定義は[7.7節](c315.xhtml#a977)で説明した構文と同じですが、struct と end の間でのみ使える new という特別な関数[*7](c434.xhtml#aAAG)を使うのが内部コンストラクタの特徴です。new 関数はフィールドの値を引数として順に受けとり、定義している構造体型のオブジェクトを返します。なお、内部コンストラクタは型名と同じ名前にする必要があります[*8](c434.xhtml#aAAH)。new 関数に渡される引数は convert 関数でフィールドの型に変換されるので、引数の型がフィールドの型に一致する必要はありません。

円の図形を表現する構造体型 Circle で内部コンストラクタを使う例をリスト9.1に示します。この内部コンストラクタは、見当違いな型の引数が渡されるのを型注釈で防ぎ、半径 r が負の値であれば例外を送出します。引数の検査をパスすれば、new 関数で生成した Circle 型のオブジェクトを返します。

![](image_rsrcB3K.jpg)

Circle 型のオブジェクトを作るには、デフォルトコンストラクタのときと同じようにコンストラクタを呼びます。ただし、呼び出されるコンストラクタはいま定義した内部コンストラクタですので、不正な値を渡すと例外が発生します。

![](image_rsrcB3M.jpg)

内部コンストラクタがあるとき、デフォルトコンストラクタは生成されません。すると、オブジェクトは必ず内部コンストラクタを経由して作られるので、内部コンストラクタで引数をチェックしておけば、無効なオブジェクトが作られるのを防げます。不変な構造体型では、フィールドのオブジェクトは別のオブジェクトに変えられないので、この戦略が特に有効です。

その他の発展的な話題は外部コンストラクタを説明した後に取り上げます。

# 9.3.2 外部コンストラクタ

外部コンストラクタは、他のコンストラクタ[*9](c434.xhtml#aA15)を呼び出してオブジェクトを作るコンストラクタです。外部コンストラクタは直接 new 関数を使用せず、他のコンストラクタを使って間接的にオブジェクトを作ります。内部コンストラクタと同様に、外部コンストラクタも型名と同じ名前にするのが通例です。

色を赤緑青各8ビットの組合せで表現する24ビットのRGB色モデルを考えます。赤緑青はそれぞれ8ビット(0~255)の整数を使って表現され、すべて同じ値を指定すると無彩色になります。無彩色用のコンストラクタはリスト9.2に示すように外部コンストラクタとして定義できます。実行例は次のとおりです。

![](image_rsrcB3N.jpg)

![](image_rsrcB3P.jpg)

外部コンストラクタを定義しても他のコンストラクタには影響しないので、デフォルトコンストラクタや内部コンストラクタは通常どおり呼び出せます。一般的な使い分けとして、内部コンストラクタは渡された引数のチェックなど必要最小限の機能に留め、外部コンストラクタでその他の便利な機能を提供します。また、外部コンストラクタは内部コンストラクタとの混同を避けるためか、構造体型定義の外側、つまり end キーワードより後で定義することが多いです。

# 9.3.3 パラメータ付きコンストラクタ

パラメトリック型を定義するとき、しばしばパラメータ付きコンストラクタを定義したいことがあります。パラメータ付きコンストラクタは Foo{T}(x) のようにパラメータ T を付けて呼び出せます。例えば、Dict 型は Dict{String, Int}() などのようにキーと値の型を指定して空の辞書を作成できます。

正規分布をパラメトリック型 Normal として定義し、パラメータ付きコンストラクタを定義する例をリスト9.3に示します。内部コンストラクタ内で呼び出す new 関数にもパラメータを付けられることに注意してください。Normal 型の内部コンストラクタは次のようにパラメータ付きで呼び出せます。常にパラメータを指定して呼び出すのは使いにくいので、パラメータの必要ない外部コンストラクタも定義しています。

![](image_rsrcB3R.jpg)

![](image_rsrcB3S.jpg)

# 9.3.4 不完全な初期化

構造体型が自身の型をフィールドに持つとき、内部コンストラクタは必要不可欠です。自身の型と同じオブジェクトへの参照があるとき、新しいオブジェクトを作ろうにもデフォルトコンストラクタに初期値を渡せないからです。この代表的な例としては、次のような木構造を表現する構造体を考えましょう。リスト9.4の Node 型は、木構造のノード(node)を表す構造体型です。index フィールドには何らかの整数を保持し、parent フィールドには自分の親ノードへの参照を持ちます。

![](image_rsrcB3T.jpg)

この例では、内部コンストラクタが2つ定義されていることに注目してください。多重ディスパッチにより、引数の数に応じて呼び出されるコンストラクタが変わります。2つ目のコンストラクタはデフォルトコンストラクタと同じ動作をしますが、1つ目のコンストラクタは引数を1つしかとりません。このコンストラクタは、index フィールドの値のみを設定し、parent フィールドは未定義のまま Node 型のオブジェクトを作ります。このように、new 関数は、実際のフィールド数より少ない引数でオブジェクトを作ることが許されているのです。これを不完全な初期化(incomplete initialization)と呼びます。このコンストラクタは、例えば木構造の根にあたるノードを作るときに必要になります。

![](image_rsrcB3U.jpg)

この例でもし未定義のフィールドを参照しようとすると、UndefRefError 例外が発生します。フィールドが未定義かどうかは、isdefined 関数でわかります。

![](image_rsrcB3V.jpg)

不完全な初期化に関していくつか注意点を述べておきます。一般には、new 関数で構造体のすべてのフィールドを初期化すべきです。フィールドを未定義のままにすることは推奨されませんので、使用は必要なときのみに留めるべきでしょう。Node 型のケースでは、parent フィールドの型を Node ではなくユニオン型([8.3節](c3S5.xhtml#a97D))を使って Union{Node, Nothing} にしておけば、未定義値の代わりに nothing を入れられるので、ここで紹介したトリックは必要ありません。また、すでにオブジェクトがセットされているフィールドを未定義にする方法は用意されていません。さらに、フィールドの型が Int などのプリミティブ型や参照を持たない不変な構造体型のときには、new 関数で初期化をしないと、未定義値ではなく、フィールドの型を持つ任意のオブジェクトに設定されるので注意が必要です。

# 9.3.5 オブジェクト解放時の処理

可変なオブジェクトには、オブジェクトが不要になったときに実行される処理を関数として登録できます。これはコンストラクタとともに使われることが多いので、ここで紹介します。

finalizer 関数は、第2引数のオブジェクトが不要になったときに、第1引数に指定された関数を呼び出します。つまり、finalizer(f, x) を実行すると、x が不要になったときに f(x) が呼び出されるというわけです。典型的には、次のように内部コンストラクタで finalizer 関数を呼び出し、オブジェクト解放時の後処理を登録すると、確実にその後処理が実行されます。

![](image_rsrcB3W.jpg)

finalizer 関数の典型的な応用は、Juliaで管理していない資源の解放です。例えば、C のライブラリをJuliaから使う状況を考えてみましょう。このとき、C のオブジェクトをJuliaは直接管理していないので、C のオブジェクトをラップしたJuliaのオブジェクトが解放されても、対応する C のオブジェクトは解放されません。Juliaのオブジェクトが解放されると同時に C のオブジェクトも解放されるには、finalizer 関数を使って C のオブジェクトを解放する処理を呼び出すようにすればよいわけです。

finalizer 関数が呼び出す処理は、オブジェクトが不要になった後、いつ実行されるか予測できないということに注意してください。したがって、オブジェクトが不要になった後、長い時間登録した後処理が行われない可能性もあります[*10](c434.xhtml#a9WH)。ある時点ですぐさま登録した後処理を実行したいときは、finalize 関数をオブジェクトに適用します。

# 9.4 多重ディスパッチを使った機能拡張

多重ディスパッチで機能を拡張できるのは、自分で定義した関数だけではありません。コレクションの要素数を返す length 関数や乗算を行う * 関数などの標準ライブラリにある関数は、多重ディスパッチを使ってユーザが定義した型にも拡張できます。本節では、標準ライブラリなどにある既存の関数の機能を多重ディスパッチで拡張する方法を紹介します。

# 9.4.1 他モジュールにある関数の拡張

他モジュールで定義された関数を拡張するには2つの方法があります。import文で関数を現在のモジュールに取り込んでから拡張する方法と、モジュールを通して関数を拡張する方法です。まずは前者の例を見てみましょう。

![](image_rsrcB3X.jpg)

これと同じことは、次のようにモジュールの関数を直接拡張する方法でも書けます。これが2つ目の方法です。import文を使わず、モジュール A に直接言及して A.foo(...) = ... のように関数を拡張していることに注目してください。もし、function キーワードを使うなら、function A.foo(...) ... のように書きます。

![](image_rsrcB3Y.jpg)

どちらの方法でメソッドを定義しても、次のように複数のメソッドが定義された A.foo 関数を呼び出せます。

![](image_rsrcB3Z.jpg)

両者を比べると、後者のほうが定義場所でどのモジュールの関数を拡張しているかがわかりやすいでしょう。前者の書き方では、import文の場所とメソッド定義の場所が離れると、関数の出どころがわかりにくくなる問題がありますので、本書では後者の書き方を推奨します。なお、using文で暗黙的に取り込まれた関数は、前者の書き方では拡張ではなく新しい関数の定義になりますので注意してください。

ここで説明した例で注目してもらいたいのは、モジュールを定義し終えた後で関数を拡張している点です。つまり、関数の機能はモジュールを定義し終えた後でも拡張できるのです。これは、標準ライブラリで定義されている関数の機能を、ユーザが定義した型にも拡張できることを意味しています。実際、Base モジュールなどにある関数の機能を後から拡張することは、Juliaでは日常的に行われます。例えば、自分で定義した数値型に加算(+ 関数)や乗算(* 関数)を定義したり、イテレータを実装したりすることは、まったく特別なことではありません。以降では、実践的な例を解説します。

# 9.4.2 Baseモジュールにある関数の拡張

Base モジュールの関数をユーザの定義した型に拡張する例を見てみましょう。既存の関数を拡張することで、標準ライブラリと共通したAPIでユーザの型を操作できるようになります。

スタック(stack)を表現する構造体型にいくつかの操作を実装します。スタックは最初に入れた要素が最後に取り出されるコレクションです。Base モジュールにはスタックの操作に適切な関数があるので、それらを拡張して操作を実装します。まずは Stack 型の定義とコンストラクタをリスト9.5に示します[*11](c434.xhtml#a9B0) [*12](c434.xhtml#a9B1)。

![](image_rsrcB40.jpg)

続いて、次の5つの基本的な関数のメソッドを Stack 型に定義します。これだけメソッドを定義しておけば、スタックとして十分実用的です。実装例をリスト9.6に示します[*13](c434.xhtml#a9E5)。

●eltype 関数

スタックの要素の型を取得する

●length 関数

スタックの要素数を取得する

●isempty 関数

スタックが空かを判定する

●push! 関数

スタックの末尾に要素を追加する

●pop! 関数

スタックの末尾から要素を取り除く

![](image_rsrcB41.jpg)

これらの関数の使用例は次のとおりです。

![](image_rsrcB42.jpg)

以降では、実用的に便利な関数拡張の発展例をいくつか紹介します。

# 9.4.3 型変換の実装

ある型を定義したとき、既存のデータ型との相互変換を定義すると何かと便利です。例えば、構造体のフィールドに新しい値を設定するときや配列の値を更新するとき、convert 関数を使って型が変換されます。convert 関数は第1引数に変換先の型をとり、第2引数に変換対象のオブジェクトをとります。

自分で定義した型に convert 関数のメソッドを追加してみましょう。具体例として、シンプルな文字列型を定義します。これから定義する ShortString 型は、UInt64 型の整数を使って短い文字列を表現する型です。単純化のため、文字集合はASCIIに限定し、終了位置はNULバイトで示すことにします。NULバイトを除けば7文字までの文字列を表現可能です。表現できる長さを限定することで、メモリサイズがたった8バイトで済むのが利点です。例えば、元素記号を文字列で表すには、この ShortString 型で十分です。

まずは、ShortString 型に加え、コンストラクタと String 型から ShortString 型への変換を定義します。リスト9.7に実装例を示します。変換には[9.2.5項](c434.xhtml#a9PZ)で説明した型によるディスパッチを使っていることに注意してください。変換する文字列が ShortString 型で表現できないときは、InexactError 例外を送出します。なお、説明のためにあえて ShortString 型を AbstractString 型の下位型にしていませんが、本来はそうすべきところです[*14](c434.xhtml#a9R0)。

![](image_rsrcB43.jpg)

convert 関数を定義しておくと、多くの場面でJuliaが型の変換を自動で行ってくれます[*15](c434.xhtml#aABA)。例えば次のようなケースでは、push! 関数に渡す引数やデフォルトコンストラクタに渡す引数をあらかじめ ShortString 型に変換しなくても、String 型のオブジェクトを渡せばデータ変換は勝手に行ってくれます。

![](image_rsrcB44.jpg)

# 9.4.4 オブジェクト表示の実装

ShortString 型はまだあまり文字列らしいとはいえません。例えば、リスト9.7のコードを使ってREPLでオブジェクトを表示すると、次のような生のバイト列が表示されてしまいます。今度はこれを修正しましょう。

![](image_rsrcB45.jpg)

オブジェクトの表示は基本的に show 関数が担います。show 関数を拡張するには、型 T に対して show(out::IO, obj::T) メソッドを定義するのが基本です。ここで、第1引数の IO 型は入出力に使われるストリームを表す抽象型であり、オブジェクト obj のテキスト表現はこの out ストリームに書き込みます。

もう1つ、print 関数もオブジェクトの表示に関わる重要な関数です。show 関数はREPLでの表示にも関わってきますが、print 関数はその名のとおり print 関数や println 関数で出力するときの表示を担います。文字列を含む一部の型では、次のように show 関数と print 関数の表示方法が異なります。文字列をREPLで表示するときはクォートやエスケープ処理が必要ですが、それ以外のときは必要ありません。なお、print 関数が実装されていないときは show 関数が使われるので、表示に差がないときは前者のみ実装すれば十分です。

![](image_rsrcB46.jpg)

オブジェクト表示の実装例をリスト9.8に示します。print 関数のメソッドは素朴にバイト列を出力しているだけですが、show 関数のメソッドは処理を String 型のメソッドに移譲しています。このように、型の変換と移譲をうまく使うと実装が単純になることが多いです。なお、この例で使用している sprint 関数は第1引数の関数を呼び出してオブジェクトを文字列化する関数です。

![](image_rsrcB47.jpg)

show 関数を ShortString 型に実装したので、次のように文字列らしく表示されるようになりました。

![](image_rsrcB48.jpg)

show 関数と print 関数のほか、Base モジュールの display 関数と repr 関数もオブジェクトの表示に関わる関数です。詳しい説明はそれぞれのドキュメントを参照してください。

# 9.4.5 イテレータの実装

for文はイテレータから要素を1つずつ取り出して反復します。この機能も多重ディスパッチを使ってユーザが定義した型に実装できます。

イテレータからfor文を使って要素を取り出す反復は、iterate 関数とwhile文を使って書き換えられます。iterate 関数はイテレータから次の要素を取り出す関数です[*16](c434.xhtml#aAD3)。iter をイテレータとすると、次に示すfor文を使った反復とwhile文を使った反復は等価です。

![](image_rsrcB49.jpg)

したがって、イテレータを実装するには iterate 関数を拡張することになります。実装するのは、次に示す2つのメソッドです。どちらのメソッドも、イテレータから取り出せる要素がもうないときには nothing を返し、それ以外のときには次の要素 item と状態 state をタプルで返します。

初期状態を返すメソッド

先のコード例の next = iterate(iter) で呼び出しているメソッドです。イテレータのみを引数にとります。

次の反復を準備するメソッド

先のコード例の next = iterate(iter, state) で呼び出しているメソッドです。イテレータと現在の状態の2引数をとります。

単純なイテレータを作ってみましょう。リスト9.9の BitRotator 型は、与えられたデータのビット列を1ビットずつ左に回転(左端から出たビットは右端に入る)しながら取り出すイテレータを実装しています。上で説明した2つのメソッドを定義していることに注目してください。ビットの回転が1周し終えたところで、iterate 関数のメソッドは nothing を返してイテレータを停止します。なお、length 関数のメソッドは必須ではありませんが、イテレータが生成する要素数があらかじめわかる場合は実装すべきです。詳しくは[9.5節](c434.xhtml#a97T)で説明します。

![](image_rsrcB4A.jpg)

このビット回転イテレータは次のように使えます。

![](image_rsrcB4B.jpg)

ちなみに、このイテレータの2つのメソッド定義は、k 引数をオプショナル引数([7.7.3項](c315.xhtml#a9PU))にして次のように1つの定義にまとめることもできます。

![](image_rsrcB4C.jpg)

nothing を返さない iterate 関数のメソッドを定義すれば、無制限に反復するイテレータも実装できます。また、ファイルから1行ずつ取り出す操作や木構造のトラバースも、イテレータを使って実装できます。練習としてぜひ実装してみてください。

# 9.4.6 演算子の拡張

数値に定義されている 1 + 2 などの演算子は、実際のところ +(1, 2) という関数呼出しの糖衣構文[*17](c434.xhtml#aABD)でしかありません。そして、この + 関数は他の関数とまったく同じようにメソッドを定義して拡張できます。ここで、その方法を説明します。

# 加減乗除などの演算子

+, -, *, / などの演算子は拡張可能です。ただし、構文が少し引っかかりやすいので、注意が必要です。例として、コンポジション(composition)型とその演算子を実装します。コンポジションとは、合計が1になる正の実数の組を使って組成を表現する方法です。例えば、大気や鉱物の成分比(組成)を扱うときに使われます。コンポジションを扱うとき、次の2つの演算を定義してコンポジションを元げんとする n-1 次元ベクトル空間![](image_rsrcB4D.jpg)n を構築する方法が知られています[*18](c434.xhtml#aAE9)。

![](image_rsrcB4E.jpg)

ここで、![](image_rsrcB4F.jpg)は各要素を全要素の合計で割って合計が1になるよう正規化する関数です。

コンポジション型の定義とその演算子の実装例をリスト9.10に示します。簡単のため、エラーチェックなどは省略しています。Base.*(...) でなく、Base.:*(...) などとコロン(:)を使っていることに注意してください[*19](c434.xhtml#aA0S)。

![](image_rsrcB4G.jpg)

計算例は次のとおりです。Vector 型の演算とは異なる演算になっていることがわかります。

![](image_rsrcB4H.jpg)

上に挙げた四則演算の演算子のほか、ASCII文字の演算子では %, ^, &, |, ![](image_rsrcAGC.jpg), ~, <, >, !, : なども拡張できます。また、一部のユニコード文字(∨, ±, ⊕, ⊗, など)や複合的な演算子(<<, <=, ==, ++, .. など)も演算子として定義・拡張できます。どのような演算子が拡張できるかをここで正確かつ網羅的に説明するのは難しい[*20](c434.xhtml#aA59)ので、実際に試してみるのがよいでしょう。

# インデックス参照と更新

obj[idx] のような角括弧([])を使ったオブジェクトのインデックス参照と更新の構文は getindex 関数と setindex! 関数で拡張できます。代表的なところでは、配列や辞書の参照や更新で活用されています。

getindex 関数と setindex! 関数の引数の順序は次の例を参考にしてください。

![](image_rsrcB4J.jpg)

[3.3節](cXU.xhtml#a96D)でオセロ盤に定義していたメソッドは、まさにこの2つの関数のメソッドです。また、リスト9.10でも getindex 関数のメソッドを定義していますので参考にしてください。

# プロパティ参照と更新

obj.prop のようにドット(.)を使ってオブジェクトを参照・更新する構文があります。このとき、prop をプロパティ(property)と呼びます。obj が構造体であるときは、プロパティとフィールドが同じである場合もありますが、一般にフィールドである必要はありません。

プロパティ参照と更新の構文は、それぞれ getproperty 関数と setproperty! 関数で拡張できます。プロパティ名は次のようにコロン(:)から始まるシンボルで指定します。

![](image_rsrcB4K.jpg)

構造体のプロパティ参照・更新の構文を拡張すると、通常の書き方では構造体のフィールドを参照・更新できなくなってしまうので注意してください。それでも構造体のフィールドを参照や更新したい場合は、拡張不可能なビルトイン関数である getfield 関数や setfield! 関数を使います。

プロパティ参照の拡張は、Pythonとの連携([15.2節](c6GX.xhtml#a98S))を可能にするPyCall.jlパッケージでも使われています。これは、Pythonの属性(attribute)参照の構文を真似るためです。

# 関数呼出し

func(x, y, ...) のような関数呼出し構文も拡張できます。ユーザが定義した型を T とすると、(obj::T)(x, y, ...) = ... のように関数呼出し構文を定義します。少し構文がわかりにくいので具体例を示します。標準ライブラリにある log2 関数を真似て、対数の底を変更[*21](c434.xhtml#a9XR)できるオブジェクトを作成してみましょう。

![](image_rsrcB4M.jpg)

# 拡張できない構文

注意点として、短絡評価の && と || 、代入の等号(=)、区切り文字のコンマ(,)などの構文は拡張できません。また、[9.1.3項](c434.xhtml#aA43)で言及した === 関数などのビルトイン関数は、総称関数ではないのでやはり拡張できません[*22](c434.xhtml#aA44)。

# 9.5 トレイト

抽象型を利用したメソッドの定義はシンプルでわかりやすいですが、メソッドと型の階層関係が直結するため柔軟性に欠ける点があります。そこで、トレイト(trait)を使った多重ディスパッチのテクニックを紹介します。トレイトは日本語で特性といった意味です。トレイトを使うと、型に実装された特性に応じた抽象的な関数のディスパッチを型の階層とは無関係に実現できます。

トレイトを使ったディスパッチの雛形をリスト9.11に示します。Trait 抽象型の下には、TraitA 型と TraitB 型の2種類のトレイトが定義されています。f 関数はこのトレイトを利用し、引数の型が TraitA を持つとき "Trait A" を返し、TraitB を持つとき "Trait B" を返します。最後に、TraitA を持つ Foo 型と、TraitB を持つ Bar 型と Baz 型を定義しています。

![](image_rsrcB4N.jpg)

この例で注目してもらいたいのは、Foo, Bar, Baz は互いに独立していて Trait と型の階層関係がなく、f 関数のメソッドは Foo, Bar, Baz のいずれにも直接的には定義されていないにもかかわらず、次のように型に定義されたトレイトに応じてディスパッチできる点です。

![](image_rsrcB4P.jpg)

リスト9.11の例は抽象的なので、具体的な応用例を紹介しましょう。[9.4.5項](c434.xhtml#a9MV)で定義したイテレータを考えます。一般に、イテレータにはその長さが前もってわかるものと、わからないものがあります。例えば、集合や1次元配列をイテレータとして見たとき、要素数はあらかじめ決まっているのでイテレータの長さもわかりますが、eachline 関数が返すファイルから1行ずつ取り出すイテレータ(EachLine 型)は、ファイルの最後まで読まないと行数はわかりませんから、イテレータの長さは前もってわかりません。イテレータを使った処理にはあらかじめ長さがわかっていると便利なものがある[*23](c434.xhtml#a9ZV)ので、長さがわかっているイテレータのときは length 関数を使って取得し、そうでないときは別の処理方法にフォールバックする、といった分岐をしたいことがあります。標準ライブラリでは、IteratorSize というトレイトがその役割を担っています。例として、代表的な IteratorSize トレイトの値を次に示します。

![](image_rsrcB4R.jpg)

HasLength() と HasShape{1}() はイテレータの長さがわかっていることを示しますが、SizeUnknown() は長さが不明であることを示します。これらのほかに、IsInfinite() という長さが無限であることを表す値もあります。HasLength() と HasShape{n}() の違いは、前者は次元を持たないイテレータであるのに対し、後者は n 次元のイテレータであることを意味します。IteratorSize トレイトのデフォルトは HasLength() です。このトレイト値のとき、イテレータに length 関数のメソッドを実装すべきであることに注意してください。実装しないと、collect 関数や内包表記など、内部で length 関数を呼び出す機能が使えません。詳しくは、IteratorSize トレイトのドキュメントとイテレータのインタフェースに関するドキュメント[*24](c434.xhtml#aA1U)を参照してください。

まとめ

本章では、非常に重要な機能である多重ディスパッチを解説しました。+ 関数や length 関数などの基本的な関数を含め、Juliaの至るところで多重ディスパッチが使われており、シンプルな仕組みでありながら非常に強力な抽象化を可能にすることがわかったのではないでしょうか。コンストラクタや演算子も多重ディスパッチを使って自然に拡張できるということも忘れないでおきましょう。多重ディスパッチは一般的なプログラミング言語にはあまり見られない機能なので、使いこなせるようになるには時間と経験が必要ですが、一度使いこなせるようになってしまえば、とても自然で強力な機能だということがわかるようになるでしょう。

注

[*1](c434.xhtml#a9VS) AMD64(x86-64)という現在一般的なCPUでは、整数の積は imul 命令で実行され、倍精度浮動小数点数の積は vmulsd 命令で実行されます(例外もあります)。[*2](c434.xhtml#a9FH) ifelse 関数はJulia 1.8から総称関数になりました。[*3](c434.xhtml#aA4J) 多重ディスパッチに似た機能として、C++には関数のオーバーロードがあります。両者には大きな違いが2つあります。まず、Juliaの多重ディスパッチは実行時に動的にメソッドを決定するのに対し、C++のオーバーロードはコンパイル時に静的に決定します。次に、Juliaの多重ディスパッチは引数の型を暗黙的に変換しないのに対し、C++のオーバーロードは引数の型を暗黙的に変換することがあります。[*4](c434.xhtml#aA9C) string 関数との違いは、print 関数ではなく show 関数を使ってオブジェクトを少しリッチに表示することです。例えば、string('a') は "a" と文字列化しますが、repr('a') は "'a'" とシングルクォート付きで文字列化します。[*5](c434.xhtml#a9F1) 引数の型の制約がまったく同じであれば古いメソッド定義は取り消されます。混乱のもとになるので、開発中など一時的な場合を除きそのような書き方は推奨されません。[*6](c434.xhtml#a9KN) インタフェース(interface)と言い換えてもよいでしょう。[*7](c434.xhtml#a9RD) 正確には、new は関数のように振る舞うだけで、実体は特殊な構文です。[*8](c434.xhtml#a9AC) [7.2.4項](c315.xhtml#a9AD)で説明したように、struct はローカルスコープを作るので、型と異なる名前のコンストラクタにすると、global キーワードなどを使わない限り外部から呼び出す方法がありません。[*9](c434.xhtml#a9RU) 厳密にいえば、自身を再帰的に呼び出すこともできますが、最終的にはデフォルトコンストラクタか内部コンストラクタを呼び出す必要があります。[*10](c434.xhtml#a9V7) ガベージコレクション([18.6節](c7N0.xhtml#a99C))のときに、不要と判断されたオブジェクトの後処理が実行されると考えられますが、これは実装の詳細なので、この動作に依存すべきではありません。そもそも、ガベージコレクションが次にいつ起きるかは予測できません。したがって、オブジェクトのライフタイム終了と同時に後処理を実行するC++のRAIIなどとは動作が異なりますので注意してください。[*11](c434.xhtml#aA0N) 標準ライブラリの Vector 型はスタックのようにも使える([5.1.3項](c251.xhtml#aA0P)を参照)ので、実用的にはこのような Stack 型を定義する必要はありません。[*12](c434.xhtml#a9PN) 内部コンストラクタで使用している要素未初期化の配列構築は[11.1.2項](c4WA.xhtml#a9PP)で説明します。[*13](c434.xhtml#aAE0) push! 関数では、要素の型変換を最初に行っています。これは変換が失敗してスタックが無効な状態になるのを防ぐためです。[*14](c434.xhtml#aA8P) AbstractString 型の下位型にしてしまうと、Base モジュールにある一部の関数のメソッドが適用されてしまうので、ここでの説明にはやや不都合です。[*15](c434.xhtml#a9DX) より正確にいえば、型変換は処理系が行っているわけではなく、標準ライブラリ内で convert 関数を明示的に呼び出して変換しています。Juliaは言語としては暗黙的な型変換をしません。[*16](c434.xhtml#a9H7) イテレータは基本的にfor文を使って反復するので、iterate 関数をユーザが直接使うことはあまりありません。[*17](c434.xhtml#a9HH) ある構文を簡潔に書き換えられるよう設計された別の構文を糖衣構文(syntactic sugar)と呼びます。[*18](c434.xhtml#a9M2) 追加で内積![](image_rsrcB4S.jpg)![](image_rsrcB4T.jpg)も定義すると、Aitchison幾何学として知られるユークリッド空間になります。[*19](c434.xhtml#aAF2) ブロードキャストの構文と区別するためだと思われます。[*20](c434.xhtml#a9C7) 優先順位なども含めて正確に知るには、Juliaのパーサを読む必要があります。また、バージョンによって細かな違いもあります。[*21](c434.xhtml#a9N4) 対数の底の変換公式は b を新しい底として logb(x)=log(x)/log(b) です。[*22](c434.xhtml#aADW) Base モジュールにあるビルトイン関数の拡張ではなく、同名の新しい総称関数として定義すれば拡張はできますが、混乱のもとなので避けたほうがよいでしょう。[*23](c434.xhtml#a9HN) 例として、collect 関数をイテレータに適用することを考えます。イテレータの要素数があらかじめわかっていれば、その長さを持つ配列を事前に用意できるので、要素を取り出しながら配列を伸張する必要がなくなり、性能の向上が期待できます。[*24](c434.xhtml#aA3U) <https://docs.julialang.org/en/v1/manual/interfaces/#man-interface-iteration>

---

# ![](image_rsrcB4U.jpg)

メタプログラミング(metaprogramming)は、普通のプログラミングとは少し異なる頭の使い方をするプログラミングです。メタプログラミングを一言で言い表そうとすれば、プログラムをプログラムする行為といえます。これだけでは禅の公案のようで意味がわからないと思いますが、本章をよく読めばきっと意味が理解できるでしょう。

プログラミング言語によってはメタプログラミングはあまり使われませんが、Juliaでは非常によく使われる機能です。すでに何度も使っている @show マクロは、メタプログラミングの一種であるマクロの機能です。また、正規表現([6.3節](c2FB.xhtml#a96Y))では r"![](image_rsrcAGC.jpg)w+" のように文字列の前に何か文字が付いたものを使いましたが、これもマクロの機能です。マクロにはコードを書き換えて別のコードを生成する機能があります。これらのすでに登場したメタプログラミングに加えて、本章では生成関数というメタプログラミングの手法も解説します。

# 10.1 実行の仕組み

メタプログラミングの話を始める前に、Juliaがプログラムを実行する仕組みを簡単に確認しておきましょう。実行モデルを理解することで、メタプログラミングがどう動き、なぜ強力な機能なのかがよりわかるようになります。

# 10.1.1 動的コンパイル

動的言語であるJuliaは、C などの静的言語のようにコンパイルと実行のフェーズが明確に分離しているわけではありません。大雑把にいえば、Juliaの処理系はコードのコンパイルと実行を図10.1のように交互に繰り返します。コンパイルのフェーズではソースコードを読み込み、さまざまな解析や最適化を行った後、CPUで実行できるコードを生成します。実行のフェーズではコンパイルで生成されたコードを実行していき、コンパイルが必要な場面に出くわしたら再びコンパイルのフェーズに戻ります。コンパイルと実行の繰り返しでJuliaは処理を進めるわけです。

![](image_rsrcB4V.jpg)

コンパイルのフェーズは、図10.1の下部に示したように、大きく分けて次の要素に分解できます。

●構文解析

●式変換

●最適化

●コード生成

メタプログラミングでは、これらのうち構文解析と式変換の中間あたりに介入します。最適化とコード生成については[17.5節](c79X.xhtml#a994)で改めて扱います。

# 10.1.2 構文解析

構文解析(syntax analysis, parsing)とは、テキストとして与えられたソースコードを解析し、Juliaのオブジェクトに変換する処理です。REPLやスクリプトファイルから得られたソースコードは、まずJuliaの処理系が構文解析を行い、内部で扱えるオブジェクトに変換されます。このオブジェクトは次の段階である式変換へと渡されます。

構文解析の動作をのぞいてみましょう。構文解析を利用者が明示的に行うには、Meta.parse 関数にコードを文字列として渡します。例えば、Meta.parse 関数に文字列 "42" を渡すと整数 42 へと変換します。

![](image_rsrcB4W.jpg)

続いて、単純な計算式を渡してみましょう。

![](image_rsrcB4X.jpg)

構文解析の結果、:(x + 3) というオブジェクトに変換されました。詳しくは[10.2.2項](c4G7.xhtml#a9KF)で説明しますが、これは式と呼ばれるオブジェクトであり、構造を持ったコードの断片を表現します。:( と ) は式を構築するための記号であり、コードの一部をこのようにオブジェクトとして取り出すことをクォート(quote)と呼びます。また、整数などのリテラルと式を合わせて、抽象構文木(abstract syntax tree, AST)と呼びます。抽象構文木はオブジェクトなので、Juliaで操作できます。メタプログラミングでは、抽象構文木をJuliaで操作してコードの変換やオブジェクトの生成を行うわけです。

# 10.1.3 評価

式を含む抽象構文木は、eval 関数で評価(evaluation)できます。評価とは、Juliaの処理系が抽象構文木を計算して値を決定することです。例えば、次のコードは変数 x を 2 に束縛し、:(x + 3) という式を eval 関数で評価しています。

![](image_rsrcB4Y.jpg)

eval 関数は、式以外のオブジェクトを受けとると、それをそのまま返します[*1](c4G7.xhtml#a9FG)。

![](image_rsrcB4Z.jpg)

eval 関数は各モジュールに固有の関数であり、各モジュールは暗黙的に定義された eval 関数を各自が持っています。eval 関数は自分が属するモジュールのグローバルスコープで渡された式を評価します[*2](c4G7.xhtml#aA22)。したがって、次のように eval 関数に同じ式を渡しても、異なるモジュールなら異なる結果を返すのが普通です。

![](image_rsrcB50.jpg)

eval 関数は任意の抽象構文木を評価できるので、やろうと思えば次のようにモジュール定義の外から関数を定義できます。一般的にいえばこれはよい作法ではないのですが、開発などでは実際便利なテクニックです。

![](image_rsrcB51.jpg)

# 10.2 抽象構文木の表現と操作

[10.1.2項](c4G7.xhtml#a9SY)で登場した抽象構文木は、メタプログラミングで最も重要なオブジェクトです。なぜなら、抽象構文木のオブジェクトこそがメタプログラミングで操作する主な対象だからです。本節では抽象構文木の表現と操作を掘り下げ、メタプログラミング技法の解説につなげます。

# 10.2.1 シンボル

まずはシンボル(symbol)と呼ばれる重要な型を紹介します。抽象構文木の中に現れる変数は、すべてシンボルで表現されます。

シンボルのリテラルは変数の前にコロン(:)を付けます。変数としては使えない function などのキーワードでも、コロンを付ければシンボルになります。次の4つはすべて有効なシンボルのリテラルです。

![](image_rsrcB52.jpg)

一部の記号は構文解析の都合上、丸括弧で囲む必要があります。

![](image_rsrcB53.jpg)

シンボルは Symbol 型のオブジェクトです。文字列の String 型と似ている部分がありますが、シンボルは変数を表現する目的に特化しています[*3](c4G7.xhtml#aA9M)。

シンボルは Symbol 型のコンストラクタを使っても作れます。このコンストラクタは、次のように与えられた引数を文字列化してから結合します。コンストラクタを使えば、コロンを使ったリテラルでは作れないようなシンボルも作れます。

![](image_rsrcB54.jpg)

# 10.2.2 式の表現

抽象構文木は、木の構造を表現する Expr 型のオブジェクトと、それ以外のオブジェクト(シンボルや整数など)から構成されます。特に Expr 型のオブジェクトは単に式(expression)とも呼ばれます。前述したように、式を作るには次のようにコードを :( と ) のペアで囲みクォートします[*4](c4G7.xhtml#aACR)。

![](image_rsrcB55.jpg)

式は構造体型のオブジェクトであり、head と args の2つのフィールドを持ちます。head フィールドは式の種類を表し、args フィールドは式を構成する要素を保持しています。簡単な例で確認しましょう。例えば、:(x + 3) という式の2つのフィールドには、次のようなものが収められています。

![](image_rsrcB56.jpg)

この例で head フィールドに収められている :call というシンボルは、この式が関数呼出しであることを意味しています。また、args フィールドは1次元の配列で、3つの要素が収められています。要素数は式の種類によって異なります。この例のように関数呼出しを表す式では、最初の要素(:+)が関数名を表し、残りの要素(:x と 3)が関数に渡される引数を表します。関数 + と変数 x がシンボルで表現されていることに注意してください。式 :(x + 3) の構造を図10.2に示します。

次のように少し複雑な計算式では、関数呼出しの式は入れ子構造をとります。これはオブジェクトの構造を出力する dump 関数に渡すと確認できます。この式の構造を図10.3に示します。

![](image_rsrcB57.jpg)

![](image_rsrcB58.jpg)

関数呼出し以外の式の例も見ておきましょう。条件分岐ができる条件演算子は次のように head フィールドが :if です。args には3つの要素があり、分岐条件と条件が成立・不成立のときに実行される抽象構文木が順に収められています。

![](image_rsrcB59.jpg)

次のようなもう少し複雑な条件演算子の式の構造を dump 関数で表示してみると、args フィールドの3つの要素がそれぞれ関数呼出し(:call)の式になっています。こうしてさまざまな種類の式が入れ子になって、任意の複雑な式が表現できます。

![](image_rsrcB5A.jpg)

代表的な式の一覧を表10.1に示します。より完全な式の一覧はJuliaのドキュメント[*5](c4G7.xhtml#a9RE)を参照してください。メタプログラミングをするときには、作成したい式の構造を dump 関数で表示し、以降で説明する方法で式を生成することになります。

![](image_rsrcB5B.jpg)

# 10.2.3 式の補間

文字列の補間([6.2.2項](c2FB.xhtml#a9G3))と同様の操作は式でもできます。すなわち、$( と ) を使って式にオブジェクトを埋め込んで新しい式が作れます。変数を埋め込むときは丸括弧を省略できます。

![](image_rsrcB5C.jpg)

また、次のように ... を使って複数の式をスプラットして補間できます。

![](image_rsrcB5D.jpg)

シンボルを補間するときには少し注意が必要です。例えば、:x を別の式に補間しようとすると、シンボルのリテラルではなく変数として埋め込まれます。したがって、:(f($(:x))) は f(:x) ではなく f(x) になります。これは、シンボルは主に変数を表現するときに使われるので、こうしたほうが利便性が高いからです。もし、シンボルを変数としてでなくシンボルのリテラルとして補間したいときには、QuoteNode という型で一度ラップしてから補間します。

![](image_rsrcB5E.jpg)

# 10.2.4 式のコンストラクタ

式のオブジェクトは Expr 型のコンストラクタを直接呼び出しても作れます。式の補間などに比べると少し難しいですが、より柔軟性が高いです。

Expr コンストラクタは第1引数に head フィールド(式の種類)をとり、第2引数以降に args フィールドの要素(式の引数)をとります。次の例では、単純な関数呼出しを表す式を作っています。

![](image_rsrcB5F.jpg)

次の例はもう少し構造が複雑な式の例です。条件式とブロック2つを使って、if 文の分岐がある式を構築しています。

![](image_rsrcB5G.jpg)

# 10.2.5 ブロックのクォート

:( と ) のペアで囲むクォートは1行で書けるような単純な式ではよく使われますが、複数行からなるコードはセミコロン(;)で分割して書く必要があり、少々読みにくいです。

![](image_rsrcB5H.jpg)

そんなときに使えるのが、quote キーワードを使ったクォートです。quote キーワードで始まり end キーワードで終わるブロックは、次のように複数行のコードをより手軽にクォートできます。先の書き方と本質的に変わるところは何もありません[*6](c4G7.xhtml#a9WU)。

![](image_rsrcB5J.jpg)

quote キーワードを使った書き方でも式の補間と評価ができます。

![](image_rsrcB5K.jpg)

# 10.3 動的なコード生成

マクロの説明に入る前に、式の操作に慣れるため eval 関数や @eval マクロを使った動的なコード生成の技法を紹介しましょう。この技法は、同じようなソースコードの繰り返し、いわゆるボイラープレート(boilerplate)を回避して、コードを簡潔に記述するのに役立ちます。例えば、次のように同じようなビットフラグを複数定義することがあるでしょう。ビットフラグは1ビットずつ左にシフトしていることに注意してください。

![](image_rsrcB5M.jpg)

段階を踏んでこのコードを動的なコード生成に書き換えていきましょう。まず、先のコードは eval 関数を使って次のように書き換えられます。[10.1.3項](c4G7.xhtml#a9GE)で説明したように、eval 関数はモジュールのグローバルスコープで式を評価するので、次の例は現在のモジュールにビットフラグの定数を定義します。

![](image_rsrcB5N.jpg)

この eval 関数は同じパターンの式を繰り返し評価してるので、for文を使って書き換えられます。つまり、式の補間を用いると、定数の定義は const $name = $bits というテンプレートで抽象化できます。

![](image_rsrcB5P.jpg)

さらに、定数は FLAG_ という共通の接頭辞を持っていることと、ビットフラグが1ビットずつ左に移動していることを考慮し、次のように冗長な繰り返しを排除できます。

![](image_rsrcB5R.jpg)

最後に、eval 関数の代わりに自動でクォートを行う @eval マクロを使うと、クォートの必要がなくなります。

![](image_rsrcB5S.jpg)

ビットフラグの数が少ないので、この例では eval 関数や @eval マクロを使う利点はあまり感じられないかもしれませんが、特定のパターンを繰り返すコードでは、メタプログラミングを使ったほうが愚直に書くより意図がわかりやすく、間違いも少なくなるでしょう。実際、標準ライブラリである Base モジュールの中では、@eval マクロが100回以上も使われています。

上で紹介した定数の定義のみならず、関数定義でも同じようなことができます。応用例として、リスト10.1に符号なし整数の最上位ビットを取得する関数を生成する例を示します。

![](image_rsrcB5T.jpg)

# 10.4 マクロ

いよいよ本章の主題であるマクロの説明に入ります。マクロ(macro)は、あるコードを別のコードやオブジェクトに変換してからコードに埋め込むメタプログラミングの一種です。マクロを使った抽象構文木の変換は、関数や他の仕組みでは実現できない機能を提供します。

マクロに相当するメタプログラミングの機能がないプログラミング言語も多くありますので、そもそもマクロという概念自体に馴染みがない人も多いでしょう。概念的にいえば、関数がオブジェクトを入力として受けとりオブジェクトを出力として返すのと同じように、マクロはコードを入力として受けとりコード(もしくはコード以外のオブジェクト)を出力として返すものです。関数と同様に、マクロはその変換に名前を付けて抽象化できます。マクロはコードを本来の意味から切り離し、別の意味を持つものに変換できるのです。

C やC++にあるマクロとJuliaのマクロは、同じ名前でもまったく仕組みが異なることに注意してください。Juliaのマクロは、C やC++のようなコードの文字列レベルでの置き換えでなく、抽象構文木をオブジェクトとして操作できる、より柔軟性のある仕組みです。実際のところ、JuliaのマクロはCommon LispやSchemeのマクロの影響を強く受けており、これらのマクロによく似ています。

# 10.4.1 マクロ呼出し

Juliaには使い方が異なる2種類のマクロがあります。両者の機能に違いはありませんが、ソースコード中でのマクロの使い方、つまりマクロ呼出し(macro call)の構文が異なります。1つは単価記号(@)をマクロ名の前に付けて呼び出すマクロです。マクロに言及するときは関数と区別するため、この構文を借用して @name のように呼ぶのが通例です。例えば、@show マクロや @enum マクロなどは、この種のマクロです。もう1つは、文字列の前に名前を付けて呼び出すマクロです。数値で使う big"..." や正規表現で使う r"..." などの書き方は、この種のマクロを呼び出す構文です。

マクロには開発ツールとして使われるもの、コンパイラへヒントを与えるもの、構文を拡張するものなど、多種多様な用途があります。例えば、@less マクロは関数呼出しで実行されるメソッドのソースコードを表示するときに使います。@inline マクロは、関数定義に付けると積極的にインライン化([18.4.2項](c7N0.xhtml#a9J4))するようコンパイラにヒントを与えます。@enum マクロは、列挙型([8.7節](c3S5.xhtml#a97H))を定義するときに使います。いずれの機能も、もしマクロがなければ、新しい構文をJuliaに導入しない限り実現できません。

まずは単価記号を使って呼び出すタイプのマクロについて、詳しく説明しましょう。次のように、マクロ呼出しの構文は関数呼出しに似ています。ただし、関数呼出しと異なり、マクロ呼出しは丸括弧とコンマを省略できます。特に理由のない限り[*7](c4G7.xhtml#aA9F)は後者の記法が好まれます。

![](image_rsrcB5U.jpg)

例として、式とその評価値を出力する @show マクロの呼出しを見ましょう。次の2つのマクロ呼出しには、機能的な差はありません。

![](image_rsrcB5V.jpg)

マクロ呼出しが関数呼出しと根本的に異なるのは、マクロ呼出しの引数は抽象構文木であるという点です。@show iseven(x) というマクロ呼出しならば、@show マクロには :(iseven(x)) という式が渡されるわけです。したがって、マクロを使えば、評価値だけでなく、iseven(x) = ... のように式も同時に出力できるのです。一方、関数に渡されるのは評価値だけなので、その値がどんな抽象構文木に由来するのかはわかりません。結果として、@show マクロの動作は関数では実現できないのです。

これだけを聞くと、関数でできることはすべてマクロでもできるという印象を与えてしまうかもしれませんが、そうではありません。マクロがわかるのは渡された抽象構文木のみで、その評価値も型もわかりません。また、マクロ呼出しは動的なコンパイルのフェーズで展開(expand)され、実行のフェーズでは影も形もなく消え去ります。つまり、次の例のように元のソースコードがマクロにより書き換えられ、その後マクロなどなかったかのように関数呼出しなどを含むコードが実行されるのです。

![](image_rsrcB5W.jpg)

続いて、単価記号を使わずに呼び出すタイプのマクロについて説明します。このタイプのマクロの実体は、@big_str や @r_str のように _str という接尾辞が付いたマクロです。つまり、典型的な呼出し方が異なるだけで、@show マクロなどと本質的な差はありません。この種のマクロは非標準文字列リテラル(non-standard string literal)とも呼ばれます。非標準文字列リテラルは、_str より前の部分を文字列リテラルの先頭に付けても呼び出せる特殊なマクロなのです。例えば、次の2つのマクロ呼出しは同じことを意味します[*8](c4G7.xhtml#a9C5)。

![](image_rsrcB5X.jpg)

# 10.4.2 標準ライブラリのマクロ

本書ではここまでにもいくつか重要なマクロを紹介してきましたが、ここでは他の場所ではあまり触れられなかった標準ライブラリのマクロを紹介します。

マクロ呼出しがどのような式に展開されるかは、@macroexpand マクロで調べられます。非再帰的に展開するには @macroexpand1 マクロを使います。

![](image_rsrcB5Y.jpg)

InteractiveUtils.jlには、関数呼出しやマクロ呼出しを受けとり、呼び出されるメソッドの情報を提示するマクロがあります。多重ディスパッチのあるJuliaでは、呼び出されるメソッドがわかりにくくなりがちなので、こうしたマクロは開発で非常に役立ちます。次の例を手元で実行し、動作を確認してください[*9](c4G7.xhtml#aAD6)。

![](image_rsrcB5Z.jpg)

マクロを呼び出すとその位置情報が得られるマクロもあります[*10](c4G7.xhtml#aAAE)。

![](image_rsrcB60.jpg)

# 10.4.3 マクロ定義の基本

マクロ定義は macro キーワードを使って開始し、end キーワードで終わります。構文は次のように関数定義に似ています。

![](image_rsrcB61.jpg)

マクロが引数として受けとった抽象構文木を処理するコードは、マクロ定義の body 部分に記述します。ここでは、抽象構文木を自由に変換し、目的のコードやオブジェクトを生成できます。return キーワードで返す値がマクロ呼出しを行った場所に埋め込まれます。関数定義と同様に、return キーワードを省略したときは、最後の式がマクロ呼出しの戻り値となります。

簡単なマクロを書いてみましょう。リスト10.2の @check マクロは、プログラムが正しく動いているかをチェックするマクロです。@check マクロに与えられた式が true と評価されれば、式と一緒に GOOD と出力しますが、そうでなければ BAD と出力します。この定義には重要な問題がありますが、それは[10.4.6項](c4G7.xhtml#a9MP)で振り返ります。

![](image_rsrcB62.jpg)

@check マクロの使用例は次のとおりです。テストしたコードとその結果が一緒に出力されるのでわかりやすいでしょう。

![](image_rsrcB63.jpg)

最初のテストケースは次のように展開されます。

![](image_rsrcB64.jpg)

#= と =# の間はソースコードの位置情報なので無視して整理すると、マクロ呼出しは次のようなコードに展開されていることがわかります。モジュール Main のグローバル変数 x が正の値かどうかをチェックし、出力するメッセージを切り替えています。

![](image_rsrcB65.jpg)

# 10.4.4 衛生的なマクロ

マクロが返す抽象構文木は、マクロ呼出し位置にそのまま埋め込まれるわけではありません。抽象構文木に含まれる変数は、ある規則に則って置換されます。マクロ展開でおそらく最もわかりにくいのは、この変数の置き換えに関する動作でしょう。しかし、マクロを定義する開発者とマクロを使う利用者の2つの視点を持てば、どの変換も合理的な動作に感じられます。展開に伴う変数の置換について、例を挙げながら丁寧に説明していきます。

マクロはその性質上、コードが生成される場所(マクロが定義される場所)とコードが埋め込まれる場所(マクロが呼び出される場所)が異なります。マクロを定義した開発者には利用者がマクロをどこで使うかはわからないので、どこで使われても意図した動作をするようにしなければなりません。したがって、マクロが生成する式に含まれる変数は、原則として、マクロを定義した場所のグローバル変数に置換されます。

具体例を見てみましょう。次の例はマクロである必要はありませんが、説明を単純にするためこのような例を使います。このマクロが Main モジュールで定義されていれば、マクロが返す式にある println という変数は、原則に従って Main.println 関数に置換されます。こうすれば、仮にマクロ呼出しのあるスコープで println が書き換えられていても、マクロの動作には影響しません。

![](image_rsrcB66.jpg)

この置換は + などの演算子にも適用されることを忘れないでください。構文が異なるだけで、+ や * のような演算子もJuliaでは普通の変数と同じ扱いです。

しかし、グローバル変数への置換はマクロ内で代入された変数には適用されません。例えば、次のように message 変数をマクロが返す式の中で定義すると、この変数はマクロを定義した場所のグローバル変数には置換されません。message という名前は次のような #1#message という奇妙な名前に置換されます[*11](c4G7.xhtml#a9HT)。しかも、この名前は実行するたびに変わります。

![](image_rsrcB67.jpg)

このように変数が置換されるのには理由があります。マクロが返す式の中で代入された変数は、その式で局所的に使われるものなので、グローバル変数と解釈するのは不合理です。また、マクロの利用者はマクロ定義内でどのような変数が定義されているかは知らないので、もし名前の置換をせずそのまま message という変数のままにしておくと、マクロ呼出しを行った場所にある変数と運悪く衝突する恐れがあります。したがって、変数を衝突の恐れがない奇妙な名前に置換する必要があるのです[*12](c4G7.xhtml#a9G4)。このような変数の置換をするマクロは一般に衛生的なマクロ(hygienic macro)と呼ばれます。

衛生的なマクロで自動的に変数が置換されるのは、次のいずれかの基準を満たす変数です。

●マクロが返す式の中で定義(代入)されている(ただし、global キーワードによる宣言がない)

●local キーワードによる宣言がある

●関数の引数である

どのような変数がマクロの外に漏れてほしくない局所的な変数かと考えれば、これらの基準は暗記しなくても自然なものに思えるでしょう。ただし、すぐに説明するように、変数の置換を回避する方法も用意されています。

# 10.4.5 変数のエスケープ

マクロを定義する側が変数の意味を決めるのではなく、マクロの利用者に任せたいことがあります。そのときは変数やそれを含む式を esc という関数でエスケープ処理し、先に述べた変数の置換を回避します。esc 関数[*13](c4G7.xhtml#a9ZA)で式をラップすると、その式にある変数はマクロ展開時に置換を受けず、そのまま呼出し元のコードに埋め込まれます。これは利用者から受けとった式の意味を変えずにマクロ展開後の式に埋め込むときに便利です。

まずは、リスト10.3に示した同じ式を繰り返し評価する @repeat マクロを見てください。

![](image_rsrcB68.jpg)

この例では、利用者がマクロに渡す n と ex が esc 関数によるエスケープを受けています。こうすると、n や ex に変数を含む式が渡されたとき、その式にある変数は置換を受けません。例えば、次の実行例では + と println は置換を受けないので、マクロ定義が行われた場所のグローバル変数ではなく、マクロを呼び出した場所の変数と解釈されます。言い換えれば、マクロを定義した開発者はその変数の意味(変数が指すオブジェクトは何か)には関知せず、マクロの利用者が変数の意味を規定します。

![](image_rsrcB69.jpg)

マクロ展開における変数の置換についてまとめておきましょう。原則として、マクロが返す抽象構文木の変数は、マクロを定義した場所のグローバル変数に置換されます。ただし、この原則には次の2つの例外があります。

1\. 変数がマクロの返す抽象構文木中で代入されるなど、マクロで局所的に使われる変数であるとき、名前が衝突しないよう衛生的なマクロにより新しい名前に置換される。

2\. esc 関数でエスケープされた抽象構文木の変数は置換されない。

# 10.4.6 実用的なマクロの実装例

ここでは標準ライブラリにあるマクロの実装を例に、実用的なマクロ定義に見られる工夫を紹介します。実装を見るのは @elapsed マクロです。このマクロは渡された式を実行し、実行に何秒かかったかを返すマクロです。

![](image_rsrcB6A.jpg)

この実装は標準ライブラリでリスト10.4に示すように書かれています[*14](c4G7.xhtml#a9PR)。たった数行の実装ですが、十分に説明しがいのあるマクロです。なお、実装で使われている time_ns 関数は、現在の時刻をナノ秒単位で返す関数です。

![](image_rsrcB6B.jpg)

quote 内のコードに注目してください。4行目の local t0 = time_ns() では、渡された式を実行する前の時刻を変数 t0 に記録しています。このマクロは Base モジュール内で定義されているので、time_ns は原則どおり Base モジュールの time_ns 関数に置換されます。一方の t0 はマクロ内で定義されているので、t0 という変数名がマクロ呼出しのスコープにある変数と衝突しないように #4#t0 のような新しい名前に置換されます。さらに、t0 には local キーワードが付いているので、衛生的なマクロで付けられた名前の変数は強制的にローカル変数となり、グローバルスコープに漏れ出すことはありません。

5行目の esc(ex) では、マクロに渡された式 ex をエスケープしています。実行時間を計測したい式である ex に含まれる変数の意味は、このマクロを書いた開発者は関知せず、マクロの利用者が決めます。したがって、このマクロの実装では式を esc 関数でエスケープして変数の置換を無効化し、マクロ呼出しを行った利用者にその意味を任せることにしています。

6行目では再び time_ns 関数を呼び出して式の評価が終わった時刻を取得し、t0 に記録された時刻との差分から実行時間を計算しています。注意してほしいのは、この計算式に含まれる - や / も time_ns と同じようにマクロを定義した場所のグローバル変数に置換されるということです。簡単な計算に使われる記号も普通の変数だということは忘れやすいので注意してください。

ここまで理解すれば、[10.4.3項](c4G7.xhtml#aAER)で定義した @check マクロの問題点もわかるでしょう。このマクロ定義では、利用者から与えられた式をエスケープ処理せずそのまま評価していました。これでは利用者から与えられた式の変数がマクロ定義と同じモジュールの変数に置換されてしまうので不都合です。したがって、esc(ex) のように式をエスケープ処理する必要があるのです。

# 10.4.7 非標準文字列リテラルの定義

名前が _str で終わるマクロは、非標準文字列リテラルとして呼び出せることは[10.4.1項](c4G7.xhtml#a9NE)で説明しました。ここではその定義方法と実用例を説明します。

非標準文字列リテラルでも、マクロ定義の構文は他のマクロと変わりません。異なるのは、第1引数が文字列であることと、第2引数にオプショナルなフラグをとれることです。例えば、正規表現のリテラルでは第1引数の文字列は正規表現自体に、第2引数のフラグは修飾子([6.3.3項](c2FB.xhtml#aA9A))として使われています。次の例を見てください。2つの引数はそれぞれ文字列としてマクロに渡されていることがわかります。

![](image_rsrcB6C.jpg)

非標準文字列リテラルのマクロ定義では、渡された文字列を加工して目的の抽象構文木やオブジェクトを返します。標準ライブラリで定義されている非標準文字列リテラルは、抽象構文木ではなく何らかのオブジェクトを返すことが多いです。例えば、v"..." という非標準文字列リテラルは、渡された文字列をパースして VersionNumber 型のオブジェクトを返しますし、r"..." は[6.3節](c2FB.xhtml#a96Y)で紹介したように Regex 型の正規表現オブジェクトを返します。

![](image_rsrcB6D.jpg)

例として、Pythonの f 文字列(f-string)に似た非標準文字列リテラルを定義してみましょう。これは { と } で囲まれた変数を補間した文字列を返す機能です。簡単な実装例をリスト10.5に示します。実装例のようにマクロの定義から文字列の解析処理を関数として分離しておくと、動作のチェックがしやすくなります。

![](image_rsrcB6E.jpg)

動作の確認をしてみましょう。次のように期待どおり動いています。

![](image_rsrcB6F.jpg)

このように、非標準文字列リテラルを使えばドメイン特化言語を導入できます。加えて、実行時の効率化としてよい面もあります。わかりやすい例として、正規表現の非標準文字列リテラルを取り上げましょう。

正規表現オブジェクトを作るには、非標準文字列リテラルを使う方法と Regex 型のコンストラクタを使う方法の2つがあります。コンストラクタに文字列を渡して使う方法には柔軟性がありますが、実行中にそこを通過するたびにコンストラクタが呼び出されて新しい正規表現オブジェクトが作られます。対して非標準文字列リテラルでは、マクロ呼出しがコンパイル時に展開されると同時に正規表現オブジェクトが一度だけ作られるので、実行時にはその正規表現オブジェクトを何度でも使い回せます。したがって、正規表現オブジェクトの作成を関数の外やループの外に追い出さず、次のようにソースコードの必要な場所に直接埋め込んでも性能は落ちません。

![](image_rsrcB6G.jpg)

# 10.5 生成関数

マクロとは別のメタプログラミングとして、Juliaは生成関数(generated function)という機能を提供しています。関数を呼び出す側から見ると生成関数は通常の関数とまったく同じように振る舞いますが、生成関数は初めて呼び出されたときに引数の型に応じて関数本体のコードを生成(generate)できます。マクロなどに比べると使用機会はあまりありませんが、これも強力なメタプログラミングの機能です。

# 10.5.1 生成関数の定義

生成関数の定義は @generated マクロを使って関数定義の構文を修飾します。構文としては通常の関数定義と変わりませんが、関数本体の書き方が大きく異なります。まずはリスト10.6の単純な生成関数の定義例を見てください。

![](image_rsrcB6H.jpg)

生成関数は渡された引数の型に応じて関数本体を生成します。genfun 関数の例では、最初に関数を呼び出したとき、引数 x は関数に渡されたオブジェクトではなく、そのオブジェクトの型に束縛されています。例えば、genfun(42) という関数呼出しであれば、引数 x は 42 ではなく Int に束縛されています。そして、生成関数の定義では、通常の関数のように戻り値を計算して返すのではなく、関数本体の実装を式として返します。genfun 関数の例でいえば、次のように引数の型が Real 型の下位型であるときには "... is Real" を出力する関数の実装になり、それ以外のときには "... is not Real" を出力する関数の実装になります[*15](c4G7.xhtml#a9WZ)。

![](image_rsrcB6J.jpg)

# 10.5.2 生成関数の応用例

生成関数の応用例を1つ紹介しましょう。リスト10.7の polynomial 関数は、genpoly 関数を使って多項式(polynomial)を生成する関数です。

![](image_rsrcB6K.jpg)

計算の説明をしましょう。生成関数になっているのは polynomial 関数ですが、実際に式を生成してるのは genpoly 関数です。このように式を生成する関数を分離すると、次のように動作確認やデバッグがしやすくなるのはマクロと同じです。

![](image_rsrcB6M.jpg)

計算に使われている fma 関数は[4.4節](c1JX.xhtml#a96K)で触れたfused multiply-add(FMA)関数[*16](c4G7.xhtml#a9BK)です。1次多項式のほうは * と + で書けば a[1]*x + a[2] に対応し、2次多項式のほうは (a[1]*x + a[2])*x + a[3] に対応します。この計算方法はホーナー(Horner)法と呼ばれ、x のべき乗を使わず fma 関数だけで多項式を計算する優れた方法です。1次から3次の多項式について、Horner法の計算式とその展開式を数式で書けば、次のとおりになります。

![](image_rsrcB6N.jpg)

多項式の計算で生成関数を使えば、プログラマが多項式をHorner法の計算式に変換する手間を省けます。また、for文などの反復を使った実装に比べ、単純な式に書き換えることでより速く計算できることが期待されます。なお、標準ライブラリには多項式の計算をする evalpoly 関数や @evalpoly マクロが用意されています。

# 10.6 マクロと生成関数の定義に関する注意

マクロや生成関数の定義には、いくつか厳しい制約があります。まず、関数本体を生成するコードでは、定義した時点で存在しない関数やメソッドは使ってはいけません。また、副作用のある処理も行ってはいけません。例えば、入出力、グローバル変数の定義や更新、乱数生成などは副作用のある処理なので、マクロや生成関数の定義では使ってはいけません[*17](c4G7.xhtml#a9P8)。定義内で副作用のある処理をしたとき、特定のバージョンではたまたま期待どおりに動くかもしれませんが、それは信頼できる動作ではありません。コードがいつ生成されるかは決定的でないので、そのタイミングに依存するメタプログラミングは避けてください。

まとめ

本章ではメタプログラミングの基本を紹介しました。特にマクロは強力な機能ですが、初めのうちは使いどころの判断が難しく、なかなか気軽に手を出しにくいかもしれません。標準ライブラリにあるマクロをよく知ると、マクロを使うべきところがわかりやすいので参考にするとよいでしょう。また、マクロを使わなくてもできることにはマクロを使うべきではないということにも注意してください。それでも、ここぞというときには非常に強力な機能なので、ぜひ機会を探してマクロなどのメタプログラミングに挑戦してみてください。

注

[*1](c4G7.xhtml#a9KX) これはPythonやJavaScriptの eval 関数と異なる動作なので注意してください。PythonやJavaScriptの eval 関数は、コードを文字列として受けとり、それを構文解釈してから評価します。[*2](c4G7.xhtml#aA2G) 関数内などで eval 関数を呼び出しても評価はグローバルスコープで起きるので注意してください。[*3](c4G7.xhtml#a99Z) シンボルは文字列にあるような部分文字列を切り出すなどの操作を備えていません。[*4](c4G7.xhtml#a9U7) :(42) のように整数などのリテラルをクォートすることもできますが、これは単に 42 と書いたものと等価であって、Expr 型の式ではありません。[*5](c4G7.xhtml#a9Y3) <https://docs.julialang.org/en/v1/devdocs/ast/>[*6](c4G7.xhtml#a9K0) 挿入される行番号の情報などに差があることがあるので、常に等価なオブジェクトというわけではありません。[*7](c4G7.xhtml#a9MJ) 例えば、@m x + y は1つの式 :(x + y) を引数とするマクロ呼出しと構文解析されるので、x, +, y を別々の引数として渡すときは @m(x, +, y) と書きます。[*8](c4G7.xhtml#a9CR) エスケープ処理などにより、すべての非標準文字列リテラルで同じというわけではありません。例えば r"![](image_rsrcAGC.jpg)d" の代わりに @r_str "![](image_rsrcAGC.jpg)d" とは書けません。[*9](c4G7.xhtml#a9M7) エディタは環境変数 EDITOR, VISUAL, JULIA_EDITOR で設定できます。[*10](c4G7.xhtml#aA5N) REPLとスクリプトで動作が異なるので注意してください。[*11](c4G7.xhtml#aACN) var"..." マクロについては[7.1.2項](c315.xhtml#aACP)を参照してください。[*12](c4G7.xhtml#aABP) このような変数を明示的に生成するには gensym 関数を使います。[*13](c4G7.xhtml#a9J2) escapeの短縮形です。[*14](c4G7.xhtml#aA5K) 若干の単純化をしています。[*15](c4G7.xhtml#aA36) もちろん、この関数の実装なら生成関数ではなく多重ディスパッチを使ったほうが簡単です。[*16](c4G7.xhtml#aA74) fma(x, y, z) は x * y + z を計算しますが、丸めが1回で済みます。[*17](c4G7.xhtml#aAAT) 副作用に依存したコード生成を行ってはならないという意味です。副作用のあるコードを生成することは問題ありません。

---

# ![](image_rsrcB6P.jpg)

---

# ![](image_rsrcB6R.jpg)

Juliaを使っている、あるいは使い始めようとしている人の中には、Juliaの科学技術計算に関する機能に魅力を感じている人も多いでしょう。実際、Juliaの標準ライブラリには科学技術計算に便利な型や操作が豊富に用意されていることもあり、この分野での応用が多く見られます。本章では、Juliaの科学技術計算に関する標準ライブラリの機能を解説します。

# 11.1 多次元配列

多次元配列は科学技術計算で使う最も重要な型の1つです。多次元配列は単純なコレクションとしても使われますし、線形代数においてベクトルや行列を表現するオブジェクトとしても使われます。Juliaには多次元配列を扱う一連の関数や構文が用意されています。本節では、多次元配列の概要から始め、構築・結合・インデクシングなどの基本を解説し、ベクトル化演算・ブロードキャスト・フュージョンなどのJuliaに特徴的な機能までを解説します。

# 11.1.1 多次元配列の概要

多次元配列(multi-dimensional array)は複数の方向に広がりを持ち、各方向に複数の値を保持できるコレクションです。多次元配列がいくつの方向に広がりを持つかを次元(dimension)と呼び、小さいほうから順に1次元配列や2次元配列などと呼ばれます[*1](c4WA.xhtml#aACG)。多次元配列は単に配列(array)とも呼ばれ、本書でも特に注意が必要でない限り、こう呼んでいます。標準ライブラリでは、Array 型([5.1.3項](c251.xhtml#aA0P))や SubArray 型([11.1.9項](c4WA.xhtml#aA9E))などが(多次元)配列の代表例です。

図11.1に1次元配列・2次元配列・3次元配列を立体的な模式図で示します。配列の各立方体がオブジェクトを1つ保持する要素(element)に対応します。オレンジ色で示された立方体が配列の最初の要素です。それぞれの配列は次元の数だけ複数の方向に広がりを持っています。1次元配列は縦方向の1方向に、2次元配列は縦と横方向に、3次元配列は縦・横・奥の3方向に特定の長さの広がりを持っていることがわかるでしょう。

![](image_rsrcB6S.jpg)

配列の各次元には長さがあり、図の配列はいずれも最初の次元(図の縦方向)の長さが5です。さらに、2次元配列と3次元配列は2番目の次元(横方向)の長さが4であり、3次元配列は3番目の次元(奥方向)の長さが3です。全次元の長さをまとめたものは配列のサイズ(size)と呼ばれます。例えば3次元配列のサイズは (5, 4, 3) です。サイズに含まれる数の積が1つの配列に含まれる要素の数です。したがって、図の配列はそれぞれ 5=5 要素、5×4=20 要素、5×4×3=60 要素を含む配列です。

1から始まるインデックス(index)を各次元について指定すると配列に含まれる要素を特定できます。インデックスは添字とも呼ばれます。説明のため最初の次元のインデックスを i、2番目の次元のインデックスを j とすると、図11.1の1次元配列にある上から2番目の要素は i=2 であり、2次元配列にある上から4番目で左から3番目の要素は i=4, j=3 と特定できます。1次元配列を v、2次元配列を M とすると、各要素はインデックスを使って vi や Mij のように書けます。3次元以上の配列でもインデックスの数が増えるだけで原理は同じです。これをJuliaでどう書くかは[11.1.5項](c4WA.xhtml#aAF8)で説明します。なお、一部のプログラミング言語では配列のインデックスが0から始まるので、Juliaとの違いに気を付けてください[*2](c4WA.xhtml#aAF9)。

配列を数学的な対象として見るとき、1次元配列をベクトル(vector)、2次元配列を行列(matrix)と呼ぶことがあります。Juliaでも1次元と2次元の Array 型には、それぞれ Vector と Matrix という別名が付いています。なお、配列でない単純な数値はスカラー(scalar)と呼ばれます。

# 11.1.2 多次元配列の構築

まずは、要素が特定の値で初期化された配列の構築方法を説明しましょう。要素を0で初期化する zeros 関数と、1で初期化する ones 関数がよく使われます。どちらの関数も、第1引数と第2引数にそれぞれ要素の型と配列のサイズをとります。例えば、第1引数に Int 型を指定し、第2引数に (4, 3) を渡せば、すべての要素が Int 型の0で初期化されたサイズ (4, 3) の2次元配列が作られます。また、サイズは引数をスプラットして次のようにも書けます。

![](image_rsrcB6T.jpg)

特によく使われる Float64 型の要素を持つ配列を構築するときは、第1引数の型の指定を省略できます。

![](image_rsrcB6U.jpg)

配列の要素を0や1以外の特定の値で初期化したいときには fill 関数を使います。この関数は第1引数に初期値を、第2引数に配列のサイズをとります。

![](image_rsrcB6V.jpg)

zeros, ones, fill は全要素を同じ値で初期化する関数でしたが、各要素を個別に指定する方法もあります。角括弧([])のペアの間に、配列の要素を直接書き下す記法をとれば、各要素を個別に指定できます。

![](image_rsrcB6W.jpg)

これらのリテラルでは、構築される配列の要素の型は指定した値から推論されます。例えば [1, 2, 3] はどの要素も Int 型の値なので、構築される配列の要素も Int 型です。要素の型が混在するときは昇格規則([4.7.2項](c1JX.xhtml#a9UJ))に従って型が決まります。例えば、[1, 0x02, 3.0] と書いたとき、要素の型はばらばらですが、昇格規則に従って Float64 型が配列要素の型になります。また、要素を1つも書かなかったとき、つまり [] のときは、要素が Any 型の空の1次元配列になります。

![](image_rsrcB6X.jpg)

配列の要素の型を指定したいときは、左括弧の前にその型を指定できます。例えば、Int16[1, 0x02, 3.0] と書けば、Float64 ではなく Int16 型になります。これは Int[] のように特定の型の空配列を作るときに便利です。

![](image_rsrcB6Y.jpg)

最後に、要素を初期化せずに配列を構築する方法を紹介します。次のように Array{T} 型のコンストラクタを呼び出すと、パラメータ T に指定された型で指定サイズの未初期化の配列を構築します。第1引数の undef は未初期化配列の構築であることを表しています。

![](image_rsrcB6Z.jpg)

また、similar 関数に配列を渡すと、その配列と同じ型とサイズを持つ未初期化の配列を構築します。要素の型とサイズはそれぞれ第2引数と第3引数で変更できます。

![](image_rsrcB70.jpg)

このように配列を構築すると初期化処理を省けるので、配列の構築がやや高速化することがありますが、注意が必要です。なぜなら、未初期化の要素に入る値はまったくの未定義で、適切に使われなければ非常にややこしい問題を引き起こすからです。通常は zeros 関数などの初期化方法を使い、要求性能を満たせないときのみ未初期化配列の構築を試すことをおすすめします。

# 11.1.3 多次元配列の連結

配列を1から構築する以外にも、スカラーや配列を連結して新しい配列を作る方法もあります。まずは簡単のため、連結する対象を1次元配列に限定して説明しましょう。vcat 関数と hcat 関数は、配列を連結するときに使う最も簡単な関数です。vcat 関数は引数として渡されたスカラーや配列を縦方向(vertical)に連結し、hcat 関数は引数を横方向(horizontal)に連結します。接尾辞の cat はconcatenate(連結する)の短縮だと考えれば覚えやすいでしょう。

具体例で考えてみましょう。長さがそれぞれ m, n の2つのベクトル a, b があるとします。vcat (a, b) は a の下に b をつなげ、サイズが (m + n,) の1次元配列を作ります。

![](image_rsrcB71.jpg)

hcat(a, b) は m と n が同じ、つまり a と b が同じ長さであるときのみ動作します。このとき、hcat(a, b) は a の右に b をつなげ、サイズが (m, 2) の2次元配列を作ります。

![](image_rsrcB72.jpg)

vcat 関数と hcat 関数は3引数以上でも同様に動作します。また、ベクトルに限らずスカラーや行列も引数にとれます。ただし、hcat 関数の例で説明したように、結合する配列の長さが一致する必要があることに注意してください。

同じ配列を繰り返して連結するときは repeat 関数が便利です。repeat 関数は第1引数に繰り返す配列を、第2引数以降にそれぞれの次元で繰り返す回数をとります。キーワード引数を使って繰り返しのパターンを制御する方法も用意されていますので、詳しくは関数のドキュメントを参照してください。

![](image_rsrcB73.jpg)

さて、11.1.2項では角括弧を使った行列の構築方法を説明しました。実はこの記法は、vcat 関数と hcat 関数を使った配列の連結と本質的に同じ動作をします。先ほどと同様に、2つのベクトル a と b があるとしましょう。このとき、[a; b] は vcat(a, b) と、[a b] は hcat(a, b) と同じ結果を返します。つまり、角括弧内のセミコロンや改行は縦方向の連結を表し、空白は横方向の連結を表します。例えば、すでに見た [1 2 3; 4 5 6] という2行3列の行列を作る記法は、1, 2, 3 と 4, 5, 6 をそれぞれ空白で横方向に連結して2つの行を作った後、それらをセミコロンで縦方向に連結したものと読むこともできます。

これはブロック行列を作るときにも便利な記法です。A, B, C, D をそれぞれ行列とすると、サイズが合致すれば次のように行列を縦横に連結して、新しい行列を作れます。なお、それぞれのブロックはスカラーやベクトルでも構いません。

![](image_rsrcB74.jpg)

3次元以上の次元の配列を連結するときには、vcat 関数や hcat 関数を一般化した cat 関数が使えます。cat 関数では dims キーワード引数で連結する方向を指定できます。例えば、dims が1や2のとき、それぞれ vcat 関数や hcat 関数と同じように連結します。

![](image_rsrcB75.jpg)

Julia 1.7では連結の記法が一般化され、3次元以上でもスカラーや配列を連結できるようになりました。次のように、セミコロンの数と同じ次元で配列を連結します。

![](image_rsrcB76.jpg)

Julia 1.9では配列のコレクションを連結する stack 関数が導入されました。同じ操作は cat 関数などを使っても可能ですが、引数を渡すときにコレクションの要素をスプラットする必要がないので、多数の配列を連結するときにはより効率的です。

![](image_rsrcB77.jpg)

ここで紹介した多次元配列の連結は、元の配列への参照ではなく、新しい配列を確保してそこに元の配列のデータをコピーすることに注意してください。例えば、連結後の配列の要素を更新しても、元の配列の対応する要素は更新されません。

# 11.1.4 多次元配列の変形

配列の要素数を変えずにサイズを変更するには reshape 関数を使います。zeros 関数などと同様に、サイズはタプルとしても指定できます。

![](image_rsrcB78.jpg)

この例では1次元配列を2次元配列に変形しましたが、要素数が変わらなければ、どの次元の配列にも変形できます。特に、ベクトルに変換するときには vec 関数が手軽です。

reshape 関数を使った配列の変形で注意しなければならないのは、変形前の配列と変形後の配列が同じデータを共有しているということです。したがって、次のように変形後の配列の要素を更新すると、それは変形前の配列にも影響します。巨大な配列を変形するときには効率的ですが、共有させたくないときは copy 関数などで配列の複製を作りましょう。

![](image_rsrcB79.jpg)

また、permutedims 関数で次元の入れ替え、dropdims 関数で次元の削除ができます。

# 11.1.5 整数を使った2つのインデクシング

[11.1.1項](c4WA.xhtml#aA6B)で説明したように、多次元配列は複数のインデックスを使って要素を参照できるデータ構造です。n 次元配列なら n 個のインデックスを使って参照する要素を一意に決定できます。A を多次元配列とすると、整数のインデックス i, j, k, ...による要素の参照は A[i,j,k,...] のように書けます。特殊な構文として、インデックスに begin キーワードや end キーワードが使えます。begin はその次元の最初の要素を、end は最後の要素を参照します。

このように、多次元配列の要素をその次元数と同じ数のインデックスを使って参照する方法をデカルトインデクシング(cartesian indexing)と呼ぶことがあります。デカルトインデクシングは数学的な記法に近いので、多次元配列を使ったアルゴリズムの疑似コードなどをJuliaに移植するときに便利な記法です。複数のインデックスは1つのオブジェクトにまとめると便利なことが多いので、次のように使える CartesianIndex という型も用意されています。

![](image_rsrcB7A.jpg)

整数のインデックスで配列の要素を参照する方法はもう1つあります。配列 A に対し1つの整数 i で A[i] と書くと、多次元配列 A を1次元に引き伸ばしたときに i 番目にくる要素を参照します。デカルトインデクシングに対して、多次元配列の要素を1つの整数インデックスを使って参照する方法を線形インデクシング(linear indexing)と呼ぶことがあります。なお、1次元配列のときは両者に差はありません。

サイズが (2, 3) の2次元配列に関して、2つのインデクシング方法の具体例を図11.2に示します。図中の赤線で描かれた矢印は、配列の要素がメモリ上で並ぶ順序を示しています。ただし、これは標準ライブラリにある Array 型の配列の場合であり、他の型の配列では異なる可能性もあることに注意してください。例えば、[11.4節](c4WA.xhtml#a986)で紹介する疎行列は、Array 型の行列とは内部の表現方法がまったく異なります。配列のメモリレイアウトについては[18.7.2項](c7N0.xhtml#a9G1)の説明も参考にしてください。

![](image_rsrcB7B.jpg)

# 11.1.6 インデクシング関数

続いてインデクシングに用いる関数をいくつか紹介します。[9.4.6項](c434.xhtml#a9K2)でも説明したように、配列要素の参照と更新はそれぞれ getindex 関数と setindex! 関数に書き換えられます。複数のインデックスを持つデカルトインデクシングにおいても、引数の数が増えるだけで原理は同じです。

![](image_rsrcB7C.jpg)

配列の最初と最後のインデックスは、それぞれ firstindex 関数と lastindex 関数で得られます。デカルトインデクシングで特定の次元のインデックスがほしいときは第2引数にその次元を指定します。

![](image_rsrcB7D.jpg)

firstindex 関数と lastindex 関数が返すインデックスは、次の例に示すようにそれぞれインデックスの begin キーワードと end キーワードを置き換えます。デカルトインデクシングの場合も対応する次元のインデックスに変換されます。

![](image_rsrcB7E.jpg)

firstindex 関数が返す値は、ほとんどの配列型では 1 ですが、そうでなければならないという決まりはありません。つまり、配列のインデックスが1始まりというのは慣習に過ぎません。例えば、OffsetArrays.jlパッケージの配列では任意の開始インデックスが選べます。したがって、配列のインデックスを使う処理を書くとき、あらゆる配列で動作するようにするには、最初のインデックスを 1 ではなく firstindex 関数が返す値(もしくは begin)にすべきです。同様に、最後のインデックスは length 関数などではなく、lastindex 関数が返す値(もしくは end)にすべきです。例えば、配列 A の全要素をfor文で順に走査する処理は次のように書けます[*3](c4WA.xhtml#a9HY)。

![](image_rsrcB7F.jpg)

このような配列の全インデックスを扱う処理は頻繁に登場するので、標準ライブラリには配列の全インデックスのイテレータを返す eachindex 関数が用意されています。このとき作られるインデックスは配列に依存し、デカルトインデクシングのときもあれば線形インデクシングのときもあります。

![](image_rsrcB7G.jpg)

特定の次元の全インデックスが必要なときは axes 関数を使います。例えば、行列の行と列を走査するfor文は axes 関数を使って次のように記述できます。

![](image_rsrcB7H.jpg)

標準ライブラリには、デカルトインデックスの集合を表現する配列型である CartesianIndices 型が用意されています。この型にはさまざまな応用[*4](c4WA.xhtml#aACX)がありますが、やや内容が高度になるので、本書では詳しく扱いません。いくつかの応用は公式サイトに解説記事[*5](c4WA.xhtml#aACY)がありますので、興味があればそちらを参照してください。

# 11.1.7 範囲や配列を使ったインデクシング

ここまでの説明では、インデックスは1つの要素を指定するスカラーでしたが、配列はスカラー以外にも次の3種類のインデックスで配列の一部を参照できます。

●インデックスの範囲[*6](c4WA.xhtml#a9RR)(3:5 など)

●インデックスの配列([2, 4, 5] など)

●Bool 型の要素を持つ配列([false, true, true, false] など)

インデックスの範囲と配列を使って、2次元配列の一部を取り出す例を見てみましょう。

![](image_rsrcB7J.jpg)

範囲にはその次元の最初の位置を指す begin キーワードや最後の位置を指す end キーワードがよく使われます。また、範囲にはコロンのみ書くこともできます。コロンのみの範囲[*7](c4WA.xhtml#aA1A)は、その次元のすべてのインデックスに相当します。

![](image_rsrcB7K.jpg)

インデックスの配列は範囲より柔軟なインデクシングが可能です。例えば、次のようにインデックスの配列はある領域を複数回参照したり、隣り合わない領域を参照したりできます。

![](image_rsrcB7M.jpg)

最後に、Bool 型の要素を持つ配列を使ったインデックスについて説明しましょう。前提として、インデックスとして使う配列の長さは参照される配列の次元の長さと一致している必要があります。この前提のもとで、インデックスの配列で要素が true になっている位置の要素が参照されます。これはロジカルインデクシング(logical indexing)と呼ばれます。ロジカルインデクシングの動作は具体例を見たほうがわかりやすいでしょう。例えば、3列の行列 A について次のように書けば、インデックスの2番目と3番目の要素が true になっているので、A の2列目と3列目を参照します。

![](image_rsrcB7N.jpg)

ロジカルインデクシングには Bool 型に特化した専用の配列型がよく使われます。例えば、1次元配列 v について、v .> 0 は BitVector 型の配列[*8](c4WA.xhtml#a9BR)を返すので、次のように v のうち正の数である要素だけを取り出した配列が作れます。

![](image_rsrcB7P.jpg)

# 11.1.8 インデクシングの注意

配列のインデクシングに関する注意を2点説明します。

第一に、スカラーによるインデックスと範囲や配列によるインデックスには1つ大きな違いがあります。それは、スカラーのインデックスでは常にその次元を削除するのに対し、範囲や配列のインデックスでは常にその次元を保持する点です。例えば、2次元配列 A に対し、A[1,2:3] と書けば、インデックスがスカラーの第1次元は消えますが、インデックスに範囲を使っている第2次元は残り、結果として1次元配列になります。A[2:2,3:3] のように、仮に次元の長さが1になったとしても、範囲や配列のインデックスであれば長さ1の次元として残ります。言葉だけでは理解しにくいので、次の例で動作をよく確認してください。

![](image_rsrcB7R.jpg)

第二に、インデックスを使って配列の領域を取り出すと、参照された領域をコピーした新しい配列が返ります。元の配列とは独立した配列になるので、片方を更新してももう一方には影響しません。次の例で、配列 A が配列 v の要素を更新した後でも変化していないことを確認してください。

![](image_rsrcB7S.jpg)

# 11.1.9 部分配列

配列の特定の領域に関数を適用することは、科学技術計算のプログラムで頻出するパターンです。例えば、行列の特定の列にある要素の総和を計算することを考えてみましょう。A[:,j] のように書けば、行列 A の j 列目をベクトルとして取り出せるので、sum(A[:,j]) とすれば j 列目にある要素の総和が容易に計算できます。このとき、配列の j 列目がベクトルとしてコピーされますが、このベクトルは計算後には不要になるので、無駄にコピーしたことになります。これは計算性能の悪化につながります。

そのようなときに役立つのが部分配列(subarray)です。部分配列は元の配列の一部をコピーするのではなく、その領域への参照を保持する配列です。部分配列は元の配列の実体は持たないので、コピーのコストが発生せず、うまく使えば性能を大きく向上させることもあります。部分配列は SubArray という型のオブジェクトを使って表現されますが、SubArray 型のコンストラクタを直接呼び出すことはあまりなく、これから紹介する関数やマクロを使って配列から部分配列を作ります。配列 Array と部分配列 SubArray の関係は、文字列 String と部分文字列 SubString の関係に対応すると考えるとわかりやすいでしょう。

部分配列は view 関数で作成できます。view 関数の第1引数には配列を、第2引数以降にはインデックスを渡します。次の例を見ると使い方がよくわかるでしょう。

![](image_rsrcB7T.jpg)

部分配列は元の配列への参照を保持するので、部分配列を更新すると、元の配列の対応する部分も更新されます。これは角括弧を使ったインデクシングとは異なる動作なので注意してください。

![](image_rsrcB7U.jpg)

なお、view 関数では begin キーワードや end キーワードが使えません。そのため、view 関数の代替として、これらのキーワードによるインデクシングを可能にする @view マクロが用意されています。なお、@view マクロは1つの配列に対するインデクシングにしか使えませんが、複数のインデクシングにも使える @views マクロも用意されています。

![](image_rsrcB7V.jpg)

view 関数や @view マクロで作った部分配列は、そのまま関数に渡して計算をしたり、インデックスを使って部分配列の一部を参照したりできます。これは、配列を引数とする多くの関数が Array 型と SubArray 型の上位型、例えば AbstractArray 型に対してメソッドを定義しているからです。

![](image_rsrcB7W.jpg)

view 関数は汎用的ですが、特定の用途には行列の各行の部分配列をイテレータとして返す eachrow 関数、各列の部分配列をイテレータとして返す eachcol 関数、およびそれらを一般化した eachslice 関数もよく使われます。また、次元と範囲を引数で指定して部分配列を作る selectdim 関数も便利です。使用方法について詳しくはドキュメントを参照してください。

# 11.1.10 ベクトル化演算

配列の各要素に同じ関数を適用する処理は頻繁に使われます。このような処理をベクトル化演算(vectorized operation)と呼びます。ベクトル化演算は、11.1.11項で説明するブロードキャストの特殊な例と考えられます。

ベクトル化演算は数値計算で頻繁に使われるので、Juliaには専用の構文が用意されています。exp 関数などの中置できない通常の関数では、関数名の後にドット(.)を付けて呼び出すと、引数として与えられた配列の各要素にその関数を適用します。次の例と図11.3で動作を確認してください。

![](image_rsrcB7X.jpg)

![](image_rsrcB7Y.jpg)

ベクトル化演算は2次元以上の配列やタプルにも適用できます。

![](image_rsrcB7Z.jpg)

複数の引数をとる関数の場合、引数として与えられた配列の対応する要素ごとの演算となります。例えば、max 関数を3要素を持つ2つの配列に対して適用すると、ベクトル化演算の結果は対応する各要素の大きいほうの値になります。図11.4に、この動作を示します。これは3引数以上の場合でも同様です。

![](image_rsrcB80.jpg)

![](image_rsrcB81.jpg)

+ 関数などの中置できる関数では、関数名の前にドットを付けて呼び出すとベクトル化演算になります。max 関数などと同様に、配列の対応する要素ごとに適用されます。

![](image_rsrcB82.jpg)

ただし、+ 関数と - 関数については、ベクトル化演算と同じように要素ごとの和と差の演算を行うメソッドが定義されています[*9](c4WA.xhtml#a9NS)。

![](image_rsrcB83.jpg)

また、- 関数や ! 関数などの前置して使う1引数関数でもベクトル化演算が使えます。

![](image_rsrcB84.jpg)

# 11.1.11 ブロードキャスト

サイズが異なる引数が対象のとき、ベクトル化演算の概念はそのままでは通用しません。例えばスカラー α をベクトル x の各要素に加えたベクトルを作りたいとしましょう。+ 関数にそれを行うメソッドは定義されていないので、α + x はエラーになります[*10](c4WA.xhtml#a9VC)。ブロードキャスト(broadcast)は、ベクトル化演算をサイズの異なるオブジェクトへ拡張します。例えば、前述の演算はブロードキャストを用いれば次のように行えます。

![](image_rsrcB85.jpg)

ブロードキャストでは、引数 A と B のサイズが次のように揃えられます。まず、A と B のサイズが左から右へ揃えられます[*11](c4WA.xhtml#aA6F)。このとき次元数が合わずに揃えられなければ、足りないほうを1で補います[*12](c4WA.xhtml#aA6G)。A と B のサイズをそれぞれ (3,) と (1, 2) とすれば、2次元である後者に合わせて前者は (3,) → (3, 1) となります。次に、対応する次元の長さが一致するように長さ1の次元が引き伸ばされます。(3, 1) と (1, 2) では、第1次元が3に、第2次元が2に引き伸ばされ、ともに (3, 2) となるわけです。なお、引き伸ばしてもサイズが合わせられなければ、ブロードキャストは失敗します。このブロードキャストの様子を図11.5に示します。

![](image_rsrcB86.jpg)

ブロードキャスト後の対応する要素の演算はベクトル化演算と同じように行われます。実行例を次に示します。

![](image_rsrcB87.jpg)

ベクトル化演算やブロードキャストは標準ライブラリの関数に限定されません[*13](c4WA.xhtml#aA73)。例えば、次のようなユーザ定義の logit 関数でも、ベクトル化演算が行えます。

![](image_rsrcB88.jpg)

# 11.1.12 フュージョン

ブロードキャストに関連した機能として、フュージョン(fusion)を説明しましょう。フュージョンは複数のベクトル化演算やブロードキャストが連続して行われるときに、中間的に作られる配列を排除して効率化する仕組みです。

次の例を見てください。スカラー α とベクトル x をかけ、さらにベクトル y を足すだけの簡単な計算です。ブロードキャスト構文を使う方法と使わない方法の2通りで記述しています。

![](image_rsrcB89.jpg)

これら2つの書き方で計算結果は同じですが、ブロードキャストを使わないほうは途中にある α * x の計算結果を保持する配列を作るのに対し、ブロードキャストを使うとフュージョンが起こり、中間配列を作らずに結果を出力先の配列に直接書き込みます。実質的には、このフュージョンの計算は次のfor文を使った計算と等価になります。

![](image_rsrcB8A.jpg)

さらに、破壊的な代入を行う .= という記法も用意されており、これを使えば新しい配列を一切作らない配列の更新を簡潔に記述できます。二項演算子と組み合わせて .+= のような複合代入演算子にすることも可能です。

![](image_rsrcB8B.jpg)

フュージョンは中間配列を作らないので、メモリ割当ての削減につながります。詳しくは[18.6.3項](c7N0.xhtml#a9B8)で扱います。

フュージョンは頻繁に使われますが、すべての関数呼出しにドットを付けて回るのは面倒なので、@. という特殊なマクロが用意されています。@. マクロは、渡された式にある関数呼出しと代入演算子をすべてブロードキャスト化演算に変換します。例えば、次のような書き換えができます。

![](image_rsrcB8C.jpg)

# 11.1.13 ビット配列

ビット配列(bit array)は Bool 型の値のみを要素とする特殊な配列です。ビット配列の型名は BitArray です。1次元ビット配列には BitVector、2次元ビット配列には BitMatrix という別名が定義されています。

false で埋められたビット配列は falses 関数で、true で埋められたビット配列は trues 関数で作ります。それぞれ通常の配列を構築する zeros 関数と ones 関数に対応しています。なお、REPLの表示では次のように 0 と 1 で要素の値が表示されますが、配列からインデクシングで取り出される値はそれぞれ false と true に対応します。

![](image_rsrcB8D.jpg)

ビット配列は次のようにブロードキャストの結果を返すときにも使われます。

![](image_rsrcB8E.jpg)

ビット配列は Bool 型を要素とする Array 型に似ていますが、後者が1要素あたり8ビットのメモリを消費するのに対し、前者は1要素あたり1ビットしか消費しません。つまり、メモリ消費量は Array{Bool} に比べて約1/8になります[*14](c4WA.xhtml#a9R5)。これはビット配列が内部で1つの整数に可能な限りビットを詰めるからです[*15](c4WA.xhtml#a9R6)。

![](image_rsrcB8F.jpg)

# 11.2 疑似乱数

疑似乱数(pseudorandom number)は、モンテカルロ法(Monte Carlo method)などのさまざまな計算アルゴリズムで使われます。Juliaは標準で高速な疑似乱数の生成器を提供しています。疑似乱数に関するツールは、基本的に標準ライブラリのRandom.jlパッケージで定義されています。例外的に、rand 関数と randn 関数は Base モジュールから提供されているので、using Random と書かなくても使えます。本節では、これら2つの関数から説明を始めます。なお、いちいち疑似乱数と正確に記述する[*16](c4WA.xhtml#aAE4)のも煩わしいので、特に注意が必要でない限り、単に乱数と呼びます。

# 11.2.1 基本的な疑似乱数生成関数

Base モジュールにある rand 関数は基本的な乱数生成関数です。rand 関数は主に2つの乱数生成機能を提供しています。1つ目の機能は、一様な浮動小数点数を生成する機能です。rand() のように引数なしで呼び出すと、0以上1未満の Float64 型の浮動小数点数を偏りなく生成し、その値を返します。このような乱数の分布を一様分布(uniform distribution)と呼びます。zeros 関数などと同様に、引数として型やサイズを渡すと、指定した型やサイズの乱数で埋められた配列を生成します。

![](image_rsrcB8G.jpg)

2つ目の機能は、範囲や配列から偏りなくサンプリング(sampling)する機能です。例えば、rand(0:255) と書くと、0以上255以下の Int 型の値からランダムに1つの値をサンプリングし、その値を返します。引数として範囲の代わりに配列を渡せば、その配列の全要素から1つサンプリングし、その値を返します。第1引数には Bool 型や UInt8 型などの基本的な数値型を渡すこともできます。こちらも第2引数以降に配列のサイズを渡すと、第1引数から独立にサンプリングされた値で埋められた指定サイズの配列を生成します。次の例で、これら rand 関数の動作を確認してください。

![](image_rsrcB8H.jpg)

最後に n が付いた randn 関数は、rand 関数と同様 Base モジュールから提供されています。rand 関数は一様分布からの乱数生成でしたが、randn は平均が0で分散が1である正規分布(normal distribution)[*17](c4WA.xhtml#a9SF)からの乱数生成をします。関数名の最後の n はnormalを意味すると考えれば覚えやすいでしょう。使い方は rand 関数と同様です。

![](image_rsrcB8J.jpg)

randn 関数が生成する正規分布のパラメータは平均が0、分散が1に固定されていますが、次のように生成した乱数に標準偏差[*18](c4WA.xhtml#a9B4)σをかけて平均μを足せば、実質的に分布のパラメータを変更できます。

![](image_rsrcB8K.jpg)

一様分布や正規分布以外の分布から乱数を生成したい場合は、Distributions.jlが便利です。このパッケージには、ポアソン分布やカイ二乗分布など、多くの分布が定義されています。

# 11.2.2 その他の疑似乱数生成関数

Random.jlパッケージには、rand 関数や randn 関数より高度な乱数生成関数が定義されています。表11.1に関数の一覧を示しますが、ここでは重要度の高い関数を例に使用方法を解説します。完全な機能については、各関数のドキュメントや公式ドキュメントを参照してください。以降で紹介する関数を使うには、モジュールを読み込むため、事前に using Random を実行する必要があることに注意してください。

![](image_rsrcB8M.jpg)

まずは rand 関数と randn 関数の破壊版である rand! 関数と randn! 関数を紹介します。これらの関数はそれぞれ対応する非破壊版と同じように分布から乱数の生成を行いますが、結果を新しい配列として返すのではなく、引数として渡された配列に生成した乱数を埋めます。例えば x = zeros(5) として確保した配列 x があるとき、rand!(x) を実行すると x の全要素はそれぞれ0以上1未満の一様分布から生成された値で更新されます。破壊的な関数を使い配列を使い回すことで、大量の乱数を繰り返し生成するプログラムでは性能が向上する効果が期待できます。

![](image_rsrcB8N.jpg)

shuffle 関数は、引数として与えられた範囲や配列の要素の順番をランダムに入れ替える関数です。例えば、shuffle(1:10) を実行すると、1から10までの整数の順序をランダムに並べ替えた1次元配列が生成されます。この関数を使えば、統計や機械学習で頻出する行列の行や列の順序をランダムに入れ替える処理を次のように簡単に書けます。

![](image_rsrcB8P.jpg)

# 11.2.3 再現性のある疑似乱数生成

乱数生成関数が生成する乱数は、当然ながら予測ができません。rand() を2回実行すれば、互いに関係のない値が生成されます。また、julia -e 'println(rand())' とコマンドを何度か実行すればわかるように、Juliaの起動直後でも生成される乱数は毎回変わります。しかし、これは技術計算などで計算結果の再現性が必要なときに不都合です。

Random.jlパッケージは、乱数生成の再現性を確保する機能を提供しています。まずは、最も手軽に再現性を確保する方法を説明しましょう。Random.jlの seed! 関数は、デフォルトの乱数生成器の状態をリセットする関数です。シード(seed)と呼ばれる整数を引数として seed! 関数を呼び出し乱数生成器の状態がリセットされると、それ以降は再現性のある乱数の列が生成されるようになります。どのような乱数列が生成されるかはシードによって異なり、同じ乱数列を生成するには同じシードでリセットする必要があります。次の例では、シードを与えて乱数生成器の状態をリセットし、rand 関数で再現性のある乱数列が生成されています[*19](c4WA.xhtml#aADK)。rand 関数以外でも同様に再現性のある乱数列が生成されます。

![](image_rsrcB8R.jpg)

乱数生成器はオブジェクトとして扱えるので、デフォルトの乱数生成器の状態に依存しない処理を記述できます。Random モジュールから提供されている MersenneTwister 型のオブジェクトは、高速で高品質な乱数生成器です。これはJulia 1.6までのデフォルトの乱数生成器でもあります。ここまでに紹介した乱数生成関数は、第1引数に MersenneTwister 型のオブジェクトをとると、それをもとに乱数を生成します。次の例では、MersenneTwister を使って再現性がある乱数列を生成しています。異なる乱数生成器から、同じ乱数列が生成されていることに注意してください。

![](image_rsrcB8S.jpg)

Julia 1.7では新しい乱数生成器である Xoshiro 型が導入され、デフォルトの乱数生成器になりました。使い方は MersenneTwister 型と同じですが、よりメモリ使用量が少なく高速とされています。さらに、Random.seed! 関数によるデフォルトの乱数生成器のシード設定が、タスク([12.1.3項](c5F8.xhtml#a9V1))単位になりました。これによって独立したタスクは互いに干渉することなく Random.seed! 関数を呼び出して再現性のある乱数生成を行えます。なお、Xoshiro 型と MersenneTwister 型はアルゴリズムが異なり、同じシードを指定しても生成される乱数列は異なるので、注意してください。

# 11.3 線形代数

線形代数(linear algebra)に関わる機能は、[11.1節](c4WA.xhtml#a983)で紹介した多次元配列とあわせて、Juliaを科学技術計算で使う大きな動機の1つでしょう。標準ライブラリにはLinearAlgebra.jlという線形代数のパッケージがあり、豊富な型と関数を提供しています。また、Juliaの言語機能の一部は、線形代数との親和性を意識して設計されています。Juliaの提供する線形代数の機能はかなり広範に及び、本節ではすべてを扱えませんので、特に応用範囲が広いと考えられる部分をかいつまんで解説します。完全な機能については公式ドキュメントを参照してください。また、本節では基本的な線形代数の知識を前提とします。

コードを見やすくするため、本節の説明で使用する変数を次のように取り決めておきます。以降、スカラーを α, β, γ などギリシャ文字の小文字、ベクトルを x, y, z などの英字の小文字、行列を A, B, C などの英字の大文字を使って表します。コードでなく数式でも同様です。

# 11.3.1 ベクトルと行列の表現

ベクトルと行列はそれぞれ1次元配列と2次元配列で表現されます。Vector 型と Matrix 型がベクトルと行列を表現する汎用的な型[*20](c4WA.xhtml#aA9J)ですが、これら以外にも構造や用途に特化した型が用意されています。一般的にいえば、抽象型である AbstractVector 型の下位型のオブジェクトがベクトル、AbstractMatrix 型の下位型のオブジェクトが行列とみなされます。もちろん、Vector 型と Matrix 型は、それぞれ AbstractVector 型と AbstractMatrix 型の下位型です。

![](image_rsrcB8T.jpg)

Vector 型以外では、範囲や1次元の部分配列もベクトルの例です。11.4節で扱う疎ベクトルもベクトルです。

![](image_rsrcB8U.jpg)

慣習として、Juliaでは数が縦方向に並んだ列ベクトル(column vector)を標準的なベクトルの表現としています。線形代数の教科書も多くが同じ慣習に則っているので、これは馴染みのあるものでしょう。adjoint(x) もしくは x' と書くと、行ベクトル(row vector)に対応する表現が得られます。後で見るように、列ベクトルと行ベクトルの違いは操作に現れます。

行列の表現には汎用的な Matrix 型以外にも、その構造に応じて多数の型が用意されています。LinearAlgebra.jlの提供する代表的な行列の型を表11.2に示します。

![](image_rsrcB8V.jpg)

行列の構造によって型を使い分けることはやや煩雑に思えるかもしれませんが、オブジェクトが使用するメモリを削減したり、多重ディスパッチでその構造に特化したアルゴリズムを使用できたりするなど、大きな利点があります。例えば、対角行列(diagonal matrix)を表す Diagonal 型の行列では、対角成分以外の要素はゼロなので明示的に要素を持つ必要はなく[*21](c4WA.xhtml#aA4M)、行列積などを高速に計算できますし、上三角行列(upper triangular matrix)を係数行列とする線形方程式系を解くときは行列分解を省略できます。

![](image_rsrcB8W.jpg)

表11.2の行列がデータを保持する方法について、少し説明を加えます。三角行列や対称行列などは、元となる行列の薄いラッパーです。つまり、次に示すようにインタフェースとなる関数(getindex 関数など)のメソッドの振る舞いを変えてはいますが、元の行列を内部のdata フィールドにそのまま保持しています。したがって、例えば元の行列 A を更新すれば、ラップしている上三角行列 U や対称行列 S にも影響しますので注意してください。

![](image_rsrcB8X.jpg)

# 11.3.2 ベクトルと行列の演算

行列とベクトルに関わる関数で使用頻度が高いものを表11.3にまとめます。行列分解については、11.3.3項で別に扱います。各関数の詳細はドキュメントに譲るとして、ここでは特に重要で説明が必要な関数について解説を加えます。

![](image_rsrcB8Y.jpg)

# スカラー倍とベクトル和

線形代数において最も基本的なベクトルの演算であるスカラー倍(scalar multiplication)とベクトル和(vector addition)は、それぞれ * 関数と + 関数で計算できます。例えば、α * x がスカラー倍、x + y がベクトル和です。整数や浮動小数点数のように加法や乗法に関して可換[*22](c4WA.xhtml#aA70)であれば、引数の順序を逆にした x * α や y + x も同じ結果になります。また、行列に関しても同様に α * X や X + Y などの演算が可能です[*23](c4WA.xhtml#aA71)。

一方、ベクトル間の x * y はメソッドが定義されていないのでエラーになります。これはNumPyや R では要素ごとの積になるので、NumPyや R に慣れた人にとっては奇妙に思えるかもしれませんが、Juliaはいろいろな面で数学に忠実な記法を採用しているので、通常の線形代数では定義されていないベクトル間の乗法はJuliaでは採用されていません。代わりに x .* y のようなベクトル化演算([11.1.10項](c4WA.xhtml#a9T1))を使った記法が定義されているので、実用上困ることはありません。行列間の要素ごとの演算でも同様です。

# ドット積と行列積

ドット積(dot product)やスカラー積(scalar product)と呼ばれるベクトル間の演算は dot 関数で計算します。dot 関数は通常2引数をとりますが、双線形形式(bilinear form)x'Ay の計算に使える dot(x, A, y) という3引数の使い方も用意されています。また、・(![](image_rsrcAGC.jpg)cdot)という別名もあります。さらに、後述する随伴を使った方法でも計算できます。

![](image_rsrcB8Z.jpg)

行列ベクトル積や行列積は * 関数で計算します。計算結果のサイズにかかわらず、行列とベクトルの積は常にベクトルを返し、行列と行列の積は常に行列を返します。

![](image_rsrcB90.jpg)

# 対角成分

行列の対角成分は diag 関数でベクトルとして取り出せます。このとき取り出されるベクトルは対角成分のコピーなので、このベクトルを更新しても元の行列の対角成分は更新されないことに注意してください。対角成分を更新するには diagind 関数を使う方法が便利です。

![](image_rsrcB91.jpg)

行列の対角成分に値を加える操作は I という特殊なオブジェクトを使用すると簡潔に記述できます。例えば、対角成分に 1/8 を加える操作は A + 1/8*I と記述できます。I は UniformScaling という型のオブジェクトであり、UniformScaling はサイズが不定の単位行列のように振る舞います。

# 転置と随伴

線形代数ではベクトルや行列の随伴(adjoint)[*24](c4WA.xhtml#aA3K)は頻繁に使われます。そのため、Juliaには x や X の随伴を表現する x' や X' という構文が用意されています。x が列ベクトルのとき、x' は特に行ベクトルとも呼ばれます。11.3.1項で説明した三角行列などと同様に、この操作も随伴を実体として作るのではなく、元のベクトルや行列への参照を保持して随伴のように振る舞うオブジェクトを作ります。また、x'*x や A'*x は * を省略して x'x や A'x とも書けます。

![](image_rsrcB92.jpg)

列ベクトルの随伴として作成した行ベクトルは、1行の行列とは振る舞いが異なることがあることに注意してください。例えば、次のように列ベクトルとの積を計算すると、行ベクトルと列ベクトルの積はスカラーになるのに対し、1行の行列と列ベクトルの積は長さが1の列ベクトルになります。行列との積でも同様の違いが生じます。

![](image_rsrcB93.jpg)

# 線形方程式

非特異な正方行列 A に対して、ベクトル x や行列 X を解とする線形方程式系である Ax=b や AX=B を解くには![](image_rsrcAGC.jpg)関数を使います。つまり、解 x は A ![](image_rsrcAGC.jpg) b、解 X は A ![](image_rsrcAGC.jpg) B で計算できます。積の順序が逆になった xA=b(x と b は行ベクトル)や XA=B であれば、棒の倒れる向きが逆の / 関数を使って b / A や B / A で解けます。A ![](image_rsrcAGC.jpg) B が A-1B に、B / A が BA-1 に対応すると考えればわかりやすいでしょう。

![](image_rsrcB94.jpg)

これらの解き方は inv 関数を使って A の逆行列を計算してから積を計算する方法、つまり inv(A) * B を計算する方法に比べて一般に高速かつ正確です。したがって、本当に逆行列が必要な場合以外は、![](image_rsrcAGC.jpg) 関数や / 関数を使いましょう。また、A が正方行列でない場合、A ![](image_rsrcAGC.jpg) B はノルム最小となる最小二乗解を返します。

# 正方行列をとる解析関数

解析関数(analytic function)[*25](c4WA.xhtml#aA47)は正方行列にも適用できます。指数関数・対数関数・三角関数などはこうした解析関数の例です。指数関数 exp の例を次に示します。

![](image_rsrcB95.jpg)

# 11.3.3 行列分解

行列分解(matrix factorization)は、行列を複数の行列の積に分解する手法です。LinearAlgebra.jlは表11.4に示すようにさまざまな種類の行列分解を提供しています。本書では行列分解の応用までは詳しく扱いませんが、行列分解のインタフェースについて例を挙げて解説します。行列分解について詳しくは、それぞれの関数のドキュメントや線形代数の教科書などを参考にしてください。

![](image_rsrcB96.jpg)

# 行列分解の基本操作

行列分解の手法と関数は一対一に対応しています。ここではQR分解を例に行列分解の使い方を説明しますが、他の行列分解でも使い方はほぼ同じです。

行列 A に対してQR分解を行うには qr(A) を実行します。QR分解は行列 A を A=QR となる直交行列 Q と上三角行列 R に分解します。qr 関数の戻り値は、分解の結果を保持するオブジェクトであり、分解された行列そのものではありません。しかし、次のように Q プロパティと R プロパティを参照することで、分解された行列を取得できます。

![](image_rsrcB97.jpg)

qr 関数が返すオブジェクトは行列成分のイテレータになっているので、次のように各成分を直接的に受けとることもできます。

![](image_rsrcB98.jpg)

QR分解の結果をよく見てみると、R 成分は Matrix 型ですが、Q 成分はそうなっていないことがわかります。このままでも行列積などの行列操作には対応していますが、汎用的な行列でないため不便なこともあります。通常の行列がほしいときは、次のように Matrix コンストラクタにQ 成分を渡せば変換できます。

![](image_rsrcB99.jpg)

各行列分解関数には、qr! などのように ! が付いた破壊版の関数も用意されています。破壊版の行列分解は、入力の行列を計算のための作業空間として使うので、やや高速になることがあります。反復アルゴリズムなどで繰り返し行列分解を行うときには特に効果があるでしょう。ただし、分解後の行列の値は予測できないので、別の計算には再利用しないよう注意してください。

# 分解する行列が持つ構造の影響

行列分解が特殊な行列の構造を仮定するとき、浮動小数点数の丸め誤差などが原因でその仮定が厳密には満たされないことがあります。例えば、コレスキー分解は正定値性を持つ対称行列にのみ適用可能ですが、わずかな誤差のせいで対称性が崩れると直接適用できません。

![](image_rsrcB9A.jpg)

この場合、次のように Symmetric 型で行列をラップすれば誤差を無視して対称行列とみなすようにできます。対角成分とそれより上の成分を使うか下の成分を使うかは、コンストラクタに渡す第2引数で制御できます。

![](image_rsrcB9B.jpg)

コレスキー分解と同様に、固有値分解でも Symmetric 型を使って対称性を強制できます。Symmetric 型を使って実対称行列であることを保証すれば、固有値は必ず実数になります。場合によっては計算が高速化することもあるので、数学的に対称性やエルミート性が仮定できるときには Symmetric 型や Hermitian 型を積極的に使うとよいでしょう。また、大規模な行列の一部の固有値・固有ベクトルのみが必要なときには、Arpack.jlパッケージの eigs 関数が役に立つので、必要に応じて検討してみてください。

# 11.4 疎な配列

配列の大部分がゼロで埋められているとき、その配列は疎(sparse)であるといいます。疎な性質をうまく使うことで、巨大な配列をコンパクトに収めたり、さまざまな計算を高速化できたりすることがあります。Juliaでは、標準ライブラリのSparseArrays.jlパッケージで疎なベクトルと行列を扱うツールを提供しています。本節では疎な配列の扱い方を簡単に紹介します。

# 11.4.1 疎なベクトルと行列の構築と基本操作

疎ベクトルや疎行列を作るには、spzeros 関数を呼び出します。spzeros 関数は、すべての要素がゼロで初期化された疎ベクトルや疎行列を返します。使い方は zeros 関数と似ています。

![](image_rsrcB9C.jpg)

spzeros 関数のほかに、sparse 関数を使って疎行列を作る方法も一般的です。sparse(I, J, V, m, n) のように5つの引数を与えて呼び出すと、I で指定された行と J で指定された列に V の値を入れます。最後の m と n は行列のサイズを指定する引数ですが、省略すれば I と J の最大値から自動的に決定されます。おおむねリスト11.1の mysparse 関数と同じような動作をすると考えればよいでしょう[*26](c4WA.xhtml#a9KE)。また、疎ベクトルを作る sparsevec 関数も用意されています。

![](image_rsrcB9D.jpg)

このほか、sprand 関数と sprandn 関数を使えば、乱数で初期化された疎ベクトルや疎行列が作れます。これらはそれぞれ一様分布の rand 関数と正規分布の randn 関数に対応しますが、第3引数に乱数で埋める要素の密度、つまり非ゼロ要素が配列の全要素に占める割合[*27](c4WA.xhtml#a9ME)をとります。

![](image_rsrcB9E.jpg)

疎ベクトルと疎行列は通常のベクトルと行列と同様の演算をサポートしています。11.3節で線形代数の演算を扱っていますので繰り返し説明はしませんが、疎なベクトルや行列に特有なものだけ簡単に説明しましょう。

nnz 関数[*28](c4WA.xhtml#a9VG)は非ゼロの要素数を返します。また、nonzeros 関数は非ゼロ要素を1次元配列として返します。

![](image_rsrcB9F.jpg)

dropzeros 関数とその破壊版である dropzeros! 関数は、疎なベクトルや行列に明示的に収められたゼロの値を削除します。後述するように、そもそも疎なベクトルや行列はゼロの値を収める必要はないので、データの読込みや計算の過程で侵入したゼロを削除すれば、計算やメモリ使用量を効率化できます。また、実質的にゼロとみなせるような、ある閾値以下の値を削除する droptol! 関数も用意されています。

![](image_rsrcB9G.jpg)

その他の関数に関しては、表11.5とドキュメントを参照してください。また、固有値問題と特異値分解を疎行列に適用する場合は、それぞれArpack.jlパッケージの eigs 関数と svds 関数を使用してください。

![](image_rsrcB9H.jpg)

# 11.4.2 疎行列の内部構造

疎配列を使う利点の1つは、巨大な配列のメモリ使用量を抑えられる点です。例えば、次のように100行100列の疎行列を作り、そのうちの約1%の値を非ゼロ値でランダムに埋めます。そうしてできた疎行列のメモリ使用量を Base.summarysize 関数を使って密行列と比較すると、およそ3%程度しかメモリを使っていないことがわかります。

![](image_rsrcB9J.jpg)

このようにメモリ使用量を大きく抑えられる理由は、疎ベクトルや疎行列が Vector 型や Matrix 型の配列とは異なる内部構造を持っているからです。疎ベクトルや疎行列はこの構造を利用してメモリ使用量と計算を効率化するのですが、その構造を知らずに間違った使い方をすると、かえって非効率になることもあります。

疎行列を例に内部構造を説明しましょう。ここまでに登場した疎行列は SparseMatrixCSC という型であり、CSC(compressed sparse column)形式という構造で行列の要素が収められています。この構造では、値がゼロの要素は明示的に保持する必要がなく、非ゼロ要素の位置と値のみを保持しています[*29](c4WA.xhtml#a9ZE)。SparseMatrixCSC 型の疎行列は次の3つのフィールドを使って行列の非ゼロ要素を保持します。

colptr フィールド

各列の非ゼロ要素が rowval フィールドと nzval フィールドのどこから始まるかを指し示す配列です。定義から、このフィールドの要素は昇順に並んでいます。colptr フィールドの長さは、最後の番兵(sentinel)も含めて「行列の列数+1」です。

rowval フィールド

非ゼロ要素がある行の位置(行番号)を保持する配列です。すなわち、j 列目にある非ゼロ要素の行番号は、rowval の colptr[j] から colptr[j+1]-1 の範囲に収められています。rowval フィールドの各列に対応する部分は昇順に並んでいるので、二分探索[*30](c4WA.xhtml#a9FJ)が行えます。

nzval フィールド

非ゼロ要素の値を保持する配列です。すなわち、j 列目にある非ゼロ要素の値は、nzval の colptr[j] から colptr[j+1]-1 の範囲に収められています。rowval フィールドの要素と1対1対応します。

4行5列の疎行列の例を次に示します。

![](image_rsrcB9K.jpg)

以上説明したことを踏まえると、疎行列は非ゼロ要素が行列の大半を占めるときは、かえって非効率な構造であることがわかります。なぜなら、疎行列は非ゼロ要素の値に加え、その位置も保持しなければならないからです。また、インデックスを使って要素の更新をする操作も密行列に比べると高コストです。これは、A[i,j] = x のように特定の位置の値を更新しようとすると、colptr フィールドと rowval フィールドから二分探索などで要素の位置を検索しなければならないからです。もし A[i,j] の値がゼロだったら、非ゼロ要素を途中に挿入するために rowval フィールドと nzval フィールドの要素をスライドするコストも発生するでしょう。

疎行列のこうした構造を明示的に使ってアルゴリズムを書くには、表11.5に示す関数群が必要になることもあります。特に、nzrange 関数・rowval 関数・nonzeros 関数は、これらのフィールドを参照するインタフェースです。本書では詳しい説明を割愛しますが、必要なときには各関数のドキュメントを参照してください。

# 11.5 統計

標準ライブラリには統計に関するツールもいくつか用意されています。非常に基本的なものは Base モジュールにありますが、やや高度なものは標準ライブラリのStatistics.jlパッケージから提供されています。また、欠損値の取り扱いや標準ライブラリに含まれない外部ライブラリの統計パッケージも本節で簡単に紹介します。

# 11.5.1 統計関数

統計関数はベクトルや行列を引数にとり、その統計量(statistic)を計算する関数です。Base モジュールでは以下のような関数が提供されています。

![](image_rsrcB9M.jpg)

より発展的な統計関数は Statistics モジュールで定義されています。Statistics モジュールでは、平均・中央値・分散・標準偏差などを計算する関数が定義されています。

![](image_rsrcB9N.jpg)

[5.2.4項](c251.xhtml#a9RB)で説明した集約関数と同じように、mean 関数も第1引数に関数をとれます。第2引数にジェネレータなどのイテレータを渡せば、モンテカルロ法などが次のように手軽にできます。

![](image_rsrcB9P.jpg)

Base モジュールと Statistics モジュールから提供されている統計関数を表11.6にまとめます。表中のいくつかの関数については、以降でもう少し詳しく説明します。

![](image_rsrcB9R.jpg)

# 11.5.2 多次元配列の統計量

統計関数の多くは、ベクトル(1次元配列)より高次元の配列でもベクトルと同じように統計量を計算します。例えば、X が行列のとき、mean(X) と mean(vec(X)) は同じ値を返します。しかし、統計量を計算するとき、2次元以上の配列すべての要素が独立に観測された値であるという状況はあまりなく、行列なら行方向か列方向に標本(個体など)が並び、もう一方の方向に独立変数や従属変数が並ぶ場合がほとんどでしょう。そのようなとき、統計関数にキーワード引数を与えて計算すると、特定の次元に関する統計量が計算できるので便利です。

統計関数は、どの方向について統計量を計算するか、つまり何番目の次元の方向で走査するかを指定する dims キーワード引数をとれます。例えば、個体の体長や体重のような変数が各列に収められているデータ行列なら、(標本)平均・標準偏差といった統計量を各列について知りたいでしょうから、1番目の次元方向(行方向)に走査する dims = 1 を指定します。次の例では、mean 関数と std 関数を使って、データ行列 X の各列の平均と標準偏差を計算しています。計算結果はベクトルではなく行列になっていることに注意してください。これはブロードキャストをしやすくします。

![](image_rsrcB9S.jpg)

共分散を計算する cov 関数と相関を計算する cor 関数は、次のように行列のみを引数としてとると、デフォルトで dims = 1 のように各列の共分散と相関を計算します。したがって、各行について共分散や相関を計算するときは、dims = 2 を指定する必要があることに注意してください。

![](image_rsrcB9T.jpg)

# 11.5.3 欠損値の取り扱い

統計処理では、欠損値(missing value)を扱わなければならないことがしばしばあります。例えば、統計処理によく用いられる R には NA という欠損値を表す値がありますが、Juliaにもそれに対応する missing という値があります。これは値に特別の意味がないことを示す nothing とは異なり、「本来存在するが、何らかの技術的な要因によって取得できなかった」ことを示す値です。

欠損値を含む計算は、値が1つに決定できないと計算結果も欠損値になります。例えば、sin(missing) も missing + 1.2 も計算結果は missing です。例外的に、| と & は引数の一方が欠損値でも、欠損値でない値を返すことがあります。これは、missing | true の第1引数が false であっても true であっても、評価結果が true になるからです。missing & false も同様です。

![](image_rsrcB9U.jpg)

欠損値の扱いで注意しなければならないのは、x == missing のような計算も常に missing になることです。欠損値はどのような値にもなりうるワイルドカードですから、x == missing が true になるか false になるかは決定できず、missing を返します。ある値が欠損値であるか判断するには、ismissing という専用の関数を用います。

![](image_rsrcB9V.jpg)

同じ理由から、統計量の計算ではデータに欠損値があると結果は missing になってしまいます。統計量を計算するときに欠損値を無視して計算するには、次の例のように skipmissing 関数を使います。すると、あらかじめデータから欠損値を取り除いた場合と同じように統計量が計算できます。

![](image_rsrcB9W.jpg)

また、引数から missing でない最初の値を返す coalesce 関数も欠損値を扱うときに便利なことがあります。

![](image_rsrcB9X.jpg)

# 11.5.4 統計関係の外部パッケージ

標準ライブラリにない発展的な統計関係のツールは、外部パッケージとして提供されています。特に重要性が高いと思われるものを表11.7に示します。

![](image_rsrcB9Y.jpg)

表形式のデータをファイルに読み書きする機能は、標準ライブラリのDelimitedFiles.jlパッケージが提供しています。しかし、これは主に数値データを扱うためのパッケージなので、統計データを扱うには不十分です。外部パッケージであるCSV.jlパッケージはより高機能なパッケージであり、CSV形式の扱いはほとんどこれで十分です。CSV.jlはデータフレームを扱うDataFrames.jlパッケージとともに使われます。

Statistics.jlパッケージにない機能は、StatsBase.jlパッケージを探すと見つかることがあります。例えば、重み付きサンプリングや頻度のカウントなどの機能はStatsBase.jlが提供しています。また、カーネル密度推定はKernelDensity.jlパッケージが提供しています。Distances.jlパッケージとDistributions.jlパッケージは、それぞれ距離と確率分布に関する広範な型と関数を提供しています。

GLM.jlパッケージとMixedModels.jlを使えば、それぞれ一般化線形モデル(generalized linear model)や混合モデル(mixed-effect model)のパラメータ推定を行えます。MultivariateStats.jlパッケージは主成分分析(principal component analysis)などの多変量解析の関数を提供しています。また、仮説検定はHypothesisTests.jlパッケージで行えます。

まとめ

本章では科学技術計算に役立つ標準ライブラリの機能を紹介しました。Juliaは汎用プログラミング言語として設計されていますが、科学技術計算で役立つ機能を備えていることがわかったと思います。特に多次元配列と線形代数の機能に関しては、他言語では見られないほど標準ライブラリが充実しており、基本的な使い方であれば標準ライブラリだけでほとんど十分なことも多いでしょう。また、ブロードキャストとフュージョンはうまく使えばメモリ使用量の削減など性能の向上([18.6.3項](c7N0.xhtml#a9B8))にも役に立ちますので、多次元配列を使った科学技術計算をする人はよく理解しておいてください。

注

[*1](c4WA.xhtml#a9H8) 技術的には0次元配列もありますが、使われることはまずありません。[*2](c4WA.xhtml#aABR) 例えば、Python, JavaScript, Cではインデックスが0から始まりますが、R, MATLAB, Fortran, Luaでは1から始まります。ただし、これを変更する方法も用意されていることがあります。Juliaに関しては[11.1.6項](c4WA.xhtml#aA63)を参照してください。[*3](c4WA.xhtml#aA9S) ただし、配列はイテレータなので、要素のインデックスが必要ないときは for x in A のように書けば十分です。[*4](c4WA.xhtml#a9Z6) 例えば、配列のある領域のインデックスを走査するなど。[*5](c4WA.xhtml#aA88) <https://julialang.org/blog/2016/02/iteration/>[*6](c4WA.xhtml#a9NP) 範囲は1次元配列なので、インデックスの範囲はインデックスの配列の特殊な例とみなせますが、ここでは説明の便宜上区別しています。[*7](c4WA.xhtml#aAFJ) 厳密にいえば、コロンのみでは範囲ではなく、Colon という型のオブジェクトです。[*8](c4WA.xhtml#a9RX) v .> 0 という記法については[11.1.11項](c4WA.xhtml#a9RY)で、BitVector 型については[11.1.13項](c4WA.xhtml#a9CG)で説明します。[*9](c4WA.xhtml#aA2F) これは数学的なベクトル和に対応するメソッドです。詳しくは[11.3.2項](c4WA.xhtml#a9MY)で説明します。[*10](c4WA.xhtml#a9MX) PythonのNumPyを使っている方は、ブロードキャストの動作の違いに注意してください。NumPyでは α + x と書いても動作しますが、それはブロードキャストがデフォルトで行われるからです。一方、Juliaでは明示しない限りブロードキャストは行われません。[11.3.2項](c4WA.xhtml#a9MY)の説明も参考にしてください。[*11](c4WA.xhtml#a9TY) NumPyのブロードキャストはJuliaとは逆に右から左へ揃えます。[*12](c4WA.xhtml#a9WG) スカラーは0次元とみなされます。[*13](c4WA.xhtml#a9TP) ブロードキャストを用いた演算は特殊なコードの書き換えで broadcast 関数の呼出しに置き換えられます。[*14](c4WA.xhtml#aA84) 配列のサイズ情報なども収める必要があるので、厳密に1/8になるというわけではありません。[*15](c4WA.xhtml#a9Y2) 結果として、表面的には配列の別々の要素を参照しているにもかかわらず、データ競合([12.2.1項](c5F8.xhtml#a9VV))が発生する可能性がありますので、並列計算でビット配列を扱うときには注意してください。[*16](c4WA.xhtml#a9ZW) 疑似乱数は表面的には本物の乱数のような振る舞いをしますが、乱数生成器のアルゴリズムと状態さえ固定すれば「乱数」を決定的(deterministic)に生成するので、本物の乱数と区別が必要なときは疑似乱数と呼ばれます。[*17](c4WA.xhtml#a9HE) 特に標準正規分布(standard normal distribution)とも呼ばれます。[*18](c4WA.xhtml#a9JM) 標準偏差は分散の正の平方根です。[*19](c4WA.xhtml#aAFH) 後述するように、Julia 1.7でデフォルトの乱数生成器が変更されたので、同じシードを用いても本書と同じ乱数が生成されるとは限りません。より一般に、異なるJuliaのバージョンでは、同じシードを用いても再現性は保証されていません。[*20](c4WA.xhtml#a9NF) [11.1節](c4WA.xhtml#a983)で説明したように、Vector 型と Matrix 型は Array 型の別名です。[*21](c4WA.xhtml#a9T9) したがって、N 次対角行列のメモリ使用量は N2 ではなく N に比例します。大きな行列では特にメモリ使用量の節約につながります。[*22](c4WA.xhtml#a9XV) 二項演算の結果が被演算子の順序によって変化しない、すなわち x⊕y=y⊕x(⊕は二項演算子)が常に成り立つ演算の性質を可換といいます。[*23](c4WA.xhtml#a9K5) 数学的には行列も線形空間の元とみなせるからです。[*24](c4WA.xhtml#a9ZD) 行列では、転置と(成分ごとの)複素共役の合成です。実行列のときは転置(transpose)と同義です。[*25](c4WA.xhtml#a9KB) 収束するべき級数で定義できる関数です。このベキ級数の変数に正方行列を代入すると、関数の定義を正方行列に拡張できます。例えば、正方行列 X の指数関数 exp(X) は![](image_rsrcB9Z.jpg)と定義されます。[*26](c4WA.xhtml#aA57) 疎行列では、ランダムな位置の参照は低速なので、単純な mysparse 関数より sparse 関数の工夫された実装のほうが高速です。[*27](c4WA.xhtml#a9N1) この密度はあくまでも期待値です。例えば1万要素の疎行列の5%を乱数で埋めようとしても、必ず500個が非ゼロ要素になるわけではないということに注意してください。[*28](c4WA.xhtml#aA7F) number of non-zerosの短縮形だと思われます。[*29](c4WA.xhtml#a9KV) ただし、ゼロ要素を明示的に保持できないわけではないことに注意してください。こうしたゼロ要素も含め、疎ベクトルや疎行列が明示的に保持する要素を構造的に非ゼロ(structurally non-zero)な要素と呼ぶことがあります。[*30](c4WA.xhtml#aAAP) ソート済みの配列から特定の値を持つ要素の位置を探索するアルゴリズムです。先頭からすべての要素を走査する線形探索に比べて効率的です。

---

# ![](image_rsrcBA0.jpg)

本章では、Juliaを使った並行プログラミングを解説します。最初の12.1節では、並行プログラミングの基礎的な概念と1つのスレッドのみを使った並行プログラミングの技法を解説します。続く12.2節では、複数のスレッドを使ってタスクを並列に動かし、プログラムの処理時間を短縮する方法を紹介します。最後の12.3節では、Juliaのプロセス間通信を使って複数ノードで処理を分散する技術の基礎を解説します。

# 12.1 並行プログラミングの基礎

並行プログラミング(concurrent programming)とは、複数の処理を同時に進行するプログラミング技法です。通常の逐次実行するプログラミングでは、実行を開始した処理は終了するまで実行し続けますが、並行プログラミングでは、実行中の処理を中断して他の処理に実行を移したり、一度中断した処理の実行を再開したりします。並行プログラミングを使えば、ユーザの入力を待つ間に別の計算を行ったり、計算の高速化が可能になったりします。

本節では、まず並行プログラミングの重要な概念を紹介し、1つのスレッドのみを使って並行プログラミングを行います。本節の内容は、12.2節で解説する並列プログラミングの基礎となります。

# 12.1.1 並行・並列・分散

まずは用語の確認をしておきましょう。並行・並列・分散の3つの用語は、互いに関連しつつも異なった意味を持っています。

並行という言葉はこれら3つの用語の中で最も広い意味を持っています。プログラムやシステムが複数の処理を同時に進行するとき、それを並行(concurrent)と呼びます。最も身近な例は、現代的なOSでしょう。1つのOS上では、テキストエディタやウェブブラウザなど、さまざまなアプリケーションの処理が同時に進行しています。テキストエディタを起動しようとするとウェブブラウザが終了したり、JuliaのREPLを起動するとストリーミング再生している音楽が止まったりするということは起きません。これはつまり、1つのOSというシステムの上で複数のアプリケーション(プロセス)が同時に動作していることを意味します。仮にCPUにコア[*1](c5F8.xhtml#a9CN)が1つしかなくても並行処理は可能です。これは、OSが複数のプロセスの実行を高速に切り替えながら処理を行うからです。

並列(parallel)とは、処理が複数のコアを使って同時に進行することを指します。したがって、並列という概念は並行に含まれています。複数のコアを使う理由は、処理全体を早く終わらせるためです。すなわち、1つの処理を複数の細かい処理に分割し、複数のコアを使って処理を早く終えることが並列処理の目的です。

分散(distributed)は処理が複数の計算機を使って同時に進行することを指します。したがって、分散という概念も並行に含まれます。なお、並列と分散はほぼ同義で使われることもありますし、独立した複数の計算機を接続して行う大規模な並列処理を特に分散と呼ぶこともあります。本書では、後者の意味で分散という言葉を使うことにします。

# 12.1.2 プロセス・スレッド・コルーチン

続いて、本章で頻繁に使うプロセス・スレッド・コルーチンという3つの用語を説明します。通常のプログラミングではこれらの概念を強く意識することはありませんが、並行プログラミングでは必要不可欠な概念です。なお、ここでの説明は実際にJuliaを使う場面を想定していますので、一般的な用法より具体的です。

プロセス(process)はOS上で動くプログラムの実行単位です。例えば、julia コマンドを実行するとREPLが立ち上がりますが、このときOSがJuliaのプロセスを作成しています。このプロセスはREPLが終了すると同時に削除されます。

プロセスと同様、スレッド(thread)もOS上の概念です。1つのプロセスの中には1つ以上のスレッドがあり、各スレッドはCPUにあるコアを1つ使って実行されます[*2](c5F8.xhtml#a9RH)。複数のスレッドを使ってうまく並列処理すれば、プロセス全体の処理の高速化が期待できます。

OSには通常複数のプロセスが同時に存在します。実行可能なプロセスやスレッドがCPUのコア数以上あるとき、OSは実行するプロセスやスレッドを短い時間で切り替えながら同時に実行します。プロセスは互いに独立しており、双方が協力しない限り、2つのプロセスが互いの詳しい情報を知るすべはありません。例えば、あるJuliaのプロセスが別のプロセスにあるオブジェクトを直接読み書きすることはできません。これは、オブジェクトを格納するメモリ空間(memory space)がプロセスごとに分離されているからです。したがって、プロセス間でデータをやりとりするには、[12.3.1項](c5F8.xhtml#a9NZ)で解説するように、プロセス間通信が必要になります[*3](c5F8.xhtml#a9P0)。一方、同じプロセス内にある複数のスレッドは互いにメモリ空間を共有しており、同じオブジェクトに並行してアクセスできます。メモリ空間を共有することは、並列計算を効率的にしますが、同時に複雑にもします。

並行プログラミングを用いないプログラムでは、反復や条件分岐などはあっても、実行の流れはひとつながりです[*4](c5F8.xhtml#a9U1)。一方、並行プログラミングでは、実行中の処理を中断して別の処理を進めたり、また元の処理を再開したり、分岐して複数の処理を並行して実行するなどの機能が必要になります。言い換えれば、プログラム内で実行の流れを中断・再開・分岐する機能が必要なわけです。これを可能にするのがコルーチン(coroutine)です。プログラムの実行中、コルーチンは動的に生成・消滅し、中断・再開可能な実行の流れを生み出します。コルーチンは同じプロセスにある他のコルーチンとメモリ空間を共有していますが、スレッドと異なり(OSレベルではなく)プログラミング言語レベルの概念です。

4つのコルーチンが存在する場合における、1スレッドのみを使ったシングルスレッドプロセスと、4スレッドを使ったマルチスレッドプロセスの違いを図12.1に示します。この模式図において、Ci と Ti はそれぞれコルーチンとスレッドを表します。1つのコルーチンはプロセス内の1つのスレッドを一時的に専有して処理を実行します。スレッドが割り当てられていないコルーチンは、実行の開始待ちか、実行が一時中断されている状態です。図の左では、コルーチン C3 のみがスレッド T1 を使って実行中であり、他のコルーチンは実行されていません。一方、図の右では、4つのコルーチンすべてが実行中です。各コルーチンがどのスレッドで実行されるかは、一般に未定です。実行中のコルーチンが切り替わる仕組みは[12.1.4項](c5F8.xhtml#a9JP)で説明します。

![](image_rsrcBA1.jpg)

# 12.1.3 タスク

Juliaにおけるコルーチンの実装をタスク(task)と呼びます。明示的に並行プログラミングの機能を使わなければ、Juliaのプロセスは1つのスレッドと1つのタスクを使った単純な構図になり、タスクを意識する必要はありません[*5](c5F8.xhtml#aA4G)。一方、並行プログラミングではスレッドやタスクを意識的に扱う必要があることが多く、プログラミングは複雑になります。

各タスクには状態があり、表12.1に示す3つの関数を使ってタスクの状態を確認できます。現在実行中のタスクは、current_task 関数で取得できます。例として、次のように実行中のタスクの状態を確認してみます。

![](image_rsrcBA2.jpg)

![](image_rsrcBA3.jpg)

タスクは Task 型で表現されます。新しいタスクオブジェクトは、Task コンストラクタを呼び出すと作成されます。コンストラクタは、そのタスクで実行する処理を関数として受けとります。次の例でわかるように、作成した直後のタスクはまだ実行されていません。タスクは後述する方法で明示的に実行権限を与えられて初めて実行されます。

![](image_rsrcBA4.jpg)

タスクは @task マクロでも作成できます。@task code はコンストラクタを使った Task(() -> code) と等価です。

![](image_rsrcBA5.jpg)

# 12.1.4 マルチタスキング

複数のタスクが実行を開始し、かつ終了も失敗もしていないとき、プログラムは並行実行されている状態にあります。こうしたタスクにスレッドを割り当てて並行実行することをマルチタスキング(multitasking)と呼びます。

タスクの実行にはスレッドが必要なので、複数のタスクがあってもスレッドが1つしかなければ、それらを同時には動かせません。複数のタスクの処理を進行させるには、スレッドを使って実行するタスクをどこかで切り替える必要があります。その1つの方法が協調的(cooperative)なマルチタスキングです。協調的とは、実行するタスクがあるタスクから別のタスクに移るとき、実行中のタスクがタスクを管理するタスクスケジューラにより強制的に実行を中断させられるのではなく、自発的に中断して別のタスクに実行の権限を譲るという意味です。このような複数タスクの実行の仕方は協調的マルチタスキング(cooperative multitasking)と呼ばれます[*6](c5F8.xhtml#a9KU)。Juliaのマルチタスキングは協調的マルチタスキングです。

類比として、人が本の原稿を書くときのマルチタスキングを考えてみましょう。本を書く作業は、次のような複数のタスクから構成されます。

●内容を考えてテキストエディタに文章を打ち込むタスク

●PDFを生成するために![](image_rsrcAG6.jpg)のコンパイルをするタスク

●生成されたPDFを確認するタスク

●本やウェブブラウザを開いて調べものをするタスク

これらのタスクは1つが完全に終わってから次に移るというものではなく、中断や再開を繰り返すことがほとんどです。例えば文章を少し書いたら、書く作業を中断してPDFのコンパイルを実行し、PDFを確認して、また書く作業に戻ります。コンパイルには少し時間がかかるので、その待ち時間にちょっとTwitterを見たり机の上にあるゴミを捨てたりもします。このとき、タスクの切り替えは自発的に行うものであって、何かに強制されてタスクが切り替わるわけではありません。また、文章を書きながら調べものはできないので、常に1つのタスクのみが進行中です。

Juliaのマルチタスキングもこれと似ています。現在実行中のタスクは、特定の関数を呼び出したとき、実行を中断して別のタスクへと切り替わります。例えば、yield 関数という関数を呼び出して他のタスクに実行権限を譲ったり、入出力関数などの呼出しで待ち時間が発生したときにタスクが切り替わったりします。中断したタスクは、他のタスクが実行を中断・終了してスレッドが利用可能になると、以前中断したところの続きから実行を再開します。

非常に簡単な例でタスクの作成と切り替えを確認しましょう。次のコードは、タスク task を作成し、yield 関数を使ってメインのタスクから task に実行を切り替えています。printtaskid 関数は、実行中のタスクのID(見やすさのため objectid 関数が返すIDの下16ビットのみ)を表示します。実行中のタスクのIDが fe37 → e429 → fe37 と遷移していることに注目してください。

![](image_rsrcBA6.jpg)

実行を終了したタスクは状態が変化し、istaskdone 関数が true を返すようになります。タスクが実行した関数の戻り値は fetch 関数で取得できます[*7](c5F8.xhtml#a9B9)。タスクの実行中に例外が発生すると、istaskfailed 関数は true を返すようになり、exception プロパティにその例外が保持されます。

![](image_rsrcBA7.jpg)

なお、一度実行を終了したタスクは yield 関数に渡しても再開できません。

# 12.1.5 タスクのスケジューリング

マルチタスキングがどう動いているかについて、もう少し詳しく見てみましょう。次のコード例は、リスト12.1に示すCPUをフルに使う処理[*8](c5F8.xhtml#aAFC)を、微妙に異なる2つのタスクで実行する例です。busy タスクは tarai 関数をすぐに実行するのに対し、lazy タスクは関数を実行する前に1秒間スリープします。

![](image_rsrcBA8.jpg)

![](image_rsrcBA9.jpg)

2つのタスクの実行時間を計測した結果は、予想に反するものかもしれません。筆者の計算機では、busy が数秒かかったのに対し、lazy は一瞬で終わってしまいました。

![](image_rsrcBAA.jpg)

このような結果になる理由は、スレッドをタスクに割り当てるスケジューリングで説明できます。実は、busy タスクが tarai 関数を最後まで実行してから別のタスクに実行を移すのに対し、lazy タスクは関数を実行開始する前にタスクを中断して別のタスクに実行を移しているのです。これは、lazy タスクの sleep 関数呼出しが原因です。Juliaの協調的マルチタスキングでは、yield 関数を呼び出す以外にも、入出力やスリープなどのスレッドは使わないが時間がかかる処理を開始して待機状態になると、そのタスクを中断して他のタスクにスレッドの使用権限を譲ります。busy タスクはCPUで計算し続ける処理なので、スレッドを他のタスクに譲らず最後まで一気に処理しますが、lazy タスクのスリープはCPUを使わない処理ですから、スリープを開始した時点で処理を中断し、元のタスクにスレッドの使用権限を譲ります。lazy タスクは一瞬で終了したように見えますが、実際は肝心の tarai 関数すら呼び出していないのです[*9](c5F8.xhtml#a9RV)。

あるタスクの処理を確実に終了させるには、wait 関数を使ってそのタスクが終了するまで待ちます。例えば、次のように yield 関数でタスクの実行を開始させた後に wait 関数を呼び出してタスクの終了を待ちます。タスクの計算結果が必要なら、代わりに fetch 関数を使えば、終了を待ったうえで結果も取得できます。

![](image_rsrcBAB.jpg)

yield 関数と似た関数に schedule 関数があります。schedule 関数はタスクを引数として受けとりますが、yield 関数のようにすぐに処理をそのタスクに譲るのではなく、Juliaのタスクスケジューラにそのタスクを登録だけして現在のタスクの処理を継続します。タスクスケジューラは、現在実行中のタスクが yield 関数を実行したり、入出力やスリープなどで待機状態になったりすると、登録された実行待ちのタスクの中から1つを取り出して実行を開始または再開します。

![](image_rsrcBAC.jpg)

[12.1.3項](c5F8.xhtml#a9V1)で述べたように、Task 型のコンストラクタや @task マクロを使ってタスクを生成しただけでは実行開始されません。タスクを実行するには、yield 関数や schedule 関数で明示的にタスクスケジューラに登録する必要があります。

yield 関数を引数なしで実行すると、タスクスケジューラに登録された任意の実行待ちのタスクを取り出して実行します。次に実行すべきタスクが特に決まっていないときは、yield() とだけ実行し、タスクスケジューラに次のタスクの選択を任せるとよいでしょう。

# 12.1.6 マクロを使った非同期実行と同期

ここまででマルチタスキングの基本的な動作を説明しました。Task コンストラクタや yield 関数を直接使うマルチタスキングは細かい制御が可能ですが、低レベルのインタフェースなのでやや煩雑です。ここでは、@sync マクロと @async マクロを使ったより高レベルな同期・非同期処理の書き方を説明します。

@async マクロは式を受けとると、その式を実行する新しいタスクを作成し、タスクスケジューラに登録して、そのタスクを返します。このとき、元のタスクは作成されたタスクの終了を待たずに後続するコードを実行し始めます。したがって、@async マクロ呼出しより後のコードが実行されていても、マクロ呼出し内の処理が完了しているとは限りません。このような実行方法を非同期実行(asynchronous execution)と呼び、マクロ名の由来にもなっています。

![](image_rsrcBAD.jpg)

非同期実行した処理は、wait 関数や fetch 関数で終了を待つこともできますが、@sync マクロを使うほうが手軽です。@sync マクロは、その内側に書かれたすべての @async マクロの非同期実行した処理が終了するまで、その先には進まないようにするマクロです。このような処理を同期(synchronization)と呼びます。例えば、次のようにコードを書くと、proc1 から proc3 はそれぞれ非同期実行されますが、すべての処理が終了するまで @async マクロのブロックより先へは進みません。

![](image_rsrcBAE.jpg)

sleep 関数を使って動作を確認してみましょう。次の例では、3秒間のスリープを3回非同期実行しています。3つのタイマはほぼ同時にスタートし、ほぼ同時に終了するので、全体でも3秒ほどで終了します。これはfor文で書き換えることもできます。

![](image_rsrcBAF.jpg)

重要な注意点として、@sync マクロ呼出しは同期する @async マクロ呼出しを字句的(lexical)に包含していなければいけません。ここでいう字句的とは、プログラムを実行しなくても、ソースコードのみから判断可能という意味です。例えば、次のように関数呼出し内部で @async マクロだけを使うと、@sync マクロで同期する方法がありませんので注意してください。この制約は、@sync と @async がともにマクロ、すなわちコードの変換で実装されていることに由来します。

![](image_rsrcBAG.jpg)

同じ関数を異なる引数で非同期実行したいときは、asyncmap 関数も便利です。asyncmap 関数は map 関数と似ていますが、関数を非同期に呼び出し、すべての関数呼出しが終了するまで待ちます。例えば、複数のファイルを並行してダウンロードする処理は、次のように簡潔に書けます[*10](c5F8.xhtml#a9VH)。

![](image_rsrcBAH.jpg)

# 12.1.7 チャネルを使ったタスク間の通信

タスク間でのデータの受け渡しには、チャネル(channel)を使う方法が手軽です。チャネルを使えば、あるタスクから別のタスクへ一方向にオブジェクトを繰り返し渡す処理が簡単に記述できます。標準ライブラリのチャネルは、Channel 型のオブジェクトとして実装されています。コンストラクタには、受け渡す型 T とバッファの最大サイズ size を Channel{T}(size) の形式で指定できます。型は省略すると Any になります。

![](image_rsrcBAJ.jpg)

チャネルには put! 関数を使って値を詰め、take! 関数を使って値を取り出します。チャネルは図12.2のようなキュー(queue)構造になっていて、最初に詰めた値が最初に取り出されます。put! 関数を呼び出したタスクは、チャネルの最大サイズを超える数の値を詰めようとするとブロックします。take! 関数は put! 関数によって値が詰められるのを待って値を取り出しますので、空のチャネルに対して take! を呼び出すと、そのタスクは値が詰められるまでブロックします。チャネルから値を取り除かない fetch という関数もあります。

![](image_rsrcBAK.jpg)

チャネルを初期化するときに最大サイズを指定しない、もしくはゼロを指定するとバッファなしチャネル(unbuffered channel)というチャネルになります。バッファなしチャネルを使うと、take! 関数でチャネルのデータが取り出されるまで、put! 関数を呼び出してデータを詰めたタスクがブロックします。同様に、put! 関数でチャネルにデータが詰められるまで、take! 関数を呼び出してデータを取り出すタスクがブロックします。データを1つずつ受け渡したいときは、バッファなしチャネルで十分です。

![](image_rsrcBAM.jpg)

チャネルはデータを何度も繰り返しタスク間で受け渡すときに非常に役に立ちます。例として、データを渡し続ける生産者(producer)とデータを受け取り続ける消費者(consumer)で通信するパターン、いわゆる生産者・消費者パターンを考えます。チャネル chan に対して、生産者は次のように受け渡すデータを詰めます。close 関数で最後にチャネルを閉じていることに注意してください。

![](image_rsrcBAN.jpg)

対して、消費者はチャネルから次のようにデータを受けとります。

![](image_rsrcBAP.jpg)

チャネルは別のインタフェースも実装しています。チャネルのコンストラクタは第1引数に関数をとると、その関数を新しいタスクで実行します。この関数にはチャネルが渡されるので、生産者として使用できます。さらに、チャネルはイテレータを実装しているので、take! 関数を明示的に呼び出さずにチャネルからデータを取り出す消費者として使用できます。これらの機能を使えば、リスト12.2に示すように、生産者・消費者パターンを簡潔に記述できます。

![](image_rsrcBAR.jpg)

# 12.2 並列プログラミング

12.1節では複数のタスクを使った並行プログラミングを解説しましたが、同時に実行されるのは1スレッドのみでした。残念ながら、1スレッドだけではCPUを集中的に使う処理は高速化できません。本節では、複数のスレッドで複数のタスクを同時に実行して処理の高速化を図る並列プログラミング(parallel programming)の方法を説明します。

# 12.2.1 並列プログラミングの概要

まずは並列プログラミングの概要から説明しましょう。現代のCPUの多くは複数のコアを持っています。サーバ用のCPUはもちろん、デスクトップやラップトップ用のCPUでもコア数は増え続けています[*11](c5F8.xhtml#a9PA)。CPUにある複数のコアを1つのプログラムで有効活用するには、複数のスレッドを使うマルチスレッドプログラミング(multithread programming)の技術が必須です。

特別なことをしなければ、Juliaのプログラムは1つのスレッドのみを使って実行されます[*12](c5F8.xhtml#a9JN)。Juliaのタスク(コルーチンの実装)はスレッドの上で実行されるので、実行可能なタスクが複数あっても、スレッドが1つしかなければ、同時進行するタスクは最大でも1つまでです。したがって、スレッドを集中的に使うプログラムを高速化するには、複数のスレッドを用意して複数のタスクを同時に動かす必要があります。タスクにスレッドを割り当てる仕事はタスクスケジューラが担いますが、[12.1.4項](c5F8.xhtml#a9JP)で説明したように協調的であることに注意してください。

マルチスレッドプログラミングでは、スレッド間でメモリ空間が共有されています。これはつまり、スレッドは別のスレッドが使っているオブジェクトを自由に読み書きできるということです。これは重要な特徴ですが、シングルスレッドプログラミングでは生じない問題が発生する可能性があります。リスト12.3と次の実行結果を見てください。グローバル変数 counter を4つのスレッドから同時に合計100万回インクリメントしていますが、処理後の計算結果は100万にはなっていません。それどころか、実行するたびに異なる値になっています。

![](image_rsrcBAS.jpg)

![](image_rsrcBAT.jpg)

これは、データ競合(data race)が発生した結果です。データ競合とは、複数のスレッドが同じオブジェクトを並行して参照し、かつ少なくとも1つのスレッドがそのオブジェクトを更新しようとしている状態です[*13](c5F8.xhtml#a9CU)。上の例では、4つのスレッドすべてが同じ counter 変数を更新しようとしているので、データ競合が発生しています。コンパイラやCPUはプログラムの実行中にデータ競合が発生しないことを前提としているので、データ競合があると、そのプログラムの動作は予想できません。したがって、データ競合は常に回避すべき状態です。

データ競合の例をもう少し考えてみましょう。例えば、単純な数値を要素とする配列を互いに重ならない複数の領域に分割し、各区画を1つのスレッドのみが読み書きするようにすれば、データ競合は発生しません[*14](c5F8.xhtml#a99V)。一方、辞書では各スレッドが仮に他のスレッドと重複しないキーを操作していたとしても、新しいペアの挿入などの操作でデータ競合が発生します。これは、現在の実装において、辞書の内部にある共有されたオブジェクトでキーの数や位置などの情報を特別な工夫をせずに管理しているからです。このように、どの操作でデータ競合が発生するかは型の実装によって異なるので、判断にはドキュメントや実装を確認する必要があります。

データ競合を回避するには、スレッドが互いに干渉しないようデータを適切に分割するか、オブジェクトへの操作が他のスレッドから観測できないようにする必要があります。後者を実現するのがアトミック操作(atomic operation)です。アトミック操作とは、スレッド間で処理中の中間的な状態が互いに見えないような操作を指します。Juliaでアトミック操作を実現するには、後述するロック機構や特殊な命令を使います。

以降では、まずJuliaが提供する並列計算のインタフェースを紹介し、データ競合を回避する方法を説明します。なお、並列計算で性能を引き出す工夫は[18.8節](c7N0.xhtml#a99E)で説明します。

# 12.2.2 マルチスレッディングのインタフェース

Juliaで複数スレッドを動かすには、起動時に使用するスレッド数を指定する必要があります。つまり、julia コマンドに \--threads N オプション、もしくは -t N オプション(-tN でも可、N はスレッド数)を渡してスレッド数を指定します[*15](c5F8.xhtml#aA0D)。例えば、julia -t4 とすると、プロセス内で4個のスレッドが使えるようになります。あるいは、JULIA_NUM_THREADS 環境変数を通じてスレッド数を指定する方法もあります。

![](image_rsrcBAU.jpg)

マルチスレッディングに関わる機能は、Base の Threads モジュールで提供されています。このモジュールは Base モジュールからエクスポートされているので、単に Threads と書けば使えます。例えば、現在のJuliaプロセスが使えるスレッド数を取得するには、Threads.nthreads 関数を呼び出します。次の例で、もし nthreads 関数が1を返す場合は、前述した複数スレッドの起動方法を見直してください。

![](image_rsrcBAV.jpg)

各スレッドには、1から nthreads 関数が返す値までの連番が一意に割り振られています。現在のタスクが使っているスレッドの番号を取得するには、Threads.threadid 関数を使います。

![](image_rsrcBAW.jpg)

実際に複数のスレッドを使うコードを書いてみましょう。Threads モジュールの @threads マクロはfor文を受けとり、複数のスレッドを使ってfor文のブロックを並列に実行するマクロです。@threads マクロは、実行したすべてのタスクが終了するまでその先へは進まないので、最後に同期をする必要はありません。例えば、次のように起動したスレッド数だけ threadid 関数を呼び出すと、for文のブロックが別々のスレッドで実行されていることを確認できます[*16](c5F8.xhtml#aA96)。なお、実行される順序は不定なので、表示されるスレッド番号の順番も不定です。

![](image_rsrcBAX.jpg)

Threads モジュールにはもう1つ、@spawn という並列実行のためのマクロがあります。こちらは @async マクロ([12.1.6項](c5F8.xhtml#a9N8))のマルチスレッディング版と思えばよいでしょう。つまり、@spawn マクロに渡された式は独立したタスクとして、いずれかのスレッドで非同期実行されます。@spawn マクロの動作の概念図を図12.3に示します。図中の枝は実行中のタスクを示し、@spawn マクロを実行すると新しい枝(タスク)が分岐して、タスクが並列に実行される様子を表しています[*17](c5F8.xhtml#a9N9)。中央の taskA から @spawn で分岐した taskB が、さらに @spawn を発行して新しい taskC を生成できることにも注目してください。@spawn マクロはこのように新しい並列実行タスクを任意の場所で自由に生成できます。

![](image_rsrcBAY.jpg)

@spawn マクロはタスクを返すので、同期をするにはそのタスクの終了を wait 関数や fetch 関数で待つ方法もありますが、@sync マクロを使うのが手軽です。先ほどの例は、@spawn マクロを使って次のように書くこともできます。

![](image_rsrcBAZ.jpg)

このほか、Threads モジュールの foreach 関数[*18](c5F8.xhtml#aA7P)はチャネルからオブジェクトを取り出して並列実行する機能を提供しています。

![](image_rsrcBB0.jpg)

# 12.2.3 具体例:マンデルブロ集合描画の並列化

マルチスレッドを使ったプログラムの高速化の例として、マンデルブロ集合(Mandelbrot set)の計算を並列化してみましょう。マンデルブロ集合とは、複素数の数列 z0=0, ![](image_rsrcBB1.jpg)が収束するような複素数 c の集合です。プロットしたマンデルブロ集合は図12.4に示すように美しく複雑な形状[*19](c5F8.xhtml#aA1G)をしています。

![](image_rsrcBB2.jpg)

数列 (zk) の収束判定は難しい問題ですが、ここでは k=1000 まで |zk|![](image_rsrcAJX.jpg)2 を満たせば収束と判断することにします。その実装例をリスト12.4に示します。

![](image_rsrcBB3.jpg)

mandelbrot 関数を呼び出してマンデルブロ集合を逐次的に描画する plot_seq 関数の実装をリスト12.5に示します。引数の x と y には浮動小数点数の範囲をとります。

![](image_rsrcBB4.jpg)

この計算の並列化は、各複素数 c の計算を独立に計算できるので単純です[*20](c5F8.xhtml#aA6W)。つまり、各画素 img[i,j] の描画は互いに独立しているので、配列 img に書き込むときにデータ競合の心配をする必要はありません。plot_seq 関数にある外側のfor文を Threads モジュールの @threads マクロで並列実行するようにしたものがリスト12.6に示す plot_par1 関数[*21](c5F8.xhtml#aA6X)です。この実装では、画像を縦に分割し、それぞれの区画を並列に描画しています。

![](image_rsrcBB5.jpg)

リスト12.6の実装を複数スレッドで並列実行すると確かに高速化するのですが、実はより効率的な実装があります。それについては[18.8.2項](c7N0.xhtml#a9PX)で詳しく説明します。

# 12.2.4 ロックによる排他制御

並列プログラミングにおいて、データ競合が深刻な問題であることは[12.2.1項](c5F8.xhtml#a9VV)で説明しました。ここでは排他制御を使ってデータ競合を回避する方法を解説します。

データ競合が生じる原因は、複数のスレッドで走るタスクが同時に同じデータを読み書きしてしまうことでした[*22](c5F8.xhtml#aAD0)。そこで、タスク間であらかじめ取り決めをし、特定の操作は1つのタスクでしか同時に行わないようにします。これを排他制御(mutual exclusion)と呼びます。排他制御されたコードの領域をクリティカルセクション(critical section)と呼びます。

排他制御を実現する代表的な方法はロック(lock)を使う方法です。ロックはmutual exclusionで使われるのでmutex[*23](c5F8.xhtml#a9G8)と呼ばれることもあります。図12.5に示すように、ロックにはUnlockedとLockedの2つの状態があります。ロックに対し lock 関数を呼び出すとUnlocked→Lockedと状態変化し、unlock 関数を呼び出すとLocked→Unlockedと状態変化します。lock 関数による前者の状態変化をロックの取得と呼び、unlock 関数による後者の状態変化をロックの解放と呼びます。

![](image_rsrcBB6.jpg)

ロックの性質として本質的なのは、ロックがLocked状態にある間、lock 関数を呼び出しても関数は返らず、状態も変化しないということです。したがって、タスク A がロックを取得してLocked状態になったロックに対し、他のタスク B が lock 関数を呼び出しても、その呼出しは返らず、したがってタスク B は lock 関数の呼出しより先へは進めません。タスク B がロックを取得して先へ進むには、タスク A がロックを解放する必要があります。この性質を利用すれば、次のように lock 関数と unlock 関数の呼出しの間には同時に1つのタスクまでしか入れないようにする排他制御が実現するわけです。

![](image_rsrcBB7.jpg)

リスト12.3をロックによる排他制御で正しく書き直したコードをリスト12.7に示します。ここで使っている ReentrantLock 型は標準ライブラリにあるロックの実装です。これは次のように期待どおり動作することが確認できます。

![](image_rsrcBB8.jpg)

![](image_rsrcBB9.jpg)

ロックの取得は trylock 関数でも可能です。trylock 関数の呼出しはただちに返り、ロックが取得できたときは true を、ロックが取得できなかったときは false を返します。したがって、戻り値を見てロックが取得できたかどうかを確認する必要があります。trylock 関数を使えば、ロックが取得できなかったときはクリティカルセクションに入らない別の処理をするといった条件分岐ができます。

# 12.2.5 ロックの種類と注意点

基本的なロックの性質と使用法がわかったところで、ロックの種類と実用的な注意点を述べておきましょう。

標準ライブラリには、ReentrantLock 型と Threads.SpinLock 型の2種類のロックがあります。ReentrantLock 型のロックは図12.5に示した2状態のロックより複雑です。このロックは、ロックを取得したタスクとその回数を記録しており、ロックを取得したタスクは、一度完全に解放しない限り、何度でも繰り返しロックを取得できます。ReentrantLock 型のロックを完全に解放するには、lock 関数や trylock 関数でロックを取得した回数と同じだけ unlock 関数でロックを解放する必要があります。

![](image_rsrcBBA.jpg)

一方、Threads モジュールにある SpinLock 型のロックは図12.5に示したような素朴なロックです。同じタスクでも複数回のロック取得はできず、lock 関数の呼出しはいつまでも戻らない(関数呼出しが完了しない)ので、扱いに注意が必要です。

![](image_rsrcBBB.jpg)

SpinLock 型のロックは ReentrantLock 型のロックに比べて軽量ですが、扱いが難しい点もあります。名前が示すとおり、SpinLock 型はCPUのコアを空転させてロックを取得しようとします。したがって、あるタスクが SpinLock 型のロックを取得しようとしている間、他のタスクはそのスレッドを使えません。場合によっては、プログラム全体が進行しなくなってしまう可能性もある[*24](c5F8.xhtml#a9R7)ので、SpinLock 型を使うときには注意が必要です。一方、ReentrantLock 型のロックは、ロックを取得しようとしたタスクがブロックしてスレッドを譲るので、他のタスクは処理を進められます。多くの場合、SpinLock 型ではなく ReentrantLock 型を使うことが推奨されます。

ロックの解放の仕方にも注意が必要です。クリティカルセクションを抜けるときに確実にロックを解放しないと、どのタスクもクリティカルセクションに入れなくなり、処理を行えなくなる危険性があります。例えば、クリティカルセクションでエラーが発生して unlock 関数の呼出しまで到達できないと、ロックは取得されたまま解放されません。したがって、ロックの解放は次のようにtry文のfinallyブロックで行うべきです[*25](c5F8.xhtml#aAFD)。なお、@lock マクロでも同じことが可能です。

![](image_rsrcBBC.jpg)

いくつかの型は、メソッドを呼び出したとき、内部で排他制御を行うようになっています。例えば、IOStream 型(open 関数でファイルを開くと返されるストリームの型)や Channel 型の大半のメソッドは、マルチスレッドで並行に呼び出しても問題ありません。一方、Array 型や Dict 型などのコレクションは自動で排他制御を行わないので、ユーザが適切に排他制御を行う必要があります。

# 12.2.6 コンディションによる同期

並列に実行されるタスク間で処理のタイミングを制御したい状況があります。例えば、データを用意するタスクとそのデータを処理するタスクが並列に実行されるとき、前者が準備を終えるまで後者は処理を開始できませんから、前者から後者へ準備が完了したというイベントを通知する必要があります。ここではその方法を紹介します。

Threads モジュールの Condition 型は、ロック機構を持ち、対になる notify 関数と wait 関数を使ってタスク間のイベント通知を可能にします。このオブジェクトをコンディション(condition)と呼び、タスク間の同期に使われます[*26](c5F8.xhtml#aA4X)。タスクはコンディションに対し notify 関数を呼び出してイベントを通知し、他のタスクは wait 関数を呼び出して通知を待ちます。通知を待つタスクの数に特に制限はありません。

コンディション cond に対し、イベントを通知するタスクは次のように記述できます。ここで、変数 ready は準備の完了状態を表す真偽値です。コンディションのロックはタスク間で共有される ready の排他制御にも使われていることに注意してください。

![](image_rsrcBBD.jpg)

対して、イベントを待機するタスクは次のように記述できます。wait 関数の呼出しは通知があるまでブロックします。wait 関数を呼び出すより前に notify 関数が呼び出されて通知が流れる可能性があるので、ready 変数を確認する必要があることに注意してください。

![](image_rsrcBBE.jpg)

スレッド間で共有している変数の更新やイベントの送受信は、コンディションのロックを取得してから行う必要があります。wait(cond) は通知を受けるまでブロックし、その間 cond のロックを解放するので、他のタスクはロックを取得して処理を進められます。実例をリスト12.8に示します。実行例は次のとおりです。メッセージが出力される順序に注目してください。

![](image_rsrcBBF.jpg)

![](image_rsrcBBG.jpg)

# 12.2.7 マクロによるアトミック操作

構造体のフィールドを更新する操作、例えば x.data += 1 は、フィールドから値を読み出し、値を変換し、新しい値をフィールドに書き込むという3つの操作から構成されます。こうした複合的な操作は、操作中の中間的な状態が他のスレッドから観測される可能性があり、複数スレッドで並行して行うと予期せぬ結果を引き起こす可能性があります。さらにいえば、フィールドの読み書きでさえも1つの命令で実現できるとは限りません[*27](c5F8.xhtml#a9DC)ので、同様の問題を引き起こす可能性があります。

本項で説明するアトミック操作は、成功するか失敗するかにかかわらず、フィールド操作の中間状態が観測されず、データ競合を回避できます[*28](c5F8.xhtml#a9BM)。なお、Julia 1.7で大きなインタフェースの変更があったため、ここではJulia 1.7以降の新しいインタフェースを扱います。Julia 1.6では動作しないので注意してください。

Juliaでアトミック操作を行うには、@atomic マクロなど、一連のアトミック操作用マクロを使います。まずは例を見てみましょう。リスト12.9は並列カウンタの例(リスト12.3)をアトミック操作を使って書き換えた実装です。Counter 型の定義とカウントをインクリメントする操作の2ヶ所で @atomic というマクロを使用していることに注目してください。この実装は次のように期待どおり動作します。

![](image_rsrcBBH.jpg)

![](image_rsrcBBJ.jpg)

リスト12.9で見たように、アトミック操作を実現するには次の2つの要素が必要です。

1\. 可変な構造体型の定義でフィールドを @atomic マクロで修飾する

2\. フィールド操作を @atomic マクロなどのアトミック操作マクロ[*29](c5F8.xhtml#a9JY)で修飾する

可変な構造体型のフィールドを @atomic マクロで修飾すると、そのフィールドはアトミック操作が可能になります[*30](c5F8.xhtml#aA9R)。以降では、リスト12.10のように定義された Atomic 構造体型にあるフィールドのアトミック操作を考えます。

![](image_rsrcBBK.jpg)

アトミック操作で基本となる操作は、フィールドの値を読み出すReadとフィールドに値を書き込むWriteです。これらに加えて、ReadとWriteの間に値を変換する操作を挟んだRead-Modify-Writeも重要な操作です。例えば、x.data += 1 という操作は概念的には次のようにRead→Modify→Writeの3つの操作から構成されます。

![](image_rsrcBBM.jpg)

これらの操作をアトミックにするには、操作を @atomic マクロで修飾します。

![](image_rsrcBBN.jpg)

@atomicswap マクロもアトミックなWrite操作を可能にしますが、@atomic マクロがフィールドにセットされた新しい値を返すのに対し、@atomicswap マクロはフィールドにセットされていた古い値を返します。

![](image_rsrcBBP.jpg)

@atomicreplace マクロは @atomicswap マクロと似ていますが、条件付きで値の交換をします。すなわち、@atomicreplace x.data expected => desired は、フィールドが特定の値 expected であれば新しい値 desired に交換しますが、そうでなければフィールドの値を交換しません。この操作はCompare-And-Swap(CAS)という名前で知られており、さまざまな並行アルゴリズムの実装で使われる重要な操作です[*31](c5F8.xhtml#a9A7)。仮にこの操作を関数で実装しようとすると、次のようになります。ただし、これは動作の説明のためのコードであって、実際にはアトミック操作ではないので注意してください。

![](image_rsrcBBR.jpg)

次の例は、@atomicreplace マクロを使ってフィールドの値を 42 に設定しようとする例です。 0 => 42 という交換はフィールドの値が 0 のときは成功し、1 のときは失敗しています。戻り値はフィールドにセットされていた値と交換が成功したかどうかを示す真偽値のタプルであることにも注意してください。

![](image_rsrcBBS.jpg)

最後に、アトミック操作用マクロがとれるメモリオーダリング(memory ordering)のパラメータについても簡単に触れておきましょう。各マクロでのパラメータの渡し方はドキュメントを参照してください。なお、メモリオーダリングの詳細な説明は本書の範囲を超えるので、詳しくは並列計算の文献などを参考にしてください。

通常、コンパイラやCPUは、メモリ操作の順序を1スレッドでの動作を変えない範囲でかなり自由に書き換えます。これは性能向上に大きく寄与するのですが、マルチスレッドでは非直感的な結果が発生することがあります。そこで、アトミック操作に対して制約を課し、メモリ操作に関してある種の性質を保証するのがメモリオーダリングの目的です。Juliaのメモリオーダリングには、制約が強いほうから順に :sequentially_consistent, :acquire_release, :monotonic があります[*32](c5F8.xhtml#a9KG)。デフォルトのパラメータは最も強い、したがって安全な制約なので、通常は変更する必要はありません。ただし、制約の弱いメモリオーダリングに比べて操作のコストが高くなる傾向にあるので、保証される性質が要件を満たせば、他のパラメータを使うこともあります。

# 12.3 分散プログラミング

本節では、プロセス間通信を使った分散プログラミングの基礎について解説します。標準ライブラリであるDistributed.jlパッケージが、プロセス間通信や計算機クラスタを使った並列計算のためのツールを提供しています。

# 12.3.1 Juliaのプロセス間通信

12.1節や12.2節で扱った処理では、複数のタスクがメモリ空間を共有していました。メモリ空間を共有するということは、タスク間でオブジェクトが共有されるということです。一方、本節で扱う分散プログラミングでは、独立したプロセスがメモリ空間を共有せずに協力して処理を進めます。したがって、まずはプロセス間でオブジェクトをやりとりするための仕組みを理解する必要があります。なお、複数のノード(計算機)を用意するのは容易ではないため、ここでは1つのノードのみを使って分散処理のデモンストレーションをします。

Juliaは独自のプロトコルを使ってプロセス間通信をします。これには標準ライブラリのSerialization.jlパッケージ([13.3節](c5Y6.xhtml#a98F))で提供されている機能を使います。プロセスが別のプロセスにオブジェクトを渡すとき、Serialization.jlの serialize 関数を使ってオブジェクトをバイト列にシリアライズし、受けとり側のプロセスは deserialize 関数を使って送られてきたバイト列のデータをデシリアライズしてオブジェクトを復元します。シリアライズとデシリアライズはDistributed.jlの関数の中で暗黙的に行われているのでユーザからは見えませんが、メモリを共有しないプロセスをまたぐ処理では、このようなオブジェクトの送受信に関わるコストがあることを意識することは重要です。

Distributed.jlパッケージが提供する分散処理のモデルは、いわゆるボス・ワーカーモデル(Boss-Worker model)です。つまり、ボスであるプロセスがその他のワーカープロセスに処理内容を指示し、ワーカープロセスはそれに従って処理を行います。ボスとワーカーは同一のノードにあることも、ネットワーク接続された別のノードにあることもあります。4つのワーカーがある場合の概念図を図12.6に示します。

![](image_rsrcBBT.jpg)

同一ノードで分散処理のために複数プロセスを立ち上げるには、julia コマンドに \--procs N オプション、もしくは -p N オプション(-pN も可、N はプロセス数)でプロセス数を指定してください。例えば julia -p4 とすれば、ワーカープロセスが4つ立ち上がります。こうして起動されたJuliaのプロセスは通信のため互いに接続された状態であり、プロセス間で通信する準備ができています。このとき、ボスはREPLのプロセスです。なお、-p オプションを使って起動すると、Distributed.jlは自動的に読み込まれます。

![](image_rsrcBBU.jpg)

分散処理の各プロセスには識別番号[*33](c5F8.xhtml#aA0K)が振られ、その番号は myid 関数で取得できます。ボスの番号は 1 です。ワーカーの識別番号のリストは workers 関数で取得できます。次のように、julia コマンドを実行して立ち上がったプロセスがボスになり、その他のワーカーと接続されます。

![](image_rsrcBBV.jpg)

# 12.3.2 処理のリモート実行

分散処理を行うためには、ボスがワーカーに指示を出す方法と、計算結果のオブジェクトをプロセス間でやりとりする方法が必要です。Distributed.jlでは、remotecall 関数が前者を、Future 型オブジェクトが後者を担います。remotecall 関数は、名前が示すとおり、遠隔地(別プロセス)で関数を呼び出す関数です。例えば、ボスが remotecall(f, i, x) を実行すると、番号 i のワーカーで f(x) が実行されます。この remotecall 関数の呼出しはワーカーでの処理を待たず、指示が終わればすぐさま Future オブジェクトを返します。このオブジェクトは処理結果を後で取得するための予約券のようなものです。remotecall 関数を呼び出すと、ワーカーでの処理は非同期的に行われ、次のように remotecall 関数が返す Future オブジェクトに fetch 関数を適用すると、処理終了までブロックしてから計算結果が得られます[*34](c5F8.xhtml#a9CE)。計算結果が必要ないときは、fetch 関数の代わりに wait 関数で処理が終わるまで待つこともできます。

![](image_rsrcBBW.jpg)

remotecall 関数と fetch 関数の呼出し時には、ボスとワーカー間でプロセス間通信が発生していることに注意してください。remotecall 関数を呼び出すと、ボスは実行する関数とその引数をシリアライズしてOSを通じてワーカーに渡し、ワーカーはそれらをデシリアライズして実行します。そして、ボスが fetch 関数で計算結果を要求すると、今度は逆にワーカーが結果をシリアライズしてボスに渡し、ボスはそれをデシリアライズして結果を得ます。

この仕組みは1つのプロセス内で複数のスレッドを使う方法とは根本的に異なり、通信にはそれなりに大きいコストがかかります。受け渡すオブジェクトの数やサイズが大きくなると、それだけシリアライズや通信にかかるコストが大きくなります。

# 12.3.3 データとコードの共有

プロセス間通信が必要な分散処理では、データとコードの共有が問題になります。ここで具体例を見ながら解説しましょう。なお、簡単のため識別番号が 2 のワーカーをワーカーの代表として使いますが、他のワーカーでも同様です。

![](image_rsrcBBX.jpg)

グローバル変数のデータは、次のように引数を使って明示的に渡しても、クロージャを使って暗黙的に渡しても同じように動作します。

![](image_rsrcBBY.jpg)

後者のようにワーカーで暗黙的に使われるグローバル変数は、ワーカーにコピーされ、Main モジュールにグローバル変数として保存(定義)されます。これは便利な機能ではありますが、あまり多用すると収拾がつかなくなるので注意してください。一般的には、前者のように引数を使って明示的に渡す方法が推奨されます。

なお、ローカル変数もグローバル変数と同じようにクロージャを使って暗黙的に渡せますが、グローバル変数と異なりローカル変数はワーカーに保存されません。

![](image_rsrcBBZ.jpg)

続いてコードの共有方法を見てみましょう。処理をリモート実行するときに気を付けるべきことは、ボスで定義されたモジュール・型・関数・マクロなどのコードは、ワーカーでは使えないということです[*35](c5F8.xhtml#a9WK)。

![](image_rsrcBC0.jpg)

ワーカーでもボスと同じように関数などを使いたいときは、@everywhere マクロを使います。@everywhere マクロは渡されたコードをボスとすべてのワーカーで実行するマクロです。

![](image_rsrcBC1.jpg)

一方、ボスでusing文やimport文を実行すると、ワーカーでも自動的に読み込まれます。ただし、名前が Main モジュールに取り込まれるわけではないので、次のような違いが生じます。

![](image_rsrcBC2.jpg)

この場合も次のように @everywhere マクロを使えば解決します。

![](image_rsrcBC3.jpg)

なお、ワーカーで使うパッケージは、ワーカーの環境で読込み可能になっている必要があることに注意してください。今回のようにボスとワーカーでファイルシステムを共有している場合は問題になりませんが、別ノードで動いているときには、何らかの方法でパッケージをあらかじめワーカーの動くノードにインストールしておかなければなりません。

配列を扱うときには、標準ライブラリのSharedArrays.jlパッケージや外部パッケージのDistributedArrays.jlパッケージの提供する配列型も使えます。本書では詳しく説明しませんので、それぞれのドキュメントを参照してください。

# 12.3.4 高レベルのリモート実行インタフェース

remotecall 関数は低レベルで少し使いにくいこともあるので、より高レベルで使いやすいマクロや関数などがいくつか用意されています。ここでいくつか重要なものを紹介します。

# @spawnat マクロ

@spawnat マクロはコードをワーカーで実行するマクロです。このマクロは、第1引数にワーカーのプロセス番号を、第2引数にワーカーで実行するコードをとります。どのワーカーで実行してもよいときは、:any を第1引数に指定すると自動的にワーカーが選ばれます。

![](image_rsrcBC4.jpg)

@spawnat マクロを使うと、次のように fetch 関数をマクロ内で呼び出して、ワーカーが別のワーカーから計算結果を得る書き方もできます。Future 型のオブジェクトを通じてワーカー間でデータを直接やりとりすれば、ワーカーの結果を一度ボスに戻してから再び別のワーカーに渡す必要がないので、多段処理を分散的に行うときには効率がよいと考えられます。

![](image_rsrcBC5.jpg)

# @distributed マクロ

@distributed マクロはfor文を分散実行するマクロです。このマクロは、次のように2引数をとる関数とfor文を引数にとり、for文の結果を最初に渡した引数で集約します。

![](image_rsrcBC6.jpg)

@distributed マクロは最初の集約関数を省略すると、集約をせずfor文を非同期に分散実行します。

# pmap 関数

pmap 関数は map 関数の分散版です。つまり、第1引数の関数に第2引数のコレクションの値を1つずつ渡してワーカーで実行します。pmap 関数は分散処理の方法を調整するパラメータをいくつも持っていますので、詳しくは関数のドキュメントを参照してください。

![](image_rsrcBC7.jpg)

# 12.3.5 複数ノードでの分散処理のセットアップ

ここまで単一のノードだけを使ってプロセス間通信を説明してきましたが、JuliaがインストールされたノードにSSH(secure shell protocol)を使ってログインさえできれば、すぐに複数ノードの分散処理を始められます。とはいえ物理的に複数のノードを用意するのは手間がかかるので、ここではDockerを使ってそのシミュレーションをしてみます。Dockerの導入については、公式ドキュメント[*36](c5F8.xhtml#a9KM)を参考にしてください。

今回のセットアップでは、Juliaがインストールされたノードをいくつか用意し、SSHを通じてログインします。ログインにはパスワードが不要な公開鍵暗号を用います。以降ではDockerのコンテナ[*37](c5F8.xhtml#aAFX)をノードとして使いますが、オンプレミスのサーバやクラウドコンピューティングサービスを使う場合も基本は同じです。

まずはコンテナのもととなるイメージを用意します。以降で使用するスクリプトはサンプルコードの dist ディレクトリ内にあります。次のようにスクリプトを実行すると、SSH鍵の生成とイメージのビルドが行われます。イメージ名は julia-worker です。

![](image_rsrcBC8.jpg)

続いて、作成したイメージからワーカーのコンテナを4つ起動します。接続に必要な情報は workers.txt ファイルに保存されます。

![](image_rsrcBC9.jpg)

最後に、リスト12.11に示すスクリプトを読み込みながらREPLを起動します。addprocs 関数の各引数の説明は、関数のドキュメントを参照してください。remotecall_fetch 関数で各ワーカーにIDを問い合わせると、次のようになります。これでボスとワーカーの通信が確認できたので、ワーカーを使って分散処理ができることがわかりました。

![](image_rsrcBCA.jpg)

![](image_rsrcBCB.jpg)

今回用意したイメージは試験用なので、30分以上経過すると自動的にコンテナが削除されます。launch.jl で起動したコンテナを手動で削除するには、stop.sh スクリプトを実行してください。

ジョブキューイングシステムで分散処理をする場合はClusterManagers.jlパッケージが使えます。使い方はここで紹介した例と同様です。

まとめ

本章では並行・並列・分散プログラミングの基本を説明しました。タスクがJuliaの並行プログラミングにおいて基礎的な実行の構成要素であり、協調的マルチタスキングで実行されます。タスク間の通信には、コンディションやチャネルを使いますが、データの受け渡しにはチャネルが特に有用です。スレッドを複数用意すれば、タスクを並列実行可能です。このとき、データ競合が発生しないよう排他制御やアトミック操作が必要であることを忘れないでください。並列化の高速化に関わる実践的な注意点は[18.8節](c7N0.xhtml#a99E)で解説します。分散処理では、プロセス間通信を使ってワーカーへの処理の依頼やデータの通信を行います。ワーカーとの通信にはSSHなどが使えるので、複数ノードを使った分散処理も容易です。

注

[*1](c5F8.xhtml#a9UK) プログラムを実行する電子回路をコアと呼びます。現代のCPUはたいてい複数のコアを持っています。[*2](c5F8.xhtml#aA8E) 1つのコアで複数のスレッドを実行するsimultaneous multithreading(SMT)機能を持ったCPUも現在では一般的です。[*3](c5F8.xhtml#a9ZP) Linux上の C であれば、mmap(2) と fork(2) を使って親子プロセス間でメモリ空間を共有する方法なども考えられますが、Juliaには fork(2) にあたるものがありません。[*4](c5F8.xhtml#a9G0) ただし、OSから見れば複数のプロセスが並行実行されています。しかし、Juliaのプロセスは自分が他のプロセスと並行実行されていることを知りません。[*5](c5F8.xhtml#a9MS) ライブラリがユーザから見えない場所で複数のスレッドやタスクを使っている可能性はあります。例えば、線形代数ライブラリのBLASは複数のスレッドを使って並列計算をしますし、REPLはユーザから入力を受け付けるタスクと計算をするタスクが分離しています。しかし、そのことはユーザからはわかりません。[*6](c5F8.xhtml#aA9D) これはコルーチンに一般的な性質でもあります。また、スケジューラからタスクの中断(preemption)を受けないという意味で、ノンプリエンプティブ(non-preemptive)なマルチタスキングと呼ぶこともあります。[*7](c5F8.xhtml#aADV) 後述するように、fetch 関数はタスクの終了を待つ機能もあります。[*8](c5F8.xhtml#a9C0) 通称「たらい回し関数」です。単純な定義でありながら、再帰呼出しが大量に発生するので実行に時間がかかります。[*9](c5F8.xhtml#a9GJ) REPLで yield(lazy) を実行すると、REPLに戻ってきた直後にタスクはユーザの入力を待つ待機状態になるので、lazy タスクの実行がすぐに再開されると考えられます。[*10](c5F8.xhtml#a9TS) ファイルのダウンロードはCPUを使い切らないので、1スレッドで実行しても処理の高速化が期待できます。なお、download 関数は Base モジュールにもありますが、Julia 1.6からは非推奨なので、標準ライブラリのDownloads.jlパッケージにある download 関数を使用してください。[*11](c5F8.xhtml#a9KZ) 筆者が2010年ごろに使っていたAMD製のPhenom II X6は6コアでしたが、2020年現在使っている同社製のRyzen 3950Xは16コアを備えています。どちらも同時代のCPUとしてはハイエンド向けですが、10年でコア数は倍以上になっています。[*12](c5F8.xhtml#aAB0) Juliaが使うライブラリ(OpenBLASなど)が内部で複数のスレッドを生成して並列実行することはあります。[*13](c5F8.xhtml#aAAX) データ競合の定義とその意味するところ(どのような結果が生じるか)はプログラミング言語によって変わりますが、Juliaには明文化された仕様がないので厳密に定義されていない用語のようです。ここでは、C++などを参考に一般的な意味でデータ競合を定義しています。[*14](c5F8.xhtml#a9WF) ただし、配列の実装に依存します。Array 型の配列であればデータ競合は発生しませんが、BitArray 型の配列は内部で複数のビットを1つの整数に集約しているので、分割の仕方によってはデータ競合が発生しえます。同様に疎行列などでもデータ競合が発生します。[*15](c5F8.xhtml#a9E4) スレッド数を auto にすると、CPUのコア数から自動で設定されます。[*16](c5F8.xhtml#aAB2) 厳密にいうと、別々のスレッドで実行されることは保証されていないはずなので、表示されるスレッド番号がかぶる可能性があります。また、標準出力は内部で適切に排他制御されているので、ここではデータ競合は発生していません。[*17](c5F8.xhtml#a99N) ただし、@spawn で生まれたタスクが必ず並列実行されるわけではありません。例えば、スレッド数よりタスク数が多ければ、スレッドが割り当てられず並列実行できないタスクがあるはずです。[*18](c5F8.xhtml#aAA2) Base モジュールの foreach 関数と同名ですが、別々のモジュールで定義されている独立した関数です。[*19](c5F8.xhtml#aA2H) (x, y) を複素平面として、-2.0![](image_rsrcAJX.jpg)x![](image_rsrcAJX.jpg)0.8, -1.4![](image_rsrcAJX.jpg)y![](image_rsrcAJX.jpg)1.4 の範囲を描画しています。黒い部分が集合の要素であり、色の付いた部分は収束の速さを表します。[*20](c5F8.xhtml#aADJ) このようにタスク間に依存関係がなく、並列化が容易な問題をembarrassingly parallelと呼ぶことがあります。[*21](c5F8.xhtml#aAE5) Julia 1.6時点での動作を変えないようにするため、@threads マクロに :static を渡しています。なくても問題なく動作しますが、Julia 1.8でデフォルトの動作が :dynamic に変更されたことに注意してください。[*22](c5F8.xhtml#aAAN) ただし、すべてのタスクが読み込むだけであれば、データ競合ではありません。[*23](c5F8.xhtml#aA4D) 「ミューテクス」と発音します。[*24](c5F8.xhtml#aA4N) 例えば、すべてのスレッドでタスクがLocked状態にあるロックを取得しようと一斉に lock 関数を呼び出すと、協調的マルチタスキングではこれらのタスクがスレッドを独占したまま他のタスクにスレッドを使う機会が回ってきません。こうなると、ロックの解放もできないので、プログラム全体が進行しません。[*25](c5F8.xhtml#a9P7) 必要に応じてcatchブロックでエラー処理をすることも忘れないでください。[*26](c5F8.xhtml#a9N6) Base モジュールにも Condition 型がありますが、こちらはスレッドセーフ性が保証されていないので、マルチスレッドプログラミングでは使えません。[*27](c5F8.xhtml#a9T3) 例えば、Int128 型の整数をメモリに読み書きするには、大半のCPUで少なくとも2つ以上の命令が必要です。[*28](c5F8.xhtml#aA3W) Julia 1.7時点では配列要素のアトミック操作は用意されていませんが、将来的にはフィールドに対する操作と同様のAPIで実装されるものと思われます。[*29](c5F8.xhtml#aA49) @atomic のほかに、後述する @atomicswap と @atomicreplace があります。[*30](c5F8.xhtml#a9R1) 不変な構造体型に @atomic マクロは必要ありません。不変な構造体のフィールドは読込み専用なので、そもそもデータ競合が発生しません。[*31](c5F8.xhtml#a9CD) したがって、実用的なCPUにはCAS用の命令が用意されています。[*32](c5F8.xhtml#aAC2) それぞれC++の std::memory_order_seq_cst, std::memory_order_acq_rel, std::memory_order_relaxed に対応します。[*33](c5F8.xhtml#a9D4) OSが管理するPIDと関係ないので混同しないでください。[*34](c5F8.xhtml#aA21) 2つの関数をともに実行する remotecall_fetch という関数もあります。[*35](c5F8.xhtml#aA7U) クロージャはシリアライズされてワーカーに渡されますので使えます。また、Base にある関数も起動時に読み込まれているので使えます。[*36](c5F8.xhtml#a9UA) <https://docs.docker.com/>[*37](c5F8.xhtml#a9DB) Dockerにおけるプログラムの実行単位です。

---

# ![](image_rsrcBCC.jpg)

本章では、システムプログラミングに関わるトピックを扱います。13.1節ではファイルシステムの操作を扱います。13.2節ではデータの入出力を行うストリームを扱います。13.3節では、Juliaのオブジェクトをファイルに保存する場合や、ファイルから読み出す場合に使ういくつかの形式を紹介します。13.4節と13.5節では、それぞれ日付や時刻の扱いとロギング方法を解説します。

# 13.1 ファイルシステム

本節ではファイルシステム(file system)の操作を扱います。最初にファイルやディレクトリのパスを扱う方法を解説し、続いてファイル削除などを含むファイルシステムの操作を解説します。最後に、一時ファイルや一時ディレクトリの管理方法を紹介します。

# 13.1.1 パスの操作

ファイルやディレクトリの位置を示すパス(path)は通常の文字列で表現します。したがって、文字列操作関数はパスにも適用できますが、システムによるパスの扱いの差異、例えばWindowsとUNIX系OSの区切り文字などの違いに対応するのは面倒なので、それらを吸収するパス操作に特化した関数がいくつか用意されています。

joinpath 関数は、与えられた文字列を実行したシステムに対応した区切り文字で結合します。

![](image_rsrcBCD.jpg)

説明を簡潔にするため、以降では必要がない限りmacOSやLinuxのパス(つまりUNIXのパス)を例として使います。UNIXのパスはスラッシュ(/)を区切り文字として使います。

splitpath 関数は joinpath 関数とは反対にシステムの区切り文字で文字列を分解します。

![](image_rsrcBCE.jpg)

splitdir 関数はパスをディレクトリパスとファイル名に分割します。どちらか一方のみが必要なときは、ディレクトリパスは dirname 関数、ファイル名は basename 関数でも取得できます。

![](image_rsrcBCF.jpg)

splitext 関数はファイルパスを拡張子とそれ以外に分割します。ファイルパスに拡張子がなければ空文字が返ります。

![](image_rsrcBCG.jpg)

abspath 関数と relpath 関数は、それぞれパスを絶対パス(absolute path)と相対パス(relative path)に変換します。絶対パスとはファイルシステムの最上位から指定するパスで、現在のディレクトリに依存しません。反対に相対パスは現在のディレクトリからの相対的な位置を示すパスです。絶対パスか相対パスかの判定は isabspath 関数で行えます。

![](image_rsrcBCH.jpg)

normpath 関数は、パスにある冗長な要素(例えば // や ..)を取り除いたパスを返します。expanduser 関数は、UNIXパスでユーザのホームディレクトリを指すチルダ(~)を展開します。

![](image_rsrcBCJ.jpg)

# 13.1.2 ファイルシステムのクエリ関数

pwd 関数(print working directory)はプロセスの現在のディレクトリを取得するときに使います。UNIXのシェルが持つコマンド名と同じ名前ですが、パスを出力するのではなく文字列で返すことに注意してください。

![](image_rsrcBCK.jpg)

ディレクトリとファイルの存在は、それぞれ isdir 関数と isfile 関数で確認できます。

![](image_rsrcBCM.jpg)

ディレクトリの直下にあるファイルやディレクトリの一覧を取得するには、readdir 関数を使います。この関数と少しのコードでディレクトリを再帰的に探索する操作も書けますが、より簡単なのは walkdir 関数を使う方法です。walkdir 関数は次の3つの要素を持つタプルのイテレータを返します。

1\. 引数として与えられたディレクトリ以下にあるディレクトリへのパス root

2\. root 直下にあるディレクトリのリスト dirs

3\. root 直下にあるファイルのリスト files

言葉だけでは難しいので、リスト13.1のコードを適当なディレクトリで実行してみて動作を確認するとよいでしょう。

![](image_rsrcBCN.jpg)

ファイルのサイズや更新時刻などの情報は stat 関数から取得できます。stat 関数はファイルに関する一連の情報を持った構造体を返すので、個別の情報を得るにはそのフィールドを参照します。stat 関数が返す構造体の代表的なフィールドを次に示しますが、詳しい説明は関数のドキュメントを確認してください。また、stat 関数を直接使わずフィールドの値を返す関数もいくつか用意されています。

![](image_rsrcBCP.jpg)

ここまでに紹介したものを含め、標準ライブラリにあるクエリ関数を表13.1に示します。

![](image_rsrcBCR.jpg)

# 13.1.3 ファイルシステムの操作関数

Juliaのファイルシステム操作の関数は、Bashなどのシェルでの操作ととてもよく似ています。例えば、標準ライブラリにある touch, cp, mv, rm などの名前が付いた関数は、それぞれ一般的なシェルの同名のコマンドに対応します。こうした関数の一覧を表13.2に示します。ここでいくつかの関数を取り上げて説明します。

![](image_rsrcBCS.jpg)

別のディレクトリへ移動するには cd 関数を使います。cd 関数は移動先のディレクトリのパスを引数にとりますが、省略するとBashと同じように homedir 関数が返すディレクトリへ移動します。また、cd 関数は第1引数に関数をとると、一時的にディレクトリを移動してその関数を実行し、また元のディレクトリに戻ります。

![](image_rsrcBCT.jpg)

ディレクトリを作るには mkdir 関数か mkpath 関数を使います。両者の違いは、mkdir 関数がすでに存在するディレクトリの直下にディレクトリを1段階だけ作れるのに対し、mkpath 関数は指定されたパスまですべての段階のディレクトリを作れる点です。例えば、/path/to/dir を作ろうとするとき、mkdir 関数では /path/to ディレクトリがすでに存在しないとその下に dir ディレクトリは作れませんが、mkpath 関数なら /path/to ディレクトリも必要であれば作ります。

# 13.1.4 一時ファイルと一時ディレクトリの管理

プログラムが一時的に使うファイルやディレクトリを一時ファイル(temporary file)や一時ディレクトリ(temporary directory)と呼びます。Juliaにはこれらのファイルやディレクトリを操作する関数も用意されています。

一時ファイルを作るには mktemp 関数を使います。mktemp() のように呼び出すと、一時ファイルのパスとストリーム([13.2節](c5Y6.xhtml#a98E))を返しますが、この一時ファイルはプログラムが終了しても残ります。しかし、次のように関数を第1引数として与えれば、関数の処理を終えたときに自動で削除してくれます。

![](image_rsrcBCU.jpg)

同様のことは一時ディレクトリを作る mktempdir 関数でもできます。一時ディレクトリは一時ファイルが複数必要なときに特に便利です。一時ディレクトリを1つ作成してそこにファイルを作成すれば、一時ディレクトリを削除したときに同時にファイルも削除されるからです。一時ファイルと一時ディレクトリの操作関数の一覧を表13.3に示します。

![](image_rsrcBCV.jpg)

# 13.2 ストリーム

テキストをターミナルに表示する、ファイルを読み書きする、ネットワークを通じてデータを送受信するなどの処理には、データの入出力を管理するオブジェクトを使用します。この種のオブジェクトはストリーム(stream)と呼ばれます。ストリームの型は IO 抽象型の下位型です。本節では、ストリームを通じたデータの入出力方法を解説します。なお、[6.4.6項](c2FB.xhtml#a9EX)で説明した IOBuffer 型のオブジェクトもストリームですが、内容が重複するので本節では扱いません。

# 13.2.1 標準ストリーム

まずは最も身近なストリームから説明しましょう。標準ストリーム(standard stream)はJuliaの起動直後から使えるストリームです。その名が示すように、標準的なデフォルトのストリームとしてさまざまな場面で使用されます。実際、println 関数などは暗黙的に標準ストリームを使用します。

標準ストリームには次の3種類があります。

●stdin

標準入力(standard input)

●stdout

標準出力(standard output)

●stderr

標準エラー出力(standard error)

REPLを使用しているとき、この3つの標準ストリームはターミナルとつながっています。これを確認するため、REPLを立ち上げ、次のように打ち込んでからEnterキーを押してください。すると、ターミナルはユーザから入力を待ち受ける状態になります[*1](c5Y6.xhtml#a9X2)。

![](image_rsrcBCW.jpg)

そこに何かしらのテキストを打ち込んでEnterキーを押すと、打ち込んだテキストが標準入力を経由してJuliaに読み込まれます。

![](image_rsrcBCX.jpg)

続いて、REPLで println(stdout, line) を実行すると、line 変数に収められたテキストが標準出力を経由してターミナルに出力されます。同様に、println(stderr, line) を実行すると、同じようにテキストが標準エラー出力を経由して出力されます。

![](image_rsrcBCY.jpg)

このように、ターミナルからユーザの入力したテキストを受けとったり、ターミナルにテキストを提示したりできるのが標準ストリームの基本的な機能です。

標準出力と標準エラー出力は一見何も変わらないように見えますが、出力するデータの用途で使い分けるのが一般的です。標準出力は通常のテキストやデータを出力するときに使われます。例えば、プログラムの計算結果を出力するときに使われるのは主に標準出力のほうです。一方の標準エラー出力は、プログラムの副次的なテキストやデータを出力するときに使われます。例えば、エラーや警告メッセージ・処理の進行状況・ログデータなどは標準エラー出力に出力することが多いです。

# 13.2.2 パイプラインとリダイレクト

標準ストリームは、ターミナルのみならず、他のプロセスやファイルとつなげる使い方もできます。リスト13.2は、標準入力から受けとったテキストの各単語の最初の文字を大文字にして標準出力に出力する簡単なプログラムです。

![](image_rsrcBCZ.jpg)

シェルで cmd1 | cmd2 を実行すると、cmd1 の標準出力が cmd2 の標準入力と接続され、データの受け渡しができます。Bashなどのシェルを起動して、titlecase.jl が echo コマンドから標準入力経由でテキストを受けとる様子を確認しましょう。次の2つ目の実行例がその例です。さらに、titlecase.jl が標準出力に出力した変換後のテキストを wc コマンドが標準入力で受けとっているのが3つ目の実行例です。

![](image_rsrcBD0.jpg)

ここで使っている wc -c は、標準入力を経由して渡されたデータのバイト数を数えます[*2](c5Y6.xhtml#aA1R)。このように標準入出力を介してデータのやりとりをする仕組みはパイプライン(pipeline)と呼ばれます。

標準入出力をプログラムではなくファイルとつなげるには、< や > を使います。cmd <file は file から cmd の標準入力にデータを渡し、cmd >file は cmd の標準出力を file に書き込みます。次の例のように cmd <input >output と書くと、この2つを同時に行えます。こうした機能はリダイレクト(redirect)と呼ばれます。

![](image_rsrcBD1.jpg)

Julia内でパイプラインやリダイレクトを行う方法は[14.2節](c6A2.xhtml#a98M)で取り上げます。

# 13.2.3 ファイル

データを読み書きするときに使うファイルもストリームの一種です。最初から利用可能な標準ストリームと異なり、ファイルはファイルのパスを指定して開く操作と使用後に閉じる操作が必要です。ファイルは open 関数で開き、対になる close 関数で閉じます。次のコードで open 関数が返す file はファイルのストリームです。

![](image_rsrcBD2.jpg)

ファイルを開くとき、デフォルトでは読込み専用で開くので、開いたファイルに書き込みできません。ファイルに書き込みたいときは、書込み可能なアクセスモードで開く必要があります。アクセスモードはファイルを開くときに "r" や "w" などの特定の文字列で指定できます。例えば、次のようにアクセスモード "w" をファイルパスの後に渡すとファイルに書き込めます。

![](image_rsrcBD3.jpg)

ファイルのアクセスモードは "w" とデフォルトの "r" 以外にもあります。例えば "a" は "w" と同様ファイルに書き込めますが、すでにファイルがあったとき、"w" はその内容を破棄して上書きするのに対し、"a" は内容を保持しつつ末尾に書き込む(追記する)という違いがあります。また、"r+" のように + を付けると可能な操作が増えます。アクセスモードの一覧表を表13.4に示します。

![](image_rsrcBD4.jpg)

ファイルを読み書きするときの注意点を取り上げます。OSが管理できる開いた状態のファイルの数には限界があり、多数のファイルを開いたままにしておくとリソースを食いつぶしてしまうので、必要な処理が済んだら確実に閉じるべきです。しかし、コードが長く複雑になると、close 関数を呼び出し忘れたり、ファイルを開いた後の処理でエラーが発生して close 関数が呼び出されなくなってしまったりする可能性があります。この問題を防ぐため、open 関数には、関数を第1引数として受けとり、その関数の処理を終えたときにファイルを自動で閉じる機能が用意されています。もし関数の処理中にエラーが発生しても、この機能を使えばファイルは確実に閉じられます[*3](c5Y6.xhtml#a9C8)。使用例は次のとおりです。

![](image_rsrcBD5.jpg)

# 13.2.4 入出力関数

ストリームからデータを読むには read 関数を使います。基本的な使い方は次のとおりです。

![](image_rsrcBD6.jpg)

read 関数には read! 関数や readuntil 関数などいくつか派生関数があります。入出力関数の概要は表13.5に示します。詳しい動作はドキュメントを参照してください。

![](image_rsrcBD7.jpg)

テキストの出力には、[6.4.5項](c2FB.xhtml#a9NT)で説明した print 関数もしくは println 関数を使うのが基本です。print 関数は第1引数にストリームをとり、第2引数以降にテキストとして出力するオブジェクトをとります。ストリームを省略した場合は標準出力がデフォルトです。

![](image_rsrcBD8.jpg)

一方、バイナリデータの出力には write 関数を使います。write 関数は引数を文字列化せず、そのままストリームに書きます。例えば、英字 a はU+0061なので、整数 0x61 を write 関数で出力すると、a とターミナルに出力されます。これに対し、print 関数では10進法で 97 と出力され、show 関数ではREPLと同じように出力されます。次の例で違いを確認してください。

![](image_rsrcBD9.jpg)

# 13.3 シリアライゼーション

オブジェクトをファイルに保存したり通信のために別の形式に変換したりすることをシリアライゼーション(serialization)と呼びます。また、シリアライズされたデータを読み込むことをデシリアライゼーション(deserialization)と呼びます。本節では、標準ライブラリにあるシリアライゼーション・デシリアライゼーション機能に加え、Julia以外でも一般的に使われるファイル形式JSON, CSV, HDF5でのシリアライゼーションとデシリアライゼーションを説明します。前者はJuliaのデータを一時的に保存したりJuliaのプロセス間でデータをやりとりしたりするときに使われ、後者はJulia以外のツールとデータをやりとりするときに使われます。

# 13.3.1 Serialization.jl

Serialization.jlパッケージは標準ライブラリが提供しているシリアライゼーションのためのツールです。他のフォーマットに比べると手軽で高速ですが、独自のバイナリ形式に保存するので、他のプログラミング言語のツールでは読み書きできません。また、Juliaの異なるバージョン間でも互換性が保証されないので、長期的な保存やデータの配布にも向きません。Serialization.jlはJuliaのプロセス間通信([12.3節](c5F8.xhtml#a98B))でも使われています。

Serialization.jlパッケージは、対になる serialize 関数と deserialize 関数を提供しています。serialize 関数はストリームを第1引数にとり、シリアライズするオブジェクトを第2引数にとります。deserialize 関数はストリームからシリアライズされた順にオブジェクトを復元します。次の例では、3つのオブジェクトを buf にシリアライズし、直後に buf からそれらのオブジェクトをデシリアライズしています。

![](image_rsrcBDA.jpg)

標準ライブラリにない型であっても特別な準備をせずにシリアライズ可能ですが、デシリアライズするときはその型があらかじめ定義されていなければなりません。例えば、あるパッケージで定義されている型のオブジェクトをシリアライズしたデータをデシリアライズするときは、あらかじめ同じパッケージを読み込んでおく必要があります。

serialize 関数と deserialize 関数のペアはオブジェクトの情報をほぼ完全に保持しますが、Ptr 型のオブジェクト(ポインタ)はシリアライズとデシリアライズで情報が保存されない例外です。ポインタは主に C とのインタフェース([15.1節](c6GX.xhtml#a98R))に使われるのですが、ポインタの値はプロセス間で保存されないので、予期せぬ動作を防ぐためにNULポインタにシリアライズされます。

# 13.3.2 JSON

JSON(JavaScript Object Notation)[*4](c5Y6.xhtml#aACH)はオブジェクトをテキストでシリアライズする標準的な形式です。元々はJavaScriptで用いられてきたものですが、現在ではどのプログラミング言語でも使われます。JSONの文法についてはここでは述べませんので、詳細はIntroducing JSON[*5](c5Y6.xhtml#aACJ)などを参照してください。

現在の標準ライブラリにはJSONを扱うパッケージはありませんが、JuliaのコミュニティではJSON.jlが標準的に用いられています。JSON形式の文字列からJuliaのオブジェクトに変換(パース)するには JSON.parse 関数を用い、その逆変換は JSON.json 関数を使うのが最も手軽です。リスト13.3に使用例を示します。

![](image_rsrcBDB.jpg)

文字列でなくファイルから読むときには、JSON.parsefile 関数を使います。同様に、文字列を経由せず直接ファイルなどに書き込むときには JSON.print 関数を使います。Base の print 関数と同様に、JSON の print 関数は第1引数に出力先のストリームをとります。

Serialization.jlと異なり、JSON.jlは一般にJuliaのオブジェクトを忠実にシリアライズできません。つまり、JSON形式にシリアライズしたオブジェクトをデシリアライズすると、元のデータと異なるデータになることがあります。例えば、UInt8 型は Int 型に、Char 型は String 型に、Missing 型は Nothing 型にというように、元の型に近い型に変換されることがあります。

![](image_rsrcBDC.jpg)

ユーザ定義型をJSONに変換するには、JSON.lower 関数のメソッドを定義します。そのほか、JSON.jlはシリアライゼーションのカスタマイズ方法を用意していますので、詳しくはパッケージのドキュメントを参照してください。

# 13.3.3 CSV

CSV(comma-separated values)[*6](c5Y6.xhtml#a9DN)は表形式のデータをシリアライズするテキスト形式です。標準ライブラリにもDelimitedFiles.jlというパッケージがあり、CSVの読み書きを行えますが、機能は豊富ではありません[*7](c5Y6.xhtml#a9DP)。ここでは機能が豊富なCSV.jlという外部パッケージのほうを紹介します。

CSV.jlはよくDataFrames.jlパッケージとともに使われます。DataFrames.jlは表形式のデータ構造であるデータフレームを扱うためのパッケージであり、統計解析などのデータ処理で頻繁に使われます。例えば、x, y, z の3つの列を持つデータフレームをCSV形式のファイルにシリアライズし、それをデシリアライズするには次のようにします。

![](image_rsrcBDD.jpg)

CSV.write 関数と CSV.read 関数は、データの読み書きに関する多数のキーワード引数を設定できます。例えば、区切り文字をコンマではなくタブ文字にするには、delim ='![](image_rsrcAGC.jpg)t' を関数にキーワード引数として渡します。その他のオプションについては関数のドキュメントを参照してください。

# 13.3.4 HDF5

HDF5(hierarchical data format 5)は多次元配列をシリアライズするバイナリ形式です。主に科学技術計算で使われる大規模な配列を保持するときに使われます。JSONやCSVなどのテキスト形式は人間も読み書きできますが、計算機にとっては読み書きが遅い形式でもあります。HDF5はバイナリ形式であり、ファイルの圧縮もサポートしているので、配列の読み書きを高速かつコンパクトに行えます。さらに、HDF5は1つの多次元配列だけでなく、複数の多次元配列を1つのファイル内に階層的なファイルシステムのように保存できます。また、メタデータとして文字列など数値以外の情報も保存できます。

HDF5形式のファイルを扱うには、HDF5.jlという外部パッケージを使うのが一般的です。例えば、HDF5.jlの h5write 関数と h5read 関数を使うと、次のように簡単に複数の配列を1つのファイルに保存できます。h5write 関数と h5read 関数の第1引数に渡しているのがファイルパスで、第2引数に渡しているのがそのファイル内でのデータの位置です。

![](image_rsrcBDE.jpg)

HDF5のグループ(ファイルシステムのディレクトリに相当)やアトリビュート(メタデータ)の扱い、さらにデータ圧縮の方法などについてはHDF5.jlのドキュメントを参照してください。本書では扱いませんでしたが、h5ls などのコマンドラインツール[*8](c5Y6.xhtml#aA25)があると、HDF5ファイルの構造や情報を簡単に確認できるので便利です。

# 13.4 日付と時刻

時間(日付や時刻)に関する機能は、標準ライブラリのDates.jlパッケージから提供されています。ただし、タイムゾーンを扱うときには外部パッケージであるTimeZones.jlも必要になるので、本節であわせて紹介します。

# 13.4.1 時間の基本操作

Dates.jlパッケージのが提供する主要な型は次の3つです。これらはすべて TimeType 抽象型の下位型です。それぞれ表現する範囲と精度が異なるので、用途に応じてこれらの型を使い分けます。これらのほかにも、後に紹介する補助的な型がいくつかあります。

DateTime 型

日付(年月日)を含めた時刻を表現する型です。日常的な意味での時刻といえば、この型で表現できます。精度は1ミリ秒です。コンストラクタは年・月・日・時・分・秒・ミリ秒の7つの引数を順にとれます。

Date 型

日付を表現する型です。精度は1日です。コンストラクタは年・月・日の3つの引数を順にとれます。

Time 型

日付を含まない高精度な時刻を表現する型です。精度は1ナノ秒です。コンストラクタは時・分・秒・ミリ秒・マイクロ秒・ナノ秒の6つの引数を順にとれます。

これらのオブジェクトを作るときには、それぞれの型のコンストラクタを呼び出します。コンストラクタの最初の引数以外は省略可能であり、省略したときはそれぞれの最小値で埋められます。例えば、DateTime 型で日を省略したら1日になり、秒を省略したら0秒になるといった具合です。次の例で動作を確認してください。

![](image_rsrcBDF.jpg)

現在の時間を取得するには、now 関数と today 関数を使います。now 関数は呼び出した瞬間の時刻を DateTime 型のオブジェクトで返し、today 関数は呼び出した瞬間の日付を Date 型のオブジェクトで返します。どちらもユーザのシステム時間を基準にします。

![](image_rsrcBDG.jpg)

時間のオブジェクトから特定の値を取り出すには専用の関数を使います。例えば、月を取り出すには month 関数を使い、時を取り出すには hour 関数を使うといった具合です。表13.6に代表的な関数の一覧を示します。戻り値はいずれも Int64 型です。

![](image_rsrcBDH.jpg)

![](image_rsrcBDJ.jpg)

# 13.4.2 時間の計算

Dates モジュールには、特定の時間の長さを表現する型が用意されています。Year, Month, Week, Day, Hour, Minute, Second, Millisecond, Microsecond, Nanosecond の10つの型は、それぞれ年からナノ秒までの時間の長さを表現する型です。これらの型はすべて Period 抽象型の下位型です[*9](c5Y6.xhtml#a9SE)。これらの型を使うと、時刻などに時間の長さを足し引きする演算ができます。

![](image_rsrcBDK.jpg)

時刻どうしを引き算すると、その差分に相当する時間の長さが Millisecond 型の値で返ってきます。

![](image_rsrcBDM.jpg)

しかし、ミリ秒単位の時間の長さは直感的に把握しづらいでしょう。そこで、CompoundPeriod 型と canonicalize 関数を使うと、人にとって扱いやすい表現に変換してくれます。CompoundPeriod 型のオブジェクトは時間の長さをミリ秒に潰さず、それぞれの時間の長さを明示的に保持できます。canonicalize 関数は、CompoundPeriod 型の値を人にとってわかりやすい形式に正規化する関数です。例えば次のように時刻の差をわかりやすく表示できるので、時間の長さを出力するときに便利です。

![](image_rsrcBDN.jpg)

これらのほか、floor 関数や ceil 関数による時間の切り下げや切り上げなどをサポートしています。詳しい動作はDates.jlパッケージのドキュメントを参照してください。

# 13.4.3 時間と文字列の変換

print 関数で時刻を出力すると、2009-08-22T20:39:06 のような形式で時刻が出力されます。これはISO 8601という規格で定義されている標準的な形式ですが、時間のデータを読み書きするときには他の形式で表示したいこともあるでしょう。そこで、Juliaによる時間の表現(オブジェクト)と、文字列による時間の表現の相互変換について説明します。

Dates.format 関数は、第1引数に時間のオブジェクトをとり、第2引数に形式を指定して文字列化する関数です。例えば、2009年8月22日を "Y/m/d" という形式で文字列化すると、Y は 2009に、m は 8 に、d は 22 にそれぞれ置換され、次のような文字列になります。

![](image_rsrcBDP.jpg)

同じコード文字を連続して書くと、置換する文字列の幅を指定できます。例えば794年を Y という形式で文字列化すると 794 と3文字になりますが、YYYY という形式で文字列化すると次のように 0794 と4文字になります。また、西暦の下2桁のみを使いたい場合、大文字の Y ではなく小文字の y を形式に使うと、y の数だけ下の桁から文字列化します。例えば、2009年を yy という形式で文字列化すると、下2桁をとった 09 になります。Dates.format 関数で指定できる形式を表13.7に示します。

![](image_rsrcBDR.jpg)

![](image_rsrcBDS.jpg)

文字列化とは逆に、文字列をパースして日付や時刻のオブジェクトを作るときにも表13.7と同じ形式が使えます。Dates.format 関数と同様に、コンストラクタの第2引数に形式を記述する文字列を渡します。一度作った形式のオブジェクトを使い回すには dateformat"..." マクロを使います。関数を呼び出すたびに形式の解析をする必要がなくなるので高速です。

![](image_rsrcBDT.jpg)

# 13.4.4 時刻の変換

時刻の表現方法は1通りだけではありません。UNIX時間という西暦1970年1月1日0時0分0秒からの経過秒数で時間を表現する方法があります[*10](c5Y6.xhtml#a9XC)。UNIX時間は人間にはわかりにくい表現ですが、計算機で時刻を扱う際には広く使われています。unix2datetime 関数は、そのUNIX時間を DateTime 型のオブジェクトに変換する関数です。これとは逆に、datetime2unix 関数は、DateTime 型のオブジェクトを倍精度浮動小数点数で表現したUNIX時間に変換します。

![](image_rsrcBDU.jpg)

使用頻度は低いでしょうが、ユリウス通日(ユリウス暦紀元前4713年1月1日正午からの経過日数)との変換を行う julian2datetime 関数および datetime2julian 関数と、Rata Dieとの変換を行う rata2datetime 関数および datetime2rata 関数も用意されています。

# 13.4.5 タイムゾーン

タイムゾーン(time zone)は、同じ標準時(standard time)を使用する地球上の領域です。協定世界時(Coordinated Universal Time, UTC)からのずれ(時差)がおおむね同じになるように地球上の領域を区切り、各領域に標準時を定めたタイムゾーンが1つ割り当てられます。例えば日本を含む領域は、日本標準時(Japan Standard Time, JST)という標準時を使用するタイムゾーンに割り当てられており、UTCから9時間先に進んでいます[*11](c5Y6.xhtml#a9US)。時差は1時間単位のものが大半ですが、1時間未満の単位の時差もあります。タイムゾーンが関わる時間を適切に扱うには、そのタイムゾーンのUTCからの時差(UTCオフセット)と夏時間(daylight saving time, DST)を考慮に入れる必要があります。特に、夏時間は日本では馴染みが薄く、導入や廃止が起きる[*12](c5Y6.xhtml#a9UT)ため扱いが面倒です。

DateTime 型は地域時(local time)を表現する型であり、タイムゾーンの情報は含まれていません。そのため、Dates.jlパッケージの機能だけではタイムゾーンを適切に扱えません。タイムゾーンを扱うには外部パッケージのTimeZones.jlパッケージを使用します。TimeZones.jlはタイムゾーンのデータベース[*13](c5Y6.xhtml#a9HK)を保持しており、UTCオフセットと夏時間を適切に扱えます。ここでは、TimeZones.jlの基礎的な使い方のみ紹介しますので、詳しい使い方はパッケージのドキュメントを参照してください。

TimeZones.jlが提供する ZonedDateTime 型は、DateTime 型にタイムゾーンの情報を加えた型です。タイムゾーン自体は VariableTimeZone 型や FixedTimeZone 型などで管理されています。タイムゾーンのオブジェクトは tz"..." マクロで作ります[*14](c5Y6.xhtml#aA30)。あるいは、localzone 関数を呼び出して、システムのタイムゾーンを取得できます。

![](image_rsrcBDV.jpg)

DateTime 型のオブジェクトとタイムゾーンのオブジェクトから ZonedDateTime 型のオブジェクトが作れます。例えば、東京の時刻をタイムゾーン付きで表現するには次のようにします。DateTime 型のコンストラクタに渡せば逆変換も可能です。

![](image_rsrcBDW.jpg)

# 13.5 ロギング

ロギング(logging)は実行中のプログラムからユーザに向けてメッセージを送ったり、バックエンドで長時間動くプログラムの実行記録をとったりするときに使われる仕組みです。同様の機能は println 関数などの関数を使っても実現可能ですが、標準ライブラリが提供する機能を使えば、ログの出力制御が統一的なインタフェースで行えます。

ロギングの発展的な機能は標準ライブラリであるLogging.jlパッケージで定義されていますが、APIの一部は Base モジュールのマクロとして使えます。本節では、まずデフォルトのロギング機能を紹介し、その後ログ出力の制御方法を解説します。

# 13.5.1 ログのレベルと出力

ログはログイベント(log event)という単位で管理されます。ログイベントには、ログの内容を表すメッセージに加え、ログレベル(log level)という重要度を表す整数が関連付けられています。この値が大きいほど重要なログイベントであることを意味します。あるログイベントを無視するか出力するかは、このログレベルが影響します。

ログレベルは内部的には細かく分けられていますが、ユーザが使うのはそのうち名前が付けられた4つのレベルだけです。表13.8に示すマクロは、それぞれのレベルに対応するログイベントを送出するマクロです。これらのマクロは Base モジュールから提供されているので、Logging.jlパッケージを読み込まなくても使えます。なお、表中のログレベルは Logging.Warn のように、Logging モジュールを通して参照できます[*15](c5Y6.xhtml#a9K6)。

![](image_rsrcBDX.jpg)

ログイベントを送出するマクロは、引数としてメッセージを文字列でとります。発生したログイベントがどう処理されるかは後述するロガー(13.5.2項)に依存しますが、まずはデフォルトの動作を見てみましょう。例えば、REPLでそれぞれのマクロを呼び出すと、次のようにレベルに応じてメッセージの形式が異なることがわかるでしょう。色が表示できるターミナルなら、上の3つのメッセージがそれぞれ赤・黄・水色で表示されているはずです。@error マクロと @warn マクロで発生したログイベントは発生した場所も表示しますが、@info マクロではメッセージのみを表示し、@debug マクロでは何も表示されません。

![](image_rsrcBDY.jpg)

これらのマクロはメッセージの後に変数をとれます。例えば、@warn "..." x1 x2 と書くと、変数 x1, x2 の値がメッセージの後に出力されます。

![](image_rsrcBDZ.jpg)

ここで、表13.8に示した各ログレベルの説明を補足しましょう。エラーログは、プログラムの実行自体は継続できる回復可能な問題が発生したときに使用します。もしプログラムの実行自体が不可能になったら、エラーログを出力するのではなく、error 関数や throw 関数を使って例外を送出するのが適切です。警告ログは、確実ではないが問題が発生する、あるいは発生した可能性が高いことを発見したときに使用します。例えば、ユーザから与えられたパラメータが、推奨される範囲の外だったときなどに警告ログを出力するとよいでしょう。情報ログは、時間のかかるプログラムで現在の実行状況などをユーザに知らせるときによく使われます。デバッグログは、ユーザでなく開発者にとって有用な情報を出力するときに使います。

# 13.5.2 ログ出力の制御

ロギングはログイベントの送出とログイベントの制御という2つの独立した仕組みに分けられます。すでに説明した @warn マクロなどの呼出しはログイベントの送出を行うだけで、その後ログイベントがどう扱われるかについては関知しません。ログイベントの制御はロガー(logger)が担当します。Juliaのプロセスには大域的なロガー(グローバルロガー)が存在し、デフォルトではこのロガーがログイベントの制御を行います。

現在使われているロガーはLogging.jlの current_logger 関数で取得できます。グローバルロガーは次のように global_logger 関数で取得・設定できます。特別な操作をしなければ、現在のロガーとグローバルロガーは一致します。

![](image_rsrcBE0.jpg)

ロガーは with_logger 関数を使ってデフォルトのロガーから変更できます。with_logger は関数とロガーを受けとり、その関数の実行中は引数として与えられたロガーでログイベントの処理を行います。

![](image_rsrcBE1.jpg)

ログイベントは、複数のフィルタを通過したもののみがロガーにより制御されます。これには、コストが比較的大きいログメッセージの生成を可能な限り避けるという意図があります。具体的には、次の3つのフィルタが上から順に適用されます。

グローバルレベルのフィルタ

プロセスの大域的なログレベルのフィルタです。このフィルタはグローバル変数を使ってログイベントのレベルをチェックします。無視するログイベントのレベルは disable_logging 関数で設定します。例えば、disable_logging(Logging.Warn) を実行すると、警告以下のレベルのログイベントを無視します。

![](image_rsrcBE2.jpg)

タスクレベルのフィルタ

タスク固有に設定されたログレベルのフィルタです。タスクレベルのフィルタは、ロガーの Logging.min_enabled_level 関数が返すログレベルで設定されます。この値は with_logger 関数を呼び出したときに設定されます。

ロガーレベルのフィルタ

現在有効なロガーによるフィルタです。ロガーレベルのフィルタは、Logging.shouldlog 関数が返す値で適用されます。この関数が false を返せばそのログイベントは無視されます。詳しい動作はドキュメントを参照してください。

これらのフィルタをすべて通過したログイベントのみがロガーの handle_message 関数のメソッドに渡り、ログとして最終的に記録されます。なお、handle_message 関数はロガーが実装するメソッドなので、新しいロガーを実装しない限り、ユーザが直接使うことはありません。

# 13.5.3 ロガーの設定

Logging.jlが用意されているロガーの一覧を表13.9に示します。これらのロガーはすべて AbstractLogger 抽象型の下位型です。

![](image_rsrcBE3.jpg)

ConsoleLogger 型のロガーの出力先と最小のログレベルは、コンストラクタの第1変数と第2変数でそれぞれ設定できます。デフォルト値はそれぞれ stderr(標準エラー出力)と Logger.Infoです。例えば、ファイルに出力しつつデバッグイベントも捉えるロガーはリスト13.4のように設定できます。

![](image_rsrcBE4.jpg)

ConsoleLogger 型にはほかにも設定項目がありますので、詳しくはドキュメントを参照してください。

まとめ

本章では、システムプログラミングに関するJuliaの機能を紹介しました。ストリームを使った入出力とファイルシステムの操作は使用機会も多く、特に重要な機能です。シリアライゼーションについては、標準ライブラリのSerialization.jlパッケージを使った独自形式と、外部パッケージを使ったJSONなどの一般的な形式の取り扱いを解説しました。これらに加え、Dates.jlパッケージによる時間の取り扱いやLogging.jlパッケージによるロギングの基本も本章で紹介しました。中にはそれほど使用頻度が高くない知識も含まれるかと思いますが、簡単に確認しておけば、必要なときにはすぐに役立つでしょう。

注

[*1](c5Y6.xhtml#a9BT) [3.3節](cXU.xhtml#a96D)でもユーザからの入力を受け付けるときに使っています。[*2](c5Y6.xhtml#a9RP) wc -c は改行文字も数えるので、見た目の文字数より1文字分多いです。[*3](c5Y6.xhtml#aA8R) 実装的にはtry文のfinallyブロック([7.6.2項](c315.xhtml#aA8S))でファイルを閉じています。[*4](c5Y6.xhtml#aA3Z) 「ジェイソン」と読みます。[*5](c5Y6.xhtml#aAA3) <https://www.json.org/json-en.html>[*6](c5Y6.xhtml#aAFP) 区切り文字がコンマとは限らないので、character-separated valuesの短縮形だと考えることもあります。[*7](c5Y6.xhtml#a9KY) DelimitedFiles.jlも数値の行列などを保存するだけなら非常に手軽で便利です。[*8](c5Y6.xhtml#aADM) <https://support.hdfgroup.org/HDF5/Tutor/cmdtoolview.html>[*9](c5Y6.xhtml#aA2Z) Period 型の直下には DatePeriod 抽象型と TimePeriod 抽象型があり、Year 型から Day 型までが DatePeriod 抽象型の下位に位置し、Hour 型から Nanosecond 型までが TimePeriod 抽象型の下位に位置します。[*10](c5Y6.xhtml#a9AG) 経過時間の基準になった時刻を一般にエポック(epoch)と呼びます。[*11](c5Y6.xhtml#aA8T) 情報通信研究機構(NICT)による日本標準時の時刻表示は<https://www.nict.go.jp/JST/JST5.html>で見られます。[*12](c5Y6.xhtml#aA38) 過去の日本でも第二次世界大戦後に短期間導入されましたが、1951年に廃止されています。[*13](c5Y6.xhtml#a9M0) <https://www.iana.org/time-zones>[*14](c5Y6.xhtml#aA01) 指定できるタイムゾーンの名前は timezone_names 関数で取得できます。[*15](c5Y6.xhtml#a9DS) Julia 1.8で Logging モジュールからエクスポートされるようになりました。

---

# ![](image_rsrcBE5.jpg)

プログラミングはJuliaの中だけで完結しなければいけないものではありません。find や grep などのJulia外で動作する外部プログラム(external program)を使えば、すべてをJuliaで書くより簡潔にやりたい仕事を終えられることがあります。本章では、Juliaから外部プログラムを実行する方法を紹介します。

本章の解説では、macOSやLinuxで標準的に使えるUNIXコマンドをいくつか使用します。Windowsの場合は、Windows Subsystem for LinuxやCygwinなどを使って、UNIXコマンドが実行できる環境を整える必要があります。

# 14.1 外部プログラム実行の基本

最初に、外部プログラムを実行するためのコマンド作成方法や実行方法の基本を解説します。

# 14.1.1 単純なコマンドの作成

Juliaには、コマンドを表現する Cmd という型が用意されており、コマンド名をバッククォート(`)で囲んで作れます。こうして作られるオブジェクトをコマンドオブジェクト(command object)、あるいは単にコマンドと呼びます。シェルで使用するときと同じように引数も指定でき、全体として `cmd arg1 arg2 ...` のように記述します。次の例は ls コマンドを表現するコマンドオブジェクトの作成例です。

![](image_rsrcBE6.jpg)

文字列と同様に、$ で変数などの式をコマンドに補間できます。

![](image_rsrcBE7.jpg)

補間する値が配列やタプルのときには、それぞれの要素がコマンドに渡される引数に対応します。

![](image_rsrcBE8.jpg)

配列やタプルを2つ以上補間すると、すべての組合せ(直積集合)が補間されます。

![](image_rsrcBE9.jpg)

補間を使うときには空白の扱いに少し注意が必要です。補間される値は、空白を含む文字列であっても、必ず1つの引数として扱われます。例えば、"Program Files" という文字列を ls コマンドの引数として補間するとき、Program と Files の2引数を持つコマンド呼出しではなく、次のような引数1つのコマンド呼出しとして扱われます。

![](image_rsrcBEA.jpg)

コマンドを記述するリテラルについては、[14.3.4項](c6A2.xhtml#aA1J)でより詳しく説明をしています。

# 14.1.2 コマンドの実行

コマンドオブジェクトは、オブジェクトが作成された時点では実行されず、run 関数などの関数に渡されて初めて実行されます。run 関数は渡されたコマンドオブジェクトを新しいプロセスで実行し、コマンドが終了するまで待ち、終了したプロセスの情報を持つ Process 型のオブジェクトを返します。ls コマンドを実行する例を見てみましょう。

![](image_rsrcBEB.jpg)

コマンドの終了ステータスが0以外のときには、そのコマンドは異常終了したとみなされます。Juliaは異常終了を検知すると例外を送出します。

![](image_rsrcBEC.jpg)

# 14.1.3 出力の捕捉

run 関数でコマンドを実行すると、標準出力はそのままターミナル画面などに流れてしまい、出力を捕捉できません。コマンドの出力を受けとりたいときには、run 関数の代わりに read 関数を使います。read 関数にコマンドオブジェクトを渡すと、その出力を配列や文字列のデータとして受けとれます。コマンドオブジェクト cmd の結果を配列(バイナリデータ)として受けとりたいときは read(cmd) を実行し、文字列として受けとりたいときには read(cmd, String) を実行します。また、readlines 関数は出力を行で分割した文字列の配列を返します。

![](image_rsrcBED.jpg)

一度に出力を受けとらず1行ずつ読み込みたいときは eachline 関数が使えます。この関数は[6.4.5項](c2FB.xhtml#a9NT)で説明したファイルを読む関数と同じものですが、コマンド用のメソッドが多重ディスパッチで定義されているわけです。eachline 関数はコマンドオブジェクトを引数として受けとると、そのコマンドを実行しながら出力を1行ずつ読み出すストリートを作ります。

![](image_rsrcBEE.jpg)

# 14.1.4 シェルの機能に関する注意

プログラムの実行に関して、重要な注意を1つしておきましょう。JuliaのコマンドはBashなどのシェルを経由せずに実行されるので、シェルに実装されているグロブ・パイプライン・リダイレクトなどの機能は、シェルと同じように記述しても利用できません。例えば、次のようなコマンドを作成して実行しても、期待どおりに動作しません。

![](image_rsrcBEF.jpg)

グロブの機能はGlob.jlパッケージを使えば補えます。パイプラインとリダイレクトについては、14.2節で紹介する機能を使えば、標準ライブラリの機能で実現できます。あるいは、シェルを直接外部プログラムとして実行することもできます。例えば、Bashの bash コマンドには与えられた文字列をシェルで実行する -c オプションがあり、次のようにシェルの機能を使用できます。-c オプションに渡す引数をシングルクォートで囲っていることに注意してください。

![](image_rsrcBEG.jpg)

# 14.2 パイプライン

[14.1.4項](c6A2.xhtml#a9BC)で説明したように、コマンド間でデータを引き渡すパイプラインをシェルと同じようには記述できません。また、ファイルに入出力するリダイレクトも記述できません。本節では、それらをJuliaで実現するパイプライン(pipeline)の仕組みについて解説します。

# 14.2.1 単純なパイプラインの構築

pipeline 関数は、コマンドやファイルを接続して1つのパイプラインを構築します。pipeline(cmd1, cmd2) のように2つのコマンド cmd1 と cmd2 を接続すると、cmd1 の標準出力は cmd2 の標準入力に接続されます。つまり、cmd1 | cmd2 というシェルのパイプラインに相当するJuliaのパイプラインができあがります。また、pipeline(cmd, file) のようにコマンド cmd とファイル file を接続すると、cmd の標準出力は file に書き込まれます。これはシェルの cmd >file に相当します。同様に、pipeline(file, cmd) のように接続すると、file の内容が読み込まれて cmd の標準入力になります。これはシェルの cmd <file に相当します。これら3つのパイプラインの具体例を次に示します。

![](image_rsrcBEH.jpg)

pipeline 関数で構築したパイプラインは、コマンドと同じように、構築した時点ではまだ実行されず、run 関数などに渡されて初めて実行されます。read 関数や eachline 関数なども通常のコマンドのときと同じようにパイプラインに対しても適用できます。

入出力のファイルにはファイルパスだけでなくストリームも指定できます。例えば出力先に devnull を指定すると、コマンドの出力は表示されなくなります。

![](image_rsrcBEJ.jpg)

3つ以上のファイルやコマンドを使ってもパイプラインを構築できます。例えば、図14.1に示すような、データファイルの圧縮形式をgzip形式からzstd形式[*1](c6A2.xhtml#a9EM)へ変換するパイプラインは次のように書けます。

![](image_rsrcBEK.jpg)

![](image_rsrcBEM.jpg)

パイプラインを組み合わせて新しいパイプラインを構築することも可能です。先ほどのパイプラインを前半と後半に分ければ、次のように解凍 decode と圧縮 encode の2つのパイプラインから構築できます。

![](image_rsrcBEN.jpg)

# 14.2.2 パイプラインのオプション

pipeline 関数にはいくつかキーワード引数を指定できます。実のところ、前述した pipeline 関数の書き方は、キーワード引数を使った書き方の簡便な記法です。pipeline 関数がとれるキーワード引数のリストを次に示します。

stdin

コマンドの標準入力を指定します。次の例はシェルの cmd <input に相当します。

![](image_rsrcBEP.jpg)

stdout

コマンドの標準出力を指定します。次の例はシェルの cmd >output に相当します。

![](image_rsrcBER.jpg)

stderr

コマンドの標準エラー出力を指定します。次の例はシェルの cmd 2>output に相当します。

![](image_rsrcBES.jpg)

append

出力をファイルに追記するかを指定します。デフォルト値は false であり、追記でなく新しいファイルを作ります。次の例はシェルの cmd >>output に相当します。

![](image_rsrcBET.jpg)

キーワード引数を使ったパイプラインの構築例をリスト14.1に示します。このパイプラインは、計算機の状態を表示する top コマンドの最初の行をファイルに追記します。

![](image_rsrcBEU.jpg)

# 14.2.3 パイプラインのエラー処理

パイプラインのコマンドは、1つでも失敗すると(終了ステータスがゼロでないと)例外が送出されます。このことはあたり前に思えるかもしれませんが、Bashなどの普通のシェルとは異なる動作です。シェルでは、パイプラインの最後のコマンドの終了ステータスがパイプライン全体の終了ステータスとなるからです。

これがどのような動作の違いを生み出すか見てみましょう。例として、シェルを使ってファイルから特定のワードを含む行を grep コマンドで抜き出し、それを gzip コマンドで圧縮して別のファイルに保存するパイプライン処理を考えてみます。grep コマンドは、検索ワードにヒットする行が1行もなかったときにゼロでない終了ステータスを返しますが、それでもBashなどのシェルを使えば、次のようにパイプラインの処理は問題なく終了します。

![](image_rsrcBEV.jpg)

一方、Juliaでは grep コマンドがゼロでない終了ステータスを返すと、その時点でそれをエラーとして検知して例外を送出します。

![](image_rsrcBEW.jpg)

これを防ぐには、ignorestatus 関数を使って特定のコマンドの終了ステータスを意図的に無視させます。なお、コマンド名を間違えるなどしてプロセスすら起動できなかったときには、例外が送出されます。

![](image_rsrcBEX.jpg)

# 14.3 発展的な外部プログラム実行

14.1節では基本的なプログラムの実行を説明しましたが、本節ではより発展的なプログラムの実行方法とその詳細を説明します。なお、あまり使用頻度の高くない機能も含まれますので、どのような機能があるかのみを簡単に把握し、後は必要に応じて参照すればよいでしょう。

# 14.3.1 コマンドの環境設定

setenv 関数を使うと、コマンドが参照できる環境変数を設定できます。これはシェルの var= val cmd ... のような機能です。setenv 関数はコマンドオブジェクトの後に、次のように名前と値のペアや文字列の配列をとれます。

![](image_rsrcBEY.jpg)

setenv 関数はさらに dir というキーワード引数をとれます。この引数にディレクトリパスを設定すると、コマンドをそのディレクトリで実行します。

![](image_rsrcBEZ.jpg)

なお、これらの設定はコマンドオブジェクト単位で設定されることに注意してください。パイプライン中のコマンドすべてに環境を設定する機能ではありません。

# 14.3.2 外部プログラムの非同期実行

コマンドやパイプラインを実行する run 関数には、wait というキーワード引数があります。この引数のデフォルト値である true では、Juliaはコマンドやパイプラインが終了するまでブロックします。この動作は外部プログラムの同期実行と呼ばれます。対して、この引数の値を false に設定すると、Juliaはコマンドやパイプラインを開始した後、その終了を待たずに次の処理に進みます。このような動作は外部プログラムの非同期実行と呼ばれます。なお、コマンドやパイプラインを開始できなかったときには、同期実行でも非同期実行でもすぐに例外を送出します。

非同期実行は時間のかかる外部プログラムを並列で実行するときに便利です。例えば、10個のデータファイルに対して外部プログラムで独立した処理を行うとき、10並列で外部プログラムのコマンドを実行すれば、処理時間の短縮が期待できます。

非同期実行したコマンドやパイプラインの結果は、run 関数の返すプロセスオブジェクト[*2](c6A2.xhtml#a9NB)を通じて参照できます。プロセスオブジェクトを wait 関数に渡すと、wait 関数は実行中のプロセスが終了するまでブロックします。例えば、次のように2つの sleep コマンドを非同期実行し、2つのプロセスオブジェクトを wait 関数で待つと、2つのプロセスは図14.2に示すようにほぼ同時に実行を開始し、両方のプロセスオブジェクトが終了を告げるまでブロックします。

![](image_rsrcBF0.jpg)

![](image_rsrcBF1.jpg)

プロセスオブジェクトを success 関数に渡すと、wait 関数と同様にプロセスが終了するまでブロックし、プロセスが問題なく終了したか、すなわち終了ステータスがゼロかを返します。true コマンドと false コマンドを使って動作を確認しましょう。

![](image_rsrcBF2.jpg)

なお、wait を false にして非同期実行すると、標準出力と標準エラー出力はどこにも出力されません。これらの出力を捉えたいときは次のようにパイプラインを使ってください。

![](image_rsrcBF3.jpg)

# 14.3.3 実行中プロセスとの通信

実行を開始したプロセスとは、プロセスの標準入出力を通じて通信できます。Juliaの入出力とプロセスの標準入出力をつなげるには、open 関数を使ってコマンドやパイプラインを実行します。open 関数はコマンドやパイプラインのオブジェクトを受けとると、その実行を開始してプロセスオブジェクトを返します。そしてプロセスオブジェクトに read 関数などを適用すると、プロセスの標準出力からデータを読み込み、write 関数などを適用すると、プロセスの標準入力に書き込みます。この方法を使えば、[14.1.3項](c6A2.xhtml#aA5B)で紹介した方法より細かな制御が可能です。

引数を無制限に出力し続ける yes コマンドからデータを読む例を次に示します。open 関数で非同期実行を開始した yes コマンドのプロセスは、close 関数が呼ばれるまで readline 関数が呼ばれるたびに同じ結果を返し続けます。

![](image_rsrcBF4.jpg)

プロセスに書き込むときは "w" を open 関数の第2引数に渡します。

![](image_rsrcBF5.jpg)

コマンドやプロセスに関わる関数を表14.1に示します。詳しい動作はそれぞれの関数のドキュメントを参照してください。なお、入出力関数は表13.5に示したストリームと同様なので省略します。

![](image_rsrcBF6.jpg)

# 14.3.4 コマンドリテラルの詳細

[14.1.1項](c6A2.xhtml#aACA)ではコマンドオブジェクトを作るリテラルの説明を最小限に留めましたが、少し複雑なコマンドを記述しようとすると、クォート処理の知識が必要になります。例えば、Perlの1行処理をJuliaから外部プログラムとして実行するときには、Juliaのコマンドリテラルの中にPerlの文字列リテラルなどが入り、やや複雑になってきます。

Juliaのコマンドリテラルは、一般的なシェルの動作を踏襲しています。つまり、コマンド名とその引数は空白で分割されますが、シングルクォート(')やダブルクォート(")で囲んだ部分は1つの引数として扱われます。ペアになったクォートの内側では、空白が保持されます。コマンドをREPLで表示すれば、それぞれの引数に下線が引かれるので、どこが1つの引数として扱われているかはわかりやすいでしょう。

![](image_rsrcBF7.jpg)

ドル($)による文字列の補間は、クォート内では抑制されます。したがって、`echo'$(name)'` を実行すると、$(name) というテキストがそのまま出力されるでしょう。この動作はAwkやPerlなどの変数の前に $ を付ける言語のコードをコマンドリテラル内に含めるときに便利です。

![](image_rsrcBF8.jpg)

バッククォートの内側では、バックスラッシュ(![](image_rsrcAGC.jpg))を使ったエスケープ処理が有効です。例えば、`echo ![](image_rsrcAGC.jpg)$name` は `echo '$name'` と実質的に同じです。クォートをリテラルに含めるときは `echo ![](image_rsrcAGC.jpg)`![](image_rsrcAGC.jpg)`hi![](image_rsrcAGC.jpg)'![](image_rsrcAGC.jpg)'` のようにエスケープをしてもよいですが、```echo ``hi![](image_rsrcAGC.jpg)'![](image_rsrcAGC.jpg)'``` のように書くことも可能です。また、| や * などの一般的なシェルで機能を持つメタ文字は、クォートするかバックスラッシュでエスケープする必要があることに注意してください。

まとめ

本章ではJuliaで外部プログラムを実行する方法を解説しました。バッククォート記法でコマンドオブジェクトを作り、pipeline 関数を使ってコマンドからパイプラインを構築し、run 関数に渡して実行するのが基本ですが、追加でさまざまな関数やキーワード引数を組み合わせれば、複雑な処理にも対応可能です。Juliaの外部プログラム呼出しに関する機能は高機能ながら使いやすいので、プログラムどうしをつなげるグルー言語としてシェルスクリプト代わりに使ってみることをおすすめします。

注

[*1](c6A2.xhtml#a9F4) Zstandard(zstd)はYann Collet氏が開発した可逆圧縮形式です。[*2](c6A2.xhtml#a9UR) 正確にいえば、パイプラインのときには(プロセスオブジェクトの集合である)プロセスチェインというオブジェクトになります。

---

# ![](image_rsrcBF9.jpg)

Juliaはそれ自体で十分強力なプログラミング言語ですが、長い歴史を持つ他のプログラミング言語と比べると、ライブラリの選択肢は少ない傾向にあります。この弱点はJuliaから他言語を呼び出すツールを使って解決できます。本章では、特によく使われるC・Python・Rの3つのプログラミング言語について、Juliaと連携する方法を紹介します。

# 15.1 C との連携

C は1970年代から存在するプログラミング言語であり、これまでに数え切れないほどのプログラムがこの言語で作られてきました。Juliaは標準ライブラリこそJulia自身で書かれていますが、ランタイムの多くは C で書かれています。また、Juliaの標準ライブラリにある関数の一部は、最終的に C で書かれた処理に移譲されています。Juliaにとっても、C はなくてはならない存在です。

こうした背景から、Juliaには C と連携する仕組みが標準で備わっています。具体的には、Juliaと C の間のデータ変換・グローバル変数の参照・関数呼出しといった操作が可能です。これらを組み合わせれば、大半の用途には十分でしょう。

本節の説明は、C に関する基礎的な知識を前提とします。また、C の言語規格としてはC99 (ISO/IEC9899:1999) を採用します。必要であれば、C99の言語仕様や『C リファレンスマニュアル第5版』(株式会社エスアイビー・アクセス)などを参照してください。あまり古くないGNU Compiler Collection (GCC)[*1](c6GX.xhtml#a9D9)やClang[*2](c6GX.xhtml#a9DA)などのコンパイラが使える環境があれば、本節を読むときに役に立つでしょう。

# 15.1.1 C との連携の概要

まずはJuliaと C との連携の概要を説明しましょう。Juliaは C のコンパイル済みライブラリを通して C と連携します。C のソースコードを直接読み込んで使うわけではありません。したがって、技術的にいえば、ライブラリは必ずしも C で記述されている必要はなく、C++やFortranで書かれているライブラリが使えることもあります。以降の説明では簡単のため、C で記述されたライブラリを想定します。

Juliaが使えるのは、特に共有ライブラリ(shared library)と呼ばれる種類のライブラリです。共有ライブラリにはデータやマシンコードとともに、変数や関数を参照するための名前が収められています。C から見ればデータはグローバル変数として、マシンコードは関数として参照できます。

共有ライブラリの内容はプログラムの実行時に仮想メモリ空間にロードされて使われます。複数のプログラムがそのロードされた領域を共有することで、システム全体でメモリ使用量を削減できます。プログラムの実行時にリンク[*3](c6GX.xhtml#aA1V)を行うことから、コンパイル時にリンクを行う静的ライブラリと対比して、動的ライブラリ[*4](c6GX.xhtml#aA1W)と呼ばれることもあります。

共有ライブラリのファイル名には通常 lib という接頭辞が付きます。共有ライブラリの拡張子は、LinuxやFreeBSDでは .so、macOSでは .dylib、Windowsでは .dll とするのが標準的です。また、ファイル名の一部にバージョンを表す数字が入ることも一般的です。例えば筆者のLinuxマシンが使っているGNU C Library (glibc)[*5](c6GX.xhtml#aAF6)のファイル名は libc.so.6 です。

共有ライブラリを使ううえで、1つ注意しておかなければならないことがあります。Juliaは共有ライブラリを通して C と連携するので、プログラミング言語レベルのインタフェースよりもむしろバイナリレベルのインタフェースであるABI(application binary interface)が重要です。型のサイズや関数の呼出し規則などはABIで規定されているので、同じソースコードからコンパイルされたとしても、ABIに互換性がなければ正しく連携できません。ABIの詳細はプラットフォームによって変わるので、共有ライブラリはJuliaを実行するプラットフォーム向けにコンパイルされたものである必要があります。なお、[16.1.6項](c6XJ.xhtml#a9TK)で説明するように、各プラットフォーム向けにコンパイルされた共有ライブラリが多数用意されているので、ある程度人気のライブラリであれば、自分でコンパイルする必要はないかもしれません。以降では、現在個人向けやサーバ用途で最もメジャーだと思われるAMD64[*6](c6GX.xhtml#a9TM)上のUNIXライクなOS(例えばmacOSやLinux)を使って解説しますが、他のプラットフォームで異なる部分があれば適宜説明を加えます。

# 15.1.2 共有ライブラリの作成

共有ライブラリをコンパイルするには、いくつか特殊なコンパイラのオプションが必要です。GCCやClangであれば、-shared オプションと -fpic オプションを付けてコンパイルしてください。-shared オプションは共有ライブラリを生成するために、-fpic は位置独立コード(position-independent code, PIC)という、リンク時にコードを自由に配置できる共有ライブラリを生成するために必要です。

ごく簡単な共有ライブラリを作成し、実際にJuliaから呼び出してみましょう。詳しい解説は以降で述べますので、まずは C との連携が簡単にできることをここで確認してください。リスト15.1に示すメッセージを出力するだけのソースコードファイルを用意してください。

![](image_rsrcBFA.jpg)

続いて、GCCを呼び出す gccコマンド(Clangの場合は clang コマンド)を使って voyager.c をコンパイルします。共有ライブラリのファイル名は libvoyager.so にしましょう。先ほど説明したとおり、-shared と -fpic の2つのオプションをコンパイラに渡します。

![](image_rsrcBFB.jpg)

そして、次のようなJuliaのコードを実行してみましょう。概要はコメントに記載しましたので、いまは詳細を気にせず眺めてみてください。

![](image_rsrcBFC.jpg)

このように、C で作成した共有ライブラリは、Juliaから非常に簡単に呼び出すことができます。ここで使ったいくつかの関数と構文は、C との連携で重要な役割を果たします。以降の説明で、これらの詳細がわかるようになるでしょう。

# 15.1.3 共有ライブラリにあるシンボルの取得

15.1.2項では共有ライブラリからグローバル変数を参照する例と、関数を呼び出す例を紹介しました。それぞれに cglobal 関数と ccall 構文を使いましたが、その最初の引数はどちらも (name,library) 形式のシンボル name と共有ライブラリ library のタプルでした。Juliaではこの形式でシンボルに関連付けられたグローバル変数や関数を指定できます。name と library に使える型は Symbol 型か String 型です。ライブラリ名にはスラッシュ(/)を含む共有ライブラリファイルへの相対パスか絶対パス、もしくはプラットフォーム依存の拡張子などを除いたライブラリ名を記述します。例えば、前者は "/path/to/libxxx.so"、後者は "libxxx" のようになるでしょう。後者の場合、Juliaが該当するライブラリを検索します[*7](c6GX.xhtml#aA29)。例えば、データの圧縮・伸張を行うlibzライブラリ[*8](c6GX.xhtml#aA2A)を使うには、次のようにします。

![](image_rsrcBFD.jpg)

(name, library)の部分は次のようにローカル変数を使って動的に切り替えられないことに注意してください。

![](image_rsrcBFE.jpg)

ある共有ライブラリを何度も ccall で呼び出す場合、ライブラリ名を定数で設定するのがおすすめです。グローバル変数と関数呼出しからなる式は library に記述できます。

![](image_rsrcBFF.jpg)

Juliaが依存する一部の共有ライブラリではライブラリ名を省略できます。例えば、jl_ が接頭辞として付くグローバル変数や関数はJulia処理系が C で実装しているAPIであり、次のように呼出し可能です。

![](image_rsrcBFG.jpg)

# 15.1.4 C と互換性のある型

関数呼出しに不可欠なJuliaと C の間のデータ受け渡しについて説明しましょう。Juliaのデータを C に渡すとき、あるいは逆にJuliaが C からデータを受けとるときは、互換性のある型を通して行います。例えば、Int64 型のJuliaの値を int 型の値を受けとる C の関数に渡したいとき、図15.1に示すように、引数の値を C の int 型と互換性のあるJuliaの Cint 型に変換してから C の関数に渡します。

![](image_rsrcBFH.jpg)

Juliaの標準ライブラリは、C の値と受け渡しできる、つまり表現が互換の型を用意しています。表15.1に基本的な型の対応関係を示します。この表からわかるように、C に対応するJuliaの型は接頭辞として C が付いた一貫性のある命名規則になっています。

![](image_rsrcBFJ.jpg)

いくつかの重要な型について、より詳しい説明を加えておきましょう。配列と構造体の扱いについては、後ほど改めて説明します。

Ptr 型

Ptr 型はポインタを表現するパラメトリック型です。主にJuliaと C の間でデータのやりとりをするときに使われます。特に、C の void * 型に対応する Ptr{Cvoid} 型は汎用ポインタ(generic pointer)とも呼ばれ、しばしば構造体などの詳細を隠蔽したいデータを取り扱うときに使われます。なお、Cvoid 型は Nothing 型の別名です。

Cstring 型

Cstring 型はNUL(![](image_rsrcAGC.jpg)0)で終端位置を示した C の文字列に対応する型です。実体としては Ptr 型と同じポインタですが、C の関数に渡す前にNUL終端であることを保証します[*9](c6GX.xhtml#aA1E)。したがって、C の関数に文字列を渡すときには Ptr{Cchar} 型ではなく Cstring 型を使うべきです。Cstring 型の値を unsafe_string 関数に渡すと、ポインタが指す位置から終端位置までのバイト列をコピーして String 型の新しい文字列を返します。

Ref 型

Ref 型はJuliaのオブジェクトを参照するための抽象型です。主にJuliaのオブジェクトを C の関数に渡すときに使われます。Ptr 型と似ていますが、Ptr 型の値は C のオブジェクトを指すのに対し、Ref 型の値はJuliaのオブジェクトを指します。具体的な使用方法は[15.1.8項](c6GX.xhtml#aA0Y)と[15.1.9項](c6GX.xhtml#aA0Z)で説明します。

固定幅ビットサイズの整数

C99ではビット数が実装依存でなく常に固定ビットサイズの整数型が導入されました。stdint.h ヘッダーファイルを読み込むと使える int16_t 型や uint64_t 型などがそうです。Juliaはこれらの型に C で始まる型の別名を用意していませんが、int16_t 型には Int16 型、uint64_t 型には UInt64 型といった具合にJuliaの整数型がそのまま使えます。

列挙型

C の列挙型(enum型)に対しては、Cint 型か @enum マクロ([8.7節](c3S5.xhtml#a97H))を使って定義した型を使います。@enum マクロはデフォルトで Cint 型と互換性のある表現を使います。

固定長の配列

固定長の配列はタプルで代用できます。例えば、int[4] は NTuple{4, Cint} とすればよいでしょう。関数との受け渡しではポインタを経由するので、Ref{NTuple{4, Cint}} のようになります。

関数

Juliaの関数を関数ポインタとして使えるようにするには、@cfunction マクロを使います。本書では説明を割愛しますので、使い方はマクロのドキュメントを参照してください。

表15.1に示したJuliaの型は、ポインタ型を除けば既存の型の別名です。例えば、先ほど挙げた Cint 型は Int32 型の別名です[*10](c6GX.xhtml#a9VF)。したがって、その実体の型名を代わりに使っても意味は変わらないのですが、一部の型はプラットフォームに依存して別名の実体が変わります。例えば、Clong 型はmacOSやLinuxでは Int 型の別名ですが、WindowsではInt32 型の別名になっています。コードがプラットフォームに依存しないようにするには、接頭辞として C がついた別名を使うことが推奨されます。

# 15.1.5 グローバル変数の読み書き

共有ライブラリのグローバル変数を取得するには cglobal 関数を使います。この関数の基本的な書式は次のとおりです。なお、[ と ] は省略可能な引数を意味します。

![](image_rsrcBFK.jpg)

[15.1.3項](c6GX.xhtml#a9MT)で説明したように、cglobal 関数の第1引数にはシンボルと共有ライブラリを指定します。共有ライブラリを省略して name のみで十分なこともあります。続く第2引数 T に型を指定すると、その型へのポインタ Ptr{T} を返します。例えば、int 型のグローバル変数を参照するなら、Cint を渡せば Ptr{Cint} 型のポインタが返ります。省略すれば汎用ポインタである Ptr{Cvoid} 型のポインタが返ります。次の例ではJuliaのスレッド数を指す jl_n_threads グローバル変数へのポインタを取得しています。

![](image_rsrcBFM.jpg)

cglobal 関数が返す値はグローバル変数へのポインタですが、興味があるのは一般にポインタが指し示す先にあります。そこで、ポインタが指す値を読み書きする unsafe_load 関数と unsafe_store! 関数が用意されています。書式は次のとおりです。

![](image_rsrcBFN.jpg)

unsafe_load 関数はポインタ ptr が Ptr{T} 型なら T 型の値を返します。unsafe_store! 関数は ptr が Ptr{T} 型なら val を T 型に変換してから書き込みます。必要であれば、第3引数にオフセット pos を指定できます[*11](c6GX.xhtml#a9XG)。

これら2つの関数についてる接頭辞 unsafe_ は、これらの操作には危険が伴うという注意喚起です。一般に、ポインタが保持するアドレスは常に有効な値を指し示すとは限りません。そのプロセスに割り当てられていないアドレスや権限のないアドレスにある値を、これらの関数で読み書きしようとすると、セグメンテーション違反(segmentation fault)が発生しますし、仮に読み書きできるアドレスでもプログラムがそのことを想定していないこともあります。また、ポインタの型パラメータ T を間違えると、関数が読み書きする値のサイズも変わることがあり危険です。いずれにせよ、これらの関数を使うときは C でポインタ操作をするのと同じ注意が必要です。

# 15.1.6 関数呼出し

共有ライブラリの関数呼出しには ccall 構文を使います[*12](c6GX.xhtml#a9BE)。ccall 構文の基本的な書式は次のとおりです。

![](image_rsrcBFP.jpg)

各引数は次のように指定します。

(name, library)

cglobal 関数と同様、ccall 構文の第1引数にはシンボルと共有ライブラリを指定します。共有ライブラリを省略して name のみで十分なこともあります。詳しい説明は[15.1.3項](c6GX.xhtml#a9MT)を参照してください。

S

C の関数が返す型を指定します。例えば int 型の値を返す C の関数なら、Cint 型を指定します。C とJuliaの型の対応表は表15.1を参照してください。

(T1, T2, ...)

C の関数が受けとる引数の型をタプルで指定します。例えば、void f(char*, int) と宣言された C の関数 f なら、(Cstring, Cint) を指定します(char* はNUL終端文字列へのポインタ型を想定)。1引数の関数には (T1,)(1要素のタプル)、引数をとらない関数には ()(空のタプル)のように、常にタプルを指定します。C とJuliaの型の対応表は表15.1を参照してください。

arg1, arg2, ...

C の関数が受けとる引数を指定します。引数の数は (T1, T2, ...) の長さに一致させます。

単純な整数や浮動小数点数の受け渡しは非常に簡単です。例として、数学関数ライブラリlibmにある C の exp 関数(指数関数)を呼び出してみます。

![](image_rsrcBFR.jpg)

[15.1.4項](c6GX.xhtml#a9J6)で説明したように、Juliaから C の関数へ渡されるデータは、ccall 構文に指定した引数の型へ変換されます。したがって、実際の引数の型と ccall 構文に指定した引数の型が完全に合致している必要はありません。例えば、次のように引数として浮動小数点数ではなく Int 型の整数を渡しても、データは C の関数に渡る前に Cdouble 型へ変換されるので、問題なく呼び出せます。

![](image_rsrcBFS.jpg)

続いて文字列の受け渡しをやってみましょう。例として使うlibcの strchar(s, c) 関数はNUL終端の文字列 s から文字 c の最初の位置を検索し、その位置のポインタを返す関数[*13](c6GX.xhtml#a9TR)です。C の関数がポインタを返す場合、その型に合致する Ptr 型を ccall 構文の戻り値の型として指定します。関数の引数がNUL終端文字列へのポインタなら、いままでと同じように Cstring 型を引数に指定します。なお、ここで使っている pointer 関数は、文字列や配列の先頭位置のポインタを返します。

![](image_rsrcBFT.jpg)

C の関数を呼び出すには、ccall 構文のほかに、@ccall というマクロを使う方法もあります。依然として型指定は必要ですが、いくぶん本来の関数呼出しに似た構文になります。先の2つの関数呼出しの例は、@ccall マクロを使って次のように書き換えられます。

![](image_rsrcBFU.jpg)

# 15.1.7 オブジェクトの保護

Juliaのオブジェクトの一部を指すポインタを通してオブジェクトを操作するとき、操作中にそのオブジェクトがガベージコレクションなどで破壊されないよう保護する必要があります。例えば、オブジェクトの一部を指すポインタを C の関数に渡すとき、関数実行中はオブジェクトを保護しなければなりません。これには、GC.@preserve マクロを使います。GC.@preserve マクロを次のように使用すると、式 expr の実行中、x1 や x2 の指すオブジェクトは保護されます。

![](image_rsrcBFV.jpg)

メモリ領域を比較する C の memcmp 関数をJuliaの関数でラップする例をリスト15.2に示します。実行例は次のとおりです。

![](image_rsrcBFW.jpg)

![](image_rsrcBFX.jpg)

GC.@preserve マクロのより詳しい説明は、ドキュメントを参照してください。

# 15.1.8 配列の受け渡し

Juliaの配列を C の関数に渡すとき、通常は Ref 型を使います。例として、リスト15.3に示す sort 関数にJuliaの配列を渡してみましょう。この関数はポインタ xs と要素数 n を引数にとり、要素をソートします[*14](c6GX.xhtml#a9YC)。コードを次のようにコンパイルし、共有ライブラリを作成してください。

![](image_rsrcBFY.jpg)

![](image_rsrcBFZ.jpg)

リスト15.3の sort 関数はJuliaから次のように呼び出せます。配列にあたる引数の型が Ref{Cdouble} であることに注意してください[*15](c6GX.xhtml#a9MF)。

![](image_rsrcBG0.jpg)

# 15.1.9 構造体の受け渡し

よくできたライブラリは実装の詳細を隠しますから、C の構造体をJuliaから直接扱わなければならないことはそう多くはありません。しかし、一部のライブラリでは構造体のフィールド[*16](c6GX.xhtml#a9DZ)を直接読み書きしなければならないこともありますので、ここでやり方を紹介しましょう。

例えば、次のような C の構造体型定義があるとします。

![](image_rsrcBG1.jpg)

これと互換性のあるJuliaの構造体型は次のように定義できます。

![](image_rsrcBG2.jpg)

このとき、Juliaの定義は可変な構造体型([8.6節](c3S5.xhtml#a97G))でも構いません。Julia側で構造体のフィールドを書き換える必要があるときは、可変な構造体型にする必要があるでしょう。C とJuliaの構造体はバイナリレベルでのレイアウトが共通しているので、C の構造体型定義を正確に写しとれば、対応するJuliaの構造体型を定義できます。

構造体のフィールドがポインタ型のときは、具体的な型パラメータ T を代入した Ptr{T} を使うことに注意してください。Vector 型などの配列は直接フィールドとして使えません。例えば、

![](image_rsrcBG3.jpg)

という C の構造体型なら、Juliaで

![](image_rsrcBG4.jpg)

と定義します。

最後に、いくつか注意点と細かい追加情報を述べておきましょう。

●C とJuliaで構造体型のフィールドの順番や型を変えてはいけません。バイナリレベルの互換性はフィールドの順番と型に依存します。

●C の関数にJuliaの構造体を渡すとき、Juliaの不変な構造体でも問題ありませんが、ポインタ経由で C に渡すときには事前に Ref で参照を作る必要があります。つまり、次に示すように C の関数を呼び出す前に ref = Ref(obj) などとして参照を作り、関数を呼び出すときには obj でなく ref のほうを引数として渡します。後で ref[] とすれば、更新後のオブジェクトがとれます。

![](image_rsrcBG5.jpg)

●C の不完全型(incomplete type)やライブラリ内部でしか使われない型へのポインタ(いわゆるopaque pointer)は Ptr{Cvoid} 型などで代用できます。例えば、15.1.10項で登場する lzma_stream 構造体型には省略した lzma_internal* internal という不完全型へのポインタ型であるフィールドがありますが、これをJuliaに写しとるには internal::Ptr{Cvoid} で代用できます。

●C のunion型は公式にJuliaでサポートされていないようですが、最大サイズのフィールドに設定すれば問題ありません。

●packed属性がついた構造体型など、サイズやアラインメントが標準と異なる構造体型はJuliaの構造体型でラップできません。

# 15.1.10 具体例:XZ Utilsのラップ

ここでは、C のライブラリにある構造体型と関数をラップする具体例を示します。コード全体は長くなるので、ここでは一部を抜粋しています。全体はサンプルコードの LZMA.jl ファイルを参照してください。

XZ Utilsはデータの可逆的な圧縮伸張を行うツール[*17](c6GX.xhtml#aA8M)です。かつてはLZMAという名前で知られ、現在でもliblzmaという共有ライブラリを提供しています。xz コマンドもXZ Utilsの一部です。

XZ Utilsの機能を使うには、次の型と関数を使う必要があります。

●処理するデータを設定する lzma_stream 構造体型

●処理結果を表す lzma_ret 列挙型

●処理内容を指示する lzma_action 列挙型

●データを変換処理する lzma_code 関数

そして、これらは次のように定義されています[*18](c6GX.xhtml#a9AS)(一部省略)。

![](image_rsrcBG6.jpg)

lzma_stream 構造体型の next_in フィールドと avail_in フィールドにはそれぞれ処理するデータへのポインタとその長さを設定します。これを lzma_code 関数の第1引数に渡すと、圧縮もしくは伸張されたデータが next_out フィールドの保持するアドレスに書き込まれます。lzma_code 関数の第2引数には通常 LZMA_RUN を渡しますが、これ以上新しいデータがないときは LZMA_FINISH を渡します。変換処理がうまくいけば、lzma_code 関数は LZMA_OK(変換が終端に達したときは LZMA_STREAM_END)を返します。lzma_code 関数が処理したデータの量は、関数を呼び出す前後の avail_in フィールドと avail_out フィールドの差分で計算できます。

残念ながら、lzma_stream 構造体型の各フィールドを読み書きする関数はXZ UtilsのAPIにありません。したがって、lzma_code 関数に渡す構造体を用意するには、C の構造体のフィールドをJuliaから読み書きする必要があります。それには、次のように C の構造体型の各フィールドを正確にJuliaへ写しとります。

![](image_rsrcBG7.jpg)

ここでは型名をJulia風に変えています。なお、フィールドの名前まで同じにする必要はありませんが、同じほうが混乱が少ないでしょう。

lzma_ret 列挙型と lzma_action 列挙型は @enum マクロを使って定義します。

![](image_rsrcBG8.jpg)

最後に lzma_code 関数をラップします。この関数は第1引数に lzma_stream 構造体型へのポインタをとるので、引数の型には Ref でラップした Ref{Stream} 型を指定します。第2引数の Action 型はそのまま渡せます。関数の戻り値は Ret 型で受けとります。

![](image_rsrcBG9.jpg)

こうしてJuliaからXZ Utilsの lzma_stream 構造体型のフィールドを読み書きし、lzma_code 関数に渡して圧縮・伸張することができるようになりました。これらの型や関数の使用例は、サンプルコードの xz.jl ファイルを参照してください。

# 15.2 Pythonとの連携

Pythonは1980年代の終わりにGuido van Rossum氏によって設計され、1991年にリリースされた[*19](c6GX.xhtml#a9HC)汎用的なプログラミング言語です。NumPyやSciPyなどの数値計算ライブラリが早い段階から発達してきたことから科学技術計算分野での人気も高く、データ解析や機械学習なども含めると、非常に多くのソフトウェア資産があります。

本節では、JuliaからPythonを利用する方法を紹介します。Pythonにはいくつかの実装が存在しますが、ここではオリジナルの実装であるCPythonを使用します。JuliaはCPythonの共有ライブラリであるlibpythonを通してPythonと連携できます。libpythonは共有ライブラリなので C との連携と同じようにも扱えますが、PythonのC API[*20](c6GX.xhtml#a9H5)は低レベルなので、使い勝手はそれほどよくありません。

そこで、本節ではPyCall.jlパッケージを使用します。PyCall.jlはJuliaからPythonへの高レベルなAPIを提供するパッケージです。このパッケージを使えば、細かいことを気にせず、Juliaの延長のような気持ちでPythonを利用できます[*21](c6GX.xhtml#a9ZB)。

# 15.2.1 PyCall.jlのインストール

まずはPyCall.jlをインストールします。特にこだわりがなければ、通常のパッケージと同じように add PyCall をREPLのパッケージ管理モードで実行するだけです。macOSとWindows環境では、PyCall.jlのインストールに合わせてPythonもインストールされます。

Pythonがインストール済みで、そのPythonをJuliaからも使いたい場合は PYTHON 環境変数に目的の python コマンドへのパスを設定してからインストールします。Bashであれば、次のようにすればよいでしょう。

![](image_rsrcBGA.jpg)

PyCall.jlで使用中のPython環境は次のように確認できます。なお、本書の執筆にあたってはLinuxのPython 3.8.5とPyCall.jl 1.92.3を使用しました。

![](image_rsrcBGB.jpg)

普段使っているPython環境をPyCall.jlでも使うようにすれば、pipなどのツールでインストールしたPythonパッケージをJuliaから使えます。以降のコード例では using PyCall は冗長なので省略します。

# 15.2.2 Python関数の呼出し

まずはPythonの関数をJuliaから呼び出す方法を解説します。PyCall.jlでは、Pythonのオブジェクトを PyObject というJuliaの型でラップしています。Pythonでは関数も他のオブジェクトと扱いは変わらないので、PyObject 型のオブジェクトとして取り扱われます。このオブジェクトはJuliaの関数と同じ構文で呼び出せます。

Pythonのビルトイン関数である divmod 関数を取得し、それを呼び出してみましょう。ビルトインオブジェクトは pybuiltin 関数で取得できます。pybuiltin 関数は関数名をシンボルか文字列で受けとります。

![](image_rsrcBGC.jpg)

キーワード引数もそのまま使えます。Pythonのビルトインクラスである int クラスの例を見てみましょう。キーワード引数 base には、引数の文字列が何進法かを指定できます。

![](image_rsrcBGD.jpg)

このように、Pythonの関数はJuliaの関数と同じように呼び出せます。Pythonの関数を呼び出しているのにJuliaのオブジェクトを引数として渡せるのが不思議に見えるかもしれません。これは、PyCall.jlがJuliaのオブジェクトをPythonの対応するオブジェクトへ内部で変換しているからです。同様に、Python関数の戻り値も自動的にJuliaオブジェクトに変換されます。

PyCall.jlが自動で変換する型は、真偽値・数値・文字列・タプル・配列・辞書など基本的なものに限られます。Pythonから自動変換しないオブジェクトが返ってきたときには、PyObject 型でラップされたPythonオブジェクトがそのまま返ります。すでに見た divmod 関数や int クラスなどがまさにその例で、こうしたPython特有の型は PyObject 型でラップしたまま扱うことになります。例えば、Pythonの set クラスのオブジェクトはJuliaの Set 型には変換されず、PyObject 型でラップされただけのものが返ります。PyObject 型のオブジェクトはそのままPython関数に引数として渡せます。

![](image_rsrcBGE.jpg)

型の変換をせず、PyObject 型のオブジェクトとして戻り値がほしいときには pycall 関数かそのマクロ版である @pycall マクロを使いましょう。pycall 関数は第1引数に関数オブジェクト、第3引数以降に関数オブジェクトに渡す引数をとり、第2引数に PyObject を指定すれば変換をせず常に PyObject 型のオブジェクトを返します。同じことは @pycall マクロなら通常の関数呼出しにより近い構文でできます。

![](image_rsrcBGF.jpg)

PythonのドキュメントもJuliaのドキュメントと同じように読めます。先のように divmod 関数を取得した後、REPLのヘルプモードでJuliaの関数と同じようにヘルプを参照してみてください。

![](image_rsrcBGG.jpg)

# 15.2.3 Pythonモジュールの利用

Pythonのビルトイン関数だけではあまり多くのことはできないので、今度はPythonのモジュールを読み込んで使ってみましょう。pyimport 関数はPythonのモジュールを読み込み、それを PyObject 型のオブジェクトとして返す関数です。モジュール名には文字列かシンボルを指定できます。モジュールの関数もビルトイン関数と変わらず特別な構文なしで呼び出せます。例えば、Pythonの標準ライブラリである math モジュールを読み込んで使うには次のようにします。

![](image_rsrcBGH.jpg)

サードパーティが開発したパッケージのモジュールも使えます。例としてSciPy[*22](c6GX.xhtml#a9SG)にあるモジュールを取り上げましょう。SciPyはPythonの科学技術計算用パッケージとして歴史が古く、Juliaには見つからない機能もSciPyを探せば見つかることもよくあります。準備として、pipやCondaなどのPythonのパッケージ管理ツールを使ってSciPyパッケージをインストールしておいてください。本書ではScipy 1.6.1を使いました。

![](image_rsrcBGJ.jpg)

scipy.stats モジュールにあるコルモゴロフ・スミルノフ(Kolmogorov-Smirnov, KS)検定を使ってみましょう。KS検定では、観測値が対象の分布(例えば正規分布)から生成されたとする帰無仮説を検定します。kstest 関数は観測値と分布をとり、統計量と p 値をタプルで返します。p 値が小さければ、観測値が対象の分布から生成されたとは考えにくいということになります。次のように乱数から生成したサンプルでは、正規分布で p 値が大きく、一様分布で p 値が小さく出ました。

![](image_rsrcBGK.jpg)

プロパティの参照にドット(.)が使えるのは、stats.kstest のようにPythonモジュールの関数を参照するときだけではありません。PyCall.jlは getproperty 関数や setproperty! 関数のメソッドを PyObject 型に定義している([9.4.6項](c434.xhtml#a9K2)を参照)ので、Pythonの構文と同じようにオブジェクトの属性も参照できます。オブジェクトのメソッド呼出しもPythonの構文と変わりません。

再び scipy.stats モジュールを使って見てみましょう。scipy.stats モジュールには多数の確率分布が定義されており、パラメータを固定した確率分布オブジェクトを作成できます。確率分布オブジェクトには、いくつものメソッドや属性が定義されています。次のコードは、Pythonで書いた確率分布オブジェクトの使用例です。

![](image_rsrcBGM.jpg)

これをPyCall.jlを使ったJuliaで書くと次のようになります。モジュールの読込みを除けば、構文的には何も変わっていないことがわかります。

![](image_rsrcBGN.jpg)

# 15.2.4 Pythonコードの埋め込み

PyCall.jlの強力な機能の1つに、Pythonコードの埋め込みがあります。これは、JuliaのソースコードにPythonのコードを直接埋め込める機能です。py"..." のように文字列の前に py を付けると、その文字列はPythonのコードとして評価されます。

まずは簡単な例を使って説明しましょう。[15.2.2項](c6GX.xhtml#a9GU)では pybuiltin 関数でPythonのビルトイン関数を取得してから関数を呼び出しましたが、Pythonコードの埋め込みを使えば、次のように直接ビルトイン関数を呼び出せます。

![](image_rsrcBGP.jpg)

次のようなJuliaの式の埋め込みも可能です。

![](image_rsrcBGR.jpg)

関数定義など、複数行からなるPythonのコードも埋め込めます。複数行のPythonコードは結果を返さないので、関数などを定義した場合はリスト15.4に示すように後で関数を取得する必要があります。

![](image_rsrcBGS.jpg)

1行でも複数行でも py"..." はPythonの __main__ モジュールで実行されます。1行の場合はPythonのビルトイン関数である eval 関数に、複数行の場合は同じくPythonのビルトイン関数である exec 関数に相当します。

# 15.2.5 配列の受け渡し

JuliaとPythonの間での配列のやりとりは頻繁に行われますが、その仕組みはやや複雑です。配列は非常に大きいサイズになることがよくあるので、配列の受け渡しではなるべくコピーを発生させたくありませんが、JuliaとPythonの配列の扱い方には無視できない違いがあります。このあたりの事情を詳しく説明しましょう。

Juliaの多次元配列([11.1節](c4WA.xhtml#a983))である Array 型において、配列のサイズは可変ですが、要素のメモリ上の配置(メモリレイアウト)は固定されています。一方、Pythonの多次元配列として最もよく使われるNumPyの ndarray クラスでは、メモリレイアウトは可変です。しかも、両者はデフォルトのメモリレイアウト([18.7.2項](c7N0.xhtml#a9G1)を参照)が異なります。したがって、JuliaからPythonに配列を渡すときはメモリレイアウトの設定を合わせる最低限の変換で済むのですが、PythonからJuliaに配列を渡すときにはメモリレイアウトが合わせられず、要素のコピーが必要です。

Pythonから返される ndarray クラスの配列をコピーなしで扱うには、Array 型以外の型に変換する必要があります。PyCall.jlはそのための PyArray という型を用意しています。これを使えば、例えば次のように @pycall マクロを使って Array 型ではなく、コピーの不要な PyArray 型のオブジェクトを返すように強制できます。

![](image_rsrcBGT.jpg)

# 15.3 R との連携

R は統計処理やデータ解析を得意とするプログラミング言語です。特に統計分野での人気が高く、R のパッケージレポジトリであるCRAN(The Comprehensive R Archive Network)[*23](c6GX.xhtml#a9UX)には非常に多くの関連パッケージが登録されています。さらに、Bioconductor[*24](c6GX.xhtml#a9UY)というバイオインフォマティクス向けのパッケージレポジトリもあり、広く使われています。本節では、Juliaから R を使う方法を紹介します。

# 15.3.1 RCall.jlのインストール

Pythonとの連携にPyCall.jlを使ったように、R との連携にはRCall.jlパッケージを使います。特定の R を使いたいときは、自分の環境に R をインストールしてください。R 環境のセットアップは本書では説明しませんので、R のウェブサイト[*25](c6GX.xhtml#aA10)を参照してください。最低限 R コマンドが実行できる環境があれば十分です。

R 環境のセットアップが終わったら、RCall.jlパッケージをインストールします。REPLのパッケージ管理モードで add RCall を実行すればRCall.jlがインストールされます。RCall.jlはインストールの過程で PATH 環境変数から R コマンドを探し、先ほどセットアップした R 環境と連携します。もし他の R 環境を使いたい場合は、R_HOME 環境変数に R の R.home 関数の戻り値を指定します。次の例を参考にしてください。

![](image_rsrcBGU.jpg)

本書では、LinuxのR 4.0.3とRCall.jl 0.13.11を用いました。

# 15.3.2 R 関数の呼出し

R の関数は R"..." マクロを使って呼び出せます。PyCall.jlと異なり、RCall.jlは R の関数が返すオブジェクトをJuliaのオブジェクトに変換しません。代わりに、R のオブジェクトをラップした RObject 型のJuliaのオブジェクトを返します。

![](image_rsrcBGV.jpg)

Juliaのオブジェクトを引数として R の関数に渡すには、$ を使った補間を使います。引数のオブジェクトは R の適切な型に自動で変換されます。

![](image_rsrcBGW.jpg)

R の関数を繰り返し使用するときは、次のように変数に関数を束縛すると便利でしょう。

![](image_rsrcBGX.jpg)

# 15.3.3 型の変換

RObject 型のオブジェクトをJuliaの対応する型のオブジェクトに変換するには rcopy 関数を使います。関数名が示唆するとおり、これはJulia側に R オブジェクトのコピーを作るので、変換後のJuliaのオブジェクトを更新しても元のオブジェクトには影響しません。例えば、次のような R のベクトルは、rcopy 関数で Vector{Float64} 型のオブジェクトに変換されます。

![](image_rsrcBGY.jpg)

逆にJuliaのオブジェクトを R のオブジェクトに変換するには、robject 関数を使います。ただし、R の関数を呼び出すとき自動で変換されるので、こちらはあまり使用機会はないでしょう。

これまでに見てきたように、Juliaのベクトルは R のベクトルに変換されます。同様に、行列(2次元配列)も行列に変換されます。

![](image_rsrcBGZ.jpg)

R の data.frame オブジェクトへはDataFrames.jlパッケージの DataFrame 型オブジェクトから変換できます。例として、RDatasets.jlパッケージにある iris データセット(3種のアヤメのデータセット)にrandomForestパッケージ[*26](c6GX.xhtml#a9EW)のランダムフォレストという分類手法を適用してみます。

![](image_rsrcBH0.jpg)

分類器から情報を取り出すには、次のように角括弧を使うのが便利です。こうすれば、R のオブジェクトから必要な部分だけJuliaへと持ってこられます。

![](image_rsrcBH1.jpg)

# 15.3.4 R のREPL

RCall.jlはJuliaのREPLから R のREPLに切り替える機能を持っています。これは R に慣れた人が一時的に R の構文を使いたいときに便利です。JuliaのREPLを立ち上げ、julia> プロンプトに何も入力されていない状態で $ キーを押下すると、プロンプトが R> に切り替わり、プロンプトに R のコードを直接書いて実行できるようになります。この状態のREPLは R の構文をそのまま使えるので、例えばtidyverse[*27](c6GX.xhtml#a9U8)でよく使われる %>% を使ったパイプライン処理や R の独特な評価がいつもどおり使えます。

まとめ

本章ではJuliaから C・Python・Rの3言語を使う方法を紹介しました。また、本章では紹介しませんでしたが、C からJuliaの共有ライブラリlibjuliaを呼び出すこともできますし、Pythonと R からJuliaを使用するパッケージ[*28](c6GX.xhtml#a9T5) [*29](c6GX.xhtml#a9T6)も開発されています。異なるプログラミング言語間の連携は難しい部分がありますが、うまくできれば開発の労力を減らせる強力な機能でもあります。ただし、他言語のツールがコマンドとして実行可能であれば、第14章で解説したように外部プログラムとして呼び出すほうが手軽なことも多いので、そちらの手段も検討してみてください。

注

[*1](c6GX.xhtml#aA2B) <https://gcc.gnu.org/>[*2](c6GX.xhtml#a9HP) <https://clang.llvm.org/>[*3](c6GX.xhtml#aAEB) 複数のオブジェクトファイル(object file)を結合する処理をリンク(link)と呼びます。[*4](c6GX.xhtml#aAAU) Windowsでは特に動的リンクライブラリ(dynamic link library, DLL)と呼ばれます。[*5](c6GX.xhtml#aAAJ) C の標準ライブラリであり、C で書かれた大半のLinuxプログラムが使っています。[*6](c6GX.xhtml#aAF7) x86-64やIntel 64とも呼ばれます。[*7](c6GX.xhtml#aA4B) ライブラリの検索はJuliaのC APIである jl_load_dynamic_library 関数が担います。[*8](c6GX.xhtml#aA1B) <https://www.zlib.net/>[*9](c6GX.xhtml#aA0R) Juliaの String 型はそもそもNUL終端なので、コストをかけず C に直接ポインタを渡せます。部分文字列などの場合はNUL終端になるようコピーを作ります。[*10](c6GX.xhtml#a9SN) C99の規格では int 型のデータサイズ(ビット数)を明確に規定してはいませんが、Julia 1.6がサポートするすべてのプラットフォームで32ビットです。[*11](c6GX.xhtml#a9ZK) ポインタ ptr のアドレスに sizeof(T) * (pos - 1) が加算されます。[*12](c6GX.xhtml#aA3H) 「ccall 関数」ではなく「ccall 構文」と呼んでいることに注意してください。ccall 構文の使い方は一見すると関数呼出しのように見えますが、実際は特殊な構文です。ccall 自体を関数オブジェクトのように扱うことはできません。[*13](c6GX.xhtml#a9P6) NUL終端であることが仮定できない場合には memchr 関数を使いましょう。[*14](c6GX.xhtml#aA2S) このアルゴリズムについては<https://arxiv.org/abs/2110.01111>を参照してください。[*15](c6GX.xhtml#a9GM) Ptr{Cdouble} でも渡せますが、15.1.7項で説明したオブジェクトの保護が必要になります。[*16](c6GX.xhtml#a9V0) C では構造体のメンバ(member)と呼ぶ方が一般的ですが、本章ではJuliaの慣習に合わせてフィールドと呼ぶことにします。[*17](c6GX.xhtml#a9SH) 本書ではXZ Utils(<https://tukaani.org/xz/>)のv5.2.4を使用しました。ソースコードのライセンスはパブリックドメインです。[*18](c6GX.xhtml#a9PD) 全体はヘッダーファイル src/liblzma/api/lzma/base.h に定義されています。[*19](c6GX.xhtml#a9WV) “A Brief Timeline of Python” <https://python-history.blogspot.com/2009/01/brief-timeline-of-python.html>[*20](c6GX.xhtml#aAA6) <https://docs.python.org/3/c-api/>[*21](c6GX.xhtml#aAEG) 本書では割愛しますが、同様の機能を提供するPythonCall.jlというパッケージもあります。[*22](c6GX.xhtml#aA6H) <https://scipy.org/>[*23](c6GX.xhtml#aA03) <https://cran.r-project.org/>[*24](c6GX.xhtml#a9XJ) <https://www.bioconductor.org/>[*25](c6GX.xhtml#aAEW) <https://www.r-project.org/>[*26](c6GX.xhtml#aAEN) <https://cran.r-project.org/web/packages/randomForest/>[*27](c6GX.xhtml#a9YV) <https://www.tidyverse.org/>[*28](c6GX.xhtml#aAEE) pyjulia: <https://github.com/JuliaPy/pyjulia>[*29](c6GX.xhtml#aAFR) JuliaCall: <https://github.com/Non-Contradiction/JuliaCall>

---

# ![](image_rsrcBH2.jpg)

---

# ![](image_rsrcBH3.jpg)

ソフトウェアの目的は問題を解決することにあります。ありがたいことに、多くの典型的な問題は、誰かが熟考のすえに解決策を見つけ、それをパッケージとして簡単に利用できるようにしてくれています。したがって、パッケージの利用方法をよく理解することは、効率的なソフトウェア開発に必要不可欠です。

パッケージ管理の方法は、プログラミング言語によってそれぞれ異なり、使うツールはもちろん、同じ言葉の指す意味も微妙に異なっています。Juliaのパッケージ管理でも、他の言語ユーザには耳慣れない概念や用語が登場します。本章では、最初にパッケージに関する概念と用語を解説し、その後でパッケージ管理・開発の実践的な方法を紹介します。

# 16.1 パッケージの基礎概念

Juliaにおけるパッケージ(package)とは、コードを再利用可能な形式で配布する単位です。パッケージ化されたソフトウェア資産は、GitHubなどのホスティングサービスやローカルのファイルシステムを通じて、別のプロジェクトで再利用できます。インストールされたパッケージは、using Foo や import Foo などのようにusing文([7.9.3項](c315.xhtml#a9FP))やimport文([7.9.4項](c315.xhtml#a9FR))を使って読み込めます。

JuliaにはPkg.jlというパッケージ管理ツールが同封されており、パッケージ管理の大部分はPkg.jlを通して行います。パッケージ管理については16.2節で詳しく扱います。本節では、パッケージの管理と開発のための基礎的な概念を解説します。新しい用語が多数登場するので一度では理解しきれないかもしれませんが、概要を把握するため、まずは目を通すことをおすすめします。

# 16.1.1 パッケージの概要

本書では、説明のためにパッケージを次の3つに分類しています。

標準パッケージ

Juliaとともに配布される標準ライブラリに含まれるパッケージ

Statistics.jlやRandom.jlなど

公式パッケージ

Generalレジストリ([16.1.5項](c6XJ.xhtml#aA8B))に登録されているパッケージ

DataFrames.jlやTranscodingStreams.jlなど

非公式パッケージ

上記のいずれでもないパッケージ

また、本書では標準パッケージ以外のパッケージを外部パッケージと呼んでいます。

これらのパッケージはいずれも共通のディレクトリ構成をとります。このディレクトリ構成はほぼ決まっているので、ファイルの配置に悩む必要はありません。典型的な構成は次のとおりです。

READMEファイル

パッケージの概要を説明するファイルです。パッケージの目的や使用方法などを簡単に説明します。READMEファイルの内容は、GitHubレポジトリのページに表示されます。ファイル名は README.md とし、Markdown形式([付録 A](c888.xhtml))で記述するのが一般的です。

ライセンスファイル

パッケージの使用許可条件を明文化するファイルです。オープンソースのソフトウェアとして公開するときはこのファイルが必要です。MITライセンスやBSDライセンスなどの寛容ライセンスがJuliaコミュニティでは一般的です。ファイル名は LICENSE などとします。

プロジェクトファイル

パッケージの管理情報を記述するファイルです。ファイル名は Project.toml とします。[16.1.4項](c6XJ.xhtml#a9CC)で詳しく説明します。

ソースディレクトリ

パッケージのモジュールを実装するソースコードを収めるディレクトリです。ディレクトリ名は src とし、パッケージのエントリポイントとしてパッケージ名と同じ名前のソースコードファイルを置きます。例えばFoo.jlというパッケージなら、src/Foo.jl ファイルがエントリポイントです。その他のソースコードは、このファイルから直接的または間接的に読み込まれます。

テストディレクトリ

パッケージのテストファイルを収めるディレクトリです。ディレクトリ名は test とし、テストを実行するスクリプトとして test/runtests.jl ファイルを置きます。

ドキュメントディレクトリ

パッケージの使用方法に関わるドキュメントを収めるディレクトリです。ディレクトリ名は docs とします。Documenter.jlパッケージを使ってMarkdownファイルからドキュメントを生成するのが一般的です。

# 16.1.2 リビジョンとバージョン

パッケージを含むソフトウェアの管理には、リビジョンやバージョンという概念があります。リビジョン(revision)とは、ソフトウェアのスナップショットです。つまり、継続的に開発が続くソフトウェアの一瞬の状態を指してリビジョンと呼びます。Gitなどのバージョン管理システムでは、リビジョンに名前を付けて管理する仕組みがあります。Gitではリビジョンをコミットと呼び、16進法で表記されたハッシュ値を使って個々のリビジョンを識別しています。リビジョンの一部にはバージョン(version)という別名が付けられます。バージョンが付与されたリビジョンは、一般ユーザのためにリリースされるのが一般的です。Gitについては[付録 C](c8EH.xhtml) にも説明がありますので、バージョン管理システムに馴染みがなければ、一読することをおすすめします。

JuliaのパッケージはGitなどのバージョン管理システムを使って開発されるので、リビジョンの概念があります。Juliaのパッケージ管理ツールは、Manifest.toml という名前のファイルに、インストールしたパッケージのリビジョンを記録しています。このファイルについては[16.1.4項](c6XJ.xhtml#a9CC)で改めて説明します。

DataFrames.jlパッケージのレポジトリ(バージョン管理されたソフトウェア開発プロジェクトの複製)を例に、git log コマンドで出力したコミットの履歴(一部)を図16.1に示します。下に行くほど古いコミットです。図中には青色でハイライトされた8個のリビジョン(コミット)が表示されており、そのうち4つのリビジョンにはオレンジ色でハイライトされたバージョンが付けられています。各コミットにはコミットの著者・コミット時刻・コミットメッセージなどの情報も表示されています。

![](image_rsrcBH4.jpg)

# 16.1.3 セマンティックバージョニング

セマンティックバージョニング(semantic versioning)はバージョンの命名に関する規則です。SemVerと呼ばれることもあります。Juliaの処理系やパッケージは、セマンティックバージョニングに従ってバージョンを付与する決まりになっています。したがって、パッケージを開発するときだけでなく、パッケージを使うときにも重要な概念ですので、ここで説明しておきましょう。なお、本項ではセマンティックバージョニングの要点だけを紹介しますので、詳細な仕様は<https://semver.org/>を参照してください。

セマンティックバージョニングでは、0以上の整数を3つ使ってバージョンを指定します。3つの数字は、1.3.11や0.12.2のようにピリオドで区切り表記します[*1](c6XJ.xhtml#aA5V)。3つの数は図16.2のように左から順に MAJOR, MINOR, PATCH と名前が付けられています。例えば、バージョンが1.3.11なら、MAJORが1、MINORが3、PATCHが11です。アルファ版やベータ版のリリースなどでは、1.2.0-alphaのようにPATCHの後に修飾子が続くこともあります。

![](image_rsrcBH5.jpg)

ソフトウェアの新しいバージョンがリリースされるとき、MAJOR, MINOR, PATCHのどれかの数字が1だけ増え、後続の数字は0に戻ります。つまり、MAJORが増えるとMINORとPATCHは0に戻り、MINORが増えるとMAJORはそのままですがPATCHは0に戻ります。PATCHが増えたときはMAJORとMINORの数字はそのままです。例えば、1.3.11の次のバージョンは、図16.3に示すように2.0.0, 1.4.0, 1.3.12のいずれかです。

![](image_rsrcBH6.jpg)

バージョンのどの番号が増えるかは、ソフトウェアに加えた変更の内容によります。セマンティックバージョニングで定められている規則は次のとおりです。

●MAJOR

後方互換性のないAPIの変更

●MINOR

後方互換性のある機能追加

●PATCH

後方互換性のあるバグ修正

ここでいう後方互換性(backwards compatibility)とは、新しくリリースされるバージョンが既存の他のプログラムと互換性があることを意味します。例えば、2つのパッケージ A と B があり、B が A の機能を使っている(依存している)としましょう。A の新しいバージョンをリリースしたとき、B の動作を変えない変更は互換性がある変更です[*2](c6XJ.xhtml#aAF3)が、動作を変えうる変更は互換性がない変更です。したがって、A の開発者が B の動作を変える可能性のある変更、すなわち後方互換性のない変更をリリースするときは、バージョンのMAJORを増やす必要があります。

本来のセマンティックバージョニングでは、MAJORが0のときは初期の開発版とみなされ、例外的にMINORやPATCHの変更でも後方互換性は保証されません。一方、Juliaのセマンティックバージョニングでは、MAJORとMINORがともに0のときのみ後方互換性を保証せず、MAJORが0であってもMINORが1以上かつ一致すれば互換性があるバージョンとみなします[*3](c6XJ.xhtml#aA5A)。具体例で説明しましょう。MAJORが1以上のとき、例えば1.3.11→1.3.12と1.3.11→1.4.0は後方互換性のある更新ですが、1.3.11→2.0.0では後方互換性は保証されません。MAJORが0でかつMINORが1以上のとき、例えば0.3.11→0.3.12は後方互換性のある更新ですが、0.3.11→0.4.0と0.3.11→1.0.0では後方互換性は保証されません。MAJORもMINORも0のとき、例えば0.0.11のときは、どのようなバージョンの更新も後方互換性は保証されません。Juliaのセマンティックバージョニングでは、MAJORが0のときの互換性の考え方が異なることに注意してください。

# 16.1.4 環境

環境(environment)とは、抽象的にいえば、パッケージ名が具体的にどのパッケージのどのリビジョンを意味するかを定めるものです。using Foo や import Foo を実行すると、Juliaは環境から Foo という名前を持つパッケージのリビジョンを探します。プロジェクトごとに環境を切り分ければ、開発や実行に必要なパッケージのリビジョンを互いに干渉することなく管理できます。管理方法は[16.2.2項](c6XJ.xhtml#a9EB)で扱います。

環境の実体は、プロジェクトファイル(project file)とマニフェストファイル(manifest file)の2つのファイルです。ファイル名はそれぞれ Project.toml と Manifest.toml です[*4](c6XJ.xhtml#a9U5)。Juliaはこれら2つの設定ファイルを参照して、読み込むパッケージのソースコードを決定します。各ファイルには、次のような役割があります。

プロジェクトファイル

プロジェクトのメタ情報と、その環境で利用可能なパッケージを記録

マニフェストファイル

環境にあるパッケージのリビジョンと、パッケージ間の依存関係を記録

大雑把な言い方をすれば、プロジェクトファイルは現在の環境が直接的に使うパッケージの情報を抽象的に記録し、マニフェストファイルはどのソースコードを使うかを(間接的に使うパッケージも含めて)具体的かつ詳細に記録します。プロジェクトファイルは人間が直接読み書きすることがありますが、マニフェストファイルはJuliaのパッケージ管理ツールがプロジェクトファイルをもとに自動生成するので、直接読み書きすることはまずありません。

ファイルの形式はどちらの設定ファイルもTOML形式のテキストファイルです。TOMLは人間が読み書きしやすく、Rustなどでも設定ファイルとして利用されています。パッケージを使用するうえでTOMLの文法を詳しく知る必要はありませんが、基本的な読み書きができると便利なので、[付録 B](c8BX.xhtml) で簡単に解説しています。例えば、Example.jlパッケージがインストールされた環境があったとき、そのプロジェクトファイルの deps セクションを見ると、次のようにパッケージの名前と識別子の対応が含まれています。

![](image_rsrcBH7.jpg)

これは、Example.jlパッケージの識別子が 7876af07-990d-54b4-ab0e-23690620f79a という文字列であることを表しています。この奇妙に長い識別子は、UUID(universally unique identifier)と呼ばれる128ビットの識別子です。Julia内部では、パッケージはその名前の文字列ではなく、UUIDを使って識別されます。UUIDはパッケージを作成するときに自動で生成され、異なるパッケージが同じUUIDを持つことはありません。

# 16.1.5 レジストリ

パッケージの情報は、レジストリ(registry)というデータベースに登録されます。レジストリはパッケージの場所やバージョン、パッケージ間の依存関係などを記録しており、パッケージ管理ツールが環境にパッケージをインストールするときなどに参照されます。一般のユーザが最もよく使うレジストリは、[16.1.1項](c6XJ.xhtml#a9ZG)で言及した、公式パッケージが登録されているGeneralレジストリ[*5](c6XJ.xhtml#a9ZH)です。Generalレジストリの実体はGitレポジトリです。

レジストリは自分でも作成できます。例えば、組織で開発したパッケージを組織内で独自に作ったレジストリに登録すると、Generalレジストリに登録されているパッケージと同じように利用できます。Generalレジストリはデフォルトのレジストリという意味で特殊ですが、レジストリの構造自体はどれも同じです。本書ではGeneralレジストリのみを使うので、レジストリの管理についての説明は割愛しますが、必要であればPkg.jlのドキュメント[*6](c6XJ.xhtml#a9A4)を参照してください。

# 16.1.6 アーティファクト

アーティファクト(artifact)とは、Pkg.jlが管理するパッケージの付属ファイルです。パッケージが依存するJulia外の共有ライブラリ(zlibやlibxml2など)やデータファイルなどはアーティファクトの例です。こうしたファイルはパッケージ間で共有されることが多いので、パッケージ本体とは別に管理されています。多くのユーザはアーティファクトの管理を意識する必要はありませんが、例えば C のライブラリに依存するパッケージや、データファイルなどをダウンロードするパッケージを開発するときには、アーティファクトの利用方法について知っておく必要があります。

ビルド済みの共有ライブラリを提供するアーティファクトの多くは、Yggdrasilというレポジトリ[*7](c6XJ.xhtml#aAEA)に登録されています。ここにはアーティファクトを生成するレシピが多数置かれており、作成したアーティファクトのレシピをここに登録しておけば、別々の人が同じライブラリのアーティファクトを生成するというような仕事の重複が避けられます。C などのライブラリを使うパッケージを開発する人は、あらかじめ確認しておくとよいでしょう。

アーティファクトという概念はJulia 1.3で導入されました。それ以前は各パッケージがそれぞれのやり方で使用するライブラリやデータファイルを自分で管理していたのですが、パッケージのインストール先ディレクトリを書き換えてしまったり、異なるバージョンをインストールするとそれらのファイルに重複が発生したりするなどの問題がありました。こうした問題を解決し、Pkg.jlがそうしたファイルの管理を統一的な仕組みで行うため、アーティファクトが導入されたという経緯があります。

# 16.1.7 デポ

Juliaのパッケージに関わるデータは、デポ(depot)というディレクトリ内に配置されます。インストールしたパッケージのソースコードもこの中に配置されます。デポの構造の概要を表16.1に示します。

![](image_rsrcBH8.jpg)

通常、デポにはシステム管理者用とユーザ用があります。システム管理者用のデポの位置は、Juliaのインストール先によって変わります。デフォルトのユーザ用デポの位置は、ユーザディレクトリ以下の .julia ディレクトリです。実際のデポの位置は DEPOT_PATH グローバル変数で参照できます。また、デポの位置は JULIA_DEPOT_PATH 環境変数で変更できます。標準パッケージ以外のパッケージに関するファイルはすべてデポに置かれるので、デポを削除すれば完全にリセットできます。

# 16.2 パッケージの管理

ここまででパッケージに関する基礎概念を説明したので、本節ではパッケージの管理方法を解説します。Juliaのパッケージ管理は、標準パッケージであるPkg.jlが担っています。

# 16.2.1 パッケージ管理モード

JuliaのREPLにはパッケージ管理専用のモードがあります。REPLを立ち上げ、julia> プロンプトが表示されている状態で ] キーを押下すると、プロンプトが (@v1.6) pkg> のように変化します。括弧内の @v1.6 は、現在有効になっている環境の名前です。この例では、環境がJulia 1.6のデフォルト環境であることを意味します。

パッケージ管理モードのプロンプトには、パッケージ管理用のコマンドを入力できます。例えば、環境の状態を表示する status コマンドを実行すると、次のようにその環境の情報が表示されます。

![](image_rsrcBH9.jpg)

この例では、デポ内にあるプロジェクトファイルの位置と、BenchmarkTools.jlのバージョン1.3.1とPkgTemplates.jlのバージョン0.7.26がインストールされていることがわかるでしょう。デフォルトのプロジェクトファイルとマニフェストファイルは、LinuxとmacOSでは ~/.julia/environments/v#.# ディレクトリに、Windowsでは例えば C:![](image_rsrcAGC.jpg)Users![](image_rsrcAGC.jpg)USERNAME![](image_rsrcAGC.jpg).julia![](image_rsrcAGC.jpg)environments![](image_rsrcAGC.jpg)v#.# ディレクトリ[*8](c6XJ.xhtml#aAEY)にあります[*9](c6XJ.xhtml#aAEZ)。ファイルパスにある2つの # は、順にJuliaのバージョンのMAJORとMINORを指します。

パッケージ管理モードで help コマンドを実行すると、利用可能なコマンドの一覧が表示されます。help status のように help コマンドの後にコマンド名を渡すと、そのコマンドの使い方が次のように表示されます。

![](image_rsrcBHA.jpg)

status コマンドには st という短縮形があり、help コマンドには ? という短縮形があります。頻繁に使うコマンドは短縮形も覚えておくとよいでしょう。以降では、パッケージ管理モードで使える重要なコマンドを紹介します。

# 16.2.2 環境の切り替え

現在有効な環境は、パッケージ管理モードで pkg> プロンプトの左に表示されます。例えば、一般ユーザが特別な設定のない状態でREPLを起動すれば、@v1.6 や @v1.7 のように、処理系バージョンごとに用意されたデフォルト環境が表示されるはずです。

パッケージの追加・更新・削除などの操作は、現在有効化されている環境のみに影響します。したがって、例えば @1.6 に追加したパッケージは、@1.7 では使えないことに注意してください。

環境はユーザが作成し、自由に切り替えられます。環境を分離すれば他のプロジェクトに干渉せずパッケージを管理できるので、あるプロジェクトでパッケージを更新したせいで別のプロジェクトが正しく動かなくなるなどの心配はありません。環境を再現する方法は[16.2.5項](c6XJ.xhtml#a9B5)で紹介します。

実際に環境を作ってみましょう。ここでは、例としてExample.jlという非常に簡単なパッケージを使います。特定の環境を有効化するには、activate コマンドを使います。activate コマンドはディレクトリのパスを引数にとると、そのディレクトリに固有の環境を有効化します。まずは ~/workspace/myproject という空のディレクトリを用意します。そして次のように activate コマンドを実行し、続けて add コマンドでExample.jlを追加すると、その環境に指定したパッケージが追加されます。pkg> プロンプトの左に表示された環境の変化に注意してください。

![](image_rsrcBHB.jpg)

このとき、~/workspace/myproject ディレクトリには、プロジェクトファイルとマニフェストファイルが作成されます。プロセスを終了した後でも、同じようにこの環境を有効化すれば、これらのファイルに記録された以前と同じ環境が再現されます。

REPL内でデフォルト環境に戻るには、次のように activate コマンドを引数なしで実行します。

![](image_rsrcBHC.jpg)

動作確認のため、myproject ディレクトリには、リスト16.1に示す短いスクリプトを置いておきます。このスクリプトはExample.jlの hello 関数を呼び出します。

![](image_rsrcBHD.jpg)

activate コマンドは環境を切り替える1つの方法ですが、REPLが使えない状況もあります。例えば、あるプロジェクトの環境を有効化してスクリプトを実行したいとき、julia script.jl のようにシェルから実行したいところですが、REPLを使わないので activate コマンドが実行できません。activate コマンドを使わずに特定の環境を有効化するには、3つの方法があります。1つ目の方法は、次のように julia コマンドに \--project オプションを使って環境を指定する方法です。これは現在のディレクトリを始点に親のディレクトリをさかのぼり、最初に見つけたプロジェクトファイルの環境を有効化します。

![](image_rsrcBHE.jpg)

\--project オプションにはディレクトリを引数として設定できます。例えば、ドット(.)を指定すれば、現在のディレクトリの環境が有効化されます。なお、@. という特殊な値[*10](c6XJ.xhtml#a9SD)も引数として指定できますが、これは何も引数を渡さない上記の動作と同じです。

環境を有効化する2つ目の方法は、JULIA_PROJECT 環境変数に環境を指定する方法です。例えば、次の2つの書き方は同じ効果があります。シェルの設定で JULIA_PROJECT 環境変数の値を @. にしておけば、現在のディレクトリの環境を自動的に有効化してくれるので便利です。

![](image_rsrcBHF.jpg)

3つ目の方法は、Pkg.jlパッケージの Pkg.activate 関数を使う方法です。例えば、スクリプトの先頭で次のように有効化したい環境のパスを指定して Pkg.activate 関数を呼び出せば、その環境を有効化できます。起動時のオプションや環境変数を書き換えるのが難しいときは、この方法を使うとよいでしょう。

![](image_rsrcBHG.jpg)

# 16.2.3 パッケージの追加・更新・削除・固定

前項ですでに登場していますが、パッケージの追加は add コマンドを使います。引数には、次のようにパッケージ名・レポジトリのパス・レポジトリのURLが指定できます。パッケージ名を指定した場合、レジストリを参照して具体的なパッケージが決まります。

![](image_rsrcBHH.jpg)

add Foo Bar Baz のように複数のパッケージを指定すると、すべて同時に追加されます。特定のバージョンを追加したいときは、add Example@0.5 のように単価記号の直後にバージョンを続けると、そのバージョンと互換性のある最新版が環境にインストールされます。バージョンは @0.3-0.5 のように範囲も指定できます。また、開発時などに特定のリビジョンを追加したいときは、add Example#master のようにハッシュ(#)の後にブランチやコミットを続けます。

環境中のパッケージを更新するには、update コマンドを使います。短縮形は up です。環境中のパッケージを削除するには remove コマンドを使います。短縮形は rm です。使い方はどちらも対象のパッケージ名を引数として指定しますが、update コマンドは引数を省略すると環境中の全パッケージを対象とします。

操作をキャンセルしたいときには undo コマンドが使えます。例えば、誤ってパッケージを削除してしまったら、直後に undo コマンドを実行すればその操作をキャンセルできます。undo コマンド自体は redo コマンドでキャンセルできます。

特定のパッケージを更新対象にしたくないときは、pin コマンドでパッケージのリビジョンを固定できます。固定したパッケージには status コマンドの表示にピンのようなマーク(![](image_rsrcBHJ.jpg))が付きます。free コマンドで固定を解除できます。次の例では、Example.jlの0.5.3がインストールされている状態でバージョンを0.5.0に固定し、状態を表示した後で、固定を解除しています。

![](image_rsrcBHK.jpg)

# 16.2.4 Pkg.jlのAPI

Pkg.jlはREPLのパッケージ管理モード以外に、Juliaから使えるAPIを提供しています。実際には、REPLのパッケージ管理モードで行われた操作もPkg.jlのAPIを使って実装されており、両者は本質的に同等の操作を行います。例えば、status コマンドは次のように使用できます。

![](image_rsrcBHM.jpg)

REPLを起動せずにコマンドラインからパッケージをインストールしたいときなどは、次のように -e オプションを使ってこのAPIを実行するとよいでしょう。

![](image_rsrcBHN.jpg)

# 16.2.5 環境の再現

既存の環境をリビジョンも含めて別の計算機で再現したい状況はよくあります。例えば、手元で開発用に構築した環境を計算用サーバに移行したい場合や、研究で行った計算の再現性を確保したい場合などです。マニフェストファイルを使えば、これは容易に実現できます。

計算機 A にある環境を計算機 B に移行する状況を考えましょう。計算機 A と B は同じ計算機でも構いません。まず、計算機 A のプロジェクトで目的の環境を構築します。次に、そのプロジェクトのプロジェクトファイルとマニフェストファイルを計算機 B のディレクトリにコピーします。続いて、コピー先のディレクトリで activate コマンドを実行して環境を有効化した後、instantiate コマンドを実行します。すると、パッケージのインストールが始まり、コピー元とコピー先のディレクトリに同じ環境が構築されます。

ここで使った instantiate コマンドは、現在の環境にインストールされていないパッケージをマニフェストファイルの情報をもとにインストールするコマンドです。リビジョンも含めてまったく同じ環境が再現されるので、そこでスクリプトなどを実行すれば、同じ結果が得られることになります。もしリビジョンまで完全に一致する必要はなく、必要なパッケージだけインストールされればよいなら、instantiate コマンドに \--project オプションを渡すか、マニフェストファイルを削除してから instantiate コマンドを実行すれば、プロジェクトファイルをもとにして新たに依存解決を行い、必要なパッケージをインストールします。

# 16.2.6 バージョンの制約

マニフェストファイルは環境を固定するときに便利ですが、実際には環境が完全に一致する必要がない場合も多くあります。例えば、Foo.jlパッケージのバージョン1.2で導入された機能を使う場合、1.2.0以降であれば1.2.1でも1.3.0でも構わないでしょう。そのようなときは、プロジェクトファイルに compat セクションを設け、そこに互換性のあるパッケージのバージョンを記載します。compat セクションに記載がないパッケージは、なるべく最新のものが追加されます。

次の例を見てください。これは、このプロジェクトが必要とするFoo.jlのバージョンが0.4.0と互換性があるものに限られるという制約を課しています。

![](image_rsrcBHP.jpg)

この結果、Juliaのセマンティックバージョニングに照らして、例えば0.4.0や0.4.1は互換性のあるバージョンとして追加される可能性がありますが、0.3.0や0.5.0は互換性が保証されない[*11](c6XJ.xhtml#a9YF)ので追加されません。

バージョン1.0.0がすでにリリースされているパッケージなら、次のように互換性のあるMAJORを指定するとよいでしょう。こうすると、互換性のある1.0.0や1.1.0などは追加される可能性がありますが、2.0.0などは互換性が保証されないので追加されません。

![](image_rsrcBHR.jpg)

バージョン1.2.0で追加された機能に依存するプロジェクトであれば、次のようにMINORまで指定します。こうすると、1.0.0や1.1.1は追加されません。

![](image_rsrcBHS.jpg)

互換性のあるバージョンが複数ある場合は、"1.2, 2" のようにコンマで区切ります。ほとんどの場合、ここで紹介したシンプルな記法で十分ですが、より精密なバージョン制約も可能です。詳しくはPkg.jlの互換性に関する説明[*12](c6XJ.xhtml#a9BX)を参照してください。

# 16.2.7 環境のスタック

デバッグやベンチマークなどに使うパッケージは、プロジェクト自体には必要ありませんが、開発中には使いたいことが多いです。こうしたパッケージを開発中のプロジェクト環境に追加してしまうと無駄に依存が増えてしまうので、別の環境に分けておきたいものです。この課題は環境のスタックで解決できます。

環境のスタックとは、図16.4に示すような複数の環境が重なった構造を指します。パッケージを読み込むとき、Juliaはスタックの環境を上から順に検索し、最初に名前が一致したパッケージを読み込みます。一番上の @ で示された環境は、現在有効化されているプロジェクト固有の環境です。その次の @v#.# は、@v1.6 などのJuliaバージョン固有の環境です。最後の @stdlib は標準パッケージの環境です。バージョン固有の環境である @v#.# にデバッガなどのプロジェクトでは直接必要ないパッケージをインストールしておけば、どの環境が有効化されているかにかかわらず、どこからでも読み込めます。@ 環境にパッケージを追加すれば、仮に @v#.# 環境に同じパッケージがあったとしても、上位にある @ 環境のパッケージが優先的に読み込まれます。

![](image_rsrcBHT.jpg)

環境のスタックの仕組みをもう少し説明しましょう。環境のスタックは LOAD_PATH グローバル変数に記載されています。デフォルト状態では、図16.4でも示したように次の3つの環境がスタックにあります。各要素の詳しい意味については、LOAD_PATH のドキュメントを参照してください。

![](image_rsrcBHU.jpg)

試しにスタックの一番下にある @stdlib を LOAD_PATH から追い出してみると、標準パッケージであるRandom.jlが読み込めなくなることがわかります。

![](image_rsrcBHV.jpg)

同様にして LOAD_PATH グローバル変数を変更すれば、パッケージの環境をかなり自由に制御できます。ただし、これはちょっとしたハックなので、あまりやらないほうがよいでしょう。なお、本書の環境に関する説明は、実用を重視してやや単純化されています。技術的な詳細は、ドキュメントのコード読み込みに関する記述[*13](c6XJ.xhtml#a9E9)を参照してください。

# 16.3 パッケージの開発

本節では、[3.3節](cXU.xhtml#a96D)で作成した Othello モジュールのパッケージ化を通して、パッケージの開発方法を紹介します。なお、パッケージ開発ではバージョン管理システムであるGitを使用するので、馴染みがない方はあらかじめ[付録 C](c8EH.xhtml) に目を通すことをおすすめします。

# 16.3.1 パッケージの生成

ここでは、テンプレートを使ってパッケージを生成します。これはパッケージ管理モードの generate コマンドでも可能ですが、機能が不十分で後の作業が煩雑になります。そこで、本書ではPkgTemplates.jlパッケージを使って実用的なテンプレートを作成することにします。

まずは、次のように add コマンドでPkgTemplates.jlをインストールしてください。再現性のため、執筆当時の最新版であるバージョン0.7に固定しましたが、より新しいバージョンがあれば、そちらのほうがよいでしょう。

![](image_rsrcBHW.jpg)

PkgTemplates.jlはGitの設定からいくつかのデフォルト値を決定するので、[付録C.2](c8EH.xhtml#aA31) を参照してあらかじめ設定しておくとよいでしょう。GitHubにパッケージのレポジトリを設置するなら、次の設定を推奨します。USER は自身のアカウント名に置き換えてください。

![](image_rsrcBHX.jpg)

続いて、パッケージを生成するためのテンプレートを用意します。これにはPkgTemplates.jlの提供する Template 型を使います。このコンストラクタは、テンプレートを調整するいくつかのキーワード引数をとれます。ここでは、リスト16.2に示すように、パッケージを設置するディレクトリ dir と、サポート対象のJuliaのバージョン julia を指定し、いくつかのプラグインを設定しています。引数の概要はコメントのとおりです。

![](image_rsrcBHY.jpg)

REPLでリスト16.2のテンプレートを読み込み、Othello.jlパッケージを生成します。テンプレートは文字列を受けとると、その名前のパッケージを生成します。

![](image_rsrcBHZ.jpg)

生成されたディレクトリに移動してください。パッケージの構造は図16.5のようになっているはずです。

![](image_rsrcBJ0.jpg)

生成されたプロジェクトファイルは次のように、パッケージ名・UUID・作者・バージョンの情報が先頭に記載されています。作者の情報はGitの設定から読み込まれます。なお、UUIDは乱数を使って生成されるので、生成するたびに変わります。

![](image_rsrcBJ1.jpg)

生成されたパッケージのディレクトリは、すでに完全なGitのレポジトリになっています。変更を加えたら、レポジトリにコミットしていきましょう。パッケージ生成直後のコミットログは次のようになっています。

![](image_rsrcBJ2.jpg)

# 16.3.2 依存パッケージの追加

次に、[3.3節](cXU.xhtml#a96D)で作成した Othello モジュールのコードを src ディレクトリの Othello.jl ファイルにコピーします。ファイルごと置き換えてもよいですし、Othello モジュールの中身だけ取り替えても構いません。続いて、[16.2.2項](c6XJ.xhtml#a9EB)で説明したようにパッケージの開発環境を有効化します。そして、REPLで using Othello を実行すると、次のようになるはずです。

![](image_rsrcBJ3.jpg)

何やら警告メッセージが表示されているのがわかります。これは、Othello.jlパッケージでPrintf.jlパッケージを使っているのに、そのことがプロジェクトファイルに記載されていないことを意味します。一般に、パッケージが使う他のパッケージのことを依存パッケージ(dependency package)と呼び、依存パッケージはプロジェクトファイルに記載されていなければなりません。これはPrintf.jlのような標準パッケージでも例外ではありません[*14](c6XJ.xhtml#aAAD)。

プロジェクトファイルに依存パッケージを記載するには、環境にパッケージを追加する add コマンドを実行します。

![](image_rsrcBJ4.jpg)

git diff コマンドを使ってプロジェクトファイルの変更を確認すると、次のように deps セクションがファイルに追加され、その直後にPrintf.jlパッケージとそのUUIDが記載されています。このように、プロジェクトの依存パッケージは deps セクションに記録されます。また、このときマニフェストファイルも生成されているはずです。

![](image_rsrcBJ5.jpg)

標準パッケージ以外にも、Generalレジストリにある公式パッケージはすべて依存パッケージにできます。例えば、テキスト出力をカラフルにできるCrayons.jlパッケージをOthello.jlで使うには、パッケージ管理モードで add Crayons を実行します。

ソースコードとプロジェクトファイルの変更内容は、次のようにしてコミットしておきましょう。

![](image_rsrcBJ6.jpg)

# 16.3.3 分割による構造化

開発が進むにつれ、最初に作った Othello.jl ファイルが大きくなり、構造が把握しにくくなってくることが考えられます。ファイルを分割して構造化すれば、管理や全体の把握が比較的しやすくなります。

ソースコードのファイルを分割して構造化するには、include 関数を使います。一例として、図16.6に示すような機能ごとの分割が考えられます。分割後の Othello.jl ファイルはリスト16.3を参照してください。

![](image_rsrcBJ7.jpg)

分割したファイルは Othello.jl ファイルから次のように取り込みます。include 関数に渡すファイルパスは、関数呼出しのあるファイルから見た相対パスです。なお、include 関数に渡すファイルパスの区切り文字には、OSによらず常にスラッシュ(/)が使えます。

![](image_rsrcBJ8.jpg)

この変更もコミットしておきます。

![](image_rsrcBJ9.jpg)

# 16.3.4 ユニットテスト

テスト(test)とは、パッケージの機能が意図どおりに動作するかをチェックするプログラムのことです。「テストする」のように動詞として使うこともよくあります。現代のパッケージ開発では、ソフトウェアに加えた変更が予期せぬ機能の不全を引き起こすこと[*15](c6XJ.xhtml#a9XN)を防ぐため、テストは欠かせない要素になっています。ソフトウェア開発においてはさまざまなレベルのテストが存在しますが、本書では機能を個別にテストするユニットテスト(unit test)のみを扱います。

まずは初期状態でテストを実行してみましょう。REPLのパッケージ管理モードで test コマンドを実行すると、現在開発中のパッケージのテストがすべて実行されます。まだ何もテストを書いていないので、次のように最後の要約には No tests とだけ出力されます。

![](image_rsrcBJA.jpg)

テストのコードは src ディレクトリのソースコードとは分け、test ディレクトリにあるファイルに記述します。test/runtests.jl ファイルがテストのエントリポイントであり、先ほど実行した test コマンドはこれを実行しています。ソースコードと同様に、テストのコードが大規模になるときは、test ディレクトリにファイルを分割して置くこともできます。

標準ライブラリには、Test.jlというユニットテスト用のパッケージが用意されています。Test.jlの機能でよく使われるのは、@test マクロと @testset マクロの2つです。@test マクロは渡された式が true に評価されるかをテストします。例えば、* 関数の機能をテストしたいとしたら、@test 2 * 3 == 6 のようにテストケースを記述するのが一般的です。@testset マクロは、こうしたテストケースをグループ化するためのマクロです。使用例として、石をひっくり返す flip 関数の動作をテストするリスト16.4を見てください。

![](image_rsrcBJB.jpg)

@test マクロに渡した式が true に評価されればテストケースは通過(pass)し、false に評価されれば失敗(fail)します。@test マクロ以外にもTest.jlにはさまざまなマクロが用意されています。例えば、@test_throws マクロは、式を評価したときに例外が送出されることを確認します。Test.jlパッケージが提供するマクロの概要を表16.2に示します。

![](image_rsrcBJC.jpg)

先のテストを runtests.jl ファイルに記述し、再び test コマンドを実行すると、次のように記述したテストケースが期待どおり通過していることがわかります。

![](image_rsrcBJD.jpg)

ここでは flip 関数のみをテストしましたが、練習として他の関数のテストも書いてみてください。テストを書いてみると、ソフトウェアとしての設計の良し悪しもわかってきます。もしテストを書きにくい機能などがあれば、機能を他の機能からうまく分離できていない可能性が高いです。

ここでの変更もコミットしておきましょう。

![](image_rsrcBJE.jpg)

なお、@testset マクロは入れ子にして使うこともできます。次のように何らかの文脈をテストに付与して構造化するときに便利です。

![](image_rsrcBJF.jpg)

# 16.3.5 ドキュメント

ドキュメント(document)には、ソースコード中に記述するdocstringと、別のファイルに記述するものがあります。前者はJuliaの標準機能ですが、後者はDocumenter.jlなどの外部パッケージを使って作成します。どちらもMarkdown形式で記述するので、Markdown形式に馴染みがない場合は[付録 A](c888.xhtml) を参考にしてください。

# docstring

ソースコード中の型や関数などの名前には、docstring というドキュメントを付けられます。ドキュメントはREPLのヘルプモード([2.3.4項](cK9.xhtml#a9GG))で参照できます。docstringは """ で囲んだ文字列の内側にMarkdown形式で記述し、定義の直前に置きます[*16](c6XJ.xhtml#a9GH)。docstringの具体例をリスト16.5に示します。

![](image_rsrcBJG.jpg)

関数のdocstringには標準的なスタイルがあります。docstringはまず呼出し方を要約したコードで始めます。このコードは空白文字4つでインデントします。省略可能な引数があれば f(x[, y]) のように角括弧でくくります。次に、関数の動作を命令形で簡潔に説明します。動作の追加説明が必要であれば、以降のパラグラフに続けます。例えば、各引数の詳しい説明が必要なら、次のように # Arguments の後に引数のリストを続けます。

![](image_rsrcBJH.jpg)

docstringには使用例を示すと親切です。使用例はリスト16.5にも示したように、# Examples の後にREPLでの実行結果を貼り付けます。jldoctest に記述した使用例は doctest と呼ばれ、結果に間違いがないかが後述するドキュメント生成時にDocumenter.jlによってテストされます。

以上がdocstringの概要です。型や関数だけでなく、マクロ・モジュール・定数などにも同様にしてdocstringを付けられます。一般に、ユーザが使うAPIにはすべてdocstringを付けることが推奨されます。より詳しい書き方は、Juliaのドキュメント[*17](c6XJ.xhtml#a9FV)を参照してください。

# Documenter.jl

パッケージの機能が非常にシンプルであれば、docstringとREADMEファイルに簡単な使い方を書いておけば十分でしょう。しかし、広く使ってもらえるパッケージを作るには、詳しいチュートリアルや使用例なども含めて記述するべきです。すると、READMEファイル1つにすべてを記述するのは無理が出てきます。そこで、ドキュメント作成を支援するDocumenter.jlパッケージの力を借ります。このパッケージは次のような機能を提供します。

●MarkdownファイルからHTMLやPDF形式のドキュメントを生成

●docstringに書かれたdoctestを実行

●リンクの相互参照などを解決

●ドキュメントをGitHub Pagesなどにデプロイ

Documenter.jlの機能は多岐にわたるので、本書では詳しく説明できませんが、基本的な使い方は簡単です。なお、本書では執筆時点での最新版であるバージョン0.27を使用しました。

[16.3.1項](c6XJ.xhtml#aA6S)で使用したテンプレートでは、Documenter.jlを使用する準備が整っています。docs/make.jl を実行すると、docs/build 以下にHTML形式のドキュメントが生成されます。ドキュメントを閲覧するには、次の2つのコマンド[*18](c6XJ.xhtml#aA6T)を実行後、docs/build にある index.html ファイルをウェブブラウザで開いてください。

![](image_rsrcBJJ.jpg)

ドキュメント生成のもととなっているファイルは docs/src にあります。初期状態では index.md ファイルのみがあります。このファイルにはMarkdown形式でドキュメントを記述します。ただし、いくつか拡張構文が使われているので、詳しくはDocumenter.jlのドキュメント[*19](c6XJ.xhtml#aA7V)を参照してください。

本項の変更もコミットしておきます。

![](image_rsrcBJK.jpg)

# 16.3.6 パッケージの公開

ソースコードとテストを書き、ドキュメントを揃えたら、パッケージを公開する準備は完了です。非公式パッケージとしてGitHubなどで公開するだけなら、公開の手続きは簡単です。次のように gh コマンド([付録C.7](c8EH.xhtml#a9V2) を参照)を使ってレポジトリを作成[*20](c6XJ.xhtml#a9V3)し、そこに git push コマンドで変更を反映するだけです。

![](image_rsrcBJM.jpg)

パッケージを公式パッケージとして登録するには、もう少し手続きが必要です。まずは、RegistratorというGitHubアプリをインストール[*21](c6XJ.xhtml#a9GB)してください。このアプリは、パッケージのGeneralレジストリへの登録作業を自動化してくれる便利なアプリです。アプリのインストールが済んだら、GitHub上で @JuliaRegistrator register というコメントを、登録したいリビジョンのコメント欄に書き込みます。通常はこれだけでGeneralレジストリへの登録作業が完了します。リリースしたバージョンのタグをGitHubに作成する作業も、TagBot[*22](c6XJ.xhtml#a9GC)が自動で行います。今後のリリース作業、つまりコードに変更を加えて新しいバージョンをリリースする作業も同様にRegistratorで行えますので、公式パッケージとして登録するならRegistratorの利用を強く推奨します。

# 16.3.7 パッケージ開発の補助ツール

Revise.jlパッケージは、REPLに読み込んだ開発中のパッケージに加えた変更を自動で反映させるツールです。Revise.jlを使えば、REPLを再起動することなく、ソースコードに加えた変更の結果をすばやく確認できます。使い方は、開発中のパッケージを読み込む前に using Revise を実行するだけです。なお、構造体型の変更など、一部の変更はRevise.jlで反映できません。

Debugger.jlパッケージは、Juliaのデバッガ(debugger)を提供します。Debugger.jlの @enter マクロを使って @enter sin(.5) を実行すると、REPLはデバッグモードに入り、コードを1行ずつ実行したり、実行中の関数で変数の値を確認したりできます。詳しい操作方法はパッケージのドキュメントを参照してください。注意点として、デバッガはコードをインタプリタで実行するので、通常の実行に比べて非常に遅くなります。

パッケージの実行に C など他の言語で書かれたライブラリが必要なとき、アーティファクト([16.1.6項](c6XJ.xhtml#a9TK))が非常に役に立ちます。特に、自動生成されるJLLパッケージと呼ばれるパッケージは、アーティファクトをJuliaのパッケージと同じように依存パッケージとして追加でき、ユーザの環境に必要なライブラリを簡単にインストールできます。使い方について詳しくはJLLパッケージのドキュメント[*23](c6XJ.xhtml#aA95)を参照してください。

まとめ

本章では、パッケージの基礎概念と管理・開発の方法を紹介しました。Juliaのパッケージシステムは他言語のツールの成功と失敗を参考にして練り上げられており、非常に完成度が高いです。特に、パッケージの環境を切り替えて運用できる機能は利便性が高いので、よく理解しておくことをおすすめします。パッケージ開発は一般の利用者には関係ないと考えられがちですが、コードをパッケージ化することで再利用性を高められ、管理も楽になります。パッケージ開発はけっして難しくないので、ぜひこの機会に挑戦してみてください。また、もし他人の作ったパッケージにバグを発見したら、それを修正して開発者にフィードバックしてみてください。

注

[*1](c6XJ.xhtml#a9Y1) ピリオドは小数点ではありません。したがって、1.3.1と1.3.10は異なるバージョンです。[*2](c6XJ.xhtml#a9VJ) 性能の改善やバグの修正などは互換性を壊す動作の変更とは考えません。[*3](c6XJ.xhtml#aAB5) <https://pkgdocs.julialang.org/v1/compatibility/>[*4](c6XJ.xhtml#aA1T) Julia を接頭辞として付けて、それぞれ JuliaProject.toml, JuliaManifest.toml としても構いません。ただし、Julia以外が使う設定ファイルと名前が衝突するなどの特別な理由がない限り、これらのファイル名は使いません。[*5](c6XJ.xhtml#a9JR) <https://github.com/JuliaRegistries/General>[*6](c6XJ.xhtml#aAAB) <https://pkgdocs.julialang.org/v1/registries/>[*7](c6XJ.xhtml#aABX) <https://github.com/JuliaPackaging/Yggdrasil>[*8](c6XJ.xhtml#a9ED) ドライブ文字やユーザ名は適宜読み替えてください。[*9](c6XJ.xhtml#aA4F) ディレクトリが存在しなければ、最初にパッケージをインストールしたときなどに生成されます。[*10](c6XJ.xhtml#aA52) . と @. の違いは、プロジェクトファイルを探すとき、前者が現在のディレクトリのみを探すのに対し、後者は現在のディレクトリから親のディレクトリをたどって探します。[*11](c6XJ.xhtml#a9VY) [16.1.3項](c6XJ.xhtml#a9VZ)でJuliaではMAJORが0でMINORが1以上のときはMINORの一致で互換性の有無を決定すると説明したことを思い出してください。[*12](c6XJ.xhtml#a9M9) <https://pkgdocs.julialang.org/v1/compatibility/>[*13](c6XJ.xhtml#aA0F) <https://docs.julialang.org/en/v1/manual/code-loading/>[*14](c6XJ.xhtml#aAF0) Base モジュールはパッケージではないので、依存パッケージとして書く必要はありません。[*15](c6XJ.xhtml#a9Z5) これはリグレッション(regression)と呼ばれます。[*16](c6XJ.xhtml#a9UD) 間に空行やコメントなどを差し込まないように注意してください。なお、必ずしも定義と同じ場所に書かなければならないわけではありません。[*17](c6XJ.xhtml#aA6U) <https://docs.julialang.org/en/v1/manual/documentation/>[*18](c6XJ.xhtml#a9S4) Othello.jlの依存パッケージを変更した直後は、Pkg.jlの resolve コマンドを実行する必要があります。ここでは、Pkg.jlのAPIを使用していますが、docs の環境を有効化し、REPLのパッケージ管理モードで resolve コマンドを実行してももちろん構いません。[*19](c6XJ.xhtml#a9JW) <https://documenter.juliadocs.org/stable/man/syntax/>[*20](c6XJ.xhtml#aA1H) \--private オプションを付けると未公開状態のレポジトリになります。後で公開設定に変更できます。[*21](c6XJ.xhtml#aA18) インストール方法は<https://github.com/JuliaRegistries/Registrator.jl>に記載されています。[*22](c6XJ.xhtml#a9GZ) <https://github.com/JuliaRegistries/TagBot>[*23](c6XJ.xhtml#a9NV) <https://docs.binarybuilder.org/dev/jll/>

---

# ![](image_rsrcBJN.jpg)

ここまでJuliaの基礎とライブラリを紹介してきましたが、本章ではJuliaでプログラムを開発するうえで有用な知識を提供します。こうした知識は必須ではありませんが、よりJuliaらしいコードを書いたり、開発を効率的に進めたりするには非常に有用です。本章の前半では、命名やフォーマッティングを含むコーディングスタイルと、Juliaで避けるべきアンチパターンを扱います。後半では、Julia処理系の動作を理解し、高速なプログラムを書くために重要な実行モデルの知識を解説します。

# 17.1 コーディングスタイル

最初に、本書のコーディングスタイルに対する立場を明らかにしておきましょう。プログラムで1つのことを実現するには、書き方はただひと通りとは限りません。変数の命名や空白の有無などを考慮すると、同じ動作をする書き方は無数にあります。しかし、機械にとっては同じように処理されても、人間にとっては読みやすさの違いがはっきりと存在します。ソースコードは書かれる回数よりも読まれる回数のほうが多いので、ノイズが少なく読みやすいコードを書くことは、他者や将来の自分にとって価値のあることです。コーディングスタイルは、そのための1つの指針を示します。

コーディングスタイルで最も重要なのは一貫性です。すなわち、その場によく馴染むコードを書くべきです。一貫性が理解の妨げになるノイズを減らし、全体の構造から詳細な動作まで把握することを助けてくれます。本章で紹介するガイドラインはその基礎となるものですが、絶対不変の金科玉条ではありません。Juliaのコミュニティで受け入れられているコーディングスタイルを継承しつつ、各プロジェクトでは一貫性のある規則を採用してそれに従うべきです。本章のコーディングスタイルは、Juliaコミュニティで広く採用されているスタイルを基礎としています。

# 17.2 命名に関するガイドライン

ソースコードで使われる変数には、Juliaの構文規則に則っていれば、どのような名前を使っても問題ありません。しかし、Juliaではその変数が何を指すかに応じて、一定の規則に則った名前を使う慣習があります。この慣習を命名規則(naming convention)と呼びます[*1](c79X.xhtml#a9DM)。Juliaの命名規則はJulia固有のものであり、他のプログラミング言語のものとは異なります。また、Juliaコミュニティ内でも完全に一貫しているわけでもありません。それでも、多くの人に受け入れられている命名規則を理解すれば、他者の書いたコードを理解する助けになりますし、自分の書くコードも他者に理解されやすくなるでしょう。以降で説明する命名規則を表17.1に示します。

![](image_rsrcBJP.jpg)

# 17.2.1 変数の命名

まずは、変数一般の命名慣習を説明しましょう。原則として、変数名には name などのように小文字と数字のみを使います。変数名が複数の単語からなるときには、my_name のようにアンダースコアで単語を連結するか、myname のようにアンダースコアを使わずに単語を連結します。my_name のようにアンダースコアを使って小文字の単語を連結する書き方はスネークケース(snake_case)と呼ばれます。アンダースコアを使うかどうかは好みによる部分が大きいのですが、一般にはアンダースコアの使用を避ける傾向にあります。変数名が極端に読みにくくなるときは、アンダースコアを挿入するのがよいでしょう。変数名のみならず、構造体型のフィールド名にも同様の慣習があります。

const キーワードなどで定義された定数には、NAME などのように大文字と数字のみを使います。複数の単語からなるときは、MY_NAME のようにアンダースコアで単語を接続するのが一般的です。標準ライブラリの ARGS や C_NULL などはこうした定数の例です。ただし、この慣習には例外も多くあります。nothing や pi などは定数ですが慣習的に小文字を使っていますし、String や Base などのように型やモジュールには後述する別の命名慣習があります[*2](c79X.xhtml#a9V8)。

科学技術計算のコードでは、行列には英字やギリシャ文字の大文字1文字、スカラーにはギリシャ文字の小文字1文字を使うことも一般的です。例えば、多変量正規分布のパラメータである共分散行列には S や Σ を用い、1次元正規分布のパラメータである標準偏差には σ を用いるようなケースです。これはJuliaの慣習というよりむしろ数学の慣習に則ったものですが、教科書や論文などのアルゴリズムをJuliaで実装するときに対応関係がわかりやすいのでよく使われます。

# 17.2.2 関数とマクロの命名

関数やマクロの命名に関する慣習は基本的に17.2.1項で紹介した変数と同じですが、アンダースコアの使用を避ける傾向が強いです。特に、ユーザが使用する機会の多いAPIとなる関数やマクロでは、アンダースコアはあまり使いません。例えば、標準ライブラリの readline 関数や @macroexpand マクロなどは2単語で構成されていますが、アンダースコアを使っていません。APIにアンダースコアが必要なほど複雑な関数名があるようなら、ライブラリの設計があまりうまくいっていないかもしれません。

関数に特有の命名慣習もあります。まず、関数が引数のオブジェクトを更新あるいは破壊する場合には、関数名の最後に ! を付けます。sort と sort! の対比がこの慣習の好例です。真偽値を返す関数の場合は、is という接頭辞を付けるのが一般的です。例えば、isempty 関数や isfile 関数などです。関数名を is で始めるのが英語として不自然になるときは、適切な動詞を三人称単数現在形で用います。例えば、haskey 関数や occursin 関数などです。それ以外の何らかの操作を伴う関数名には、英語動詞の原形(命令形)を使うのが一般的です。split 関数や findall 関数などがこの例です。ただし、オブジェクトの情報を取得する単純な関数なら length 関数のように名詞をそのまま使うことがよくありますし、数学関数などは exp 関数のように慣習的な名前をそのまま使います。

# 17.2.3 型とモジュールの命名

型やモジュールの名前は、Name などのように英字の大文字で始め、小文字を続けます。名前が複数の単語からなるときは、MyName などのように各単語の最初の文字を大文字にし、それ以外を小文字にします。この書き方は、大文字がラクダのコブのように見えることからキャメルケース(CamelCase)と呼ばれています。標準ライブラリでは、String 型や LinearAlgebra モジュールなどの名前が典型例です。なお、Juliaではパッケージ名とモジュール名を同名にするので、パッケージ名もこの慣習に従います。ただし、公式パッケージにはいくつか追加のガイドラインがあるので、Pkg.jlのドキュメントにある記述を参照してください[*3](c79X.xhtml#a9RC)。

Juliaでは、モジュール内で定義される型などに、そのモジュールと同じ名前を使えません。衝突を回避するには、モジュール名のほうを複数形にし、型名を単数形にする方法が一般的です。Dates.jlパッケージの Dates モジュールと、その中で定義されている Date 型はその好例です。もちろん、衝突の心配がないときにはモジュール名を複数形にする必要はありません。

# 17.2.4 命名に困ったときは

ここまでJuliaの命名に関するガイドラインを示しましたが、実際にプログラミングをしてみるといろいろと迷うこともあるでしょう。ここで示したガイドラインがそのまま適用できないこともあると思います。そのようなときは、Julia本体のソースコードを参考にしましょう。GitHubのレポジトリにあるソースコードは、ブラウザから手軽に参照できます。@less マクロを使えば、ソースコードをREPLで確認することも容易です。Juliaの標準ライブラリのソースコードは多くの人の目を経ていますので、一般的に受け入れられる慣習を知るには重要な資料です。また、広く使われているパッケージのソースコードなども参考になります。

一方で、あまり気にせず進めてしまう勇気も必要です。細かい違いはまず問題にはなりませんし、たいていの決断は後からでも修正できます。繰り返しになりますが、ここで示したガイドラインは唯一絶対の規則ではありません。重要なのは、各プロジェクト内での一貫性です。

# 17.3 フォーマッティング

フォーマッティング(formatting)とは、ソースコードの表面的な配置に関わるスタイルを指します。ここでは、主にJulia本体のガイドライン[*4](c79X.xhtml#aABF)と、BlueStyle[*5](c79X.xhtml#aABG)と呼ばれるコーディングスタイルのガイドラインに則ったフォーマッティングを紹介します。本書のソースコードもおおむねこれらのコーディングスタイルに従っています。なお、本書では使用しませんが、自動でフォーマッティングを行うJuliaFormatter.jlパッケージも開発されています。

# 17.3.1 改行

1行が長いソースコードは、エディタで折返しが発生し読みにくくなりがちなので、適当なところで改行します。目安として、1行を92文字以内に収めることを推奨します。改行文字にはU+000A(LINE FEED, LF)を使います[*6](c79X.xhtml#a9VX)。U+000D(CARRIAGE RETURN, CR)やWindowsで一般的なCR+LFは使いません。また、ファイルの末尾は改行文字で終えます。

# 17.3.2 空白

空白には常に空白文字(U+0020)を用い、タブ文字(U+0009)などは使いません。配列やタプルの要素を区切るコンマ(,)の直後には空白文字を1つ挿入します。それ以外に余計な空白文字は挿入しません。

![](image_rsrcBJR.jpg)

演算子を二項演算子として中置して使うときは、演算子の前後に空白文字を1つ挿入します。一方、単項演算子として使うときや、冪演算(^)やコロン(:)を使う演算子は前後に空白文字を入れないのが通例です。

![](image_rsrcBJS.jpg)

例外的に、コロンの直前や直後にある二項演算子の周囲には、空白文字を挿入しないことがよくあります。また、計算式が長く複雑になるときも空白文字は省略されがちです。例えば、1:n - 1 ではなく 1:n-1 と書いたり、tbl[c - 'a' + 10] を tbl[c-'a'+10] のように詰めて書いたりすることもあります。このあたりのさじ加減は恣意的なので、あまり神経質になりすぎないほうがよいでしょう。

まれに、構文上の曖昧さを回避するため空白文字が必要な場合があります。例えば、3.+2 という書き方は、次の2通りの解釈が可能なので許されていません。

![](image_rsrcBJT.jpg)

また、角括弧の内側では空白文字の有無で結果が変わる場合もあります。

![](image_rsrcBJU.jpg)

# 17.3.3 丸括弧の使用

式をグループ化する丸括弧は、必要ない場所では極力使用しないようにします。例えば、次のようにwhile文やif文の条件式、マクロ呼出しなどでは丸括弧を省略するのが普通です。

![](image_rsrcBJV.jpg)

例外的に、タプルでは丸括弧が不要な場所でも、タプルであることをわかりやすくするために使うことがよくあります。

![](image_rsrcBJW.jpg)

また、演算子の優先順位が複雑な式では丸括弧を意図的に使うこともあります。例えば、x << n + 1 は (x << n) + 1 という意味ですが、x << (n + 1) と誤解される恐れがあるので、わかりやすさのため丸括弧を補ってもよいでしょう。

# 17.3.4 インデント

インデント(indent)はコードの構造を把握しやすくする目的で挿入されます。if や struct など、end と対になるキーワードが作るコードのブロックをインデントします。例外的に、module キーワードではインデントをしません。module と end の間でインデントをすると、非常に広い範囲で無意味なインデントが発生してしまうからです。

インデントには常に4つの空白文字を使います。タブ文字は使いません。適切にインデントされたコードの例をリスト17.1に示します。

![](image_rsrcBJX.jpg)

関数定義や関数呼出しで引数の数が多いときには、すべてを1行で書き切るには長くなりすぎることがあります。そのときは次のように各引数の後に改行を適切に挿入して、1行を複数行に分割するのが普通です。このとき、最後の引数の後にコンマを挿入すると、引数を追加するなどの編集がしやすくなります。

![](image_rsrcBJY.jpg)

# 17.3.5 モジュールの利用

モジュールの提供する機能を利用するには、using文かimport文を使います。これらは1文に複数のモジュールを指定することもできますが、1文につき1つのモジュールを指定するスタイルを推奨します。さらに、次のようにモジュール名の辞書順で上から順に並べます。

![](image_rsrcBJZ.jpg)

using文は using Foo のように書くと、モジュール Foo がエクスポートする名前を暗黙的に取り込みます。この機能はREPLや短いコードで一時的に使うときには便利ですが、規模が大きくなるにつれ、コード中で使われている関数や型の出どころがわかりにくくなる欠点のコストが大きくなります。したがって、REPLや簡単なスクリプトなどでの使用を除けば、using文を使うときには取り込む名前のリストをコロンの後に明示すべきです。

[9.4.1項](c434.xhtml#a9KW)で説明したように、using文とimport文は関数を拡張するときの動作が異なります。import文を安易に使うとコードの可読性を下げてしまいがちなので、常にusing文を使うことを推奨します。import文はusing文を使って次のように書き換えられます。

![](image_rsrcBK0.jpg)

# 17.4 アンチパターン

Juliaには避けるべきアンチパターンがいくつかあります。本節では、そのうち代表的なものを紹介します。

# 17.4.1 不必要に強い型注釈

関数の引数に付ける型注釈は多重ディスパッチに必要な機能ですが、この制約を不必要に強くしてしまうと、関数の汎用性を失うことになってしまいます。特にモジュールのAPIなど、ユーザが使用する関数では、なるべく汎用性を失わないような配慮が必要です。

リスト17.2に示す isin 関数には引数に強い制約が付いていますが、これではかなり利用範囲を制限してしまいます。例えば、isin(3, 0:9) を実行できません。この場合、型注釈を取り払ってしまったほうがよいでしょう。もちろん引数 col がイテレータを実装していないと実行できませんが、それは実行時にわかることです。引数の型注釈は多重ディスパッチには影響しますが、制約を強くしても関数の性能には影響しないことにも注意してください。

![](image_rsrcBK1.jpg)

同様に、部分配列にも適用できる AbstractArray 型のほうが Array 型の型注釈より適切な場合があるでしょう。多くのメソッドは Array 型特有の操作に依存せず実装できるからです。AbstractString 型と String 型の選択でも同様のことがいえます。

一方、関数定義に具体的な型注釈を付けることで、その関数が期待する引数がコードを読む人にわかりやすくなるという利点はあります。自作モジュールの内部のみで使う関数など、あらかじめ引数の型が具体的にわかっており、汎用性が重要でない関数では、読みやすさを優先して強い制約を付けることもあります。型注釈を付けるかどうかは、汎用性と可読性のバランスを考慮しつつ、状況に応じた判断が必要です。

# 17.4.2 戻り値の型が不安定な関数

一般に、引数の型が同じであれば、関数が返す値の型も常に同じ型であるべきです。引数の値によって関数が返す値の型が変わるのはユーザにとって混乱のもとですし、性能にも悪影響を与える可能性があります。特に、return キーワードを使って複数の場所から返りうる関数定義では、このようなことが起きやすいので注意してください。

この問題がよりわかりにくいケースもあります。リスト17.3を見てください。Juliaに慣れていないとわかりづらいですが、この関数は引数 xs が空かどうかで戻り値の型が変わる可能性があります。なぜなら、Int 型の 0 で初期化された変数 s の型は、s += x で昇格([4.7.2項](c1JX.xhtml#a9UJ))が起きると別の型に変わりうるからです。

![](image_rsrcBK2.jpg)

例えば、同じ Vector{Float64} 型の引数を渡しても、次のようにベクトルが空かどうかで戻り値の型が変わります。

![](image_rsrcBK3.jpg)

リスト17.3を修正するには、配列 xs の要素の型に応じて s の初期値を変える必要があります。この例以外にも、条件分岐などで実行時のパスが切り替わることで、関数が返す型が不安定になることがあります。こうした関数を診断する方法と具体的な解決策については[17.6節](c79X.xhtml#a995)で扱います。

もちろん、意図があって異なる型を返す関数を定義することはあります。よくあるのは match 関数や findfirst 関数のような、探索するものが見つかればそれを返し、見つからなければ nothing を返すような関数です。こうした関数を定義するときは、その動作を関数のドキュメントに記載すべきです。

# 17.4.3 型の乗っとり

自分が管理していない型と関数に勝手にメソッドを定義してしまうことを型の乗っとり(type piracy)[*7](c79X.xhtml#a9FE)といいます。例えば、次のように Base モジュールの Vector 型に * 関数のメソッドを勝手に定義してしまうのは型の乗っとりの例です。このような定義をしても許されるのは、Vector 型と * 関数を管理しているJulia本体の開発者だけです。

![](image_rsrcBK4.jpg)

Juliaでは動的言語らしく関数にメソッドを追加できますし、既存のメソッドを書き換えることも言語機能のうえでは禁止されていません。また、既存の関数に自分が管理する型のメソッドを定義したり、既存の型に自分が管理する関数のメソッドを定義したりすることは、これまでにも紹介してきたように何も問題ありません。つまり、型か関数の少なくとも一方が自分で管理しているものであれば、そこに新しいメソッドを定義しても型の乗っとりにはあたりません。しかし、先の例のように他所で定義された型に他所で定義された関数のメソッドを新しく定義したり再定義したりすることは、その型と関数の開発者の意図しない動作を誘発し、さまざまな問題を引き起こすことにつながります。型の乗っとりは影響の範囲が予想しにくく、思わぬ誤作動を生む原因になりますので、開発中の一時的なものを除けば避けるべきです。

# 17.4.4 一貫性のない引数の順序

関数がとる引数の順序に一貫性があれば、余計なトラブルを避けられます。Juliaでは、標準ライブラリにある関数と一貫性のある引数の順序が推奨[*8](c79X.xhtml#aA98)されています。大まかなガイドラインとして、引数には次に示す優先順位があります。上の引数ほど優先度が高く、最初のほうに置きます。

![](image_rsrcBK5.jpg)

実際にはガイドラインだけでは判断が難しい引数の順序もありますが、そのときは標準ライブラリにある似た機能の関数を参考にしましょう。標準ライブラリの関数を拡張して新しいメソッドを定義するときには、その関数の引数の順序を最大限尊重しましょう。

# 17.5 実行モデル

C やRustなどのプログラミング言語では、処理系がソースコードをマシンコードまでコンパイルし、あらかじめ実行可能ファイルを作ってから実行します[*9](c79X.xhtml#aA91)。対して、R やPythonなどのプログラミング言語では、ソースコードの予備的なコンパイルを必要とせず、マシンコードより高レベルなコードを仮想マシンで実行します。ユーザの視点では、R やPythonの処理系はソースコードを直接実行しているようにも見えます。Juliaの処理系は、予備的なコンパイルを必要としない点は R やPythonの処理系に似ていますが、マシンコードまで実行時にコンパイルできる点は C やRustに似ています。こうした特殊な事情から、他のプログラミング言語に慣れ親しんだ人にとって、Juliaの動作は奇妙に感じられるところもあります。

本節では、ユーザが処理系に持つ理解(メンタルモデル)と実際の動作のギャップを埋めるために、Juliaの実行モデルを概説します。Juliaのソースコードがマシンコードまでコンパイルされて実行される仕組みは複雑なので、本節の解説はかなりテクニカルな内容を含んでいます。このような仕組みを知らなくてもJuliaのプログラミングはできるので、詳しく知る必要はないと考えるかもしれません。半分はそのとおりなのですが、一方でJuliaの挙動を理解し、性能を最大限引き出すプログラムを書くには、その仕組みを理解することが実際のところ近道なのです。

# 17.5.1 ソースコードからマシンコードまでの表現

以降の説明を見通しよくするため、まずはコンパイルで使われるさまざまなコードの表現(representation)について概説しましょう。Juliaの処理系が行うコンパイルとは、REPLやスクリプトから受けとったソースコードを計算機で実行できるマシンコードまで変換する処理です。このとき、ソースコードはマシンコードへ直接変換されるのではなく、複数の中間表現(intermediate representation, IR)を経由して変換されます。本書では、ソースコードとマシンコードの間にある中間表現を含め、プログラムの実行に必要な情報の断片をコードと呼びます。

図17.1にソースコードから中間表現を経てマシンコードまで変換される流れを示しました。各コードは、それぞれ変換前のコードを実行するために必要なすべての情報を持っています。言い換えれば、各段階のコードはそれぞれ前段階のコードとは独立しており、前段階のコードを参照せずにコンパイルを完了できます。

![](image_rsrcBK6.jpg)

図中のコードは、上から下に向けて徐々に抽象度が低くなっています。図の一番上に位置するソースコードは最も抽象度が高く、動作の詳細は未定です。例えば、x * y というソースコードの * 関数が整数の積なのか、行列積なのか、あるいは文字列結合なのかといったことは、この段階ではまだ決まっていません。一方、図の反対側に位置するマシンコードは最も抽象度が低く、CPUがどの命令をどの順序で実行するかが細かく決まっています[*10](c79X.xhtml#aA7R)。x * y というソースコードは、マシンコードまで変換されると整数積を計算するCPUの命令1つに変換されているかもしれませんし、もっと複雑な命令列に変換されているかもしれません。

あるソースコードがどのようなマシンコードにコンパイルされるかは、そのソースコードが置かれた状況、すなわち文脈によって決まります。ソースコードとマシンコードの間にある中間表現は、コードを解析して文脈を補強し、最終的に高速なマシンコードを生成する手助けをします。

図17.1に示した各コードの概要を上から順に説明しましょう。ここではコンパイルの大まかな流れがわかれば十分ですから、細かいところまで気にする必要はありません。後ほど具体例を交えて説明します。

ソースコード(source code)

REPLやスクリプトに人の手で記述する[*11](c79X.xhtml#aAAM)コードです。ソースコードの実体は、UTF-8でエンコーディングされたテキストです。

抽象構文木(abstract syntax tree)

ソースコードを構文解析すると、抽象構文木になります。ソースコード中の空白や糖衣構文など、実行に必要ない情報はここで落とされます。[第10章](c4G7.xhtml)で説明したように、抽象構文木はメタプログラミングの主要な操作対象です。抽象構文木の実体は Expr 型のオブジェクト[*12](c79X.xhtml#a9TX)です。

低レベル中間表現(lowered code)

抽象構文木にある高レベルな構文要素を低レベルな構文要素に変換したものが低レベル中間表現です。デバッガなどで使われるインタプリタには、低レベル中間表現を実行するものがあります。低レベル中間表現の実体として、Core モジュールの CodeInfo という型のオブジェクトがJulia処理系の内部で使われます。

型付き中間表現(typed code)

低レベル中間表現の上で型推論を行い、コード中の要素に型情報を付与したものが型付き中間表現です。型に関する情報は後の強力な最適化を可能にします。実体は低レベル中間表現に型情報を付与したオブジェクトです。

LLVM中間表現(LLVM IR)

LLVM[*13](c79X.xhtml#a9WA)とは、コンパイラを作るためのフレームワークの1つです。そのLLVMの中核を担うアーキテクチャ非依存の中間表現がLLVM中間表現であり、Juliaに限らずClang[*14](c79X.xhtml#a9WB)など、さまざまな処理系で利用されています。LLVMはLLVM中間表現を対象としたさまざまな解析や最適化を実装しています。Juliaの型付き中間表現はLLVMの提供するAPIを使ってLLVM中間表現へと変換されます。

マシンコード(machine code)

CPUで実行されるコードです。LLVM中間表現からマシンコードへはLLVMの一連のプログラムを使って変換されます。マシンコードはJuliaを実行するCPUのアーキテクチャによって大きく異なるので、LLVMは実行するCPUに応じて生成するマシンコードを切り替えます。

# 17.5.2 ソースコードが実行されるまで

コードがどう変換されるかを、具体例を使ってより詳しく見ていきます。ユーザは中間表現の意味を完璧に理解する必要はなく、大まかなコンパイルの様子がわかれば大丈夫です。なお、生成されるコードは、プラットフォームやJuliaのバージョンによって詳細が異なることに注意してください。ここでは、Linux用のJulia 1.7.2の結果を例として示しています。

# 構文解析

構文解析([10.1.2項](c4G7.xhtml#a9SY))とは、入力として与えられたテキストを分析・分解し、抽象構文木へと変換する処理です。ソースコードにある細かな相違は、構文解析の過程で消えてなくなります。例えば、for x in iter と書くか for x = iter と書くか、それとも for x ∈ iter と書くかといった違いは、構文解析で消えて抽象構文木には残らないので、動作にも影響を与えません。空白の多寡も同様です。ほかにも、演算の優先順位を示す括弧は、入れ子構造を表現できる抽象構文木では不要なので落とされます。

構文解析では、プログラマから与えられたソースコードが構文的に正しいかだけが検証されます。例えば、開き括弧と閉じ括弧が正しくペアになっているか、using キーワードの後にモジュール名が続くかなどです。構文的に正しいことは、そのコードを問題なく実行できることを意味しません。未定義の変数を使ったり、インストールされていないパッケージのモジュールを読み込むコードを書いたりしても、構文解析ではそのエラーを検出できません。

具体例として、リスト17.4の loop 関数[*15](c79X.xhtml#a9X8)の構文解析の結果(抽象構文木)を次に示します。[10.1.2項](c4G7.xhtml#a9SY)で説明したように、Meta モジュールの parse 関数は構文解析を行います。出力がやや長いのでここでは適当に省略していますが、手元でソースコードと抽象構文木の対応を確認してください。

![](image_rsrcBK7.jpg)

![](image_rsrcBK8.jpg)

# 低レベル化

低レベル化(lowering)[*16](c79X.xhtml#a9XT)とは、抽象構文木にある高レベルな構文要素を低レベルな構文要素へと変換する処理です。例えば、低レベル化の後ではマクロ呼出しは展開され、for文などのループは分岐とジャンプを使った命令列に書き換えられています。前段階の構文解析と低レベル化を合わせてフロントエンド(frontend)と呼ぶことがあります。

低レベル中間表現を少しのぞいてみましょう。関数を低レベル化した結果は、標準ライブラリにあるInteractiveUtils.jlパッケージ[*17](c79X.xhtml#a9YZ)の code_lowered 関数で得られます[*18](c79X.xhtml#a9Z0)。この関数は、対象の関数と引数の型を受けとり、低レベル中間表現のオブジェクトを返します。REPLで次のように入力してみましょう。

![](image_rsrcBK9.jpg)

この中間表現は一番上の s = 0.0 から一番下の return %20 まで21個の命令で構成されています。各命令は代入・関数呼出し・ジャンプ(goto)・リターン(return)などの単純な命令です。このコードには、元のソースコードにはなかった @ や % などが付いた変数名が現れていますが、これらは抽象構文木から変換するときにコンパイラが挿入するローカル変数です。

コードの左側には、1から4までの番号が付いた区画(ブロック)が表示されているのがわかります。例えば、1番目のブロックは s = 0.0 から goto #4 if not %5 までの6個の命令で構成されています。goto の直後にある # がついた数字は、ジャンプ先のブロック番号を示します。低レベル中間表現の各ブロックはベーシックブロック(basic block)と呼ばれ、ブロックの途中から始めたり途中で抜けたりすることなく、最初の命令から最後の命令までまとめて実行されるという特徴があります。つまり、goto 命令がブロック先頭の命令以外にジャンプすることはなく、goto や return などのブロックを抜ける命令はベーシックブロックの途中には登場しません。

# 型推論と最適化

低レベル中間表現に変換されたコードは、次に型推論と最適化の処理を受けます。code_typed 関数はその結果である型付き中間表現を表示します。

![](image_rsrcBKA.jpg)

型付き中間表現は前段階の低レベル中間表現と似ていますが、重要な違いが2つあります。まず、ほとんどの命令に :: で示された型注釈が付与されています。これは、低レベル中間表現に対するコードの解析(型推論)の結果、付与された情報です。型推論器は、低レベル中間表現の上で抽象解釈(abstract interpretation)として知られる解析を行い、引数の型や呼び出す関数などのコードの文脈から各命令が返す型を推論します。型推論については17.6節で改めて取り上げます。

次に、コードの表現が静的単一代入(static single-assignment, SSA)形式と呼ばれる形式に変換されています。SSA形式はJuliaに限らず多くのコンパイラで中間表現の形式として使われており[*19](c79X.xhtml#aA8F)、最適化に適した形式だと考えられています。SSA形式では変数への代入は1回きりで、先ほどの低レベル中間表現のように変数への再代入はしません。この制約の結果、型付き中間表現には![](image_rsrcBKB.jpg)(ファイ)で示される新しい種類の命令が必要になります。この命令は、そのまま![](image_rsrcBKC.jpg)ノードと呼ばれ、どのブロックから制御が移されたかによって返す値が変わります。例えば、最後のブロックの先頭にある![](image_rsrcBKB.jpg)(#8 => %20, #4 => 0.0) と書かれた命令は、第8ブロックから現在のブロックにきたときには変数 %20 の値を返し、第4ブロックからきたときには 0.0 を返すという意味です。

型推論器による解析の後、いくつか基本的な最適化が行われます。定数畳み込み(constant folding)・関数インライン化(function inlining)・デッドコード削除(dead code elimination)などは、こうした型情報を使う最適化です[*20](c79X.xhtml#aAAZ)。

型付き中間表現には![](image_rsrcBKC.jpg)ノードのほか、πノードなどいくつか特殊なノードがあります。詳細はJuliaのドキュメント[*21](c79X.xhtml#aA3S)を参照してください。

# LLVMコード生成

型付き中間表現は続いてLLVM中間表現に変換されます。LLVM中間表現は、型付き中間表現と同じSSA形式の中間表現です。また、LLVM中間表現は特定のプログラミング言語に依存しない中間表現であり、C の clang やRustの rustc など、さまざまな言語のコンパイラで使われています。

Juliaを含むさまざまなコンパイラがLLVM中間表現を使うのには理由があります。コンパイラ基盤ライブラリであるLLVMには、LLVM中間表現のコードを分析したり変換したりするパス(pass)という機構が実装されています。パスの開発には現代のコンパイラ技術の粋が結集されており、LLVM中間表現を経由すれば少ない労力でその恩恵を受けられます。つまり、Juliaのような後発の言語でも、最先端のコンパイラ技術を活用できるというわけです。

あるメソッドのLLVM中間表現は code_llvm 関数で表示できます。このマクロはデフォルトでLLVMのパスが最適化したLLVM中間表現を表示します。次の例では余計なデバッグ情報を非表示にするために、debuginfo オプションに :none を渡しています。

![](image_rsrcBKD.jpg)

LLVM中間表現の構造は、型付き中間表現の構造と同じです。つまり、あるメソッドのLLVM中間表現は1つ以上のベーシックブロックから構成され、各ベーシックブロックは1つ以上の命令から構成されます。上のメソッドは LABEL: で示された4つのベーシックブロックから構成されています。

LLVM中間表現で使われる命令はLLVM独自のものです。命令の形式はそれぞれ異なりますが、基本的には引数をとり結果を返す関数に似た命令か、分岐やリターンなどプログラムの実行を制御する命令です。例えば、最初の icmp sgt i64 %0, 0 は関数の引数 %0(元の引数 n に対応)と整数 0 を比較する命令 icmp(integer compare)です。命令名のすぐ後にある sgt はsigned greater thanの短縮であり、引数の値を符号付きで比較し、第1引数が第2引数より大きければ真の値を返します。また、br 命令は、比較命令の結果を受けて、2つのベーシックブロックのいずれかに分岐します。本書ではこれ以上LLVMの命令を説明しませんので、詳細を知りたい方はLLVMのドキュメント[*22](c79X.xhtml#a9HW)を参照してください。

# マシンコード生成

LLVM中間表現は、LLVMによって最終的に実行環境に対応したマシンコードへと変換されます。次の例は、LinuxのAMD64で生成したマシンコードです。code_native 関数は次のようにマシンコードを表示します[*23](c79X.xhtml#aA5Z) [*24](c79X.xhtml#aA60)。なお、AppleのM1チップなど、Apple siliconの計算機では異なるマシンコードが生成されます。

![](image_rsrcBKE.jpg)

![](image_rsrcBKF.jpg)

マシンコードの説明はここではしませんが、ソースコードが最終的に40個程度の基本的な命令[*25](c79X.xhtml#a9PF)からなる、非常に簡潔な命令列にコンパイルされていることがわかります。Pythonなどの仮想マシンを使った実行では、ずっと多くの命令をCPUで実行することになるでしょう。

# 17.6 型推論

17.5節で説明したように、Juliaのコンパイラはコード生成の過程で型推論(type inference)を行います。この型推論で型がうまく推論できるかどうかが、その後の最適化の可否を大きく左右し、したがって性能に大きな影響を与えます。ここで型推論の原理を理解していきましょう。

# 17.6.1 型推論の概要

Juliaの処理系は、例えば 2 + 3 という式の計算結果が Int 型になることを実際の計算をせずに推論できます。これは、2つの引数 2 と 3 がともに Int 型のリテラルであり、+ 関数の +(::Int,::Int) メソッド[*26](c79X.xhtml#aA0U)が Int 型を返すように定義されているからにほかなりません。ここでは、この推論結果の型を→の後に書いて Int + Int → Int と略記することにします。

2 + 3.0 ではどうでしょうか。こちらも、計算結果の型は Float64 になるとうまく推論できます。理由は先ほどと同じで、+ 関数に Int 型と Float64 型の引数を受けとって昇格の結果 Float64 型の値を返すメソッド Int + Float64→ Float64 が1つだけ定義されているからです。では、2 + (rand() < 0.5 ? 3 : 3.0) はどうでしょうか。この式の戻り値の型は1つに定まりません。もし rand 関数が0.5未満の値を返せば Int + Int → Int メソッドが呼び出され、0.5以上の値を返せば Int + Float64 → Float64 メソッドが呼び出されるので、式全体として Int 型か Float64 型か決定できないことが理由です。このとき、コンパイラは仕方がないので、この式の型を Union{Int, Float64} と推論します。Juliaの世界では、このように式の型が1つに決定できない状況を型の不安定性(type instability)と呼んでいます。型の不安定性はしばしばユーザの混乱や性能の劣化につながります。

型の不安定性は、その式のみならず、他の式にも伝播します。例えば、式の結果を変数 x に代入し、続けて x * 4 という計算をしたとすると、x → Union{Int, Float64} ですから x * 4 → Union{Int, Float64} と推論されます。したがって、次の関数 f の戻り値の型は不安定になります。

![](image_rsrcBKG.jpg)

もし関数の戻り値の型に不安定性があれば、それは関数の外にも伝播します。例えば、[f() for _ in 0:9] → Vector{Union{Int, Float64}} と推論され、配列の要素の型が不安定になります。

# 17.6.2 型の不安定性の診断

型の不安定性は、InteractiveUtils.jlの code_warntype 関数を使って容易に診断できます。使い方は code_typed 関数などと同様です。前述の f 関数を code_warntype 関数に渡すと、次のように型推論の結果を表示します。色表示が可能なターミナルで表示すれば、型が不安定な式が赤字で強調されているはずです。特に、ここでは関数本体を示す Body の型注釈に注目してください。

![](image_rsrcBKH.jpg)

より複雑なコードで型の不安定性を診断するときには、外部パッケージのJET.jlやCthulhu.jlが便利です。詳しい説明はパッケージのドキュメントに譲りますが、診断を再帰的に行えるツールを提供しています。

# 17.6.3 型の不安定性が生じやすいケース

実際のコードで型の不安定性が生じやすいケースをいくつか解説します。

# 定数でないグローバル変数

定数でないグローバル変数を使うと、型を推論できない式が簡単に作れてしまいます。定数でないグローバル変数はいつでもどこからでも値を更新できるので、変数の型を1つに決定できないからです。例えば、次の正規分布の確率密度関数 pdf は、2つのパラメータμとσが定数でないグローバル変数なので、戻り値の型が推論できません。

![](image_rsrcBKJ.jpg)

試しに @code_warntype マクロで推論された戻り値の型を確認してみると、Any になってしまっています。これは、戻り値の型がまったくわからない最悪の状態です。

![](image_rsrcBKK.jpg)

1つの解決方法は、const キーワードを付けてグローバル変数を定数([7.2.3項](c315.xhtml#a9PY))として定義する方法です。これは数学定数や物理定数など、プログラム中で変化することがない変数などには有効な方法です。また、Julia 1.8以降ではグローバル変数に型注釈を付ける解決策もあります。

よりよい解決方法は、pdf 関数を修正して、次のようにグローバル変数を関数の引数にしてしまう方法です。こうすれば、定数でないグローバル変数への参照はなくなるので、関数の戻り値の型を決定できます。

![](image_rsrcBKM.jpg)

この変更をした後では、戻り値の型は期待どおりに Float64 と推論されるようになります。

![](image_rsrcBKN.jpg)

# 型が不安定なローカル変数

変数の型が不安定になりうるのは、グローバル変数を使う場合だけではありません。ローカル変数も使い方によっては型が不安定になってしまいます。よくあるのが、次のような昇格に起因するパターンです。関数 f は引数 flag によってローカル変数 x のビットを立てるか否かを切り替えています。

![](image_rsrcBKP.jpg)

このとき、昇格により UInt32 | Int → Int と推論されるので、flag が false のときは x の型は UInt32 のままですが、true だと Int 型になってしまいます。

![](image_rsrcBKR.jpg)

この例で問題を解決する方法は2つあります。1つ目は、x |= 1 << 4 ではなく x |= UInt32(1) << 4 と書くことで昇格が発生しないようにする方法です。2つ目は、x を定義するとき、x::UInt32 = 0 のように型注釈を付けて特定の型を強制する方法です。こうすると、昇格が発生しても x の型は元の UInt32 に引き戻されます。

ローカル変数を初期化するとき、その型が引数の型に依存するときは、typeof 関数や eltype 関数を使って引数から型を取り出すのが一般的なやり方です。典型例として、[17.4.2項](c79X.xhtml#a9C9)で扱ったリスト17.3の sum_unstable 関数を思い出してください。合計値を保持する変数 s を s = 0 と初期化するのではなく、リスト17.5のように eltype 関数と zero 関数を使って要素の型と同一になるようにします。こうすれば、配列 xs の要素の型が決まっているとき、s の型がループの途中で変わることはありません。

![](image_rsrcBKS.jpg)

いずれにせよ、実行されるパスによってローカル変数の型が変化しないように注意が必要です。

# 要素の型が不明なコレクション

コレクションの要素の型があらかじめわからないと、そのコレクションから取り出したオブジェクトの型はコンパイラにはわかりません。例えば、次の関数では配列に引数 x を push! で入れ、直後に pop! で取り出しているのですが、その取り出したオブジェクトの型はJulia 1.7には推論できません。これは、[] が Vector{Any} 型の配列を構築するので、stack が要素の型の不明な配列になってしまうことが原因です。辞書やチャネルなど、他のコレクションでも同様の問題は発生しえます。

![](image_rsrcBKT.jpg)

この問題はコレクションの初期化時に要素の型を明示することで解決します。例えば、先の例で [] の代わりに typeof(x)[] と書けば、配列の要素は x のオブジェクトと同じ型に初期化されるので、型の不安定性はなくなります。辞書などを初期化するときにも、可能であれば Dict{String, Int} のように具体的な型をパラメータに代入しましょう。

# 型が決まらない構造体型のフィールド

構造体型のフィールドに付ける型注釈を抽象型にしてしまうと、コレクションの場合と同様に、そこから取り出した値の型が決定できません。例えば、複素数を表現する型を次のように定義すると、re フィールドと im フィールドの値の型は実行して値を取り出すまでわからないので、コンパイラは効率的なコードを生成できません。

![](image_rsrcBKU.jpg)

単純な解決策は、re::Float64 のようにフィールドの型を決め打ちしてしまうことです。こうすると、フィールドの型は当然1つに定まるので、コンパイラは高度な最適化を施せます。

一方で、フィールドの型を決め打ちしてしまうと、応用範囲は制限されてしまいます。これは汎用的なライブラリなどを実装するときには特に不都合です。型安定性と汎用性を両立するには、次のようにパラメトリック型を使ってフィールドの型をパラメータ化してしまうのがよいでしょう。

![](image_rsrcBKV.jpg)

フィールドが複数の型の値をとりうるときは、ユニオン型([8.3節](c3S5.xhtml#a97D))を使えば型の不安定性を最小限に抑えられます。例えば、値が nothing になりうるフィールドの型注釈は次のようにすればよいでしょう。

![](image_rsrcBKW.jpg)

# 変数の捕捉

クロージャ([7.7.7項](c315.xhtml#a9HD))を使うときにも型の不安定性が生じやすいケースがあります。次のように、外側の関数 f のローカル変数 x を内側の関数 g で書き換えるコードを考えます。

![](image_rsrcBKX.jpg)

関数 g の内側で代入している変数 x は、外側の変数 x と同一の変数([7.2.5項](c315.xhtml#a9TJ)を参照)です。このように、クロージャが使う外側で定義された変数を、捕捉された変数(captured variable)と呼ぶことがあります。捕捉された変数への代入は解析が難しく、型の不安定性が生じることがあります。

次に示すように、Julia 1.7では関数 f の戻り値の型が Int であると推論できません。

![](image_rsrcBKY.jpg)

型注釈を使って変数を x::Int = 0 と定義すると、少なくとも型の推論は可能になります。あるいは、次のように捕捉された変数の代入を回避すると、ボックス化(boxing)というメモリを割り当てる処理が不要になり、より効率的です。

![](image_rsrcBKZ.jpg)

まとめ

本章では、Juliaの開発者が知っておいたほうがよい実践的な知識を紹介しました。命名やフォーマッティングは一貫性のあるコードを書くために重要です。アンチパターンで紹介した不必要に強い型注釈に関しては、強い注釈を付けるほど性能が向上すると誤解しているケースが見受けられます。逆に、型に無頓着なせいで型の不安定性が発生しているケースもよくありますので、本章の内容をよく理解しておいてください。実行モデルと型推論の知識はJuliaの性能を引き出すために重要です。

注

[*1](c79X.xhtml#aA24) 本来強制力のない慣習なので、規則と呼ぶのはやや不適切なように思われますが、慣習に従い規則という言葉を使うことにします。[*2](c79X.xhtml#a9SV) なお、stdin などは実は定数ではないので、大文字ではありません。[*3](c79X.xhtml#a9EY) <https://pkgdocs.julialang.org/dev/creating-packages/>[*4](c79X.xhtml#aAA8) <https://github.com/JuliaLang/julia/blob/master/CONTRIBUTING.md>[*5](c79X.xhtml#aA06) <https://github.com/invenia/BlueStyle>[*6](c79X.xhtml#aAE1) 改行文字はエディタの設定で変更できます。[*7](c79X.xhtml#a9ZC) piracyはいわゆる海賊行為を指す英語です。[*8](c79X.xhtml#a9XD) <https://docs.julialang.org/en/v1/manual/style-guide/#Write-functions-with-argument-orderingsimilar-to-Julia-Base>[*9](c79X.xhtml#aA17) 正確にいえば、これらの特性はプログラミング言語というより処理系の特性です。ここでは各言語の一般的な処理系を想定しています。[*10](c79X.xhtml#a9W2) プログラムの動作を変えない範囲でCPUが命令の実行順序を変えることはあります。これをアウト・オブ・オーダー実行(out-of-order execution)といいます。[*11](c79X.xhtml#aA27) まれにプログラムで生成することもあります。[*12](c79X.xhtml#a9BS) 説明を単純にするため、Expr 型ではない 42 や "foo" などのリテラルは考えません。[*13](c79X.xhtml#aA2X) <https://llvm.org/>[*14](c79X.xhtml#a9E7) <https://clang.llvm.org/>[*15](c79X.xhtml#aA34) ![](image_rsrcBM0.jpg)となるので、n が大きくなるにつれ、この関数の戻り値は円周率に収束します。[*16](c79X.xhtml#a99T) 動詞lower(低くする)の文脈に合う適切な訳語がないので、「レベル」を補って「低レベル化」と訳しています。[*17](c79X.xhtml#a9M8) REPLはInteractiveUtils.jlを自動で読み込みます。[*18](c79X.xhtml#a9DE) 直接引数を与えて呼び出す @code_lowered マクロもあります。以降で登場する code_ から始まる関数も同様です。[*19](c79X.xhtml#aAAR) 例えばGCCでもSSA形式の中間表現が使われています。LLVM中間表現もSSA形式です。[*20](c79X.xhtml#aA86) 定数畳み込みなど一部の最適化は、実際には型推論と同時に行われているようです。[*21](c79X.xhtml#aAFE) <https://docs.julialang.org/en/v1/devdocs/ssair/>[*22](c79X.xhtml#a9J0) <https://llvm.org/docs/LangRef.html>[*23](c79X.xhtml#a9W4) 正確にいえば、マシンコードはバイナリデータであり、code_native 関数が表示するのはマシンコードを逆アセンブル(disassemble)したテキストの表現です。[*24](c79X.xhtml#aACC) デフォルトはAT&T(GAS)構文ですが、Intel構文が好みの方は例のように syntax = :intel オプションを指定してください。[*25](c79X.xhtml#a9YY) sin 関数の実行に必要な命令は除きます。[*26](c79X.xhtml#aAA5) 実際のメソッド定義は型パラメータを使って抽象化されています。

---

# ![](image_rsrcBM1.jpg)

本章では、Juliaプログラムの性能改善に関わる話題を取り上げます。性能を改善する技術は多岐にわたりますが、主に次に挙げる技術を扱います。

●陥りがちなJulia特有の問題

●メモリ割当ての削減

●CPUの特性に関わる高速化

●並列処理による高速化

Juliaは相当の性能に到達できるプログラミング言語ですが、Juliaには初学者が陥りがちなアンチパターンがいくつかあります。Juliaのような自由度の高い動的言語では、そうしたあまりよくないコードでも動いてしまうのですが、少しの修正で劇的に性能が改善することが多くあります。まずは、性能改善に向けた事前準備から説明しましょう。

# 18.1 事前準備

実際にプログラムの性能を計測したり改善に取り組む前に、必要な事前準備について簡単に説明しましょう。これから解説する事前準備は、意味のある計測をしたり作業の手戻りを減らしたりするために重要です。なお、本章で示すベンチマークの具体的な結果は、特に言及がなければ筆者の現在の開発環境[*1](c7N0.xhtml#a9ZR)で実行した結果です。

# 18.1.1 実行環境の用意

プログラムを実行する環境を構築するとき、なるべく性能の計測に影響を与える要素を少なくするには、次の点に気を付けるべきです。

まず、計測する計算機では、その対象プログラム以外はなるべく同時に走らせないようにします。同時に使えるCPUやメモリのリソースは限られていますので、他のプログラムとリソースの取り合いが発生すると、その影響が無視できないほど大きくなる可能性があります。特に、並列処理をするプログラムなどはCPUの計算リソースを占有しやすいので注意が必要です。特定のCPUコアを計測するプログラム以外に使わせたくなければ、Linuxでは cpuset[*2](c7N0.xhtml#a9W3)を使えばCPUのコアを計測するプログラムで独占できます。

次に、性能の計測に影響を与える設定を見直しましょう。OSによっては電源オプションなどで消費電力ではなく性能を優先する設定があります。さらに、近年のCPUは動作周波数を要求に合わせて動的に変更する機能を持っているので、安定したベンチマークをとるにはOSやドライバの設定を確認するとよいでしょう[*3](c7N0.xhtml#a9KP)。

ラップトップであればバッテリ駆動ではなく電源に接続し、性能が安定するようにしましょう。また、長時間計測をするとCPUの温度が危険な温度に達することを防ぐために動作周波数を抑えるサーマルスロットリングが働き、本来の性能を発揮できないこともあるので、特に小型のラップトップなどでは冷却にもある程度気を配る必要があります。

# 18.1.2 プログラムのバージョン管理

実行環境が用意できたら、プログラムのバージョン管理をします。既存のパッケージはすでにGitでバージョン管理されているでしょうから、そのレポジトリを開発用に手元にクローンすれば準備完了です。しかしそれ以外のJuliaプログラムであっても、バージョン管理をすることを強く推奨します。

Gitなどのバージョン管理システムを使えば、変更を加えた後でも任意のコミット(リビジョン)をコマンド1つで復元できます。したがって、もし性能だけを改善したつもりで動作も変わってしまったとしても、バージョン管理システムを使っていればすぐに元に戻してやり直せますが、そうでなければ元の状態に戻すだけで多大な手間がかかるでしょう。また、変更を徐々に加えて性能を改善したときは、各変更がどの程度の効果をもたらしたかを容易に計測できます。Gitを使えば、GitHubにソースコードのバックアップをとったり公開したりするときにもコマンド1つでできますので、いつでもバージョン管理をするようにしましょう。Gitに関しては[付録 C](c8EH.xhtml) に使い方の簡単な説明があります。

# 18.1.3 テストとデータの用意

リファクタリングや性能改善など、プログラムの動作を変えずにソースコードを変更するときには、テストが大変役に立ちます。ソースコードを編集する過程で意図せず動作を変えてしまうリグレッションを早い段階で検知できるからです。テストを簡単に実行できるようにしておけば、計測→プロファイル→編集→テスト→再計測という改善のサイクルを高速に回せますので、コマンド1つでテストを実行し、結果をひと目で確認できるようにしておくべきでしょう。

プログラムがパッケージ化してあるときには[16.3.4項](c6XJ.xhtml#a9JK)で説明したようにテストを用意すればよいですが、スクリプトしかないプログラムでも簡単なテストはできます。単純な例ですが、リスト18.1のようなスクリプトの関数をテストするには、リスト18.2のようなテストスクリプトを用意すればよいでしょう。リスト18.1の最後の行は、スクリプトが直接的に実行されたか、あるいは include 関数などで間接的に読み込まれたかで分岐します。PROGRAM_FILE グローバル変数は julia コマンドに渡されたスクリプト名を保持するので、ファイルパスを返す @__FILE__ マクロと比較すればこのような分岐が可能なわけです。

![](image_rsrcBM2.jpg)

![](image_rsrcBM3.jpg)

![](image_rsrcBM4.jpg)

テストを用意したら、ベンチマークやプロファイルに使うテストデータも用意します。画像処理なら画像ファイル、機械学習アルゴリズムなら訓練データといった具合です。シミュレーションなどは少数のパラメータだけで十分なこともあるかもしれませんが、テストデータやパラメータセットは複数用意することが推奨されます。なぜなら、計算はたいていの場合データの特性に依存して実行速度が変わるので、1つのデータのみを使って実装の改善を行うと、性能がそのデータに特化して他のデータで遅くなってしまう可能性があるからです。こうした過剰適合した最適化を避けるには、現実的なデータを複数使い、それらの平均値などを指標にするのが常道です。

# 18.2 ベンチマーク

本節では、Juliaで書かれたプログラムのベンチマークをする方法を紹介します。プログラムの性能を計測することは、ある意味で性能を改善することより重要です。本節で紹介する内容は、以降のプロファイルや性能改善の基礎になります。

# 18.2.1 経過時間・ユーザCPU時間・システムCPU時間

プログラムの実行時間は、何を計測するかによって異なる定義があります。例えば、後述する time コマンドや hyperfine コマンドを使ってプログラムの実行時間を計測すると、次の3つの時間が表示されます。

経過時間(elapsed time, wall-clock time, real time)

プログラムの終了時刻から開始時刻を引いた差分を経過時間と呼びます。言い換えれば、プログラムの開始から終了まで実際のストップウォッチを使って計測する現実世界の時間です。

ユーザCPU時間(user CPU time)

プログラムがユーザモードで実行された時間の長さをユーザCPU時間と呼びます。ユーザが書いたプログラムのコードがCPU上で実行される時間ともいえます。並列処理などで複数のCPUを使えば、すべてのCPUで実行した時間が合計されます。

システムCPU時間(system CPU time)

プログラムがOSに要求した処理が実行された時間の長さをシステムCPU時間と呼びます。ユーザが書いたプログラムではなく、OSのプログラムのコードがCPU上で実行される時間ともいえます。

これらの3つの時間のうち、特に重要なのは経過時間です。なぜなら、処理の高速化の目的は、さまざまな技術を使ってこの時間を短くすることだからです。本書で実行時間といえば、通常この経過時間を指します。ユーザCPU時間とシステムCPU時間は、プログラムの経過時間を短縮するヒントを得るために参照することもあります。ただし、CPUの利用時間に対して課金されるサービスなどを使う場合は、むしろこちらを最小化したいこともあるでしょう。

# 18.2.2 コマンドラインツール

プログラムの実行時間を計測する標準的なコマンドラインツールは、もちろんJuliaのプログラムでも使えます。例えば、多くの time コマンド[*4](c7N0.xhtml#a9CH)は与えられたコマンドを1回実行し、その実行にかかった時間などを報告します。GNU Timeの time コマンド[*5](c7N0.xhtml#a9CJ)は割り当てられたメモリの最大サイズも報告します。次の例は、GNU Timeの time コマンドで1秒間スリープするだけのプログラムの実行時間を計測する例です[*6](c7N0.xhtml#a9CK)。

![](image_rsrcBM5.jpg)

この結果から、Juliaの起動時間などを含めて、実行時間は1秒より少し長いこと(0:01.10 elapsed)がわかります。また、スリープ処理はCPUのリソースをほとんど使用しないので、ユーザCPU時間(0.13user)とシステムCPU時間(0.50system)は非常に短いこともわかります。

コマンドを複数回実行して実行時間の統計量を得たいなら、hyperfine コマンド[*7](c7N0.xhtml#a9C6)が便利です。hyperfine コマンドを使って、先ほどと同様に1秒間スリープするだけのプログラムの実行時間を計測してみましょう。次のように、Time の後に経過時間(1.105 s)とその標準偏差(0.002 s)が表示されています。

![](image_rsrcBM6.jpg)

Juliaは実行時にコンパイルを行うので、プログラムの実行時間が短すぎるとコンパイルのオーバーヘッドが大きくなりすぎ、プログラムの計測したい部分をうまく計測できないことがあります。また、Juliaの処理系を起動すること自体にも、C などで作成した実行可能ファイルに比べると、無視できないオーバーヘッドが発生します。1つの目安として、数秒以下で終了するようなプログラムであれば、以降で紹介するJulia用の計測ツールを使ったほうがよい場合が多いでしょう。Julia用の計測ツールを使えば、プログラムの狙った部分、例えば関数呼出し1つなどをより正確に計測できます。

# 18.2.3 標準ライブラリのツール

標準ライブラリにあるベンチマークツールを紹介します。前述のコマンドラインツールより細かい粒度の計測ができ、インストール不要でいつでも使えるのが利点です。

@time マクロは与えられた式を実行し、経過時間とメモリ割当て量を報告します。さらに、コンパイルが行われた場合、その時間の割合も表示します。例えば、REPLで10万要素の配列を sort 関数でソートする時間を計測すると、次のようになります[*8](c7N0.xhtml#aA9G)。

![](image_rsrcBM7.jpg)

この結果を見ると、実行時間とメモリ割当てが1回目と2回目で大きく異なることがわかります。これは、処理系が初回の実行時にメソッドをコンパイルするからです。つまり、1回目はコンパイルにかかるコストと実際の実行時間の合計が計測され、2回目以降はコンパイル済みの処理を呼び出すだけなので正味の実行時間が計測されます。1回目の出力にだけ90%を超えるコンパイル時間が表示されていることにも注意してください。

一般的には1回目の処理は無視して2回目以降の計測結果を参考にします。あるいは、次のように1回目は計測せず少量のデータのみを渡してコンパイルさせ、その後でデータ全体を使って計測することもよく行われます。

![](image_rsrcBM8.jpg)

標準ライブラリにあるベンチマークに使えるツールの一覧を表18.1に示します。詳しい動作はドキュメントを参照してください。例えば、@elapsed マクロと @allocated マクロは、次のように計測結果をオブジェクトとして返すので、ベンチマークの出力形式を自分で制御したいときには便利です。

![](image_rsrcBM9.jpg)

![](image_rsrcBMA.jpg)

# 18.2.4 BenchmarkTools.jlパッケージ

@time マクロなどのマクロは、式の評価を一度しか行いません。計測したタイミングで運悪く他のプログラムがCPUを長く使用したり、時間のかかるガベージコレクションが走ったりすると、実行時間はその分だけ長くなります。これでは計測結果が不安定になり、結果の比較には不都合です。そこで役立つのがBenchmarkTools.jlというパッケージです。このパッケージは式を繰り返し実行して計測し、実行時間とメモリ割当ての統計量を評価する便利なツールを提供しています。ここでは、BenchmarkTools.jlのバージョン1.3を使用しています。

# @btime マクロ

BenchmarkTools.jlの提供するマクロで手軽なのが @btime マクロです。このマクロは渡された式を繰り返し実行し、最短の経過時間を報告します。次のように @time マクロと使い方はほとんど同じですが、コンパイルは計測前に勝手に行うので、事前にコンパイルする必要はありません。

![](image_rsrcBMB.jpg)

BenchmarkTools.jlのマクロで時間を計測するときは、$ を使って式を補間できます。補間された式は一度だけ評価されてからベンチマークを開始するので、例えばデータを生成する部分を計測から排除したいときに有用です。実行時間の短い関数をベンチマークするときも、定数でないグローバル変数をそのまま渡すと変数参照のコストが加算されてしまうので、補間するほうがよいでしょう。

![](image_rsrcBMC.jpg)

# @benchmark マクロ

計測結果を詳しく見るときは @benchmark マクロを使います。@benchmark マクロは最短の経過時間を報告するのではなく、計測結果をまとめたオブジェクトを返します。このオブジェクトはREPLでは図18.1のように表示されます[*9](c7N0.xhtml#a9PH)。

![](image_rsrcBMD.jpg)

図18.1の読み方を上から順に解説しましょう。前述したように、BenchmarkTools.jlは実行時間を複数回計測します。最初の行に表示されている samples がその回数です。この例では、1050 samples と表示されているように、1,050回計測されました。さらに、実行時間が極端に短い場合には、計測にかかるオーバーヘッドの影響を低減するため、複数回の評価をまとめて計測します。この例では、1 evaluation と表示されているように、各計測では1回だけ評価をしています。これらの回数はBenchmarkTools.jlが自動で調整しているので、あまり気にする必要はありませんが、ユーザが調整もできます。

続いて、左のカラムには、上から順に実行時間の範囲(最小値と最大値)・中央値・平均と標準偏差が表示されています。同様に、右のカラムには、メモリ管理にかかった時間の割合が表示されています。これらの下には、実行時間のヒストグラムが表示されています。最後の行には、割り当てられたメモリ割当ての量と回数が表示されています。

なお、@benchmark マクロに渡す式があまりに簡単だと、正確に計測できないこともあります。例えば、1 + 2 のような式で計測すると、実行時間が0.02ナノ秒のようにありえないほど速く出ることがあります[*10](c7N0.xhtml#aA3A)。これはコンパイラが簡単な計算を最適化で消去してしまっているせいだと考えられます。

# 反復評価の注意点

@time マクロなどと異なり、@btime マクロや @benchmark マクロは渡された式を繰り返し実行します。したがって、ベンチマークする式がオブジェクトを書き換えるような処理だと、計測ごとに異なるオブジェクトを処理することになり、一貫したベンチマークにはなりません。例えば、次の挿入ソート(InsertionSort)を用いた sort! 関数のベンチマークには間違いがあります。

![](image_rsrcBME.jpg)

このような処理のベンチマークを正しく行うには、2つの点に気を付ける必要があります。1つ目は、計測ごとに新しいオブジェクトを用意することです。@btime マクロと @benchmark マクロは、計測前に毎回実行する処理を設定する setup オプションを用意しています。このオプションに渡された式は計測前に実行されるので、経過時間などには含まれません。2つ目は、サンプルあたりの評価回数を1回に設定することです。実行時間が短く1サンプルあたり複数回の評価が行われてしまうと、やはり一貫した計測にならないので、evals オプションを使って1サンプルあたりの評価回数を1回のみにします。

先の例を、この2点に気を付けて正しく sort! 関数をベンチマークすると次のようになります。先ほどとはベンチマーク結果が大きく異なることに注目してください[*11](c7N0.xhtml#aA37)。

![](image_rsrcBMF.jpg)

# 18.3 プロファイル

プログラムの中で計算に時間がかかる部分はホットスポット(hot spot)と呼ばれます。プログラムのホットスポットを発見するには、プログラムを実行して実行時間やメモリ割当てなどの統計情報を収集するプロファイル(profile)が欠かせません。本節では、Juliaでのプロファイル方法を解説します。

# 18.3.1 実行時間のプロファイル

ソースコードの各位置でどれだけ時間が使われているかをプロファイルするには、標準ライブラリにあるProfile.jlパッケージを使います。このパッケージは、@profile というマクロを提供しており、このマクロに渡された式を1回実行して統計情報を収集します。収集した統計情報はJulia内部のバッファに蓄積され、Profile.print 関数を呼び出すと、その要約が出力されます。リスト18.3に簡単な使用例を示します。

![](image_rsrcBMG.jpg)

リスト18.3を実行すると、図18.2のような出力が得られます。出力の最初の行に書かれているように、各行にはオーバーヘッド・インデント・カウントに続いて、ファイル名と行番号・関数(メソッド)が表示されます。プロファイラの動作とこの結果の読み方はこれから説明します。

![](image_rsrcBMH.jpg)

Profile.jlが提供するプロファイラは、統計的プロファイラ(statistical profiler)というタイプのプロファイラです。この種のプロファイラは、実行中の関数呼出しの連鎖であるスタックトレースを使って、プロファイル中に実行していた関数の場所を記録します。すなわち、定期的にプログラムの実行を中断し、その瞬間に実行中のスタックトレースを記録し、プログラムを再開します。この1回分の記録をスナップショット(snapshot)と呼びます。一定間隔で繰り返しスナップショットをとれば、コードの各位置は費やした実行時間に比例した回数だけ記録されるので、頻繁に記録されている位置がホットスポットになっているというわけです。

図18.2の説明に戻りましょう。最後の行に表示された数値がプロファイルに記録されたスナップショットの数であり、各行のインデントされた数字がスナップショットに記録された回数(カウント)です。したがって、カウントが大きい値を探せば、そこがホットスポットになっているはずです。カウントの直後にはファイル名と行番号が続くので、対応するソースコードの位置はすぐにわかります。例えば、sleeper 関数の3行目(sleeper.jl:3 の行)のカウントは約930です。プロファイラはデフォルト設定で1ミリ秒ごとにスナップショットをとるので、1秒がカウント1000回に相当します。これは3行目で sleep(1) を実行して1秒スリープしていることと整合します[*12](c7N0.xhtml#aA7D)。他の sleep 関数を呼び出している場所も、カウントはスリープ時間に比例しています。また、deepsleeper 関数は自身を再帰的に呼び出すので、スタックトレースが深くなっていることにも注目してください。

プロファイル結果を見るとき、スタックトレースは浅いところ、つまりプロファイル結果の表示では左側ほど、スナップショットは自然と頻繁に記録されることに注意してください。注目すべきなのは、スタックトレースの深いところ、つまり右側でカウントが大きい場所ですが、これは左端に表示されているオーバーヘッドに注目すれば見つけやすいです。オーバーヘッドは、その関数自体で費やされた(他の関数呼出しを排除した)カウントを示します。実用的には、オーバーヘッドを最初に見てホットスポットのあたりを付け、スタックトレースを参考にしながら関数呼出しを上位(左側)へたどっていって、ホットスポットになっている場所を絞っていくのがよいでしょう。

最後にProfile.jlを使ったプロファイルに関する注意点を列挙します。

●プロファイルをする前にリスト18.3のように対象の関数を実行しておくと、コンパイルにかかるコストをプロファイルから排除できます。

●極端に実行時間が短いプログラムでは、スナップショットを十分な回数とれないことがあります。そのときは、ループ内で同じ関数を繰り返し実行するなどして1秒程度は実行されるように調整する必要があります。

●実行時間が長いプログラムをプロファイルすると、スナップショットが内部のバッファに記録しきれないことがあります。そうすると警告が出力されますので、Profile.init 関数を使ってバッファのサイズをあらかじめ大きくとったり、スナップショットを記録する間隔を長くしてください。詳細は関数のドキュメントを参照してください。

●ネストが深くなる再帰関数などの関数呼出しをプロファイルすると、デフォルトの表示方法ではスタックトレースが深くなりすぎて見づらいことがあるので、Profile.print 関数に format = :flat というキーワード引数を渡して表示をフラットにするとよいでしょう。Profile.print にはほかにもいくつか表示をコントロールするオプションがありますので、ドキュメントに一度目を通すことを推奨します。

●独立したプロファイルを複数回行うときは、そのまま連続して @profile マクロを呼び出してプロファイルすると結果が混ざってしまうので、2回目以降のプロファイル前に Profile.clear 関数を呼び出してバッファに蓄積されたスナップショットをいったん消去してください。

●結果をテキストではなくグラフとして可視化したいときは、ProfileView.jlやPProf.jlといったパッケージがあります。

# 18.3.2 メモリ割当てのプロファイル

プログラムの実行にはメモリは欠かせませんが、必要以上のメモリ割当ては実行速度の低下につながります。Juliaに慣れてくると、どこでメモリ割当てが生じているか自然とわかってくるのですが、初めのうちは意図せぬところで不要なメモリを割り当ててしまいがちです。ここでは、メモリがどこでどれだけ割り当てられているかをプロファイルする方法を解説します。

ヒープに割り当てられるメモリの量は、Juliaのメモリプロファイラでプロファイルできます。メモリプロファイラを有効にするには、\--track-allocation オプションを julia コマンドに渡して実行します。すると、メモリプロファイラはプログラムの終了時にコードの各位置で割り当てられたメモリ量の合計をログファイルとしてスクリプトのあるディレクトリに書き出します。これはJuliaの起動時から終了時まで、コンパイルなどを含むすべてのメモリ割当てを記録するので、一部の処理のメモリ割当てのみをプロファイルしたいときは、少し工夫が必要です。

リスト18.4に示すプログラムをメモリプロファイラで見てみましょう。先頭で定義されている allocator 関数は内部で配列を3つ割り当てるだけで、それ以外には何もしません。allocator 関数の処理のみを正確にプロファイルするには、事前に一度関数を呼び出して関数をコンパイルします。これはすでに説明したベンチマークやプロファイルと同じ要領です。次に、Profile.jlの clear_malloc_data 関数を呼び出し、その時点までのメモリ割当ての記録をリセットします。そして、プロファイルしたい処理を実行し、プログラムを終了します。これで、allocator(100) で行われたメモリ割当てのみのプロファイルが完了します。

![](image_rsrcBMJ.jpg)

メモリプロファイラから出力されたファイルは、FILE.PROC.mem というような名前をしています。FILE と PROC はそれぞれファイル名とプロセス番号を指します。プロセス番号は、複数のJuliaプロセスを同時に走らせたときに、出力ファイルを区別するために付けられています。例えば、リスト18.4のプロファイル結果は次のようになります。

![](image_rsrcBMK.jpg)

3つの zeros 関数呼出しの左に、数字が表示されているのがわかるでしょう。この数字が割り当てられたメモリ量(単位はバイト)を表します。配列のサイズは要素のサイズ(今回は Float64 型なので8バイト)と要素の数の積で概算できます。すなわち、3つの関数呼出しは上から順に 8n, 8n2, 8n3 となるわけです。今回は n=100 としたので、割り当てられたメモリ量は概算どおりになっていることがわかるでしょう[*13](c7N0.xhtml#aAAS)。

# 18.3.3 イベントのプロファイル

実行時間とメモリのプロファイル以外に、実行中に発生したイベントのプロファイルもプログラムが遅くなる原因を発見する有効な手段です。ここでいうイベントとは、CPUで実行された分岐命令やカーネルで発生したページフォルトなどを指します。LinuxPerf.jlパッケージは、そうしたイベントの統計情報を収集して報告する機能を提供しています。なお、LinuxPerf.jlは名前のとおりLinuxでしか使えません[*14](c7N0.xhtml#a9DK)のでご注意ください。

LinuxPerf.jlの機能をすべて使うには、perf_event_paranoid パラメータを1に設定する必要があります。デフォルトのパラメータでも多くの場合は問題ありませんが、必要があれば次のようにパラメータを変更してください[*15](c7N0.xhtml#aA6C)。

![](image_rsrcBMM.jpg)

LinuxPerf.jlは @pstats というマクロを提供しています。この名前はperformance statisticsを短縮したものです。@pstats マクロは、渡された式を1回実行し、その間にCPUやカーネルで発生したイベントの回数を記録します。結果の要約は、REPLでは次のように表示されます。

![](image_rsrcBMN.jpg)

デフォルトの設定では、@pstats マクロの出力は3つのグループに分かれています。左端に表示されている┌から└までが1つのグループを意味します。1つのグループは同時に計測された統計情報であり、グループ内での比較が可能です。先の結果では、最初の2つのグループがCPUのイベントであり、最後の1つがカーネルのイベントです。

個別の統計情報を詳しく説明するとかなり長くなってしまうので、ここでは概要のみを簡単に説明します。最初のグループは、CPUのボトルネックがフロントエンドとバックエンド[*16](c7N0.xhtml#a9RM)のどちらにあるかを診断するときに便利です。大雑把にいえば、CPUがストール(stall)したりせず、どれくらい効率的に動いているかがわかります。2番目のグループは、分岐命令の割合や分岐予測のミスを発見するときに役立ちます。最後のグループは、ページフォルトなど、カーネルが捉えた速度低下の原因になりうる要素を発見するときに使えます。なお、@pstats マクロにはいくつか設定項目がありますので、詳細はドキュメントを参照してください。[18.7節](c7N0.xhtml#a99D)でもいくつか使用例を示しています。

# 18.4 コンパイラの活用

Juliaの高速化で一番重要なのは、なんといってもコンパイラの有効活用です。コンパイラの能力を引き出せるコードの書き方をすることが、最も基本的で有効な最適化手法です。実際、型を少し意識してコードを書き換えるだけで、実行速度が何十倍も変わることすらあります。本節では、Juliaが能力を発揮できるコードの書き方を紹介します。

# 18.4.1 型推論と最適化

[17.6節](c79X.xhtml#a995)で説明した型推論、特に型の不安定性は性能に大きな影響を与えます。式に型の不安定性があると、実行時にはどうしても分岐などの余計な処理が必要になるからです。例えば、変数 x の型が Union{Int, Float64} と推論されたとすると、処理系は次のような分岐処理を挟み込む必要があるわけです。単純なユニオン型であれば、いくつかの分岐処理を挿入すればよいので深刻な問題ではないのですが、型が1つに決定できる状況と比べるとコストがかかることは確かです。

![](image_rsrcBMP.jpg)

ユニオン型よりさらに深刻な型の不安定性が発生する状況もあります。場合によっては、式の型がまったくわからず、単純な分岐では対応しきれないので、実行時にメソッドテーブルからメソッドを探索する必要があることもあるでしょう。このような式が何億回と繰り返されるループ内にあったりすると、けっして無視できない性能悪化を引き起こします。

型の不安定性が生じる例を[17.6.3項](c79X.xhtml#a9X6)で紹介したので、実際に性能に与える影響を計測してみましょう。次の例は、配列要素の合計値を計算するときにかかる時間を計測しています。2つの配列は同じ値を保持していますが、一方の要素は Float64 型であり、他方は Any 型です。この結果から、型を決定できないコードは、性能に深刻な悪影響を与えることがわかります。

![](image_rsrcBMR.jpg)

最後に、Juliaでの型推論と最適化の位置付けについて少し補足しておきます。Juliaの型推論は、HaskellやRustといった静的な型付けを行うプログラミング言語の型推論とはかなり違ったものです。静的型付けを行うプログラミング言語では、プログラムのある種の正しさを検証するために型推論を使うのに対し、Juliaではもっぱら最適化のために行います。したがって、前者では型が推論できない式があるとエラーになるのに対し、Juliaでは事前に型がわからなくても実行時にはわかりますから、型推論の時点ではエラーになりません。いわばJuliaの型推論はベストエフォート的な位置付けで、動作に影響を与えたり、何らかの性質を保証したりするためのものではないのです[*17](c7N0.xhtml#a9SS)。また、以降の内容は現在の型推論器の実装に依存する解説なので、今後改善して、これらの工夫が必要なくなる可能性はあります。

# 18.4.2 コンパイラへのヒント

Juliaの処理系は、プログラマがコンパイラにヒントを与えて最適化を支援する機能をいくつか提供しています。

# 型注釈

コンパイラが式の型を推論できなくても、プログラマには明らかなことはあります。そのときは、[7.8.1項](c315.xhtml#a9FS)で説明したように、期待する型 T を使って式の後に ::T と書けば、コンパイラに型を教えることができます。もし実際の型が期待する型と一致しなければ、Juliaは実行時に TypeError 例外を送出します。実用的には、例えば非同期実行されるタスクから fetch 関数で結果を受けとると、Julia 1.7では戻り値の型を推論できないので、プログラマが型を教えてやることは有効です。

![](image_rsrcBMS.jpg)

# 境界チェックの省略

配列などの要素を A[i] のように参照・更新するとき、Juliaは添字 i が配列 A の有効な範囲に収まっているかを確認します。すなわち、i が firstindex(A) と lastindex(A) の間にあるかを確認し、そうでなければ BoundsError 例外を送出します。これは安全のための機能なのですが、分岐が必要になるので多少コスト[*18](c7N0.xhtml#a9K8)がかかります。

そこで @inbounds マクロを使うと、この安全装置を外して、いっそうの最適化を可能にするヒントをコンパイラに与えられます。@inbounds マクロは、次のように参照・更新の式のみに付けることもできますし、for文などの式全体に付けることもできます。for文内で何度も参照・更新するときは、後者のほうが見やすく手軽でしょう。

![](image_rsrcBMT.jpg)

@inbounds マクロを付けると、もし範囲外の参照や更新が起きても、その結果は予測できません。運よくセグメンテーション違反でプログラムが異常終了するかもしれませんし、運悪く初期化されていない値をメモリから拾ってしまい、間違った計算を続けるかもしれません。後者は発見自体が難しいバグになります[*19](c7N0.xhtml#a9SA)。境界チェックの省略はやや危険な機能ですので、@inbounds マクロは乱用せず最小限に留めることを推奨します。

# 関数のインライン化

関数のインライン化(inlining)とは、呼び出される関数(callee)の本体を、関数の呼出し元(caller)に展開して埋め込む最適化を指します。関数の呼出しには、引数と戻り値の受け渡しやレジスタの退避などの無視できないコストがかかりますので、関数をインライン化してしまったほうが処理が高速になることがあります。また、関数の中身を展開して初めて可能になる最適化もあるので、その意味でもインライン化は高速化に寄与します。欠点として、コードのサイズが大きくなる傾向にあります。

コンパイラはインライン化すべき関数を自動で判断してインライン化する機構を持っていますが、プログラマの判断で積極的にインライン化したいこともあります。そのために、標準ライブラリには @inline という関数のインライン化を積極的に行うよう促すマクロが用意されています。@inline マクロを付けて定義された関数(より正確にはメソッド)はインライン化されやすくなります。ただし、再帰関数などそもそもインライン化できない関数などもありますので、インライン化が保証されるわけではありません。このマクロは次のように使います。

![](image_rsrcBMU.jpg)

Julia 1.8以降では、次のように関数の呼出し側からもインライン化を要求できます。

![](image_rsrcBMV.jpg)

@inline マクロとは反対に、インライン化をしないようヒントを出す @noinline というマクロも用意されています。使い方は @inline マクロと同様です。

# 浮動小数点数演算の最適化

浮動小数点数は実数の近似表現ではあるのですが、数学的な実数とは異なる性質を持ちます。有名な例を挙げると、結合法則 (x+y)+z=x+(y+z) は任意の実数で成立しますが、浮動小数点数では一般に成立しません[*20](c7N0.xhtml#a9FF)。加算のみならず、除算や乗算などでも同様のことは発生します。浮動小数点数で計算の順序を入れ替えたり、除算を逆数の乗算で書き換えたりするなどの操作は、計算結果を変えてしまう恐れがあるので、コンパイラの通常の最適化では許されていません。

@fastmath マクロはこの制約を取り払い、浮動小数点数が実数と同じ性質を持つことを仮定して最適化を行えるようにします。したがって、@fastmath マクロを適用したコードと適用しないコードでは、コード自体が同じでも、計算結果が異なる可能性があります。IEEE 754のような浮動小数点数の規格を前提とした誤差解析などは適用できなくなるので、使用には注意が必要です。

# SIMD命令を使った最適化

SIMD(single instruction, multiple data)命令とは、1つの命令で複数のデータを同時に処理する命令です。例えば、4つの浮動小数点数の加算を1つのSIMD命令で行うことで、処理を大幅に高速化できることがあります。この種の命令は最近のCPUにはたいてい実装されています[*21](c7N0.xhtml#a9P2)。

for文に @simd マクロを付けると、コンパイラにSIMD命令を使った最適化を促します。ただし、このマクロは現在実験的なマクロとみなされており、将来のリリースで利用できなくなる可能性もあります。また、@simd マクロを使うにはかなり厳しい制約がありますので、実際に使えるかどうかはドキュメントを参照してください。@simd マクロを使わなくてもSIMD命令を使った最適化を行うこともあるので、むやみに使うのではなく、ベンチマークで効果を確かめてから導入すべきでしょう。

標準の Base.llvmcall 関数とLLVMIRを使ってもSIMD化が可能ですが、やや難易度が高いので本書では扱いません。高レベルなSIMD命令のインタフェースとして、SIMD.jlやLoopVectorization.jlといった外部パッケージがあります。

# 18.5 多重ディスパッチを使ったテクニック

型とメソッドは、それぞれデータ構造とアルゴリズムを決定します。本節では、2つの例を通して、これらの選択が性能に与える影響を考察していきます。

# 18.5.1 パラメータのエンコーディング

3次元空間の座標の点や色空間の色を表現するときなど、配列のサイズが固定されている状況で使える多重ディスパッチを使った重要な高速化のテクニックを紹介しましょう。

型の基本的な構造が同じであっても、固定されたパラメータ(サイズなど)に特化したデータ型を作成すると、高速に実行できるコードが生成されやすくなります。例として、長さが4のベクトルに特化した Vec4 型を作成してみましょう。今回はノルムを計算する norm 関数を取り上げてみます。実装例をリスト18.5に示します。

![](image_rsrcBMW.jpg)

Vector 型と Vec4 型の norm 関数の実行時間を計測してみると、次のように Vec4 型のほうがいくぶん高速なことがわかります。なお、単純な計算ではあまりに実行時間が短いので、sum 関数を使って1,000回分の実行時間をまとめて計測しています。

![](image_rsrcBMX.jpg)

なぜ同じ構造の型でも実行速度に違いが出るのでしょうか。それは Vector 型の norm メソッドがループを使って実装されていることと関係があります。ループは Vector 型ベクトルの要素を順に走査し、最後の要素に達したらループを抜けます。このとき、最後の要素に達したかどうかを毎回判断し、達していなければループの先頭に戻る操作を繰り返す必要があります。一方、Vec4 型ベクトルは長さが固定されているので、このような余計な操作は必要ありません。さらに、SIMD命令を使って処理を並列化する機会も増えると思われます。つまり、余計な処理を省いて特定のサイズに特化した最適化を行えることが、性能上の大きな利点になります。

同様の高速化は他のサイズや関数でも有効ですが、いちいち自分で実装するのは手間です。そこで、StaticArrays.jlパッケージが便利です。このパッケージは、ここで解説した Vec4 型や norm 関数のような機能をすでに実装しています。しかも、配列のサイズをパラメータとして持つパラメトリック型として定義しているので、配列のサイズは要求に応じて自由に設定できます。例えば、StaticArrays.jlの @SVector マクロや @SMatrix マクロを使って配列を作ると、次のように SArray という型の配列が作られます。この型は配列のサイズをパラメータとして型にエンコーディングしているので、そのサイズに最適化されたメソッドを生成可能[*22](c7N0.xhtml#aA5C)なのです。

![](image_rsrcBMY.jpg)

# 18.5.2 特殊な構造を持つ配列

LinearAlgebra.jlが提供する Diagonal 型やSparseArrays.jlが提供する SparseMatrixCSC 型は、標準的な Matrix 型とは異なる構造をとります。Diagonal 型の行列は対角行列に特化した構造を持ち、対角成分以外の要素を陽に保持しません。SparseMatrixCSC 型の行列も同様に非ゼロ要素のみ保持します。

こうした行列の持つ特殊な構造は、アルゴリズムの選択にも関係します。例えば、対角行列 A=(aij) と一般の行列 B=(bij) の行列積 C=(cij)=AB を考えてみましょう。行列 A は対角成分以外はゼロ、すなわち aik=0 (i≠k) なので、行列積の成分 cij を計算するのに必要な項は次のようにほとんど消え、残るのは aii と bij の積のみです[*23](c7N0.xhtml#a9NJ)。

![](image_rsrcBMZ.jpg)

Juliaでは、多重ディスパッチのおかげで、こうした特殊な型に適用できるアルゴリズムをメソッドとしてごく自然に記述できます。これが性能に与える影響を実際に見てみましょう。次の結果が示すように、A * B という書き方はまったく同じなのにもかかわらず、前者は後者に比べて数倍高速でした。

![](image_rsrcBN0.jpg)

こうした特化した型やメソッドは、もちろんユーザ自身でも定義できます。試しに、階数が1以下の行列に特化した型を定義してみましょう。階数が1以下の行列 A は、A=uv' と2つの列ベクトル u, v の積で表現できます。実装例をリスト18.6に示します。

![](image_rsrcBN1.jpg)

Rank1Matrix 型の行列積などのメソッドが、型特有の構造を反映した定義になっていることに注目してください。例えば、行列 A=uv' と一般の行列 B の行列積 AB は、AB=(uv')B=u(B'v)' という式変形を使って、行列 uv' の構築を回避しています。実際、このような工夫をすると、特に大きな行列では、次のように相当の高速化を実現できます。

![](image_rsrcBN2.jpg)

ここでは要点を説明するため階数を1に固定しましたが、同様の考え方で低階数行列に特化した行列型を定義できるでしょう。特異値分解を使って元の行列の低階数近似行列を計算すれば、抽象的なコードを保ちつつ、計算の高速化に役立つかもしれません。

# 18.6 メモリ割当て

プログラムの実行にはメモリ割当てが必要不可欠です。しかし、余計なメモリ割当てはそれ自体が大きなコストになりえますし、キャッシュなどの貴重なリソースを圧迫する原因にもなります。本節では、どのようなときにメモリ割当てが発生し、それをどう回避するかを解説します。

# 18.6.1 いつメモリ割当てが発生するか

オブジェクトを作成するとき、メモリ割当て(memory allocation)が必要になることがあります。メモリ割当てとは、計算に必要な記憶領域(メモリ)を確保することですが、その領域は主に2つの領域に分けられます。1つは、スタック(stack)と呼ばれる領域で、関数呼出しで一時的に使われるメモリ領域です。もう1つはヒープ(heap)と呼ばれる領域で、関数呼出しをまたいでも記憶を保持できるようになっています。スタックに確保されるメモリの量は関数ごとに決まっていて、これを削減する手段は少なく、削減が性能に与える影響もあまりありません。一方、ヒープには巨大な配列や大量の小さなオブジェクトを割り当てることができ、うまく削減できれば大きな性能向上が見込めます。したがって、ここではヒープに割り当てられるメモリの削減に注目することにします。以降、メモリ割当てといったときは、ヒープへのメモリ割当てを指すと考えてください[*24](c7N0.xhtml#a9J3)。

メモリ割当てがいつどれくらい発生するかは、Juliaの経験を積むとある程度わかるようになってきます。例えば、数・文字列のリテラル・タプルといったオブジェクトを作るとき、多くの場合メモリ割当ては発生しませんが、配列のリテラルや文字列の連結などでオブジェクトを作ると、新しいメモリ領域が必要になり、メモリ割当てが発生することがあります。ただし、実際に発生するかはさまざまな要因に依存するので、判断には計測が必要です。

![](image_rsrcBN3.jpg)

あるコードの実行に要したメモリ割当て量は、次のように @time マクロや @btime マクロでベンチマークをとるとわかるので、いろいろなコードを書いてみてベンチマークをしてみると、徐々にメモリ割当ての勘所がつかめてくるでしょう。本節の以降の部分では、典型的なメモリ割当てが発生するケースを回避方法とともに解説をしています。

![](image_rsrcBN4.jpg)

ヒープに割り当てられたオブジェクトのメモリは、そのオブジェクトが不要になるとランタイムによって回収されます。この回収処理をガベージコレクション(garbage collection)と呼びます。ガベージコレクション中はユーザのプログラムを実行できないので、性能低下の原因になりえます。

# 18.6.2 不変な構造体型か可変な構造体型か

Juliaには構造体型を定義するキーワードが2種類あります。不変な構造体型を定義する struct と、可変な構造体型を定義する mutable struct です。どちらもフィールドが不変か可変かという違い以外は同じように振る舞いますが、メモリ割当ての観点から見ると大きな違いが発生することがあります。

リスト18.7を見てみましょう。この例では、非常にシンプルなデータベースを実装しています。Database 型のデータベースは、シリアル番号が付いた Record 型のオブジェクトを管理する型です。

![](image_rsrcBN5.jpg)

このデータベースに100万件のレコードを追加すると、次のような結果になりました。

![](image_rsrcBN6.jpg)

次に、Record 型の struct を mutable struct に変え、再度同じベンチマークをしてみましょう。

![](image_rsrcBN7.jpg)

実行時間が延び、メモリ割当ての回数も量も増えています[*25](c7N0.xhtml#a9KH)。特に、メモリ割当ての回数は約10回から約100万回へと急激に増えています。これはもちろん struct から mutable struct へと変更したことの影響です。

可変な構造体型のオブジェクトが作られるとき、オブジェクトを記録するのに必要なメモリが新しく割り当てられます。したがって、可変な構造体型の場合には、Record 型のオブジェクトへの参照[*26](c7N0.xhtml#aA3M)が配列に置かれ、その参照先に Record オブジェクトの実体を保持するメモリがあるわけです。一方、struct で定義した Record 型は、スタック領域やすでに割り当てられた配列の中に参照を使わず直接割り当てられます[*27](c7N0.xhtml#aA3N)。この様子を図18.3に示します。

![](image_rsrcBN8.jpg)

結論としては、不変な構造体は可変な構造体より軽量なので、可能であればいつでも不変にすべきです。構造体が本当に可変である必要があるときのみ mutable を付けて構造体型を定義しましょう。

なお、Record 型のフィールドが配列に図18.3のように連続的(contiguous)に収められるのは、data フィールドの型が Float64 だからです。文字列や他の可変な(mutable struct で定義された)構造体が data フィールドにあるときはこのようになりません。

# 18.6.3 配列のメモリ割当て削減

配列は科学技術計算で使用する機会が最も多いデータ構造でしょう。配列はサイズも大きくなりがちなので、不用意に使うと多くのメモリを浪費してしまいます。ここでは、配列に関わるメモリ割当て削減のテクニックを紹介します。

# 部分配列

配列の一部に何らかの処理を適用したいことはよくあります。例えば、行列の i 列目から j 列目までの範囲について、列の総和を計算するなどの処理です。そのとき気を付けなければならないのは、配列 A に対して A[:,i:j] と書くと、その時点で i 列目から j 列目までの範囲で A のコピーが作られてしまうということです。

そこで役に立つのが[11.1.9項](c4WA.xhtml#aA9E)で紹介した部分配列です。部分配列は元の配列への参照は保持しますが、コピーは作りません。部分配列自体を作るコストはほとんど無視できるほど小さいので、配列の一部を使う計算で高速化が期待できます。部分配列を手軽に作れる @view マクロを使って、行列 A の左半分の各列の総和を計算する処理を比較してみましょう。

![](image_rsrcBN9.jpg)

このベンチマーク結果のメモリ割当てに注目してください。この結果に現れているように、4メガバイト近く割り当てられていたメモリ使用量が、部分配列を使えば4キロバイト程度まで抑えられ、結果として実行速度も大きく向上しました。この例が示すように、無駄なメモリ割当てを防止することは、特に配列を使った処理では大きな速度向上をもたらすことがあります。

# 破壊的関数

メモリ使用量を減らす標準的な方法は、一度割り当てた配列を何度も使い回すことです。例えば、行列積の計算に使う標準的な関数は * 関数ですが、別に mul! 関数が用意されています。この関数は、行列の引数を3つ受けとり、第2引数と第3引数の行列積を第1引数に書き込みます[*28](c7N0.xhtml#aA3T)。* 関数と異なり配列を新しく割り当てる必要がないので、次のようにまったくメモリを割り当てずに行列積を実行できます。ただし、出力行列は2つの入力行列とメモリを共有してはいけない、例えば mul!(A, A, B) とはできないので気を付けてください。

![](image_rsrcBNA.jpg)

mul! 関数のほか、行列などの配列を破壊的に操作する関数として fill!, map!, sum!, accumulate!, cumsum!, cumprod!, conj!, transpose!, adjoint! などの関数が用意されています。行列分解にも lu! や qr! のように破壊的関数があり、入力として与えられた行列を作業領域として使ってメモリ割当てを削減します。さらに、ldiv! 関数や rdiv! 関数を使えば、行列分解の結果を使って入力行列を破壊しながら線形方程式の解を計算できます。それぞれ![](image_rsrcAGC.jpg)関数と / 関数の破壊版だと考えればよいでしょう。

破壊的関数は、特に反復が必要なアルゴリズムで効果を発揮します。メモリを割り当てずに実行すれば、不要になったオブジェクトのメモリを回収する必要もなくなり、全体としてスループットが向上します。ホットスポットになっているループでメモリ割当てが発生しているときは、破壊的関数で書き換えられないか検討してみるとよいでしょう。

# 融合ブロードキャスト

[11.1.11項](c4WA.xhtml#a9RY)で説明したように、Juliaにはスカラーや配列を拡張して要素ごとに同じ計算を適用するブロードキャスト構文があります。例えば A を行列、b を列ベクトルとして A .+ b を実行すると、A のすべての列に b を要素ごとに加えた新しい行列が返されます。このとき、計算結果を = ではなく .= で受けると、出力配列を新しく割り当てるのではなく、破壊的関数のようにあらかじめ割り当てた配列に書き込めます。

![](image_rsrcBNB.jpg)

同様のことは、α .* A .+ f.(b) のようにブロードキャストを連続して複数の演算に対しても適用できます。つまり、フュージョン([11.1.12項](c4WA.xhtml#a9XA))を使えば、中間的な計算結果を保持する配列の割当てを排して、効率的な要素ごとの計算ができるのです。ただし、フュージョンは途中で融合が切れてしまうと、そこで配列の割当てが発生してしまうかもしれないので注意してください。演算の数が多くなるときは、自動で演算をブロードキャスト化する @. マクロを使うとよいでしょう。

![](image_rsrcBNC.jpg)

# 18.6.4 文字列の分解と結合

文字列の分解と結合についても重要なメモリ削減のテクニックを紹介しておきましょう。部分配列の SubArray 型と同様に、文字列にも部分文字列を表す SubString 型があります。文字列の一部を切りとるような操作では、その部分のコピーを作るのではなく、SubString 型のオブジェクトを作るほうがメモリ割当てが少なくなります。リスト18.8に文字列を2つに分割する split 関数の実装例を示します。前者の split_sub 関数が SubString 型を返し、後者の split_str 関数は String 型を返します。どちらも第1引数の isprefix が最初に false を返すまで文字列 s を走査し、その場所で文字列を分割します。

![](image_rsrcBND.jpg)

文字列をランダムに生成して関数を適用してみると、split_sub 関数のほうが split_str 関数よりメモリ割当てが少ないです。また、結果を示していませんが、実行時間もより安定しています。これは、メモリ割当てが発生しないので、ガベージコレクションなどのメモリ管理が必要ないことが大きな要因です。

![](image_rsrcBNE.jpg)

反対に、文字列を結合するとき、join 関数や string 関数を1回だけ呼び出して目的の文字列を作るのは問題ないのですが、文字列を徐々に結合する方法は大量の中間産物を作ることになり非効率的です。文字列を徐々に構築する必要があるときは、[6.4.6項](c2FB.xhtml#a9EX)で説明したように、IOBuffer 型のバッファを使って構築しましょう。ストリームに出力することが目的であれば、文字列をまったく作らずに出力先のストリームに直接書き出してしまうのも効率的です。

# 18.7 CPUの性能を引き出す

プログラムのコードは、計算機の心臓部であるCPU(central processing unit)で実行されます。現代のCPUには高速化のためのさまざまな工夫が施されており、その特性に逆らわないコードを書けば、自然とプログラムの性能向上につながります。本節では、そうしたCPUの特性を紹介しながら、Juliaで使える高速化の手法を解説します。

# 18.7.1 キャッシュ

CPUがメモリから命令やデータを取得する速度と比較すると、現代のCPUの計算速度は圧倒的に高速です。したがって、CPUが計算に必要な命令やデータをその都度メモリ[*29](c7N0.xhtml#a9MD)へとりにいこうとすると、CPUはほとんどの時間を待機に費やすことになり、本来の性能を発揮できません。この問題を解決するために、CPU内部にあるキャッシュ(cache)は、メモリの部分的なコピーを保持し、メモリに代わって要求された命令やデータを返します。こうして、キャッシュがメモリの遅さをうまく隠蔽します。

しかし、キャッシュのサイズはメモリのサイズに比して小さく、メモリ全体をコピーすることはできません。CPU外部に設置できるメモリに比べ、CPU内部に設置されるキャッシュはダイ[*30](c7N0.xhtml#aA1D)のサイズやコストなどの制約から、かなり小さくせざるを得ません。したがって、キャッシュはメモリから近々必要になりそうなところを選別してコピーすることになります。この選別の基準を考えるうえで重要な原理は、現実のプログラムはメモリ領域を一様な頻度でランダムに参照しているわけではなく、次のような参照の局所性が存在するということです。

時間的局所性(temporal locality)

参照されたメモリ領域は近いうちに再度参照される可能性が高いという性質

空間的局所性(spatial locality)

参照されたメモリ領域の周辺は近いうちに参照される可能性が高いという性質

つまり、キャッシュに置くべきデータは、最近参照されたメモリ領域とその周辺の領域ということになります。これらの性質に基づきCPUが自動でキャッシュ管理を行う[*31](c7N0.xhtml#a9KK)ので、参照の局所性を無視したコードを書いてしまうと、キャッシュの力を最大限発揮できません。したがって、高速化のためには、参照の局所性を考慮したコードを書くべきです。

現代のCPUでは、キャッシュは一枚岩ではなく、複数のキャッシュが階層化された構成をしています。各キャッシュにはL1, L2, L3(L は“level”の短縮)と名前が付いており、演算装置から近い順にL1→L2→L3(→メモリ)と階層化されています。メモリからデータを取得するとき、そのデータがキャッシュにあり、そこから返されることをキャッシュヒット(cache hit)と呼び、それ以外をキャッシュミス(cache miss)と呼びます。ある層のキャッシュでキャッシュミスが発生すると、それより下位の、つまり演算装置から遠いキャッシュやメモリから目的のデータをとってこなければなりません。下位のキャッシュ(あるいはメモリ)ほどデータの取得に時間がかかるので、キャッシュミスが頻発するとプログラムの動作は遅くなります。一方で、キャッシュは下位のものほど多くのデータを保持できます。各キャッシュの容量を図18.4に面積で示しています[*32](c7N0.xhtml#a9R3)。このように、キャッシュの速度と容量にはトレードオフ[*33](c7N0.xhtml#a9R4)があることを記憶しておいてください。

![](image_rsrcBNF.jpg)

空間的局所性を破ると、どれほど性能に影響があるかを実験してみましょう。次のように引数 idx で指定された順序で配列 A の要素を合計する関数 sum を定義し、配列の要素を1から順に参照するケースとランダムな順序に参照するケースを比較します。1から順に配列の要素を参照すれば、隣り合う要素が連続して参照されるので空間的局所性は高いですが、ランダムな順に配列の要素を参照すると、次に参照される要素がわからないので空間的局所性は低くなります。

![](image_rsrcBNG.jpg)

この2つのケースは、同じメソッドを使って同じ回数の計算をしているにもかかわらず、実行速度が6倍以上も異なりました。この例はやや人工的ですが、以降では現実のプログラムで参照の局所性に気を付けるべきケースをいくつか紹介します。

# 18.7.2 多次元配列のメモリレイアウト

2次元以上の多次元配列は、メモリ上では直線状に引き伸ばされて配置されます。結果的に、例えば2次元配列(行列)では、列と行のどちらをメモリ上で連続するように配置するかという2つの選択肢があります。Array 型など、Juliaの標準的な配列は前者の配置です[*34](c7N0.xhtml#a9CW)。一方、C の配列やNumPyの ndarray 型のデフォルトは後者の配置になっています。前者の列がメモリ上で連続する配置を列優先(column-major)レイアウトと呼び、後者の行が連続する配置を行優先(row-major)レイアウトと呼びます。両者の違いを図18.5に示します。列優先と行優先レイアウトに優劣はありませんが、多次元配列を扱うときには参照の局所性に関して注意すべき事項があります。

![](image_rsrcBNH.jpg)

3次元の配列 A を例にとって説明しましょう。3次元以上では行や列といった概念は意味をなしませんが、メモリレイアウトの仕組みは同じです。つまり、A[i,j,k] のように3つの添字があるとき、A[i,j,k] と A[i+1,j,k] がメモリ上で連続しているのが列優先レイアウトで、A[i,j,k] と A[i,j,k+1] が連続しているのが行優先レイアウトです。言い換えれば、メモリ上で連続した要素を走査するとき、配列の最初の添字が最も速く変化するのが列優先レイアウトであり、最後の添字が最も速く変化するのが行優先レイアウトです。したがって、例えば配列を走査するとき、メモリレイアウトが空間的局所性に与える影響を考慮すると、列優先レイアウトでは次のように第1次元の i に関するfor文を最も内側に置くべきです[*35](c7N0.xhtml#a9YB)。

![](image_rsrcBNJ.jpg)

簡単な例として、行列の各行の最小値を計算する処理を考えてみましょう。この処理はリスト18.9に示す rowmins_row 関数のように実装できます。i 行目で見つけた最小値は配列 a の i 番目に記録されます。

![](image_rsrcBNK.jpg)

さて、rowmins_row 関数の内側のfor文を見てみると、添字 j で行に沿って走査しています。これは列優先の行列では理想的とはいえません。そこで、リスト18.10の rowmins_col 関数のように書き換えてみましょう。

![](image_rsrcBNM.jpg)

両者を単純な配列でベンチマークすると、次のように大きな性能差が見られます。このように、メモリレイアウトを意識したコードを書くことは非常に重要です。

![](image_rsrcBNN.jpg)

LinuxPerf.jlを使ってキャッシュミスの頻度を調べると、rowmins_row 関数に比べて rowmins_col 関数のキャッシュミスはずっと少ないことがわかります。

![](image_rsrcBNP.jpg)

配列のみならず、構造体を扱うときもメモリレイアウトに気を配る必要があります。構造体が保持するデータのサイズが大きくなれば、1つのキャッシュブロック[*36](c7N0.xhtml#a9H6)に収まらなくなり、メモリの参照パターンによっては、余計なキャッシュミスが頻発する恐れがあります。構造体のフィールドの順序を入れ替えたり、なるべく小さなサイズで済むようにしたりする工夫、例えば複数のビットフラグを1つの整数に詰めるなどをすれば、キャッシュミスを減らせることがあります。また、多数の構造体を配列に保存するときは、メモリの参照パターンに応じて、そのまま構造体の配列(array of structures)にするか、フィールドを別個の配列にばらした配列の構造体(structure of arrays) にするかなどを考慮すべきでしょう。StructArrays.jlパッケージを使えば、配列の構造体を比較的簡単に作成できます。いずれにせよ、データを配列や構造体に配置するときは、どのような参照パターンが頻発するかを考慮して、キャッシュミスの少ない配置を考えることが重要です。

キャッシュミスのほか、OSの仮想メモリに関してTLB(translation lookaside buffer)ミスやスワッピング(swapping)なども性能低下の原因になりうるので、調べてみるとよいでしょう。

# 18.7.3 パイプライン

キャッシュとともに、CPUの実行の妨げになりうるのが分岐命令です。分岐命令はif文やfor文などの実行に不可欠ですが、状況によっては実行に非常に時間がかかることがあります。それを理解するために、CPUが命令を実行する仕組みから説明しましょう。

CPUが命令列を実行するとき、個々の命令を逐次的に実行するのではなく、複数の命令を並列に実行できます。自動車の組立てラインを想像してみましょう。組立てラインは、1台を組み立て終えてから次の1台を組み立て始めるのではなく、例えばエンジン→駆動系→タイヤ→ドアのように、1台の組立て作業が複数の段階に分けられ、複数台の組立てが流れ作業で同時に行われます。これと同じように、CPUが命令列を実行するとき、各命令は複数の段階に分解され、複数の命令が同時に実行されます。これをパイプライン(pipeline)と呼びます。パイプラインでの処理が1段階進むと、待機中の次の命令がパイプラインに入り、さらに1段階進むと次の命令がパイプラインに入ることを繰り返しますので、CPUのある一瞬を見ると、複数の命令が処理中の状態になっています。

5段のパイプラインを持つ(相当に単純化した)CPUでの実行の概念図を図18.6に示します。右に行くほどCPU内での時刻(クロックサイクル, clock cycle)が進みます。図中の F D E M W は分解された命令の各段階を表し、それぞれ命令フェッチ(fetch)・デコード(decode)・実行(execution)・メモリアクセス(memory access)・ライトバック(write back)を意味します[*37](c7N0.xhtml#aA4Y)。例えばサイクル5のとき、命令1から命令5の別々の段階が処理されます。パイプライン化をしても、1つの命令を実行し終えるまでにかかる時間であるレイテンシ(latency)は変わりませんが、1サイクルで複数の命令を処理できるので、単位時間あたりに処理できる平均的な命令数(スループット, throughput)を大きく向上できます。

![](image_rsrcBNR.jpg)

図18.6に示した動作は、理想的なパイプラインの動作です。実際には、分岐命令やデータの依存関係など、さまざまな要因でパイプラインの円滑な動作が妨げられます。こうした要因は、一般にハザード(hazard)と呼ばれます。以降では、分岐命令に関わるCPUの動作を確認します。

# 18.7.4 分岐予測と投機的実行

パイプライン化された処理で大きな問題になるのが、分岐命令の存在です。分岐命令があると、後続の命令列がその次に実行されるとは限らないので、パイプラインが分岐命令を処理し始めても次に処理すべき命令がわからず、新しい命令をパイプラインに入れられません。そこで重要になる技術が分岐予測(branch prediction)と投機的実行(speculative execution)です。分岐予測は分岐命令がどちらに分岐するかを分岐命令の処理前に予測し、投機的実行は予測された分岐先の命令列を分岐先の決定前に処理し始めます。予測が合っていれば、投機的実行された命令列の処理はそのまま続きますが、分岐予測はあくまでも予測ですので、外れることもあります。予測が外れると、CPUは投機的実行された命令の効果を取り消し、正しい分岐先の命令列をパイプラインに入れ直して改めて実行し始めます。これはパイプラインで実行中の処理が無駄になるので、大幅な時間の無駄遣い[*38](c7N0.xhtml#aA5W)です。

分岐予測が性能に与える影響を実験してみましょう。リスト18.11に示す sortmerge! 関数は、ソート済み配列 A と B の要素を配列 C に昇順にコピーします。このとき、C の要素がソートされた状態を保つように、A と B の先頭要素である a と b の大小比較をし、小さいほうを先に C に書き込みます[*39](c7N0.xhtml#aABU)。もし A と B の要素が同じ分布にランダムに従っていれば、a と b の大小は事前に予測できませんから、分岐予測も困難になります。一方、B の要素が A の要素より平均的に大きければ、大小関係の予測は比較的容易になります。

![](image_rsrcBNS.jpg)

次の例では、A と B の要素がランダムに正規分布に従う場合における、sortmerge! 関数の実行速度を比較しています。平均が同じ分布のときは分岐予測は困難ですが、平均の差が広がるに従って予測が容易になり、実行時間が短くなっていく様子がわかります。

![](image_rsrcBNT.jpg)

分岐予測で予測できるのは、頻度だけではありません。次のように、分岐のパターンを不規則なパターンから規則的なパターンに変えても、やはり分岐予測が容易になり、実行時間が短縮されます。

![](image_rsrcBNU.jpg)

以上の例からわかるように、分岐予測がうまく機能するかは性能に大きな影響を与えます。したがって、分岐予測ができるようなコードを書くか、あるいは分岐命令自体を排除したコードを書くことが性能の向上につながります。例えば、2つの分岐があり、どちらを先に実行しても動作が変わらないならば、順序を入れ替えるなどの工夫が考えられます。

ちなみに、配列の境界チェックにも分岐命令が関係しています。配列の添字は正しいプログラムであれば配列の範囲内に収まるので、分岐予測はほぼ間違いなく的中します。したがって、境界チェックの分岐にかかるコストは比較的小さいと考えられます。[18.4.2項](c7N0.xhtml#a9J4)で説明したように、@inboudns マクロを使うとこの分岐命令がなくなり、命令数が減る分だけ実行速度が少し向上[*40](c7N0.xhtml#aA9K)するのです。

# 18.7.5 具体例:Base64符号化

CPUのパイプラインを念頭に置いて、これからループ処理を高速化する方法を考えてみます。具体的な処理の例として、Base64によるバイナリデータのエンコーディングを取り上げます。Base64は任意のバイナリデータを64種類の印字可能なASCII文字列に変換する符号化手法です。電子メールやHTMLなどのテキストしか受け付けないフォーマットに、画像などのバイナリデータをテキスト化して埋め込むときに使われます。

Base64で変換されたデータは64種類のASCII文字しか使えないので、1文字あたり符号化できる情報量は log264=6 ビットです。これでは8ビットある1バイトのバイナリデータを符号化するには少し足りないので、ASCII文字を4文字(4×6=24ビット)まとめて3バイト(3×8=24ビット)のバイナリデータを表現します[*41](c7N0.xhtml#aA5P)。この符号化にはリスト18.12に示す hex2char 関数を使用します。この関数は6ビット(0-3F16)のバイナリデータをASCII文字に変換する関数です。7ビットしか使わないASCII文字は Char 型にする必要はないので、UInt8 型の値を返すことにします。

![](image_rsrcBNV.jpg)

# 最初の実装

まず、基礎となるBase64の実装を紹介します。リスト18.13に示す encode1 関数は、なるべくシンプルにBase64による符号化を実装したものです。関数の入力はバイナリデータ data であり、出力はASCII文字の配列 str です。中央に位置するwhile文では data から符号化するデータを1バイトずつ変数 x に取り出し、前のループから持ち越されたビット h と連結してから、hex2char 関数でASCII文字に変換します。この変換結果は出力の str に書き込みます。変数 i と j は、それぞれ入力 data と出力 str の次に読み書きする位置です。変数 k は、h にまだ符号化されていないビットが何ビット残っているかを保持します。while文より後の処理は、入力の長さが3の倍数でないときのちょっとした末端処理なので、気にする必要はありません。これからこの関数を2段階に分けて高速化していきます。

![](image_rsrcBNW.jpg)

# ループ展開

リスト18.13に示した encode1 関数では、変数 k に前回のループから繰り越されるビット数を記録していました。しかしよく考えると、k のとりうる値は0, 2, 4, 6の4通りしかなく、0→2→4→6→0のパターンを周期的に繰り返します。したがって、while文の中身は k を消去して次のようなコードに書き換えられます。

![](image_rsrcBNX.jpg)

このように連続するループの反復を融合させることをループ展開(loop unrolling)と呼びます。すぐ後で説明するように、ループ展開を行うとCPUのパイプラインが効率的に稼働し高速化につながることがあります。

単純にループ展開したコードは冗長になりやや見にくいので、整理してリスト18.14のように書き換えてしまうとよいでしょう。こうすると、data から取り出した3バイトが4文字のASCII文字として str に書き込まれる様子がよくわかります。また、ビットの境界が data と str でちょうど一致するので、次の反復に未処理のビットを繰り越す変数 h も必要なくなることに注目してください。while文の直後にある finish! 関数は半端なビットの処理をする関数ですが、ここでは重要でないので実装の説明は割愛します。

![](image_rsrcBNY.jpg)

ループ展開の有無でどれくらい高速化できたかを検証してみましょう。約90メガバイトのJPEG画像[*42](c7N0.xhtml#a9MB)を符号化してみると、encode1 関数と比べてループ展開を行った encode2 関数は2倍程度高速化していることがわかりました。

![](image_rsrcBNZ.jpg)

LinuxPerf.jlを使ってプロファイルをしてみると、encode2 では実行される命令数が大きく減っていることがわかります。ループを実行するたびに更新していた変数を排除したことが奏功しているようです。

![](image_rsrcBP0.jpg)

ループ展開で高速化される理由は複合的ですが、一般に次のような要因が挙げられます。

●ループで持ち越される命令間の依存関係が減り、パイプラインで命令を並列実行しやすくなる。

●SIMD命令を利用できる機会が増える。

●ループ内の分岐が不要になる。

●分岐や変数の更新など、ループ処理のオーバーヘッドが減る。

なお、今回のようにループ展開で高速化することはよくあるのですが、ほとんど変わらないか遅くなるようなケースも考えられます。また、ループ展開をするとコードが冗長になることも多いので、ホットスポットになっていない場合は無理にする必要はないでしょう。高速化は必ず効果を計測しましょう。

# ルックアップテーブル

前述した encode2 関数の @pstats マクロの結果を見てみると、実行される命令数は減ったものの、1サイクルあたりに実行される命令数(insns per cycle)が1を下回っています。現代のCPUは1サイクルで複数の命令を実行できるスーパースカラー(superscalar)が主流[*43](c7N0.xhtml#aAEJ)ですので、CPUの能力にはまだ余裕があります。

リスト18.12の hex2char 関数を思い返してみると、この関数には内部に複数の分岐処理があります。18.7.4項で述べたように、分岐予測ができないと、性能に著しい悪影響を及ぼします。JPEG画像のような圧縮された画像データは規則性に乏しいので、この分岐予測は非常に難しくなります。実際、前述のLinuxPerf.jlでのプロファイル結果を見ると、分岐命令の約23%も予測を外しています。

そこで、分岐を含む関数の代わりにルックアップテーブル(lookup table)を使って予測の難しい分岐を排除してみましょう。リスト18.15に示した encode3 関数は、hex2char 関数の代わりに ENCODE_TABLE というグローバル変数に保持したルックアップテーブルを使って6ビットのデータをASCII文字に変換しています。このルックアップテーブルは64バイトしか使用しないので、演算装置に最も近いL1キャッシュに十分収まります。非常に単純な書き換えですが効果は絶大です。encode2 関数と比較すると、大幅に高速化されました。

![](image_rsrcBP1.jpg)

![](image_rsrcBP2.jpg)

プロファイルの結果を見ると、分岐命令の割合が大きく減り、分岐予測のミスもほとんどなくなっています。また、1サイクルあたりに実行される命令数も1を大きく上回っています。

![](image_rsrcBP3.jpg)

以上、ここまでBase64の処理をループ展開とルックアップテーブルを使って高速化しました。コードに少し工夫を加えるだけで、かなり速度に違いが出ることがわかります。

# 18.8 並列化

本節では並列化を使った高速化を解説します。まずは並列計算に特徴的な高速化の限界や注意点を紹介し、続いてJuliaで並列化を行うときに有用なパターンを紹介します。

# 18.8.1 高速化の限界

まずは並列化で達成できる高速化の限界について解説します。単純に考えると、n 個のコアを使って n 並列実行すれば n 倍の高速化が達成できそうですが、実際にそうなることはまずありません。プログラムの実行時間のうち、並列化できる部分の割合を p とすると、n 個のコアで達成できる高速化の限界は次の式で与えられます。

![](image_rsrcBP4.jpg)

この法則をアムダール則(Amdahl’s law)[*44](c7N0.xhtml#a9VT)と呼びます。例えば、全体の9割が並列化できる(p=0.9)プログラムを16コア(n=16)で並列化したとしても、実行速度は6.4倍までしか高速化できません。さらに、上の式から Speedup<(1-p)-1 であることがわかるので、どんなにコア数 n を大きくしても (1-P)-1 倍以上の高速化は達成できません。異なる p について高速化の限界を図18.7に示します。たとえ99%の部分を並列化したとしても、破線で示す理想的な高速化とはそれなりの乖離があることがわかります。

![](image_rsrcBP5.jpg)

アムダール則から得られる教訓は、高速化のためには並列化できない部分を極力減らさなければならないということです。例えば、あるJuliaのプログラムが逐次実行でガベージコレクションに5%の時間を費やしていたとしましょう。Julia 1.6時点では並列ガベージコレクションをサポートしていないので、この5%の部分は並列化できません。したがって、仮にプログラムの残りの部分を完全に並列化できたとしても、並列化可能な部分の割合 p は95%に留まります。その結果、16並列で実行しても約9.1倍まで、32並列で実行しても約12.5倍までしか高速化できません。この場合、並列化の恩恵を十分に受けるには、メモリ割当てを減らしてガベージコレクションにかかる時間を減らす必要があるわけです。

# 18.8.2 タスクの粒度

並列実行できる部分を抜き出したら、それを並列実行できるタスクに分割する方法を考えます。このとき、タスクの粒度は大きすぎても小さすぎても高速化の妨げになります。分割したタスクの粒度が大きすぎると、スレッドに割り振れるタスクが枯渇し、十分な並列数を確保できません。逆に粒度が小さすぎると、並列化に伴うオーバーヘッド[*45](c7N0.xhtml#aAAK)が無視できないほど大きくなり、全体として計算コストが上がってしまいます。

タスクの分割数とオーバーヘッドの関係の概念図を図18.8に示します。横方向の長さは計算コストを表しています。斜線で示す部分は分割されたタスクを表し、図に示された3種類の分割において、合計の長さは同一です。赤色の部分は並列化に伴うオーバーヘッドを表し、各部分の長さはどれも同一です。分割数が小さい、つまり粒度が大きいと、利用可能なスレッドに割り振れるタスクを十分に生成できませんが、分割数が大きくなるほどオーバーヘッドの総和が大きくなり、図に示すようにかえって全体のコストが膨らみます。

![](image_rsrcBP6.jpg)

図18.8では分割されたタスクのコストがすべて同一でしたが、実際には同じコストになるよう分割できるとは限りません。事前のコスト見積もりも難しいので、分割数は並列数にピッタリ合わせるよりさらに細かく分割し、空いたスレッドに分割したタスクを動的に割り振るほうが効率的です。

具体例として、[12.2.3項](c5F8.xhtml#aAB9)で扱ったマンデルブロ集合描画の並列化を取り上げます。リスト12.6では @threads マクロを使って並列化しましたが、Julia 1.6の実装は 1:n の範囲をスレッド数だけ同じ大きさに分割します。つまり図12.4の画像を縦にスレッドの数と同じ数のブロックに分けて並列計算します。しかし、画像の各区画の計算コストは同じではありません。図12.4の黒い部分(マンデルブロ集合の要素)では収束判定するまで maxiters 回反復するのに対し、それ以外の部分では収束しないことがより速くわかるので、黒い部分が多い中央付近の区画ほど計算コストも大きいことになります。したがって、スレッド数とちょうど同じ数だけ分割する戦略は、タスクの計算コストをうまく分散させるうえで効率的ではありません。

この問題を解決した実装 plot_par2 をリスト18.16に示します。blocksize は画像を分割するときのブロックの大きさを指定するパラメータで、デフォルトでスレッド数の32倍に分割しています。図12.4の描画を4スレッドで並列化したベンチマークでは、元の逐次実行が60.2秒なのに対し、plot_par1 関数が36.0秒、plot_par2 関数が15.2秒となりました。plot_par1 関数に比べ、plot_par2 関数は効率的に並列化できていることがわかります。

![](image_rsrcBP7.jpg)

# 18.8.3 フォールスシェアリング

並列実行されるプログラムで特有の現象として、フォールスシェアリング(false sharing)を紹介しましょう。まずはリスト18.17に関数 parsum のコードを示します。この関数は行列の総和を並列に計算します。

![](image_rsrcBP8.jpg)

異なるパラメータ k に対し、parsum 関数を倍精度浮動小数点数を要素とする 220 行128列の行列 A に適用し、実行時間を計測した結果を図18.9に示します。実行時間は1スレッド(T1)では k に依存しませんが、4スレッド(T4)では k が小さいときに長くなっています。これはフォールスシェアリングによって引き起こされていると考えられます。

![](image_rsrcBP9.jpg)

フォールスシェアリングはCPUが持つキャッシュと関係があります。[18.7.2項](c7N0.xhtml#a9G1)で説明したように、数十バイトほどのデータをひとかたまりとするキャッシュブロックがキャッシュがデータを管理する単位です。もし、あるコアでキャッシュブロック上にあるデータを1ビットでも更新すると、その結果はキャッシュに書き戻されます。このとき、同じCPU上の他のコアで同じキャッシュブロックを保持していれば、そのコアにも更新結果を共有する必要があります。したがって、同じキャッシュブロック上にあれば、コア間で実際にデータを共有していなかったとしても、内部的には共有しているのと同じことになります。これがフォールスシェアリングです。

リスト18.17をもう一度確認してみましょう。parsum 関数の配列 s は4スレッドで並列実行しているとき、スレッド間で共有されています。各スレッドは配列 s 上の別々の領域を使うのでデータ競合は発生しませんが、s の隣接した領域を使用しているので、フォールスシェアリングが発生する可能性があります。特に、k が2以下のとき、異なるスレッドが書き込む配列要素の間隔は4以下なので、配列要素が8バイトの倍精度浮動小数点数であることを考えると、異なるスレッドの更新するデータが同じキャッシュブロック(64バイト)に載っている可能性が高いです。一方、k が3以上のとき、この間隔は8以上なので同じキャッシュブロック上にはありません。したがって、k が2以下のときのみフォールスシェアリングが発生し、図18.9に示すような性能の劣化を引き起こしているのです。

フォールスシェアリングは配列だけでなく、構造体や変数でも発生しえます。例えば、4つのスレッドから4つの変数に頻繁に読み書きがあるとしましょう。もし、これら4変数がメモリ上の隣り合う位置に配置されれば、フォールスシェアリングが発生する可能性があります。なお、フォールスシェアリングが発生するのはメモリに書込みがあるときだけですので、データを読むだけで更新がない場合やレジスタを更新する場合は発生しません。

# 18.8.4 MapReduceパターン

並列計算の強力なパターンにMapReduceがあります。MapReduceにおいて、計算はタスクを分割して並列実行するMapと、並列実行した結果を集約するReduceに分かれます。4並列で実行するMapReduceパターンの概念図を図18.10に示します。図に示すように、Mapでデータが複数の部分データに分割されて処理され、Reduceで処理後のデータを集約します。

![](image_rsrcBPA.jpg)

実用例として、画像のヒストグラムを計算するプログラムを並列化してみます。このプログラムは、画像の各画素の赤緑青(RGB)の分布を計算します。各色が8ビットで量子化されているとき、ヒストグラムは256個のビンに分けられます。画像を走査して1つの画素を処理するたびに、RGBの各値(0以上255以下)を得てヒストグラムの対応するビンをインクリメントします。リスト18.18に逐次実行の例を示します。なお、画像の読込みにImages.jlパッケージを使用しています。

![](image_rsrcBPB.jpg)

まずは、MapReduceパターンではなくロックを使った排他制御([12.2.4項](c5F8.xhtml#a9BH))での並列化を考えてみましょう。配列 hist_r などをスレッド間で共有すると、複数のスレッドから読み書きが発生するので、データ競合が発生します。したがって、同時に1つのスレッドのみが配列を更新できるように排他制御が必要なわけです。この実装例をリスト18.19に示します。

![](image_rsrcBPC.jpg)

逐次実行とロックを使った並列化の実行時間を比べましょう。[18.7.5項](c7N0.xhtml#aA1Z)で使った画像で計測します。計測には4スレッドを使用しました。

![](image_rsrcBPD.jpg)

驚くことに、ロックを使って排他制御した並列実行のほうが100倍以上も遅くなっています。これにはさまざまな要因が考えられます。1つはロックの取得と解放自体に時間がかかることです。ヒストグラムを更新する処理にはほとんど時間がかからないので、相対的にロック処理にかかるコストが大きくなります。また、18.8.3項で説明したように、CPUのコア間でヒストグラムのキャッシュを共有する必要があるので、そのコストもかかります。さらに、1つのスレッドがロックを取得した状態で割り当てられた実行時間が切れ、他のスレッドがロックを取得できない可能性もあります。いずれにせよ、今回のプログラムでは、単純なロックを使った排他制御は有効な手段ではないことがわかります。

続いて、MapReduceによる並列化を試してみましょう。実装例をリスト18.20に示します。前半のMapでは、画素のインデックスを partition 関数で分割し、map 関数で各区画にタスクを割り当てています。各タスクは自分に割り当てられた区画のヒストグラムのみを計算します。このとき、スレッド間でヒストグラムを共有していないので、排他制御は必要ありません。これはMapReduceを使った並列化の利点です。後半のReduceでは、foldl 関数を使って各タスクの計算結果を集約しています。集約は単純にヒストグラムの各要素を加算するだけなので非常に単純です[*46](c7N0.xhtml#a9HX)。

![](image_rsrcBPE.jpg)

前回と同様に4スレッドで計測したMapReduceパターンの実装の実行時間を次に示します。逐次実行と比較すると、約3.9倍高速化していることがわかります。

![](image_rsrcBPF.jpg)

# 18.8.5 ネスト並列化

@spawn マクロの優れた点は、並列実行されるタスクの生成を入れ子(ネスト)にできる点にあります。ここでは、ネストした並列化で可能になるエイトクイーンパズル([1.3.1項](cCH.xhtml#aA5X))の高速化を考えてみましょう。

まずはタスクを分割する方法を考えます。エイトクイーンパズルでは、最初のクイーンを1列目のどの位置に置くかに着目すると、独立した部分タスクに分割できることがわかります。つまり、1列目の位置が異なれば互いに同じ解を見つけることがないので、1列目の位置でタスクを分割し、部分タスクを並列に解いて合計すれば、元の問題の答えになります。これは18.8.4項のMapReduceパターンと同じ構造です。リスト18.21にこの実装例である nqueens_flata 関数を示します。

![](image_rsrcBPG.jpg)

次に、ネスト並列の方法を考えましょう。前述した分割の考え方を一般化し、一度分割した部分タスクをさらに小さい部分タスクへと再帰的に分割します。しかし、あまりにタスクが小さくなりすぎると、[18.8.2項](c7N0.xhtml#a9PX)で説明したように、オーバーヘッドが増えてかえって遅くなってしまいます。そこで大まかな方針として、再帰的に解を探索するとき、探索空間が大きいうちはタスクを分割して並列実行し、探索空間が小さくなってきたら逐次実行に切り替えることにします。この方針で solve 関数の並列版である parsolve 関数を実装し、それを呼び出す nqueens_nested 関数をリスト18.22に示します。parsolve 関数は探索空間が大きいうちは並列タスクを生成して再帰的に自分自身を呼び出し、十分小さくなる(n - file < 12)と代わりに逐次実行する solve 関数を呼び出しています。

![](image_rsrcBPH.jpg)

複数スレッドを使って nqueens_flat 関数と nqueens_nested 関数を実行し、その実行時間を計測する実験を行いました。盤面のサイズは N=15 に固定しています。その結果を図18.11に示します。上下のプロットの横軸はどちらもスレッド数ですが、上が実際の実行時間を示し、下が1スレッドの実行時間と比較した高速化の倍率[*47](c7N0.xhtml#a9BU)を示します。全体を通して、ネスト並列化をした nqueens_nested 関数のほうがコアをうまく使い切れていることがわかります。

![](image_rsrcBPJ.jpg)

まとめ

本章では、Juliaのプログラムを高速化するさまざまなテクニックを紹介しました。プログラムの高速化には、正しいベンチマークとプロファイルが大前提です。やみくもに高速化を図るのではなく、必ずベンチマークとプロファイルに基づいた判断をしてください。また、Juliaの処理系と計算機の事情をよく理解すれば、大幅な高速化が達成できることがわかります。Juliaの並列処理機能は比較的新しく、使い方が難しくまだ未発達な部分はありますが、工夫次第で十分実用可能です。

注

[*1](c7N0.xhtml#a9EG) CPU: AMD Ryzen 9 3950X (16C32T), RAM: 64 GiB (DDR3-3200), Disk: Western Digital WD_BLACK SN750, OS: Linux Ubuntu 20.04, Julia: 1.7.2[*2](c7N0.xhtml#a9V9) <https://github.com/lpechacek/cpuset>[*3](c7N0.xhtml#aA8G) LinuxではCPU Frequency Scalingについての説明が<https://www.kernel.org/doc/html/v5.8/admin-guide/pm/cpufreq.html>にあります。例えば、sysfs で /sys/devices/system/cpu/cpufreq/boost の値を 0 にすると、動作周波数のブースト(boost)を無効化できます。<https://wiki.archlinux.org/title/CPU_frequency_scaling>にも説明があります。さらに、simultaneous multithreading(SMT)やaddress space layout randomization(ASLR)も計測が不安定化する要因になりうるので、無効化したほうがよいかもしれません。[*4](c7N0.xhtml#aA90) GNU Time以外にも time コマンドの実装はいくつもあります。例えば、BashやFishなどのシェルはそれぞれ独自の time コマンドを提供しています。[*5](c7N0.xhtml#a9ZM) <https://www.gnu.org/software/time/>[*6](c7N0.xhtml#a9GS) command コマンドはシェルの関数やビルトインコマンドを無視してプログラムを実行するBashやFishのビルトインコマンドです。[*7](c7N0.xhtml#a9S7) <https://github.com/sharkdp/hyperfine>[*8](c7N0.xhtml#aA14) @time マクロは計算結果を返すので、この例のようにREPLではセミコロンを末尾に付けて結果の表示を抑制したほうが読みやすいでしょう。[*9](c7N0.xhtml#a9FW) スクリプトなどで @benchmark マクロを実行しても結果は出力されないので注意してください。スクリプトなどでもREPLと同じように表示したいときは、返されたオブジェクトを display 関数に渡します。例えば display(@benchmark sort($xs)) とします。[*10](c7N0.xhtml#aA55) CPUの動作周波数が5GHzで整数の加算が1サイクルだとしても、0.2ナノ秒はかかる計算なので、ありえない結果だといってよいでしょう。[*11](c7N0.xhtml#a9E0) 挿入ソートは対象の配列がソート済みか否かで計算量が大きく変わる極端な例です。入力サイズ N に対して、ソート済みであれば O(N) で終了しますが、よく知られているように平均的には O(N2) の時間がかかります。[*12](c7N0.xhtml#a9BZ) 若干カウントが少なくなるのは、プロファイラが自身のオーバーヘッド分を除去しているからだと思われます。[*13](c7N0.xhtml#aA26) 配列型のオブジェクトは要素以外にサイズなどのメタデータも持つので、実際には要素のサイズと要素数の積より少し大きくなります。[*14](c7N0.xhtml#a9SX) この制約はLinuxカーネルの perf_event_open というシステムコールに依存しているのが原因です。他のOSでは、macOSのInstrumentsやIntelのIntel VTune Profilerなどで可能かもしれません。[*15](c7N0.xhtml#aA77) パラメータの変更に伴うセキュリティリスクについては<https://www.kernel.org/doc/html/latest/admin-guide/perf-security.html>を参照してください。[*16](c7N0.xhtml#aAEK) フロントエンドはCPUが命令を取得したり意味を解析したりする段階を指し、バックエンドは命令を実際に実行したりメモリからデータをとってきたりする段階を指します。[*17](c7N0.xhtml#a9YA) ただし、JET.jlパッケージのように、型推論の機能を検証に使うプロジェクトもあります。[*18](c7N0.xhtml#aADA) 近年のCPUには分岐予測という機能があり、分岐の結果を予測して分岐先のコードを先回りして実行することで分岐のコストを大幅に下げています。これを投機的実行(speculative execution)と呼びます。境界チェックの分岐はほぼ間違いなく分岐予測が的中するので、分岐自体にかかるコストは小さいですが、分岐がなくなることで結果的に他の最適化が可能になって高速化につながることがあります。[*19](c7N0.xhtml#aA7Y) julia コマンドのオプションには \--check-bounds オプションがあり、yes を渡すと常に境界チェックを行います。[*20](c7N0.xhtml#aAED) 例えば、(1.0 + 1e+20) + -1e+20 は 0.0 ですが、1.0 + (1e+20 + -1e+20) は 1.0 になります。16桁程度の精度しかない倍精度浮動小数点数では、1.0 と 1e+20 の加算で極端に小さい 1.0 の影響が無視されることが原因です。[*21](c7N0.xhtml#a9VK) AMD64ではSSEやAVX、ArmではNEONなどのSIMD拡張命令が実装されています。[*22](c7N0.xhtml#a9XY) メソッドの生成にはメタプログラミングが使われています。[*23](c7N0.xhtml#a9XZ) 煩雑になるので添字の範囲は省略しています。[*24](c7N0.xhtml#aAE8) 言語のレベルではJuliaにスタックとヒープの区別はありません。この区別はいわば実装の詳細であり、プログラムの動作に違いが生じない限り、Juliaはどの領域にも自由にメモリを割り当てられます。実際、処理系の改善により、コードを一切変更しなくてもヒープへのメモリ割当てが削減されることがあります。[*25](c7N0.xhtml#aA33) とはいえ実行時間の延びは2~3割程度でした。Juliaのメモリ割当ては結構高速です。オブジェクトのサイズが大きくなると、割当てにも時間がかかる傾向があります。[*26](c7N0.xhtml#a9R8) この参照の実体は Record オブジェクトへのポインタです。[*27](c7N0.xhtml#a9WS) ただし、これはすべてのケースで必ずそうなるというわけではありません。一時的に使われる可変な構造体は最適化によってまったく割り当てられないこともありますし、Julia 1.5より前では struct でもメモリ割当てが発生することがあります。[*28](c7N0.xhtml#aAC3) mul! 関数には5引数をとるメソッドもあります。[*29](c7N0.xhtml#aA45) 現在主流のノイマン(von Neumann)型アーキテクチャでは、命令もデータも区別せずメモリに保持されます。したがって、本書では、区別せずにどちらもデータと呼ぶことが多いです。[*30](c7N0.xhtml#a9HZ) CPUの集積回路が設置される半導体をダイ(die)と呼びます。[*31](c7N0.xhtml#a9VB) CPUにはキャッシュ管理を行う命令がありますが、高度な話題なので本書では扱いません。[*32](c7N0.xhtml#a9GK) AMD Ryzen 9 3950Xのキャッシュを参考にしています。L1から順に1, 8, 64メガバイトです。[*33](c7N0.xhtml#aAFK) 一般に、階層が下がるほどに速度が数倍遅くなり、容量は数倍増えるのが大まかな傾向です。[*34](c7N0.xhtml#a9S3) これはFortranや R といったプログラミング言語の配列も同様です。[*35](c7N0.xhtml#aA62) ただし、単純に全要素を走査するときは、[11.1.6項](c4WA.xhtml#aA63)で説明した eachindex 関数を使うべきでしょう。[*36](c7N0.xhtml#aAB7) キャッシュに収めるデータの最小単位をキャッシュブロック(cache block)またはキャッシュライン(cache line)と呼びます。典型的なキャッシュブロックのサイズは64バイト程度です。[*37](c7N0.xhtml#aA7G) 各段階が具体的に何をしているかは、本書では重要ではないので割愛します。[*38](c7N0.xhtml#a9D3) 深いパイプラインを持つ最近のCPUでは、分岐予測のミスによるペナルティは10サイクル以上(上限は条件などによりかなりばらつきがある)と見積もられています。[*39](c7N0.xhtml#a9WX) これはマージソート(merge sort)などのソートアルゴリズムで行われる処理の一部です。[*40](c7N0.xhtml#a9ET) ただし、分岐がなくなることで新たな最適化が可能になり、実行速度が大きく向上する場合もあります。[*41](c7N0.xhtml#a9BF) 符号化するバイナリデータのバイト数が3の倍数にならないときは0で埋め、等号で長さを調整するなどの処理をしますが、末端の処理が性能に与える影響は少ないので、本書では説明を省略します。[*42](c7N0.xhtml#aA1N) <https://en.wikipedia.org/wiki/Mona_Lisa>[*43](c7N0.xhtml#a9U2) 例えば、Zen2のように4個のALU(arithmetic logic unit)を持つコアであれば、原理的には最大で4つの算術・論理演算を同時に行えます。[*44](c7N0.xhtml#a9W7) 式の導出は次のとおりです。プログラムを逐次実行したときの実行時間を T とし、そのうち pT を並列化で n 倍高速化したとすると、並列実行したときの実行時間は![](image_rsrcBPK.jpg)となります。この式を T で割って逆数をとると、目的の式が得られます。[*45](c7N0.xhtml#aA8V) タスクの作成や同期などにかかるコストです。例として、筆者の計算機では @spawn マクロで新しいタスクを作成する時間は1マイクロ秒程度でした。[*46](c7N0.xhtml#aAA1) 今回は計算コストの小さいReduce部を逐次実行していますが、この部分を並列化する工夫も考えられます。加算のように結合的な演算であれば、部分和を並列に計算して並列化可能です。[*47](c7N0.xhtml#a9X9) 下の破線で示す直線は並列計算における理想的な高速化(t スレッドで実行時間が 1/t)です。スレッド数が同じなら縦軸の値が大きいほど、複数のスレッドを効率よく使えていることになります。

---

# ![](image_rsrcBPM.jpg)

Markdown とは、John Gruber氏とAaron Swartz氏が共同開発[*1](c888.xhtml#a9UM)したマークアップ言語です。現在では、その手軽さから、ブログ記事やドキュメントの作成など、文章を記述するときの一般的な形式になっています。JuliaのドキュメントもMarkdownで記述するのが一般的です。また、GitHubやDiscourseなどのウェブサービスでもMarkdownが採用されています。なお、Markdownの文章はHTMLに変換されてウェブブラウザで読まれることが多いですが、そのまま読むこともそれほど難しくありません。

Markdownには厳格な仕様が存在せず[*2](c888.xhtml#aA7T)、互いに微妙に異なる方言が数多くありますが、基本的な文法は共通しています。ここでは、Markdownの中で一般的と思われる要素を紹介します。

# A.1 基本仕様

Markdownでは、特殊な意味を持つ文字(メタ文字)以外はそのままテキストとして扱われます。したがって、自然言語で記述されたテキストファイルは、そのままでMarkdownと解釈できます。例えば、次の文章は3つのパラグラフからなるMarkdown形式のテキストです。

![](image_rsrcBPN.jpg)

Markdownには特殊な意味を持つメタ文字がいくつか存在し、テキストにメタ文字を挿入することで、強調やリストなどの要素を作成できます。

# A.2 インライン要素

アスタリスク(*)やバッククォート(`)は、次のように文章の一部のフォントを変更するときに使われます。

![](image_rsrcBPP.jpg)

特に、** はテキストの強調、`はコードの挿入によく使われます。

![](image_rsrcBPR.jpg)

ハイパーリンクを挿入するには、次のように [TEXT](URL) と記述します。

![](image_rsrcBPS.jpg)

# A.3 見出し

見出しを作るにはハッシュ(#)を使います。ハッシュの数が少ないほど上位の見出しになります。テキストの次の行に = や - を並べて見出しとする方法もあります。

![](image_rsrcBPT.jpg)

# A.4 リスト

リストを作るには、アスタリスクを使います。このように作られるリストは、順序のないリストです。アスタリスクの代わりにハイフン(-)やプラス(+)を使うこともできます。

![](image_rsrcBPU.jpg)

順序を持つリストは、次のように書きます。

![](image_rsrcBPV.jpg)

リストの各項目には、別のリストなどを入れ子にできます。

# A.5 コードブロック

インデントをすると、その部分はコードブロックとみなされます。Juliaでは、次のようにdocstringの記述でよく用いられます。

![](image_rsrcBPW.jpg)

あるいは、バッククォート3つで囲んでも、コードブロックになります。バッククォートの直後には言語名を付けることができ、一部のウェブサービスではシンタックスハイライトされるようになります。

![](image_rsrcBPX.jpg)

# A.6 引用

パラグラフの行頭を > で始めると、引用になります。ドキュメントや書籍などから文章を引用するときに使います。

![](image_rsrcBPY.jpg)

# A.7 画像と罫線

画像は ![TEXT](URL)、罫線は3個以上の連続したハイフン(-)で挿入します。

![](image_rsrcBPZ.jpg)

# A.8 Juliaの拡張

JuliaのMarkdownには、次のような機能が追加されています。

![](image_rsrcBR0.jpg)の数式

![](image_rsrcAG6.jpg)の数式を記述するには、``![](image_rsrcAGC.jpg)![](image_rsrcAGC.jpg)sum_k x^{k-1} p[k]`` のようにバッククォート2つで囲みます。バックスラッシュはエスケープする必要があります。

脚注(footnote)

[^LABEL]: に続けて脚注の文章を記述します。本文から脚注への参照は [^LABEL] と書きます。

![](image_rsrcBR1.jpg)

勧告(admonition)

!!! note や !!! warning に続けて勧告の文章を記述します。

![](image_rsrcBR2.jpg)

表(table)

表は次のように行と列を区切って表現します。:-- は左揃え、\--: は右揃え、:--: は中央揃えです。

![](image_rsrcBR3.jpg)

これらについては、標準ライブラリであるMarkdown.jlのドキュメントを参照してください。また、Documenter.jl自体もいくつか構文の拡張を提供しているので、詳しくはDocumenter.jlのドキュメントを参照してください。

注

[*1](c888.xhtml#a9T7) <https://daringfireball.net/projects/markdown/>[*2](c888.xhtml#aADX) CommonMark(<https://commonmark.org/>)のように形式を厳密に定めようとする取組みもあります。

---

# ![](image_rsrcBR4.jpg)

TOML(Tom’s obvious, minimal language)は、Tom Preston-Werner氏[*1](c8BX.xhtml#aA0A)によって設計された、設定ファイルの記述言語です。JSONやYAMLなどと目的は類似していますが、TOMLは文法が簡潔で、その名が示唆するとおり「見ればわかる」ような言語です。Juliaの標準ライブラリには、TOML形式を扱うTOML.jlパッケージがあります。Juliaのみならず、Rustでも設定ファイルのファイル形式として採用されています。

TOMLの仕様はウェブサイト[*2](c8BX.xhtml#a9H4)で公開されています。執筆当時の最新版は1.0.0ですので、それに則って特に重要と思われるTOMLの仕様を解説します。

# B.1 基本仕様

TOMLのドキュメントは、UTF8でエンコーディングされたテキストファイルです。ファイルの拡張子には、.toml を用います。タブ文字(U+0009)と空白文字(U+0020)のみが空白として、LF(U+000A)とCRLF(U+000DとU+000A)のみが改行として扱われます。インデントなど、リテラルの外にある空白は無視されますが、大文字と小文字は区別されます。

# B.2 型とリテラル

TOMLがサポートする型とそのリテラルは次のとおりです。インラインでない配列とテーブルの表記は付録B.3で解説します。

真偽値

true と false のいずれか。

整数

64ビットまでの符号付き整数。0x(16進法)・0o(8進法)・0b(2進法)が接頭辞として付けられる。位どりに _ を差し込める。42, +123, -99, 0xDEADBEEF, 1_200_000 など。

浮動小数点数

倍精度浮動小数点数。位どりに _ を差し込める。3.14, -1.23, 6.022e23, inf など。

文字列

Unicodeの文字列。ダブルクォートの文字列ではバックスラッシュエスケープが使えるが、シングルクォートの文字列では使えない。また、Juliaと同じように """ や ' を使った複数行の文字列リテラルもある。"Julia![](image_rsrcAGC.jpg)nPython", 'C:![](image_rsrcAGC.jpg)path![](image_rsrcAGC.jpg)to![](image_rsrcAGC.jpg)file.txt' など。

日付・時刻

RFC3339[*3](c8BX.xhtml#a9RJ)の日付と時刻(部分的に省略可)。やや複雑なので、詳細はTOMLとRFC3339の仕様を参照。2012-02-14, 2020-01-03T18:46:09.240170994+09:00 など。

インライン配列

型が同じ値の列。要素の型が異なる配列は禁止されている。[1, 2, 3, 4], ["foo", "bar", "baz"] など。

インラインテーブル

キーと値の集合。{ x = 1, y = 2 }, { ver = "1.6", lts = true } など。

# B.3 文法

# B.3.1 コメント

ハッシュ(#)から行末までがコメントです。コメントは無視されます。

![](image_rsrcBR5.jpg)

# B.3.2 キーと値のペア

キーと値のペアは、等号(=)で対応付けます。値には、付録B.2で紹介したリテラルが使えます。キーには、ASCIIの英数字のほかに、アンダースコア(_)とハイフン(-)も許されています。また、キーをシングルクォート(')やダブルクォート(")で囲むことで、空白文字や非ASCII文字もキーにできます。次の例は、有効なキーと値のペアの記法です。

![](image_rsrcBR6.jpg)

ドット(.)でキーを分けると、階層構造を持ったキーも表現できます。

![](image_rsrcBR7.jpg)

# B.3.3 テーブル

TOMLにはインラインテーブル以外にも、ネストしたテーブルを簡潔に記述する記法があります。[ と ] でキーを囲むと、続いて現れるペアが、そのキーの下にあるテーブルになります。本書ではテーブルをセクションと非公式に呼ぶことがあります[*4](c8BX.xhtml#aAF1)。例えば、次の2つの書き方は等価です。

![](image_rsrcBR8.jpg)

このTOMLドキュメントは、次のようなJuliaの辞書に変換できます。

![](image_rsrcBR9.jpg)

# B.3.4 配列

テーブルと同様に、配列にも簡潔な記法が用意されています。[[ と ]] でキーを囲むと、続いて現れるペアが、そのキーの下にある配列の要素になります。また、続けて現れる同名のキーが、1つの配列を指します。例えば、次の2つの書き方は等価です。

![](image_rsrcBRA.jpg)

このTOMLドキュメントは、次のようなJuliaの辞書に変換できます。

![](image_rsrcBRB.jpg)

注

[*1](c8BX.xhtml#aA48) GitHubの創業者として有名です。[*2](c8BX.xhtml#aAC0) <https://toml.io/en/>[*3](c8BX.xhtml#aA67) <https://tools.ietf.org/html/rfc3339>[*4](c8BX.xhtml#aA3Y) 例えば、プロジェクトファイル([16.1.4項](c6XJ.xhtml#a9CC))中の [deps] で始まるテーブルを deps セクションと呼んでいます。

---

# ![](image_rsrcBRC.jpg)

Git[*1](c8EH.xhtml#a9T2)は、分散型のバージョン管理システムです。GNU/Linuxの創始者として有名なLinus Torvals氏がLinuxカーネルのソースコードを管理するために開発を始めましたが、現在では数多くのソフトウェアのバージョン管理システムとして採用されています。Juliaの開発においても、事実上の標準ツールとして扱われています。

GitHub[*2](c8EH.xhtml#a9WP)は、Gitを基礎としたソフトウェア開発を支援するウェブサービスです。Gitレポジトリのホスティングを始めとして、ソフトウェアの不具合報告や変更要求を円滑にする機能があります。また、開発者間のコラボレーションやソフトウェアの配布もできます。Juliaのコミュニティでは、Julia処理系と大多数のパッケージがGitHubで開発されています。

ここでの解説は、本書を読むのに必要最低限のものです。より発展的な内容は、『Pro Git』の日本語訳[*3](c8EH.xhtml#aA2W)が公開されていますので、そちらを参照するとよいでしょう。

# C.1 Gitの基礎概念

まず最初に、Gitの基礎概念を簡単に解説します。大まかな意味を理解したら、Gitを使いながら覚えていくことを推奨します。

Gitにおいてレポジトリ(repository)とは、Gitで管理されたソフトウェア開発プロジェクトの複製の1つです。1つの開発プロジェクトでは、複数の場所に独立したレポジトリが置かれることがあります。例えば、開発者AliceとBobのローカルファイルシステムにそれぞれレポジトリがあり、GitHubにまた別のレポジトリを置くといった具合です。このように複数の場所に独立したレポジトリの複製を設置できる特徴から、Gitは分散型(distributed)のバージョン管理システムと呼ばれます。

レポジトリはソフトウェアの変更履歴を記録しています。レポジトリが記録しているある瞬間のソースコードの状態をコミット(commit)と呼びます。コミットとは開発段階のスナップショットである、と言い換えてもよいでしょう。Git以外のバージョン管理システムでは、Gitのコミットに相当するものをリビジョン(revision)と呼ぶこともあります。

最も単純なソフトウェア開発では、変更履歴はコミットがつながった分岐のない直線上のグラフとして表現できます。図C.1では、JuliaのGitレポジトリに記録されている最初の4つのコミットを示しています。図中の a9cbc036ac などの文字列は、Gitがコミットを識別する名前であり、ハッシュ(hash)と呼ばれています。実際のハッシュは、16進法表記で40文字の長い文字列ですが、ここでは先頭の10文字のみを表示しています。各コミットは、ハッシュと変更の内容以外にも、変更の日時・作者・変更の要約など、多くのメタデータを持っています。コミットが記録する内容を見れば、いつ、誰が、どのような理由で、どのような変更を加えたかがすぐにわかります。

![](image_rsrcBRD.jpg)

コミットにはブランチ(branch)という別名を付けられます。デフォルトのブランチ名はmainです[*4](c8EH.xhtml#aA9N)。ブランチに新たな変更を加えることをコミットするといい、コミットするとブランチの先端は新たなコミットを指し示すようになります。いわば、ブランチは開発中のソフトウェアに付ける動的な名前です。例えば、mainブランチにコミットすると、ブランチの先端は図C.2に示すように、コミット d から e へと移動します。

![](image_rsrcBRE.jpg)

現実の開発では、履歴が一直線になるとは限りません。複数の開発者が、同じコミットから別々のコミットを「生やす」ことがあります。すると、一直線だった履歴が2つに分岐します。また、複数の開発者がそれぞれの修正を持ち寄って、履歴を合流させることがあります。こうした分岐と合流のある履歴の例を図C.3に示します。c というコミットから分岐したbugfixブランチは、d と f という2つのコミットを経て再び本流のmainブランチに合流しています。e から分岐したreleaseブランチは、合流せずそのまま h と j の2つのコミットを重ねています。

![](image_rsrcBRF.jpg)

コミットには、タグ(tag)という別名も付けられます。タグはブランチと異なり、コミットをしても移動しない静的な名前です。この特性から、タグはソフトウェアのリリース版を指し示すときによく使われます。例えば、v0.5.0やv1.2.1といったバージョンを示すタグ名を付けるのが一般的です。

# C.2 Gitのセットアップ

Gitを使うには、Gitのコマンドラインインタフェースである git コマンドが必要です。各プラットフォームでの git コマンドのインストール方法は、Gitのダウンロードページ[*5](c8EH.xhtml#a9P3)に記されています。このページの説明に従って、ターミナルから git コマンドが使えるようにインストールしてください。

Gitがインストールできたら、名前と電子メールアドレスの初期設定を行います。次の NAME と EMAIL を、それぞれ自分の名前と電子メールアドレスに置き換えて実行してください。

![](image_rsrcBRG.jpg)

この設定はホームディレクトリにある .gitconfig ファイルにテキストで記録され、後で変更もできます。また、GitHubアカウントを持っている場合は、同様にして github.user にGitHubのアカウント名を設定することを推奨します。

# C.3 新しいレポジトリの作成

ローカルのファイルシステムに新しくレポジトリを作成するには、git init コマンドを使います。例えば ~/workspace/myproject ディレクトリで新しく開発プロジェクトを始める場合、次のようにディレクトリを作成してそこに移動し、git init を実行します。

![](image_rsrcBRH.jpg)

git init を実行すると、.git ディレクトリが作られます。ソフトウェアの変更履歴など、Gitが管理するデータはこのディレクトリに保存されます。

![](image_rsrcBRJ.jpg)

# C.4 既存レポジトリの複製

Gitのような分散型のバージョン管理システムでは、既存のレポジトリを手元に複製できます。レポジトリを複製するには、git clone コマンドにレポジトリのURLと複製を置くディレクトリを順に渡します。

![](image_rsrcBRK.jpg)

出力先のディレクトリには、次のようにレポジトリの内容がコピーされています。

![](image_rsrcBRM.jpg)

# C.5 変更のコミット

[付録C.3](c8EH.xhtml#a9EF)で作ったレポジトリに戻り、README.txt というテキストファイルを作成しましょう。このファイルには、Hi, all. とだけ書いておきます。

![](image_rsrcBRN.jpg)

いま作成したファイルをGitに記録させるには、まず git add コマンドにファイル名を渡して、記録する差分を確定させます。

![](image_rsrcBRP.jpg)

続いて、git commit コマンドを実行し、差分をコミットします。オプションなしで実行すると、テキストエディタが起動するので、差分の要約を記述して保存します。-m オプションを使うと、次のようにエディタを起動せずにコマンドラインから要約を記述できます。

![](image_rsrcBRR.jpg)

現在のmainブランチに記録されているコミットは最初の1つのみです。さらにファイルを加えたり、既存のファイルを編集したりすると、再びコミットする機会が訪れます。例えば、New text. という1行を README.txt に加えて、同じようにコミットしてみましょう。

![](image_rsrcBRS.jpg)

これでmainブランチに記録されているコミットは2つになりました。git log コマンドを実行すると、現在のブランチにあるコミットが新しいほうから順に表示されます[*6](c8EH.xhtml#a9N3)。

![](image_rsrcBRT.jpg)

# C.6 ブランチとタグの作成

レポジトリの初期状態では、mainブランチが現在開発中のブランチです。バグの修正や実験的な機能のために開発を分岐させたい場合、git branch コマンドを使ってブランチを作成し、git switch コマンド[*7](c8EH.xhtml#aAEH)でブランチを切り替えます。どちらのコマンドもブランチ名を引数にとります。git branch は引数を与えずに実行すると、レポジトリにあるブランチの一覧を表示し、現在のブランチをアスタリスク(*)で示します。次の例では、mainブランチからbugfixというブランチを作成し、bugfixブランチに開発を切り替えています。

![](image_rsrcBRU.jpg)

ブランチを切り替えると、以降のコミットは切り替えた先のブランチに追加されます。したがって、mainとbugfixブランチにそれぞれ別のコミットを加えると、履歴が分岐し始めます。

コミットにタグを付けるには、git tag コマンドにタグ名とブランチ名を渡します。また、git tag は引数を与えずに実行すると、レポジトリにあるタグの一覧を表示します。次の例では、現在のmainブランチ先端が指し示すコミットにv0.1.0というタグを付けています。

![](image_rsrcBRV.jpg)

作成したブランチやタグは、同じコマンドの -d オプションで削除できます。例えば、先ほど作ったタグを削除するには次のようにします。

![](image_rsrcBRW.jpg)

# C.7 GitHubとの連携

作成したレポジトリをGitHubに設置するにはGitHubのアカウントが必要です。まずは<https://github.com/>にウェブブラウザでアクセスし、アカウントを作成してください。

次に、GitからGitHubの操作をできるようにするために、認証をする必要があります。方法はいくつかありますが、一時的に発行されるトークン(token)を使った認証方法の概要を紹介します。なお、かつて存在したアカウント名とパスワードを使った認証はセキュリティの都合から2021年8月に廃止され、トークンかSSHキーを使った認証が必要になりました。詳細は公式のドキュメント[*8](c8EH.xhtml#a9VP)を参照してください。

認証に使うトークンを保存し、自動的に使用するように設定しておくと、Gitを通じたGitHubの操作が楽になります。次のように設定すると、認証時に発行されるトークンが自動で使われるようになりますので、設定しておくとよいでしょう[*9](c8EH.xhtml#a9N7)。

![](image_rsrcBRX.jpg)

GitHubとの連携には、GitHubが開発したGitHub CLI[*10](c8EH.xhtml#a9ZY)を使うのが最も手軽です。GitHub CLIの gh コマンドは、認証やレポジトリ操作を含め、GitHubでの開発に必要な機能を提供しています。gh auth login コマンドを実行して指示に従うと、認証が行われ、git コマンドや gh コマンドでGitHubの操作が可能になります。

![](image_rsrcBRY.jpg)

認証の状態は、次のように gh auth status コマンドで確認できます。

![](image_rsrcBRZ.jpg)

最後に、レポジトリのデフォルトのブランチ名をGitHubと合わせるため、次のようにGitの設定をしておくとよいでしょう[*11](c8EH.xhtml#a9FD)。

![](image_rsrcBS0.jpg)

# C.8 GitHubレポジトリの作成と同期

手元で作成したレポジトリをGitHubに設置するには、gh repo create コマンドを実行します。このコマンドを実行すると、いくつかの選択肢が対話的に提示されます。このとき、レポジトリをPublicに設定すると、レポジトリが誰でも閲覧できる状態になるので注意してください。次のように最後にコミットをプッシュするかと質問されるので、Yesを選択すればGitHubにコミットが記録されます。

![](image_rsrcBS1.jpg)

GitHub上に作成したレポジトリの複製を手元に新しく作成するには、git clone コマンドを使います。git clone コマンドには、レポジトリのURLを指定します。以降、GitHub上のレポジトリに加えられたコミットは、git pull コマンドで手元のレポジトリに反映できます。反対に、手元のレポジトリに加えたコミットをGitHub上のレポジトリに反映するには、git push コマンドを使います。

複数人で同じGitHubレポジトリを共有して作業をしている場合、手元とGitHubのレポジトリの内容に齟齬が発生することがあります。これをコンフリクト(conflict)と呼びます。コンフリクトの解決方法については、本書の範囲を超えますので、詳しくは冒頭で紹介した『Pro Git』などの書籍を参照してください。

注

[*1](c8EH.xhtml#a9SB) <https://git-scm.com>[*2](c8EH.xhtml#aA75) <https://github.com>[*3](c8EH.xhtml#a9SZ) <https://git-scm.com/book/ja/v2>[*4](c8EH.xhtml#aABV) 2020年10月にGitHubはデフォルトのブランチ名をmasterからmainに変更しました。ここではそれに従うことにします。設定方法は[付録C.7](c8EH.xhtml#a9V2)を参照してください。[*5](c8EH.xhtml#a9YD) <https://git-scm.com/downloads>[*6](c8EH.xhtml#a9MC) コミットのハッシュはコミットした時刻などの影響を受けて変化するので、筆者のハッシュと一致することはありません。[*7](c8EH.xhtml#a9VU) git switch コマンドはGit 2.23で実験的に導入されました。それ以前のバージョンでは git checkout コマンドなどを使う必要があります。[*8](c8EH.xhtml#aAE6) <https://docs.github.com/en/authentication>[*9](c8EH.xhtml#a9UN) トークンはファイルシステムに平文で保存されるので、セキュリティには注意してください。必要であれば、Git Credential Managerなどの使用を検討してください。[*10](c8EH.xhtml#a9ST) <https://cli.github.com/>[*11](c8EH.xhtml#aA5U) この設定はGit 2.28で導入されました。

---

# 用語一覧

英字

ABI

ASCII

Baseモジュール

Coreモジュール

CPU

CSC

CSV

docstring

doctest

fused multiply-add(FMA)演算

Git

GitHub

HDF5

JITコンパイル

JSON

Juliaup

JupyterLab

LLVM中間表現

LTS

Mainモジュール

MAJOR

MapReduce

Markdown

MINOR

NaN

nothing

PATCH

raw文字列リテラル

REPL

SIMD

SSH

TLB

TOML

ulp

Unicode

UTF-8

UUID

Visual Studio Code

和字

あ

値

アーティファクト

アトミック操作

後読み

アムダール則

アンダーフロー

アンチパターン

い

依存パッケージ

一時ディレクトリ

一時ファイル

位置引数

一様分布

一般カテゴリ

イテレーション

イテレータ

インスタンス

インデックス

インデント

インライン化

う

上三角行列

え

衛生的なマクロ

エラー

エンコーディング

演算

演算子

お

オーバーフロー

オーバーロード

オブジェクト

オプショナル引数

オペレーティングシステム

か

下位型

解析関数

外部コンストラクタ

外部パッケージ

外部プログラム

仮数

型

型システム

型推論

型注釈

型付き中間表現

型の乗っ取り

型の不安定性

ガベージコレクション

可変

可変長エンコーディング

可変長引数

環境

関数

関数定義

関数呼出し

き

キー

疑似乱数

基数

キャッシュ

キャッシュヒット

キャッシュミス

キャメルケース

キュー

協調的マルチタスキング

協定世界時

行ベクトル

共変性

行優先

共有ライブラリ

行列

行列分解

キーワード

キーワード引数

く

空間的局所性

偶数への最近接丸め

クォート

具象型

クリティカルセクション

クロージャ

クロックサイクル

グローバルスコープ

グローバル変数

け

経過時間

計算機イプシロン

結合性

結合則

結合文字

欠損値

検索

こ

公式パッケージ

合成済み文字

構造体型

構築

構文解析

後方互換性

誤差

コードポイント

コマンドオブジェクト

コマンドモード

コミット

コメント

コルーチン

コレクション

コンストラクタ

コンディション

コンフリクト

コンポジション

さ

再帰関数

サイズ

先読み

三項演算子

三乗根

サンプリング

し

ジェネレータ

時間的局所性

式

識別子

次元

辞書

辞書式順序

指数

指数表記

システムCPU時間

シード

シバン

シャドーイング

集合

修飾子

集約

主成分分析

上位型

昇格

昇格規則

条件演算子

条件分岐

小数点

シリアライゼーション

真偽値

シングルディスパッチ

シングルトン

シンボル

す

随伴

数学関数

スカラー

スカラー積

スカラー倍

スクリプト

スクリプトファイル

スコープ

スタック

スタックトレース

ストリーム

スネークケース

スーパースカラー

スプラッティング

スループット

スレッド

スワッピング

せ

正規化

正規化数

正規表現

正規分布

整数

整数仮数

生成関数

静的言語

静的単一代入

精度

正の無限大

セグメンテーション違反

絶対パス

セマンティックバージョニング

線形インデクシング

線形代数

そ

疎

総称関数

相対誤差

相対パス

挿入ソート

添字

疎行列

束縛

ソースコード

ソフトスコープ

疎ベクトル

た

対角行列

代入

タイムゾーン

対話的実行環境

タグ

多項式

多次元配列

多重ディスパッチ

タスク

タプル

単項演算子

単精度

短絡評価

ち

地域時

置換文字列

チャネル

中間表現

抽象解釈

抽象型

抽象構文木

て

定数

低レベル化

低レベル中間表現

デカルトインデクシング

デカルト積

デシリアライゼーション

テスト

データ競合

デバッガ

デフォルト

デフォルトコンストラクタ

デポ

転置

と

同期

投機的実行

統計的プロファイラ

統計量

動的言語

ドキュメント

ドット積

トレイト

な

内部コンストラクタ

内包表記

名前空間

名前付きタプル

名前無しタプル

に

二項演算子

2の補数表現

ね

ネスト並列化

は

バイアス

倍精度

排他制御

排他的論理和

パイプライン

配列

破壊的関数

ハザード

バージョン

パス

パッケージ

パッケージ管理モード

ハッシュ

バッファなしチャネル

ハードスコープ

パラメータ

パラメトリック型

範囲

半精度

反復

ひ

被演算子

引数

非公式パッケージ

非数

非正規化数

ビット演算

ビット配列

否定

否定論理積

否定論理和

非同期実行

非標準文字列リテラル

ヒープ

評価

表現

表現誤差

標準エラー出力

標準時

標準出力

標準ストリーム

標準入力

標準パッケージ

ビルトイン関数

ふ

ファイルシステム

フィボナッチ数

フィールド

フォーマッティング

フォールスシェアリング

不完全な初期化

複合代入演算

複製

複素数

符号

符号付き整数

符号なし整数

浮動小数点数

負の無限大

部分適用

部分配列

部分文字列

不変

不変性

フュージョン

ブランチ

プリミティブ型

プロジェクトファイル

プロセス

ブロードキャスト

プロパティ

プロファイル

フロントエンド

プロンプト

分岐

分岐予測

分散

へ

ベアモジュール

並行

並行プログラミング

並列

並列プログラミング

ベクトル

ベクトル化演算

ベクトル和

ベーシックブロック

別名

ヘルプモード

変換

変数

ほ

ボイラープレート

補完

ボス・ワーカーモデル

捕捉された変数

ボックス化

ホットスポット

ま

マクロ

マクロ呼出し

マシンコード

マッチング

マニフェストファイル

マルチスレッドプログラミング

マルチタスキング

丸め

丸めモード

む

無限大

無条件ジャンプ

無名関数

め

命名規則

メソッド

メタプログラミング

メタ文字

メモリオーダリング

メモリ空間

メモリ割当て

も

文字

文字クラス

文字集合

モジュール

文字列

文字列補間

戻り値

ゆ

融合ブロードキャスト

優先順位

有理数

ユークリッド除法

ユーザCPU時間

ユニオン型

ユニットテスト

よ

要素

予約語

4倍精度

ら

乱数

り

リアクティブプログラミング

リダイレクト

リテラル

リビジョン

リフレクション

量化子

履歴

る

ルックアップテーブル

ループ

ループ展開

れ

例外

レイテンシ

レキシカルスコープ

レジストリ

列挙型

列ベクトル

列優先

レポジトリ

連結

ろ

ロガー

ローカルスコープ

ローカル変数

ロギング

ログイベント

ログレベル

ロジカルインデクシング

ロック

論理演算

論理積

論理和

---

著者紹介

佐さ藤とう建けん太た

博士(農学)

2019年東京大学大学院農学生命科学研究科博士課程単位取得退学

2019年理化学研究所生命機能科学研究センターリサーチアソシエイト

現在 Karolinska Institutet Postdoctoral Researcher

著書 (共著)『1から始めるJuliaプログラミング』コロナ社(2020)

Juliaの日本語コミュニティを主催するなど、@bicycle1885のアカウント名でJuliaの普及活動に力を入れている。

---

本作品は、2023年5月、小社よりKS情報科学専門書として刊行されたものを電子書籍化したものです。

---

◎本電子書籍内の外部リンクに関して

ご利用の端末によっては、リンク機能が制限され正しく動作しない場合があります。また、リンク先のwebサイト、メールアドレス、電話番号は、事前のご連絡なく削除あるいは変更されることもございます。ご了承ください。

---

Juliaジュリアプログラミング大たい全ぜん

2023年7月1日発行

著者:佐さ藤とう建けん太た

©Kenta Sato 2023

発行者 ![](image_rsrcBS2.jpg)橋明男

発行所株式会社講談社

東京都文京区音羽2-12-21

〒112-8001

編集株式会社講談社サイエンティフィク

◎本電子書籍は、購入者個人の閲覧の目的のためにのみ、ファイルの閲覧が許諾されています。私的利用の範囲をこえる行為は著作権法上、禁じられています。

23J0616E

01

---

![](image_rsrcBS3.jpg)
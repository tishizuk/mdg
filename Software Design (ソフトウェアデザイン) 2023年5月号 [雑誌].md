##### **ご利用の前に必ずお読みください** 

本書は紙書籍『Software Design 2023年5月号』(ISBN0582705)を元に製作した電子書籍です。紙書籍とはデザインやレイアウトが異なり、ご覧になる端末により表示が異なる場合があります。表示設定は端末の標準設定を推奨します。配信後に補足訂正等でデータの再配布を行う場合があります。更新方法は購入先の電子書店のヘルプ等をご確認ください。本書は情報提供のみを目的としており、掲載内容の運用結果について技術評論社および著者は一切の責任を負いません。掲載内容は特に断りのない限り執筆時点より以前の情報のため、変更される場合があります。特に、ソフトウェアはバージョンアップされる場合があり、本書での説明とは機能内容や画面図などが異なってしまうこともありえます。以上をあらかじめご承諾の上、ご利用をお願いします。本文中に記載されている製品の名称は、すべて関係各組織、各社の商標または登録商標です。 

**SOFTWARE DESIGN 2023/May** 

## **CONTENTS 5** 

**<u>Special Feature 1 AI・機械学習の理解が進む Python×数学ブートキャンプ必須4分野の基礎を徹底的に固めよう</u>** <u>序章</u> **<u>機械学習/AIと数学の関係「機械学習の3+1ステップ」に見る数学の役割</u>** <u>中井悦司第1章</u> **<u>確率・統計確率分布の基本と確率モデルの考え方</u>** <u>北村友和、小島諒介第2章</u> **<u>微分・積分基本の考え方から多変数関数の微分とその意味まで</u>** <u>辻真吾第3章</u> **<u>線形代数行列計算と一次変換の基本を学ぼう</u>** <u>中井悦司第4章</u> **<u>集合・位相「距離空間」を理解するための第一歩</u>** <u>北村友和、小島諒介</u> 

**<u>Special Feature 2 ブラウザでOSが動かせる!? WebAssemblyの可能性を探る環境に依存しないアプリケーションを作るしくみ</u>** 

<u>第1章</u> **<u>WebAssemblyでできること、できないこと</u>** <u>ポータビリティとセキュリティを活かした成功事例山本悠滋</u> 

##### <u>第2章</u> **<u>動かしてわかるWebAssembly</u>** 

<u>ゼロからはじめるWebAssembly Text Format 福岡秀一郎</u> 

##### <u>第3章</u> **<u>挑戦! アプリケーションのWebAssembly化</u>** 

<u>EmscriptenによるC++アプリケーション出力の実践こーのいけ</u> 

###### **<u>Extra Feature</u>** 

<u>ソフトウェアのセキュリティ&品質管理の新要件SBOM[新連載]</u> **<u>SBOMが注目される理由</u>** <u>小保田規生</u> 

<u>[短期連載]Denoで始めるサーバサイドTypeScript開発[3]</u> **<u>DenoでREST APIを作ろう</u>** <u>ゴリラ(監修:日野澤歓也、田中優亮)</u> 

###### **<u>Á La Carte</u>** 

<u>ITエンジニア必須の最新用語解説[173]</u> **<u>X.1060</u>** <u>杉山貴章</u> 

**<u>読者プレゼントのお知らせ</u>** 

**<u>SD BOOK REVIEW</u>** 

**<u>SD NEWS & PRODUCTS</u>** 

**<u>Reader's Link</u>** 

**<u>Column</u>** 

<u>平林万能IT技術研究所 2ndシーズン[12]</u> **<u>警察庁広域重要指定114号グリコ森永事件の犯人を追え!──「キツネ狩り」で「21面相」を見つけ出せ!編</u>** <u>平林純</u> 

<u>結城浩の再発見の発想法[最終回]</u> **<u>10年間の連載を振り返って</u>** <u>結城浩</u> 

<u>ハピネスチームビルディング[14]</u> 

**<u>レビュー指摘が多い原因がレビュアーにもあることを共有する</u>** <u>小島優介</u> 

<u>エンジニアのためのやる気UPエクササイズ[9]</u> **<u>目指せ! シックスパックエンジニア</u>** <u>えくろプロテイン</u> 

<u>分解教室[最終回]</u> **<u>PlayStation VR2</u>** <u>清水洋治</u> 

<u>ひみつのLinux通信[111]</u> **<u>選択</u>** <u>くつなりょうすけ</u> 

<u>こんなときどうする? エンジニアも知っておきたい法律知識[21]</u> **<u>AI技術を利用したソフトウェア開発契約の法的性質</u>** <u>弁護士山口宏和</u> 

<u>あなたのスキルは社会に役立つ〜エンジニアだからできる社会貢献〜[137]</u> **<u>CIVIC TECH FORUM2022が残したもの</u>** <u>Pen、辰己学、德永美紗、福島</u> 

<u>健一郎</u> 

###### **<u>Development</u>** 

<u>オンラインホワイトボード「Miro」徹底活用術[8]</u> **<u>ワイヤーフレームを作成しよう</u>** <u>山川宏賢</u> 

###### <u>メールセキュリティ対策の現場[新連載]</u> **<u>なりすましメールのしくみと防止技術を知る</u>** <u>中村成陽</u> 

<u>Google Cloudを軸に実践するSREプラクティス[2]</u> **<u>クラウドネイティブなインフラのTerraformによるIaC化</u>** <u>小森裕介、 (監修)キャディ株式会社Platformグループ</u> 

<u>three.jsでお手軽3Dプログラミング[3]</u> **<u>描画ループと時間ベースでのアニメーション</u>** <u>可児亘</u> 

<u>なるほど納得Go言語[4]</u> **<u>Goにおける変数宣言──[前編]ゼロ値と初期化</u>** <u>崎原晴香(H.Saki)</u> 

<u>最強の開発環境探求の道[5]</u> **<u>テキストエディタをカスタマイズする</u>** <u>中山慶祐</u> 

<u>リソースから考えるBCPの手引き[5]</u> **<u>外的リソースへの対応</u>** <u>角道淳平、中村勝敏</u> 

<u>楽しいFlutter〜しくみがわかるともっとハマる〜[最終回]</u> **<u>パッケージの作成と利用</u>** <u>杉本真二</u> 

<u>AWS活用ジャーニー[8]</u> **<u>Amazon Route 53、ACM、ELB</u>** <u>武田隆志</u> 

**<u>OS/Network</u>** <u>魅惑の自作シェルの世界[6]</u> **<u>複合コマンドの実装(前編)</u>** <u>上田隆一</u> 

<u>Pythonでネットワーク自由自在[11]</u> **<u>トラブルシューティングの自動化をPythonで実現してみよう (1)</u>** <u>小澤昌樹</u> 

###### <u>[Ansible]現場を支えるPlaybook[6]</u> **<u>申請書いらずのセルフサービスポータルの実現</u>** <u>中村洋介</u> 

ロゴデザインデザイン集合ゼブラ+坂井哲也表紙デザイン藤井耕志(Re:D Co.) 表紙写真 



/ Adobe Stock 

本文デザイン 

- ●安達恵美子 

- ●石田昌治(マップス) 

- ●近藤しのぶ 

- ●SeaGrape 

●轟木亜紀子、阿保裕美、佐藤みどり、相馬喜代子(トップスタジオデザイン室) 

- ●伊勢歩、横山慎昌(BUCH+) 

- ●堂上未来(Re:D Co.) 

- ●森井一三 

# 第 <mark>173</mark> 回 

### **ITエンジニア必須の最新用語解説** 

**テーマ募集** 

本連載では、最近気になる用語など、今後取り上げてほしいテーマを募集しています。 <u>sd@gihyo.co.jp宛にお送りください。</u> 

**TEXT:(有)オングス杉山貴章 SUGIYAMA Takaaki** **<u>takaaki@ongs.co.jp</u>** 

#### **X.1060** 

##### **企業のセキュリティを強化する「X.1060」** 

近年、企業のビジネス活動における情報システムの重要性が増すにしたがって、それを狙うサイバー攻撃も高度化・多様化の一途をたどっています。サイバー攻撃から組織を守るためには、情報システム部門などの一部だけで対応に当たるのではなく、組織全体としてセキュリティ体制を作り上げる必要があります。そこで、その指針として注目されているのが国際標準の「 X.1060 」です。 X.1060 は、電気通信の標準化を行っている国連機関の「 ITUT (国際電気通信連合電気通信標準化部門)」が 2021 年 6 月に承認した国際勧告です注1 ~~。~~ 日本では情報通信技術委員会( TTC )が日本語版として「 JT-X1060 - サイバーディフェンスセンターを構築・運用するためのフレームワーク」を作成し標準として定めています(後述)。 

X.1060 では、サイバーディフェンスセンター(以下、 CDC )について「ビジネス活動におけるサイバーセキュリティリスクを管理するためのセキュリティサービスを提供する主体」と定義しています。具体的には、経営者や最高セキュリティ責任者が策定したセキュリティポリシーに基づいて、実際に組織内に展開するさまざまなセキュリティサービスを選択し、対策を構築・運用するのが CDC の役割になります。この CDC を組織内においてどのように構築し、継続的に運用していくかをガイドラインとしてとまとめたフレームワークが X.1060 です。 

##### **CDC構築・運用の3つのプロセス** 

X.1060 のフレームワークでは、 CDC の構築および運用について次の 3 つのプロセスが設けられています。 

- ・構築プロセス 

- ・マネジメントプロセス 

- ・評価プロセス構築プロセスは、1セキュリティ対応を行う組織において実施するサービスを選択し、2選択したそれぞれのサービスを誰が実施するのかを割り当て、3現状を認識するためのサービススコアの測定や中⻑期的な目標設定を行う、という 3 つのフェーズで構成されます。 X.1060 では選択や割り当ての基準、スコア測定の指針などが定められています。マネジメントプロセスでは、日々の業務の中でどのようにセキュリティ対策を実施していくのかが示されています。このプロセスでは、短期的なサイクルの中でセキュリティ対策の運用とインシデント対応を行いつつ、⻑期的なサイクルで戦略的に CDC の発展に取り組むことが求められます。評価プロセスでは、構築プロセスの 3 つのフェーズで行った内容についてそれぞれ評価を行い、次の構築に役立てられるようにします。この 3 つのプロセスを 1 サイクルとし、繰り返し継続的に改善を重ねることで、組織のセキュリティ体制を実戦的なものとして維持し続けることが可能になります。 

##### **日本の組織構造との親和性** 

X.1060 は国際標準ではありますが、制定にあたっては経済産業省がまとめた「サイバーセキュリティ経営ガイドライン」や日本セキュリティオペレーション事業者協議会( ISOG-J )による「セキュリティ対応組織の教科書」をはじめ、日本で使用されているセキュリティ組織に関する資料の考え方が積極的に取り入れられています。そのため、日本企業の組織構造や商習慣に合わせやすいという利点があります。 

X.1060 の勧告から約半年後、前述したように同様の内容を日本語ドキュメントとしてまとめた「 JT-X1060 」が制定されました。これは正式な日本標準として認定されているため、国内では X.1060 の代わりに JT-X1060 を利用することもできます。さらに ISOG-J では、 X.1060 における新しい知見を取り込んでより実戦的な内容としてまとめ直した「セキュリティ対応組織の教科書第 3.0 版」を公開しています。これらのドキュメントを活用することで、組織における体系的なセキュリティ体制の構築を推し進め、サイバー攻撃などのリスクへの対応力の向上が期待できるとされています。 

**X.1060** 

<u>https://www.itu.int/rec/T-REC-X.1060-202106-I</u> 

- 注1) 正式名称は「Framework for the creation and operation of a cyber defence centre」。 

   - (本文に戻る) 

### **_平林万能_** 

### **_IT技術研究所_** 

**<mark>2ndシーズン</mark>** 



平林純 (ひらばやしじゅん) <u>@hirax</u> 

# **第** 12 **回警察庁広域重要指定114 号グリコ森永事件の犯人を追え!──「キツネ狩り」で「21面相」を見つけ出せ!編** 

**脅迫・挑戦状で日本を騒がした21面相による「グリコ森永事件」** 

映画『風の谷のナウシカ』が公開された 1984 年。その 3 月から翌 1985 年にかけて起きたのが「グリコ森永事件」です。かい人 21 面相と名乗る犯人が、食品会社に脅迫状を送り、警察や報道メディアには挑戦状を送りつけ、日本中を騒がしました。警察が公開したキツネ目の男の似顔絵を覚えている人もいることでしょう。前回(本誌 2023 年 4 月号)では、 1984 年 12 月に北海道のアマチュア無線家が偶然受信した「 21 面相と玉三郎による 7MHz 帯の無線交信 (北海道テープ)」が、事件の舞台となった京阪地区から届いた電波(無線)であるか? ── を可能性検証しました。検証結果は「京阪地区からの電波である可能性は十分にある」というものでした。 

**北海道テープ、21面相の電波がほぼ受信できなかった理由とは?** 

北海道テープに録音されているのは、ほとんどが玉三郎の声で、 21 面相の声はほぼ録音されていません。「(玉三郎より) 21 面相の電波が少し弱い」という程度でなく「 “ ほとんど ” 届いていない」ということであれば、 

- ・北海道からの21面相や玉三郎の距離差 

- ・21面相と玉三郎による送信電波強度差でなく、 21 面相の送信電波が「北海道に届く幾何条件ではなかった」と推測されます。 

**21面相は7MHz帯アンテナを水平横向き・南⻄に伸ばしてた?** 

前回記事で計算・可視化したように、棒状のアンテナから送信される電波は、おおよそアンテナと垂直な方向に広がっていきます。 21 面相がアンテナを水平横向き、たとえば南⻄方向に伸ばしていたとすると、送信された電波は北海道方向では受信できません( **図** 1)。その一方で、もしも玉三郎のアンテナの向きが垂直もしくは北海道に電波が向かう南東・北⻄方向だったとすると、北海道に電波を届けることができます。つまり、玉三郎と 21 面相の差は、アンテナの向きだったのかもしれません。 



**無線の送信位置を突き止める「キツネ狩り」で21面相を探せ!** 

21 面相と玉三郎の無線交信は、 1983 年の 11 月ごろから 1984 年の 10 月ごろにも、 430MHz 帯で偶然受信されています。受信したのは、大阪府の高槻市や奈良市内のアマチュア無線家で、奈良市内での受信時には「(アンテナの向きを変えて探索した結果)奈良市に対して北北⻄の方向から届く・大阪府北部方面からの電波だった」ということがわかっています。そこで、「無線が送られてくる方向を手掛かりに送信している人 (キツネ)を捕まえる」無線競技 “Fox hunting (キツネ狩り) ” のようにして、「キツネ目の男」ならぬ「かい人 21 面相」の居場所を突き止めてみましょう( **図** 2)。 



**21面相を名乗るキツネがいたのは奈良から北北⻄の見通せる場所!?** 

430MHz 帯の電波は直進性が高く、届くのはほぼ見通しがきく範囲です。そこで、京阪地区の地形・地図情報を手に入れて、奈良市内から見通せる場所を抽出してみます。まず、国土地理院提供注1 ~~の~~ 地理院地図の「 3D 」機能を使い、京阪地区の標高情報と地図画像・衛星画像をダウンロードします( **図** 3)。そして、奈良市内を中心として「地形に遮られずに電波が届く場所」を、 3 次元直線的な周囲への探索を行うことで抽出します ( **図** 4)。 





**21面相が無線交信をしたのは高槻市・⻑岡京市の里山近く!?** 

コードを実行して得られた結果( **図** 5)を眺めると、奈良市内からでは、生駒山地(生駒金剛山脈)や平城宮北部の丘陵に遮られて、北北⻄の方向の「奈良から見通せる場所」はとても限られることがわかります。具体的には「奈良市内に 430MHz 帯の電波が届きそうな場所」の候補となるのは、大阪府高槻市から京都府⻑岡京市の里山近く、に限られます。しかも、里山といっても住宅地があるような場所はほんどなくて、「電波が届く場所で、住宅地があるような場所」は、たとえば⻑岡京市の「野山注2 ~~」~~ 近辺くらいです。この場所からならば、高槻市内もある程度見通せますから、 “ 奈良市内や高槻市内で無線交信が受信された ” という条件に当てはまります。グリコ森永事件の犯人は、⻑岡京市で盗難した車を使い、ハウス食品からの現金強奪を試みました。そして、丸大食品の脅迫時に現れた「キツネ目の男」は、⻑岡京市の神足駅(現 ⻑岡京駅)から列車に乗り込みました。 21 面相というキツネは、⻑岡京市「野山」にいた ...... というのが今回の推理結果です。 

##### **<mark>迷宮事件をデジタルで推理「なるほど、実におもしろい」</mark>** 

迷宮入り事件の事件現場に行かずとも、デジタル技術による分析・捜査ができます。そんな推理をしていると、 TV ドラマ『ガリレオ』の台詞を口にしたくなるかもしれません。「なるほど、それは実におもしろい」 

<mark>本記事コード・補足説明</mark> <u><mark>https://github.com/hirax/SD202305</mark></u> 

注2) 別の奇妙な迷宮事件現場としても有名です。 (本文に戻る) 

### **最終回結城浩の** 

#### **<mark>再発見</mark> の** **<mark>発想法</mark>** 



**結城浩 (ゆうきひろし)** <u>https://www.hyuki.com/ @hyuki</u> 

#### **第120回** 

#### **10年間の連載を振り返って** 



##### **はじめに** 

結城浩の「再発見の発想法」は今回が第 120 回となります。 10 年ひと区切りということで、今回が最終回とさせていただきます。 2013 年の連載開始から⻑きにわたってご愛読くださり、ありがとうございました! 

今回は最終回なので編集部からお許しをいただき、この連載を振り返る内容にしたいと思います。 



##### **連載開始のアイデア** 

プログラマーが「部屋を片付けなくちゃ」という気持ちを「部屋をガベージコレクトしなくちゃ」と軽口で表現することがあります。もちろんこれは、散らかった部屋を「メモリ空間」に見立てて、物品を片付ける作業を「未使用メモリを再利用のために集める 

**ガベージコレクション** 」にたとえて表現しているのです。そのような技術用語と日常生活の関連付けから本連載のアイデアが生まれました。本連載のねらいは、 **技術用語を通して日常生活に新しい発想を得よう** というものです。技術用語の中には技術者の発想が含まれており、そこから日常生活のための新たなヒントを再発見しようというわけです。そのために本連載では技術用語をごく簡潔に紹介し、そこに含まれている大切な発想や考え方を見つけ出し、そしてそれを日常生活に当てはめるとどうなるかを記述する構成にしました。このような、本連載のねらいと記事の構成は 10 年間でまったく変わっていません。連載開始の時点でイメージしていた技術用語は、ほんの十数個でした。先ほども登場した **ガベージコレクション** のほかに、 **バッファ** 、 **プロトコル** 、 **ボトルネック** など、プログラマーが「たとえ」としてよく使う技術用語です。そこからまさか 10 年間も続く⻑い連載になるとは想像もしていませんでした。ほとんどの回は楽しく執筆できたのですが、なかなか適切な技術用語が見つからず苦労したこともありました。そんなときには必ずしも技術用語にこだわらず **パレートの法則** や **アドホック** なども使いました。結果的に、技術用語を中心に 119 個の記事を書き、そこから得られる技術者の発想を日常生活に当てはめた連載となりました。読者さんからも好評をいただき、楽しんでいただけたようです。感謝します。取り扱った技術用語の一覧は Web に置いてありま ~~す~~ <u>注1</u> ~~。~~ この一覧をご覧になれば、良く言えば広範囲の分野から、悪く言えば種々雑多な分野から用語を選択していることがわかります。 



##### **10年間の変化** 

2013 年から現在までで最も大きな変化といえば、もちろん、私たちの社会生活を現在も揺さぶっている新型コロナウイルスの流行でしょう。指数関数的に広がっていく感染をどのように抑えたらいいかという話題で世の中が持ちきりのときに扱った用語は、ずばり **指数関数的爆発** でした(本誌 2020 年 6 月号)。この回は広く読まれるべき記事だと思ったので、編集部から許可を得たうえで雑誌発売までの 1 ヵ月間、無料で PDF を公開させていただきました( 2020 年 4 月 20 日〜 5 月 17 日)。技術用語を通じて、時代の変化を感じるときはしばしばありました。たとえばセキュリティ用語の **ランサムウェア** は以前から存在しましたが、近年になってターゲットが個人から企業に移り、ニュースで大々的に取り上げられることも多くなりました。セキュリティ用語としてはほかに、 Spectre と呼ばれるハードウェアの脆弱性が話題になったときに **投機的実行** という技術用語を扱いました。また、 2020 年 8 月に中国が「 TLS 1.3 と ESNI を組み合わせた通信をブロックしている」という報道があった際には、ESNIを記事にしました。時代を感じさせるのはセキュリティ用語に限りません。クラウドを活用する **クラウドストレージ** や、機械学習・ AI 技術に関連した **過学習** や、暗号資産に関連したNFTなども、時代の流れを感じさせる用語だと思います。 



##### **日常生活とIT** 

連載記事を書いていて気づいたことがあります。それは、現代では IT が日常生活とかなり融合しているという事実です。たとえば **コピー&ペースト** や **インターフェース** は、もはや技術用語というよりは日常用語として当たり前に使っているでしょう。 

OS のプロセス関連の技術用語は日常生活との親和性が良いため、記事が書きやすいと感じました。たとえば **デッドロック** や、 **コンテキストスイッチ** 、それに **アイドル** などです。これは、私たちの日常生活では、複数人が協力し合って作業したり、複数の作業を並行して行ったりすることが多いため、 OS のプロセス管理を比喩として使うのに向いているからだと思います。同様にアルゴリズムの技術用語も、日常生活に関連付けて考えやすかったですね。たとえば、路地で家を探すことを **バックトラック** に見立てたり、部屋に家具を置くときに **ファーストフィット** で考えたりしました。また当然ながらセキュリティ用語は、日常生活にも当てはまりやすいものが多くありました。悪意を持った代理人や中間搾取は **中間者攻撃** の一種になりますし、クレーマーによる頻繁な電話はDoS **攻撃** そのものです。ちょっと変わったところでは、郵便受けに溢れるダイレクトメールや郵便物は、その家の住人が不在であることを示す **サイドチャネル攻撃** になります。 



##### **最後に** 

詩人が生み出す豊かな言葉や表現は、私たちの世界のとらえ方や日々の生活を豊かにしてくれます。それと同じように、技術者が生み出す技術用語は、私たちの発想に新しい視点をこれからも与えてくれるでしょう。新しい技術用語を見聞きしたときには、その意味や使われ方を考えてみてください。そこには、複雑な問題に対する技術的な解決のヒントが隠れているものです。あなたの周りを見回して、同様の問題が日常生活で見つからないかを探してみましょう。技術用語から得た技術者の発想を活かすことはできないでしょうか。これからもぜひ、考えてみてくださいね。 



##### **書籍のご案内** 

本連載は、 SB クリエイティブ社から書籍『再発見の発想法』として刊行されました( 2021 年刊)。連載で取り上げた技術用語のうち 36 個を厳選し、読みやすくグルーピングして加筆修正したものです。よろしければご覧ください。 



### **<u>リモートワークでも楽しいチーム開発をめざす</u> ハピネスチームビルディング** 

**第 14 回** 

**レビュー指摘が多い原因がレビュアーにもあることを共有する** 

小島優介(こじまゆうすけ) <u>@kojimadev kojimadev@gmail.com</u> 株式会社デンソークリエイト 



**レビューで指摘が多いのは作成者のせい?** 

みなさんは、開発経験が少ないメンバーが作った成果物(ソースコードやドキュメント)に対して、レビューで大量の指摘をしたことはないでしょうか。筆者は十年以上前、駆け出しのチームリーダーのころ、そういうことが何度もありました。大量の指摘を修正してもらうという行為は、手戻りであるため、それだけプロジェクトの進捗を遅らせることになりました。また、指摘されたメンバーも、そういうことが何度もあると、レビューされることに対してマイナスのイメージを持ち、開発をあまり楽しく感じていなかったかもしれません。恥ずかしい話ですが、当時の筆者は、 **レビューで大量の指摘が出て手戻りする原因は、成果物の作成者にあると思い込んでいました** 。しかし、今、振り返ってみると、 **手戻りの原因はレビュアー (レビューする人)である筆者にもあった** と思います。今回はそのときの筆者の失敗談を紹介したうえで、開発経験の少ないメンバーが楽しく開発するために、現在のチームでどのように取り組んでいるかを紹介します。 



**レビューの指摘内容** 

当時のレビューは次のようなものです。 

- ・レビュー対象の成果物は、ソースコードもしくはドキュメント (テスト仕様書など) 

- ・作成者は、類似の成果物を作成した経験はあるがその回数が少ない 

- ・レビュアーは、類似の成果物を作成した経験が十分にある 

- ・レビュアーは、自分がその成果物をレビューする役割であることを事前に知っている 

- ・レビュー時間は1時間程度このようなレビューにおいて、筆者は一度に 20 件以上の指摘をしたことが何度かありました。そのレビューで挙がった指摘がどんなものだったのか一例を紹介します。 

- ・以前のレビュー時に指摘した内容とほぼ同じ間違いをしている 

- ・ (事前に対応方針を説明済みであるにもかかわらず)方針どおりに作られていない 

- ・成果物の目的を理解できていないような間違いをしている 

- ・判断理由があいまい(「なぜこのように作ったのか?」と質問すると「なんとなく」と回答される) 

- ・前回指摘した箇所のみを修正し、ほかの箇所で同じ原因の問題を修正できていない上の例のとおり、レビュアーからすると「基本的なことができていない」と思えるような指摘が多くありました。 



**筆者のレビューの反省点** 

前述のように当時の筆者は、多くの指摘が検出されるのは「作成者に原因がある」と思い込んでいました。自分のやり方に原因がある可能性を考えていませんでした。しかし、よくよく考えれば、経験が少ないときは、説明したとおりにできなくて当然です。どのような考え方で作業するのか、何に気をつけて成果物を作成するのかを説明されても、経験がないので具体的にイメージすることができないからです。また、 **レビュアーからすると「基本的なこと」と思うことでもミスをするのはしかたがありません** 。作業に慣れていないと、焦ったり慌てたりして落ち着いて考えられなくなるからです。そのため、 **うまくできないことを前提にして、経験から効率よく学ぶことをサポートしたほうが良い** のですが、筆者はできていませんでした。また、当時の筆者は「 **わからないことや困ったことがあれば、成果物を作り切る前に相談すること** 」を作成者に求めていました。しかし、これも作成者からすれば難しいことです。経験が少ないときは、とにかく成果物をなんとか作ることで頭がいっぱいとなり、自分の考えが足りていないという自覚が持てず、相談する必要性を感じていないことが多くあります。作成者が若手であれば、手戻りを減らすために適切なタイミングで相談するという仕事のやり方に慣れていないこともあります。そのため、 **レビュアーが適切なタイミングで状況を確認する機会を作ることが必要** ですが、筆者はそれもできていませんでした。それと、そのころのレビューでは、 **悪い点ばかりを指摘して、良い点に対するポジティブフィードバックをしていませんでした** 。しった「できていないところを叱咤する」より「できたところを称賛する」ほうが教育効果が高い(エンハンシング効果など)と言われていますが、その反対のことをやっていました。ダメ出しばかりを受けては、モチベーションが低下して、その後の生産性も下がる可能性が高くなります。そのうえ、当時、筆者は「レビューは育成するために最適の機会」と思っていたので、今後どういう考え方で取り組むと良いかなども併せて指導していました。それは作成者の成⻑ のために良かれと思ってやったことですが、作成者の立場からしたら、何十件も指摘されたうえで、そんな指導をされても、もはや頭に入らず、説教のように聞こえる可能性が高いでしょう。 **育成目的で指導するのであれば、作成者のモチベーションを高めて主体性を発揮させたほうが良い** のですが、筆者はできていませんでした。 



**現在のチームでの取り組み** 

現在のチームでは、同じ前提で成果物を作成してもらう際は、ペアプログラミング(以下ペアプロ)もしくはモブプログラミングを実施しています。 **ペアプロで、その成果物をどのように考えて作るのかを体験してもらって、その考え方をしっかり理解してもらうことが重要だからです** 。対象成果物がソースコードであってもドキュメントであっても、同じようにペアプロで考え方を理解してもらうことを優先しています。そうすれば、先に挙げた指摘内容の多くは事前に除去できます。わからないことや困ったことがあっても随時質問してもらって解消できます。もしかしたら当時の筆者は「ペアプロをする時間なんてない」と言うかもしれませんが、「大量の指摘をするレビュー時間+修正確認で何度も差し戻す時間」と「ペアプロにかかる時間」は、それほど変わらないと今は感じています。また、すべてをペアプロで作らなくても、最初の 1 時間だけでもペアプロをする効果はあります。何より、ペアプロで考え方をしっかり学んでもらったほうが、その後の生産性は上がります。かつての筆者はそれをしなかったため、その後も大量のレビュー指摘を繰り返していました。ペアプロがメンバーの成⻑に大きな効果があるのは間違いありません。筆者のチームで感じたペアプロでのメンバーの成⻑については「ペアプロ・モブプロでメンバーが驚くほど成⻑した話」という Qiita の記事 <u>注1</u> ~~に~~ まとめていますので、ご参照ください。また、ペアプロなら、大量の指摘で駄目出しすることもないので、作成者のモチベーションを下げません。むしろ、 **ペアプロの大きなメリットは楽しいこと** であり、相手を称賛するチャンスがたくさんあるので、モチベーションを上げる効果もあります。ペアプロの会話を通して楽しく感じてもらえる指導のやり方については、本連載の第 4 回(本誌 2022 年 7 月号)の「コーチングプログラミングで楽しく成⻑する」注2 ~~を~~ ご参照ください。そして、 **もう** 1 **つ大事なことは「レビューで大量の指摘がある場合、その一因はレビュアーにもある」ということをチームで共有** することです。それを共有していれば、チームの誰がレビュアーになっても、筆者と同じ失敗を回避できます。今回紹介した考え方が、開発経験の少ないメンバーがレビューを恐れずに楽しく開発するための参考になれば幸いです。 



### **<u>オンラインホワイトボード「Miro」徹底活用術</u>** 

###### **無限に広がる共創ツールでチームを活性化しよう** 

**第 8 回ワイヤーフレームを作成しよう** 



Agile Studio 山川宏賢(やまかわひろさと)(株)永和システムマネジメント <u>https://www.agile-studio.jp/</u> 

こんにちは、 Agile Studio の山川です。みなさん、ワイヤーフレームは作っていますか? **ワイヤーフレーム** とは、 Web デザインやアプリ開発においてレイアウト作成に役立つフレームワークで、 Web サイトやアプリ上の要素をビジュアル化したものです。ワイヤーフレームを作れるツールには Figma 、 Adobe XD などたくさんありますが、実は Miro でも作れます。今回はワイヤーフレーム作成における Miro のオススメポイントや使い方を紹介します。 

##### **簡単な操作で誰でもワイヤーフレームが作れる** 

Miro では、図形やアイコンをドラッグ&ドロップなどの操作でワイヤーフレームを作っていきます。テンプレートもいくつか用意されているので、ワイヤーフレーム作成の経験がないチームでも始めやすくなっています注1 ~~。~~ 

専門知識が不要なため、チームメンバー全員がワイヤーフレームの作成に参加できます。見た目の細部にこだわったものを Miro で作ることは難しいですが、 **ワイヤーフレームの作成を属人化させずチームメンバー全員の所有物にできる** という点が、 Miro でワイヤーフレームを作る一番のオススメポイントです。 

##### **Miroでワイヤーフレームを作ってみる** 

簡単な操作でワイヤーフレームを作成できると説明しましたが、 Miro で作成する際のコツがいくつかあります。今回は **図** 1のワイヤーフレーム作成をお題として、作成のポイントを順番に紹介します。 



##### **画面フレームを置く** 

まずは **画面フレーム** を置きます。アプリの中からワイヤーフレームライブラリを選択すると、 **図** 2が開きます。画面に使われる多くの部品が用意されていることがわかりますね。一番上に「スマートフォン」「タブレット」「ブラウザ」という基本となるワイヤーフレームがあるので、その中から 1 つを選んで置いてみましょう。今回はスマートフォン向けのレイアウトを作るので、「スマートフォン」を選択して好きな場所に配置します。一度選択したあとに続けていくつもフレームを置けますが、今回はホワイトボード外をクリックして終了します。このスマートフォンの形を模したフレームの上に、さまざまな部品などを置いてレイアウトしていくことになります。なお、選択したメニューの[デバイスを表示]で、デバイス(黑い枠の部分)の表示有無を切り替えたり、スマートフォンとタブレットでは[回転]アイコンで回転させたりすることもできます( **図** 3)。 



##### **図形を置く** 

図形を使うと、イメージしている画面により近づけられます。たとえば画面にヘッダーやフッターを作りたい場合、図形から四角形を選択し、必要に応じてテキストを追加するだけでできあがりです。また、部品を置くための「枠」や説明用の「吹き出し」も図形で表現できます( **図** 4)。ワイヤーフレーム作成で図形はマストアイテムです。使いこなせば図形だけでたいていのものは作れます。ちなみに、 **図** 5のナビゲーションメニューも図形のみで作成しています。 



##### **入力部品を置く** 

ワイヤーフレームのコンポーネントメニューから、「ボタン」「テキスト入力欄」「チェックボックス」など、さまざまな入力部品が選択できます( **図** 6)。 





**「見る」ページを作る** 

**図** 1の完成イメージの「見る」ページを作成するために、入力部品を置いていきましょう。まずは写真の部品を配置します。写真の部品は「小さなカード」または「大きなカード」を配置します( **図** 7)。カードは色の選択はもちろん、表示形式の選択も 3 種類から可能なので、「小さなカード」を配置した後に表示形式を「大きなカード」に変更することもできます。次は New マークの部品を配置します。 New マークは「チップ」を配置することで表示できます( **図** 8)。色や塗りつぶしのタイプも自由に選択できます。最後にスクロールの部品を配置します。スクロールは「スクロール」を配置することで表示できます。 **図** 9を見るとスクロールできるイメージが伝わりますね。 





**「探す」ページを作る** 

続いて、 **図** 1の完成イメージの「探す」ページを作成するために入力部品を置いていきましょう( **図** 10)。まずは検索項目の部品を配置します。検索項目は「ラジオボタン」を配置することで表示できます( **図** 11)。色や部品の状態(未選択/選択済み/無効)が選択できます。なお、似たような部品には「リスト」や「チェックボックス」があります。次に検索ボタンを配置します。検索ボタンは「ソリッドボタン」または「ボタン」を配置することで表示できます。これらの部品も状態(有効/無効)を選択できます( **図** 12)。無効にすると、選択できない状態がひと目でわかるような見た目に変更できます。また、表示形式も「塗りつぶし」「ライン」「テキストリンク」の 3 種類から選択できます。旗マークを選択するとアイコンが選択できるようになっており、ボタン内にアイコンを配置することも可能です。 



##### **アイコンを置く** 

ヘッダータイトルの左右にナビゲーションメニューのアイコンとアバターアイコンを配置します。ワイヤーフレームの「アイコン」からイメージに合うアイコンを見つけて配置します( **図** 13)。アイコン数が多いので、検索して見つけるのがお勧めです。検索する際は英語で検索しないと結果が表示されないので注意してください。 





**アバターアイコンを作る** 

アバターアイコンには、ワイヤーフレームライブラリーの中に用意されているものを使うのもよいですが、画像を使うことでリアリティのあるアバターアイコンを作ることができます。使いたい画像を貼り付けて、 **図** 14 **左** のように画像選択時のメニューから1トリミングのアイコンを選択し、トリミングの形状を選択するリストが表示されたら2円形を選びます。これで画像が円形になります( **図** 14 **右** )。よくあるアバターアイコンが作成できましたね。あとはオブジェクトのサイズを適宜変更するだけで、すぐに使えるオリジナルアバターアイコンの完成です。 

##### **画面や状態の遷移を表す** 

画面や状態が遷移するイベントは線で表します。始点となるオブジェクトを選択し、上下左右に表示される丸印をクリックすることで線を引くことができます( **図** 15)。また、線はカスタマイズすることもできます。色や形はもちろんですが、線に図形や文字を挿入することもできます( **図** 16)。補足の文字を入れる際の手間が少ないので、文字を挿入できる機能はとても便利です。 



###### ◆ ◆ ◆ 

今回はワイヤーフレーム作成における Miro のオススメポイントや使い方を紹介しました。これまでの連載では無料プランで利用できる機能を中心に紹介しましたが、次回は有料プランで広がる使い方を紹介します。注1) 詳しくはテンプレート画面で「wire frame」と検索して探してみてください。 (本文に戻る) 



<!-- Start of picture text -->
フィジカル&メンタルに効く<br>エンジニアのための<br>やる気UPエクササイズ<br>第9回目指せ!シックスパッ<br>クエンジニア<br>えくろプロテイン株式会社Gozal/CTO YouTuber/えくろプロテイ<br>ンちゃんねる<br>@ekuro0513<br><!-- End of picture text -->

##### **シックスパックも夢ではない** 

こんにちは。健康オタクエンジニアのえくろです。突然ですが、シックスパック注1 ~~エ~~ ンジニアってかっこよくないですか? 「イケメンに限るでしょ ...... 」と思われた方もいるかもしれません。しかし、オクラホマ州立大学の研究によると、女性は顔と同じくらいかそれに次いで男性のお腹周りを見ているということがわかっています。さらに、理想的なウエストサイズは胸囲を 1 とした場合に 0.7 であることも報告されています( 0.9 、 0.8 、 0.7 を比較した場合)注2 ~~。~~ つまり、見た目においてはお腹周りをスッキリさせることが非常に大切と言えます。とはいえ、「キツい運動は無理」「何から始めたらいいかわからない」という方も多いでしょう。ということで、今回は効率的なシックスパックの作り方やエンジニアにお勧めの手っ取り早くお腹周りをスッキリさせるエクササイズを紹介します。 

##### **シックスパックになるために必要なこと** 

シックスパックというと、まず腹筋運動を思い浮かべるかもしれませんが、それはあまり効率の良い方法ではありません。実はすでにみなさんの腹筋は割れています。割れている腹筋の上に脂肪が乗っているため、割れているように見えないだけなのです( **図** 1)。ですから、シックスパックになるために必要なのは腹筋をすることではなく、「脂肪を落とすこと」です。それでは、脂肪を落とすために必要なことは何でしょうか。それはカロリー収支をマイナスにすることです。 **図** 2はダイエットピラミッドと言って、ダイエットの基本ルールを示したものです。土台やの部分ができていなければいくら上の部分に気をつけても痩せることはできません。つまり、カロリー収支がマイナス(消費カロリー >摂取カロリー)にならない限り、いくらサプリを飲んでも痩せられないのです。「消費カロリーということは結局運動か ...... 」と思われた方、実はダイエット初期においては運動をしなくても食事制限で十分に体重を減らせることがわかっています注3 ~~。~~ 

また、仕事で忙しい方にとって運動する時間を確保するのは難しざせつく、それが原因で挫折してしまう人も多いです。そのため、まずは食事制限でしっかり成功体験を積み上げてダイエットに弾みをつけるのがお勧めです注4 ~~。~~ 

それでは、運動は必要ないのかというとそんなことはありません。食事制限だけでダイエットをしているとどこかのタイミングで体重の減りが落ち着いてきます。そのタイミングで運動も取り入れてください。運動は消費カロリーを増やしてくれるだけでなく、食事制限よりも内臓脂肪を減らしてくれることがわかっています注5 ~~。~~ ちなみに、最も痩せる運動は「会話しながら実施できるくらいの有酸素運動を 30 分以上続けること」です。 Apple Watch などで心拍数を測れる人は、「( 220 -年齢)の 50 〜 70 %」の心拍数を目安にしてウォーキングやランニングをしてみてください。逆に、早く痩せたいからといって有酸素運動をやり過ぎたり過度な食事制限をしたりすると、基礎代謝が低下して痩せにくい体質になってしまいます。消費カロリーと摂取カロリーの差が 500kcal 以内のカロリー収支にすることで筋肉をそれほど落とすことなく脂肪を落とせることがわかっています。そのため、焦らずコツコツとカロリー収支を調整しましょう注6 ~~。~~ 

ここまでは、シックスパックになるために、まず「 1 .食事制限をしてカロリー収支をマイナスにする」「 2 .適度な運動をして内臓脂肪を燃やす」ことについてお話ししました。しかし、これらは一朝一夕で効果が出るわけではありません。 

##### **お腹を凹ますエクササイズ** 

もっと早く効果を実感したいという方向けにお勧めのエクササイズがあります。それが「ドローイン」です。ドローインとは体幹を支えるインナーマッスルのエクササイズで、姿勢を良くしたり下腹を凹ませたりする効果があります。脂肪を燃焼させるエクササイズではありませんが、手っ取り早く見た目を変えるにはもってこいです。とくにデスクワークによって姿勢が悪くなっているエンジニアや、お腹の力が緩んでしまい内臓の位置が下がってしまっている人に効果的です。やり方は簡単で、仰向けになって下腹を凹ますだけです( **写真** 1)。息をはきながら腰と床のすき間を埋めるようにお腹を凹ませてください(イメージしづらい方は腰の下に手をいれて手をつぶすイメージでやってみてください)。 10 秒キープを 1 日 10 回やってみましょう。実際の動きは次の動画で確認してください。 

<u>https://vt.tiktok.com/ZS8V3qoy8/</u> 



ドローインは慣れてくれば立った状態でもできるようになります。そうすれば仕事中でもすき間時間を見つけて鍛えられます。ぜひマスターして今夏はシックスパックエンジニアを目指しましょう! 

注1) 腹筋が6つに割れているように見える状態のこと。 (本文に戻る) 注4) 本連載の第7回(本誌2023年3月号)で「楽に食事量をコントロールする方法」についても紹介しているので、興味ある方はそちらもご覧ください。 (本文に戻る) 

#### **<mark>百聞は一見に如かず</mark>** 

### **<mark>分解教室</mark>** 



清水洋治 (しみずひろはる) (株)テカナリエ代表取締役 CEO http://www.techanalye.com/ 

### **最終回 PlayStation VR2** 

##### **VR機材の発展** 

次世代の大きな期待デバイスの 1 つに VR があります。 Meta や HTC 、 PICO など Wi-Fi でインターネットに直接接続し使用する機種が増えています。そんな中 SONY は、 PlayStation 5 に接続して使用する PS VR2 を 2023 年 2 月に販売開始しました。 PS5 の初代機の発売が 2020 年、 3 年目の VR 投入となっています。 

**図** 1は、 PS VR2 の梱包箱( Horizon Call of The Mountain 同梱版)と内部梱包物の一覧です。 PS4 に接続して使用する初代 PS VR と一番異なるのは左右の手に持って使う Sense コントローラが備わったことです。 PS5 と VR2 は USB-C を介して有線で接続されます。配線は 4.5m なので固定場所で使うのには十分の⻑さですが、 VR2 を装着したまま、隣の部屋などに移動するには若干短いものになっています。付属のイヤホンをそのまま使うこともできますが、 3.5mm のオーディオ端子があるので、ヘッドフォンや別のイヤホンを使うことも可能です。 



**PlayStation VR2 Senseコントローラの中身** 

**図** 2は、 Sense コントローラの分解と内部部品一式の様子です。左右は対称になっています。内部には電池、タッチパッド、バイブレータ、モーションセンサー、通信チップなどを搭載した基板、充電用の USB-C 端子などになっています。握りの部分に上記は収められています。コントローラの円形部には LED ライトが備わっています。 



**図** 3は、 Sense コントローラ基板上の主要チップの様子です。 Wi-Fi 通信とコントローラ機能を持った SoC は SONY 製の CXD90064GG が採用されています。 CXD90064GG は、 PS VR2 だけでなく、 PS5 本体に付属されるコントローラにも採用されるチップです。 CXD90064 の電源を最適化する電源 IC は、ルネサス製が採用されています。また 6 軸モーションセンサは TDK 製が採用されています。昨今の製品では日本メーカーの半導体の採用比率が低いものとなっていますが、 PS VR2 では日本メーカーのチップがもっとも多いものとなっているという特徴があります。 



##### **ヘッドセット本体の分解** 

**図** 4は、 PS VR2 のヘッドセットの分解の様子です。本体はヘッド固定部と画像処理部でできています。固定部は配線経路になっており、チップなど部品は入っていません。画像処理部はディスプレイとプロセッサ基板と外部カメラの 3 つの機能が組み合わされています。 



**図** 5は、 PS VR2 の画像処理側の 3 部品の様子です。プロセッサ基板、 4 カメラ(外部映像)、左右の映像を映し出すディスプレイとなっています。 



**ヘッドセット基板の分解** 

**図** 6は、 PS VR2 の画像処理基板のメインチップです。 SONY 製の CXD90067 プロセッサと CXD90068 電源 IC のチップセットを活用しています。 



**表** 1は、 PS4 向けの初代 PS VR と PS5 向けの PS VR2 の比較です。初代ではシングルディスプレイだったものが VR2 では左右分離のディスプレイ構造になり、また画像処理プロセッサも米 MARVELL 製から SONY 製に切り替わっています。 



**表** 2は、 SONY が PS5 、 PS VR2 で開発した新チップの一覧です。 CXD9006X 、 8 チップが新規に開発され、 PS5 プラットフォームができあがっているのです。今後も半導体の進化が、新しい機能、プラットフォームを作っていくことになります。今回で最終回になりますが、当社は今後もハードウェア進化を観察し続けてまいります。ありがとうございました。 





# **序章** 

### **機械学習/AIと数学の関係「機械学習の3+1ステップ」に見る数学の役割** 



中井悦司(なかいえつじ) <u>@enakai00</u> 

##### **Pythonを使って数学を学ぼう** 

本誌の愛読者のみなさんはよくご存じのように、本誌では、これまでに何度か「数学」をテーマにした特集が組まれてきました。機械学習ブームが広がる中で、「 IT エンジニアが数学をどう学ぶべきか」という話題が聞こえるようになってきた当初、「機械学習を使いたければライブラリをインポートすればいいだけ。数学なんか知らなくてもいい」という過激な意見を見かけることもありましたが、機械学習のしくみを理解して、実践的に使いこなすうえでは、やはり数学の知識は欠かせないでしょう。 

── と言っても、数学にもさまざまな分野があり、数学に馴染みのない方は、どこから手をつければよいのか ...... と悩むかもしれません。この特集では、機械学習との関連が深い、次の 4 つの分野について、それぞれの基礎を学びます。 

- ・確率・統計 

- ・微分・積分 

- ・線形代数 

- ・集合と位相さらに、我々 IT エンジニアには、プログラミング言語という強い味方があります。各章では、 Python のコードを用いて、数値計算やグラフによるビジュアライゼーションで実際の計算結果を確認します。あるいは、計算手順をアルゴリズムに置き換えて、自動計算するプログラムを自分で実装してみるのもおもしろいかもしれません。この特集を通じて、「 IT エンジニア的な数学の学び方」をぜひ発見してください。 

##### **「機械学習の3+1ステップ」に見る数学との関係** 

さきほど紹介した 4 つの分野は、機械学習とどのように関わってくるのでしょうか? ここでは、「機械学習の 3 + 1 ステップ」とひも付けて紹介してみましょう。これは、機械学習モデルを利用する際の流れを整理したもので、次の 4 つのステップになります。 

1. 機械学習モデルを設計する 

2. 誤差関数を定義する 

3. 誤差関数を最小化するようにモデルを学習する 

4. 学習済みモデルの動作を分析するモデルを作って予測処理を行うには、 1 〜 3 のステップで十分ですが、最近は、 Explainable AI (説明可能な AI )というキーワードとともに、入力データをモデルがどのように処理して予測しているのか、すなわち、「予測の理由」を説明するための技術も注目されるようになりました。これが最後の 4 のステップです。そして、それぞれのステップに関連するのが、先ほどの 4 つの分野なのです( **図** 1)。 





###### **「線形代数」は機械学習モデルの基本要素** 

機械学習のモデルは、入力データを受け取ったあと、一定の計算処理を行って予測結果を出力します。これは、まさに数学の「関数」にあたります。機械学習の研究が進む中で、さまざまな種類の関数を用いたモデルが考案されてきましたが、その中で最も基本となるのが一次関数を利用した「線形モデル」です。 

**図** 2のように、平面上の 2 種類のデータを直線で分類するモデルは、その一例です。この例では、入力データは _x_ 1 、 _x_ 2 の 2 つしかありませんが、実用的なモデルでは、時には数千種類を超える、膨大な種類のデータを入力します。高速に予測できるという特徴があり、ニューラルネットワークが実用化される以前は、さまざまな場面で活用されてきました。現在では、ニューラルネットワークが主流になっており、線形モデルを利用する場面は減っていますが、一次関数の重要性は変わりません。ニューラルネットワークと言えば、 **図** 3のように多数の「ノード」が組み合わされた図が登場しますが、個々のノードは、「一次関数+活性化関数」という組み合わせでできており、これを多段に積み重ねることで入力データに隠された特徴を抽出していきます。この一次関数の性質を徹底的に調べるのが線形代数です。線形代数の教科書では、さまざまな行列の計算が登場しますが、行列計算には「複数の一次関数をまとめて計算する」という役割があります。複数の種類にデータを分類する「線形多項分類器」などは、まさに複数の一次関数による予測を行っており、そのしくみは、行列計算によってスッキリと理解できるでしょう。 



###### **誤差関数に隠された「確率・統計」の考え方** 

機械学習のモデルが用意できたら、次はモデルの学習処理を行います。この際に必要になるのが「誤差関数」です。先ほど触れたように、機械学習モデルの実体は数学の「関数」ですが、その中にはチューニング対象のパラメータが含まれています。たとえば、 **図** 2 の線形モデルは、次の一次関数で表されます。ここでは、 _w_ 0 、 _w_ 1 、 _w_ 2 の 3 つがチューニング対象のパラメータで、この値を変化させると、対応する分割線が変化します。そして、このモデルが「最も適切に分類できる」ためのパラメータ値を見つけることが、学習処理にほかなりません。そのためには、「何を持って適切に分類できているとするのか」を決める基準が必要になります。これを与えるのが誤差関数です。誤差関数の値が小さくなるほど、より高い精度で分類できることになるので、誤差関数の値がなるべく小さくなるようにチューニングしていきます。素朴に考えると、分類精度、つまり、分類の正解率を基準にして、これが大きくなるようにチューニングすればよいと考えるかもしれませんが、実はそうはいきません。この次に説明するように、機械学習ライブラリは、微分計算をもとにした「勾配降下法」のアルゴリズムでパラメータをチューニングするため、誤差関数は微分可能な連続関数でないといけません。たとえば、データ数が 100 個だとすると、正解率の値は、 1/100 、 2/100 、 3/100...... と飛び飛びの値を取るので、連続関数にはならないのです。そこで、一般には、モデルが正しく予測する「確率」を計算して、この確率をもとに誤差関数を定義します。機械学習ライブラリを使っていると、「カテゴリカルクロスエントロピー」といった名前の誤差関数が登場しますが、これらは確率・統計の世界の用語で、まさに確率をもとにして計算されています。機械学習で確率・統計を利用する場面は、誤差関数だけではありませんが、確率・統計を勉強すれば、まずはこのあたりから機械学習との関連が見えてくるかもしれません。 



###### **微分計算で実現する勾配降下法** 

誤差関数が決まったら、次は、勾配降下法で誤差関数の値を小さくするようにモデルのパラメータをチューニングします。勾配降下法のしくみは、 **図** 4を見れば一発で理解できるでしょう注1 ~~。~~ 左の 3 次元のグラフは、誤差関数を模式的に表したもので、水平方向の 2 軸がパラメータの値、垂直方向の軸が誤差関数の値になります。誤差関数が最小になる点は、このグラフの「谷底」にあたるので、適当なパラメータ値から出発して、誤差関数の斜面を下っていけば、誤差関数の最小値にたどり着きます。一方、その右にあるのは、誤差関数を微分して求めた「勾配ベクトル」の様子です。これは、それぞれのパラメータ値において、その点から誤差関数の斜面を上っていく方向を示します。したがって、斜面を下るには、勾配ベクトルの反対方向にパラメータの値を修正すればよいのです。このように、機械学習ライブラリでモデルの学習を行う際、その内部では、「微分計算で勾配ベクトルを求めたうえで、その反対方向にパラメータを修正する」という処理が繰り返されます。微分・積分の知識があれば、学習が進捗する様子を見ながら、その背後で行われている処理が理解できます。学習がうまくいかないときの対処法など、一歩踏み込んだ理解にもつながるでしょう。 



###### **「集合と位相」で機械学習のしくみを理解する** 

そして最後に求められるのが、学習が終わったモデルの動作を分析するという作業です。たとえば、最近の機械学習モデルでは、「潜在空間」にデータの特徴量をマッピングするという処理がよく行われます。 **図** 5は、 word2vec と呼ばれる機械学習モデルで有名になった図ですが、 4 つの英単語が 3 次元空間にマッピングされており、それぞれの単語の意味上の関係と空間上の位置関係に何か関連性がありそうです。このような潜在空間におけるデータの分布を分析することで、このモデルが学習した内容を調べることができます。そして、このような空間上に散らばったデータの関連性を理解する際は、集合と位相の考え方が役に立ちます。数学の中でも少し抽象度の高い分野ですが、機械学習のしくみをより深く理解する道具として役に立つでしょう。そういえば、以前に筆者が公開したブログ記事「機械学習におけるカーネル法について」注2 ~~が~~ 注目を集めたことがありました。 **図** 6 はこのブログ記事からの引用ですが、これは与えられたデータを高次元の空間にマッピングすることで、分類を可能するというカーネル法の原理を説明したものです。ここにもまた、集合と位相の考え方が隠されていそうです。 



##### **さいごに** 

ここでは、「機械学習の 3 + 1 ステップ」にひも付けて、数学の 4 つの分野と機械学習の関わりを紹介しました。ただし、これらはあくまで一例にすぎません。実際には、機械学習のさまざまな場面で、さまざまな分野の数学が関連しています。本特集を通じて、 4 つの分野のどれか 1 つにでも興味を感じたら、まずはそこから勉強を進めるとよいでしょう。 

> 注1) 図4のグラフを表示するコードは、http://bit.ly/3XUljog で公開しています。コードの説明は書籍『JAX/Flaxで学ぶディープラーニングの仕組み』(中井悦司著、マイナビ出版、2023年)を参照してください。 

- (本文に戻る) 

# **第 1 章** 

### **確率・統計** 

#### **確率分布の基本と確率モデルの考え方** 

北村友和(きたむらともかず) <u>@pkitamura</u> 小島諒介(こじまりょうすけ) <u>@kojima_r</u> 株式会社スクラムサイン <u>https://scrumsign.com</u> 

**機械学習モデルの受託開発を行っている株式会社スクラムサインでは、モデルの説明性の観点から確率モデルを取り扱うことも多いです。本章では、確率・統計の基本的な考えから、実務にも応用していける確率モデリングまで、Pythonのコードを参考に理解を深めていくことにします。** 

##### **代表値・統計量** 

まずはデータ解析において、データから計算される何らかの特徴を表現した数値である「統計量」に関して説明します。データ全体を見てもデータが多すぎてわからない場合などが多いので、データ分析において最初に確認すべき量です。 



###### **変数と確率変数** 

統計で扱う変数とプログラミングで扱う変数の関係について、簡単に述べておきます。変数とは「値が変化するもの」です。プログラミングでは変数 _x_ に対して x=1 のように値を代入しますが、 x=2 の場合もあれば、 x=3 の場合もあります。統計ではこういった確率的にいろんな値をとる「確率変数」を扱います。 



###### **平均・中央値・分散・標準偏差** 

同一の性質を持つ数値が複数あるときに、それらの集団の代表の値(代表値といいます)を知りたいことがあります。代表的な代表値は、平均値( mean )や中央値( median )です。実際のデータ解析で代表値を計算する場合は、データの特性に応じて適切に用いることが重要です。平均値はデータを合計したものをデータ数で割った値です。各データの値が _x_ 1 、 _x_ 2 、 ... 、 _x n_ である _n_ 個のデータがあるとき、その平均 _x_ は、として計算できます。 Σ (シグマ)はその中の式( _x i_ )を _i_ = 1 から _n_ まで足し合わせるという意味です。中央値はデータを大きさ順に並べて中央に位置するデータの値です。偶然に大きい値もしくは小さい値をもつ「外れ値」があった場合に影響を受けにくいといった特性があります。平均値や中央値に加え、集団の中での値のばらつきを表す指標として、分散( variance )や標準偏差( standard deviation )があります。分散 σ<sup>2</sup> は平均 _x_ を用いて、次のように表します。この式はデータのそれぞれの値と平均との差を 2 乗して足し合わせたものをデータ数で割ったものです。意味合いとして、そのデータが平均からどれだけのばらつきがあるかを表したものとみなせます。さらにこの値の平方根をとったものが標準偏差です。平均値や中央値を計算した際にはこれらの値も同時に計算し、ばらつきを確認するようにしましょう。 

**図** 1では「外れ値」がない場合とある場合それぞれに関して、平均値、中央値、標準偏差を計算しています注1 ~~。~~ 

###### **▼図1 平均値・中央値・標準偏差の計算** 

**import numpy as np** 

**<mark># 外れ値なし</mark> x=[3,4,5,3,4,5,3,4,5] print("平均値:", np.mean(x)) print("中央値:", np.median(x)) print("標準偏差:", np.std(x))** **<mark># 外れ値あり</mark> x=[3,4,5,3,4,5,3,4,5,100] print("平均値:", np.mean(x)) print("中央値:", np.median(x)) print("標準偏差:", np.std(x))** 

**平均値: 4.0 中央値: 4.0 標準偏差: 0.816496580927726 平均値: 13.6 中央値: 4.0 標準偏差: 28.810414783546594** 



###### **2変数間の関係** 

先ほどは 1 つの変数に複数の値がある場合に関して扱いましたが、 2 つの変数があるときに、その 2 つがどういった関係にあるのかを知りたいことは多いでしょう。たとえば、天気と製品の売り上げにどういった関係があるのかなど実用においても 2 つの関係はよく議論に上がると思います。天気を表す変数と売上金額(万円)の変数の 2 つの関係を [(' 晴れ ',120),(' 晴れ ',150),(' 曇り ',100),(' 雨 ',70)] のようにタプルのリスト形式で表すことにしましょう。ここで「 ' 晴れ ' 」「 ' 曇り ' 」「 ' 雨 ' 」のような大小関係がない値をとっている変数を名義尺度、もしくはカテゴリ変数と呼び、数値の変数とは異なる扱いをする必要があります。プログラミングで言う「型」に近い概念です。実際の分析ではこのような変数の種類(さらに細かく変数を分けることも多いです)に気をつける必要がありますが、変数の種類が異なると取り扱いが難しいため、ここでは簡単のために天気の代わりに気温を用いて数値化した変数を扱うことにします。たとえば、ある日の気温が 15°C で、その日の売上金額が 120 万円のとき、 

(15,120) のように組で表し、 1 週間の記録をリストを用いて表したとしましょう。これを売上金額の変数を縦軸、気温の変数を横軸にとって散布図をプロットすると **図** 2のようになります。これらの 2 つの変数には比例関係があることが見てとれます。 

###### **▼図2 売上金額(万円)と気温( °C )の散布図** 

**import matplotlib.pyplot as plt pair_list=[(15,120),(14,80),(17,150),(16,150),(16,110),(15,100),(13,70)] plt.scatter([x for x,y in pair_list], [y for x,y in pair_list])** 

**<matplotlib.collections.PathCollection at 0x7fd5c0b84fd0>** 



こういった、 2 つの数値変数間の関係を扱う方法の 1 つを「相関」と言います。相関とは、直感的には、一方が変化した際に他方も変化するように相互に関係しあうことを定量化したものです。この関係の強さは「相関が強い」「相関が弱い」という表現をします。具体的には、相関係数は- 1 から 1 の範囲をとり、- 1 や 1 に近ければ相関が強く、 0 に近いほど相関が弱いということを表します。 2 つの変数組( _x_ , _y_ )が _n_ 個あり、その _i_ 番めを( _x i_ , _y i_ )と書くとき、これらの変数の相関の関係は「ピアソンの積率相関係数」と呼ばれます(単に相関係数とも言います)。相関係数 _r_ を求めるには、 _x i_ の平均 _x_ と _y i_ の平均 _y_ を用いて、次のように計算できます。数式的にはやや複雑なため、 **図** 2の相関係数を **図** 3のようにプログラムで計算してみます。結果を見ると相関が高いことがわかります。数式をプログラムに落とし込むにはやや慣れが必要ですが、見比べてもらえれば、同じ計算をしていることがわかると思います注 <u>2</u> ~~。~~ また、このプログラムを使ってデータの値を変えると相関係数も変化することが確認できるので遊んでみてください。 

###### **▼図3 相関係数の計算** 

**x_i = [x for x,y in pair_list] y_i = [y for x,y in pair_list] x_bar = np.mean(x_i) y_bar = np.mean(y_i) numerator = np.sum([(x-x_bar)*(y-y_bar) for x,y in pair_list]) denominator1 = np.sqrt(np.sum([(x-x_bar)**2 for x in x_i])) denominator2 = np.sqrt(np.sum([(y-y_bar)**2 for y in y_i]))** 

**r_xy = numerator/(denominator1*denominator2) r_xy** 

**0.9042001578168488** 

##### **確率と確率分布** 

**図** 2のグラフを見ると、気温が 15°C のときでも売上金額は 100 万円のときと 120 万円のときがあることがわかると思います。これは売上金額は温度によって確実に決まるものではなく、「確率的に振る舞っている」とも言えます。ここでは、このような確率的な振る舞いをより詳細に見ていくことにします。たとえばプログラムにおいて確率的な振る舞いは、 random 関数を使った乱数で実現できます。乱数は一度生成するだけだとその性質はわかりにくいですが、何度も生成し、どの値が何回出るかを調べてヒストグラムを描くことでその性質を見ることができます。ヒストグラムとはデータをいくつかの区間ごとに区切って横軸にとり、縦軸にその区間に含まれるデータの個数を表したものです。たとえば、 **図** 4のようなプログラムを書いて実行すると、乱数を 1,000 回発生させてヒストグラムを描くことができます。 

###### **▼図4 一様分布からの疑似乱数の発生** 

###### **plt.hist(np.random.random(1000), color="gray")** 

**(array([ 90., 108., 106.,  97.,  99.,  94., 106.,  76., 115., 109.]), array([0.00129021, 0.10115967, 0.20102912, 0.30089858, 0.40076804, 0.50063749, 0.60050695, 0.70037641, 0.80024587, 0.90011532, 0.99998478]),** 

**<BarContainer object of 10 artists>)** 



より数学的には、その値がどれくらい出やすいかを表す確率(密度)分布と呼ばれるものを考えます。実は、 **図** 4では、 0 から 1 まですべての値の出やすさが同じ分布からサンプルしていた、ということです。このような分布には **一様分布** という名前が付いています。十分にサンプルすれば、ヒストグラムを確率分布すべての面積 1 になるように正規化したものは、もとの確率分布そのものと一致します。このことは、 **図** 4の疑似乱数の発生数(サンプル数)を増やすと **図** 5のようにヒストグラムが平らに近くなることからも確認できます。実践では、たとえば、最近ではソシャゲのガチャなどでレアを引く確率は低く、ノーマルの確率は高いなど、均等な確率を扱わないことのほうが多いと思います。こういった、非均等な確率も、この均等な確率を使って実現することが多いです。すでに実装されているライブラリを使う場合も多いと思います。そのほか代表的な分布をいくつか紹介します。 



**ベルヌーイ分布** 

「代表値・統計量」節では、変数の種類に関してお話ししましたが、確率変数にも離散的な値をとる離散確率変数と、実数値をとる連続確率変数があり、これらを区別することが多いです。たとえば先ほど例に挙げた一様分布は実数値をとるので、連続確率変数になります。ベルヌーイ分布は、最も簡単な離散確率変数の分布です。たとえばコインを投げて表になる確率が _p_ 、裏になる確率が 1 - _p_ となるような、 2 種類の値しかとらない変数の従う分布です。離散確率変数では、すべての結果(ベルヌーイの場合は 2 種類のみ)の確率は足して 1 になる必要があります。プログラム上では、ベルヌーイ分布は一様しきいち乱数を閾値( _p_ )で切って「離散化」することで実現することが多いです。さらに、このベルヌーイ分布からのサンプルを _n_ 回行うということは、「確率 _p_ で表が出るコインを _n_ 枚投げる」という試行に相当します。このような分布には「二項分布」という名前が付いており、 _n_ 回コインを投げて表が _k_ 回出る確率を _P_ ( _X_ = _k_ )と書くとすると、その値は次の式で計算できます。実際にこの式を直接計算することもできますが、プログラムを使ってこの試行をシミュレートすれば、ヒストグラムを描いて分布の概形を見ることもできます( **図** 6 **)** 。 

###### **▼図6 コインで表が出る回数の分布** 

**all_sum=[] n=1000 p=0.5 for _ in range(1000): sum_ = 0 for _ in range(1000):** 

**sum_ +=np.random.random()>0.5 all_sum.append(sum_)** 

###### **plt.hist(all_sum)** 

**(array([  2.,   9.,  37.,  84., 206., 259., 206., 135.,  52.,  10.]), array([440. , 450.7, 461.4, 472.1, 482.8, 493.5, 504.2, 514.9, 525.6, 536.3, 547. ]), <BarContainer object of 10 artists>)** 



このように、一様分布(からのサンプル)をもとにさまざまな分布(からのサンプル)を作ることができるというのがわかると思います。確率的な挙動をするといっても、その分布はさまざまです。たとえば、「毎回確率 0.001 で当たりになるくじを最大 10,000 回引くとき、何回めで当たりを引くか」という確率変数を作ったとすると、 **図** 7のようになります。 

###### **▼図7 当たりくじを何回目で引くかの分布** 

**all_sum=[] for _ in range(1000): for i in range(10000): if np.random.random()<0.001: all_sum.append(i) break** 

**plt.hist(all_sum, bins=30)** **<mark>(..略..)</mark>** 

**<BarContainer object of 30 artists>)** 





###### **正規分布** 

連続確率変数の確率分布としてよく用いられるのが、正規分布です。正規分布の結果は **図** 8のように山なりの分布図になります。 

###### **▼図8 正規分布からのサンプルのヒストグラム** 

###### **plt.hist(np.random.normal(size=1000))** 

**<mark>(..略..)</mark>** 

**<BarContainer object of 30 artists>)** 



正規分布の確率分布 _p_ ( _x_ ) は、平均 μ と標準偏差 σ を使って次のように表します注3 ~~。~~ 



π は円周率です。この式からも、平均を中心にして左右対称になることがわかると思います。正規分布は物理的な世界においてもよく現れる分布です。 



###### **カイ二乗分布** 

一様分布やベルヌーイ分布に従う確率変数を操作して異なる分布に従う確率変数を構築できたように、正規分布に従う確率変数を操作することで異なる分布を持つ変数も作れます。正規分布に従う確率変数を 2 乗したものを _k_ 個足し合わせたものは、自由度 _k_ のカイ二乗分布と呼ばれる分布になります( **図** 9)。なお、この利用例は第 4 章のコラム「異常検知にチャレンジ」で紹介しています。 

###### **▼図9 カイ二乗分布に従うサンプルのヒストグラム** 

**N=10000 plt.hist(np.random.normal(size=N)**2+np.random.normal(size=N)**2 +np.random.normal(size=N)**2+np.random.normal(size=N)**2, bins=100, color="gray")** 

**<mark># 直接作る例 # plt.hist(np.random.chisquare(4,size=N), alpha=0.5, color="gray")</mark> plt.show()** 



##### **複数の確率変数を扱う** 



###### **同時確率** 

「 2 変数間の関係」項では、気温と売上金額という 2 つの変数間の関係を扱いました。ここでは、これらの 2 つの変数の確率分布を考えて、その分布を見ていきましょう。仮に、これらの 2 つの分布が独立で、気温は 10 〜 20 ( °C )の範囲、売り上げは 100 〜 200 (万円)の範囲で一様分布に従うとしましょう。このような複数の確率変数を考える際には、これらの変数の値が同時値をとる確率をもとにした「同時確率(密度)分布」というものを考えます。このような分布を可視化するのに、 1 変数の場合はヒストグラムで描画しましたが、 2 変数の場合は、縦軸と横軸に変数の値をとり、それらの値を領域に分割し、色の濃さでデータの個数を表すこととします。こういった図はヒートマップと呼ばれます。 **図** 10はこのヒートマップを用いて上述の例を表示したものです。確率分布としては、この場合は体積が 1 になるように正規化される必要があります。 

###### **▼図10 2変数の独立な一様分布からのサンプル** 

**import numpy as np import matplotlib.pyplot as plt** 

**x=np.random.uniform(10,20,size=10000) y=np.random.uniform(100, 200, size=10000) plt.hist2d(x,y, bins=100,range=[[0,30],[0,300]],cmap="gray_r") plt.colorbar()** 

**<matplotlib.colorbar.Colorbar at 0x7f6ef8ff8a00>** 



これらの分布は、気温と売上金額が互いに影響を与えない「独立」の状態であるため、現実的ではないかもしれません。片方がもう片方の変数に依存する場合を考えるとさらに多彩な場合が考えられます。気温は正規分布に従っていて、おおよそ温度を表す変数 _x_ の値の 10 倍の値に売上金額の変数 _y_ がなっていると仮定すると、この状況は _y_ = 10× _x_ + ε として書くことができます。ここで、 ε はこの式からの売上金額のずれを表しています。 **図** 11では、温度を表す変数 _x_ は平均 15 、標準偏差 3 の正規分布に、 ε は平均 0 、標準偏差 3 の正規分布に従う場合の同時確率分布に対応したヒートマップを示しています。この図から、 **図** 2と同様に、気温と売上金額が相関している状況を同時確率分布を用いて表現できていることがわかるでしょう。 

###### **▼図11 2変数の非独立な正規分布(例:気温と売上金額)からのサンプル** 

**import numpy as np import matplotlib.pyplot as plt** 

**x = np.random.normal(15,3,size=100000) e = np.random.normal(0,30,size=100000) y = 10*x + e plt.hist2d(x,y, bins=100,range=[[0,30],[0,300]],cmap="gray_r") plt.colorbar()** 

**<matplotlib.colorbar.Colorbar at 0x7f6ef8ff8a00>** 



離散確率変数の場合はどうでしょうか? 2 つの確率変数 _X_ 、 _Y_ の分布を同時に考えたものを同時確率分布と言い、 _P_ ( _X_ = _x_ , _Y_ = _y_ )と表します。たとえばサイコロを 2 つ投げたときに出る目の確率を考えると、同時確率は、 _P_ ( _X_ = 1, _Y_ = 1 )= 1/36 、 _P_ ( _X_ = 1, _Y_ = 2 )= 1/36 、 ... 、 _P_ ( _X_ = 6, _Y_ = 6 )= 1/36 のようになります。ベルヌーイ分布のときはコインの裏表のような 2 つの値でしたが、このようにサイコロのような 3 つ以上の値も扱えます。独立な離散確率変数の例としてサイコロを 2 つ投げた場合のヒートマップを描くと **図** 12のようになります(確率として見ることができるよう、足して 1 になるように正規化して表示しています)。この場合、 _X_ と _Y_ は独立です。より厳密には、 _P_ ( _X_ = _x_ , _Y_ = _y_ )= _P_ ( _X_ = _x_ )・ _P_ ( _Y_ = _y_ )が常にすべての変数について成り立つ場と言えます。 **図** 12のヒートマップはすべての場合で _P_ ( _X_ = _x_ , _Y_ = _y_ )= 1/36 、 _P_ ( _X_ = _x_ )= _P_ ( _Y_ = _y_ )= 1/6 なので、これが成り立っていることが確認できます。 

###### **▼図12 2つの独立なサイコロを投げた結果のサンプル** 

**import seaborn as sns** 

**dice=[1,2,3,4,5,6] x=np.random.choice(dice, size=100000) y=np.random.choice(dice, size=100000) hist,_,_ = np.histogram2d(x, y,bins=len(dice),range=[[1,6],[1,6]])** 

###### **<mark># seabornはxとyが逆なので注意</mark> sns.heatmap(hist/100000,annot=True,xticklabels=dice,yticklabels=dice,vmax=1,v min=0,cmap="gray_r")** 

###### **<Axes: >** 



次に、「サイコロを振って出た目を _X_ として、その _X_ 回だけコインを投げて表が出た数を _Y_ とする」としましょう。このとき、 _X_ と _Y_ は直感的に独立ではなくなることがわかるでしょうか。この場合もこのプロセスをプログラムとして書いて何回も繰り返し、それぞれの状況をカウントすることで確率を計算できます( **図** 13)。サイコロを振って _n_ が出る確率( _X_ )が 1/6 、 _n_ 回コインを振って表が出る確率( _Y_ )は( 1/2 )<sup>_n_</sup> ・ _n C k_ となるため、同時確率(マス目の数値)は _P_ ( _X_ = _n_ , _Y_ = _k_ )= 1/6 ・(( 1/2 ) _n_ ・ _n C k_ )で計算できます。ここで、 _n_<sup>_C_</sup> _k_<sup>は</sup><sup>_n_個のあるものの中から</sup><sup>_k_個を選ぶ「場合の数」を表していま</sup> す。このように、確率を正確に計算するにはしばしば「場合の数」が重要になります。 

###### **▼図13 非独立な2つの離散確率変数からのサンプル** 

**import seaborn as sns dice=[1,2,3,4,5,6] x=np.random.choice(dice, size=100000) y=[] for trial_x in x:** 

**trial_y=sum([np.random.choice([0,1]) for _ in range(trial_x)]) y.append(trial_y) hist,_,_ = np.histogram2d(x, y,bins=(len(dice),len(dice)+1),range=[[1,6],[0,6]])** **<mark># seabornはxとyが逆なので注意</mark> sns.heatmap(hist/100000,annot=True,xticklabels=[0]+dice,yticklabels=dice) plt.xlabel("k") plt.ylabel("n")** 

**Text(33.0, 0.5, 'n')** 



_n_ = 5 、 _k_ = 3 の場合を計算すると、サンプリングで計算した結果に近いことがわかると思います( **図** 14)。 

###### **▼図14 組み合わせによる直接的な確率計算** 

**import math** 

**n=5 k=3 prob=1/6*((1/2)**(n)*math.comb(n,k)) prob** 

**0.041666666666666664** 



###### **周辺確率** 

ここまで 2 つの変数の値の関係を含めた同時確率を扱ってきましたが、片方の確率だけに注目したい場合があります。これを周辺確率と言って、 _Y_ に着目したときの周辺確率は、のように書きます。ここで左辺には _X_ が出てこないのを不思議に思うかもしれませんが、 _Y_ だけに注目している場合は矛盾が生じないので書かないのが普通です。ですので、単に _P_ ( _Y_ = _y_ )と書いていても、ほかに登場している確率変数がある場合には「それらの周辺確率なのだな」と推測しなければならない場合があります。たとえば **図** 15は、 _x_ を無視して _y_ のみをカウントすることに相当するので、 _y_ についての確率を計算できます。 

###### **▼図15 周辺確率の計算** 

**import seaborn as sns dice=[1,2,3,4,5,6] x=np.random.choice(dice, size=100000) y=[] for trial_x in x: trial_y=sum([np.random.choice([0,1]) for _ in range(trial_x)]) y.append(trial_y) hist,_ = np.histogram(y,bins=len(dice)+1,range=[0,6]) for i, y_count in enumerate(hist): print("P( y =",i,") =", y_count/len(y)) P( y = 0 ) = 0.16518 P( y = 1 ) = 0.30987 P( y = 2 ) = 0.25724 P( y = 3 ) = 0.1681 P( y = 4 ) = 0.07631 P( y = 5 ) = 0.02056 P( y = 6 ) = 0.00274** 



###### **条件付き確率** 

たとえばデータベースの検索などでは特定の条件を満たすテーブルを作りたいことがあるでしょう。このように、条件のある確率を考えていきます。 2 変数 _X_ と _Y_ があるときに、ある変数 _X_ の値が特定の値 _x_ をとるという条件のもとで _Y_ に関する確率のことを条件付き確率と言い、と書くことができます。また、同時確率に関して次の式が成り立つことがわかっています。条件付き確率からのサンプルをとる場合、プログラム的には _X_ の値が特定の値 _x_ をとるときだけに関して集計します。たとえば _x_ = 3 のときの _Y_ の確率 _P_ ( _Y_ = _y|X_ = 3 )を求めるには、 **図** 15の 8 行めの前に **if trial_x==3** という条件を追加すればよいことになります( **図** 16)。 

###### **▼図16 x=3のときの条件付き確率の計算** 

**<mark>(..略..)</mark> if trial_x==3:** **<mark># ここに条件を追加</mark> y.append(trial_y) hist,_ = np.histogram(y,bins=len(dice)+1,range=[0,6]) for i, y_count in enumerate(hist): print("P( y =",i," | x = 3 ) =", y_count/len(y))** 



###### **ベイズの定理** 

条件付き確率の式と同時確率の式から次の式を導出することができます。この式は導出自体は簡単ですが、ベイズの定理と呼ばれる重要な式になっています。条件付きの確率に関して左辺と右辺で _X_ と _Y_ が入れ替わっていることがわかるでしょう。次節ではこのベイズの定理を使ったアプリケーションが活躍するので、その有用さが理解してもらえるかと思います。 



###### **条件付き独立** 

たとえばデータベースにおいて、全年代を集計した場合と年代ごとに区切った場合で関係性が異なる場合があります。このような確率を考えるときに条件付き独立というものを使います。条件付き独立では、すべての _x_ と _y_ と _z_ について、次の式が成り立ちます。教科書などでは _P_ ( _X_ , _Y|Z_ ) = _P_ ( _X|Z_ ) _P_ ( _Y|Z_ ) と書くことも多いと思います。 

##### **モデルを考えよう** 



###### **モデルとパラメータ** 

ベルヌーイ分布における _p_ 、正規分布における μ や σ 、カイ二乗分布における _k_ は、分布を決めているのでパラメータとなります。前節で行ったように複数の確率変数を組み合わせてより複雑な分布を表現する場合であっても、すべてのパラメータを指定することで、分布は 1 つに決まります。このようにパラメータを決めると確率分布が決まるものを確率分布のクラスといい、これを目的や対象に応じて設定したものを確率モデルと呼びます。ここでいうクラスはパラメータを決めてインスタンスを作るという意味で、プログラミングで言うところのクラスと似た意味で使われています。 



###### **確率モデルを用いて予測してみよう** 

#### **ナイーブベイズモデル** 

ここではナイーブベイズモデルと呼ばれる確率モデルを用いて簡単な予測をしてみましょう。ナイーブベイズモデルとは、条件付き独立をベースにした確率モデルです。ベイズという名前が入っているとおり、予測の際にベイズの定理を使うことが特徴の 1 つです。たとえば顧客にどの商品を薦めるのがいいのかという問題を考えてみましょう。ここに商品 A 、 B 、 C があり、 3 つすべての商品を購入していない顧客に、過去に買った商品からいずれかの商品を推薦するとします。推薦する商品を表す変数 _Y_ は A 、 B 、 C のいずれかをとる 3 値の離散確率変数とし、これらとは別に過去に買ったことがある商品 _X_ 1 、 _X_ 2 は 0 または 1 をとる離散確率変数とします。ここでは、過去に買った商品が与えられたとき買う可能性の高い商品を表す変数 _Y_ の確率が知りたいので、 _P_ ( _Y_ = _y|X_ 1 = _x_ 1, _X_ 2 = _x_ 2 ) を計算したくなるでしょう。買う可能性の高い商品を 1 つに決める際には _y_ を A 、 B 、 C のすべての商品に関して計算し、最も確率の大きい商品を予測とするのが妥当かと思います。この計算はベイズの定理を使って次のように変形できます。この右辺は同時確率なので、同時確率を計算できればよいことになります。ナイーブベイズモデルではこのとき、次の仮定を置きます。 



_x_ 1 と _x_ 2 は 0 または 1 を、 _y_ は商品 A 、 B 、 C のいずれかをとりますが、この仮定はそのどの場合も満たします。これは _X_ 1 と _X_ 2 は _Y_ に関して条件付き独立であるということになります。左辺には変数が 3 個もあるので、「複数の確率変数を扱う」節で行ったのと同様に単純にテーブルを書くと 2×2×3 のテーブルを書く必要があり、非常に大きくなります。しかしこの仮定によって、小さいテーブル 3 つを作ればいいことになります。もっと多く( _d_ 個)の商品の購入を手掛かりにした場合も同様に、と書けるので、小さいテーブルを用意しておき、必要に応じて掛け算で計算できます。この場合、条件付き独立の仮定によって個別の商品に分解して考えることがより重要になってきます。 

**図** 17では、この確率モデルを前述のクラスと同様の方針で NaiveBayesModel クラスに実装していま ~~す~~ <u>注4</u> ~~。~~ prob は上記の方針で同時確率を計算しており、 predict はそれを用いて商品を予測しています。 

###### **▼図17 ナイーブベイズモデルの実装例** 

**class NaiveBayesModel(): def __init__(self, d, param_x_bar_y, param_y):** 

**<mark># (..略..)パラメータ初期化</mark> def prob(self,X,y): prob_y = self.param_y[y] prob_x_bar_y=1 for x_, y_ in zip(X,y): for i in range(self.d): if x_[i]==1: prob_x_bar_y *= self.param_x_bar_y[y_][i] else: prob_x_bar_y *= 1-self.param_x_bar_y[y_][i] return prob_x_bar_y*np.prod(prob_y) def fit(self, X, y): for j in range(self.num_class): self.param_x_bar_y[j] = np.mean(X[y==j],axis=0) self.param_y = np.mean(np.eye(self.num_class)[y], axis=0) def predict(self, x): prob_x = sum([self.prob([x],[j]) for j in range(self.num_class)]) prob_y_bar_x = [self.prob([x],[j])/prob_x for j in range(self.num_class)] return np.argmax(prob_y_bar_x)** 

#### **パラメータの決定** 

パラメータを決定するには既存のデータが必要になります。具体的には、商品 _X_ 1 、 _X_ 2 の購入の有無と、商品 A 、 B 、 C のいずれを購入したかのペアを事前に考えます。これらのペアの情報から _P_ ( _X_ 1 = _x_ 1 _|Y_ = _y_ )を決めればよいのですが、これらはデータ中から _y_ を買った人のうち _x_ 1 を購入した人の数を数えればよいだけです。 



_P_ ( _Y_ = _y_ )に関しても次のように決めます。さいゆうほうこの決定方法は最尤法と呼ばれます。データがどのパラメータに従う分布から得られるかを考えて、その確率の値が最も高くなるパラメータを選択する方法です。この値はデータとモデルのフィットもっとゆうどの度合い(尤もらしさ)とも考えられるので尤度と呼ばれています。ナイーブベイズモデルにおいてはこのパラメータの決め方が尤度が最も高くなる決め方です。 

**図** 18のコードでは fit の前後でデータに対して prob を計算しています。ここでは、すべてのデータに関して同時確率を掛け合わせたものが尤度となり、それが最大になるようにパラメータを決定しているので、必ず fit 後の確率が高くなることが確認できます。 

###### **▼図18 ナイーブベイズモデルの利用例** 

**<mark># 以下が与えられたとする。 # X:0または1のデータ数×d次元のarray # y:0、1、2のデータ数次元のarray # 初期パラメータ # param_x_bar_y*:P(X=x|Y=y)に対応する初期パラメータ。</mark>** 

**<mark>#                ⻑さ4のarray # param_y*:P(Y=y)に対応する初期パラメータ。 #          ⻑さ3のarray</mark>** 

**model = NaiveBayesModel(4, [param_x_bar_y1, param_x_bar_y2,param_x_bar_y3], param_y) print("before:", model.prob(X,y)) model.fit(X,y) print("after: ", model.prob(X,y)) pred_y=[model.predict(x) for x in X] before: 2.472669935414271e-10 after:  8.898925781250004e-07 [1, 2, 2, 1, 1, 1, 1, 2, 2, 2]** 

##### **まとめ** 

データを代表する統計量から、ナイーブベイズなどを通じて確率的な振る舞いをモデル化することまで駆け足で進めてきました。確率統計は機械学習のベースとなります。現実の複雑で不確実な事象を含む予測や異常検知(第 4 章コラム参照)などのタスクに対して、確率を使ったモデリングをすることで実用的なアプリケーションに発展させていくことができます。今回は誌面の都合で割愛した事項が多くありますが、この記事をきっかけにして、興味が湧いた箇所から勉強を進めていただけるとうれしいです。注1) NumPyには、統計量や代表値に関連した関数があります。 

<u>https://numpy.org/doc/stable/reference/routines.statistics.html</u> (本文に戻る) 

- 注2) 実際に使う場合は、相関係数を計算するNumPyのnp.corrcoef関数を用いるとよいでしょう。こちらは3つ以上の変数を与えた場合にも対応するために2次元の配列で返ってきますが、[0,1]の要素に注目すれば図3の計算と一致していることがわかると思います。 (本文に戻る) 

- 注3) この分布はnp.random.chisquare関数を用いて直接作成することもできます。 (本文に戻る) 

- 注4) 実装において、必ずしも確率モデルをクラスで実装する必要はないですが、ここではわかりやすさのためにクラスを使って実現することとします。 

   - (本文に戻る) 

# **第 2 章** 

### **微分・積分** 

#### **基本の考え方から多変数関数の微分とその意味まで** 



辻真吾(つじしんご) <u>https://tsjshg.info/</u> 

**AIの革命的な進化を支える機械学習アルゴリズムをしっかりと理解するためには、内部で大活躍している「微分・積分」の知識が欠かせません。本章ではこの微分・積分について、基本となる考え方から、多変数関数の微分とその意味までを解説します。Pythonによる計算結果やグラフを見ながら、高校数学の代表的な微分・積分を学び直しましょう。** 

##### **微分と積分の基本** 

しょぎょうむじょう諸行無常は仏教の用語で、世の中にずっと変わらないものは何一つとして無いという意味です。つまり、すべてのものは変化していくという主張です。私たちが何もしなくても、太陽は時々刻々とその位置を変え、夜になれば星が空に瞬きます。ボールを投げれば飛んでいき、よほどの豪速球でなければいつかは地面に落下します。自然科学の基本は観察です。変化し続けるものを理解したいと思うとき、まずはその変化を観察して詳しく調べることからはじめると良さそうです。簡単な関数を例に、これを実践してみましょう。 



###### **変化を調べる** 

_f_ ( _x_ ) = _x_<sup>3</sup> という関数を考えます。 _y_ = _f_ ( _x_ ) として- 5 ≦ _x_ ≦5 の範囲で _xy_ 平面にグラフを描くと **図** 1のようになります注1 ~~。~~ 

**▼図1** **_y_ =** **_x_**<sup>**3**</sup> **のグラフを描画** 

**import matplotlib.pyplot as plt import numpy as np def x3(x): return np.power(x, 3)** 

**<mark># -5から5まで100個のデータ</mark> x = np.linspace(-5, 5, 100) y = x3(x) plt.plot(x, y, label="$x^3$") plt.legend()** **<mark># 凡例の表示</mark> plt.show()** 



ここで _x_ の変化に対する _y_ の値の変化に注目します。たとえば、 _x_ が 0 から 1 まで変化すると _y_ は 1 増え、 _x_ が 3 から 4 まで増えると _y_ は 37 増えます。 _x_ の変化量は 1 で同じにもかかわらず、どこで変化するかによって _y_ の変化量はかなり違うことがわかります。 

- これをもっと詳しく知るために、次の関数を作ります。 _x_ が _x_ = _a_ 

- から _x_ = _a_ + _h_ まで変化するとき、 _y_ の変化量を _x_ の変化量で割った値 (変化量の割合)を返す関数です。 

**def calc_change_rate(f, a, h): return (f(a+h) - f(a))/h** 

- _a_ を 0 と 3 、 _h_ を 1 として計算すると次のようになります。 

###### **calc_change_rate(x3, 0, 1)** 

**1.0** 

**calc_change_rate(x3, 3, 1)** 

**37.0** 

次に、- 5 ≦ _a_ ≦5 となる整数をとり、 _h_ = 1 として変化量を計算して _y_ = _x_<sup>3</sup> のグラフに重ねて描画してみます( **図** 2)。 

###### **▼図2 変化量の計算結果をグラフに重ねる** 

**x = np.linspace(-5, 5, 100)** 

**<mark># -5から5までの整数(全部で11個)</mark>** 

**a = np.arange(-5, 5+1)** 

**r = calc_change_rate(f=x3, a=a, h=1)** 

**plt.plot(x, x3(x), label="$x^3$") plt.plot(a, r, 'bo', label="change rate") plt.legend() plt.show()** 



もう少し解像度を上げてみましょう。 _a_ を整数ではなく _x_<sup>3</sup> の関数の描画に使っている値と同じにし、 _h_ の値を小さくします。ある場所 _x_ = _a_ から _x_ を 1 増やすのではなく、少しだけ増やすということです ( **図** 3)。 

###### **▼図3 図2から変化量のグラフを滑らかにする** 

**r = calc_change_rate(f=x3, a=x, h=0.0001)** 

**plt.plot(x, x3(x), label="$x^3$") plt.plot(x, r, linestyle="dashed", label="change rate") plt.legend() plt.show()** 



_f_ ( _x_ ) = _x_<sup>3</sup> という関数の変化量が見えてきました。たとえば、- 2 より小さい _x_ や、 2 より大きい _x_ に対しては、 _x_ が増えると _y_ の値も大きく増えることがわかります。これらと比較すると、 _x_ = 0 の周辺では _x_ の増加に対して _y_ の値がそれほど増えていないということが見てとれます。さらに、 _x_ = 0 における _f_ ( _x_ ) = _x_<sup>3</sup> の変化量に注目します。 calc_change_rate() 関数を使ってこの値を計算できますが、計算結果は次のように _h_ によって変わります。 

**calc_change_rate(x3, 0, 1) 1.0** 

**calc_change_rate(x3, 0, 0.001) 1e-06** 

###### **calc_change_rate(x3, 0, 0.00001)** 

###### **1.0000000000000002e-10** 

_h_ を小さくしていくと、計算結果もどんどん小さくなっているように見えます。 calc_change_rate() 関数の中では _h_ で割る計算があるので、 _h_ を 0 にはできません。どこまで _h_ を小さくできるでしょうか。試しに思い切り小さな値にしてみましょう。 

###### **calc_change_rate(x3, 0, 1.0e-400)** 

**nan** 

nan は非数( not a number )を意味します。正しい計算結果が得られていません。変化量を詳しく知るために、 _h_ をできるだけ小さくしたいのですが、 0 にはできません。どうしたらよいのでしょうか。これを数学が解決してくれます。 



###### **極限という発想** 

_f_ ( _x_ ) = _x_<sup>3</sup> という関数の正確な変化量を知るために数学を使います。 _h_ を限りなく小さくする、つまり限りなく 0 に近づけるという操作を、次の数式で表現します。式( 1 )で単純に、 _h_ = 0 としてしまうと 0/0 になってしまい計算できません。 lim の中の式を展開して整理してみます。ここまで来れば、 _h_ が限りなく 0 に近づくと、式全体がどうなるか見えてきました。 3 _a_<sup>2</sup> だけが残りそうです。つまり、 _f_ ( _x_ ) = _x_<sup>3</sup> という関数の _x_ = _a_ における変化量は、 3 _a_<sup>2</sup> という式で表現できるというわけです。これが **微分** と呼ばれる計算です。変化量を限りなく小さな領域で調べるという突飛な発想が、微分という概念を生み出しました。 



###### **関数の微分** 

_x_ の関数 _f_ ( _x_ ) を _x_ で微分して得られる関数は、次のような記号で表現します(いま、 _f_ ( _x_ ) = _x_<sup>3</sup> とします)。 



_n_ を整数としたとき _f_ ( _x_ ) = _x_<sup>_n_</sup> について一般に次のことが成立します。微分して得られる関数を **導関数** と呼び、導関数に具体的な _x_ の値を代入して得られる数値を **微分係数** と呼びます。 1 つの数値(スカラー)は、入力によらず同じ値を返す定数関数と考えられるので、微分すると 0 になります。変化量がいつも 0 ということです。 

_a_ と _b_ を定数とします。関数 _f_ ( _x_ ) と _g_ ( _x_ ) の和を微分したものは、各関数を微分した結果の和になります。また、関数の定数倍は結果に影響しません。つまり次の式が成り立ちます。 





###### **足し算としての積分** 

1 日 100 円貯金すると、 30 日目には 3,000 円貯まります。では、貯金を開始した日を 1 日目として、 _i_ 日目は 100 _i_ 円貯金すると、 _n_ 日目の貯金額はいくらになっているでしょうか。これは、次のような式で表現できます。式( 2 )は単純な等差数列の和なので簡単に計算できます。計算方法を復習しておきましょう。とすると _2S_ ( _n_ ) は、 _n_ 個の 100( _n_ + 1) が並んでいることになるので、となります。式( 3 )に _n_ = 10 を代入することで 10 日後の貯金額は 5,500 円だとわかります。式( 3 )のグラフを描いてみます。 _n_ を日数と考えた自然数の場合に加え、実数 ( _x_ ) のグラフも重ねると **図** 4のようになります。日が経つにつれ貯金が増えていくのがわかります。実際には 1 日ごとに貯金箱にお金を入れるので、グラフの丸い点のところだけが現実です。ただ、実数 _x_ のグラフも重ねているので、 3.5 日目の貯金額という値も計算できます。 1 日ごとではなく、時々刻々と貯金が増えていると考えるわけです。 

###### **▼図4 貯金額の変化** 

**x = np.linspace(0, 13, 100) n = np.arange(1, 13)** 

**def s(t):** 

**<mark># 混乱がないように引数をtにします</mark> return 50*t*(t+1)** 

**plt.plot(x, s(x), label="$S(x)$") plt.plot(n, s(n), 'bo', label="$S(n)$") plt.legend() plt.show()** 



そこで、ある瞬間に貯金がどれくらい増えているのかを考えてみます。捕らぬ狸の皮算用とはまさにこのことですが、ある瞬間の金額の変化を知りたいのです。変化量を知りたいときは微分です。式 ( 3 )は、 _n_ 日目の時点における貯金額を求める式です。これを _n_ で微分してみます。 _n_ は自然数としていたので、式の形はそのままに実数 _x_ の関数として微分すると次のようになります。得られた式( 4 )は _xy_ 平面上の直線です。 **図** 5を見ながらこの式の意味を考えていきます。式( 3 )は、時々刻々どれくらい貯金が増えるかを表現する式です。 3 日目が終わった時点で貯金がいくらになるかを考えてみます。 _x_ = 3 のとき、この直線と _x_ 軸、 _y_ 軸で囲まれる台形( **図** 5のグレー部分)の面積を計算してみると、となることがわかります。この面積の計算結果は式( 3 )の値と一致します。つまり、式 ( 3 )を微分して得られた式( 4 )のグラフを描いて、面積を計算したところ、元の関数の値と同じになることがわかりました。これが **積分** の計算です。次のような記号を使った式で表現できます。なぜ面積を計算すると貯金総額になるのか、 _n_ が整数の場合で考えてみます。 _n_ 日目に、 100 _n_ 円貯金するという状況をあらためて **図** 6に示します。 1 つの正方形は 100 円を表現します。 



_n_ 日目における貯金の総額は、そこまでに正方形がいくつあるかを数えればわかります。たとえば、 3 日目までに貯金できる額は、正方形が 6 つあるので 600 円となります。ここで、 1 日ごとに正方形を積み上げるのではなく、限りなく短い時間間隔を考えて正方形を積み上げていくことを想像してみてください。もはや 1 つの正方形は 100 円のような大金ではなく、気が遠くなるような小さな金額です。どんどん細かくしていけば、ガタガタとした階段状の図形ではなく、 **図** 5のような直線になりそうです。微分を計算したときに使った極限という発想が、積分の計算でも使われているというわけです。式( 5 )の計算結果が 600 になることを確かめましょう。積分は、微分の逆の演算です。微分して 100 _x_ + 50 になる多項式を探します。少し考えると、という式が思い浮かぶでしょう。ここで _C_ は **積分定数** と呼ばれる定数です。これは、定数を微分すると 0 になるため、積分すると出てきてしまうものです。式( 6 )は _f_ ( _x_ ) = 100 _x_ + 50 の **原始関数** と呼ばれ、 _F'_ ( _x_ ) = _f_ ( _x_ ) になっています。また、式( 5 )は **定積分** という計算です。原始関数を使って次のような計算で具体的な値を求めることができます。積分は、変化する量を細かく積み上げていく計算と考えることができます。変化を観察して、細かく分解していく演算が微分であり、逆に細かく分解した変化を積み上げて足し合わせていく演算が積分というわけです。 

##### **微分への理解を深めよう** 

機械学習アルゴリズムの理解においては、積分よりも微分のほうが必要な場面が多いため、微分に関する知識を掘り下げていきます。「関数の微分が何の役に立つのか」「合成関数の微分と多変数関数の微分」について紹介します。 



###### **微分係数と関数の増減** 

_f_ ( _x_ ) = _x_ ( _x_ - 1)( _x_ + 1) という 3 次関数を考えます。展開すると _f_ ( _x_ ) = _x_<sup>3</sup> - _x_ となるので、 _f'_ ( _x_ ) = 3 _x_<sup>2</sup> - 1 となります。 _xy_ 平面にこれらのグラフと _y_ = 0 の水平な線を描いてみます( **図** 7)。 

**▼図7** **_y_ =** **_x_**<sup>**3**</sup> **-** **_x_ 、** **_y_ =3** **_x_**<sup>**2**</sup> **-1、** **_y_ =0のグラフを描画 x = np.linspace(-1.1, 1.1, 100)** 

**def f(x): return x**3 - x** 

**def diff_f(x): return 3 * x**2 - 1** 

**def zero(x):** **<mark>'''y=0に水平な直線を引くための関数'''</mark> return np.zeros(x.shape[0])** 

**plt.plot(x, f(x), label="$f(x)=x^3 - x$") plt.plot(x, diff_f(x), linestyle="dashed", label="$f^{\prime}(x)=3x^{2} - 1$") plt.plot(x, zero(x), linestyle="dotted", label="y=0")** 

**plt.legend() plt.show()** 



_x_ =- 1, 0, 1 のとき、 _f_ ( _x_ ) = 0 となります。 _y_ = _f_ ( _x_ ) のグラフは、 _x_ = - 1 付近では増加していて、その後減少に転じます。また、 _x_ = 0.5 を過ぎたあたりから再び増加しているのがわかります。こうした _y_ の値の増減は、 _f'_ ( _x_ ) の符号で判断できます。微分係数が正の場合、 _x_ が増加すると _y_ の値は増加します。逆に、微分係数が負なら _x_ が増加すると _y_ の値は減少します。 _f_ ( _x_ ) = _x_<sup>3</sup> - _x_ の増加と減少が入れ変わるのは _x_ がいくつのときでしょうか。これは、 _f'_ ( _x_ ) = 0 を解いて、次の値だとわかります。つまり、 _f_ ( _x_ ) の定義域を- 1 ≦ _x_ ≦1 に限定して、最大値と最小値を求めると、になるはずです注2 ~~。~~ 

SciPy には関数の最小値を探してくれる minimize_scalar() 関数があるので、これを利用してみます。探索する範囲を bounds 引数で指定します( **図** 8)。関数の最小値が無事見つかり、微分から計算したものと一致しました。 

###### **▼図8 minimize_scalar()で最小値を求める** 

###### **from scipy import optimize** 

**optimize.minimize_scalar(f, bounds=(-1, 1))** 

**fun: -0.3849001794597505** 

**message: '\nOptimization terminated successfully;\nThe returned value satisfies the termination criteria\n(using xtol = 1.48e-08 )'** 

**nfev: 15 nit: 11 success: True x: 0.5773502693888115** 



なので、これも合っていることがわかります。なお、ここでは省略しますが、 _f_ ( _x_ ) が返す値を- 1 倍すれば、同じ関数 minimize_scalar() を使って最大値を計算できます。関数の微分を調べると関数の増減がわかり、その最小値や最大値を探すときに大いに役に立つということがわかります。 



###### **ネイピア数とシグモイド関数** 

次の式で定義される数を **ネイピア数** と呼び _e_ で表現します。円周率 _π_ と同じように数学において重要な定数の 1 つです。 



_n_ が大きくなるとこの式の意味は、 1 にちょっとだけ数を足して、それを何回も掛け合わせるという操作になります。これがある 1 つの実数に収束し、それを _e_ とします。具体的には 2.718281828459045 ・・・のような数です。この _e_ を底とした指数関数を考えます。式( 7 )のグラフを _y_ = _f_ ( _x_ ) として _xy_ 平面に描くと **図** 9のようになります。 

###### **▼図9 式(7)のグラフを描画** 

**x = np.linspace(-5, 5, 100)** 

**plt.plot(x, np.exp(x), label="$e^{x}$") plt.legend() plt.show()** 



_e_ は 1 より大きいので、 _x_ > 0 の領域で _x_ が増加すると _y_ の値は急激に増加していきます。また、 _x_ < 0 では、 _x_ が小さくなると _y_ の値はどんどん小さな値になっていき、限りなく 0 に近づいていきます。 _x_ = 0 のときは底にかかわらず 1 となります。この関数は微分してもまったく形が変わらないという大きな特徴があります。この指数関数を使って、次のような関数を考えます。式( 8 )で定義される _f_ ( _x_ ) は、 **シグモイド関数** と呼ばれる関数です。ニューラルネットワークの活性化関数などとしてもよく利用されます。グラフを描くと **図** 10のようになりなります。 

###### **▼図10 式(8)(シグモイド関数)のグラフを描画** 

**def sigmoid(x):** 

**return 1 / (1 + np.exp(-x))** 

**plt.plot(x, sigmoid(x), label=r"$\frac{1}{1+e^{-x}}$") plt.legend() plt.show()** 



シグモイド関数は、 _x_ が増加すると _f_ ( _x_ ) の値も増加する単調増加関数です。 _x_ が小さくなると関数の値は限りなく 0 に近づき、 _x_ が大きくなると関数の値は限りなく 1 に近づきます。シグモイド関数の微分を計算します。多項式の微分と違って少し難しそうなので、まずは答えを調べます。 SymPy を使うと関数の微分を簡単に計算できます( **図** 11)。 

###### **▼図11 SymPyでシグモイド関数の微分を計算** 

**from sympy import var, exp, diff, init_printing** **<mark># 実行環境に応じて数式を見やすく表示してくれる</mark> init_printing()** 

**<mark># 変数xを定義します</mark> var("x")** **<mark># シグモイド関数を作ります</mark> f = 1 / (1 + exp(-x))** **<mark># 関数fをxで微分します</mark> diff(f, x)** 





###### **合成関数の微分** 

シグモイド関数のように少し複雑な関数を微分するときによく使われるのが、 **合成関数の微分** という考え方です。まずは _f_ ( _x_ ) = _e_<sup>-</sup><sup>_x_</sup> を _x_ で微分することを考えましょう。 _e_<sup>_x_</sup> であれば、微分しても形が変わらないので、そのままで良かったのですが、いまは肩に- _x_ が乗っています。これを新たに _u_ としましょう。もう少し詳しく書くと、 _u_ ( _x_ ) =- _x_ とするわけです。新たな変数 _u_ についてであれば _f_ ( _u_ ) = _e_<sup>_u_</sup> と書けるので、次のような関係が成り立ちます。関数 _f_ ( _x_ ) の計算を 2 つのステップに分けました。まず関数 _u_ ( _x_ ) =- _x_ で _u_ を求め、関数 _f_ ( _u_ ) = _e_<sup>_u_</sup> で最終的な計算結果を得ます。この一連の操作を _f u_ と書いて、 _f_ と _u_ の **合成関数** と呼びます。ある関数を微分しようと思うとき、合成関数を考えると微分の計算が簡単になることがあります。これは、 _f u_ という合成関数を _x_ で微分するとき、次の式が成立するためです。 



_f_ ( _u_ ) = _e_<sup>_u_</sup> なので、 _u_ で微分してもそのままです。また、 _u_ ( _x_ ) =- _x_ なので、 _x_ で微分すると _u'_ ( _x_ ) =- 1 となります。これらをまとめると次のようになります。そのままでは微分できないように見える複雑な式を、合成関数で書き直して微分するテクニックが合成関数の微分です。実際にシグモイド関数に適用してみます。シグモイド関数の分母にある 1 + _e_<sup>-</sup><sup>_x_</sup> を 1 つの変数 _u_ にまとめます。そうすると、シグモイド関数は、という形になります。これは _u_<sup>-1</sup> なので、 _u_ で微分すると- _u_<sup>-2</sup> になります。この結果に、 _u_ ( _x_ ) = 1 + _e_<sup>-</sup><sup>_x_</sup> を _x_ で微分した結果をかけ合わせます。 



SymPy で得られた結果と同じになりました。元のシグモイド関数と合わせて描画してみます( **図** 12)。シグモイド関数の増加率が大きくなる _x_ = 0 付近で、導関数の値(微分係数)が大きくなっているのがわかります。 

###### **▼図12 式(9)のグラフを描画** 

**x = np.linspace(-5, 5, 100)** 

**def diff_sigmoid(x):** **<mark>'''シグモイド関数の微分'''</mark>** 

**return sigmoid(x)*(1 - sigmoid(x))** 

**plt.plot(x, sigmoid(x), label=r"$\frac{1}{1+e^{-x}}$") plt.plot(x, diff_sigmoid(x), linestyle="dashed", label=r"$\frac{e^{-x}}{(1+e^{-x})^{2}}$")** 

**plt.legend() plt.show()** 





###### **多変数関数の微分** 

機械学習アルゴリズムの入力となる説明変数の数は、数百から数千になることも珍しくありません。アルゴリズムの内部でこうしたデータをモデル化する場合、ここまで紹介してきた 1 変数関数ではとても太刀打ちできませんので、変数が 2 つ以上ある **多変数関数** を考えます。幸いにも、 1 変数での理論がほとんどそのまま通用する部分もあります。また、 **偏微分** という考え方は 2 変数以上の多変数関数で共通なので、 2 変数でイメージをつかめれば多変数関数の理解に役立ちます。まず、簡単な 2 変数関数を考えます。 



_z_ = _f_ ( _x_ 1, _x_ 2 )として、 _xyz_ 空間にこの関数の姿を描画してみます ( **図** 13)。すると、 3 次元空間内で 1 つの平面になります。この関数を微分することを考えます。 

###### **▼図13 式(10)の3次元図を描画** 

**from mpl_toolkits.mplot3d import Axes3D x1 = np.linspace(-4, 4, 20) x2 = np.linspace(-4, 4, 20) x1, x2 = np.meshgrid(x1, x2)** 

**def f(x1, x2): return (x1+x2) z = f(x1, x2) fig, ax = plt.subplots(figsize=(7,7), subplot_kw={"projection": "3d"}) ax.plot_surface(x1, x2, z, cmap="gray") ax.set_xlabel('$x_{1}$') ax.set_ylabel('$x_{2}$') plt.show()** 



この関数には変数が 2 つあるので、 2 つの変数に自由気ままに動き回られてはたまったものではありません。そこで、どちらか 1 つの変数に注目して、残りの変数はすべて定数と考えます。こうすると、 1 変数での微分の知識をそのまま使えます。変数 _x_ 1 で微分してみましょう。多変数関数なので偏微分と呼びます。偏微分の表現には、 ∂ (ラウンドディー)という記号を使います。ずいぶんシンプルになりました。 _x_ 1 を変数だと思って微分すると 1 になります。もうひとつの変数 _x_ 2 は定数とみなすので、微分すると 0 です。この式の解釈も、 1 変数のときの知識を生かせます。 _x_ 1 が増えると、関数の値はちょうど同じだけ増加します。つまり 1 倍の変化です。 _x_ 1 での偏微分の式には、 _x_ 2 が出てこないので、この関数の増加率は _x_ 2 の値にはまったく依存しません。もう少し複雑な関数を使って、多変数関数の微分とその役割についてみていくことにします。 

##### **偏微分の意味** 

ここまで解説してきた知識を使って、 2 変数のシグモイド関数とその偏微分の意味を考えます。 1 変数のイメージを 2 変数以上の多変数関数へ広げていきます。 



###### **2変数のシグモイド関数** 

式( 8 )のシグモイド関数を多変数関数にします。単純に _x_ = _x_ 1 + _x_ 2 としてみましょう。次のような関数になります。 



_z_ = _f_ ( _x_ 1, _x_ 2) として _xyz_ 空間に描画すると **図** 14のようなグラフになります。 

###### **▼図14 式(11)の3次元図を描画** 

**def sigmoid2(x1, x2): return 1 / (1 + np.exp(-1 * (x1+x2))) z = sigmoid2(x1, x2) fig, ax = plt.subplots(figsize=(7,7), subplot_kw={"projection": "3d"}) ax.plot_surface(x1, x2, z, cmap="gray") ax.set_xlabel('$x_{1}$') ax.set_ylabel('$x_{2}$') plt.show()** 



_z_ = _x_ 1 + _x_ 2 で作られた平面が、滑らかに曲がっている様子が見てとれるでしょうか。 1 変数のシグモイド関数と同じように、 _x_ 1 + _x_ 2 の値が小さくなると関数の値は限りなく 0 に近づき、 _x_ 1 + _x_ 2 の値が大きくなると関数の値は限りなく 1 に近づきます。グラフの中心になっている原点( 0,0 )付近がもっとも変化の急な場所です。同じ関数(式( 11 ))から、 _xy_ 平面のコンター図を作ります。 **コンター図** は 3 次元空間での変化を 2 次元平面で可視化する方法です。 _z_ 方向の値が同じ _xy_ 平面での位置を等高線で結びます。値の変化を濃淡や色の変化で表現することもできます。ここでは、 3 次元空間で関数を真上から見て、値の小さな場所が濃く、値の大きな場所が薄いグレーになっているコンター図を描きます( **図** 15)。 3 次元プロットと見比べながら、関数の形をイメージしてください。 

**▼図15 式(11)のコンター図を描画** 

**fig, ax = plt.subplots() im = ax.contourf(x1, x2, z, cmap='gray') plt.colorbar(im) ax.set_xlabel('x1')** 

###### **ax.set_ylabel('x2') plt.show()** 





###### **偏微分とベクトル** 

2 変数関数は変数が 2 つあるので、どちらの変数で偏微分するかによって 2 種類の導関数( **偏導関数** )が得られます。これは、 1 変数の場合と大きく違う点です。 1 変数関数を微分すると導関数は 1 つに決まるので、導関数の値が正なら関数は増加傾向、負なら減少傾向と判断できました。 2 変数以上の多変数関数では、どうしたらよいでしょうか。偏導関数は変数の個数だけあるので、これらをまとめることを考えます。元の変数の順番に並べてベクトルを作ってみます。これは、 ∇ ( **ナブラ** )という記号を使って次のように表現できます。式( 11 )を使って具体的に計算してみます。偏微分を計算する前に、 SymPy で答えを調べておきましょう。偏微分も同じ diff() 関数で計算できます( **図** 16)。 

**▼図16 SymPyで偏微分を計算** 

**var('x1 x2') f = 1 / (1 + exp(-x1 - x2)) diff(f, x1)** 



**diff(f, x2)** 



どちらの変数で偏微分しても同じ関数になることがわかりました。 1 変数関数の微分で紹介した合成関数の微分は、 2 変数以上でもそのまま通用するので、実際に計算してみます。とするので、とできます。まず、次の計算をしておきましょう。 



_v_ =- ( _x_ 1 + _x_ 2) と置いて、合成関数の微分の知識を使うと、式 ( 12 )が成り立つことがわかります。 



SymPy の結果と一致することがわかりました。いまはどちらの変数で偏微分しても式( 12 )の結果になるので、 ∇ _f_ は次のようなベクトルです。だいぶややこしい式になってきました。これは何を意味するのでしょうか。 ∇ _f_ で表現されるベクトルは、 ( _x_ 1, _x_ 2 )の値によって変わります。具体的な数値を計算し、ベクトルを矢印で表現してコンター図に重ねてみます( **図** 17)。 

###### **▼図17 式(13)のベクトルとコンター図に重ねて描画** 

**x1 = np.linspace(-4, 4, 10) x2 = np.linspace(-4, 4, 10) x1, x2 = np.meshgrid(x1, x2) z = sigmoid2(x1, x2) def diff_sigmoid2(x1, x2): return np.exp(-1 * (x1+x2))/((1+np.exp(-1*(x1+x2)))**2) u = diff_sigmoid2(x1, x2) v = diff_sigmoid2(x1, x2) fig, ax = plt.subplots() im = ax.contourf(x1, x2, z, cmap='gray') plt.colorbar(im)** **<mark># 矢印を書くためのコード</mark> plt.quiver(x1, x2, u, v) ax.set_xlabel('x1') ax.set_ylabel('x2') plt.show()** 



2 変数シグモイド関数のコンター図に、 ∇ _f_ で計算されるベクトルを矢印で追加しました。このベクトルは **勾配ベクトル** と呼ばれます。勾配ベクトルの向きは、 _x_ 1 や _x_ 2 が変化したとき、関数の値がもっとも大きくなる方向を向いています。勾配ベクトルと 180 度逆の方向へ進めば、関数の値は小さくなります。 2 変数シグモイド関数は、海底から大地が迫り上がるような形をしていますので、勾配ベクトルがこの傾斜を表現していることがわかります。もうひとつ別の関数を考えて、勾配ベクトルの可視化をしてみます。次の関数は、原点( 0, 0 )で最小値となり、原点からどの方向へ進んでも関数の値が大きくなります( **図** 18)。 



###### **▼図18 式(14)の関数を可視化** 

**x1 = np.linspace(-2, 2, 20) x2 = np.linspace(-2, 2, 20) x1, x2 = np.meshgrid(x1, x2)** 

**def cone(x1, x2): return 1 - np.exp(-(x1**2 + x2**2))** 

**z = cone(x1, x2) fig, ax = plt.subplots(figsize=(7,7), subplot_kw={"projection": "3d"})** 

**ax.plot_surface(x1, x2, z, cmap="gray") ax.set_xlabel('$x_{1}$') ax.set_ylabel('$x_{2}$') plt.show()** 



とがった帽子をひっくり返したような形になっています。 ∇ _f_ を計算します。 SymPy を使えば微分をすぐ計算できます( **図** 19)。余力がある方は、合成関数の微分を使って手で計算してみてください。 

###### **▼図19 式(14)の偏微分を計算** 

**var('x1 x2') f = 1 - exp(-x1**2 - x2**2) diff(f, x1) diff(f, x2)** 



今度は、 _x_ 1 と _x_ 2 で偏微分した結果が違います。まとめると次のようになります。コンター図を描いて、その上に勾配ベクトルを描画します( **図** 20)。 

###### **▼図20 式(15)のコンター図と勾配ベクトルの描画** 

**x1 = np.linspace(-2, 2, 10) x2 = np.linspace(-2, 2, 10) x1, x2 = np.meshgrid(x1, x2) z = cone(x1, x2)** 

**def diff_cone(x1, x2, on='x1'):** **<mark>'''どちらの変数で微分しているかで結果を変更'''</mark> if on == 'x1': return 2 * x1 * np.exp(-1 * (x1**2 + x2** 2)) return 2 * x2 * np.exp(-1 * (x1**2 + x2** 2))** 

**u = diff_cone(x1, x2) v = diff_cone(x1, x2, on='x2')** 

**fig, ax = plt.subplots() im = ax.contourf(x1, x2, z, cmap='gray') plt.colorbar(im) plt.quiver(x1, x2, u, v) ax.set_xlabel('x1') ax.set_ylabel('x2') plt.show()** 



式( 14 )で定義された関数は原点( 0, 0 )のとき最小値 0 をとり、そこからどの方向へ進んでも関数の値は大きくなります。この様子が勾配ベクトルの矢印からもわかります。また、関数の値を最小化したい場合は、勾配ベクトルと 180 度逆の方向へ進めばよいわけです <u>注3</u> ~~。~~ 

##### **まとめ** 

微分積分の基本から、多変数関数の微分とその意味までを、理解の助けになりそうな Python のコードとともに解説してきました。関数の微分を計算してその値を調べることで、関数の値の増減を知ることができます。これは、目的となる関数の値を最小化する数理最適化の計算に必要不可欠な技術です。線形回帰分析、サポートベクターマシン、人工ニューラルネットワークなどの機械学習アルゴリズムでは、内部で数理最適化と同じ関数の最小値を求める計算が行われています。こうしたアルゴリズムの原理を理解しようと思うとき、この章で説明した微分の知識が必要になります。変化を観察し自然の原理を解き明かすために発展してきた微分積分は、 21 世紀の現在、機械学習アルゴリズムの内部で大活躍し、 AI の革命的な進化を支えています。便利なライブラリを使えば、複雑な計算をいとも簡単に実行できる時代です。アルゴリズムをなんとなく理解している状態から、 1 歩先に進むには数学が欠かせません。コードを使った具体的な計算や可視化を駆使すれば、とっつきにくい数式の理解を助けてくれます。数学書とコンピュータがあれば、数学の無限とも思える世界を旅することができます。ぜひ一緒に楽しみながら学び続けていきましょう。 

**参考文献** 

- 井口和之著『Pythonで理解する微分積分の基礎』(技術評論社、2022年) Pythonのコードを使って微分積分を理解したい方におすすめです。極限の計算から微分方程式までをSymPyを中心としたPythonのコードで丁寧に解説しています。 

- 原岡喜重著『はじめての解析学』(講談社、2018年) 微分積分を少し勉強した経験があり、大学で学ぶ解析学も含めた全体像を知りたい方は、ぜひこの本を手に取ってみてください。微分積分の本質的な意味から、複素解析、量子力学までを解説しているにもかかわらず、わかりやすい解説で読者をまったく飽きさせません。 

- 加藤公一著『機械学習のエッセンス』(SBクリエイティブ、2018年) 機械学習アルゴリズムの数学的な背景を本格的に学びたい方向けです。妥協を許さない数式による解説と、実際に試せるPythonのコードが豊富なため、アルゴリズムを基本から理解することができます。 

- アミーア・アレクサンダー著、足立恒雄訳『無限小世界を変えた数学の危険思想』 (岩波書店、2015年) 

微分積分の基礎となる無限小の考え方が、どれほど革新的であったかがわかる名著です。無限小が、17世紀の知識人に多大な影響を与え、世界の歴史を変える姿が臨場感たっぷりに書かれています。歴史が好きな方にもお勧めです。 

- 注1) 本章で使用しているPythonのコードはGitHubにて確認できます。 <u>https://github.com/tsjshg/sd2305</u> 

(本文に戻る) 

- 注2) -1≦ _x_ ≦ 1の範囲に限定すると _f_ ( _x_ )の最大値と最小値になりますが、関数の定義域 ( _x_ の値の範囲)を広げると _f_ ( _x_ )は-∞から+∞まで変化できるので、最大や最小ではありません。一般的に、ここで求めた値は極大値や極小値と呼ばれます。 (本文に戻る) 

- 注3) 2変数以上の多変数関数では状況がこれほど単純ではない場合もあります。たとえば鞍点(あんてん)では、進む方向によって関数の値が増加することもあれば減少することもあり、最適化という観点から見るとやっかいな存在です。こうした場所を微分を使って見つけることもできます。詳しくは微分積分(解析学)の教科書を参考にしてください。 

   - (本文に戻る) 

# **第 3 章** 

### **線形代数** 

#### **行列計算と一次変換の基本を学ぼう** 



中井悦司(なかいえつじ) <u>@enakai00</u> 

**行列は線形代数においてとても重要な考え方です。しかし、学校で習ったときは、その使い道をイメージできなかった人も多いのではないでしょうか。本章では、ベクトルと行列の基本的な考え方から、行列計算を利用したPythonでの図形処理について学びます。** 

##### **行列で計算するメリット** 

線形代数と言えば、行列を用いた計算が必ず登場します。行列を利用するシーンにはさまざまありますが、そのひとつに「複数の一次関数をまとめて計算する」という場合があります。たとえば、 _x_ 1 羽の鶴と _x_ 2 匹の⻲がいるとして、頭の数の合計 _y_ 1 と足の数の合計 _y_ 2 を計算するとどうなるでしょうか? 

まず、鶴と⻲は、どちらも 1 匹あたりの頭の数は 1 つですので(当たり前?)、頭の数の合計は、です。一方、足の数は、それぞれ、 2 本と 4 本ですので、足の数の合計は、となります。行列の計算ルールはこのあとで説明しますが、これら 2 つの計算式は、行列を用いると次のように 1 つにまとめることができます。この式に含まれる 3 つの行列を左から順に、 **y** 、 _A_ 、 **x** という記号で表すと、次のシンプルな形にまとまります。この程度の計算であれば、わざわざ行列で表すメリットは見えないかもしれませんが、動物の種類(入力データ)の数が増える、あるいは、計算したいもの(出力データ)の数が増えるといった場合でも、いつでも同じ式( 4 )の形に書ける点が重要です。たとえば、このあとで説明するように、 2 次元の平面ベクトルに対する一次変換の計算は、行列を用いることで、 3 次元の空間ベクトルの計算に簡単に拡張できます。あるいは、機械学習では膨大なデータを用いた計算処理を行いますので、データ数の大小に関係なく、いつでも同じルールで計算できるという性質が大切になります。 

##### **行列計算の基本ルール** 

それでは、行列計算の基本ルールを説明していきましょう。先ほど式( 3 )( 4 )に登場した行列 _A_ は、次の形をしていました。このように、縦横に数字を並べたものが行列ですが、一般に、行数(縦の⻑さ)が _N_ で、列数(横の⻑さ)が _M_ の行列を「 _N_ × _M_ 行列」、あるいは、「 _N_ × _M_ サイズの行列」と呼びます( **図** 1)。たとえば、式( 5 )の行列 _A_ は 2×2 行列で、式( 3 )の両端にある次の 2 つの行列は 2×1 行列で ~~す~~ <u>注1</u> ~~。~~ 





そして、行列に含まれる個々の数字を「行列の成分」と呼びます。 **図** 1に含まれる成分のように、 _n_ 行 _m_ 列の位置にある成分を _a nm_ という記号で表します。行列の計算を Python で実装する場合は、 2 次元リストを用いて行列を表現できます。 **図** 2は、式( 5 )の 2×2 行列を定義して、 2 行 1 列の成分 _a_ 21 を表示する例です。 

###### **▼図2 Pythonの2次元リストで行列を表現** 

**A = [[1, 1],** **<mark># 2x2行列を定義</mark> [2, 4]] print(A[1][0])** **<mark># 2行1列の成分を表示</mark> 2** 

行と列の位置が 2 次元リストの 2 つの添え字に対応しています。この際、数学の行列計算では行数や列数は 1 から数えますが、 Python のリストでは 0 から数えるので、添え字の値が 1 だけずれる点に注意が必要です。同じサイズの行列同士は、足し算、引き算を行うことができて、それぞれ、対応する成分の足し算、引き算になります。また、行列の定数倍は、それぞれの成分を定数倍します。次は行列を _k_ 倍する例ですが、この _k_ 自身は行列ではなく、普通の数である点に注意してください。 



##### **行列の掛け算** 

行列同士の掛け算には、少し特別なルールがあります。 _N_ × _K_ 行列 _A_ と _K_ × _M_ 行列 _B_ があったとき、これらの積 _AB_ は、 _N_ × _M_ 行列になります。 **図** 3は、 2×3 行列 _A_ と 3×2 行列 _B_ の積を計算する例になります。図からわかるように、行列 _A_ と行列 _B_ の積を _C_ = _AB_ とするとき、 _C_ の _n_ 行 _m_ 列成分 _C nm_ は、前にある行列 _A_ の _n_ 行目に並んだ成分と後ろにある行列 _B_ の _m_ 列目に並んだ成分から計算されます。取り出した行、および、列に含まれる成分はどちらも _K_ 個で、前から順に対応する成分同士を掛けて合計したものが _C nm_ になります。言葉で説明するとわかりづらいですが、 Python のリストを用いて行列の掛け算を実装してみるのも良い練習になるかもしれません。筆者の実装例は **リスト** 1のとおりですが、この関数 matmul は、のちほど一次変換の計算にも使いますので、 Google Colaboratory などの環境で実際に入力しておいてください。 

###### **▼リスト1 Pythonによる行列の掛け算の実装例** 

**def matmul(A, B):** **<mark># Aの列数とBの行数が一致することを確認</mark> assert(len(A[0]) == len(B))** **<mark># 行列のサイズを取得</mark> N, K, M = len(A), len(A[0]), len(B[0])** **<mark># 計算結果となるN×M行列を用意</mark> C = [[0 for m in range(M)] for n in range(N)]** **<mark># n行m列成分を計算</mark> for n in range(N): for m in range(M): C[n][m] = sum([A[n][k] * B[k][m] for k in range(K)]) return C** 

この掛け算のルールを用いると、冒頭で紹介した 2 つの計算式 ( 1 )( 2 )と、行列を用いた計算式( 3 )は、たしかに同じ内容になっていることがわかります。なお、行列の掛け算を計算する場合は、 2 つの行列の順序に注意が必要です。 **図** 3の例であれば、 _AB_ は計算できても、逆の順番の掛け算 _BA_ は計算できません。また、行数と列数が一致する行列を「正方行列」と呼びます。行列 _A_ と行列 _B_ を同じサイズの正方行列とすると、この場合は、 _AB_ と 

_BA_ のどちらも計算可能になりますが、次の例のように、これらの計算結果は一致するとは限りません。つまり、普通の数の掛け算と異なり、行列の掛け算では、必ずしも _AB_ = _BA_ とはならないのです。なお、この例のように、正方行列同士の掛け算を行うと、同じサイズの正方行列が得られます。したがって、 _A_ 、 _B_ 、 _C_ を同じサイズの正方行列とした場合、 _ABC_ のように 3 つの正方行列を掛けることができます。この場合、 _AB_ と _BC_ のどちらを先に計算しても結果は同じになります。公式的に言うと、次の結合法則が成り立ちます。 



_A_ 、 _B_ 、 _C_ が正方行列でない場合でも、 _ABC_ が計算できるサイズであれば、同じ結合法則が成り立ちます。 4 つ以上の行列を掛ける場合も同様で、隣り合ったどの 2 つから掛け算を計算していってもかまいません。また、先に説明した定数倍、および、足し算・引き算と掛け算を組み合わせた計算については、普通の数の計算と同様に、次の分配法則が成り立ちます。ここでは、 _A_ 、 _B_ 、 _C_ は行列で、 _k_ 、 _l_ は定数倍するための普通の数です。この場合も、 _AB_ 、 _AC_ が計算できるサイズであれば、 _A_ 、 

_B_ 、 _C_ は正方行列でなくてもかまいません。 

##### **単位行列と逆行列** 

正方行列の掛け算では、とくに「単位行列」と「逆行列」が重要な役割を持ちます。まず、単位行列は、次のように対角成分がすべて 1 でほかの成分がすべて 0 の正方行列です。ここでは 3×3 行列の例を示していますが、ほかのサイズでも同様です。一般に記号 _I_ を用いて表します。実際に計算するとわかるように、正方行列同士の掛け算では、行列 _A_ に左右どちらから単位行列 _I_ を掛けても結果は _A_ のままです。つまり、単位行列は、普通の数の掛け算における「 1 」と同じ性質を持ちます。次に、ある正方行列 _A_ に対して、その逆行列 _A_<sup>-1</sup> は、 _A_ に掛けると (左右のどちらから掛けても)その結果が単位行列になるという性質を持ちます。つまり、逆行列 _A_<sup>-1</sup> を掛ける操作は、普通の数で言うと、「 _A_ で割る」という操作に対応します。行列の計算に割り算はありませんが、逆行列を利用することで、割り算に相当する計算ができます。ただし、任意の正方行列 _A_ に対して、その逆行列 _A_<sup>-1</sup> は、必ずしも存在するとは限りません。逆行列が存在するかどうかを判定する際に役立つのが、「行列式」です。行列式は、このあとで説明する一次変換と深い関わりがあるのですが、ここでは、最も簡単な 2×2 行列の場合の公式を先に紹介しておきます。 2×2 行列の成分をとするとき、行列式の値は、次で計算されます。一般に、行列 _A_ に対する行列式の値を det _A_ と表しますが、これは普通の数になります。そして、行列式 det _A_ の値が 0 でなければ、逆行列 _A_<sup>-1</sup> が存在します。 2×2 行列の場合、逆行列は具体的に次で計算されます。また、 2 つの(同じサイズの)正方行列 _A_ 、 _B_ に対して、次の関係が成り立ちます。式( 10 )の両辺では、 _A_ と _B_ の順序が入れ替わっている点に注意してください。これが成り立つことは、次の計算で確認できます。最初の等号は、隣り合ったどの 2 つから掛け算を計算してもかまわないという、式( 6 )の結合法則を用いています。ここでは、行列 _AB_ の右から行列 _B_<sup>-1</sup> _A_<sup>-1</sup> を掛けていますが、左から掛けた場合も同様の計算が成り立ちます。行列 _AB_ に行列 _B_<sup>-1</sup> _A_<sup>-1</sup> を掛けると単位行列 _I_ になることから、行列 _B_<sup>-1</sup> _A_<sup>-1</sup> が行列 _AB_ の逆行列( _AB_ )<sup>-1</sup> ということになります。ちなみに、逆行列で _A_ と _B_ の順序が反対になることを「靴下の法則」と呼ぶ人がいます。これは「靴下を履いて靴を履いたら、脱ぐときはその逆の順番が必要」ということから来ているようです。そしてもう一つ余談をすると、冒頭の鶴と⻲の計算では、鶴と⻲ の数 _x_ 1 、 _x_ 2 がわかっている前提で、頭と足の数 _y_ 1 、 _y_ 2 を計算することを考えましたが、実際の「鶴⻲算」はこの逆です。頭と足の数 _y_ 1 、 _y_ 2 がわかっているとして、鶴と⻲の数 _x_ 1 、 _x_ 2 を求める必要があります。これは、逆行列を使うと一発で答えがでます。式( 9 )の公式を用いると、式( 5 )の逆行列は、次のように計算できます。この _A_<sup>-1</sup> を式( 3 )の両辺に左から掛けると、左辺は、になり、一方、右辺は、となります。これらが等しいことから、次の関係が得られます。この結果を用いれば、頭と足の数 _y_ 1 、 _y_ 2 から鶴と⻲の数 _x_ 1 、 _x_ 2 を簡単に逆算できます。 

##### **平面ベクトルの一次変換** 

先ほどの鶴⻲算は、数学的には「連立一次方程式」の問題になります。行列を用いることで簡単に解くことができましたが、行列が活躍する場面はこれだけではありません。ここでは、行列の重要な応用例である「ベクトルの一次変換」を説明します。はじめに、 2 次元の平面ベクトルで説明したあと、同じ内容を 3 次元の空間ベクトルへと拡張していきます。まず、( _x, y_ )座標を持つ 2 次元平面上の点は、式( 2, 3 )のように、 _x_ 座標と _y_ 座標の値の組で表すことができます。このような、平面上の点を表す値の組を次のように縦に並べたものを「平面ベクトル」と呼びます。平面ベクトルは 2×1 行列と見ることもできて、行列の計算ルールを適用すると、平面ベクトルの定数倍、および、平面ベクトル同士の足し算は次のように計算されます。これらの操作は、 **図** 4のように図形的に解釈できます。つまり、平面ベクトル **x** は、原点 _O_ から点 **x** に向かって伸びた「矢印」を表しており、この矢印の⻑さを _k_ 倍に伸ばしたり、 2 つの矢印を組み合わせた新しい矢印を合成する操作に対応しています。平面ベクトルの中でもとくに、次の 2 つを「標準基底」と呼びます。これらは、 _x_ 軸、および、 _y_ 軸の方向を向いた⻑さが 1 の平面ベクトルで、任意の平面ベクトル **x** は、これらの標準基底を用いて、次の形で書き表せます。次のように書くとわかるように、 2 つの係数 _x_ 1 、 _x_ 2 は、 **x** の成分に一致します。平面ベクトル **x** と標準基底 **e** 1 、 **e** 2 の関係は、 **図** 5のように図形的に理解することもできるでしょう。そして、平面ベクトルの一次変換とは、 4 つのパラメータ _a_ 、 _b_ 、 _c_ 、 _d_ によって決まる変換で、平面ベクトルを新しい平面ベクトルに次の計算式で変換します。数式だけ見ていても何をやっているのかわかりませんが、実はこれによって、平面全体を拡大・縮小したり、あるいは回転したりできます。ゲームプログラミングでキャラクターを拡大・縮小、あるいは、回転する際に利用できる変換なのです。順を追って説明していきましょう。まず、一次変換には、「標準基底の行き先が決まれば、すべてのベクトルの行き先が決まる」という大きな特徴があります。これは、次のように理解できます。はじめに、先ほどの式( 12 )( 13 )を行列を使って次のように表します。パラメータを並べた行列をと置くと、次のようにシンプルにまとめることもできます。つまり、平面ベクトルの一次変換とは、 2×2 行列 _A_ を左から掛ける操作にほかなりません。そして、ここに式( 11 )の関係を代入します。先に式( 7 )で説明した分配法則を利用すると、次の計算が成り立ちます。最後の _A_ **e** 1 、 _A_ **e** 2 は、標準基底 **e** 1 、 **e** 2 を一次変換 _A_ で移した先の平面ベクトルです。これらを **e** 1 **′** 、 **e** 2 **′** という記号で表すと、結局のところ次の関係が成り立ちます。これは、式( 11 )において、標準基底 **e** 1 、 **e** 2 をそれぞれの行き先 **e** 1 **′** 、 **e** 2 **′** に置き換えたものにほかなりません。ここで、式( 11 )を図形的に表した **図** 5を思い出します。平面ベクトル **x** は、標準基底 **e** 1 、 **e** 2 をそれぞれ _x_ 1 倍、 _x_ 2 倍に伸ばして合成した位置にあります。同様に、式( 15 )からは、平面ベクトル **x′** は、 **e** 1 **′** 、 **e** 2 **′** をそれぞれ _x_ 1 倍、 _x_ 2 倍に伸ばして合成した位置にあることがわかります。この事実は、 **図** 6のように図示できます。 **図** 5には標準基底を単位としたマス目が描かれていましたが、 **図** 6では **e** 1 **′** 、 **e** 2 **′** を単位とした、変形したマス目に変わっています。そして平面ベクトル **x** は、このマス目の変形に合わせて、 **x′** に移動しています。つまり、一次変換というのは、標準基底を移動することによって、これに合わせて平面全体を一様に変形する効果があるのです。 **図** 7のように、標準基底を回転させてあげれば、これに合わせて平面上のすべてのベクトル **x** が回転することになります。 



##### **Pythonによる回転処理の実装例** 

それでは、本当に一次変換で回転処理ができるのか、 Python のコードで実装してたしかめてみましょう。そのためには、回転処理に対応する一次変換 _A_ を決定する必要があります。そこでまずは、式 ( 14 )の行列 _A_ で標準基底 **e** 1 、 **e** 2 を変換した結果を見てみましょう。この計算結果からわかるように、行列 _A_ に含まれる縦の 2 列が、それぞれ、標準基底 **e** 1 、 **e** 2 の行き先に対応しています。したがって、希望する行き先に合わせてこれらの値を決めれば良いのです。たとえば、 45 度回転するのであれば、 **図** 8から次のように決まります。行列 _A_ の形に再構成すると、次になります。あとは、平面上の任意の点に対して、この行列 _A_ を左から掛けると、 45 度回転した位置の点が得られるというわけです。 **リスト** 2はこれを実際に計算するコードの例です。 

###### **▼リスト2 Pythonによる回転処理の実装例** 

**import random** **<mark># 45度回転の行列</mark> A = [[1/1.414, -1/1.414], [1/1.414, 1/1.414]]** 

**<mark># 回転前の座標を乱数で生成</mark> points = [] for _ in range(10): x1 = random.uniform(-0.6, 0.6) x2 = random.uniform(-0.6, 0.6) x = [[x1], [x2]] points.append(x)** 

**<mark># 回転後の座標を一次変換で計算</mark> new_points = [] for x in points: new_points.append(matmul(A, x))** 

ここでは、乱数で 10 個の点を生成したあとに、それらに行列 _A_ を掛けて移動先の点を取得しています。行列を掛ける処理は、先に実装した関数 matmul を使用しています。また、行列 _A_ に含まれる平方根の値は、 ≒1.414 という近似値を使っています。 

**リスト** 3は、計算結果を表示するコードです。これを実行すると、 **図** 9のような結果が得られます。左が回転前で、右が回転後の様子ですが、たしかに 45 度回転していることがわかります。行列 _A_ の内容を変えれば、回転のほかにも拡大・縮小・反転などの処理が実行できます。先ほど説明したように、行列 _A_ の縦の 2 列に標準基底 **e** 1 、 **e** 2 の行き先を指定するだけですので、思いどおりの変換ができるか実際に試してみてください。一例を挙げると、横に 1/2 に圧縮して、縦に 2 倍に拡大するならば、次の行列が使えます。 

###### **▼リスト3 回転処理の計算結果を表示するコード** 

**import matplotlib.pyplot as plt** **<mark># 回転前の点を表示</mark> fig = plt.figure(figsize=(10, 4)) subplot = fig.add_subplot(1, 2, 1) xs = [x for x, y in points] ys = [y for x, y in points] subplot.scatter(xs, ys) subplot.set_xlim((-1, 1)) subplot.set_ylim((-1, 1)) subplot.set_aspect('equal')** 

**<mark># 回転後の点を表示</mark> subplot = fig.add_subplot(1, 2, 2) xs = [x for x, y in new_points] ys = [y for x, y in new_points]** 

**subplot.scatter(xs, ys) subplot.set_xlim((-1, 1)) subplot.set_ylim((-1, 1)) subplot.set_aspect('equal')** 





##### **行列式の図形的な意味** 

先に行列式を説明した際に、行列式 det _A_ は一次変換に深い関わりがあると言いました。実は、この値は、行列 _A_ が表す一次変換の「拡大率」に対応します。先の **図** 5において、標準基底が作る 1 つのマス目の面積は 1 ですが、一次変換したあとの **図** 6では、これは平行四辺形のマス目に変形されています。この変形されたマス目の面積は、行列式の絶対値 |det _A_ | にちょうど一致します。実際、 **図** 10の右を見ながら **e** 1 **′** 、 **e** 2 **′** が作る平行四辺形の面積を計算すると | _ad_ - _bc_ | になっており、式( 8 )で紹介した行列式の絶対値に一致しています注 <u>2</u> ~~。~~ 

このように、行列式の絶対値 |det _A_ | はマス目の拡大率になっているわけですが、 **図** 10からわかるように、これは平面全体の拡大率と同じものです。ちなみに、先ほど Python のコードで実行した 45 度回転の場合はどうなっているでしょうか? この場合は、回転処理だけで、拡大は行っていませんので、拡大率は 1 です。一方、対応する行列 _A_ は式( 16 )になっており、次の計算から、この行列式はちょうど 1 になっていることがわかります。それでは、行列式が 0 になる、すなわち、拡大率が 0 になるのはどのような場合でしょうか? これには、標準基底の行き先 **e** 1 **′** 、 **e** 2 **′** が並行になるような場合があります。 **図** 11は **e** 1 **′** 、 **e** 2 **′** がぴったり一致する例を示していますが、この場合、元の平面は、 **e** 1 **′** 、 **e** 2 **′** 方向の直線に押しつぶされてしまい、マス目の面積は 0 になる、つまり、拡大率は 0 になることがわかります。 



##### **空間ベクトルへの拡張** 

ここまで、 2 次元平面上の平面ベクトルに対する一次変換を説明してきましたが、これを 3 次元以上の空間に拡張するのは容易です。たとえば、 3 次元空間の点は、( _x, y, z_ )の 3 つの座標で決まるので、次の空間ベクトル **x** で表すことができます。この場合、標準基底は、次の 3 つになります。先ほどの空間ベクトル **x** は、次のように標準基底を用いて表すことができます。そして、空間ベクトルの一次変換は、次のような 3×3 サイズの正方行列で表されます。平面ベクトルの場合と同じ議論を適用すると、行列 _A_ に含まれる縦の 3 列が、それぞれ、標準基底 **e** 1 、 **e** 2 、 **e** 3 の行き先 **e** 1 **′** 、 **e** 2 **′** 、 **e** 3 **′** に対応することがわかります。そして、式( 17 )に左から行列 _A_ を掛けることで、空間ベクトル **x** の行き先は次のように決まります。先の **図** 5、 **図** 6の関係と同様に、標準基底 **e** 1 、 **e** 2 、 **e** 3 が作る体積 1 の立方体は、一次変換によって、 **e** 1 **′** 、 **e** 2 **′** 、 **e** 3 **′** が作る平行六面体(変形された立方体)に移ります。これに合わせて、空間全体が一様に変形されることになります。したがって、 3 次元空間の中で立体的に回転・拡大・縮小したければ、まずは、標準基底 **e** 1 、 **e** 2 、 **e** 3 の行き先 **e** 1 **′** 、 **e** 2 **′** 、 **e** 3 **′** を決定して、それらを並べることで、対応する一次変換の行列 _A_ を決定できます。ちなみに、回転と拡大・縮小など、複数の変換を組み合わせる際は、行列の掛け算が利用できます。たとえば、回転処理を行う行列を _A_ 、拡大処理を行う行列 _B_ をとした場合、空間ベクトル **x** を回転した結果は _A_ **x** になります。これをさらに拡大した結果は **x′** = _B_ ( _A_ **x** ) ですが、式( 6 )の結合法則を用いると、となります。つまり、はじめに _A_ と _B_ の掛け算 _BA_ を計算しておけば、これが、回転 _A_ と拡大 _B_ を続けて実行する行列ということになります。最後に、一次変換を表す行列 _A_ に対して、行列式の絶対値 |det _A_ | が拡大率を表すという点も同じです。 3 次元以上の場合の行列式の公式は、計算式が複雑になるので具体的には示しませんが、 |det _A_ | は、 **e** 1 **′** 、 **e** 2 **′** 、 **e** 3 **′** が作る平行六面体の体積に一致する形になっています。 

##### **まとめ** 

本章では、線形代数の入門として、行列の基本的な計算方法と、ベクトルの一次変換について説明しました。また、一次変換で図形を回転する Python のサンプルコードを紹介しました。このサンプルコードでは、行列の計算方法を理解するために、 Python のリストを用いて計算処理を愚直に実装しましたが、数値計算用のライブラリである NumPy を使用すれば、さまざまな行列計算をより高速に実行できます。ここでは、 NumPy の使用方法は説明しませんでしたが、ネットで検索すれば、さまざまな教科書やチュートリアルが見つかります。線形代数の基本が理解できたら、次は、 NumPy を用いたより実践的なコードの実装にも挑戦すると良いでしょう。 



##### **量子コンピュータと線形代数** 

最近、量子コンピュータに関する話題をよく耳にしますが、実は、量子コンピュータの計算原理は線形代数にあります。「0と1の重ね合わせ状態を使用する」といった説明を聞いたことがあるかもしれませんが、量子コンピュータが計算に使用する「量子ビット」の状態は、2次元ベクトルで表現できて、2次元ベクトルの2つの成分のそれぞれが、「0」と「1」の2種類の値に対応します。そして、この2次元ベクトルに行列を掛けることで、量子ビットの状態が変化していきます。2次元ベクトルの成分が複素数値になるという特殊な点もありますが、線形代数を学ぶメリットは、機械学習だけにとどまるわけでないことをぜひ知っておいてください。 

- 注1) このような1列だけの行列は、「縦ベクトル」とも呼びます。同じく、1行だけの行列は「横ベクトル」とも呼びます。 (本文に戻る) 

- 注2) この平行四辺形の面積が | _ad_ - _bc_ | になることは、図形の問題として考えればそれほど難しくありません。興味ある方は、紙と鉛筆で計算してみてください。 (本文に戻る) 

# **第 4 章** 

### **集合・位相** 

#### **「距離空間」を理解するための第一歩** 

北村友和(きたむらともかず) <u>@pkitamura</u> 小島諒介(こじまりょうすけ) <u>@kojima_r</u> 株式会社スクラムサイン <u>https://scrumsign.com</u> 

**検索などにおいて「検索キーワードに近いものから表示」するといった機能は物理的な「近さ」ではなく抽象的な意味での「近さ」にあたるものと言えるでしょう。本稿ではこういった抽象的な意味での「距離」を扱う方法について解説します。** 

##### **はじめに** 

大学数学で学ぶ集合と位相は、集合論と位相空間論を合わせたものです。集合論ではものの集まりを数学的に「集合」と定義し、集合の「濃度」や、無限にも可算無限と非可算無限があることなど、現代数学を基礎づけている抽象的な概念を学んでいきます。また位相空間論は、集合に開集合( _a_ よりも大きく _b_ よりも小さい実数の区間)や閉集合(端点 _a_ と _b_ を含む区間)、近傍などの数学的構造を設けて、集合の要素同士に「近さ」の概念を付け加えたものです。本章では、「集合と位相」の中でも有用な概念である「距離」にフォーカスをあてていきます。実際に機械学習では、必要に応じてさまざまな種類の距離を使い分けています。 

##### **距離の定義** 

「距離」とは一般にどのようなものでしょうか? オブジェクト指向を勉強した人であれば特定のインターフェースを満たすものを扱うことの便利さに気づくと思いますが、距離とは、一般に次の性質を持つ量として語られます。集合 _X_ と _d_ : _X_ × _X_ → R( _X_ の元を 2 つとって実数を返す写像)は、任意の _a_ 、 b 、 _c_ ∈ _X_ に対して次の条件を満たす。 

- ・ _d_ ( _a_ , _b_ )= _0_ ⇔ _b_ = _a_ 

- → 距離が0であれば2つの点は一致するし、逆に、2つの一致する点は距離が0となる 

- ・ _d_ ( _a_ , _b_ )= _d_ ( _b_ , _a_ ) 

- → 2つの点を入れ替えても距離は変わらない(対称性) 

- ・ _d_ ( _a_ , _b_ )+ _d_ ( _b_ , _c_ ) _≥ d_ ( _a_ , _c_ ) 

- → _a_ から _c_ へ行く場合は、 _b_ に寄り道したほうが、寄り道しない場合に比べて合計の距離は⻑くなる(三角不等式) 

上記の定義では「集合 _X_ 」と抽象的に書いてありますが、この集合 _X_ にはさまざまな対象を選択できるので、以降で紹介していきます。 

##### **n次元実数空間上の距離** 

まずは、ベクトル間の距離に関して紹介します。これは通常の物理的な距離(現実の 3 次元空間の距離)をさらに _n_ 次元に一般化したものとみなすことができます。 



###### **ユークリッド距離** 

_n_ 個の実数を並べたベクトルに対して、のように距離を定義したものを、 _n_ 次元の場合のユークリッド距離といいます。「ある地点とある地点の直線距離」と言われるものです。ユークリッド距離について、距離になる場合とならない場合をプログラムで確認してみましょう。 **図** 1は 3 点 A 、 B 、 C の⻑さを計算し、ある点からある点に行く際にほかを経由すると距離が⻑くなるかを確認するものです。先ほどの三角不等式の条件を満たすことが確認できるでしょう。ただし、平方根をとるのを忘れると三角不等式を満たさなくなります。 

###### **▼図1 三角不等式を満たさないケース** 

**import numpy as np import matplotlib.pyplot as plt** 

**def d(a,b):** **<mark># ユークリッド距離</mark> return np.sqrt(np.sum((np.array(a)-np.array(b))**2))** **<mark># 平方根をとるのを忘れた場合 # return np.sum((np.array(a)-np.array(b))**2)</mark>** 

**a=[0,0] b=[3,1] c=[3,4] print("check?:",d(a,b)+d(b,c),">",d(a,c)) print("check?:",d(b,c)+d(c,a),">",d(b,a)) print("check?:",d(b,a)+d(a,c),">",d(b,c))** 

**<mark>(..略..)</mark> plt.show()** 

**check?: 6.16227766016838 > 5.0 check?: 8.0 > 3.1622776601683795 check?: 8.16227766016838 > 3.0** 





###### **マンハッタン距離** 

京都や札幌などで見られる、碁盤の目状に道が整備されている場合の実際の距離は、で計算します。これはマンハッタン距離と呼ばれるもので、すべての要素の差分によって計算されます。 



###### **チェビシェフ距離** 

最大値を与える計算を用いて計算する距離も存在します。チェビシェフ距離が代表的です。たとえば 2 地点間によって「バスでの移動距離」「新幹線での移動距離」「飛行機での移動距離」という 3 要素で示す場合はほぼ飛行機での移動距離によって決まり、飛行機での移動距離がまったく同じ場合は新幹線での移動距離によって決まり、それも一緒の場合はバスでの移動によって決まる、といった具合です。最も差が大きい部分が全体の距離に関して支配的になるので、このチェビシェフ距離を使うのが適していると言えます。 

◆ ◆ ◆ 

**図** 2に、 2 次元の場合のユークリッド距離、マンハッタン距離、チェビシェフ距離を用いたときの違いを図示しました。マスの中の数字が中央のマスから各マスへの距離に相当します。 





###### **マハラノビス距離** 

上記の距離のほかにも、機械学習でデータ間の距離を計算するのによく用いられる距離に「マハラノビス距離」があります。これは、多変数ベクトル _x_ 、 _y_ とその平均ベクトル _x_ 、 _y_ を用いて次のように定義されます。ここでの Σ は「分散共分散行列」と呼ばれる行列です。分散の多次元版で、データから計算できる行列という理解で問題ありません。また _T_ は、ここでは縦ベクトルを横ベクトルに変換する「転値」と呼ばれる操作になります。この距離は、直観的にはデータの中心から「分散を考慮して」どれだけ離れているかを表しており、 Σ が単位行列のとき、ユークリ 

###### ッド距離と等しくなります。 

##### **文字列間の距離** 

ベクトルの次に機械学習やデータ解析でよく利用される距離として、文字列間の距離があります。 



###### **ハミング距離** 

「文字列のうち何文字異なるか」を示したものをハミング距離と言います。たとえば「 abcd 」と「 abxd 」だと 3 文字めのみが異なるので、ハミング距離は 1 となります。記号列や整数列といったものでもよく利用される距離です。 



###### **編集距離** 

より文字列らしさを反映している方法として「片方の文字列から片方の文字列にたどり着ける最小の編集(操作)回数」を表した数値である編集距離(レーベンシュタイン距離)もよく利用されます。うまく 1 回の操作を対称になるようにとることで、対称性の定義を満たします。通常、編集は文字の削除・挿入・置換の 3 つで構成されます。たとえば「 abcd 」と「 bcdx 」は文字がすべて異なるのでハミング距離は 4 ですが、「 abcd 」から「 bcdx 」への編集は 1 文字めの「 a 」の削除と 4文字めに「 x 」を追加の 2 回が最小なので、編集距離は 2 となります。また、⻑さが異なる場合として「 abcd 」と「 xabbde 」の間の編集距離を考えます。 1 文字めに「 x 」を追加、 4 文字めの「 c 」を「 b 」に置換、 6 文字めに「 e 」を追加、という編集が最小回数の 1 つになり、この場合の編集距離は 3 です。実際にプログラムで確認してみましょう( **図** 3)。 

###### **▼図3 編集距離の計算** 

**<mark># python-Levenshteinパッケージをインストール</mark> !pip install python-Levenshtein** 

**import Levenshtein string1 = "abcd" string2 = "xabbde" print(Levenshtein.distance(string1, string2))** **<mark>(..略..)</mark> 3** 

文字列以外にも対象間の距離を編集の最小回数で定義することもしばしば行われます。 



###### **関数間の距離** 

関数間の距離というものもあります。最も有名な距離は次のように定義されます。関数間の距離は積分を用いることが多いので計算機上で厳密に計算することは難しいですが、理論的に利用する場合や数値的に近似として利用することが多いです。ただし、これを距離とするにはこの積分が有界である(無限に飛んでいかない)必要があります。積分範囲は定義域全体で定義することが多いため、 **図** 4では実数全域としています。 

###### **▼図4 関数間の距離の計算** 

**def f(x): return np.exp(-x**2)** 

**def g(x): return np.exp(-(x-2)**2/4) def h(x): return np.exp(-(x-4)**2/2)** 

**dx=0.1 def d(a,b): x=np.arange(-10,10,dx) return sum(np.abs(a(x)-b(x))*dx)** 

**print("check?",d(f,g)+d(g,h),">", d(f,h)) print("check?",d(g,f)+d(f,h),">", d(g,h)) print("check?",d(f,h)+d(h,g),">", d(f,g))** 

**<mark>(..略..)</mark> plt.legend()** 

**check? 7.074012816602511 > 4.197544934683021 check? 7.685218549712154 > 3.5863392015733777 check? 7.783884136256399 > 3.487673615029133 <matplotlib.legend.Legend at 0x7fae51ebb070>** 



##### **確率分布の距離** 

関数間に距離が定義できるのと同じように、同じ定義域の確率変数のばらつきのようなものを考慮することで、確率分布の距離を考えることもできます。ここでは、確率変数の場合に用いられる全変動距離( total variation distance )の例を示します。離散的な確率間の全変動距離は次のように定義されます。同様に、実数をとる連続確率分布を持つ場合は次のように計算できます。関数間の距離の式を 1/2 したものと一致することがわかると思います。全変動距離の厳密な定義ではさらに一般の場合に適用できるように記述されますが、ここでは簡単のためにこのように書いています。 



##### **KLダイバージェンス** 

分布間の距離ではKLダイバージェンスと呼ばれる量もよく利用されます。KLダイバージェンスは三角不等式および対称性を満たさないため、距離ではないですが、機械学習ではしばしば登場するためここで示しておきます。離散確率変数に関しては次のように定義されます。ここで、QとPを別の確率分布を表す記号として用いています。QとPが同一の分布の場合にはlogの部分が常に0となるため、このKLダイバージェンスは0になることがわかります。連続確率変数の場合、確率密度分布 _p_ 、 _q_ に関する距離は次のように定義されます。 



##### **機械学習への距離の利用事例** 

距離はさまざまな場面で用いられるため、すべて事例を挙げることは困難ですが、ここではとくによく利用される例をいくつか挙げておきます。 



###### **距離とクラスタリング** 

データ解析の初期段階において、データの特徴をつかまえるためにデータをある尺度で部分集合(クラスタ)に分割するクラスタリングが行われます。 

DBSCAN ( Density Based Spatial Clustering of Applications with Noise )はクラスタリング手法の 1 つです。次の 2 つのパラメータを使用します。 

- ・ min_points:クラスタとして取り扱う最小限の点の数を制御する 

- ・ ε:min_pointsを満たすクラスタの点を探索する際に、ある点からどのくらい離れたところまで見るかを制御する次のように、集合をデータの「密な」領域でクラスタへと分割します。 

- 1 データ内のある点を選ぶ 

- 2 その点からε距離以内(距離の取り方で変わる)にあるほかの点を探し、そのような点が見つからない場合は1に戻る 

- 3 ε距離以内に別の点が見つかったら、これを候補とし、2と3を繰り返す。繰り返した結果、min_pointsで定めたクラスタのための規定数を超えた点が見つかるとそれを1つのクラスタとして扱う 

- 4 各点をすべて訪問した時点で停止する 

**図** 5は、 3 つのクラスタを持つデータ(左上)と、そのデータを 3 種類の距離で DBSCAN クラスタリングした結果です。マンハッタン距離(左下)は斜めの移動距離がユークリッド距離(右上)より大きくカウントされる影響でクラスタが細かく分割されています。マハラノビス距離(右下)は距離を測るときに分散共分散行列が考慮されるので、今回のケースでは 3 つのクラスタをとらえられています [ 1 ][ 2 ]。 





###### **最近傍法・k近傍法** 

距離を用いた方法として最近傍法があります。識別や予測において、すでにあるデータに対して一番近いデータを検索し、それと同じラベルを出力する方法です。 k 近傍法はこれを改良した方法で、 _k_ 番めまでに近いデータを検索し、それらをもとに出力を多数決などで決めます。たとえば **、** バツ印とマル印のデータ点があるとき、新たな黑い星のデータがどちらに属するのか、 5 番め( _k_ = 5 )までに近いデータ点を見ることで決めるとします。データ間の距離をユークリッド距離で求めるとすると、 **図** 6の場合バツ印が多いので、この新たな黑い星のデータはバツ印に属すると予測できます。 



##### **まとめ** 

今回は、集合と位相の中でも実用上重要な概念である「距離」にフォーカスをあて説明しました。文字列や確率分布、そして関数の間も距離として測れることに驚かれた方もいらっしゃっるでしょう。どのような距離を採用するかにより結果が違ってくることなどを通じて、距離の違いを実感していただけたと思います。集合と位相は、現代数学の土台を作っている重要な分野です。集合と位相を学び抽象的な距離を「構成」していくための条件を学んでいくのですが、初学者はそこまでの道のりが非常に⻑いことに驚かれるかと思います。ですが、時間をかけてじっくり理解していけるようになると、数学のおもしろさが実感できると思います。これがきっかけで興味を持たれた方はぜひ参考文献<sup>[3][4]</sup> などを読んでみてください。最後に、筆者らが運営している数学と機械学習のコミュニティ Math & Codin ~~g~~ <u>注1</u> ~~を~~ 紹介します。 Math & Coding は数学と機械学習の理論や実装を深く学びたい方が、その時々のテーマとする本を題材に集い議論するコミュニティです。参加者同士の活発なディスカッションを行いながら理解を深めています。 2023 年 4 月時点では「群論」と「ベイズ深層学習」の勉強会を月 2 回行っています。関心がある方はぜひご参加ください。 

###### **参考文献** 

- [1] Ester, M., H. P. Kriegel, J. Sander, and X. Xu, “A Density-Based Algorithm for Discovering Clusters in Large Spatial Databases with Noise”. In: Proceedings of the 2nd International Conference on Knowledge Discovery and Data Mining, Portland, OR, AAAI Press, pp. 226-231. 1996 <u>http://www.cs.ecu.edu/~dingq/CSCI6905/readings/DBSCAN.pdf</u> 

- [2] A Comprehensive Introduction to Clustering Methods 

   - <u>https://shairozsohail.medium.com/a-comprehensive-introduction-to-clusteringmethods-1e1e4f95b501</u> 

- [3] 松坂和夫著、『集合・位相入門』、岩波書店、2018年 

- [4] 内田伏一著、『集合と位相(増補新装版)』、裳華房、2020年 



##### **異常検知にチャレンジ** 

#### **異常検知とは** 

クレジットカードの利用ログから不正利用を検知したり、工場におけるセンサーデータを使って機械の故障を検知したりなど、異常検知は産業において幅広く利用されています。私たちの身の回りにはデータがあふれていますが、もし興味のある対象の観測値を生成する確率分布がわかったとすると、逆にどの程度、普段のパターンから外れたかということを数値化できます。 

#### **ホテリングのT**<sup>**2**</sup> **法について** 

今回は教師なし異常検知で基本的なホテリングのT<sup>2</sup> 法を見ながら確率分布の有用性を体験していきます。ホテリングのT<sup>2</sup> 法とは、データの異常度 _a_ ( _x'_ )を次の数式で求める手法です注A ~~。~~ 



この式を簡単に説明すると「あるデータ _x'_ が与えられたときに、その平均μから離れているかをデータの分散Σを考慮して計算する」という意味です。ホテリングのT<sup>2</sup> 法でしきいちは、この結果が閾値を超えたら「異常である」と判定します。今回はデータ数が項目数 (M)より大きい場合において自由度がMのカイ二乗分布に近似的に従うことを利用して異常を検知します。 

#### **異常検知の実装** 

**図** Aは、異なる平均値を持つ正規分布に基づいて1,000個のデータポイントを生成しています。このとき、3個のセンサーが1秒ごとに値を記録しログに吐き出していると想定します。800秒から1,000秒が故障の状態です。故障をシミュレートするために、1つのセンサーに800秒からガウスノイズを加えています( **図** B)。このときのカイ二乗分布を **図** Cに示します。ここでは、定義式で求めた異常度がカイ二乗分布に従うことを利用するため、このカイ二乗分布のうち、異常検知される確率が0.5%になるように閾値 (threshold)を設定します。確率0.5%になる点を選択するためには、閾値より右側の面積が0.005になるように閾値を選択しま ~~す~~ <u>注B</u> ~~。~~ **図** Cの太くなっている部分がその領域に対応します。 

###### **▼図A 異常検知のための人工データの作成** 

**<mark># 人工データの作成</mark> def synthetic_sensor_data(num_sensors,num_examples): mean = np.array([(i+1)*15 for i in range(num_sensors)])** 

**cov = np.ones((num_sensors,num_sensors))*0.5 cov += np.identity(num_sensors) * 0.5** 

**X = np.random.multivariate_normal(mean, cov, num_examples)** 

- **<mark># 故障をシミュレート</mark>** 

**interval = 800** 

**X[interval:,1] += np.random.normal(loc=2,scale=1,size=len(X)-interval) return X** 

**data = synthetic_sensor_data(3, 1000) data** 

**array([[13.91966668, 30.70347887, 45.11406869], [15.3025998 , 29.9912101 , 44.2504958 ],** 

**[16.01946324, 31.92105081, 44.98406769],** 

**<mark>(..略..)</mark>** 

- **[13.60155288, 33.98796045, 44.42696506],** 

- **[14.29341746, 32.62369566, 43.70088372], [17.43139934, 33.94916832, 46.67220802]])** 





ではまず、モデルとして500秒までのデータでトレーニングします。具体的にはマハラノビス距離に必要な精度行列を学習させています。次に異常と判定するアラートの閾値を算出します。冒頭にも記載していますが今回のように正規分布の次元数がデータ数より十分に大きい場合、マハラノビス距離の2乗は自由度が3のカイ二乗分布で近似できることがわかっています。800秒めからは、ほとんどのデータが閾値以上に位置していることから、正常な動作をしていることがわかります( **図** D)。 

###### **▼図D ホテリングのT**<sup>**2**</sup> **法を用いた異常検知** 

**training_data = data[:500].copy()** 

**cov_inv = np.linalg.pinv(np.cov(training_data.values.T)) T2 = []** 

**for i in range(len(data)):** 

**t2_score = distance.mahalanobis(data.loc[i,:],data.mean(axis=0),cov_inv)**2 T2.append(t2_score)** 

**fig = px.line(T2)** 

**fig.add_shape(type='line',x0=0,y0=threshold,x1=1000,y1=threshold,line=dict(c olor='Red',),xref='x',yref='y')** 

**fig.show()** 



これを応用発展させれば多変量の時系列データの異常を自動検知することができます <u>注C</u> ~~。~~ 

- 注A この異常度 _a_ ( _x'_ )はマハラノビス距離の2乗とも対応しています。 (本文に戻る) 

注B プログラム中ではこの計算をchi2.isfで実現しています。 

   - (本文に戻る) 

- 注C ホテリングT<sup>2</sup> 法に興味を持たれた方は、以下の文献をご参照ください。・杉山将、井手剛著、『異常検知と変化検知』、講談社、2015年・Anomaly Detectiont ─ Hotelling’s T2 Statistic <u>https://medium.com/datascience-devops/anomaly-detectiont-hotellings-t2statistic-cffb833c3052</u> 

(本文に戻る) 



# **第** **_1_ 章** 

### **WebAssemblyでできること、できないこと** 

##### **ポータビリティとセキュリティを活かした成功事例** 

山本悠滋(やまもとゆうじ) <u>@igrep</u> 株式会社インターネットイニシアティブ 

**近年、ブラウザ上で実行できる新たなプログラミング言語としてWebAssemblyが加わりました。WebAssemblyを使ってSQLiteやNode.jsをブラウザ上で動かす事例も出てきています。実際のところ、どんな課題を解決でき、どれほどの可能性を秘めているのでしょう? 本章でいくつかの採用例を見ながら探ってみましょう。** 

##### **WebAssemblyの概要** 

WebAssembl ~~y~~ <u>注1</u> ~~(~~ 以下、略称の「 Wasm ~~」~~ <u>注2</u> ~~と~~ 呼びます)は、ブラウザをはじめとするさまざまな環境で動作し、なおかつさまざまなプログラミング言語からコンパイルできることを目指した、仮想マシンに対する命令セットです。特定のハードウェアやプログラミング言語への依存を極力排除することでポータビリティを高めているほか、実行速度やファイルサイズ、セキュリティを重視して設計されています。 Wasm は 2015 年に発表されたあと、 2019 年に W3C より、 JavaScript 以外で唯一のブラウザ上で直接動くプログラミング言語として標準化されました。公認の Web 標準となったのです。 

##### **WebAssemblyの技術要素、概念** 

Wasm に関わる諸概念を、プログラムを Wasm で動かすまでの流れに沿って紹介しましょう。まず、原則として Wasm は人間が直接読み書きすることを想定した言語ではないため、通例、ほかのプログラミング言語で記述してからコンパイルすることで生成します。 Wasm にコンパイルすることができるコンパイラはさまざまな言語向けに開発されていますが、とくによく実用されているものは、現状 C や C++ 、 Rust などの言語向けに限られるようです(理由は後述します)。コンパイラによって Wasm の生成ができたら、次はいよいよ Wasm の実行です。前述のとおり Wasm は「仮想マシンに対する命令セット」なので、 Wasm を直接解釈して演算する CPU などのハードウェアはありません注3 ~~。~~ これは、 x86 や Arm などにおいて定義される「機械語」と大きく異なる点です。実行できるのはあくまで **仮想** のマシンなのです。したがって、 Wasm を解釈して動作させるには、「 Wasm ランタイム」と呼ばれるプログラムを使用しなければなりません。 Wasm ランタイムには、お馴染み Google Chrome や Firefox 、 Safari などブラウザが組み込んだものに加え、 Wasmtime や WasmEdge といった、コマンドラインアプリケーションやサーバサイドなど、幅広い環境で動くことを想定した汎用的なものがあります。いずれも Wasm を解釈し、何らかの形で実行することができます。 

Wasm ランタイムや Wasm ランタイムを組み込んだアプリケーション(以降「 Wasm ホスト」と呼びます)は通常、 Wasm によるプログラムを「サンドボックス」と呼ばれる環境で実行します。ここでいう「サンドボックス」とは、 Wasm ホストが扱うメモリなどの領域から、 Wasm でできたプログラムのために切り離した、専用の領域を指します。 Wasm ホストのセキュリティホールといった例外を除いて、 Wasm によるプログラムは、 Wasm ホストが意図的に許可した形でしか、 Wasm ホスト側が管理している領域、すなわちサンドボックス (砂場)の外にアクセスできません。具体的には次のような情報を分離し、 Wasm によるプログラムがアクセスできる領域を制限します。 

- **・メモリ領域** 

Wasmによるプログラムは「線形メモリ」「スタック」「グローバル変数」「(関数などへの参照を格納する)テーブル」という 4種類の領域を管理します。これらの領域は、Wasmホストが管理するメモリの領域から独立していて、Wasmによるプログラムは、これら以外の領域を直接読み書きすることができません 

- **・呼び出せる関数** 

   - Wasmには組み込みで呼べる関数がまったくありません。そのため、Wasmによるプログラムは、Wasmホストから明示的にインポートした関数、あるいはWasmによるプログラム自身で定義した関数しか呼び出せません 

- **・ジャンプ先の命令** 

私たちがプログラミングの際、記述するif文やfor文などといった制御構文の一部は、機械語などにコンパイルする際「ジャンプ」と呼ばれる命令に置き換えられます注4 ~~。~~ 通常の機械語における「ジャンプ」は、CPUごとに異なる制約はあるものの、理論上プログラム内のどこにでも飛べます。一方、Wasmにおける「ジャンプ」は、brという命令に渡す静的な値(プログラムの実行によって書き換えられることがない値)で指定する場所にしか飛べません。この飛び先は、Wasmのプログラム内よりももっと狭い、 brを囲っているブロックの最後の位置までに限定されます 

Wasm では以上のような工夫を凝らすことで、ありふれた機械語と異なり、 Wasm のプログラムにスタックオーバーフローなどの脆弱性があったとしても、悪用されて Wasm ホストまで影響が及ぶ心配がありません。そうした脆弱性を利用してジャンプ先のアドレスを書き換える、といった攻撃手法を根本的に回避することができるのです。 

Wasm は、ブラウザにおける JavaScript のように、知らない誰かが書いたコードを不意にダウンロードして実行する、という条件においても安全に実行できるよう、上記のとおり厳格な制限の下で実行するよう定められています。 C 言語など、 JavaScript と違ってメモリを自由に操作できる言語を効率よくかつ安全に操作するには、これだけの配慮が必要だったのです。 

**WebAssemblyへの期待と現実** 

Wasm は、「 JavaScript より高速に動作し、今後 JavaScript を置き換えるものだ」という期待を当初から受けてきました。とくに「 JavaScript より高速に動作」という点については、盛んに宣伝され、そうした前提で Wasm を解説する記 ~~事~~ <u>注5</u> ~~も~~ あったほどでした。そうした想定には理論的に妥当な点もあったものの、多くの場合、実際に JavaScript 製アプリケーションを Wasm で置き換えて高速化するほどのものとはなりませんでした。顕著な例として、「 Zaplib 」という、 JavaScript から Rust による Wasm への段階的な移行を支援するライブラリのポストモーテム(事後検証)注6 ~~が~~ 挙げられます。このポストモーテムによると、試しに数人のユーザーに Zaplib を使ってもらったところ、実際に Rust で書き換えたほうが速かったアプリケーションは想定よりはるかに少なかったそうです。 

Wasm の公式サイトにおける FAQ で述べられているよう ~~に~~ <u>注7</u> ~~、~~ そもそも Wasm は JavaScript を置き換えるものではなく、あくまで「補完する」ことを目指しています。後述するようにその「補完する」ケースでは十分有益な実績を残していますし、速度の面で JavaScript 

に負けるケースが多かろうと、必ずしも問題ではないのかもしれません。とはいえ、この事実は Wasm に関心を持った人たちをいまだに失望させ続けています。そこで、安易に速度のために Wasm を採用して後悔する、なんてことにならないよう、おもにどういった要因があるのか紹介しましょう。 

#### **JavaScriptが十分に最適化される** 

JavaScript は、⻑年ブラウザ間ですさまじい開発競争が行われた結果、信じられないほど強力な最適化が施されるようになりました。 .js ファイルをダウンロードしてから最初に実行する際、変数やオブジェクトのプロパティがどのように使用されているかを解析して、うまくいったらより効率よく扱える型に固定する、などさまざまな手法で、 JavaScript が仕様上抱えるパフォーマンス上の問題に対し、巧みな策を講じてきました注8 ~~。~~ 

結果、特定のプログラムを Wasm で書いて JavaScript より高速に実行できるかどうかを見通すのは思いのほか難しい、というのが現状です。後述する、 JavaScript のほうがパフォーマンス上有利な点と総合した結果、 JavaScript のほうが Wasm より速くなることもまったく珍しくありません( **図** 1)。 



#### **ブラウザのAPI呼び出しのコスト** 

「 JavaScript のほうがパフォーマンス上有利な点」として最もよく挙げられるのが、ブラウザの API 呼び出しのコストです。現在ほとんどの Web ページ上で動作している JavaScript のコードは、ブラウザが提供する API を用いて DOM などを操作するのに、 CPU 時間の多くを費やします。これらの API は C++ からコンパイルした機械語(いわゆるネイティブコード)で実装されているうえ、もともと JavaScript から呼び出して JavaScript の値を操作する前提で設計されています。そのため、 Wasm からそうした API を呼ぶ場合、多かれ少なかれ実行にオーバーヘッドがかかります。引数や戻り値が、整数や浮動小数点数といったプリミティブな値のみであればそのコストは小さいのですが、文字列や配列といった複合的な値であった場合、やりとりの都度コピーしなければならないので、とくに時間がかかるようになります。 Wasm で行うことは、なるべくそうしたブラウザの API 呼び出しを伴わない、純粋な計算に収まるようにしましょう。これに関連して、ブラウザが提供する API と同じ機能を Wasm で再実装してしまうのも避けましょう。前述のとおり、ブラウザの API はもとより C++ をコンパイルしたネイティブコードで実装されているのに加え、各ブラウザベンダーの地道な努力によって最適化されています。呼び出しコストや Wasm を実行するコストが加わる時点で、かなうわけがないのです。しかも Wasm で実装したものを利用する場合、 Wasm のファイルをダウンロードしなければならない分、余計なコストがかかってしまいます。実際の例として、画像のリサイズをブラウザの API で行った場合と Wasm で行った場合とで比較した記事があります。 

<u>https://qiita.com/yokra9/items/f9e98a9b47fe2d1234b0</u> 

こちらの例では、 Wasm によって画像をリサイズした場合、 OpenCV.js を使ってよりうまくいった場合でも、ブラウザの API を直接使った場合より 2 倍前後の実行時間がかかる結果となりました。 

#### **ファイルサイズの問題** 

Wasm を真剣に使用するうえで(あるいは、 Web アプリケーション全般を真剣に作るうえで)考慮すべき事項として、転送するデータのサイズが挙げられます。 Wasm は、前身である asm.js が抱えていた、ファイルサイズが大きくなり過ぎるという問題を解決するために生まれましたが、残念ながら一般的な JavaScript のコードほど小さくできてはいません。よくある実行時間のベンチマークでは見落とされがちな点なのでご注意ください。いかに実行速度が速かろうと、アプリケーション初期のロードに時間がかかってしまうと、ユーザー体験に負の影響を与えることになります。言語によって多少事情は異なるものの、大きくなったサイズのうち多くを占めているのが、ほかのプログラミング言語から Wasm を作ったときに同梱される「ランタイムライブラリ」です。ランタイムライブラリには、我々がプログラミングするうえで日常的に使うデータ構造(配列とか、ハッシュマップとか、文字列とか)とそれを操作する関数に加え、ガベージコレクター( GC )といった、まさに「縁の下の力持ち」と呼ぶべき存在も含まれています。 C や C++ 、 Rust のような、ランタイムライブラリが小さい言語以外では、この問題がとくに顕著なものとなります。そのせいか、ランタイムライブラリが大きめのプログラミング言語の処理系では、 Wasm の採用がまだそれほど進んでいません。そのような状況を打破すべく、 Wasm 1.0 の仕様ができてからというもの、 Wasm の仕様策定に携わる人たちは、昨今の言語処理系が備えるランタイムライブラリにおいて比較的一般的な機能を、 Wasm のプログラムではなく Wasm ランタイム側が提供できるよう、新しい仕様を数多く策定しています。まだまだ発展途上で普及していないものがほとんどですが、具体的には次の仕様です。 

- **<u>注</u>** <u>9</u> 

- **・** G ~~C~~ 

   - Wasmのプログラム側ではなくWasmランタイム側がライフタイムを管理する、配列や構造体といった各種の参照型など 

- **・文字列** **~~型~~** **<u>注</u>** <u>10</u> 

参照型のうち、文字列型についてよりリッチなAPIを提供することで、JavaScriptの文字列との互換性を高めたり、ランタイムライブラリで実装する分量を減らしたりする試み 

- **・例外処** **~~理~~** **<u>注</u>** <u>11</u> 

try/catchなど、例外処理のための命令など。実は多くのブラウザで実装済み 

###### **・スタック切り替** **~~え~~** **<u>注</u>** <u>12</u> 

グリーンスレッドやasync/await、限定継続など、現代の並行プログラミングで欠かせないしくみの実装に必要なプリミティブ余談ですが、もちろん上記以外にも新たな仕様がたくさん議論されています。気になった方は WebAssembly の GitHub Organizatio ~~n~~ <u>注13</u> におけるリポジトリをのぞいてみてください! 

##### **WebAssemblyの事例** 

前節では、「 Wasm は JavaScript より高速で、 JavaScript を置き換えるものだ」という世間からの期待が、実際には実現しそうもないことを理由とともに解説しました。繰り返しになりますが、 Wasm 公式の FAQ で述べているように、そもそも Wasm は JavaScript を置き換えるものではなく、あくまで「補完する」ことを目指しています。そのためやはり、実際に Wasm が使われている現場では、その速度でもって JavaScript を置き換える、という趣旨で Wasm を採用していません。以降で示すのは、おもに Wasm のポータビリティや、セキュリティに注目して採用した事例です。 

#### **SQLite** 

Wasm のポータビリティを活かして既存のアプリケーションをブラウザ上で動くようにした事例は、すでに数多くあります。そのうち SQLite は、 Google Chrome の開発チームが、 Web SQL Databas ~~e~~ <u>注14</u> ~~を~~ 廃止する代わりの移行先として、 File System Access AP ~~I~~ <u>注15</u> ~~と~~ Wasm にコンパイルした SQLite を組み合わせて利用することを推奨し、話題になりました注16 ~~。~~ 

#### **WebContainer** 

WebContaine ~~r~~ <u>注17</u> ~~は~~ 、ブラウザ上で動く IDE を提供している StackBlit ~~z~~ <u>注18</u> ~~が~~ 開発した、 Node.js やよく使われるコマンドを、すべてブラウザ上で動かすための技術です。フロントエンド開発者の間ではお馴染みの、 Vite や Webpack などの Node.js 製ツールに加え、 Node.js による Web サーバも動くことが確認されています。従来 StackBlitz は、 Node.js を始めとする開発に必要なアプリケーションを、クラウド上の仮想マシンで実行してきました。しかしその方法では遅延が大きい、オフラインでは使用できない、仮想マシンのコストがユーザーの負担となる、といった問題がありました。 WebContainer を使って多くのコマンドをクライアントサイドで実行できるようにした結果、これらの問題を一気に解決できたのです。 

<u>https://blog.stackblitz.com/posts/webcontainer-api-is-here/</u> 

#### **Figma** 

UI デザインツール、 Figma では、もともと C++ で実装したデスクトップアプリケーションを Wasm の前身、 asm.js にコンパイルすることによってブラウザ上で動かしていました。それを主要なブラウザが Wasm をサポートするとともに Wasm に移植したところ、初期のロード時間を 1/3 以下に抑えることに成功しました。 Wasm が asm.js の問題を乗り越えるために生まれたという背景を考えると、これはまさに設計者冥利に尽きる成果でしょう。 

<u>https://www.figma.com/ja/blog/webassembly-cut-figmas-loadtime-by-3x/</u> 

Figma にはもう 1 つ Wasm を採用している事例があります。 Figma のプラグイン機構です。もともと Figma では、 ShadowReal ~~m~~ <u>注19</u> ~~と~~ いう策定中の仕様の shim (各ブラウザが実装する前に JavaScript で再現したもの)を利用し、 JavaScript を分離した環境で実行することでプラグイン機構を提供してきました。しかし、この利用している shim に脆弱性が見つかったことをきっかけに、 Figma では代わりに QuickJS <u>注20</u> ~~と~~ いう C 言語で書かれた JavaScript 処理系を Wasm にコンパイルして採用することにしました。 

<u>https://www.figma.com/blog/an-update-on-plugin-security/</u> 

#### **ブラウザ以外でWasmを動かす事例** 

Wasm の「(ブラウザにおける JavaScript のように)知らない誰かが書いたコードを不意にダウンロードして実行できる」くらいのセキュリティの高さと、特定のハードウェアやプログラミング言語への依存を減らすことで得たポータビリティの高さを活かして、ブラウザ以外の環境、とりわけサーバサイドのアプリケーションとして Wasm を動かそう、という取り組みが盛んに行われています。最近発表された中では、 Docker の Wasm サポートの反響が大きいでしょう。 

<u>https://www.docker.com/blog/docker-wasm-technical-preview/</u> 

Docker は、 WasmEdge というランタイムを利用し、コンテナの代わりに Wasm を実行するしくみを提供するようになりました。執筆時点でこの機能はまだ Beta 版ですが、従来のコンテナよりサイズが小さく、より本質的な、アプリケーションのコードにしぼった仮想化ができると期待されます。そのほか、お馴染み Kubernetes 上で Wasm を動かす試みもいくつかあります。ここでは wasmCloud や、 wasmCloud と Kubernetes を組み合わせて利用した、 Adobe 社の事例を紹介しましょう。 

<u>https://www.cncf.io/blog/2022/11/17/better-together-a-kubernetesand-wasm-case-study/</u> 

Adobe は、 wasmClou ~~d~~ <u>注21</u> ~~と~~ いう、 Wasm によるアプリケーションを分散環境の中で管理するプラットフォームを活用して、画像から背景を取り除く Rust 製の関数を wasmCloud 上で動作させました。結果、関数はサーバレス環境のごとくリクエストに応じて必要なときだけ立ち上がり、なおかつ起動時間も短いので、リソースを極めて効率よく使えるものとなりました。それから、 Adobe における既存の Kubernetes クラスタと wasmCloud を連携させることで、 Wasm によるアプリケーションを Kubernetes クラスタ上で稼働させる例も上記サイトの記事で紹介されています。 wasmCloud はあくまで Wasm 製のアプリケーションを管理するシステムなので、すでにある Kubernetes クラスタと統合するしくみは今後サーバサイドで Wasm を普及させるうえで必須となるでしょう。 



#### **Shopify Functions** 

e コマースプラットフォーム Shopify では、外部の開発者が独自の拡張を Shopify に加える方法として、比較的⻑く Wasm に着目してきました。従来より、多くの Web サービスでは REST や GraphQL などによる Web API が提供されており、当然 Shopify でも以前から提供していましたが、 Web API で扱えるのは Shopify に保存されたデータのみでした。一方、 Shopify を利用する各ストアでは、 Shopify 本体では抱えきれないほど、 Shopify のバックエンドを拡張する多様なニーズがありました。ディスカウントや配送、支払い方法について、各ストアで独自のロジックを加えたいのです。これまでは「 Shopify Scripts 」というしくみでそうした需要をカバーしてきましたが、拡張に使えるのは独自に簡略化した Ruby のみであったりと、非常に限られたものでした。しかし、「 Shopify Functions 」を使えば、各ストアの開発者が Wasm によって Shopify のバックエンドのロジックを拡張できるので、いろいろなプログラミング言語が使えます。しかも、「 Shopify Functions 」で開発した Wasm モジュールは「 Shopify アプリストア」で販売することまでできるようになりました。ブラウザ以外における Wasm の利用は、このような、アプリケーションのプラグイン機構としての採用が多く見られます。 

<u>https://www.shopify.com/jp/blog/partner-shopify-functions</u> 



#### **Helpfeel社** 

昨年開催された JSConf JP 2022 において、発表された事例です。 Helpfeel 社は自社の FAQ システム「 Helpfeel 」において、 FAQ の記事を記述するのに用いる独自の構文「 Helpfeel 記法」を、前処理で用いる Python 製のバッチ、 Node.js 製のアプリケーションサーバ、ブラウザ、それぞれでパースする必要がありました。そこで、 Helpfeel では Rust で Helpfeel 記法のパーサーを実装して Wasm にコンパイルし、これら 3 つの実行環境すべてで同じパーサーが動作するようにしました。 

<u>https://www.youtube.com/watch?v=t-I0ts4TTok</u> 



#### **WASM-4** 

WASM- ~~4~~ <u>注22</u> ~~は~~ 、ゲームボーイのような小さなゲームを Wasm で作成するためのゲームエンジンです。ブラウザで遊ぶことももちろんできますが、ブラウザ以外にも、 Wasm が動作して、画面や入力用のハードウェアが動くあらゆる機器で動かせるよう、 RAM の消費量はわずか 64KB 、画面の解像度は 160 ピクセル四方、使える色は 4 色まで抑えてあります( **図** 2)。 



- 注2) 「キャズム」や「〜イズム」などからの類推なのか、「ワズム」と発音するようです。 

   - (本文に戻る) 

- 注3) Wasmを直接解釈するハードウェアを作ろうという試み(たとえば、 



   - <u>https://github.com/lastmjs/wasm-metal)はありますが、少なくとも仕様上想定さ</u> れているわけではないので、ここでは「ありません」と断言しています。 (本文に戻る) 

- 注4) 具体的な命令の名前は機械語の種類によって異なりますが、通例「ジャンプ」で通じます。 

(本文に戻る) 注5) たとえば次のURLの記事。 <u>https://postd.cc/what-makes-webassembly-fast/</u> (本文に戻る) 注8) 詳しくは、たとえば次のスライドがわかりやすいです。 <u>https://tugawa.github.io/files/jssst-ss-2021.pdf</u> (本文に戻る) 

# **第** **_2_ 章** 

### **動かしてわかるWebAssembly** 

##### **ゼロからはじめるWebAssembly Text Format** 

福岡秀一郎(ふくおかしゅういちろう) <u>@__syumai syumai</u> 





**第2章では、WebAssemblyのテキスト形式である「WebAssembly Text Format」 (WAT)を取り上げ、基本的な文法と動かし方を解説します。WATはWebAssemblyを利用するうえで、文字情報として読み書きしたい際には必須です。簡単なプログラムを作成、実行しながら、WATの理解を深めていきましょう。** 

WebAssembly のバイナリは、おもに、 C 言語や Rust などのプログラミング言語によって記述されたプログラムから変換することで生成します。ブラウザ上で動作するプログラミング言語と言えば JavaScript がありますが、 JavaScript のコードが文字情報として簡単に読み書きできるのに対し、 WebAssembly のバイナリについては、 JavaScript のコードと同じ感覚で直接読み書きすることはできません。しかしながら、 WebAssembly にはテキスト形式の「 WebAssembly Text Format 」(頭文字を取って、 WAT ともいいます)が用意されており、バイナリ形式と相互に変換できるので、文字情報として読み書きしたいときはこれを使います。たとえば、 WAT は、次のような用途で利用できます。 

- ・ WebAssemblyコンパイラによって生成されたバイナリのデバッグ 

- ・自作コンパイラの動作検証本章では、この WAT の基本的な文法と、動かし方についての解説を行います。本章の執筆にあたって、 WebAssembly の仕 ~~様~~ <u>注1</u> ~~だ~~ けでなく、 MDN の WebAssembly のドキュメン ~~ト~~ <u>注2</u> ~~と~~ 、 Rick Battagline さんの “The Art of WebAssembly ~~”~~ <u>注3</u> ~~を~~ 参考にしました。たいへん参考になる情報をありがとうございました。 

##### **WATを動かす** 

いきなりではありますが、実際に動いたり、動かなかったりすることを確認しながら進むことで、学習効率が上がると考えられますので、さっそく WAT の動作環境を整えましょう。 

#### **必要なツールの導入** 

今回使うのは、次の 2 つのツールです。 

- ・ Node.js v18.15.0(執筆時点で最新のLTS) 

- ・ wabt v1.0.32(The WebAssembly Binary Toolkit) 

Node.js は、すでにお使いの方も多いと思います。もし、まだ導入していなければ、公式サイト注4 ~~よ~~ り入手できるインストーラか、お使いのパッケージマネージャにてインストールしてください。 

wabt は、 WAT を WebAssembly バイナリに変換する “wat2wasm” 、その逆の変換を行う “wasm2wat” などを始めとする、 WebAssembly のバイナリを操作するのに使用できるツールをまとめたツールキットです。入手については、 GitHub 上注5 ~~で~~ 、 Windows 、 macOS 、 Linux 向けのビルド済みの実行可能ファイルが配布されているので、ここからダウンロードしたものを使用できます。ビルド済みのファイルは、 tar.gz 形式で配布されているので、これを展開したうえで、展開先ディレクトリの bin ディレクトリ内に格納されているコマンドを呼び出す形で使いましょう注6 ~~。~~ パッケージマネージャを使用したい場合は、 Mac では Homebrew を使い、 **brew install wabt** コマンドを実行することで、 Ubuntu などの Debian 系のディストリビューションでは、 **apt install wabt** コマンドを実行することで簡単に導入できます。自分でビルドを行う場合は、 GitHub 上の READM ~~E~~ <u>注7</u> ~~に~~ 記載の内容に従ってください。 

#### **初めて動かすWAT** 

WebAssembly を実行するには、実行対象となる WebAssembly のバイナリと、それを実行するための JavaScript のコードが必要で ~~す~~ <u>注8</u> ~~。~~ ここでは、次の手順で実行の準備を進めます。 

- 1 WATファイルを準備する 

- 2 WATファイルをWebAssemblyバイナリに変換する 

- 3 Node.jsでWebAssemblyバイナリを動かすためのJavaScriptコードを準備する 

- 4 実行する上記の作業を行うにあたって、事前にファイルを保存するためのディレクトリを作成し、ターミナルエミュレータの作業ディレクトリをそのディレクトリへ変更しておきましょう。 



#### 1 **WATファイルを準備する** 

ここで準備する WAT ファイルの内容が、初めて動かす WebAssembly バイナリの内容となります。一般的に、新しいプログラミング言語に入門したときに初めて実行するコードは、 “Hello, world!” という文字列を標準出力に出力するものとなります。しかし、 WebAssembly でこの出力を行うには、やや複雑な操作が必要になってしまうため、今回は、数値を 2 つ受け取って加算するだけの、単純な関数を使用します。 

add.wat ファイルを作成し、そのファイルの内容を **リスト** 1のとおりに書き換え、保存してください。 WAT の記法は、 LISP などのプログラミング言語でよく使われる S 式の形式で定義されていますが、この文法については追って解説を行いますので、まずはそのまま入力しましょう。 

###### **▼リスト1 数値を2つ受け取って加算するadd関数(add.wat)** 

**(module (func (export "add") (param i32) (param i32) (result i32) (i32.add (local.get 0) (local.get 1)) ) )** 

#### 2 **WATファイルをWebAssemblyバイナリに変換する** 

wabt の wat2wasm コマンドを使って、 WAT を WebAssembly バイナリに変換します。ここでは、環境変数で wabt のコマンドへのパスが設定されていることを前提に進めますが、もし設定していない場合は、コマンドへの絶対パスか、相対パスに読み替えて実行してください。 **図** 1のコマンドを実行すると、 add.wasm ファイルが出力されます。 

###### **▼図1 WATファイルをWebAssemblyバイナリに変換するコマンド** 

**$ wat2wasm add.wat** 

#### 3 **Node.jsでWebAssemblyバイナリを動かすためのJavaScriptコードを準備する** 

Node.js では、 WebAssembly AP ~~I~~ <u>注9</u> ~~を~~ 使用できます。 WebAssembly API は、 WebAssembly JavaScript Interfac ~~e~~ <u>注10</u> ~~と~~ して W3C で標準化されているので、 Node.js 上で WebAssembly バイナリを動かすために学んだ知識を、そのままブラウザ向けの開発で活かすことができます。 WebAssembly バイナリを JavaScript で動かすには、バイナリを読み取った結果をインスタンス化する必要があります。 WebAssembly API には、この目的のために使用できる WebAssembly.instantiate 関数があります。外部 URL から取得した WebAssembly バイナリをインスタンス化する場合は、 HTTP Response をストリームしながらインスタンス化を行う WebAssembly.instantiateStreaming 関数もあり、効率的に処理を行えるためこちらが推奨されますが、今回は、ローカルのファイルを読み取ってインスタンス化するので、 

WebAssembly.instantiate 関数を利用するのが適切です。 

WebAssembly.instantiate 関数は、第 1 引数にインスタンス化対象の WebAssembly バイナリのデータを受け取ります。第 2 引数には、 WebAssembly インスタンスに JavaScript 側から渡したい要素を設定できる importObject を受け取ります。 add 関数の実行には、 JavaScript 側から渡す必要のある要素がとくに存在しないので、今回は第 2 引数を省略します。それでは、 **リスト** 2のコードを、 add.mjs というファイル名で保存しましょう。 

###### **▼リスト2 WebAssemblyの実行を行うJavaScriptのコード(add.mjs)** 

**import fs from "node:fs/promises"; const file = await fs.readFile("add.wasm");** 

**const result = await WebAssembly.instantiate(file); console.log(result.instance.exports.add(1, 2));** 

**リスト** 2のコードでは、2のステップで生成した add.wasm ファイルを読み取り、そのインスタンス化の結果を result 変数に代入しています。 result 変数は、 WebAssembly バイナリから生成されたインスタンスを instance プロパティに持ち、このインスタンスは exports プロパティに、インスタンス内から外部に公開する値を持ちます。ここには、 WebAssembly のモジュール内からエクスポートされた関数などが含まれます。 add.wat ファイルでエクスポートした関数に付けた名前は add だったので、この関数は、 result.instance.exports.add と参照することで取得できます。取得した add 関数に対して 1 、 2 という数値を渡して呼び出し、その結果を console.log に出力してこのコードは終了しています。 



#### **4実行する** 

では、最後に3のステップで保存した add.mjs を実行してみましょう( **図** 2)。 

###### **▼図2 WebAssemblyの実行を行うコマンドと実行結果** 

**$ node add.mjs 3** 

想定どおり、 1 、 2 という数字を足し合わせた結果の 3 が出力されることが確認できました。ここまでの手順で、 WAT を WebAssembly バイナリに変換して、 Node.js で実際に動かす一通りの手順を確認できました。この先の内容を読み進めるにあたって、適宜、こちらのサンプルを書き換えながら動かしてみてください。 

**WAT / WebAssemblyの基礎** 

ここからは、 WAT の文法および、その説明に必要となる WebAssembly の基本的な概念についての説明を行います。 

#### **S式(S-expressions)** 

WAT は、本章の冒頭で述べたとおり、 S 式によって書き表されます。 S 式は、プログラムをネストされた木構造による式として表現するデータ形式です。 

WebAssembly の S 式の各ノードは、 **()** によってくくられたトークンの列で表現されます。 **()** 内に初めて現れるトークンが、そのノードの種類を表します。たとえば、先ほどの “add” 関数の例で現れた **(module ...** という表現の中で現れている **module** は、このノードがモジュールを表すことを示しており、その中に書かれた **(func ...** の部分は、モジュールに対する関数定義を表しています。ここで注意すべきなのは、同じ **(func ...** という表現であっても、どのノードを親に持つかによって意味が変わる点です。 **リスト** 3中の、モジュールを親に持つ func ノードは関数定義を表しますが、 export ノードを親に持つ func ノードは、インスタンスからエクスポートする対象の関数を示します。 

###### **▼リスト3 WATによってWebAssemblyモジュールを表現するS式の例** 

**(module (func $f (result i32) i32.const 42)** **<mark>;; 42という数値を返す関数$fを定義する</mark> (export “example” (func $f)** **<mark>;; 関数$fを“example”としてexportすることを示す</mark> ) )** 

#### **値(Values)** 

WebAssembly は、値の型として数値型( Numeric types )、ベクトル型( Vector types )、参照型( Reference types )の 3 つを提供します。数値型は 4 種類で、 32 ビット/ 64 ビットの整数、浮動小数点数 ( IEEE 754 )をそれぞれ表す、 i32 、 i64 、 f32 、 f64 です。整数型 ( i32 、 i64 )の符号は区別されないため、使用する命令によって区別を行います。たとえば、 i32.div_s ( 32 ビット整数の符号付き除算)と i32.div_u ( 32 ビット整数の符号なし除算)は区別されます。 

#### **モジュール(Modules)** 

WebAssembly のプログラムはモジュール( modules )にまとめられ、これが、デプロイ/ロード/コンパイルの単位となります。そのため、 WAT によって表現された WebAssembly プログラムは、モジュールをルートノードとする 1 つの S 式として記述されます。 **リスト** 4の例は、 WAT で表現した、最もシンプルな WebAssembly モジュールです。 

**▼リスト4 最もシンプルなWebAssemblyモジュール** 

**(module)** 

モジュールは、 WebAssembly プログラムの動作に使われる、関数 ( functions )、型( types )、メモリ( memories )、テーブル ( tables )、グローバル変数( globals )の定義をまとめます。それに加えて、必要であれば、インポート/エクスポートの宣言、データセグメント( data )・要素セグメント( elements )によるメモリ/テーブルの初期化、開始関数( start func )の指定ができます。 **表** 1は、モジュールの構成要素の簡単な説明をまとめたものです。メモリ、テーブル、グローバル変数は、インポートまたはエクスポートした場合に JavaScript 側から直接操作することが可能で、グローバル変数は、複数のモジュールで共有することも可能です。上記の内容は、 WebAssembly バイナリ形式のモジュールレコードの仕様ですが、 WAT にも対応する文法がそれぞれ定義されています ( **リスト** 5)。 

###### **▼リスト5 WebAssemblyモジュールの複数の構成要素を含んだWATの例** 

**(module (memory 1)** **<mark>;; メモリ定義</mark> (global $g (mut i32) (i32.const 100))** **<mark>;; グローバル変数定義、初期化</mark> (func $f (result i32) global.get $g)** **<mark>;; 関数定義</mark> (export "example" (func $f))** **<mark>;; エクスポート宣言</mark> )** 

補足として、 WAT では一番外側の丸括弧と、 **module** の記載を省略することも可能です。本章でも一部この省略表記を使用しています。 **リスト** 6は、 **リスト** 5に省略記法を適用した例です。 

###### **▼リスト6 リスト5から(module ... )の記載を省略したWATの例** 

**(memory 1)** **<mark>;; メモリ定義</mark>** 

**(global $g (mut i32) (i32.const 100))** **<mark>;; グローバル変数定義、初期化</mark>** 

**(func $f (result i32) global.get $g)** **<mark>;; 関数定義</mark> (export "example" (func $f))** **<mark>;; エクスポート宣言</mark>** 

#### **コメント(Comments)** 

すでに例の中で使用していますが、 WAT では、行コメントとして **;;** という記法が、ブロックコメントとして **(; ... ;)** という記法が利用可能です。 

##### **WATの関数定義** 

先ほど、モジュールの説明に記載したとおり、関数定義はモジュールの直下で、型情報や、ローカル変数の宣言、実行する命令の列を指定する形で行います。 **リスト** 7に、これらの要素をすべて含んだ関数定義を示します(ローカル変数に値を保存して、再度読み込む部分が冗⻑ですが、ご容赦ください)。 

###### **▼リスト7 WATの関数定義の例** 

**(func** **<mark>;; 型情報(パラメータ、結果の型の宣言)</mark> (param $a i32) (param $b i32) (result i32)** **<mark>;; ローカル変数の宣言</mark> (local $sum i32)** **<mark>;; 実行する命令の列</mark> (i32.add (local.get $a) (local.get $b)) local.set $sum local.get $sum )** 

#### **スタックマシン** 

関数の詳細な説明に入る前に、 WebAssembly の計算モデルが、スタックマシンをベースにしていることに触れておきましょう。スタックマシンは、メモリがスタック型のデータ構造として表される計算モデルで、命令の処理対象の値や、処理結果の値をスタック上にポップ、あるいはプッシュします。 WebAssembly の関数のボディは、順番に実行される命令の列で構成されています。これらの命令が処理する値は、スタックトップから取得され、演算の結果が再びスタックトップにプッシュされます。たとえば、 i32.add 命令は、スタックトップの 2 つの値を取り出して加算します。加算した結果は、再びスタックトップに積まれ、後に続く別の命令によって使用されます。具体的な例を **リスト** 8に示します。 

###### **▼リスト8 WATの関数によるスタックの使用方法の解説** 

**(func (result i32) i32.const 1** **<mark>;; スタックトップにi32型の1という数値をプッシュする</mark> i32.const 2** **<mark>;; スタックトップにi32型の2という数値をプッシュする ;; スタックトップに積まれた2という数値と1という数値を取り出し、加算する ;; 加算した結果の3という数値をスタックトップにプッシュする</mark> i32.add )** **<mark>;; スタックトップに3という数値が積まれた状態で関数が戻ったので、戻り値は3</mark>** 

WebAssembly の命令を追うときは、スタックにどのような値が積まれた状態になっているかを意識しながら読み進めましょう。 

#### **関数のエクスポート** 

ここから先の例を、実際に Node.js で動かしながら試せるように、事前に、関数のエクスポート方法について触れておきます。モジュールの構成要素である、関数、メモリ、テーブル、グローバル変数はエクスポートすることが可能です。たとえば、関数のエクスポートは、 **リスト** 9のように行います。 

###### **▼リスト9 index指定による関数のエクスポートの例** 

**(module** **<mark>;; 関数を定義する</mark> (func (result i32) i32.const 42)** **<mark>;; indexが0の関数を、“example”という名前でエクスポートする</mark> (export "example" (func 0)))** 

エクスポートされた関数へのアクセス方法は、本章の “WAT を動かす ” で述べたとおりです。エクスポート対象の関数を識別するために、 ID を割り当てることもできます( **リスト** 10)。 

###### **▼リスト10 ID指定による関数のエクスポートの例** 

**(module** **<mark>;; 関数$fを定義する</mark> (func $f (result i32) i32.const 42)** **<mark>;; 関数$fを、“example”という名前でエクスポートする</mark> (export "example" (func $f)))** 

また、省略記法として、関数定義の中にエクスポートの指定をインラインで書くことも可能です( **リスト** 11)。 

###### **▼リスト11 インラインでの指定による関数のエクスポートの例** 

**(module** **<mark>;; 関数を定義し、“example”という名前でエクスポートする</mark> (func (export "example") (result i32) i32.const 42))** 

いずれの方法を使っても結果は同じですので、自分に合った方法を選んで使いましょう。 

#### **関数の型情報** 

ここから、関数の詳細な説明に入っていきます。関数の型情報は、パラメータ( param )と、結果( result )を順番に並べることで宣言します。パラメータは、 **(param i32)** といった形式で、 param ノードにそのパラメータの型を与えることで宣言します。パラメータは 0 個以上の任意の個数を宣言でき、 **(param $a i32)** のように、後から参照しやすくするための ID を割り当てることができます。パラメータに対して設定された値の取得は、 local.get 命令によって行いますが、 ID を割り当てない場合は、 **リスト** 12のように、 index を使って取得する形となります。 

###### **▼リスト12 関数のパラメータ宣言の例** 

**(func (param i32) (param i32) (result i32) (i32.add (local.get 0) (local.get 1)) )** 

ID を割り当てない、無名のパラメータには、 param に続けて型名だけを並べて宣言する省略記法があります。 **リスト** 13の **(param i32 i32)** は、 **(param i32) (param i32)** と 2 つ並べて書いたものと同じ意味になります。 

###### **▼リスト13 関数のパラメータ宣言の省略記法の例** 

**(func (param i32 i32) (result i32)** 

**(i32.add (local.get 0) (local.get 1)) )** 

関数の結果として返す値の型と個数は、 result ノードによって宣言します。一般的には 0 個、または 1 個の結果を宣言しますが、 WebAssembly の関数は多値返却が可能なため、パラメータ同様、 0 個以上の任意の個数を宣言できます注11 ~~。~~ 省略記法についてもパラメータと同様のルールがあり、 result に続けて型名だけを並べて宣言できます。関数の結果としては、関数が戻り値を返すとき、スタック上に存在する値が結果の個数分返されます。 **リスト** 14は、受け取った 2 つのパラメータの順序を入れ替えて返す、 i32 型の結果を 2 つ持つ関数の例です。 

###### **▼リスト14 関数の結果の宣言の省略記法の例** 

**(func (param i32 i32) (result i32 i32) local.get 1 local.get 0 )** 

#### **関数のローカル変数宣言** 

関数のローカル変数は、型情報の後に続けて宣言します。宣言は、 **(local i32)** といった形で行います。パラメータ同様、ローカル変数は ID を割り当てることができ、 ID を割り当てる場合は **(local $x i32)** といった形で宣言します。 ID を割り当てない場合は、 index を使って参照されます。ローカル変数の index は、関数のパラメータと、ローカル変数の通し番号となります。 **リスト** 15は、 WAT の関数定義の冒頭で紹介した例( **リスト** 7)の、 ID 割り当てをすべて取り除いたものです。 

###### **▼リスト15 index指定によるローカル変数の使用例** 

**(func (param i32 i32) (result i32) (local i32)** **<mark>;; パラメータのindexは0、1</mark> (i32.add (local.get 0) (local.get 1))** **<mark>;; ローカル変数のindexは2</mark> local.set 2 local.get 2 )** 

#### **関数のボディ** 

関数のボディは、これまで見てきたとおり、 WebAssembly の命令の列を並べたものです。スタックマシンの説明で示した例がわかりやすいので、こちらを再度示します( **リスト** 16)。 

###### **▼リスト16 関数のボディの基本的な形態の例(再掲)** 

**(func (result i32) i32.const 1** **<mark>;; スタックトップにi32型の1という数値をプッシュする</mark> i32.const 2** **<mark>;; スタックトップにi32型の2という数値をプッシュする</mark> i32.add** **<mark>;; スタックトップに積まれた2つの値を加算してプッシュする</mark> )** 

関数のボディの記法には、通常の記法と別に折りたたみ式命令 ( Folded Instructions )という記法が存在します。折りたたみ式命令では、 i32.add などといった命令と、それに対して与える引数を **()** でまとめた S 式として表記できます。 **リスト** 16を、折りたたみ式命令に書き換えたものを **リスト** 17に示します。 

###### **▼リスト17 関数のボディにおける折りたたみ式命令の使用例** 

**(func (result i32) (i32.add (i32.const 1) (i32.const 2)) )** 

これは個人の主観ですが、折りたたみ式命令のほうが読みやすく、かつコンパクトに記述できることが多いので、本章ではこちらの形式をおもに使用しています。 

#### **関数で使用できる命令** 

WAT の関数で使用できる命令には、これまで示してきたような値の操作を行う種類の命令と、プログラムの実行フローを制御する制御命令( Control Instructions )があります。値の操作を行う種類の命令としては、たとえば i32 型には四則演算を行う、 i32.add (和)、 i32.sub (差)、 i32.mul (積)、 i32.div_s / i32.div_u (商)、 i32.rem_s / i32.rem_u (剰余)などが定義されています。数値同士の比較を行う、 i32.eq (等しい)、 i32.ne (不等)、 i32.lt_s / i32.lt_u (以下)などもあります。こうした計算に使われる命令が、数値型ごとに定義されています。制御命令には、条件によって制御を行う if 、 loop であったり、処理のまとまりを表現する block 、関数を終了する return 、別の関数を呼び出す call 、テーブルに保持された関数への参照を使って呼び出しを行う call_indirect などがあります。高度なプログラムは、こうした制御命令を組み合わせることによって表現されます。最後に、 WAT の制御構造を利用して実装した FizzBuzz の例を挙げます( **リスト** 18)。 

###### **▼リスト18 WATによるFizzBuzz関数の例** 

**(func (export "fizzbuzz") (param $n i32) (result i32) (if (i32.eq (i32.rem_s (local.get $n) (i32.const 15)) (i32.const 0)) (then (return (i32.const -3))))** **<mark>;; 15で割り切れるなら、FizzBuzzを表す-3を返す</mark> (if (i32.eq (i32.rem_s (local.get $n) (i32.const 3)) (i32.const 0)) (then (return (i32.const -1))))** **<mark>;; 3で割り切れるなら、Fizzを表す-1を返す</mark> (if (i32.eq (i32.rem_s (local.get $n) (i32.const 5)) (i32.const 0)) (then (return (i32.const -2))))** **<mark>;; 5で割り切れるなら、Buzzを表す-2を返す</mark> local.get $n)** **<mark>;; それ以外の場合は、入力された数値をそのまま返す</mark>** 

ここでは、 WAT 内から直接文字列を出力するという形式は採らず、 Fizz 、 Buzz 、 FizzBuzz にそれぞれ対応する数値を負の数で返すようにしています。 

JavaScript 側では、 WebAssembly から返ってきた値をもとに switch を行い、その結果を出力しています( **リスト** 19)。なお、ここでの fizzbuzz.wasm は **リスト** 18を wat2wasm コマンドで WebAssembly バイナリに変換したものです。上記のコードを実際に実行すると期待どおりに Fizz 、 Buzz 、 FizzBuzz と出力されることが確認できると思います。もちろんですが、より簡潔な実装も可能と思いますので、ぜひご自身で試してみてください。 

**▼リスト19 WATによるFizzBuzz関数を呼び出すJavaScriptのコードの例** 

**import fs from "node:fs/promises"; const file = await fs.readFile("fizzbuzz.wasm"); const result = await WebAssembly.instantiate(file); const { fizzbuzz } = result.instance.exports; for (let i = 1; i <= 20; i++) { const v = fizzbuzz(i); switch (v) { case -1: console.log("Fizz"); break; case -2: console.log("Buzz"); break; case -3: console.log("FizzBuzz"); break; default: console.log(v); break; } }** 

###### ◆ ◆ ◆ 

本章では、 WAT および、 WAT を理解するのに必要な WebAssembly の基礎と、 WAT から変換した WebAssembly バイナリを動かす最低限の環境の準備方法について解説を行いました。より踏み込んだ内容について知りたい方は、冒頭で紹介した MDN の WebAssembly のドキュメントや、 Rick Battagline さんの “The Art of WebAssembly” をぜひ読んでみてください。 

- 注6) 環境により方法が異なりますが、こちらのbinディレクトリにパスを通すと使いやすいでしょう。 

(本文に戻る) 

- 注8) WASI(WebAssembly System Interface)の形式に対応したバイナリであれば、 Wasmtime( <u>https://wasmtime.dev/)やWasmer(</u> 

   - <u>https://wasmer.io/)といったWebAssemblyランタイムで直接実行することもでき</u> ます。 

   - (本文に戻る) 

- 注11) JavaScriptからWebAssemblyの関数を呼び出したとき、結果を1つだけ持つ関数の結果はその関数の結果の値がそのまま返されますが、結果が複数存在する場合は配列が返されます。 

   - (本文に戻る) 

# **第** **_3_ 章** 

### **挑戦! アプリケーションの WebAssembly化** 

##### **EmscriptenによるC++アプリケーション出力の実践** 



こーのいけ <u>@ko_noike</u> 

**本章では実際のWebアプリケーションにWebAssemblyを組み込んでいく方法について実践していきます。WebAssembly化の実践例としてはC++/Emscriptenを使用したプログラムをWebAssemblyにして組み込む方法について説明します。** 

##### **WebAssembly化の利用シナリオ** 

Web アプリケーションを何でもかんでも WebAssembly 化するのはオススメできません。次のような WebAssembly の特徴が活かせるシチュエーションでこそ使うべきです。 

1. アプリケーションの性質上とくにパフォーマンスが求められるとき 

2. 既存のライブラリなどのコード資産が活かせるとき 

3. WebAssemblyのセキュリティサンドボックスに期待したいとき 

1. のようにパフォーマンスが求められるシチュエーションとしては、映像・音声などのストリームを扱うときや、 3D モデリングなどを行うときが考えられます。 2. の既存のライブラリ資産としては FFmpeg のような映像関係や、マークダウン形式のパース、 SVG や画像へのレンダリングなどが考えられます。 3. としてはユーザーにちょっとした改変を許可しつつ、悪さができないようにしたいケースで、 JavaScript エンジンを WebAssembly で動かして、ユーザーのソースコードをそこで実行するというようなケースです。 

##### **WebAssemblyのプロダクト事例** 

WebAssembly の採用事例として最も有名なものは Google Earc ~~h~~ <u>注1</u> ~~と~~ Google Mee ~~t~~ <u>注2</u> ~~で~~ しょう。 Meet の背景ぼかしや仮想背景などの背景加工処理の事例が有名ですが、現在では音声フィルタにも別の WebAssembly を使っています。 WebRTC によるビデオ通話と WebAssembly は相性が良いのでほかにも株式会社スタジアムによるインタビューメーカー注3 ~~と~~ いうサービスの仮想背景機能や、株式会社時雨堂の提供する WebRTC SFU パッケージ Sora の周辺ツール ~~群~~ <u>注4</u> などでも WebAssembly が利用されています。また、 WebRTC 以外で見ていくと、デザインツールの Figm ~~a~~ <u>注5</u> ~~の~~ UI では全面的に利用されていますし、 Microsoft の Clipcham ~~p~~ <u>注6</u> ~~で~~ は、映像編集に FFmpeg を利用した WebAssembly が使われています。 FRAIM 株式会社のクラウドドキュメントサービス注7 ~~や~~ 、株式会社 HIKKY の VR サービ ~~ス~~ <u>注8</u> ~~な~~ どでも使われているようです。 

##### **WebAssembly出力で知っておくべきこと** 

多くの場合、 JavaScript で記述したのと同じことを WebAssembly 化すると、 WebAssembly の .wasm ファイルは JavaScript のサイズに比べて大きくなってしまいます。数キロバイトが数メガバイトになるほどのインパクトがあります。これが最初に何でもかんでも WebAssembly 化することをオススメできないとした理由です。言語やツールキットによってはビルドしたときに .wasm ファイルだけでなく、グルーコードとなる .js ファイルを生成することがあります。後述する Emscripte ~~n~~ <u>注9</u> ~~で~~ は比較的大きなグルーコードを生成します。これは Emscripten でさまざまな機能を実現するためで、単純な機能のときには必要ないのですが、多くのことを実現しようとすると必ず必要になってきます。このような事情を勘案しないで、「 Hello World がこんなに大容量になってしまう!」という安直なブログ記事が多いことに Emscripten の開発チームは辟易しています。 

#### **WebAssemblyとEmscripten** 

C/C++ 言語を WebAssembly のバイナリとしてコンパイルする機能は LLVM 自体に備わっているのですが、より便利な Emscripten というツールキットを使うことが一般的です。 make や cmake を使った既存のライブラリのビルドや OpenGL や SDL といったライブラリのポート、 malloc/free などを含めた C/C++ の標準ライブラリの WebAssembly 化、 WebAssembly 特有のリンカオプションなどの設定ができます。 Emscripten の構成要素は大きく分けて各種ビルドツールチェーンのコマンドを置き換えるスクリプトと、 WebAssembly に特化した機能を提供するライブラリの 2 つがあります。 1 つめのスクリプトとしては C/C++ コンパイラを置き換える emcc/em++ 、 make コマンドや cmake コマンドの実行時に Emscripten 環境となるようラップする emmake/emcmake といったビルドツールチェーンを置き換える Python スクリプトがあります。 2 つめのライブラリの中には、 malloc や free といった基本的なメモリ管理やファイル I/O などの機能を WebAssembly 上でも動作するようにした標準ライブラリ、 SDL/OpenGL などのよく使われるライブラリ、 JavaScript との連携機能などの WebAssembly ならではの API を提供する Emscripten 固有のライブラリが含まれています。 

WebAssembly はまだ発展途上の技術であることもあってか、 Emscripten は比較的頻繁にリリースされます。そのため、インストールされている Emscripten のバージョン管理を行う EMSDK <u>注10</u> ~~と~~ いうツールを用いてインストールすることが一般的です。 

##### **WebAssembly化の実践** 

それでは実際の Web アプリケーションに WebAssembly を組み込んでいく手順を見ていきましょう。本稿では Emscripten を使った 2 つのプロジェクトを解説します。 Emscripten では main 関数を持ち、フレームごとのコールバック関数で処理をする、 **アプリケーション** としての形式と、 main 関数を持たず JavaScript から呼び出される関数だけを提供する **ライブラリ** としての形式の 2 つがあります注11 ~~。~~ 

1 つめは Emscripten に組み込まれている SDL ライブラリを使ったアプリケーション形式のプロジェクトのビルドについて解説します。 2 つめは OpenCV ライブラリを使った関数モジュールとしての WebAssembly です。掲載したものに加えてほかの言語での WebAssembly 化の例などを <u>https://github.com/kounoike/software-design2023-05-samplecode にて公開しますので参考にしてください。</u> 

#### **Emscriptenのインストール** 

Emscripten のインストールは **図** 1のように EMSDK 経由で行います。 emscripten-core/emsdk リポジトリをクローンし、 Emscripten の最新バージョンを install/activate し、 emsdk_env.sh で必要な環境変数をセットすることで完了します。 EMSDK は Windows/macOS/Linux で動作します。 Windows では若干手順が異なるので公式サイトを参考にしてください。また、 git や cmake などのツールチェーンも必要になりますので、適宜インストールしてください。 Emscripten は多くの場合 3 週間程度で新しいバージョンがリリースされるので、時々 **git pull** して install/activate しなおすとよいでしょう。執筆時点での latest は 3.1.34 です。 

**▼図1 Emscriptenのインストール** 

**$ git clone https://github.com/emscripten-core/emsdk ~/emsdk** 

**$ ~/emsdk/emsdk install latest** 

**$ ~/emsdk/emsdk activate latest** 

**$ . ~/emsdk/emsdk_env.sh** 

#### **アプリケーション形式のプロジェクトのコーディングとビルド** 

実践例の 1 つめとしてアプリケーション形式の WebAssembly プロジェクトである sdl-demo プロジェクトの作成方法を説明します。このアプリケーションは SDL <u>注12</u> ~~で~~ BGM 音楽を流し、簡単なアニメーションをしつつ、マウス操作で表示が切り替わる、ゲームなどを作るときの基礎となるようなアプリケーションです。アプリケーションのソースコード main.cpp を **リスト** 1に示します。 main 関数の 1 で SDL の初期化処理、 2 で BGM の再生処理を行い、 loop 関数の中の 3 で各フレームでの SDL 描画処理を記述してアニメーションを実現しています。マウスクリックに対するコールバックを 4 で設定しています。 loop 関数がフレームごとのコールバック関数であることは、 5 で emscripten_set_main_loop を呼び出すことで指定しています。ビルドの設定は CMake で行います。 CMake によるビルド手順・設定を記述した CMakeLists.txt が **リスト** 2です。 sdl-demo という名前の C++ プロジェクトを設定しています。 src/main.cpp をソースファイルとすることを指定し、リンク時には **USE_SDL=2** オプションで Emscripten で使う SDL ライブラリのバージョン 2 を使うこと、 -- embed-file オプションで Emscripten の仮想ファイルシステムに BGM の MP3 ファイルを追加することを指定しています。 set_property で .html 拡張子を指定することで、 wasm ファイル、グルーコードの JavaScript ファイル、それらを読み込んで実行する HTML ファイルを出力するようにしています。 

###### **▼リスト2 CMakeLists.txt** 

**cmake_minimum_required(VERSION 3.23) project(sdl-demo LANGUAGES CXX)** 

**add_executable(sdl-demo src/main.cpp) target_compile_options(sdl-demo PRIVATE "SHELL:-s USE_SDL=2" "SHELL:-s USE_SDL_MIXER=2" ) target_link_options(sdl-demo PRIVATE "SHELL:-s USE_SDL=2" "SHELL:-s USE_SDL_MIXER=2" "SHELL:--embed-file ${CMAKE_CURRENT_SOURCE_DIR}/BGM.mp3@/BGM.mp3" ) set_property(TARGET sdl-demo PROPERTY SUFFIX .html)** 

続いて WebAssembly へのビルドを行います。 WebAssembly へのビルドは CMakeLists.txt ファイルを置いたディレクトリに build ディレクトリを作成し、 **emcmake cmake ..** と **cmake --build .** の 2 つのコマンドを実行することでできます( **図** 2)。通常 CMake プロジェクトの場合は 

**cmake ..** のように実行しますが、 Emscripten を利用する場合はラッパースクリプトである emcmake を挟みます。 

###### **▼図2 sdl-demoプロジェクトのビルド** 

**$ mkdir build $ cd build $ emcmake cmake .. $ cmake --build .** 

ビルドすると、 sdl-demo.wasm 、 sdl-demo.js 、 sdl-wasm.html の 3 つのファイルが build ディレクトリに作成されます。 WebAssembly は HTML ファイルを直接開いた際は実行されないため、 HTTP サーバを経由してブラウザで開きます。 EMSDK には WebAssembly 向けの簡易的な HTTP サーバを起動する emrun コマンドがありますので、 **emrun sdl-wasm.html** と実行するとブラウザでページが表示され、記述したコードが実行されます。この例では作成したプログラムを自動的に実行するように設定された sdl-demo.js 、 sdl-demo.html ファイルが Emscripten により作成されるので、自分で JavaScript を書く必要はありません。ページの見た目を変更したい場合は生成された sdl-demo.html を読んで適宜修正するとよいでしょう。ブラウザで sdl-demo.html を開くと **図** 3のようになります。 



#### **ライブラリ形式のプロジェクトのコーディングとビルド** 

次の例としてライブラリ形式のプロジェクトである opencv-demo プロジェクトを示します。このプロジェクトでは映像処理を行うアプリケーションの画像処理部分を WebAssembly でライブラリとして提供しています。また、既存の開発資産を活用する際の例として、有名な画像処理ライブラリである OpenC ~~V~~ <u>注13</u> ~~を~~ リンクする方法を含めて説明しています。具体的には Web カメラなどから取り込んだ画像を上下反転する関数を WebAssembly で作成し、取り込んだ画像と上下反転した画像の両方を表示するよう、 HTML と JavaScript で記述します。 

**リスト** 3に示す main.cpp では OpenCV ライブラリを利用して上下反転を行う関数 flip を定義しています。また、 JavaScript との間で大きなデータをやりとりするため、固定の buffer を用意して、そのアドレスを JavaScript に渡すための getBuffer 関数も定義しています。最後にこれらの JavaScript から呼び出したい関数を EMSCRIPTEN_BINDINGS で指定しています。 



**リスト** 4に CMake の設定を行う CMakeLists.txt を示します。ここで今回はリンクオプションに **SHELL:-s MODULARIZE=1** 、 **SHELL:-s EXPORT_NAME=createModule** 、 **SHELL:--bind** を指定します。 **MODULARIZE=1** を指定すると Emscripten が出力するグルーコード自体では WebAssembly の読み込みなどが行われず、初期化を行う関数だけが提供されます。初期化を行う関数の名前は EXPORT_NAME で指定します。 EMSCRIPTEN_BINDINGS を使って JavaScript から呼び出せる関数を指定した場合は、 1 のように --bind オプションが必要です。 EMSCRIPTEN_BINDINGS 以外の、 JavaScript から呼び出せる関数を指定する方法としては、ビルド時に EXPORTED_FUNCTIONS オプションを指定する方法とソースコードに EMSCRIPTEN_KEEPALIVE を指定する方法がありますが、どちらの方法でも JavaScript から呼び出す際の名前に **_** のプレフィックスが付いてしまうところが難点です。 EMSCRIPTEN_BINDINGS は関数に限らず enum やクラスなども 

JavaScript から呼び出せるようにできるので、今後は EMSCRIPTEN_BINDINGS を使うとよいと思います。 

**▼リスト4 CMakeLists.txt** 

**cmake_minimum_required(VERSION 3.23) project(opencv-demo LANGUAGES CXX) set(CMAKE_TOOLCHAIN_FILE $EMSDK/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake) set(ENBALE_PIC FALSE) set(BUILD_SHARED_LIBS OFF) set(BUILD_ZLIB ON) set(WITH_ITT OFF) set(WITH_IPP OFF) set(BUILD_LIST core,imgproc) set(CPU_BASELINE '') set(CPU_DISPATCH '') set(CV_ENABLE_INTRINSICS ON) set(CMAKE_CXX_FLAGS "-s USE_PTHREADS=0 -msimd128") include(FetchContent) FetchContent_Declare( opencv GIT_REPOSITORY https://github.com/opencv/opencv.git GIT_TAG 4.7.0 ) FetchContent_MakeAvailable(opencv) add_executable(opencv-demo src/main.cpp) target_link_options(opencv-demo PRIVATE "SHELL:-s MODULARIZE=1" "SHELL:-s EXPORT_NAME=createModule" "SHELL:--bind"** **<mark>//</mark>** <mark>1</mark> **) target_include_directories(opencv-demo PRIVATE ${CMAKE_CURRENT_BINARY_DIR} ${opencv_SOURCE_DIR}/include ${OPENCV_MODULE_opencv_core_LOCATION}/include ${OPENCV_MODULE_opencv_imgproc_LOCATION}/include ) target_link_libraries(opencv-demo opencv_core opencv_imgproc) set_property(TARGET opencv-demo PROPERTY SUFFIX .js)** 

WebAssembly で定義した関数使う側の HTML ファイルとして index.html を **リスト** 5に示します。ここでは先ほど指定した名前の createModule を関数として呼び出して得られる Promise オブジェクトを介して WebAssembly 関数を呼び出します。 WebAssembly の初期化を行う createModule 関数、 Web カメラ取り込みの初期化を行う navigator.mediaDevices.getUserMedia メソッドはどちらも async 関数であるため、 Promise.all を使って同時に初期化します。 getUserMedia が返すストリームを video タグにセットし、 canvas を経由して各フレームの RGBA 配列を ImageData として取得します。その内容を 

WebAssembly のメモリ空間に書き込みます。 **リスト** 5では Emscripten が提供する Uint8Array としてのビューである HEAPU8 プロパティを経由して書き込みます。 **リスト** 3の 1 でバッファ領域のポインタであった getBuffer 関数の戻り値はこの HEAPU8 プロパティのオフセットになります。 RGBA 配列を渡したあとは flip 関数によって画像の上下反転を行い、反転した RGBA 配列を再び HEAPU8 プロパティ経由で取得し、その内容で ImageData を作成して canvas に描画しています。 

###### **▼リスト5 index.html** 

**<html> <head><title>wasm opencv demo</title></head> <body> <video autoplay playsinline></video><canvas width="640" height="480"> </canvas> <script src="build/bin/opencv-demo.js"></script> <script> const [width, height] = [640, 480] Promise.all([navigator.mediaDevices.getUserMedia({ video: { width, height } }), createModule()]). then(([stream, wasm]) => { const video = document.querySelector('video') const ctx = document.querySelector('canvas').getContext('2d') const offset = wasm.getBuffer() const frameFunc = () => {** 

**ctx.drawImage(video, 0, 0) const inputData = ctx.getImageData(0, 0, width, height) wasm.HEAPU8.set(inputData.data, offset) wasm.flip() const outputData = new ImageData(new Uint8ClampedArray(wasm.HEAPU8.slice(offset, offset + width * height * 4)), width, height) ctx.putImageData(outputData, 0, 0) video.requestVideoFrameCallback(frameFunc) } video.srcObject = stream video.requestVideoFrameCallback(frameFunc) }) </script> </body></html>** 

ブラウザで index.html を開くと **図** 4のようになります。 



##### **実用に向けて注意すべきこと** 

前述したとおり、 WebAssembly としてビルドすると多くの場合で JavaScript よりもファイルサイズが大きくなってしまいます。先の例では sdl-demo.wasm が 5.6MB 、 opencv-demo.wasm が 1.3MB となっています。 WebAssembly ファイルのダウンロード時間がユーザー体験の妨げとならないよう十分注意する必要があります。具体的には gzip などの HTTP 通信上での圧縮を利用する手法や、 WebAssembly の初期化が非同期であることを活用して、その他の機能と同時に初期化していくなどの手法があります。また、高速化を狙って WebAssembly 化を行う場合、 SIMD やマルチスレッドなどの機能をうまく使わないと同じ機能の JavaScript コードを置き換えるに足る速度を得られないことがあります。その場合はボトルネックの切り分けを行います。ボトルネックになっている部分が外部ライブラリの場合はライブラリのビルド方法の確認、ビルドオプションの調整などを行います。ボトルネック部分が自分で記述したコードの場合はループの書き換えでコンパイラの最適化で SIMD 命令が生成されやすいようにしたり、 WebAssembly の SIMD Intrinsics で自分で SIMD 化したり、 pthread ライブラリを用いて並列化したりといった対策が考えられます。実践例の中で利用した OpenCV ライブラリは画像処理ライブラリとしてよく最適化されたコードになっており、 WebAssembly の SIMD 命令にも対応した SIMD Intrinsic ~~s~~ <u>注14</u> ~~に~~ よる抽象化されたベクトル化、 pthread によるスレッドプールを利用した並列化が良く考えられて実装されています。高速化で困った際には OpenCV のソースコードを参照すると参考になるかもしれません。少し前までは WebAssembly SIMD や WebAssembly Threads の実装状況がブラウザによって異なり対応が必要だったのですが、 Safari 16.4 で SIMD サポートが追加になり、モダンブラウザではとくに心配することはなくなりました。こうした注意点とは別に、実現したい機能がブラウザ組み込みの機能でできないか確認することも重要です。 CSS フィルタや WebCodecs などはネイティブコードでブラウザに組み込まれている機能のため、 WebAssembly で同じことをするよりも非常に高速に実行できます。実は先の opencv-demo の例で示した上下反転する程度のことであれば、 CSS フィルタだけでも実現できます。やりたいことが本当に上下反転だけの場合は WebAssembly でなく CSS フィルタで実装するべきでしょう(もちろん、今回は解説のためにわざと WebAssembly で実装しました)。また、 WebGL や WebGPU といったブラウザで GPU を使う機能もあります。画像処理などは同じ処理であっても CPU と GPU では実行時間に大きな差が出ることがあります。その場合はそういう処理を GPU で行うようにする工夫が必要になります。 Emscripten の場合は OpenGL ライブラリを使ってアプリケーションを記述すると、ブラウザ上では WebGL を使って実行されるため、既存の 3D アプリケーションなどの移植も比較的容易です。 WebGPU についてはまだブラウザ側の対応が進んでいませんが、 WebGPU を WebAssembly の中から Emscripten の提供するライブラリを使って呼び出すことも可能です。 

##### **終わりに** 

本章では Emscripten を用いた C++ アプリケーションの WebAssembly 化の初歩を解説しました。 Emscripten プロジェクトはどのような C/C++ ソースであっても WebAssembly 化できることを目標として開発されているように思います。実際にそれで FFmpeg や SQLite 、さらには Ruby や Python のようなスクリプト言語の WebAssembly 化を実現できています。一方で本章で解説しなかったほかのプログラミング言語の WebAssembly 化に目を向けると、 Rust の場合は HTML/JavaScript の DOM オブジェクトや Web API との連携を実現する web_sy ~~s~~ <u>注15</u> ~~ク~~ レートの開発にとくに力が入っている印象があります。 C# などの .NET Framework の言語の場合は Blazo ~~r~~ <u>注16</u> ~~と~~ いう、 .NET ランタイムをそのまま WebAssembly に移植することで .NET Framework がほぼそのままブラウザでも動くようにしたようです。そのほかにも D 言語、 AssemblyScript 、 Nim 言語、 Zig 言語など、 WebAssebmbly としてビルドできる言語はいろいろあります。「 WebAssembly としてビルドできる」といっても言語によって上記のような周辺環境の違いや、生成される WebAssembly バイナリの効率などに違いはありますが、どの言語でも WebAssembly 環境への対応は注目されていて、今後の発展が期待できます。 

- 注11) 正確にはアプリケーション形式にJavaScriptから呼び出される関数を含めてもよいのですが、複雑になるので本章の説明では割愛します。 (本文に戻る) 

- 注14) x86 CPUのSSE、AVX命令や、ARM CPUのNEON命令WebAssemblyのSIMD命令はそれぞれ命令が異なり、C/C++への書き方も異なりますが、それぞれの命令には共通部分もあります。それをまとめて扱えるようにしたのがOpenCVの Intrinsicsです。 

   - (本文に戻る) 

#### **短期連載** 

##### **ソフトウェアのセキュリティ&品質管理の新要素** 

## **Software Bill Of Materials SBOM** 

#### **今後求められる運用と立ちふさがる課題** 

##### **1 SBOMが注目される理由** 

小保田規生(こぼたのりお) @nori0428 ソニーグループ株式会社 OSPOメンバー昨今一部の国で、ソフトウェア製品やサービスを提供する際に、セキュリティや品質管理の観点からSBOM(ソフトウェア部品表)の提供を求める動きが加速しています。本連載では、SBOMにまつわる議論、対策、課題などの最新事情をお伝えします。 

##### **品質管理やセキュリティの業界を騒がすSBOM** 

昨今、ソフトウェアの品質管理や、セキュリティに関係する業界では、 SBOM ( Software Bill Of Materials )という言葉が飛び交い、非常に活発に議論されています。また、一部の国の法律では、製品やサービスとともに SBOM 提供を求める条項が盛り込まれつつあり、すでに一部の企業では SBOM に対してどのように適応していくか、といった検討を始めているところもあります。ところで、 SBOM とはいったい何でしょうか?また、そもそもなぜそれほど必要とされており、どのように用いられるものなのでしょうか? 

本連載では、 SBOM が重視されるようになってきた背景と、その作成方法や利用方法、さらには SBOM 自身が抱えている課題について紹介します。また、 SBOM はその出自から、ライセンスコンプライアンス面から語られる場合と、セキュリティ面から語られる場合の 2 種類の文脈が存在します。それら 2 つの文脈がどのように絡み合い、どのような議論が起きているのか、筆者が知る限り詳しく紹介したいと思いますので、よろしければお付き合いください。 

##### **SBOMとは?** 

さて、そもそも、 SBOM とはいったいどのようなものなのでしょうか? 

SBOM を定義づけするときは多くの場合において、 

NTIA ( National Telecommunications and Information Administration 、アメリカ合衆国国家電気通信情報管理庁)が発行する資料や Web サイトが参照されます。たとえば、 NTIA が 2021 年に発行した SBOM at a Glanc ~~e~~ <u>注1</u> ~~で~~ は次のように記載されています。 

_An SBOM is a formal, machine-readable inventory of software components and dependencies, information about those components, and their hierarchical relationships._ 

この資料は一般社団法人 JPCERT コーディネーションセンターによって日本語訳も提供注2 ~~さ~~ れており、その中では次のように翻訳されています。 

_SBOM とは、ソフトウェアコンポーネントやそれらの依存関係の情報も含めた、機械処理可能なインベントリーです。_ 

しかし、この文章からだけでは、なかなか理解するのが難しいですね。ですので、公式 Debian ディストリビューションのパッケージ <u>注3</u> ~~を~~ 例にとって考えてみたいと思います。 

Debian 11“bullseye” には約 97,000 のパッケージが含まれていま ~~す~~ <u>注4</u> が、この中から、 busybox ( busybox-static )を例として取り上げます。 

busybox パッケージの詳細情報は Web サイ ~~ト~~ <u>注5</u> ~~上~~ に記載されていますので、まずはそれを確認してみましょう。すると、 busybox パッケージは libc6 パッケージに依存していることがわかります。では次は、 libc6 パッケージの詳細情 ~~報~~ <u>注6</u> ~~を~~ 見てみます。 libc6 は libcrypt1 と libgcc-s1 に依存しています。このように依存関係を次々にたどって各パッケージの詳細を見ていくと、 libcrypt1 は libc6 と相互依存関係となっていることや、 libgcc-s1 は gcc-10-base に依存しており、 libcrypt1 と同様に libc6 と相互依存関係を持っていることがわかります。最終的にパッケージ間の依存関係は **図** 1のような構造となります。この構造を、テキストとして資料化したものが SBOM です。つまり、 **あるソフトウェアについて依存関係を含めた構造を調べ、特定のフォーマットに沿って、** JSON **や** XML **などで記載したテキストファイル** のことを SBOM と呼んでいます。特定のフォーマットについては本連載の第 2 回で詳しく説明する予定ですが、代表的なフォーマットとして、 OWASP が定義する CycloneD ~~X~~ <u>注7</u> ~~や~~ Linux Foundation SPDX WG が定義する SPDX <u>注 8</u> ~~(~~ ISO/IEC 5962:202 ~~1~~ <u>注9</u> ~~)~~ が存在しています。 

##### **SBOMが必要とされる背景と歴史** 

ここでは、 SBOM という単語が取りざたされる背景について、少し掘り下げてみましょう。まず、 SBOM という単語が話題に上ることが多くなってきた時期などを Google Trends で見てみると、 2021 年の夏くらいから急激に注目を集めていることがわかります( **図** 2)。さて、その理由は何でしょうか? 



<u>https://trends.google.co.jp/trends/explore?date=2011-05-01%202023-02-19&q=SBOM</u> 



###### **セキュリティ面から注目されるSBOM** 

もしかすると読者のみなさんの中にもご存じの方がいるかもしれませんが、 2021 年 5 月に、米国において Exective Order 1402 ~~8~~ <u>注10</u> ~~と~~ いう大統領令に署名がされ、公開されました。この大統領令が署名されることになった発端は、 SolarWinds ~~社~~ <u>注11</u> の製品に仕込まれたバックドアの発見に端を発する一連の大規模なサイバーセキュリティ攻撃です。攻撃の概要を説明します。 SolarWinds 社は「 Orion Platform 」というネットワーク上の IT プラットフォームを監視するための製品を販売しており、世界中の多くの組織に導入されていました。この製品の正規のアップデートを通じて、 SolarWinds 社が気がつかないうちに、バックドアが埋め込まれ、配信されてしまったのです。その後、そのバックドアを通じた攻撃が開始され、世界中の組織が影響を受けました。情報窃取などの被害組織数は最大で約 18,000 件だったようです。この事件では、多くの米国政府機関も攻撃の対象となっており、米国政府は事実上⻑期にわたり気がつくことができなかったことを非常に問題視しました。 FBI 、 CISA 、 ODNI 、 NSA が共同声明を出したり、多くの報道がなされたりしたため、注目を集めることとなりました。この攻撃の詳細については 2020 年 12 月の piyolo ~~g~~ <u>注12</u> ~~に~~ 詳しくまとめられています。さて、この事件には、押さえなければいけない重要なポイントがあります。それは、昨今のソフトウェアの作成や供給手順は非常に複雑になっていること、もしその途中のどこかで悪意を持つ者がマルウェアなどを仕込んだ場合、それらの攻撃に早期に気がつき適切な対処を取ることは非常に困難になっている、ということです。そのため、この事件をきっかけとして、「私たちが利用する製品やソフトウェアが、誰によってどのように作られていて、そして誰からどのような経路で受け渡されるものか」という、 **ソフトウェアサプライチェーンの透明性** が、セキュリティ関係者たちに重要視されることとなりました。大統領令 14028 そのものは、このようなソフトウェアサプライチェーンセキュリティへの対応を含む、米国のサイバーセキュリティ改善活動に関して指示する文書です。そして、この文書の 4 章「ソフトウェアサプライチェーンセキュリティの強化」に、 SBOM という単語が出てきます(以下は筆者翻訳)。 

_Sec. 4. Enhancing Software Supply Chain Security. ( .. 略 .. )_ 

_( vii )購入者(この場合は連邦政府)に各製品のソフトウェア部品表( SBOM )を直接提供するか、公開 Web サイトで公開すること_ 

ここに製品に関する SBOM を提供すること、ということが記載されています。また、 10 章の「定義と目的」には、次のように書かれています (以下は筆者翻訳)。 

_Sec. 10. Definitions. For purposes of this order: ( .. 略 .. )_ 

_( j )「ソフトウェア部品表」または「 SBOM 」という用語は、ソフトウェアの構築に使用されるさまざまなコンポーネントの詳細とサプライチェーン関係の情報を記録したものを意味します。ソフトウェア開発者やベンダーは、既存のオープンソースおよび商用ソフトウェアコンポーネントを組み立てることによって製品を作成することが多く、 SBOM は製品内のこれらのコンポーネントを列挙したものになります。_ 

_SBOM とは食品の包装に記載されている成分表に似たものと考えられるでしょう。_ 

国内外さまざまなイベントの発表では SBOM が食品の成分表に例えられていることが多いのですが、じつは大統領令 14028 が起源です (もちろん、内部で議論されていた方々にとっては別ですが)。こういったことも含めて、筆者は、大統領令 14028 が SBOM という単語を世に広く知らしめる最大の要因だったと理解しています。 



**ライセンスコンプライアンス面で注目されていたSBOM** 

次に、先ほどの **図** 2のトレンドグラフに、いくつかイベントを付け加えてみましょう( **図** 3)。先ほど少し触れましたが、 SBOM の代表的なフォーマットとして認知され、現在も活発に活動が継続しているものとして、 CycloneDX と SPDX という 2 つの仕様が存在します。じつは、これら 2 つの仕様の歴史は結構⻑く、 SPDX の始まりは 2009 年注13 ~~で~~ あり、 v1.0 のリリースは 2011 年 8 ~~月~~ <u>注14</u> ~~で~~ す。 CycloneDX の v1.0 リリースは 2018 年 3 月注15 ~~で~~ す。このように、両仕様とも先のサイバーセキュリティに関する大統領令が発行される数年前に公開されています。じつに、 CycloneDX は 5 年以上、 SPDX にいたっては 10 年以上もコミュニティが継続してその仕様をメンテナンスしている、ということになります。ここで SPDX という単語をどこかで見たことがある方がいらっしゃるかもしれません。たとえば、 Linux カーネルなど一部のオープンソースソフトウェア ( OSS )では、ヘッダファイルやソースコードの先頭に次のような 1 

行のコメントが記載されていることがあります。 

###### **// SPDX-License-Identifier: GPL-2.0-only** 

SPDX License Identifier とは、 SPDX 仕様の一部であり、さまざまな OSS ライセンスをライセンスごとのユニークな識別子(上記例では GPL-2.0-only )で表すことができる仕様となっており、 SPDX の Web サイト注16 ~~で~~ 公開されています。 

Linux カーネルのソースコードにこの SPDX License Identifier が付与されたのは、 2017 年 11 月のパッチがマー ~~ジ~~ <u>注17</u> ~~さ~~ れたのが最初です。現状の Linux カーネルは、数多くのソフトウェアエンジニアが著作者となってメンテナンスされている OSS であり、また商用、非商用問わず、広く利用されている OSS でもあります。 OSS は誰でも自由にそのソフトウェアを改変、利用、配布できるという特性を持ちますが、(当たり前ですが)そのライセンスに記載されていることを守る必要があります。 Linux カーネルの各ファイルに SPDX License Identifier が記載されているのは、これらのファイルがどのようなライセンスで提供されていて、どのようなことを守らなければならないかをわかりやすくするためです。このように、 OSS ライセンスコンプライアンスという面からは、 SPDX の仕様はかなり古くから重要であると考えられていました。さらに、昨今の数多くの企業におけるソフトウェア開発は自社のみで開発しているわけではなく、複雑なサプライチェーンを伴って開発されています。すると、セキュリティと同様に、その途中のどこかでライセンスコンプライアンスが守られない場合、大きな問題を起こすことになります( **図** 4)。 



<u>https://github.com/OpenChain-Project/Reference-Material/blob/master/Education-ForSuppliers/ISO-5230-2020-Supplier-Education-Leaflet/Official/2.1/ja/supplier-leaflet-ja.pdf</u> 

ソフトウェアサプライチェーンにおけるライセンスコンプライアンスをどのように解決していくかという議論を行う OpenChain projec ~~t~~ <u>注18</u> ~~の~~ 活動が 2017 年ごろより活発化し、 2020 年ごろからは SPDX にも注目が集まってきていました。じつは OSS ライセンスコンプライアンスの観点からも、 SBOM というものが重要であるという認識がここ数年で広がりを見せていたのです。ライセンスコンプライアンスに関わるコミュニティと、ソフトウェアセキュリティに関わるコミュニティが大統領令 14028 をきっかけとして出会い、議論参加者が大きく増加、それに伴って SBOM に関する標準化や法制化が急速に進んでいる、というのがここ数年の盛り上がりの背景となっています。 

##### **法制化の動向** 

米国の大統領令 14028 については前述のとおりですので、ここではまず、 EU において 2022 年 9 月に草案が提出された、 Cyber Resilience Act ( CRA )注19 ~~を~~ 紹介します。 



###### **Cyber Resilience Act** 

米国の大統領令に倣って、 EU 圏におけるサイバーセキュリティを改善するために定められた法案です。この法案は EU 域内で流通するソフトウェア製品やサービスのほとんどすべてを規制の対象としており、非常に影響が大きいものとなっています。また、 2023 年後半の発効、 2025 年後半から適用することを目指していますので、 EU 圏で商売を行っている企業としては、今年から対応を開始していかないと、大きな問題を抱えることになりえます。 

CRA で特筆すべきことの 1 つは、その対象製品、対象サービスの幅広さです。 CRA では Proposal for a Regulation on cybersecurity requirements for products with digital element ~~s~~ <u>注20</u> ~~の~~ 第 2 条で、対象製品、サービスを明確に定義しており、CRA **で明確に対象外と定義されたものを除き、すべてのデジタル製品が適用対象** だと定義されています。また、それぞれの企業法務の方と確認していただきたいと思いますが、明確に対象外と定義されているものは、たとえば医療機器規制などほかの規制で対象とされているものなど同等の規制があるものを対象外としているだけであり、実質的には今まで対象外とされていた製品をすべて対象範囲に含めるような規制となっているように読めます。さらに、対象製品は認証機関によって認定を受けなければならないこと、市場監視機関によってその適合性を監視され、もし違反した場合には1,500 **万ユーロまたは当該企業の全世界売り上げの** 2.5 **%のどちらか高いほうを罰則金として支払うという厳しい罰則** があることも非常に大きな特徴となっています。企業に求められることは 10 条と 11 条に非常に細かく規定されており、正しくは原文などをあたっていただく必要がありますが、簡単にまとめると次のとおりです。 

- 1 CRAが指示するセキュリティ要件を満たした設計、開発、製造を行うこと 

- 2 サイバーセキュリティ上のリスクアセスメントを実施し、その結果を製品の技術文書に含めて提供すること 

- 3 製品寿命まで、もしくは市場に製品の提供を開始してから5年間、CRAが指示する脆弱性対応要件を満たすこと 

- 4 技術文書を認証機関にて評価し、適合性証明書の発行を受けること 

- 5 市場に製品の提供を開始してから10年間、上記技術文書と適合性証明書を保管すること。また監視当局からの要求が発生した場合はそれらを提出すること 

- 6 脆弱性を発見した場合、もしくはインシデントが発生した場合は 24時間以内にENIS ~~A~~ <u>注21</u> ~~に~~ 通知すること 

- 7 OSSコンポーネントの脆弱性を発見した場合は、そのコンポーネントを維持する個人や団体に報告することなお、例外として「技術革新や研究を妨げないために、商業活動の過程以外で開発または供給された OSS は、本規則の対象とすべきではない」と 10 項の冒頭に記載されていますので、これら規制がオープンソースコミュニティに及ぶことはなく、オープンソースコミュニティ側の方々はあまり心配する必要はありません。さて、 CRA の中で SBOM に関して言及されている部分はたった 2 ヵ所です。まず、 CRA が指示する脆弱性対応要件の中に、 SBOM の作成要件が非常に簡単に記載されています(以下は筆者翻訳)。 

_( 1 )少なくとも製品のトップレベルの依存関係をカバーし、製品に含まれる脆弱性とコンポーネントを特定可能な、一般的に使用される機械可読形式の SBOM を作成すること_ 

あとは、本文冒頭に「欧州委員会は SBOM の形式や要素を指定することができる」と書かれているのみであって、とくにどのような内容をどのようなフォーマットで記載する必要があるのか、といったことは書いてありません。そのため、企業としては一般的に使用される SBOM 、つまり本記事の冒頭に紹介した、代表的なフォーマット、 CycloneDX や SPDX の動向を注視し、内容を理解し、必要によっては仕様策定に参画していく必要があるでしょう。 



###### **IMDRF** 

International Medical Device Regurations Forum ( IMDRF ~~)~~ <u>注22</u> ~~と~~ は、医療機器規制について国際的に議論している集まりです。この集まりにおいても、 2020 年 3 月に Principles and Practices for Medical Device Cybersecurit ~~y~~ <u>注23</u> ~~と~~ いう文書が発行されています。日本語翻訳版も公開されています注24 ~~。~~ 

こちらは CRA ほど厳しい指令ではありませんが、 2020 年から 3 年程度を目途にこのガイダンスを導入していくとして検討が行われてきており、すでに日本国内でも対応が始まりつつあります。このガイダンスにおいても SBOM の利用や提供について言及されており、今後、さらに多くの業界に SBOM の活用が広がっていくものと考えられます。このガイダンスでは 5.5.2 項に顧客向けセキュリティ文書についての記述があり、その文書に含めるべき項目として SBOM の記載があります。 

_・医療機器に実装される商用、オープンソース及び市販のソフトウェア部品のサイバーセキュリティに関する情報及びサポートをオペレータに提供するためのソフトウェア部品表( Software Bill of Materials : SBOM )。名前、作成元、バージョン、ビルド番号によって各ソフトウェア部品が特定されるため、 SBOM を使用することで、必要とされる透明性が確保される。 ( .. 略 .. )_ 

_SBOM を使用することにより、アプリケーションで使用されているコンポーネントを可視化して顧客に提示できると共に、潜在的セキュリティリスクを特定できるため、購入決定に必要な情報を提供することが可能となる。製造業者は、 SBOM の展開で使用される形式、構文、マークアップに関する業界のベストプラクティスを活用することが望ましい。_ 

このガイダンスにおいても、「名前、作成元、バージョン、ビルド番号によって各ソフトウェア部品が特定される」と、必要な項目についていくつか例示はされているものの「製造業者は、 SBOM の展開で使用される形式、構文、マークアップに関する業界のベストプラクティスを活用することが望ましい」と記載されていて、 CRA 

と同じく関連する企業は CycloneDX や SPDX の動向に注意する必要があるでしょう。 

**経済産業省サイバー・フィジカル・セキュリティ確保に向けたソフトウェア管理手法等検討タスクフォース** 

ガイドラインや規制などではありませんが、日本においても SBOM に関する検討が進んでいま ~~す~~ <u>注25</u> ~~。~~ たとえば、 2023 年 2 月 28 日に「 SBOM ( Software Bill of Materials )の導入に関する手引(仮称・案)」注26 ~~と~~ いう文書が発行されています。 SBOM とは何かという説明から、実際に作成、運用したときの課題などが幅広く紹介され、運用する際のチェックリストまで公開されています。 SBOM の管理運用に携わる方はこちらの活動も注視されると良いでしょう。 

##### **SBOMを生成してみよう** 

さて、連載第 1 回の最後は実際に SBOM を生成してみましょう。さまざまな SBOM 生成ツールがありますが、ここでは、 Microsoft がオープンソースとして公開している SBOM Too ~~l~~ <u>注27</u> ~~を~~ 利用してみたいと思います。このツールはダウンロードするだけで利用できるようになっていますので、 README.md に書かれているとおり、 GitHub のリリースページ注28 ~~か~~ らダウンロードして動作させます ( **図** 5)。 

**▼図5 SBOM Toolをダウンロードする** 

**Windows(PowerShell) $ Invoke-WebRequest -Uri "https://github.com/microsoft/sbomtool/releases/latest/download/sbom-tool-win-x64.exe" -OutFile "sbom-tool.exe"** 

**Linux(curl) $ curl -Lo sbom-tool https://github.com/microsoft/sbom-** 

**tool/releases/latest/download/sbom-tool-linux-x64 $ chmod +x sbom-tool** 

SBOM を生成するソフトウェアとして、冒頭で用いた busybox の deb パッケー ~~ジ~~ <u>注29</u> ~~で~~ ある busybox_1.30.1-6+b3_amd64.deb をダウンロードしてきます。ダウンロードが終わったら、解析して SBOM を生成させてみましょう。 **図** 6を実行し、少し待つと、 /path/to/download/_manifest/spdx_2.2/manifest.spdx.json というファイルが作成されます。中をのぞいてみると、 **図** 7のように、エラーもなく、 SPDX ver2.2 形式の JSON ファイルができています。 **"name": "busybox 1.30.1"** などのタグも入っており、一見正しいようですが ...... しかし、このファイルは 67 行しかありません( **図** 8)。 

###### **▼図6 debパッケージからSBOMを生成する** 

**$ ./sbom-tool generate -b /path/to/download -pn busybox -pv 1.30.1 -ps "Norio Kobota" -nsb "https://norio.kobota/sbom-tool-test"** 

###### **▼図7 SBOMの中を確認** 

**$ less /path/to/download/_manifest/spdx_2.2/manifest.spdx.json { (..略..) "spdxVersion": "SPDX-2.2", "dataLicense": "CC0-1.0", "SPDXID": "SPDXRef-DOCUMENT", "name": "busybox 1.30.1", "documentNamespace": "https://norio.kobota/sbom-tooltest/busybox/1.30.1/luhEOpNnm0-zEE6XiAp-CQ", "creationInfo": { "created": "2023-03-04T13:09:32Z", "creators": [ "Organization: Norio Kobota", "Tool: Microsoft.SBOMTool-0.3.3" ] }, "documentDescribes": [** 

**"SPDXRef-RootPackage" ] }** 

###### **▼図8 図7のSBOMは67行しかない** 

**$ wc -l download/_manifest/spdx_2.2/manifest.spdx.json 67 download/_manifest/spdx_2.2/manifest.spdx.json** 

じつはこの方法では、 busybox のバイナリパッケージが 1 ファイルありますよ、という事実だけを記述した SPDX ファイルしか生成されず、詳細情報は何も生成されません。私たちが管理運用すべき SPDX は、もっと詳細な情報が必要になるはずです。では、該当する busybox のソースコードから生成してみたらどうなるでしょうか。対象のソースコードを「 <u>http://deb.debian.org/debian/pool/main/b/busybox/busybox_1.30.1.orig.ta r.bz2 」からダウンロードし、</u> /path/to/download/busybox-1.30.1 以下に展開し、再度 SBOM を生成させてみます。 **図** 9を実行し、しばらく待つと、同じように _manifest ディレクトリが生成され、今度は 53,000 以上の行数を持つ JSON ファイルができました( **図** 10)。内容を確認してみると展開したディレクトリに含まれる各ファイルごとに情報が記載されているようです。 

###### **▼図9 ソースコードからSBOMを生成する** 

**$ ./sbom-tool generate -b /path/to/download/busybox-1.30.1 -bc /path/to/download/busybox-1.30.1 -pn busybox -pv 1.30.1 -ps "Norio Kobota" -nsb "https://norio.kobota/sbom-tool-test"** 

###### **▼図10 図9で出力したSBOMは53,000行以上ある** 

**$ wc -l busybox-1.30.1/_manifest/spdx_2.2/manifest.spdx.json 53047 busybox-1.30.1/_manifest/spdx_2.2/manifest.spdx.json** 

さて、今回はここまで。果たして、私たちは市場で必要とされる SBOM の要件を満たす SBOM を生成できているのでしょうか? 

◆ ◆ ◆ 

今回は SBOM に関する歴史と背景、法令に関する動向について紹介し、最後に SBOM ( SPDX )生成ツールを動かし、 SBOM 出力の例を取り上げました。次回は、 SBOM の種類とその詳細について説明し、今回出力した SBOM がオープンソースライセンスコンプライアンス対応やセキュリティ対応に十分活用できるものなのかどうか、また製品開発の現場で管理運用できるものなのかどうか、詳しく見ていくことにします。 

- 注21) European Network and Information Security Agencyの略。EUネットワーク情報セキュリティ機関。 

   - (本文に戻る) 

### **Denoで始めるサーバサイドTypeScript 開発** 

#### **Node.jsを改善した新しいランタイムを体験しよう第3回 DenoでREST APIを作ろう** 

**DenoはNode.jsの問題点を解消するために作られたランタイムであり、整備された支援ツールによる開発体験や、セキュリティを意識したモダンな設計から注目を集めています。本連載で、Denoを活用した快適なTypeScriptサーバサイド開発を体験しましょう。第3回からは、DenoによるREST APIの作成と、それをDeno Deployで公開する方法を紹介していきます。** 

ゴリラ <u>@gorilla0513 skanehira</u> 日野澤歓也(ひのさわよしや) <u>@kt3k</u> Deno Land Inc. 田中優亮(たなかゆうすけ) <u>@yusuktan magurotuna</u> Deno Land Inc. 

#### **はじめに** 

これまでの連載では簡単な grep や Markdown プレビュアーといった CLI の作り方について解説してきましたが、 Deno は CLI だけではなく REST API (以降 API )の実装も可能です。実際に Markdown プレビュアーでは HTTP と WebSocket といった Web の技術を使用していました。今回は、 oak モジュールを使用した簡単な API の作り方、そしてそれを Deno Deplo ~~y~~ <u>注1</u> ~~で~~ 公開する方法について解説していきます。 Deno Deploy は Deno が提供している AWS Lambda のようなサーバレスサービスで、自分でサーバを用意しなくても Deno で実装した API を簡単に公開できます。データを処理する関数の実装から、 HTTP サーバを実装して Deno Deploy にデプロイする方法までを、 2 回にわたって解説します。 

#### **食事データを記録するAPIを実装しよう** 

突然ですが、コロナになってからリモートワークで運動不足により体重が増えてしまって、ダイエットしたい方も多いのではないかなと思います。いろいろなダイエット法がありますが、個人的に最初は自分が普段の食事の栄養価(カロリーやタンパク質の量など) を把握するところから始めるのが良いと思っています。つまり、レコーディングダイエットです。今回は自分の日々の食事データを記録する簡単な API を実装して、ダイエットに役立てていきましょう。 



##### **APIの仕様** 

食事の栄養価を記録したり、確認や編集できたりする必要があるので、 **表** 1の API を今回は実装していきます。また、 API とのデータのやりとりは JSON を使用します。最終的に curl コマンドを使用して、 **図** 1のようにデータの登録と取得を行うことができるようになります。 

###### **▼図1 食事データの登録と取得** 

**# データを登録 $ curl -X POST -H "Content-Type: application/json" http://localhost:8080/meals -d '{"name": "パスタ", "protein": 10, "fat": 10, "carbo": 30, "calorie": 250}'** 

**# 登録したデータ一覧を取得 $ curl -s localhost:8080/meals | jq [ { "id": 1, "name": "パスタ", "protein": 10, "fat": 10, "carbo": 30, "calorie": 250, "created_at": "2023-03-09T05:25:57.148Z" } ]** 

誌面の都合で最低限の機能しか実装していませんが、ぜひこれをベースに拡張して自分だけの API にカスタマイズしてみてください。 



##### **開発環境とプロジェクト構成** 

筆者の環境は次のとおりです。動作確認はこの環境で行っています。また、プロジェクト構成は **図** 2のとおりです。 

- ・ OS:macOS Ventura 

- ・ Deno:v1.31.1 

- ・ Docker Desktop:v4.16.2 

###### **▼図2 プロジェクトの構成図** 

**. ├── .env // DBの接続情報など ├── app.ts // ミドルウェアなどの初期化** 

**├── compose.yaml // DBコンテナの定義 ├── db.ts // DBコネクションの初期化など ├── deps.ts // 依存モジュールの管理 ├── main.ts // HTTPサーバの実行 ├── meal.ts // 食事の登録などの処理 ├── router.ts // HTTPルータの実装 └── scripts └── create_table.ts // テーブルを作成するスクリプト** 



##### **ローカルDBを用意する** 

食事データを記録するためにデータベース(以降、 DB )が必要ですので、まずは Docker で動く DB ( PostgreSQL )を用意します。 DB を直接 OS にインストールしても良いのですが、 Docker を使用すれば簡単に DB を作成できますし、不要になった際も手軽に削除できて便利です。なお、 Deno Deploy で API を動かすときは Supabas ~~e~~ <u>注2</u> ~~の~~ Database 機能を使用します。ではまず、 compose.yaml ファイルを用意して、 DB コンテナの定義を追記していきます( **リスト** 1)。次に **docker compose up -d** で DB コンテナを起動します。無事に立ち上がれば、 **図** 3のコマンドを実行すると結果が返ってきます。 

###### **▼リスト1 compose.yaml:DBコンテナ定義** 

**services: db: image: postgres:15.1 container_name: postgres ports: - 5432:5432 environment: - POSTGRES_USER=postgres - POSTGRES_PASSWORD=postgres - POSTGRES_DB=postgres** 

###### **▼図3 SQLを実行** 

**$ docker exec -it postgres psql -U postgres -d postgres -c "select 1" ?column?** 

**---------1 (1 row)** 

これで DB の用意ができたので、次は食事データを記録するテーブルを作っていきます。今回はテーブルを作成するスクリプト scripts/create_table.ts を用意します。まずは .env ファイルを用意して、 **リスト** 2の DB 接続情報を追記します。 

###### **▼リスト2 .env:DB接続情報** 

**HOST_NAME=localhost PORT=5432 DB_USER=postgres DATABASE=postgres PASSWORD=postgres TABLE_NAME=meals** 

続けて deps.ts に必要なモジュールを追加します( **リスト** 3)。 **リスト** 3❶の postgres モジュールは DB に対して SQL を発行したり結果を取得したりするのに使用します。 **リスト** 3❷の dotenv モジュールは .env ファイルがある場合、 .env に定義されている環境変数を実行時に追加してくれます。これにより Deno.env.get() で .env に定義されている値を取得できるようになります。 

###### **▼リスト3 deps.ts:モジュールを追加する** 

**export * as postgres from "https://deno.land/x/postgres@v0.17.0/mod.ts";** **<mark>←</mark>** <mark>❶</mark> **import "https://deno.land/std@0.178.0/dotenv/load.ts";** **<mark>←</mark>** <mark>❷</mark> 

次に db.ts にコネクションプールとテーブルを作成する関数を追加します( **リスト** 4)。 **リスト** 4❶は DB への TLS 接続を無効化しています。簡易実装のため今回は TLS を無効にしていますが、 API を公開する場合は必ず証明書を使用して TLS で接続するようにしてください。そのほかの実装ポイントに関してはコメントを参照してください。 

###### **▼リスト4 db.ts1:コネクションプールの作成とテーブル作成関数を用意する** 

**import { postgres } from "./deps.ts"; const config: postgres.ClientOptions = { password: Deno.env.get("PASSWORD"), port: Deno.env.get("PORT"), hostname: Deno.env.get("HOST_NAME"), user: Deno.env.get("DB_USER"), database: Deno.env.get("DATABASE"), tls: { enabled: false,** **<mark>←</mark>** <mark>❶</mark> **}, };** **<mark>// 2つのコネクションをもつコネクションプール用意する</mark> const pool = new postgres.Pool(config, 2, true);** 

**<mark>// コネクションプールからコネクションを取得する</mark> export async function getConnection(): Promise<postgres.PoolClient> { return await pool.connect(); }** 

**<mark>// 値を取得できなかった場合はmealsをテーブル名とする</mark> export const TABLE_NAME = Deno.env.get("TABLE_NAME") || "meals";** 

**<mark>// テーブルを作成する関数</mark> export async function createTable() { const conn = await getConnection(); try {** **<mark>// idはDB側で連番生成させるのでSERIALを使用する // created_atも同様にDB側でINSERTした時点の日付を自動生成するため、 // default CURRENT_TIMESTAMPを使用</mark> await conn.queryObject(`** 

**CREATE TABLE IF NOT EXISTS ${TABLE_NAME} ( id SERIAL PRIMARY KEY, name varchar(20) NOT NULL, protein int NOT NULL, fat int NOT NULL, carbo int NOT NULL, calorie int NOT NULL, created_at timestamp default CURRENT_TIMESTAMP ) `);** 

**} finally {** 

**<mark>// 使い終わったコネクションを開放して、コネクションプールに戻す</mark> conn.release(); } }** 

テーブルを作成する関数を用意できたので、続けてテーブル作成スクリプト scripts/create_table.ts を実装していきます( **リスト** 5)。といっても単純に createTable() を呼び出すだけです。これで準備が整いました。 

###### **▼リスト5 scripts/create_table.ts:テーブル作成スクリプト** 

**import { createTable } from "../db.ts";** 

**await createTable();** 

実際に **図** 4のスクリプトを実行して、テーブルを作成します。正常に処理が完了したら、 **図** 5のようにテーブルが作成されます。 

###### **▼図4 テーブル作成スクリプトを実行** 

**$ deno run --allow-read --allow-net --allow-env scripts/create_table.ts** 



これでテーブルの用意ができて、 SQL を実行するしくみも整ったので、次はいよいよデータを操作する API の実装をしていきます。 



##### **APIを実装する** 

最初に meal.ts ファイルを用意して、リクエストボディとレスポンスボディのデータ型を定義します( **リスト** 6)。 

###### **▼リスト6 meal.ts1:リクエストとレスポンスの型を定義** 

**<mark>// レスポンスのデータ型</mark> export type Meal = { id: number; name: string; protein: number; fat: number; carbo: number; calorie: number; created_at: string; };** 

**<mark>// リクエストのデータ型 // idとcreated_atはDB側が自動でinsertしてくれるためそれを除外</mark> export type MealEntry = Omit<Meal, "id" | "created_at">;** 



##### **データを処理する関数を実装する** 

ここでは、冒頭に説明した各 API に対応する関数を実装していきます。これらの関数は、先ほど実装した getConnection() を使用して DB に対する SQL 文を発行することでデータ処理を行います。 

SQL を発行する際には、コネクション取得と使い終わったコネクションをプールに戻す処理(以後、リリース)が必要です。各関数でこの処理を繰り返し実装するのは避けたいので、まずは db.ts にコネクション取得からコネクションをリリースしてくれるヘルパー関数 withConnection() を用意します。 **図** 6は、ここで **リスト** 3の db.ts に追加するコードの差分です。 withConnection() に渡す callback 関数 run() がコネクションを引数にとり、関数内でコネクションを使用して SQL を発行します。そして run() の処理が完了したら finally で解放します。 

###### **▼図6 db.ts** 2: **withConnection()を用意** 

**diff --git a/db.ts b/db.ts index d123db8..a1b9af9 100644 --- a/db.ts +++ b/db.ts @@ -37,3 +37,14 @@ CREATE TABLE IF NOT EXISTS ${TABLE_NAME} ( conn.release(); } } + +export async function withConnection<T>( +  run: (conn: postgres.PoolClient) => Promise<T>, +): Promise<T> { +  const conn = await getConnection(); +  try { +    return await run(conn); +  } finally { +    conn.release(); +  } +}** 

ヘルパー関数を使うことによってデータ処理の実装に集中できるようになったので、続けて meal.ts に食事データ一覧を取得する関数 

list() を実装していきます。 **リスト** 6を **図** 7のように変更します。 

###### **▼図7 meal.ts2:食事データ一覧を取得する処理** 

**diff --git a/meal.ts b/meal.ts index af06a0e..da41d97 100644 --- a/meal.ts +++ b/meal.ts @@ -1,3 +1,5 @@ +import { TABLE_NAME, withConnection } from "./db.ts"; + export type Meal = { id: number; name: string; @@ -9,3 +11,10 @@ export type Meal = { }; export type MealEntry = Omit<Meal, "id" | "created_at">; + +export async function list(): Promise<Meal[]> { +  return await withConnection(async (conn) => { +    const resp = await conn.queryObject<Meal>(`SELECT * from ${TABLE_NAME}`); +    return resp.rows; +  }); +}** 

コネクション conn は queryObject() 関数を持っていて、この関数の第 1 引数に SQL 文字列を渡すことで最終的に SQL が実行されます。基本的に SQL を実行するときは queryObject() を使用していきます。レスポンス resp には rows フィールドがあり、これが SELECT したデータが入った配列となっています。次に meal.ts に食事データを登録する関数 record() を実装します( **図** 8)。 

**▼図8 meal.ts** 3 **: 食事データを登録する処理** 

**diff --git a/meal.ts b/meal.ts index da41d97..c604986 100644 --- a/meal.ts +++ b/meal.ts @@ -18,3 +18,14 @@ export async function list(): Promise<Meal[]> { return resp.rows; }); } + +export async function record(meal: MealEntry): Promise<void> { +  await withConnection(async (conn) => { +    await conn.queryObject( +      `INSERT INTO ${TABLE_NAME} +      (name, protein, fat, carbo, calorie) VALUES +      ($name, $protein, $fat, $carbo, $calorie)`, ←** ❶ **+      meal, +    ); +  }); +}** 

**図** 8❶の部分ですが、これは実行する SQL 文で変数を使用する場合の postgres モジュール独自の書き方です。 **$** のあとに第 2 引数のオブジェクト(今回は meal )のフィールド名を書くことで、最終的に PostgreSQL のプリペアド文に置換され実行されます。たとえば meal が **{name: "パスタ", ...}** の場合、最終的には **リスト** 7のような SQL が実行されます。 

###### **▼リスト7 実際に実行されるSQL文の例** 

**PREPARE insert_meals (int, varchar, ...) AS INSERT INTO meals VALUES($1, $2, ...); EXECUTE insert_meals (nextval('meals_id_seq'), 'パスタ', ...);** 

なお、プリペアド文を実行する理由ですが、引数の値を適切にエスケープして SQL インジェクションを避けるためです。たとえば、 

**INSERT INTO ${TABLE_NAME} (name) VALUES(nextval('meals_id_seq'), ${meal.name})** 

というように書いてしまうと、引数の値は適切にエスケープされません。この場合、 meal.name に **'パスタ'); DELETE FROM meals; --"** という値が入ると、次の 2 つの SQL 文が実行されて、データはすべて削除されてしまいます。 

**INSERT INTO meals VALUES (nextval('meals_id_seq'), 'パスタ'); DELETE FROM meals; --)** 

続けて、 meal.ts にデータを更新する関数 update() を実装します( **図** 9)。更新したいカラムに対応するフィールドだけがあればいいので、 **図** 9❶の Partial で各フィールドをオプショナルにします。 **図** 9❷ は各フィールドから SQL 文の一部を生成しています。たとえば **{name: "トマト", protein: 3}** の場合、 SQL の **name = $name, protein = $protein** の部分を生成しています。 

###### **▼図9 meal.ts** 4: **update()関数を実装** 

**diff --git a/meal.ts b/meal.ts index 17977d9..2e08cd3 100644 --- a/meal.ts +++ b/meal.ts @@ -33,3 +33,17 @@ export async function record(meal: MealEntry): Promise<void> { }); } + +export async function update( +  id: number, +  meal: Partial<MealEntry>, ←** ❶ 

**+): Promise<void> { +  await withConnection(async (conn) => {** 



<!-- Start of picture text -->
+    const columns = Object.keys(meal).map((k) => `${k} = $${k}`).join(  ← ❷<br>+      ", ",<br>+    );<br>+<br>+    const data = { id, ...meal };<br>+    await conn<br>+      .queryObject(`UPDATE ${TABLE_NAME} SET ${columns} WHERE id = $id`,<br>data);<br>+  });<br>+}<br><!-- End of picture text -->

最後に meal.ts にデータを削除する remove() 関数を実装します( **図** 10)。これで一通り、データ処理の部分の実装が終わりました。 

**▼図10 meal.ts** 5: **remove()関数を実装 diff --git a/meal.ts b/meal.ts index 2e08cd3..53c7e7f 100644 --- a/meal.ts +++ b/meal.ts @@ -58,3 +58,9 @@ export async function update( .queryObject(`UPDATE ${TABLE_NAME} SET ${columns} WHERE id = $id`, data); }); } + +export async function remove(id: number): Promise<void> { +  await withConnection(async (conn) => { +    await conn.queryObject(`DELETE from ${TABLE_NAME} where id = $id`, { id }); +  }); +}** 

###### ◆ ◆ ◆ 

次回は HTTP サーバの実装と、 Deno Deploy にデプロイして API を公開する方法について解説していきます。お楽しみに。注2) Supabaseについては次回の連載で解説します。https://supabase.com 

(本文に戻る) 

### **SD BOOK REVIEW** 

# **プログラマー脳** 



Felienne Hermans 著、水野貴明翻訳、水野いずみ監修 B5変形判/288ページ 

3,000円+税秀和システム 

ISBN=978-4-7980-6853-4 

コードを読む/書くとき、プログラマーの「脳」は、どのように働いているのか? 本書はそんな疑問に、人間の脳が情報をどのように記憶・思考しているのかを研究する「認知科学」の観点から答えている。短期記憶、⻑期記憶、ワーキングメモリといった脳の記憶パターンの分類や、コードの読みづらさと関連する「認知的負荷」の概念を通じて、「読みやすいコードとは何か」を考察し、プログラミングを効率化する方法を導き出している。とはいえ、認知科学によって斬新な効率化テクニックが見つかるわけではなく、紹介されるのは、よく知られる「コメントを書く」、「変数名を適切に設定する」といったものが多い。しかし、そんな当たり前に行ってきたことが、これまで脳への負荷をいかに少なくしていたのかという新しい発見が得られるはずだ。 

# **AWSではじめるクラウドセキュリティ** 



松本照吾、桐谷彰一、畠中亮、前田駿介著 A5判/320ページ 2,680円+税テッキーメディア ISBN=978-4-910313-03-0 

本書は、AWSにおけるセキュリティ対策を学びながら、併せてクラウドサービス一般におけるセキュリティの基礎を学べる書籍だ。本書は「クラウドセキュリティの基本」→「AWSにおけるセキュリティ対策」→「セキュリティ対策のハンズオン」という3部構成になっている。したがって、最初に考え方を押さえ、AWSでの具体的なセキュリティ方策を学んだあと、最後に手を動かすという流れで学べるのだ。業務や各種対策に追われて1冊通して読む余裕がない、あるいは現場で関わっていないがポイントだけ知っておきたいという読者もいるかもしれない。その場合は、第1部をさっと一読して、必要に応じて第2部、第3部を読み進めていくとよいだろう。クラウドサービスを運用している人なら、手元に置いておきたい1冊だ。 

# **今さら聞けない暗号技術&認証・認可** 



大竹章裕、瀬戶口聡、庄司勝哉、光成滋生、谷口元紀、くつなりょうすけ、ほか著 

B5判/160ページ 

1,980円+税技術評論社 

ISBN=978-4-297-13354-2 

本書は、弊誌のセキュリティ技術に関する過去記事を再編集して収録した書籍だ。それゆえに理論から実践まで「いいとこ取り」の内容となっている。公開鍵暗号、電子署名、デジタル証明書などの章では、理論やしくみを図や数式で解説する。SSL/TLS、 SSHの章では、ユースケースやコマンド例とともに使い方を説明する。OAuth、 OpenID Connectの章では認証認可のフローやリクエスト/レスポンスの例を示しながら解説する。このように、章ごとにまったく違うが、各テーマに適した方法で解説される。理論も使い方も運用も1冊で把握したいという効率重視の方に本書はピッタリだが、上に挙げたセキュリティ用語の理解があやふやだという方にも本書は向いている。通読するだけで復習となり、その知識を確かなものにできるだろう。 

# **データベース速攻入門** 



堀内康夫、徳尾秀敬、とみたまさひろ、株式会社リブセンス、村田太、ほか著 B5判/192ページ 2,200円+税技術評論社 ISBN=978-4-297-13362-7 

データベースは、Webサイトやアプリケーション、業務システムなど、さまざまなところに組み込まれている。今や開発者にとって避けては通れない知識の1つと言える。本書は、データベースの基本知識を解説した弊誌の特集記事をまとめた書籍だ。データを整理するための手法であるデータモデリングについてはプロダクトによらない普遍的な解説を行っている。データ操作言語であるSQLの記法と50題の演習問題により、 SQL力を磨くための知識も身につくだろう。さらに、SQLデータベースの中でもよく使われるMySQLについて、アプリケーション開発者が押さえておきたい5つの特徴も解説している。Amazon RDSとAmazon DynamoDBの使い分けポイントまで紹介しているので、いざというときに役に立つ知識が満載だ。 

# **新連載** 

### **メールセキュリティ対策** の **現場第 1 回なりすましメールのしくみと防止技術を知る** 

「電子メール」は生活に不可欠なものとして数十年定着している一方、なりすましメールなど悪用されるケースも絶えず、事業者はその対策に日夜追われています。本連載では、メールセキュリティ対策を担当するエンジニアが、悪用メールのしくみやそれを防ぐしくみについて解説します。今時のメールセキュリティ対策の現場をのぞいてみましょう。中村成陽(なかむらまさはる) ヤフー株式会社 

##### **はじめに** 

ヤフー株式会社のエンジニアの中村成陽です。筆者の担当サービスは「 Yahoo! メール」で、開発を担当しています。所属チームがスパムメール対策をしていることから、日々 Yahoo! メールを標的とするフィッシングメールなどと戦っています。今回の連載では「メールセキュリティ対策の現場」と題し、筆者の業務での経験も交えながら次の 2 点についてお話ししたいと思います。 

- ・スパムメールに対する対策にはどういったものがあるのか 

- ・正規のメール送信者、あるいはメールサービスプロバイダーは何に気をつけなければいけないのか 

##### **そもそも「なりすましメール」とは何か** 

メール(電子メール)はインターネットの世界の ID として、⻑年にわたって必要不可欠なものとなっています。しかし、メールの通信に使われる SMTP というプロトコルが古いために、そのままではセキュリティのことはほとんど考えられていません。また、広く使だまわれているからこそ、このメールを悪用し利用者を騙す行為はあとを絶ちません。 

**なりすましメール** とは、送信者自身のものではない「うそのメールアドレス」を詐称、つまり別のメールアドレスからのものであると偽って送られたメールのことです。 

###### **<u>2つの送信元メールアドレス</u>** 

送信元メールアドレスには、手紙でいえば封筒に記載する差出人となる EnvelopeFrom アドレス( SMTP で MAIL コマンドにて設定するアドレス)と、中身の便箋に記載する HeaderFrom アドレス(メールデータの From ヘッダに設定するアドレス)の 2 つがあり、これらそれぞれに別のアドレスを設定できます。 

Yahoo! メールの Web 版やアプリを含め、メールソフトで表示される送信元メールアドレスは通常は HeaderFrom アドレスのほうなので、ここに詐称したいメールアドレスを設定することで、なりすましメールを送信することが可能となってしまいます。 

###### **<u>古いメールのしくみを悪用する「フィッシングメール」</u>** 

そもそも、電子メールというしくみが考案されたのは今から 50 年以上も前の話です。メールをやりとりするための共通プロトコルである SMTP( Simple Mail Transport Protocol )が RFC で標準化されたのも 30 年ほど前のことです。つまり、メールはセキュリティを考慮する必要がない時代に生まれたものであることから、このようななりすましが容易にできてしまうしくみとなっています。このしくみを悪用されてしまっているのが、いわゆる **フィッシン** かた **グメール** と呼ばれるものです( **図** 1)。大手の EC サービスなどを騙ってメールを送り、メールから遷移したリンク先で ID やパスワードを盗むといった手口が横行してしまっています。なお、このヘッダ From のアドレスには、 

- ・詐称元となっている本物のドメインを指定する 

- ・スパムメール配信者(以降 **スパマー** と呼ぶ)が所有しているドメインを指定する 

- ・別のサービスや企業のドメインを設定するなど、さまざまなケースがあります。それぞれスパマーにとってメリット/デメリットがありますが、その詳細については後述します。ちなみに手法としては、 DisplayName という From ヘッダに指定して「メーラーの From アドレス欄に表示される文字列を指定できるしくみ」を悪用し、本物のサービス名を詐称しているケースがほとんどです。 

##### **フィッシングメールとその被害** 

このフィッシングメールについては、近年(とくにコロナ禍以降)急増しているというデータがあります。「フィッシング対策協議会」が毎月発表している「フィッシング報告状況注1 ~~」~~ によると、同協議会に寄せられたフィッシング報告件数が、コロナ禍前の 2019 年 9 月は 6,218 件だったのに対し、 2022 年 9 月には 102,025 件と約 16 倍以上となっています( **図** 2)。 



<u>https://www.antiphishing.jp/report/monthly/202209.html</u> 

詐称元となっているサービスや企業は、先述した大手 EC サービスをはじめ、クレジットカード会社、金融機関、運送会社など、スパマーにとって情報を抜き取るとメリットとなるものが多いです。残念ながら、 Yahoo! メールアドレス宛にもこういったフィッシングメールの送信は行われており、毎日非常に多くのメールの受信をブロック、あるいは迷惑メール判定をしています。送信方法や内容も日々巧妙化してしまっているというのが現状です。 

##### **なりすましメールを防ぐ技術** 

なりすましメールに対して指をくわえて見ているわけにはいきません。対応策として生まれたのが **送信ドメイン認証** と呼ばれる技術です。 SMTP の仕様としては詐称できてしまう送信元のメールアドレスですが、確実にこの From アドレスのドメインから送られたメールであること(第三者から送られたメールではないこと)を保証するための技術が、送信ドメイン認証です。ドメインの所有者のみが DNS レコードを編集可能であるという点が、送信元の証明になります。その認証アプローチの違いによりいくつか種類があります。ここでは次の 2 つを紹介します。 

- ・ SPF(Sender Policy Framework) 

- ・ DKIM(DomainKeys Identified Mail) 

##### **SPF認証** 

SPFは、メールの送信元 IP アドレスから「本物かなりすましか」を判断する技術です。 SPF 認証の流れを **図** 3に示します。メール送信を行うドメイン( EnvelopeFrom アドレスに設定するもの)において、 DNS の TXT レコードに対し、決められた書式でそのドメインからのメールが送信される IP アドレスを記載しておきます。これをSPF **レコード** と言います。受信側はこのレコードを参照し、メールの送信元 IP アドレスと照らし合わせることで、なりすましメールであるかどうかを判断します。書式さえ守れば IP アドレスを記載するだけで完了しますので、比較的手軽に導入できます。その反面、転送されたメールでは正しく認証ができない場合があります。 

###### **<u>Yahoo! JAPANにおけるSPFレコード</u>** 

「 Yahoo! JAPAN 」の各サービスからのお知らせメールなどが配信される mail.yahoo.co.jp というドメインでは、 **図** 4のように SPF レコードが宣言されています( 2023 年 3 月現在)。さりげなく grep していますが、「 v=spf1 」から始まる TXT レコードが SPF レコードであり、受信側はこの部分を参照して SPF による認証を行います。厳密には SPF のバージョンを示しているのですが、現状こちらしかありませんので、おまじないのように「 v=spf1 」と記載いただければ大丈夫です。 

###### **▼図4 mail.yahoo.co.jpにおけるSPFレコード** 

**$ host -t txt mail.yahoo.co.jp | grep "v=spf1" mail.yahoo.co.jp descriptive text "v=spf1 include:bulk-spf.yahoo.co.jp include:gisa-spf.yahoo.co.jp include:relay-spf.yahoo.co.jp include:_spf.salesforce.com include:_spf.willap.jp ~all"** 

このあとに、実際にそれぞれの送信元としてマッチした場合の認証結果について記載をしてきます。「 include: 」とあるのは「ここに設定されているドメインで指定されている SPF レコードも再帰的に確認を行う」という意味です。送信元の IP アドレスがこの include 先に含まれている場合でも、 SPF の認証は成功となります。この include 先にあるドメインの SPF レコードは **図** 5のようになっています( 2023 年 3 月現在)。「 ip4:124.83.128.0/17 」の部分がこの IPv4 アドレスを条件指定しており、このようにサブネットでの記載も可能です。この範囲の送信元であれば認証成功、つまり正規のメールとして扱うという意思表示となります。 

###### **▼図5 include先にあるドメインのSPFレコード** 

**$ host -t txt bulk-spf-west.yahoo.co.jp | grep "v=spf1" bulk-spf-west.yahoo.co.jp descriptive text "v=spf1 ip4:124.83.128.0/17 ip4:114.111.64.0/18 ip4:114.110.48.0/20 ip4:183.79.0.0/16 ~all"** 

なお、この「 ip4 」の前には qualifier という処理を指定できる識別子を指定できます。 qualifier には **表** 1のような種類があります。つまり、先述の内容は「 +ip4:124.83.128.0/17 」として「 + 」を付与しても同じ意味となります。また「 all 」というのは、その名のとおり「それまでに指定のないものすべて」という意味になるため、「 ~all 」は qualifier の意味と組み合わせて「その他は不正なメールとしてください」という意味です。余談ですが、 Web サーバでのみの利用ドメインなど、メール送信を行わないドメインについては、次のように SPF レコードを宣言しておくと良いです。 

**v=spf1 -all** 

「 -all 」は「すべて不正なメールで、配信も拒否される可能性がある」という宣言になりますので、「このドメインからメールが送られることはありません」と明示することが可能となります。 

**DKIM認証** 

DKIMは、送信者がメールヘッダ( DKIM-Signature ヘッダ)に電子署名を付与し、受信側でそれを検証することで認証を行う技術です。 DKIM 認証の流れを **図** 6に示します。送信側では秘密鍵で署名してメールを送り、その検証に必要な公開鍵は SPF の送信元 IP アドレスと同様、送信元ドメインの TXT レコードで公開しておき、受信側はそれを参照して認証を行います。そのしくみ上、メールの改ざんについても検知をすることが可能です。 



DKIM には **セレクタ** と呼ばれる公開鍵の識別ができるしくみがあります。これにより、 1 つのドメインに対して複数の公開鍵で運用することが可能となります。通常、鍵交換を行う際は、別のセレクタに宣言を行いそこに切り替えるという方法がとられます。送信者が電子署名を付与する DKIM-Signature ヘッダではこのセレクタの指定も必要となり、受信側はドメインだけでなくこのセレクタも含めて適切な公開鍵を参照し、認証を行います。具体的には、公開鍵は「セレクタ <u>._domainkey. ドメイン」の</u> TXT レコードに宣言します。 DKIM レコードの例は **図** 7のとおりです。「 v=DKIM1; 」は SPF と同様バージョンを指します。こちらもおまじないのように記述してください。「 k=rsa 」は電子署名の鍵の形式を指します。サポートしているのは RSA のみですので、このように記載してください。省略もできます。「 p= 」のあとが公開鍵のデータで、 Base64 エンコードしたものを保持します。当然ながら、こちらは必須となります。 

###### **▼図7 DKIMレコードの例** 

**$ host -t txt セレクタ._domainkey.ドメイン v=DKIM1; k=rsa; p=xxxxxxxxx....** 

署名方法には次の 2 種類の方式があります。 

- **・作成者署名** :HeaderFromアドレスのドメインで署名を行う 

- **・第三者署名** :別のドメインで署名を行うどのドメインで署名を行うかは DKIM-Signature ヘッダの「 d 」というタグに指定します。なお、セレクタもこのヘッダに指定します( s タグ)。多くのメール配信サービスでは、デフォルトで付いている DKIM 署名は送信者自身が設定する必要のない第三者署名であることが多いです。しかし、メール送信者が保有するドメインでの電子署名をメールに付与する作成者署名のほうがより堅牢な署名方式であると言えます。後述する予定の「ブランドアイコン」という Yahoo! メールでの取り組みでは、この作成者署名が導入の条件の 1 つとなっています。 



SPF や DKIM については Yahoo! メールでも従来より導入されており、国内外問わずほとんどのメールサービスで取り入れられています。最低限いずれか一方でも正しく設定した状態でメールを送信することが非常に重要です。これらが設定されていない、あるいは認証に失敗しているメールについては、一般的に受信側でのレピュテーションは著しく低下してしまいます。みなさんの正規のメールを確実に届けるためにも、ぜひこれらは設定をするようにお願いします。 

**まとめ** 

今回はなりすましメールとはそもそも何なのか、そしてそれらを防ぐための認証技術である SPF と DKIM について説明しました。これらによりそれぞれの観点でメールのなりすましを判断できるようになりましたが、認証に失敗したメールの扱いが受信側に委ねられているなど、いくつか課題もあります。そういった課題を解決するために生まれた認証方式について、次回は解説していきたいと思います。 

### **Google Cloudを軸に実践する SREプラクティス** 

##### **信頼性を高めるサービス基盤の構築** 

**第 2 回クラウドネイティブなインフラの TerraformによるIaC化** 

**現代のソフトウェア開発で求められる「スピード」「高い柔軟性」「質の高いサービス」に応えるため、Googleから提唱されたSRE(Site Reliability Engineering)。本連載では、信頼性の高いサービスを提供するために、SREプラクティスを実践するエンジニア陣が、Google Cloudを軸に構築するサービス基盤やその技術を解説します。** 



小森裕介(こもりゆうすけ)キャディ株式会社 



<u>@littleforest12</u> 



キャディ株式会社 Platformグループ <u>https://caddi-inc.com/</u> 



#### **はじめに** 

第 1 回(本誌 2023 年 4 月号)では、キャディにおける Google Cloud を中心としたサービス基盤の全体像を紹介し、信頼性向上のために筆者らが心掛けている技術選定基準について触れました。今回からは数回にわたって、「 Terrafor ~~m~~ <u>注1</u> ~~」~~ を中心とした IaC ( Infrastructure as Code )の実践例を紹介予定です( **図** 1)。今回は「インフラの信頼性」という側面で考える IaC の重要性と、 Google Cloud を中心としたクラウドベースのシステムの IaC において、なぜ Terraform との相性が良いのかを説明します。 

#### **クラウドネイティブにおけるIaCの必要性** 

システムのインフラをコードとして記述すること、すなわち Infrastructure as Code ( IaC )は、すでに当たり前のプラクティスとなりつつあります。たとえば、オンプレミス上のシステムや、パブリッククラウド上で Amazon Web Services ( AWS )の EC2 など仮想マシン( VM )ベースのシステムを構築する際は、 Ansible を使って IaC を実現する事例が増えてきました。また、 AWS CloudFormation を利用して AWS 上のリソースを作成している方も多いでしょう。 



##### **手作業によるインフラ管理の課題** 

AWS 、 Google Cloud 、 Microsoft Azure などのパブリッククラウドは、いずれも Web 上の UI から VM インスタンスを起動するなどの操作が可能で、手軽なことがメリットの 1 つです。その一方、クラウドネイティブなアーキテクチャで実運用可能なシステムを構築するには、さまざまなサービスを組み合わせる必要があり、膨大な設定が必要となります。これらを WebUI からいわゆる「ポチポチ」で設定するのは非現実的で、言うまでもなく次のような問題が生じます。 

- ・構築作業に時間がかかる 

- ・設定内容を記録しにくい 

- ・全体の見通しが悪く、レビューしにくい 

WebUI によるインフラ構築は手軽であるため、学習やコンセプト検証など、試行錯誤が主体の作業とは相性が抜群です。しかし、実運用するシステムを構築・維持するうえで WebUI だけが設定手段となっている場合は、従来のサーバ構築よりも非効率になってしまいます。また、 WebUI では設定内容の記録が困難なことも大きな問題です。ここから、さまざまな問題が派生します。通常、システム運用時には、開発環境、テスト環境、本番環境など、複数の環境が必要になります。これらを手作業で同じように構築するのは手間がかかりますし、ミスも発生します。記録が困難であることからノウハウを残しにくく、インフラの品質も属人的スキルに依存しがちになります。変更管理ができないことから、インフラの変更意図がわかりにくく、障害発生時に元の構成に戻すことも困難でしょう。設定内容のレビューをするにしても、 Web 上の管理画面から確認が必要で、大規模なシステムでは確認箇所が広範囲にわたります。このように見通しが悪い状況ではレビュー効率が悪く、問題を適切に見つけることも困難です。 



##### **IaCによる解決** 

そこで必要となるのが、 IaC の考え方です。旧来なら、シェルスクリプトなどで OS の状態を変更したり、設定ファイルを書き換えたりといった手続き的な処理の自動化も、広義の IaC と捉えることもできました。すなわち、「サーバ構築手順書」をそのままプログラム化するような考え方です。また、主要なクラウドサービスでは、インフラ操作のための CLI コマンドが提供されています。 AWS であれば **aws** 、 Google Cloud では **gcloud** 、 Microsoft Azure なら **az** といったコマンドです。 WebUI の代わりにこれらの CLI を利用することで、作業効率や記録の問題はある程度解決できるでしょう。しかし、 CLI コマンドによる構築は手続き的です。このため、見通しの悪さやレビューのしにくさの改善に対しては、あまり寄与しません。現代の理想的な IaC としては、インフラの「状態」を宣言的に記述することが考えられます。ツールによって、記述されたコードの状態と等しくなるように、インフラを自動設定するのです。 

#### **インフラの信頼性に寄与するIaC** 

さて、 IaC というと「自動化による効率改善」というイメージが強いかもしれません。もちろんこれは大きなメリットであり、「開発」「テスト」「本番」など、複数環境の構築も容易になります。しかし、筆者らがそれ以上に大きなメリットと捉えているのが、次に挙げるような信頼性向上への寄与です。・レビューしやすくなる 

- ・ツールによるチェックができる 

- ・変更管理ができる 

- ・再現性・自動デプロイにつなげられる 

- ・再利用化による知見が共有できる 



##### **プルリクエストによる相互レビュー** 

IaC 化されたコードは GitHub などのソースコードリポジトリで管理でき、アプリケーションコードと同じようにプルリクエストによるレビューが可能になります。これによって、構築や変更前にチェックができ、品質向上につながります。 



##### **ツールによる自動チェック** 

さらに、チェックツールを導入することでレビュー自体を半自動化できます。たとえば、筆者らが IaC ツールとして採用している Terraform では、 tflin ~~t~~ <u>注2</u> ~~や~~ tfse ~~c~~ <u>注3</u> ~~と~~ いったチェックツールがあります。 tflint では、設定内容の妥当性、命名規則、ベストプラクティスに従っているかなど、プログラミング言語の静的チェックツールと同じようなチェックが可能です。 tfsec では、セキュリティ上問題となる設定がないかの確認ができます。このような自動チェックを事前に実施することで、人間によるレビューはより本質的な点に絞ることができ、作業効率と精度の両面に寄与できます。 



##### **変更管理が可能** 

コードリポジトリで変更管理が可能になることで、構成変更に起因する障害が発生したとき、ロールバックもしやすくなります。クラウドネイティブなシステムでは、アプリケーションとインフラ構成が密接に絡みます。たとえば、アプリケーションがパブリッククラウドの提供するキューイングサービス( AWS なら SQS 、 Google Cloud なら Cloud Pub/Sub など)を利用するとします。その場合、アプリケーションコードと、キューを構成するインフラコードのバージョンには整合性が求められるでしょう。インフラが IaC 化されてリポジトリ上で管理されていれば、アプリケーションとインフラの整合性を取るのが容易になります。 



##### **再現性** 

IaC 化の大きなメリットは再現性が得られることですが、これには信頼性向上という側面もあります。たとえば、誤って環境を壊してしまっても元に戻すことができるので、ダウンタイムを最小化できます。筆者らは過去に、あるプロダクトの開発環境において不注意で Kubernetes ( K8s )クラスタを削除してしまったことがありました。幸いにもこれらは IaC 化されていたため、比較的短時間で復旧できました。また、ディザスタリカバリの観点でも事業の継続性に寄与します。 



##### **再利用化による知見の共有** 

手作業によるインフラ構築は、ノウハウが担当者の中に閉じがちで暗黙知となりやすいです。 Terraform や Ansible など、たいていの構成管理ツールでは、コードをモジュール化して再利用する機能が提供されています。ノウハウをモジュール化して再利用することで、インフラの品質向上が望めます。たとえば、 Terraform の Google Cloud 向けモジュールでは、管理者がシステムにアクセスするための「踏み台サーバ( Bastion server )」を構築するためのモジュールである bastion-host が公開されています注4 ~~。~~ このモジュールでは、ネットワークや認証のしくみもセットで構築してくれるので、踏み台サーバ構築のノウハウが再利用できる状態と言えます。 

#### **IaCのデメリット** 

一方で、次のような点は IaC のデメリットと捉えられることもあります。 

- ・初期構築が大変 

- ・変更コストがかかる 



##### **初期構築の大変さ** 

初期構築について、とくに慣れない段階では時間がかかります。筆者らも開発チームから IaC について相談を受ける際は、開発初期段階では無理に IaC 化しなくても良いとアドバイスしています。 

IaC 化を進めるには、手作業で構築してから、ツールでコード生成するのが 1 つの方法です。たとえば Terraform の場合、主要なパブリッククラウドに対応している Terraforme ~~r~~ <u>注5</u> ~~や~~ 、 Google Cloud であれば **gcloud resource-config** コマンド注6 ~~と~~ いった選択肢があります。 Terraformer は、利用するクラウドサービスに対応する Terraform プロバイダの手動インストールが必要という手間はありますが、さまざまなクラウドからコード生成できる点が魅力的です。 **gcloud resourceconfig** は、普段から gcloud コマンドを利用していれば、追加インストールが不要なので、手軽に利用できます。筆者も現段階では両者を軽く試用した程度ですが、 Terraformer のほうが Terraform コードをきちんとファイルに分けて出力してくれるため、見通しが良さそうと感じています。いずれにせよ、自動生成されたコードは土台と割り切ってしまい、それらを参考にしながらコードをきれいにしていく必要はあるでしょう。また、組織として知見が溜まってくれば、ベースとなるコードを共有することで、イニシャルコストを下げられそうです。筆者らもこの点に関してはまだ手探りの段階です。 



##### **変更コスト** 

IaC 化されたインフラの変更コストは、信頼性向上との裏返しではあります。インフラのコードを Git リポジトリで管理し、プルリクエストをレビューしてから反映するというプロセスは、 WebUI から変更するのに比べると手間がかかる印象を持たれるでしょう。実際のところ、次回以降で説明するように CI/CD パイプラインを構築することで多くの作業は自動化でき、それほどの手間はかからなくなります。しかし、とくに慣れないうちは心理的ハードルが高くなるのは否めません。たとえば「パフォーマンス調整のために K8s クラスタ上でノードプールのインスタンス数を変更する」といったケースでも、プルリクエストとレビューが必要になります。一方で、プルリクエストに変更理由を記載して妥当性をレビューできるのは、インフラの信頼性を保つうえで大きなメリットです。このため、信頼性とアジリティ(機敏性)のバランスが重要です。キャディのプロダクトにおけるインフラについては、 IaC による管理を基本としつつも、「検証のための一時的な変更などは、開発環境に限って WebUI からの変更を OK とする」というルールを導入しました。このようにして、信頼性と変更コストのバランスをとっています。 

#### **Terraformを採用する理由** 

筆者らが IaC ツールとして Terraform を採用する大きな理由は、次の 2 点です。 

- ・クラウドサービス上のリソースが扱える 

- ・さまざまなクラウドサービスに対応している 



##### **クラウドサービス上のリソースが扱える** 

IaC ツールというと Ansible が有名であり、本誌でもたびたび取り上げられています。しかし、 Ansible と Terraform では、 **図** 2のように位置付けが少し異なります。 



Ansible の想定している主な役割はサーバ設定であり、 OS よりも上のレイヤがターゲットです。一方 Terraform は、クラウド上のリソース作成や設定変更を主な役割と想定しており、 Ansible がカバーするレイヤをメインターゲットとしていません。 Ansible と Terraform はそれぞれ基本的なしくみも異なります。 Ansible は設定対象のサーバ(マネージドノードと呼ばれる)に SSH でログインして「モジュール」と呼ばれる小さなプログラムを送り込み、そのモジュールがマネージドノードを設定します( **図** 3 **上** ) 。一方、 Terraform ではターゲットとなるクラウドサービスの Web API を呼び出すことで、リソース作成や変更を実現します( **図** 3 **下** )。このような設計思想の違いはありますが、互いの領域が重なっていないわけではありません。 

Ansible では、 AWS や Google Cloud のリソースを構築するためのモジュールが提供されており、クラウドサービスのリソース管理もできます。一方、 Terraform でも Provisioners という機能で Ansible のようなサーバの内部の設定変更も実現できます。ただし、 Terraform に関しては 

Provisioners のドキュメント注7 ~~に~~ よると、あくまでも「最後の手段」と位置付けられていることから、主要な使い方でないことがわかります。なお、各クラウドサービスへの対応状況については注意が必要です。筆者らが利用している Google Cloud については、 Terraform では Google と HashiCorp の Terraform チームがメンテナンスする公式の Provide ~~r~~ <u>注8</u> ~~が~~ 提供されており、頻繁にメンテナンスされています。一方 Ansible の Google Cloud 向けコレクショ ~~ン~~ <u>注9</u> ~~の~~ 開発状況は、 GitHub の履歴を見る限りそれほど活発ではなかったものの、 2022 年 12 月からリリース頻度が早くなっており、今後に注目です。 



##### **幅広いクラウドサービスへの対応** 

IaC ツールを選定する際、まずクラウドサービス固有のツールが選択肢に挙がるでしょう。 AWS CloudFormatio ~~n~~ <u>注10</u> ~~や~~ Google Cloud Deployment Manage ~~r~~ <u>注11</u> ~~な~~ どです。これらのツールは、当該クラウドサービスとの高い親和性が利点である一方、幅広いクラウドサービスには対応できないという弱点があります。 

Terraform には「 Provider 」というプラグイン機構があり、クラウドサービスの対応はそれぞれの Provider によって実現されています。 Provider は、 Terraform Registr ~~y~~ <u>注12</u> ~~で~~ 公開されており、コード内で宣言するだけで、 Terraform が自動的に Provider をダウンロードしてくれます。このようなしくみから、 Provider があれば、さまざまなクラウドサービスに対応できます。もちろん、必要に応じて Provider の自作もできます。また、 AWS 、 Google Cloud 、 Azure など主要なパブリッククラウドについては、 HashiCorp 社によるオフィシャル Provider が提供されているため、安心して活用できます。 Google Cloud では、 Terraform の利用に関する公式ドキュメント注13 ~~も~~ 充実しています。キャディのインフラでは、 Google Cloud を中心としつつも、さまざまな XaaS を組み合わせているため、インフラをコード化するツールとして Terraform が最適といえるのです。 

#### **まとめ** 

今回はまず、信頼性の高いインフラを運用する基盤となる IaC の考え方を紹介しました。そして、 Google Cloud を中心としたクラウドネイティブなインフラを構築している弊社にとって、 IaC ツールとして Terraform が最適であると判断した理由を解説しました。今後は GitHub Actions と Terraform の組み合わせでインフラの CI/CD 実践例を紹介する予定です。次回はまず、 Terraform に触れたことがない方向けに Terraform の基本を解説します。お楽しみに。 

##### **Column Ansibleと比較してTerraformが良い点** 

筆者自身、キャディへ入社する前はAnsibleを⻑く利用していました。いささか主観的ですが、双方を使った経験から、Ansibleと比べてTerraformのほうが良いと感じる点を紹介します。なお、筆者が最後にAnsibleを触ったのは2021年秋ごろなので、その後改善されている点があればご了承ください。 



###### **純粋に宣言的な書き方ができる** 

Ansibleでは手続き的な書き方ができてしまうので、YAMLでプログラミングをするよべきとううなイメージになりがちです。また、コードを書くときにも冪等性を意識した書き方をする必要があり、習熟に時間がかかりました。コードを読み解くのにも慣れが必要です。一方、Terraformのコードは純粋に宣言的なので、「あるべき姿を記述する」ことに集中でき、コードの可読性も高いです。冪等性は、クラウドサービスまたはTerraformの Provider内部で担保されるためです。 



###### **エラーメッセージが読みやすい** 

Ansibleのエラーは、メッセージを含むJSONが改行なしに出力されるため、読み解くのが非常に困難です。Terraformのエラーは整形されてわかりやすく表示されます( **図** A)。これはTerraformに限らず、HashiCorp製品全般に言えます。 





###### **環境の影響を受けない** 

設計上仕方のないことですが、Ansible自体がPythonで動作するため、ホストで実行されるPythonの影響を受けます。古いホストでAnsibleを動かす際、Ansibleが要求するバ 

<u>注A</u> ージョンのPythonがインストールされていないことがありました。このため、ven ~~v~~ でAnsible用のPython環境を隔離するといった対応が必要になりました。 TerraformはGo言語で実装されており、ランタイムが不要なバイナリとして提供されるため、このような影響を受けることがありません。 



###### **特性の差に対する理解が必要** 

Ansibleにはない概念として、TerraformにはState <u>注B</u> ~~が~~ あるため、その考え方を理解しないと混乱してしまいます。Stateとは管理対象リソースの状態を保存したファイルのことで、TerraformはStateを中心にコードと実環境の差分をチェックする考え方です。 AnsibleはPlaybookの記述内容を正とすることで、冪等性を実現します。また前述のように、TerraformはOSよりも上のレイヤに対しては使えません。たとえば、クラウド上に構築した仮想マシンの内部を設定したいといった場合は、依然として Ansibleが有力な選択肢です。このようなケースでクラウド自体のリソース管理もしたい場合は、すべてをAnsibleで統一するというのも良いかもしれません。キャディではすべてのアプリケーションをDockerコンテナ化しており、仮想マシンを使用するケースは踏み台サーバなど限られています。 

AnsibleがカバーしているOSよりも上のレイヤはDockerfileとして記述できるため、すべてをTerraformでカバーできています。 

###### 注A) 仮想環境を分離するPythonの標準機能です。 

(本文に戻る) 

注B) Stateについては次回に詳しく解説します。 

(本文に戻る) 

###### 注9) <u>https://galaxy.ansible.com/google/cloud</u> 

なお、Ansibleにおける「コレクション」とは、一連の機能に関するモジュールの集まりのことです。 

(本文に戻る) 

### **Canvas上で3Dモデルが動く! お手軽 3Dプログラミング three.jsで** 

可児亘(かにわたる) <u>@watabo_shi</u> 株式会社アクアリング Webブラウザには3Dグラフィックスを描画するための標準API「WebGL API」が備わっています。本連載では、そのWebGL APIを手軽に扱えるようにした「three.js」という JavaScriptライブラリを使って3Dプログラミングに挑戦していきます。 

#### **第3回描画ループと時間ベースでのアニメーション** 

#### **描画ループを作る** 

私たちが普段 Web サイトでよく目にする WebGL や three.js を使った表現は、 3D グラフィックスがリアルタイムにアニメーションしたり、ユーザーの操作にインタラクティブに反応したりするものがほとんどです。今回は描画ループを作りレンダリング結果を連続的に変更することでアニメーションを実装していきます。 

##### **~~<u>ア</u>~~ ニメーションとFPS** 

ブラウザでアニメーションを作る場合、描画ループと呼ばれるコールバックループを作り、連続する静止画を高速に再描画して 3D オブジェクトの位置や回転などを再描画のたびに少しずつ変更していくことで動いているように見せます。アニメーションにおける 1 つ分の静止画のことを **フレーム** と言い、 1 秒間で描画するフレームの枚数のことをFPS **(** Frames Per Second **)** または **フレームレート** と言います。 

##### **~~<u>描</u>~~ 画ループを作る方法** 

JavaScript で描画ループを作るやり方には、setInterval()を使う方法とrequestAnimationFrame()を使う方法の 2 通りがあります。 

setInterval() は第 1 引数にコールバック関数、第 2 引数に実行間隔のミリ秒を渡すと、指定した間隔でコールバックを定期的に呼び出します( **リスト** 1)。 

###### **▼リスト1 setInterval()での描画ループ** 

**let frameCount = 0; const render = () => { frameCount++; console.log(frameCount); };** **<mark>// render関数を60FPSで実行</mark> setInterval(render, 1000 / 60);** 

requestAnimationFrame() はブラウザに次のフレームの再描画をリクエストするための関数です( **リスト** 2)。 

###### **▼リスト2 requestAnimationFrame()での描画ループ** 

**let frameCount = 0; const render = () => { frameCount++; console.log(frameCount);** **<mark>// 1次のフレームでrender関数を実行</mark> requestAnimationFrame(render);** 

###### **};** 

**render();** **<mark>// 最初のフレーム</mark>** 

引数にコールバック関数を渡すと次のフレームの再描画が走る直前にコールバック関数が実行されますが、あくまで次の 1 フレーム分の再描画時に呼び出させるものなので描画ループ内で呼び出して毎フレーム実行することでループを継続する必要があります( **リスト** 2の1)。コードだけを見ると再帰呼び出しのように見えますが、 setInterval() と同じようにコールバック関数がほぼ一定間隔で実行されるものと思っておけば良いでしょう( **図** 1)。 



requestAnimationFrame() を使うと、ほとんどの環境では 60FPS ( 16.66 ミリ秒間隔)で動作しますが、実行環境に応じて最適な頻度でアニメーションが更新されるため、 144Hz などの高リフレッシュレートのディスプレイではなめらかなアニメーションを実現できます。そのため、ブラウザでのアニメーションの実装には requestAnimationFrame() の使用が推奨されています( **図** 2)。 



##### **~~<u>描</u>~~ 画ループで3DCGをアニメーションさせてみる** 

それでは実際に requestAnimationFrame() を使って 3D モデルをアニメーションさせてみましょう。前回(本誌 2023 年 4 月号)のサンプルプログラムを基にして修正していきます注1 ~~。~~ 

group のインスタンス作成直後に指定していた XY 回転の記述をなくし、 init 関数の末尾で render 関数を定義して実行します。 render 関数の中で毎フレーム group の Y 軸回転を少しずつ加算したあとにレンダリングすることで 3D モデルをアニメーションさせます( **リスト** 3 **、図** 3)。 

###### **▼リスト3 描画ループで3Dモデルを回転させる** 

**const init = () => {** **<mark>(..略..)</mark> const group = new SnowGroup();** **<mark>(..略..)</mark> const render = () => {** **<mark>//</mark>** <mark>1</mark> **<mark>y軸回転に毎フレーム0.01を加算</mark> group.rotation.y += 0.01;** 

**renderer.render(scene, camera);** **<mark>// 次のフレームの描画をリクエスト</mark> requestAnimationFrame(render); }; render();** **<mark>// 最初のフレームを描画</mark> };** 



##### **~~<u>時</u>~~ 間ベースでのアニメーション** 

ここで 1 つ問題があります。 requestAnimationFrame() を使った実装では、 **図** 2のように表示するディスプレイのリフレッシュレートによって実行間隔が異なる場合があるため、 **リスト** 3の1のように固定値を加算させる方法だとリフレッシュレートの違う環境では動く速度に差異が生じてしまいます。この問題を解消するには、 three.js の Clock クラスを使用します。 Clock クラスは getElapsedTime() 関数でインスタンス生成時からの経過秒数を取得することができ( **リスト** 4)、経過秒数を描画ループ内で使うことでリフレッシュレートに依存しない時間ベースでのアニメーションを実現できます( **リスト** 5)。 

###### **▼リスト4 Clockクラスから経過秒数を取得する** 

**const clock  = new THREE.Clock();** **<mark>// インスタンス生成時からの経過秒数</mark>** 

**const seconds = clock.getElapsedTime();** 

###### **▼リスト5 時間ベースでアニメーションさせる** 

**const init = () => {** **<mark>(..略..)</mark> const clock = new THREE.Clock(); const render = () => {** **<mark>// 経過秒数を取得</mark> const seconds = clock.getElapsedTime();** **<mark>// 1秒で45°回転する</mark> group.rotation.y = seconds * Math.PI * 0.25; renderer.render(scene, camera); requestAnimationFrame(render); }; requestAnimationFrame(render); };** 

#### **クラスにまとめる** 

ここまで index.js の init 関数に処理を書き足してきましたが、この調子で関数を追記していくと処理の流れを追いにくくなるため、 render 関数をメンバに持つ App クラスとして別ファイルに分離しましょう( **リスト** 6 **、** 7)。 renderer や scene などの変数はメンバ関数から利用できるよう先頭に this を付けてメンバ変数にしておきます。 

###### **▼リスト6 init関数の中身をAppクラスに書き換え(App.js)** 

**import * as THREE from 'three'; import { SnowGroup } from './SnowGroup'; class App { constructor() {** 

**const width = window.innerWidth; const height = window.innerHeight; this.renderer = new THREE.WebGLRenderer({ canvas: document.getElementById('canvas'), antialias: true }); this.renderer.setSize(width, height); this.renderer.setPixelRatio(window.devicePixelRatio); this.renderer.setClearColor('#000000');** 

**this.camera = new THREE.PerspectiveCamera(45, width / height, 1, 1000); this.camera.position.z = 3;** 

**this.group = new SnowGroup(); this.light = new THREE.PointLight('#ffffff'); this.light.position.x = 0.5; this.light.position.y = 0.5; this.light.position.z = 2.5;** 

**this.scene = new THREE.Scene(); this.scene.add(this.group); this.scene.add(this.light); this.clock = new THREE.Clock(); this.render(); } render() { const seconds = this.clock.getElapsedTime(); this.group.rotation.y = seconds * Math.PI * 0.25; this.renderer.render(this.scene, this.camera); requestAnimationFrame(this.render.bind(this));** **<mark>//</mark>** <mark>1</mark> **} } export { App };** 

**▼リスト7 init関数の中身をAppクラスに書き換え(index.js)** 

**import { App } from './App';** 

**const init = () => { new App(); }** 

**<mark>(..略..)</mark>** 

クラス化したことにより requestAnimationFrame() への render 関数の渡し方が変更になっていることに気をつけましょう( **リスト** 6の 1)。そのまま **requestAnimationFrame(this.render);** と書いてしまうと、 render 関数内にある this の参照先が Window オブジェクトになってしまうためエラーになります。 **this.render.bind(this)** を渡すことで、 render 関数内の this を App クラスに拘束して正しく実行されるようにしています。 

#### **アニメーションライブラリと組み合わせてみる** 

時間ベースでのアニメーションを実装するには、描画ループ内で自分で値を動かす方法のほかに、アニメーションライブラリを使う方法もあります。 

Web アニメーションでよく利用されている GSAP ( GreenSock Animation Platform ~~)~~ <u>注2</u> ~~と~~ 組み合わせることで少ないコードで小気味よい 3D アニメーションを簡単に作れます。 GSAP は一般的に HTML 要素をアニメーションさせる際に利用されることが多いですが、 Object のプロパティを指定してアニメーションさせることもできます。最初に GSAP の npm パッケージをインストールしておきます。 

###### **$ npm install gsap** 

GSAP にはさまざまなアニメーション関数がありますが、ここでは初期値から目標値までのアニメーションを作る gsap.fromTo() を例に解説していきます。まず第 1 引数に値を動かしたいオブジェクト、第 2 引数には第 1 引数と同じオブジェクトの形式で初期値を設定します。そして第 3 引数には目標値とイージングの種類やアニメーション時間などの設定を渡すことで簡単にオブジェクトのプロパティをアニメーションさせることができます( **リスト** 8)。 

###### **▼リスト8 gsap.fromTo()の使い方** 

**<mark>// GSAPをインポート</mark> import { gsap } from 'gsap';** **<mark>// プロパティの値を動かしたいObject</mark> const target = { value: 0 };** **<mark>// 初期値</mark> const from = { value: 0 };** **<mark>// 目標値とイージングやアニメーション時間などの設定</mark> const to = { value: 1, ease: 'expo.out', duration: 1.0 };** **<mark>// targetのvalueを0→1にアニメーション</mark> gsap.fromTo(target, from, to);** 

**イージング** とは、アニメーションの動きを滑らかにするための緩急のことで、 Web アニメーションではイージングの有無や種類の選定しだいでサイト全体の印象や UI を操作したときの気持ちよさに直結します。イージングには大きく分けるとイーズイン( **図** 4の1)/イーズアウト( **図** 4の2)/イーズインアウト( **図** 4の3)の 3 種類あり、これらを目的に応じて使い分けます。 



GSAP では power2 、 expo 、 back などデフォルトで 10 種類以上のイージング注3 ~~が~~ 利用でき、それぞれのイージングに対して in 、 out 、 inOut が指定できるため、第 3 引数の ease プロパティに適用したいイージングの名前を指定するだけで滑らかなアニメーションを作成可能です。ここでは value プロパティ 1 つを 0 から 1 に動かすアニメーションを作成していますが、プロパティの数を増やすことで複数プロパティを同時にアニメーションさせることもできます。 

gsap.fromTo() の使い方がわかったところで、 3D モデルを拡大するアニメーションを GSAP で追加してみましょう。 

App クラスに gsap.fromTo() で group.scale を 0 から 1 に拡大させる enter 関数を作成し、コンストラクタの最後で呼び出すことでページ表示時に実行します( **リスト** 9、 **図** 5)。 

**▼リスト9 3Dモデルが拡大するイントロアニメーション** 

**import { gsap } from 'gsap';** **<mark>// GSAPをインポート</mark> import * as THREE from 'three'; import { SnowGroup } from './SnowGroup'; class App {** 

**constructor() {** **<mark>(..略..)</mark> this.enter();** **<mark>// アニメーションを再生</mark> } enter() {** **<mark>// group.scaleのxyzを0から1にアニメーションさせる</mark> const from = { x: 0, y: 0, z: 0 }; const to = { x: 1, y: 1, z: 1, ease: 'back.out', duration: 1.5 }; gsap.fromTo(this.group.scale, from, to); }** **<mark>(..略..)</mark> } export { App };** 



GSAP ではライブラリ側で描画ループを作成するため、 render 関数への変更は不要です。 

GSAP はオブジェクトのプロパティを動かすものなので、 **リスト** 10のような書き方をすることで three.js 側の Vector3 クラスをそのまま GSAP のアニメーションに利用することができます。 

###### **▼リスト10 Vector3をGSAPでアニメーションさせる** 

**enter() { const from = new THREE.Vector3(0, 0, 0);** **<mark>// 初期値</mark> const to = new THREE.Vector3(1, 1, 1);** **<mark>// 目標値</mark>** 

**gsap.fromTo( this.group.scale, from, { ...to, ease: 'back.out', duration: 1.5 }** **<mark>//</mark>** <mark>1</mark> **); }** 

**リスト** 10の1ではスプレッド演算子( **...** )で to の x 、 y 、 z プロパティを fromTo() の第 3 引数の Object に展開して Vector3 クラスと GSAP の設定値をマージしています。 

◆ ◆ ◆ 

今回は描画ループを作って 3D モデルの rotation と scale を動かしましたが、 3D モデルやライトの position も描画ループや GSAP を使ってアニメーションさせてみましょう! 

- 注1) 本記事で紹介するサンプルプログラムは次のGitHubリポジトリで公開しています。 <u>https://github.com/watab0shi/software-design-threejs-3d-programmingsamples/tree/main/chapter-03</u> 

   - (本文に戻る) 

#### **「らしさ」の本質に迫る** 

### **なるほど納得/ Go 言語** 

崎原晴香(さきはらはるか) (H.Saki) <u>@saki_engineer https://zenn.dev/hsaki</u> 



**<u>第 4 回 Goにおける変数宣言──[前編]ゼロ値と初</u> 期化** 



**「Goらしさ」とは何でしょうか。本連載では、「Goらしさ」とはいったい何を指すのか、背景からじっくり考察し、「Goらしく」書くためのヒントを紹介します。第4回では、Goの変数宣言、とくにゼロ値と初期化について考えます。** 

前回(本誌 2023 年 4 月号)は変数や定数にどのような命名をするべきかについてお話ししてきました。命名が必要になるような識別子はコードの中に溢れており、いい名前を付けることは可読性のあるプログラムを書くうえでとても重要なトピックでした。今回からは、命名から一歩先に進んで、変数をどう宣言し初期化するべきかについて解説していきます。変数宣言は、「変数に自分が欲しい値を格納する」というだけですが意外と奥が深いです。 Go における各データ型のゼロ値が何なのか、そのゼロ値はそのまま利用できるものなのかを把握したうえで、過不足ない記述で変数宣言をできるようにしていきましょう。 

###### 第1回の復習 

Goにおけるシンプルさ=「明確で過不足がないこと(clear and precise)」 

- ・明確さ 

   - ・コードの書き手の意図が明確であること 

   - ・そのコードがどのように動作するか明確であること 

- ・過不足がない 

   - ・プログラマーが必要とする機能を過不足なく提供 

   - ・プログラマーが優れたプログラムを書くための機能を過不足なく提供 

##### **ゼロ値と初期化** 

まず、ゼロ値と初期化済みという言葉について最初に確認しておきます。 

- ・ゼロ値:予約語varで宣言したときにできる初期状態で格納されている値 

- ・変数の初期化:varで宣言したあとに、=や:=を使って明示的に値をセットする行為ここで重要なのは、ゼロ値というのはあくまで「 var で変数宣言されたときの値」だということです。たとえば int 型でいう 0 や []int 型でいう⻑さ 0 のスライス、 map 型でいう「 key-value はまだ何もセットされていないが、値の追加が今すぐ可能なもの」のような、 

- ・ (なんとなく)0っぽい値 

- ・多くの人が期待するようなその型の初期値とは異なるかもしれないということです。そのため、まずは主要な型のゼロ値が何か、想像するようなデフォルト値にするためにはどう初期化をすればよいかを確認しておきましょう。 

# **プリミティブ型** 

int や bool 、 string 型のようなプリミティブ ~~型~~ <u>注1</u> ~~の~~ 場合は、ゼロ値には次の値がセットされています。 

- ・ int系/uint系/float系/byteなど数字を表す型:0 

- ・ bool:false 

- ・ string:空文字列"" 

ここからわかることは、 null や NaN 、 nil のような「そのままでは使えない値」ではなくて、通常どおり int や bool 、文字列として利用できる値がゼロ値として使われているということです。ここに関しては、みなさんが想像するような「その型の初期値」がそのままゼロ値として使われているパターンかと思います。そのため、変数を宣言したあとにセットされているゼロ値をそのまま使いたいという場合には、わざわざ明示的な初期化代入を行う必要はありません。むしろ var による変数宣言だけで済ませるほうが「デフォルトのゼロ値を使いたいんだ」という意図が明確になります。 

**<mark>// (イマイチ)明示的な初期化</mark> x := 0** 

**<mark>// (good)ゼロ値をそのまま使う</mark> var x int** 

逆に、ゼロ値以外の「特別な」値をセットする場合には **:=** を使って宣言+初期化を一度に済ませるほうが短く済みます。 

**<mark>// (イマイチ)varを使った宣言→代入して初期化</mark> var x int x = 1** 

**<mark>// (イマイチ)varを使った宣言+初期化......記述量が多い</mark>** 

**var x int = 1** 

**<mark>// (good):=を使った初期化</mark> x := 1** 

# **map型** 

map 型のゼロ値は nil です。わかりやすく言い換えると、次のように var で宣言したばかりの map 型の変数には nil が入っているということです注2 ~~。~~ 

**var m1 map[int]int fmt.Println(m1 == nil)** **<mark>// true</mark>** 

そして重要なのは「ゼロ値である nil マップは、『空のマップ』とは違う」ということです。空のマップといって想像するのは、「まだ key-value のセットは何もセットされていないが、これからいくらでも設定していくことができる」というものだと思います。しかし、 nil マップに新しく keyvalue セットを設定することはできませ ~~ん~~ <u>注3</u> ~~。~~ 

**<mark>// nilマップ</mark> var m1 map[int]int m1[1] = 2** **<mark>// panic: assignment to entry in nil map</mark>** 

**<mark>// 空のマップ</mark>** 

###### **m2 := map[int]int{} m2[3] = 4** **<mark>// OK</mark>** 

これをふまえたうえで、 map 型の変数を宣言/初期化するときに使うべき記法は、次のようにまとめられます注4 ~~。~~ 

- 1 変数の宣言だけして、初期化はしなくてよい:varでnilマップを作る 

- 2 空のマップを作りたい:makeを使った初期化 

- 3 初期化の段階で、入れておきたいkey-valueのセットが決まっている場合:リテラルを用いた初期化 

1 のパターンは、パッケージのグローバル変数として map を用意して、 map 本体の値は main 関数の中で代入するといったパターンが考えられるでしょう( **リスト** 1)。 

###### **▼リスト1 グローバル変数としてmapを用意する** 

**import "github.com/aws/aws-lambda-go/lambda" var glbMap map[int]int func handler() {** **<mark>// glbMapを使った処理</mark> } func main() { glbMap = map[int]int{ 1: 2, 2: 4, 3: 6, } lambda.Start(handler) }** 

2 のパターンでわざわざ make 関数を使う理由は、「 write しても panic しない、空のマップなのだ」ということを明確にするためです ( **リスト** 2)。 

###### **▼リスト2 make関数で空のマップだと明確にする** 

**<mark>// writeしたらpanicするnilマップはこのような形でしか定義できない</mark> var m1 map[int]int** 

**<mark>// 空マップをリテラルで定義してしまったら、nilマップ定義の記法と見た目が似てい</mark> る m2 := map[int]int{}** 

**<mark>// ならば、makeを使ったほうが「writeしてもpanicしない」と読み手に正確に伝わる</mark> m3 := make(map[int]int)** 

また、マップに格納される要素の最大数が事前に予測できる場合には、 **make(map[int]int, 5)** のように容量を指定し必要分のメモリを事前に確保しながら make することで、メモリアロケーションを最低限にすることができます。 

nil でも空でもない map を作りたい場合には、 3 のようにリテラルを使い明示的な初期化を行うことになります( **リスト** 3)。 

###### **▼リスト3 リテラルで明示的に初期化する** 

**m := map[int]int{ 1: 2, 2: 4, 3: 6, }** 

# **スライス** 

スライスのゼロ値も、 map と同じく nil です。そして nil スライスと要素が 1 つも入っていない空のスライスが別物として扱われるのも map と類似しているポイントです( **リスト** 4)注5 ~~。~~ 

###### **▼リスト4 nilスライスと空のスライスのゼロ値** 

**<mark>// nilスライス</mark> var s1 []int fmt.Println(s1 == nil)** **<mark>// true</mark>** 

**<mark>// 空のスライス(=要素数が0)</mark> s2 := []int{} fmt.Println(s2 == nil)** **<mark>// false</mark>** 

ただ、 map とは異なるのは「 nil スライスは、要素 0 の空スライスとしても **振る舞う** ことができる」というポイントです。具体的には、 nil スライスに対して append を使って要素を追加しても panic になることはありません。 

**var s1 []int** **<mark>// nilスライス</mark> s1 = append(s1, 1)** **<mark>// OK</mark>** 

そのため、宣言/初期化の方針としては次のようなものになるかと思います。 

- ❶ ⻑さ0のスライスを作りたい: 

   - ・基本的にはvar宣言だけで済ませて、nilスライスを⻑さ0スライスとして扱うのが簡単 

   - ・ nilと空スライスを区別したい事情がある場合には、⻑さ0スライスはmake([]int, 0)による初期化で作 ~~成~~ <u>注6</u> 

- ❷ スライスに入り得る要素数が事前にわかっている場合: make([]int, 0, 6)のように、make関数の第3引数にcapを指定する初期化であらかじめスライス用のメモリを確保することで、メモリアロケーションを最低限に抑える 

- ❸ 初期化の段階で、ゼロ値以外の要素を入れておきたい場合: []int{1, 2, 3}のようにリテラルを代入する 

nil スライスと空スライス、どちらも⻑さ 0 のスライスとして扱えるのにもかかわらず両者を区別しなくてはいけない状況としては JSON エンコードをする場面が 1 つ考えられます。 

**リスト** 5のように、 nil スライスを利用した変数 m1 と空スライスを利用した変数 m2 では、 JSON エンコード結果が異なってくるのがわかります( **図** 1)注7 ~~。~~ 

###### **▼リスト5 nilスライスと空スライスをそれぞれ利用した変数** 

**type MyStruct struct { Slice []int } func main() { m1 := MyStruct{} PrintJSON(m1) m2 := MyStruct{Slice: []int{}} PrintJSON(m2) } func PrintJSON(m MyStruct) { b, _ := json.Marshal(m) fmt.Println(string(b)) }** 

**▼図1 リスト5のJSONエンコード結果** 

**{"Slice":null} {"Slice":[]}** 

また、このように nil スライスと空スライスを使い分けるユースケースが存在する以上、 **リスト** 6のように「⻑さ 0 のスライスであること」を判定するために nil と比べるというのは誤ったやり方です。⻑ さ 0 判定は、素直に **len(a) == 0** で行いましょう注8 ~~。~~ 

###### **▼リスト6 nilスライスを空スライスと比べる** 

**<mark>// nilスライス</mark> var s1 []int fmt.Println(s1 == nil)** **<mark>// true</mark> fmt.Println(len(s1))** **<mark>// 0 // 空のスライス(=要素数が0)</mark> s2 := []int{} fmt.Println(s2 == nil)** **<mark>// false</mark> fmt.Println(len(s2))** **<mark>// 0</mark>** 

**<mark>// nilスライスと空スライスを同様に扱うことができるのはlenを使った方法</mark>** 

_When designing interfaces, avoid making a distinction between a nil slice and a non-nil, zero-length slice, as this can lead to subtle programming errors._ 

_(訳)気づきづらいバグの原因になってしまうため、インターフェースを設計するときに nil スライスと⻑さ 0 である空スライスを明示的に区別しないようにしましょう_ <u>注9</u> _~~。~~_ 

# **チャネル型の場合** 

チャネル型のゼロ値は nil チャネルです。 nil チャネルは値の送受信はできず、 close 操作を試みても panic になるという性質があります。 

- ・ nilチャネルへの送信を試みる→永久にブロック 

- ・ nilチャネルからの受信を試みる→永久にブロック 

- ・ nilチャネルをclose→panic 

そのため、通常思い浮かべるような送受信を行うためのチャネルを作るためには、 var を使った宣言だけでは足りず、必ず make 関数を使った初期化が必要となります( **リスト** 7)。 

###### **▼リスト7 チャネル型の変数宣言と初期化** 

**var ch1 chan int** **<mark>// nilチャネル</mark> ch2 := make(chan int)** **<mark>// 送受信可能な初期化済みチャネル</mark>** 

# **関数型の場合** 

関数のゼロ値は nil です。「呼び出しても何も起こらない空の関数」とはなりません注10 ~~。~~ 

**<mark>// 「呼び出しても何も起こらない空の関数」の例</mark> f := func(){} f()** 

nil 関数は呼び出すことができません。試みると panic が発生します。関数型の変数を明示的な初期化なしに定義するということは少ないかと思いますが、この「 nil 関数の呼び出しは panic の原因になる」という事実は頭の片隅に置いておきましょう。 

**var f func()** **<mark>// nil関数=ゼロ値</mark>** 

**<mark>// ↓実行するとpanicを引き起こす</mark>** 

**f()** **<mark>// panic: runtime error: invalid memory address or nil pointer dereference</mark>** 

###### **Column nilチャネルの使いどころ** 

送受信ができないnilチャネルは、select文中に用意されたcaseを無効化するときに使われることがあります。 

**select{ case <-ch1:** **<mark>// (do something)</mark> case <-ch2: ch1 = nil** **<mark>// これ以降、case <-ch1には流れなくなる</mark> }** 

# **ポインタ型の場合** 

関数型と同様に、ポインタのゼロ値は nil です。 nil ポインタをデリファレンスして値を取り出すことはできません。試みると panic が発生します注11 ~~。~~ 

**var p *int _ = *p** **<mark>// panic: runtime error: invalid memory address or nil pointer dereference</mark>** 

「デリファレンスすると、型 T のゼロ値が取り出せるようなポインタ型 *T 」の変数を作りたい場合もあるかと思います。そのときには、 T がプリミティブ型の場合は組み込み関数の new を使うことになります。たとえば、デリファレンスしたら int のゼロ値 0 が取り出せる *int 型の変数を作りたかったら、次のように new(int) を使った初期化をすれば OK で ~~す~~ <u>注12</u> ~~。~~ 

**p := new(int) fmt.Println(*p)** **<mark>// 0(=デリファレンスしてもpanicしない)</mark>** 

構造体の場合も、デリファレンスしたときに取得したいのがゼロ値である場合にはプリミティブ型同様に new(T) を使うことができます。ただ、ゼロ値以外を用意する場合にはポインタではない素の構造体型を用意するときと表記を似せて統一感を出すために、 new よりも &T{} とリテラルで初期化するほうが好まれる傾向があります ( **リスト** 8)注13 ~~。~~ 

###### **▼リスト8 ポインタ型のゼロ値** 

**<mark>// 素の構造体型変数の定義</mark>** 

**v := T{FieldA: 1}** **<mark>// (A)</mark>** 

**<mark>// BとCの両者ともにポインタ型を作るが、Aに似ている後者のほうが好まれる</mark>** 

**<mark>// (B)</mark> p := new(T) p.FieldA = 1** 

**<mark>// (C)</mark> p := &T{FieldA: 1}** 

# **プリミティブ型以外のゼロ値の挙動まとめ** 

ここまで「ゼロ値と、多くの人が期待するゼロっぽい値というのは異なるかもしれない」という視点にて、いろいろな型のゼロ値の振る舞いを確認してきました。その結果を表にしてまとめたものが **表** 1です。マップ/スライス/チャネルを期待どおり動かすためには make による初期化が有効なことがこの表からわかります。 

##### **次回に向けて** 

「期待され得る」初期値は型によってさまざまですが、 Go でのゼロ値がそれと一致するかしないかを確認してもらえたかと思います。各型のゼロ値について正しく把握できたところで、次回は Go での有名な格言 “Make the zero value useful.” という言葉について解説していきたいと思います。 

###### **今回のレビュー協力:** 

syumaiさん( <u>@__syumai)</u> 

注1) A Tour of Goでは“Basic types”という言葉が使われています。 <u>https://go.dev/tour/basics/11</u> (本文に戻る) 注2) The Go Playgroundでの実行はこちら→ <u>https://go.dev/play/p/RJByP9bPbw6</u> (本文に戻る) 注3) The Go Playgroundでの実行はこちら→ <u>https://go.dev/play/p/OE1vVGZcToI</u> (本文に戻る) 注4) <u>https://github.com/uber-go/guide/blob/master/style.md#initializing-mapsにもほぼ同</u> じ内容が記載されています。 (本文に戻る) 注5) The Go Playgroundでの実行はこちら → <u>https://go.dev/play/p/fCGjlqnqBNN</u> (本文に戻る) 注6) 空スライスは **make([]int, 0)** 以外にもリテラル[]int{}の代入でも作れますが、❷と類似した書き方をするという意味でもmakeを使うほうが筆者は好きです。 (本文に戻る) 注7) The Go Playgroundでの実行はこちら → <u>https://go.dev/play/p/nA5nOcg9HFN</u> (本文に戻る) 注8) The Go Playgroundでの実行はこちら → <u>https://go.dev/play/p/deZR51wvlXy</u> (本文に戻る) 注9) Go Wiki - Go Code Review Comments <u>https://github.com/golang/go/wiki/CodeReviewComments#declaringempty-slices</u> 訳文は筆者による意訳です。 (本文に戻る) 注10) The Go Playgroundでの実行はこちら → <u>https://go.dev/play/p/ZHoLiT8tBhG</u> (本文に戻る) 注11) The Go Playgroundでの実行はこちら → <u>https://go.dev/play/p/aaWEEeqzVpU</u> (本文に戻る) 

注12) The Go Playgroundでの実行はこちら 

→ <u>https://go.dev/play/p/SlTAK4oCdOI</u> (本文に戻る) 注13) <u>https://github.com/uber-go/guide/blob/master/style.md#initializing-struct-references</u> にもほぼ同じ内容が記載されています。 

(本文に戻る) 

### **最強の開発環境探求の道** 



**中山慶祐** (なかやまけいすけ) GMOペパボ株式会社 

#### **<mark>第 5 回</mark> テキストエディタをカスタマイズする** 

仕事を効率化し安心して作業ができる“最強の開発環境”はエンジニア一人一人にとって異なります。無数の組み合わせが考えられる環境から自分にぴったりの構成・設定を見つけることは、いわば“探求の道”。そこで本連載では、シェル、ターミナル、エディタを中心とした自分好みの開発環境を構 <u>築、設定、管理する方法を紹介します。</u> 

今回はテキストエディタについてです。代表的なエディタである Visual Studio Code (以降 VSCode )と Vim について、設定のカスタマイズや同期方法を紹介します。 

##### **VSCodeとVim** 

テキストエディタは開発環境を構成する主要なツールです。ソフトウェア技術者にとって最も触れる時間の⻑いツールである場合もあるでしょう。本連載では VSCode と Vim の 2 つのソフトウェアを扱います。本記事で紹介する設定例は、 GitHub <u>注1</u> ~~で~~ 公開しています。必要に応じて参照してください。なお、記事の内容は Ubuntu もしくは macOS 上で実行することを想定しています。 

##### **VSCodeの概要** 

VSCode は、 2015 年にマイクロソフトが発表した現在世界中で人気のあるテキストエディタです。単体の GUI アプリケーションとしてだけでなく、 Web ブラウザ上でも動作しま ~~す~~ <u>注2</u> ~~。~~ テキスト編集をはじめ、検索、 Git の操作、差分表示などの機能を備えています。 

##### **Vimの概要** 

Vim は、 Unix や Linux で広く使われるテキストエディタです。 1980 年代に vi というテキストエディタを参考に作成されて以降、さまざまな環境に移植され、ソフトウェア技術者に⻑く愛されています。現在も開発が続けられ、 2022 年 6 月には Vim 9.0 がリリースされています。もともとは CUI 環境で動作するソフトウェアですが、 GUI 環境で動作するバージョンもあります。複数のモードを備えていて、慣れると高速にテキスト編集できることが特徴です。 

##### **VSCodeとVimの共通点** 

これら 2 つのテキストエディタには次のような共通点があります。 

- ・マルチプラットフォームに対応している 

- ・さまざまなプログラミング言語の編集に対応している 

- ・カスタマイズや、機能拡張ができ ~~る~~ <u>注3</u> 

- ・オープンソースソフトウェアである、もしくはそれをもとに作られている注4 

- ・ 2023年現在、ソフトウェア技術者に広く利用されている本連載では、 GUI 環境と CUI 環境のそれぞれの代表的なエディタとして VSCode と Vim を扱います。どちらもメジャーなエディタであり、使い方を知っていて損はないでしょう。もちろん開発環境の探求の意味では、 Emacs などほかのエディタを使ってみることもすばらしいことですが、今回はこの 2 つにとどめます。 

##### **VSCodeのカスタマイズと同期** 

まずは VSCode についてです。簡単な使い方、設定のカスタマイズ、同期について紹介します。 

##### **VSCodeのインストール** 

VSCode は、公式サイ ~~ト~~ <u>注5</u> ~~よ~~ りダウンロードしてインストールできます。 Ubuntu であれば、 deb 形式のファイルをダウンロードし、次のように **dpkg -i** コマンドの引数に指定してインストールします。 

**$ sudo dpkg -i ~/Downloads/code_1.76.11678294265_amd64.deb** 

macOS であればダウンロードした ZIP ファイルを解凍し、中の Visual Studio Code.app をアプリケーションディレクトリへ移動してください。インストールできたら VSCode を利用してみましょう。インストールされた VSCode のアイコンをクリックしてアプリケーションを起動します。次に、編集対象のファイルを含むディレクトリを、左上のメニュー[ File ]から[ Open Folder... ]を選択して開きます。 

##### **VSCodeの画面構成** 

VSCode は次に示す 5 つの画面領域で構成されています( **図** 1)。 



- ・ **エディター** :ファイルの内容が表示され、編集作業を行う領域・ **アクティビティバー** :サイドバーの表示を切り替えるアイコンが並ぶ領域 

- ・ **サイドバー** :ファイルの一覧の表示や、文字列の検索などを行う領域 

- ・ **パネル** :デバッガの出力表示や、ターミナル操作のための領域 

- ・ **ステータスバー** :開いているファイルの情報などを表示する領域エディター領域でファイルを編集しつつ、周辺にある領域が補助的な操作を受け付けます。 Git 、コードフォーマッタ、デバッガなど、ソフトウェア開発に関わるさまざまなツールと連携することで、ファイルの直接的な編集に限らず、ソフトウェア開発に必要な作業を行えます。 

##### **クイックオープンとコマンドパレット** 

VSCode には便利な 2 つの操作方法として、クイックオープンとコマンドパレットが用意されています。 

**クイックオープン** は、ファイルを部分一致で検索して開くことができる機能です。 **Ctrl** + **P** ( Ubuntu )や ⌘ + **P** ( macOS )を押下して入力欄を開き、ファイル名の一部を入力すると、現在開いているディレクトリ内にあるファイルが候補に出現します。 **図** 2では「 . 」を入力して「 . 」が含まれるファイルを選んでいます。候補を上下矢印キーで選び **Enter** で確定すると、そのファイルが開かれます。 



**コマンドパレット** は、 VSCode 上のあらゆる操作をコマンドで行えるものです。 **F1** か、もしくは **Ctrl** + **Shift** + **P** ( Ubuntu )や ⌘ + **Shift** + **P** ( macOS )を押下して入力欄を開きます。たとえば「 File: Open Folder 」と入力すると、画面上部のメニューから操作したのと同様に、特定のディレクトリを開くことができます。コマンドは部分一致で検索できるので、すべて入力する必要はありません。たとえば「 Open Fold 」と入力すれば、[ File: Open Folder ]が選択肢に現れます( **図** 3)。選択肢を上下矢印キーで選んで **Enter** を押せば、フォルダを選択するウィンドウが表示されます。特定のディレクトリを開く操作以外にも用意されている便利なコマンドを **表** 1にまとめました。 



##### **VSCodeの設定カスタマイズ:テーマの変更** 

VSCode の設定は JSON を拡張した形式のファイルに記述されます。設定ファイルを直接編集する以外に、チェックボックスなどで設定を選択できるグラフィカルな画面も用意されています。前者を開くにはコマンドパレットから[ Preferences: Open User Settings (JSON) ]を選択します。後者を開くには同様に[ Preferences: Open User Settings ]を選択してください。 **図** 4ではタブを右端にドラッグして画面分割し、左側にグラフィカルな設定画面、右側に設定ファイルの内容を表示しています。ここでは、カラーテーマの変更を通して設定の追加を試してみましょう。左側の設定画面内の上部の検索欄に「 theme 」と入力して、 [ Workbench: Color Theme ]という項目を探します。プルダウンメニューの中の項目を選んで、デフォルトの[ Dark+ (default dark) ]以外のものに変更してください。 **図** 5では[ Light High Contrast ]を選択しました。選択したカラーテーマが適用され、ウィンドウ全体の見た目が変わっています。そして、右側のタブに表示された設定ファイルへ、次の行が現れました。 



**"workbench.colorTheme": "Default High Contrast Light",** 

次に、追加された行を次のものに置き換えてファイルを上書き保存します。 

**"workbench.colorTheme": "Red",** 

すると即座に別のカラーテーマに切り替わることがわかります。最後に、追加された行を削除します。すると、デフォルトのテーマに戻ることが確認できます。今回のカラーテーマのように、 VSCode では設定ファイルに値を記入しない場合に利用される値が定まっています。設定のデフォルト値は、コマンドパレットに「 Open Default Settings (JSON) 」と入力して閲覧できるファイルから確認できます。ファイルは読み込み専用で開かれるので、編集できません。設定を変更するときは先ほどの「 User Settings 」を編集してください。 

##### **VSCodeの設定を同期する** 

VSCode の設定内容は、内蔵のSettings Syncという機能を用いることで、インターネットを介して複数の環境で同期できます。アクティビティバー下部の⻭車のアイコンをクリックして[ Turn on Setting Sync... ]を選びます( **図** 6)。続いて「 Please sign in to synchronize your data across devices. 」と表示され、同期内容を選択する画面が開きます( **図** 7)。今回は設定を同期するので、「 Settings 」にチェックが入っていることを確認します。ほかに同期したいもの(したくないもの)があれば、チェックを有効(無効)に切り替えます。次に、どのアカウントを同期に用いるか選ぶ画面が開きます( **図** 8)。同期には Microsoft アカウントもしくは GitHub アカウントが必要です。ここでは後者を選ぶこととして、[ Sign in with GitHub ]を選びます。するとブラウザが開くのでそこで GitHub にログインし、ひも付けを許可します。許可したら VSCode へ遷移します。 GitHub アカウントにひも付けたことで、同様にひも付けたほかのマシンと VSCode の設定内容が同期されるようになります。 

##### **Vimのカスタマイズと同期** 

次に Vim についてです。基本操作、設定のカスタマイズ、同期方法を紹介します。 

##### **Vimの起動** 

Vim は Ubuntu や macOS にデフォルトでインストールされているため、事前の準備なしにすぐに使用できます。 Vim を起動するには vim コマンドを実行します。引数にファイルのパスを与えれば、指定したファイルが開かれます。 

**~/dotfiles/.bashrcをVimで編集するときは、次のコマンドを実行する $ vim ~/dotfiles/.bashrc** 

##### **Vimの基本操作** 

Vim は複数のモードからなる独自の操作体系を持ちます。起動時は **ノーマルモード** と呼ばれる状態であり、ここでのキー入力は文字入力として扱われません。それぞれのキー入力は Vim への操作を表すことになっています。たとえば **hjkl** の各キーはそれぞれカーソルを左下上右に移動します。そのほか、ノーマルモードで利用できる操作のうちいくつかを **表** 2に示します。こういった操作の前に数字を押下しておくと、その回数分を繰り返して操作できます。たとえば **x** はカーソル上の文字を 1 文字削除しますが、 **10x** は 10 文字削除します。ファイルへの文字入力は、 **i** を押してノーマルモードから **インサートモード** へ切り替えることで行えます。インサートモードに切り替わると、切り替わったことを表す表示「 -- INSERT -- 」が画面下部へ表示されます( **図** 9)。インサートモードでは、各種キー入力がファイルへの文字挿入として扱われます。ひととおり入力が終わったら、 **Esc** を押すとノーマルモードに戻ることができます。これらの操作は、慣れないうちはひとつひとつに時間がかかるでしょう。一方で慣れてしまえば、すべての操作がキーボードのみで完結し、繰り返し同じキーを入力する必要もなく、テキストの編集作業を高速にこなすことができます。テキストの編集機会はソフトウェア技術者にとって非常に頻繁にあるので、時間をかけて習得する価値があると筆者は考えます。 

Vim の操作への習熟は本連載では扱いませんが、操作を覚えるためのチュートリアルが Vim に付属しています。 vimtutor コマンドをターミナル上で実行すると開始できます。試してみるとよいでしょう。次項以降の内容に必要な操作は都度紹介しますので、本記事の内容を試す限りは、そのまま読み進めていただいても構いません。 

##### **Vimの設定カスタマイズ:行番号を表示する** 

Vim の設定は、ホームディレクトリに設置されたファイル ~/.vimrc へ記述します。実際に編集して設定の変更を試してみましょう。今回は画面左側に行番号を表示するよう設定します。 ~/.vimrc に記述する内容は次のとおりです。 

###### **set number** 

設定の記述も Vim を用いて行うこととします。順を追って操作を説明するので、手元で実行しながら試してみてください。まず、ターミナルを開いて次のコマンドを実行し、 Vim を起動します。 

###### **$ vim ~/.vimrc** 

引数に指定された ~/.vimrc が開かれます。このとき、ファイルがまだ存在しなくても問題ありません。初めて保存するときに作成されるので安心してください。 

Vim が起動した時はノーマルモードです。はじめに **i** を押してインサートモードに切り替えます。次に、キーボードから「 set number 」と記述します。記述を終えたら **Esc** を押してインサートモードからノーマルモードに切り替えます。続いて **:w** → **Enter** と押下してファイルを上書き保存します。以上で設定を記述できました。設定を読み込むには、 **:q** → **Enter** で一度 Vim を終了するか、「 :source ~/.vimrc 」 → **Enter** を押下して Vim を起動したまま再読み込みします。設定が読み込まれると **図** 10のように行番号が表示されます。 



##### **コマンドラインモードを使ってカラースキームを試す** 

Vim には、文字色や背景色を定めたカラースキームが複数同梱されています。コマンドラインモードを使ってこれらを試し、好みの見た目に変えてみましょう。ノーマルモード上で **:** を押すと **コマンドラインモード** に切り替わります。先ほど、 **:** から始まり **Enter** で終わる操作を何度か行っていましたが、実はコマンドラインモードを用いていました。このモードでは、 **Enter** を押すことでコマンドを実行できます。 

**:colorscheme** と入力し、続けて **Tab** を押下すると、設定できるカラースキーム名が補完されます。繰り返し **Tab** を押せば、順に次の候補に切り替わります。ここでは pablo を選ぶことにします。 **Tab** を何度か押下して pablo と表示されたら、続けて **Enter** を押してコマンドを実行します( **図** 11)。すると、文字色や背景色が変わったことを確認できるでしょう。気に入ったカラースキームがあれば ~/.vimrc に追記するとよいでしょう。 ~/.vimrc を開き、ノーマルモードで **o** を押下します。するとカーソルの下に新たな行が挿入され、インサートモードに切り替わるので、「 colorscheme pablo 」と記述して、ファイルを保存します。追記後の ~/.vimrc は次のような内容になります。 

**set number colorscheme pablo** 

~/.vimrc を再読み込みすると、カラースキームが反映されたことを確認できます。なお、コマンドラインモードで実行したコマンドと ~/.vimrc への記述は、どちらも同じ表記です。これは Vim Script と呼ばれる形式の記述です。コマンドラインモードは Vim Script のコード片を実行できるモードです。一方、 ~/.vimrc は起動時に実行される Vim Script が記述されたファイルです。設定のカスタマイズは、一時的に有効にしたいものや動作を試したいものをコマンドラインモードで実行し、永続化したい設定を ~/.vimrc に記述するとよいでしょう。 

##### **Vimの設定の同期** 

Vim の設定はテキストファイル ~/.vimrc で管理されるため、このファイルをほかのマシンと同期すれば設定も同期されます。設定ファイルを同期する管理構成は、本連載初回(本誌 2023 年 1 月号)にて紹介しました。詳細はそちらを参照していただくこととして、今回は ~/.vimrc をどのように同期するか簡単に紹介します。 

Git とシンボリックリンクを用いた dotfiles の管理構成は、次のようなものです。 

- ・ ~/dotfilesディレクトリを作り、設定ファイルを配置してGitで履歴管理する 

- ・各アプリケーションが設定ファイルを読み込めるよう、ホームディレクトリへシンボリックリンクを作成するすでに dotfiles の管理リポジトリがあれば、 **図** 12の手順で .vimrc をリポジトリに追加できます。ご自身の管理リポジトリがなければ、本連載初回やサンプルコードを参考に、 Git リポジトリを作成していただくと試すことができます。 

###### **▼図12 .vimrcをリポジトリに追加する** 

- **↓設定ファイルをdotfilesディレクトリへ移動する** 

- **$ mv ~/.vimrc ~/dotfiles/.vimrc ↓移動した設定ファイルからホームディレクトリ上へシンボリックリンクを貼り、Vim** 

- **を起動したときに設定が読み込まれるようにする $ ln -s ~/dotfiles/.vimrc ~/.vimrc ↓dotfilesディレクトリへ移動する** 

- **$ cd ~/dotfiles** 

- **↓.vimrcファイルをGitリポジトリへ追加する** 

- **$ git add .vimrc** 

- **↓.vimrcファイルの追加をコミットする** 

- **$ git commit -m ""** 

- **↓.vimrcファイルの追加をリモートリポジトリへ反映する** 

- **$ git push origin main** 

##### **VimとNeovim** 

Vim と同様の操作体系を持つテキストエディタにNeovi ~~m~~ <u>注6</u> ~~が~~ あります。 Vim からフォークしたソフトウェアで、新機能の追加、 Lua 言語によって記述された設定ファイルやプラグインのサポートなどが行われているものです。 

Vim と Neovim はどちらも開発が続けられており、人気のあるソフトウェアです。基本的な操作方法は上述に紹介したものと同じですが、プラグインの対応範囲やデフォルトで有効になっている設定が異なります。 Neovim が気になるならば、一度インストールして試してみるとよいでしょう。 Ubuntu では **sudo apt install neovim** 、 macOS では Homebre ~~w~~ <u>注7</u> ~~を~~ 用いて **brew install neovim** でインストールできます。 

◆ ◆ ◆ 

今回は、テキストエディタの VSCode と Vim を紹介しました。次回は開発環境を構成する各種ツールの拡張機能やプラグインをまとめて紹介します。注2) GitHubのリポジトリのページで「.」を押下すると、リポジトリのファイルをブラウザ内に起動したVSCodeで閲覧できます。 



<u>https://docs.github.com/ja/codespaces/the-githubdev-web-based-</u> 

<u>editor</u> 

(本文に戻る) 

注3) 拡張機能やプラグインについては、連載の次回記事で扱います。 

(本文に戻る) 

注4) VimはGPL互換のライセンスで公開されています。 ( <u>https://github.com/vim/vim/blob/master/LICENSE)</u> 

一方、VSCodeの主要部分はMITライセンスで公開されていますが、配布されているバイナリにはプロプライエタリなものも含まれています。( <u>https://code.visualstudio.com/docs/supporting/faq#_what-does-built-on-opensource-mean)</u> 

(本文に戻る) 

注7) brewコマンドを利用するには、事前にHomebrewをインストールする必要があります。詳細はHomebrewのWebページを参照してください。 <u>https://brew.sh/index_ja</u> (本文に戻る) 



<u>@ryosuke927</u> 

### **第 5 回外的リソースへの対応** **<u>リソースから考えるBCPの手引き</u>** 

災害やサイバー攻撃などの脅威が発生しても事業を続けるための計画(BCP)について、事業に必要なリソースの目線から考えておくべきポイントを整理していきます。今回は外的リソースに対する対応を考えます。角道淳平(かくどうじゅんぺい)、中村勝敏(なかむらかつとし) NECソリューションイノベータ株式会社 <u>https://www.necsolutioninnovators.co.jp/sl/bcp/index.html</u> 

##### **はじめに** 

本連載では、 BCP ( Business Continuity Plan :事業継続計画)について IT 技術者が意識しておくべきポイントをお伝えしていきます。 BCP における各リソースのうち、今回は、「外的リソース」について考えていきます。 

##### **外的リソースとは** 

「外的リソース」とは社会インフラや外部サービス、業務委託先、サプライチェーンなど、「自組織では直接対策をとることができない」リソースのことです( **表** 1)。社会インフラには、電気や水道、交通網、通信網などが含まれます。交通網は道路や電車など、通信網は電話などのキャリア回線のことです。外部サービスとは、自組織の事業のために使用する、自組織以外が提供しているサービス注1 ~~で~~ す。これらの「外的リソース」は、事業を続けるために必要不可欠なものでありながら、これまでの連載で説明したような「そのリソース自体への直接的な対策」が打ちにくいものです。自組織で管理している情報システム( IT リソース)であれば、耐震対策や予備機の準備などができますが、自組織以外が提供しているサービスやキャリア回線に対して、対策をとることは難しいでしょう。では、外的リソースに対しては何を意識しておけばよいのでしょうか? 「外的リソースが使えるように対策をとる」のではなく、「外的リソースが使えない場合にどう事業を継続するかを考えておく」ことがポイントになります。それでは、脅威による外的リソースと自組織の事業への影響を整理してみましょう。 

- **・自然災害による影響** 

社会インフラは断線、断水、損壊などにより機能不全となり、自組織の事業活動に影響あり。外部サービス、業務委託先、サプライチェーンなどは業務システムや建屋、要員の被災によりサービス停止や業務停止が発生し、自組織の事業活動に影響あり 

- **・感染症による影響** 

   - 人が罹患し、事業継続が困難になると、社会インフラ(交通網など)、外部サービス、業務委託先、サプライチェーンなどの業務レベル(稼働率)が低下する。結果、自組織の事業活動に影響あり 

- **・サイバー攻撃による影響** 

   - 社会インフラや外部サービス、業務委託先、サプライチェーンなどで利用されている情報システムが被害を受け、サービス提供が停止、サービスレベルが低下。結果、自組織の事業活動に影響あり 

##### **脅威への対応** 

前節でお伝えしたように、外的リソースへの対応は、外的リソースが機能不全/停止した場合を考えて、どう事業継続するか、を考えることになります。対応としては「外的リソース使用不可時の代替」と「代替ができない場合の事業継続判断」に分けて考えるとよいでしょう。社会インフラなど、外的リソースの利用が事業継続上必須であり、そのリソースが停止している場合は、復旧を待つしかない場合もあるかもしれません。外的リソース全体に言えることですが、必ず別の手段がとれるわけではないため「復旧を待つ」という判断もあり得ることを意識しておいてください。検討の軸としては、これまでの連載と同様に「1事前対応」「2 初動対応」「3復旧対応」の 3 つのフェーズで整理してみます。他リソースと同様、外的リソースへの対応も事前対応が非常に重要です。考えるポイントは「外的リソース停止時の代替」です。代替できない場合の「復旧を待つ」という判断も含めて、判断基準を考えるのも事前対応になります。 

###### **1被害を極小化するための「事前対応」** 

###### **<u>社会インフラ</u>** 

社会インフラには、電気、水道、交通網、通信網など、さまざまなものがあり、個々に対応は異なりますが、考え方は前述のとおり、「代替」または「代替できない場合の判断」となります。いくつかの対応を例示します。停電が発生することを想定し、自家発電設備や蓄電池を準備しておくことや、ビルに非常用電源が備わっている場合は、有事の際の利用契約をしておくことが大切です。とくに、ビルに非常用電源が備わっていても、有事の際に供給されるのはビルの共有スペースだけで、各フロアには供給されない、といったことがあります。拠点で事業を継続する場合は、停電対応を確認しておきましょう。断水への対応としては、人命の観点では備蓄水の準備があります。また、事業にて水を使用する必要がある場合は、予備の水を用意しておく「貯水」のほか、限られた水を使用する優先事業を決めておく「節水」や、すぐにできることではありませんが、井戶水などをあらかじめ用意しておくことなどが考えられます。どの対応も難しい場合は、「復旧を待つ」という選択肢も考えておく必要があるかもしれません。また、交通網の停止に対しては、勤務場所の変更(別拠点の要員で実施)やリモートワークへの切り替えなどが考えられるため、事前に変更や切り替えのための準備をしておきます。「どの活動を別拠点に移すのか」「リモートワーク用の IT 機器の準備」などが考えられます。自然災害の場合は、自組織も被害を受けていることが想定されるため、リモートワークへ切り替えるか、被害を受けていない別拠点の要員で実施するか、状況による判断基準を整備しておくとよいでしょう。通信網の被害の場合は、事業継続に大きな影響が考えられます。インターネット自体が完全に停止することは考えにくいと言われていますが、電話などのキャリア回線が使用できなくなる状況が想定されます。よって、複数回線(複数キャリア)の契約をしておくことや、衛星電話などの別手段を用意する、通信網を使わない手段での活動を考えておく、などが対応となります。 

###### **<u>外部サービス</u>** 

外部サービス、たとえばメールサービス、人事・経理システムや、 PaaS や IaaS 上に構築した業務システムなどが停止してしまったら、事業に大きな影響があり、困ってしまいます。 IT リソースと同じように対応を考えたいところですが、外部サービスに何か対策を打つことは難しいため、社会インフラと同様、外部サービスが使えない場合の代替策を考えておきましょう。まず、優先度の高い事業で利用している外部サービス停止時のインパクトの大きさを検討します。サービスの契約書や SLA (サービスレベルアグリーメント)などから、サービス稼働レベルや有事の際の免責事項などを確認し、外部サービス停止のリスクがある場合は、自組織側で対応を考えることになります。場合によっては「外部サービス停止のリスクを許容する」ことも選択肢になるでしょう。具体的な対応については、メールサービスや会議サービスなど、コミュニケーションのために必要なサービスであれば別の手段( 1 つのツールではなく、複数のツールや代替手段)を用意しておくことが必要になります。業務システムや人事・経理システムなどのサービスでは、外部サービスの代わりに、自前のツールで一時的な代替ができないか、手作業で実施(極端な言い方をすれば紙ベースで処理をするなど)できないか、といった観点で代替策を考えておきましょう。同様のサービスを複数契約しておき、有事の際に切り替えたり、自前のシステムをバックアップ用に保有したりといった対策もありますが、費用面から困難だと思いますし、サービスが異なるとデータの移行も簡単ではありません。有事の際に切り替える他サービスの目星をあらかじめつけておくだけでも、意味はあるでしょう。 

###### **<u>業務委託先</u>** 

業務委託先については、外部サービスと同様の対応が考えられます。委託契約の内容を事前に確認し、委託業務停止時のインパクトを整理しておきましょう。また、あらかじめ有事の際の対応やエスカレーションルールについて業務委託先と話し合っておくことも重要です。委託している作業内容によっては、自組織で一時的に引き取って継続する、ということが考えられるかもしれません。システム開発などを委託している場合は引き取ることが難しいかもしれないため、どこまでならできるか、を整理しておきましょう。 

###### **<u>サプライチェーン</u>** 

サプライチェーンは、事業を行ううえでの各プロセスに欠かせない要素です。調達や物流、販売など、 1 つでも止まると事業に大きな影響が発生してしまいます。外部サービスや業務委託とは考え方が異なり、サプライチェーン全体での継続を考えなければなりません。具体的には、調達先の複数化、物流経路の複数化などです。 1 ヵ所でボトルネックになってしまわないように、 1 つの調達先の依存度を下げ、複数の調達先と契約しておくことが事前対応となります。また、汎用的な部品の調達において、あらかじめ複数の調達先と契約するのではなく、有事の際に契約する調達先(部品の代替)を選定しておくことも対応といえます。コストとの兼ね合いにはなりますが、ある程度の在庫を保有しておくということも考えられるでしょう。最近では、サプライチェーン側の BCP 策定状況を取引要件に加えている企業も増えています。自組織と同様の対応をサプライチェーンでも実施しているかを確認することも事前対応の 1 つになるでしょう。 

###### ◆ ◆ ◆ 

ここまでが外的リソースにおける「事前対応」となります。 **表** 2 に整理します。 



###### **2発生/発災時の被害拡大を防ぐための「初動対応」** 

次に、有事の際の「初動対応」についてです。「事前対応」にしっかりと取り組んだうえで、「初動対応」では基準に沿った判断をすることになります。 

###### **<u>社会インフラ</u>** 

社会インフラの被害状況を確認したうえで、「事前対応」で整理した代替手段での対応や、代替できない場合の待機などを判断します。具体的には、停電対応(自家発電設備切り替えなど)の判断や、電気が使える事業拠点への切り替え、リモートワークの実施などの決定・指示です。自家発電設備への切り替えができた場合も、電気が使用できる時間は限られているため、その後の行動や事業継続方針を早く決定し、指示を出すことになります。断水対応としては、水道復旧までの間、貯水の利用や節水の指示を行うことになります。交通網の障害時は移動の取りやめ(リモートワークへの切り替え)、別手段での移動(自然災害発生時は極力移動しない)などが考えられ、通信網の障害時はキャリア切り替え(複数契約している場合)や衛星電話などの代替手段の利用を行います。一方で、代替手段がなければ、事前対応であらかじめ決めておいた基準に基づき「社会インフラの復旧を待つ(そのほかの対応が可能な活動を行う)」という判断が考えられます。 

###### **<u>外部サービス</u>** 

まず、外部サービスの被害を確認します。どのサービスが使えないのか、サービスの契約部門からサービス提供元への確認を行うことになります。その際、復旧の見込みが立っているかも確認しておきます。次に、該当サービスの停止による影響を整理し、関係部門に対応を連絡することになります。復旧見込みの有無やその内容により、事前対応で整理した基準に基づき、「復旧を待つ」かサービスの切り替え、自組織での引き取り、サービスの新規契約などの判断・指示を行います。 

###### **<u>業務委託先</u>** 

外部サービスと考え方は同じです。まずは被害状況と復旧見込みを確認します。そのうえで、復旧を待つのか、自組織で引き取って対応するのかを検討、判断することになります。 

###### **<u>サプライチェーン</u>** 

サプライチェーンについては、事前対応で検討したとおり、どこで被害が発生しているのかを把握したうえで、代替手段への切り替えを行います。もし部品や材料を複数の調達先と契約しているのであれば、 1 つの調達先が被災して停止した場合、ほかの調達先に普段より多くの発注をかけるなどして被害の影響を最小限にとどめるようにします。複数の調達先と契約していない場合、汎用的な部品などであれば、代替品の調達を行うことになります。調達先がない場合は、事業を止め「復旧を待つ」という判断が必要になるでしょう。 ◆ ◆ ◆ ここまでが「外的リソース」における初動対応となります。 **表** 3 に整理します。 



###### **3被害発生から事業を復旧するための「復旧対応」** 

最後に「復旧対応」について考えましょう。「復旧対応」については、リソースを分けず、共通的に整理します。外的リソースの停止時、「事前対応」「初動対応」により、代替策の実施や復旧を待つ判断をし、事業継続に向けた活動をすることになります。外的リソースが復旧するまではその状態のまま、平常時に比べると低いレベルでの事業継続を行っている状態です。代替策をとっていた場合は、外的リソースの復旧見込みに対し、通常の活動に戻していく対応が必要になります。外的リソースの復旧見込みを把握し、完全復旧前から切り戻し活動の検討を始めましょう。外部サービスや業務委託の代替策において、たとえば手作業で記録をとっていたり、自組織で引き取って対応を行ったりしている場合は、切り戻し後に、元の外部サービスや委託先に処理を登録したり、対応を引き継いだりすることになりますので、切り戻しの方法なども早めに検討を進めるとよいでしょう。外的リソースの完全復旧により、通常時の事業に戻すことができれば、復旧対応は完了です。事後活動になりますが、代替策や切り戻しの中での改善点などを整理しておくとよいでしょう。ここまでが外的リソースにおける「復旧対応」となります。 **表** 4 に整理します。 



##### **まとめ** 

今回は「外的リソース」について、対応のポイントを整理しました。これまでと比べて、外的リソースにもさまざまなものがあり、それぞれの対応が異なっていることが見えてきました。一方で、大きなくくりで見ると、外的リソースは直接対策をとることができないものであるため、外的リソース停止時の「代替」「代替できない場合の判断」という選択肢を用意することが共通のポイントでもありました。自組織ではコントロールできないリソースについて、事業におけるその重要度を十分に考慮したうえで、どの程度の期間まで外的リソースの停止が許容できるのかを検討しておきましょう。その検討結果を踏まえて、自組織で対応する範囲と外的リソースに依存する範囲を整理し、 BCP に盛り込んでおく必要があります。第 2 回(本誌 2023 年 2 月号)から今回まで、リソースと脅威を軸にして BCP での対応を考えてきました。 BCP というと、脅威による被害想定があり、対応の行動方針があり、復旧優先業務があり、その中に各リソースが登場するような流れで考えられていることがほとんどですが、本連載のように、リソースから事業継続を考えてみると、リソースによる対応の違いや難しさなどが今まで以上に見えてきたのではないでしょうか。これからの BCP は、リソースを軸として、脅威に依らない行動手順や対応計画を考えていくことが求められてきます。本連載で整理したポイントが参考になれば幸いです。 

##### **次回に向けて** 

次回が本連載の最終回となります。これまで整理した各リソースの対応を参考に、 BCP の活動の 1 つである「訓練」にフォーカスしてみます。 BCP 訓練は、検討した BCP の評価や定着化のために重要な活動です。 BCP 訓練の必要性や目的に応じた実施手法、定着化のための取り組みポイントなどを紹介します。訓練を計画する際に大事なことは「どのような訓練シナリオを組み立てるか」です。訓練シナリオを作る際に、各リソースの「事前対応」「初動対応」「復旧対応」のどこにスポットをあてるか、といった考え方もお伝えしたいと思います。引き続きよろしくお願いします。 

##### **column 外的リソースの対応事例** 

東日本大震災の際に、製造業のある企業では、物流センターの被災やガソリンの不足により、製品の配送などの物流が止まってしまう懸念がありました。この企業では、配送ルートの変更および限られた配送トラックの使い方の変更により、出荷遅延・在庫備蓄に対応しました。 

- **・配送ルートの変更** 

   - 通常の配送ルートは被災地を通るため移動が困難であることから、ほかの物流セうかい 

   - ンターを通る迂回ルートを確立。また、通常の出荷センターからの出荷ではなく、製造拠点からの直接配送を実施 

- **・限られた配送トラックの使い方変更** 被災地への支援物資を運んだ帰りのトラックを確保し、空のトラックに部品や製品を積載して配送 

- **・部品メーカーの東北地区への出荷停止対応** 

   - 関東地方の1拠点に部品を集約し、完成品配送のトラックを流用して東北地区に入庫複数の調達先を準備しておくだけでなく、限られた稼働リソースを有効活用することも外的リソースへの対応の1つになります。 

- 注1) インターネットを経由して提供されているサービスやプラットフォーム。たとえばサービスとしてはメールサービス、人事・経理システム、Web会議サービス、 SNS、検索サービス、翻訳サービスなどです。プラットフォームとしてはPaaS、 IaaSなどが考えられます。 

   - (本文に戻る) 

##### **<mark>最終回</mark> しくみがわかるともっとハマる** 

#### **楽しい** 

## **Flutter** 

杉本真二(すぎもとしんじ) TechTrain 



<u>@sugitlab</u> 株式会社TechBowl、 

#### **第** 

#### **7 回パッケージの作成と利用** 

Googleが開発するクロスプラットフォーム開発用フレームワーク「Flutter」の基本的な扱い方としくみを解説します。Flutterは比較的簡単に動くものが書ける一方で、その裏 ~~<u>側</u>~~ ではさまざまなことが行われています。Flutterのしくみに一歩足を踏み入れ、Flutterにハマっていきましょう。 

#### **はじめに** 

本連載は今回で最終回です。最後のテーマはパッケージの開発です。アプリケーションの開発を進める過程で、複数のプロジェクトで共通して利用したい実装が出てくることでしょう。そんな実装をパッケージとして取り扱い、再利用するしくみについて取り上げます。 

#### **Flutterパッケージ振り返り** 

Flutter アプリケーションの開発では必然的にさまざまなパッケージを利用することになります。純粋に Flutter SDK だけで開発することももちろん可能ですが、よほどの理由がない限りはパッケージを利用することになるでしょう。パッケージの利用といえば、 **flutter pub add package_name** というコマンド( **<u>package_name</u>** は各種パッケージの名称)でプロジェクトに追加して利用しているものだ、ということは多くの Flutter 利用者が学習の初期に理解することだと思います。この連載でも provider パッケージ注1 ~~を~~ 利用する際に **pub add** コマンドを使用しています。今回はこのようなパッケージを自作し、利用する方法とそのしくみについて実際に手を動かしながら理解していきましょう。 

#### **パッケージはどこから来て、どこで管理されているのか** 

Flutter アプリケーションを開発する際に追加したパッケージは開発環境(手元の PC )にダウンロードされ、適切に管理されています。 Flutter のプロジェクトには、そのプロジェクト内で利用しているパッケージが pubspec.yaml で管理されており、 **flutter pub get** を実行してまとめてダウンロードします。では、これらのパッケージはどこからやってくるのでしょうか。これはご存じの方が多いと思いますが、 pub.de ~~v~~ <u>注2</u> ~~か~~ らやってきます。 

pub.dev は Dart と Flutter のための公式のパッケージリポジトリです。 pub のコマンドを利用すると、何も指定しない場合は自動的に pub.dev のパッケージリポジトリから当該パッケージを検索するしくみになっています。 **flutter pub add provider** と実行した場合は、 <u>https://pub.dev/packages/provider からダウンロードします。</u> 

では、このダウンロードされたパッケージはどこに保存されるのでしょう。実は、ホームディレクトリ( OS によって異なります)直下に .pub-cache というフォルダが用意され、その中に保存されています。手元の PC に複数の Flutter プロジェクトがあり、それらの複数のプロジェクトが同じパッケージを利用していたとしても、重複してダウンロードしなくてもよいように一元管理されています。たとえば、 macOS では Provider パッケージは次のフォルダに保存されます。 

/Users/UserName/.pub-cache/hosted/pub.dev/provider-6.0.5 (UserNameにはログインユーザー名が入る) 

この例にあるとおり、パッケージ名の末尾にバージョン番号を付けたフォルダ名で管理されますので、同じパッケージであってもバージョンが異なる場合は別に管理されるので安心です。 

###### **COLUMN** 

##### **.pub-cache掃除のススメ** 

Flutterでアプリケーション開発を続けていると、使用していないパッケージがこの.pub-cacheに溜まっていきます。ストレージを圧迫しているかもしれませんので、不要なものについては定期的にお掃除することをお勧めします。.pub-cacheから消してしまっても、各Flutterプロジェクトで **flutter pub get** を実行した際に不足分はpub.devからダウンロードされるので問題はありません。 

#### **独自のパッケージを開発する** 

独自のパッケージを開発する場合は Flutter から提供されているパッケージ用のテンプレートを利用すると便利です。次のコマンドでプロジェクトを作成しましょう。 

**$ flutter create --template=package my_package ←** **<u>my_package</u> にはパッケージ名を設定する** 



##### **HEY SD READERパッケージを作る** 

ここではサンプルとして “HEY SD READER” パッケージを作ります。 

###### **$ flutter create --template=package hey_sd_reader** 

プロジェクトの生成が終了したら、まずは lib/hey_sd_reader.dart を開きます。テンプレートでは最初に「 1 を加えるメソッド」が用意されています。このパッケージはシンプルに Dart の Counter クラスを提供するパッケージです。 Flutter のパッケージを作ってみようと考えると、まずは独自の Widget をパッケージとして作ってみよう、という方が多いのではないでしょうか。そこで、 HEY SD READER パッケージでは “HEY SD READER” というダイアログを表示するボタンを提供するパッケージにしてみようと思います。 



##### **パッケージの中身を実装する** 

ここは本題ではないのでサクッと済ませます。 lib/hey_sd_reader.dart の中身を次のように指定しました。 

###### **export 'src/hey_sd_reader.dart';** 

Dart および Flutter のパッケージでは、パッケージを import して使う際は lib フォルダ直下のファイルを参照するようになっています。つまり、パッケージが提供するクラスや関数については lib 直下に実装します。一方、公開する必要がないコードについては lib 直下ではなく、 lib/src 以下に記述します。パッケージには直接関係がない実装 

- や、公開しているクラスの内部処理など、パッケージ利用者には使ってほしくない実装を lib/src 以下に実装します。このパッケージでは Widget のクラスを実装して公開します。 lib/hey_sd_reader.dart に直接 Widget の実装を記述することもできますが、パッケージが公開する実装とそうではない実装を明確に分け、間違って公開すべきではないものが公開されないよう、実際の実装を lib/src 以下に移動させています。この形式で分割しておくと、公開されるものがどれか、という重要な情報を export しているファイル、という形式で簡単に管理できるようになります。今回のように **export 'src/hey_sd_reader.dart';** を記述しておくと、 **図** 1のように公開/非公開を管理できます。 

###### **▼図1 「export 'src/hey_sd_reader.dart';」と記述した場合の公開の範囲** 

**lib ├─ hey_sd_reader.dart └─ src ├─ hey_sd_reader.dart ←公開 ├─ some_secret_code.dart ←非公開 ├─ some_internal_code.dart ←非公開 └─ some_testing_code.dart ←非公開** 

lib/src/hey_sd_reader.dart には **リスト** 1のようにダイアログを表示できる Widget を実装しました。 

###### **▼リスト1 独自パッケージの実装(lib/src/hey_sd_reader.dart)** 

**import 'package:flutter/material.dart'; class HeySdReader extends StatelessWidget { const HeySdReader({super.key}); @override Widget build(BuildContext context) {** 

**return ElevatedButton( onPressed: () { showDialog<void>( context: context, builder: (BuildContext context) { return AlertDialog( title: const Text('HEY SD READER!!'), actions: [ TextButton( onPressed: () {}, child: const Text('OK'), ), ], ); }, ); }, child: const Text( 'HEY SD READER', ), ); } }** 

パッケージの中身の準備はこれで完了です。詳細は GitHub のリポジトリ注3 ~~に~~ て公開しているので、そちらを参照してください。 



##### **パッケージを公開するための準備をする** 

パッケージを pub.dev に公開するにはパッケージに関する情報をまとめておく必要があります。 

###### **・** README 

pub.devのReadmeページに表示されるパッケージの解説文として使用される。可能な限り詳しく書く 

- **・** LICENSE 

   - ライセンス文として使用される。MITライセンスなど作成したパッケージにとって適切なライセンス条項を記述する 

- **・** CHANGELOG 

pub.devのChangelogページで使用される。追加した機能や、廃止した機能などを記述しておく 

- **・** example 

pub.devのExampleページで使用される。パッケージの利用方法を具体的に記したソースコードを含めておくことで利用者が簡単にパッケージを使い始められる 



##### **pub.devでアカウントを登録する** 

<u>https://pub.dev にアクセスし、右上の[</u> Sign in ]をタップし、 Google アカウントでサインインします( **図** 2)。サインインに成功すると、トップに[ My pub.dev ]というメニューが表示されますので、そのメニューから、[ Create publisher ]を選択します( **図** 3)。 



Create a verified publisher ページで所有されているドメイン名を入力します( **図** 4)。ドメインをお持ちでない場合はあらかじめ Google Domains などで取得します(有料です)。無事に有効なドメインであると認証されれば、 My pub.dev ページにドメイン名が表示されます。パッケージはドメインに所属する形式で配布します。 **図** 5のキャプチャでは “sugitlab.dev” が登録したドメインです。 





##### **pub.devに公開する** 

pub.dev に公開しましょう。なお、公開にあたり次の要点を押さえておいてください。 

- ・パッケージはインターネットで全世界に公開される 

- ・一度公開されたパッケージは取り下げることができない 

pub.dev のポリシーについては pub.dev のペー ~~ジ~~ <u>注4</u> ~~を~~ 参考にしてください。まずは公開前のテストです。公開したいパッケージのプロジェクトで次を実行してください。 

**$ flutter pub publish --dry-run** 

エラーが出た場合は対処が必要ですが、ここまでの手順に従っていれば大丈夫です。次が本番です。 **flutter pub publish** を実行します。 **図** 6の最下行のように「 pub.dev にリリースしますか?」と聞かれていますので、 y をタイプします。少し待てば **図** 7のようにリリースに成功します。 



###### **▼図7 pub.devへのリリース成功の出力** 

**Do you want to publish hey_sd_reader 0.0.1 to https://pub.dev (y/N)? y Uploading... Successfully uploaded https://pub.dev/packages/hey_sd_reader version 0.0.1.** 

リリースに成功していれば、 <u>https://pub.dev/packages/hey_sd_reader</u> にパッケージが公開されていることが確認できます( **図** 8)。次に、[ Admin ]タブを表示し、 Publisher をひも付けます( **図** 9)。無事にひも付けが成功すると、パッケージのページに Publisher が表示されます( **図** 10)。 



#### **pub.devに公開せずパッケージを利用する** 

pub.dev へ公開したくないが、複数のプロジェクトから利用したい個人的なパッケージを作りたい場合は、 GitHub から取得するという方法も採れます。 



##### **GitHubにあるパッケージを直接取得して利用する** 

GitHub にあるパッケージを利用する方法はとてもシンプルです。 **リスト** 2のように pubspec.yaml の dependencies に、 git と指定し、リポジトリのパスを指定します。 SSH で指定しておくと private リポジトリでも簡単に利用できます。 

###### **▼リスト2 GitHubのパッケージを取得する設定(pubspec.yaml)** 

**dependencies: flutter: sdk: flutter hey_sd_reader: git: url: git@github.com:sugitlab/hey_sd_reader.git** 

url の指定は <u>https://github.com/sugitlab/hey_sd_reader.git と、</u> URL で指定しても利用できますが、 private リポジトリの場合は SSH の設定をしたうえで、 **リスト** 2のとおり 

git@github.com:sugitlab/hey_sd_reader.git と指定するようにしてください。 

pub.dev にあるパッケージならバージョン番号を指定しますが、 GitHub から利用する場合はバージョン番号を指定できません。そのため、何もしなければ最新の状態が取り込まれます。任意の時点の変更を取り込む場合は、 **リスト** 3のように ref を指定します。 

###### **▼リスト3 GitHubから任意の時点のパッケージを取得する設定(pubspec.yaml)** 

**hey_sd_reader: git: url: git@github.com:sugitlab/hey_sd_reader.git ref: branch_name ↑** **<mark>branch_name には任意のブランチ名を設定する</mark>** 

当該のリポジトリを、モノレポ( 1 つのリポジトリに複数のパッケージがある)として開発している場合は、利用したいパッケージへのパスを追加で指定します。たとえば、 multiple_packages というパッケージがあり、 src/packages ディレクトリ以下に hey_sd_reader を管理しているとします。その場合は **リスト** 4のように path を指定します。 

###### **▼リスト4 リポジトリがモノレポの場合の設定(pubspec.yaml)** 

**hey_sd_reader: git: url: git@github.com:sugitlab/multiple_packages.git path: src/packages** 

詳しくは Dart 言語の仕様として公式のドキュメン ~~ト~~ <u>注5</u> ~~が~~ ありますので、そちらを参照してください。 

#### **最後に** 

今回はパッケージの作り方とそのしくみについて取り扱いました。本連載の内容を一通り把握すると、中規模程度の Flutter アプリケーションで必要とされる技術とそのしくみについては十分にカバーできると考えています。もちろん、もっと大規模な実装や複雑な実装をするにはこの連載では取り扱っていない、外部 API の利用、 Firebase などの BaaS の利用、プラットフォームの Method Channel の利用などさまざまな知識が必要です。本連載で Flutter のおもしろさに触れ、もっともっとと、新しいことにチャレンジされる方が増え、 Flutter がもっと盛り上がることを願っています。 

### **AWS活用ジャーニー** 

###### **第8回** 

###### **Amazon Route 53、ACM、ELB** 

武田隆志(たけだたかし)クラスメソッド株式会社 <u>https://dev.classmethod.jp/author/takeda-takashi/</u> 





<u>@2017takeda @TAKEDA-Takashi</u> 



今回は、DNSサービス「Route 53」、SSL証明書発行サービス「ACM」、ロードバランササービス「ELB」を使った、独自ドメインでHTTPS通信ができるマルチAZ構成のWebシステムを構築します。 

#### **Amazon Route 53とは** 

Amazon Route 53 (以下、 Route 53 )とは、高い可用性とスケーラビリティを備えた DNS サービスです。正引きや逆引きといった通常の DNS 機能に加え、ヘルスチェックやトラフィック分散のための機能を提供します。さまざまな AWS サービスが依存しているということもあり、 SLA が 100 %に設定されているのも特徴です。 



##### **ホストゾーン** 

ホストゾーンは example.com などのドメインおよびそのサブドメインのレコードを管理するためのコンテナです。インターネット上で名前解決ができるパブリックホストゾーンと、 Amazon VPC 内で名前解決をするプライベートホストゾーンが提供されています。ドメインは Route 53 で登録するか、別のレジストラで登録したものを移管あるいは委譲して管理できます。 



##### **ヘルスチェック** 

Route 53 には指定したエンドポイントが正常にアクセスできるのかヘルスチェックを行う機能があります。エンドポイントに障害が発生した場合、トラフィックが正常なエンドポイントに向かうようルーティングを変更します。これをDNS **フェイルオーバー** といいます。 DNS フェイルオーバーを設定して、アクティブ/アクティブおよびアクティブ/スタンバイを構成できます。複数のリソースを同時に使用し、それぞれにルーティングさせたい場合はアクティブ/アクティブ構成を取ります。ヘルスチェックで検出された異常なリソースはクエリの応答に使用しなくなります。通常使用するプライマリリソースと、普段はスタンバイ状態として備えておくセカンダリリソースを用意する場合はアクティブ/スタンバイ構成を取ります。すべてのプライマリリソースで異常が発生した場合、正常なセカンダリリソースのみクエリの応答に使用します。 



##### **ルーティングポリシー** 

Route 53 ではレコードを作成する際にルーティングポリシーを指定します。ルーティングポリシーとはクエリの応答に、どのレコードを使用するのか決定するルールのことです。現在 Route 53 では 8 種類のルーティングポリシーがサポートされており、用途に合わせて使い分けます( **表** 1)。通常の 1 レコードに対して 1 リソースをひも付ける用途や、単純な DNS ラウンドロビンであればシンプルルーティングポリシーで十分です。しかしヘルスチェックを行えないため異常が発生しているリソースも使用してしまいます。ほかの 7 種類のルーティングポリシーはいずれもヘルスチェックの設定ができま 

###### す。地理的近接性ルーティングポリシーと IP ベースルーティングポリシーはプライベートホストゾーンでは使用できません。 



#### **ACMとは** 

AWS Certificate Manager (以下、 ACM )は、 SSL / TLS 証明書を無料で簡単に作成・管理できるサービスです。 AWS Private Certificate Authority ( Private CA )を使用することで、プライベートな証明書も作成できます。現在、インターネット上の Web サイトの多くが HTTPS で通信されます。これは暗号化された HTTP 通信で、安全にクライアント-サーバ間で通信できます。証明書は Web サイトが信頼できるかを確認し、暗号化通信をするためにも重要なものです。 ACM を使用することで簡単に HTTPS をサポートした Web サイトを公開できます。 



##### **ドメインの所有権の検証** 

ACM で証明書を作成するためには、発行されるドメインの所有権を証明する必要があります。証明する方法として DNS 検証と E メール検証の 2 種類提供されています。 

DNS 検証とは、 ACM から提供された DNS レコードを DNS サーバに登録する方法です。指定されたレコードを DNS サーバに登録することで、そのドメインの所有者であることを証明するわけです。 E メール検証とは、 ACM から所定のメールアドレス宛にメールが送信され、そのメールに含まれている承認リンクにアクセスすることでドメインの所有者であることを証明する方法です。発行した証明書の有効期限は約 1 年で、毎年更新する必要があります。 DNS 検証は自動更新に対応していますが、 E メール検証は対応していません。そのため DNS 検証を使用することが推奨されています。またあとから E メール検証を DNS 検証に切り替えることはできませんので注意してください(再作成が必要です)。 

Route 53 でドメイン管理をしておりマネジメントコンソールで証明書を発行する場合、 **ワンクリックで** DNS **の設定が完了** します。また AWS CloudFormation という IaC ( Infrastructure as Code )サービスでも、証明書の発行から DNS 検証までを自動で行えます。 

#### **ELBとは** 

Elastic Load Balancing (以下、 ELB )は、 1 つ以上のアベイラビリティゾーン( AZ )内の複数のターゲットにトラフィックを分散するロードバランササービスです。 ELB では機能の異なる 3 種類のロードバランサを提供しています( **表** 2)。 



Application Load Balancer ( ALB )は L7 のロードバランサです。パスベースルーティング、スティッキーセッションなど Web システムを負荷分散するうえで必要となる機能を提供します。 

Network Load Balancer ( NLB )は L4 のロードバランサです。 TLS のオフロードや UDP がサポートがされ、静的 IP アドレス( EIP )を設定できます。 ALB をターゲットに指定できるため、 ALB の前段のロードバランサとして配置できます。 

Gateway Load Balancer ( GWLB )は L3 Gateway および L4 ロードバランサの機能を提供します。おもにネットワーク型のセキュリティ製品を VPC に追加するためなどに使用します。 GWLB は GWLB エンドポイント( GWLBE )を通じてパケットを受け取り中継します。その際トンネリングプロトコルである Geneve を使用し、セキュリティ製品を透過的に追加することを可能にします。 



##### **マルチAZの必要性** 

クラウドでシステムを設計するうえでDesign for Failureがとても重要です。「いかに障害を起こさないか」ではなく「障害は起きる前提で、どうシステムを落とさないか」という考え方です。この「障害は起きる」というのは利用者が構築したシステムに限りません。 AWS のサービス障害や AZ 障害、リージョン障害も含まれます。近年の AZ 障害としては 2021 年 2 月に起きたものが記憶に新しいでしょう注1 ~~。~~ 障害に備える基本的な方法としてマルチ AZ 構成があります。複数の AZ にサーバを配置し、ロードバランサを使用して適切にリクエストを振り分けることで耐障害性を高められます。 

**マルチAZ構成の実践的なWebシステムの構築** 

実践的なシステム構築の例として、次のような要件の Web システムを構築してみましょう。 

- ・独自ドメインでアクセスできる 

- ・ HTTPSでアクセスできる 

- ・ AZレベルの障害が起きてもダウンしない今回紹介したサービスを含め、次のサービスを使用します。 

- ・ Route 53 

- ・ ACM 

- ・ ELB(ALB) 

- ・ VPC 

- ・ ECS(Fargate)/ECR 

なお、今回は作業工程をすべて CLI のコマンドで紹介しています。普段マネジメントコンソールしか触らない方は、何の設定をしているのか画面と比較しながら進めてみるとよいでしょう。 



##### **構築するシステムの構成図** 

一般的なマルチ AZ 構成の Web システムを構築します( **図** 1)。今回は 2AZ 構成としていますが、より高い可用性が求められる場合は 3AZ 構成にすることも検討しましょう。また、本筋ではないので DB は構成から外しています。 RDS を使用しマルチ AZ 配置をすれば簡単に追加できますので、一通り構築できた方は追加タスクとしてやってみてください。システム構築に使用した一式は GitHub で公開しています注2 ~~。~~ 自身の環境で同じ構成を構築してみたい方や、タスク定義などの完全なリストを確認したい方などは参考にしてください。コマンドを列挙する中で、環境依存の値などは一部変数にしています( **図** 2)。適宜読み替えてください。 

###### **▼図2 スクリプトで使用する変数の定義** 

**AWS_ACCOUNT_ID=123456789012 REGION=ap-northeast-1 AZ1="${REGION}a" AZ2="${REGION}c"** 

**ECR_REPO_NAME=ha-web-system ECS_TASK_EXEC_ROLE_NAME=ecsTaskExecutionRole ECS_CLUSTER_NAME=ha-web-cluster DOMAIN_NAME=example.com** 



##### **VPCの作成** 

まずはネットワークの構築として VPC を作成していきます。今回は東京リージョンの 2AZ に、パブリックサブネットとプライベートサブネットをそれぞれ作成します( **図** 3)。マネジメントコンソールで VPC を作成する場合、インターネットゲートウェイ( IGW )なども自動で作成されますが、 CLI の場合は自分で作成します。 

###### **▼図3 VPCを作成するスクリプト** 

**VPC_ID=$(aws ec2 create-vpc --cidr-block 10.0.0.0/16 --tag-specifications "ResourceType=vpc,Tags=[{Key=Name,Value=HA-Web}]" --query Vpc.VpcId -- output text)** 

**aws ec2 modify-vpc-attribute --vpc-id "$VPC_ID" --enable-dns-hostnames '{"Value":true}'** 

**SUBNET_PUBLIC1_ID=$(aws ec2 create-subnet --vpc-id "$VPC_ID" --cidr-block 10.0.1.0/24 --availability-zone "$AZ1" --query Subnet.SubnetId --output text) SUBNET_PUBLIC2_ID=$(aws ec2 create-subnet --vpc-id "$VPC_ID" --cidr-block 10.0.2.0/24 --availability-zone "$AZ2" --query Subnet.SubnetId --output text) SUBNET_PRIVATE1_ID=$(aws ec2 create-subnet --vpc-id "$VPC_ID" --cidr-block 10.0.3.0/24 --availability-zone "$AZ1" --query Subnet.SubnetId --output text) SUBNET_PRIVATE2_ID=$(aws ec2 create-subnet --vpc-id "$VPC_ID" --cidr-block 10.0.4.0/24 --availability-zone "$AZ2" --query Subnet.SubnetId --output text)** 

**aws ec2 modify-subnet-attribute --subnet-id "$SUBNET_PUBLIC1_ID" --mappublic-ip-on-launch aws ec2 modify-subnet-attribute --subnet-id "$SUBNET_PUBLIC2_ID" --mappublic-ip-on-launch** 

**IGW_ID=$(aws ec2 create-internet-gateway --query InternetGateway.InternetGatewayId --output text)** 

**aws ec2 attach-internet-gateway --vpc-id "$VPC_ID" --internet-gateway-id "$IGW_ID"** 

**RTB_PUBLIC_ID=$(aws ec2 create-route-table --vpc-id "$VPC_ID" --query RouteTable.RouteTableId --output text)** 

**aws ec2 create-route --route-table-id "$RTB_PUBLIC_ID" --destination-cidr-block** 

###### **0.0.0.0/0 --gateway-id "$IGW_ID"** 

**aws ec2 associate-route-table --subnet-id "$SUBNET_PUBLIC1_ID" --route-tableid "$RTB_PUBLIC_ID"** 

**aws ec2 associate-route-table --subnet-id "$SUBNET_PUBLIC2_ID" --route-tableid "$RTB_PUBLIC_ID"** 

**RTB_PRIVATE_ID=$(aws ec2 create-route-table --vpc-id "$VPC_ID" --query RouteTable.RouteTableId --output text)** 

**aws ec2 associate-route-table --subnet-id "$SUBNET_PRIVATE1_ID" --route-tableid "$RTB_PRIVATE_ID"** 

**aws ec2 associate-route-table --subnet-id "$SUBNET_PRIVATE2_ID" --route-tableid "$RTB_PRIVATE_ID"** 



##### **Dockerイメージの作成とECRへの登録** 

ECS サービスとして動かすコンテナの Docker イメージを作成します( **図** 4)。今回は Node.js を使用したコンテナを Web サービスの本体とし、 nginx をリバースプロキシとして動かします。 /api/hello というパスでリクエストを受け取ると Node.js のプログラムが呼び出され処理をします。作成した Docker イメージは ECR のプライベートリポジトリに登録するため、リポジトリの作成もしています。 

###### **▼図4 ECRにDockerイメージを登録するスクリプト** 

**aws ecr get-login-password | docker login --username AWS --password-stdin "${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com"** 

**aws ecr create-repository --repository-name "$ECR_REPO_NAME" --imagescanning-configuration scanOnPush=true** 

**docker build -t webapp ./apps/webapp/ docker tag webapp:latest "${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/${ECR_REPO_NAME}: webapp" docker push** 

**"${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/${ECR_REPO_NAME}:** 

**webapp"** 

**docker build -t nginx ./apps/nginx/ docker tag nginx:latest "${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/${ECR_REPO_NAME}: nginx" docker push** 

**"${AWS_ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/${ECR_REPO_NAME}: nginx"** 

リポジトリの認証に get-login-password コマンドを使用しています。このコマンドは古いバージョンの CLI ではサポートしていません。実行しようとしてエラーになった場合は、バージョンアップを試してみてください。 



##### **タスク定義の登録** 

先ほど登録した Docker イメージをタスクとして動かすためにタスク定義を登録します( **図** 5)。 Web サービスとリバースプロキシのコンテナは分かれていますが、タスク定義でこれらをまとめています ( **リスト** 1)。 image プロパティで指定しているのは、先ほどビルドしてプッシュした Docker イメージです。 nginx コンテナはリバースプロキシとして ALB と webapp を中継する役割をもつためポートマッピングの設定をしています。 

###### **▼図5 タスク定義を登録するスクリプト** 

**aws ecs register-task-definition --cli-input-json file://apps/task-definition.json** 

###### **▼リスト1 task-definition.jsonのコンテナ定義部分** 

**"containerDefinitions": [ { "name": "nginx", "image": "123456789012.dkr.ecr.ap-northeast-1.amazonaws.com/ha-websystem:nginx", "essential": true,** 

**"portMappings": [ { "containerPort": 80, "hostPort": 80, "protocol": "tcp", "appProtocol": "http" } ], "logConfiguration": {** **<mark>(..略..)</mark> } }, { "name": "webapp", "image": "123456789012.dkr.ecr.ap-northeast-1.amazonaws.com/ha-websystem:webapp", "essential": true, "logConfiguration": {** **<mark>(..略..)</mark> } } ]** 



##### **ターゲットグループおよびALBの作成** 

次に ALB を作成します。 ALB はインターネットからリクエストを受信する必要があるためパブリックサブネットに作成します( **図** 6)。 ALB では受け取ったリクエストをルーティングするルールをリスナー、ルーティング先をターゲットグループとして定義します。 EC2 など既存のインスタンスがある場合は、インスタンスをグループに追加しておきますが、今回は ECS サービスで動的に起動されるため、グループだけ作成します。またひとまずここでは HTTP だけ受信できるようにリスナーを作成します。 

**▼図6 ALBを作成するスクリプト** 

**SG_ALB_ID=$(aws ec2 create-security-group --group-name alb-sg --vpc-id "$VPC_ID" --description "Allow all inbound traffic" --output text)** 

**aws ec2 authorize-security-group-ingress --group-id "$SG_ALB_ID" --protocol all - -cidr 0.0.0.0/0** 

**ALB_ARN=$(aws elbv2 create-load-balancer --name web-alb --subnets "$SUBNET_PUBLIC1_ID" "$SUBNET_PUBLIC2_ID" --security-groups "$SG_ALB_ID" --query LoadBalancers[].LoadBalancerArn --output text)** 

**TARGET_GROUP_ARN=$(aws elbv2 create-target-group --name web-target-gr -- protocol HTTP --port 80 --target-type ip --vpc-id "$VPC_ID" --query TargetGroups[].TargetGroupArn --output text)** 

**aws elbv2 create-listener --load-balancer-arn "$ALB_ARN" --protocol HTTP --port 80 --default-actions "Type=forward,TargetGroupArn=${TARGET_GROUP_ARN}"** 

なお、 ALB にアタッチする SG でインバウンドをすべて許可していますが、テスト用途であれば IP を絞るなどしたほうがセキュアになります。このシステムを作成してしばらくすると不正なリクエストを多く観測できました。 



##### **ECSクラスターおよびECSサービスの作成** 

ECS サービスを動かしていくクラスターを作成し、先ほど登録したタスク定義から ECS サービスを作成していきます( **図** 7)。ここまで問題なく作成できると、 ALB に割り当てられているホスト名注3 ~~で~~ アクセスできます。 ECS タスク実行ロールには、 ECR からイメージをプルする権限や CloudWatch Logs へログを出力する権限が必要なため、 AWS 管理ポリシーを追加でアタッチしています。また今回、 ECS サービスはプライベートサブネットに配置します。そのため NAT ゲートウェイか VPC エンドポイントを用意しなければ ECR や CloudWatch Logs と通信できません。今回は VPC エンドポイントを使用する方式で、スクリプトで作成しているように 4 つのエンドポイントが必要となります。 

###### **▼図7 ECSクラスターとECSサービスを作成するスクリプト** 

**aws iam create-role --role-name "$ECS_TASK_EXEC_ROLE_NAME" --assumerole-policy-document file://apps/ecs-tasks-trust-policy.json aws iam attach-role-policy --role-name "$ECS_TASK_EXEC_ROLE_NAME" -- policy-arn arn:aws:iam::aws:policy/servicerole/AmazonECSTaskExecutionRolePolicy** 

**aws iam attach-role-policy --role-name "$ECS_TASK_EXEC_ROLE_NAME" -- policy-arn arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly aws iam attach-role-policy --role-name "$ECS_TASK_EXEC_ROLE_NAME" -- policy-arn arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy** 

**aws ecs create-cluster --cluster-name "$ECS_CLUSTER_NAME"** 

**SG_ECS_TASK_ID=$(aws ec2 create-security-group --group-name ecs-task-sg -- vpc-id "$VPC_ID" --description "ECS Task" --output text)** 

**aws ec2 authorize-security-group-ingress --group-id "$SG_ECS_TASK_ID" -- protocol all --source-group "$SG_ALB_ID"** 

**SG_VPC_ENDPOINT_ID=$(aws ec2 create-security-group --group-name vpcendpoint-sg --vpc-id "$VPC_ID" --description "VPC Endpoint" --output text)** 

**aws ec2 authorize-security-group-ingress --group-id "$SG_VPC_ENDPOINT_ID" -- protocol all --cidr 0.0.0.0/0** 

**aws ec2 create-vpc-endpoint --vpc-id "$VPC_ID" --vpc-endpoint-type Interface -- service-name "com.amazonaws.${REGION}.ecr.dkr" --subnet-ids "$SUBNET_PRIVATE1_ID" "$SUBNET_PRIVATE2_ID" --security-group-ids "$SG_VPC_ENDPOINT_ID" aws ec2 create-vpc-endpoint --vpc-id "$VPC_ID" --vpc-endpoint-type Interface -- service-name "com.amazonaws.${REGION}.ecr.api" --subnet-ids "$SUBNET_PRIVATE1_ID" "$SUBNET_PRIVATE2_ID" --security-group-ids "$SG_VPC_ENDPOINT_ID" aws ec2 create-vpc-endpoint --vpc-id "$VPC_ID" --service-name "com.amazonaws.${REGION}.s3" --route-table-ids "$RTB_PRIVATE_ID" aws ec2 create-vpc-endpoint --vpc-id "$VPC_ID" --vpc-endpoint-type Interface -- service-name "com.amazonaws.${REGION}.logs" --subnet-ids** 

**"$SUBNET_PRIVATE1_ID" "$SUBNET_PRIVATE2_ID" --security-group-ids "$SG_VPC_ENDPOINT_ID"** 

**aws ecs create-service --cluster "$ECS_CLUSTER_NAME" --service-name webservice --task-definition web-service --desired-count 2 --launch-type FARGATE -- network-configuration "awsvpcConfiguration={subnets= [${SUBNET_PRIVATE1_ID},${SUBNET_PRIVATE2_ID}],securityGroups= [${SG_ECS_TASK_ID}]}" --load-balancers "targetGroupArn=${TARGET_GROUP_ARN},containerName=nginx,containerPort= 80"** 



##### **ホストゾーンおよびDNSレコードの作成** 

作成した ECS サービスに独自ドメインを割り当てます( **図** 8)。まずはドメインを管理するために Route 53 でホストゾーンを作成します注4 ~~。~~ 続いてホスト名から ALB の IP アドレスを引ける(正引き)ように A レコードを追加します。この際、通常の DNS サーバであれば IP アドレスを指定しますが、 Route 53 では **エイリアスレコード** という機能が提供されており、直接 IP アドレスを指定せずに A レコードを追加できます。 

###### **▼図8 ホストゾーンとDNSレコードを作成するスクリプト** 

**HOSTED_ZONE_ID=$(aws route53 create-hosted-zone --name "$DOMAIN_NAME" --caller-reference $(date +%Y-%m-%d-%H-%M-%S) --query HostedZone.Id --output text | awk -F'/' '{print $NF}')** 

**ALB_DNS_NAME=$(aws elbv2 describe-load-balancers --load-balancer-arns "$ALB_ARN" --query 'LoadBalancers[].DNSName' --output text)** 

**ALB_HOSTED_ZONE_ID=$(aws elbv2 describe-load-balancers --load-balancerarns "$ALB_ARN" --query 'LoadBalancers[].CanonicalHostedZoneId' --output text)** 

**aws route53 change-resource-record-sets --hosted-zone-id "$HOSTED_ZONE_ID" --change-batch '{"Changes":[{"Action":"CREATE","ResourceRecordSet": {"Name":"www.'"$DOMAIN_NAME"'","Type":"A","AliasTarget":** 

**{"HostedZoneId":"'"$ALB_HOSTED_ZONE_ID"'","DNSName":"'"$ALB_DNS_NAME "'","EvaluateTargetHealth":false}}}]}'** 

この設定ができると、 ALB に自動で振られたホスト名ではなく独自ドメイン注5 ~~で~~ アクセスできます。 



##### **証明書の作成とELBへの割り当て** 

独自ドメインでの HTTP 通信は確認できました。仕上げとして HTTPS 通信できるよう ACM で証明書を発行し ALB にひも付けましょう( **図** 9)。 ACM で発行した証明書はドメインの所有権を検証する必要があります。今回は DNS 検証をするため、所定の CNAME レコードを追加します。検証できたら、 HTTPS を受信するリスナーを証明書を指定して追加します。これで目標としていた独自ドメインで HTTPS 通 ~~信~~ <u>注6</u> ~~で~~ きる高可用性の環境が完成しました。 

###### **▼図9 証明書を作成しALBへ割り当てるスクリプト** 

**ACM_CERT_ARN=$(aws acm request-certificate --domain-name "$DOMAIN_NAME" --subject-alternative-names "www.${DOMAIN_NAME}" -- validation-method DNS --query CertificateArn --output text)** 

**aws acm describe-certificate --certificate-arn "$ACM_CERT_ARN" --query Certificate.DomainValidationOptions[].ResourceRecord --output text | awk '{print "aws route53 change-resource-record-sets --hosted-zone-id \"'"$HOSTED_ZONE_ID"'\" --change-batch \047{\"Changes\": [{\"Action\":\"UPSERT\",\"ResourceRecordSet\":{\"Name\":\"" $1 "\",\"Type\":\"" $2 "\",\"TTL\":300,\"ResourceRecords\":[{\"Value\":\"" $3 "\"}]}}]}\047"}' | sh** 

**aws elbv2 create-listener --load-balancer-arn "$ALB_ARN" --protocol HTTPS --port 443 --default-actions "Type=forward,TargetGroupArn=${TARGET_GROUP_ARN}" -- certificates "CertificateArn=${ACM_CERT_ARN}"** 

###### **ベストプラクティス** 

- ヘルスチェックやルーティングポリシーを使用して低レイテンシーや可用性を向上させる 

- 可能であれば(CNAMEではなく)エイリアスレコードを使用する 

- ACMではEメール検証よりDNS検証を使用する 

- Design for Failureを前提としてシステム設計をする 

- 注3) 筆者の環境では「http://ha-web-syatem-lb-769801853.ap-northeast- 

   - 1.elb.amazonaws.com/api/hello」。 

   - (本文に戻る) 

- 注4) ドメインレジストラへは別途登録してください。 

   - (本文に戻る) 

- 注5) http://www.example.com/api/hello 

   - (本文に戻る) 

- 注6) https://www.example.com/api/hello 

   - (本文に戻る) 

##### **“シェルを書いた”と主張したい** 

#### **魅惑の自作シェルの世界** 

##### **作ればわかるその役割と合理性** 

**<mark>第6回 ● 複合コマンドの実装(前編)</mark>** 



上田隆一(うえだりゅういち) 



<u>@ryuichiueda</u> 

**プログラミング言語のRustを使ってBashのクローンを自作することでシェルのしくみを学んでいきます。本連載を通して堂々と「(シェルスクリプトではなく)シェルを書いた」と言えるようになりましょう。** 

##### **はじめに** 

今回と次回は、自作シェル「寿司シェル( sush )」が複合コマンドを解釈して実行できるようにしていきましょう。実装する複合コマンドは、次のように、 **()** で複数のジョブを囲ってグループ化するタイプのものです注1 ~~。~~ 

**2つのechoの出力をまとめてnlに渡す** 

- **$ ( echo a ; echo b ) | nl** 

- **1  a** 

- **2  b** 

本連載では、このタイプの複合コマンドを「丸括弧コマンド」と呼びましょう注2 ~~。~~ 

Rust のプログラミングという観点からは、今回は「トレイト」というものが出てきます。概念が難しいかもしれませんが、単純コマンド注3 ~~と~~ 複合コマンドを同じコマンドとして扱うという具体的な用途に使っていくので、簡単な例よりもかえって理解できるかもしれません。 



###### **環境と必要な前提知識** 

コードについては、 Ubuntu 22.04 LTS で cargo 1.65.0 を使ってビルドし、検証しています。本記事を読むにはそれなりに知識が必要ですので、ミスマッチを避けるために、 

- ・ BashやZshなどの機能が多めのシェルの操作の経験と、基本的な用語の理解 

- ・プログラミングにおいて、構造体や、それに似たものを使った経験と、基本的な理解の 2 点を、読むための前提知識として挙げておきます。ただ、単純におもしろいと感じていただけるなら、ぜひ目を通してみてください。 

##### **前回のおさらいと今回の予習** 

本連載のリポジトリ注4 ~~の~~ sd/202305_0 ブランチに、前回の内容を少し整理したものがあります。今回はここから話を進めましょう。このブランチは Git でチェックアウトすれば取り出せますが、新規にダウンロードする手順も **図** 1に示しておきます。チェックアウトで済ませた人も、 **図** 1後半の動作確認をお願いします。 

###### **▼図1 コードをダウンロード/実行する手順** 

**rusty_bashディレクトリのないところに移動しておく $ git clone https://github.com/shellgei/rusty_bash.git $ cd rusty_bash $ git fetch** 

###### **$ git checkout sd/202305_0 ビルドと動作確認** 

**$ cargo run (..略..)** 

**echo あいう; echo えお ←コマンドが2つ以上実行できることを確認あいうえお** 

- ※ 本記事中の絵文字はtwemojiを使用しています。 <u>https://twemoji.twitter.com/</u> "twemoji" by Twitter, Inc. and other contributors is licensed under CC BY 4.0. <u>https://creativecommons.org/licenses/by/4.0/deed.ja</u> 



###### **さまざまなコマンドの受け入れのための整理** 

sd/202305_0 ブランチでは、これからさまざまな複合コマンドを実装するためにファイルを整理しました。具体的には、まず src ディレクトリ下の elements ディレクトリ内に、 command.rs というファイルと command というディレクトリを作りました。そして次に、 simple_command.rs を command ディレクトリの下に command/simple.rs と名前を変えて移動しました。このブランチの src 内で tree コマンドを実行すると、 elements ディレクトリの下は **図** 2のようになっているはずです。 



###### 新たに作成した command.rs のコードは今のところ、 

###### **pub mod simple;** 

のみで、 simple.rs をサブモジュールとして読み込んでいます。今後、複合コマンドを実装していくと、 **pub mod** ...... **;** の行が増えていきます。 



###### **標準入力からのコードの受け入れ** 

もう 1 つ大きな変更点として、標準入力からコード(ワンライナーやスクリプト)を読み込めるようにしました。おもにテストのためです注5 ~~。~~ **図** 3のように動作確認をお願いします。標準入力からのコードの受け付けもシェルの重要な機能ですが、実装について説明していると⻑くなってしまいますので、またの機会ということにします。 

###### **▼図3 標準入力からコードを読み込んでsushを実行する** 

**↓cargo runから実行する場合** 

**$ echo 'echo あいうえお' | cargo run Finished dev [unoptimized + debuginfo] target(s) in 0.06s Running `/home/ueda/GIT/rusty_bash/target/debug/sush`** 

**あいうえお ←標準入力から読み込んだ「echo あいうえお」を実行** 

**↓cargo runやcargo buildでビルドされたコマンドを直接実行する場合(sushのパスは適宜変更を)** 

**$ echo 'echo かきくけこ' | ../target/debug/sush かきくけこ** 

##### **2種類以上のコマンドを扱うための抽象化** 



###### **トレイト** 

ここからはコードを新たに実装していきます。 sd/202305_0 ブランチのコードに書き足したり、 sd/202305_1 以降のブランチを参考にしたりして読み進めてください。追加の実装では、まずパイプラインが「単純なコマンド」以外の種類のコマンドも、要素として持てるようにします。現状では elements/pipeline.rs に定義してある **Pipeline** 構造体は、 

**pub struct Pipeline { pub commands: Vec<SimpleCommand>, pub text: String, }** 

というように、 **SimpleCommand** のベクタしか持てません。ここの **commands** のフィールドを、 

###### **pub commands: Vec<さまざまなコマンドの型>,** 

というようにしたいのですが、ベクタは同じ型の要素しか持てません。どうすればいいかというところですが、 sush では「トレイト」を使う方法でこの問題を解決してみます。ほかに列挙型を使う方法も考えられるので、これは絶対ではありません。「トレイト」というのは、複数の型に共通のメソッドを実装するときに定義するものです注6 ~~。~~ たとえば **Pipeline** 構造体のメソッドで、コマンドを 1 つずつ実行するための **exec** は、 **リスト** 1のように、 16 行目で **commands** フィールドの各要素の **exec** メソッドを呼び出しています。つまり **commands** の要素はすべて **exec** メソッドを持っていなければなりません。逆に言うと、 **exec** があれば、型の違うインスタンスが **commands** に同居できるかもしれません。トレイトを使うと、ある条件のもと、このような同居が可能になります。 

###### **▼リスト1 Pipeline型のexecメソッド** 

**14 pub fn exec(&mut self, core: &mut ShellCore) { 15     for command in self.commands.iter_mut() { 16         command.exec(core); 17     } 18 }** 



###### **Commandトレイトの定義** 

では、単純コマンドと丸括弧コマンドを同居させるために、トレイトを使いましょう。まず、 command.rs に **リスト** 2のように追記し、 **Command** というトレイトを作ります。 8 〜 11 行目がトレイトの定義で、 **exec** と **get_text** という 2 つのメソッドの入出力の型を定義しています。 **exec** はコマンドを実行するためのメソッド、 **get_text** は、パースの際に取り込んだ文字列を **clone** (コピー)して渡すためのメソッドです。 

###### **▼リスト2 Commandトレイトの定義(sd/202305_1ブランチのcommand.rs)** 

**6 use crate::ShellCore; 7 8 pub trait Command { 9     fn exec(&mut self, core: &mut ShellCore); 10     fn get_text(&self) -> String; 11 }** 

**Pipeline** 構造体では、さまざまなコマンドのインスタンスを、現状この 2 つのメソッドのみを通して操作することになります。 



**SimpleCommand型へのCommandトレイトの実装** 

次に、 **SimpleCommand** に **Command** トレイトで定義したメソッドを漏れなく実装します。この作業は、「 **SimpleCommand** に **Command** トレイトを実装する」と表現されます注7 ~~。~~ 

具体的には、 elements/command/simple.rs を、 **リスト** 3のように変更します。一番大きな変更は **exec** メソッドを **impl SimpleCommand {}** の括弧内ではなく、 **impl Command for SimpleCommand {}** の括弧内に移動したことです。これで、 **リスト** 2の **exec** と、 **SimpleCommand** の **exec** が結び付けられます。また、 **リスト** 2のもう 1 つのメソッド **get_text** も実装します。 **get_text** は、 **SimpleCommand** の **text** フィールド(コマンドに相当する文字列が格納されるフィールド)をコピーして返します。 

###### **▼リスト3 SimpleCommandにCommandトレイトを実装(sd/202305_1ブランチ)** 

**5 use super::Command;** **<mark>// Commandトレイトの場所を追加 (..略..)</mark>** 

**20 impl Command for SimpleCommand {** **<mark>// 「impl SimpleCommand」を変更</mark> 21     fn exec(&mut self, core: &mut ShellCore) {** **<mark>// pubを取る (..略..)</mark>** 

**52     fn get_text(&self) -> String { self.text.clone() }** **<mark>// 追加</mark> 53 }** **<mark>// impl Command for ......を閉じる</mark> 54** 

**55 impl SimpleCommand {** **<mark>// set_cargsの前に挿入</mark> 56     fn set_cargs(&mut self) {** 

**Command** トレイトと関係ないメソッドや関連関数は、もとの **impl SimpleCommand{}** の括弧内に実装します。 **リスト** 3の 55 行目のように **impl SimpleCommand** を書き直し、その下にトレイトと関係ないメソッドや関連関数を配置します。さらに、 **Pipeline** 構造体のメソッド内で **Command** トレイトのメソッドが使えるように、 elements/pipeline.rs のヘッダ部に、 

**6 use super::command::Command;** 

と 1 行追加します。これで **cargo build** を実行すると、エラーも警告も出さずにビルドできます。 



###### **commandsフィールドの型の切り替え** 

ここでいよいよ、 **Pipeline** 構造体の **commands** フィールドで、異なるコマンドの型を受け入れられるように変更を加えます。しばらくビルドが通らなくなります。変更箇所を **リスト** 4に示します。 5 、 10 、 22 、 23 行目を、コメントに従って書き換えます。 22 行目で使っている **command::parse** は、まだ実装していません。 



**リスト** 4の中で一番難解なのは 10 行目で、 **Vec** の中身が **Box<dyn** 

**Command>** という型になっています。本連載ではシェルのほうに興味があるというのが建前なので、「異なる型を持つインスタンスのベクトルはこのように作る」と丸暗記でもいいのですが、説明は一通りしておきます。まず、 **dyn Command** の **Command** は、 **リスト** 2で定義したトレイトのことです。その前に付いている **dyn** というキーワードは動的にトレイトのメソッドを割り当てる( dinamically dispatch )という意味で ~~す~~ <u>注 8</u> ~~。~~ **dyn Command** で、 **Command** トレイトを持つインスタンスを指すポインタのような型になります。さらに、その外側の **Box<T>** 型は、 **「dyn Command** 型」のデータを「ヒープ領域」に置いて指し示すためのポインタです。プログラムの中で動的に( C 言語なら malloc 、 C++ なら new に相当する操作で) メモリを確保してデータを置く場合、このヒープ領域が使われます <u>注9</u> ~~。~~ **Box<T>** 型のポインタのサイズは(指し示しているデータのサイズは異なるものの)固定なので、 **Vec<Box<......>>** 型は同じ型、サイズの要素の配列になります。そのため、コンパイラが「型やサイズがそろ揃っていない」と怒ることがなくなります。以上、ポインタだらけでややこしいのですが、 **Vec<Box<dyn Command>>** というのは「ポインタのベクタであり、そのポインタが、 **Command** トレイトのさまざまなインスタンスを指している」と解釈できます。 

**リスト** 4では、 **Pipeline::parse** 関数の中で 2 点の変更をしています。 1 点目は 22 行目で、 **parse** を **SimpleCommand::parse** から **command::parse** に切り替えています。 **command::parse** は、先述のようにこれから実装します。 2 点目は 23 行目で、 **command.text.clone()** から **command.get_text()** に切り替えている点です。この変更は、 **command** がもはやどの型かわからず、 **text** というフィールドを持っているかわからなくなることに伴う変更です。代わりに **リスト** 3で実装した **get_text** を呼び出すことで、直接フィールドを使うことを防いでいます。 



###### **Debugトレイトの実装** 

この時点で **cargo build** すると、「 **command::parse** がない」というエラーと、もう 1 つ、 **図** 4のように、 **#[derive(Debug)]** にケチをつけるエラーが出ます。まず後者について解決しましょう。 

###### **▼図4 #[derive(Debug)]に関するエラー** 

**(..略..) 8  | #[derive(Debug)] |          ----- in this derive macro expansion (..略..)** 

**#[derive(Debug)]** については、前回、 **elements** 内で定義されている構造体にすべて付けました。こうすることで、 **println!** の中でフォーマット **{:?}** を指定すると、デバッグのために、インスタンスのデータをすべて表示注10 ~~す~~ ることができました。このとき、たとえば **Pipeline** 構造体に **#[derive(Debug)]** を付けたなら、その中の要素になっているデータの型 **SimpleCommand** 構造体にも **#[derive(Debug)]** を付ける必要があり、実際に付けていました。そう考えると、 **Pipeline** の要素が **SimpleCommand** から **Box<dyn Command>** に変わってしまったので、 **Command** トレイト側に **#[derive(Debug)]** に相当する対策が必要となることがわかります。ただ、トレイトには **# [derive(Debug)]** を付けられないので、代わりに **リスト** 5のように、 **Debug** というトレイトに用意されている **fmt** というメソッドを **dyn Command** に対して実装します。 

###### **▼リスト5 elements/command.rsにDebugトレイトのfmtメソッドを実装(sd/202305_3 ブランチ)** 

**7 use std::fmt; 8 use std::fmt::Debug; 9 10 impl Debug for dyn Command { 11     fn fmt(&self, fmt: &mut fmt::Formatter) -> fmt::Result { 12         fmt.debug_struct("COMMAND").finish() 13     } 14 }** 

**リスト** 5の実装は最低限のもので、 **{:?}** で出力を求められると、ただ「 **COMMAND** 」と出力します。これだと実際のデータ ( **SimpleCommand** のデータ)が書き出せなくなりますが、この実装で済ませておきます注11 ~~。~~ これで **cargo build** すると、エラーが 1 つなくなります。 



###### **command::parseの実装** 

次に、 **command::parse** を実装しましょう。 command.rs に、トレイトとは関係ない独立した関数として実装します。 **リスト** 6に例を示します。これで、 **cargo build** 時のエラーがすべてなくなります。 

**▼リスト6 elements/command.rsにparseを実装(sd/202305_4ブランチ)** 

**7 use crate::Feeder;** **<mark>// 追加(use std::fmtの上に)</mark> 8 use self::simple::SimpleCommand;** **<mark>// 追加 (..略..)</mark> 23 pub fn parse(text: &mut Feeder, core: &mut ShellCore) -> Option<Box<dyn Command>> { 24     if let Some(a) = SimpleCommand::parse(text, core){ 25         Some(Box::new(a)) 26     }else{ 27         None 28     } 29 }** 

**parse** でやることは、 **Command** トレイトが実装された構造体の **parse** でパースできるか試して、パースできたら **Box<dyn Command>** 型として解釈して、 **Some** にくるんで返すということです。今のところ、 **Command** トレイトが実装されているのは **SimpleCommand** だけなので、 24 行目で **SimpleCommand** のインスタンスが戻ってきたら **a** という名前で受け取り、インスタンスを **Box<SimpleCommand>** 型にするために、 25 行目のように、 **Box::new** に **a** を渡しています。とくに **SimpleCommand** から **dyn Command** に明示的に変換する処理は書いてありませんが、 **parse** 関数の戻り値に従って、自動的に解釈されるようです。 

##### **丸括弧コマンドの実装** 

さて、ここからは丸括弧コマンドの実装に入ります。まず、 elements/command.rs に、 

**4 pub mod simple;** 

**5 pub mod paren;** **<mark>// 追加</mark>** 

と、 **paren** というサブモジュールを追加します。例は sd/202305_5 ブランチにあります。「 paren 」というのは丸括弧のことです。この追加は、 elements/command/ の下、 simple.rs と同じところに paren.rs というファイルを作ることを意味します。 

paren.rs では、とりあえず **リスト** 7のように **ParenCommand** 構造体を定義し、トレイトを実装します。 7 〜 11 行目で定義した **ParenCommand** 構造体は、丸括弧コマンドに対応する部分の文字列を保持する **text** フィールドと、 **()** に囲まれたコード(本連載でいう「スクリプト」に相当)を保持するための **script** フィールドで構成しました。 Bash では **()** の中には必ずコードがないといけないのですが、実装の都合上、 **script** フィールドは **Option<Script>** 型にしてあります。つまりユーザーから見えない裏側では、 **script** が **None** (値がない)という瞬間があることになります。 

###### **▼リスト7 elements/commandにparen.rsを追加(sd/202305_5ブランチ)** 

**4 use crate::{ShellCore,Feeder,Script}; 5 use super::Command; 6 7 #[derive(Debug)] 8 pub struct ParenCommand { 9     pub text: String, 10     pub script: Option<Script>, 11 } 12 13 impl Command for ParenCommand { 14     fn exec(&mut self, _: &mut ShellCore) {}** 

**15     fn get_text(&self) -> String { self.text.clone() } 16 }** 

13 〜 16 行目のトレイトの実装部分では、とりあえず **exec** は空のものを作り、 **get_text** では **text** を返すコードを実装します。 



###### **パーサーの関数の呼び出し** 

次に、 **ParenCommand::parse** を実装し、これを呼び出せるようにしましょう。まず **リスト** 8のように、仮の **parse** 関数を実装します。 

**▼リスト8 paren.rsにparseを追加(sd/202305_5ブランチ)** 

**4 use crate::{ShellCore,Feeder,Script};** **<mark>(..略..)</mark> 18 impl ParenCommand {** 

**19     pub fn parse(_: &mut Feeder, _: &mut ShellCore) -> Option<ParenCommand> { 20         eprintln!("ParenCommand::parse"); 21         None 22     } 23 }** **<mark>(..略..)</mark>** 

これを **リスト** 9のように、 command.rs の **parse** 関数から呼び出すようにします。 if 文で同じようなコードを並べるので、あえてインデントはなくしました。これでコマンドのパースの際は、毎回 **ParenCommand** かどうかチェックしてから、そうでない場合に **SimpleCommand** かどうかがチェックされるようになります。この順番については、今のところ適当です。 

**▼リスト9 command.rsのparseにParenCommand::parseの呼び出しを追加 (sd/202305_5ブランチ)** 

**10 use self::paren::ParenCommand;** **<mark>// 追加 (..略..)</mark>** 

**25 pub fn parse(feeder: &mut Feeder, core: &mut ShellCore) -> Option<Box<dyn** 

**Command>> { 26     if let Some(a) =      ParenCommand::parse(feeder, core) { Some(Box::new(a)) }** **<mark>// 追加</mark> 27     else if let Some(a) = SimpleCommand::parse(feeder, core){ Some(Box::new(a)) } 28     else{ None }** 

これで **cargo run** して、たとえば **( echo a )** などと打って(実行して) みると、次のように反応があるはずです。 

**( echo a ) ParenCommand::parse ParenCommand::parse (: command not found** 

まだ複合コマンドとは解釈されず、「 **(** というコマンドに引数 **echo** 、 **a** 、 **)** を与えたもの」として解釈されます。 

◆ ◆ ◆ 

まだ丸括弧コマンドは実行できませんが、トレイトを使うことで、単純コマンドと丸括弧コマンドを同じ型のように扱う準備は完了しました。 **リスト** 9で **SimpleCommand** も **ParenCommand** も、 **dyn Command** という型のように扱われており、コンパイルも通っていることに注目すると、理解できるかと思います。次回は、丸括弧コマンドを実行できるところまで実装します。また、注 2 で述べた、丸括弧コマンドとサブシェルの関係についても説明します。 

- 注1) 丸括弧で囲ったコマンドにパイプでnlをつないでみましたが、これはまだsushでは解釈、実行できません。 (本文に戻る) 

- 注2) 「サブシェル」と呼ばれることがありますが、サブシェルには別の意味があるので、本連載では丸括弧コマンドとわかりやすい名前で呼びます。次回になぜサブシェルと呼ばれるのか説明します。 (本文に戻る) 

- 注3) 「コマンド名 <u>引数引数 ......」と記述するタイプの、プログラムを1つだけ起動す</u> るコマンドのことです。前回(本誌2023年4月号)以前に出てきた用語は、「https://b.ueda.tech/?page=sd_rusty_bash」にまとめてあります。 (本文に戻る) 

- 注5) testというディレクトリにtest.bashというテスト用のシェルスクリプトが置いてあるので、興味があれば読んでみてください。 (本文に戻る) 

- 注6) 連載当初から言っているように、筆者はシェルには詳しいものの、Rustのエキスパートではなくざっくりとしか説明できないので、詳しくは「https://doc.rust- <u>jp.rs/book-ja/ch10-02-traits.html」などをご一読お願いします。</u> (本文に戻る) 

- 注7) リスト3の20行目を翻訳すると、このような表現になっていることがわかります。 (本文に戻る) 

- 注9) コンパイル時に存在やサイズが確定しているデータのためのメモリは「スタック領域」に確保されます。ヒープ領域も、スタック領域も、プロセスに割り当てられたメモリ空間の中に作られます。 

   - (本文に戻る) 

- 注10) 今度機会があれば説明しますが、 **dbg!** というマクロを使うとインスタンスの内容を整形して表示できるので、だいたいの場合、 **println!** より便利です。 (本文に戻る) 

- 注11) 正直に書いておくと、まだ深掘りしていません。 

   - (本文に戻る) 

# **効率化自動化高機能化を実現** 

**Pythonでネットワーク自由自在エンジニアのためのプログラミング** 

**第 11 回トラブルシューティングの自動化をPythonで実現してみよう(1)** 

小澤昌樹(おざわまさき)日本ネットワーク技術者協会 @ozamasa 





##### **はじめに** 

ネットワーク運用において、トラブルはつきものです。よくあるネットワークトラブルとしては、次のようなものがあります。 

###### 1. **ネットワークがつながらない** 

原因として、デバイスに、IPアドレスやサブネットマスク、デフォルトゲートウェイなどが適切に設定されていないといったことが多いと思いますが、ケーブルの接続不良などの物理的な原因も考えられます。コンピュータとルータが正しく接続されているか、ケーブルやWi-Fiが問題ないか確認することも必要です 

###### 2. **インターネットに接続できない** 

   - まずは、ルータの設定が正しいかどうかを確認してください。もし設定が正しいなら、ISP(インターネットサービスプロバイダ)側のネットワーク障害や、DNSサーバの不具合などが原因として考えられます 

3. **ネットワークが遅い** 

   - 単純なネットワーク遅延の場合もありますが、パケットロス、ネットワーク帯域の不足、ルータの性能不足なども原因として考えられます 

4. **プリンタやファイルサーバが使えない** 

   - プリンタやファイルサーバへの接続不良だけでなく、ドライバの不具合も原因として考えられます。また、プリンタやファイルサーバへのアクセス権限不足やセキュリティ設定の不備なども考えられますこれらのトラブルは、使用するユーザーが確認作業をすることで事前に回避できることも多いと思います。同時に、ネットワーク管理者としては、定期的にネットワークを監視して、問題を早期に発見し、何らかの対処をすることが重要になってきます。一方、トラブルが発生してしまった場合には、まずその原因を突き止める必要があります。トラブルの原因を特定するには、トラブルが発生したネットワーク機器のログを調べることを優先しましょう。ログには、その機器で発生したイベントやエラーメッセージが記録されているので、トラブルの原因がわかる場合があります。今回は、まず定期的なネットワーク監視について説明します。 

##### **今回使用するPythonライブラリ** 

本題に入る前に、今回使用する Python ライブラリを紹介します。 



###### **subprocess** 

Python で外部コマンドを実行することのできる標準ライブラリです。このモジュールを使用すると、システムコマンドを実行したり、 Python スクリプトや外部プログラムとの間でパイプを作成したりすることができます。また、 Python プログラムから任意の外部プログラムを制御することもできます。今回は、 ping コマンドを実行するために使います。 



**sys** 

コマンドライン引数を取得するために、 sys モジュールの argv を使用します。 



**re** 

正規表現の処理を行う標準ライブラリです。正規表現を使用して、文字列の抽出、置換、分割などができます。 



###### **email** 

電子メールの送受信を扱うためのライブラリで、 Python の標準ライブラリとして提供されています。 email ライブラリを使用することで、電子メールのヘッダや本文を構築し、 SMTP サーバを介して電子メールを送信したり、受信した電子メールを解析したりすることができます。 email ライブラリには多数の機能が含まれていますが、今回は MIMEText と formatdate を使用します。 

- ・ MIMETextは、email.mime.textモジュールで定義されているクラスの1つで、テキスト形式のメッセージを表現するために使用されます。このクラスを使用すると、件名、送信元アドレス、宛先アドレス、本文などの情報を含むMIME形式のメッセージオブジェクトを作成できます 

- ・ formatdateは、email.utilsモジュールで提供される関数です。この関数は、メール送信日時のフォーマットを生成するために使用されます。たとえば、「 **Mon, 2 Jun 2023 01:23:45 +0900** 」といった RFC 2822形式の日付文字列を生成できます。email.utilsモジュールには、そのほかにも、アドレス文字列の解析やメッセージID文字列の作成など、電子メールに関するユーティリティ関数が含まれています 



###### **smtplib** 

SMTP プロトコルクライアントの機能を備えた、メールの送受信ができる標準ライブラリです。 MIMEText と組み合わせて使用することで、メールのタイトルや本文、ファイルの添付などを設定できます。 

##### **定期的なネットワーク監視** 

さて、ネットワークは、さまざまな要因によってトラブルが発生する可能性があります。しかし、定期的にネットワーク監視を行うことで、ネットワークの状態を把握し、問題を早期に発見し対処できます。監視によって得られた情報をもとに、ネットワークの最適化や改善を行うことで、より安定したネットワークを提供できます。さらに、その情報をトラブルシューティングとして自動化する取り組みまで昇華させると、運用の効率化にもつながります。ネットワークを定期的に監視する方法として、 ping コマンドを使用できます。このコマンドの実行結果には、送信したパケット数、受信したパケット数、パケットロスの割合、応答時間などが含まれており、これらの情報からネットワークの状態を把握できます。なお、今回のプログラムは Python 3.10.6 で動作確認しています。また、提示したサンプルプログラムは誌面の都合上、インデントを半角スペース 2 つとしています。 



###### **1. レイテンシの平均値を計算するスクリプト** 

まず、 ping コマンドの実行結果に含まれる応答時間を使って、ネットワークのレイテンシ(応答時間)の平均値を計算するスクリプトを作成しましょう。一般的に、ネットワーク接続が速い場合はレイテンシが低く、遅い場合はレイテンシが高くなります。ネットワークのレイテンシは、さまざまな要因によって異なりますが、ネットワークの混雑度やホストの負荷の影響を受けます。 LAN のようなローカル環境で ping を実行する場合、レイテンシの平均は 1 〜 10 ミリ秒程度です。一方、インターネットの場合、数十ミリ秒〜数百ミリ秒の範囲内で変動することがよくあります。また、物理的に遠距離の場合や、ネットワークが混雑している場合は数百ミリ秒から数秒となることもあります。たとえば、「 192.168.100.1 」に対して、 ping コマンドを 4 回実行すると、 **図** 1のような結果が得られます。各行に含まれる「 **time=** 」の後ろがレイテンシです。 

###### **▼図1 192.168.100.1にpingコマンドを4回実行した結果** 

**$ ping -c 4 192.168.100.1 PING 192.168.100.1 (192.168.100.1): 56 data bytes 64 bytes from 192.168.100.1: icmp_seq=0 ttl=255 time=17.585 ms** 

**64 bytes from 192.168.100.1: icmp_seq=1 ttl=255 time=3.467 ms 64 bytes from 192.168.100.1: icmp_seq=2 ttl=255 time=3.405 ms 64 bytes from 192.168.100.1: icmp_seq=3 ttl=255 time=11.653 ms --- 192.168.100.1 ping statistics --4 packets transmitted, 4 packets received, 0.0% packet loss round-trip min/avg/max/stddev = 3.405/9.027/17.585/5.972 ms** 

このことを、 subprocess モジュールを使用して ping コマンドを実行し、 re モジュールを使用して正規表現でレイテンシの値だけを抽出、 4 回の平均値を計算するといった Python スクリプトにすると、 **リスト** 1のようになります。 

###### **▼リスト1 pingコマンドの実行結果からレイテンシの平均を算出するPythonスクリプト** 

**import subprocess import sys import re def func(host): response = subprocess.run(** **<mark>#</mark>** <mark>1</mark> **["ping", "-c", "4", host], stdout=subprocess.PIPE, universal_newlines=True) lines = response.stdout.strip().split('\n')** **<mark>#</mark>** <mark>2</mark> **times = [] for line in lines: match = re.search(r"time=(\d+\.?\d*)", line)** **<mark>#</mark>** <mark>3</mark> **if match: times.append(float(match.group(1))) avg_latency = sum(times) / len(times)** **<mark>#</mark>** <mark>4</mark> **print(f"{host}: {avg_latency} ms")** **<mark>#</mark>** <mark>5</mark> **if __name__ == "__main__": host = sys.argv[1] func(host)** 

- 1 subprocessモジュールを使用してpingコマンドを4回実行します。引数の「stdout=subprocess.PIPE」は標準出力をパイプするための設定で、「universal_newlines=True」は標準出力を文字列型で取得するための設定です 

- 2 pingコマンドの実行結果を、split()メソッドを使って改行コードで分割し、配列に格納します 

- 3 実行結果を1行ずつ処理し、reモジュールのsearch()メソッドを使用して、レイテンシの値だけを正規表現で抽出します。抽出された値は、別の配列に格納します 

- 4 抽出したレイテンシから、その平均を求めます 

- 5 f文字列を使って、ホストとレイテンシの平均値をフォーマットして表示しますこのスクリプトを「 192.168.100.1 」と「 www.google.com 」に対して実行すると、 **図** 2のようにレイテンシの平均を求めることができます。 

###### **▼図2 リスト1の実行結果** 

**$ python3.10 ping_laytency.py 192.168.100.1 192.168.100.1: 5.65425 ms $ python3.10 ping_laytency.py www.google.com www.google.com: 16.78625 ms** 



###### **2. パケットロスを取得するスクリプト** 

ping コマンドを実行した結果には、パケットロスの割合も含まれています。ここでは、この情報を使用して、ネットワークのパケットロスを取得するスクリプトを作成します。一般的に、パケットロスの発生率は数パーセント以下です。パケットロスが発生する原因は、ネットワーク環境やトラフィックの状況によって異なりますが、ネットワークが混雑していたり、パケットサイズが大き過ぎたりする場合に発生します。とくに音声や映像のストリーミングなど、リアルタイム性が求められる場合には、パケットロスが発生すると品質が低下するため、注意が必要です。また、 NIC やネットワークケーブルの物理的な不良によっても発生することがあります。先ほどと同じように、 ping コマンドの実行結果から、 re モジュールを使用して正規表現でパケットロスの値を抽出する Python スクリプトは、 **リスト** 2のようになります。 **リスト** 1との主な違いは、 re モジュールの search() メソッドに与える引数で、「 **packet loss** 」を探してパケットロスの値を抽出している点です。 

###### **▼リスト2 pingコマンドの実行結果からパケットロスの値を抽出するPythonスクリプト** 

**import sys import subprocess import re def func(host): response = subprocess.run( ["ping", "-c", "4", host], stdout=subprocess.PIPE, universal_newlines=True) lines = response.stdout.strip().split('\n') loss = 0 for line in lines: match = re.search(r"(\d+)% packet loss", line) if match: loss = float(match.group(1)) break print(f"{host}: {loss}%") if __name__ == "__main__": host = sys.argv[1] func(host)** 

先ほどと同様に、「 192.168.100.1 」および「 www.google.com 」を引数に与えて実行すると、 **図** 3のようにパケットロスを抽出できます。 

###### **▼図3 リスト2の実行結果** 

**$ python3.10 ping_packetloss.py 192.168.100.1 192.168.100.1: 0.0% $ python3.10 ping_packetloss.py www.google.com www.google.com: 0.0%** 

このように、 ping コマンドの実行結果から、さまざまな情報を抽出する処理を作ることができます。このスクリプトを定期的に実行し、値の変化を把握していれば、異常な値を検知したときに、ネットワークに問題があることを早期に発見できます。 

##### **異常値を検出したらメールを送信する** 

ping コマンドの実行結果から値を取得するスクリプトを実行しても、取得したレイテンシの平均値やパケットロスの値は問題ない範囲にあることが多いです。そのため一定時間ごとに監視を行い、あるしきい値を超えた場合にはメールで通知するようにするとよいでしょう。 

Python を使ってメールを送る方法はいくつかありますが、ここでは標準ライブラリの smtplib を用いた方法を紹介します。まず、 smtplib をインポートして、 SMTP サーバに接続します。 SMTP サーバのアドレスとポート番号、ユーザー、パスワードは、メールサービスプロバイダから提供されます。たとえば、 **リスト** 3 のようにして SMTP サーバに接続します。 

###### **▼リスト3 SMTPサーバへの接続設定** 

**import smtplib smtp_server = "[SMTPサーバ]" smtp_port = 587 smtp_user = "[ユーザー]" smtp_password = "[パスワード]" smtp_connection = smtplib.SMTP(smtp_server, smtp_port) smtp_connection.starttls() smtp_connection.login(smtp_user, smtp_password)** 

次に、メール本文とヘッダを作成します。 MIMEText と formatdate をインポートして、 MIMEText クラスのオブジェクトを作成します ( **リスト** 4)。 

###### **▼リスト4 メール本文とヘッダの作成** 

**from email.mime.text import MIMEText from email.utils import formatdate mail_body = "メールの本文"** 

**mail_message = MIMEText(mail_body) mail_message["From"] = "[送信メールアドレス]" mail_message["To"] = "[受信メールアドレス]" mail_message["Subject"] = "メールの件名" mail_message["Date"] = formatdate()** 

最後に、メールを送信します( **リスト** 5)。送信するには、 SMTP サーバに接続した smtp_connection オブジェクトを使用します。送信が完了したら、 SMTP サーバから切断しましょう。 

###### **▼リスト5 メールの送信設定** 

**smtp_connection.sendmail(mail_from, [mail_to], mail_message.as_string()) smtp_connection.quit()** 

この一連のメール送信処理は、先ほどのスクリプトに組み込むことができます。たとえば、「レイテンシが 20ms 以上の場合」や「パケットロスが 10 %以上の場合」など、判断基準を設けてメールを送信できます。 

##### **Pythonスクリプトを定期的に実行する** 

Python スクリプトは、手動で実行するのではなく、定期的に自動実行させることで、ネットワーク運用におけるタスクの自動化や効率化を図ることができます。次に、代表的な定期実行の方法を紹介します。 

###### 1. cron **を使った実行** 

LinuxやmacOSなどのOSには、cronというタスクスケジューラがあります。cronを使うことで、指定したスクリプトを定期的に実行できます 

###### 2. Windows **タスクスケジューラを使った実行** 

Windowsにもタスクスケジューラという機能があり、cronと同じように、指定したスクリプトを定期的に実行できます 

###### 3. Python **のスケジューラライブラリを使った実行** 

Pythonでも、スケジューラを簡単に扱えるライブラリがいくつかあります。例を挙げると、scheduleライブラ ~~リ~~ <u>注1</u> ~~や~~ APScheduler <u>注2</u> ~~ラ~~ イブラリなどです。これらのライブラリの詳しい使い方や設定方法については、各ライブラリのドキュメントを参考にしてくださいこのほかに、クラウドサービスを利用する方法もあります。状況に応じて選択してください。 

##### **今回のまとめ** 

定期的にネットワーク監視することで、ネットワークの状態を把握し、潜在的な脅威を早期に検知し、対策を講じることができます。さらに、ネットワーク監視は、ネットワークのパフォーマンスを最適化するためにも必要です。ネットワークのボトルネックを特定し、改善点を把握できます。次回は、ネットワーク運用でトラブルが発生した場合に原因追求する方法について考えます。具体的には、ネットワーク機器のログを扱う方法について紹介する予定です。 

#### **<mark>Ansible</mark>** 

### **現場を支えるPlaybook** 

#### **<mark>インフラ作業自動化&効率化アイデアレシピ</mark>** 

**<mark>第 6 回</mark>** 

##### **申請書いらずのセルフサービスポータルの実現** 

さまざまなITインフラの現場でAnsibleとPlaybookが活躍しています。本連載は現場で実際に活用されるPlaybookに注目し、秘められた工夫やエピソードについて紹介する短期連載です。第6回では、 Ansibleをセルフサービスポータルとして活用することで、コミュニケーションコストを減らす方法を紹介します。中村洋介(なかむらようすけ) 株式会社エーピーコミュニケーションズ 

#### **はじめに** 

突然ですが、読者のみなさんはパブリッククラウドを使っていますか。拡張性が高い、ファシリティ管理が不要である、イニシャルコストが抑えられるなど、さまざまな利便性があります。また、調達期間の短さも魅力でしょう。では、プライベートクラウドはどうでしょうか。社内で提供されている仮想化基盤はあるけれど、申請のオーバーヘッドが面倒で活用できていないな、パブリッククラウドならワンクリックなんだけどな、などと思われた場合は、自動化のチャンスかもしれません。今回は、セルフサービスポータルとして Red Hat Ansible Automation Platform (以下 AAP )を利用し、マネージド仮想ルータ提供機能を開発したお話を紹介します。 

#### **課題と背景** 



##### **プライベートクラウドへのニーズ** 

社内向けに提供されているプライベートクラウドに対して、ある特定のシステム(以下、システム A )に接続したい、というニーズが複数の利用ユーザーから同時期に発生しました。このニーズに対応するべく、対象システムへの接続機能を持った仮想ルータを、マネージドサービスとして導入することにしました。マネージドサービスという形でなくとも、最低限のリーチャビリティはプライベートクラウドで整備し、ルータの調達や外部とのインテグレーションなどはユーザーに委ねるという方式もありました。しかし、同じプライベートの各ユーザーが、同じシステムに対しての接続機能を、別々に開発するのは無駄があると考えたため、プライベートクラウドとしての機能提供という方針にしました。このように、プライベートクラウドの 1 機能という形でサービスを提供しておけば、各ユーザーが個別に機能を開発する必要がなくなりますし、将来的に別システムへの接続要件が発生した場合でも、ルータへの機能追加という形でプライベートクラウド側で拡張可能となります。 



##### **セルフサービス化** 

ただ仮想ルータを構築して引き渡すだけだと、仮想ルータに対するユーザー要件の違いを想定したときに、少々面倒なことになります。各ユーザーはシステム A への接続という共通の目的を持つ一方で、プライベートクラウド内ではおのおの異なるシステムを管理しています。接続するシステムが異なれば、仮想ルータに設定すべきコンフィグも変わりますので、仮想ルータに対してユーザーごとのカスタマイズが必要になります。カスタマイズ済みの仮想ルータをユーザーに引き渡すためには、事前に必要な要件を吸い上げておかなくてはなりません。たとえば、各ユーザーに対して事前にヒアリングする、 Excel などでヒアリングシートを作成して記入してもらうなどのやり方がありますが、これだとユーザーもプライベートクラウド管理者側も相応のコストが発生します。そこで今回は、仮想ルータを、それ自身のデプロイ機能やカスタマイズ機能を含む運用自動化機能と併せて、セルフサービスとして提供しようと考えました( **図** 1)。つまり、プライベートクラウド管理者は、システム A への接続機能を持った仮想ルータを、最低限の設定を施してユーザーに引き渡し、カスタマイズが必要な部分は、ユーザーに直接実施してもらおう、ということです。仮想ルータのカスタマイズ・運用を、この運用自動化機能を通してユーザーに引き渡すことで、利便性が向上するだけでなく、ユーザーが可能な操作を限定できます。仮想ルータはあくまでマネージドサービスとして提供するため、プライベートクラウドとしても、仮想ルータに必要な設定を入れておいたり、監視をしたりする必要があります。このため、予期せぬ設定変更などを防止する観点から、仮想ルータに対する管理者権限をユーザーに渡すことはできません。あらかじめ実施できる内容を決めたうえで運用自動化機能を提供すれば、ユーザーは仮想ルータにログインできなくとも、カスタマイズができるようになり、プライベートクラウド管理者は予期せぬ設定変更を未然に防止できます。 



##### **AAPをセルフサービスポータルに** 

プライベートクラウドでは AAP が利用可能であったため、これをセルフサービスポータルとして利用しました。ユーザーは仮想ルータの設定を変更したい場合、期待する値を、申請書やヒアリングシートの代わりに、 AAP の Surve ~~y~~ <u>注1</u> ~~や~~ 起動プロンプトに対して入力します。入力を受け取った AAP は、そのまま仮想ルータに対して値を設定し、結果をユーザーに返します。こうしてユーザーはオンデマンドですばやくデプロイや設定変更が可能となり、プライベートクラウド管理者は申請の受理や設定変更作業から解放されます。 



##### **運用自動化機能について** 

運用自動化機能を実装するにあたって、さまざまな Playbook を開発しました。 show コマンドを実行して結果を出力するだけの単純なものから、各種設定変更まで、多くのユースケースに対応しました。以降は、 Playbook 開発において工夫したポイントや、設計例について紹介します。 

#### **Playbook開発全体の課題と対策** 



##### **物量と納期** 

仮想ルータの運用自動化機能は、仮想ルータにログインせずとも、ユーザーの要件に合うような設定変更を可能としなければなりません。必要な機能はルーティングプロトコルの閉塞、閉塞解除、 VRRP (ルータを冗⻑化するためのプロトコル)の切り替え、 ACL (アクセス制御リスト)の変更など、多岐にわたり、開発対象のロールは 30 近くになりました。限られた時間の中で開発を完了するためには、開発チーム内でいかに効率よく分担するかがカギになりました。この分担について、工夫した点が大きく 2 つあります。 



##### **ロールの粒度をそろえる** 

工夫の 1 つめは、ロールの粒度をそろえることです。たとえば、 ACL を変更する Playbook について、以下のような処理になるとします。 

1. 変更前のコンフィグの表示 

2. ACLの設定変更 

3. startup-configの更新、またはcommit 

4. 変更後のコンフィグの表示この処理を実現する Playbook を書く場合、すべての処理を 1 つのロールで実装することも可能ですし、コンフィグの表示や保存は別ロールで実装するという考え方もあります。自動化したい作業が決まっていても Playbook の構成をどうするかは開発者の考え方しだいとなってしまいます。こうなると、ロールの粒度が合わず、 Playbook を保存する Git リポジトリの見通しが悪くなったり、 startup-config の更新など、どの Playbook でも使うようなタスクが各ロールで実装されていたり、あるいは、されているものとそうでないものが混在したりする恐れがあります。これを避けるために、まずは開発するロールを先に決めました。具体的には **図** 2のような構成です。階層ごとに説明していきます。まず、 roles ディレクトリの直下にロールの実行対象となるノードやグループを示す名前の routers/ ディレクトリを作成しています。その下はコンフィグの場所を示すディレクトリ( acl/ 、 config/ 、 route/ )です。今回はわかりやすくするためにこのような構成にしていますが、実際は設定によってコンフィグがネストされていることがあります。そのような場合は、コンフィグの構造に合わせて、ディレクトリもネストします。最後は、その設定に対する操作用( configure/ 、 show/ )です。基本的には変更ロールの configure と表示ロールの show になりますが、 config ディレクトリについては backup ロールなどが含まれます。ちなみに、こうしたロールを呼び出す Playbook は **リスト** 1のようになります。 

###### **▼リスト1 ロールを呼び出すPlaybook** 

**--- hosts: routers gather_facts: false become: true tasks: - name: import router/acl/show import_role: name: router/acl/show - name: import router/acl/configure import_role: name: router/acl/configure - name: import router/config/save import_role: name: router/config/save - name: import router/acl/show import_role: name: router/acl/show** 

このように、あらかじめ作るロールとその役割を構造化して示しておくことで、リポジトリの見通しが良くなり、各開発メンバーは迷うことなく担当機能の開発に集中できるようになりました。 



##### **インプットとアウトプットを決める** 

工夫の 2 つめは、インプットとアウトプットを決めたことです。これは、 Playbook 間で使用感のバラツキが発生してしまうことを防ぐための対策です。たとえば、 ACL 変更 Playbook は Survey で設定したいコンフィグを入力するけれど、スタティックルート変更 Playbook はあらかじめ入力用のファイルを作成しておく必要がある、といったような、似たような目的なのに使い勝手が大きく異なるような状況は、ユーザーにとって不親切になると考えました。そこで実装に入る前にユーザーと、作成するべき機能すべてについて、インプットとアウトプットを相談し、合意しました。こうすることで、どの Playbook も使用感がある程度統一でき、開発メンバーもロジックの検討に専念できました。何より、ユーザーが直接見たり、操作したりする部分と実行結果が先に確認できるため、リリース直前に期待とのギャップに戶惑う、というようなリスクを軽減できるのが、最大のメリットでしょう。 

#### **バックアップ/リストアに関するPlaybook** 



##### **Ansible×Gitでバックアップ/リストア** 

ここまでは Playbook 開発全体について、背景や工夫を説明してきました。以降では、実際に開発した Playbook のうち、バックアップ /リストアについて、掘り下げて紹介します。バックアップ/リストア機能を実装するにあたって、前提が大きく 2 つありました。 1 つは、当然ですが、 AAP を通してユーザーが管理できること、もう 1 つは、バックアップの保存先が Git リポジトリであることです。 



##### **Gitリポジトリの構成** 

まずは、 Git リポジトリでどのようにバックアップファイルを保存しているかを説明します。バックアップ用の Git リポジトリは 1 つで、ユーザー同士の住み分けはブランチを分けることで実現しています( **図** 3)。ユーザーは直接 Git リポジトリを参照することはできず、 AAP のジョブテンプレートを通したバックアップファイルの取得と保存だけが可能です。このように、認証情報を秘匿したまま特定の操作だけを許容するような作りができるのは、 AAP の強みと言えるでしょう。バックアップファイルの名前は固定で、バックアップを取得するたびに上書きしていきます。ファイルの世代管理については Git が得意とするところですので、そちらに任せます。 2 世代以上前のバックアップにさかのぼりたい場合は、対象世代のコミットのハッシュ値を指定します。詳しくは後述しますが、これがユーザーが指定できるインプットとなります。 



##### **インベントリ** 

インベントリは、テナントごとに作成します。ここでいうテナントとは、プライベートクラウドを利用している部署やチームを指します。インベントリには、テナントごとに異なる値、たとえば払い出された仮想ルータの IP アドレスなどが変数で定義されており、各テナントが参照すべきバックアップ保存先ブランチ名もここに含まれます( **リスト** 2)。 

###### **▼リスト2 テナントごとのインベントリ** 

**[routers] router-01 ansible_host=10.0.0.1 router-02 ansible_host=10.0.0.2 [vars:routers] ansible_connection=network_cli ansible_network_os=**** backups_branch=tenant_a backups_repo: git.example.com/project01/backups.git** 



##### **バックアップ** 

バックアップは次の流れで行います。 

1. 仮想ルータのコンフィグ内容を取得、表示する 

2. バックアップリポジトリをクローンする 

3. 取得したコンフィグをファイルとしてリポジトリに配置し、リモートリポジトリにプッシュする 

4. クローンしたリポジトリを削除する 

5. 最新コミットのハッシュ値を表示するロールの main.yml を **リスト** 3に示します。 

###### **▼リスト3 バックアップ用のロールのmain.yml** 

**--- name: import config block: - name: import git_clone.yml** 

###### **import_tasks: git_clone.yml** 

- **name: import push_backup.yml import_tasks: push_backup.yml** 

###### **always:** 

- **name: import delete_files.yml import_tasks: delete_files.yml** 

- **name: import print_commit_hash.yml import_tasks: print_commit_hash.yml** 

おおむね通常の Git リポジトリの更新作業を、 Ansible を通じて実行するようなイメージです。コンフィグ内容の取得、表示については、ほかの Playbook でも利用するため、別でロールを作成し、バックアップ用のロールと併せて Playbook から呼び出しています。ポイントは最後のコミットハッシュ値の表示です。これは、リストアを実行する際に、 2 世代以上前のバックアップを使用したい場合に必要となります。 



##### **リストア** 

リストアは次の流れで行います。 

1. 任意の世代のバックアップリポジトリをクローンする 

2. クローンしたリポジトリに含まれるバックアップファイルを、 scpを使って仮想ルータに転送する 

3. 仮想ルータのコマンドを使って、転送したバックアップファイルをリストアする 

4. クローンしたリポジトリを削除する 

5. 現状の仮想ルータのコンフィグ内容を取得、表示するロールの main.yml を **リスト** 4に示します。 

###### **▼リスト4 リストア用のロールのmain.yml** 

**---** 

- **name: import config block:** 

- **name: import git_clone.yml import_tasks: git_clone.yml** 

- **name: import put_config.yml import_tasks: put_config.yml** 

**always:** 

- **name: import delete_files.yml import_tasks: delete_files.yml** 

リストアの流れを **図** 4に示します。ユーザーがジョブテンプレートを実行すると、 AAP の Survey 機能を通じて、復元したいバックアップのコミットハッシュ値が問われます。希望の世代がある場合は、 AAP のジョブ履歴から、バックアップジョブテンプレートを実行したときのログを探して、そこに出力されているコミットハッシュ値を入力します。コミットハッシュ値の入力を省略すると、最新のバックアップファイルを用いてリストアします。バックアップファイルが AAP を経由しているのは、仮想ルータ自身が直接 Git リポジトリからファイルを取得できないためです。以上が、バックアップ/リストアの概要です。 



##### **初期構築への応用** 

先ほど紹介したリストア用の Playbook ですが、実は、仮想ルータ払い出し直後の初期構築処理にも応用しています。初期構築でやることも、どこかに保存してあるコンフィグを仮想ルータに適用する、という操作になるため、リストアとやることがほとんど変わりません。唯一の違いは、使用するコンフィグファイルが、ユーザーが取得したバックアップか、プライベートクラウド管理者が用意した初期コンフィグか、という点です。そこで、リストアジョブテンプレートを実行する際に、インプットとして特定のフラグを立てることで、バックアップファイルでなく、初期コンフィグを参照するようなしくみを実装しています。このフラグは、ユーザーからも Survey を通じて選択可能になっており、自分たちで取得したコンフィグではなく、払い出し直後の初期コンフィグに戻すこともできます。先ほどのリストア兼初期構築用ジョブテンプレートと、プライベートクラウドの機能を組み合わせることで、仮想ルータのデプロイを自動化しており、ユーザーが AAP でジョブテンプレートの実行ボタンをクリックすると、約 4 分ほどで仮想ルータのデプロイが完了します。 

#### **おわりに** 

Ansible は便利な自動化ツールの 1 つですが、実際に商用環境で活用するとなると、習得にかかる学習コストや Playbook の開発、テストにかかる工数などは無視できません。優先度を付けつつ、よりベネフィットが得やすいポイントを狙って自動化していくことが、とても大切だと思います。そういう意味では、人や組織がオーバーヘッドとなっている業務、とくに定型の申請書をやりとりするようなものがあれば、自動化のチャンスです。このような考え方は自動化 2.0 と呼ばれ、従来の手作業をスクリプトなどに置き換える自動化 1.0 の次のステップとして位置づけられています。 

Ansible 単体ではセルフサービスポータル機能まで担うのは難しく、 AAP のように他ツールとの連携が必要ですが、うまく活用できれば、コミュニケーションコストを大きく減らし、相応の恩恵を得られます。今回紹介したお話が、読者のみなさんの、自動化ライフの一助となれば幸いです。 

> 注1) ざっくりと言うと、GUIからPlaybookを実行する際にダイアログを出すような機能のこと。 (本文に戻る) 

##### **こんなときどうする?** 

### **エンジニアも知っておきたい法律知識** 

**本連載では、IT業界で仕事をするうえで不可欠となる法律や契約書の読み方などの基本的な法的知識について、IT法務に携わる機会が多いSTORIA法律事務所の弁護士が毎号解説します。法律を少しでも身近に感じていただき、トラブルを未然に回避する一助となれましたらうれしく思います。** 

弁護士山口宏和(やまぐちひろかず)STORIA法律事務所 <u>http://storialaw.jp/</u> 





<u>h-yamaguchi@storialaw.jp</u> 

#### **第 21 回 AI技術を利用したソフトウェア開発契約の法的性質** 

#### **今回の設例** 

AI ベンダーである X 社は、 PoC ( Proof of Concept ~~)~~ <u>注1</u> ~~で~~ 良い結果が出たユーザー企業 Y 社と AI モデルの開発契約を締結しようとしています。先日 Y 社の担当者から X 社に連絡があり、「今回の開発契約は、準委任契約ではなく、請負契約でお願いしますね」と言われました。そもそも「請負契約」と「準委任契約」の違いは何でしょうか。また、今回の AI モデルの開発契約を「請負契約」として締結しても X 社にとって問題ないのでしょうか。 

#### **請負契約と準委任契約の違い** 

##### **<u>請負契約</u>** 

請負契約は、請負人が「仕事の完成」を約束し、注文者がこれに対して報酬を支払うことを内容とする契約です(⺠法 632 条)。たとえば、建物の建築がその典型です。 

##### **<u>準委任契約</u>** 

一方、準委任契約は、仕事の完成ではなく、一定の事務処理行為を行うことを約束する契約です。履行の割合に応じて報酬を支払う履行割合型(⺠法 648 条)、事務処理行為により生じる成果に対して報酬を支払う成果完成型(⺠法 648 条の 2 )という 2 つの類型があります。「準委任契約である場合は、業務や成果物について受任者側であるベンダーは何ら責任を負わず、例え業務や成果物の内容に問題があってもユーザーはベンダーに対して債務不履行責任を問えない」というのは **よくある誤解です** 。準委任契約においても、受任者側は善管注意義務(⺠法 656 条、 644 条)を負い、業務や成果物のクオリティは「システムやソフトウェア開発の専門家であるベンダー一般に期待される水準」であることが求められています。そのため、その水準を下回る業務や成果物であった場合には、準委任契約であってもユーザーはベンダーに対して債務不履行責任を問うことができます。 

##### **<u>債務不履行責任における違い</u>** 

「債務不履行責任を負う」とは、「契約の相手方に対して損害賠償責任を負う」あるいは「契約の相手方から契約を解除される」ということを意味します。請負契約において請負人が仕事を完成できなかった場合には、請負人は債務不履行責任を負います。一方で、成果完成型の準委任契約の場合で、報酬の支払条件とされる成果が達成できなかったとしても、(報酬は支払ってもらえませんが)受任者は善管注意義務を果たしていれば債務不履行責任を負いません。 

#### **AI・データの利用に関する契約ガイドライン** 

請負契約と準委任契約の違いを押さえたうえで、 AI モデル(学習済みモデル)の開発契約のお話に移ります。経済産業省「 AI ・データの利用に関する契約ガイドライン( AI 編)」(以下、経産省 AI ガイドライン ~~)~~ <u>注2</u> ~~の~~ p. 18 以下では、 AI 技術を利用したソフトウェア開発は従来のソフトウェア開発と比較して学習済みモデルの内容・性能などに、 

1 契約締結時に不明瞭な場合が多い 

2 学習用データセットによって左右されるといった特徴があると指摘されています。また p. 48 以下には「契約締結時までに仕様や検収基準の確定は難しいことが多い」「未知の入力(データ)に対しては、学習済みモデルがユーザー/ベンダーのいずれもが想定しない挙動をしないという保証が困難」などの理由から、学習済みモデル生成の各段階には準委任契約がその実態に馴染みやすい旨が書かれています。 

#### **設例の回答** 

ではユーザー企業 Y 社が AI 開発契約を請負契約として締結することを求めた場合に、ベンダーとして X 社はどのように対応すればよいのでしょうか。 

##### **ユーザー企業に対する説明** 

AI モデルの開発契約について請負契約での締結を求めるユーザー企業の中には、そもそも AI モデルの性質の理解が十分ではなかったり、 AI モデルの開発契約の交渉にあまり慣れていなかったりするケースがよくあります。そのため X 社は、まずは Y 社に対して、 AI モデル開発の特徴について説明し、それらの特徴から請負契約ではなく準委任契約がより実態に馴染む点への理解を求めることが重要です。交渉材料として経産省 AI ガイドラインを引用し、準委任契約での締結が実務上のスタンダードである旨を説明することも考えられます。 

##### **ユーザー企業がどうしても請負契約として締結することを希** **<u>望する場合</u>** 

では Y 社がどうしても請負契約として締結することを希望した場合、 X 社は AI モデルの開発契約を締結できる余地はないのでしょうか。この場合に考えられるのは、「 AI モデルの開発契約を請負契約として締結するが、 X 社が完成義務を負う『仕事』の範囲を、 X 社として約束できる範囲に明確に限定する」という方法です。経産省 AI ガイドライン p. 48 の脚注 43 では、「既知のデータを評価用データとし、当該データに対しての性能保証をする」という例が挙げられています。この場合には、何を評価用データとするか、その評価条件は何か、という点を明確にし、ベンダーとして約束できる範囲に限定することが重要です。 

#### **まとめ** 

前述の経産省 AI ガイドラインに加えて、「研究開発型スタートアップと事業会社のオープンイノベーション促進のためのモデル契約書 ver2.0 ( AI 編)注3 ~~も~~ 公開されていますので、そちらもぜひ AI モデルの開発契約交渉のご参考になさってください。本連載では、できる限り法律等の専門用語を使わず作成することを心がけているため、法律とは異なる簡易な表現を用いたり、例外的なケースについて省略したりしています。その正確性や最新性について保証している趣旨ではありませんので、具体的な事例については弁護士等の専門家にご相談くださいますようお願いします。注1) 新たな概念やアイデアの実現可能性を検証すること。 (本文に戻る) 

### **あなたのスキルは社会に役立つ** 

#### **エンジニアだからできる社会貢献** 

東日本大震災の発生直後に発足したHack For Japanや「市⺠が主体となって自分たちの街の課題を技術で解決するコミュニティ作り支援」を掲げるCode for Japanのメンバーを始めとして、日本各地で技術を活用した社会貢献活動が行われています。本連載では、防災や減災、地域の活性化や課題解決、そして人材育成など、「エンジニアだからできる社会貢献」の取り組みをお届けします。 

##### **第137回** 

#### **CIVIC TECH FORUM2022が残したもの** 

- ●一般社団法人イトナブ石巻 Pen 

- ●株式会社Box Japan 辰己学(たつみまなぶ) 

- ●Code for Fukuoka 德永美紗(とくながみさ) 

- ●一般社団法人コード・フォー・カナザワ福島健一郎(ふくしまけんいちろう) 

一般社団法人シビックテックジャパンが主催する「 CIVIC TECH FORUM (以下 CTF ) 2022 」が 2023 年 1 月 21 日(土)に仙台、金沢、奈良、福岡、ならびにオンラインの配信で開催されました。今回は CTF2022 に関する当日のリポートを、当日拠点オーナーとして携わった 4 人で記事にまとめていきます。 

#### **CIVIC TECH FORUMとは** 

そもそも CTF とは、 2015 年から始まったイベントです。テクノロジーの力を使い、社会課題や市⺠課題さらに自らの課題を自分たちで解決するアクションを「シビックテック」として掲げ、それをテーマとしてフォーラムである公共広場のように、シビックテックに興味があるさまざまな人に集まっていただき、実例や実践、ノウハウやチャレンジを共有する場としてイベントは進められてきました。これまでの CTF を振り返ったときに、 3 つのシーズンがあったと運営側は考えています。これはイベントの形式の変更を指すもので、 3 つのシーズンに分けてイベントを大きく変化させてきました。第 1 シーズンは、 2015 年から 2017 年を指します。一般的に、シビッれいめいきクテック黎明期とも考えることができますが、日本にも Code for XX による地域のブリゲードが誕生してきたころです。このころの CTF は、シビックテックに近い領域と想定される有識者をお招きして、「シビックテック」という言葉がもつ可能性についてお話をしていただくことにより、テクノロジーと市⺠活動がどのように混ざり合っていくかの道筋を示唆する場を作ってきました。その後、日本の各地にシビックテックのコミュニティやプレイヤーが生まれてきたことを受けて、 2018 年より第 2 シーズンを迎えることになります。第 2 シーズンではシビックテックのプレイヤーにスポットを当て、プレイヤー 50 人によるライトニングトーク形式での発表の場へ CTF は変化していきました。シビックテックプレイヤーによるライトニングトークによって、各地のシビックテック活動に関する取り組みやその思いが共有されて、シビックテックに関する仲間を増やす場へ変化をすることができました。 2020 年に関しては、 COVID-19 (新型コロナウイルス)というテーマを基に、新型コロナウイルス関連で生まれたシビックテック活動をライトニングトーク形式のオンラインで発表する取り組みも行い、これ以降は感染症の心配もあったためにオンラインで開催することを余儀なくされました。そして 2022 年、 CTF は第 3 シーズンを迎えることになります。次にスポットを当てたのは「地域」です。この背景には、日本各地におけるシビックテックプレイヤーの成熟度が関係しています。熟練を積んだシビックテックプレイヤーが地元の課題に向かい合うことによって、その地域の課題解決に大きく貢献してきている事実があります。一方「シビックテック」という言葉に過度にこだわることにより、テクノロジーによる課題解決に注目が行きがちです。しかし、そもそも地域課題に対する取り組みにおいてはテックであってもノンテックであっても同じではないか、という結論を得ました。そこで本年は「シビックとテック」というテーマを掲げ、地域特有の課題に対し、テック側、ノンテック側がどのように取り組んでいるのか、おのおのの拠点から発表し、それを日本中のシビックテックプレイヤーに共有しました。これにより、もう 1 つ変化を遂げた CTF を開催することができたと思います。 

#### **仙台拠点テーマ:「防災」** 

ここからは拠点ごとにどのようなテーマを掲げてプログラムを行ったか振り返っていきます。仙台拠点に関しては、 Pen が取り扱います。筆者は、一般社団法人イトナブ石巻に所属し、「みんなのトイレマッププロジェクト」に取り組んでいます。筆者は、東京の六本木で行われた CTF2019 から毎回参加や登壇などをしており、 CTF2021 からは運営スタッフとして、広報などの作業にあたっています。今回も引き続き CTF の運営スタッフ兼仙台の拠点オーナーとして携わりました。今回のこの記事の取りまとめもしています。仙台拠点のテーマとして掲げたのは「防災」です。なぜならここ仙台という土地は、ご存じのように東日本大震災を経験した土地であり、いまだに 2011 年 3 月 11 日の大地震から生じている余震にも遭っており、再び東北の太平洋沖を震源とする大きな地震が来るのではないかとも言われています。加えて昨今の温暖化による気象の変化により、新たな自然災害に遭遇する時代にもなりました。筆者たちは来る自然災害にどう向き合うことができるかという観点で、このテーマを扱うことになりました。仙台拠点は、ファシリテーターを Code for Shiogama の小泉勝志郎氏にお願いし、 2 名と 1 団体の防災に関する取り組みをうかがうことができました。 1 人目は、東日本大震災以降に情報支援団体「ネトボラ宮城」を立ち上げ、現在は一般社団法人情報支援レスキュー隊 ( IT DART )で活動している佐藤大氏。佐藤氏には、自然災害が発生直後の行政と、⺠間の取り組むべきことをまとめていただき、ご自身が参加されている IT DART がどんな支援を行うことができたかをお聞きしました。 

2 人目は、プライムバリュー代表の吉田亮之氏。吉田氏は避難所などが開設後に支援物資をオーダーしていくときに、これまでの場合 FAX などといった時間を要する課題があったことを挙げられました。そこで、ご自身の会社で開発されているクラウドサービスを使うことによって、災害が発生した際にクラウド上で自治体と企業をつなげ、必要物資が迅速に届けられるシステムを作ったそうです。 3 組目はきずな F プロジェクト。高校生から 20 代のグループで、中学における震災学習をきっかけに、自らの被災し家族を失った経験から、紙芝居を作り、それを発表することによって後世に震災時の話を語り部として語り継いでいく活動をしているそうです。当日も宮城県石巻市での語り部活動があったにもかかわらず、石巻市からリモートで参加してくださりました。最後に、登壇したみんなでクロストークを行い、互いのプロジェクトの印象に残った点を挙げていきました。個別で見ると、プロジェクトで行っていることは違いがあっても、みな来たるべき震災に向き合おうという、同じ思いを持っていることを確認することができました。 

#### **福岡拠点テーマ:「文化財」** 

福岡拠点に関しては、福岡県で Code for Fukuoka というシビックテック団体の代表として活動しており、 CTF は 2019 年〜 2021 年に登壇、 2020 年から運営側として関わっている徳永美紗が紹介します。まずは福岡拠点のテーマを「文化財」とした理由を 2 つ紹介します。 1 つは 2020 年から文化財関係の方とご縁があり、小学校の文化財に関する学習において AR コンテンツを福岡市の方とエンジニアコミュニティで作り、これがまさに今回のメインテーマである「シビックとテック」だと感じたことです。そしてもう 1 つは CTF2022 の多拠点開催が決まったときから福岡なら会場はエンジニアカフェと決めており、エンジニアカフェとは重要文化財をエンジニアコミュニティの場とした福岡市の施設で、これもまた今回のメインテーマにぴったりと感じ、エンジニアカフェが文化財であることからテーマを決定しました。福岡のセッションは、福岡市博物館の有馬総館⻑による福岡市の文化財状況の講演に始まり、 AR コンテンツを作った福岡市史跡整備活用課の中尾さん、福岡 XR 部というエンジニアコミュニティの⻑峰さん、そして遺跡が大好きで遺跡グッズ制作やイベントを実施しているコダイプレスの中村さんをさらにお迎えして、筆者がモデレータとして「市⺠が文化財を身近に感じるには」というテーマでトークセッションをしました。 AR コンテンツの話やコダイプレスのグッちょうかんずズの話、そして有馬総館⻑から昔の鳥瞰図における示唆など、 1 時間では話し足りないまま盛況のうちに終わりました。市⺠が文化財を身近に感じるうえでテクノロジーにできることはたくさんあるだろうとの結論ではありましたが、今回をきっかけとしてもっと具体的なところは登壇者たちで今後も議論を重ねていきたいと思っています。 

#### **金沢拠点テーマ:「政治」** 

金沢拠点に関してはオーナーを担当した福島健一郎が担当します。筆者は石川県で一般社団法人コード・フォー・カナザワ( Code for Kanazawa )というシビックテック団体の代表理事を務めて県内や国内のシビックテック活動と啓蒙を続けながら、 CTF は第 1 回から登壇者や運営スタッフとして関わっています。金沢拠点のテーマがなぜ「政治」なのか? それは地域の課題解決において、そもそも選挙によって選ばれる政治家の方たちが、そもそもどういう課題を重視し、それをどう解決すべきか、そしてそこにどれだけの税金を使うかなどを決定している、つまり、「選挙」によって地域の社会は大きく左右されるという事実があるからです。ただ、そうであるのに地方選挙では投票率も低く、政治家に対して無関心な人たちも多いのが事実です。シビックテックを実践することも大事ですが、そもそも選挙や政治に私たちが関心を向けていくことはとても大切ではないかと Code for Kanazawa は感じています。そういった問題提起から、 Code for Kanazawa は、昨年行われた石川県知事選挙で初めて「石川選挙ナビ」という候補者政策比較アプリをリリースしました。当日の金沢会場では、その開発者チームである 3 人のメンバーとイベント参加者全員でグループディスカッションを行いました。「選挙で自分が望む候補者を選ぶためにはどんな情報があればいいのか?」「そもそもみんなはどう候補者を選んでいるのか?」など活発な議論が行われました。グループディスカッションの議論をまとめると、政治や選挙に対して無関心であることが一番良くなく、少しでもわかりやすく提示できるようなサービスがあることが大事と考えられ、今後も Code for Kanazawa としては継続的に議論や開発を行っていきたいという話になりました。まずは今年の統一地方選挙においても新しい試みにチャレンジする予定です。 

#### **奈良拠点テーマ:「こども」** 

奈良会場のオーナーを務めさせていただきました辰己学です。奈良県生駒市在住、普段は Box Japan という SaaS 企業でソリューションエンジニア職に従事しています。地元生駒市が 2015 年に立ち上げた市⺠ PR チーム「いこまち宣伝部」の初代部員であり、これがきっかけで 2019 年にシビックテックフォーラムに LT で初登壇、 2021 年に運営委員に加わり、 CTF2022 では奈良会場の企画運営を担当させていただきました。奈良会場のテーマ「こども」を選んだのは、ひとつは筆者自身がシビックテックに関わるきっかけになったのが娘の誕生だったこと、もうひとつはおとな世代にとっては「もの珍しい」「最先端の」テックも、現代のこども世代にとっては「あって当然」のテックであり、こども目線を体感することが新しいシビックテックのフェーズを考えるひとつの契機になるのでは?と思ったからでした。奈良会場は 5 組の発表者 ── 奈良県立図書情報館・乾聰一郎氏、奈良県 CIO ・前田真人氏、生駒市広報広聴課・村田充弘氏、奈良県生駒市教育指導課教育改革担当・尾崎えり子氏、生駒小学校現役生 2 人 ── が「こども」をテーマに順にスピーチするオムニバス形式を取りました。おとな世代の教育や行政を通じたこどもへの想いを聞いた後、ギガスクール構想により学校で日常的にパソコンを使っている現役小学生のライフとテックの思いの丈を聞かせてもらう、という構成を取りました。参加したみなさんそれぞれの新しいシビックテックのフェーズを考える契機として胸に残り続けていてくれたらと思います。そして、こどもシビックテッカーが当たり前になるくらいにこのテーマが発展してほしいです。 

#### **CTF2022が残したものとは** 

CTF が大切なことにしている点に、ログを残すことがあります。実際当日に発表したことを Web 上や YouTube に残すことによって、社会課題に取り組んだことを後世に残すことを今まで意識して取り組んできました。筆者たちは、このイベントを行うことにより、何を残せたのだろうと最後に考えてみます。今回は、プレイヤーから地域にフォーカスを変えることによって、その地域ごとにある特有の課題に関して深く知ることができました。そうした課題は、地元で活動し問題に取り組んでいる人だからこそ気づくことができ、結果 CTF という場でスポットを当てることができます。そもそも今回、どうして多拠点での開催に至ったのかといえば、新型コロナウイルスによる感染症の問題があったために、大人数で集まることが厳しいと想定されたからです。その中で、リアル感を持ってイベントを開催したいと思ったときに、地方に出て多拠点という選択肢が生まれてきました。つまり今回のイベントでは、社会において大きな社会課題があったとしても、工夫することによってより良いものに変えていけることを後世に残せたのではないかと、筆者たちは考えています。社会課題に取り組むということは、テクノロジーの有無ではないのです。その地域に課題があり、個人個人がそれに対し気持ちを抱いています。さらにそれが共鳴し合うときに、みんなで取り組めることを実感することができました。筆者たちもさまざまな社会課題を乗り越えてきたというログを残しておくことで、後世になり新たな社会課題が生じたときにこれらのログが役に立てばと思っています。 CTF はこれまで「何を成し遂げたか」ではなく「どのような気持ちで取り組みたいか」という気持ちを大切にしてきました。社会には自分が気づかない課題が多々あります。自分で気づくことができないなら、こうした場に参加し、新しい発見ができるかもしれません。それは社会課題やそれに対する気持ちに触れることであり、結果シビックテッカーとしてより良い社会を作ることができるのではないでしょうか。こうしたおのおのの課題に対して共有し、共鳴し合う場を、これからも提供できればと思っています。 

#### <mark>SD N</mark> EWS <mark>& P</mark> RODUCTS 



##### **サイバネットシステム、セキュリティリスクレーティングプラットフォーム「SecurityScorecard Ratings」を販売開始** 

サイバネットシステム(株)は3月1日、SecurityScorecard社が開発、販売するセキュリティプラットフォーム「SecurityScorecard Ratings」の国内における販売および技術サポートを開始した。 

SecurityScorecard Ratingsは、任意の企業や組織のドメイン名を登録するだけで、ターゲットだけでなく、ターゲットのグループ会社や取引先企業まで含めたサプライチェーン攻撃のリスクを可視化できるソリューションだ。セキュリティリスクを定常的に監視し、 10のカテゴリでセキュリティレベルを評価する。評価結果は0〜100点の点数とA〜D、Fの 5段階両方で表示し、評価スコアが低下した場合は警告メッセージを通知し、改善目標や対応課題、改善策の提案まで行う。ターゲットのネットワークに侵入することなく監視できるので、事前の内部ネットワークの構成/設定変更などは不要だ。さらに、このプラットフォームは取引先のセキュリティ監査を効率化する「Security Assessments」という質問票管理用の機能も搭載しているのが特徴である。なお、セキュリティリスクの評価判定ロジックはWeb上で公開されているそうだ。 

**CONTACT** 

**サイバネットシステム(株)** 



<u>https://www.cybernet.co.jp/</u> 



###### **コンテック、** 

##### **産業用エッジAIコンピュータ「EPC-C5000Aシリーズ」を発売** 

(株)コンテックはエッジAI/HPCに最適なGPUボード搭載の産業用エッジAIコンピュータを開発した。「EPC-C5000Aシリーズ」として、構成パーツを用途に合わせて選択できる BTO(注文後組み立て)方式で、4月下旬より受注を開始する予定とのこと。おもな特徴は次のとおり。 

- ・第10世代CPU Intel Xeon W/CoreプロセッサシリーズComet Lakeに対応:第10世代 Xeonおよび第10世代Coreプロセッサを搭載可能。CPUはEmbeddedモデルを採用している 

- ・ NVIDIA製GPUボードを選択可能:GPUボード搭載モデルは、拡張スロットにNVIDIA製 GPUボードを搭載 

- ・周辺機器を自在に拡張可能。豊富なインターフェース:VGA、HDMI、DisplayPort、 LAN×3、USB3.2 Gen2×6、シリアル×1、オーディオ、電源SW信号コネクタなどの拡張インターフェースを搭載 

- ・ OSシャットダウン不要の電源断運用に対応:電源障害からのデータ保護とストレージへの書き込みを禁止する「電断プロテクト」機能を搭載 

###### **CONTACT** 

**(株)コンテック** <u>https://www.contec.com/</u> 



###### **NTTデータ先端技術、** 

##### **統合運用管理ソフトウェア「Hinemos」のセキュリティオプションを提供開始** 

NTTデータ先端技術(株)は4月1日、同社が開発する統合運用管理ソフトウェア「Hinemos」のセキュリティオプションを提供開始した。 

Hinemosは「収集・蓄積」「見える化・分析」「自動化」といった運用管理に必要な機能をワンパッケージで提供するソフトウェアで、無償版、従量課金制の「Hinemosサブスクリプション」(従量課金制)、という2種類の形態で提供されている。今回のセキュリティオプションは、Hinemosサブスクリプションのサービスメニューとして2種類追加された。セキュリティオプションの詳細は次のとおり。 

- ・ Hinemos セキュリティネットワーク診断オプション:ネットワーク機器やサーバといったネットワークシステムを検査し、セキュリティ上の問題がないか確認するオプション。問題を発見したら、サービスへの影響と改善策を提示し、システム改善を支援する 

- ・ Hinemos セキュリティ情報配信オプション:脆弱性に関する情報収集業務を支援するオプション。各種脆弱性情報や不正アクセス監視の統計情報をユーザー用専用ポータルサイトとメールで提供する 

###### **CONTACT** 

NTT **データ先端技術(株)** <u>https://www.intellilink.co.jp/</u> 



**GitHub、** 

##### **すべてのパブリックリポジトリに対してSecret scanning アラートの一般提供(GA)を開始** 

GitHub社は米国時間2月28日、すべてのパブリックリポジトリでSecret scanningアラートを無料で利用できるようになったと発表した。2022年12月にベータ版をリリースして以降、7万ものパブリックリポジトリでSecret scanningアラートが有効化され、漏洩したシークレットのトリアージに大きく貢献しているという。 

DevOpsコンサルタント兼トレーナーであるRob Bosが14,000のパブリックGitHub Action リポジトリを調査したところ、1,000件以上のシークレットを発見したそうだ。Secret scanningを有効にしておくとシークレットに関する通知が届くため、パートナーが自動で取り消しを行うことで、意図せずシークレットを含めてしまった場合でもコードの安全性が少し高まるという。パブリックリポジトリの所有者や管理者であれば誰でも、使用しているすべてのリポジトリに対しワンクリックでSecret scanningアラートを有効化できる。企業の管理者や Organizationのオーナーは、複数のリポジトリに対して一括で有効化できる。共有ユーザーをシークレットの漏洩から保護することに興味があるサービスプロバイダは、Secret scanningパートナープログラムへの参加を検討するとよいだろう。 

**CONTACT** 

**ギットハブ・ジャパン合同会社** 



<u>https://github.co.jp</u> 



**PHPerたちの文化祭、「PHPerKaigi 2023」レポート** 

現在PHPを使用している/過去にPHPを使用していた/これからPHPを使いたいと思っているエンジニアが集まり、技術的なノウハウを共有するためのイベント「PHPerKaigi 2023(ペチパーカイギ 2023)」が3月23日〜25日の3日間にわたって開催された。2018年から始まった本イベントは今年で6回目。オフライン会場を軸としたオフライン・オンラインハイブリッド開催で、オフライン会場は練馬区立区⺠・産業プラザ Coconeriホール、配信はニコニコ生放送にて行われた。メインセッションでは、2つのトラックで発表(トーク)が行われた。PHPの話題を中心にしつつも、PHPerたちによるPHPに限らない多彩な題材のセッションが行われていた。 PHPのコアな内容だけでなく、初心者がとっつきやすい発表も多かった印象だ。また、LT 大会では、会場の参加者にペンライトが配布され、登壇者の「推し色」に設定したペンライトを振って応援する催しもあった。2トラックのメインセッションのほかに、現地参加者であれば自由に発表できるアンカンファレンスや、発表を終えた登壇者に直接質問したり感想を伝える「Ask the Speaker」のブースが設けられていた。本レポートでは、メインセッションの2つのトークについて紹介する。 

###### **時間を気にせず普通にカンニングもしつつ ISUCON12 本選問題を PHP でやってみる(sji さん)** 

Webサービスチューニングのコンテスト「ISUCON」の問題にPHPを使って取り組み、どこまでスコアを伸ばせるか挑戦したレポート。sjiさんは、参考実装としてPHPが用意されているのにPHPを使っての本戦進出者がいないことを嘆きつつ、言語・処理系とボトルネックの関係など、PHPで高速化を実践する際の知見をまとめていた。 

###### **パフォーマンスを改善せよ!大規模システム改修の仕事の進め方 (前田啓佑さん)** 

突然、15年の間保守されてきたデータベースシステムを15%高速化せよ、とのミッションに取り組むことになったという登壇者の体験談をもとに、大規模システムの改修をどのように進めていくべきなのかまとめた発表。Go言語の作者の1人であるロバート・C・パイク氏の「推測するな、計測せよ」という言葉を引用しながら効果測定の重要さを伝えていた。 

###### **CONTACT** 

PHPerKaigi 2023 **実行委員会** <u>https://phperkaigi.jp/2023/</u> 



###### **Lazuli、** 

##### **SaaSのクラウド製品マスタ「Lazuli PDP」の機能をアップデート** 

Lazuli(株)はSaaS型のクラウドサービス「Lazuli PDP」の機能をアップデートし、3月1日から提供を開始した。 

Lazuli PDPは、複数の外部データベースに点在している商品情報を一括して管理し、データを活用しやすい形に整理、拡張するためのProduct Data Platformである。メーカーや小売業者などが保有する多様な商品マスタデータを、AIを使って名寄せし製品の特徴からメタタグの付与や関連付けを行う。ユーザーはそれらの情報をLazuli PDPを通じて利用できる。今回のアップデートのポイントは次の3点。 

- ・ユーザーが利用する画面が大幅に刷新された。これまでのユーザーの利用状況や改善要望から、Lazuli PDPにとってコアとなる共通機能を画面に追加した 

- ・ GS1 Japan(流通システム開発センター)とパートナーシップ提携を行い、JANコードにひも付けられたJICFS/IFDBの商品情報を提供できるようになった 

- ・「プレイグラウンド」という、研究開発中の実験的な機能を試してもらえる領域を用意した。この中で、OpenAIのAPIを使って、ユーザーが商品情報を生成・加工できる機能を提供することが予定されている 

###### **CONTACT** 

Lazuli(株) 



<u>https://lazuli.ninja/</u> 



###### **沖電気工業、** 

##### **AIによる配送計画最適化サービス「LocoMoses」の販売を開始** 

沖電気工業(株)は3月27日、物流分野におけるルート配送の計画をAIによって最適化するサービス「Loco Moses」の販売を開始した。本サービスは、同社が開発した「コスト最小型ルート配送最適化AI」を用いて、最適な積載量やルートを短時間で自動的に立案できる SaaS型のサービスだ。本サービスの特徴は次の3点が挙げられる。 

- ・効率的な「分配配送」を取り入れた計画立案ができる 

- ・高速道路の利用を個別にルール設定できる 

- ・道路封鎖などの配送リスクの恐れが高いルートをあらかじめ除外できる物流業界ではこれまで、配送計画の立案を一部の熟練社員に依存しており、作業の属人化が大きな問題の1つとなっていた。さらに近年は、燃料費の高騰、ドライバーの不足、 CO2排出量の抑制といった背景から、より効率の高い配送計画の立案が求められている。同社はこれらの課題を解決するため、ロンコ・ジャパン社と共同でAIを開発。AIには熟練社員の技能を取り入れ、配送現場で実運用における実験を繰り返して開発を進めた。なお、本サービスの価格は個別見積もりとなっている。 

###### **CONTACT** 

**沖電気工業(株)** 



<u>https://www.oki.com/</u> 



###### **マップマーケティング、** 

##### **地図システム開発支援用「TerraMap API」にて、登記所備付地図データの無償トライアル提供を開始** 

マップマーケティング(株)は地図システム開発を支援するWeb API「TerraMap API」にて、登記所備付地図データの無償トライアル提供を3月中旬より開始した。登記所備付地図データは、2023年1月23日より法務省がG空間情報センターを通じて提供を始めたオープンデータだ。地図XML形式で提供されているため、システムで利用するにはデータ加工作業が必要になる。データをGeoJSON形式に変換するツールがデジタル庁より無償提供されているが、GIS(地理情報システム)に慣れてないユーザーが利用するには難易度が高いという。同社は「TerraMap API」を利用してデータを配信することで、より多くのユーザーが登記所備付地図データを簡単に利用できることを目指す。今回の無償トライアルでは公共座標系のみの地図が配信対象で、登記所備付地図データのポリゴンや属性情報を取得できる。とくにデベロッパーや商業施設の管理を行う不動産業界、遺産相続などで登記データが必要な金融業界などで、自社でデータ整備をせずにシステム開発が可能となる。ほかのオープンデータや自社データと組み合わせることでさらに用途も広がるとのことだ。 

###### **CONTACT** 

**マップマーケティング(株)** 



<u>https://www.mapmarketing.co.jp/</u> 



###### **オラクル、** 

##### **Java 20をリリース** 

オラクル社は3月21日、プログラミング言語Javaの最新バージョンであるJava 20の提供を開始した。Java開発キットOracle JDK(Java SE Development Kit)、およびそのリファレンス実装であるOpenJDKも併せてアップデートされている。 

Java 20では、7つのJDK Enhancement Proposal(JEP)に基づくアップデートのほか、パフォーマンスや安定性、およびセキュリティ面が改良されているとのこと。 JEPによるアップデートの内容は、以前のリリースで実装された正式導入前の機能(プレビュー、インキュベーター)のさらなる改善が中心だ。詳細を次に示す。 

- ・ JEP 432:レコード・パターン(第2プレビュー) 

- ・ JEP 433:Switchのパターンマッチング(第4プレビュー) 

- ・ JEP 429:スコープ値(インキュベーター) 

- ・ JEP 436:仮想スレッド(第2プレビュー) 

- ・ JEP 437:構造化された並行性(第2インキュベーター) 

- ・ JEP 434:外部関数と外部メモリAPI(第2プレビュー) 

- ・ JEP 438:ベクトルAPI(第5インキュベーター) 

Java 19で追加された、従来のスレッド機能より軽量な「仮想スレッド(Virtual Threads)」と、マルチスレッド処理をひとつの作業単位として扱えるようになる「構造化された並行性(Structured concurrency)」機能が改善されたほか、新機能としてスレッド内/スレッド間でイミュータブルなデータを共有できる「スコープ値(Scoped Values)」が追加されている。これにより、ハイスループットな並行処理アプリケーションを作成、維持、観察するプロセスがより効率化できるようになっているという。 

Javaは6ヵ月ごとに新バージョンがリリースされており、Java 20のサポート期間は次のリリースが行われる2023年9月までとなる。次期バージョンのJava 21は、2021年9月に公開されたJava 17以来のLTS(⻑期リリース)版になる予定だ。 

###### **CONTACT** 

**日本オラクル(株)** 



<u>https://www.oracle.com/jp</u> 



###### **ネオジャパン、** 

##### **グループウェア「desknet's NEO」V7.5を提供開始** 

(株)ネオジャパンはグループウェア「desknet's NEO」のV7.5を3月29日に提供開始した。今回のバージョンアップでは、ユーザーの声を受け15機能・50項目以上の機能改善を行った。おもな改善点は次のとおり。 



###### **スケジュール・設備予約** 

- ・これまで、ほかのユーザーが追加した予定の変更や削除は、その予定に登録された全ユーザーに対する変更権限がないと行えなかったが、設定を切り替えることで予定の登録者(つまりその予定の参加者)なら誰でも変更/削除できるようになった 

- ・予定登録時に、同じ日時に予定が重複している場合には警告メッセージを表示できるようになった 



###### **インフォメーション(いわゆる掲示板の機能)** 

- ・作成途中のインフォメーションを下書き保存できるようになった 

- ・インフォメーション機能を開いたときの初期表示を「未読」「既読」「すべて」から選べるようになった 



###### **文書管理(ファイル管理機能)** 

- ・複数の文書をまとめてダウンロード可能になった 

- ・文書の改訂履歴として残せる数が最大10世代から最大99世代に拡張された 



###### **Webメール** 

- ・メール作成時、宛先に指定したアドレスをパネル表示できるようになった。これによりマウスのドラッグ&ドロップ操作で、宛先/CC/BCCを簡単に変更できる 

- ・宛先を指定する際などに、アドレス帳(おもに社外の宛先を管理)と利用者名簿(社内の宛先を管理)のどちらを先に表示するかを設定できるようになった 

- このほかにも、ワークフロー(申請業務機能)、ネオツイ(SNS機能)、AppSuite(ノ 

- ーコードの業務アプリ作成ツール)にも数々の機能改善が行われている。 

###### **CONTACT** 

**ネオジャパン(株)** 



<u>https://www.neo.co.jp/</u> 

### Reader's Link 

**今** 月号のカバーを見てびっくりされた読者の方もいるのではないでしょうか。そう、5月号のカバーを飾る動物は猫ではなくうさぎです! 担当者は猫好きですが、うさぎのちまっとした姿もかわいらしくて良いものです。「うさぎを表紙にするなら卯年の2023年しかない」とは編集⻑の談。これから1年ほどは、カバーのうさぎを目印に本誌を探してくださいね。 

#### **<u>3月号読者プレゼント当選者</u>** 

- 1 **折りたたみ** PC **スタンド「** Twelve South Curve Flex **」** 小栗徹様(東京都) 

- 2 **「ノベルティ** RTA50i **風マグカップ」** 

   - 牧野正治様(埼玉県)、ゆうき様(鹿児島県)、仲村直博様(福島県)、高井理充様(神奈川県)、⻑濱のり子様(沖縄県)、オミオ元気様(宮城県)、山岸玲央様(愛知県)、石垣徹様(神奈川県)、今井智大様(神奈川県) 

- 3 **ココネノベルティタンブラー&** T **シャツセット** 角田学様(東京都) 

- 4 **『最短コースでわかる** Python **プログラミングとデータ分析』** 國島京子様(福島県)、ねこみらーじゅ様(埼玉県) 

- 5 **『ゼロから学ぶ** Rust **』** あき様(埼玉県)、田中克則様(和歌山県) 

#### 6 **『** CISO **のための情報セキュリティ戦略』** 曽利雅樹様(埼玉県)、とっぽ様(愛知県) 

7 **『** R **ユーザのための** tidymodels[ **実践** ] **入門』** 福田收真様(京都府) 

※ 当選しているにもかかわらず、本誌発売日から1ヵ月経ってもプレゼントが届かない場合は、編集部(sd@gihyo.co.jp)までご連絡ください。アカウント情報の不備(本名が登録されていない、プレゼント応募後に住所が変更されたなど)によってお届けできないことがあります。2ヵ月以上連絡が取れない場合は、再抽選させていただくことがあります。 

#### **2023年3月号についてたくさんのお便りありがとうございました!** 

**第1特集** 

**なぜ、Kubernetesを使うのか? 今まで単体のコンテナでの使用がメインでしたので、知らないことばかりでした。かずさん/愛知県** 

**序章があることで、特集でカバーされている内容と、自分がどこから読むと良いのかがわかってとても良かったです。高橋健一さん/千葉県** 

**第1特集を読んで、コンテナというものがどんなものか、なんとなくわかった気になりました。書籍も購入して学びたいです。** 

AJYA **さん/愛知県** 

**コンテナ導入により改善された点や、新たに出てきた課題、Kubernetesのしくみなどがわかりやすくまとめられていた。山本陽平さん/東京都** 

**コンテナの使い方の復習になりました。村橋究理基さん/北海道** 

**Kubernetesは3ヵ月に一度のスパンでアップグレードされ、それについては第2章でも触れられていましたが、ではみなさんそのアップグレードをどうしているのか、というところについて非常に興味があり、今回久しぶりに手に取りました。運用方法、かつそのユーザーの声があるとよりおもしろい内容となっていたと思います。田川京太朗さん/東京都** 

**昨今Kubernetes、Dockerはとても取り上げられているが、なぜ?を知る一歩になった。ももよしさん/東京都** 

**使い方、中身のしくみだけでなく、誕生した経緯、持っている課題がまとまっていて Kubernetesが必要とされる理由が理解できました。コンテナオーケストレーションが必要となった背景もわかりやすかったです。** 

yasio **さん/静岡県** 

**Kubernetesを使う理由を、コンテナを利用するうえでの課題から解説していく特集でした。Kubernetesをまだ使っていない方、すでに業務で使っている方、どちらにも参考にしてもらえたようです。** 

**第2特集** 

**ITエンジニアスキルアッププログラム社外でのエンジニアスキルの考え方を知れて良かったです。エールさん/東京都** 

**ICTの分野は次から次へと新技術が登場しますが、現状維持では時代遅れになるので、より必要とされるにはどのように技術をインプットして学んだことをアウトプットするか、自身に本当に必要なスキルは何かということを、あらためて考えさせられました。** N/M **さん/兵庫県** 

**参考になった。熟練者と初心者の比較でやっているつもりができていないことに気がついた。** 

IR023 **さん/千葉県** 

**自分のキャリア、人生を歩んでゆくうえで大いに参考になりました。漆黑の翼さん/神奈川県** 

**転職して間もないタイミングだったので、エンジニアとしてのスキルアップなどの話題に興味が湧いた。石川正浩さん/大阪府** 

**教育プログラムを考えていく立場なので参考になります。** 

**さねゆきさん/東京都** 

**当たり前に必要だがなかなか理解してもらえない項目なので再確認できて助かった。エースさん/神奈川県** 

**4月から研修講師の仕事が始まるのですが、とても参考になりました! 実務寄りの具体的な情報の引き出しが増えて良かったです。** TYY **さん/奈良県** 

**自分はエンジニアではなくエンジニアリングマネージャーなので部下の教育・成⻑にも直結する内容で良かった。ぴーちゃんさん/東京都** 

**技術の進化が早いIT業界、流れに乗り遅れないためにも自分自身をマネジメントしていきたいですね。自身のスキルアップのためだけでなく、部下の教育の参考になるとの声も。** 

**特別企画** 

**先取り! C++23** 

**C++23以前のバージョンでの変更点についてもまとめられていて、知らなかった変更点を知ることができて良かった。原田範昭さん/静岡県** 

**C++のこれまでの進化と最新機能を概観できてとてもよかったです。しばらくC++から離れているうちに格段の進化を遂げているのだと実感しました。** maplehills2566 **さん/埼玉県** 

**おもしろいですが、C++はもうほとんど使わなくなってしまいました。** KK **さん/愛知県** 

**最近、頻繁に使うわけではないけど、出番はときどきあるC++の最新情報がコンパクトにまとまっていてすごく助かる。C++はわかりやすくて好き。** romeosheart **さん/⻑崎県** 

**最近利用していないが、概要が理解できた。みじんこさん/千葉県** 

**シンプルな内容だったが使わない技術なので、キャッチアップに良かった。** Hiroto Mukai **さん/東京都** 

**C++のバージョンアップの歴史と最新バージョンを先取りして紹介しました。だいぶとっつきやすくなっている模様です。** 

###### **短期連載** 

**Denoで始めるサーバサイドTypeScript開発[1] ちょうどDenoについて勉強しようと思ったとこなので良いタイミングの記事。花岡裕明さん/愛媛県** 

**以前触ってみた際はtest/lint/formatが標準機能としてあったことに感動しました。npmとも連携できるようになりましたしこれからが楽しみです。** goute **さん/大阪府** 

**やっと市⺠権を得てきたという印象のDenoについて知れてよかった。たいやきさん/東京都** 

**初めて聞いたので、勉強になりました。ましましさん/東京都** 



**Denoを使ったTypeScript開発について紹介する連載です。Denoは** 

**JavaScript/TypeScriptのイメージが強いですが、WebAssemblyバイナリの実行などもできるんです。** 

###### **短期連載** 

**画像生成AIのしくみ[後編]** 

**数学的な知識を不要とするしくみの説明がなされており、非常にわかりやすかったです。ふうび一世を風靡したGANからまたトレンドが変わっていることを知り、移り変わりが激しいことをあらためて感じました。** T.K **さん/千葉県** 

**AIには興味があるので、もう少しじっくり読んでみたいと思います。ゆんどさん/⻑野県** 

**後編では画像を生成するための拡散モデルという技術について紹介しました。まだまだ話題の生成AI、どこまで進化していくのでしょうか。** 

アンケートはhttps://gihyo.jp/magazine/support/questionnaireからご回答ください。コメントを掲載させていただいた方には、1,000円分のQUOカードをお送りします。みなさまのご意見・ご感想をお待ちしております! 



##### SD Staff Room 

● 前号より。雑誌の継続は塩原君のためだ。彼はSDを心底好きでわずかな誤植を見つけると入稿後でも印刷所に出向き修正するという情熱の編集者だった。PDF校正も彼が始めこうした。編集作業のIT化の嚆矢だ。なくしちゃならんと僕はふんばり十数年、やっと次の世代に渡せる。感謝以外なにもない。(白ひげ) 

● 毎年3月に息子のピアノ教室の発表会があります。息子(7歳)の演奏はもちろんのこと、よその中高校生の子たちの上手な演奏も楽しみの1つです。演奏している姿を見ながら聴くと、細かな音も聴き分けられるような気がして味わい深いです。コンサートに行く時間がとれない私にはとても貴重な時間です。(よし) 

● どうしても開かない瓶のふたに出会ってしまいました。昨年、体重とともに筋力も相当落ちたため、自分の力で開けられないのは必然だったのかもしれません。ですが、これまで「握力がそこそこある人」として生きてきたので、まさか裏技を検索して開ける日が来るとは。しかし粘着テープって、便利ですね。(か) 

● 週末はアマチュアの合唱活動をしています。毎年GWにTokyo Cantatという一大イベントがあり、今回このイベント内の「合唱指揮コンクール」で選ばれ本選に出場することに。出場者は普段合唱団を指揮している方々ばかり、一方私はこれが指揮の初舞台。胸を借りる思いでとにかく楽しみたいです。(に) 

- 変な映画のおかしなところに突っ込みを入れながら見るのが好きです。しかし先日、 

- 「そういう楽しみ方をするようになったら大変」と人に言われてショックを受けました。そしてそのショックをまた変な映画で癒してしまいました。もう泥沼ですね。ちょっと反省しました。でもやめる気はありません。(や) 

- トラックボールを使い始めました。理由はセールでKensington SlimBlade Proをちょっと 

- 安く買えたから。操作が独特なのは知っていたので、慣れるのに1ヵ月はかけてやるという心積もりで購入しましたが、想像していたよりずっと直感的で、数日で違和感なく使えるように。(く) 

- ワークショップで習った技法をさっそく使って小さな布花を作りました。小指の先の半 

- 分くらいの花を作るのは正直老眼が進んだ目では辛いものがありますが、ちまちまとした作業は好きですし、できあがったときの達成感のためにひたすら頑張りました。作ったミモザとデージーのお花は小さなブーケに。かわいいです。(ま) 

本誌に記載の商品名などは、一般に各メーカーの登録商標または商標です。 



### **PRESENT 読者プレゼントのお知らせ** 

『Software Design』をご愛読いただきありがとうございます。本誌サイト <u>https://sd.gihyo.jp/の「読者アンケート」からアクセスし、アンケートにご協力ください</u> (アンケートに回答するにはgihyo.jpへのお名前と住所のアカウント登録が必要となります)。ご希望のプレゼント番号を入力いただいた方の中から抽選でプレゼントを差し上げます。締め切りは2023 **年** 5 **月** 17 **日** です。プレゼントの発送まで日数がかかる場合がありますが、ご了承ください。ご記入いただいた個人情報は、プレゼントの抽選および発送以外の目的で使用することはありません。アンケートの回答は誌面作りのために集計いたしますが、それ以外の目的ではいっさい使用いたしません。入力いただいた個人情報は、作業終了後に責任を持って破棄いたします。 



###### **Wi-Fiルータ「Aterm WX4200D5」** 

Wi-Fi 6対応のWi-Fiルータです。通信速度は5GHz帯は最大3,603Mbps(規格値)、 2.4GHz帯は最大574Mbps(規格値)です。メッシュ中継機能を搭載しており、対応ルータと連携することでメッシュネットワーク/メッシュWi-Fiを構築可能。スマートフォン、タブレット向け専用アプリ「Aterm ホ-ムネットワークリンク」で、外出先からでも自宅に設置したルータを管理できます。 

**提供元 NECプラットフォームズ** **<u>https://www.aterm.jp/</u>** 

**1名** 



**ワイヤレス左手用デバイス「ACK05」** 



自由にカスタマイズ可能なキーとホイールを搭載した左手用デバイスです。多方向レイアウトをサポートしており、デバイスの向きを気にせず使えます。1,000mAhバッテリー駆動で、有線/無線接続で利用できます。Windows/macOS/Linuxに対応(Linuxは Bluetooth接続不可)。 

**提供元 XPPen** **<u>https://www.xp-pen.jp/</u>** 

**1名** 



**PHPerKaigiノベルティ「PHPerKaigi 2023 Tシャツ」** 



PHPを愛するエンジニア=PHPerたちが、知見を共有するイベント「PHPerKaigi 2023」のノベルティTシャツです。PHPのマスコットである象のモチーフがあしらわれています。2種類のTシャツ(いずれもMサイズ)のうちどちらか一方をプレゼント。 **提供元 PHPerKaigi 2023 実行委員会** 

**<u>https://phperkaigi.jp/2023/</u>** 

**2名** 



###### **プログラマー脳** 

**Felienne Hermans 著、水野貴明翻訳、水野いずみ監修** 



脳と心の働きを探る認知科学の観点から、プログラミングを効率化する方法を解説する書籍です。読みやすいコードの書き方やミスを防ぐテクニックを、コードを読むときの脳の認識プロセスなどから考えます。 

**提供元秀和システム** 

**<u>https://www.shuwasystem.co.jp/</u>** 

**2名** 



**AWSではじめるクラウドセキュリティ松本照吾、桐谷彰一、畠中亮、前田駿介著** 



AWSを題材に、クラウドにおけるセキュリティを学習できる書籍です。セキュリティやクラウドの基本から、AWSにおけるセキュリティの実装方法や具体的なセキュリティ対策のハンズオンまで紹介しています。 

**提供元テッキーメディア** 

**<u>https://techiemedia.co.jp/</u>** 

**2名** 



**今さら聞けない暗号技術&認証・認可大竹章裕、瀬戶口聡、庄司勝哉、ほか著** 



Webシステムのセキュリティを支える認証・認可や暗号技術について解説する書籍です。本誌のこれらの題材に関連する過去記事を厳選してまとめました。基礎教養から具体的な規約・プロダクトまで学べます。 

**提供元技術評論社** 

**<u>https://gihyo.jp/book/</u>** 

**2名** 



###### **データベース速攻入門** 

**堀内康夫、徳尾秀敬、とみたまさひろ、ほか著** 



本誌のデータベースに関連する特集記事を再収録した書籍です。データモデリングの基本から、SQL文の書き方、AWSのデータベースサービスの使い分けまで、現場ですぐに役立つ知識を身につけられます。 

**提供元技術評論社** 

**<u>https://gihyo.jp/book/</u>** 

**2名** 

##### **電子版書籍について** 

本書は紙の書籍『Software Design 2023年5月号』(ISBN0582705)を電子書籍化したものです。紙書籍とは一部レイアウトやデザインが異なります。本書の更新履歴や補足情報は技術評論社ウェブ <u>サイトをご参照ください。</u> 

本書の一部または全部を著作権法の定める範囲を超え、無断で複写、複製、転載、テープ化、ファイルに落とすことを禁じます。造本には細心の注意を払っておりますが、万一、ページの乱れやページの抜け等がございましたら、小社デジタル事業部までお知らせください。 

##### **電子版奥付** 

**書名** Software Design 2023年5月号 **電子版発行日** 2023年4月18日初版第1刷発行 **発行人** 片岡巌 **編集人** 池本公平 **編集** 吉岡高弘山﨑香 ⻄原康智山本紘彰栗木琢実 **編集アシスタント** 根岸真理子 **広告** 立山英樹 **発行所** 株式会社技術評論社東京都新宿区市谷左内町21-13 **電話** 03-3513-6150 販売促進部 03-3513-6180 デジタル事業部 **電子版製本** 株式会社グリープ <u>https://greep.jp/</u> 

©2023 技術評論社 ISBN978-4-297-13450-1